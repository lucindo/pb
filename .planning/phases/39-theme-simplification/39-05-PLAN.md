---
phase: 39-theme-simplification
plan: 05
type: execute
wave: 3
depends_on:
  - 39-02
  - 39-03
  - 39-04
files_modified:
  - src/content/content.no-removed-themes.test.ts
autonomous: true
requirements:
  - THM-01
  - THM-02
  - THM-03
requirements_addressed:
  - THM-01
  - THM-02
  - THM-03
must_haves:
  truths:
    - "A new vitest test file at src/content/content.no-removed-themes.test.ts exists and fails CI if any forbidden moss/slate/dusk token reappears in src/components/, src/app/, src/content/, or src/styles/."
    - "The drift-guard scans every non-test `.ts`, `.tsx`, and `.css` file under all four roots (per D-05); test files (*.test.ts / *.test.tsx) are excluded via the collectFiles filename filter; the guard file itself is excluded by its own `.test.ts` suffix."
    - "Forbidden tokens (D-04) total 12 entries: 9 plain-substring tokens (`moss`, `slate`, `dusk`, `Moss`, `Slate`, `Dusk`, `Musgo`, `Ardósia`, `Crepúsculo`) + 3 regex categories (persisted-value literal `/theme:\\s*['\"](moss|slate|dusk)['\"]/`; CSS selector `/\\[data-theme=['\"]?(moss|slate|dusk)['\"]?\\]/`; object-key entry `/['\"](moss|slate|dusk)['\"]\\s*:/`)."
    - "The drift-guard test PASSES against the post-Plans-02/03/04 clean state — every deprecated token was deleted by the prior wave-2 plans, so the guard runs to completion with zero hits."
    - "The test IS the lock per D-06 — any future deliberate phase that re-introduces a deprecated palette (or claims one of these reserved names) explicitly DELETES this file with rationale recorded in that phase's SUMMARY. Deleting this file is the intentional unlock."
    - "D-03 satisfied: an automated fs-scan vitest case mirrors src/content/content.no-variants.test.ts (Phase 38 VAR-06 verbatim structural twin); same imports, same collectFiles helper, same 4-root SCAN_FILES concatenation, same describe + 2-it() shape (sanity floor + main sweep)."
    - "D-04 satisfied: forbidden token list covers all 4 classes — plain-substring lowercase ids (3), plain-substring EN display strings (3), plain-substring PT-BR display strings (3), and 3 regex categories (persisted-value literal + CSS selector + object-key entry)."
    - "D-05 satisfied: surface coverage = render paths (components + app), i18n catalogs (content), and CSS tokens (styles); 4 scanned roots."
    - "D-06 satisfied: the WHY-comment header at the top of the file explicitly documents the exit-ramp policy (delete-the-file IS the unlock)."
    - "Closing green gate: `pnpm tsc --noEmit && pnpm test --run && pnpm build` all exit 0; `git grep -i 'moss\\|slate\\|dusk' src/ | grep -v 'src/content/content.no-removed-themes.test.ts'` returns 0 matches (the belt-and-suspenders audit per CONTEXT §specifics + Phase 38 close-of-phase pattern)."
    - "STATE_VERSION is UNCHANGED across the entire Phase 39 — `git diff main -- src/storage/storage.ts | grep -nE '^[+-].*STATE_VERSION'` returns 0 matches (per D-01 — no bump, same stance as Phase 38)."
  artifacts:
    - path: src/content/content.no-removed-themes.test.ts
      provides: "fs-scan drift-guard that fails CI if any of the 12 forbidden tokens (9 plain-substring + 3 regex) reappears in src/components/, src/app/, src/content/, or src/styles/ — Phase 39 long-term 3-theme-only invariant lock."
      contains: "moss"
      min_lines: 90
  key_links:
    - from: src/content/content.no-removed-themes.test.ts
      to: src/components/
      via: "node:fs recursive readdirSync collectFiles helper (copied verbatim from content.no-variants.test.ts analog)"
      pattern: "collectFiles\\(.*COMPONENTS_DIR|collectFiles\\(.*components"
    - from: src/content/content.no-removed-themes.test.ts
      to: src/app/
      via: "node:fs recursive readdirSync collectFiles helper"
      pattern: "collectFiles\\(.*APP_DIR|collectFiles\\(.*app"
    - from: src/content/content.no-removed-themes.test.ts
      to: src/content/
      via: "node:fs recursive readdirSync collectFiles helper"
      pattern: "collectFiles\\(.*CONTENT_DIR|collectFiles\\(.*__dirname"
    - from: src/content/content.no-removed-themes.test.ts
      to: src/styles/
      via: "node:fs recursive readdirSync collectFiles helper — same 4th-root scope as Phase 38 VAR-06"
      pattern: "collectFiles\\(.*STYLES_DIR|collectFiles\\(.*styles"
---

<objective>
Create the Phase 39 drift-guard at `src/content/content.no-removed-themes.test.ts` — the load-bearing artifact of Phase 39 per CONTEXT §specifics. Mirror `src/content/content.no-variants.test.ts` (Phase 38 VAR-06 — the verbatim structural twin per PATTERNS) with one substantive difference: the forbidden-token list is the Phase 39 12-entry set per CONTEXT D-04 (9 plain-substring + 3 regex categories). Same imports, same `collectFiles` recursive helper with `.ts/.tsx/.css` extension filter + `.test.ts`/`.test.tsx` exclusion, same four-root scope (`src/components/`, `src/app/`, `src/content/`, `src/styles/`), same `SCAN_FILES` flat concatenation, same describe + 2-it() shape (sanity-floor `> 10` files + main forbidden-token sweep). The drift-guard fires only AFTER Plans 02/03/04 land (else it would flag the deletion-target tokens at the wave-2 commit boundaries). Closes the runtime lock for THM-01..03 — the deletion is the deletion, the test is the contract that it stays deleted.

Run the closing green gate for Phase 39: `pnpm tsc --noEmit && pnpm test --run && pnpm build` all exit 0; the cumulative grep audit returns 0 hits outside the drift-guard file; STATE_VERSION is unchanged from main.

Per CONTEXT §specifics: this test is how the Mono Zen visual lock survives Phase 41+ contributions. Re-introduction of a deprecated palette (or a new palette under one of the reserved names) is a deliberate phase decision that explicitly deletes this file with rationale recorded in that phase's SUMMARY (D-06 / Phase 37 D-11 / Phase 38 D-06 precedent).

Output: 1 new file at `src/content/content.no-removed-themes.test.ts` (collocates with `content.no-variants.test.ts` and `content.no-stats-ui.test.ts` so future readers find all three drift-guards via the same `src/content/content.*` glob — PATTERNS §"Placement rationale").

Per Phase 38 Plan 04 precedent: 2 tasks. Task 1 creates the drift-guard. Task 2 runs the closing green gate + the belt-and-suspenders cumulative grep audit + STATE_VERSION lock check.
</objective>

<execution_context>
@$HOME/.claude-personal-personal/get-shit-done/workflows/execute-plan.md
@$HOME/.claude-personal-personal/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/39-theme-simplification/39-CONTEXT.md
@.planning/phases/39-theme-simplification/39-PATTERNS.md
@.planning/phases/39-theme-simplification/39-01-SUMMARY.md
@.planning/phases/39-theme-simplification/39-02-SUMMARY.md
@.planning/phases/39-theme-simplification/39-03-SUMMARY.md
@.planning/phases/39-theme-simplification/39-04-SUMMARY.md
@.planning/phases/38-variant-removal/38-04-SUMMARY.md
@src/content/content.no-variants.test.ts

<interfaces>
<!-- The analog file (verbatim structural twin). Sourced from 39-PATTERNS.md §"Plan 5 — drift-guard test" + the Phase 38 38-04-PLAN.md analog. -->

From src/content/content.no-variants.test.ts (analog — copy structurally with only the forbidden-token list and describe labels swapped):

Imports + helper (copy verbatim — PATTERNS §"Plan 5 — Imports + helper"):
```typescript
/// <reference types="node" />

import { describe, it, expect } from 'vitest'
import { readFileSync, readdirSync, statSync } from 'node:fs'
import { resolve, join } from 'node:path'

function collectFiles(dir: string, acc: string[] = []): string[] {
  for (const entry of readdirSync(dir)) {
    const full = join(dir, entry)
    const st = statSync(full)
    if (st.isDirectory()) {
      collectFiles(full, acc)
    } else if (
      (entry.endsWith('.ts') || entry.endsWith('.tsx') || entry.endsWith('.css')) &&
      !entry.endsWith('.test.ts') &&
      !entry.endsWith('.test.tsx')
    ) {
      acc.push(full)
    }
  }
  return acc
}

const COMPONENTS_DIR = resolve(__dirname, '..', 'components')
const APP_DIR        = resolve(__dirname, '..', 'app')
const CONTENT_DIR    = resolve(__dirname)
const STYLES_DIR     = resolve(__dirname, '..', 'styles')

const SCAN_FILES: string[] = [
  ...collectFiles(COMPONENTS_DIR),
  ...collectFiles(APP_DIR),
  ...collectFiles(CONTENT_DIR),
  ...collectFiles(STYLES_DIR),
]
```

Forbidden-token list (CONTEXT D-04 — 12 entries; replaces the Phase 38 14-entry list):

```typescript
const FORBIDDEN_TOKENS: Array<{ label: string; match: (text: string) => boolean }> = [
  // Plain substring (case-sensitive lowercase theme ids — 3 entries)
  { label: "'moss' (theme id)",  match: (t) => t.includes('moss') },
  { label: "'slate' (theme id)", match: (t) => t.includes('slate') },
  { label: "'dusk' (theme id)",  match: (t) => t.includes('dusk') },
  // Plain substring (case-sensitive EN display strings — 3 entries)
  { label: "'Moss' (EN display string)",  match: (t) => t.includes('Moss') },
  { label: "'Slate' (EN display string)", match: (t) => t.includes('Slate') },
  { label: "'Dusk' (EN display string)",  match: (t) => t.includes('Dusk') },
  // Plain substring (PT-BR display strings — 3 entries; D-04 errs on inclusion per Claude's Discretion)
  { label: "'Musgo' (PT-BR Moss)",       match: (t) => t.includes('Musgo') },
  { label: "'Ardósia' (PT-BR Slate)",    match: (t) => t.includes('Ardósia') },
  { label: "'Crepúsculo' (PT-BR Dusk)",  match: (t) => t.includes('Crepúsculo') },
  // Regex — persisted-value literals (catches `theme: 'moss'` / `theme: 'slate'` / `theme: 'dusk'`)
  {
    label: "theme: 'moss'/'slate'/'dusk' (persisted-pref literal)",
    match: (t) => /theme:\s*['"](moss|slate|dusk)['"]/.test(t),
  },
  // Regex — CSS attribute selectors (catches theme.css re-entry)
  {
    label: "[data-theme='moss'/'slate'/'dusk'] CSS selector",
    match: (t) => /\[data-theme=['"]?(moss|slate|dusk)['"]?\]/.test(t),
  },
  // Regex — object-key entries (catches favicon palette / contrast floors / etc. re-entry)
  {
    label: "'moss'/'slate'/'dusk':  object-key entry",
    match: (t) => /['"](moss|slate|dusk)['"]\s*:/.test(t),
  },
]
```

Total: 12 entries (9 plain-substring + 3 regex categories). The forbidden-token shape matches Phase 38's exactly.

Describe block (copy verbatim with describe label + failure-message context swapped — PATTERNS §"Plan 5 — Describe block"):
```typescript
describe('Phase 39 drift-guard: no removed theme tokens (CONTEXT D-04 / D-05 / D-06)', () => {
  it('scans a non-empty set of production files', () => {
    expect(
      SCAN_FILES.length,
      'Phase 39 drift-guard scanned zero files — collectFiles or scan-root resolve is broken',
    ).toBeGreaterThan(10)
  })

  it('no forbidden moss/slate/dusk token appears in src/components/, src/app/, src/content/, or src/styles/', () => {
    const hits: string[] = []
    for (const file of SCAN_FILES) {
      const text = readFileSync(file, 'utf-8')
      for (const token of FORBIDDEN_TOKENS) {
        if (token.match(text)) {
          hits.push(`${file}: ${token.label}`)
        }
      }
    }
    expect(
      hits,
      `Forbidden moss/slate/dusk tokens found (Phase 39 3-theme-only invariant violated):\n${hits.join('\n')}`,
    ).toEqual([])
  })
})
```

Header comment (adapted from Phase 38 VAR-06 header — PATTERNS §"WHY this file exists"):
```
// src/content/content.no-removed-themes.test.ts
//
// Phase 39 drift-guard (CONTEXT D-03 / D-04 / D-05 / D-06).
//
// Scanned roots: src/components/, src/app/, src/content/, src/styles/
//   - components/ + app/ cover all render paths.
//   - content/ catches moss/slate/dusk i18n copy re-entering via strings.ts.
//   - styles/ catches [data-theme='moss'/'slate'/'dusk'] CSS rules re-entering
//     via theme.css.
//
// Forbidden token classes (CONTEXT D-04):
//   1. Plain substring — 9 entries (3 lowercase theme ids + 3 EN display strings + 3 PT-BR display strings)
//   2. Regex — persisted-pref literals (theme: 'moss'/'slate'/'dusk')
//   3. Regex — CSS attribute selectors ([data-theme='moss'/'slate'/'dusk'])
//   4. Regex — object-key entries ('moss'/'slate'/'dusk': — catches favicon palette / contrast floor / etc.)
//
// WHY this file exists (CONTEXT D-06): Phase 39 collapsed the 5-palette theme system
// to 3 options (light / dark / system) per the spike-010 visual lock. This drift-guard
// locks that done-state against future regressions. It is the lock — any future phase
// that re-introduces a deprecated palette (or claims one of these reserved names)
// explicitly deletes this file with rationale recorded in that phase's SUMMARY.
// Deleting this file is the intentional unlock.
//
// Analog: src/content/content.no-variants.test.ts (Phase 38 VAR-06 — verbatim structural twin)
```

Self-exclusion: this file's filename `content.no-removed-themes.test.ts` ends in `.test.ts`, so the `collectFiles` reject clause filters it from its own scan. The file contains literal `'moss'`, `'slate'`, `'dusk'`, etc. inside the `t.includes(...)` and regex bodies — without the test-file exclusion, the guard would self-flag.

WARNING — case-sensitivity collision risk (per PATTERNS): the substring `'Slate'` (capitalized) could collide with English prose containing the word "slate" in comments or copy. The Phase 39 codebase grep at time of plan creation shows zero such collisions. If a future contributor writes a comment like `"this color is a slate grey"`, the guard will flag it — that's intentional friction per D-06 (re-introduction is a deliberate phase decision). Per CONTEXT D-04 (Claude's Discretion): err on inclusion.
</interfaces>
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Future contribution → src/ | A contributor's PR re-introduces a deprecated palette — without this drift-guard, moss/slate/dusk scaffolding could re-enter components / app / content / styles unnoticed |
| Drift-guard self-exclusion | This file itself contains the literal forbidden-token strings; the `.test.ts` filename filter in `collectFiles` is load-bearing — if removed, the test would self-flag and always fail |
| New file-system surface | The test reads files under 4 scoped roots via `node:fs` `readdirSync` + `readFileSync` — running in Vitest's node environment with hardcoded root paths (no user input crosses the boundary) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-39-12 | T (tampering — future regression of the theme-axis cleanup) | src/components, src/app, src/content, src/styles | mitigate | The drift-guard test fails CI on any of the 12 forbidden tokens; per D-06 future re-introduction is a deliberate phase decision that explicitly deletes this file |
| T-39-13 | I (info — vacuous pass) | The drift-guard test itself | mitigate | Sanity-floor it() asserts `SCAN_FILES.length > 10` — a broken `__dirname` resolve or renamed scan root would otherwise silently produce an empty list and pass vacuously |
| T-39-14 | I (info — self-flag) | The drift-guard file's own forbidden-token literals | mitigate | `collectFiles` rejects `.test.ts` and `.test.tsx` files via filename filter; the guard file's `.test.ts` suffix excludes it from its own scan |
| T-39-15 | I (info — path-traversal in fs read) | node:fs `readFileSync` call inside the it() loop | accept | No new attack surface — scan roots are hardcoded constants (`COMPONENTS_DIR`, `APP_DIR`, `CONTENT_DIR`, `STYLES_DIR`); no user input crosses the boundary; the test only string-searches matched content (no execution); same disposition as Phase 38 T-38-15 |
| T-39-16 | D (denial — false positive on legitimate "slate" / "dusk" English) | Comments / strings using those words in non-theme contexts | accept | Per CONTEXT D-04 (Claude's Discretion: err on inclusion); friction at re-introduction is intentional. Current codebase has zero such collisions (verified at plan creation time). If false-positive friction proves excessive in a future review, the planner may narrow the substring entries to regex-only — but D-04 locks the current scope. |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Create src/content/content.no-removed-themes.test.ts drift-guard mirroring content.no-variants.test.ts with the 12-token Phase 39 forbidden list</name>
  <files>
    - src/content/content.no-removed-themes.test.ts (NEW)
  </files>
  <read_first>
    - src/content/content.no-variants.test.ts (entire file — this is the structural analog; mirror imports, the collectFiles helper, the SCAN_FILES concatenation, and the describe/it/aggregation shape verbatim apart from the adaptations documented in this plan's <interfaces> block)
    - .planning/phases/39-theme-simplification/39-PATTERNS.md §"Plan 5 — drift-guard test" — full forbidden-token list + describe block + header comment + self-exclusion warning
    - .planning/phases/39-theme-simplification/39-CONTEXT.md §decisions D-03 / D-04 / D-05 / D-06
    - .planning/phases/39-theme-simplification/39-02-SUMMARY.md (confirm Plan 02 deleted theme.css blocks + faviconPalette entries)
    - .planning/phases/39-theme-simplification/39-03-SUMMARY.md (confirm Plan 03 deleted strings.ts entries + rotated hook/App test fixtures)
    - .planning/phases/39-theme-simplification/39-04-SUMMARY.md (confirm Plan 04 excised index.html FOUC tokens)
    - .planning/phases/38-variant-removal/38-04-SUMMARY.md (Phase 38 VAR-06 close-out — pattern reference)
    - src/content/content.no-stats-ui.test.ts (Phase 37 STATS-05 original precedent — useful if the no-variants analog reads ambiguously)
  </read_first>
  <action>
    Create the new file `src/content/content.no-removed-themes.test.ts`. Mirror `src/content/content.no-variants.test.ts` structurally (verbatim structural twin per PATTERNS) — triple-slash node-types reference, vitest + node:fs + node:path imports, recursive `collectFiles` helper with `.ts/.tsx/.css` extension filter + `.test.ts`/`.test.tsx` exclusion, four-root scope, single describe with 2 it() cases (sanity-floor + main sweep). The ONLY substantive differences are the header comment text, the describe label, the failure-message context, and the FORBIDDEN_TOKENS list shape (12 entries per D-04 — replaces the Phase 38 14-token list).

    Step-by-step instructions (follow in order):

    1. Header comment block (≈25 lines, mirroring the Phase 38 VAR-06 file's structure):
       - Line 1 file-path comment: `// src/content/content.no-removed-themes.test.ts`
       - Purpose: `// Phase 39 drift-guard (CONTEXT D-03 / D-04 / D-05 / D-06).`
       - Scanned roots block (per PATTERNS §"WHY this file exists" header comment) — 4 roots + WHY each is in scope.
       - Forbidden token classes block — enumerate the 4 classes (9 plain-substring + 3 regex).
       - WHY this file exists block (D-06 lock/unlock note).
       - Analog pointer: `// Analog: src/content/content.no-variants.test.ts (Phase 38 VAR-06 — verbatim structural twin)`.

    2. Triple-slash node reference + imports — copy verbatim from the no-variants analog (the analog's reason-comment about `tsconfig.app.json` excluding @types/node is identical here; carry the comment over):
       ```typescript
       // Reason: node:fs and node:path are available in the Vitest jsdom test environment.
       // tsconfig.app.json has types:["vite/client"] which excludes @types/node; the triple-slash
       // reference adds Node.js type coverage for this test-only file without altering tsconfig.app.json.
       /// <reference types="node" />

       import { describe, it, expect } from 'vitest'
       import { readFileSync, readdirSync, statSync } from 'node:fs'
       import { resolve, join } from 'node:path'
       ```

    3. `collectFiles` helper — copy verbatim from the no-variants analog (same 4-root accept clause: `.ts || .tsx || .css`; same reject clause: `.test.ts || .test.tsx`). Comment-block above the function explains the load-bearing test-file exclusion (the guard file's own literal token strings would self-flag without it).

    4. Four scanned-root constants + SCAN_FILES — copy verbatim from the no-variants analog:
       ```typescript
       const COMPONENTS_DIR = resolve(__dirname, '..', 'components')
       const APP_DIR        = resolve(__dirname, '..', 'app')
       const CONTENT_DIR    = resolve(__dirname)
       const STYLES_DIR     = resolve(__dirname, '..', 'styles')

       const SCAN_FILES: string[] = [
         ...collectFiles(COMPONENTS_DIR),
         ...collectFiles(APP_DIR),
         ...collectFiles(CONTENT_DIR),
         ...collectFiles(STYLES_DIR),
       ]
       ```

    5. FORBIDDEN_TOKENS array — 12 entries per <interfaces> block (REPLACES the Phase 38 14-token list):
       - 9 plain-substring entries: `'moss'`, `'slate'`, `'dusk'`, `'Moss'`, `'Slate'`, `'Dusk'`, `'Musgo'`, `'Ardósia'`, `'Crepúsculo'` (case-sensitive).
       - 3 regex entries: persisted-value literal (`/theme:\\s*['\"](moss|slate|dusk)['\"]/`), CSS selector (`/\\[data-theme=['\"]?(moss|slate|dusk)['\"]?\\]/`), object-key entry (`/['\"](moss|slate|dusk)['\"]\\s*:/`).

       Use the labels from the <interfaces> block verbatim (so any failure message reads consistently with the planning artifacts).

    6. Single describe + 2 it() cases — copy verbatim from the no-variants analog with these substitutions:
       - describe title: `'Phase 39 drift-guard: no removed theme tokens (CONTEXT D-04 / D-05 / D-06)'`.
       - Sanity-floor it() title: `'scans a non-empty set of production files'` (unchanged from analog).
       - Sanity-floor assertion message: `'Phase 39 drift-guard scanned zero files — collectFiles or scan-root resolve is broken'`.
       - Main sweep it() title: `'no forbidden moss/slate/dusk token appears in src/components/, src/app/, src/content/, or src/styles/'`.
       - Main sweep failure message: `'Forbidden moss/slate/dusk tokens found (Phase 39 3-theme-only invariant violated):\n${hits.join(\'\\n\')}'`.

    7. Sanity-run the new test:
       - `pnpm test src/content/content.no-removed-themes.test.ts --run` MUST exit 0 with 2 passing tests.
       - If it fails: the failure message names the file + the token. DO NOT loosen the token list. Trace back to the failing Plan (02/03/04) deletion and surface the gap.
       - Common false-failure traceback: a stray `'slate'` comment in surviving code (e.g. "this color is a slate grey") would trip the substring entry. Investigate and either rotate the comment wording or — only if the planner explicitly approves — narrow the substring entry to a regex per the §threat_model T-39-16 disposition.

    8. DO NOT touch any other file in this task. The test file is self-contained and discovered by vitest's globs.

    9. Commit: `test(39): drift-guard for removed theme tokens (CONTEXT D-04 D-05 D-06)`.
  </action>
  <verify>
    <automated>pnpm test src/content/content.no-removed-themes.test.ts --run</automated>
  </verify>
  <acceptance_criteria>
    - File `src/content/content.no-removed-themes.test.ts` exists (`test -f src/content/content.no-removed-themes.test.ts`).
    - First ~30 lines contain the header comment block naming Phase 39 + the 4 scanned roots + the 4 forbidden-token classes + the D-06 lock/unlock note.
    - File contains `/// <reference types="node" />` in the first ~30 lines.
    - `grep -n "import.*vitest" src/content/content.no-removed-themes.test.ts` returns ≥1 match.
    - `grep -n "import.*node:fs" src/content/content.no-removed-themes.test.ts` returns ≥1 match.
    - `grep -nE "STYLES_DIR" src/content/content.no-removed-themes.test.ts` returns ≥1 match (the 4th scanned root).
    - `grep -nE "entry\\.endsWith\\(['\"]\\.css['\"]\\)" src/content/content.no-removed-themes.test.ts` returns ≥1 match (the .css filter extension carries over from the Phase 38 VAR-06 analog).
    - `grep -nE "endsWith\\(['\"]\\.test\\.tsx['\"]\\)" src/content/content.no-removed-themes.test.ts` returns ≥1 match (the .test.tsx self-exclusion).
    - `grep -cE "t\\.includes\\(['\"](moss|slate|dusk|Moss|Slate|Dusk|Musgo|Ardósia|Crepúsculo)['\"]\\)" src/content/content.no-removed-themes.test.ts` returns 9 (all 9 plain-substring tokens present).
    - `grep -nE "/theme:\\\\s\\*\\['\"](moss\\|slate\\|dusk)" src/content/content.no-removed-themes.test.ts` returns ≥1 match (the persisted-value regex).
    - `grep -nE "data-theme=\\['\"]?\\(moss\\|slate\\|dusk" src/content/content.no-removed-themes.test.ts` returns ≥1 match (the CSS-selector regex).
    - `grep -nE "\\\\s\\*:" src/content/content.no-removed-themes.test.ts` returns ≥1 match for the object-key regex pattern (one of the regex entries).
    - `pnpm test src/content/content.no-removed-themes.test.ts --run` exits 0 with 2 passing tests.
    - `pnpm tsc --noEmit` exits 0.
    - File line count is in the analog range (≥ 90 lines, ≤ ~160 lines — proportional to the Phase 38 analog ≈130 lines + the slightly different token list shape).
  </acceptance_criteria>
  <done>The drift-guard exists at src/content/content.no-removed-themes.test.ts, passes against the current clean codebase, and will fail CI if any of the 12 forbidden tokens (9 plain-substring + 3 regex categories) reappears in src/components/, src/app/, src/content/, or src/styles/ (excluding test files).</done>
</task>

<task type="auto">
  <name>Task 2: Run the closing green gate for Phase 39 and verify all 5 ROADMAP success criteria</name>
  <files>(no source files modified — verification only)</files>
  <read_first>
    - .planning/phases/39-theme-simplification/39-01-SUMMARY.md
    - .planning/phases/39-theme-simplification/39-02-SUMMARY.md
    - .planning/phases/39-theme-simplification/39-03-SUMMARY.md
    - .planning/phases/39-theme-simplification/39-04-SUMMARY.md
    - .planning/phases/39-theme-simplification/39-CONTEXT.md §decisions D-01, D-04, D-08, §specifics (the "merciless cleanup" + manual-audit pattern)
    - .planning/ROADMAP.md §Phase 39 (the 5 ROADMAP success criteria)
  </read_first>
  <action>
    Run the full closing green gate for Phase 39. No file edits — this task verifies the cumulative state of Plans 01 + 02 + 03 + 04 + 05 (this plan) and produces the data the Phase 39 SUMMARY / VERIFICATION steps will reference.

    1. `pnpm tsc --noEmit` — must exit 0.

    2. `pnpm test --run` — must exit 0 across the entire suite. Test count should be ~1095 + 3 (Plan 01's 2 new THM-05 cases + Plan 05's drift-guard 2 cases — net +4 minus any deletions in faviconPalette.test.ts's 6 removed cases; planner should not pre-commit to a specific count, just confirm the suite is green).

    3. `pnpm build` — must exit 0 (Vite + PostCSS parses theme.css cleanly after the L152-320 deletion; Vite parses index.html cleanly after Plan 04's L18 surgical excision).

    4. `pnpm test src/content/content.no-removed-themes.test.ts -t "Phase 39 drift-guard" --run` — must exit 0 with 2 passing tests.

    5. Belt-and-suspenders cumulative grep audit (CONTEXT §specifics + Phase 38 close-of-phase pattern):
       - `git grep -i 'moss\|slate\|dusk' src/ | grep -v 'src/content/content.no-removed-themes.test.ts'` — MUST return 0 matches (only the drift-guard test file is allowed to contain the literal token strings).
       - `git grep -i 'moss\|slate\|dusk' index.html` — MUST return 0 matches.
       - `git grep -E "\\[data-theme=['\"](moss|slate|dusk)['\"]\\]" src/styles/ | grep -v 'src/content/content.no-removed-themes.test.ts'` — MUST return 0 matches.
       - `git grep -E "theme:\\s*'(moss|slate|dusk)'" src/ | grep -v 'src/content/content.no-removed-themes.test.ts'` — MUST return 0 matches.
       - `git grep -E "(Musgo|Ardósia|Crepúsculo)" src/ | grep -v 'src/content/content.no-removed-themes.test.ts'` — MUST return 0 matches (PT-BR display strings).

    6. STATE_VERSION lock per CONTEXT D-01 (no bump):
       - `git diff main -- src/storage/storage.ts | grep -nE "^[+-].*STATE_VERSION"` — MUST return 0 matches (the constant is unchanged across all five plans). Same stance as Phase 38.

    7. Phase 39 ROADMAP success criteria 1-5 are observable:
       - SC1 (`theme.css` and `ThemeId` union contain only light/dark/system; Moss/Slate/Dusk deleted — THM-01..04): verified by step 5 grep + Plan 01's settings.ts narrowing (verified via `grep -n "export type ThemeId" src/domain/settings.ts`).
       - SC2 (ThemePicker renders exactly 3 options; System resolves to OS — THM-04): verified by step 2's passing ThemePicker.test.tsx suite (Plan 03's 3-option label assertion).
       - SC3 (persisted moss/slate/dusk lands on system on read AND re-persists as system — THM-05): verified by step 2's passing prefs.test.ts THM-05 two-level regression (Plan 01).
       - SC4 (faviconPalette = 2 entries; favicon.sync.test.ts guards 2-palette; pre-paint no FOUC — THM-06, THM-07): verified by step 2's passing favicon.sync test + Plan 04's index.html alignment.
       - SC5 (WCAG luminance contrast guard regenerates against 2-palette + system surface and passes — THM-08): verified by step 2's passing theme.contrast.test.ts suite (Plan 02's THEME_05_FLOORS collapse).

    8. Drift-guard surface coverage per CONTEXT D-05:
       - The 4 scanned roots cover render paths (components + app), i18n catalogs (content), and CSS tokens (styles). The empirical proof is step 4's passing run.

    9. Record each command's exit code + summary for the Phase 39 SUMMARY. No commit is created by this task — it is verification only. The Plan 05 commit landed in Task 1.
  </action>
  <verify>
    <automated>pnpm tsc --noEmit && pnpm test --run && pnpm build</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm tsc --noEmit` exits 0.
    - `pnpm test --run` exits 0 across the whole suite.
    - `pnpm build` exits 0.
    - `pnpm test src/content/content.no-removed-themes.test.ts --run` exits 0 with 2 passing tests.
    - `git grep -i 'moss\|slate\|dusk' src/ | grep -v 'src/content/content.no-removed-themes.test.ts'` returns 0 matches.
    - `git grep -i 'moss\|slate\|dusk' index.html` returns 0 matches.
    - `git grep -E "\\[data-theme=['\"](moss|slate|dusk)['\"]\\]" src/styles/ | grep -v 'src/content/content.no-removed-themes.test.ts'` returns 0 matches.
    - `git grep -E "theme:\\s*'(moss|slate|dusk)'" src/ | grep -v 'src/content/content.no-removed-themes.test.ts'` returns 0 matches.
    - `git grep -E "(Musgo|Ardósia|Crepúsculo)" src/ | grep -v 'src/content/content.no-removed-themes.test.ts'` returns 0 matches.
    - `git diff main -- src/storage/storage.ts | grep -nE "^[+-].*STATE_VERSION"` returns 0 matches (STATE_VERSION lock confirmed).
    - Phase 39 ROADMAP success criteria 1-5 are observably TRUE.
  </acceptance_criteria>
  <done>Full green gate (tsc + test + build) is clean, the Phase 39 drift-guard passes, no forbidden tokens survive in production code or index.html, STATE_VERSION is locked. Phase 39 ROADMAP success criteria 1, 2, 3, 4, 5 are all observable. Phase 39 is complete.</done>
</task>

</tasks>

<verification>
After both tasks complete:
- `pnpm tsc --noEmit` exits 0.
- `pnpm test --run` exits 0 across the entire suite.
- `pnpm build` exits 0.
- The new drift-guard file `src/content/content.no-removed-themes.test.ts` exists and the 2 it() cases pass.
- THM-01..08 are observably closed across Plans 01-05; D-01..D-12 from CONTEXT.md are all referenced by at least one plan.
- The belt-and-suspenders audit (5 cumulative grep checks + STATE_VERSION lock) confirms the merciless-cleanup mandate from CONTEXT §specifics.
- One atomic commit scoped `(39)` from this plan: `test(39): drift-guard for removed theme tokens (CONTEXT D-04 D-05 D-06)`.
</verification>

<success_criteria>
- src/content/content.no-removed-themes.test.ts exists, mirrors the no-variants.test.ts analog structurally, scans 4 roots, filters `*.test.ts` AND `*.test.tsx`, accepts `.ts`/`.tsx`/`.css` files, checks 12 forbidden tokens (9 plain-substring + 3 regex categories per CONTEXT D-04).
- The drift-guard test passes against the post-Plans-02/03/04 clean state.
- The full pnpm test + build suite passes; tsc is green.
- THM-01..03 are observably locked (CI fails if any forbidden token returns); THM-04..08 closed across earlier plans.
- STATE_VERSION unchanged across the phase.
- All 8 phase requirements (THM-01..08) are closed across Plans 01-05.
- The cumulative grep audit shows zero moss/slate/dusk references in src/ or index.html outside the drift-guard test file.
</success_criteria>

<output>
After completion, create `.planning/phases/39-theme-simplification/39-05-SUMMARY.md` summarising:
- The new drift-guard file path, line count, and key structural choices (mirrors the no-variants.test.ts analog verbatim; 4 roots; .css filter; 12 tokens).
- The exact 12-token forbidden list with the rationale link to CONTEXT D-04.
- Confirmation that the 2 it() cases pass against the post-Plans-02/03/04 clean state.
- Closing the phase: THM-01..08 are all observable across the five plans; D-01..D-12 are all referenced; STATE_VERSION is locked; Phase 39 ROADMAP success criteria 1-5 are all observable TRUE.
- Pointer to the SUMMARY exit clause (CONTEXT D-06): future re-introduction phase explicitly deletes this file with rationale recorded in that phase's SUMMARY. The drift-guard is the lock; deleting it is the unlock.
- Belt-and-suspenders audit results (the 5 cumulative grep checks from Task 2 step 5, each returning 0 matches).
- Pointer forward to the next phase per ROADMAP. Phase 39 closes; Phase 41 (Mono Zen palette + tokens) is unblocked: surviving 2 palettes (light + dark) plus a system follow — Phase 41 will retune those 2 palette hex values and add new tokens (`borderSoft`, `textSoft`, `orbHalo1-3`, `onAccent`) + semibold Inter typography. Phase 39's drift-guard remains as the long-term lock against deprecated-palette re-introduction.
- Tiger Style commit hygiene: 1 atomic commit this plan (the drift-guard test file), scoped `(39)`.
</output>
</content>
</invoke>
