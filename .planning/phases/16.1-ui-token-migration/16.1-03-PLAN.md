---
phase: 16.1
plan: 03
type: execute
wave: 2
depends_on: [16.1-01]
files_modified:
  - src/components/ThemePicker.tsx
  - src/components/TimbrePicker.tsx
  - src/components/VariantPicker.tsx
  - src/components/LanguagePicker.tsx
autonomous: true
requirements: [THEME-UI-01]
tags: [theme, tailwind-v4, picker, tsx-migration]

must_haves:
  truths:
    - "ThemePicker section label + unselected-state buttons rebind on Dark/Moss/Slate/Dusk swap (closes WR-01 'Phase 16 original sin')"
    - "TimbrePicker, VariantPicker, LanguagePicker section labels + ternary disabled/active text rebind on theme swap"
    - "ThemePicker selected-state buttons (line 32 selectedClasses const) remain untouched (already correct per PATTERNS.md Reference site A)"
    - "All 4 pickers' existing tests pass without modification (D-06)"
  artifacts:
    - path: "src/components/ThemePicker.tsx"
      provides: "Tokenized section label + tokenized unselectedClasses const; selectedClasses untouched"
      contains: "var(--color-breathing-"
    - path: "src/components/TimbrePicker.tsx"
      provides: "Tokenized section label + tokenized ternary active-text branch"
      contains: "var(--color-breathing-accent-strong)"
    - path: "src/components/VariantPicker.tsx"
      provides: "Tokenized section label + tokenized ternary active-text branch"
      contains: "var(--color-breathing-accent-strong)"
    - path: "src/components/LanguagePicker.tsx"
      provides: "Tokenized section label + tokenized ternary active-text branch"
      contains: "var(--color-breathing-accent-strong)"
  key_links:
    - from: "src/components/ThemePicker.tsx (unselected branch)"
      to: "secondary-button canonical string from plan 02"
      via: "same recipe — border + bg + text trio + hover/active"
      pattern: "border-\\[var\\(--color-breathing-accent\\)\\] bg-\\[var\\(--color-breathing-surface\\)\\]"
    - from: "ThemePicker selected branch (line 32)"
      to: "DO NOT MODIFY (Reference site A — already canonical)"
      via: "selectedClasses const unchanged"
      pattern: "border-2 border-\\[var\\(--color-breathing-accent\\)\\]"
---

<objective>
Wave 1 / Plan 03: Migrate Group D (the picker family) from `16.1-PATTERNS.md` §4 — `ThemePicker`, `TimbrePicker`, `VariantPicker`, `LanguagePicker`. The four pickers share an almost-identical 2-line surface: a section label (`text-slate-900`) and a ternary disabled/active text class (`text-[var(--color-breathing-muted)]` already correct on disabled / `text-slate-700` on active needs migration). ThemePicker has one additional line — the `unselectedClasses` const on line 33 — which is THE bug that motivated Phase 16.1 (the "Phase 16 original sin" / WR-01, AP-01).

Purpose: Close the WR-01 regression (selected ThemePicker button rebinds per palette, unselected buttons stay teal-on-white) by token-migrating `unselectedClasses`. Apply identical 2-line label/ternary substitution to the 3 stub pickers so when Phase 17/18/19 wire up real timbre/variant/language selection logic on top of these stubs, the picker surface already rebinds correctly.

Output: 4 modified TSX files (~10 banned-class occurrences replaced). ThemePicker.tsx:32 selectedClasses const is untouched (already canonical per PATTERNS.md Reference site A). No test edits.
</objective>

<execution_context>
@$HOME/.claude-personal-personal/get-shit-done/workflows/execute-plan.md
@$HOME/.claude-personal-personal/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/16.1-ui-token-migration/16.1-CONTEXT.md
@.planning/phases/16.1-ui-token-migration/16.1-PATTERNS.md
@.planning/phases/16.1-ui-token-migration/16.1-RESEARCH.md
@.planning/phases/16.1-ui-token-migration/16.1-01-SUMMARY.md
@src/components/ThemePicker.tsx
@src/components/TimbrePicker.tsx
@src/components/VariantPicker.tsx
@src/components/LanguagePicker.tsx
@src/components/StatsFooter.tsx

<interfaces>
PATTERNS.md §4 Group P-STATE / P-SUB locked recipes:

Section label (P-SUB — same across all 4 pickers, line 22 in ThemePicker, line 20 in Timbre/Variant, line 22 in Language):
- BEFORE: `text-sm font-semibold text-slate-900`
- AFTER:  `text-sm font-semibold text-[var(--color-breathing-accent-strong)]`

Ternary active text — TimbrePicker:21, VariantPicker:21, LanguagePicker:23:
- BEFORE: `text-sm ${disabled ? 'text-[var(--color-breathing-muted)]' : 'text-slate-700'}`
- AFTER:  `text-sm ${disabled ? 'text-[var(--color-breathing-muted)]' : 'text-[var(--color-breathing-accent-strong)]'}`
- Note: the disabled branch was already migrated in Phase 16 (probably during Phase 15 stub creation). Only the active branch (`text-slate-700`) is the migration target.

ThemePicker.tsx unselectedClasses (line 33 — the "Phase 16 original sin" / WR-01):
- BEFORE: `const unselectedClasses = 'border border-teal-200 bg-white text-teal-800 hover:bg-teal-50 active:bg-teal-100'`
- AFTER:  `const unselectedClasses = 'border border-[var(--color-breathing-accent)] bg-[var(--color-breathing-surface)] text-[var(--color-breathing-accent-strong)] hover:bg-[var(--color-breathing-bg-soft)] active:bg-[var(--color-breathing-bg-soft)]'`
- This is structurally identical to the canonical secondary-button string locked by plan 02 (Close/Keep/Cancel pattern) but without the `min-h-12 rounded-full ... shadow-sm transition motion-reduce:transition-none focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-breathing-accent focus-visible:ring-offset-2` prefix (those parts live on the parent button JSX, not on the const).

PATTERNS.md §4 Group P-STATE "ThemePicker.tsx special case (line 33)":
- ONLY line 33 (unselectedClasses) migrates.
- Line 32 (selectedClasses) is the Reference site A — already canonical. DO NOT touch it.
- After migration both branches use the same token tuple BUT:
  - selectedClasses: `border-2` width + `bg-[var(--color-breathing-bg-soft)]` (bg-soft as fill) + `text-[var(--color-breathing-accent-strong)]` (accent-strong as text)
  - unselectedClasses: `border` single-pixel width + `bg-[var(--color-breathing-surface)]` (surface as fill) + `text-[var(--color-breathing-accent-strong)]` (same accent-strong as text)
  Different border-width and different bg role; same text token. Verified per PATTERNS.md §1 Reference site A "Note".

Existing tests (DO NOT edit):
- src/components/ThemePicker.test.tsx (8 tests — Phase 16, includes assertion that unselectedClasses is applied to non-selected buttons; assertions test class string presence by substring, so they remain green if the const value changes but the spread on the button is preserved)
- src/components/TimbrePicker.test.tsx, VariantPicker.test.tsx, LanguagePicker.test.tsx (Phase 15 stubs — test label rendering + disabled-state ARIA; orthogonal to color tokens)
- src/app/App.settings.test.tsx (asserts picker disabled-state behavior + dialog disable threading; no color assertions)

CRITICAL — ThemePicker.tsx line 32 (PRESERVE verbatim):
`const selectedClasses = 'border-2 border-[var(--color-breathing-accent)] bg-[var(--color-breathing-bg-soft)] text-[var(--color-breathing-accent-strong)]'`

Current banned-class occurrences (verified grep 2026-05-13):
- ThemePicker.tsx: lines 22 (label), 33 (unselectedClasses) — 2 occurrences
- TimbrePicker.tsx: lines 20 (label), 21 (ternary active) — 2 occurrences
- VariantPicker.tsx: lines 20 (label), 21 (ternary active) — 2 occurrences
- LanguagePicker.tsx: lines 22 (label), 23 (ternary active) — 2 occurrences
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate ThemePicker (closes WR-01 'Phase 16 original sin') + 3 stub pickers</name>
  <files>src/components/ThemePicker.tsx, src/components/TimbrePicker.tsx, src/components/VariantPicker.tsx, src/components/LanguagePicker.tsx</files>
  <read_first>
    - src/components/ThemePicker.tsx (current state — line 22 label, line 32 selectedClasses PRESERVE, line 33 unselectedClasses migrate)
    - src/components/TimbrePicker.tsx (line 20 label, line 21 ternary active-branch)
    - src/components/VariantPicker.tsx (same shape as TimbrePicker)
    - src/components/LanguagePicker.tsx (line 22 label, line 23 ternary)
    - .planning/phases/16.1-ui-token-migration/16.1-PATTERNS.md §1 Reference site A (ThemePicker:32 — already canonical; do not regress)
    - .planning/phases/16.1-ui-token-migration/16.1-PATTERNS.md §4 Group P-STATE (ThemePicker special case + 3-stub picker pattern)
    - .planning/phases/16.1-ui-token-migration/16.1-PATTERNS.md §5 AP-01 (the canonical "this is the bug we are fixing" anti-pattern)
    - .planning/phases/16.1-ui-token-migration/16.1-CONTEXT.md D-05 + D-06 (arbitrary-value form, class-composition only)
    - src/components/StatsFooter.tsx (Reference site B — already-tokenized text-color pattern; informs the section-label migration)
    - .planning/phases/16.1-ui-token-migration/16.1-02-SUMMARY.md (canonical secondary-button string locked by plan 02 — informs the unselectedClasses construction)
  </read_first>
  <action>
    Edit ONE className/const string per location below. Per CONTEXT.md D-05 use the arbitrary-value form. Per CONTEXT.md D-06 change only className text, no JSX/prop/handler edits.

    **ThemePicker.tsx:**

    - Line 22 (section label `<p id="theme-picker-label">`):
      - Replace `text-slate-900` with `text-[var(--color-breathing-accent-strong)]`.
      - Preserve `text-sm font-semibold` verbatim. Preserve `id="theme-picker-label"` verbatim (a11y — D-06).

    - Line 32 (`selectedClasses` const): **DO NOT TOUCH.** Reference site A — already canonical per PATTERNS.md §1.

    - Line 33 (`unselectedClasses` const):
      - Replace the const value entirely with: `'border border-[var(--color-breathing-accent)] bg-[var(--color-breathing-surface)] text-[var(--color-breathing-accent-strong)] hover:bg-[var(--color-breathing-bg-soft)] active:bg-[var(--color-breathing-bg-soft)]'`
      - Mechanical replacement of the 5-class string. Quote style: single quotes (match the existing const literal).

    **TimbrePicker.tsx:**

    - Line 20 (section label `<p>`):
      - Replace `text-slate-900` with `text-[var(--color-breathing-accent-strong)]`.
      - Preserve `text-sm font-semibold` verbatim.

    - Line 21 (ternary in the descriptor `<p>` className template literal):
      - Replace `text-slate-700` with `text-[var(--color-breathing-accent-strong)]`.
      - Preserve the ternary structure verbatim: `text-sm ${disabled ? 'text-[var(--color-breathing-muted)]' : 'text-[var(--color-breathing-accent-strong)]'}`.

    **VariantPicker.tsx:**

    - Line 20 (label): same migration as TimbrePicker:20.
    - Line 21 (ternary): same migration as TimbrePicker:21.

    **LanguagePicker.tsx:**

    - Line 22 (label): same migration.
    - Line 23 (ternary): same migration as TimbrePicker:21 (note: line numbers differ slightly — verify by reading the file before editing).

    After all 4 files edited:
    - `npx tsc --noEmit` — exits 0.
    - `npm run lint` — exits 0.
    - `npm run build` — exits 0.
    - `npm test -- --run src/components/ThemePicker.test.tsx src/components/TimbrePicker.test.tsx src/components/VariantPicker.test.tsx src/components/LanguagePicker.test.tsx src/app/App.settings.test.tsx` — exits 0.

    Commit as `feat(16.1-03): migrate ThemePicker unselected branch + 3 stub picker labels to --color-breathing-* tokens (closes WR-01)`.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run lint && grep -cE '\b(text|bg|border)-(slate|teal)-[0-9]|\b(text|bg)-(white|black)\b' src/components/ThemePicker.tsx src/components/TimbrePicker.tsx src/components/VariantPicker.tsx src/components/LanguagePicker.tsx | awk -F: '{sum+=$2} END {print "banned:", sum}' && grep -c "selectedClasses = 'border-2 border-\\[var(--color-breathing-accent)\\]" src/components/ThemePicker.tsx && grep -c "unselectedClasses = 'border border-\\[var(--color-breathing-accent)\\]" src/components/ThemePicker.tsx && npm test -- --run src/components/ThemePicker.test.tsx src/components/TimbrePicker.test.tsx src/components/VariantPicker.test.tsx src/components/LanguagePicker.test.tsx src/app/App.settings.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE '\b(text|bg|border)-(slate|teal)-[0-9]|\b(text|bg)-(white|black)\b' src/components/ThemePicker.tsx src/components/TimbrePicker.tsx src/components/VariantPicker.tsx src/components/LanguagePicker.tsx | awk -F: '{sum+=$2} END {print sum}'` returns 0 (all 8 banned-class occurrences across the 4 pickers eliminated).
    - `grep -c "selectedClasses = 'border-2 border-\[var(--color-breathing-accent)\] bg-\[var(--color-breathing-bg-soft)\] text-\[var(--color-breathing-accent-strong)\]'" src/components/ThemePicker.tsx` returns 1 (Reference site A preserved verbatim — AP-07-equivalent sentinel for the picker selected state).
    - `grep -c "unselectedClasses = 'border border-\[var(--color-breathing-accent)\] bg-\[var(--color-breathing-surface)\]" src/components/ThemePicker.tsx` returns 1 (new tokenized unselected-state const).
    - `grep -c "text-\[var(--color-breathing-accent-strong)\]" src/components/TimbrePicker.tsx` returns 2 (label + ternary active branch); same for VariantPicker.tsx and LanguagePicker.tsx.
    - `grep -c "text-\[var(--color-breathing-muted)\]" src/components/TimbrePicker.tsx src/components/VariantPicker.tsx src/components/LanguagePicker.tsx | awk -F: '{sum+=$2} END {print sum}'` returns >= 3 (disabled branch of each picker — preserved from Phase 15/16, sentinel that those weren't accidentally rewritten).
    - All 4 picker test files pass; `App.settings.test.tsx` passes (picker disable threading preserved — D-06).
    - `npx tsc --noEmit && npm run lint && npm run build` exits 0.
  </acceptance_criteria>
  <done>
    All 4 pickers are token-migrated. ThemePicker's "Phase 16 original sin" (WR-01) is closed: unselected and selected buttons now both reference `var(--color-breathing-*)` tokens, so a theme swap rebinds the full picker grid (not just the selected button). The 3 stub pickers have tokenized labels + active-text ternaries; when Phase 17/18/19 wire up real selection logic, the picker chrome will already rebind correctly. Combined with plan 02, the secondary-button + section-label patterns are locked across 8 of the 16 migration-surface files.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| no new boundaries | className substitution only; ThemePicker continues to use `useThemeChoice` (Phase 16 hook); no new state, no new event paths. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16.1-03-01 | Tampering | ThemePicker.tsx:32 selectedClasses (Reference site A) | mitigate | Verbatim-string grep sentinel in acceptance_criteria; any deviation flips the gate red. |
| T-16.1-03-02 | Spoofing (a11y) | Disabled-state muted text branch | mitigate | `grep -c 'text-\[var(--color-breathing-muted)\]'` >= 3 sentinel ensures the disabled branch is not accidentally rewritten when the active branch is migrated. |

This is a pure className substitution plan. No XSS / injection / auth surface introduced.
</threat_model>

<verification>
After Task 1 completes, run the per-commit green-gate:

```bash
npx tsc --noEmit && npm run lint && npm run build && npm test -- --run
```

Expected: exit 0.

Plan-level grep sentinels (run as a post-plan smoke check):

```bash
# All 4 pickers token-migrated:
grep -cE '\b(text|bg|border)-(slate|teal)-[0-9]|\b(text|bg)-(white|black)\b' \
  src/components/ThemePicker.tsx src/components/TimbrePicker.tsx \
  src/components/VariantPicker.tsx src/components/LanguagePicker.tsx
# Expected: 0 on every line.

# Reference site A preserved (ThemePicker selectedClasses verbatim):
grep -F "selectedClasses = 'border-2 border-[var(--color-breathing-accent)] bg-[var(--color-breathing-bg-soft)] text-[var(--color-breathing-accent-strong)]'" src/components/ThemePicker.tsx
# Expected: 1 matching line.

# WR-01 fix (ThemePicker unselectedClasses tokenized):
grep -F "unselectedClasses = 'border border-[var(--color-breathing-accent)] bg-[var(--color-breathing-surface)] text-[var(--color-breathing-accent-strong)] hover:bg-[var(--color-breathing-bg-soft)] active:bg-[var(--color-breathing-bg-soft)]'" src/components/ThemePicker.tsx
# Expected: 1 matching line.
```
</verification>

<success_criteria>
1. 8 of 8 banned-class occurrences across ThemePicker, TimbrePicker, VariantPicker, LanguagePicker eliminated.
2. ThemePicker.tsx:32 `selectedClasses` const is byte-identical to its pre-plan value (Reference site A preserved).
3. ThemePicker.tsx:33 `unselectedClasses` const is the canonical tokenized secondary-button form (closes WR-01).
4. Three stub pickers (Timbre/Variant/Language) have tokenized section labels and tokenized ternary active-text branches.
5. Disabled-branch `text-[var(--color-breathing-muted)]` reference in Timbre/Variant/Language pickers is preserved untouched (no accidental over-rewrite).
6. All 4 picker test files and `App.settings.test.tsx` pass without modification.
7. `npx tsc --noEmit && npm run lint && npm run build && npm test -- --run` exits 0.
</success_criteria>

<output>
After completion, create `.planning/phases/16.1-ui-token-migration/16.1-03-SUMMARY.md` summarizing:
- 4 files migrated, 8 banned-class lines cleared
- WR-01 status: closed (ThemePicker unselected branch now tokenized; the "Phase 16 original sin" no longer reproduces on theme swap)
- Reference site A preservation confirmed (selectedClasses byte-identical sentinel)
- Test counts: 22 (8 ThemePicker + 4 Variant + 5 Timbre + 5 Language = stub counts may differ; record actual counts) — pre/post plan identical
- Note for downstream phases: the canonical secondary-button form is now locked in 3 different shapes (full Close/Keep button in plan 02, unselected picker button here, anchor button in plan 04) — Phase 17/18/19 should copy whichever shape matches their consumer
</output>
