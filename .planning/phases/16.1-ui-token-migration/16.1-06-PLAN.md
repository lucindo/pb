---
phase: 16.1
plan: 06
type: execute
wave: 3
depends_on: [16.1-01, 16.1-02, 16.1-03, 16.1-04, 16.1-05]
files_modified:
  - src/components/SessionControls.tsx
autonomous: false
requirements: [THEME-UI-01]
tags: [theme, tailwind-v4, primary-action, on-accent-token, tsx-migration, uat]

must_haves:
  truths:
    - "Start/Stop primary action button rebinds per palette on Dark/Moss/Slate/Dusk swap"
    - "text-white -> text-[var(--color-breathing-on-accent)] consumes the D-01 token defined by plan 01 (per-palette contrast-tuned foreground for accent-strong background)"
    - "bg-teal-700 / hover:bg-teal-800 / active:bg-teal-900 migrated to var(--color-breathing-accent-strong) / accent / accent-strong"
    - "Per-palette manual UAT confirms primary-button text is legible against accent-strong bg in all 5 palettes (light/dark/moss/slate/dusk)"
    - "shadow-teal-900/20 colored-shadow carry-forward documented (NOT in banned regex list — preserved verbatim like SettingsStepper:36 + SessionReadout:29,64)"
  artifacts:
    - path: "src/components/SessionControls.tsx"
      provides: "Tokenized Start/Stop primary action button (both lines 52 mt-6 layout + 69 inline-mute layout)"
      contains: "var(--color-breathing-on-accent)"
  key_links:
    - from: "src/components/SessionControls.tsx:52,69 text foreground"
      to: "src/styles/theme.css --color-breathing-on-accent (per-palette)"
      via: "text-[var(--color-breathing-on-accent)]"
      pattern: "var\\(--color-breathing-on-accent\\)"
    - from: "src/components/SessionControls.tsx:52,69 background"
      to: "src/styles/theme.css --color-breathing-accent-strong (per-palette)"
      via: "bg-[var(--color-breathing-accent-strong)]"
      pattern: "bg-\\[var\\(--color-breathing-accent-strong\\)\\]"
---

<objective>
Wave 2 / Plan 06: Migrate the single highest-stakes file — `src/components/SessionControls.tsx` lines 52 and 69 — the Start/Stop primary action button (Group P-ACCENT-FG from PATTERNS.md §4). This is the only consumer of the D-01 `--color-breathing-on-accent` token created in plan 01. It is also the canonical proof of the phase contract: when the user swaps theme, the most visible interactive element rebinds.

Purpose: Complete the migration of the primary action button — the largest, most frequently-touched, highest-visibility chrome surface in the app. Then perform a manual per-palette UAT checkpoint to verify the new on-accent foreground clears the THEME-05 ≥ 1.5 luminance floor visually (the contrast guard test extension from plan 01 already gates this automatically; the UAT is the perception-side confirmation per CONTEXT.md "Specifics" + VALIDATION.md "Manual-Only Verifications").

Output: 1 modified TSX file (2 banned-class lines = 2 sites × ~6 banned-class fragments each = ~12 fragment substitutions). 1 manual checkpoint with per-palette visual smoke screen.
</objective>

<execution_context>
@$HOME/.claude-personal-personal/get-shit-done/workflows/execute-plan.md
@$HOME/.claude-personal-personal/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/16.1-ui-token-migration/16.1-CONTEXT.md
@.planning/phases/16.1-ui-token-migration/16.1-PATTERNS.md
@.planning/phases/16.1-ui-token-migration/16.1-RESEARCH.md
@.planning/phases/16.1-ui-token-migration/16.1-VALIDATION.md
@.planning/phases/16.1-ui-token-migration/16.1-01-SUMMARY.md
@.planning/phases/16.1-ui-token-migration/16.1-02-SUMMARY.md
@.planning/phases/16.1-ui-token-migration/16.1-03-SUMMARY.md
@.planning/phases/16.1-ui-token-migration/16.1-04-SUMMARY.md
@.planning/phases/16.1-ui-token-migration/16.1-05-SUMMARY.md
@src/components/SessionControls.tsx
@src/styles/theme.css

<interfaces>
PATTERNS.md §4 Group P-ACCENT-FG locked recipe (RESEARCH §"Example 1" + PATTERNS.md §4):

Both SessionControls.tsx:52 (legacy single-button `mt-6 w-full` layout) and SessionControls.tsx:69 (flex-1 inline-mute layout) have a near-identical className.

- BEFORE (line 52, full string):
  `mt-6 min-h-11 w-full rounded-full bg-teal-700 px-6 py-4 text-lg font-semibold text-white shadow-lg shadow-teal-900/20 transition hover:bg-teal-800 active:bg-teal-900 motion-reduce:transition-none focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-breathing-accent focus-visible:ring-offset-2`

- BEFORE (line 69, full string — differs from 52 only in `min-h-11 flex-1 rounded-full` vs `mt-6 min-h-11 w-full rounded-full`):
  `min-h-11 flex-1 rounded-full bg-teal-700 px-6 py-4 text-lg font-semibold text-white shadow-lg shadow-teal-900/20 transition hover:bg-teal-800 active:bg-teal-900 motion-reduce:transition-none focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-breathing-accent focus-visible:ring-offset-2`

- AFTER (line 52):
  `mt-6 min-h-11 w-full rounded-full bg-[var(--color-breathing-accent-strong)] px-6 py-4 text-lg font-semibold text-[var(--color-breathing-on-accent)] shadow-lg shadow-teal-900/20 transition hover:bg-[var(--color-breathing-accent)] active:bg-[var(--color-breathing-accent-strong)] motion-reduce:transition-none focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-breathing-accent focus-visible:ring-offset-2`

- AFTER (line 69):
  `min-h-11 flex-1 rounded-full bg-[var(--color-breathing-accent-strong)] px-6 py-4 text-lg font-semibold text-[var(--color-breathing-on-accent)] shadow-lg shadow-teal-900/20 transition hover:bg-[var(--color-breathing-accent)] active:bg-[var(--color-breathing-accent-strong)] motion-reduce:transition-none focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-breathing-accent focus-visible:ring-offset-2`

Per-fragment substitution rule:
- `bg-teal-700`           -> `bg-[var(--color-breathing-accent-strong)]`        (primary fill — high-emphasis tier)
- `text-white`            -> `text-[var(--color-breathing-on-accent)]`          (D-01 foreground on accent — purpose-built token)
- `hover:bg-teal-800`     -> `hover:bg-[var(--color-breathing-accent)]`         (per PATTERNS.md §4 Group P-ACCENT-FG note 3: slight visual lighten on hover; flat 7-token surface has no hover-then-strong tier; accepted per RESEARCH §"Code Examples" Example 1 notes)
- `active:bg-teal-900`    -> `active:bg-[var(--color-breathing-accent-strong)]` (same as base — hover and active visually collapse; the press-state cue is then carried by the `transition` alone)
- `shadow-teal-900/20`    -> PRESERVE (NOT in banned regex list; same carry-forward as SessionReadout:29,64 `shadow-teal-900/5` and SettingsStepper:36 `shadow-teal-900/5` — out of 16.1 scope per CONTEXT.md "Out of scope (explicit)")
- `focus-visible:ring-breathing-accent` -> PRESERVE verbatim (AP-03; Tailwind v4 auto-prefixed utility, already token-aware)

Justification for `shadow-teal-900/20` preservation:
- RESEARCH §"Example 1" notes 3 raise this as Assumption A3 (unverified) — "If Tailwind v4 rejects shadow-[rgb(from_var(--color-breathing-accent-strong)_r_g_b_/_0.20)], fallback is shadow-lg alone (drop the colored tint) or inline style".
- PATTERNS.md §4 Group P-ACCENT-FG note 2 confirms this is RESEARCH-flagged unverified.
- Banned regex list does NOT include the `shadow-*-N/N` pattern; only `bg-*`, `text-*`, `border-*` colored utilities.
- Therefore: per CONTEXT.md "Out of scope (explicit)" listing palette retuning and shadow as out of 16.1 scope, the `shadow-teal-900/20` stays verbatim. Document this in the SUMMARY as a v1.x carry-forward like the `shadow-breathing-card` Phase 16 D-02 carry-forward.

D-01 token consumer mapping (the SessionControls migration is THE consumer that motivates D-01):
- `--color-breathing-on-accent` defined in plan 01 — 5 palette values:
  - Light: `#ffffff` (white on #475569 slate-700)
  - Dark:  `#0f172a` (dark slate-900 on #5eead4 pale-teal — critical Pitfall-2 mitigation)
  - Moss:  `#ffffff` (white on #115e59 deep teal)
  - Slate: `#ffffff` (white on #3730a3 indigo-700)
  - Dusk:  `#1e0550` (deep violet on #f59e0b amber — dark-on-warm)

This is the ONLY codebase consumer of `--color-breathing-on-accent` (plus body::selection in plan 07 — `src/index.css` D-03). If a future destructive-red migration in plan 07 also consumes `--color-breathing-on-accent`, that's a second consumer; this plan owns the first.

Existing tests (DO NOT edit):
- src/components/SessionControls.test.tsx (asserts Start/Stop button rendering, disabled state, focus ring; no color assertions)
- src/app/App.session.test.tsx (asserts session start/stop flow + button enable/disable; no color assertions)

Current banned-class occurrences (verified grep 2026-05-13):
- SessionControls.tsx: lines 52 + 69. Each line has the same 5 banned fragments: `bg-teal-700`, `text-white`, `hover:bg-teal-800`, `active:bg-teal-900`, and (implicitly via `\bbg-teal-` regex) `shadow-teal-900/20` — wait, the regex `\bbg-teal-[0-9]` matches `bg-teal-` followed by digit; `shadow-teal-900/20` is `shadow-teal-`, NOT `bg-teal-` / `text-teal-` / `border-teal-`. So `shadow-teal-900/20` is NOT in the banned regex. Confirmed by re-reading CONTEXT.md D-04 — the 10 patterns are explicitly `text|bg|border-(slate|teal)-N` plus `text|bg-(white|black)`. Shadow is not in scope.
- Per line, the 4 banned fragments matched by the regex are: `bg-teal-700`, `hover:bg-teal-800`, `active:bg-teal-900`, `text-white`. Total = 4 × 2 lines = 8 banned-fragment occurrences (counted differently by the line-level grep which counts each line once if any match exists; but the regex-level audit catches 4 distinct patterns per line).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate SessionControls.tsx lines 52 + 69 to D-01 on-accent + accent-strong tokens</name>
  <files>src/components/SessionControls.tsx</files>
  <read_first>
    - src/components/SessionControls.tsx (full file — 83 lines; the migration surface is concentrated on lines 52 + 69, both Start/Stop button JSX)
    - src/styles/theme.css (verify the 5 `--color-breathing-on-accent` declarations are in place from plan 01 — sanity check before consuming them)
    - .planning/phases/16.1-ui-token-migration/16.1-PATTERNS.md §4 Group P-ACCENT-FG (the LOCKED recipe — line-by-line substitution table)
    - .planning/phases/16.1-ui-token-migration/16.1-RESEARCH.md §"Code Examples" Example 1 (justifies the hover -> accent (NOT accent-strong) lighten and the active -> accent-strong same-as-base collapse)
    - .planning/phases/16.1-ui-token-migration/16.1-RESEARCH.md §"Pitfall 2" (Dark-palette text-white-on-pale-teal failure mode that motivates D-01)
    - .planning/phases/16.1-ui-token-migration/16.1-01-SUMMARY.md (per-palette --color-breathing-on-accent hex values landed — verifies the token is present before consumption)
    - .planning/phases/16.1-ui-token-migration/16.1-CONTEXT.md D-01 + D-05 + D-06
  </read_first>
  <action>
    Edit ONLY lines 52 and 69 of `src/components/SessionControls.tsx`. Per CONTEXT.md D-05 (arbitrary-value form) + D-06 (className text only).

    For BOTH lines (the only difference between 52 and 69 is the layout prefix — `mt-6 min-h-11 w-full rounded-full` vs `min-h-11 flex-1 rounded-full`), apply this fragment-by-fragment substitution VERBATIM:

    | Before (banned fragment) | After (token form) |
    |--------------------------|---------------------|
    | `bg-teal-700`            | `bg-[var(--color-breathing-accent-strong)]` |
    | `text-white`             | `text-[var(--color-breathing-on-accent)]` |
    | `hover:bg-teal-800`      | `hover:bg-[var(--color-breathing-accent)]` |
    | `active:bg-teal-900`     | `active:bg-[var(--color-breathing-accent-strong)]` |
    | `shadow-teal-900/20`     | **PRESERVE — DO NOT TOUCH** (NOT in banned regex; CONTEXT.md "Out of scope" carry-forward) |
    | `focus-visible:ring-breathing-accent` | **PRESERVE — DO NOT TOUCH** (AP-03; auto-prefixed Tailwind v4 utility, already token-aware) |
    | every other class on each line | **PRESERVE VERBATIM** (`mt-6` / `flex-1`, `min-h-11`, `w-full`, `rounded-full`, `px-6 py-4`, `text-lg`, `font-semibold`, `shadow-lg`, `transition`, `motion-reduce:transition-none`, `focus-visible:outline-none`, `focus-visible:ring-2`, `focus-visible:ring-offset-2`) |

    Concretely, the post-migration line 52 should be byte-identical to the AFTER (line 52) string in the `<interfaces>` block. Same for line 69 against AFTER (line 69). Verify by `grep -F` matching the full expected string.

    After edit:
    - `npx tsc --noEmit` — exits 0.
    - `npm run lint` — exits 0.
    - `npm run build` — exits 0 (Tailwind v4 codegen accepts all arbitrary-value forms; the `var(--color-breathing-on-accent)` reference resolves because the token is declared in `theme.css` from plan 01).
    - `npm test -- --run src/components/SessionControls.test.tsx src/app/App.session.test.tsx` — exits 0.
    - Full chain `npm test -- --run` — exits 0 (the `theme.no-hardcoded-classes.test.ts` guard is still `.skip`'d at the describe level per plan 01; plan 07 flips it to green).

    Commit as `feat(16.1-06): migrate SessionControls primary button to --color-breathing-on-accent + accent-strong tokens (consumes D-01)`.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run lint && grep -cE '\b(text|bg|border)-(slate|teal)-[0-9]|\b(text|bg)-(white|black)\b' src/components/SessionControls.tsx && grep -c 'shadow-teal-900/20' src/components/SessionControls.tsx && grep -c 'var(--color-breathing-on-accent)' src/components/SessionControls.tsx && grep -c 'var(--color-breathing-accent-strong)' src/components/SessionControls.tsx && grep -c 'focus-visible:ring-breathing-accent' src/components/SessionControls.tsx && npm test -- --run src/components/SessionControls.test.tsx src/app/App.session.test.tsx && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE '\b(text|bg|border)-(slate|teal)-[0-9]|\b(text|bg)-(white|black)\b' src/components/SessionControls.tsx` returns 0 (both lines 52 and 69 cleared of banned classes — 4 banned fragments per line × 2 lines = 8 fragment substitutions verified).
    - `grep -c 'shadow-teal-900/20' src/components/SessionControls.tsx` returns 2 (CONTEXT.md Out-of-Scope carry-forward preserved on BOTH layout variants — same pattern as SessionReadout's `shadow-teal-900/5` preservation; document in SUMMARY as a v1.x carry-forward).
    - `grep -c 'var(--color-breathing-on-accent)' src/components/SessionControls.tsx` returns 2 (line 52 + line 69 — first codebase consumer of D-01 token).
    - `grep -c 'var(--color-breathing-accent-strong)' src/components/SessionControls.tsx` returns 4 (bg = 2 sites + active:bg = 2 sites = 4 occurrences).
    - `grep -c 'var(--color-breathing-accent)\b' src/components/SessionControls.tsx | xargs -I{} sh -c 'test {} -ge 2'` — hover:bg-[var(--color-breathing-accent)] on both lines.
    - `grep -c 'focus-visible:ring-breathing-accent' src/components/SessionControls.tsx` returns 2 (AP-03 sentinel — focus ring preserved on both Start/Stop button variants).
    - `npm test -- --run src/components/SessionControls.test.tsx src/app/App.session.test.tsx` exits 0.
    - `npx tsc --noEmit && npm run lint && npm run build` exits 0.
  </acceptance_criteria>
  <done>
    SessionControls primary action button is fully tokenized on both layout variants (line 52 `mt-6 w-full` and line 69 `flex-1`). The D-01 `--color-breathing-on-accent` token has its first codebase consumer; the foreground text on accent-strong now rebinds per palette (white on light/moss/slate, dark slate-900 on dark, deep violet on dusk). The `shadow-teal-900/20` colored shadow is preserved as a v1.x carry-forward (same out-of-scope treatment as the `shadow-breathing-card` Phase 16 D-02 carry-forward).
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Per-palette UAT — primary action button contrast across all 5 palettes</name>
  <what-built>
    Task 1 migrated `SessionControls.tsx:52` and `:69` to consume the D-01 `--color-breathing-on-accent` token. The contrast guard test extension (plan 01) already asserts `contrastRatio(accent-strong, on-accent) >= 1.5` per palette via WCAG luminance math — that's the automated gate. This checkpoint is the perception-side confirmation per CONTEXT.md "Specifics" + VALIDATION.md "Manual-Only Verifications" row 2: visually confirm the Start/Stop button text is LEGIBLE on every palette.

    Why human-only: WCAG luminance ratio ≥ 1.5 is the floor; visual legibility is a stricter ceiling. The automated test catches mathematical regressions; this UAT catches perceptual surprises (e.g., a high-contrast pair that still looks visually muddy due to hue interaction).
  </what-built>
  <how-to-verify>
    1. Stop any running dev server. Start a fresh one: `npm run dev`. Note the local URL (typically `http://localhost:5173/`).
    2. Open the app in Chrome (preferred — Tailwind v4 alpha-modifier path was confirmed in plans 04 + 05; Chrome 122+ for `::backdrop` inheritance).
    3. Open SettingsDialog via the gear icon (top-left).
    4. For EACH of the 5 palettes (Light, Dark, Moss, Slate, Dusk), select it in the ThemePicker, close the dialog, and verify the Start/Stop primary button:
       - **Light palette** (`accent-strong = #475569` slate-700, `on-accent = #ffffff` white): button reads `Start` (or `Stop` if session active). White text on dark-slate background must be clearly legible. Expected contrast: very high (>= 7).
       - **Dark palette** (`accent-strong = #5eead4` pale-teal, `on-accent = #0f172a` slate-900): dark text on light pale-teal. CRITICAL — this is the Pitfall-2 mitigation site. Text must read clearly as dark slate; if it appears muddy or low-contrast, the D-01 hex needs re-tuning (escalate to a follow-up plan; this is the high-stakes site).
       - **Moss palette** (`accent-strong = #115e59` deep teal, `on-accent = #ffffff` white): white text on deep teal. Should be very high contrast.
       - **Slate palette** (`accent-strong = #3730a3` indigo-700, `on-accent = #ffffff` white): white text on indigo. Should be very high contrast.
       - **Dusk palette** (`accent-strong = #f59e0b` amber, `on-accent = #1e0550` deep violet): deep violet text on amber. Cross-spectrum pair — verify legibility; if amber-on-violet looks fuzzy at the body-text size, escalate.
    5. For each palette, also briefly verify:
       - Hover state (mouse over the button): background shifts to `--color-breathing-accent` (medium emphasis tier, lighter than accent-strong); the slight visual lighten is intentional per PATTERNS.md §4 Group P-ACCENT-FG note (the flat 7-token surface has no hover-then-strong tier).
       - Active state (click and hold): background collapses back to `--color-breathing-accent-strong` (same as base). This is intentional — the `transition` carries the press-state cue.
       - Focus ring (Tab to the button): `focus-visible:ring-breathing-accent` ring appears in the palette's accent color.
    6. Confirm the rest of the chrome already rebinds (sanity smoke for Wave 1 plans 02-05): dialog shells, pickers, anchors, stepper, MuteToggle, SessionReadout time chip, BreathingShape phase label — all visibly rebind per palette.
    7. Stop the dev server (Ctrl-C).
  </how-to-verify>
  <resume-signal>
    Type "approved" if all 5 palettes' Start/Stop button text is visually legible AND the rest of the migrated chrome (plans 02-05) visibly rebinds. Plan 07 will then close the phase (index.css migration, REQUIREMENTS flip, STATE update, guard test flip to green, optional destructive `text-white` migration, phase SUMMARY).

    Type "rejected: [palette name] — [observation]" if any palette fails the contrast/legibility check. The most likely failure mode is Dark or Dusk (the dark-on-light pairs). Suggested remediation paths:
      (a) Re-tune the failing palette's `--color-breathing-on-accent` hex in `theme.css` (planner's discretion per CONTEXT.md "Claude's Discretion"; the contrast test re-runs automatically and must stay >= 1.5).
      (b) Re-tune the failing palette's `--color-breathing-accent-strong` hex (broader change — affects every consumer that uses accent-strong as bg; consult plans 02-05 SUMMARYs first).

    Document the rejection + remediation in a follow-on task within this plan rather than spinning a new plan — keep the per-palette UAT as a single atomic checkpoint.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| no new boundaries | className substitution only; no new JS, no new event handlers, no new auth/data surface. The D-01 token is consumed via CSS cascade — no runtime computation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16.1-06-01 | Spoofing (a11y) | Primary action button text legibility | mitigate | Two gates: (a) automated — `theme.contrast.test.ts` (plan 01 extension) asserts `contrastRatio(accent-strong, on-accent) >= 1.5` per palette on every commit; (b) manual — Task 2 per-palette UAT confirms perceptual legibility. Both gates must pass for the plan to close. |
| T-16.1-06-02 | Tampering | shadow-teal-900/20 carry-forward | accept | Out of 16.1 scope per CONTEXT.md "Out of scope"; documented in SUMMARY as v1.x carry-forward; identical treatment to the existing `shadow-breathing-card` Phase 16 D-02 carry-forward and the `shadow-teal-900/5` on SessionReadout / SettingsStepper. No security implication. |

Pure className substitution + manual UAT plan. No injection / XSS / data / auth surface introduced.
</threat_model>

<verification>
After Task 1 completes (before Task 2 checkpoint):

```bash
npx tsc --noEmit && npm run lint && npm run build && npm test -- --run
```

Plan-level grep sentinels:

```bash
# SessionControls fully token-migrated:
grep -cE '\b(text|bg|border)-(slate|teal)-[0-9]|\b(text|bg)-(white|black)\b' src/components/SessionControls.tsx
# Expected: 0.

# D-01 token consumed twice (both layout variants):
grep -c 'var(--color-breathing-on-accent)' src/components/SessionControls.tsx
# Expected: 2.

# accent-strong consumed 4 times (bg=2 + active:bg=2):
grep -c 'var(--color-breathing-accent-strong)' src/components/SessionControls.tsx
# Expected: 4.

# Carry-forward sentinel (shadow-teal-900/20 NOT in banned regex; preserved):
grep -c 'shadow-teal-900/20' src/components/SessionControls.tsx
# Expected: 2.

# Focus ring preserved (AP-03):
grep -c 'focus-visible:ring-breathing-accent' src/components/SessionControls.tsx
# Expected: 2.
```

After Task 2 approval, phase-level grep sentinel BEFORE plan 07:

```bash
# Production .tsx files — the entire migration surface — should now have ZERO banned classes
# EXCEPT the 2 destructive text-white pairs on EndSessionDialog:81 + ResetStatsDialog:76 (plan 07 decision).
grep -rEn --include='*.tsx' --exclude='*.test.tsx' --exclude='*.stories.tsx' \
  '\b(text|bg|border)-(slate|teal)-[0-9]|\b(text|bg)-(white|black)\b' src/
# Expected after plan 06: 2 hits (both text-white on red destructive buttons).
```
</verification>

<success_criteria>
1. SessionControls.tsx contains ZERO banned-regex matches on both lines 52 and 69.
2. `--color-breathing-on-accent` is consumed 2× (once per layout variant) — this is the first codebase consumer of D-01.
3. `--color-breathing-accent-strong` is consumed 4× (bg=2 + active:bg=2).
4. `shadow-teal-900/20` is preserved on both lines (v1.x carry-forward documented).
5. `focus-visible:ring-breathing-accent` is preserved on both lines (AP-03).
6. SessionControls.test.tsx and App.session.test.tsx pass without modification.
7. `npx tsc --noEmit && npm run lint && npm run build && npm test -- --run` exits 0.
8. Task 2 per-palette UAT approved: all 5 palettes show legible Start/Stop button text against the accent-strong background, AND the rest of the migrated chrome (plans 02-05) visibly rebinds per palette.
9. After this plan ships, the entire production migration surface is theme-bound except for the 2 destructive `text-white` lines on End/Reset (plan 07's decision).
</success_criteria>

<output>
After completion AND human approval, create `.planning/phases/16.1-ui-token-migration/16.1-06-SUMMARY.md` summarizing:
- 1 file migrated, 2 lines (52 + 69), 8 banned-fragment substitutions across both lines, 1 carry-forward (`shadow-teal-900/20` ×2)
- D-01 token: first consumer landed; per-palette UAT confirms legibility on all 5 palettes
- The per-palette UAT result (per-palette legibility verdict — table of 5 palettes with "approved" or remediation notes if any)
- The phase-level grep state heading into plan 07: 2 remaining banned-class hits (text-white on red destructive End/Reset buttons)
- Note for plan 07: those 2 hits MAY be migrated to text-[var(--color-breathing-on-accent)] for a complete grep-zero result, OR the Wave 0 D-04 walker may be carve-out (option (b) from plan 01 Task 3 was rejected; option (a) is the plan-07 path).
</output>
