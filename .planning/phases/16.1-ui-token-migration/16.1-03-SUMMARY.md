---
phase: 16.1
plan: 03
status: complete
files_modified:
  - src/components/ThemePicker.tsx
  - src/components/TimbrePicker.tsx
  - src/components/VariantPicker.tsx
  - src/components/LanguagePicker.tsx
commits:
  - eb37659 feat(16.1-03) migrate ThemePicker unselected branch + 3 stub picker labels — closes WR-01
---

# Plan 16.1-03 SUMMARY — Picker Family Migration

## What Shipped

4 picker files migrated. 8 banned-class occurrences cleared. Closes WR-01 / AP-01 — the "Phase 16 original sin" (unselected theme buttons stayed teal-on-white across Dark/Moss/Slate/Dusk swaps).

- **ThemePicker.tsx**: section label `text-slate-900` -> `text-[var(--color-breathing-accent-strong)]`; `unselectedClasses` const (line 33) fully retokenized — border/bg/text/hover/active. `selectedClasses` (line 32, Reference site A) preserved byte-identical.
- **TimbrePicker.tsx + VariantPicker.tsx + LanguagePicker.tsx**: section label `text-slate-900` + ternary active branch `text-slate-700` -> `text-[var(--color-breathing-accent-strong)]`. Disabled branch `text-[var(--color-breathing-muted)]` preserved.

## Sentinels (verified)

- `grep -cE '\b(text|bg|border)-(slate|teal)-[0-9]|\bbg-white\b'` on 4 pickers → 0
- ThemePicker `selectedClasses` (Reference site A) preserved byte-identical
- AP-01 closed: unselected theme buttons now rebind on palette swap

## Gates

`tsc + eslint + build + test` exit 0. Picker tests (ThemePicker, TimbrePicker, VariantPicker, LanguagePicker) pass without edit (D-06).
