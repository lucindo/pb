---
phase: 16.1
slug: ui-token-migration
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-05-13
---

# Phase 16.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | vitest 4.1.5 (verified in package.json per RESEARCH.md) |
| **Config file** | `vitest.config.ts` (Phase 16 baseline) |
| **Quick run command** | `npm test -- --run` |
| **Full suite command** | `npm run tsc && npm run lint && npm run build && npm test -- --run` |
| **Estimated runtime** | ~30s test, ~60s full chain |

---

## Sampling Rate

- **After every task commit:** Run `npm test -- --run` (quick)
- **After every plan wave:** Run full chain (`tsc && lint && build && test`) — phase contract requires zero-exit at every commit boundary (success criterion #4)
- **Before `/gsd-verify-work`:** Full chain green
- **Max feedback latency:** ~30s (quick), ~60s (full)

---

## Per-Phase Verification Map

Planner populates this table per task. Skeleton:

| Task ID | Plan | Wave | Requirement | Test Type | Automated Command | Status |
|---------|------|------|-------------|-----------|-------------------|--------|
| 16.1-XX-YY | XX | N | THEME-UI-01 | grep / vitest | `{command}` | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [ ] Tailwind v4 alpha-modifier probe (per CONTEXT.md D-02) — render a component with `bg-[var(--color-breathing-surface)]/70`, assert computed style parses to expected rgba
- [ ] No new framework install required (vitest already in place)

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Visual rebind of Start/Stop, dialogs, pickers across Dark/Moss/Slate/Dusk palettes | Success criterion #1 | Automated grep proves token usage; only human eye proves perceived rebind | Run app, switch palette via ThemePicker, screenshot each of 5 themes covering Start/Stop, EndSessionDialog, ResetStatsDialog, LearnDialog, SettingsDialog, SettingsStepper, ThemePicker unselected buttons |
| `--color-breathing-on-accent` contrast on primary action button (per CONTEXT.md D-01) | Success criterion #3 | THEME-05 contrast test catches automated regressions; per-palette UAT confirms perceived legibility | In each of 5 palettes, verify primary action button text is legible against accent-strong background |

---

## Automated Guards (extending Phase 16 baseline)

| Guard | File | What it asserts |
|-------|------|-----------------|
| `theme.contrast.test.ts` (existing) | `src/themes/__tests__/theme.contrast.test.ts` | Each palette's accent vs bg luminance ratio ≥ 1.5. Extend to cover `accent-strong` vs new `on-accent` token. |
| `theme.no-hardcoded-classes.test.ts` (NEW per CONTEXT.md D-04) | `src/themes/__tests__/theme.no-hardcoded-classes.test.ts` | Greps `src/**/*.tsx` for the 10 banned regex patterns; excludes `*.test.tsx`, `*.stories.tsx`, `src/themes/**`. Fails CI on regression. |
| Accessibility regex assertions (existing, 11+ per RESEARCH.md) | (existing test files) | Focus-visible rings, hit-area floors, aria-* preserved across migration (success criterion #5). |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers alpha-modifier probe + new `on-accent` token wiring
- [ ] No watch-mode flags
- [ ] Feedback latency < 60s for full chain
- [ ] `nyquist_compliant: true` set in frontmatter after planner fills per-task map

**Approval:** pending
