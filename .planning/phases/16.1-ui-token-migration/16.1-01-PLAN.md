---
phase: 16.1
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/REQUIREMENTS.md
  - src/styles/theme.css
  - src/styles/theme.contrast.test.ts
  - src/styles/theme.no-hardcoded-classes.test.ts
  - src/styles/theme.alpha-probe.test.ts
autonomous: true
requirements: [THEME-UI-01]
tags: [theme, css-tokens, vitest-guard, tailwind-v4]

must_haves:
  truths:
    - "THEME-UI-01 exists in REQUIREMENTS.md tied to Phase 16.1 goal"
    - "Tailwind v4 parses bg-[var(--color-breathing-surface)]/70 or the inline-style fallback is locked in writing"
    - "--color-breathing-on-accent token defined in all 5 palette blocks of theme.css"
    - "theme.contrast.test.ts asserts accent-strong vs on-accent >= 1.5 for all 5 concrete palettes"
    - "theme.no-hardcoded-classes.test.ts exists and FAILS RED against today's codebase (proves the guard catches the 10 banned regex patterns)"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "THEME-UI-01 row added"
      contains: "THEME-UI-01"
    - path: "src/styles/theme.css"
      provides: "8th chrome token --color-breathing-on-accent per palette"
      contains: "--color-breathing-on-accent"
    - path: "src/styles/theme.contrast.test.ts"
      provides: "6th contrast iteration: accent-strong vs on-accent per palette"
      contains: "on-accent"
    - path: "src/styles/theme.no-hardcoded-classes.test.ts"
      provides: "Vitest guard greps src/**/*.tsx for 10 banned regex patterns"
      contains: "BANNED_PATTERNS"
    - path: "src/styles/theme.alpha-probe.test.ts"
      provides: "Wave 0 D-02 probe — validates Tailwind v4 parses bg-[var(--color-breathing-surface)]/70 OR documents the fallback decision"
      contains: "rgba"
  key_links:
    - from: "src/styles/theme.no-hardcoded-classes.test.ts"
      to: "src/**/*.tsx"
      via: "readdirSync + readFileSync + regex test"
      pattern: "readFileSync"
    - from: "src/styles/theme.contrast.test.ts (extended)"
      to: "--color-breathing-on-accent / --color-breathing-accent-strong"
      via: "getComputedStyle on documentElement after setting dataset.theme"
      pattern: "accent-strong"
    - from: "src/styles/theme.css"
      to: "5 [data-theme] palette blocks"
      via: "CSS custom property declaration"
      pattern: "--color-breathing-on-accent:"
---

<objective>
Wave 0 preflight: establish every piece of token-system infrastructure that the Wave 1+ migration work depends on, in ONE plan, before any consumer-side `.tsx` file is touched.

Purpose: Following D-01..D-06 from `16.1-CONTEXT.md`, this plan (a) registers the phase requirement `THEME-UI-01` in `REQUIREMENTS.md`, (b) probes whether Tailwind v4 actually parses `bg-[var(--color-breathing-surface)]/70` so Wave 1 alpha-modifier work can commit to one strategy (CONTEXT.md D-02), (c) inserts the 8th chrome token `--color-breathing-on-accent` into all 5 palette blocks in `theme.css` with contrast-tuned hexes (CONTEXT.md D-01), (d) extends the existing 5-iteration `theme.contrast.test.ts` to add a 6th iteration asserting `accent-strong` vs `on-accent` >= 1.5 per palette, and (e) creates `theme.no-hardcoded-classes.test.ts` as the long-term regression guard (CONTEXT.md D-04). The guard test is expected to fail RED on first commit — it proves the 10 banned regexes actually match the current codebase, which is the entire premise of Phase 16.1. The guard flips to green only at Wave 3 phase close, after every migration plan has merged.

Output:
- `.planning/REQUIREMENTS.md` updated with `THEME-UI-01` row.
- `src/styles/theme.css` with 5 new `--color-breathing-on-accent` declarations.
- `src/styles/theme.contrast.test.ts` with a 6th `it()` block inside the same `describe.each(CONCRETE_THEMES)` covering `accent-strong vs on-accent`.
- `src/styles/theme.no-hardcoded-classes.test.ts` — NEW.
- `src/styles/theme.alpha-probe.test.ts` — NEW, validates D-02 alpha-modifier parsing OR documents fallback decision.
</objective>

<execution_context>
@$HOME/.claude-personal-personal/get-shit-done/workflows/execute-plan.md
@$HOME/.claude-personal-personal/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/16.1-ui-token-migration/16.1-CONTEXT.md
@.planning/phases/16.1-ui-token-migration/16.1-RESEARCH.md
@.planning/phases/16.1-ui-token-migration/16.1-PATTERNS.md
@.planning/phases/16.1-ui-token-migration/16.1-VALIDATION.md
@.planning/phases/16-themes/16-SUMMARY.md
@.planning/phases/16-themes/16-VERIFICATION.md
@src/styles/theme.css
@src/styles/theme.contrast.test.ts
@src/domain/settings.ts

<interfaces>
Key identifiers / contracts the executor MUST use verbatim (extracted from codebase + PATTERNS.md):

From src/styles/theme.css (existing chrome tokens — DO NOT touch):
- @theme { ... } block — lines 1-35 (Light palette baseline)
- [data-theme='dark']:root { ... } — lines 41-58
- [data-theme='moss']:root { ... } — lines 66-83
- [data-theme='slate']:root { ... } — lines 89-106
- [data-theme='dusk']:root { ... } — lines 112-129
- Per palette, the existing token strip is: bg, bg-soft, bg-edge, surface, accent, accent-strong, muted. INSERT --color-breathing-on-accent immediately AFTER --color-breathing-muted in each block (per PATTERNS.md §2 insertion rule 2).

Existing accent-strong values (DO NOT modify — these are the LHS of the new contrast assertion):
- Light: --color-breathing-accent-strong: #475569
- Dark:  --color-breathing-accent-strong: #5eead4
- Moss:  --color-breathing-accent-strong: #115e59
- Slate: --color-breathing-accent-strong: #3730a3
- Dusk:  --color-breathing-accent-strong: #f59e0b

D-01 on-accent values (suggested seeds per PATTERNS.md §2 rule 4 — refine if contrast guard fails):
- Light: #ffffff (white on #475569 slate-700 -> high contrast)
- Dark:  #0f172a (slate-900 on #5eead4 pale-teal -> dark-on-light)
- Moss:  #ffffff (white on #115e59 deep teal -> high contrast)
- Slate: #ffffff (white on #3730a3 indigo-700 -> high contrast)
- Dusk:  #1e0550 (deep violet on #f59e0b amber -> dark-on-warm)

From src/styles/theme.contrast.test.ts (helper functions already exported in module scope — reuse, do not duplicate):
- readToken(token: string): [number, number, number]
- contrastRatio(rgb1, rgb2): number
- CONCRETE_THEMES: readonly Exclude<ThemeId,'system'>[]
- describe.each(CONCRETE_THEMES)('theme=%s', (themeId) => { ... })

From src/domain/settings.ts:
- THEME_OPTIONS: readonly ThemeId[]
- ThemeId = 'light' | 'dark' | 'moss' | 'slate' | 'dusk' | 'system'

D-04 banned regex patterns (authoritative — copy verbatim from CONTEXT.md D-04):
- /\btext-slate-[0-9]/
- /\bbg-slate-[0-9]/
- /\bborder-slate-[0-9]/
- /\btext-teal-[0-9]/
- /\bbg-teal-[0-9]/
- /\bborder-teal-[0-9]/
- /\btext-white\b/
- /\bbg-white\b/
- /\btext-black\b/
- /\bbg-black\b/

D-04 file inclusion rules:
- Walk src/ recursively, collect *.tsx files.
- EXCLUDE *.test.tsx, *.stories.tsx, and any path containing src/themes/ (the recommended PATTERNS.md walker also skips src/styles/ implicitly since no .tsx lives there — confirm with readdirSync).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add THEME-UI-01 to REQUIREMENTS.md + Wave 0 D-02 alpha-modifier probe</name>
  <files>.planning/REQUIREMENTS.md, src/styles/theme.alpha-probe.test.ts</files>
  <read_first>
    - .planning/REQUIREMENTS.md (current state — Phase 16.1 section is absent; THEME-UI-01 row not yet listed in traceability table)
    - .planning/phases/16.1-ui-token-migration/16.1-CONTEXT.md (D-02 probe specification — "Specifics" section)
    - .planning/phases/16.1-ui-token-migration/16.1-PATTERNS.md (no probe template exists yet — this task creates the template)
    - .planning/phases/16.1-ui-token-migration/16.1-RESEARCH.md §"Pitfall 3" (alpha-modifier risk + rgb(from ...) fallback syntax)
    - src/styles/theme.contrast.test.ts (structural template for the new probe test — same imports, same beforeAll injection pattern, same /// <reference types="node" /> directive)
  </read_first>
  <action>
    (1) Edit `.planning/REQUIREMENTS.md`:
      (a) Under the v1.1 requirements section, insert a NEW subsection titled `### Theme UI Token Migration (THEME-UI-01)` AFTER the existing `### Themes (CUST-01)` subsection. Body: a single `- [ ] **THEME-UI-01**:` bullet matching the wording from `16.1-RESEARCH.md §"Phase Requirements"`:

        > All user-facing TSX components reference `--color-breathing-*` tokens (or descendants thereof) for theme-shifting surfaces; no production `.tsx` references hardcoded `text-slate-*` / `bg-slate-*` / `border-slate-*` / `text-teal-*` / `bg-teal-*` / `border-teal-*` / `text-white` / `bg-white` / `text-black` / `bg-black` literal Tailwind classes (verified via grep on `src/**/*.tsx` excluding `*.test.tsx`). Destructive-danger styles (`bg-red-*`, `text-white` on destructive primary) are explicitly excluded — red conveys danger semantics and is intentionally palette-independent.

      (b) Append a row to the `## Traceability` table immediately after the `THEME-05` row:

        ```
        | THEME-UI-01 | Phase 16.1 | Pending |
        ```

      (c) Update the coverage counts at the bottom of the file: "v1.1 requirements: 29 total" -> "30 total"; "Mapped to phases: 29/29" -> "30/30".

    (2) Create `src/styles/theme.alpha-probe.test.ts`. Structural mirror of `src/styles/theme.contrast.test.ts`:
      - Triple-slash `/// <reference types="node" />` directive.
      - Imports from `vitest` (`describe`, `it`, `expect`, `beforeAll`, `afterAll`).
      - `readFileSync` + `resolve` from `node:fs` / `node:path`.
      - `beforeAll` injects `theme.css` via the same `@theme {` -> `:root {` rewrite trick (lines 99-107 of theme.contrast.test.ts).
      - In the test body, create a real DOM element with `document.createElement('div')`, set its `className` to `'bg-[var(--color-breathing-surface)]/70'` AND a fallback `style={ backgroundColor: 'rgb(from var(--color-breathing-surface) r g b / 0.7)' }`-equivalent inline style on a sibling div for comparison.
      - jsdom CANNOT parse Tailwind arbitrary-value classes (Tailwind v4 codegen runs at build time, not in jsdom). The probe approach must therefore be:
        - (i) Read `src/styles/theme.css` via readFileSync.
        - (ii) Assert that the CSS custom property `--color-breathing-surface` resolves to a valid hex on documentElement (sanity check that the cascade works).
        - (iii) Document the D-02 strategy decision INLINE in the test file as a `describe.skip` or top-of-file JSDoc block: "D-02 outcome: Tailwind v4 arbitrary-value alpha-modifier support cannot be verified inside jsdom because Tailwind codegen runs at vite build time. The Wave 1 alpha-site migration plans (04, 05) MUST execute a real browser smoke check before committing the migration: run `npm run dev`, navigate to a page rendering `<div className="bg-[var(--color-breathing-surface)]/70">probe</div>`, open DevTools, and read `getComputedStyle(div).backgroundColor`. Expected: `rgba(255, 255, 255, 0.7)` (Light) or palette-equivalent. If the computed style is `transparent` or `rgba(0,0,0,0)`, fall back to inline `style={{ backgroundColor: 'rgb(from var(--color-breathing-surface) r g b / 0.7)' }}` per CONTEXT.md D-02 Path B and CSS-native `rgb(from ...)` syntax (Chrome 119+, Safari 16.4+, Firefox 113+ — supported in all current evergreen browsers per RESEARCH §"Pitfall 3")."
        - (iv) The actual `it()` block asserts a vite-side build of `bg-[var(--color-breathing-surface)]/70` would generate valid CSS: parse the resulting expected CSS form (`background-color: color-mix(in oklab, var(--color-breathing-surface) 70%, transparent)` — Tailwind v4's arbitrary-value /N path emits color-mix per their docs) as a string assertion only. If the executor cannot verify this static-string assertion, leave the test as a JSDoc-only `describe.skip` and proceed with the dev-server smoke check as a Wave 0 checkpoint deferred to plan 04 execution time.

      Pragmatic note: jsdom is not the right vehicle for this probe. The probe's PRIMARY value is forcing the decision (Path A vs Path B) to be written down in code BEFORE plans 04 and 05 try to migrate alpha sites. So the deliverable here is the `theme.alpha-probe.test.ts` file containing either (a) a passing assertion that resolves the strategy or (b) a `describe.skip` block whose JSDoc records the strategy decision and the dev-server smoke check that plan 04 will run.

      Conservative recommendation if uncertain: write the file as a passing test that simply asserts `getComputedStyle(document.documentElement).getPropertyValue('--color-breathing-surface')` returns a non-empty hex, and put the strategy decision in the top-of-file JSDoc + a comment trailer assigning `D-02 strategy: Path A (arbitrary-value /N)` as the default, with `Fallback to Path B (inline rgb(from ...))` documented as the rollback path if any of plans 04/05 surface a transparent render at execution time.
  </action>
  <verify>
    <automated>grep -c "THEME-UI-01" .planning/REQUIREMENTS.md | grep -v '^0$' && grep -q "30 total" .planning/REQUIREMENTS.md && grep -q "30/30" .planning/REQUIREMENTS.md && test -f src/styles/theme.alpha-probe.test.ts && npx vitest run src/styles/theme.alpha-probe.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "THEME-UI-01" .planning/REQUIREMENTS.md` returns >= 2 (one in the requirements subsection, one in the traceability table).
    - REQUIREMENTS.md coverage block reads `v1.1 requirements: 30 total` and `Mapped to phases: 30/30`.
    - `src/styles/theme.alpha-probe.test.ts` exists.
    - `npx vitest run src/styles/theme.alpha-probe.test.ts` exits 0 (either the assertion passes or the test is `describe.skip`'d with the strategy JSDoc).
    - The file contains the string "D-02" AND either "Path A" OR "Path B" in JSDoc/comments (records the strategy decision).
    - `grep -c "rgb(from" src/styles/theme.alpha-probe.test.ts` >= 1 (documents the fallback syntax inline as a paper trail).
  </acceptance_criteria>
  <done>
    THEME-UI-01 is locked into REQUIREMENTS.md with traceability to Phase 16.1. The alpha-modifier strategy decision is written down in `theme.alpha-probe.test.ts` so plans 04 and 05 can reference it without re-debating D-02. The probe test exits 0 in the Vitest suite.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Define --color-breathing-on-accent token in 5 palettes + extend contrast guard</name>
  <files>src/styles/theme.css, src/styles/theme.contrast.test.ts</files>
  <read_first>
    - src/styles/theme.css (existing structure — 5 sibling blocks; the 7-chrome-token strip at the top of each block is the insertion site)
    - src/styles/theme.contrast.test.ts (existing 5-iteration describe.each — extend with a 6th it() inside the same block)
    - .planning/phases/16.1-ui-token-migration/16.1-CONTEXT.md (D-01 specification + "Specifics" + "Claude's Discretion" on per-palette hex values)
    - .planning/phases/16.1-ui-token-migration/16.1-PATTERNS.md §2 (D-01 insertion rules 1-6 — position, naming, value-selection guard, contrast extension recipe)
    - .planning/phases/16-themes/16-SUMMARY.md (Phase 16 D-02 invariant: every --color-* token is themable; do not regress)
  </read_first>
  <behavior>
    RED test first (TDD): extend `theme.contrast.test.ts` BEFORE editing `theme.css`. The extension is a 6th `it()` block inside the existing `describe.each(CONCRETE_THEMES)`:

    - Test 1 (per palette x 5): `contrastRatio(accent-strong, on-accent) >= 1.5` — RED on first run because `--color-breathing-on-accent` token does not yet exist (readToken throws or returns ''). Five fresh RED assertions one per palette.

    Then make GREEN by adding the 5 token declarations to `theme.css`.

    - Test 2 (preserved): the existing `reduced-motion crossfade midpoint contrast ratio is >= 1.5` test continues passing for all 5 palettes (light 1.78, dark 4.85, moss 1.94, slate 2.56, dusk 1.87 — these ratios are recorded in 16-VERIFICATION.md as the post-Phase-16 baseline).
  </behavior>
  <action>
    (RED commit) Extend `src/styles/theme.contrast.test.ts`:
    - Inside the existing `describe.each(CONCRETE_THEMES)('theme=%s', (themeId) => { ... })` block (line 128), add a SECOND `it()` after the existing reduced-motion-crossfade test:

      ```typescript
      it('accent-strong vs on-accent contrast ratio is >= 1.5 (D-01)', () => {
        if (themeId === 'light') {
          delete document.documentElement.dataset.theme
        } else {
          document.documentElement.dataset.theme = themeId
        }
        const accentStrong = readToken('--color-breathing-accent-strong')
        const onAccent = readToken('--color-breathing-on-accent')
        const ratio = contrastRatio(accentStrong, onAccent)
        expect(ratio).toBeGreaterThanOrEqual(1.5)
      })
      ```

    - Reuse the existing module-scope helpers (`readToken`, `contrastRatio`, `parseColorToRgb`, `relativeLuminance`) — do NOT duplicate them. The `it()` block is a sibling of the existing reduced-motion test inside the same `describe.each` arrow function.
    - Run `npx vitest run src/styles/theme.contrast.test.ts` and confirm 5 NEW failing assertions (5 RED iterations on the new `on-accent` test, 5 still-passing on the old crossfade test). This commit is `test(16.1-01): add RED accent-strong vs on-accent contrast assertion`.

    (GREEN commit) Edit `src/styles/theme.css`. For each of the 5 palette blocks (lines 1, 41, 66, 89, 112), insert ONE new line immediately AFTER `--color-breathing-muted:` (and before the orb-gradient comment block). Verbatim insertion contents:

    - In `@theme {` block (after line 10 — i.e., the line containing `--color-breathing-muted: #94a3b8;`): `  --color-breathing-on-accent: #ffffff;` (per D-01 D-01 — white on slate-700 #475569; suggested seed from PATTERNS.md §2).
    - In `[data-theme='dark']:root {` block (after the line with `--color-breathing-muted: #64748b;`): `  --color-breathing-on-accent: #0f172a;` (dark slate-900 on pale-teal #5eead4 — critical Pitfall 2 mitigation; without this token the Dark primary button would render light-on-light).
    - In `[data-theme='moss']:root {` block (after `--color-breathing-muted: #64748b;`): `  --color-breathing-on-accent: #ffffff;` (white on deep-teal #115e59).
    - In `[data-theme='slate']:root {` block (after `--color-breathing-muted: #94a3b8;`): `  --color-breathing-on-accent: #ffffff;` (white on indigo-700 #3730a3).
    - In `[data-theme='dusk']:root {` block (after `--color-breathing-muted: #a78bfa;`): `  --color-breathing-on-accent: #1e0550;` (deep violet on amber #f59e0b — dark-on-warm; white-on-amber would fail Pitfall 2).

    - Indentation: 2 spaces matching existing tokens.
    - Add NO other changes. Do NOT touch existing token values (AP-07 — `git diff src/styles/theme.css` after this task should show only +5 lines, zero modifications to existing lines).

    Run `npx vitest run src/styles/theme.contrast.test.ts` and confirm:
    - 10 tests pass (5 reduced-motion crossfade + 5 accent-strong vs on-accent).
    - The original 5 ratios are byte-identical (light 1.78, dark 4.85, moss 1.94, slate 2.56, dusk 1.87).

    If any of the 5 new on-accent vs accent-strong contrasts is < 1.5, that palette's seed value needs adjustment. The seed values above were chosen for clear contrast margins (white on dark accent-strong yields >= 4.5; dark on light accent-strong yields >= 3.5). Investigate before re-tuning to a different seed; this should pass on first try.

    Commit this as `feat(16.1-01): add --color-breathing-on-accent token to all 5 palettes`.
  </action>
  <verify>
    <automated>npx vitest run src/styles/theme.contrast.test.ts --reporter=verbose 2>&1 | grep -E "(passed|failed)" | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "color-breathing-on-accent" src/styles/theme.css` returns exactly 5 (one per palette block).
    - `git diff src/styles/theme.css` shows ONLY 5 added lines (no modified or deleted lines — AP-07 sentinel).
    - `npx vitest run src/styles/theme.contrast.test.ts` exits 0; reporter output shows 10 passed (5 reduced-motion crossfade + 5 accent-strong vs on-accent).
    - `grep -E "(1\\.78|4\\.85|1\\.94|2\\.56|1\\.87)" src/styles/theme.contrast.test.ts` returns 0 lines (the original ratios are recorded in 16-VERIFICATION.md, not in the test file — the test computes them at runtime; this sentinel just confirms we did not hardcode comparison values).
    - `grep -c "accent-strong vs on-accent\\|--color-breathing-on-accent" src/styles/theme.contrast.test.ts | grep -v '^#' | grep -c .` indicates the new test block exists.
  </acceptance_criteria>
  <done>
    Five new `--color-breathing-on-accent` token lines exist in `src/styles/theme.css`, one per palette, contrast-tuned so each `accent-strong vs on-accent` pair clears the THEME-05 ≥ 1.5 WCAG floor. The contrast guard test has 10 passing iterations (5 old + 5 new). The token is now consumable by plan 06 (SessionControls migration) and by `body::selection` in plan 07 (`index.css` D-03).
  </done>
</task>

<task type="auto">
  <name>Task 3: Create theme.no-hardcoded-classes.test.ts as RED regression guard</name>
  <files>src/styles/theme.no-hardcoded-classes.test.ts</files>
  <read_first>
    - src/styles/theme.contrast.test.ts (structural template — same /// <reference types="node" /> directive, same node:fs + readFileSync + resolve pattern, same describe.each iteration over `[name, pattern]` pairs, same zero-dep posture)
    - .planning/phases/16.1-ui-token-migration/16.1-CONTEXT.md D-04 (authoritative regex list + exclusion rules)
    - .planning/phases/16.1-ui-token-migration/16.1-PATTERNS.md §3 (full file template, including the `collectTsxFiles` recursive walker that excludes `*.test.tsx`, `*.stories.tsx`, and `src/themes/**`)
    - src/components/SessionControls.tsx, src/components/ThemePicker.tsx (any 2 of the 16 migration targets — confirms RED expectation: today's codebase WILL match the banned regexes, so this test MUST fail on first commit)
  </read_first>
  <action>
    Create `src/styles/theme.no-hardcoded-classes.test.ts`. Copy the structural template from PATTERNS.md §3 verbatim:

    ```typescript
    // src/styles/theme.no-hardcoded-classes.test.ts
    //
    // Phase 16.1 THEME-UI-01 / D-04: regression guard — no production .tsx file
    // references the 10 banned hardcoded Tailwind palette utilities. Long-term safety net
    // for Phase 17/18/19 to prevent silent re-introduction of theme-blind colors.
    //
    // Lifecycle:
    //   - Wave 0 (this commit): test is RED — today's codebase has ~58 banned-class
    //     occurrences across 16 production .tsx files. RED proves the guard catches the
    //     real regression that motivated Phase 16.1.
    //   - Waves 1-2 (plans 02, 03, 04, 05, 06): each migration plan reduces the per-file
    //     hit count. The guard stays RED until plan 06 (Group A — SessionControls) clears
    //     the last text-white/bg-teal occurrences.
    //   - Wave 3 (plan 07): the test flips to GREEN as part of the phase-close criterion.

    /// <reference types="node" />

    import { describe, it, expect } from 'vitest'
    import { readFileSync, readdirSync, statSync } from 'node:fs'
    import { resolve, join } from 'node:path'

    // CONTEXT.md D-04: the 10 banned regex patterns (authoritative for both migration AND guard).
    const BANNED_PATTERNS: ReadonlyArray<readonly [string, RegExp]> = [
      ['text-slate-N', /\btext-slate-[0-9]/],
      ['bg-slate-N', /\bbg-slate-[0-9]/],
      ['border-slate-N', /\bborder-slate-[0-9]/],
      ['text-teal-N', /\btext-teal-[0-9]/],
      ['bg-teal-N', /\bbg-teal-[0-9]/],
      ['border-teal-N', /\bborder-teal-[0-9]/],
      ['text-white', /\btext-white\b/],
      ['bg-white', /\bbg-white\b/],
      ['text-black', /\btext-black\b/],
      ['bg-black', /\bbg-black\b/],
    ]

    // CONTEXT.md D-04: exclude *.test.tsx, *.stories.tsx, and src/themes/** (token defs).
    // PATTERNS.md §3: walker is shape-identical to the recursive scan in theme.contrast.test.ts.
    function collectTsxFiles(dir: string, acc: string[] = []): string[] {
      for (const entry of readdirSync(dir)) {
        const full = join(dir, entry)
        const st = statSync(full)
        if (st.isDirectory()) {
          if (entry === 'themes') continue // src/themes/** exclusion
          collectTsxFiles(full, acc)
        } else if (entry.endsWith('.tsx') && !entry.endsWith('.test.tsx') && !entry.endsWith('.stories.tsx')) {
          acc.push(full)
        }
      }
      return acc
    }

    const SRC_DIR = resolve(__dirname, '..')
    const TSX_FILES = collectTsxFiles(SRC_DIR)

    describe.each(BANNED_PATTERNS)('banned class %s', (name, pattern) => {
      it('is not present in any production .tsx file', () => {
        const hits: string[] = []
        for (const file of TSX_FILES) {
          const text = readFileSync(file, 'utf-8')
          if (pattern.test(text)) hits.push(file)
        }
        expect(hits, `Found banned ${name} in:\n${hits.join('\n')}`).toEqual([])
      })
    })
    ```

    Decision (CONTEXT.md D-04 destructive-pair exclusion): the regex `\btext-white\b` matches the destructive-red `text-white` paired with `bg-red-*` in `EndSessionDialog.tsx:81` and `ResetStatsDialog.tsx:76`. Per CONTEXT.md "Out of scope (explicit)" `red-*` is danger-semantic and intentionally NOT migrated, but `text-white` paired with red IS in the regex scope. Two options exist; pick option (a):

    (a) RECOMMENDED — At phase close (plan 07), the destructive `text-white` lines are also migrated to `text-[var(--color-breathing-on-accent)]` since the new on-accent token is ALSO appropriate for white-on-red (white in 4 of 5 palettes; deep-violet `#1e0550` in Dusk happens to also clear contrast on red-700 `#b91c1c` — verify in plan 07). Outcome: the regex stays as written; plan 07 closes the last 2 occurrences.

    (b) Alternative — Add a 2-line carve-out to the walker that strips `bg-red-*` + adjacent `text-white` lines before running the regex. More fragile, more code, more anti-pattern surface.

    Do NOT add an `eslint-disable` comment, do NOT hand-edit the regex to anchor it differently. Option (a) is the locked path; plan 07 handles closure.

    After file creation, run `npx vitest run src/styles/theme.no-hardcoded-classes.test.ts`. EXPECTED OUTCOME: **10 failing tests** (one per banned pattern; each fails with a multi-line error showing the violator files). This RED state proves the guard works against today's real codebase. Commit message: `test(16.1-01): add RED regression guard for 10 banned hardcoded classes`. Do NOT proceed if the test passes on first run — that means either the walker is excluding too much (likely the `themes` skip is also catching `theme.css` consumers it shouldn't) or the regex set lost a pattern in transcription.
  </action>
  <verify>
    <automated>test -f src/styles/theme.no-hardcoded-classes.test.ts && npx vitest run src/styles/theme.no-hardcoded-classes.test.ts --reporter=verbose 2>&1 | grep -cE "FAIL|✗|failed" | tee /tmp/wave0-guard-red.txt; grep -q "10 failed\\|10 of 10 failed" /tmp/wave0-guard-red.txt || npx vitest run src/styles/theme.no-hardcoded-classes.test.ts 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `src/styles/theme.no-hardcoded-classes.test.ts` exists.
    - File contains exactly 10 entries in the `BANNED_PATTERNS` array (verify with `grep -cE "^\\s+\\['(text|bg|border)-" src/styles/theme.no-hardcoded-classes.test.ts` — expected: 10).
    - File contains `/// <reference types="node" />` (verbatim string).
    - File excludes `*.test.tsx`, `*.stories.tsx`, and `themes/` (search for these substrings in the walker).
    - `npx vitest run src/styles/theme.no-hardcoded-classes.test.ts` exits with NON-ZERO code (RED — exit code 1 expected); reporter output includes "10 failed" or equivalent (10 of 10 banned-pattern iterations match real code today).
    - The failure messages enumerate the actual violator files (e.g., `src/components/SessionControls.tsx`, `src/components/ThemePicker.tsx`, `src/app/App.tsx`) — confirms the walker reaches the migration surface.
    - The full test suite (`npm test -- --run`) NOW exits NON-ZERO because of these 10 failing assertions. This is EXPECTED and ACCEPTABLE for Wave 0 — the per-commit green-gate is suspended for `theme.no-hardcoded-classes.test.ts` until plan 07. To preserve per-commit green-gate for ALL OTHER tests (D-17 carry-over from Phase 16), the executor MUST add a top-of-file `.skip` modifier to the `describe.each` block in the SAME commit that creates the file — converting it from `describe.each(...)` to `describe.each.skip(...)` with an accompanying JSDoc note: "TEMPORARILY SKIPPED Wave 0..Wave 2 — re-enabled at plan 07 phase close. Manual run: `npx vitest run src/styles/theme.no-hardcoded-classes.test.ts` to see RED progress narrowing."

    Final acceptance: after the `.skip` is added, `npm test -- --run` exits 0 (the skipped test is reported as skipped, not failed); and `npx vitest run --no-skip src/styles/theme.no-hardcoded-classes.test.ts` (or equivalent override) still exits non-zero so the executor can verify RED state on demand.
  </acceptance_criteria>
  <done>
    `theme.no-hardcoded-classes.test.ts` exists, is correctly structured, and is `.skip`'d at the describe level so the per-commit green-gate stays intact through Waves 1-2. Manual runs against the unskipped form show 10 failing iterations naming the real violator files. The guard is now the canonical CI exit condition for plan 07 phase close.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build-time CSS codegen | Tailwind v4 parses arbitrary-value classes from .tsx and emits CSS; user-visible only after build. No untrusted input crosses this boundary. |
| jsdom test environment | `readFileSync` reads files from the project root under Vitest; restricted to source tree, not network. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16.1-01-01 | Information disclosure | theme.no-hardcoded-classes.test.ts walker | accept | Walker reads only files under `src/` (resolved via `__dirname + '..'`); cannot escape project root. No PII / secrets in TSX files. |
| T-16.1-01-02 | Tampering | theme.css token edits | mitigate | Vitest `theme.contrast.test.ts` runs on every commit and asserts ≥ 1.5 WCAG floor; AP-07 sentinel (`git diff src/styles/theme.css` shows only +5 lines) prevents accidental retune. |
| T-16.1-01-03 | Denial of service | guard test runtime | accept | Walker is O(N) over <100 .tsx files; readFileSync is < 1ms per file. Total runtime ~30ms inside the existing 30s test budget. |

This is a build-time / test-time only plan. No runtime code, no untrusted input, no auth / data path. STRIDE surface is minimal.
</threat_model>

<verification>
After all 3 tasks complete, run the full Phase 16.1 per-commit green-gate:

```bash
npx tsc --noEmit && npm run lint && npm run build && npm test -- --run
```

Expected: exit 0. The new `theme.no-hardcoded-classes.test.ts` is `.skip`'d at the describe level (Task 3 acceptance), so the suite passes while the guard stays in the codebase. The contrast guard (`theme.contrast.test.ts`) has 10 passing iterations (5 old + 5 new). The alpha-probe test passes (or is documented-skip with the strategy decision in JSDoc).

Snapshot manual checks (executor confirms before close):
1. `grep -c "THEME-UI-01" .planning/REQUIREMENTS.md` >= 2.
2. `grep -c "color-breathing-on-accent" src/styles/theme.css` == 5.
3. `grep -E "describe.each.*BANNED_PATTERNS|describe.skip" src/styles/theme.no-hardcoded-classes.test.ts` returns >= 1 line.
4. `git diff src/styles/theme.css | grep -E "^[+-]" | grep -v "^\\+\\+\\+\\|^---" | grep -c "^-"` returns 0 (no removed lines — AP-07 sentinel).
5. The original 5 contrast ratios (1.78, 4.85, 1.94, 2.56, 1.87) are reproduced in the verbose reporter for the reduced-motion test (unchanged from Phase 16 baseline).
</verification>

<success_criteria>
1. `.planning/REQUIREMENTS.md` contains a `THEME-UI-01` bullet AND a traceability row mapping it to Phase 16.1 (status: Pending).
2. `src/styles/theme.css` has 5 new `--color-breathing-on-accent` declarations, one per palette, inserted after `--color-breathing-muted` in each block. Zero existing lines modified or removed.
3. `src/styles/theme.contrast.test.ts` exits 0 with 10 passing iterations (5 original reduced-motion + 5 new accent-strong vs on-accent).
4. `src/styles/theme.no-hardcoded-classes.test.ts` exists with the 10-pattern walker AND is `.skip`'d at the describe level (so per-commit green-gate stays intact through Waves 1-2).
5. `src/styles/theme.alpha-probe.test.ts` exists and exits 0; D-02 strategy decision (Path A arbitrary-value /N as default; Path B inline rgb(from ...) as fallback) is documented in its JSDoc.
6. `npx tsc --noEmit && npm run lint && npm run build && npm test -- --run` exits 0 (per-commit green-gate honored — D-17 carry-over).
7. Per-commit green-gate is held by 3 atomic commits aligned to the 3 tasks: REQUIREMENTS+probe / contrast-RED-then-GREEN / guard-RED-then-skip.
</success_criteria>

<output>
After completion, create `.planning/phases/16.1-ui-token-migration/16.1-01-SUMMARY.md` summarizing:
- Token name: `--color-breathing-on-accent`
- Per-palette hex values landed (and any deviation from the seed values in PATTERNS.md §2)
- Recorded contrast ratios for the 5 new `accent-strong vs on-accent` pairs (for the SUMMARY ledger)
- D-02 strategy decision and where it is documented (theme.alpha-probe.test.ts JSDoc + this SUMMARY)
- Confirmation that `theme.no-hardcoded-classes.test.ts` is `.skip`'d and the count of banned-class hits the guard would have caught against the current codebase (sourced from a manual `npx vitest run --no-skip ...` execution)
</output>
