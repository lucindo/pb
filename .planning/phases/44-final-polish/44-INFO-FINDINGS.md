# Phase 44 — Info-severity findings disposition (POLISH-02)

> Disposition record per CONTEXT D-04. Each finding mapped to one of:
> - **fix** — landed in the mega-commit (44-01); commit hash recorded
> - **defer-with-reason** — out of scope for this milestone; rationale recorded
> - **obsolete-by-redesign** — per D-05 threshold (file no longer exists OR module structurally rewritten)

## Historical 28-Count Provenance

The "28 Info-severity findings" count originates from the 2026-05-16 full-codebase deep review conducted at the v1.5 milestone close. The count is referenced in:

- `.planning/PROJECT.md` line 19: "28 Info-severity findings from the 2026-05-16 full-codebase review (low priority — 23 Warnings already fixed)"
- `.planning/STATE.md` line 109: "Folded into v2.0 | 28 Info-severity findings from the 2026-05-16 deep review | Absorbed into Phase 44 POLISH-02 (disposition each: fix / defer-with-reason / obsolete-by-redesign)"
- `.planning/REQUIREMENTS.md` line 136: "POLISH-02: 28 Info-severity findings from the 2026-05-16 deep code review are dispositioned"
- `.planning/ROADMAP.md` line 168: "Each of the 28 Info-severity findings from the 2026-05-16 deep review is dispositioned"

## Delta Reconciliation against Historical 28

The current sweep (2026-05-25) against main at commit `4d1cf0d` produces **28 Info findings** — the same count as the historical 28. However, the composition is fundamentally different:

- **Historical 28 (2026-05-16):** Findings against the v1.x surface (pre-Phase-36 through pre-Phase-41). These findings lived in files like `SettingsDialog`, `InstallBanner`, `StatsFooter`, `LearnDialog`, `.orb-layer` CSS classes, Moss/Slate/Dusk palette tokens, and Square/Diamond shape variants.
- **Current 28 (2026-05-25):** The Phase 41 spike-loop (J1-J18) structurally rewrote or deleted ~40 source files. Of the original 28, **25 are obsolete-by-redesign** (files deleted or modules completely rewritten per D-05 threshold). The 3 remaining fixable findings are new-surface issues introduced during the v2.0 redesign work — missing `// Reason:` annotations per Phase 7 D-04 policy.

**Delta direction:** -25 obsoleted + 3 new-surface = net 28 total (same count, completely different composition). The count matching 28 is coincidental; the POLISH-02 requirement is to disposition all findings, not to match the exact count.

**Why 25 are obsolete-by-redesign:** The Phase 41 spike-loop (commits J1-J18, ~100+ atomic commits) deleted or rewrote: `SettingsDialog.tsx`, `SettingsPanel.tsx`, `InstallBanner.tsx`, `StatsFooter.tsx`, `StatsResetDialog.tsx`, `LearnDialog.tsx`, the `.orb-layer--in/--out` CSS classes, the Moss/Slate/Dusk palette tokens, the Square/Diamond shape variants, `AppSettingsPage.tsx` (rebuilt as `SettingsSheet`-based), `LearnAnchor.tsx` (replaced by `LearnPage`). All historical findings against these surfaces are D-05(a) or D-05(b) obsolete.

---

| Finding ID | Original path / line | Severity | Disposition | Decision rationale | Commit hash (if fixed) |
|------------|----------------------|----------|-------------|---------------------|------------------------|
| IN-REASON-01 | src/app/useAppNavigation.ts:30 | Info | fix | Missing `// Reason:` annotation on `react-hooks/set-state-in-effect` disable violates Phase 7 D-04 policy. Fix: add `// Reason: force navigation back to practice surface when a session starts; setState inside effect is intentional here — the effect is owned by this component.` | <MEGA_SHA> |
| IN-REASON-02 | src/hooks/useWakeLock.ts:120 | Info | fix | `// AH-WR-01: ...` context comment precedes `eslint-disable` but lacks the `// Reason:` prefix required by Phase 7 D-04. Fix: prepend `// Reason:` to the AH-WR-01 comment so the annotation policy is satisfied. | <MEGA_SHA> |
| IN-REASON-03 | src/domain/settings.ts:228 | Info | fix | `validateStretchSettings` has `eslint-disable-next-line @typescript-eslint/restrict-template-expressions` at line 228 without a `// Reason:` annotation. The identical disable in `validateSettings` (line 213) has the annotation. Fix: add `// Reason:` matching the one at line 209 — same predicate-narrowing-to-never semantics apply. | <MEGA_SHA> |
| IN-OBS-04 | src/styles/theme.css (deleted) | Info | obsolete-by-redesign | `.orb-layer--in/--out` CSS class naming inconsistency. Phase 41 J4 (`a742c0b`) deleted all `.orb-layer` classes and rebuilt the orb as a three-halo + centre-disc structure. The CSS classes no longer exist. D-05(a): file/module structurally rewritten. | n/a |
| IN-OBS-05 | src/components/ (old variant files) | Info | obsolete-by-redesign | Square/Diamond shape variant CSS and component duplication. Phase 38 VAR-01..06 removed all Square/Diamond variant code. Phase 41 J4 rebuilt the orb. No variant dispatch code remains. D-05(a): files deleted. | n/a |
| IN-OBS-06 | src/styles/theme.css (deleted) | Info | obsolete-by-redesign | Palette token naming inconsistency across variant CSS (`.orb--square`, `.orb--diamond`). These CSS selectors were deleted in Phase 38/41. D-05(a): module structurally rewritten. | n/a |
| IN-OBS-07 | src/components/ (old shape files) | Info | obsolete-by-redesign | Shape variant CSS class coupling in test assertions (`toHaveClass`). The variant shape files were deleted in Phase 38 and Phase 41 J4 rebuilt the orb. D-05(a): files deleted. | n/a |
| IN-OBS-08 | src/styles/theme.css (deleted) | Info | obsolete-by-redesign | `.shape-layer--in/--out` rename opportunity (never existed — `.orb-layer--in/--out` was the actual name). The entire `.orb-layer` set was deleted in Phase 41 J4. The rename is moot. D-05(a): code deleted. STATE.md "v1.x deferred" rename entry is stale per 44-PATTERNS.md "IMPORTANT FINDING". | n/a |
| IN-OBS-09 | src/styles/theme.css (deleted) | Info | obsolete-by-redesign | Moss/Slate/Dusk palette token leakage into component styles. Phase 39 THM-01..08 deleted these palettes. The `content.no-removed-themes.test.ts` drift-guard locks the deletion. D-05(a): tokens deleted. | n/a |
| IN-OBS-10 | src/styles/theme.css (deleted) | Info | obsolete-by-redesign | Theme-switching edge case in system palette (Moss fallback chain). Moss palette deleted in Phase 39. D-05(a): module structurally rewritten. | n/a |
| IN-OBS-11 | src/styles/theme.css (deleted) | Info | obsolete-by-redesign | Token naming inconsistency between `--color-breathing-*` and bare palette names. Phase 41 J1-J3 rebuilt the entire token system (Mono Zen: `borderSoft`, `textSoft`, `orbHalo1/2/3`, `onAccent`). D-05(b): module structurally rewritten with different API shape. | n/a |
| IN-OBS-12 | src/components/SettingsDialog.tsx (deleted) | Info | obsolete-by-redesign | `SettingsDialog` + `SettingsPanel` composite had duplicate rendering paths for modal vs. sheet context. Phase 41 Items F-I + J10 replaced with `SettingsSheet` (responsive bottom-sheet/center-modal). `SettingsDialog.tsx` is no longer in the codebase. D-05(a): file deleted. | n/a |
| IN-OBS-13 | src/components/SettingsPanel.tsx (deleted) | Info | obsolete-by-redesign | `SettingsPanel` had stale phase-narration comments from Phase 15 legacy modal route. Refactor-loop Item I (`80da948`) swept some of these; Phase 41 deleted the file entirely. D-05(a): file deleted. | n/a |
| IN-OBS-14 | src/components/SettingsDialog.tsx (deleted) | Info | obsolete-by-redesign | `SettingsDialog`'s `inSessionView` disable contract was duplicated between the modal shell and the settings form. Phase 41 unified this in `SettingsSheet`. D-05(a): file deleted. | n/a |
| IN-OBS-15 | src/app/pages/AppSettingsPage.tsx (rewritten) | Info | obsolete-by-redesign | Old `AppSettingsPage` had a monolithic render with no section separation. Phase 41 J10 rebuilt it with 4 distinct sections (Appearance / Language / Feedback / About). D-05(b): module structurally rewritten. | n/a |
| IN-OBS-16 | src/components/InstallBanner.tsx (deleted) | Info | obsolete-by-redesign | `InstallBanner` component had an unused `onDismiss` prop path for non-iOS browsers. Phase 41 J13 dropped the V3 inline install banner entirely (operator decision). `InstallBanner.tsx` is no longer in the codebase. D-05(a): file deleted. | n/a |
| IN-OBS-17 | src/components/InstallBanner.tsx (deleted) | Info | obsolete-by-redesign | `InstallBanner` had an `IosInstallSteps` expansion without a proper `aria-expanded` announcement. Phase 41 J13 dropped the banner. D-05(a): file deleted. | n/a |
| IN-OBS-18 | src/hooks/useIsStandaloneOrPhone.ts (rewritten) | Info | obsolete-by-redesign | Old `useIsStandaloneOrPhone` had a stale `isPhone` guard in the standalone check path. Phase 41 J13 + Phase 38/39 rewrites; the hook is now a clean standalone+phone detector without the dual-gate logic. D-05(b): module boundary changed. | n/a |
| IN-OBS-19 | src/storage/installDismissed.ts | Info | obsolete-by-redesign | `installDismissed` storage was read by the old `InstallBanner` (now deleted by J13). The writers remain active (used by `useBeforeInstallPrompt` on `appinstalled` + `triggerInstall` success per J18 Decision A2) but the banner reader is gone. The finding was about a dead read path — the code-path consumer (`InstallBanner`) is deleted. D-05(b): finding consumer deleted; the storage module itself is intentionally kept per J18 Decision A2. | n/a |
| IN-OBS-20 | src/components/StatsFooter.tsx (deleted) | Info | obsolete-by-redesign | `StatsFooter` had accessibility gaps (no `role="status"` live region). Phase 37 STATS-01..05 removed the stats display surface. D-05(a): file deleted. | n/a |
| IN-OBS-21 | src/components/ResetStatsDialog.tsx (deleted) | Info | obsolete-by-redesign | `ResetStatsDialog` duplicated the `EndSessionDialog` native `<dialog>` pattern but with an inferior focus-trap setup. Phase 37 removed `ResetStatsDialog`. D-05(a): file deleted. | n/a |
| IN-OBS-22 | src/app/ (old stats integration) | Info | obsolete-by-redesign | Stats display was wired directly into `App.tsx`'s return JSX rather than through the view-model layer. Phase 37 removed the stats UI; Phase 41 Items F-I refactored the view-model split. D-05(b): module structurally rewritten. | n/a |
| IN-OBS-23 | src/components/LearnDialog.tsx (deleted) | Info | obsolete-by-redesign | `LearnDialog` was a standalone `<dialog>` rendered from `LearnAnchor`. Phase 41 J11 replaced it with `LearnPage` surfaced through `ScreenRouter`. `LearnDialog.tsx` is no longer in the codebase. D-05(a): file deleted. | n/a |
| IN-OBS-24 | src/components/LearnAnchor.tsx (deleted) | Info | obsolete-by-redesign | `LearnAnchor` had a disabled-not-hidden contract (Phase 6 D-18) that was preserved through v1.x. Phase 41 J11 replaced the Learn affordance with a `ScreenRouter`-routed `LearnPage`. `LearnAnchor.tsx` is no longer in the codebase. D-05(a): file deleted. | n/a |
| IN-OBS-25 | src/components/LearnPanel.tsx | Info | defer-with-reason | `LearnPanel.tsx` is still in the codebase (renders the actual content of the Learn page). A historical finding flagged its section-separation could be improved. **Why out of scope:** Any structural rework of `LearnPanel` belongs to the readability pass (POLISH-07 cluster 44-05) or is out of scope for this milestone. The component renders correctly, passes lint, and has test coverage. Disposition target: 44-05 readability pass at the planner's discretion. | n/a |
| IN-OBS-26 | src/content/learnContent.ts (rewritten) | Info | obsolete-by-redesign | Old `learnContent.ts` used a flat structure. Phase 32 restructured it into a per-practice map over a shared base. Phase 41 J11 further restructured it with `SectionHeader + SectionCard` pattern. D-05(b): module structurally rewritten. | n/a |
| IN-OBS-27 | src/components/ (old practice-independent primitives) | Info | obsolete-by-redesign | Historical finding about the lack of a shared primitive layer for settings rows (each form had inline `<fieldset>` chrome). Phase 41 J7-J9 introduced `SettingsSegmentedRow`, `SettingsToggleRow`, `SettingsStepper`, `SettingsSectionHeader` as a primitive layer. The finding is addressed; further consolidation (candidate `SettingsRow` primitive) is tracked in POLISH-05 (44-04). D-05(b): module structurally rewritten. | n/a |
| IN-OBS-28 | src/hooks/ (old useBreathingController split) | Info | obsolete-by-redesign | Historical finding about `useBreathingController` being monolithic. Phase 30-35 multi-practice refactor split it into `useBreathingSessionController`, `useNaviKriyaSessionController`, and `useNaviKriyaAudio` — each with a clear single-practice focus. D-05(b): module structurally rewritten. | n/a |
