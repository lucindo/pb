---
phase: 44-final-polish
plan: 02
type: execute
wave: 2
depends_on: ["44-01"]
files_modified:
  - src/**/*.test.ts
  - src/**/*.test.tsx
autonomous: true
requirements:
  - POLISH-03
user_setup: []

must_haves:
  truths:
    - "Every Vitest test name is tight (describes-the-behavior, not the implementation) — no `it('renders correctly')` / `it('test 1')` / `describe('Component')`-style placeholders"
    - "No redundant test cases — duplicated coverage between `App.*.test.tsx` integration files and the per-component unit tests is removed (analog: Item F2 `94958e8` -9 tests)"
    - "Zero re-introduction of design-token-locking assertions (D-14 / [[no-design-locking]]); any `toHaveClass('<token>')` swept and replaced with behavioral / aria / prop-API assertions (analog: Item H `4457259`)"
    - "Drift-guard tests preserved (7 files — listed in PATTERNS.md 'Drift-guard tests POLISH-03 MUST NOT remove')"
    - "Test suite has zero flake across 3× consecutive runs (POLISH-03 ROADMAP wording)"
    - "Final test count is documented in 44-02-SUMMARY.md (pre-sweep / post-sweep / net delta with one-line rationale)"
  artifacts:
    - path: "src/**/*.test.{ts,tsx}"
      provides: "Tightened test names + dedup'd redundant cases + zero design-token-locking assertions"
    - path: ".planning/phases/44-final-polish/44-02-SUMMARY.md"
      provides: "Final test count delta + flake-check evidence + per-cluster commit SHA"
  key_links:
    - from: "any deleted test file"
      to: "44-02-SUMMARY.md SUMMARY rationale"
      via: "deletion-unlocking discipline (Phase 39 D-06 exit-ramp policy)"
      pattern: "Deleted:\\s+src/.*\\.test\\.(ts|tsx)"
    - from: "any renamed test name"
      to: "the matching behavior assertion"
      via: "describe-the-behavior-not-the-implementation rule (Tiger Style)"
      pattern: "it\\(['\"]<verb>"
---

<objective>
Sweep `src/**/*.test.{ts,tsx}` for (a) loose / implementation-leaking test names that should be tightened per Tiger Style "describe-the-behavior-not-the-implementation", (b) redundant test cases where an integration test in `App.*.test.tsx` duplicates a component-level test elsewhere, and (c) any newly-introduced (or surviving) design-token-locking assertions that violate D-14 / [[no-design-locking]]. Preserve all 7 drift-guard tests verbatim. Run a flake check (3× consecutive `npm test -- --run`) and document the final test count delta in 44-02-SUMMARY.md.

Purpose: Close POLISH-03 (tight Vitest names, redundancy removal, no flake, final count documented). Builds on three load-bearing analogs from the refactor loop — J18.1 dead-test deletion, Item F2 redundant-overlap deletion, Item H design-token-locking removal — each addressing one cleanup category.

Output: Per-cluster commit(s) under the `test(44): ...` prefix per PATTERNS.md "Atomic commit scope-prefixing" + 44-02-SUMMARY.md with the test-count delta block.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/44-final-polish/44-CONTEXT.md
@.planning/phases/44-final-polish/44-PATTERNS.md
@.planning/REFACTOR-LOOP-STATE.md
@.planning/phases/44-final-polish/44-01-SUMMARY.md
</context>

<propose_step_checklist>

## MANDATORY (per CONTEXT D-13 + [[propose-step-checklist]])

Before executing any task in this plan, the executor MUST first produce a PROPOSE block leading with:

1. **Downstream Constraints** — What downstream tasks (other Phase 44 clusters, future v2.x work, milestone close, drift-guard tests) might this change constrain? List 2-5 concrete items.

2. **Applicable Memory Rules** — Which rules from `~/.claude-personal/projects/-Users-lucindo-Code-hrv/memory/MEMORY.md` apply to this work? At minimum surface (cite by `[[rule-name]]`):
   - `[[propose-step-checklist]]` — meta-rule, always applies
   - `[[no-design-locking]]` — applies if any test edit or token assertion (D-14 EXPLICIT for this plan)
   - `[[use-lsp-for-renames]]` — applies if any symbol/property/CSS-class rename
   - `[[design-logic-separation]]` — applies if refactor touches any boundary
   - `[[ack-dont-fix-inline]]` — applies during operator feedback dumps
   - `[[spike-locked-values-are-not-decisions]]` — applies if any spike-010-locked visual would be touched

3. **THEN** — Goal / Scope / Risk / Verification / Commit message (the standard propose-block content).

No exceptions for "small" cleanup tasks. The Section A + Section B preface is the gate, not the content.

This requirement is operator-locked at CONTEXT D-13 verbatim: "Mandatory propose-step checklist applies to every Phase 44 plan per [[propose-step-checklist]]: Downstream Constraints + Applicable Memory Rules sections BEFORE Goal/Scope/Risk. No exceptions for 'small' cleanup plans."

</propose_step_checklist>

<tasks>

<task type="auto">
  <name>Task 1: Audit and propose — surface every test-name tightening, redundancy removal, and design-token-locking-assertion swap candidate</name>
  <files>
    - (read-only audit) src/**/*.test.ts
    - (read-only audit) src/**/*.test.tsx
    - .planning/phases/44-final-polish/44-02-AUDIT-NOTES.md (new — proposal record; this is a notes artifact, NOT shipped to main with code)
  </files>
  <read_first>
    - .planning/phases/44-final-polish/44-CONTEXT.md (D-14 [[no-design-locking]] guard verbatim; canonical_refs section listing the 4 named drift-guard tests)
    - .planning/phases/44-final-polish/44-PATTERNS.md ("Drift-guard tests POLISH-03 MUST NOT remove (CONTEXT canonical_refs)" block — 7 files total; "[[no-design-locking]] guard (D-14)" block; "Tight Vitest naming pattern" table; "Final test count documentation" block)
    - .planning/REFACTOR-LOOP-STATE.md (Item F2 commit `94958e8` — 9-test deletion in App.dialog.test.tsx; Item H commit `4457259` — 7 primitive test files swept of class-locking assertions; Item I commit `80da948` — sibling-pattern audit caught what H missed)
    - (representative live samples) src/components/PageShell.test.tsx, src/components/Card.test.tsx, src/components/Eyebrow.test.tsx, src/components/IconButton.test.tsx, src/components/Pill.test.tsx, src/components/PickerCardGrid.test.tsx, src/components/TopAppBar.test.tsx (the 7 files Item H touched — sweep their sibling files for the same patterns Item I caught for comments)
    - src/app/App.dialog.test.tsx (Item F2 target — confirm the 9-test deletion already landed; do NOT re-delete)
    - src/content/content.no-removed-keys.test.ts (preserve verbatim — drift-guard)
    - src/content/content.no-removed-themes.test.ts (preserve verbatim — drift-guard)
    - src/content/content.no-removed-variants.test.ts (preserve verbatim — drift-guard)
    - src/audio/previewContext.no-audioengine-import.test.ts (preserve verbatim — drift-guard)
    - src/content/content.no-review-markers.test.ts (preserve verbatim — drift-guard)
    - src/content/content.no-stats-ui.test.ts (preserve verbatim — drift-guard)
    - src/styles/theme.no-hardcoded-classes.test.ts (preserve verbatim — drift-guard)
  </read_first>
  <action>
    Produce `.planning/phases/44-final-polish/44-02-AUDIT-NOTES.md` (this is an internal notes artifact for Task 2; NOT a shipped doc artifact — it documents the proposal so Task 2's commit is principled). The notes must have THREE explicit sections, each derived from a different analog:

    Section 1 — TIGHT-NAME CANDIDATES (Tiger Style "describe-the-behavior-not-the-implementation" — PATTERNS.md table):
    Use the table grep — surface every `it('renders correctly')` / `it('test 1')` / `it('handles X')` / `describe('Component')` (no behavior descriptor) match across `src/**/*.test.{ts,tsx}`. For each, propose the tightened name following the table pattern (`it('<verb> <subject> <outcome>')`). DO NOT edit yet. Use grep commands:
    ```
    grep -rn "it(['\"]renders correctly\|it(['\"]test \|it(['\"]works\|it(['\"]handles " src/ --include="*.test.ts" --include="*.test.tsx"
    grep -rn "describe(['\"][A-Z][a-zA-Z]*['\"]" src/ --include="*.test.ts" --include="*.test.tsx"  # describe('Component') without behavior cluster suffix
    ```

    Section 2 — REDUNDANCY CANDIDATES (Item F2 analog, commit `94958e8`):
    For each component with both a unit test (`src/components/<X>.test.tsx`) AND an `App.*.test.tsx` integration block that covers the same component, audit whether the App-level coverage is genuinely integration (multi-component state flow) or redundant duplication. Item F2's lesson: 9 EndSessionDialog tests in `App.dialog.test.tsx` duplicated the dedicated `EndSessionDialog.test.tsx`. List candidates with: (a) the App-level describe block path + line range, (b) the unit-test file it duplicates, (c) the behavioral delta (zero = redundant; nonzero = keep). Use:
    ```
    grep -rln 'describe.*EndSessionDialog\|describe.*OrbShape\|describe.*ThemePicker\|describe.*CuePicker\|describe.*TimbrePicker\|describe.*MuteToggle\|describe.*SetupCard\|describe.*SettingsSheet\|describe.*FeedbackTime\|describe.*FeedbackCount' src/app/
    ```

    Section 3 — DESIGN-TOKEN-LOCKING CANDIDATES (D-14 / Item H analog, commit `4457259`):
    Sweep for re-introduced `toHaveClass('<class>')` assertions that lock design tokens (Tailwind classes, CSS variable references). Use:
    ```
    grep -rn "toHaveClass(['\"]p-[0-9]\|toHaveClass(['\"]size-[0-9]\|toHaveClass(['\"]bg-\[var\|toHaveClass(['\"]text-\[var\|toHaveClass(['\"]border-\[var" src/ --include="*.test.ts" --include="*.test.tsx"
    grep -rn 'toHaveClass(' src/ --include="*.test.ts" --include="*.test.tsx" | grep -v -E '(visually-hidden|sr-only)'
    ```
    For each match, propose the behavioral / aria / prop-API substitute per Item H's swap pattern (drop class assertion → assert `aria-pressed` / `aria-current` / `role` / `toBeEnabled` / `toHaveAccessibleName` instead).

    Section 4 — PRESERVE LIST (drift-guards):
    Echo the 7 drift-guard file paths from `<read_first>` verbatim with a "DO NOT MODIFY" note. Any proposed deletion that lands on one of these paths is REJECTED in advance — Phase 39 D-06 exit-ramp policy requires a deliberate operator unlock rationale, not in scope for Phase 44.

    Section 5 — TIGER STYLE INTERPRETATION NOTE:
    Per CONTEXT Claude's Discretion: "planner picks per-test based on Tiger Style 'describe-the-behavior-not-the-implementation' rule". The audit notes record the planner's per-candidate judgment (what the new name will be) BEFORE any edit, so the per-cluster commit in Task 2 has a principled audit trail.
  </action>
  <verify>
    <automated>test -f .planning/phases/44-final-polish/44-02-AUDIT-NOTES.md &amp;&amp; grep -c '^### \|^## Section ' .planning/phases/44-final-polish/44-02-AUDIT-NOTES.md ; grep -E '^- src/.*\.test\.(ts|tsx)' .planning/phases/44-final-polish/44-02-AUDIT-NOTES.md | head -20 ; echo '---DRIFT-GUARD PRESERVE CHECK---' ; grep -c 'content\.no-removed-keys\|content\.no-removed-themes\|content\.no-removed-variants\|previewContext\.no-audioengine-import\|content\.no-review-markers\|content\.no-stats-ui\|theme\.no-hardcoded-classes' .planning/phases/44-final-polish/44-02-AUDIT-NOTES.md</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/44-final-polish/44-02-AUDIT-NOTES.md` exists and contains 5 sections matching the header pattern (`## Section 1` through `## Section 5` OR `### TIGHT-NAME CANDIDATES` style)
    - Section 4 (PRESERVE LIST) lists all 7 drift-guard test file paths verbatim from the `<read_first>` block
    - Every TIGHT-NAME / REDUNDANCY / DESIGN-TOKEN-LOCKING candidate row includes the source file path AND the proposed action (rename to / delete because / swap to behavioral assertion)
    - Zero candidate row proposes modifying or deleting any of the 7 drift-guard tests; if any does, the proposal is marked REJECTED with rationale citing Phase 39 D-06 exit-ramp policy
    - Section 5 records the planner's Tiger Style interpretation for the at-least-3 most-impactful renames (so Task 2 is principled, not ad-hoc)
  </acceptance_criteria>
  <done>Audit notes complete; every candidate has a proposed action; drift-guards explicitly preserved.</done>
</task>

<task type="auto">
  <name>Task 2: Apply test-name tightening + redundancy removal + design-token-locking-assertion swaps + flake-check + commit</name>
  <files>
    - src/**/*.test.{ts,tsx} (exact files determined by Task 1 audit notes)
    - .planning/phases/44-final-polish/44-02-SUMMARY.md (new — final test count delta + flake-check evidence)
    - (delete after commit) .planning/phases/44-final-polish/44-02-AUDIT-NOTES.md (internal artifact; absorbed into SUMMARY)
  </files>
  <read_first>
    - .planning/phases/44-final-polish/44-CONTEXT.md (D-02 per-cluster commit; D-11 per-commit green-gate; D-14 [[no-design-locking]])
    - .planning/phases/44-final-polish/44-PATTERNS.md ("Final test count documentation" block — exact pre/post/net format; "Flake check (POLISH-03 ROADMAP 'no flake in close sweep')" block — 3× run; "Per-cluster commit (D-02)" block at end of cluster 44-02 — exact commit message shape)
    - .planning/phases/44-final-polish/44-02-AUDIT-NOTES.md (from Task 1)
    - .planning/REFACTOR-LOOP-STATE.md (Item H commit `4457259` — exact swap pattern for design-token-locking; Item F2 commit `94958e8` — net -9 with behavioral coverage preserved)
    - .planning/phases/44-final-polish/44-01-SUMMARY.md (the pre-sweep test count baseline post-mega-commit; may differ from CONTEXT baseline)
  </read_first>
  <action>
    Apply the audit notes from Task 1, in this order (to keep diffs reviewable):

    1. **Tight-name renames first** — edit `it()` and `describe()` strings per Section 1 of audit notes. No behavior change.
    2. **Redundancy removal second** — delete redundant `describe(...)` blocks per Section 2 of audit notes. The behavioral coverage MUST remain (per Item F2 lesson "behavioral coverage preserved" — every assertion that was unique to the deleted block must already exist in the unit-test file the block duplicated; if not, do NOT delete).
    3. **Design-token-locking-assertion swaps third** — replace `toHaveClass('<token>')` calls per Section 3 of audit notes with behavioral / aria / prop-API equivalents (Item H pattern). If the test can no longer make a useful assertion after the swap, the whole `it()` is deleted (behavioral coverage = zero).

    DO NOT touch any drift-guard test (Section 4 of audit notes). DO NOT touch any file outside `src/**/*.test.{ts,tsx}` (per D-02 the test-cleanup commit MUST NOT cross into other clusters).

    Run the per-commit green-gate after edits:
    ```
    npx tsc --noEmit -p tsconfig.app.json &amp;&amp; npm run lint &amp;&amp; npm test -- --run &amp;&amp; npm run build
    ```

    Then run the flake check per PATTERNS.md "Flake check" block — 3× consecutive `npm test -- --run`, capture the tail-5 of each:
    ```
    for i in 1 2 3; do echo "=== run $i ===" ; npm test -- --run 2>&amp;1 | tail -5 ; done
    ```
    If any run shows a different pass/fail count or a different failing test, surface the flaky test name and decide (with operator if needed): fix or quarantine.

    Commit using the PATTERNS.md "Per-cluster commit (D-02)" exact shape, scope prefix `test(44):`:
    ```
    test(44): tighten test names + remove redundancy + drop design-token-locking assertions (POLISH-03)

    Pre-sweep:  107 files / 1155 tests   (CONTEXT baseline at commit 580dc53)
    Post-sweep: <X> files / <Y> tests    (this commit)
    Net:        ±<X-107> files / ±<Y-1155> tests   (rationale: <one-line>)

    Tight-name renames: <N> tests (Tiger Style describe-the-behavior-not-the-implementation)
    Redundancy removed: <N> tests (analog: Item F2 commit `94958e8`; behavioral coverage preserved)
    Token-locking swaps: <N> tests (analog: Item H commit `4457259`; aria/prop-API substitutes)

    Drift-guards preserved (7 files; see 44-02-SUMMARY.md PRESERVE LIST).
    Flake check: 3× consecutive `npm test -- --run` zero-flake.

    Verified:
      npx tsc --noEmit -p tsconfig.app.json   # clean
      npm run lint                            # 0/0
      npm test -- --run                       # <X> files / <Y> tests pass
      npm run build                           # clean
    ```

    Write `44-02-SUMMARY.md` per PATTERNS.md "Final test count documentation" block:
    ```
    Test count delta:
      Pre-sweep:  107 files / 1155 tests
      Post-sweep: <X> files / <Y> tests
      Net:        ±<X-107> files / ±<Y-1155> tests   (rationale: ...)
    ```
    Plus flake-check evidence (3 run outputs), per-category counts (renames / deletions / swaps), drift-guard PRESERVE-list confirmation. Delete `44-02-AUDIT-NOTES.md` after absorbing it (the SUMMARY supersedes).
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.app.json &amp;&amp; npm run lint &amp;&amp; npm test -- --run &amp;&amp; npm run build &amp;&amp; for i in 1 2 3; do npm test -- --run 2>&amp;1 | tail -1 ; done ; echo '---DRIFT-GUARDS STILL PRESENT---' ; ls src/content/content.no-removed-keys.test.ts src/content/content.no-removed-themes.test.ts src/content/content.no-removed-variants.test.ts src/audio/previewContext.no-audioengine-import.test.ts src/content/content.no-review-markers.test.ts src/content/content.no-stats-ui.test.ts src/styles/theme.no-hardcoded-classes.test.ts ; echo '---TOKEN-LOCKING ASSERTIONS REMAINING---' ; grep -rn "toHaveClass(['\"]p-[0-9]\|toHaveClass(['\"]size-[0-9]\|toHaveClass(['\"]bg-\[var" src/ --include="*.test.ts" --include="*.test.tsx" | wc -l ; git log --oneline -3</automated>
  </verify>
  <acceptance_criteria>
    - All 4 green-gate commands exit 0 (tsc / lint / test / build)
    - 3× consecutive `npm test -- --run` all report the SAME pass count (zero flake — per POLISH-03 ROADMAP wording)
    - All 7 drift-guard files still exist at their canonical paths (`ls` returns success for each)
    - `grep "toHaveClass('p-[0-9]\\|...')"` returns 0 hits (or only matches in skipped-test fixtures explicitly documented in SUMMARY)
    - `git log --oneline -3` shows a `test(44):` prefixed commit
    - `44-02-SUMMARY.md` exists and contains the exact test-count delta block (Pre-sweep / Post-sweep / Net) with the rationale line
    - `44-02-SUMMARY.md` PRESERVE LIST echoes the 7 drift-guard paths
    - The post-sweep test pass count is recorded (drift from 1155 is allowed and documented; net-negative deltas have one-line rationales for each deleted block per Item F2 / Item H precedent)
    - `.planning/phases/44-final-polish/44-02-AUDIT-NOTES.md` is deleted (absorbed into SUMMARY) OR explicitly kept-with-rationale
  </acceptance_criteria>
  <done>POLISH-03 closed: tight names, no redundancy, no design-token-locking, no flake, final count documented, drift-guards preserved.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test-file edits → CI gate | Test renames and deletions are bounded to `src/**/*.test.{ts,tsx}`; per-commit green-gate (tsc + lint + test + build) catches regressions before commit. |
| drift-guard test surface → coverage of removed-pattern locks | Drift-guards are STRUCTURAL LOCKS; deletion silently unlocks the corresponding banned pattern. Phase 44-02 explicitly preserves all 7. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-44-02-01 | Tampering | accidental drift-guard deletion silently unlocks removed-pattern re-introduction (Phase 38/39/40/41 done-state) | mitigate | Task 1 audit notes Section 4 lists all 7 drift-guards as PRESERVE; Task 2 acceptance criteria includes an explicit `ls` of each path post-commit |
| T-44-02-02 | Repudiation | redundant-test deletion could lose unique behavioral coverage (Item F2 lesson) | mitigate | Task 2 step 2 verbatim from Item F2's discipline: "every assertion that was unique to the deleted block must already exist in the unit-test file the block duplicated; if not, do NOT delete" |
| T-44-02-03 | Tampering | re-introduced design-token-locking assertions (D-14 / [[no-design-locking]]) silently re-bind tests to downstream-modifiable values | mitigate | Task 1 Section 3 grep surface + Task 2 acceptance grep returning 0 hits |
| T-44-02-SC | Tampering | no package installs in this plan | accept | Test-cleanup plan modifies only test files; no dependency surface change |

</threat_model>

<verification>

- All 7 drift-guard test files exist at the paths in `<read_first>`
- 0 hits for the design-token-locking grep
- 0 flake across 3× consecutive `npm test -- --run`
- 44-02-SUMMARY.md test-count delta block populated
- Per-cluster commit landed under `test(44):` prefix

</verification>

<success_criteria>

POLISH-03: ✅ when tight names landed, redundancy removed, no design-token-locking assertions remaining, 3× flake-check zero-flake, final test count documented in 44-02-SUMMARY.md.

</success_criteria>

<output>
Create `.planning/phases/44-final-polish/44-02-SUMMARY.md` when done. Include:
- Test count delta block (Pre-sweep / Post-sweep / Net with rationale)
- Flake-check evidence (3-run output tail)
- Per-category counts (renames / deletions / swaps)
- Drift-guard PRESERVE-list (the 7 file paths)
- Per-cluster commit SHA + diff-stat
</output>
</content>
</invoke>