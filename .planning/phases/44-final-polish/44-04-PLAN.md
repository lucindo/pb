---
phase: 44-final-polish
plan: 04
type: execute
wave: 4
depends_on: ["44-01", "44-02", "44-03"]
files_modified:
  - src/styles/theme.contrast.test.ts  # archived comment at :121 — drop the now-mysterious `.orb-layer--in/--out` reference
  - src/components/SettingsRow.tsx  # NEW primitive — extracts shared row chrome
  - src/components/SettingsRow.test.tsx  # NEW unit tests for the primitive
  - src/components/SettingsSegmentedRow.tsx  # thin adapter wrapping SettingsRow
  - src/components/SettingsToggleRow.tsx  # thin adapter wrapping SettingsRow
  - src/components/SettingsStepper.tsx  # thin adapter wrapping SettingsRow
  - .planning/STATE.md  # update Pending Todos — disposition the obsolete `.orb-layer` rename entry
autonomous: true
requirements:
  - POLISH-05
user_setup: []

must_haves:
  truths:
    - "CONTEXT D-06's `.orb-layer` rename target is dispositioned correctly: PATTERNS.md verified the CSS classes are already deleted (Phase 41 J4 commit `a742c0b`); the only remaining hit is a stale comment at `src/styles/theme.contrast.test.ts:121` which is updated to drop the now-mysterious reference (reconciliation per PATTERNS.md 44-04 cluster `IMPORTANT FINDING` block)"
    - "STATE.md's Pending Todos `.orb-layer` rename entry is dispositioned `obsolete-by-redesign` per D-05 threshold (file content no longer exists — class deleted in J4)"
    - "Primitive duplication audit surfaces `SettingsRow` as the substituted refactor candidate (per PATTERNS.md grep verification: 3 components share `border-t border-[var(--color-border-soft)] py-3` + `<fieldset>` row chrome)"
    - "`SettingsRow` primitive extracted; 3 existing rows (Segmented / Toggle / Stepper) become thin adapters; behavioral byte-equivalence verified by all 3 adapter tests passing unchanged (analog: Item E `bd22ca5`)"
    - "No logic / state / audio / persistence changes per [[design-logic-separation]] — extraction is layout-only"
    - "Per-commit green-gate (tsc + lint + test + build) passes; JS bundle size delta recorded"
  artifacts:
    - path: "src/components/SettingsRow.tsx"
      provides: "Shared <fieldset> row chrome primitive — label + children + aria"
      min_lines: 20
      exports: ["SettingsRow"]
    - path: "src/components/SettingsRow.test.tsx"
      provides: "Behavioral unit tests for SettingsRow primitive (aria contract + children rendering + label rendering)"
    - path: "src/styles/theme.contrast.test.ts"
      provides: "Updated comment block at :121 — drops the now-mysterious `.orb-layer--in/--out` reference (per reconciliation finding)"
  key_links:
    - from: "src/components/SettingsSegmentedRow.tsx"
      to: "src/components/SettingsRow.tsx"
      via: "thin adapter wraps SettingsRow with the segmented-control child"
      pattern: "import.*SettingsRow"
    - from: "src/components/SettingsToggleRow.tsx"
      to: "src/components/SettingsRow.tsx"
      via: "thin adapter wraps SettingsRow with the toggle child"
      pattern: "import.*SettingsRow"
    - from: "src/components/SettingsStepper.tsx"
      to: "src/components/SettingsRow.tsx"
      via: "thin adapter wraps SettingsRow with the stepper child"
      pattern: "import.*SettingsRow"
---

<objective>
Two-part refactor pass per POLISH-05:

**Part A — Reconciliation finding (per PATTERNS.md 44-04 cluster IMPORTANT FINDING):** CONTEXT D-06 / D-07 reference the `.orb-layer--in/--out` → `.shape-layer--in/--out` CSS class rename. Live-code grep verification (`grep -rn '\.orb-layer\|\.shape-layer' src/`) returns exactly ONE hit: an archived comment at `src/styles/theme.contrast.test.ts:121` describing the historical assertion. The CSS classes were deleted in Phase 41 J4 commit `a742c0b` as part of the orb rewrite (orb is now halos + centre disc, no `.orb-layer--in/--out` gradient crossfade). There is NO class to rename. The STATE.md "v1.x deferred" entry for this rename is STALE per D-05 obsolete-by-redesign threshold (file content no longer exists). Action: update the single stale comment at `theme.contrast.test.ts:121` to drop the now-mysterious reference, and disposition the STATE.md Pending Todos entry as obsolete-by-redesign.

**Part B — Operator-approved-if-safe substitute (per PATTERNS.md 44-04 cluster "Primitive duplication audit" block):** Extract `SettingsRow` primitive from the 3 existing `Settings*Row` components. Live-code grep confirms all 3 share `<fieldset aria-label className="border-t border-[var(--color-border-soft)] py-3 ...">` + `<span className="text-[15px] font-normal text-[var(--color-breathing-text)]">{label}</span>` + `<child>`. The 3 components become thin adapters wrapping `SettingsRow` with their own child (Stepper / SegmentedControl / Toggle). Mirrors Item E commit `bd22ca5` (PickerCardGrid<T> extracted; 4 pickers → 22-40 LOC adapters; bundle -2.4 KB; behavioral byte-equivalence verified by all 4 picker tests passing unchanged).

Purpose: Close POLISH-05 (refactor pass for redesign-introduced duplication). Honors D-06 "moderate scope" + D-07 "if it doesn't break things" by substituting an obsolete rename target with a real duplication candidate the grep surfaced. The substitution is approved-in-advance by PATTERNS.md and recorded in this plan's reconciliation rationale per CONTEXT D-06 wording reconciliation.

Output: One `refactor(44): extract SettingsRow primitive + reconcile obsolete .orb-layer rename (POLISH-05)` commit + 44-04-SUMMARY.md with bundle delta + reconciliation rationale + behavioral byte-equivalence evidence.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/44-final-polish/44-CONTEXT.md
@.planning/phases/44-final-polish/44-PATTERNS.md
@.planning/REFACTOR-LOOP-STATE.md
@.planning/phases/44-final-polish/44-01-SUMMARY.md
@.planning/phases/44-final-polish/44-02-SUMMARY.md
</context>

<propose_step_checklist>

## MANDATORY (per CONTEXT D-13 + [[propose-step-checklist]])

Before executing any task in this plan, the executor MUST first produce a PROPOSE block leading with:

1. **Downstream Constraints** — What downstream tasks (other Phase 44 clusters, future v2.x work, milestone close, drift-guard tests) might this change constrain? List 2-5 concrete items. **Especially for this plan: the SettingsRow extraction constrains AppSettingsPage / SetupCard / SettingsSheet consumers; record their adapter prop-API stability as a Downstream Constraint.**

2. **Applicable Memory Rules** — Which rules from `~/.claude-personal/projects/-Users-lucindo-Code-hrv/memory/MEMORY.md` apply to this work? At minimum surface (cite by `[[rule-name]]`):
   - `[[propose-step-checklist]]` — meta-rule, always applies
   - `[[no-design-locking]]` — applies if any test edit or token assertion (SettingsRow.test.tsx must NOT use toHaveClass)
   - `[[use-lsp-for-renames]]` — applies if any symbol/property/CSS-class rename (D-07 EXPLICIT for this plan)
   - `[[design-logic-separation]]` — applies if refactor touches any boundary (D-07 EXPLICIT: layout-only, NO logic / state / audio / persistence)
   - `[[ack-dont-fix-inline]]` — applies during operator feedback dumps
   - `[[spike-locked-values-are-not-decisions]]` — applies if any spike-010-locked visual would be touched (extraction preserves visual output byte-for-byte)

3. **THEN** — Goal / Scope / Risk / Verification / Commit message (the standard propose-block content).

No exceptions for "small" cleanup tasks. The Section A + Section B preface is the gate, not the content.

This requirement is operator-locked at CONTEXT D-13 verbatim: "Mandatory propose-step checklist applies to every Phase 44 plan per [[propose-step-checklist]]: Downstream Constraints + Applicable Memory Rules sections BEFORE Goal/Scope/Risk. No exceptions for 'small' cleanup plans."

</propose_step_checklist>

<interfaces>
Key types and contracts the executor needs. Extracted from the 3 Settings*Row source files.
Executor uses these directly — no further codebase exploration required for the extraction shape.

From src/components/SettingsSegmentedRow.tsx, SettingsToggleRow.tsx, SettingsStepper.tsx (live grep — PATTERNS.md verified):
- All three wrap their content in: `<fieldset aria-label={ariaLabel} className="... border-t border-[var(--color-border-soft)] py-3 ...">`
- All three render a label slot: `<span className="text-[15px] font-normal text-[var(--color-breathing-text)]">{label}</span>`
- All three accept a `label: string` prop and an `ariaLabel: string` prop (verify exact prop names by reading the files)
- The unique child is each row's domain control (SegmentedControl / Toggle / Stepper)

Target primitive shape (executor: confirm via Read of all 3 source files in Task 1; the shape below is the planner's proposal subject to live-code confirmation):

```
export interface SettingsRowProps {
  label: string;
  ariaLabel: string;
  children: React.ReactNode;
}

export function SettingsRow({ label, ariaLabel, children }: SettingsRowProps): JSX.Element;
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Reconciliation — update the stale `.orb-layer` comment at theme.contrast.test.ts:121 + disposition STATE.md Pending Todos entry</name>
  <files>
    - src/styles/theme.contrast.test.ts (comment edit at :121)
    - .planning/STATE.md (Pending Todos section — disposition the `.orb-layer` rename entry)
  </files>
  <read_first>
    - .planning/phases/44-final-polish/44-CONTEXT.md (D-06 / D-07 verbatim — the rename specification)
    - .planning/phases/44-final-polish/44-PATTERNS.md ("IMPORTANT FINDING — `.orb-layer--in/--out` rename is OBSOLETE" block — the verbatim reconciliation argument; option (a) recommended)
    - .planning/STATE.md (Pending Todos section lines 62-68 — the entry says "`.orb-layer--in/--out` → `.shape-layer--in/--out` rename for naming consistency")
    - src/styles/theme.contrast.test.ts lines 110-130 (the archived comment block around the now-mysterious `.orb-layer--in/--out` reference)
  </read_first>
  <action>
    Step 1 — Confirm the finding holds at HEAD: run `grep -rn '\.orb-layer\|\.shape-layer' src/ --include="*.css" --include="*.tsx" --include="*.ts"`. Expected: exactly ONE hit at `src/styles/theme.contrast.test.ts:121` (a comment). If MORE hits surface (e.g. a CSS class snuck back in), STOP — surface to operator before proceeding; the reconciliation argument no longer holds.

    Step 2 — Edit `src/styles/theme.contrast.test.ts` at the comment block around :121. Replace the now-mysterious `.orb-layer--in/--out` reference with a self-explanatory description. The comment block currently describes a historical assertion ("It asserted the perceptual distance of an `.orb-layer--in` / `.orb-layer--out`..."). Rewrite to describe the WHY of the assertion's deletion: the orb body is now halos + centre disc per Phase 41 J4 (commit `a742c0b`); the gradient crossfade asserted by the old test no longer exists in the codebase; readers can no longer grep for the class name. Use a tight WHY-comment per Tiger Style.

    Step 3 — Update `.planning/STATE.md` Pending Todos section. The current entry on line 64 reads:
    > "v1.x deferred: `.orb-layer--in/--out` → `.shape-layer--in/--out` rename for naming consistency; per-variant token sets; live idle preview; additional shape variants. (Many of these were obviated by Phase 41's orb rewrite — re-audit at v2.0 close.)"

    Disposition the `.orb-layer` rename portion: split the entry so the rename is marked obsolete-by-redesign with rationale ("CSS classes deleted in Phase 41 J4 commit `a742c0b`; only archived comment at theme.contrast.test.ts:121 remained, updated in 44-04 commit `<SHA>`"). Leave the other items in the entry (per-variant token sets / live idle preview / additional shape variants) untouched unless they too have been obviated.

    Step 4 — Run the per-commit green-gate: `npx tsc --noEmit -p tsconfig.app.json && npm run lint && npm test -- --run && npm run build`. The contrast test must still pass (the comment edit is non-semantic). Test count must equal post-44-02 count.

    DO NOT commit yet — this task's edits combine with Task 2 into a single `refactor(44):` commit per D-02. The combined commit is principled: both reconciliation AND the substitute extraction land together as the POLISH-05 close.
  </action>
  <verify>
    <automated>echo '---ORB-LAYER GREP (should be 1 — the updated comment)---' &amp;&amp; grep -rn '\.orb-layer\|\.shape-layer' src/ --include="*.css" --include="*.tsx" --include="*.ts" ; echo '---STATE.md DISPOSITION CHECK---' &amp;&amp; grep -A2 'orb-layer\|obsolete-by-redesign' .planning/STATE.md | head -10 ; echo '---GREEN GATE---' &amp;&amp; npx tsc --noEmit -p tsconfig.app.json &amp;&amp; npm run lint &amp;&amp; npm test -- --run 2>&amp;1 | tail -3 &amp;&amp; npm run build 2>&amp;1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn '\.orb-layer\|\.shape-layer' src/` returns exactly 1 hit at `src/styles/theme.contrast.test.ts` (the updated comment block) — OR returns 0 hits if the comment was rewritten to remove the literal token entirely
    - The updated comment at theme.contrast.test.ts:~121 describes the WHY of the deletion (orb rewrite → halos + centre disc; gradient crossfade no longer exists) without leaving a now-mysterious class-name token
    - `.planning/STATE.md` Pending Todos section either splits the original entry OR adds a marginal disposition note explicitly marking the `.orb-layer` rename portion `obsolete-by-redesign` per D-05; the rationale cites Phase 41 J4 commit `a742c0b`
    - All 4 green-gate commands exit 0
    - Test pass count equals post-44-02 count from 44-02-SUMMARY.md
    - NO commit yet — combined commit lands in Task 2
  </acceptance_criteria>
  <done>Reconciliation edits staged; tests green; ready for combined commit with Task 2.</done>
</task>

<task type="auto">
  <name>Task 2: Extract SettingsRow primitive + retrofit 3 adapter rows + add primitive unit tests + commit combined refactor</name>
  <files>
    - src/components/SettingsRow.tsx (new)
    - src/components/SettingsRow.test.tsx (new)
    - src/components/SettingsSegmentedRow.tsx (modified — thin adapter)
    - src/components/SettingsToggleRow.tsx (modified — thin adapter)
    - src/components/SettingsStepper.tsx (modified — thin adapter)
    - .planning/phases/44-final-polish/44-04-SUMMARY.md (new)
    - (modified — comment from Task 1) src/styles/theme.contrast.test.ts
    - (modified — disposition from Task 1) .planning/STATE.md
  </files>
  <read_first>
    - .planning/phases/44-final-polish/44-CONTEXT.md (D-06 "moderate scope"; D-07 "if it doesn't break things" + LSP-rename safety; [[design-logic-separation]] / [[use-lsp-for-renames]] memory rules)
    - .planning/phases/44-final-polish/44-PATTERNS.md ("Primitive duplication audit (D-06 'moderate scope') — concrete dedup candidates" block — the grep verification + candidate primitive shape; "LSP-rename safety contract (D-07)" block; "Per-cluster commit (D-02)" block — exact commit message shape)
    - .planning/REFACTOR-LOOP-STATE.md (Item E commit `bd22ca5` — PickerCardGrid<T> extraction analog: 4 pickers → 22-40 LOC adapters, JS bundle -2.4 KB, behavioral byte-equivalence by all 4 picker tests passing unchanged)
    - src/components/SettingsSegmentedRow.tsx (full — extract the exact shared chrome)
    - src/components/SettingsToggleRow.tsx (full — confirm the chrome matches)
    - src/components/SettingsStepper.tsx (full — confirm the chrome matches)
    - src/components/SettingsSegmentedRow.test.tsx (if exists — confirm the prop API + behavioral contract being preserved)
    - src/components/SettingsToggleRow.test.tsx (if exists — same)
    - src/components/SettingsStepper.test.tsx (if exists — same)
  </read_first>
  <action>
    Step 1 — Confirm the dedup candidate at HEAD: run `grep -l 'border-t border-\[var(--color-border-soft)\]' src/components/` and `grep -l 'fieldset' src/components/ --include="*.tsx" | grep -v test`. Both should return the same 3 files: SettingsSegmentedRow.tsx, SettingsToggleRow.tsx, SettingsStepper.tsx. If the grep returns a different set (more or fewer files), the extraction scope changes — record the actual set in 44-04-SUMMARY.md and adjust the extraction to cover ALL files that share the chrome (do not silently shrink scope).

    Step 2 — Read the 3 source files in full. Identify the exact shared chrome: the `<fieldset>` wrapper with its className + the label `<span>` with its className. Identify the prop API each row uses to feed those slots (label, ariaLabel, plus the unique domain control as children).

    Step 3 — Create `src/components/SettingsRow.tsx` with the primitive. Use the interface from the `<interfaces>` block above (confirm by Read in Step 2; adjust if the live-code prop names differ — record any deviation in SUMMARY). The primitive MUST:
    - Wrap children in the exact `<fieldset>` shape currently in the 3 rows (preserves accessibility contract)
    - Render the label `<span>` with the exact className currently used (preserves visual output)
    - Accept `label`, `ariaLabel`, `children` props at minimum (additional props if the live-code grep surfaces them)
    - Be a pure layout primitive — NO state, NO effects, NO event handlers (per [[design-logic-separation]])
    - Have NO inline-style className strings that lock design tokens (per [[no-design-locking]] / D-14) — the CSS variable `var(--color-border-soft)` reference is fine because it's an indirection, but no raw Tailwind utilities that downstream phases might modify

    Step 4 — Create `src/components/SettingsRow.test.tsx`. Per Item E lesson + Item H pattern, write BEHAVIORAL tests, not class-locking tests:
    - "renders fieldset with provided aria-label"
    - "renders label text inside the row"
    - "renders children inside the row"
    Use jest-dom queries by role + accessible name, NOT toHaveClass. Per [[no-design-locking]] / D-14.

    Step 5 — Retrofit each of the 3 adapter rows. Each should shrink to a thin wrapper around `<SettingsRow label={label} ariaLabel={ariaLabel}>{<its-unique-control />}</SettingsRow>`. Remove the duplicated chrome from each. Per Item E ("4 pickers each shrink from ~60 LOC to ~22-40 LOC adapters"), expect ~50% LOC reduction per adapter. Preserve every prop the adapter currently exposes — downstream consumers (`AppSettingsPage`, `SetupCard`, `SettingsSheet`, per primitive consumers) MUST NOT need to change. Behavioral byte-equivalence — every existing adapter test passes UNCHANGED.

    Step 6 — Run the per-commit green-gate: `npx tsc --noEmit -p tsconfig.app.json && npm run lint && npm test -- --run && npm run build`. All four must exit 0. New tests in SettingsRow.test.tsx pass. Existing 3 adapter tests pass UNCHANGED (behavioral byte-equivalence — Item E lesson). Build output's JS bundle size is recorded for the SUMMARY's delta block.

    Step 7 — Commit using PATTERNS.md "Per-cluster commit (D-02)" exact shape, scope prefix `refactor(44):`. The commit message names BOTH the reconciliation (Task 1) AND the extraction (Task 2) — they land together as the POLISH-05 close:

    ```
    refactor(44): extract SettingsRow primitive + reconcile obsolete .orb-layer rename (POLISH-05)

    Two-part POLISH-05 close:

    Part A — CONTEXT D-06's .orb-layer--in/--out → .shape-layer--in/--out rename
    is OBSOLETE per live-code grep (the CSS classes were deleted in Phase 41 J4
    commit `a742c0b`; orb is now halos + centre disc). The only remaining hit
    was an archived comment at src/styles/theme.contrast.test.ts:121 referencing
    the deleted classes; comment rewritten to drop the now-mysterious token.
    STATE.md Pending Todos entry dispositioned obsolete-by-redesign per D-05.

    Part B — SettingsRow primitive extracted from the 3 Settings*Row components
    (Segmented / Toggle / Stepper) which share <fieldset> + label-span chrome
    verbatim. Mirrors Item E commit `bd22ca5` (PickerCardGrid<T> extraction):
    each adapter shrinks to a thin wrapper; behavioral byte-equivalence
    verified by all 3 adapter tests passing unchanged.

    Bundle delta: <before> → <after> KB (<delta>).
    Adapter LOC: SegmentedRow <before>→<after>, ToggleRow <before>→<after>, Stepper <before>→<after>.

    Verified:
      npx tsc --noEmit -p tsconfig.app.json   # clean
      npm run lint                            # 0/0
      npm test -- --run                       # <X> files / <Y> tests pass (delta: +1 file / +3 tests from SettingsRow.test.tsx)
      npm run build                           # clean. JS bundle <delta>.
    ```

    Step 8 — Write 44-04-SUMMARY.md per PATTERNS.md. Include: reconciliation rationale (Part A — verbatim from PATTERNS.md IMPORTANT FINDING block, plus the comment-edit diff + STATE.md disposition), extraction details (Part B — primitive shape + adapter LOC reduction + bundle delta + behavioral byte-equivalence evidence by listing the 3 adapter tests passing unchanged), per-cluster commit SHA.
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.app.json &amp;&amp; npm run lint &amp;&amp; npm test -- --run 2>&amp;1 | tail -5 &amp;&amp; npm run build 2>&amp;1 | tail -5 &amp;&amp; echo '---SETTINGSROW PRIMITIVE EXISTS---' &amp;&amp; test -f src/components/SettingsRow.tsx &amp;&amp; test -f src/components/SettingsRow.test.tsx &amp;&amp; echo '---ADAPTER IMPORTS SETTINGSROW (3 imports expected)---' &amp;&amp; grep -l "from './SettingsRow'\|from '\./SettingsRow'" src/components/SettingsSegmentedRow.tsx src/components/SettingsToggleRow.tsx src/components/SettingsStepper.tsx &amp;&amp; echo '---ORB-LAYER GREP FINAL---' &amp;&amp; grep -rn '\.orb-layer' src/ &amp;&amp; echo '---COMMIT---' &amp;&amp; git log --oneline -3</automated>
  </verify>
  <acceptance_criteria>
    - All 4 green-gate commands exit 0
    - `src/components/SettingsRow.tsx` exists with exported `SettingsRow` function (or named export equivalent)
    - `src/components/SettingsRow.test.tsx` exists with at least 3 behavioral tests (aria contract + label rendering + children rendering); ZERO `toHaveClass(...)` assertions (per [[no-design-locking]] / D-14)
    - All 3 adapter files (SettingsSegmentedRow.tsx, SettingsToggleRow.tsx, SettingsStepper.tsx) import from `./SettingsRow` and wrap their unique control in it
    - All 3 adapter LOC reduced (record before/after numbers in SUMMARY — Item E precedent is ~50% reduction)
    - All EXISTING tests for the 3 adapter files pass UNCHANGED (behavioral byte-equivalence — Item E lesson; the test files in `src/components/Settings*Row.test.tsx` and `src/components/SettingsStepper.test.tsx` if they exist should not need edits)
    - `grep -rn '\.orb-layer' src/` returns 0 hits OR the documented updated-comment hit only
    - `git log --oneline -3` shows a single `refactor(44):` commit whose body names BOTH Part A (reconciliation) and Part B (extraction)
    - 44-04-SUMMARY.md exists with: Part A reconciliation rationale (citing PATTERNS.md IMPORTANT FINDING block + STATE.md disposition), Part B extraction details (primitive shape + adapter LOC reduction + bundle delta + tests-unchanged proof), commit SHA, green-gate evidence
    - NO logic / state / audio / persistence files were modified (only `src/components/Settings*Row*.tsx` + `src/styles/theme.contrast.test.ts` comment + `.planning/STATE.md`) per [[design-logic-separation]]
  </acceptance_criteria>
  <done>POLISH-05 closed: reconciliation landed; SettingsRow primitive extracted; behavioral byte-equivalence preserved; STATE.md cleaned.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| primitive extraction → existing adapter consumers | The 3 adapters (Segmented / Toggle / Stepper) are consumed by AppSettingsPage / SetupCard / SettingsSheet / etc. The prop API at the adapter level MUST stay unchanged; only the internal implementation changes. Per-commit green-gate (test) catches any prop-API drift. |
| comment edit → archived test rationale | The theme.contrast.test.ts:121 edit is a comment-only change; the test's behavior is unchanged. |
| STATE.md disposition → bookkeeping integrity | Updating the Pending Todos entry follows D-05 obsolete-by-redesign threshold with explicit commit-pin citation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-44-04-01 | Tampering | adapter prop-API drift during extraction breaks downstream consumers (AppSettingsPage / SetupCard / SettingsSheet) | mitigate | Per-commit green-gate; behavioral byte-equivalence assertion — all 3 existing adapter tests pass UNCHANGED (Item E precedent) |
| T-44-04-02 | Tampering | accidental logic / state / audio change during what should be a layout-only refactor | mitigate | Files-modified list is bounded to layout files + 1 comment edit + STATE.md; explicit acceptance criterion that NO logic / state / audio / persistence files were modified per [[design-logic-separation]] |
| T-44-04-03 | Tampering | re-introduction of design-token-locking assertions in SettingsRow.test.tsx | mitigate | Explicit acceptance criterion: ZERO `toHaveClass(...)` in SettingsRow.test.tsx; behavioral tests only per Item H pattern (D-14 / [[no-design-locking]]) |
| T-44-04-04 | Repudiation | unclear which commit closed the obsolete `.orb-layer` STATE.md entry | mitigate | STATE.md disposition update cites the upcoming commit SHA; combined `refactor(44):` commit body names both Part A and Part B; SUMMARY records full rationale |
| T-44-04-SC | Tampering | no package installs in this plan | accept | Pure refactor + comment edit + STATE.md edit; no dependency surface change |

</threat_model>

<verification>

- `grep -rn '\.orb-layer' src/` returns ≤ 1 hit (the updated comment) or 0
- STATE.md Pending Todos entry for `.orb-layer` rename dispositioned obsolete-by-redesign with commit citation
- `src/components/SettingsRow.tsx` exists and exports the primitive
- `src/components/SettingsRow.test.tsx` exists with 0 `toHaveClass` assertions
- All 3 adapter files import from `./SettingsRow`
- Per-commit green-gate green (tsc + lint + test + build)
- `git log --oneline -1` shows the combined `refactor(44):` commit
- Bundle size delta recorded in SUMMARY

</verification>

<success_criteria>

POLISH-05: ✅ when (a) the `.orb-layer` reconciliation lands (comment updated + STATE.md dispositioned) AND (b) `SettingsRow` primitive extracted with 3 adapters refactored AND (c) behavioral byte-equivalence verified (existing adapter tests unchanged) AND (d) per-commit green-gate green.

</success_criteria>

<output>
Create `.planning/phases/44-final-polish/44-04-SUMMARY.md` when done. Include:
- Part A reconciliation: PATTERNS.md IMPORTANT FINDING summary + theme.contrast.test.ts comment diff + STATE.md disposition before/after
- Part B extraction: primitive shape + adapter LOC reduction per file + JS bundle delta + behavioral byte-equivalence proof (3 adapter tests passing unchanged)
- Per-cluster commit SHA + diff-stat
- Confirmation that no logic / state / audio / persistence files were modified per [[design-logic-separation]]
</output>
</content>
