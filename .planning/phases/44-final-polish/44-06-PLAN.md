---
phase: 44-final-polish
plan: 06
type: execute
wave: 6
depends_on: ["44-01", "44-02", "44-03", "44-04", "44-05"]
files_modified:
  - .planning/phases/44-final-polish/44-SECURITY.md
autonomous: true
requirements:
  - POLISH-06
user_setup: []

must_haves:
  truths:
    - "`/gsd:secure-phase 44` runs against the full v2.0 milestone changed-file surface (commits since the v1.5 ship at ~2026-05-19)"
    - "44-SECURITY.md output covers all 3 D-15 attack surfaces: preview audio path (`src/audio/previewContext.ts`, Phase 40), query-string dev toggles (`?breathingShape=` + `?orbIdle=` via `src/featureFlags.ts`, Phase 41 J5/J6), font asset hosting (`@fontsource-variable/inter` woff2 bundling via Workbox precache, Phase 41 J2)"
    - "Threat register inherits Phase 40's 11 threats verbatim for the preview audio path (40-SECURITY.md status: verified) AND adds NEW registers for query-string + font asset surfaces"
    - "44-SECURITY.md frontmatter: status `verified`, threats_open: 0, asvs_level: 2 (inheriting Phase 40 level)"
    - "Body sections present: Trust Boundaries / Threat Register / Accepted Risks Log / Security Audit Trail / Sign-Off (per 40-SECURITY.md analog)"
  artifacts:
    - path: ".planning/phases/44-final-polish/44-SECURITY.md"
      provides: "Cumulative v2.0 milestone security review with STRIDE threat register + per-threat disposition + audit trail + sign-off"
    - path: ".planning/phases/44-final-polish/44-06-SUMMARY.md"
      provides: "Per-cluster commit SHA + threat count rollup + inheritance map (which threats from prior SECURITY.md were re-verified) + new threats added in 44-06"
  key_links:
    - from: "44-SECURITY.md preview-audio threat rows"
      to: ".planning/phases/40-timbre-preview-cue/40-SECURITY.md"
      via: "inheritance — Phase 40 threats T-40-01..T-40-11 re-verified verbatim with Phase 44 re-verification timestamps"
      pattern: "T-40-[0-9]{2}"
    - from: "44-SECURITY.md query-string threat rows"
      to: "src/featureFlags.ts"
      via: "NEW threat register — query-string parser is the entry point"
      pattern: "parseQueryFeatureFlag\\|queryFeatureFlag"
    - from: "44-SECURITY.md font asset threat rows"
      to: "@fontsource-variable/inter precache config"
      via: "NEW threat register — font bundling via Workbox is the attack surface"
      pattern: "@fontsource-variable/inter\\|fontsource"
---

<objective>
Run `/gsd:secure-phase 44` against the full v2.0 milestone changed-file surface (Phases 36–41). Output `.planning/phases/44-final-polish/44-SECURITY.md` covering the 3 D-15 attack surfaces:

1. **Preview audio path** (`src/audio/previewContext.ts`, Phase 40, J5) — already covered by `40-SECURITY.md` (11 threats, ASVS L2, status `verified`); 44-06 inherits the disposition and re-verifies it still holds.
2. **Query-string dev toggles** (`?breathingShape=` and `?orbIdle=`, Phase 41 J5/J6, wired via `src/featureFlags.ts`) — NO prior SECURITY.md covers this (Phase 41 used spike-loop format and didn't produce one). Threat model is NEW for 44-06.
3. **Font asset hosting** (`@fontsource-variable/inter` woff2 bundling via Workbox precache, Phase 41 J2) — NO prior SECURITY.md covers this. Threat model is NEW for 44-06.

Purpose: Close POLISH-06 (security re-review across the full milestone surface). Inherits Phase 40's verified disposition for the preview-audio surface and constructs new threat registers for the two surfaces Phase 41 introduced without producing a SECURITY.md.

Output: `44-SECURITY.md` with frontmatter `status: verified, threats_open: 0` + body sections (Trust Boundaries / Threat Register / Accepted Risks Log / Security Audit Trail / Sign-Off). Per-cluster commit `docs(44): security review for v2.0 milestone surface (POLISH-06)`. 44-06-SUMMARY.md with threat count rollup.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/44-final-polish/44-CONTEXT.md
@.planning/phases/44-final-polish/44-PATTERNS.md
@.planning/phases/40-timbre-preview-cue/40-SECURITY.md
@.planning/phases/40-timbre-preview-cue/40-CONTEXT.md
@.planning/phases/41-spike-mono-zen/41-CONTEXT.md
</context>

<propose_step_checklist>

## MANDATORY (per CONTEXT D-13 + [[propose-step-checklist]])

Before executing any task in this plan, the executor MUST first produce a PROPOSE block leading with:

1. **Downstream Constraints** — What downstream tasks (other Phase 44 clusters, future v2.x work, milestone close, drift-guard tests) might this change constrain? List 2-5 concrete items. **Especially for this plan: 44-SECURITY.md inheritance from 40-SECURITY.md depends on HEAD `previewContext.ts` matching the surface Phase 40 analyzed — record any HEAD drift as a Downstream Constraint.**

2. **Applicable Memory Rules** — Which rules from `~/.claude-personal/projects/-Users-lucindo-Code-hrv/memory/MEMORY.md` apply to this work? At minimum surface (cite by `[[rule-name]]`):
   - `[[propose-step-checklist]]` — meta-rule, always applies
   - `[[no-design-locking]]` — N/A this plan (doc-only)
   - `[[use-lsp-for-renames]]` — N/A this plan (doc-only)
   - `[[design-logic-separation]]` — N/A this plan (doc-only)
   - `[[ack-dont-fix-inline]]` — applies during operator feedback dumps
   - `[[spike-locked-values-are-not-decisions]]` — applies if any spike-010-locked visual would be touched

3. **THEN** — Goal / Scope / Risk / Verification / Commit message (the standard propose-block content).

No exceptions for "small" cleanup tasks. The Section A + Section B preface is the gate, not the content.

This requirement is operator-locked at CONTEXT D-13 verbatim: "Mandatory propose-step checklist applies to every Phase 44 plan per [[propose-step-checklist]]: Downstream Constraints + Applicable Memory Rules sections BEFORE Goal/Scope/Risk. No exceptions for 'small' cleanup plans."

</propose_step_checklist>

<tasks>

<task type="auto">
  <name>Task 1: Pre-flight — build the v2.0 changed-file set + confirm the 3 D-15 attack surfaces exist at HEAD</name>
  <files>
    - (read-only) git log + src/ paths
    - .planning/phases/44-final-polish/44-06-CHANGED-FILES.md (new — internal scoping artifact; absorbed into SUMMARY at Task 3)
  </files>
  <read_first>
    - .planning/phases/44-final-polish/44-CONTEXT.md (D-15 verbatim — 3 attack surfaces named)
    - .planning/phases/44-final-polish/44-PATTERNS.md ("Phase 44-06 threat scope (D-15 verbatim)" block; "Threat model construction (per D-15 wording)" block — changed-file-set boundary command)
    - .planning/phases/40-timbre-preview-cue/40-SECURITY.md (full — the verbatim 11-threat register to inherit for surface #1)
    - .planning/phases/40-timbre-preview-cue/40-CONTEXT.md (the preview-context construction discipline; D-01/D-02/D-03 latency / iOS-suspension / synchronous-call-path WHYs)
    - .planning/phases/41-spike-mono-zen/41-CONTEXT.md ("ORB-05 / ORB-06 — dev toggles use query-string params, NOT VITE_* build-time env vars" — surface #2 background; J2 font system background — surface #3)
    - src/featureFlags.ts (surface #2 — confirm the query-string parser entry point and what it consumes the URL parameter for)
    - src/audio/previewContext.ts (surface #1 — re-confirm 40-SECURITY.md disposition still holds at HEAD)
    - package.json (confirm `@fontsource-variable/inter` is still bundled — surface #3 attack vector)
  </read_first>
  <action>
    Step 1 — Build the v2.0 changed-file set per PATTERNS.md "Threat model construction" verbatim command:
    ```
    git log --since=2026-05-20 --name-only --pretty=format: | sort -u | grep -E '^src/'
    ```
    Record into `.planning/phases/44-final-polish/44-06-CHANGED-FILES.md` as a flat list. This is the threat-surface boundary — every file in the list is in scope; files OUTSIDE the list are not in 44-06 scope (per D-15 "the full milestone surface").

    Step 2 — Confirm each of the 3 D-15 attack surfaces exists at HEAD:
    - Surface #1: `ls src/audio/previewContext.ts` exists; the file's D-01/D-02/D-03 comment block is intact (per 44-03 sweep should NOT have touched it — verify)
    - Surface #2: `grep -n 'parseQueryFeatureFlag\|featureFlag' src/featureFlags.ts` — the query-string parser exists; identify which URL parameters it consumes (`?breathingShape=`, `?orbIdle=`) and what code consumes its output (OrbShape / OrbContainer per PATTERNS.md)
    - Surface #3: `grep -n '@fontsource-variable/inter' package.json src/styles/ src/main.tsx src/main.ts` (whichever files import it) — confirms the font is bundled; check `vite.config.ts` / `workbox.config.*` for the precache wiring (per CONTEXT 41 J2)

    Step 3 — Record findings in `44-06-CHANGED-FILES.md` per attack surface — for each, list:
    - Surface name (1 / 2 / 3)
    - Files-at-HEAD that constitute the surface
    - Prior SECURITY.md inheritance (1 = 40-SECURITY.md; 2 = none; 3 = none)
    - Brief structural description (what crosses the boundary: gesture-only signal / URL-borne strings / binary font payload)
    - Any HEAD-vs-prior-SECURITY drift (Surface 1 only) — did Phase 41+ change `previewContext.ts`? If yes, the inheritance is partial and 44-06 has additional review work for that delta.
  </action>
  <verify>
    <automated>test -f .planning/phases/44-final-polish/44-06-CHANGED-FILES.md &amp;&amp; head -20 .planning/phases/44-final-polish/44-06-CHANGED-FILES.md ; echo '---SURFACE 1 PREVIEW-CONTEXT EXISTS---' &amp;&amp; ls -la src/audio/previewContext.ts ; echo '---SURFACE 2 FEATURE-FLAGS EXISTS---' &amp;&amp; ls -la src/featureFlags.ts &amp;&amp; grep -n 'breathingShape\|orbIdle' src/featureFlags.ts | head -5 ; echo '---SURFACE 3 FONT BUNDLED---' &amp;&amp; grep -n '@fontsource-variable/inter' package.json ; echo '---PHASE 40 SECURITY.MD STATUS---' &amp;&amp; head -8 .planning/phases/40-timbre-preview-cue/40-SECURITY.md</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/44-final-polish/44-06-CHANGED-FILES.md` exists with a flat list of v2.0-changed files under `src/`
    - Surface #1 (preview audio path) confirmed: `src/audio/previewContext.ts` exists; the D-01/D-02/D-03 comment block intact at HEAD
    - Surface #2 (query-string dev toggles) confirmed: `src/featureFlags.ts` exists and contains references to `breathingShape` and `orbIdle`
    - Surface #3 (font asset hosting) confirmed: `@fontsource-variable/inter` appears in `package.json` dependencies
    - 44-06-CHANGED-FILES.md records inheritance status for each surface (1 inherits 40-SECURITY.md; 2 + 3 are NEW)
    - HEAD-vs-40-SECURITY.md drift assessed for Surface #1 (does HEAD's previewContext.ts match the surface 40-SECURITY.md analyzed?)
  </acceptance_criteria>
  <done>Changed-file set materialized; 3 attack surfaces verified at HEAD; inheritance map drafted.</done>
</task>

<task type="auto">
  <name>Task 2: Invoke `/gsd:secure-phase 44` agent + assemble 44-SECURITY.md</name>
  <files>
    - .planning/phases/44-final-polish/44-SECURITY.md (new — the agent output)
  </files>
  <read_first>
    - .planning/phases/44-final-polish/44-PATTERNS.md ("Frontmatter shape (target — `40-SECURITY.md:1-8`)" block; "Body section outline (mirror `40-SECURITY.md`)" block; "Per-cluster commit (D-02)" block at end of cluster 44-06)
    - .planning/phases/40-timbre-preview-cue/40-SECURITY.md (FULL — copy the structure verbatim, adjust phase + add 2 new threat registers)
    - .planning/milestones/v1.1-phases/13-inner-ring-ux-symmetry/13-SECURITY.md (per PATTERNS.md secondary analog — canonical "verified, threats_open: 0" frontmatter)
    - .planning/phases/44-final-polish/44-06-CHANGED-FILES.md (from Task 1 — the threat surface boundary)
  </read_first>
  <action>
    Invoke `/gsd:secure-phase 44` agent. The agent reads the Phase 44 PLAN files, the changed-file set from Task 1, and the prior SECURITY.md (40-SECURITY.md) and produces `44-SECURITY.md` with the frontmatter + body sections per PATTERNS.md.

    Frontmatter target shape (verbatim from PATTERNS.md "Frontmatter shape" block):
    ```yaml
    ---
    phase: 44
    slug: final-polish
    status: verified         # or 'open' if threats_open > 0
    threats_open: 0          # target — close all in this audit
    asvs_level: 2            # inherit Phase 40 level; bump to 3 only if operator requires
    created: 2026-05-25
    ---
    ```

    Body section outline (verbatim from PATTERNS.md "Body section outline (mirror 40-SECURITY.md)" block):
    - `# Phase 44 — Security`
    - `## Trust Boundaries` — table with 4 rows from PATTERNS.md "Threat model construction" block (gesture→AudioContext / URL-query→render / network→sw-cache→DOM / in-process TS-narrowed)
    - `## Threat Register` — STRIDE-categorized table; inherit 11 from 40-SECURITY.md verbatim (T-40-01..T-40-11) with re-verification timestamps; add ~5-10 NEW threats for query-string + font-asset surfaces (T-44-06-01..T-44-06-NN). Each row: Threat ID | Category | Component | Disposition (mitigate/accept/transfer) | Mitigation | Status
    - `## Accepted Risks Log` — AR-44-NN rows for each `accept` disposition (Phase 40 inherited + any new)
    - `## Security Audit Trail` — 1 row: `2026-05-25 | <N total> | <N closed> | 0 | gsd-security-auditor (ASVS L2)`
    - `## Sign-Off` — checkbox list + **Approval:** verified 2026-05-25

    Per D-15 the 3 new-threat focus areas drive new register entries:

    Query-string parser threats to enumerate (the agent will produce — this list is a planner-side prompt for completeness):
    - Tampering — malformed query string crashes featureFlags parser
    - Tampering — URL parameter triggers unexpected feature flag combination (e.g. orb idle ambient + breathing shape V2 — does this cause visual breakage?)
    - Spoofing — adversarial URL shared via link mass-applies a dev-only orb variant to unsuspecting user (low-severity — visual-only impact, no data exfiltration)
    - Information Disclosure — URL parameters appear in browser history / referer headers / shared screenshots (low risk — values are visual mode names, not secrets)

    Font asset threats to enumerate:
    - Tampering — woff2 file integrity in Workbox precache (mitigated by Workbox precache hash-checking by default)
    - Information Disclosure — fontsource-variable/inter is a public package; no PII surface
    - Denial of Service — font payload size impacts initial load (per CONTEXT 41 J2 the bundle is bounded; per POLISH-08 still zero net-new runtime deps)
    - Spoofing — supply-chain risk on `@fontsource-variable/inter` package (transitively trusted; npm signature + lockfile pin mitigates)

    All threats MUST have a disposition (mitigate / accept / transfer); `mitigate` rows MUST cite specific implementation (e.g. "Workbox precache integrity hash; cf. workbox.config.* line X"); `accept` rows MUST cite rationale.

    Target outcome: `threats_open: 0` (every threat dispositioned). If the agent surfaces a Tampering threat with no clear mitigation at HEAD, surface to operator BEFORE writing `status: verified` — Phase 44 close gate requires zero open threats per POLISH-06 ROADMAP wording.
  </action>
  <verify>
    <automated>test -f .planning/phases/44-final-polish/44-SECURITY.md &amp;&amp; head -10 .planning/phases/44-final-polish/44-SECURITY.md ; echo '---REQUIRED SECTIONS---' &amp;&amp; grep -c '^## Trust Boundaries\|^## Threat Register\|^## Accepted Risks Log\|^## Security Audit Trail\|^## Sign-Off' .planning/phases/44-final-polish/44-SECURITY.md ; echo '---INHERITED T-40 THREATS COUNT---' &amp;&amp; grep -c 'T-40-' .planning/phases/44-final-polish/44-SECURITY.md ; echo '---NEW T-44-06 THREATS COUNT---' &amp;&amp; grep -c 'T-44-06-' .planning/phases/44-final-polish/44-SECURITY.md ; echo '---OPEN THREATS COUNT FROM FRONTMATTER---' &amp;&amp; grep '^threats_open:' .planning/phases/44-final-polish/44-SECURITY.md ; echo '---STATUS FROM FRONTMATTER---' &amp;&amp; grep '^status:' .planning/phases/44-final-polish/44-SECURITY.md</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/44-final-polish/44-SECURITY.md` exists
    - Frontmatter contains all 6 required keys: `phase: 44`, `slug: final-polish`, `status` (verified or open), `threats_open: 0` (target — must be 0 for close), `asvs_level: 2`, `created: 2026-05-25`
    - Body contains all 5 required sections: `## Trust Boundaries`, `## Threat Register`, `## Accepted Risks Log`, `## Security Audit Trail`, `## Sign-Off`
    - Threat Register inherits at least 11 T-40-* rows (Phase 40 verbatim) — confirm via grep count
    - Threat Register adds at least 5 new T-44-06-* rows (query-string + font-asset coverage)
    - Every threat has a disposition cell populated (mitigate / accept / transfer)
    - Every `mitigate` row cites specific implementation (not generic advice — per planner_authority_limits "Mitigations reference specific implementation (not generic advice)")
    - Status MUST be `verified` AND `threats_open: 0` — if not, the close criterion fails and the agent run is incomplete; surface to operator
  </acceptance_criteria>
  <done>44-SECURITY.md assembled per PATTERNS.md shape; status verified; 0 open threats.</done>
</task>

<task type="auto">
  <name>Task 3: Commit 44-SECURITY.md + write 44-06-SUMMARY.md</name>
  <files>
    - .planning/phases/44-final-polish/44-06-SUMMARY.md (new)
    - (delete after commit) .planning/phases/44-final-polish/44-06-CHANGED-FILES.md
  </files>
  <read_first>
    - .planning/phases/44-final-polish/44-PATTERNS.md ("Per-cluster commit (D-02)" block at end of cluster 44-06)
    - .planning/phases/44-final-polish/44-SECURITY.md (from Task 2 — the artifact to commit)
    - .planning/phases/44-final-polish/44-06-CHANGED-FILES.md (from Task 1 — absorbed into SUMMARY)
  </read_first>
  <action>
    Run per-commit green-gate (sanity): `npx tsc --noEmit -p tsconfig.app.json && npm run lint && npm test -- --run && npm run build`. This plan only touches `.planning/` artifacts so the gate is procedural — all 4 must still exit 0.

    Commit using PATTERNS.md "Per-cluster commit (D-02)" verbatim shape:
    ```
    docs(44): security review for v2.0 milestone surface (POLISH-06)

    /gsd:secure-phase 44 against the full Phase 36–41 changed-file set.
    Threat surfaces in scope per CONTEXT D-15:
      - Preview audio path (src/audio/previewContext.ts) — inherits 40-SECURITY.md
      - Query-string dev toggles (?breathingShape=, ?orbIdle=) — new register
      - Font asset hosting (@fontsource-variable/inter via Workbox precache) — new register

    Output: 44-SECURITY.md (status: verified, threats_open: 0)
    ```

    Write 44-06-SUMMARY.md including: threat count rollup (inherited T-40-* count + new T-44-06-* count + total + open=0), inheritance map (Surface 1 inherits from 40-SECURITY.md — link; Surface 2/3 are NEW — list the new threat IDs), per-cluster commit SHA, green-gate evidence, sign-off confirmation. Delete `44-06-CHANGED-FILES.md` (absorbed into SUMMARY).
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.app.json &amp;&amp; npm run lint &amp;&amp; npm test -- --run 2>&amp;1 | tail -3 &amp;&amp; npm run build 2>&amp;1 | tail -3 ; echo '---COMMIT---' &amp;&amp; git log --oneline -3 ; echo '---SUMMARY EXISTS---' &amp;&amp; test -f .planning/phases/44-final-polish/44-06-SUMMARY.md &amp;&amp; head -20 .planning/phases/44-final-polish/44-06-SUMMARY.md</automated>
  </verify>
  <acceptance_criteria>
    - All 4 green-gate commands exit 0
    - `git log --oneline -3` shows a `docs(44):` prefixed commit naming the security review
    - 44-06-SUMMARY.md exists and contains: threat count rollup (inherited / new / total / open=0), inheritance map, per-cluster commit SHA, green-gate evidence, sign-off confirmation
    - `.planning/phases/44-final-polish/44-06-CHANGED-FILES.md` deleted (absorbed into SUMMARY) OR retained-with-rationale
  </acceptance_criteria>
  <done>POLISH-06 closed: 44-SECURITY.md committed; sign-off complete; SUMMARY records the rollup.</done>
</task>

</tasks>

<threat_model>

> Per [[security_enforcement]] this plan IS the security review for the broader v2.0 milestone. The threat model below identifies trust boundaries crossed by THIS plan's own activity (the doc-creation process).

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| security-agent output → 44-SECURITY.md | The `/gsd:secure-phase` agent runs against the v2.0 changed-file set and writes the SECURITY.md; the human operator at PR-time is the final check. |
| inheritance from 40-SECURITY.md → 44-SECURITY.md | 40-SECURITY.md status `verified` is inherited; if HEAD's `previewContext.ts` has drifted from the file 40-SECURITY.md analyzed, inheritance is partial and the delta requires fresh review. |
| commit → main | Per-commit green-gate (tsc + lint + test + build) is the structural gate; this plan only touches `.planning/` so the gate is procedural. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-44-06-meta-01 | Information Disclosure | 44-SECURITY.md publicly enumerates the milestone's attack surfaces | accept | Single-developer + Claude workflow per PROJECT.md; the audit-trail value of a public SECURITY.md outweighs the disclosure risk; identical disposition to 40-SECURITY.md |
| T-44-06-meta-02 | Tampering | inheritance from 40-SECURITY.md is silently broken by HEAD drift on `previewContext.ts` | mitigate | Task 1 explicit HEAD-vs-prior-SECURITY drift assessment for Surface #1; if drift exists, 44-SECURITY.md records the delta and the new threats it surfaces |
| T-44-06-meta-03 | Tampering | new threats added in 44-06 (query-string + font asset) miss attack vectors the planner-side prompt didn't enumerate | mitigate | The security-agent runs against the full changed-file set, not just the 3 named surfaces — any other surface it surfaces is added to the register |
| T-44-06-SC | Tampering | no package installs in this plan | accept | Doc-only plan; no dependency surface change |

</threat_model>

<verification>

- 44-SECURITY.md frontmatter `status: verified` AND `threats_open: 0`
- All 5 required body sections present
- ≥11 T-40-* inherited rows (Phase 40 verbatim)
- ≥5 new T-44-06-* rows
- Every threat has disposition + mitigation/rationale
- `docs(44):` commit landed
- 44-06-SUMMARY.md records the rollup

</verification>

<success_criteria>

POLISH-06: ✅ when 44-SECURITY.md status = `verified`, threats_open = 0, all 3 D-15 surfaces covered (1 inherited + 2 new registers), per-cluster commit landed, SUMMARY records the rollup.

</success_criteria>

<output>
Create `.planning/phases/44-final-polish/44-06-SUMMARY.md` when done. Include:
- Threat count rollup (inherited T-40-* count + new T-44-06-* count + total + open=0)
- Inheritance map (Surface 1 ← 40-SECURITY.md / Surface 2 + 3 NEW with new threat IDs)
- Per-cluster commit SHA + diff-stat
- Green-gate evidence
- Sign-off confirmation (the 44-SECURITY.md `## Sign-Off` section)
</output>
</content>
