---
phase: 44-final-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/44-final-polish/44-REVIEW.md
  - .planning/phases/44-final-polish/44-REVIEW-FIX.md
  - .planning/phases/44-final-polish/44-INFO-FINDINGS.md
  - src/**/*  # exact files determined by /gsd:code-review --all --fix output
autonomous: true
requirements:
  - POLISH-01
  - POLISH-02
user_setup: []

must_haves:
  truths:
    - "`/gsd:code-review --all` against current `main` produces a recoverable Info-severity finding list whose count is reconciled against the historical 28 from the 2026-05-16 deep review"
    - "Every Warning-severity finding surfaced by the sweep is either fixed in the mega-commit or explicitly dispositioned in 44-INFO-FINDINGS.md"
    - "Each of the 28 historical Info-severity findings (or the reconciled delta) carries a fix / defer-with-reason / obsolete-by-redesign disposition with rationale"
    - "The mega-commit boundary stays scoped to POLISH-01 + POLISH-02 only — no test-name edits, no Tiger Style comment edits, no rename, no security review, no readability rewrites cross into this commit"
    - "Per-commit green-gate (tsc + lint + test + build) passes on the mega-commit"
  artifacts:
    - path: ".planning/phases/44-final-polish/44-REVIEW.md"
      provides: "Full-codebase /gsd:code-review --all output with frontmatter findings.{critical,warning,info,total}"
    - path: ".planning/phases/44-final-polish/44-REVIEW-FIX.md"
      provides: "Per-finding fix log with files_modified + commit pin"
    - path: ".planning/phases/44-final-polish/44-INFO-FINDINGS.md"
      provides: "28-row (or reconciled-delta) disposition table per D-04 exact schema"
  key_links:
    - from: "44-INFO-FINDINGS.md rows with Disposition=fix"
      to: "the mega-commit SHA"
      via: "Commit hash column in the table"
      pattern: "[0-9a-f]{7,40}"
    - from: "44-REVIEW.md frontmatter status"
      to: "44-INFO-FINDINGS.md preamble (delta reconciliation against historical 28)"
      via: "count cross-reference"
      pattern: "info:\\s*[0-9]+"
---

<objective>
Run `/gsd:code-review --all --fix` against the full `src/` tree, land all Warning-severity fixes plus the operator-approved subset of Info-severity fixes as a single mega-commit (D-01), and produce `44-INFO-FINDINGS.md` — the per-finding disposition table for the 28 Info-severity findings from the 2026-05-16 deep review (D-02 / D-03 / D-04 / D-05).

Purpose: Close POLISH-01 (zero Warning at milestone close) and POLISH-02 (every Info finding dispositioned with rationale) — the two most surface-broad requirements of Phase 44. The mega-commit shape is locked by CONTEXT D-01 (operator decision: "one big sweep" not per-cluster sharding); the bounded scope is locked by D-02 (POLISH-03/04/05/06/07 each commit per cluster downstream).

Output: `44-REVIEW.md` (raw reviewer output) + `44-REVIEW-FIX.md` (per-finding fix log) + `44-INFO-FINDINGS.md` (28-row disposition table) + one `fix(44): code-review --all --fix sweep` mega-commit.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/44-final-polish/44-CONTEXT.md
@.planning/phases/44-final-polish/44-PATTERNS.md
@.planning/phases/36-housekeeping-bookkeeping-reset/36-REVIEW.md
@.planning/phases/36-housekeeping-bookkeeping-reset/36-REVIEW-FIX.md
@.planning/phases/36-housekeeping-bookkeeping-reset/deferred-items.md
</context>

<propose_step_checklist>

## MANDATORY (per CONTEXT D-13 + [[propose-step-checklist]])

Before executing any task in this plan, the executor MUST first produce a PROPOSE block leading with:

1. **Downstream Constraints** — What downstream tasks (other Phase 44 clusters, future v2.x work, milestone close, drift-guard tests) might this change constrain? List 2-5 concrete items.

2. **Applicable Memory Rules** — Which rules from `~/.claude-personal/projects/-Users-lucindo-Code-hrv/memory/MEMORY.md` apply to this work? At minimum surface (cite by `[[rule-name]]`):
   - `[[propose-step-checklist]]` — meta-rule, always applies
   - `[[no-design-locking]]` — applies if any test edit or token assertion
   - `[[use-lsp-for-renames]]` — applies if any symbol/property/CSS-class rename
   - `[[design-logic-separation]]` — applies if refactor touches any boundary
   - `[[ack-dont-fix-inline]]` — applies during operator feedback dumps
   - `[[spike-locked-values-are-not-decisions]]` — applies if any spike-010-locked visual would be touched

3. **THEN** — Goal / Scope / Risk / Verification / Commit message (the standard propose-block content).

No exceptions for "small" cleanup tasks. The Section A + Section B preface is the gate, not the content.

This requirement is operator-locked at CONTEXT D-13 verbatim: "Mandatory propose-step checklist applies to every Phase 44 plan per [[propose-step-checklist]]: Downstream Constraints + Applicable Memory Rules sections BEFORE Goal/Scope/Risk. No exceptions for 'small' cleanup plans."

</propose_step_checklist>

<tasks>

<task type="auto">
  <name>Task 1: Pre-flight verification — confirm CONTEXT baseline and recover the 28-finding count provenance</name>
  <files>
    - (read-only) package.json
    - (read-only) src/** (for `npm run lint` / `npx tsc` / `npm test -- --run`)
    - (read-only) .planning/PROJECT.md, .planning/STATE.md (grep for "28 Info" provenance)
  </files>
  <read_first>
    - .planning/phases/44-final-polish/44-CONTEXT.md (D-03 finding-recovery steps + baseline section at commit `580dc53`)
    - .planning/phases/44-final-polish/44-PATTERNS.md ("Pre-flight verification — baseline matches CONTEXT (D-11)" block)
    - .planning/phases/36-housekeeping-bookkeeping-reset/deferred-items.md (the original 53-error lint-debt log — confirms the Phase 41 resolution)
    - package.json (confirm `dependencies` is `react` + `react-dom` + `@fontsource-variable/inter` asset only)
  </read_first>
  <action>
    Confirm the CONTEXT.md baseline holds at the current HEAD before the mega-commit lands. Run the three gate commands in sequence:

    1. `npm run lint` — expect 0 errors, 0 warnings (was 53/3 in deferred-items.md; resolved during Phase 41 per CONTEXT D-11).
    2. `npx tsc --noEmit -p tsconfig.app.json` — expect clean.
    3. `npm test -- --run` — expect 107 files / 1155 tests pass.

    Then recover the 28-finding provenance per D-03: `grep -rn "28 Info" .planning/` to confirm which document holds the count (likely PROJECT.md §v1.x Carry-Forwards or v1.5-MILESTONE-AUDIT.md). Record the source path in the task SUMMARY so the next task's `44-INFO-FINDINGS.md` preamble can cite it.

    If any of the three gates is dirty before starting, STOP — the mega-commit must not muddy the baseline; surface as a CONTEXT clarification.
  </action>
  <verify>
    <automated>npm run lint 2>&amp;1 | tail -3 ; npx tsc --noEmit -p tsconfig.app.json 2>&amp;1 | tail -3 ; npm test -- --run 2>&amp;1 | tail -5 ; grep -rn "28 Info" .planning/ | head -5</automated>
  </verify>
  <acceptance_criteria>
    - `npm run lint` exits 0 with output containing "0 errors" and "0 warnings" (or equivalent silent-clean output)
    - `npx tsc --noEmit -p tsconfig.app.json` exits 0 with empty stdout
    - `npm test -- --run` exits 0; final line reports `Test Files  107 passed` and `Tests  1155 passed` (deltas allowed — record actual)
    - `grep -rn "28 Info" .planning/` returns ≥1 hit; the file path is recorded in the task SUMMARY
    - `dependencies` block in `package.json` contains exactly `react`, `react-dom`, and `@fontsource-variable/inter` (the last is documented as an asset, not a code dep, per Phase 41 disposition)
  </acceptance_criteria>
  <done>Baseline confirmed green; 28-finding provenance located and recorded in task notes.</done>
</task>

<task type="auto">
  <name>Task 2: Run `/gsd:code-review --all` in review-only mode + generate `44-REVIEW.md` + reconcile Info count against historical 28</name>
  <files>
    - .planning/phases/44-final-polish/44-REVIEW.md (new)
    - .planning/phases/44-final-polish/44-INFO-FINDINGS.md (new — preamble + empty table at this step; dispositions filled in Task 3)
  </files>
  <read_first>
    - .planning/phases/44-final-polish/44-CONTEXT.md (D-03 / D-04 / D-05 exact dispositions)
    - .planning/phases/44-final-polish/44-PATTERNS.md ("REVIEW.md frontmatter shape (target — analog 36-REVIEW.md:1-23)" block AND "Finding recovery (per D-03)" block AND "Table shape (per D-04 exact spec)" block)
    - .planning/phases/36-housekeeping-bookkeeping-reset/36-REVIEW.md (frontmatter shape analog — copy structure verbatim, adjust phase + counts)
    - .planning/phases/36-housekeeping-bookkeeping-reset/deferred-items.md (defer-with-reason paragraph template)
  </read_first>
  <action>
    Invoke `/gsd:code-review --all` against the current `main` in REVIEW-ONLY mode (no `--fix` yet — that's Task 3 per D-01's one-shot mega-commit boundary). Capture the reviewer agent output into `.planning/phases/44-final-polish/44-REVIEW.md` using the frontmatter shape from `36-REVIEW.md:1-23`:

    - `phase: 44-final-polish`
    - `reviewed: <ISO timestamp>`
    - `depth: deep` (since `--all` is full-tree)
    - `files_reviewed: <N>` (the count emitted by the reviewer)
    - `findings: { critical: <N>, warning: <N>, info: <N>, total: <N> }`
    - `status: issues_found` (or `clean` if zero)

    Reconcile the Info-severity count against the historical 28 per D-03 — document the delta in `44-INFO-FINDINGS.md` preamble (e.g. "Reviewer surfaced 31 Info findings against current main; 28 historical (2026-05-16) + 3 net-new from Phase 41 surface; mapping recorded below"). If the count is LOWER than 28, the gap is obsolete-by-redesign (per D-05 (a) "file no longer exists") and that disposition is recorded for the missing IDs.

    Create `44-INFO-FINDINGS.md` per D-04 exact spec — markdown table with header `Finding ID | Original path/line | Severity | Disposition | Decision rationale | Commit hash (if fixed)`. Populate the rows but leave `Disposition` and `Commit hash` columns empty for now (filled in Task 3 after `--fix` runs).
  </action>
  <verify>
    <automated>test -f .planning/phases/44-final-polish/44-REVIEW.md &amp;&amp; head -15 .planning/phases/44-final-polish/44-REVIEW.md ; test -f .planning/phases/44-final-polish/44-INFO-FINDINGS.md &amp;&amp; head -20 .planning/phases/44-final-polish/44-INFO-FINDINGS.md ; grep -c '^| IN-\|^| W-\|^| C-' .planning/phases/44-final-polish/44-INFO-FINDINGS.md || true</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/44-final-polish/44-REVIEW.md` exists and contains the 7 required frontmatter keys: `phase`, `reviewed`, `depth`, `files_reviewed`, `findings`, `status`, plus the `findings` map with all 4 sub-keys (`critical`, `warning`, `info`, `total`)
    - `.planning/phases/44-final-polish/44-INFO-FINDINGS.md` exists and contains the D-04 exact table header (`| Finding ID | Original path / line | Severity | Disposition | Decision rationale | Commit hash (if fixed) |`)
    - The preamble of `44-INFO-FINDINGS.md` explicitly cites the historical 28-count provenance (the file path recovered in Task 1)
    - The preamble explicitly reconciles the current Info count against 28 (delta documented with one-line rationale per direction: +N because <reason> / -N because <reason>)
    - Table row count ≥ 28 OR the preamble documents which historical IDs are obsolete-by-redesign with their D-05 trigger (file deleted in J18 / module structurally rewritten)
  </acceptance_criteria>
  <done>44-REVIEW.md captures the full reviewer output; 44-INFO-FINDINGS.md skeleton is in place with reconciled finding rows ready for disposition in Task 3.</done>
</task>

<task type="auto">
  <name>Task 3: Run `/gsd:code-review --all --fix` mega-commit (POLISH-01 + POLISH-02) + complete 44-INFO-FINDINGS.md dispositions + 44-REVIEW-FIX.md</name>
  <files>
    - .planning/phases/44-final-polish/44-INFO-FINDINGS.md (complete the Disposition + Commit hash columns)
    - .planning/phases/44-final-polish/44-REVIEW-FIX.md (new — per-finding fix log)
    - src/** (the exact set of files modified by the reviewer agent's `--fix` output — recorded inline in 44-REVIEW-FIX.md)
  </files>
  <read_first>
    - .planning/phases/44-final-polish/44-CONTEXT.md (D-01 locked: one mega-commit; D-02 locked: bounded to POLISH-01+02; D-05 obsolete-by-redesign threshold; D-11 per-commit green-gate)
    - .planning/phases/44-final-polish/44-PATTERNS.md ("The mega-commit boundary (D-01 + D-02)" block AND "Bounded scope (D-01 / D-02) — what the mega-commit MUST NOT touch" block AND "REVIEW-FIX.md frontmatter (target — analog 36-REVIEW-FIX.md:1-10)" block AND "No Analog Found" row on per-finding atomicity discipline within a single commit)
    - .planning/phases/36-housekeeping-bookkeeping-reset/36-REVIEW-FIX.md (per-finding `### Fixed Issues` block format — copy structure verbatim)
    - 44-REVIEW.md from Task 2 (the finding inventory)
    - 44-INFO-FINDINGS.md from Task 2 (the disposition skeleton)
  </read_first>
  <action>
    Invoke `/gsd:code-review --all --fix` against the current `main` — the reviewer agent applies its own automated fixes. Per D-01 this runs ONCE as a single-iteration mega-commit (no per-finding sharding).

    BOUNDED SCOPE (D-02 — verbatim PATTERNS.md): the mega-commit MUST NOT touch these surfaces (each owns its own downstream cluster):
    - Test-name tightening / redundant-test removal → cluster 44-02 (POLISH-03)
    - Tiger Style comment audit → cluster 44-03 (POLISH-04)
    - `.shape-layer` rename + primitive dedup → cluster 44-04 (POLISH-05)
    - Readability remainder beyond J18 → cluster 44-05 (POLISH-07)
    - Security review → cluster 44-06 (POLISH-06)
    - Invariant verification → cluster 44-07 (POLISH-08, POLISH-09)

    If the reviewer surfaces a finding that overlaps with one of these clusters (e.g. "this comment is narration-of-WHAT"), defer-with-reason in `44-INFO-FINDINGS.md` and let the target cluster pick it up.

    For EACH finding in `44-INFO-FINDINGS.md`:
    - If the reviewer fixed it → set Disposition=`fix`, write the rationale (one line), record the upcoming mega-commit SHA (placeholder `<MEGA_SHA>` then patch after commit)
    - If the file no longer exists OR the surrounding code was structurally rewritten in the spike-loop → set Disposition=`obsolete-by-redesign` per D-05; cite the J# item or commit that obsoleted it (e.g. "File deleted in J18.1 commit `75af962`")
    - If the finding is out of scope for this milestone (overlaps with another cluster, or accepted-risk) → set Disposition=`defer-with-reason` per D-05 ("prefer fix-or-defer over obsolete" when in doubt); write the rationale paragraph following the `deferred-items.md` format (Why out of scope / Concrete evidence / Disposition target)

    Write `44-REVIEW-FIX.md` per D-04 with frontmatter (analog `36-REVIEW-FIX.md:1-10`):
    - `phase: 44-final-polish`, `fixed_at: <ISO>`, `review_path: .planning/phases/44-final-polish/44-REVIEW.md`
    - `iteration: 1` (per D-01 one-shot)
    - `findings_in_scope: <N>` (all Warning + the POLISH-02 Info subset operator chose to fix)
    - `fixed: <N>`, `skipped: <N>` (deferred — rationale in 44-INFO-FINDINGS.md)
    - `status: all_fixed` (target — POLISH-01 says zero Warning at close) OR `partial`

    Then for each fixed finding, write a `### <Finding ID>: <one-line title>` block with `Files modified:` + `Applied fix:` paragraph + `Verification:` (the per-finding gate evidence).

    Run the per-commit green-gate before commit:
    ```
    npx tsc --noEmit -p tsconfig.app.json &amp;&amp; npm run lint &amp;&amp; npm test -- --run &amp;&amp; npm run build
    ```

    All four must exit 0 (per CONTEXT D-11 baseline / Tiger Style "lint is part of the gate again"). If lint fails, do NOT skip — Phase 36 deferred lint-half is closed as no-op per D-11; the gate now includes lint.

    Commit with the mega-commit message shape from PATTERNS.md "The mega-commit boundary (D-01 + D-02)" block (verbatim):

    ```
    fix(44): code-review --all --fix sweep — close all Warning findings + POLISH-02 Info disposition (POLISH-01, POLISH-02)

    Per-file edits driven by /gsd:code-review --all output (full src/ scan against
    Phase 36–41 surface). Closes the 28 Info-severity findings from the 2026-05-16
    deep review per disposition table in .planning/phases/44-final-polish/44-INFO-FINDINGS.md.

    Verified:
      npx tsc --noEmit -p tsconfig.app.json   # clean
      npm run lint                            # 0 errors, 0 warnings
      npm test -- --run                       # 107 files / 1155 tests pass (delta if any)
      npm run build                           # clean
    ```

    After commit, patch the `Commit hash` column in `44-INFO-FINDINGS.md` and the `commit` field in `44-REVIEW-FIX.md` per-finding blocks with the actual SHA. Commit the doc patch in a second `docs(44): pin mega-commit SHA in 44-INFO-FINDINGS.md` commit (so the doc artifact carries the live link).
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.app.json &amp;&amp; npm run lint &amp;&amp; npm test -- --run &amp;&amp; npm run build &amp;&amp; test -f .planning/phases/44-final-polish/44-REVIEW-FIX.md &amp;&amp; grep -E '^\| IN-|^\| W-' .planning/phases/44-final-polish/44-INFO-FINDINGS.md | grep -v -E '\|\s*\|\s*$' | wc -l ; git log --oneline -5</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit -p tsconfig.app.json` exits 0
    - `npm run lint` exits 0 with 0 errors AND 0 warnings (POLISH-01 close criterion)
    - `npm test -- --run` exits 0; test-pass count is recorded in the SUMMARY (drift from 1155 allowed and documented)
    - `npm run build` exits 0
    - Every row in `44-INFO-FINDINGS.md` has a non-empty `Disposition` cell (one of: `fix`, `defer-with-reason`, `obsolete-by-redesign`)
    - Every `Disposition=fix` row has a non-empty 7+ character hex `Commit hash` cell (matches the mega-commit SHA OR the SHA-pin docs commit if a 2nd commit was needed)
    - `44-REVIEW-FIX.md` exists with frontmatter `phase`, `fixed_at`, `review_path`, `iteration: 1`, `findings_in_scope`, `fixed`, `skipped`, `status`
    - `git log --oneline -5` shows a `fix(44): code-review --all --fix sweep` commit (mega-commit) — exactly ONE commit lands the bulk of the sweep per D-01; a follow-up `docs(44): pin mega-commit SHA` commit is acceptable
    - `git show HEAD --stat | grep -E '\.test\.(ts|tsx)$' | wc -l` is 0 OR every modified test file is documented as an explicit reviewer-fix in 44-REVIEW-FIX.md (per D-02 the mega-commit MUST NOT do test-name cleanup — that's 44-02)
    - `git show HEAD --stat | grep -E 'shape-layer|orb-layer|SettingsRow'` returns empty (per D-02 the mega-commit MUST NOT do the .shape-layer rename or primitive dedup — that's 44-04)
  </acceptance_criteria>
  <done>Mega-commit landed; zero Warning findings open; all Info findings dispositioned; per-commit green-gate green.</done>
</task>

</tasks>

<threat_model>

> Phase 44-01 ships code edits driven by an external reviewer agent; the security-review cluster (44-06) consumes the changed-file set as part of its threat-register input. The threat model below identifies trust boundaries crossed by THIS plan's own activity.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| reviewer-agent output → repository | The `/gsd:code-review --fix` agent runs its own edits against `src/`; the human reviewer (operator at PR-time) is the final check. No untrusted input crosses, but the reviewer's diffs cross from agent process → working tree. |
| reviewer-agent output → 44-INFO-FINDINGS.md table | Same boundary; the disposition table is human-authored after consuming the agent output. |
| commit → main | Per-commit green-gate (tsc + lint + test + build) is the structural gate; CONTEXT D-11 says lint deferral is closed. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-44-01-01 | Tampering | reviewer-agent edits to `src/` | mitigate | Per-commit green-gate (tsc + lint + test + build) catches any structural break; per-finding diff in 44-REVIEW-FIX.md gives reviewer-at-PR-time a tractable surface despite the mega-commit shape (D-01) |
| T-44-01-02 | Information Disclosure | 44-INFO-FINDINGS.md table reveals known issues | accept | Audit trail value > information disclosure risk; this is a single-developer + Claude workflow per PROJECT.md, no external attackers in scope; the table is bookkeeping not a remediation roadmap |
| T-44-01-03 | Repudiation | mega-commit obscures per-finding authorship | mitigate | 44-REVIEW-FIX.md per-finding `### <Finding ID>: <title>` block + `Commit hash` column in 44-INFO-FINDINGS.md gives full audit trail despite the single-commit shape |
| T-44-01-SC | Tampering | no package installs in this plan | accept | This plan does not install or upgrade any npm packages; the package-legitimacy gate does not apply. Future runs of `/gsd:code-review --fix` could in principle suggest dependency changes but operator decision D-08 says zero net-new runtime deps remains an invariant (POLISH-08) — any suggested install would be deferred. |

</threat_model>

<verification>

## Phase-level verification (44-01 only — clusters 44-02..07 verified independently)

- `.planning/phases/44-final-polish/44-REVIEW.md` exists with valid frontmatter (findings counts populated)
- `.planning/phases/44-final-polish/44-REVIEW-FIX.md` exists with valid frontmatter (status `all_fixed` or `partial` with rationale)
- `.planning/phases/44-final-polish/44-INFO-FINDINGS.md` exists with every row dispositioned per D-05 (one of `fix` / `defer-with-reason` / `obsolete-by-redesign`)
- Mega-commit SHA recorded in both `44-REVIEW-FIX.md` and the `Commit hash` column of `44-INFO-FINDINGS.md` for every `Disposition=fix` row
- POLISH-01: `npm run lint` exits 0 with 0 errors AND 0 warnings on the mega-commit HEAD (this is the close criterion; subsequent clusters preserve it)
- POLISH-02: every historical Info finding has a disposition entry; delta against current-main Info findings is reconciled in the preamble

</verification>

<success_criteria>

POLISH-01: ✅ when `npm run lint` exits 0 with 0/0 on the mega-commit HEAD, and 44-REVIEW.md `findings.warning: 0`.

POLISH-02: ✅ when every row in 44-INFO-FINDINGS.md has a non-empty Disposition cell AND every `fix` row has a Commit hash; the 28-historical reconciliation paragraph in the preamble explicitly accounts for the count.

</success_criteria>

<output>
Create `.planning/phases/44-final-polish/44-01-SUMMARY.md` when done. Include:
- Mega-commit SHA + diff-stat
- 44-REVIEW.md findings counts (critical / warning / info / total)
- 44-INFO-FINDINGS.md row counts by disposition (fix / defer-with-reason / obsolete-by-redesign)
- 28-historical reconciliation paragraph (delta direction + rationale)
- Any deferred-cross-cluster findings (e.g. "IN-CMT-12 deferred to 44-03 (Tiger Style comment audit)")
- Per-commit green-gate verification block
</output>