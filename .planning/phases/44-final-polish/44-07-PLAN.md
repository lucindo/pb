---
phase: 44-final-polish
plan: 07
type: execute
wave: 7
depends_on: ["44-01", "44-02", "44-03", "44-04", "44-05", "44-06"]
files_modified:
  - .planning/phases/44-final-polish/44-VERIFICATION.md
  - .planning/REFACTOR-LOOP-STATE.md  # mark Items F6 + I approved per D-12 (stale "awaiting approval" label is bookkeeping leak)
autonomous: true
requirements:
  - POLISH-08
  - POLISH-09
user_setup: []

must_haves:
  truths:
    - "POLISH-08 verified: `dependencies` block in `package.json` at HEAD contains exactly `react` + `react-dom` (with `@fontsource-variable/inter` documented as a runtime ASSET, not a code dep — per Phase 41 disposition; CONTEXT D-15 + POLISH-08 wording)"
    - "POLISH-09 verified: per-commit green-gate (tsc + lint + test + build) held through milestone close — every Phase 44 cluster commit (44-01..44-06) ran the gate before commit; CONTEXT baseline at commit `580dc53` re-verified at phase start; Phase 41 spike-loop per-item-gate evidence inherited from 41-VERIFICATION.md POLISH-09 row"
    - "44-VERIFICATION.md frontmatter: status `passed`, score `9/9 POLISH-XX requirements satisfied`, overrides_applied 0"
    - "Per-requirement evidence table covers all 9 POLISH-XX IDs with status + evidence + commit pins"
    - "REFACTOR-LOOP-STATE.md Items F6 + I dispositioned 'approved' per D-12 (stale 'awaiting operator approval' label is a bookkeeping leak — the work is on main at commits `fe14c47` + `80da948`)"
  artifacts:
    - path: ".planning/phases/44-final-polish/44-VERIFICATION.md"
      provides: "Per-requirement evidence table covering all 9 POLISH-XX IDs with commit pins"
    - path: ".planning/phases/44-final-polish/44-07-SUMMARY.md"
      provides: "Per-cluster commit SHA + invariant verification evidence + bookkeeping closeout (REFACTOR-LOOP-STATE.md F6/I approval)"
    - path: ".planning/REFACTOR-LOOP-STATE.md"
      provides: "F6 + I marked done (approved) — closes the stale 'awaiting operator approval' bookkeeping leak per D-12"
  key_links:
    - from: "44-VERIFICATION.md POLISH-08 row"
      to: "package.json `dependencies` block at HEAD"
      via: "verification grep returning the 3-line dependency block (react + react-dom + fontsource-asset)"
      pattern: "\"react\":\\s*\".*\"|\"react-dom\":\\s*\".*\""
    - from: "44-VERIFICATION.md POLISH-09 row"
      to: "Phase 41 41-VERIFICATION.md POLISH-09 row + cluster 44-01..44-06 commit gates"
      via: "inheritance + replay — Phase 41 verified per-item-gate; Phase 44 verifies each cluster's commit gate"
      pattern: "POLISH-09|per-commit green-gate"
---

<objective>
Two-part invariant verification per POLISH-08 + POLISH-09:

**POLISH-08 — zero net-new runtime deps held through milestone close.** Verify `dependencies` block in `package.json` at HEAD contains exactly `react` + `react-dom`. `@fontsource-variable/inter` is documented as a runtime ASSET (woff2 files in dist/, not code dep) per Phase 41 disposition; the inclusion is correctly recorded as asset-not-dep.

**POLISH-09 — per-commit green-gate held through milestone close.** Verify (a) CONTEXT baseline at commit `580dc53` passes the gate (already in CONTEXT — re-verify at phase start), (b) every Phase 44 cluster commit (44-01..44-06) passes the gate (re-run after each cluster lands; recorded in each cluster's SUMMARY), (c) the Phase 41 spike-loop per-item-gate claim (41-VERIFICATION.md POLISH-09 row) is the evidence for Phases 36–41.

**Bookkeeping closeout (per D-12):** mark REFACTOR-LOOP-STATE.md Items F6 + I as approved. F6 (UiStringsContext) shipped at commit `fe14c47`; I (sibling-pattern stale-comment cleanup) shipped at commit `80da948`. Both are on main; the "awaiting operator approval" label is a bookkeeping leak D-12 directs Phase 44 to close.

Purpose: Close POLISH-08 + POLISH-09 (the two invariant requirements) and produce `44-VERIFICATION.md` — the per-requirement evidence table covering ALL 9 POLISH-XX IDs (POLISH-01..09) with commit pins. This is the milestone-close verification artifact.

Output: `44-VERIFICATION.md` + cleanup of REFACTOR-LOOP-STATE.md (F6/I → approved) + one `docs(44): invariant verification — POLISH-08 + POLISH-09` commit + 44-07-SUMMARY.md.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/44-final-polish/44-CONTEXT.md
@.planning/phases/44-final-polish/44-PATTERNS.md
@.planning/phases/41-spike-mono-zen/41-VERIFICATION.md
@.planning/REFACTOR-LOOP-STATE.md
@.planning/phases/44-final-polish/44-01-SUMMARY.md
@.planning/phases/44-final-polish/44-02-SUMMARY.md
@.planning/phases/44-final-polish/44-03-SUMMARY.md
@.planning/phases/44-final-polish/44-04-SUMMARY.md
@.planning/phases/44-final-polish/44-05-SUMMARY.md
@.planning/phases/44-final-polish/44-06-SUMMARY.md
</context>

<propose_step_checklist>

## MANDATORY (per CONTEXT D-13 + [[propose-step-checklist]])

Before executing any task in this plan, the executor MUST first produce a PROPOSE block leading with:

1. **Downstream Constraints** — What downstream tasks (other Phase 44 clusters, future v2.x work, milestone close, drift-guard tests) might this change constrain? List 2-5 concrete items. **Especially for this plan: 44-VERIFICATION.md is the milestone-close artifact; each evidence cell pins to a cluster commit SHA — those SHAs MUST come from each cluster's SUMMARY (not invented).**

2. **Applicable Memory Rules** — Which rules from `~/.claude-personal/projects/-Users-lucindo-Code-hrv/memory/MEMORY.md` apply to this work? At minimum surface (cite by `[[rule-name]]`):
   - `[[propose-step-checklist]]` — meta-rule, always applies
   - `[[no-design-locking]]` — N/A this plan (doc-only)
   - `[[use-lsp-for-renames]]` — N/A this plan (doc-only)
   - `[[design-logic-separation]]` — N/A this plan (doc-only)
   - `[[ack-dont-fix-inline]]` — applies during operator feedback dumps
   - `[[spike-locked-values-are-not-decisions]]` — applies if any spike-010-locked visual would be touched

3. **THEN** — Goal / Scope / Risk / Verification / Commit message (the standard propose-block content).

No exceptions for "small" cleanup tasks. The Section A + Section B preface is the gate, not the content.

This requirement is operator-locked at CONTEXT D-13 verbatim: "Mandatory propose-step checklist applies to every Phase 44 plan per [[propose-step-checklist]]: Downstream Constraints + Applicable Memory Rules sections BEFORE Goal/Scope/Risk. No exceptions for 'small' cleanup plans."

</propose_step_checklist>

<tasks>

<task type="auto">
  <name>Task 1: POLISH-08 verification — assert `dependencies` block is `react` + `react-dom` (with fontsource asset disposition)</name>
  <files>
    - (read-only) package.json
  </files>
  <read_first>
    - .planning/phases/44-final-polish/44-CONTEXT.md (D-15 + POLISH-08 invariant; baseline section confirms `dependencies` at commit `580dc53` is `react` + `react-dom` + `@fontsource-variable/inter` asset)
    - .planning/phases/44-final-polish/44-PATTERNS.md ("POLISH-08 verification (zero net-new runtime deps)" block — exact verification command + 41-VERIFICATION.md line 43 reference for the asset-not-dep disposition)
    - .planning/phases/41-spike-mono-zen/41-VERIFICATION.md (the POLISH-08 row from Phase 41 — `@fontsource-variable/inter is documented as a runtime ASSET` disposition; copy verbatim into 44-VERIFICATION.md POLISH-08 row)
    - package.json (the `dependencies` block to verify)
  </read_first>
  <action>
    Run the PATTERNS.md "POLISH-08 verification" command verbatim:
    ```
    git show HEAD:package.json | grep -A20 '"dependencies"'
    ```

    Expected output (per CONTEXT baseline at commit `580dc53`):
    ```
    "dependencies": {
      "@fontsource-variable/inter": "^5.2.8",
      "react": "^19.2.5",
      "react-dom": "^19.2.5"
    }
    ```

    Record the actual output. If extra packages appear (beyond the 3), STOP — POLISH-08 invariant is violated; surface to operator. The acceptable "non-code" entries are only `@fontsource-variable/inter` (the asset); any other addition is a new runtime code dep and breaks the invariant.

    Cross-reference: if a v1.5 ship tag exists (`git tag --list 'v1.5*'`), compare against `git show <v1.5-tag>:package.json | grep -A20 '"dependencies"'`. The two blocks should match in code-dep content (modulo the `@fontsource-variable/inter` asset which was added in Phase 41 J2 — verified-as-asset per Phase 41 41-VERIFICATION.md line 43). If a v1.5 ship tag does NOT exist, use the last v1.5 ship commit per PATTERNS.md note.

    Record both blocks (HEAD + prior) in scratch notes for Task 3's 44-VERIFICATION.md table.
  </action>
  <verify>
    <automated>echo '---PACKAGE.JSON DEPENDENCIES AT HEAD---' &amp;&amp; git show HEAD:package.json | grep -A20 '"dependencies"' ; echo '---V1.5 TAG IF AVAILABLE---' &amp;&amp; git tag --list 'v1.5*' ; echo '---DEPS COUNT CHECK (expect 3: react, react-dom, fontsource)---' &amp;&amp; git show HEAD:package.json | grep -A20 '"dependencies"' | grep -E '^\s*"' | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - `git show HEAD:package.json | grep -A20 '"dependencies"'` returns a block containing exactly `react`, `react-dom`, and `@fontsource-variable/inter` (3 entries — the third is documented as an asset)
    - The dependency count line (grep -E '^\s*"' | wc -l) returns 3
    - If any OTHER package appears in the dependencies block, the task FAILS with operator-surface — POLISH-08 invariant violated
    - Scratch notes record both HEAD-block and (if v1.5 tag exists) v1.5-tag block for cross-reference
  </acceptance_criteria>
  <done>POLISH-08 invariant verified — 0 net-new runtime code deps held through milestone close.</done>
</task>

<task type="auto">
  <name>Task 2: POLISH-09 verification — assert per-commit green-gate held at CONTEXT baseline + every Phase 44 cluster commit</name>
  <files>
    - (read-only) git log + per-cluster SUMMARYs
  </files>
  <read_first>
    - .planning/phases/44-final-polish/44-CONTEXT.md (D-11 verbatim — "Per-commit green-gate fully restored. Baseline: lint clean (0/0), tsc clean, 1155 tests pass, build clean. ... POLISH-09 verification confirms this held through milestone close.")
    - .planning/phases/44-final-polish/44-PATTERNS.md ("POLISH-09 verification (per-commit green-gate held)" block — exact verification protocol; "Replay isn't tractable per-commit ... Instead, POLISH-09 verification asserts: (1) CONTEXT baseline ... (2) every Phase 44 cluster commit ... (3) Phase 41 spike-loop per-item-gate claim")
    - .planning/phases/41-spike-mono-zen/41-VERIFICATION.md (POLISH-09 row — the per-item-gate claim is the evidence for Phases 36–41)
    - .planning/phases/44-final-polish/44-01-SUMMARY.md (mega-commit gate evidence)
    - .planning/phases/44-final-polish/44-02-SUMMARY.md (test-cleanup gate evidence + flake-check evidence)
    - .planning/phases/44-final-polish/44-03-SUMMARY.md (comment-sweep gate evidence + test-pass-count parity)
    - .planning/phases/44-final-polish/44-04-SUMMARY.md (refactor gate evidence + bundle delta)
    - .planning/phases/44-final-polish/44-05-SUMMARY.md (readability gate evidence + test-pass-count parity)
    - .planning/phases/44-final-polish/44-06-SUMMARY.md (security gate evidence)
  </read_first>
  <action>
    Per PATTERNS.md "POLISH-09 verification" block, three-part assertion:

    Part 1 — CONTEXT baseline re-verification: run the gate at HEAD against the CONTEXT baseline expectation:
    ```
    npx tsc --noEmit -p tsconfig.app.json &amp;&amp; npm run lint &amp;&amp; npm test -- --run &amp;&amp; npm run build
    ```
    All 4 must exit 0. Lint count must be `0 errors, 0 warnings`. Test pass count is recorded (drift from 1155 allowed and documented across cluster SUMMARYs).

    Part 2 — Per-cluster commit gate evidence: walk each cluster SUMMARY from 44-01..44-06 and confirm each records green-gate evidence. Per cluster:
    - 44-01: mega-commit gate evidence (tsc/lint/test/build all green; 0 Warning findings)
    - 44-02: test-cleanup gate evidence + 3× flake-check zero-flake
    - 44-03: comment-sweep gate evidence + test-pass-count == post-44-02 count
    - 44-04: refactor gate evidence + behavioral byte-equivalence + bundle delta
    - 44-05: readability gate evidence (or fold-into-44-03 note) + test-pass-count parity
    - 44-06: security gate procedural evidence (tsc/lint/test/build still green since 44-06 only touched .planning/)

    Build a table mapping each cluster commit SHA → gate evidence summary. If any cluster SUMMARY does NOT record gate evidence, POLISH-09 verification fails for that cluster and the SUMMARY must be amended before 44-07 closes.

    Part 3 — Phase 41 spike-loop per-item-gate inheritance: cite `41-VERIFICATION.md` POLISH-09 row as the evidence for Phases 36–41. Per CONTEXT D-11: "Phase 41 work already resolved the 53-error backlog. POLISH-09 verification confirms this held through milestone close." The deferral of lint-half from Phase 36 is closed as no-op — record this explicitly in 44-VERIFICATION.md POLISH-09 row rationale.

    Record commit count for the v2.0 milestone for context:
    ```
    git log --oneline <v1.5-ship>..HEAD | wc -l    # total commits in v2.0 — recorded but not gating
    ```

    Record findings in scratch notes for Task 3's 44-VERIFICATION.md table.
  </action>
  <verify>
    <automated>echo '---HEAD GATE---' &amp;&amp; npx tsc --noEmit -p tsconfig.app.json &amp;&amp; npm run lint 2>&amp;1 | tail -3 &amp;&amp; npm test -- --run 2>&amp;1 | tail -3 &amp;&amp; npm run build 2>&amp;1 | tail -3 ; echo '---CLUSTER SUMMARIES PRESENT---' &amp;&amp; for f in 44-01 44-02 44-03 44-04 44-05 44-06; do test -f .planning/phases/44-final-polish/$f-SUMMARY.md &amp;&amp; echo "$f-SUMMARY.md exists" || echo "MISSING $f-SUMMARY.md"; done ; echo '---PHASE 41 VERIFICATION INHERITANCE---' &amp;&amp; grep -A2 'POLISH-09' .planning/phases/41-spike-mono-zen/41-VERIFICATION.md | head -5</automated>
  </verify>
  <acceptance_criteria>
    - All 4 head-gate commands exit 0
    - `npm run lint` reports 0 errors AND 0 warnings (matches CONTEXT D-11 baseline)
    - Every cluster SUMMARY (44-01..44-06) exists and records green-gate evidence — if any is MISSING, surface as a Phase 44 close blocker
    - 41-VERIFICATION.md POLISH-09 row exists and is citable (inheritance for Phases 36–41 evidence)
    - Scratch notes contain a per-cluster commit SHA → gate-evidence summary table
  </acceptance_criteria>
  <done>POLISH-09 invariant verified — per-commit green-gate held through milestone close.</done>
</task>

<task type="auto">
  <name>Task 3: Assemble 44-VERIFICATION.md (9-row evidence table) + REFACTOR-LOOP-STATE.md F6/I approval + commit</name>
  <files>
    - .planning/phases/44-final-polish/44-VERIFICATION.md (new)
    - .planning/REFACTOR-LOOP-STATE.md (mark F6 + I approved per D-12)
    - .planning/phases/44-final-polish/44-07-SUMMARY.md (new)
  </files>
  <read_first>
    - .planning/phases/44-final-polish/44-PATTERNS.md ("VERIFICATION.md frontmatter shape (target — `41-VERIFICATION.md:1-29`)" block — exact frontmatter; "Body section outline (mirror `41-VERIFICATION.md`)" block + the 9-row per-requirement evidence table template; "Per-cluster commit (D-02)" block at end of cluster 44-07 — exact commit message shape)
    - .planning/phases/41-spike-mono-zen/41-VERIFICATION.md (FULL — copy structure verbatim, adjust phase + populate per-cluster commit SHAs from cluster SUMMARYs)
    - .planning/REFACTOR-LOOP-STATE.md (F6 + I rows — change status from "implemented — awaiting operator approval" to "✓ done — commit `<SHA>`" per D-12)
    - .planning/phases/44-final-polish/44-CONTEXT.md (D-12 verbatim — F6 (UiStringsContext) + I (sibling-pattern stale-comment cleanup) inherited as done work; commits `fe14c47` + `80da948`)
    - All 6 cluster SUMMARYs from 44-01..44-06 (for per-requirement evidence and commit SHA citations)
  </read_first>
  <action>
    Step 1 — Build 44-VERIFICATION.md per PATTERNS.md "VERIFICATION.md frontmatter shape" block:
    ```yaml
    ---
    phase: 44-final-polish
    verified: 2026-05-25T<HH:MM:SS>Z
    status: passed                       # 0 open Warning findings + invariants hold
    score: 9/9 POLISH-XX requirements satisfied
    overrides_applied: 0
    re_verification:
      previous_status: none
      previous_score: n/a
      gaps_closed: []
      gaps_remaining: []
      regressions: []
    human_verification: []               # POLISH is automated; no UAT
    ---
    ```

    Body per PATTERNS.md "Body section outline (mirror 41-VERIFICATION.md)" block:
    - `# Phase 44 Verification — Final polish closeout`
    - `**Verifier:** Operator + per-cluster verification (each cluster ran tsc + lint + tests + build before commit)`
    - `## Requirements Coverage`
      - `### POLISH — Final polish (9/9 satisfied)`

    Per-requirement evidence table (verbatim template from PATTERNS.md, populate from cluster SUMMARYs):

    | ID | Status | Evidence |
    |---|---|---|
    | POLISH-01 | ✅ verified | `/gsd:code-review --all --fix` mega-commit at `<44-01 commit SHA>`. 44-REVIEW.md status `clean` (0 Warning). |
    | POLISH-02 | ✅ verified | 28 Info findings dispositioned in `44-INFO-FINDINGS.md`. `<N fixed>` fixed in mega-commit; `<N>` defer-with-reason; `<N>` obsolete-by-redesign. |
    | POLISH-03 | ✅ verified | Test cleanup at `<44-02 commit SHA>`. Final count: `<X>` files / `<Y>` tests. No flake in 3× consecutive runs. |
    | POLISH-04 | ✅ verified | Tiger Style WHY-only comment sweep at `<44-03 commit SHA>`. `<N>` files touched; comments only. |
    | POLISH-05 | ✅ verified | Refactor pass at `<44-04 commit SHA>`. SettingsRow primitive extracted; .orb-layer rename dispositioned obsolete-by-redesign (PATTERNS.md IMPORTANT FINDING). |
    | POLISH-06 | ✅ verified | `44-SECURITY.md` written at `<44-06 commit SHA>`. status `verified`, threats_open: 0. |
    | POLISH-07 | ✅ verified | Readability remainder closed by `<44-05 commit SHA or fold-into-44-03>`. Zero leftover Square/Diamond/Moss/Slate/Dusk/Chime refs. |
    | POLISH-08 | ✅ verified | `dependencies` in `package.json` at HEAD is `react` + `react-dom`. `@fontsource-variable/inter` is an asset, not a code dep (per Phase 41 disposition; cf. 41-VERIFICATION.md line 43). |
    | POLISH-09 | ✅ verified | Per-commit green-gate held through milestone close. CONTEXT baseline verified at phase start; each cluster commit (44-01..44-06) ran gate before commit; Phase 41 per-item-gate inherited from 41-VERIFICATION.md POLISH-09 row. |

    Each `<SHA>` placeholder MUST be replaced with the actual commit SHA from the cluster's SUMMARY (cluster 44-01-SUMMARY.md, 44-02-SUMMARY.md, etc.). The mega-commit may have a follow-up SHA-pin docs commit per 44-01 plan — use the mega-commit SHA in the POLISH-01/02 rows.

    Step 2 — Update REFACTOR-LOOP-STATE.md per D-12. Find the rows for F6 and I:
    - F6 row currently reads: `| F6 | `UiStringsContext` provider replacing drilling of `uiStrings`... | **implemented — awaiting operator approval** |`
    - I row currently reads: `| I | Sibling-pattern stale-comment cleanup... | **implemented — awaiting operator approval** |`

    Change both to: `| F6 | ... | ✓ done — commit `fe14c47` |` and `| I | ... | ✓ done — commit `80da948` |`. Update the "Current focus" section at the bottom of the file to reflect that the refactor loop is closed (no current focus; v2.0 close pending after Phase 44).

    Step 3 — Run per-commit green-gate (sanity, since this plan only touches `.planning/`):
    ```
    npx tsc --noEmit -p tsconfig.app.json &amp;&amp; npm run lint &amp;&amp; npm test -- --run &amp;&amp; npm run build
    ```

    Step 4 — Commit per PATTERNS.md "Per-cluster commit (D-02)" exact shape, scope prefix `docs(44):`:
    ```
    docs(44): invariant verification — POLISH-08 + POLISH-09 (POLISH-08, POLISH-09)

    Output: 44-VERIFICATION.md (status: passed, 9/9 POLISH requirements satisfied)

    POLISH-08 evidence: package.json dependencies unchanged since v1.0 — react + react-dom only.
    POLISH-09 evidence: per-commit green-gate held; baseline at commit 580dc53 confirmed at start
    of phase; every 44-NN cluster ran tsc + lint + test + build before commit.

    Also closes REFACTOR-LOOP-STATE.md bookkeeping leak per CONTEXT D-12 —
    Items F6 (commit fe14c47) + I (commit 80da948) marked approved.

    Verified:
      npx tsc --noEmit -p tsconfig.app.json   # clean
      npm run lint                            # 0/0
      npm test -- --run                       # final count locked
      npm run build                           # clean
    ```

    Step 5 — Write 44-07-SUMMARY.md including: POLISH-08 verification block (dependencies block at HEAD), POLISH-09 verification block (per-cluster commit SHA → gate evidence table), REFACTOR-LOOP-STATE.md cleanup confirmation (F6/I approved citations), per-cluster commit SHA, green-gate evidence.
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.app.json &amp;&amp; npm run lint &amp;&amp; npm test -- --run 2>&amp;1 | tail -3 &amp;&amp; npm run build 2>&amp;1 | tail -3 ; echo '---VERIFICATION.MD EXISTS---' &amp;&amp; test -f .planning/phases/44-final-polish/44-VERIFICATION.md &amp;&amp; head -15 .planning/phases/44-final-polish/44-VERIFICATION.md ; echo '---9 ROWS IN EVIDENCE TABLE---' &amp;&amp; grep -c '^| POLISH-' .planning/phases/44-final-polish/44-VERIFICATION.md ; echo '---REFACTOR-LOOP F6 + I APPROVED---' &amp;&amp; grep -E '^\| F6 \||^\| I \|' .planning/REFACTOR-LOOP-STATE.md ; echo '---STATUS FRONTMATTER---' &amp;&amp; grep -E '^status:|^score:|^overrides_applied:' .planning/phases/44-final-polish/44-VERIFICATION.md ; echo '---COMMIT---' &amp;&amp; git log --oneline -3</automated>
  </verify>
  <acceptance_criteria>
    - All 4 green-gate commands exit 0
    - `.planning/phases/44-final-polish/44-VERIFICATION.md` exists
    - Frontmatter contains all required keys: `phase: 44-final-polish`, `verified` (ISO timestamp), `status: passed`, `score: 9/9 POLISH-XX requirements satisfied`, `overrides_applied: 0`, `re_verification` block with all 5 sub-keys, `human_verification: []`
    - Body Requirements Coverage table has exactly 9 rows for POLISH-01..POLISH-09; each Status cell reads `✅ verified`; each Evidence cell cites the specific cluster commit SHA from the cluster's SUMMARY
    - REFACTOR-LOOP-STATE.md F6 row reads `✓ done — commit \`fe14c47\`` (no longer "awaiting operator approval")
    - REFACTOR-LOOP-STATE.md I row reads `✓ done — commit \`80da948\`` (no longer "awaiting operator approval")
    - REFACTOR-LOOP-STATE.md "Current focus" section updated (refactor loop closed; v2.0 close pending)
    - `git log --oneline -3` shows a `docs(44):` prefixed commit naming both POLISH-08 + POLISH-09 + REFACTOR-LOOP-STATE.md cleanup
    - 44-07-SUMMARY.md exists with all 4 required blocks (POLISH-08 verification / POLISH-09 verification / REFACTOR-LOOP cleanup / commit SHA + gate evidence)
  </acceptance_criteria>
  <done>POLISH-08 + POLISH-09 closed; 44-VERIFICATION.md committed; REFACTOR-LOOP-STATE.md bookkeeping closed; Phase 44 ready for milestone close.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| package.json `dependencies` block → POLISH-08 invariant | One-line invariant; CI gate (tsc + lint + build) catches structural breakage. |
| per-cluster SUMMARY gate evidence → POLISH-09 invariant | Inheritance — POLISH-09 trusts each cluster SUMMARY's gate-evidence claim; if a SUMMARY lies, the verification chain is compromised. Mitigated by Task 2 walking the actual cluster SUMMARYs and the head-gate re-verification. |
| REFACTOR-LOOP-STATE.md disposition → bookkeeping integrity | Updating F6/I status to approved is a one-line edit per row; the underlying commits already on main (`fe14c47` + `80da948`) are the canonical source of truth. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-44-07-01 | Tampering | unrecorded new runtime dep in package.json breaks POLISH-08 silently | mitigate | Task 1 explicit grep check + dep-count assertion (expect 3 entries: react + react-dom + fontsource-asset); if count ≠ 3 the task fails with operator-surface |
| T-44-07-02 | Tampering | cluster SUMMARY claims green-gate evidence falsely; POLISH-09 verification trusts it | mitigate | Task 2 head-gate re-verification at HEAD + walk-and-confirm of each cluster SUMMARY's evidence block; head-gate is the ground-truth re-run |
| T-44-07-03 | Repudiation | REFACTOR-LOOP-STATE.md F6/I approval landed without naming the actual implementation commits | mitigate | Task 3 acceptance criterion explicitly requires the commit SHA citations (`fe14c47` + `80da948` per D-12) in the row dispositions |
| T-44-07-SC | Tampering | no package installs in this plan | accept | Doc-only plan; no dependency surface change |

</threat_model>

<verification>

- POLISH-08: `dependencies` block at HEAD has exactly `react` + `react-dom` + `@fontsource-variable/inter` (asset)
- POLISH-09: head-gate exits 0 across all 4 commands; every cluster SUMMARY has gate evidence; Phase 41 per-item-gate inherited
- 44-VERIFICATION.md has 9 evidence rows with cluster commit SHAs
- REFACTOR-LOOP-STATE.md F6/I marked approved with commit citations
- `docs(44):` commit landed
- 44-07-SUMMARY.md populated

</verification>

<success_criteria>

POLISH-08: ✅ when `dependencies` block at HEAD verified as exactly `react` + `react-dom` (with fontsource asset disposition).

POLISH-09: ✅ when head-gate verified at phase close; every cluster SUMMARY contains gate evidence; Phase 41 inheritance cited.

Phase 44 close-ready: ✅ when 44-VERIFICATION.md committed with all 9 POLISH-XX rows verified AND REFACTOR-LOOP-STATE.md bookkeeping closed.

</success_criteria>

<output>
Create `.planning/phases/44-final-polish/44-07-SUMMARY.md` when done. Include:
- POLISH-08 verification block (HEAD `dependencies` block + asset-not-dep disposition)
- POLISH-09 verification block (per-cluster commit SHA → gate evidence table + head-gate re-run)
- REFACTOR-LOOP-STATE.md cleanup confirmation (F6 / I approved with commit citations)
- Per-cluster commit SHA + diff-stat
- Phase 44 close-ready signal (9/9 POLISH requirements verified)
</output>
</content>
