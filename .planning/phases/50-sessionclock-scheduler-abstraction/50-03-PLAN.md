---
phase: 50-sessionclock-scheduler-abstraction
plan: 03
type: execute
wave: 2
depends_on:
  - 50-01
files_modified:
  - src/hooks/useNaviKriyaAudio.ts
  - src/hooks/useNaviKriyaAudio.test.tsx
  - src/hooks/useNKEngine.ts
  - src/hooks/useNKEngine.test.tsx
  - src/hooks/useNaviKriyaSessionController.ts
  - src/domain/naviKriyaSession.ts
  - src/domain/naviKriyaSession.test.ts
  - src/domain/naviKriyaSettings.ts
autonomous: true
requirements:
  - ABSTR-03
  - ABSTR-04
  - DEPS-01
  - QUAL-01
requirements_addressed:
  - ABSTR-03
  - ABSTR-04
  - DEPS-01
  - QUAL-01
tags:
  - sessionclock
  - hooks
  - navikriya
  - rename
must_haves:
  truths:
    - "`useNaviKriyaAudio.begin()` constructs an `AudioContext` exactly once (the existing call site at L44; D-08 invariant preserved) and immediately wraps it via `createAudioSessionClock(audioCtx)`; the single `audioCtx.currentTime + SAFE_LEAD_SEC` read at L75 becomes `clock.now() + SAFE_LEAD_SEC` — byte-identical at runtime because Plan 50-01's `createAudioSessionClock.now()` returns `audioCtx.currentTime` per D-03 Option A."
    - "`useNKEngine` accepts a `SessionClock` parameter and reads time exclusively via `clock.now()`; zero `performance.now()` references in `src/hooks/useNKEngine.ts` source (D-09)."
    - "NKEngineRecord time fields are seconds-shaped: `startedAtSec` (was `startedAtMs`), `omSec` (was `omMs`), `pendingDelaySec` (was `pendingDelayMs`); `NK_LEAD_MS` is renamed to `NK_LEAD_SEC` with value `5` (was `5000`) in `naviKriyaSession.ts` (D-02 cascade)."
    - "NKOnComplete callback signature reports `elapsedSec` (not `elapsedMs`)."
    - "`setTimeout(stepOmRef.current, delayMs)` boundary at useNKEngine.ts L92: the local variable is renamed to `delaySec` and the boundary call multiplies by 1000 — the ONLY surviving ms-shaped value is the literal arg to setTimeout."
    - "`useNaviKriyaSessionController` constructs/threads a SessionClock into `useNKEngine(clock)`; uses `createWallSessionClock()` because NK engine timing is for elapsed-stats only, not audio scheduling (the NK audio clock is constructed inside useNaviKriyaAudio.begin and is per-session, not per-render)."
    - "`audioCtx.currentTime` no longer appears in `src/hooks/useNaviKriyaAudio.ts` source lines — the single read at L75 is replaced by `clock.now()`. Drift-guard (Plan 50-07) passes by construction."
    - "**Revision 1 Warning #6 — `estimateNaviKriyaDurationMs` consumers pre-enumerated:** the only consumer of `estimateNaviKriyaDurationMs` in the entire src/ tree is `estimateNaviKriyaDurationMinutes` at `src/domain/naviKriyaSession.ts:47` (verified by `git grep -n estimateNaviKriyaDurationMs src/` returning exactly 2 lines, both inside `src/domain/naviKriyaSession.ts`). No external consumers exist; the rename to `estimateNaviKriyaDurationSec` is fully scoped to this one file."
    - "**Revision 1 Warning #12 — phase-level invariant note:** Phase 50 has exactly TWO `createAudioSessionClock` invocations: one inside `createAudioEngine` (HRV AC), one inside `useNaviKriyaAudio.begin()` (NK AC). They wrap DIFFERENT AudioContexts and MUST NOT be conflated. The engine's `clock` member exposes only the HRV-AC clock; NK-AC's clock is local to NK's lifecycle. NK code MUST NOT read `engine.clock` and HRV code MUST NOT read NK's local `clock` variable."
  artifacts:
    - path: "src/hooks/useNaviKriyaAudio.ts"
      provides: "Wraps the begin()-constructed AudioContext via createAudioSessionClock; cueWhen() reads clock.now() + SAFE_LEAD_SEC."
      contains: "createAudioSessionClock"
    - path: "src/hooks/useNKEngine.ts"
      provides: "useNKEngine(clock: SessionClock) signature; 3 performance.now() reads converted to clock.now(); NKEngineRecord seconds-shaped."
      contains: "clock.now()"
    - path: "src/domain/naviKriyaSession.ts"
      provides: "NK_LEAD_SEC = 5; ms→sec rename of public constants; estimateNaviKriyaDurationSec replaces estimateNaviKriyaDurationMs (sole consumer estimateNaviKriyaDurationMinutes updated in-file per revision 1 Warning #6)."
      contains: "NK_LEAD_SEC"
  key_links:
    - from: "src/hooks/useNaviKriyaAudio.ts"
      to: "src/audio/sessionClock.ts"
      via: "createAudioSessionClock(audioCtx) inside begin()"
      pattern: "createAudioSessionClock\\(audioCtx\\)"
    - from: "src/hooks/useNKEngine.ts"
      to: "src/audio/sessionClock.ts (via audioEngine.ts re-export)"
      via: "SessionClock import + clock.now() calls"
      pattern: "clock\\.now\\(\\)"
---

<objective>
Migrate the Navi Kriya pair (`useNaviKriyaAudio` + `useNKEngine`) off direct `performance.now()` and `audioCtx.currentTime` reads onto the `SessionClock` interface. `useNaviKriyaAudio.begin()` keeps its `new AudioContext()` call site (D-08 invariant — factories wrap, never construct; NK AC ownership stays in `begin()`) but immediately wraps the AC via `createAudioSessionClock` and reads `clock.now()` for cue timing. `useNKEngine` accepts a SessionClock arg for elapsed-stats math; its NK audio path is independent of this clock because the NK audio clock is per-session, constructed at `begin()`. ms→sec cascade applied to NK constants per D-02.

**D-03 / D-10 resolution (Option A) makes the cueWhen substitution byte-identical at runtime.** Plan 50-01's `createAudioSessionClock.now()` returns `audioCtx.currentTime` (NOT `performance.now() / 1000`), so swapping `useNaviKriyaAudio.ts:75` from `audioCtx.currentTime + SAFE_LEAD_SEC` to `clock.now() + SAFE_LEAD_SEC` is a literal identifier swap with no value change. `scheduleNKFrontMarker(when, ...)` continues to receive an AC-shaped time value — correctness is preserved. Phase 51's actual work is caller-level rebase of `useSessionEngine` (per ROADMAP Phase 51 #3), NOT a swap of this factory body.

**Revision 1 Warning #6 — `estimateNaviKriyaDurationMs` consumers pre-enumerated:** `git grep -n estimateNaviKriyaDurationMs src/` returns exactly 2 lines, both inside `src/domain/naviKriyaSession.ts`:
- L35: function declaration `export function estimateNaviKriyaDurationMs(settings: NaviKriyaSettings): number`
- L47: sole consumer `estimateNaviKriyaDurationMinutes` — `Math.round(estimateNaviKriyaDurationMs(settings) / 60_000)`

No external consumers exist. The rename is fully scoped to this one file. `estimateNaviKriyaDurationMinutes`'s body will become `Math.round(estimateNaviKriyaDurationSec(settings) / 60)` (dividing seconds by 60 instead of ms by 60_000).

**Revision 1 Warning #12 — phase-level invariant note** (also recorded in Plans 50-01 and 50-04): Phase 50 has exactly TWO `createAudioSessionClock` invocations:
1. Inside `createAudioEngine` (HRV AC) — exposed via `engine.clock`.
2. Inside `useNaviKriyaAudio.begin()` (NK AC) — local to NK's lifecycle; NEVER stored on the engine; NEVER read by HRV code.

These wrap DIFFERENT AudioContexts and MUST NOT be conflated. NK has its own AudioContext (per D-08 — explicit out-of-scope at Phase 50 to fold into the engine), and its clock subscribes only to the NK AC's statechange events. HRV's engine.clock subscribes only to the engine's HRV AC's statechange events. Any cross-wiring would break iOS audio-session lifecycle independence between HRV and NK.

Purpose: ABSTR-03 partial satisfaction (NK pair is callers 2 and 3 of 5). ABSTR-04 maintained — test parity at the baseline established at end of Plan 50-02. D-08 user-gesture-chain AC construction preserved verbatim — the only change inside `begin()` is one extra synchronous line that wraps the AC.

Output: NK audio path reads `clock.now()` instead of `audioCtx.currentTime`; NK engine consumes a clock arg. NK_LEAD_MS → NK_LEAD_SEC (5 seconds); NKEngineRecord fields renamed; tests assert seconds-shaped values. setTimeout-boundary multiply-by-1000 is the only ms-shaped surviving value.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/50-sessionclock-scheduler-abstraction/50-CONTEXT.md
@.planning/phases/50-sessionclock-scheduler-abstraction/50-PATTERNS.md
@.planning/phases/50-sessionclock-scheduler-abstraction/50-01-SUMMARY.md

<interfaces>
<!-- Existing useNaviKriyaAudio.begin signature (src/hooks/useNaviKriyaAudio.ts L59): -->
const begin = useCallback((getTimbre: () => TimbreId): NaviKriyaAudioSession => { ... }, [])

<!-- AC construction at L44 (D-08 invariant — NK AC ownership stays here): -->
function createOptionalAudioContext(): AudioContext | null {
  try { return new AudioContext() } catch { return null }
}

<!-- Single audioCtx.currentTime read at L75: -->
const cueWhen = (): number => audioCtx.currentTime + SAFE_LEAD_SEC

<!-- Current useNKEngine signature (src/hooks/useNKEngine.ts L71): -->
export function useNKEngine(): NKEngineApi

<!-- After this plan: -->
export function useNKEngine(clock: SessionClock): NKEngineApi

<!-- Current NK constants (src/domain/naviKriyaSession.ts L9-10): -->
export const NK_LEAD_MS = 5000
export const NK_LAST_OM_HOLD_MULTIPLIER = 1.5

<!-- After this plan: -->
export const NK_LEAD_SEC = 5
export const NK_LAST_OM_HOLD_MULTIPLIER = 1.5  // unchanged (dimensionless multiplier)

<!-- 3 performance.now() call sites in useNKEngine.ts: L132, L187, L218 -->

<!-- Revision 1 Warning #6: estimateNaviKriyaDurationMs consumers (pre-enumerated): -->
<!-- src/domain/naviKriyaSession.ts:35   export function estimateNaviKriyaDurationMs(...)  -->
<!-- src/domain/naviKriyaSession.ts:47   Math.round(estimateNaviKriyaDurationMs(settings) / 60_000)  -->
<!-- ↑ Exactly 2 references, both in the same file. No external consumers. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wrap useNaviKriyaAudio's AC via createAudioSessionClock inside begin() and substitute clock.now() at L75</name>
  <files>src/hooks/useNaviKriyaAudio.ts, src/hooks/useNaviKriyaAudio.test.tsx</files>
  <read_first>
    - .planning/phases/50-sessionclock-scheduler-abstraction/50-CONTEXT.md (D-03 resolved Option A: `createAudioSessionClock.now()` returns `audioCtx.currentTime` from Phase 50; D-07 createAudioSessionClock factory wraps an AC; D-08 NK AC construction stays in begin() — factory WRAPS, never constructs; D-09 audioCtx.currentTime appears ONLY inside the factory after Phase 50, NOT in this caller)
    - .planning/phases/50-sessionclock-scheduler-abstraction/50-PATTERNS.md (`src/hooks/useNaviKriyaAudio.ts` migration subsection — Construction-stays-in-begin pattern at L42-49 + L59-61; the 1 audioCtx.currentTime read at L75 routes through clock.now())
    - src/hooks/useNaviKriyaAudio.ts (entire file — confirm begin() body and the cueWhen closure)
    - src/audio/sessionClock.ts (Plan 50-01 — createAudioSessionClock signature, including the revision 1 scheduleImpl optional second arg; useNaviKriyaAudio passes ONLY the AC — no scheduleImpl — so its clock.schedule is a no-op at Phase 50)
    - src/hooks/useNaviKriyaAudio.test.tsx (existing test setup — confirm whether tests stub the AC; the FakeAudioContext polyfill in vitest setup is the same one used by useAudioCues tests)
  </read_first>
  <action>
    Edit `src/hooks/useNaviKriyaAudio.ts`:

    1. Add import: `import { createAudioSessionClock } from '../audio/sessionClock'`.

    2. Inside `begin` (L59-98), immediately after the AC null-check at L63-68, construct the clock. The added line is: `const clock = createAudioSessionClock(audioCtx)`. **Per revision 1 Warning #12: this is THE second of exactly two `createAudioSessionClock` invocations in Phase 50 — the first is inside `createAudioEngine` in `audioEngine.ts`. This NK clock wraps the NK AC, is local to NK's lifecycle, and MUST NOT be conflated with the engine's HRV clock.** Per revision 1 Blocker #2: NK passes NO `scheduleImpl` (the second factory arg is omitted), so NK's `clock.schedule()` is a no-op at Phase 50 — NK continues to call `scheduleNKFrontMarker` / `scheduleNKBackMarker` / `scheduleNKTick` / `scheduleEndChord` directly per Plan 50-03's Phase 50 scope. Place the construction line immediately after the null-return guard and before the existing `const timbre = getTimbre()` line. Document with an inline comment: "// Phase 50 D-08: wrap the AC immediately after construction. The audioCtx is the source of truth (NK AC ownership stays in begin per D-08); the clock is the typed surface for time reads (D-09 — audioCtx.currentTime is no longer read directly by this caller). Per D-03 Option A (resolved 2026-05-27), clock.now() returns audioCtx.currentTime — byte-identical to the pre-refactor read. Per revision 1 Warning #12: this is the NK clock, NOT the engine clock; they wrap distinct AudioContexts."

    3. Replace L75 `const cueWhen = (): number => audioCtx.currentTime + SAFE_LEAD_SEC` with `const cueWhen = (): number => clock.now() + SAFE_LEAD_SEC`. This is a one-token swap. **Per D-03 Option A:** at runtime, `clock.now()` evaluates to `audioCtx.currentTime`, so the substitution is byte-identical. The drift-guard (Plan 50-07) bans the direct `audioCtx.currentTime` token read in this file; the substitution removes that token from this file's source.

    4. Do NOT modify the existing AC construction at L44 (`new AudioContext()` via `createOptionalAudioContext`), the NOOP fallback path, the close()/closeAfterEndCue() teardown paths, the mutedRef pattern, or the per-cue scheduleNK* dispatches (Plan 50-06 covers the optional further migration through clock.schedule() — at Phase 50 the direct scheduleNKFrontMarker / scheduleNKBackMarker / scheduleNKTick / scheduleEndChord calls stay because they take `audioCtx` directly, and the drift-guard only bans `audioCtx.currentTime` reads, not `audioCtx` argument passing to per-cue builders).

    5. Per D-08: do NOT touch the `new AudioContext()` call site at L44. The factory pattern is wrap-don't-construct. The drift-guard's regex for `\bnew\s+AudioContext\b` matches the token in `createOptionalAudioContext`'s body. Acceptance criterion below specifies that the existing single occurrence remains exactly one, and Plan 50-07's drift-guard will be configured to exempt this file's `new AudioContext` ban (per D-08 invariant; see Plan 50-07's exemption mechanism).

    6. The clock variable is captured into the four returned closures (frontMarker / backMarker / tick / endCue) via `cueWhen()`. Since `cueWhen` already closed over `audioCtx`, the change is byte-for-byte at the value level (clock.now() === audioCtx.currentTime per Option A) and at the closure-capture level (one closed-over name swap).

    7. Update `src/hooks/useNaviKriyaAudio.test.tsx`: tests should pass without modification because the FakeAudioContext polyfill's `currentTime` continues to drive `clock.now()` exactly as it drove `audioCtx.currentTime` before (Option A makes them the same value). If a test explicitly verifies `audioCtx.currentTime` is read by `cueWhen` via spying on a property read, update the assertion to verify `clock.now()` is called — but most NK tests assert on cue scheduling outcomes (cue was called at expected `when`), not on the time-source identity. Run the suite and fix only what breaks; expected diff is minimal or zero.
  </action>
  <verify>
    <automated>pnpm test:run src/hooks/useNaviKriyaAudio.test.tsx</automated>
    <automated>pnpm test:run</automated>
    <automated>pnpm build</automated>
    <automated>pnpm lint</automated>
    <source_assertion>grep -v '^ *//' src/hooks/useNaviKriyaAudio.ts | grep -v '^ *\*' | grep -c "audioCtx\\.currentTime" returns 0</source_assertion>
    <source_assertion>grep -c "createAudioSessionClock" src/hooks/useNaviKriyaAudio.ts returns at least 2 (import + invocation)</source_assertion>
    <source_assertion>grep -c "clock\\.now()" src/hooks/useNaviKriyaAudio.ts returns 1</source_assertion>
    <source_assertion>grep -c "new AudioContext" src/hooks/useNaviKriyaAudio.ts returns 1 (the existing call at L44 — D-08 invariant preserved)</source_assertion>
  </verify>
  <acceptance_criteria>
    - `src/hooks/useNaviKriyaAudio.ts` imports `createAudioSessionClock` from `'../audio/sessionClock'`.
    - `begin()` constructs the clock on a new line immediately after the AC null-check (one new line per the action spec); the cueWhen closure reads `clock.now() + SAFE_LEAD_SEC`.
    - **Revision 1 Warning #12:** the inline comment at the clock construction site cites "NK clock, NOT engine clock; they wrap distinct AudioContexts."
    - **Revision 1 Blocker #2:** `createAudioSessionClock(audioCtx)` is called with exactly ONE argument (no scheduleImpl) — NK's clock.schedule is a no-op at Phase 50.
    - `new AudioContext()` (via `createOptionalAudioContext`) appears exactly once at L44 — D-08 NK AC ownership invariant preserved.
    - `audioCtx.currentTime` appears zero times in source lines (excluding JSDoc/comments) of `src/hooks/useNaviKriyaAudio.ts`.
    - Full suite passes; test count unchanged from end of Plan 50-02.
    - `pnpm build` exits 0; `pnpm lint` exits 0.
    - Byte-identical end-user behavior: an NK practice session schedules its front-marker / back-marker / tick cues at exactly the same `when` values as pre-refactor (because `clock.now() === audioCtx.currentTime` per Option A).
  </acceptance_criteria>
  <done>
    NK audio path consumes SessionClock; cueWhen reads clock.now(); existing AC construction preserved; behavior byte-identical per Option A; revision 1 Warning #12 invariant comment in place.
  </done>
</task>

<task type="auto">
  <name>Task 2: ms→sec rename of NK_LEAD_MS and NKEngineRecord time fields in naviKriyaSession.ts (revision 1 Warning #6 — sole external consumer pre-enumerated)</name>
  <files>src/domain/naviKriyaSession.ts, src/domain/naviKriyaSession.test.ts, src/domain/naviKriyaSettings.ts</files>
  <read_first>
    - .planning/phases/50-sessionclock-scheduler-abstraction/50-CONTEXT.md (D-02 ms→sec rename in useNKEngine and naviKriyaSession; LSP rename for cross-file cascade)
    - .planning/phases/50-sessionclock-scheduler-abstraction/50-PATTERNS.md (useNKEngine.ts rename cascade — NK_LEAD_MS → NK_LEAD_SEC; NK_OM_SECONDS no rename — already seconds-shaped; estimateNaviKriyaDurationMs internal multiplies; LSP rename pattern)
    - src/domain/naviKriyaSession.ts (entire file — NK_LEAD_MS at L9, NK_OM_SECONDS at L3-7, NK_LAST_OM_HOLD_MULTIPLIER at L10, estimateNaviKriyaDurationMs at L35-44, estimateNaviKriyaDurationMinutes at L46-48)
    - src/domain/naviKriyaSession.test.ts (tests for these constants and helpers)
    - src/domain/naviKriyaSettings.ts (verified during revision 1: does NOT reference NK_LEAD_MS or estimateNaviKriyaDurationMs)
  </read_first>
  <action>
    **Revision 1 Warning #6 pre-flight check** (run before edits):
    Run `git grep -n estimateNaviKriyaDurationMs src/` and confirm the output is exactly:
    ```
    src/domain/naviKriyaSession.ts:35:export function estimateNaviKriyaDurationMs(settings: NaviKriyaSettings): number {
    src/domain/naviKriyaSession.ts:47:  return Math.round(estimateNaviKriyaDurationMs(settings) / 60_000)
    ```
    These are the ONLY two references in the entire src/ tree. The function is defined and consumed within the same file. If this grep returns ANY other line, STOP and report a new consumer requiring scope expansion — the action below assumes the rename is fully in-file.

    Run `git grep -n NK_LEAD_MS src/` and inventory all consumers. Expected consumers (verified during revision 1):
    - `src/domain/naviKriyaSession.ts` — declaration
    - `src/hooks/useNKEngine.ts` — multiple imports and call sites (handled by Task 3)
    Add any other matches to the files_modified list for this task before editing.

    Edit `src/domain/naviKriyaSession.ts`:

    1. Rename `NK_LEAD_MS = 5000` → `NK_LEAD_SEC = 5`. Update the export. Use LSP rename to propagate every consumer (do NOT sed/perl, per use-lsp-for-renames memory). The LSP rename will touch `src/hooks/useNKEngine.ts` (Task 3 handles that file's other edits in parallel).

    2. `NK_OM_SECONDS` is already seconds-shaped (per the existing source: `fast: 1.75, medium: 2.16, slow: 3.0`). Leave the value and the constant name unchanged — it IS the canonical seconds-shaped source. Document this in a comment if not already present: "// Already seconds-shaped per D-02 — no rename needed."

    3. **Revision 1 Warning #6 — rename `estimateNaviKriyaDurationMs` → `estimateNaviKriyaDurationSec` (sole consumer at L47 is in-file).** Use LSP rename. The body changes are:
       - L37: `const omMs = NK_OM_SECONDS[settings.omLength] * 1000` → `const omSec = NK_OM_SECONDS[settings.omLength]` (no `* 1000`).
       - L40-43: the body computes `rounds * (...) * omMs + rounds * 2 * NK_LEAD_MS`. After renames: `rounds * (...) * omSec + rounds * 2 * NK_LEAD_SEC`. Result is in seconds (not milliseconds).
       - L47 sole consumer `estimateNaviKriyaDurationMinutes` body: `Math.round(estimateNaviKriyaDurationMs(settings) / 60_000)` → `Math.round(estimateNaviKriyaDurationSec(settings) / 60)`. The function signature `estimateNaviKriyaDurationMinutes(settings): number` returns minutes — unchanged externally; only the inner division boundary changes (60 instead of 60_000).

    4. Tests in `naviKriyaSession.test.ts`: assertions on `NK_LEAD_MS === 5000` become `NK_LEAD_SEC === 5`. Assertions on `estimateNaviKriyaDurationMs(...)` returning a ms value become `estimateNaviKriyaDurationSec(...)` returning a seconds value (divide expected literal by 1000). Assertions on `estimateNaviKriyaDurationMinutes` are UNCHANGED in expected return value (still minutes-shaped) — only the internal arithmetic shifts. Per `no-design-locking`: prefer behavior assertions over magic numbers where possible.

    5. `src/domain/naviKriyaSettings.ts`: verified during revision 1 — does NOT reference NK_LEAD_MS or estimateNaviKriyaDurationMs. No edits required. (Still included in files_modified for completeness — LSP rename will skip it.)

    Test count constraint (ABSTR-04): 1:1 mapping. No additions, no removals, no skips.
  </action>
  <verify>
    <automated>pnpm test:run src/domain/naviKriya</automated>
    <automated>pnpm test:run</automated>
    <automated>pnpm build</automated>
    <automated>pnpm lint</automated>
    <source_assertion>grep -c "NK_LEAD_MS" src/domain/naviKriyaSession.ts returns 0</source_assertion>
    <source_assertion>grep -c "NK_LEAD_SEC" src/domain/naviKriyaSession.ts returns 1</source_assertion>
    <source_assertion>grep -rn "NK_LEAD_MS" src/ returns 0 matches (all consumers updated by LSP rename)</source_assertion>
    <source_assertion>grep -rn "estimateNaviKriyaDurationMs" src/ returns 0 matches (revision 1 Warning #6 — sole consumer at naviKriyaSession.ts:47 renamed in-file)</source_assertion>
    <source_assertion>grep -c "estimateNaviKriyaDurationSec" src/domain/naviKriyaSession.ts returns 2 (declaration + sole consumer at L47)</source_assertion>
  </verify>
  <acceptance_criteria>
    - `src/domain/naviKriyaSession.ts` exports `NK_LEAD_SEC = 5` (and NO export of `NK_LEAD_MS`).
    - `estimateNaviKriyaDurationMs` is renamed to `estimateNaviKriyaDurationSec` and its body returns seconds.
    - `estimateNaviKriyaDurationMinutes` body reads `estimateNaviKriyaDurationSec(settings) / 60` (sole consumer updated in-file per revision 1 Warning #6).
    - `grep -rn "NK_LEAD_MS" src/` returns zero matches (all consumers updated).
    - `grep -rn "estimateNaviKriyaDurationMs" src/` returns zero matches (revision 1 Warning #6).
    - Full suite passes; test count unchanged.
    - `pnpm build` exits 0; `pnpm lint` exits 0.
  </acceptance_criteria>
  <done>
    NK domain constants and helpers are seconds-shaped; LSP rename complete; estimateNaviKriyaDurationMs's sole in-file consumer updated; full suite green.
  </done>
</task>

<task type="auto">
  <name>Task 3: Migrate useNKEngine off performance.now() onto clock.now() and rename NKEngineRecord time fields</name>
  <files>src/hooks/useNKEngine.ts, src/hooks/useNKEngine.test.tsx, src/hooks/useNaviKriyaSessionController.ts</files>
  <read_first>
    - .planning/phases/50-sessionclock-scheduler-abstraction/50-CONTEXT.md (D-01 seconds-shaped now(); D-02 ms→sec; D-07 createWallSessionClock for non-audio-scheduling time reads; D-09 only factories may touch performance.now())
    - .planning/phases/50-sessionclock-scheduler-abstraction/50-PATTERNS.md (`src/hooks/useNKEngine.ts` migration subsection — 3 performance.now() calls at L132, L187, L218; rename cascade table — NKEngineRecord.startedAtMs → startedAtSec, omMs → omSec, pendingDelayMs → pendingDelaySec; NK_OM_SECONDS multiplication boundary; setTimeout(stepOmRef.current, delayMs) → setTimeout(..., delaySec * 1000); test diff scope re-asserts seconds-shaped values)
    - src/hooks/useNKEngine.ts (entire file — confirm exact line numbers of performance.now() and the schedule() helper at L89-93)
    - src/hooks/useNaviKriyaSessionController.ts (L54 `const nkEngine = useNKEngine()` — must thread a SessionClock arg)
    - src/audio/sessionClock.ts (Plan 50-01 — confirm createWallSessionClock export; the revision 1 SessionClock interface now has 6 members including onClose)
    - src/hooks/useNKEngine.test.tsx (existing assertions on NK engine state — to be updated for seconds)
  </read_first>
  <action>
    Edit `src/hooks/useNKEngine.ts`:

    1. Add import: `import type { SessionClock } from '../audio/audioEngine'`.

    2. Update signature: `export function useNKEngine(clock: SessionClock): NKEngineApi` (was `useNKEngine(): NKEngineApi`).

    3. Update import of NK constants: `import { NK_LAST_OM_HOLD_MULTIPLIER, NK_LEAD_SEC, NK_OM_SECONDS } from '../domain/naviKriyaSession'` (NK_LEAD_MS removed per Task 2; NK_LEAD_SEC added). Same for re-export at L17: `export { NK_LAST_OM_HOLD_MULTIPLIER, NK_LEAD_SEC, NK_OM_SECONDS } from '../domain/naviKriyaSession'`.

    4. Rename NKEngineRecord fields via LSP rename:
       - `omMs: number` → `omSec: number`
       - `startedAtMs: number` → `startedAtSec: number`
       - `pendingDelayMs: number` → `pendingDelaySec: number`

    5. Rename NKOnComplete callback field: `elapsedMs: number` → `elapsedSec: number`.

    6. Replace the 3 performance.now() call sites:
       - L132: `const elapsedMs = performance.now() - e.startedAtMs` → `const elapsedSec = clock.now() - e.startedAtSec` (and the immediately-following `elapsedMs: elapsedMs` rename to `elapsedSec: elapsedSec` in the onComplete invocation).
       - L187: `startedAtMs: performance.now()` → `startedAtSec: clock.now()`
       - L218: `elapsedMs: performance.now() - e.startedAtMs` → `elapsedSec: clock.now() - e.startedAtSec`

    7. Update the schedule helper at L89-93 — the `delayMs` parameter name and the setTimeout boundary. The helper accepts a seconds-shaped value, stores it on the engine record as seconds, and multiplies by 1000 ONLY at the setTimeout boundary:
       - Rename the parameter to `delaySec`.
       - Change `eng.current.pendingDelayMs = delayMs` to `eng.current.pendingDelaySec = delaySec`.
       - Change `setTimeout(() => { stepOmRef.current() }, delayMs)` to `setTimeout(() => { stepOmRef.current() }, delaySec * 1000)`.
       - Add an inline comment at the setTimeout call: "// D-02 boundary: setTimeout takes ms; engine record is seconds-shaped. Multiply at this boundary only."

    8. Update all schedule() call sites:
       - L117: `schedule(NK_LEAD_MS)` → `schedule(NK_LEAD_SEC)`
       - L124: `schedule(NK_LEAD_MS)` → `schedule(NK_LEAD_SEC)`
       - L158: `schedule(e.omMs * NK_LAST_OM_HOLD_MULTIPLIER)` → `schedule(e.omSec * NK_LAST_OM_HOLD_MULTIPLIER)`
       - L160: `schedule(e.omMs)` → `schedule(e.omSec)`
       - L190: `pendingDelayMs: NK_LEAD_MS` → `pendingDelaySec: NK_LEAD_SEC`
       - L204: `schedule(NK_LEAD_MS)` → `schedule(NK_LEAD_SEC)`

    9. Update L185: `omMs: NK_OM_SECONDS[settings.omLength] * 1000` → `omSec: NK_OM_SECONDS[settings.omLength]` (no `* 1000`).

    10. Update useCallback dep arrays: the `clock` value is captured in the `stepOm` callback (via closure) — since it's a parameter, it's stable across renders if the caller threads a memoized clock. Add `clock` to the dep array of `start` (L168-205) since `start` calls `clock.now()` indirectly via the engine record initialization. Audit each useCallback for dep correctness; document any disabled exhaustive-deps rule with a Reason comment per project conventions.

    Edit `src/hooks/useNaviKriyaSessionController.ts`:

    1. Add import: `import { createWallSessionClock } from '../audio/sessionClock'`.

    2. Construct a memoized wall clock instance: `const nkClock = useMemo(() => createWallSessionClock(), [])`. Document: "// Phase 50 D-07: wall clock for useNKEngine elapsed-stats math (not for audio scheduling — NK audio clock is constructed per-session inside useNaviKriyaAudio.begin() and is independent of this). D-09 — only legitimate performance.now() use is inside the factory. Revision 1 Warning #12: this wall clock is for NK engine stats only; the NK AUDIO clock is a SEPARATE createAudioSessionClock invocation inside useNaviKriyaAudio.begin() — they MUST NOT be conflated."

    3. Update the call at L54: `const nkEngine = useNKEngine(nkClock)`.

    Edit `src/hooks/useNKEngine.test.tsx`:

    1. Every test construction `useNKEngine()` must now pass a clock. Use `createWallSessionClock()` from `'../audio/sessionClock'`, or a small `vi.fn()`-based stub clock with controllable `now()`. **Per revision 1 Blocker #1: the stub MUST include `onClose: () => () => undefined` as a no-op** (the SessionClock interface gained a 6th member in revision 1).

    2. Assertions on the `elapsedMs` field of the onComplete result → `elapsedSec`. Numeric literals in ms → divide by 1000.

    3. Assertions on `e.startedAtMs` (if any test inspects the internal record — should be discouraged but may exist) → `startedAtSec`. Same for `omMs` → `omSec`.

    4. Vitest fake-timer-driven tests: the wall clock reads `performance.now() / 1000`, which Vitest fake timers control via `vi.setSystemTime` / `vi.advanceTimersByTime`. Those drives continue to work. Where the test previously asserted "after 10000 ms, elapsedMs was 10000", it now asserts "after 10000 ms of vi.advanceTimersByTime, elapsedSec was 10" (the test setup still calls fake-timer-by-ms; the assertion just compares against the seconds-shaped output).

    Test count constraint: 1:1 mapping. No additions, no removals.
  </action>
  <verify>
    <automated>pnpm test:run src/hooks/useNKEngine.test.tsx</automated>
    <automated>pnpm test:run</automated>
    <automated>pnpm build</automated>
    <automated>pnpm lint</automated>
    <source_assertion>grep -c "performance\\.now()" src/hooks/useNKEngine.ts returns 0</source_assertion>
    <source_assertion>grep -c "clock\\.now()" src/hooks/useNKEngine.ts returns 3</source_assertion>
    <source_assertion>grep -c "startedAtMs\\|elapsedMs\\|pendingDelayMs\\|omMs" src/hooks/useNKEngine.ts returns 0</source_assertion>
    <source_assertion>grep -c "delaySec \\* 1000" src/hooks/useNKEngine.ts returns 1 (the setTimeout boundary)</source_assertion>
    <source_assertion>grep -c "useNKEngine(nkClock)" src/hooks/useNaviKriyaSessionController.ts returns 1</source_assertion>
  </verify>
  <acceptance_criteria>
    - `src/hooks/useNKEngine.ts` contains zero `performance.now()` references in source lines.
    - `src/hooks/useNKEngine.ts` contains exactly 3 `clock.now()` references (at the 3 original sites).
    - NKEngineRecord, NKOnComplete, and the schedule helper use seconds-shaped field/parameter names: `startedAtSec`, `omSec`, `pendingDelaySec`, `elapsedSec`, `delaySec`.
    - Exactly one `* 1000` conversion exists in the file — inside the schedule helper's setTimeout call (D-02 boundary).
    - `src/hooks/useNaviKriyaSessionController.ts` constructs `nkClock` via `useMemo(() => createWallSessionClock(), [])` and passes it to `useNKEngine`.
    - **Revision 1 Warning #12:** comment on the `nkClock` construction cites the "NK audio clock is SEPARATE from this wall clock" invariant.
    - **Revision 1 Blocker #1:** test stubs for SessionClock include the `onClose` member (6-member interface).
    - Full suite passes; test count unchanged from end of Task 2.
    - `pnpm build` exits 0; `pnpm lint` exits 0.
  </acceptance_criteria>
  <done>
    useNKEngine consumes SessionClock; NK engine record seconds-shaped; controller wires the clock; full suite green.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | NK audio clock and NK engine clock are both in-process; no external I/O, no new untrusted inputs. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-50.03-01 | Tampering | npm install during this plan | accept | DEPS-01: zero new entries in `package.json`. |
| T-50.03-02 | Information disclosure | NK timing fields exposed as seconds vs ms | accept | Same as Plan 50-02 — unit conversion is identity-preserving; no new exposure. |
| T-50.03-03 | Tampering | LSP rename across NK pair (engine ↔ session domain ↔ tests) | mitigate | LSP rename catches every site; per-commit green-gate (`pnpm build` + `pnpm lint` + `pnpm test:run`) verifies. Post-rename grep gates in acceptance criteria assert zero ms-shaped tokens remain. Revision 1 Warning #6 pre-flight grep enumerates the sole external consumer (`estimateNaviKriyaDurationMinutes` at L47) before edits. |
| T-50.03-04 | Denial of service | setTimeout boundary using `delaySec * 1000` — floating-point multiplication precision | accept | `setTimeout` accepts a float and floors internally; `5 * 1000 = 5000` is exact; `1.75 * 1000 = 1750` is exact. NK_OM_SECONDS values (1.75, 2.16, 3.0) all multiply to integer ms (1750, 2160, 3000) within IEEE-754 representability — no rounding drift. |
| T-50.03-05 | Spoofing | clock.now() returning audioCtx.currentTime (Option A) substituted for direct audioCtx.currentTime read | accept | Per Plan 50-01 / D-03 Option A, the values are equal by construction. Byte-identical end-user behavior preserved (Task 1 acceptance criterion). No new attack surface. |
| T-50.03-06 | Repudiation | NK clock conflated with engine clock (revision 1 Warning #12) | mitigate | Inline comments at every `createAudioSessionClock` and `createWallSessionClock` invocation in NK code explicitly call out the invariant: "NK clock is local to NK's lifecycle; NEVER stored on the engine; NEVER read by HRV code." Tests rely on FakeAudioContext per-instance — no cross-instance coupling. |

**Reasoning:** Pure identifier-rename + unit-conversion refactor with one architectural change (the NK audio AC is now wrapped by createAudioSessionClock immediately after construction). The wrap is a no-op for end-user behavior at Phase 50 because Option A makes `clock.now() === audioCtx.currentTime`. No new attack surface. Revision 1 Warning #12 invariant is documented via inline comments to prevent future conflation.
</threat_model>

<verification>
- `pnpm build` exits 0; `pnpm lint` exits 0; `pnpm test:run` exits 0.
- Test count unchanged from end of Plan 50-02.
- `git diff package.json` is empty (DEPS-01).
- `grep -rn "performance\\.now()" src/hooks/useNKEngine.ts src/hooks/useNaviKriyaAudio.ts` returns zero matches.
- `grep -rn "audioCtx\\.currentTime" src/hooks/useNaviKriyaAudio.ts` returns zero matches (source lines; comments may reference history).
- `grep -rn "NK_LEAD_MS" src/` returns zero matches.
- **Revision 1 Warning #6:** `grep -rn "estimateNaviKriyaDurationMs" src/` returns zero matches.
- Drift-guard (Plan 50-07) will pass by construction against these two files at the close of this plan.
</verification>

<success_criteria>
- ABSTR-03 partial: NK pair (useNaviKriyaAudio + useNKEngine) consumes SessionClock; zero direct performance.now() / audioCtx.currentTime reads in source lines.
- ABSTR-04: test parity preserved.
- D-08 invariant preserved: NK AC construction stays in `useNaviKriyaAudio.begin()`; the factory wraps the AC after construction.
- D-02 ms→sec cascade applied to NK constants and engine record.
- D-03 Option A substitution at L75 is byte-identical (clock.now() === audioCtx.currentTime).
- **Revision 1 Warning #6:** sole external consumer of `estimateNaviKriyaDurationMs` (`estimateNaviKriyaDurationMinutes` at L47) pre-enumerated and updated in-file.
- **Revision 1 Warning #12:** phase-level invariant note recorded in truths + inline comments at every NK clock construction site.
- DEPS-01 + QUAL-01 verified.
- Byte-identical end-user behavior: NK practice plays the same sequence of cues at the same intervals as before. Validation: a full NK session (1 round, fast OM) completes within ±5 ms of the pre-refactor timing (an artifact of the setTimeout(delaySec * 1000) boundary; not a regression).
</success_criteria>

<output>
Create `.planning/phases/50-sessionclock-scheduler-abstraction/50-03-SUMMARY.md` when done. Confirm Option A substitution at L75 is byte-identical. Document the `git grep` output from the Task 2 pre-flight check (revision 1 Warning #6) — exactly 2 lines, both in naviKriyaSession.ts. Document any cross-file consumers of `NK_LEAD_MS` that surfaced via LSP rename (expected: useNKEngine.ts only).
</output>
</content>
