---
phase: 50-sessionclock-scheduler-abstraction
plan: 06
type: execute
wave: 3
depends_on:
  - 50-01
  - 50-04
files_modified:
  - src/audio/audioEngine.ts
  - src/audio/audioEngine.test.ts
autonomous: true
requirements:
  - ABSTR-01
  - ABSTR-04
  - DEPS-01
  - QUAL-01
requirements_addressed:
  - ABSTR-01
  - ABSTR-04
  - DEPS-01
  - QUAL-01
tags:
  - sessionclock
  - audio
  - audioengine
  - facade
must_haves:
  truths:
    - "`audioEngine.ts` has an internal `schedule(when: number, cue: Cue): void` function that performs `switch (cue.kind)` dispatch to the existing per-cue scheduler primitives (`scheduleCountdownTick`, `scheduleInCueForTimbre`, `scheduleOutCueForTimbre`, `scheduleEndChord`) per D-04 / D-05."
    - "`scheduleLeadIn` is refactored into a thin facade that builds 4 `Cue` values (3 `lead-in-tick` + 1 `in`) and calls `schedule(when, cue)` for each — replacing the prior 4 direct calls to `scheduleCountdownTick` / `scheduleInCueForTimbre`. The 3-2-1 timing pattern, the `closed` / `muted` early-return guards, the `firstInCueTime` return value, and the `activeCues.add` bookkeeping are preserved verbatim (only the dispatch path changes)."
    - "`scheduleNextCue` is refactored into a thin facade that builds 1 `Cue` value (`kind: newPhase`) and calls `schedule(clampedAudioTime, cue)` — replacing the prior direct `scheduleInCueForTimbre` / `scheduleOutCueForTimbre` ternary. The SAFE_LEAD_SEC clamp at L324 stays in the facade (AUDIO-02 D-01/D-02 callee-side clamp invariant). pruneExpiredCues continues to run before the schedule."
    - "`playEndChord` is refactored into a thin facade that builds 1 `Cue` value (`kind: 'end-chord'`) and calls `schedule(when, cue)`. The endChordTailUntil bookkeeping at L343 is preserved — it captures the returned cue's `cleanupAt` for close() deferral."
    - "External engine API (the AudioEngine interface members `scheduleLeadIn`, `scheduleNextCue`, `playEndChord`) is unchanged. Wave 2 consumers (useAudioCues from Plan 50-04) call these the same way as pre-refactor."
    - "Engine internals continue to read `audioCtx.currentTime` directly (e.g., pruneExpiredCues at L286, the SAFE_LEAD_SEC clamp at L324, playEndChord's `when` at L337) — these reads are INSIDE the engine and OUTSIDE the drift-guard scope (which targets the 5 caller files exclusively per 50-CONTEXT.md specifics)."
    - "`Cue.kind === 'nk-front' | 'nk-back' | 'nk-tick' | 'countdown-tick'` cases in the `schedule` dispatch are present but currently UNUSED at Phase 50 (NK paths in useNaviKriyaAudio still call the per-cue schedulers directly per Plan 50-03 — D-05's NK migration through schedule() is documented as available but not exercised at Phase 50)."
    - "**Revision 1 Blocker #2 — `scheduleImpl` plumbed at construction (NOT post-hoc reassignment):** Plan 50-04 Task 1 step 3 constructs the clock as `createAudioSessionClock(audioCtx)` with no scheduleImpl. This plan AMENDS that construction to `createAudioSessionClock(audioCtx, scheduleImpl)` where `scheduleImpl = (when, cue) => internalSchedule(when, cue)`. No post-hoc `clock.schedule = ...` reassignment (the readonly clock contract is preserved). The amendment is a 1-line edit in createAudioEngine that happens AFTER the internal schedule function is defined."
    - "**Revision 2 Blocker #1 (carried from Plan 50-01 + Plan 50-04) — augmented clock type preserved across the move:** Plan 50-04 typed the engine''s internal clock reference as `SessionClock & { notifySuspended(): void }` (the augmented factory return type) so the L445 InvalidStateError catch could call `clock.notifySuspended()`. This plan''s clock move (step 3 + step 5) MUST preserve that augmented-type annotation — the new construction site at ~L295 uses the same type annotation `const clock: SessionClock & { notifySuspended(): void } = createAudioSessionClock(audioCtx, schedule)`. The L445 `clock.notifySuspended()` call (already in place from Plan 50-04) continues to type-check after the move. The engine.clock public member still widens to `SessionClock` at the boundary so external consumers cannot see notifySuspended."
    - "**Revision 2 Warning #6 — ordering parity (intermediate state ↔ final state):** Plan 50-04''s intermediate state had `clock` constructed at audioEngine.ts L177 (immediately after `new AudioContext()`). This plan''s final state moves the construction to ~L295 (post-schedule-function definition). Both states pass the per-commit green-gate: `pnpm build` + `pnpm lint` + `pnpm test:run` are green at each plan close. The clock''s listener attachment, subscribers Set lifecycle, `notifySuspended()` escape hatch, and L445 InvalidStateError fan-out are independent of the construction-site line number — moving it does NOT change observable behavior. The intermediate-state `clock.schedule()` is a no-op (no scheduleImpl supplied at Plan 50-04); after this plan it forwards to the engine''s internal dispatch."
    - "**Revision 1 Warning #9 — test count delta explicitly documented:** Phase 50 adds the following test files: drift-guard (Plan 50-07, ≥1 test), dispatch (Plan 50-06, 8 tests — one per Cue arm), sessionClock unit tests (Plan 50-01). Total Phase 50 test count = 1283 baseline + new additions (sessionClock.test.ts: ≥18 tests, audioEngine dispatch: 8 tests, drift-guard: 1-3 tests). The 1283 existing tests pass with the same assertion shape (renames only — Plans 50-02 through 50-05 perform identifier renames, not behavior changes). NO existing tests are removed; NO skips introduced."
  artifacts:
    - path: "src/audio/audioEngine.ts"
      provides: "Internal schedule(when, cue) with full Cue dispatch; facade refactor of scheduleLeadIn / scheduleNextCue / playEndChord; clock construction amended to pass scheduleImpl (revision 1 Blocker #2); engine internals continue to read audioCtx.currentTime directly."
      contains: "switch \\(cue.kind\\)"
  key_links:
    - from: "src/audio/audioEngine.ts (scheduleLeadIn / scheduleNextCue / playEndChord facades)"
      to: "src/audio/audioEngine.ts (internal schedule function)"
      via: "internal function call (schedule(when, cue))"
      pattern: "schedule\\(.*,.*\\{ kind:"
    - from: "src/audio/audioEngine.ts (schedule function)"
      to: "src/audio/cueSynth.ts and src/audio/nkCueSynth.ts (per-cue primitives)"
      via: "switch (cue.kind) dispatch"
      pattern: "switch \\(cue.kind\\)"
    - from: "src/audio/audioEngine.ts (createAudioEngine)"
      to: "src/audio/sessionClock.ts (createAudioSessionClock with scheduleImpl arg)"
      via: "scheduleImpl plumbed at construction (revision 1 Blocker #2)"
      pattern: "createAudioSessionClock\\(audioCtx,"
---

<objective>
Refactor `audioEngine.ts` so that `scheduleLeadIn`, `scheduleNextCue`, and `playEndChord` become **thin facades** over an internal `schedule(when, cue)` function that dispatches based on `cue.kind` per D-05. The internal `schedule` is the binding point for ABSTR-01's `SessionClock.schedule` member.

**Revision 1 Blocker #2 — `scheduleImpl` plumbed at construction (NOT post-hoc reassignment):** Plan 50-01 added an optional `scheduleImpl` parameter to `createAudioSessionClock(audioCtx, scheduleImpl?)`. Plan 50-04 Task 1 step 3 constructed the clock as `createAudioSessionClock(audioCtx)` with no scheduleImpl. This plan AMENDS that construction: after the internal `schedule` function is defined inside `createAudioEngine`, the clock construction line is changed to pass `schedule` as the second arg. To keep the order correct (the clock must be constructed AFTER the schedule function is defined, but BEFORE the engine object literal that references `clock`), the clock construction is MOVED from Plan 50-04's position (immediately after `new AudioContext()`) to a NEW position (immediately after the internal `schedule` function is defined, ~L295). The intervening statechange listener wiring is unaffected because Plan 50-04 already DELETED the local listener — the clock owns the only listener.

**Important ordering caveat:** Plan 50-04 currently places `const clock = createAudioSessionClock(audioCtx)` immediately after `const audioCtx = new AudioContext()` at L177. This plan REVISES that placement: the clock construction is MOVED to just after `function schedule(when, cue): void { ... }` (~L295), with `scheduleImpl = schedule` passed as the second arg. The engine object literal's `clock,` field at ~L368 reads the now-properly-wired clock. No post-hoc reassignment, no readonly violation.

External API surface stays unchanged — useAudioCues (Plan 50-04 consumer) sees the same `engine.scheduleLeadIn` / `engine.scheduleNextCue` / `engine.playEndChord` shapes AND the same `engine.clock` member.

Per CONTEXT specifics: engine internals are NOT in drift-guard scope. The `audioCtx.currentTime` reads at L286 (pruneExpiredCues), L324 (clamp), L337 (playEndChord when), L370 (now()), and L394 (close tail) all remain. The drift-guard (Plan 50-07) scans the 5 caller files exclusively.

The `Cue` discriminated union from Plan 50-01 is consumed in full by the dispatch — every `kind` has a switch arm (8 total: `'in'`, `'out'`, `'lead-in-tick'`, `'end-chord'`, `'nk-front'`, `'nk-back'`, `'nk-tick'`, `'countdown-tick'`). NK kinds are currently unused at Phase 50 (Plan 50-03 keeps direct calls to scheduleNK* primitives) — they exist so Phase 52's lookahead can pre-schedule typed Cue values without an interface change (D-06).

Purpose: ABSTR-01 fully satisfied (the `schedule(when, cue)` member is functional, not a stub — and the wiring is structural via constructor plumbing, not post-hoc mutation). ABSTR-04 maintained — test parity preserved through the facade refactor (with revision 1 Warning #9 explicit test count delta documentation).

Output: audioEngine.ts has an internal `schedule(when, cue)` with full dispatch; the clock is constructed with `scheduleImpl = schedule` (revision 1 Blocker #2); existing public methods become facades; engine tests pass; full suite green.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/50-sessionclock-scheduler-abstraction/50-CONTEXT.md
@.planning/phases/50-sessionclock-scheduler-abstraction/50-PATTERNS.md
@.planning/phases/50-sessionclock-scheduler-abstraction/50-01-SUMMARY.md
@.planning/phases/50-sessionclock-scheduler-abstraction/50-04-SUMMARY.md

<interfaces>
<!-- Current scheduleLeadIn body (L297-317): see PATTERNS.md "Facade pattern for scheduleLeadIn" -->
<!-- Becomes (per PATTERNS.md): 4 schedule() calls with kind: 'lead-in-tick' (×3) and kind: 'in' (×1) -->

<!-- Current scheduleNextCue body (L319-332): -->
<!-- Becomes: 1 schedule() call with kind: 'in' | 'out' (the newPhase value); SAFE_LEAD_SEC clamp stays. -->

<!-- Current playEndChord body (L334-344): -->
<!-- Becomes: 1 schedule() call with kind: 'end-chord'; endChordTailUntil bookkeeping stays. -->

<!-- New internal function (added in this plan): -->
function schedule(when: number, cue: Cue): void {
  switch (cue.kind) {
    case 'lead-in-tick':    activeCues.add(scheduleCountdownTick(audioCtx, when, audioCtx.destination, sessionTimbre)); return
    case 'in':              activeCues.add(scheduleInCueForTimbre(audioCtx, when, audioCtx.destination, sessionTimbre, cue.phaseDurationSec)); return
    case 'out':             activeCues.add(scheduleOutCueForTimbre(audioCtx, when, audioCtx.destination, sessionTimbre, cue.phaseDurationSec)); return
    case 'end-chord': {
      const c = scheduleEndChord(audioCtx, when, audioCtx.destination, sessionTimbre)
      activeCues.add(c)
      endChordTailUntil = Math.max(endChordTailUntil, c.cleanupAt)
      return
    }
    case 'countdown-tick':  activeCues.add(scheduleCountdownTick(audioCtx, when, audioCtx.destination, sessionTimbre)); return
    case 'nk-front':        activeCues.add(scheduleNKFrontMarker(audioCtx, when, audioCtx.destination, sessionTimbre)); return
    case 'nk-back':         activeCues.add(scheduleNKBackMarker(audioCtx, when, audioCtx.destination, sessionTimbre)); return
    case 'nk-tick':         activeCues.add(scheduleNKTick(audioCtx, when, audioCtx.destination, sessionTimbre)); return
  }
}

<!-- Revision 1 Blocker #2 — clock construction (MOVED from Plan 50-04 position to after the schedule function): -->
const clock: SessionClock = createAudioSessionClock(audioCtx, schedule)   // ← scheduleImpl plumbed at construction
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add internal schedule(when, cue) dispatch function inside createAudioEngine; AMEND clock construction to pass scheduleImpl (revision 1 Blocker #2 — no post-hoc reassignment)</name>
  <files>src/audio/audioEngine.ts</files>
  <read_first>
    - .planning/phases/50-sessionclock-scheduler-abstraction/50-CONTEXT.md (D-04 closed Cue catalog; D-05 engine internal dispatch from cue.kind to existing per-cue schedulers; existing scheduleLeadIn/scheduleNextCue/playEndChord become thin facades; engine internals are NOT in drift-guard scope)
    - .planning/phases/50-sessionclock-scheduler-abstraction/50-PATTERNS.md (`src/audio/audioEngine.ts` MODIFIED subsection — Facade pattern for scheduleLeadIn / scheduleNextCue / playEndChord; cueSynth.ts + nkCueSynth.ts dispatch target table)
    - src/audio/audioEngine.ts (entire file — confirm exact line numbers for scheduleLeadIn at L297-317, scheduleNextCue at L319-332, playEndChord at L334-344, pruneExpiredCues at L285-294; closure-captured state: closed, muted, sessionTimbre, audioCtx, activeCues, endChordTailUntil)
    - src/audio/cueSynth.ts (scheduleInCueForTimbre, scheduleOutCueForTimbre, scheduleCountdownTick signatures)
    - src/audio/nkCueSynth.ts (scheduleEndChord, scheduleNKFrontMarker, scheduleNKBackMarker, scheduleNKTick signatures)
    - src/audio/sessionClock.ts (Plan 50-01 with revision 1 amendments — Cue discriminated union; createAudioSessionClock now accepts optional scheduleImpl as second arg)
    - .planning/phases/50-sessionclock-scheduler-abstraction/50-04-SUMMARY.md (Plan 50-04 placed the clock construction immediately after `const audioCtx = new AudioContext()` — this plan MOVES that line to after the schedule function definition per revision 1 Blocker #2)
  </read_first>
  <action>
    Edit `src/audio/audioEngine.ts`:

    1. Add import: `import type { Cue } from './sessionClock'`. (The `SessionClock` re-export from Plan 50-01 Task 3 already exists; add the `Cue` import. The named import of `createAudioSessionClock` already exists from Plan 50-04 Task 1.)

    2. Verify imports for cueSynth + nkCueSynth primitives are present (they already are — confirm `scheduleInCueForTimbre`, `scheduleOutCueForTimbre`, `scheduleCountdownTick`, `scheduleEndChord`, `scheduleNKFrontMarker`, `scheduleNKBackMarker`, `scheduleNKTick` are all imported at the top of audioEngine.ts).

    3. **Revision 1 Blocker #2 — DELETE the current clock construction line placed by Plan 50-04.** Plan 50-04 added `const clock: SessionClock & { notifySuspended(): void } = createAudioSessionClock(audioCtx)` immediately after `const audioCtx = new AudioContext()` at L177 (the augmented-type annotation is from revision 2 Blocker #1 — required for the L445 `clock.notifySuspended()` call). This plan REMOVES that entire line (it will be re-added below the schedule function in step 4-5, preserving the augmented-type annotation so the L445 call continues to type-check).

    4. Inside `createAudioEngine`, after `pruneExpiredCues` is defined (~L285-294) and before the `const engine: AudioEngine = { ... }` literal (~L296), add the internal `schedule` function:
       ```
       // Phase 50 D-04 / D-05: internal dispatch from typed Cue to per-cue primitives.
       // Public methods (scheduleLeadIn, scheduleNextCue, playEndChord) become facades over this.
       // The closed/muted guards live in the facades (so each facade can choose its own
       // behavior, e.g., scheduleNextCue clamps the time, scheduleLeadIn returns firstInCueTime).
       // This function assumes the facade has already gated; do NOT add closed/muted checks here.
       function schedule(when: number, cue: Cue): void {
         switch (cue.kind) {
           case 'lead-in-tick':    activeCues.add(scheduleCountdownTick(audioCtx, when, audioCtx.destination, sessionTimbre)); return
           case 'countdown-tick':  activeCues.add(scheduleCountdownTick(audioCtx, when, audioCtx.destination, sessionTimbre)); return
           case 'in':              activeCues.add(scheduleInCueForTimbre(audioCtx, when, audioCtx.destination, sessionTimbre, cue.phaseDurationSec)); return
           case 'out':             activeCues.add(scheduleOutCueForTimbre(audioCtx, when, audioCtx.destination, sessionTimbre, cue.phaseDurationSec)); return
           case 'end-chord': {
             const c = scheduleEndChord(audioCtx, when, audioCtx.destination, sessionTimbre)
             activeCues.add(c)
             endChordTailUntil = Math.max(endChordTailUntil, c.cleanupAt)
             return
           }
           case 'nk-front':  activeCues.add(scheduleNKFrontMarker(audioCtx, when, audioCtx.destination, sessionTimbre)); return
           case 'nk-back':   activeCues.add(scheduleNKBackMarker(audioCtx, when, audioCtx.destination, sessionTimbre)); return
           case 'nk-tick':   activeCues.add(scheduleNKTick(audioCtx, when, audioCtx.destination, sessionTimbre)); return
         }
       }
       ```
       The function closes over `audioCtx`, `sessionTimbre`, `activeCues`, and `endChordTailUntil` — all already in scope.

       **Cue payload field-set finalization (per Plan 50-01 Task 1 step 2):** `'in'` and `'out'` cues carry `phaseDurationSec` (used by `scheduleInCueForTimbre` / `scheduleOutCueForTimbre`); `'in'` and `'out'` also carry `timbre`, but the engine ignores that field and uses its closed-over `sessionTimbre` per Phase 18 D-08 (capture-at-session-start posture). NK kinds and `'lead-in-tick'`/`'countdown-tick'`/`'end-chord'` carry no payload beyond `kind` — the engine resolves audioCtx + sessionTimbre + destination from its closure.

       Add an inline comment near the `case 'in'` / `case 'out'` arms: "// cue.timbre is in the type union for callers that pre-schedule cues without engine context (Phase 52 lookahead). At the engine layer, sessionTimbre is the source of truth per Phase 18 D-08 — we ignore cue.timbre here."

    5. **Revision 1 Blocker #2 + revision 2 Blocker #1 — construct the clock with scheduleImpl plumbed at construction AND preserve the augmented-type annotation.** Immediately after the `schedule` function definition (so `schedule` is in scope), add:
       ```
       // Phase 50 D-07: wrap the AC. D-11 + revision 1 Blocker #1: the clock owns the audioCtx
       // 'statechange' listener and fans suspend/resume/close to subscribers.
       // Revision 1 Blocker #2: scheduleImpl plumbed at construction (NOT post-hoc reassignment).
       // The clock's schedule() forwards to the engine's internal schedule function.
       // No readonly violation — the clock object's members are set once at construction.
       // Revision 2 Blocker #1: the local clock reference is typed as the AUGMENTED factory return
       // type `SessionClock & { notifySuspended(): void }` so the L445 InvalidStateError catch
       // (already in place from Plan 50-04) can call clock.notifySuspended(). The engine.clock
       // public member is widened to SessionClock at the assignment boundary — external consumers
       // cannot call notifySuspended.
       // Revision 2 Warning #6: this construction site (~L295, post-schedule-function) is the
       // FINAL position for the clock. Plan 50-04 placed it at L177 as intermediate; both
       // positions pass the per-commit green-gate. Moving the construction line does NOT change
       // observable behavior (listener attachment, subscriber Sets, notifySuspended escape hatch
       // are all independent of the construction-site line number).
       // Revision 1 Warning #12: this is THE engine clock (HRV AC); useNaviKriyaAudio
       // constructs its own SEPARATE clock for the NK AC — they MUST NOT be conflated.
       const clock: SessionClock & { notifySuspended(): void } = createAudioSessionClock(audioCtx, schedule)
       ```

    6. The `clock` variable is captured into the engine object literal at ~L368 (per Plan 50-04 Task 1 step 8). That literal already references `clock,` — no change needed there, just verify the reference resolves to the new construction site.

    7. Do NOT yet rewrite the existing scheduleLeadIn / scheduleNextCue / playEndChord — Task 2 handles those.

    8. Add unit tests in `src/audio/audioEngine.test.ts`: at minimum 8 tests covering each `cue.kind` arm of the switch dispatch. Use the existing FakeAudioContext polyfill; assert that the corresponding per-cue scheduler was invoked (via `vi.spyOn` on the imported primitives) and that `activeCues` grew by exactly 1. For `'end-chord'`, additionally assert that `endChordTailUntil` was updated.

       **Revision 1 Warning #9 — test count delta explicitly documented:**
       - These 8 dispatch tests cover NEW internal surface area (the `schedule(when, cue)` switch arms). They are ADDITIVE — no existing tests are removed or skipped.
       - Phase 50 cumulative test count delta:
         * Plan 50-01 sessionClock.test.ts: ≥18 tests (revision 1: 12 + 6 across factories, including onClose + scheduleImpl tests)
         * Plan 50-06 dispatch tests (this task): +8 tests
         * Plan 50-07 drift-guard test: +1 test (or +3 if split per banned pattern)
         * Total new tests: ≥27 (or ≥29 with split drift-guard)
         * Total Phase 50 test count: 1283 baseline + ≥27 = ≥1310
       - The 1283 existing tests pass with the same assertion shape (Plans 50-02 through 50-05 perform identifier renames + Blocker #4 path (a) source-rename; no behavior changes).
       - Document the running test count delta in this plan's SUMMARY.
  </action>
  <verify>
    <automated>pnpm test:run src/audio/audioEngine.test.ts</automated>
    <automated>pnpm test:run</automated>
    <automated>pnpm build</automated>
    <automated>pnpm lint</automated>
    <source_assertion>grep -c "function schedule" src/audio/audioEngine.ts returns at least 1</source_assertion>
    <source_assertion>grep -c "switch (cue.kind)" src/audio/audioEngine.ts returns 1</source_assertion>
    <source_assertion>grep -c "case 'in':\\|case 'out':\\|case 'lead-in-tick':\\|case 'end-chord':\\|case 'nk-front':\\|case 'nk-back':\\|case 'nk-tick':\\|case 'countdown-tick':" src/audio/audioEngine.ts returns 8</source_assertion>
    <source_assertion>grep -c "createAudioSessionClock(audioCtx, schedule)" src/audio/audioEngine.ts returns 1 (revision 1 Blocker #2: scheduleImpl plumbed at construction)</source_assertion>
    <source_assertion>**Revision 2 Warning #7 — reworded for explicit no-arg rejection:** `grep -cE "createAudioSessionClock\(audioCtx[^,]" src/audio/audioEngine.ts` returns 0 (the no-arg form `createAudioSessionClock(audioCtx)` and any single-arg variant ending in something other than `,` are rejected; the only legitimate form post-Plan-50-06 is the 2-arg `createAudioSessionClock(audioCtx, schedule)`). Equivalent alternate: `grep -cP "createAudioSessionClock\(audioCtx\)" src/audio/audioEngine.ts` returns 0. The intent: any call to `createAudioSessionClock` in this file MUST include the schedule arg — revision 1 Blocker #2 forbids the no-scheduleImpl form once the engine has its internal dispatch function.</source_assertion>
    <source_assertion>grep -c "createAudioSessionClock(audioCtx, schedule)" src/audio/audioEngine.ts returns 1 (positive assertion: exactly one 2-arg call exists)</source_assertion>
    <source_assertion>grep -c "clock\\.schedule = \\|clock as " src/audio/audioEngine.ts returns 0 (revision 1 Blocker #2: NO post-hoc reassignment, NO cast-based mutation)</source_assertion>
  </verify>
  <acceptance_criteria>
    - `src/audio/audioEngine.ts` contains an internal `schedule(when, cue)` function with a `switch (cue.kind)` block covering all 8 `Cue` variants.
    - Each case arm calls the appropriate per-cue scheduler from cueSynth.ts / nkCueSynth.ts and adds the returned handle to `activeCues`.
    - The `'end-chord'` case additionally updates `endChordTailUntil` per the existing close()-deferral pattern.
    - **Revision 1 Blocker #2:** the clock is constructed via `createAudioSessionClock(audioCtx, schedule)` — scheduleImpl plumbed at construction. NO post-hoc reassignment (no `clock.schedule = ...`, no `(clock as ...).schedule = ...`).
    - The clock construction line placed by Plan 50-04 (`createAudioSessionClock(audioCtx)` with one arg, immediately after `new AudioContext()`) is DELETED and replaced by the 2-arg construction at the post-schedule-function site.
    - 8 new tests cover the dispatch arms; all pass.
    - **Revision 1 Warning #9:** test count delta (≥27 new tests across Phase 50, none removed) explicitly documented in this plan's SUMMARY.
    - Full suite passes; test count = prior baseline + 8 (the 8 new dispatch tests from this task).
    - `pnpm build` exits 0; `pnpm lint` exits 0.
  </acceptance_criteria>
  <done>
    Internal schedule(when, cue) function exists with full dispatch; clock.schedule wired via revision 1 Blocker #2 scheduleImpl plumbing (NOT post-hoc reassignment); new dispatch tests pass; revision 1 Warning #9 test-count documentation in place; full suite green.
  </done>
</task>

<task type="auto">
  <name>Task 2: Refactor scheduleLeadIn / scheduleNextCue / playEndChord as thin facades over schedule()</name>
  <files>src/audio/audioEngine.ts, src/audio/audioEngine.test.ts</files>
  <read_first>
    - .planning/phases/50-sessionclock-scheduler-abstraction/50-CONTEXT.md (D-05 — existing engine methods become thin facades that build a Cue value and call schedule(); engine internals continue to read audioCtx.currentTime directly per CONTEXT specifics)
    - .planning/phases/50-sessionclock-scheduler-abstraction/50-PATTERNS.md (`src/audio/audioEngine.ts` MODIFIED subsection — exact facade body shapes for all three methods; SAFE_LEAD_SEC clamp stays in scheduleNextCue facade; endChordTailUntil bookkeeping stays in playEndChord facade; mute-fade-out preserved per D-13)
    - src/audio/audioEngine.ts (from Task 1 — confirm the internal `schedule` function exists)
    - src/audio/audioEngine.test.ts (existing tests for these three methods — confirm they assert on cue scheduling outcomes, not on internal implementation details)
    - **Important:** Plan 50-02 (revision 1 Blocker #4 path (a)) renamed `plan.inhaleMs` → `plan.inhaleSec` at the BreathingPlan source. The L313 `plan.inhaleMs / 1000` is now `plan.inhaleSec` (no `/1000`). The facade body below reflects this.
  </read_first>
  <action>
    Edit `src/audio/audioEngine.ts`. Replace the bodies of `scheduleLeadIn`, `scheduleNextCue`, and `playEndChord` with facade implementations.

    **scheduleLeadIn (L297-317):**
    Preserve: `if (closed) return null`, `if (muted) return firstInCueTime`, the `firstInCueTime` return value, and the 3 ticks at +0/+1/+2 + first In cue at +3 timing.
    Replace the 4 direct primitive calls with 4 `schedule(when, cue)` calls. Per Plan 50-02 revision 1 Blocker #4 path (a): `plan.inhaleMs` was renamed to `plan.inhaleSec` at source, so the body reads `plan.inhaleSec` directly (no `/1000`):
    ```
    scheduleLeadIn(startAudioTime: number, plan: BreathingPlan): number | null {
      const firstInCueTime = startAudioTime + LEAD_IN_DURATION_SEC
      if (closed) return null
      if (muted) return firstInCueTime
      schedule(startAudioTime + 0 * LEAD_IN_TICK_INTERVAL_SEC, { kind: 'lead-in-tick' })
      schedule(startAudioTime + 1 * LEAD_IN_TICK_INTERVAL_SEC, { kind: 'lead-in-tick' })
      schedule(startAudioTime + 2 * LEAD_IN_TICK_INTERVAL_SEC, { kind: 'lead-in-tick' })
      // 260510-tc9 Bug 2: pass the In phase duration so the decay envelope stretches with phase length at low BPM.
      // Per Plan 50-02 revision 1 Blocker #4 path (a): plan.inhaleSec is the source-level renamed field; no /1000 needed.
      schedule(firstInCueTime, { kind: 'in', phaseDurationSec: plan.inhaleSec, timbre: sessionTimbre })
      return firstInCueTime
    },
    ```
    Note: the `timbre` field in the `'in'` cue payload is present for type completeness but the engine's internal schedule ignores it (per Task 1 inline comment) — `sessionTimbre` is the source of truth.

    **scheduleNextCue (L319-332):**
    Preserve: `if (closed) return`, `if (muted) return`, `pruneExpiredCues()`, the SAFE_LEAD_SEC clamp.
    Replace the ternary cue construction:
    ```
    scheduleNextCue({ newPhase, audioTime, phaseDurationSec }: { newPhase: 'in' | 'out'; audioTime: number; phaseDurationSec: number }): void {
      if (closed) return
      if (muted) return
      pruneExpiredCues()
      // AUDIO-02 D-01/D-02 callee-side clamp.
      const clampedAudioTime = Math.max(audioTime, audioCtx.currentTime + SAFE_LEAD_SEC)
      schedule(clampedAudioTime, { kind: newPhase, phaseDurationSec, timbre: sessionTimbre })
    },
    ```
    The `audioCtx.currentTime` read inside the clamp stays — engine internals are not in drift-guard scope.

    **playEndChord (L334-344):**
    Preserve: `if (closed) return`, `if (muted) return`, the SAFE_LEAD_SEC offset, the `endChordTailUntil = Math.max(...)` bookkeeping.
    Move the `endChordTailUntil` update into the schedule() function's `'end-chord'` arm (already done in Task 1). The facade becomes:
    ```
    playEndChord(): void {
      if (closed) return
      if (muted) return
      const when = audioCtx.currentTime + SAFE_LEAD_SEC
      schedule(when, { kind: 'end-chord' })
    },
    ```
    Verify: the `endChordTailUntil` update happens inside the schedule()'s `'end-chord'` case (Task 1 — the `c.cleanupAt` is read from the returned cue handle inside the schedule function). The Math.max idempotency property is preserved (the schedule function's max-update is the same logic).

    **setMuted (L346-363):** Do NOT modify. The mute-fade-out path is preserved per D-13 (`applyMuteFadeOut` stays as the active mute through Phase 50).

    Update existing tests in `src/audio/audioEngine.test.ts`:
    - Tests that asserted on the per-cue scheduler being called: they still pass because the facade now routes through `schedule` which routes to the same primitive. If a test asserted call-count, it should still be exactly 1 per cue. If a test inspected the call args, the audioCtx + destination + timbre args are passed through unchanged.
    - Tests that asserted on `firstInCueTime` return: unchanged (same return path).
    - Tests that asserted on `activeCues.size` growth: unchanged (each `schedule` call adds exactly 1 to `activeCues`).
    - Tests that asserted on `endChordTailUntil` after playEndChord: unchanged (the update now lives in the schedule()'s `'end-chord'` arm but is otherwise identical math).
    - If a test asserted on the *order* of internal primitive calls (e.g., "scheduleCountdownTick called before scheduleInCueForTimbre"), it should still pass because the facade schedules in the same order via schedule().

    Test count constraint (ABSTR-04): no test deletions, no skips. Existing tests should pass unchanged. The 8 new dispatch tests from Task 1 are additive (revision 1 Warning #9 documents the delta).
  </action>
  <verify>
    <automated>pnpm test:run src/audio/audioEngine.test.ts</automated>
    <automated>pnpm test:run</automated>
    <automated>pnpm build</automated>
    <automated>pnpm lint</automated>
    <source_assertion>grep -c "scheduleCountdownTick(audioCtx, startAudioTime" src/audio/audioEngine.ts returns 0 (the lead-in 3 ticks now go through schedule())</source_assertion>
    <source_assertion>grep -c "scheduleInCueForTimbre(audioCtx, firstInCueTime" src/audio/audioEngine.ts returns 0 (the first In cue now goes through schedule())</source_assertion>
    <source_assertion>grep -c "schedule(.*, { kind: 'lead-in-tick' })" src/audio/audioEngine.ts returns 3</source_assertion>
    <source_assertion>grep -c "schedule(.*, { kind: 'in'" src/audio/audioEngine.ts returns at least 1</source_assertion>
    <source_assertion>grep -c "schedule(.*, { kind: 'end-chord' })" src/audio/audioEngine.ts returns 1</source_assertion>
    <source_assertion>grep -c "schedule(.*, { kind: newPhase" src/audio/audioEngine.ts returns 1</source_assertion>
  </verify>
  <acceptance_criteria>
    - `scheduleLeadIn` body contains 4 `schedule(...)` calls (3 lead-in-tick + 1 in); zero direct `scheduleCountdownTick` / `scheduleInCueForTimbre` calls. Per Plan 50-02 revision 1 Blocker #4 path (a): reads `plan.inhaleSec` directly (no `/1000`).
    - `scheduleNextCue` body contains 1 `schedule(clampedAudioTime, { kind: newPhase, ... })` call; the SAFE_LEAD_SEC clamp is preserved verbatim.
    - `playEndChord` body contains 1 `schedule(when, { kind: 'end-chord' })` call; the `endChordTailUntil` update is now inside the schedule function's `'end-chord'` arm.
    - `setMuted` body is unchanged (D-13 mute-fade-out preserved).
    - All existing audioEngine.test.ts assertions still pass.
    - Full suite passes; test count = prior baseline + 8 (the 8 new dispatch tests from Task 1).
    - `pnpm build` exits 0; `pnpm lint` exits 0.
  </acceptance_criteria>
  <done>
    scheduleLeadIn / scheduleNextCue / playEndChord are thin facades over schedule(); external API unchanged; engine tests green.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| engine internals → SessionClock.schedule | The clock's schedule method is now wired to the engine's internal dispatch (via revision 1 Blocker #2 scheduleImpl plumbing); external callers can pre-schedule cues via engine.clock.schedule() in Phase 52. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-50.06-01 | Tampering | npm install during this plan | accept | DEPS-01: zero new entries in `package.json`. |
| T-50.06-02 | Spoofing | facade behavior diverges from prior direct calls | mitigate | Each facade preserves its prior early-return guards (closed/muted), its return value (firstInCueTime), its bookkeeping (activeCues.add, endChordTailUntil = Math.max(...)). Acceptance criteria assert all of these via grep + test pass. |
| T-50.06-03 | Information disclosure | new internal schedule(when, cue) entry point exposed via engine.clock.schedule | accept | The schedule entry point requires a typed Cue value (closed discriminated union per D-04); arbitrary input is rejected by the type checker. At runtime, an invalid kind reaches the switch default — which is unreachable given the type — but TypeScript exhaustiveness check enforces every case. No new attack surface relative to the existing per-cue schedulers (which are already exported from cueSynth.ts / nkCueSynth.ts and could be called by any in-tree module). |
| T-50.06-04 | Tampering | clock.schedule reassignment after construction | **resolved** | **Revision 1 Blocker #2: this threat is RESOLVED, not accepted.** Plan 50-01 added an optional `scheduleImpl` parameter to `createAudioSessionClock`; this plan plumbs `schedule` through that parameter at construction. The clock object's `schedule` member is set ONCE inside the factory body and never reassigned. The `readonly` semantics on the AudioEngine interface (`readonly clock: SessionClock`) hold. No cast-based mutation, no post-hoc reassignment. |

**Reasoning:** Pure structural refactor — the runtime behavior is byte-identical (same primitives invoked with same args, same order, same bookkeeping). The dispatch indirection adds one switch-case lookup per scheduled cue, which is negligible. No new dependencies, no new I/O, no new untrusted inputs. Revision 1 Blocker #2 fix preserves the readonly contract structurally.
</threat_model>

<verification>
- `pnpm build` exits 0; `pnpm lint` exits 0; `pnpm test:run` exits 0.
- Test count = baseline + 8 dispatch tests (Plan 50-01 sessionClock tests + Task 1 dispatch tests; Plan 50-07 drift-guard test is yet to be added at this point in execution).
- `git diff package.json` is empty (DEPS-01).
- All existing audioEngine.test.ts tests pass unchanged (the facade refactor is transparent to the test layer).
- `grep -rn "performance\\.now" src/audio/audioEngine.ts` — irrelevant; engine internals are not in drift-guard scope. Verify only that no NEW `performance.now` reads were added (none should be).
- **Revision 1 Blocker #2 source assertion:** `grep -c "clock\\.schedule = \\|clock as " src/audio/audioEngine.ts` returns 0 (no post-hoc reassignment).
- **Revision 2 Warning #7 reworded grep gate:** `grep -cE "createAudioSessionClock\\(audioCtx[^,]" src/audio/audioEngine.ts` returns 0 (no-arg form explicitly rejected). `grep -c "createAudioSessionClock(audioCtx, schedule)" src/audio/audioEngine.ts` returns 1.
- **Revision 2 Blocker #1 augmented-type preservation:** `grep -c "SessionClock & { notifySuspended" src/audio/audioEngine.ts` returns at least 1 (the local-reference type annotation at the new ~L295 construction site).
- **Revision 2 Warning #6 ordering parity:** both Plan 50-04 close (clock at L177, no scheduleImpl) AND this plan close (clock at ~L295, with scheduleImpl=schedule) pass the per-commit green-gate. Per-commit green-gate verification: `pnpm build` + `pnpm lint` + `pnpm test:run` all exit 0.
</verification>

<success_criteria>
- ABSTR-01 fully satisfied: `SessionClock.schedule(when, cue)` is functional (wired to the engine's internal dispatch via revision 1 Blocker #2 scheduleImpl plumbing).
- ABSTR-04: existing test parity preserved through the facade refactor; new dispatch tests cover the new internal surface.
- D-04 + D-05 honored: closed Cue catalog drives a complete switch dispatch; the 3 public methods become facades.
- D-13 honored: applyMuteFadeOut stays as the active mute mechanism through Phase 50.
- **Revision 1 Blocker #2:** scheduleImpl plumbed at construction; no post-hoc reassignment; readonly contract preserved structurally.
- **Revision 1 Warning #9:** test count delta (≥27 new tests cumulative across Phase 50, none removed) explicitly documented in SUMMARY.
- **Revision 2 Blocker #1 — augmented clock type preserved across the construction-site move:** the local clock reference at the new ~L295 site is typed as `SessionClock & { notifySuspended(): void }`. The L445 `clock.notifySuspended()` call (already in place from Plan 50-04) continues to type-check.
- **Revision 2 Warning #6 — ordering parity:** the intermediate state (Plan 50-04 close, clock at L177) and the final state (this plan close, clock at ~L295) both pass the per-commit green-gate independently. The move is a pure refactor with no observable behavior change.
- **Revision 2 Warning #7 — reworded grep gate:** the verify section enforces `createAudioSessionClock\(audioCtx[^,])` rejects the no-arg form explicitly; the 2-arg form is positively asserted.
- DEPS-01 + QUAL-01 verified.
- Byte-identical end-user behavior: every cue plays at the same `when` value with the same envelope/duration/timbre as pre-refactor.
</success_criteria>

<output>
Create `.planning/phases/50-sessionclock-scheduler-abstraction/50-06-SUMMARY.md` when done. Document the clock construction site (moved from Plan 50-04 position L177 to post-schedule-function position ~L295 per revision 1 Blocker #2 + revision 2 Warning #6 ordering parity). Confirm the local clock reference at the new site is typed as the augmented factory return type `SessionClock & { notifySuspended(): void }` (revision 2 Blocker #1 — required for the L445 `clock.notifySuspended()` call from Plan 50-04 to continue type-checking). Document the test count delta cumulative for Phase 50 per revision 1 Warning #9: 1283 baseline + Plan 50-01 (≥22 with revision 2 notifySuspended tests) + this plan (+8) + Plan 50-07 (+1 to +3) = ≥1314. Confirm no post-hoc reassignment occurred (revision 2 Warning #7 reworded grep gate verified).
</output>
</content>
