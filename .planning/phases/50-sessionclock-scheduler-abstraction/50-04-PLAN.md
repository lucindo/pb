---
phase: 50-sessionclock-scheduler-abstraction
plan: 04
type: execute
wave: 2
depends_on:
  - 50-01
files_modified:
  - src/hooks/useAudioCues.ts
  - src/hooks/useAudioCues.test.tsx
  - src/audio/audioEngine.ts
  - src/audio/audioEngine.test.ts
autonomous: true
requirements:
  - ABSTR-01
  - ABSTR-03
  - ABSTR-04
  - DEPS-01
  - QUAL-01
requirements_addressed:
  - ABSTR-01
  - ABSTR-03
  - ABSTR-04
  - DEPS-01
  - QUAL-01
tags:
  - sessionclock
  - hooks
  - audio
  - audiocues
  - statemachine
must_haves:
  truths:
    - "`useAudioCues` consumes the `SessionClock` interface via `engine.clock` (exposed on AudioEngine); zero `performance.now()` references in `src/hooks/useAudioCues.ts` source lines and zero direct `audioCtx.currentTime` reads (D-09)."
    - "`useAudioCues` no longer passes `onStateChange` to `createAudioEngine`. Instead, after the engine is constructed, the hook subscribes via `engine.clock.onSuspend(handleSuspend)`, `engine.clock.onResume(handleResume)`, and (revision 1 Blocker #1) `engine.clock.onClose(handleClose)`."
    - "**Revision 2 Warning #4 — `AudioStatusFlag` enum citation corrected:** The existing `AudioStatusFlag` state machine (`'ok'` | `'needs-resume'` | `'unavailable'`) — Plan 06 D-34..D-38 — keeps its shape verbatim. (The enum has THREE values, not four; iteration-1 references to `'failed'` were incorrect citations and have been dropped per src/audio/audioStatus.ts.) Only the *source* of the suspend/resume/close transitions moves from a callback prop to THREE subscriptions; the gesture-recovery seam, the `needs-resume` flow (Plan 06 D-36 + Pitfall 5), and the byte-identical `'unavailable'` setter on `'closed'` state are preserved."
    - "**Revision 1 Blocker #1 — `'closed'` state path preserved byte-identical:** `engine.clock.onClose(() => setAudioStatus('unavailable'))` is wired alongside the suspend/resume subscriptions. This replaces the prior `else if (state === 'closed') { setAudioStatus('unavailable') }` branch at useAudioCues.ts:164-165. Behavior is byte-identical: on AC `'closed'` transition, audioStatus becomes `'unavailable'` exactly as before. Phase 50 success criterion #3 (byte-identical end-user behavior) satisfied."
    - "`engine.now()` call site at useAudioCues.ts:265 becomes `engine.clock.now()`; the returned `audioNow` helper at L459 likewise reads `engine.clock.now()`."
    - "`AudioEngine` interface exposes a new `readonly clock: SessionClock` member; `createAudioEngine` constructs it once internally via `createAudioSessionClock(audioCtx, scheduleImpl?)` (revision 1 Blocker #2 — scheduleImpl plumbed at construction in Plan 50-06; this plan passes no scheduleImpl) and stores it on the returned engine object."
    - "**Revision 2 Blocker #1 — L445 InvalidStateError fan-out PRESERVED via `clock.notifySuspended()` (engine-only escape hatch):** Pre-refactor `audioEngine.ts` L429-449 synchronously invokes `opts.onStateChange?.(readState())` AFTER an `audioCtx.resume()` rejection with `InvalidStateError`. This is a SYNTHETIC state report — the AC was already `'suspended'` before resume(), stays `'suspended'` after the rejection, and NO statechange event fires. Plan 50-01 (revision 2) added the engine-only escape hatch `clock.notifySuspended()` on the augmented factory return type `SessionClock & { notifySuspended(): void }`. This plan REPLACES `opts.onStateChange?.(readState())` at L445 with `clock.notifySuspended()` (called via the engine's internal augmented-type reference). The fan-out path is preserved byte-identical: `notifySuspended()` invokes the same `suspendSubscribers` Set that a natural `'suspended'` statechange would invoke via the clock's own listener. iOS Safari recovery flow (Plan 06 D-38) is preserved end-to-end. Iteration-1 disposition (b) — 'AC fires statechange naturally' — was grounded in an incorrect assumption about pre-refactor behavior and has been rejected in revision 2."
    - "**Revision 1 Blocker #3 — architectural path committed:** `createAudioEngine` no longer keeps a local statechange listener. The CLOCK owns the single AC statechange listener and fans suspend/resume/close to external subscribers via `engine.clock.on*`. The L243-257 local listener is DELETED; the L378 removeEventListener is DELETED. The L445 InvalidStateError fan-out is REWIRED to `clock.notifySuspended()` per revision 2 Blocker #1 (NOT deleted). The internal listener (if needed) handles ONLY engine-internal state, not external subscribers."
    - "`reconstructEngine` re-subscribes `handleSuspend`/`handleResume`/`handleClose` against the new engine's `clock` and tears down the old subscriptions (stored unsubscribe refs) on close. Subscriptions never leak across reconstruction."
    - "**Revision 1 Warning #12 — phase-level invariant note:** Phase 50 has exactly TWO `createAudioSessionClock` invocations: one inside `createAudioEngine` (HRV AC — exposed via `engine.clock`), one inside `useNaviKriyaAudio.begin()` (NK AC — local to NK's lifecycle). They wrap DIFFERENT AudioContexts and MUST NOT be conflated. The engine's `clock` member exposes only the HRV-AC clock; NK-AC's clock is local to NK."
    - "**Revision 2 Warning #6 — ordering parity at plan close:** This plan's intermediate state constructs `clock` at audioEngine.ts L177 (immediately after `new AudioContext()`). Plan 50-06 will MOVE that construction to ~L295 (post-schedule-function definition) so the engine's internal `schedule` function can be plumbed as `scheduleImpl`. Both intermediate (this plan close) and final (Plan 50-06 close) states pass the per-commit green-gate: `pnpm build` + `pnpm lint` + `pnpm test:run` are all green at each plan close. The clock's listener attachment, subscribers Set lifecycle, and `notifySuspended()` escape hatch are independent of the construction-site line number — moving the construction site does NOT change observable behavior."
  artifacts:
    - path: "src/audio/audioEngine.ts"
      provides: "AudioEngine interface adds `readonly clock: SessionClock`. createAudioEngine constructs the clock via createAudioSessionClock(audioCtx) and assigns it to the returned object. The engine's internal augmented-type clock reference (typed as `SessionClock & { notifySuspended(): void }`) is used at L445 to call `clock.notifySuspended()` per revision 2 Blocker #1. AudioEngineOptions.onStateChange REMOVED. Per revision 1 Blocker #3: the L243-257 local listener is REMOVED; the L378 removeEventListener is REMOVED. Per revision 2 Blocker #1: the L445 InvalidStateError catch block REWIRES the fan-out from `opts.onStateChange?.(readState())` to `clock.notifySuspended()` (NOT deleted)."
      contains: "readonly clock: SessionClock"
    - path: "src/hooks/useAudioCues.ts"
      provides: "Migrated to clock.onSuspend/onResume/onClose subscriptions; AudioStatusFlag machine preserved; engine.now() reads route through engine.clock.now(); L66 JSDoc on the audioNow interface method updated per revision 2 Warning #5 (Returns engine.clock.now() = audioCtx.currentTime per D-03 Option A)."
      contains: "engine\\.clock\\.onSuspend\\|engine\\.clock\\.onResume\\|engine\\.clock\\.onClose"
  key_links:
    - from: "src/hooks/useAudioCues.ts"
      to: "src/audio/sessionClock.ts (via engine.clock)"
      via: "clock.onSuspend / clock.onResume / clock.onClose subscription wiring"
      pattern: "clock\\.onSuspend\\|clock\\.onResume\\|clock\\.onClose"
    - from: "src/audio/audioEngine.ts (createAudioEngine)"
      to: "src/audio/sessionClock.ts (createAudioSessionClock)"
      via: "internal construction in createAudioEngine; the engine's internal reference is typed as the augmented factory return type so it can call notifySuspended() per revision 2 Blocker #1"
      pattern: "createAudioSessionClock\\(audioCtx"
    - from: "src/audio/audioEngine.ts (engine.resume() L429-449 InvalidStateError catch)"
      to: "src/audio/sessionClock.ts (createAudioSessionClock notifySuspended escape hatch)"
      via: "clock.notifySuspended() synchronously fans 'suspended' to suspendSubscribers — preserves the iOS Safari recovery flow (Plan 06 D-38) byte-identically"
      pattern: "clock\\.notifySuspended\\(\\)"
---

<objective>
Migrate `useAudioCues` off the `AudioEngineOptions.onStateChange` callback channel onto the `SessionClock.onSuspend` / `onResume` / `onClose` subscription channel per D-11 (extended in revision 1 to include `onClose` per Blocker #1). Replace the 2 direct `engine.now()` reads with `engine.clock.now()`. The migration preserves the `AudioStatusFlag` state machine (Plan 06 D-34..D-38) verbatim — only the *trigger source* changes; the `needs-resume` gate, gesture-recovery seam, `setAudioStatus` flow, AND the `'closed'` → `'unavailable'` setter are byte-identical.

**Revision 2 Warning #4 — `AudioStatusFlag` enum citation corrected.** Per `src/audio/audioStatus.ts`, the actual enum is `'ok' | 'needs-resume' | 'unavailable'` (THREE values, no `'failed'`). Iteration-1 references to a fourth `'failed'` value were incorrect and have been removed throughout this plan.

**Revision 1 Blocker #1 — `'closed'` state preserved byte-identical via `onClose` subscriber.** The existing code at `useAudioCues.ts:164-165` fires `setAudioStatus('unavailable')` whenever `audioCtx.state === 'closed'`. Phase 50 success criterion #3 (byte-identical end-user behavior) requires this path to survive the migration. Plan 50-01 added `onClose(cb): () => void` to the SessionClock interface. This plan wires `engine.clock.onClose(() => setAudioStatus('unavailable'))` alongside the suspend/resume subscriptions. Behavior is byte-identical.

**Revision 1 Blocker #2 — `scheduleImpl` plumbed at construction (not post-hoc reassignment).** Plan 50-01 added an optional `scheduleImpl` parameter to `createAudioSessionClock`. Plan 50-06 will construct the clock as `createAudioSessionClock(audioCtx, engineScheduleFn)`. This plan (50-04) constructs the clock with NO scheduleImpl arg (so engine.clock.schedule is a no-op until Plan 50-06 lands the engine-internal dispatch). The clock object stays `readonly` — no post-hoc reassignment.

**Revision 2 Blocker #1 — L445 InvalidStateError fan-out preserved via `clock.notifySuspended()` engine-only escape hatch.** The plan-checker's iteration-2 verification confirmed that the pre-refactor `audioEngine.ts` L429-449 invokes `opts.onStateChange?.(readState())` SYNCHRONOUSLY after an `audioCtx.resume()` rejection with `InvalidStateError`. This is the documented iOS Safari recovery path (Plan 06 D-38) — the AC was already `'suspended'` before resume(), stays `'suspended'` after the rejection, and no natural statechange event fires. Iteration-1 disposition (b) — "the AC's natural statechange to 'suspended' after the rejection fires the clock's listener" — was grounded in an incorrect assumption about pre-refactor behavior. The clock's listener cannot cover this case.

Plan 50-01 (revision 2) added an engine-only escape hatch: `createAudioSessionClock` now returns the augmented type `SessionClock & { notifySuspended(): void }`. The `notifySuspended()` method synchronously fans `'suspended'` to `suspendSubscribers` using the same `fanSuspend()` helper that the natural statechange listener uses — observable behavior is byte-identical between natural and synthetic suspend paths.

This plan (50-04) REPLACES the L445 fan-out with `clock.notifySuspended()`:
- BEFORE: `opts.onStateChange?.(readState())`
- AFTER: `clock.notifySuspended()`

The engine's internal clock reference is typed as the augmented return type (so the call type-checks). External consumers of `engine.clock: SessionClock` cannot see `notifySuspended` — the escape hatch stays scoped to the engine's closure.

**Revision 1 Blocker #3 — architectural path committed for the local statechange listener.** The committed path: DELETE the local listener `onStateChange` at L243-257 AND the `removeEventListener` at L378. The CLOCK owns the single AC statechange listener for ALL fan-out (external subscribers via `engine.clock.on*`).

Touchpoint enumeration for audioEngine.ts (revision 1 Blocker #3 + revision 2 Blocker #1):

| Line | Pre-refactor purpose | Disposition under committed path |
|------|---------------------|----------------------------------|
| L243-257 | Local listener `onStateChange` that calls `opts.onStateChange?.(readState())` | **REMOVED** (revision 1 Blocker #3). External subscribers now consume `engine.clock.on*` per Plan 50-04 Task 2. The clock's own internal listener (added in Plan 50-01) fans suspend/resume/close to those subscribers. |
| L378 | `audioCtx.removeEventListener('statechange', onStateChange)` inside `engine.close()` | **REMOVED** (revision 1 Blocker #3). The local listener no longer exists, so removeEventListener has no target. The clock's listener is owned by the clock; its lifecycle is independent (and at engine.close() the AC closes → the clock receives a `'closed'` event → it fires `closeSubscribers` → useAudioCues sets `'unavailable'` per Blocker #1). The clock's listener itself is automatically detached when the AC garbage-collects after close. |
| L445 | `if ((err as DOMException)?.name === 'InvalidStateError') { opts.onStateChange?.(readState()) }` inside `engine.resume()` catch block | **REWIRED** (revision 2 Blocker #1). The pre-refactor synchronous state report is a SYNTHETIC suspend — the AC was already `'suspended'` before resume(), no natural statechange fires after the rejection. The line `opts.onStateChange?.(readState())` is REPLACED with `clock.notifySuspended()` (called via the engine's internal augmented-type reference). The `if ((err as DOMException)?.name === 'InvalidStateError')` check stays — it narrows the catch to the iOS Safari InvalidStateError class per Plan 06 D-38. Inline comment: "Revision 2 Blocker #1: prior `opts.onStateChange?.(readState())` was a synchronous state report after the rejection (NOT a statechange-event-driven fan-out). The AC was already 'suspended' before resume() was called, stays 'suspended' after the rejection — no natural statechange event fires. Calling `clock.notifySuspended()` (engine-only escape hatch on the augmented factory return type from Plan 50-01 revision 2) synchronously fans the suspended event to suspendSubscribers via the same `fanSuspend()` helper used by the natural statechange listener. iOS Safari recovery flow (Plan 06 D-38) preserved byte-identically: useAudioCues' handleSuspend subscriber sets `audioStatus` to `'needs-resume'` (when visibilityResumeAttemptedRef.current is true) exactly as it did pre-refactor." |

The committed path keeps a SINGLE listener on the AC — the clock's own listener — and external code consumes it via the three subscription channels. The engine's internal augmented-type reference enables one engine-only synthetic-suspend path via `clock.notifySuspended()` for the iOS Safari InvalidStateError recovery. Engine internals never observe statechange events directly; they only act in their own synchronous lifecycle methods (close(), resume()).

Per CONTEXT specifics: engine internals are NOT in drift-guard scope. The `audioCtx.currentTime` reads at L286 (pruneExpiredCues), L324 (clamp), L337 (playEndChord when), L370 (now()), and L394 (close tail) all remain. The drift-guard (Plan 50-07) scans the 5 caller files exclusively.

**Revision 2 Warning #6 — ordering parity at plan close.** This plan constructs `clock` at audioEngine.ts L177 (immediately after `new AudioContext()` — see Task 1 step 3). Plan 50-06 will MOVE that construction to ~L295 (post-schedule-function definition) so the engine's internal `schedule` function can be plumbed as `scheduleImpl`. Both intermediate (this plan close) and final (Plan 50-06 close) states pass the per-commit green-gate: `pnpm build` + `pnpm lint` + `pnpm test:run` are all green at each plan close. The clock's listener attachment, subscribers Set lifecycle, and `notifySuspended()` escape hatch are independent of the construction-site line number — moving the construction site does NOT change observable behavior. The intermediate-state `clock.schedule()` is a no-op (no scheduleImpl supplied at this plan); after Plan 50-06 it forwards to the engine's internal dispatch.

Purpose: ABSTR-03 partial satisfaction (`useAudioCues` is caller 4 of 5). ABSTR-01 partial satisfaction (the clock surface gains its primary consumer for `onSuspend`/`onResume`/`onClose` PLUS the engine's internal `notifySuspended()` consumer). ABSTR-04 maintained — test parity preserved.

Output: `useAudioCues` subscribes via `engine.clock.onSuspend`/`onResume`/`onClose`. AudioEngine exposes `clock`. AudioEngineOptions.onStateChange is removed. All `engine.now()` reads in this file route through `engine.clock.now()`. The internal L243-257 listener is deleted; the L378 removeEventListener is deleted; the L445 fan-out is REWIRED to `clock.notifySuspended()`. The L66 JSDoc on the audioNow interface method is updated per revision 2 Warning #5.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/50-sessionclock-scheduler-abstraction/50-CONTEXT.md
@.planning/phases/50-sessionclock-scheduler-abstraction/50-PATTERNS.md
@.planning/phases/50-sessionclock-scheduler-abstraction/50-01-SUMMARY.md

<interfaces>
<!-- Current AudioEngine interface (src/audio/audioEngine.ts L43-57): -->
export interface AudioEngine {
  scheduleLeadIn(startAudioTime: number, plan: BreathingPlan): number | null
  scheduleNextCue(args: { newPhase: 'in' | 'out'; audioTime: number; phaseDurationSec: number }): void
  playEndChord(): void
  setMuted(muted: boolean): void
  readonly muted: boolean
  now(): number
  close(): Promise<void>
  resume(): Promise<void>
  readonly state: AudioContextState | 'interrupted'
}

<!-- After this plan: -->
export interface AudioEngine {
  // ... existing members unchanged ...
  readonly clock: SessionClock          // ← NEW: SessionClock surface (D-11 + revision 1 onClose; revision 2: external API stays at the 6-member SessionClock — notifySuspended is engine-internal only)
}

<!-- Current AudioEngineOptions (src/audio/audioEngine.ts L65-72): -->
export interface AudioEngineOptions {
  timbre: TimbreId
  bypassSilentMode: boolean
  onStateChange?: (state: AudioContextState | 'interrupted') => void
}

<!-- After this plan: -->
export interface AudioEngineOptions {
  timbre: TimbreId
  bypassSilentMode: boolean
  // onStateChange removed — useAudioCues now subscribes via engine.clock.onSuspend / onResume / onClose (D-11 + revision 1 Blocker #1).
}

<!-- Current AudioStatusFlag (src/audio/audioStatus.ts) — revision 2 Warning #4 cited verbatim: -->
export type AudioStatusFlag = 'ok' | 'needs-resume' | 'unavailable'
// (THREE values — no 'failed'. Iteration-1 plan references to 'failed' were incorrect.)

<!-- Current useAudioCues handleStateChange (L153-179): -->
const handleStateChange = useCallback(
  (state: AudioContextState | 'interrupted'): void => {
    if (state === 'running') { setAudioStatus('ok'); ... }
    else if (state === 'closed') { setAudioStatus('unavailable') }    // ← revision 1 Blocker #1: this branch becomes handleClose subscriber
    else if ((state === 'suspended' || state === 'interrupted') && visibilityResumeAttemptedRef.current) {
      setAudioStatus('needs-resume')
    }
  }, [])

<!-- After this plan: split into handleSuspend / handleResume / handleClose — see action. -->

<!-- Revision 1 — SessionClock interface (from Plan 50-01, 6 members) — unchanged in revision 2: -->
export interface SessionClock {
  now(): number
  schedule(when: number, cue: Cue): void
  setMasterGain(value: number, rampSec: number): void
  onSuspend(cb: () => void): () => void
  onResume(cb: () => void): () => void
  onClose(cb: () => void): () => void   // ← NEW in revision 1 (Blocker #1)
}

<!-- Revision 2 Blocker #1 — augmented factory return type (from Plan 50-01 revision 2). -->
<!-- Engine-only escape hatch; NOT a public SessionClock member. -->
export function createAudioSessionClock(
  audioCtx: AudioContext,
  scheduleImpl?: (when: number, cue: Cue) => void,
): SessionClock & { notifySuspended(): void }

<!-- Inside createAudioEngine's closure, the clock reference is typed as the augmented return type so it can call notifySuspended(). -->
<!-- The reference assigned to engine.clock is widened to SessionClock for the public interface. -->
const clock: SessionClock & { notifySuspended(): void } = createAudioSessionClock(audioCtx)
// engine.clock = clock   // assignment widens to SessionClock at the boundary
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add `readonly clock: SessionClock` to AudioEngine interface; construct via createAudioSessionClock with the engine's internal reference typed as the augmented return type; commit to revision 1 Blocker #3 architectural path (delete local listener + L378 removeEventListener); revision 2 Blocker #1: REWIRE L445 fan-out from `opts.onStateChange?.(readState())` to `clock.notifySuspended()`</name>
  <files>src/audio/audioEngine.ts, src/audio/audioEngine.test.ts</files>
  <read_first>
    - .planning/phases/50-sessionclock-scheduler-abstraction/50-CONTEXT.md (D-07 createAudioSessionClock wraps an AC; D-08 factory wraps don't construct; D-11 onSuspend/onResume wired real via clock.subscribers; engine internals are NOT in drift-guard scope; D-13 mute fade-out preserved through Phase 50)
    - .planning/phases/50-sessionclock-scheduler-abstraction/50-PATTERNS.md (`src/audio/audioEngine.ts (MODIFIED — adds SessionClock re-export + internal facade refactor)` — engine constructs clock once after AC; subscribed-by-useAudioCues migration subsection)
    - src/audio/audioEngine.ts (entire file — confirm createAudioEngine internal structure at L173-235; the existing statechange listener at L243-257 must be REMOVED per revision 1 Blocker #3 committed path; L378 removeEventListener REMOVED; **L445 fan-out REWIRED to `clock.notifySuspended()` per revision 2 Blocker #1, NOT deleted**)
    - src/audio/sessionClock.ts (Plan 50-01 revision 2 — createAudioSessionClock signature with the augmented return type `SessionClock & { notifySuspended(): void }`; confirm the factory's notifySuspended() body calls the same private `fanSuspend()` helper as the statechange listener)
    - src/audio/audioStatus.ts (revision 2 Warning #4 — confirm AudioStatusFlag enum is exactly `'ok' | 'needs-resume' | 'unavailable'` (3 values, no 'failed'); cite verbatim in any AudioStatusFlag reference in this plan or in updated comments)
    - src/audio/audioEngine.test.ts (existing test that drives onStateChange callbacks — these tests must be updated to drive via clock subscriptions, and the L445 InvalidStateError test must assert via `clock.notifySuspended()` fan-out per revision 2 Blocker #1)
  </read_first>
  <action>
    Edit `src/audio/audioEngine.ts`:

    1. Add import: `import { createAudioSessionClock, type SessionClock } from './sessionClock'`. (The `export type { SessionClock }` re-export from Plan 50-01 Task 3 already exists in this file — keep it.) The augmented factory return type `SessionClock & { notifySuspended(): void }` is inferred from the factory's signature; no separate import needed.

    2. Extend the `AudioEngine` interface (L43-57) with one new member:
       ```
       readonly clock: SessionClock   // D-11 + revision 1 Blocker #1 / ABSTR-01: SessionClock surface for subscribers (onSuspend/onResume/onClose) and time reads (now/schedule). Revision 2 Blocker #1: the engine's internal reference is typed as the augmented factory return type `SessionClock & { notifySuspended(): void }` for the L445 InvalidStateError escape hatch; the public engine.clock member is widened to SessionClock so external consumers cannot call notifySuspended.
       ```
       Add JSDoc citing D-11, ABSTR-01, revision 1 Blocker #1, and revision 2 Blocker #1.

    3. Inside `createAudioEngine` (L173+), immediately after `const audioCtx = new AudioContext()` at L177, construct the clock. **Critical:** the local reference inside `createAudioEngine`'s closure MUST be typed as the augmented factory return type (so it can call `notifySuspended()` at L445). The assignment to `engine.clock` widens to `SessionClock`:
       ```
       // Phase 50 D-07: wrap the AC immediately after construction. D-11 + revision 1 Blocker #1:
       // the clock owns the audioCtx 'statechange' listener and fans suspend/resume/close to subscribers.
       // Revision 1 Blocker #2: no scheduleImpl supplied at this plan — Plan 50-06 will reconstruct as
       //   createAudioSessionClock(audioCtx, engineScheduleFn) when the engine-internal dispatch lands.
       // Revision 2 Blocker #1: the local clock reference is typed as the AUGMENTED factory return type
       //   `SessionClock & { notifySuspended(): void }` so the engine can call clock.notifySuspended() at
       //   the L445 InvalidStateError catch (preserving the iOS Safari recovery path byte-identically).
       //   The engine.clock public member is widened to SessionClock — external consumers cannot see
       //   notifySuspended. The escape hatch is engine-only.
       // Revision 1 Warning #12: this is THE engine clock (HRV AC); useNaviKriyaAudio constructs its own
       //   SEPARATE clock for the NK AC — they MUST NOT be conflated.
       const clock: SessionClock & { notifySuspended(): void } = createAudioSessionClock(audioCtx)
       ```

    4. **Revision 1 Blocker #3 committed path — REMOVE the existing local statechange listener at L243-257:** delete the `const onStateChange = (): void => { opts.onStateChange?.(readState()) }` line and the `audioCtx.addEventListener('statechange', onStateChange)` line. The clock now owns the single AC statechange listener; the engine has no internal need for its own. Add a comment marking the deletion site: "// Revision 1 Blocker #3 (committed path): the prior local statechange listener was DELETED. The clock owns the single AC statechange listener (added by createAudioSessionClock per Plan 50-01) and fans suspend/resume/close to external subscribers (useAudioCues via engine.clock.on*). Engine internals do not observe statechange events directly; they act in their own synchronous lifecycle methods (close(), resume()). The L445 InvalidStateError synthetic-suspend path uses clock.notifySuspended() per revision 2 Blocker #1 — it does NOT need its own listener."

    5. **REMOVE the L378 `audioCtx.removeEventListener('statechange', onStateChange)`** inside `engine.close()`. The local listener no longer exists, so the removal has no target. Add a comment: "// Revision 1 Blocker #3: prior removeEventListener removed — the local listener was deleted in step 4. The clock's listener is owned by the clock; its lifecycle is independent (the AC's close will fire 'closed' on the clock's listener which fans to closeSubscribers per Plan 50-01)."

    6. **Remove `onStateChange` from `AudioEngineOptions`** (L71):
       ```
       export interface AudioEngineOptions {
         timbre: TimbreId
         bypassSilentMode: boolean
         // onStateChange removed in Phase 50 / D-11 + revision 1 Blocker #1. Subscribers consume engine.clock.onSuspend / onResume / onClose.
       }
       ```

    7. **Revision 2 Blocker #1 — REWIRE the L445 fan-out** inside `engine.resume()` catch block. The line `opts.onStateChange?.(readState())` is REPLACED (NOT deleted) with `clock.notifySuspended()`:
       ```
       async resume(): Promise<void> {
         if (closed) return
         try {
           await audioCtx.resume()
         } catch (err) {
           // Plan 06 D-38: narrow the Plan 01 D-09 silent-absorb posture. The specific
           // iOS-Safari failure mode is `DOMException { name: 'InvalidStateError' }`
           // raised when resume() is invoked from a non-gesture context (the
           // visibilitychange listener qualifies as non-gesture on iOS Safari per device
           // diagnostic 05.1-UAT.md Task 2). Surface THIS error class via the state-change
           // path so the hook can transition to 'needs-resume' and surface the
           // user-tappable affordance (D-29). All other errors continue silent-absorb
           // (Plan 01 D-09 preserved for unknown failure modes).
           //
           // Revision 2 Blocker #1: prior `opts.onStateChange?.(readState())` was a SYNCHRONOUS
           // state report — the AC was already 'suspended' before resume() was called, stays
           // 'suspended' after the rejection, and no natural statechange event fires. We replace
           // the prior call with `clock.notifySuspended()` (engine-only escape hatch on the
           // augmented factory return type from Plan 50-01 revision 2) which synchronously fans
           // the suspended event to suspendSubscribers via the same fanSuspend() helper used by
           // the natural statechange listener. iOS Safari recovery flow preserved byte-identically:
           // useAudioCues' handleSuspend subscriber sets audioStatus to 'needs-resume' when
           // visibilityResumeAttemptedRef.current is true, exactly as it did pre-refactor.
           // eslint-disable-next-line @typescript-eslint/no-unnecessary-condition
           if ((err as DOMException)?.name === 'InvalidStateError') {
             clock.notifySuspended()   // ← Revision 2 Blocker #1: synthetic-suspend fan-out via the augmented-type local reference.
           }
           // Else: silent. No console.debug (discretion #4). The session continues on visuals
           // only — same posture as Phase 3 D-10 and Phase 5 D-09.
         }
       },
       ```

       The call type-checks because the local `clock` reference is typed as `SessionClock & { notifySuspended(): void }`. If the local reference were typed as plain `SessionClock`, the call would fail type-checking — which is the structural guarantee that external consumers cannot bypass the encapsulation.

    8. Assign `clock` to the returned engine object near the existing return literal in `createAudioEngine` (the object literal at ~L296-455 that includes `now`, `close`, `resume`, etc.):
       ```
       const engine: AudioEngine = {
         // ... existing members ...
         clock,   // D-11 + revision 1 Blocker #1 / ABSTR-01: SessionClock surface. The assignment widens to SessionClock — engine.clock cannot call notifySuspended().
         // ...
       }
       ```
       The assignment widens the augmented type to `SessionClock` at the boundary — external code that reads `engine.clock` sees only the 6-member read-only interface.

    9. The engine's own `now()` method (L370) continues to return `audioCtx.currentTime` directly. Per CONTEXT specifics, the drift-guard scope is exactly the 5 caller files, NOT the engine implementation — so `audioCtx.currentTime` reads inside `audioEngine.ts` are permitted.

    10. Update `src/audio/audioEngine.test.ts`: any test that constructs an engine with `{ onStateChange: someCallback }` must be updated. Two migration paths per test:
       - (a) If the test was using `onStateChange` to observe AC state transitions, switch to subscribing via `engine.clock.onSuspend(cb)` / `engine.clock.onResume(cb)` / **`engine.clock.onClose(cb)`** and assert the cb is invoked.
       - (b) If the test was using `onStateChange` to drive a state machine for assertions, the assertion is now indirectly verified via clock subscriptions — same logic, same FakeAudioContext, different subscriber attachment point.
       Per ABSTR-04: test count is preserved. If a test asserted the prior `onStateChange` API shape (e.g., type-shape assertion), remove the assertion as it no longer applies — count is still preserved because the test body can assert the new shape.

       **Revision 2 Blocker #1 — L445 InvalidStateError test:** tests that previously exercised the L445 fan-out (the iOS-Safari narrowed catch from Plan 06 D-38) must be REWIRED to assert via `clock.notifySuspended()`. Stage: (i) construct an engine with a FakeAudioContext whose `resume()` rejects with `DOMException { name: 'InvalidStateError' }`; (ii) attach a `vi.fn()` subscriber via `engine.clock.onSuspend(cb)`; (iii) call `engine.resume()`; (iv) assert `cb` was invoked exactly once. The test directly verifies the synchronous synthetic-suspend path. NO reliance on a natural statechange event (which the FakeAudioContext does NOT fire in this scenario — matching real iOS Safari behavior). Document the test rationale inline: "Revision 2 Blocker #1: the AC was already 'suspended' before resume(), stays 'suspended' after the rejection, no statechange fires. clock.notifySuspended() is the engine-only synthetic-suspend path that preserves the iOS Safari recovery flow."
  </action>
  <verify>
    <automated>pnpm test:run src/audio/audioEngine.test.ts</automated>
    <automated>pnpm test:run</automated>
    <automated>pnpm build</automated>
    <automated>pnpm lint</automated>
    <source_assertion>grep -c "readonly clock: SessionClock" src/audio/audioEngine.ts returns 1</source_assertion>
    <source_assertion>grep -c "createAudioSessionClock(audioCtx)" src/audio/audioEngine.ts returns 1</source_assertion>
    <source_assertion>grep -c "SessionClock & { notifySuspended" src/audio/audioEngine.ts returns at least 1 (the augmented-type annotation on the internal clock reference — revision 2 Blocker #1)</source_assertion>
    <source_assertion>grep -c "clock\\.notifySuspended()" src/audio/audioEngine.ts returns 1 (the L445 InvalidStateError catch — revision 2 Blocker #1)</source_assertion>
    <source_assertion>grep -c "onStateChange" src/audio/audioEngine.ts returns 0 (the option, the local listener var, and the call sites all removed — revision 1 Blocker #3 + revision 2 Blocker #1)</source_assertion>
    <source_assertion>grep -c "addEventListener.*statechange" src/audio/audioEngine.ts returns 0 (revision 1 Blocker #3: the local listener is REMOVED; the clock's listener is inside createAudioSessionClock per Plan 50-01)</source_assertion>
    <source_assertion>grep -c "removeEventListener.*statechange" src/audio/audioEngine.ts returns 0 (revision 1 Blocker #3: no local listener to remove)</source_assertion>
    <source_assertion>Revision 2 Warning #3 positive assertion: `grep -c "readState()" src/audio/audioEngine.ts` returns at most 1 (only the `get state()` getter at L452-454 reads readState() after the L445 deletion; the L243-257 listener's readState() call is gone)</source_assertion>
  </verify>
  <acceptance_criteria>
    - `AudioEngine` interface has a `readonly clock: SessionClock` member with JSDoc citing D-11 + ABSTR-01 + revision 1 Blocker #1 + revision 2 Blocker #1.
    - `createAudioEngine` constructs the clock via `createAudioSessionClock(audioCtx)` (with NO scheduleImpl arg at this plan — Plan 50-06 will reconstruct with scheduleImpl). The local reference is typed as `SessionClock & { notifySuspended(): void }` so the engine can call `clock.notifySuspended()` at L445 (revision 2 Blocker #1).
    - The returned engine object includes `clock` in its literal; the assignment widens to `SessionClock` so external consumers cannot call `notifySuspended`.
    - `AudioEngineOptions.onStateChange` is removed.
    - **Revision 1 Blocker #3:** The local statechange listener (audioEngine.ts L243-257) is REMOVED; the L378 removeEventListener is REMOVED. The clock owns the single AC statechange listener.
    - **Revision 2 Blocker #1:** The L445 fan-out (formerly `opts.onStateChange?.(readState())` inside the InvalidStateError catch) is REWIRED to `clock.notifySuspended()` — NOT deleted. The `if ((err as DOMException)?.name === 'InvalidStateError')` check stays. Comments cite revision 2 Blocker #1.
    - **Revision 2 Warning #3 positive assertion:** `grep -c "readState()" src/audio/audioEngine.ts ≤ 1` (the only remaining read is the `get state()` getter; the L445 readState() call is gone, replaced by clock.notifySuspended() which does not read state).
    - **Revision 1 Warning #12:** the clock-construction inline comment cites "this is THE engine clock (HRV AC); useNaviKriyaAudio constructs its own SEPARATE clock for the NK AC — they MUST NOT be conflated."
    - `pnpm test:run src/audio/audioEngine.test.ts` exits 0; updated tests subscribe via `engine.clock` rather than `onStateChange`; the InvalidStateError test directly asserts `clock.notifySuspended()` fan-out (revision 2 Blocker #1).
    - Full suite passes; test count unchanged from end of Plan 50-03.
    - `pnpm build` exits 0; `pnpm lint` exits 0.
  </acceptance_criteria>
  <done>
    AudioEngine exposes clock; createAudioEngine constructs and returns it; the internal reference is typed as the augmented return type; onStateChange option removed; revision 1 Blocker #3 committed path implemented (local listener + L378 removeEventListener REMOVED); revision 2 Blocker #1 implemented (L445 fan-out REWIRED to clock.notifySuspended()); engine tests migrated to subscriptions including the InvalidStateError synthetic-suspend assertion; revision 2 Warning #3 grep gate verified; full suite green.
  </done>
</task>

<task type="auto">
  <name>Task 2: Migrate useAudioCues from onStateChange callback to engine.clock.onSuspend / onResume / onClose subscriptions; preserve AudioStatusFlag machine (3-value enum per revision 2 Warning #4) including byte-identical 'unavailable' on close (revision 1 Blocker #1); update stale JSDoc at L263-264 (revision 1 Warning #11) AND at L66 audioNow interface method (revision 2 Warning #5)</name>
  <files>src/hooks/useAudioCues.ts, src/hooks/useAudioCues.test.tsx</files>
  <read_first>
    - .planning/phases/50-sessionclock-scheduler-abstraction/50-CONTEXT.md (D-11 onSuspend/onResume wired real; "useAudioCues migrates from consuming onStateChange in AudioEngineOptions to subscribing through the SessionClock surface — but the existing AudioStatusFlag state machine (needs-resume flow, gesture-recovery seam) keeps its shape"; D-09 zero performance.now() / audioCtx.currentTime in this file)
    - .planning/phases/50-sessionclock-scheduler-abstraction/50-PATTERNS.md (`src/hooks/useAudioCues.ts (MODIFIED — onStateChange → onSuspend/onResume)` migration subsection — existing handleStateChange split into handleResume + handleSuspend + handleClose; subscription wiring inside start(); reconstructEngine resubscription)
    - src/audio/audioStatus.ts (revision 2 Warning #4 — the AudioStatusFlag enum is exactly `'ok' | 'needs-resume' | 'unavailable'`; any 'failed' reference in comments or tests within useAudioCues.ts or useAudioCues.test.tsx is incorrect and must be left alone if it does not exist, OR removed if it does. Audit for 'failed' references and remove them.)
    - src/hooks/useAudioCues.ts (entire file — handleStateChange at L153-179 including the 'closed' branch at L164-165; start() at L237-289; reconstructEngine at L326-414; reads of engine.now() at L265 and L459; engine.scheduleNextCue at L453; engine.playEndChord at L463; **the stale JSDoc comment at L263-264 referencing `session.start(performance.now())` — revision 1 Warning #11 requires update**; **the L66 JSDoc on the audioNow interface method reading "Returns audioCtx.currentTime" — revision 2 Warning #5 requires update**)
    - src/audio/audioEngine.ts (after Task 1 of this plan — confirm engine.clock surface and the removed onStateChange option, plus the new clock.notifySuspended() call at L445)
    - src/audio/sessionClock.ts (Plan 50-01 revision 2 — onSuspend / onResume / onClose contract; unsubscribe function semantics; the augmented return type containing notifySuspended is engine-internal and NOT visible to useAudioCues)
    - src/hooks/useAudioCues.test.tsx (existing test setup — confirm how it drives state transitions; whether it injects a mock onStateChange or drives FakeAudioContext directly; note: useAudioCues tests are extensive and exercise the full AudioStatusFlag state machine including 'unavailable' on close)
  </read_first>
  <action>
    Edit `src/hooks/useAudioCues.ts`:

    1. Remove the existing `handleStateChange` callback at L153-179. Replace with THREE callbacks that split the prior state-machine logic (revision 1 Blocker #1 adds the third):
       - `handleResume`: equivalent to the `state === 'running'` branch — sets `visibilityResumeAttemptedRef.current = false` and `setAudioStatus('ok')`. The `engine` null-check at L157-160 stays (use a useRef-based read of engineRef.current at the top of handleResume).
       - `handleSuspend`: equivalent to the `(state === 'suspended' || state === 'interrupted') && visibilityResumeAttemptedRef.current` branch — `setAudioStatus('needs-resume')`. Same engine null-check guard.
       - **`handleClose` (revision 1 Blocker #1):** equivalent to the `state === 'closed'` branch — `setAudioStatus('unavailable')`. Same engine null-check guard. Document with comment: "// Revision 1 Blocker #1: preserves the byte-identical setAudioStatus('unavailable') setter (formerly at L164-165 in the unified handleStateChange). engine.clock.onClose fires this when the AC transitions to 'closed' — whether via stop()/reconstructEngine paths OR an unexpected browser-side AC kill. Phase 50 success criterion #3 (byte-identical behavior) satisfied."

       Each handler is wrapped in `useCallback` with empty deps `[]` (mirrors the existing handleStateChange's `[]` deps at L178).

       **Per revision 2 Warning #4:** `AudioStatusFlag` has exactly 3 values: `'ok' | 'needs-resume' | 'unavailable'`. No `'failed'` value exists. Any comment in the rewritten handlers or in surrounding context that references a fourth `'failed'` value is incorrect; do not add such comments. If an existing comment in the file references `'failed'`, audit it and remove it (the value is not real).

    2. Inside `start()` at L237-289, after `engineRef.current = engine` (L255), subscribe to the engine's clock — THREE subscriptions per revision 1 Blocker #1:
       ```
       const unsubResume = engine.clock.onResume(handleResume)
       const unsubSuspend = engine.clock.onSuspend(handleSuspend)
       const unsubClose = engine.clock.onClose(handleClose)
       // Store unsubs in a ref so stop()/reconstructEngine can call them on teardown.
       clockUnsubsRef.current = [unsubResume, unsubSuspend, unsubClose]
       ```
       Add a new ref at hook scope: `const clockUnsubsRef = useRef<Array<() => void>>([])`. Document with a comment: "// Phase 50 D-11 + revision 1 Blocker #1: clock subscriptions (suspend/resume/close) live alongside engineRef. Must be torn down before engineRef is nulled (close()) or reassigned (reconstructEngine). Revision 2 Blocker #1: engine.resume() rejection with InvalidStateError fires the suspend channel via clock.notifySuspended() (engine-internal), so handleSuspend MUST be subscribed before any resume() call that might reject — confirmed by start() ordering (subscriptions added immediately after engineRef.current = engine, before any external code can call engine.resume())."

    3. Update `createAudioEngine` call at L254: remove the `onStateChange: handleStateChange` argument. Becomes:
       ```
       const engine = await createAudioEngine({ timbre, bypassSilentMode })
       ```

    4. Inside `stop()` at L294-308, BEFORE `engineRef.current = null` (L299), unsubscribe:
       ```
       for (const off of clockUnsubsRef.current) off()
       clockUnsubsRef.current = []
       ```
       Same teardown inside `reconstructEngine` at L326-414, BEFORE nulling `engineRef.current` at L356. After constructing `newEngine` at L364-365, resubscribe (three subscriptions per revision 1 Blocker #1):
       ```
       const unsubResume = newEngine.clock.onResume(handleResume)
       const unsubSuspend = newEngine.clock.onSuspend(handleSuspend)
       const unsubClose = newEngine.clock.onClose(handleClose)
       clockUnsubsRef.current = [unsubResume, unsubSuspend, unsubClose]
       ```

    5. Replace `engine.scheduleLeadIn` usage — leave it alone (Plan 50-06 makes scheduleLeadIn a facade over schedule(); the external API stays the same).

    6. Replace `engine.now()` at L265: `const startAudioTime = engine.now()` → `const startAudioTime = engine.clock.now()`. D-03 Option A makes this byte-identical at runtime (engine.clock.now() returns audioCtx.currentTime).

    7. Replace `engine.now()` at L459 (inside the `audioNow` returned helper): `return engineRef.current?.now() ?? null` → `return engineRef.current?.clock.now() ?? null`.

    8. **Revision 1 Warning #11 — update the stale JSDoc comment at L262-264** (the block immediately before `const startAudioTime = engine.now()`):
       The current comment reads:
       ```
       // D-09 + D-13 + D-14: schedule the lead-in. Anchor at engine.now() — the
       // audioCtx.currentTime instant of lead-in start, which App.tsx co-anchors with
       // session.start(performance.now()) for the dual-clock alignment.
       ```
       Update to reflect that session capture now uses `clock.now()`:
       ```
       // D-09 + D-13 + D-14: schedule the lead-in. Anchor at engine.clock.now() — the
       // audioCtx.currentTime instant of lead-in start (per D-03 Option A, the clock
       // returns audioCtx.currentTime). The session-engine co-anchors via clock.now()
       // for the dual-clock alignment (Phase 50: useSessionEngine consumes a
       // createWallSessionClock() per Plan 50-02; Phase 51 will swap to the engine's
       // audio clock via the same SessionClock seam).
       ```
       Cite revision 1 Warning #11: "Comment updated post Phase 50 migration — session capture uses clock.now(), not performance.now()."

    9. **Revision 2 Warning #5 — update the stale L66 JSDoc on the `audioNow` interface method** (the AudioCues interface declaration at L66-68):
       The current comment reads:
       ```
       /** Returns audioCtx.currentTime, or null if AC unavailable. App.tsx uses this for
        *  the dual-anchor (Pitfall 2). */
       audioNow(this: void): number | null
       ```
       Update to:
       ```
       /** Returns engine.clock.now() (= audioCtx.currentTime per D-03 Option A), or null if
        *  AC unavailable. App.tsx uses this for the dual-anchor (Pitfall 2). Revision 2
        *  Warning #5: comment updated post Phase 50 — audioNow reads through the SessionClock
        *  seam (engine.clock.now()); the underlying source is still audioCtx.currentTime via
        *  D-03 Option A. */
       audioNow(this: void): number | null
       ```
       Cite revision 2 Warning #5: "JSDoc on the audioNow interface method updated to reflect the SessionClock seam — the underlying read source is unchanged (D-03 Option A: clock.now() returns audioCtx.currentTime), but the call path goes through engine.clock.now() instead of audioCtx.currentTime directly."

    10. Update useCallback dep arrays:
       - `start` (L289): change `[handleStateChange]` → `[handleResume, handleSuspend, handleClose]`.
       - `reconstructEngine` (L326-414): add `handleResume`, `handleSuspend`, and `handleClose` to its dep array.

    11. Update `src/hooks/useAudioCues.test.tsx`:
       - Any test that injects an `onStateChange` mock callback into `createAudioEngine` must be updated. Two migration paths:
         (a) Drive the FakeAudioContext's statechange transition directly via the existing test polyfill; the clock's listener fans to the hook's subscribers; assert `audioStatus` updates as before.
         (b) Replace the test's introspection of `onStateChange.mock.calls` with assertions on the resulting `audioStatus` state (preferred — assertion is on the hook's behavior, not on the internal callback shape per `no-design-locking`).
       - **Tests for the `state === 'closed'` path (revision 1 Blocker #1):** these MUST pass post-migration because the `onClose` subscriber preserves byte-identical behavior. Drive a FakeAudioContext statechange to `'closed'`; assert `audioStatus` becomes `'unavailable'` — same assertion as pre-refactor, just a different code path inside useAudioCues. If a test previously checked that the unified `handleStateChange` was invoked with `'closed'`, rewrite to check that the resulting state is `'unavailable'` (behavior, not implementation).
       - **Tests for the iOS Safari InvalidStateError path (revision 2 Blocker #1):** if useAudioCues.test.tsx has tests that trigger an `engine.resume()` rejection with `InvalidStateError`, these now flow through `clock.notifySuspended()` at the engine layer (Task 1 step 7) and into `handleSuspend` at the hook layer. Assert: with `visibilityResumeAttemptedRef.current = true` (set by a prior visibility-driven resume attempt), the `audioStatus` becomes `'needs-resume'` after the rejection. This is byte-identical to pre-refactor behavior; the path through `clock.notifySuspended()` is transparent to the hook (it just receives a suspend event).
       - **Revision 2 Warning #4 — `AudioStatusFlag` enum citations:** if any test comment or assertion references a `'failed'` AudioStatusFlag value, REMOVE it. The enum has only 3 values: `'ok' | 'needs-resume' | 'unavailable'`. Assertions on these 3 values are correct; assertions on `'failed'` (if any exist) are dead code from a prior incorrect citation.
       - Test count constraint: 1:1 mapping. No additions, no removals, no skips.
  </action>
  <verify>
    <automated>pnpm test:run src/hooks/useAudioCues.test.tsx</automated>
    <automated>pnpm test:run</automated>
    <automated>pnpm build</automated>
    <automated>pnpm lint</automated>
    <source_assertion>grep -c "performance\\.now" src/hooks/useAudioCues.ts returns 0 (revision 1 Warning #11 — including in comments)</source_assertion>
    <source_assertion>grep -v '^ *//' src/hooks/useAudioCues.ts | grep -v '^ *\*' | grep -c "audioCtx\\.currentTime" returns 0</source_assertion>
    <source_assertion>grep -c "engine\\.clock\\.now()" src/hooks/useAudioCues.ts returns at least 1</source_assertion>
    <source_assertion>grep -c "engineRef.current?\\.clock\\.now()" src/hooks/useAudioCues.ts returns at least 1</source_assertion>
    <source_assertion>grep -c "clock\\.onSuspend\\|clock\\.onResume\\|clock\\.onClose" src/hooks/useAudioCues.ts returns at least 6 (start subscribes 3 + reconstructEngine resubscribes 3)</source_assertion>
    <source_assertion>grep -c "onClose" src/hooks/useAudioCues.ts returns at least 4 (handleClose declaration + 2 subscriptions + 1 unsubscribe storage)</source_assertion>
    <source_assertion>grep -c "onStateChange" src/hooks/useAudioCues.ts returns 0</source_assertion>
    <source_assertion>Revision 2 Warning #5 — L66 JSDoc audit: `grep -c "Returns audioCtx.currentTime" src/hooks/useAudioCues.ts` returns 0 (the literal old-form string is gone); `grep -c "Returns engine.clock.now()" src/hooks/useAudioCues.ts` returns at least 1 (the updated form is present)</source_assertion>
    <source_assertion>Revision 2 Warning #4 — verify no spurious 'failed' citations: `grep -c "'failed'" src/hooks/useAudioCues.ts` returns 0 (the enum has no such value)</source_assertion>
  </verify>
  <acceptance_criteria>
    - `src/hooks/useAudioCues.ts` contains zero `performance.now` references in source OR comment lines (revision 1 Warning #11 — stale L263-264 JSDoc updated to reflect clock.now() session capture).
    - `src/hooks/useAudioCues.ts` contains zero `audioCtx.currentTime` references in source lines (excluding JSDoc/comments).
    - `engine.now()` call sites are replaced with `engine.clock.now()` (1 inside start at L265, 1 inside the audioNow helper at L459).
    - `clock.onSuspend(handleSuspend)`, `clock.onResume(handleResume)`, **and `clock.onClose(handleClose)` (revision 1 Blocker #1)** are called inside `start()` and re-called inside `reconstructEngine` after the new engine is built.
    - **Revision 1 Blocker #1:** `handleClose` is a useCallback that fires `setAudioStatus('unavailable')`. The `'closed'` → `'unavailable'` setter is preserved byte-identical (replacing the old L164-165 branch).
    - Unsubscribe functions are stored in `clockUnsubsRef` and called inside `stop()` and at the top of `reconstructEngine` (before nulling engineRef). The ref holds exactly 3 unsubs after subscription, 0 after teardown.
    - `onStateChange` no longer appears in `src/hooks/useAudioCues.ts` source lines.
    - `AudioStatusFlag` machine ('ok', 'needs-resume', 'unavailable') is preserved — assertions in `useAudioCues.test.tsx` exercise the same transitions; the trigger source moves from a callback to a subscription, but the resulting state values are identical.
    - **Revision 2 Warning #4 — no spurious 'failed' enum value:** no comment or test in this file references a `'failed'` AudioStatusFlag value.
    - **Revision 1 Blocker #1 — `'closed'` → `'unavailable'` path preserved:** explicit tests in useAudioCues.test.tsx drive a FakeAudioContext to `'closed'` and assert `audioStatus === 'unavailable'`. These pass without modification beyond the test setup migration from `onStateChange` mock to direct statechange driving.
    - **Revision 2 Blocker #1 — iOS Safari recovery preserved:** the test path that exercises `engine.resume()` rejection with `InvalidStateError` (when `visibilityResumeAttemptedRef.current = true`) results in `audioStatus === 'needs-resume'`, byte-identical to pre-refactor. The hook is transparent to whether the suspend event arrived via natural statechange or via `clock.notifySuspended()`.
    - **Revision 2 Warning #5 — L66 JSDoc updated:** the `audioNow` interface method's JSDoc reads "Returns engine.clock.now() (= audioCtx.currentTime per D-03 Option A)" — NOT the old "Returns audioCtx.currentTime".
    - Full suite passes; test count unchanged.
    - `pnpm build` exits 0; `pnpm lint` exits 0.
    - Drift-guard (Plan 50-07) passes by construction against this file.
  </acceptance_criteria>
  <done>
    useAudioCues subscribes via engine.clock (three channels: suspend/resume/close); AudioStatusFlag machine preserved including byte-identical 'unavailable' on close (revision 1 Blocker #1) and byte-identical 'needs-resume' on InvalidStateError-via-clock.notifySuspended (revision 2 Blocker #1); stale JSDoc at L263-264 updated (revision 1 Warning #11); stale JSDoc at L66 audioNow interface method updated (revision 2 Warning #5); spurious 'failed' enum references audited and removed (revision 2 Warning #4); full suite green.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| AC statechange events → hook state machine | Migrated from callback prop (synchronous, one-shot per construct) to three subscriptions (synchronous, must be torn down explicitly). |
| Engine internals → SessionClock subscribers via synthetic-suspend escape hatch | Revision 2 Blocker #1: the engine's L445 InvalidStateError catch calls `clock.notifySuspended()` via its internal augmented-type reference. The fan-out reaches the same `suspendSubscribers` Set as a natural statechange. The escape hatch is engine-only — external consumers see only `engine.clock: SessionClock` and cannot call notifySuspended. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-50.04-01 | Tampering | npm install during this plan | accept | DEPS-01: zero new entries in `package.json`. |
| T-50.04-02 | Denial of service | unbounded subscriber Set growth across mount/unmount cycles | mitigate | `clockUnsubsRef` ensures each `start()` is paired with three `off()` calls (per revision 1 Blocker #1) in `stop()` and at the top of `reconstructEngine`. Acceptance criterion above enforces the 3-unsub storage pattern. Plan 50-01 Task 2 already tested the unsubscribe paths for all three channels. |
| T-50.04-03 | Repudiation | dual-channel statechange (clock + remaining engine internals) | mitigate | **Revision 1 Blocker #3 committed path:** the local listener at L243-257 is DELETED; the L378 removeEventListener is DELETED. The L445 InvalidStateError catch is REWIRED (revision 2 Blocker #1) to call `clock.notifySuspended()` — this uses the clock's existing fan-out path, NOT a separate listener. The clock's listener is the SOLE statechange consumer. No dual-channel risk. |
| T-50.04-04 | Spoofing | hooks bypass the clock to read time directly | mitigate | Drift-guard (Plan 50-07) verifies `performance.now()` and `audioCtx.currentTime` do not appear in this file's source lines. Verified at Plan 50-07's commit. |
| T-50.04-05 | Tampering | `'closed'` state no longer fans to subscribers, potentially leaving `audioStatus` stuck on `'ok'` after unexpected close | **resolved** | **Revision 1 Blocker #1: this threat is RESOLVED, not accepted-with-followup.** Plan 50-01 added `onClose(cb)` to SessionClock. Plan 50-04 wires `engine.clock.onClose(handleClose)` where `handleClose` fires `setAudioStatus('unavailable')`. Byte-identical to the pre-refactor L164-165 branch. Phase 50 success criterion #3 satisfied. No follow-up needed. |
| T-50.04-06 | Repudiation | L445 InvalidStateError fan-out path drops iOS Safari `'needs-resume'` transition because no natural statechange fires after the rejection | **resolved** | **Revision 2 Blocker #1: this threat is RESOLVED.** Plan 50-01 revision 2 added the engine-only escape hatch `clock.notifySuspended()` on the augmented factory return type `SessionClock & { notifySuspended(): void }`. This plan REWIRES the L445 fan-out from `opts.onStateChange?.(readState())` to `clock.notifySuspended()` — preserving the synchronous synthetic-suspend report byte-identically. The iOS Safari recovery flow (Plan 06 D-38) is preserved end-to-end: a rejected `audioCtx.resume()` invokes `handleSuspend` via the clock's fan-out, which sets `audioStatus = 'needs-resume'` when `visibilityResumeAttemptedRef.current = true`. The encapsulation boundary holds: external consumers see only `engine.clock: SessionClock` (6-member read-only interface) — they cannot call `notifySuspended` to forge suspend events. Test Task 1 step 10 directly asserts this path. |
| T-50.04-07 | Elevation of privilege | external code calls `notifySuspended` to forge suspend events | accept | The augmented factory return type `SessionClock & { notifySuspended(): void }` is local to `createAudioEngine`'s closure. The engine.clock public member is widened to `SessionClock` at the boundary. External code that imports `SessionClock` (via the `export type { SessionClock } from './sessionClock'` re-export from audioEngine.ts, or directly from `./sessionClock`) does NOT see `notifySuspended` in the type. Plan 50-01 Task 2 (revision 2) includes a `@ts-expect-error` test that locks this in at compile time. ASVS L1: access control enforced structurally via TypeScript narrowing. |

**Reasoning:** Phase 50 shifts the trigger source for one state machine from a callback prop to three subscriptions (suspend/resume/close per revision 1 Blocker #1). The state machine shape is preserved verbatim (3-value enum per revision 2 Warning #4). No new attack surface. Revision 1 Blocker #3 commits to deleting the local listener entirely — single listener on the AC. Revision 2 Blocker #1 preserves the iOS Safari InvalidStateError synthetic-suspend path via the engine-only `clock.notifySuspended()` escape hatch on the augmented factory return type — the encapsulation boundary holds, external code cannot bypass it.
</threat_model>

<verification>
- `pnpm build` exits 0; `pnpm lint` exits 0; `pnpm test:run` exits 0.
- Test count unchanged from end of Plan 50-03 (the only deltas across the phase are the new sessionClock.test.ts tests, the new dispatch tests in Plan 50-06, and the new drift-guard test in Plan 50-07).
- `git diff package.json` is empty (DEPS-01).
- `grep -rn "performance\\.now\\|audioCtx\\.currentTime" src/hooks/useAudioCues.ts` returns zero matches in source AND comment lines (revision 1 Warning #11 + revision 2 Warning #5 include the JSDoc updates).
- `grep -rn "onStateChange" src/audio/audioEngine.ts src/hooks/useAudioCues.ts` returns zero matches.
- `AudioStatusFlag` enum unchanged: `'ok' | 'needs-resume' | 'unavailable'` (revision 2 Warning #4 — THREE values, no `'failed'`).
- **Revision 1 Blocker #3:** `grep -c "addEventListener.*statechange\\|removeEventListener.*statechange" src/audio/audioEngine.ts` returns 0.
- **Revision 2 Blocker #1:** `grep -c "clock.notifySuspended()" src/audio/audioEngine.ts` returns 1 (the L445 InvalidStateError catch).
- **Revision 2 Warning #3 positive assertion:** `grep -c "readState()" src/audio/audioEngine.ts ≤ 1` (only the `get state()` getter reads readState() after this plan).
</verification>

<success_criteria>
- ABSTR-01: SessionClock's `onSuspend` / `onResume` / `onClose` surface has a real consumer; the augmented factory return type's `notifySuspended` escape hatch has its engine-internal consumer.
- ABSTR-03 partial: `useAudioCues` is caller 4 of 5 to consume SessionClock; zero direct `performance.now()` / `audioCtx.currentTime` reads.
- ABSTR-04: test parity preserved.
- D-11 honored: clock owns the AC statechange listener; subscribers consume suspend/resume; AudioStatusFlag machine preserved.
- D-09 honored: drift-guard passes by construction for this file.
- **Revision 1 Blocker #1 — `'closed'` → `'unavailable'` byte-identical:** `engine.clock.onClose` is wired; handleClose preserves the L164-165 behavior. Phase 50 success criterion #3 satisfied.
- **Revision 1 Blocker #2 — `scheduleImpl` plumbed at construction:** this plan passes no scheduleImpl (Plan 50-06 will reconstruct with one); the clock object is never reassigned post-hoc.
- **Revision 1 Blocker #3 — architectural path committed:** local statechange listener DELETED; L378 removeEventListener DELETED. Clock's listener is sole AC statechange consumer.
- **Revision 1 Warning #11 — stale JSDoc updated:** comment at L263-264 reflects `clock.now()` session capture (no `performance.now()` reference).
- **Revision 1 Warning #12 — phase-level invariant note:** clock-construction inline comment documents the HRV/NK clock distinction.
- **Revision 2 Blocker #1 — L445 fan-out REWIRED to `clock.notifySuspended()`:** the synchronous synthetic-suspend path is preserved byte-identically via the augmented factory return type's escape hatch. iOS Safari recovery (Plan 06 D-38) is preserved end-to-end. The engine's internal augmented-type reference makes the call type-check; the public `engine.clock: SessionClock` member widens to 6 members so external consumers cannot bypass the encapsulation.
- **Revision 2 Warning #3 — positive assertion:** `grep -c "readState()" src/audio/audioEngine.ts ≤ 1`. The L243-257 listener's readState() call is gone; the L445 readState() call is gone (replaced by clock.notifySuspended() which does not read state).
- **Revision 2 Warning #4 — AudioStatusFlag citation corrected:** the enum is `'ok' | 'needs-resume' | 'unavailable'` (3 values, no `'failed'`); all plan references corrected.
- **Revision 2 Warning #5 — L66 JSDoc updated:** the `audioNow` interface method's JSDoc reads "Returns engine.clock.now() (= audioCtx.currentTime per D-03 Option A)".
- **Revision 2 Warning #6 — ordering parity at plan close:** the intermediate-state clock construction site (L177, immediately after `new AudioContext()`) passes the green-gate; Plan 50-06 will MOVE it to ~L295 (post-schedule-function) without changing observable behavior; the final state also passes the green-gate. Both intermediate and final states are independently green per the per-commit invariant.
- DEPS-01 + QUAL-01 verified.
- Byte-identical end-user behavior: the iOS lock-unlock recovery flow (needs-resume → user-gesture-resume → ok) walks the same path as v2.1; the kitchen-sink iOS gesture-preservation in reconstructEngine (L313-326) is untouched.
</success_criteria>

<output>
Create `.planning/phases/50-sessionclock-scheduler-abstraction/50-04-SUMMARY.md` when done. Confirm revision 1 Blocker #1 (`onClose` wiring), Blocker #3 (deleted local listener + L378 removeEventListener), Warning #11 (L263-264 JSDoc update), Warning #12 (invariant note) AND revision 2 Blocker #1 (L445 REWIRED to `clock.notifySuspended()`), Warning #3 (readState() positive grep gate), Warning #4 (AudioStatusFlag enum citation corrected), Warning #5 (L66 audioNow JSDoc update), Warning #6 (ordering parity acknowledged for Plan 50-06 move) are all in place. Document the construction site at L177 as intermediate; note that Plan 50-06 will move it to ~L295 with no observable behavior change.
</output>
</content>
