---
phase: 13-inner-ring-ux-symmetry
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
  - .planning/research/FEATURES.md
  - .planning/research/ARCHITECTURE.md
  - .planning/STATE.md
  - src/styles/theme.css
  - .planning/todos/pending/2026-05-11-reduced-motion-still-shows-out-phase-boundary-cue.md
  - .planning/todos/completed/2026-05-11-reduced-motion-still-shows-out-phase-boundary-cue.md
autonomous: true
requirements:
  - WARMUP-01

must_haves:
  truths:
    - "Under `prefers-reduced-motion: reduce`, the `.orb-ring--inner` element is not rendered in layout in any state (Out phase, In phase, or 3-2-1 lead-in) (D-01, D-03, D-04)."
    - "Under normal motion, the existing Out-phase inner-ring fade-in behavior is byte-identical to the v1.0.1 baseline — no normal-motion CSS rule is changed (D-01, D-05)."
    - "The `.orb-layer--out` opacity crossfade transition remains the sole substitute phase indicator under reduced motion — D-07 (Phase 2) preserved verbatim (D-12)."
    - "The CR-01 caveat (`do NOT set transform: none !important here`) inside the `@media (prefers-reduced-motion: reduce)` block is preserved verbatim (D-06)."
    - "ROADMAP.md §Phase 13 Goal + SC1 + SC2 reflect the actual scope (reduced-motion inner-ring suppression), not the rejected symmetric-arrival framing (D-08)."
    - "REQUIREMENTS.md WARMUP-01 bullet text reflects the actual scope (D-08)."
    - "FEATURES.md (lines 41, 103-105, 114, 125, 152, 206-209) and ARCHITECTURE.md (lines 21, 45-48, 310-315, 383, 432) carry `[2026-05-12 update]` callouts pointing at `13-CONTEXT.md` D-01/D-03 — historical text preserved verbatim (D-08, Phase 12 D-02 idiom)."
    - "STATE.md Pending Todos sub-bullet for the 2026-05-11 carry-forward todo reflects RESOLVED-by-Phase-13 framing (D-08)."
    - "The folded todo file `.planning/todos/pending/2026-05-11-reduced-motion-still-shows-out-phase-boundary-cue.md` is moved to `.planning/todos/completed/` via `git mv` (D-02, D-09)."
    - "Each of the three Phase 13 commit boundaries passes the green-gate: `npx tsc --noEmit && npm run lint && npm run build && npm test` exits 0 with the 409/409 Vitest baseline preserved (D-10, no test changes per D-07)."
    - "No TSX file is modified. No test file is modified. No storage / audio / timing / hook file is modified — ROADMAP §13 SC3 invariant holds (D-01)."
    - "Zero net-new npm dependencies — pure CSS + doc edits + file move (D-11)."
    - "Phase name, slug, and directory `13-inner-ring-ux-symmetry` are NOT renamed mid-milestone; ROADMAP §13 H3 header text `### Phase 13: Inner-Ring UX Symmetry` stays as-is (D-13)."
  artifacts:
    - path: ".planning/ROADMAP.md"
      provides: "Phase 13 Goal + SC1 + SC2 rewritten + line-47 v1.1 index entry tagline rewritten per D-08"
      contains: "Under `prefers-reduced-motion: reduce`, the inner reference ring"
    - path: ".planning/REQUIREMENTS.md"
      provides: "WARMUP-01 bullet text rewritten per D-08"
      contains: "the inner reference ring (`.orb-ring--inner`) is not rendered"
    - path: ".planning/research/FEATURES.md"
      provides: "Six `[2026-05-12 update]` callouts appended at lines 41, 103-105, 114, 125, 152, 206-209 — historical text preserved verbatim"
      contains: "[2026-05-12 update]"
    - path: ".planning/research/ARCHITECTURE.md"
      provides: "Five `[2026-05-12 update]` callouts appended at lines 21, 45-48, 310-315, 383, 432 — historical text preserved verbatim"
      contains: "[2026-05-12 update]"
    - path: ".planning/STATE.md"
      provides: "Pending Todos sub-bullet for `2026-05-11-reduced-motion-still-shows-out-phase-boundary-cue` rewritten to RESOLVED-by-Phase-13 framing per D-08"
      contains: "RESOLVED directly by Phase 13"
    - path: "src/styles/theme.css"
      provides: "New `.orb-ring--inner { display: none; }` rule inside the existing `@media (prefers-reduced-motion: reduce)` block + JSDoc comment rewrite at lines 99-107 + JSDoc comment extension at lines 142-153"
      contains: ".orb-ring--inner"
    - path: ".planning/todos/completed/2026-05-11-reduced-motion-still-shows-out-phase-boundary-cue.md"
      provides: "Folded todo relocated from pending/ to completed/ via git mv — content unchanged"
      contains: "resolves_phase: 13"
  key_links:
    - from: "src/styles/theme.css `@media (prefers-reduced-motion: reduce)` block"
      to: ".orb-ring--inner element rendered by BreathingShapeBody + BreathingShapeLeadIn"
      via: "CSS @media gate"
      pattern: "@media \\(prefers-reduced-motion: reduce\\)[\\s\\S]*\\.orb-ring--inner[\\s\\S]*display: none"
    - from: "ROADMAP.md §Phase 13 Goal + SC1 + SC2"
      to: "REQUIREMENTS.md WARMUP-01"
      via: "Shared reduced-motion-suppression framing (D-08)"
      pattern: "the inner reference ring \\(`\\.orb-ring--inner`\\)"
    - from: "FEATURES.md / ARCHITECTURE.md historical content"
      to: ".planning/phases/13-inner-ring-ux-symmetry/13-CONTEXT.md"
      via: "`[2026-05-12 update]` callout forward pointers"
      pattern: "13-CONTEXT\\.md"
---

<validation_note>
No DISCOVERY.md / VALIDATION.md was produced for Phase 13. The phase is a CSS suppression
(one rule add + two comment edits), a doc rewrite sweep, and a `git mv` of a folded todo —
no novel external surface, no library choice, no new test seam. CONTEXT.md D-01 through D-13
are highly prescriptive (verbatim selectors, line numbers, exact prose). User selected
"Continue anyway" at the no-research / no-VALIDATION gate (Nyquist override). This plan
may fail plan-checker Dimension 8 (validation evidence) — that failure is expected and
accepted for Phase 13's narrow scope.
</validation_note>

<objective>
Implement WARMUP-01 (reduced-motion `.orb-ring--inner` suppression) and the supporting doc
rewrite sweep so future planners cannot re-derive the rejected "symmetric arrival cue"
framing from the v1.1-research artifacts.

Purpose: Restore D-07 (Phase 2) — the `.orb-layer--out` opacity crossfade — as the **sole**
substitute phase indicator under `prefers-reduced-motion: reduce`, by removing the
redundant inner reference ring which carries no information when the orb is locked at
`MID_SCALE`. Simultaneously, correct the v1.1-research milestone-artifact set
(ROADMAP §13, REQUIREMENTS WARMUP-01, FEATURES.md, ARCHITECTURE.md, STATE.md) so the
scope-misframe documented in 13-CONTEXT.md D-01 is annotated and the corrected scope
is the canonical source for downstream phases.

Output: One CSS rule added inside the existing `@media (prefers-reduced-motion: reduce)`
block; two JSDoc comment edits in the same file; six milestone-artifact doc edits;
one `git mv` of the folded carry-forward todo. Three commits, single plan, single wave.
</objective>

<execution_context>
@$HOME/.claude-personal-personal/get-shit-done/workflows/execute-plan.md
@$HOME/.claude-personal-personal/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/13-inner-ring-ux-symmetry/13-CONTEXT.md
@.planning/phases/13-inner-ring-ux-symmetry/13-PATTERNS.md
@.planning/todos/pending/2026-05-11-reduced-motion-still-shows-out-phase-boundary-cue.md
@src/styles/theme.css

<interfaces>
<!--
  No TypeScript interfaces are consumed or produced by this plan. The CSS edit is
  presentation-only and the React component code (`BreathingShape.tsx`,
  `usePrefersReducedMotion.ts`) is NOT modified. Listing the load-bearing constants here
  so the executor does not re-explore the codebase.
-->

From src/styles/theme.css (current state, lines 99-117 — the ring rule we are gating):

```css
/* 260510-tc9 Bug 1 (iter 3): inner reference ring is the Out-phase arrival cue.
   - Hidden by default (opacity 0) so it does not distract during In phase or the
     3-2-1 lead-in countdown (where there is no data-phase attribute on root).
   - Fades in/out at the same 400ms ease-in-out as the orb-layer phase crossfade
     (see .orb-layer--in / .orb-layer--out), so the ring appears in lockstep with
     the orb shifting from teal to blue and recedes back during the next In flip.
   - Reduced-motion: the transition is intentionally PRESERVED, mirroring the
     orb-layer--out reduced-motion contract (D-07) — the fade-in IS the phase
     indicator when the orb is locked at MID_SCALE. */
.orb-ring--inner {
  border-color: var(--color-ring-inner);
  border-width: 1.5px;
  opacity: 0;
  transition: opacity 400ms ease-in-out;
}

[data-phase='out'] .orb-ring--inner {
  opacity: 1;
}
```

From src/styles/theme.css (current state, lines 142-158 — the @media block we are extending):

```css
/* Reduced-motion (D-05, D-07, D-09):
   - The .orb scale transition was removed (see .orb rule above), so this block
     no longer needs to suppress it. Kept for the dialog modal fade only.
   - Intentionally PRESERVE the .orb-layer--out opacity transition — it is the
     substitute phase indicator under reduced-motion (D-07).
   - CR-01: Do NOT set `transform: none !important` here. BreathingShape sets the
     reduced-motion fixed scale via inline `transform: scale(var(--orb-scale-mid))`
     when usePrefersReducedMotion() is true. Inline styles do NOT win over an
     `!important` stylesheet rule, so a `transform: none !important` here would
     silently nuke the JS-controlled MID_SCALE and render the orb at scale(1).
   - F3: dialog modal fade also disabled here (per UI-SPEC: "in reduced-motion,
     dialog appears immediately"). */
@media (prefers-reduced-motion: reduce) {
  dialog.modal-fade {
    transition: none !important;
  }
}
```

Test files that REMAIN GREEN unchanged (D-07): `display: none` keeps elements in the DOM,
so all three existing inner-ring assertions still find the element via `querySelector`:
- `src/app/App.session.test.tsx:99-108` — `shape.querySelector('[aria-hidden="true"].orb-ring--inner')` (non-null) → still passes
- `src/components/BreathingShape.test.tsx:169-188` — Body inner-ring template (className + width/height style) → still passes
- `src/components/BreathingShape.test.tsx:255-262` — lead-in inner-ring template → still passes
</interfaces>

</context>

<tasks>

<!-- ============================================================ -->
<!-- COMMIT 1: Doc rewrite (zero-code) — D-08, lands first per D-09 -->
<!-- ============================================================ -->

<task type="auto">
  <name>Task 1: Rewrite ROADMAP.md §Phase 13 + REQUIREMENTS.md WARMUP-01 + STATE.md pending-todo bullet</name>
  <files>.planning/ROADMAP.md, .planning/REQUIREMENTS.md, .planning/STATE.md</files>
  <read_first>
    - `.planning/phases/13-inner-ring-ux-symmetry/13-CONTEXT.md` (D-08 verbatim text)
    - `.planning/phases/13-inner-ring-ux-symmetry/13-PATTERNS.md` §`.planning/ROADMAP.md §Phase 13` / §`.planning/REQUIREMENTS.md line 16` / §`.planning/STATE.md line 74`
    - `.planning/ROADMAP.md` lines 47, 57-68, 161 (current state to be rewritten)
    - `.planning/REQUIREMENTS.md` line 16 + traceability row at line 99 (line 99 row is unchanged; status column flips only when phase closes)
    - `.planning/STATE.md` lines 72-75 (Pending Todos block — only the carry-forward sub-bullet is rewritten, NOT the `Phase 13 planning: …` bullet)
  </read_first>
  <action>
    Apply three independent prose edits across the three milestone-artifact files. Use the Edit tool (one Edit per logical change). NO code blocks inside this action — the verbatim replacement strings are in 13-CONTEXT.md D-08 and the patterns are in 13-PATTERNS.md.

    (a) `.planning/ROADMAP.md` — three edits in this file:
      1. Line 47 v1.1 index entry tagline: replace "Pure CSS + minimal TSX warm-up; symmetric arrival cue at both In and Out phase boundaries" with a new tagline reflecting the actual scope. Suggested wording from PATTERNS.md §ROADMAP: "Reduced-motion inner-ring suppression — `.orb-layer--out` crossfade restored as sole substitute phase cue".
      2. Lines 57-67 §"Phase 13: Inner-Ring UX Symmetry" block: rewrite Goal + SC1 + SC2 verbatim per 13-CONTEXT.md D-08. The H3 header text "### Phase 13: Inner-Ring UX Symmetry" stays unchanged (per D-13: no rename mid-milestone). SC3 and SC4 stay verbatim. `**Depends on**:`, `**Requirements**:`, `**Plans**:`, and `**UI hint**:` lines stay unchanged.
         - New Goal: "Under `prefers-reduced-motion: reduce`, the inner reference ring (`.orb-ring--inner`) is no longer rendered — the `.orb-layer--out` opacity crossfade remains the sole reduced-motion phase indicator (D-07 preserved)."
         - New SC1: "Under `prefers-reduced-motion: reduce`, the inner reference ring is not drawn in any state (Out, In, lead-in)."
         - New SC2: "Out-phase inner-ring behavior under **normal motion** is pixel-identical to v1.0.1 baseline (only the reduced-motion branch changes)."
      3. Line 161 traceability row: UNCHANGED at this commit. (Status column flips Not started → In progress when execution begins and → Complete on phase close; that is handled by the executor / orchestrator, not by Phase 13 plan work.)

    (b) `.planning/REQUIREMENTS.md` — one edit:
      - Line 16 WARMUP-01 bullet: replace the existing description after `**WARMUP-01**:` with the D-08 rewrite verbatim: "Under `prefers-reduced-motion: reduce`, the inner reference ring (`.orb-ring--inner`) is not rendered — `.orb-layer--out` opacity crossfade alone carries the substitute phase indicator (D-07). Pure CSS change in `src/styles/theme.css` `@media (prefers-reduced-motion: reduce)` block; no TSX, storage, audio, timing touch."
      - The `- [ ] **WARMUP-01**: ` prefix is preserved. Single-line bullet (no line breaks inside the description) per PATTERNS.md constraints.
      - Traceability table row at line 99 stays unchanged.

    (c) `.planning/STATE.md` — one edit:
      - Line 75 carry-forward sub-bullet: replace "Carry-forward from v1.0.1: `2026-05-11-reduced-motion-still-shows-out-phase-boundary-cue` — note: Phase 13 (WARMUP-01) adds In-phase inner ring; verify this does not worsen the out-phase boundary cue reduced-motion issue." with the D-08 rewrite verbatim from PATTERNS.md §STATE.md: "Carry-forward from v1.0.1: `2026-05-11-reduced-motion-still-shows-out-phase-boundary-cue` — RESOLVED directly by Phase 13 (WARMUP-01 reframed as reduced-motion `.orb-ring--inner { display: none }`; no separate verification step). Todo moves to `.planning/todos/completed/` on phase close per 13-CONTEXT.md D-09."
      - Single-line bullet, backticks around filename + code identifiers. The preceding bullet on line 74 (`Phase 13 planning: /gsd-plan-phase 13`) is NOT touched by Phase 13 plan work — it is removed by the orchestrator post-planning.

    Commit-1 boundary: DO NOT commit yet. Tasks 2 and 3 also land in Commit 1 (doc rewrite) per D-09. Commit-1 message draft: `docs(13): reframe WARMUP-01 as reduced-motion inner-ring suppression (D-01, D-08)`.
  </action>
  <verify>
    <automated>
grep -F "the inner reference ring (\`.orb-ring--inner\`) is no longer rendered" .planning/ROADMAP.md \
  &amp;&amp; grep -F "inner reference ring is not drawn in any state (Out, In, lead-in)" .planning/ROADMAP.md \
  &amp;&amp; grep -F "Out-phase inner-ring behavior under **normal motion** is pixel-identical" .planning/ROADMAP.md \
  &amp;&amp; ! grep -F "symmetric arrival cue at both In and Out phase boundaries" .planning/ROADMAP.md \
  &amp;&amp; grep -F "the inner reference ring (\`.orb-ring--inner\`) is not rendered" .planning/REQUIREMENTS.md \
  &amp;&amp; ! grep -F "symmetric inner-ring arrival cue at both In-phase and Out-phase boundaries" .planning/REQUIREMENTS.md \
  &amp;&amp; grep -F "RESOLVED directly by Phase 13" .planning/STATE.md \
  &amp;&amp; ! grep -F "verify this does not worsen the out-phase boundary cue reduced-motion issue" .planning/STATE.md
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep -F "the inner reference ring (\`.orb-ring--inner\`) is no longer rendered" .planning/ROADMAP.md` returns ≥ 1 match.
    - `grep -F "inner reference ring is not drawn in any state (Out, In, lead-in)" .planning/ROADMAP.md` returns ≥ 1 match.
    - `grep -F "Out-phase inner-ring behavior under **normal motion** is pixel-identical" .planning/ROADMAP.md` returns ≥ 1 match.
    - `grep -F "symmetric arrival cue at both In and Out phase boundaries" .planning/ROADMAP.md` returns 0 matches (old framing removed from line 47 tagline).
    - `grep -F "the inner reference ring (\`.orb-ring--inner\`) is not rendered" .planning/REQUIREMENTS.md` returns ≥ 1 match.
    - `grep -F "symmetric inner-ring arrival cue at both In-phase and Out-phase boundaries" .planning/REQUIREMENTS.md` returns 0 matches.
    - `grep -F "RESOLVED directly by Phase 13" .planning/STATE.md` returns 1 match.
    - `grep -F "verify this does not worsen the out-phase boundary cue reduced-motion issue" .planning/STATE.md` returns 0 matches.
    - Line 161 of `.planning/ROADMAP.md` still contains `| 13. Inner-Ring UX Symmetry | v1.1 | 0/? | Not started | - |` (traceability row unchanged at this commit).
    - Line 99 of `.planning/REQUIREMENTS.md` still contains `| WARMUP-01 | Phase 13 | Pending |` (traceability row unchanged at this commit).
  </acceptance_criteria>
  <done>
    ROADMAP §Phase 13 Goal + SC1 + SC2 + line-47 tagline reflect reduced-motion suppression framing.
    REQUIREMENTS WARMUP-01 bullet reflects reduced-motion suppression framing.
    STATE.md carry-forward sub-bullet reflects RESOLVED-by-Phase-13 framing.
    Rejected "symmetric arrival cue" prose has been removed from all three files.
    Traceability table rows in ROADMAP (line 161) and REQUIREMENTS (line 99) are byte-identical to pre-task state — only Goal/SC/bullet prose changed.
  </done>
</task>

<task type="auto">
  <name>Task 2: Append `[2026-05-12 update]` callouts to FEATURES.md (6 locations) per Phase 12 D-02 idiom</name>
  <files>.planning/research/FEATURES.md</files>
  <read_first>
    - `.planning/phases/13-inner-ring-ux-symmetry/13-CONTEXT.md` (D-08 sub-bullet for FEATURES.md)
    - `.planning/phases/13-inner-ring-ux-symmetry/13-PATTERNS.md` §`.planning/research/FEATURES.md` (per-line callout table)
    - `.planning/research/FEATURES.md` (target lines 41, 103-105, 114, 125, 152, 206-209 — read each target paragraph in context)
    - `REVIEW.md:391` (Phase 12 D-02 exemplar `[2026-05-12 update]` callout — read for idiom match)
  </read_first>
  <action>
    Append `[2026-05-12 update] …` callouts at the SIX FEATURES.md locations specified in 13-CONTEXT.md D-08 and detailed in 13-PATTERNS.md §FEATURES.md. NEVER overwrite existing prose — append at the END of the affected paragraph / cell / bullet so the historical scoping narrative is preserved (Phase 12 D-02 idiom: append-only annotation).

    Use 13-PATTERNS.md §`.planning/research/FEATURES.md` per-line table for the exact callout text per location. Each callout MUST:
      - Begin with the literal marker `[2026-05-12 update]` (square brackets, lowercase "update", single space after the closing bracket).
      - State the correction in one sentence.
      - Include a forward pointer to `.planning/phases/13-inner-ring-ux-symmetry/13-CONTEXT.md` with the specific D-NN tag (D-01 or D-03).
      - Be appended at end of paragraph (or table cell); single sentence or two short sentences per Phase 12 D-02 constraint.

    Specific application (6 callouts) per 13-PATTERNS.md §FEATURES.md per-line table:
      1. Line 41 (Insights-table row "Inner-ring symmetry (warm-up)" final cell): append the callout noting symmetric-cue framing rejected; actual scope is reduced-motion `.orb-ring--inner { display: none }`. Cite 13-CONTEXT.md D-01.
      2. Lines 103-105 (Dependency-diagram block referencing the add of `[data-phase='in'] .orb-ring--outer` fade-in rule): append the callout under that block stating the rule add was rejected; reduced-motion suppression is the implementation. Cite 13-CONTEXT.md D-03.
      3. Line 114 (claim "zero dependency risk: it is a pure CSS + JSX addition"): append the callout stating implementation is pure CSS only (no JSX addition); a single rule inside the existing `@media (prefers-reduced-motion: reduce)` block. Cite 13-CONTEXT.md D-03.
      4. Line 125 (checklist row `[x] Inner-ring UX symmetry — Add [data-phase='in'] CSS rule …`): append the callout stating feature shipped as reduced-motion `.orb-ring--inner` suppression; symmetric-arrival framing annotated, not implemented. Cite 13-CONTEXT.md D-01.
      5. Line 152 (prioritization-matrix row "Inner-ring UX symmetry"): append the callout stating priority/value/cost unchanged; scope reduced to one CSS line per 13-CONTEXT.md D-03.
      6. Lines 206-209 (§"Inner-Ring UX Symmetry (warm-up)" block, "Touches: theme.css + BreathingShape.tsx ... Risk: Negligible."): append the callout under that § stating "Touches" reduced to theme.css only (no BreathingShape.tsx edit); risk unchanged. Cite 13-CONTEXT.md D-03 and the canonical_refs "Source NOT edited" entry.

    DO NOT touch `.planning/research/PITFALLS.md` (lines 98, 322 are unchanged per D-08 — they apply to CUST-03 Phase 17, not WARMUP-01). DO NOT touch `.planning/research/STACK.md` or `.planning/research/SUMMARY.md`.

    Still no commit yet (this is part of Commit 1).
  </action>
  <verify>
    <automated>
# Each of the 6 line ranges must contain at least one new `[2026-05-12 update]` callout.
# Use sed to slice each range, then grep for the marker.

[ "$(grep -c '\[2026-05-12 update\]' .planning/research/FEATURES.md)" -ge 6 ] \
  &amp;&amp; sed -n '38,52p' .planning/research/FEATURES.md | grep -F "[2026-05-12 update]" \
  &amp;&amp; sed -n '100,110p' .planning/research/FEATURES.md | grep -F "[2026-05-12 update]" \
  &amp;&amp; sed -n '112,118p' .planning/research/FEATURES.md | grep -F "[2026-05-12 update]" \
  &amp;&amp; sed -n '123,130p' .planning/research/FEATURES.md | grep -F "[2026-05-12 update]" \
  &amp;&amp; sed -n '150,156p' .planning/research/FEATURES.md | grep -F "[2026-05-12 update]" \
  &amp;&amp; sed -n '205,215p' .planning/research/FEATURES.md | grep -F "[2026-05-12 update]" \
  &amp;&amp; grep -c "13-CONTEXT.md" .planning/research/FEATURES.md | awk '$1 &gt;= 6 { exit 0 } { exit 1 }'
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '\[2026-05-12 update\]' .planning/research/FEATURES.md` returns ≥ 6.
    - Each of the six target line windows (slightly widened to absorb pre-edit line drift): 38-52, 100-110, 112-118, 123-130, 150-156, 205-215 contains at least one `[2026-05-12 update]` callout when sliced with `sed -n '<lo>,<hi>p'`.
    - `grep -c "13-CONTEXT.md" .planning/research/FEATURES.md` returns ≥ 6 (every callout includes a forward pointer per Phase 12 D-02 constraint).
    - The original feature prose at line 41 still contains the historical text `Inner-ring symmetry (warm-up)` and `orb-ring--in-arrival` — preservation check (callouts are append-only): `grep -F "orb-ring--in-arrival" .planning/research/FEATURES.md` returns ≥ 1 match.
    - The original feature prose at line 125 still contains `[x] Inner-ring UX symmetry` — preservation check: `grep -F "[x] Inner-ring UX symmetry" .planning/research/FEATURES.md` returns ≥ 1 match.
    - No `[2026-05-12 update]` callout exceeds two sentences (per Phase 12 D-02 idiom — addenda, not rewrites).
  </acceptance_criteria>
  <done>
    All six FEATURES.md target locations carry append-only `[2026-05-12 update]` callouts.
    Historical scoping narrative is preserved verbatim (only appended, never overwritten).
    Every callout cites `13-CONTEXT.md` with a specific D-NN tag.
  </done>
</task>

<task type="auto">
  <name>Task 3: Append `[2026-05-12 update]` callouts to ARCHITECTURE.md (5 locations) per Phase 12 D-02 idiom + Commit 1</name>
  <files>.planning/research/ARCHITECTURE.md, .planning/ROADMAP.md, .planning/REQUIREMENTS.md, .planning/STATE.md, .planning/research/FEATURES.md</files>
  <read_first>
    - `.planning/phases/13-inner-ring-ux-symmetry/13-CONTEXT.md` (D-08 sub-bullet for ARCHITECTURE.md)
    - `.planning/phases/13-inner-ring-ux-symmetry/13-PATTERNS.md` §`.planning/research/ARCHITECTURE.md` (per-line callout table)
    - `.planning/research/ARCHITECTURE.md` (target lines 21, 45-48, 310-315, 383, 432 — read each target paragraph in context)
  </read_first>
  <action>
    Two phases in this task — both required for Commit 1 to land cleanly.

    Phase A — Append `[2026-05-12 update]` callouts at the FIVE ARCHITECTURE.md locations specified in 13-CONTEXT.md D-08 and detailed in 13-PATTERNS.md §ARCHITECTURE.md. Same constraints as Task 2 (append-only, single-sentence, marker `[2026-05-12 update]`, forward pointer to `13-CONTEXT.md` with D-NN tag). Specific application per 13-PATTERNS.md per-line table:
      1. Line 21 (BreathingShape directory-tree comment "BreathingShape (variant dispatch) ← CUST-03 + inner-ring fix"): append the callout noting the "inner-ring fix" is reduced-motion suppression in theme.css only; no BreathingShape.tsx edit. Cite 13-CONTEXT.md D-01.
      2. Lines 45-48 (Feature-by-Feature §"Warm-up: Inner-Ring UX Symmetry" — "Issue B carry-forward …" paragraph): append the callout noting Issue B was reduced-motion-only in actual user UAT (2026-05-12); normal-motion inner-ring behavior is correct as-is. Fix is one CSS rule in theme.css `@media (prefers-reduced-motion: reduce)`, not BreathingShape.tsx. Cite 13-CONTEXT.md D-01 / D-03.
      3. Lines 310-315 (§"Phase A — Inner-ring UX symmetry (warm-up)" with "Investigate exact inner-ring arrival timing" / "Fix inner-ring coincidence with orb edge at MIN_SCALE"): append the callout stating scope reduced — no timing investigation, one CSS rule (`display: none`) under reduced motion. Cite 13-CONTEXT.md D-03.
      4. Line 383 (table row "BreathingShape | Dispatch to body/lead-in | Dispatch by variant prop; inner-ring fix"): append the callout in-cell stating the inner-ring fix lives in theme.css, not BreathingShape.tsx. Cite 13-CONTEXT.md D-01.
      5. Line 432 (table row "src/components/BreathingShape.tsx | Inner-ring fix; variant dispatch | LOW"): append the callout in-cell stating BreathingShape.tsx receives no Phase 13 edit; theme.css carries the change. Cite 13-CONTEXT.md D-01.

    Phase B — Commit 1 boundary. After Phase A is verified, stage the FIVE doc-rewrite files (the ones produced by Tasks 1+2+3 Phase A) and commit:
      - Files staged: `.planning/ROADMAP.md`, `.planning/REQUIREMENTS.md`, `.planning/STATE.md`, `.planning/research/FEATURES.md`, `.planning/research/ARCHITECTURE.md`
      - Use `git add` with explicit file paths (NOT `git add -A` or `git add .`) per CLAUDE.md safety protocol
      - Commit message (HEREDOC): `docs(13): reframe WARMUP-01 as reduced-motion inner-ring suppression (D-01, D-08)` with a body referencing 13-CONTEXT.md D-08 and listing the affected milestone-artifact files.

    Then run the per-commit green-gate (D-10) BEFORE handing off to Task 4. The doc-only commit MUST NOT perturb tsc/lint/build/test.
  </action>
  <verify>
    <automated>
# Phase A — ARCHITECTURE.md callouts present at all 5 target windows
[ "$(grep -c '\[2026-05-12 update\]' .planning/research/ARCHITECTURE.md)" -ge 5 ] \
  &amp;&amp; sed -n '18,28p' .planning/research/ARCHITECTURE.md | grep -F "[2026-05-12 update]" \
  &amp;&amp; sed -n '42,55p' .planning/research/ARCHITECTURE.md | grep -F "[2026-05-12 update]" \
  &amp;&amp; sed -n '308,320p' .planning/research/ARCHITECTURE.md | grep -F "[2026-05-12 update]" \
  &amp;&amp; sed -n '380,390p' .planning/research/ARCHITECTURE.md | grep -F "[2026-05-12 update]" \
  &amp;&amp; sed -n '428,438p' .planning/research/ARCHITECTURE.md | grep -F "[2026-05-12 update]" \
  &amp;&amp; grep -c "13-CONTEXT.md" .planning/research/ARCHITECTURE.md | awk '$1 &gt;= 5 { exit 0 } { exit 1 }' \
  &amp;&amp; ( npx tsc --noEmit &amp;&amp; npm run lint &amp;&amp; npm run build &amp;&amp; npm test ) \
  &amp;&amp; [ -n "$(git log -1 --pretty=%H -- .planning/research/ARCHITECTURE.md)" ] \
  &amp;&amp; git log -1 --pretty=%s | grep -E "^docs\\(13\\)"
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '\[2026-05-12 update\]' .planning/research/ARCHITECTURE.md` returns ≥ 5.
    - Each of five target line windows (slightly widened): 18-28, 42-55, 308-320, 380-390, 428-438 contains at least one `[2026-05-12 update]` callout via `sed -n '<lo>,<hi>p'`.
    - `grep -c "13-CONTEXT.md" .planning/research/ARCHITECTURE.md` returns ≥ 5.
    - Historical "Issue B carry-forward" paragraph at ARCHITECTURE.md:45-48 is preserved (grep returns ≥ 1 match for `"Issue B carry-forward from Phase 5.1"` — callouts are append-only).
    - `.planning/research/PITFALLS.md` is UNCHANGED — `git diff HEAD~1 -- .planning/research/PITFALLS.md` returns empty output.
    - `npx tsc --noEmit` exits 0.
    - `npm run lint` exits 0.
    - `npm run build` exits 0.
    - `npm test` exits 0 with 409/409 Vitest baseline preserved (no new tests added per D-07).
    - `git log -1 --pretty=%s` matches `^docs(13)` (Commit 1 landed).
    - `git log -1 --name-only` shows exactly these five files staged: `.planning/ROADMAP.md`, `.planning/REQUIREMENTS.md`, `.planning/STATE.md`, `.planning/research/FEATURES.md`, `.planning/research/ARCHITECTURE.md` — NO source-tree file, NO test file, NO `theme.css`, NO todo file in this commit.
  </acceptance_criteria>
  <done>
    All five ARCHITECTURE.md target locations carry append-only `[2026-05-12 update]` callouts.
    Commit 1 (`docs(13): …`) is on HEAD; the doc-rewrite group is atomically landed.
    Green-gate (D-10) passes: tsc/lint/build/test all exit 0; 409/409 Vitest baseline preserved.
  </done>
</task>

<!-- ============================================================ -->
<!-- COMMIT 2: CSS suppression (D-03, D-05, D-06) — single file -->
<!-- ============================================================ -->

<task type="auto">
  <name>Task 4: Add `.orb-ring--inner { display: none }` to theme.css `@media` block + rewrite JSDoc comments + Commit 2</name>
  <files>src/styles/theme.css</files>
  <read_first>
    - `.planning/phases/13-inner-ring-ux-symmetry/13-CONTEXT.md` (D-03, D-04, D-05, D-06)
    - `.planning/phases/13-inner-ring-ux-symmetry/13-PATTERNS.md` §`src/styles/theme.css:154-158` / §`:99-107` / §`:142-153` / §"CSS block-comment idiom"
    - `src/styles/theme.css` (full file, current state — read with the offset/limit you already have in context; lines 85-158 are the relevant span)
  </read_first>
  <action>
    Three independent edits in `src/styles/theme.css`, plus the Commit 2 boundary at the end. Use the Edit tool (one Edit per logical change). NO code blocks inside this action — copy the EXACT prose templates from 13-PATTERNS.md.

    (a) JSDoc comment rewrite above `.orb-ring--inner` rule (D-05) — lines 99-107:
      - Preserve the `/* … */` block-comment form and the bullet-with-hanging-indent style (5-column continuation indent).
      - Rewrite the lead-line tag from `260510-tc9 Bug 1 (iter 3): inner reference ring is the Out-phase arrival cue.` to a Phase 13 form. Keep tagging style consistent with the file: parenthesised decision IDs. Suggested form per PATTERNS.md: `Phase 13 (D-03, D-05): inner reference ring is the Out-phase arrival cue under normal motion; suppressed under reduced motion.` Note: this is a tag-line rewrite + scope extension, NOT a deletion of the historical narrative.
      - Preserve the first two history bullets verbatim (the "Hidden by default" and "Fades in/out" bullets describing normal-motion behavior — D-01 invariant: normal motion is correct as-is).
      - REPLACE the third bullet (`- Reduced-motion: the transition is intentionally PRESERVED, mirroring the orb-layer--out reduced-motion contract (D-07) — the fade-in IS the phase indicator when the orb is locked at MID_SCALE.`) with a new bullet stating: (i) under `prefers-reduced-motion: reduce` the ring is suppressed via `display: none` in the `@media` block below; (ii) `.orb-layer--out` opacity crossfade alone carries the substitute phase indicator (cite D-07); (iii) historical note — ring was originally kept under reduced motion per Phase 7 UAT 2026-05-11; user UAT correction 2026-05-12 reversed this.
      - Constraints (PATTERNS.md §"CSS block-comment idiom"): `- ` bullet prefix; 5-column continuation indent; cross-reference selectors by literal name (`.orb-layer--out`); decision tags in `(D-XX)` parentheses; no `//` line comments.

    (b) JSDoc comment extension above `@media` block (D-06) — lines 142-153:
      - Insert ONE new bullet AFTER the `- F3:` bullet (chronological order — F3 < Phase 13). Suggested wording per PATTERNS.md: `- Phase 13 (D-03): .orb-ring--inner is suppressed (display: none) — the inner reference ring carries no information when the orb is locked at MID_SCALE; the .orb-layer--out opacity crossfade remains the sole substitute phase cue.`
      - PRESERVE the `CR-01: Do NOT set transform: none !important here. …` bullet verbatim — D-06 calls this out as load-bearing.
      - The header tag `Reduced-motion (D-05, D-07, D-09):` MAY be extended to `Reduced-motion (D-05, D-07, D-09, D-03):` — at planner discretion per D-06. Recommended: extend the tag so the decision-trail is locally complete in the comment.

    (c) CSS rule add inside `@media (prefers-reduced-motion: reduce)` block (D-03) — lines 154-158:
      - ADD `.orb-ring--inner { display: none; }` as a sibling rule alongside the existing `dialog.modal-fade { transition: none !important }` rule. Use 2-space indentation inside `@media`, single property per declaration, one blank line between the two rules (matches the PATTERNS.md §`theme.css:154-158` exemplar).
      - Selector: `.orb-ring--inner` (D-04 — NOT `[data-phase='out'] .orb-ring--inner`). The broader selector encodes the intent "ring is suppressed under reduced motion, full stop" and is robust to future render-site additions.
      - NO `!important` — the default `.orb-ring--inner` rule at line 108 only sets `opacity` / `border-*` / `transition`; the cascade already wins for the `@media` branch.
      - DO NOT add `transform: none !important` or any other property to the rule. DO NOT alter the existing `dialog.modal-fade { transition: none !important }` rule.

    (d) Commit 2 boundary. After (a)+(b)+(c) are verified, stage `src/styles/theme.css` and commit. Commit message (HEREDOC): `feat(13): suppress .orb-ring--inner under reduced motion (WARMUP-01, D-03, D-05, D-06)` with a body citing 13-CONTEXT.md decision tags and noting D-07 phase-indicator contract restored as sole indicator.

    Then run the per-commit green-gate (D-10). Since no test was changed (D-07), the existing 409/409 Vitest baseline MUST hold.
  </action>
  <verify>
    <automated>
# (a) display: none rule landed
grep -F ".orb-ring--inner" src/styles/theme.css | grep -F "display: none" \
  &amp;&amp; awk '/@media \(prefers-reduced-motion: reduce\)/,/^}/' src/styles/theme.css | grep -F ".orb-ring--inner" \
  &amp;&amp; awk '/@media \(prefers-reduced-motion: reduce\)/,/^}/' src/styles/theme.css | grep -F "display: none" \
  &amp;&amp; awk '/@media \(prefers-reduced-motion: reduce\)/,/^}/' src/styles/theme.css | grep -F "dialog.modal-fade" \
  &amp;&amp; awk '/@media \(prefers-reduced-motion: reduce\)/,/^}/' src/styles/theme.css | grep -F "transition: none !important" \
  &amp;&amp; ! awk '/@media \(prefers-reduced-motion: reduce\)/,/^}/' src/styles/theme.css | grep -F "transform: none !important" \
  &amp;&amp; ! grep -F "Reduced-motion: the transition is intentionally PRESERVED" src/styles/theme.css \
  &amp;&amp; grep -F "Do NOT set" src/styles/theme.css | grep -F "transform: none !important" \
  &amp;&amp; grep -F "Phase 13" src/styles/theme.css \
  &amp;&amp; ( npx tsc --noEmit &amp;&amp; npm run lint &amp;&amp; npm run build &amp;&amp; npm test ) \
  &amp;&amp; git log -1 --pretty=%s | grep -E "^feat\\(13\\)"
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep -F ".orb-ring--inner { display: none; }" src/styles/theme.css` returns ≥ 1 match. (Whitespace tolerance: the equivalent check `grep -E "\\.orb-ring--inner\\s*\\{\\s*display:\\s*none" src/styles/theme.css` also accepted.)
    - The new rule sits INSIDE the `@media (prefers-reduced-motion: reduce)` block: `awk '/@media \(prefers-reduced-motion: reduce\)/,/^}/' src/styles/theme.css | grep -F ".orb-ring--inner"` returns non-empty.
    - The existing `dialog.modal-fade { transition: none !important }` rule remains inside the same `@media` block: `awk '/@media \(prefers-reduced-motion: reduce\)/,/^}/' src/styles/theme.css | grep -F "transition: none !important"` returns non-empty.
    - CR-01 invariant: NO `transform: none !important` appears anywhere inside the `@media` block: `awk '/@media \(prefers-reduced-motion: reduce\)/,/^}/' src/styles/theme.css | grep -F "transform: none !important"` returns empty.
    - CR-01 caveat comment preserved: `grep -F "Do NOT set" src/styles/theme.css | grep -F "transform: none !important"` returns ≥ 1 match (the load-bearing JSDoc bullet is still in place).
    - Old D-05 comment text removed: `grep -F "Reduced-motion: the transition is intentionally PRESERVED" src/styles/theme.css` returns 0 matches.
    - New Phase 13 tag present in JSDoc: `grep -F "Phase 13" src/styles/theme.css` returns ≥ 1 match.
    - `npx tsc --noEmit` exits 0.
    - `npm run lint` exits 0.
    - `npm run build` exits 0.
    - `npm test` exits 0 with 409/409 Vitest baseline preserved (no test changes per D-07).
    - `git log -1 --pretty=%s` matches `^feat(13)` (Commit 2 landed).
    - `git log -1 --name-only` shows EXACTLY `src/styles/theme.css` and no other files in this commit.
    - `git log -1 --pretty=%H -- src/components/BreathingShape.tsx` returns no commit at this HEAD (ROADMAP §13 SC3 invariant: zero TSX edit).
  </acceptance_criteria>
  <done>
    The `.orb-ring--inner { display: none; }` rule is inside the `@media (prefers-reduced-motion: reduce)` block.
    The JSDoc comment above `.orb-ring--inner` cites D-03/D-05/D-07 and explains the historical Phase 7 UAT → 2026-05-12 user UAT correction.
    The JSDoc comment above the `@media` block has one new `Phase 13 (D-03)` bullet; CR-01 caveat preserved verbatim.
    Commit 2 (`feat(13): …`) is on HEAD; touches only `src/styles/theme.css`.
    Green-gate (D-10) passes: tsc/lint/build/test all exit 0; 409/409 Vitest baseline preserved; no TSX or test file edited.
  </done>
</task>

<!-- ============================================================ -->
<!-- COMMIT 3: Todo file move (D-02, D-09) — git-tracked rename -->
<!-- ============================================================ -->

<task type="auto">
  <name>Task 5: `git mv` folded todo to completed/ + Commit 3 + final green-gate</name>
  <files>
    .planning/todos/pending/2026-05-11-reduced-motion-still-shows-out-phase-boundary-cue.md,
    .planning/todos/completed/2026-05-11-reduced-motion-still-shows-out-phase-boundary-cue.md
  </files>
  <read_first>
    - `.planning/phases/13-inner-ring-ux-symmetry/13-CONTEXT.md` (D-02, D-09)
    - `.planning/phases/13-inner-ring-ux-symmetry/13-PATTERNS.md` §`.planning/todos/pending/2026-05-11-…md → completed/` (Phase 12 ASSETS-01 precedent)
    - `.planning/todos/pending/2026-05-11-reduced-motion-still-shows-out-phase-boundary-cue.md` (current file — verify `resolves_phase: 13` in frontmatter)
    - `.planning/todos/completed/` directory listing (confirm target directory exists)
  </read_first>
  <action>
    Move the folded carry-forward todo from `.planning/todos/pending/` to `.planning/todos/completed/` via `git mv` so rename detection is preserved in `git log --follow`. File content is NOT edited — the `resolves_phase: 13` frontmatter declared at capture time is the machine-readable marker; readers learn the resolution path from `13-CONTEXT.md` D-02 and this plan.

    Steps:
      1. Run `git mv .planning/todos/pending/2026-05-11-reduced-motion-still-shows-out-phase-boundary-cue.md .planning/todos/completed/2026-05-11-reduced-motion-still-shows-out-phase-boundary-cue.md` (single command).
      2. Stage check: `git status --porcelain` should show `R  .planning/todos/pending/2026-05-11-…md -> .planning/todos/completed/2026-05-11-…md` (a rename, not delete+add).
      3. Commit (HEREDOC): `docs(13): close folded reduced-motion todo (D-02, D-09)` with body referencing 13-CONTEXT.md D-02 and noting WARMUP-01 implementation landed in Commit 2.
      4. Run the FINAL per-commit green-gate (D-10): `npx tsc --noEmit && npm run lint && npm run build && npm test` — all four MUST exit 0; Vitest baseline 409/409 preserved.

    DO NOT edit the todo file content. DO NOT use `mv` + `git add` — that loses rename detection per the PATTERNS.md Phase 12 ASSETS-01 precedent constraint. DO NOT add any new files in this commit.
  </action>
  <verify>
    <automated>
[ -f .planning/todos/completed/2026-05-11-reduced-motion-still-shows-out-phase-boundary-cue.md ] \
  &amp;&amp; [ ! -f .planning/todos/pending/2026-05-11-reduced-motion-still-shows-out-phase-boundary-cue.md ] \
  &amp;&amp; grep -F "resolves_phase: 13" .planning/todos/completed/2026-05-11-reduced-motion-still-shows-out-phase-boundary-cue.md \
  &amp;&amp; git log -1 --pretty=%s | grep -E "^docs\\(13\\)" \
  &amp;&amp; git log -1 --name-status | grep -E "^R" | grep -F "2026-05-11-reduced-motion-still-shows-out-phase-boundary-cue.md" \
  &amp;&amp; ( npx tsc --noEmit &amp;&amp; npm run lint &amp;&amp; npm run build &amp;&amp; npm test )
    </automated>
  </verify>
  <acceptance_criteria>
    - `[ -f .planning/todos/completed/2026-05-11-reduced-motion-still-shows-out-phase-boundary-cue.md ]` exits 0 (file at completed path).
    - `[ ! -f .planning/todos/pending/2026-05-11-reduced-motion-still-shows-out-phase-boundary-cue.md ]` exits 0 (file gone from pending path).
    - `grep -F "resolves_phase: 13" .planning/todos/completed/2026-05-11-reduced-motion-still-shows-out-phase-boundary-cue.md` returns 1 match (frontmatter preserved; content unchanged).
    - `git log -1 --name-status` for HEAD shows an `R` (rename) entry, NOT separate `A` (add) and `D` (delete) entries — confirms `git mv` rename detection.
    - `git log -1 --pretty=%s` matches `^docs(13)` and refers to closing the folded todo.
    - `git log -1 --name-only` shows EXACTLY the two paths (rename source + dest); no other files in this commit.
    - `npx tsc --noEmit` exits 0.
    - `npm run lint` exits 0.
    - `npm run build` exits 0.
    - `npm test` exits 0 with 409/409 Vitest baseline preserved.
    - `git log -3 --pretty=%s` (HEAD~2..HEAD inclusive) matches the three-commit sequence: `docs(13): reframe WARMUP-01 …` → `feat(13): suppress .orb-ring--inner …` → `docs(13): close folded reduced-motion todo …`.
  </acceptance_criteria>
  <done>
    Folded todo lives in `.planning/todos/completed/` with content byte-identical to the prior pending file.
    `git log --follow` will track the rename across the move.
    Commit 3 is on HEAD; the three-commit Phase 13 sequence is complete.
    Final green-gate (D-10) passes: tsc/lint/build/test all exit 0; 409/409 Vitest baseline preserved across all three commits.
    No TSX file modified across Phase 13 (ROADMAP §13 SC3 invariant: `change touches only CSS and BreathingShape.tsx` is satisfied trivially — only CSS in practice).
    No test file modified across Phase 13 (D-07: optional gap-fill skipped — existing assertions pass unchanged since `display: none` keeps the element in DOM).
    Zero net-new npm dependencies (D-11).
  </done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

Phase 13 introduces **no new trust boundary**. The phase scope is:
- One CSS rule addition inside an existing `@media` block (presentation-only).
- Two CSS JSDoc comment edits (comments are stripped from production bundles by the standard Vite/PostCSS pipeline; no runtime surface).
- Five doc-only edits to `.planning/` markdown files (out-of-tree planning artifacts; not shipped in any build).
- One git-tracked file rename in `.planning/todos/` (out-of-tree planning artifacts; not shipped in any build).

There is no input crossing a boundary, no new endpoint, no new dependency, no new storage write, no new network call, no new render path, no new auth check.

| Boundary | Description |
|----------|-------------|
| (none introduced or modified) | Phase 13 ships zero new code paths and zero modifications to existing trust boundaries. |

## STRIDE Threat Register

ASVS L1 baseline assessment per security_enforcement gate. Each threat category is examined and dispositioned with a concrete justification for why it is N/A for this phase.

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-01 | Spoofing | (no auth/identity surface in phase) | accept | Phase 13 introduces no identity, session, or authentication change. The app has no server, no user accounts, and no auth flow; the CSS rule and doc edits do not interact with any identity surface. N/A. |
| T-13-02 | Tampering | `src/styles/theme.css` rule add | accept | The new rule is `display: none` gated by `@media (prefers-reduced-motion: reduce)`. CSS is delivered as a static asset; tampering requires arbitrary file system write, which is out of the app's threat surface. Even if maliciously rewritten, the failure mode is presentational (ring appears or disappears in reduced motion) — no data integrity or privilege impact. N/A for ASVS L1. |
| T-13-03 | Repudiation | (no logging/audit surface in phase) | accept | App has no audit log; the CSS suppression has no observable effect on persisted state. Git history (the three Phase 13 commits) provides full attribution of the change itself per D-09. N/A. |
| T-13-04 | Information Disclosure | `display: none` element | accept | `display: none` keeps the `.orb-ring--inner` `<span>` in the DOM (screen readers ignore `display: none` content by default — see code_context in 13-CONTEXT.md). No new data is exposed; the element renders `aria-hidden="true"` already (existing render path). The element carries no user data, no PII, no secrets — it is a decorative ring. N/A. |
| T-13-05 | Denial of Service | (no resource consumption surface in phase) | accept | The new CSS rule reduces work (fewer layout boxes under reduced motion). No new computation, no new network request, no new memory allocation. The doc edits and the file rename are zero-runtime-cost. N/A. |
| T-13-06 | Elevation of Privilege | (no privilege model in phase) | accept | App has no privilege model — single-user, single-device, local-storage-only. CSS and doc edits introduce no new code path that could be coerced to run with elevated privilege. N/A. |
| T-13-07 | Input Validation | (no input surface in phase) | accept | No new input is accepted from any source. The `@media` query is a browser-evaluated user-agent preference (not user input); `prefers-reduced-motion: reduce` evaluates to a boolean inside the CSS engine. No validation surface to add. N/A for ASVS L1. |
| T-13-08 | Secrets / Credentials | (no secret surface in phase) | accept | No secret is read, written, generated, hashed, or transmitted. No new env var, no new key. The repo's existing secret surface (none — purely client-side static site) is unchanged. N/A. |
| T-13-09 | Supply Chain | (no new dependency in phase) | accept | Per D-11 ("Zero net-new runtime deps" milestone invariant): no `npm install`, no new package, no new third-party CSS, no new external font/asset/CDN reference. The CSS rule uses only standard CSS properties (`display`, `@media`); the doc edits are pure Markdown. N/A. |
| T-13-10 | Cryptography | (no crypto surface in phase) | accept | No hashing, encryption, signing, or random-number generation introduced. N/A. |
| T-13-11 | Logging / Monitoring | (no telemetry surface in phase) | accept | App has no telemetry. The new CSS rule and doc edits introduce no logging hook. N/A. |
| T-13-12 | Configuration | (no config surface in phase) | accept | No new config flag, no new env variable, no new build setting. The `@media` rule lives in the same `theme.css` file shipped today; its parsing and application are governed by browser-side CSS engine semantics, not by application configuration. N/A. |

**ASVS L1 verdict:** No mitigations required. Block-on-`high` gate trivially satisfied — no threat in this register is rated `high` (all `accept` with concrete N/A justifications). The phase's effective threat surface is the empty set.
</threat_model>

<verification>

## Phase-level gate

After all five tasks complete, the following MUST hold (D-10):

- Three commits land in order on the active branch:
  1. `docs(13): reframe WARMUP-01 as reduced-motion inner-ring suppression (D-01, D-08)` — touches 5 files (ROADMAP / REQUIREMENTS / STATE / FEATURES / ARCHITECTURE).
  2. `feat(13): suppress .orb-ring--inner under reduced motion (WARMUP-01, D-03, D-05, D-06)` — touches 1 file (`src/styles/theme.css`).
  3. `docs(13): close folded reduced-motion todo (D-02, D-09)` — 1 rename in `.planning/todos/`.
- Per-commit green-gate (`npx tsc --noEmit && npm run lint && npm run build && npm test`) exited 0 at each of the three commit boundaries.
- Vitest baseline preserved: 409/409 tests passing (no test added — D-07 optional gap-fill skipped).
- `src/components/BreathingShape.tsx` shows zero edits across the three Phase 13 commits (`git log HEAD~2..HEAD -- src/components/BreathingShape.tsx` returns empty).
- `src/components/BreathingShape.test.tsx` and `src/app/App.session.test.tsx` show zero edits across the three Phase 13 commits.
- `package.json` and `package-lock.json` show zero edits (D-11).
- `.planning/research/PITFALLS.md` shows zero edits across the three Phase 13 commits (D-08: PITFALLS lines 98, 322 unchanged).

## Manual sanity check (optional, post-commits)

Open dev server with reduced motion enabled at the OS level (macOS: System Settings → Accessibility → Display → "Reduce motion" ON). Start a session. Confirm:
- Out-phase no longer shows an inner reference ring (`.orb-ring--inner` hidden via `display: none`).
- `.orb-layer--out` opacity crossfade still visibly transitions from In to Out phase (D-07 / D-12 invariant).
- Outer ring (`.orb-ring--outer`) remains visible across both phases (unchanged).
- 3-2-1 lead-in countdown: no inner ring visible (also gated by the broader `.orb-ring--inner` selector per D-04).
- Disable reduced motion → restart session → confirm Out-phase inner-ring fade-in is byte-identical to v1.0.1 (SC2 invariant).

</verification>

<success_criteria>

- WARMUP-01 (REQUIREMENTS.md line 16): under `prefers-reduced-motion: reduce`, `.orb-ring--inner` not rendered in layout in any state.
- ROADMAP §Phase 13 SC1: inner reference ring is not drawn in any state (Out, In, lead-in) under reduced motion — satisfied by `display: none` rule in the `@media` block.
- ROADMAP §Phase 13 SC2: Out-phase inner-ring behavior under **normal motion** is pixel-identical to v1.0.1 baseline — satisfied trivially (no normal-motion CSS rule changed; only the `@media (prefers-reduced-motion: reduce)` branch gained one rule).
- ROADMAP §Phase 13 SC3: change touches only CSS — satisfied (zero TSX edits, zero test edits, zero storage/audio/timing/hook touches).
- ROADMAP §Phase 13 SC4: `tsc && lint && build && test` exit 0 — satisfied at each of the three commit boundaries per D-10.
- All five milestone-artifact files (ROADMAP / REQUIREMENTS / STATE / FEATURES / ARCHITECTURE) reflect the corrected reduced-motion-suppression scope; FEATURES.md and ARCHITECTURE.md preserve historical scoping narrative via append-only `[2026-05-12 update]` callouts (Phase 12 D-02 idiom).
- Folded todo `.planning/todos/pending/2026-05-11-reduced-motion-still-shows-out-phase-boundary-cue.md` lives in `.planning/todos/completed/` with `git mv` rename detection preserved.

</success_criteria>

<output>
After completion, create `.planning/phases/13-inner-ring-ux-symmetry/13-01-SUMMARY.md`
containing:
  - The three commit SHAs + subjects (one line each).
  - Confirmation that `display: none` rule lives inside the `@media (prefers-reduced-motion: reduce)` block at `src/styles/theme.css`.
  - Confirmation that ROADMAP §13 traceability row (line 161) and REQUIREMENTS.md WARMUP-01 status (line 99) MAY be flipped to "Complete" by the orchestrator on phase close.
  - Confirmation that 409/409 Vitest baseline preserved across all three commits.
  - Cross-reference back to `13-CONTEXT.md` D-01 through D-13 (no decision overridden during execution).
</output>
