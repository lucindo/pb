---
phase: 23-license-readme
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - LICENSE
  - README.md
autonomous: true
requirements: [DOCS-01, DOCS-02]

must_haves:
  truths:
    - "A LICENSE file exists at repo root containing the full MIT License text"
    - "The LICENSE copyright line reads 'Copyright (c) 2026 Renato Lucindo'"
    - "The README states a configurable BPM range of 1 – 7 (not 3.5 – 7)"
    - "The README states the codebase has 839 tests (not 363+)"
    - "The README Features list names 5 palettes, EN/PT-BR switching, 3 visual variants, 4 audio timbres, and the BPM-stretch session pattern"
    - "The README License section states the project is MIT-licensed and points to the LICENSE file, with no 'if present' conditional phrasing"
    - "The README keeps its claim-safe, non-medical positioning and the Forrest-attribution boundary intact"
    - "The per-commit green-gate (tsc && lint && build && test) still passes"
  artifacts:
    - path: "LICENSE"
      provides: "MIT License text with Renato Lucindo 2026 copyright"
      contains: "MIT License"
    - path: "README.md"
      provides: "Accurate, claim-safe project README"
      contains: "839"
  key_links:
    - from: "README.md License section"
      to: "LICENSE"
      via: "text pointer to the LICENSE file"
      pattern: "MIT"
---

<objective>
Make the repository distribution-ready: add an MIT `LICENSE` file at the repo root and refresh `README.md` so every fact is accurate and the claim-safe positioning is intact.

Purpose: Distribution-readiness — the repo currently has no LICENSE file and the README cites stale facts (BPM range `3.5 – 7`, `363+` tests) and omits v1.1/v1.2 features.
Output: A net-new `LICENSE` file and a corrected, expanded `README.md`. No `src/` files touched.
</objective>

<execution_context>
@$HOME/.claude-personal-personal/get-shit-done/workflows/execute-plan.md
@$HOME/.claude-personal-personal/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-license-readme/23-CONTEXT.md
@README.md
@package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the MIT LICENSE file at repo root</name>
  <files>LICENSE</files>
  <read_first>
    - .planning/phases/23-license-readme/23-CONTEXT.md (D-01, D-02 — net-new file, exact copyright line)
    - .planning/ROADMAP.md §"Phase 23: LICENSE + README" (success criterion #1 — locks the copyright line)
  </read_first>
  <action>
    Create a net-new `LICENSE` file at the repository root (no LICENSE file currently exists — D-02). Use the standard, unmodified MIT License text. The first line is the title `MIT License`. The copyright line MUST read exactly `Copyright (c) 2026 Renato Lucindo` (locked verbatim by D-01 and ROADMAP success criterion #1). Use the canonical MIT permission and warranty paragraphs unchanged — do not alter, add, or remove clauses. The courtesy note about crediting Forrest Knutson stays in the README License section, NOT in this LICENSE file (D-07) — keep `LICENSE` pure MIT text only.
  </action>
  <verify>
    <automated>test -f LICENSE && grep -q '^MIT License' LICENSE && grep -q 'Copyright (c) 2026 Renato Lucindo' LICENSE && grep -q 'WITHOUT WARRANTY OF ANY KIND' LICENSE && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `LICENSE` exists at the repository root.
    - `LICENSE` first line is exactly `MIT License`.
    - `LICENSE` contains the line `Copyright (c) 2026 Renato Lucindo`.
    - `LICENSE` contains the standard MIT permission grant ("Permission is hereby granted, free of charge") and warranty disclaimer ("WITHOUT WARRANTY OF ANY KIND").
    - `LICENSE` does not contain the Forrest Knutson courtesy note (that belongs only in README).
  </acceptance_criteria>
  <done>`LICENSE` exists at repo root with full standard MIT text and the exact `Copyright (c) 2026 Renato Lucindo` line.</done>
</task>

<task type="auto">
  <name>Task 2: Refresh README.md for accuracy and completeness</name>
  <files>README.md</files>
  <read_first>
    - README.md (the file being edited — read current state in full before changing anything)
    - .planning/phases/23-license-readme/23-CONTEXT.md (D-03..D-08 — refresh depth, structure, License-section wording, courtesy note)
    - .planning/ROADMAP.md §"Phase 23: LICENSE + README" (success criteria #2, #3, #4)
    - package.json (confirm current dependency versions and scripts before describing them)
  </read_first>
  <action>
    Refresh `README.md` in place — keep the existing section structure (intro, About HRV, About Forrest, Features, Tech, Getting Started, Project Structure, Privacy, License) per D-05; correct and expand content, do not restructure.

    Accuracy fixes (D-03):
    - In the Features list, change `Configurable BPM (3.5 – 7)` to `Configurable BPM (1 – 7)` — the actual range is `1 – 7`.
    - In the Tech section, change `363+ tests across the codebase as of v1.0` to `839 tests as of v1.3` (verified test count is 839; reference v1.3, the current milestone).
    - Sweep every other factual claim while editing and correct any other stale value found (e.g. confirm the Project Structure tree still reflects the actual `src/` layout — it omits v1.1/v1.2 components such as `SettingsDialog`, theme/variant/timbre/language pickers; update the structure description so it is accurate without becoming an exhaustive file listing).

    Feature additions (D-04) — add Features bullets for capabilities that exist now but the README never mentioned. Cover all five: 5 named palettes (theme switching), EN/PT-BR language switching, 3 visual variants (Orb, Square, Diamond), 4 audio timbres, and the BPM-stretch session pattern (Warm-up → Stretch ramp → Settle). After this the Features list must be complete and accurate as of v1.3.

    License section rewrite (D-06, D-07): replace the stale `See LICENSE if present. Otherwise the source is provided as-is...` conditional phrasing. State plainly that the project is MIT-licensed and point to the `LICENSE` file. Keep the informal courtesy note below the MIT pointer — the request to credit Forrest Knutson as the practice's source and keep the "not medical advice" framing — and make clear it is a courtesy ask, not a license term.

    Claim-safe guardrails (ROADMAP #3, #4): the refreshed README must contain no medical, therapeutic, or diagnostic claims; preserve the existing "guided breathing practice, not medical advice" framing and the "independent and unaffiliated" Forrest positioning. Ensure the Forrest-attribution boundary reads explicitly — references to Forrest Knutson and Resonant Breathing are attribution/inspiration only; his name, content, and apps remain his.

    No new assets (D-08): the README stays text-only — no screenshot, no badges, no live-demo link, no CONTRIBUTING note. Match the README's existing calm, claim-safe voice for all new wording.
  </action>
  <verify>
    <automated>grep -q 'Configurable BPM (1 – 7)' README.md && grep -q '839' README.md && ! grep -q '363+' README.md && ! grep -q '3.5 – 7' README.md && ! grep -q 'if present' README.md && grep -qi 'MIT' README.md && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - README.md contains the string `Configurable BPM (1 – 7)`.
    - README.md contains `839` and does not contain `363+`.
    - README.md does not contain `3.5 – 7`.
    - README.md Features section names theme/palette switching, EN/PT-BR language switching, 3 visual variants (Orb/Square/Diamond), 4 audio timbres, and the BPM-stretch session pattern.
    - README.md License section states the project is MIT-licensed, points to the `LICENSE` file, and does not contain the phrase `if present`.
    - README.md License section retains the courtesy note crediting Forrest Knutson and the "not medical advice" framing.
    - README.md contains no medical, therapeutic, or diagnostic claims; the "guided breathing practice, not medical advice" and "independent and unaffiliated" wording is intact.
    - README.md keeps all nine original sections (intro, About HRV, About Forrest, Features, Tech, Getting Started, Project Structure, Privacy, License).
    - README.md adds no screenshots, badges, demo links, or CONTRIBUTING section.
  </acceptance_criteria>
  <done>README.md is accurate as of v1.3 — corrected BPM range and test count, complete Features list, rewritten License section pointing to `LICENSE`, claim-safe positioning and Forrest-attribution boundary intact, no new assets.</done>
</task>

<task type="auto">
  <name>Task 3: Verify the green-gate still passes</name>
  <files></files>
  <read_first>
    - package.json (the `build`, `lint`, `test` script definitions)
  </read_first>
  <action>
    Run the per-commit green-gate to confirm this docs-only phase introduced no regression. Run `npx tsc -b`, `npm run lint`, `npm run build`, and `npm run test:run` in sequence. Since this phase touches only `LICENSE` and `README.md` — zero `src/` files — every gate must pass trivially. If any gate fails, the failure is unrelated to this phase's edits; investigate before declaring the phase done. Do not modify any `src/` file to make a gate pass — that would be out of phase scope.
  </action>
  <verify>
    <automated>npx tsc -b && npm run lint && npm run build && npm run test:run</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc -b` exits 0.
    - `npm run lint` exits 0.
    - `npm run build` exits 0.
    - `npm run test:run` exits 0 and reports 839 passing tests.
  </acceptance_criteria>
  <done>The full green-gate (`tsc && lint && build && test`) exits 0; the 839-test suite passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

This phase touches only repo-root documentation files (`LICENSE`, `README.md`). There is no runtime code, no user input, no data flow, no network call, and no new dependency. There are no trust boundaries crossed by this phase's changes.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-01 | Information Disclosure | README.md content | accept | README is public-facing documentation by design; it contains no secrets, credentials, or PII. No mitigation needed — disclosure is the intended purpose of a README. |
| T-23-02 | Tampering | LICENSE / README.md | accept | Standard repository file-integrity assumptions apply (git history, branch protection). This phase introduces no new tampering surface beyond any existing repo file. |

No `high` threats. This phase touches no runtime code, no inputs, and no data flows — the honest threat surface is minimal and fully covered by the two accepted dispositions above.
</threat_model>

<verification>
- `LICENSE` exists at repo root with standard MIT text and the `Copyright (c) 2026 Renato Lucindo` line (DOCS-01).
- `README.md` is accurate as of v1.3: BPM range `1 – 7`, `839` tests, complete Features list, MIT-pointer License section (DOCS-02).
- The green-gate (`tsc && lint && build && test`) exits 0 (ROADMAP success criterion #5).
- README claim-safe positioning and Forrest-attribution boundary intact (ROADMAP success criteria #3, #4).
</verification>

<success_criteria>
- DOCS-01: repo root contains a `LICENSE` file with full MIT License text and the exact copyright line.
- DOCS-02: README accurately describes the app, its dev/build setup, and its claim-safe positioning with no medical claims.
- All five ROADMAP Phase 23 success criteria are TRUE.
</success_criteria>

<output>
After completion, create `.planning/phases/23-license-readme/23-01-SUMMARY.md`.
</output>
