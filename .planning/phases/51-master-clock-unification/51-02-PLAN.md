---
phase: 51-master-clock-unification
plan: 02
type: execute
wave: 2
depends_on:
  - 51-01
files_modified:
  - src/hooks/useAudioCues.ts
  - src/hooks/useAudioCues.test.tsx
  - src/hooks/useSessionEngine.ts
  - src/hooks/useSessionEngine.test.tsx
  - src/hooks/useBreathingSessionController.ts
  - src/hooks/useBreathingSessionController.test.tsx
autonomous: true
requirements:
  - CLOCK-01
  - CLOCK-02
  - CLOCK-03
  - DEPS-01
  - QUAL-01
requirements_addressed:
  - CLOCK-01
  - CLOCK-02
  - CLOCK-03
  - DEPS-01
  - QUAL-01
tags:
  - sessionclock
  - hooks
  - audiocues
  - breathing
  - clock-unification
  - hrv
must_haves:
  truths:
    - "`useAudioCues` returns a stable `clock: SessionClock` member on its `UseAudioCues` controller interface (D-03). The reference is built from `createSwappableSessionClock(createWallSessionClock())` inside the hook body via `useMemo(() => createSwappableSessionClock(createWallSessionClock()), [])` so the proxy identity is stable across renders, ALWAYS exists (even before AC construction), and exists for the lifetime of the hook instance."
    - "Inside `useAudioCues.start()`, immediately after `engineRef.current = engine` and before the existing `engine.clock.on*` subscription block at L296-298, the hook calls `proxyClock.setSource(engine.clock)` so subsequent `clock.now()` reads delegate to the AC clock (D-05 swap moment). The lead-in setTimeout chain BEFORE this point runs on the wall-clock initial source — that is intentional and acceptable per CONTEXT D-05."
    - "Inside `useAudioCues.reconstructEngine()`, immediately after `engineRef.current = newEngine` (currently at L448) and BEFORE the new `engine.clock.on*` re-subscription block at L453-456, the hook calls `proxyClock.setSource(newEngine.clock)` (D-11 step 2). The proxy provides ONLY `now()` delegation to `useSessionEngine` — it is the source of truth for SessionEngine's `clock.now()` reads ONLY. The existing `handleSuspend / handleResume / handleClose` external subscribers (registered at L296-298 in start() and L453-456 in reconstructEngine()) continue to subscribe via `engine.clock.on*` DIRECTLY (UNCHANGED from Phase 50). The existing `clockUnsubsRef` teardown-before-resubscribe pattern at L408-411 + L453-456 stays exactly as-is. The proxy's D-04 subscription-forwarding semantics are locked by Plan 51-01 unit tests but are NOT exercised in production at Phase 51 — no production caller subscribes via the proxy in this phase."
    - "After `proxyClock.setSource(newEngine.clock)` in reconstructEngine, the hook invokes a NEW `onSessionClockReanchored?: (newClockNow: number) => void` callback (passed as a 3rd optional arg to `useAudioCues`, mirroring the existing `onReanchorRequired` at L91) with `newEngine.clock.now()` as the argument (D-10). This fires BEFORE the existing `onReanchorRequiredRef.current?.(reanchorAudioTime)` invocation at L479 (D-11 step 3 ordering). The callback identity is captured into an `onSessionClockReanchoredRef` mirror (same pattern as L149-152 for `onReanchorRequired`)."
    - "`useSessionEngine` accepts an optional `onSessionClockReanchored?: (newClockNow: number) => void` callback exposed on its returned `SessionEngine` interface as a method like `reanchorSessionClock(newClockNow: number): void` (or via a controller-supplied callback prop — Claude's discretion at task time). When invoked while `state.status === 'running'`, the engine recomputes `startedAtSec = newClockNow - currentState.lastFrame.elapsedSec` (D-10 — preserves the pre-reanchor elapsed across the AC swap). Implementation uses a `setState` updater that narrows on `status === 'running'` and returns a new `RunningSessionState` with the rewritten `startedAtSec` (other fields untouched). When `status !== 'running'`, the method is a no-op (no state transition; reanchor on idle is meaningless)."
    - "`useBreathingSessionController.ts:83` no longer constructs `createWallSessionClock()`. The wall-clock instance + the `useMemo` wrapper are DELETED. The hook now reads `audio.clock` from `useAudioCues(...)` and passes it directly into `useSessionEngine(initialSettings, activeStretchSettings, audio.clock)`. CRITICAL ordering: `audio = useAudioCues(...)` must be called BEFORE `session = useSessionEngine(..., audio.clock)`. Today the order is L84 (session) → L112 (audio) — that order MUST FLIP. Re-order so `useAudioCues` is invoked before `useSessionEngine`, and `useSessionEngine` receives `audio.clock` instead of the deleted `sessionClock`."
    - "`useBreathingSessionController.ts` wires a new `onSessionClockReanchored` callback into `useAudioCues` (the new 3rd arg). The callback signature is `(newClockNow: number) => void` and its body invokes the engine's new reanchor method: `session.reanchorSessionClock(newClockNow)`. The existing `onAudioReanchorRequired` callback at L105-110 is preserved verbatim — both callbacks fire on reconstruction (per D-11 the session-clock reanchor fires FIRST, then the audio-anchor reanchor). The callback identity is wrapped in `useCallback` so the dep array of `useAudioCues` stays stable."
    - "`useAudioCues.audioNow()` at L530-534 continues returning `engineRef.current?.clock.now() ?? null` (UNCHANGED — `audioNow()` is the HRV boundary-scheduler dual-anchor primitive and reads from `engineRef.current` directly, not via the proxy, because it must return `null` when the AC is absent / closed)."
    - "`engine.clock` subscribers (`handleSuspend`/`handleResume`/`handleClose` at L173-206) continue subscribing through `engine.clock.on*` directly (UNCHANGED — these are AC-statechange consumers, NOT `now()` consumers; the proxy is unrelated to this subscription path). `clockUnsubsRef` teardown-before-resubscribe at L408-411 + L453-456 stays exactly as-is."
    - "**Unmount asymmetry vs Plan 51-03 (explicit, intentional):** The unmount cleanup at L211-231 of `useAudioCues.ts` does NOT need a `setProxyClockSource(createWallSessionClock())` revert because the hook + its consumers (`useSessionEngine` via `audio.clock` + `useBreathingSessionController`) all unmount together — no post-unmount `clock.now()` read is possible on the proxy from outside the hook tree. This is asymmetric with NK Plan 51-03 which DOES add a synchronous proxy revert in `useNaviKriyaAudio.close()` + `closeAfterEndCue()` + unmount, because NK's proxy is kept alive across `begin()` / `close()` cycles AND post-end stats reads can fire from `useNKEngine.end()`'s `onComplete` chain after `close()` was called but before unmount. The HRV `stop()` path DOES revert the proxy to wall-clock (see Task 2 action — defensive revert mirroring NK D-06), but the unmount path does not. Do NOT add a redundant unmount-time proxy revert."
    - "**Stretch ramp pause+resume (D-09):** `stretchRamp.ts` math is UNCHANGED by this plan — the stretch ramp reads `elapsedSec` from `SessionState.lastFrame.elapsedSec`, which is computed inside `useSessionEngine`'s rAF tick from `clock.now() - startedAtSec`. With the proxy now backed by the AC clock during running sessions, AC suspension freezes `elapsedSec` and resume continues the ramp position exactly where it left off — no special ramp-pause handling needed at the domain layer (D-09)."
    - "**Ambient scale (CLOCK-02):** CLOCK-02 strict reading is explicitly deferred per CONTEXT `<deferred>` section (CLOCK-02-strict-reading). No `useAmbientScale.ts` modification at Phase 51. The drift-guard at `src/audio/sessionClock.driftGuard.test.ts` confirms `useAmbientScale.ts` has no banned tokens; the file continues to consume `createWallSessionClock()` per Phase 50 D-10 resolution. Satisfied by construction per CONTEXT auto-resolved interpretation — `useAmbientScale` is idle-only (used inside `OrbIdle` in `OrbShape.tsx:273`), there is no in-session ambient surface, and idle has no AC. No `performance.now()` cascade reaches an in-session animation path. If a future phase introduces an in-session ambient surface, that phase will feed it the session's audio-backed proxy clock — explicitly NOT in Phase 51 scope per CONTEXT `<deferred>` CLOCK-02-strict-reading."
    - "**Animation phase progress (CLOCK-03):** satisfied by construction — `useSessionEngine.ts:159` already invokes `completeIfNeeded(currentState, clock.now())` inside the rAF tick (Phase 50 — see useSessionEngine.ts L142-160). After this plan, `clock.now()` is the audio-backed clock during a running session (proxy points at `engine.clock` post-`setSource(engine.clock)` in `start()`). No further animation-path change required."
    - "DEPS-01 holds: `package.json` `dependencies` unchanged."
    - "QUAL-01 holds: `pnpm typecheck && pnpm lint && pnpm test:run` exits 0 at every commit boundary. Test count grows by the number of new behavioral tests added in `useAudioCues.test.tsx` (see Task 2 — 5 new tests) + `useSessionEngine.test.tsx` (see Task 1 — 3 new tests) + `useBreathingSessionController.test.tsx` (see Task 3 — ≥1 smoke test) for a total of ≥9 new tests in this plan. Existing 1359 baseline (post Plan 51-01) holds + new tests."
  artifacts:
    - path: "src/hooks/useAudioCues.ts"
      provides: "Adds proxy clock construction (createSwappableSessionClock around an initial createWallSessionClock); adds `clock: SessionClock` to `UseAudioCues` interface; adds 3rd optional ctor arg `onSessionClockReanchored?: (newClockNow: number) => void` with mirror ref; calls `proxyClock.setSource(engine.clock)` in start() post-engineRef-assignment and in reconstructEngine post-newEngine-assignment; fires `onSessionClockReanchored(newEngine.clock.now())` BEFORE the existing `onReanchorRequired` callback in reconstructEngine."
      contains: "createSwappableSessionClock"
    - path: "src/hooks/useSessionEngine.ts"
      provides: "Adds `reanchorSessionClock(newClockNow: number): void` method to `SessionEngine` interface; implementation uses a setState updater that narrows on 'running' and returns a new RunningSessionState with rewritten startedAtSec; no-op when not running."
      contains: "reanchorSessionClock"
    - path: "src/hooks/useBreathingSessionController.ts"
      provides: "Deletes the `createWallSessionClock()` instance at L83 + the `sessionClock` useMemo wrapper; flips the `audio = useAudioCues(...)` and `session = useSessionEngine(...)` ordering so audio precedes session; passes `audio.clock` into `useSessionEngine`; wires a new `onSessionClockReanchored` useCallback into `useAudioCues` that calls `session.reanchorSessionClock(newClockNow)`."
    - path: "src/hooks/useAudioCues.test.tsx"
      provides: "Adds tests proving (a) `clock` is exposed on UseAudioCues; (b) `clock.now()` returns wall-clock-shape before start; (c) after start, `clock.now()` returns the AC's currentTime (via FakeAudioContext); (d) after reconstructEngine, the new `onSessionClockReanchored` callback fires BEFORE the existing `onReanchorRequired` (assert by inspecting callback order via `vi.fn()` mock.invocationCallOrder)."
    - path: "src/hooks/useSessionEngine.test.tsx"
      provides: "Adds tests proving `reanchorSessionClock(newClockNow)` rewrites `startedAtSec = newClockNow - lastFrame.elapsedSec` when running, preserves elapsedSec exactly (next rAF tick computes the same elapsedSec the snapshot held pre-reanchor), and is a no-op when not running."
    - path: "src/hooks/useBreathingSessionController.test.tsx"
      provides: "Smoke test confirming the controller still wires session + audio together correctly after the L83 wall-clock deletion + hook-order flip — existing tests in this file should pass with minimal change (mostly the test-double `audio.clock` member must be supplied by the fake)."
  key_links:
    - from: "src/hooks/useAudioCues.ts"
      to: "src/audio/swappableSessionClock.ts"
      via: "createSwappableSessionClock import"
      pattern: "createSwappableSessionClock"
    - from: "src/hooks/useAudioCues.ts"
      to: "src/audio/sessionClock.ts"
      via: "createWallSessionClock import (used as the initial proxy source)"
      pattern: "createWallSessionClock"
    - from: "src/hooks/useBreathingSessionController.ts"
      to: "src/hooks/useAudioCues.ts"
      via: "audio.clock prop read"
      pattern: "audio\\.clock"
    - from: "src/hooks/useBreathingSessionController.ts"
      to: "src/hooks/useSessionEngine.ts"
      via: "useSessionEngine(..., audio.clock) — clock arg now sourced from useAudioCues, not from a controller-local createWallSessionClock"
      pattern: "useSessionEngine\\("
    - from: "src/hooks/useBreathingSessionController.ts"
      to: "src/hooks/useSessionEngine.ts"
      via: "session.reanchorSessionClock invocation from inside the onSessionClockReanchored callback"
      pattern: "reanchorSessionClock"
---

<objective>
Rebase HRV session elapsed time + animation phase progress onto `audioCtx.currentTime` via the Phase 50 `SessionClock` seam (CLOCK-01 + CLOCK-03). This is the caller-level work the Phase 51 milestone goal calls for: `useSessionEngine` already invokes `clock.now()` inside its rAF tick (Phase 50 — `completeIfNeeded(currentState, clock.now())` at L159), and `startedAtSec` is already captured from `clock.now()` at session start (Phase 50 — `startSession(..., clock.now())` at L253). The ONLY thing missing is the clock SOURCE: today `useBreathingSessionController.ts:83` constructs a `createWallSessionClock()` and passes it into `useSessionEngine`. After this plan, the source is an audio-backed `SessionClock` (created by `createAudioSessionClock(audioCtx)` inside `createAudioEngine`, exposed via `engine.clock`), threaded through the stable proxy from Plan 51-01.

The reanchor path (D-10 / D-11) preserves session elapsed across the Phase 5.1 iOS engine reconstruction: when `useAudioCues.reconstructEngine` builds a new engine with a new AC (whose `currentTime` starts at ~0), the proxy's source swaps to the new clock and a new `onSessionClockReanchored` callback fires before the existing `onAudioReanchorRequired` callback — `useSessionEngine` recomputes `startedAtSec = newClockNow - elapsedSecAtReanchor` so `elapsed = clock.now() - startedAtSec` keeps reading the same elapsed value across the swap.

Purpose: Closes diagnosis #1 (lock/unlock sync) and #2 (foreground drift) for HRV breathing AND Stretch — both practices route through `useBreathingSessionController` → `useSessionEngine`. Practice-time semantics (D-07: AC-suspend freezes elapsed) is the user-visible behavior change; behavioral tests live in Plan 51-04.

Output:
- HRV / Stretch audio + animation in phase across iOS lock/unlock (CLOCK-04 architectural prereq; UAT in Plan 51-05).
- Foreground drift architecturally eliminated by single-source unification (CLOCK-05 closes by construction).
- Reanchor path preserves session elapsed across Phase 5.1 reconstruction (D-10 / D-11 invariants).
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<execution_notes>
## Mid-Plan Soft Checkpoint (recommended, not a hard gate)

This plan touches 6 source files (3 production + 3 test) and Task 2 in particular is high-context (proxy useMemo + signature change + start swap + reconstructEngine swap + reanchor invocation + stop revert + interface change + JSDoc + test-double sweep + 5 new tests). To keep the plan reviewable and to catch any regression early, the executor SHOULD verify Task 2 lands cleanly before starting Task 3.

After Task 2 (the `useAudioCues` proxy wiring + tests):
1. Run `pnpm typecheck` — must exit 0.
2. Run `pnpm lint src/hooks/useAudioCues.ts src/hooks/useAudioCues.test.tsx` — must exit 0 (no new errors beyond baseline).
3. Run `pnpm test:run src/hooks/useAudioCues.test.tsx` — all tests pass including the 5 new D-03/D-05/D-10/D-11 tests.
4. Run `pnpm test:run src/audio/sessionClock.driftGuard.test.ts` — drift-guard still passes.

If any of the above fail, fix the Task 2 regression FIRST before flipping the `useBreathingSessionController` hook-call order in Task 3 — Task 3's typecheck depends on Task 2's `clock: SessionClock` member on `UseAudioCues`, and a broken Task 2 will surface as a confusing typecheck failure in the controller file (which is not where the bug lives).

This is a SOFT signal — not a `checkpoint:human-verify` task and not auto-paused. The plan remains `autonomous: true`. The plan-checker noted that the combined task scope is at the warning boundary; this note exists so the executor knows to take a verification beat between Task 2 and Task 3 rather than blast through all 3 tasks before running the full test suite.
</execution_notes>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/51-master-clock-unification/51-CONTEXT.md
@.planning/phases/50-sessionclock-scheduler-abstraction/50-CONTEXT.md
@.planning/phases/51-master-clock-unification/51-01-PLAN.md
@src/audio/sessionClock.ts
@src/audio/swappableSessionClock.ts
@src/audio/audioEngine.ts
@src/hooks/useAudioCues.ts
@src/hooks/useSessionEngine.ts
@src/hooks/useBreathingSessionController.ts
@src/hooks/useAudioCues.test.tsx
@src/hooks/useSessionEngine.test.tsx
@src/hooks/useBreathingSessionController.test.tsx
@src/domain/sessionController.ts

<interfaces>
<!-- Extracted from the canonical files referenced above. -->

From src/audio/sessionClock.ts:
- `SessionClock` interface — 6 members: `now`, `schedule`, `setMasterGain`, `onSuspend`, `onResume`, `onClose`.
- `createWallSessionClock(): SessionClock`.

From src/audio/swappableSessionClock.ts (Plan 51-01 output):
- `createSwappableSessionClock(initialSource: SessionClock): { clock: SessionClock; setSource(next: SessionClock): void }`.

From src/audio/audioEngine.ts:
- `AudioEngine.clock: SessionClock` (readonly) — exposed at L84; constructed via `createAudioSessionClock(audioCtx, schedule)` at L403.
- `AudioEngineOptions` has fields `timbre: TimbreId` and optional `bypassSilentMode?: boolean` — NO `onStateChange` (removed at Plan 50-04).

From src/hooks/useAudioCues.ts (current shape; this plan modifies it):
- `useAudioCues(initialMuted?: boolean, onReanchorRequired?: (newAudioAnchor: number) => void): UseAudioCues` — signature at L89-92. Add a 3rd optional arg `onSessionClockReanchored?: (newClockNow: number) => void`.
- Existing `engineRef`, `clockUnsubsRef`, `handleSuspend/Resume/Close`, `start()`, `reconstructEngine()`, `stop()`, `audioNow()` — preserve all behavior; add proxy swap calls at the documented sites.

From src/hooks/useSessionEngine.ts:
- `SessionEngine` interface at L42-78 — add `reanchorSessionClock(newClockNow: number): void`.
- `clock: SessionClock` arg at L90.
- rAF tick at L132-186 (the body that calls `completeIfNeeded(currentState, clock.now())`).
- `startSession` / `startStretchSession` / `extendTimedSession` from `../domain/sessionController` already pass through `clock.now()`.

From src/domain/sessionController.ts:
- `RunningSessionState.startedAtSec: number` — the field to rewrite on reanchor.
- `RunningSessionState.lastFrame.elapsedSec: number` — the pre-reanchor elapsed value used to compute the new `startedAtSec`.

From src/hooks/useBreathingSessionController.ts (current shape; this plan modifies it):
- L75-83: `initialMute` then `activeStretchSettings` then `sessionClock = useMemo(() => createWallSessionClock(), [])` — the `sessionClock` line + its `useMemo` wrapper are DELETED.
- L84: `session = useSessionEngine(initialSettings, activeStretchSettings, sessionClock)` — receives `audio.clock` instead.
- L112: `audio = useAudioCues(initialMute, onAudioReanchorRequired)` — receives 3rd arg `onSessionClockReanchored`.
- ORDER FLIP: today session is at L84, audio at L112. After this plan, audio MUST precede session.
- L105-110: `onAudioReanchorRequired` callback — preserved verbatim.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add reanchorSessionClock to useSessionEngine + tests</name>
  <files>src/hooks/useSessionEngine.ts, src/hooks/useSessionEngine.test.tsx</files>
  <read_first>
    - `src/hooks/useSessionEngine.ts` (the full file — current `SessionEngine` interface at L42-78, current `useSessionEngine` body especially the rAF tick at L132-186, `start` callback at L237-255, `extendDuration` callback at L267-288).
    - `src/domain/sessionController.ts` (full file — `RunningSessionState` shape at L23+, `startedAtSec` field, `extendTimedSession` signature for the same shape of state-rewrite pattern at L109-160).
    - `src/hooks/useSessionEngine.test.tsx` (existing tests — match the existing describe/it/expect shape and FakeAudioContext helpers; identify the cleanest place to add new tests).
    - `.planning/phases/51-master-clock-unification/51-CONTEXT.md` D-10 (callback signature + reanchor math) and D-11 (firing order — session-clock reanchor BEFORE audio-anchor reanchor; this task implements only the session side; ordering is enforced by the caller in Task 2).
  </read_first>
  <behavior>
    - Test 1 — `reanchorSessionClock` rewrites `startedAtSec` while running: start a session via `session.start()` at `clock.now() = 100`; advance the clock to `clock.now() = 130` and wait one rAF tick so `lastFrame.elapsedSec ≈ 30`; call `session.reanchorSessionClock(0.5)` (simulating the new AC's `currentTime ≈ 0.5` post-reconstruction); assert `state.startedAtSec === 0.5 - 30 = -29.5` (the exact recomputation per D-10) AND `state.lastFrame.elapsedSec` after the next rAF tick is still ≈ 30 (elapsed preserved).
    - Test 2 — `reanchorSessionClock` is a no-op when not running: with `state.status === 'idle'`, call `session.reanchorSessionClock(0.5)`; assert `state` reference is unchanged (`===` identical) and no setState fires.
    - Test 3 — `reanchorSessionClock` ordering vs rAF tick: with a running session, call `reanchorSessionClock(newClockNow)` between two manual rAF advances; assert the post-reanchor rAF tick computes `lastFrame.elapsedSec` from the NEW `startedAtSec` (= newClockNow - preReanchorElapsedSec) consistently — no jump, no discontinuity.
  </behavior>
  <action>
    Add a new method `reanchorSessionClock(newClockNow: number): void` to the `SessionEngine` interface in `src/hooks/useSessionEngine.ts` (next to `extendDuration` at L77 — same useCallback-with-setState-updater pattern).

    Implementation: a `useCallback` named `reanchorSessionClock` with deps `[]` (the setState updater reads `currentState`, no closure over `state`):

    Body uses a setState updater that:
    - If `currentState.status !== 'running'`, return `currentState` unchanged (no-op).
    - If `currentState.status === 'running'`, compute `newStartedAtSec = newClockNow - currentState.lastFrame.elapsedSec` and return a new `RunningSessionState` object literal with `startedAtSec: newStartedAtSec`. Spread the rest: `...currentState, startedAtSec: newStartedAtSec`. The `lastFrame` field is NOT rewritten in this updater — the next rAF tick will recompute `lastFrame.elapsedSec = clock.now() - startedAtSec` and the elapsed value is preserved by construction (D-10 invariant).

    Add per-method JSDoc citing D-10 + D-11 + Phase 50 D-02 (seconds-shaped startedAtSec).

    Expose `reanchorSessionClock` on the returned `SessionEngine` object literal at L290-299.

    Verify: the rAF tick at L132-186 does NOT need modification — it already reads `currentState.startedAtSec` via `completeIfNeeded(currentState, clock.now())` and the domain helper at `src/domain/sessionController.ts` already computes `elapsedSec = clock.now() - state.startedAtSec`. The reanchor rewrite is observed automatically on the next tick.

    Add the 3 new tests to `src/hooks/useSessionEngine.test.tsx` — one `describe('reanchorSessionClock')` block at the end of the existing test suite. Each test maps 1:1 to the `<behavior>` block above. Use the existing test patterns from this file for advancing a mock clock + driving rAF (read the file first to copy the established posture for fake-time + rAF advancement).

    DO NOT modify `useBreathingSessionController` in this task — Task 3 handles the caller wiring. DO NOT modify `useAudioCues` in this task — Task 2 handles the audio-side wiring. The interface signature change (`SessionEngine` gaining `reanchorSessionClock`) DOES require updating any test-double `SessionEngine` literals in test files; sweep the test files and add a no-op `reanchorSessionClock: () => undefined` where needed to keep the type-checker happy (analog: how Plan 50-04 swept partial fake-engine literals for the new `clock` member — see 50-04-SUMMARY.md "Two partial fake-engine literals").
  </action>
  <verify>
    <automated>pnpm typecheck && pnpm test:run src/hooks/useSessionEngine.test.tsx 2>&1 | tail -5</automated>
    Source assertions:
    - `grep -c "reanchorSessionClock" src/hooks/useSessionEngine.ts` returns ≥3 (interface decl + method impl + return-literal export).
    - `grep -cE "describe\\(.reanchorSessionClock|it\\(.reanchorSessionClock" src/hooks/useSessionEngine.test.tsx` returns ≥3 (one describe + ≥3 it blocks — collapsing the `\\(` to either describe or it).
    - `grep -c "currentState.lastFrame.elapsedSec" src/hooks/useSessionEngine.ts` returns ≥1 (the new updater reads this field).
    - `grep -c "newClockNow" src/hooks/useSessionEngine.ts` returns ≥2 (signature + body usage).
  </verify>
  <acceptance_criteria>
    - `SessionEngine` interface exports `reanchorSessionClock(newClockNow: number): void`.
    - `useSessionEngine` returns a function under `reanchorSessionClock` that, when invoked while running, rewrites `startedAtSec = newClockNow - lastFrame.elapsedSec` via a setState updater.
    - When invoked while not running, the method is a no-op (state reference unchanged).
    - All 3 new tests pass under `pnpm test:run src/hooks/useSessionEngine.test.tsx`.
    - Full suite `pnpm test:run` exits 0 (test count ≥1359 baseline + 3 new = ≥1362).
    - QUAL-01: `pnpm typecheck && pnpm lint && pnpm test:run` exits 0 at commit boundary.
    - DEPS-01: `git diff package.json` shows zero changes.
    - The drift-guard at `src/audio/sessionClock.driftGuard.test.ts` passes (no new banned tokens in caller files).
  </acceptance_criteria>
  <done>
    `useSessionEngine` has a clean reanchor entry point. Task 2 wires the firing site in `useAudioCues.reconstructEngine`; Task 3 wires the controller-level callback that invokes `session.reanchorSessionClock`.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire swappable proxy into useAudioCues + onSessionClockReanchored callback</name>
  <files>src/hooks/useAudioCues.ts, src/hooks/useAudioCues.test.tsx</files>
  <read_first>
    - `src/hooks/useAudioCues.ts` (full file — especially L89-92 signature, L94-129 ref declarations, L264-337 `start()`, L383-486 `reconstructEngine()`, L339-367 `stop()`, L289-299 existing clock subscription pattern, L408-411 existing teardown pattern).
    - `src/audio/swappableSessionClock.ts` (Plan 51-01 — proxy factory signature).
    - `src/audio/sessionClock.ts` (`createWallSessionClock` as the initial proxy source; `SessionClock` type).
    - `src/hooks/useAudioCues.test.tsx` (existing tests — match the established posture for FakeAudioContext-driven assertions; identify which existing tests need a no-op `clock` member added to test-double `UseAudioCues` returns).
    - `.planning/phases/51-master-clock-unification/51-CONTEXT.md` D-03 + D-05 + D-10 + D-11.
  </read_first>
  <behavior>
    - Test 1 — Proxy clock exposed: after rendering the hook (no `start()` called yet), assert the returned controller has a `clock` member that conforms to `SessionClock` (has `now`, `schedule`, `setMasterGain`, `onSuspend`, `onResume`, `onClose`).
    - Test 2 — `clock.now()` returns wall-clock before start: before calling `start()`, `clock.now()` matches `performance.now() / 1000` (within ms tolerance — use `vi.useFakeTimers()` to make this deterministic).
    - Test 3 — `clock.now()` returns AC `currentTime` after start: drive `start(plan, timbre)` against a FakeAudioContext with a known `currentTime` value; await the start promise; assert `clock.now()` returns the AC's `currentTime` (NOT `performance.now() / 1000`).
    - Test 4 — Reanchor callback fires BEFORE audio-anchor reanchor callback: pass `vi.fn()` mocks for BOTH `onReanchorRequired` (2nd arg) AND `onSessionClockReanchored` (3rd arg); drive the iOS recovery path that exercises `reconstructEngine` (existing test patterns in this file already exercise this — e.g., the D-41 (c) test); assert both mocks were called once AND `onSessionClockReanchored.mock.invocationCallOrder[0] < onReanchorRequired.mock.invocationCallOrder[0]` (D-11 ordering).
    - Test 5 — `onSessionClockReanchored` receives `newEngine.clock.now()`: after reconstruction, the value passed to the callback matches the new AC's `currentTime` at the reanchor instant.
  </behavior>
  <action>
    Add the proxy clock construction inside the hook body, between L137 (`audioStatus` state setup) and L141 (`visibilityResumeAttemptedRef`):

    Declare a stable proxy reference via `useMemo(() => createSwappableSessionClock(createWallSessionClock()), [])`. Destructure into local consts: `const { clock: proxyClock, setSource: setProxyClockSource } = useMemo(...)`. (Alternative: store the whole returned object in a ref; Claude's discretion at task time — but the proxy MUST be created exactly once per hook instance per D-03 stable-identity invariant.)

    Add `import { createSwappableSessionClock } from '../audio/swappableSessionClock'` and `import { createWallSessionClock } from '../audio/sessionClock'` at the top (the existing import block).

    Add a 3rd optional arg to the `useAudioCues` function signature:
    `useAudioCues(initialMuted?, onReanchorRequired?, onSessionClockReanchored?: (newClockNow: number) => void)`.

    Mirror the existing `onReanchorRequiredRef` pattern at L149-152 for the new callback:
    - `const onSessionClockReanchoredRef = useRef<typeof onSessionClockReanchored>(onSessionClockReanchored)`.
    - `useEffect` that updates `.current` on every render.

    Add `clock: SessionClock` to the `UseAudioCues` interface (between `audioAvailable` and `muted` at L34-37; pick a sensible slot — alongside `audioStatus` is also reasonable). Export the proxy via the returned object at L540-552 as `clock: proxyClock`.

    Inside `start()`:
    - After `engineRef.current = engine` (currently at L288) and BEFORE the existing `engine.clock.on*` subscription block at L296-298, insert `setProxyClockSource(engine.clock)` (D-05 swap moment). Add an inline comment citing D-05.

    Inside `reconstructEngine()`:
    - After `engineRef.current = newEngine` (currently at L448) and BEFORE the new `engine.clock.on*` re-subscription block at L453-456, insert `setProxyClockSource(newEngine.clock)` (D-11 step 2). Add an inline comment citing D-11 step 2.
    - AFTER `setProxyClockSource(newEngine.clock)` and the `clockUnsubsRef` reassignment at L456, BEFORE the existing `onReanchorRequiredRef.current?.(reanchorAudioTime)` at L479, insert: `onSessionClockReanchoredRef.current?.(newEngine.clock.now())` (D-11 step 3 — fires BEFORE the audio-anchor reanchor).

    Inside `stop()` (at L339-367):
    - After `engineRef.current = null` (currently at L356), call `setProxyClockSource(createWallSessionClock())` to revert the proxy to wall-clock for any post-stop reads (mirrors D-06's `close()` posture for NK). This is a safe defensive move — without it, `clock.now()` would read from a closed AC's `currentTime` (which most browsers return as a frozen value, but is unspecified across implementations). Add an inline comment citing the "revert to wall on close" pattern from CONTEXT D-06.

    Verify the proxy ALSO needs `createWallSessionClock()` to be re-invoked per stop (not the same instance as the initial source) — `createWallSessionClock()` is a free function returning a fresh `SessionClock` each call; using a fresh instance on revert avoids any cross-session subscription state.

    Update existing test-double `UseAudioCues`-shape literals to include a no-op `clock` member (analog: Plan 50-04 Deviation #3 — the same kind of sweep). Sweep `src/hooks/useAudioCues.test.tsx` for any `fakeEngine` / `fakeNewEngine` / `fakeFirstEngine` returns that mock `UseAudioCues` or partial AudioEngine.

    Add the 5 new tests from the `<behavior>` block to `src/hooks/useAudioCues.test.tsx` in a new `describe('SessionClock proxy + onSessionClockReanchored (Phase 51 D-03/D-05/D-10/D-11)')` block at the end of the suite.

    Update the JSDoc on the `UseAudioCues.clock` member: cite D-03 (stable proxy identity), D-05 (HRV swap moment), D-11 (reconstruction swap + reanchor ordering).

    NOTE on the unmount cleanup at L211-231: do NOT add a `setProxyClockSource(createWallSessionClock())` revert there. The hook + its consumers (`useSessionEngine` via `audio.clock` + `useBreathingSessionController`) all unmount together — no post-unmount `clock.now()` read is possible on the proxy from outside the hook tree. This is asymmetric with NK Plan 51-03 (which DOES revert on close + closeAfterEndCue + unmount because NK's proxy is kept alive across begin/close cycles AND post-end stats reads can fire from `useNKEngine.end()`'s `onComplete` chain). The `stop()`-time revert above is sufficient for HRV; no unmount-time revert is needed.

    Use LSP for any signature renames if you change a typename; do NOT sed/perl across hook + test files (per `use-lsp-for-renames` memory rule).
  </action>
  <verify>
    <automated>pnpm typecheck && pnpm test:run src/hooks/useAudioCues.test.tsx 2>&1 | tail -5</automated>
    Source assertions:
    - `grep -c "createSwappableSessionClock" src/hooks/useAudioCues.ts` returns ≥1.
    - `grep -c "setProxyClockSource\\|setSource" src/hooks/useAudioCues.ts` returns ≥3 (start swap + reconstruct swap + stop revert).
    - `grep -c "onSessionClockReanchored" src/hooks/useAudioCues.ts` returns ≥4 (signature param + ref decl + ref-mirror effect + invocation site in reconstructEngine).
    - `grep -c "clock: proxyClock" src/hooks/useAudioCues.ts` returns ≥1 (the returned-object export).
    - The new `onSessionClockReanchored` invocation MUST appear BEFORE `onReanchorRequiredRef.current?.(reanchorAudioTime)` in the source: `grep -n "onSessionClockReanchoredRef\\|onReanchorRequiredRef" src/hooks/useAudioCues.ts` and confirm the `onSessionClockReanchoredRef.current?(...)` line number is LESS than the `onReanchorRequiredRef.current?(...)` line number inside `reconstructEngine`.
    - Drift-guard: `pnpm test:run src/audio/sessionClock.driftGuard.test.ts` passes (no new `performance.now()` / `audioCtx.currentTime` reads in this file).
  </verify>
  <acceptance_criteria>
    - `UseAudioCues` interface exposes `clock: SessionClock`.
    - `useAudioCues` accepts 3 args (the 3rd is optional `onSessionClockReanchored`).
    - The proxy clock is constructed exactly once per hook instance via `useMemo` (or equivalent stable-ref) and survives all renders.
    - `setProxyClockSource(engine.clock)` is called in `start()` after `engineRef.current = engine`.
    - `setProxyClockSource(newEngine.clock)` is called in `reconstructEngine()` after `engineRef.current = newEngine`.
    - `setProxyClockSource(createWallSessionClock())` is called in `stop()` after `engineRef.current = null`.
    - NO `setProxyClockSource` call is added inside the unmount cleanup at L211-231 (per the unmount asymmetry truth above).
    - `onSessionClockReanchoredRef.current?.(newEngine.clock.now())` is called in `reconstructEngine()` after `setProxyClockSource` and BEFORE `onReanchorRequiredRef.current?.(reanchorAudioTime)`.
    - All 5 new tests pass + existing 38 tests in `useAudioCues.test.tsx` still pass.
    - QUAL-01: `pnpm typecheck && pnpm lint && pnpm test:run` exits 0 at commit boundary.
    - DEPS-01: `git diff package.json` shows zero changes.
    - The drift-guard at `src/audio/sessionClock.driftGuard.test.ts` passes.
  </acceptance_criteria>
  <done>
    `useAudioCues` exposes a stable audio-backed proxy `clock` consumable by `useSessionEngine` AND fires `onSessionClockReanchored` before the existing audio-anchor reanchor on iOS recovery. **SOFT CHECKPOINT (see `<execution_notes>` above):** before starting Task 3, verify Task 2 lands cleanly — `pnpm typecheck && pnpm lint src/hooks/useAudioCues.ts src/hooks/useAudioCues.test.tsx && pnpm test:run src/hooks/useAudioCues.test.tsx && pnpm test:run src/audio/sessionClock.driftGuard.test.ts` all exit 0. Task 3 wires the controller-level callback that consumes both signals.
  </done>
</task>

<task type="auto">
  <name>Task 3: Wire useBreathingSessionController onto audio.clock + onSessionClockReanchored</name>
  <files>src/hooks/useBreathingSessionController.ts, src/hooks/useBreathingSessionController.test.tsx</files>
  <read_first>
    - `src/hooks/useBreathingSessionController.ts` (full file — especially L67-83 args/initial setup, L84 useSessionEngine call, L92-110 ref decls + onAudioReanchorRequired, L112 useAudioCues call, L114-128 audio destructuring, L179-232 startOrCancel).
    - `src/hooks/useAudioCues.ts` (post Task 2 — the new 3rd arg shape + the new `audio.clock` return-object member).
    - `src/hooks/useSessionEngine.ts` (post Task 1 — the new `reanchorSessionClock(newClockNow: number): void` method on `SessionEngine`).
    - `src/hooks/useBreathingSessionController.test.tsx` (existing test patterns — identify which mocks must add `audio.clock` or `session.reanchorSessionClock`).
    - `.planning/phases/51-master-clock-unification/51-CONTEXT.md` D-03 + D-10 + D-11 (callback flow + ordering).
  </read_first>
  <action>
    **Pre-flight check before starting this task:** confirm Task 2 lands cleanly (see Task 2 `<done>` block — the soft checkpoint). If `pnpm typecheck` or `pnpm test:run src/hooks/useAudioCues.test.tsx` fails post-Task-2, fix Task 2 first — Task 3's typecheck depends on Task 2's `clock: SessionClock` member on `UseAudioCues`.

    Flip the hook-call order: today the file has `session = useSessionEngine(...)` at L84 BEFORE `audio = useAudioCues(...)` at L112. After this plan, `audio = useAudioCues(...)` MUST come first because `useSessionEngine` consumes `audio.clock`.

    Delete L77-83 `sessionClock = useMemo(() => createWallSessionClock(), [])` AND remove the `createWallSessionClock` import (it is no longer used in this file). The `sessionClock` const is replaced by `audio.clock` from the (now-earlier) `useAudioCues` invocation.

    Declare a new `useCallback` `onSessionClockReanchored: (newClockNow: number) => void` BEFORE the `useAudioCues` invocation. Body: `session.reanchorSessionClock(newClockNow)` — but since `session` is declared AFTER `audio` post-flip, you'll need to thread `session.reanchorSessionClock` via a ref OR (cleaner) declare both via two separate hook calls where the callback closes over a ref that mirrors `session.reanchorSessionClock`. Recommended approach (mirrors the existing `onAudioReanchorRequired` posture at L105-110 which uses `sessionFrameRef.current?.elapsedSec`):

    1. Declare `audio = useAudioCues(initialMute, onAudioReanchorRequired, onSessionClockReanchored)` FIRST.
    2. Declare `session = useSessionEngine(initialSettings, activeStretchSettings, audio.clock)` SECOND.
    3. Declare `const sessionRef = useRef<SessionEngine | null>(null)` AND a `useEffect(() => { sessionRef.current = session }, [session])` so the callback can read the live `session.reanchorSessionClock` without needing `session` in scope at callback-declaration time.
    4. Define `onSessionClockReanchored = useCallback((newClockNow: number) => { sessionRef.current?.reanchorSessionClock(newClockNow) }, [])` — declared BEFORE `useAudioCues` (callback identity stable; reads through `sessionRef.current` to avoid temporal-dead-zone of `session`).

    This pattern mirrors the existing `sessionFrameRef.current?.elapsedSec` read inside `onAudioReanchorRequired` at L107-108 — same ref-indirection idiom for callbacks that must reference values declared LATER in the hook body.

    Alternative (Claude's discretion if cleaner at execution time): use the `onAudioReanchorRequired`-equivalent existing pattern more directly — e.g., a `sessionReanchorRef` typed as `useRef<((newClockNow: number) => void) | null>(null)` populated via a `useEffect` mirror. Same outcome; pick whichever reads cleaner in the actual file.

    Preserve `onAudioReanchorRequired` at L105-110 verbatim — both reanchor callbacks fire on reconstruction (D-11 ordering: session-clock first, then audio-anchor).

    Sweep `useBreathingSessionController.test.tsx` for test-double `audio` literals that mock `UseAudioCues` returns. Add a no-op `clock` member (with no-op `now`, `schedule`, `setMasterGain`, `onSuspend`, `onResume`, `onClose` returning no-op unsubscribes) to each — analog: Plan 50-04 Deviation #3. Also sweep for test-double `session` literals that mock `SessionEngine` returns and add `reanchorSessionClock: () => undefined` to each.

    Add ≥1 smoke test in `useBreathingSessionController.test.tsx`: render the controller; assert it does NOT crash; assert `session` and `audio` are both wired (e.g., calling `controller.audio.onMuteOrResumeClick()` doesn't throw). The existing tests should pass with minimal changes — mostly the test-double sweep above.

    DO NOT change `onAudioReanchorRequired` semantics — D-11 mandates the session-clock reanchor fires FIRST, then the audio-anchor reanchor; both flows are preserved.
  </action>
  <verify>
    <automated>pnpm typecheck && pnpm test:run src/hooks/useBreathingSessionController.test.tsx 2>&1 | tail -5</automated>
    Source assertions:
    - `grep -c "createWallSessionClock" src/hooks/useBreathingSessionController.ts` returns 0 (deleted).
    - `grep -c "useSessionEngine(initialSettings, activeStretchSettings, audio.clock)" src/hooks/useBreathingSessionController.ts` returns 1.
    - `grep -c "onSessionClockReanchored" src/hooks/useBreathingSessionController.ts` returns ≥2 (useCallback decl + useAudioCues 3rd arg).
    - `grep -c "reanchorSessionClock" src/hooks/useBreathingSessionController.ts` returns ≥1 (callback body invocation).
    - Hook-call ORDER check: `grep -n "useAudioCues\\|useSessionEngine" src/hooks/useBreathingSessionController.ts` confirms `useAudioCues` invocation line number is LESS than `useSessionEngine` invocation line number (the order flip).
  </verify>
  <acceptance_criteria>
    - `createWallSessionClock` import + invocation removed from `useBreathingSessionController.ts`.
    - `useSessionEngine` receives `audio.clock` as its 3rd arg.
    - Hook-call order is `audio` before `session` in the file.
    - `useAudioCues` is called with 3 args; the 3rd is `onSessionClockReanchored: useCallback(...)`.
    - `onSessionClockReanchored` body calls `session.reanchorSessionClock(newClockNow)` via a ref indirection.
    - All existing controller tests pass after the test-double sweep.
    - Smoke test confirms the controller renders without crashing post-flip.
    - QUAL-01: `pnpm typecheck && pnpm lint && pnpm test:run` exits 0 at commit boundary.
    - DEPS-01: `git diff package.json` shows zero changes.
    - The drift-guard at `src/audio/sessionClock.driftGuard.test.ts` passes.
  </acceptance_criteria>
  <done>
    HRV / Stretch session timing is now derived from `audioCtx.currentTime` via the swappable proxy in `useAudioCues.clock`. The reanchor callback chain preserves session elapsed across iOS engine reconstruction (D-10 / D-11). Plans 51-04 + 51-05 cover behavioral tests + iOS UAT respectively.
  </done>
</task>

</tasks>

<threat_model>
No new threat surface — pure refactor of in-browser audio/animation timing wiring, no new I/O, no new deps, no new user-input surface. Existing iOS audio recovery threat surface (Phase 5.1) preserved by D-10 / D-11 reanchor flow ordering. The Phase 50 subscriber-Set growth threat (T-50.04-02) is preserved by the proxy's teardown-before-resubscribe pattern (Plan 51-01) and by the existing `clockUnsubsRef` teardown pattern in `useAudioCues` (`stop()` + `reconstructEngine()` teardown remain in place — UNCHANGED). The new `onSessionClockReanchored` callback path is closure-captured via a ref (mirror of the existing `onReanchorRequiredRef` pattern at L149-152) so the callback identity does NOT leak across renders.
</threat_model>

<verification>
- `pnpm typecheck && pnpm lint && pnpm test:run` exits 0 after each commit boundary.
- `useAudioCues` exposes a stable proxy `clock` that delegates to wall-clock pre-start, AC clock post-start, and reverts to wall-clock on stop.
- `useSessionEngine` exposes `reanchorSessionClock(newClockNow)` that rewrites `startedAtSec` to preserve `lastFrame.elapsedSec` across an AC reconstruction.
- `useBreathingSessionController` passes `audio.clock` into `useSessionEngine` (no controller-local wall clock).
- The reanchor callback flow fires `onSessionClockReanchored` BEFORE `onReanchorRequired` on Phase 5.1 reconstruction.
- Drift-guard at `src/audio/sessionClock.driftGuard.test.ts` still passes (no new banned tokens in the 5 caller files).
- Existing `useAudioCues` + `useSessionEngine` + `useBreathingSessionController` test suites all pass with the test-double sweep applied.
</verification>

<success_criteria>
- CLOCK-01 satisfied: `useSessionEngine` elapsed derives from `audio.clock.now() - sessionStartCtxTime` where `audio.clock` is audio-backed during a running session.
- CLOCK-03 satisfied by construction: the rAF tick at `useSessionEngine.ts:159` already calls `completeIfNeeded(currentState, clock.now())`; after this plan, `clock.now()` is the audio-backed proxy during a running session, so animation phase progress is derived from the audio clock each rAF tick.
- DEPS-01 + QUAL-01 hold at every commit boundary.
- D-10 / D-11 reanchor flow is wired: session-clock reanchor fires before audio-anchor reanchor; session elapsed is preserved across AC reconstruction.
- Plans 51-04 (behavioral tests) + 51-05 (iOS UAT) can layer on top of this without touching the wiring.
</success_criteria>

<output>
Create `.planning/phases/51-master-clock-unification/51-02-SUMMARY.md` when done. Record:
- Truths satisfied + artifacts created + key links verified.
- Test count delta from Plan 51-01 baseline.
- Per-task commits (the 3 tasks may need to land as 1, 2, or 3 commits — Plan 50-02/50-04 precedent for combining tasks when the per-commit green-gate forces it; document the decomposition decision).
- Any deviations from plan (e.g., the exact ref-indirection chosen for `onSessionClockReanchored` in `useBreathingSessionController`, the slot chosen for the new `clock` member in `UseAudioCues` interface).
- Whether the soft mid-plan checkpoint (between Task 2 and Task 3) was useful — did Task 2 land cleanly on first attempt, or did the verification beat catch a regression?
- Discoveries for downstream Plans 04 + 05 (e.g., are there any test-double sweep sites outside the named test files that future plans need to know about).
</output>
