---
phase: 51-master-clock-unification
plan: 05
type: execute
wave: 3
depends_on:
  - 51-02
  - 51-03
files_modified:
  - .planning/phases/51-master-clock-unification/51-IOS-UAT.md
autonomous: false
requirements:
  - CLOCK-04
  - DEPS-01
  - QUAL-01
requirements_addressed:
  - CLOCK-04
  - DEPS-01
  - QUAL-01
tags:
  - sessionclock
  - uat
  - ios
  - manual-test
  - checkpoint
  - clock-unification
must_haves:
  truths:
    - "`.planning/phases/51-master-clock-unification/51-IOS-UAT.md` exists and documents an executable test plan for CLOCK-04 (the iOS lock/unlock sync requirement). The document is the artifact the operator can run against a real iOS device to confirm the milestone goal."
    - "The UAT plan covers at minimum: (a) HRV breathing — lock device mid-session, wait 30+ seconds, unlock, verify audio + animation remain in phase on resume; (b) Stretch — same test with a stretch session; (c) Navi Kriya — same test with a NK session (because the NK stats rebase landed in Plan 51-03, NK stats should also exclude the lock duration); (d) Long-lock test: lock for 5+ minutes mid-HRV-session and verify clean resume; (e) Brief-lock test: lock for < 5 seconds and verify near-instant resume (verifies the optimistic visibilitychange resume path at `useAudioCues.ts:249-262` still works); (f) Pre-existing Phase 5.1 affordance: lock for long enough that iOS kills the AC entirely, unlock, verify the morphing MuteToggle / `audioStatus === 'needs-resume'` recovery affordance still appears and works (per Phase 5.1 reconstruction path preserved in Plan 51-02 Task 2)."
    - "Each scenario in the UAT plan has explicit PASS / FAIL criteria the operator can mark off. PASS criteria reference observable end-user behavior (audio resumes in phase, animation does not burst, no perceptual mismatch). FAIL criteria identify the specific regression class (audio drifts ahead of animation, animation bursts, mute affordance fails to appear in expected scenario, etc.)."
    - "The UAT plan covers all 4 success criteria from ROADMAP Phase 51 (lines 148-153 of ROADMAP.md): (1) iOS lock/unlock keeps audio + animation in phase; (2) foreground no-drift (cross-referenced to Plan 51-04 B7 architectural confirmation); (3) elapsed derives from `SessionClock.now() − sessionStartCtxTime`; (4) no regression in BPM cadence, ratio splits, total-duration completion across all three practices."
    - "The UAT plan covers BOTH the Phase 51 caller-level rebase AND the Phase 5.1 iOS recovery affordance interaction (D-10 / D-11 ordering). Specifically: scenario (f) above MUST verify that the session-clock reanchor preserves the pre-lock elapsed across an AC-recovery cycle (the user sees the session continue from where they left off, not from 0)."
    - "The UAT plan documents the device matrix the operator commits to testing: at minimum the operator's primary iOS device (model + iOS version). The plan acknowledges (per STATE.md L99-101 deferred-items) that S2 Android Chrome wake-lock UAT, iOS standalone-PWA Wake Lock < 18.4, and iOS Pitfall 6 phone-call interrupted state are NOT in v2.2 scope and explicitly out-of-scope for this UAT."
    - "**Checkpoint:** the plan is `autonomous: false` and includes a `checkpoint:human-verify` task that pauses execution until the operator confirms each UAT scenario PASS/FAIL. The checkpoint resume signal is the operator's explicit confirmation (e.g., 'all UAT pass' or 'UAT failures: <list>')."
    - "If the UAT surfaces regressions, the operator's response becomes input to `/gsd:plan-phase 51 --gaps` (the gap-closure workflow) — this plan does NOT directly fix regressions; it captures them. The SUMMARY records UAT outcomes verbatim."
    - "DEPS-01 holds: no package change is needed for UAT — this plan only adds a markdown document."
    - "QUAL-01 holds: `pnpm typecheck && pnpm lint && pnpm test:run` exits 0 at the (single) commit boundary that adds the UAT document."
  artifacts:
    - path: ".planning/phases/51-master-clock-unification/51-IOS-UAT.md"
      provides: "Manual test plan for CLOCK-04 + ROADMAP Phase 51 success criteria #1-#4. Includes device matrix, scenario list (a-f minimum), per-scenario PASS/FAIL criteria, scope-exclusion note (S2 Android, iOS PWA < 18.4, Pitfall 6 phone-call interruption — all deferred per STATE.md)."
      min_lines: 80
  key_links:
    - from: ".planning/phases/51-master-clock-unification/51-IOS-UAT.md"
      to: ".planning/ROADMAP.md"
      via: "explicit cross-references to ROADMAP Phase 51 success criteria #1-#4"
      pattern: "ROADMAP Phase 51"
    - from: ".planning/phases/51-master-clock-unification/51-IOS-UAT.md"
      to: ".planning/REQUIREMENTS.md"
      via: "explicit cross-reference to CLOCK-04 + CLOCK-05 + CLOCK-01 + CLOCK-03"
      pattern: "CLOCK-0[1345]"
    - from: ".planning/phases/51-master-clock-unification/51-IOS-UAT.md"
      to: ".planning/phases/51-master-clock-unification/51-CONTEXT.md"
      via: "explicit cross-references to D-07 (practice-time semantics), D-10 / D-11 (reanchor flow), Phase 5.1 affordance preservation"
      pattern: "D-(07|10|11)"
---

<objective>
Close CLOCK-04 (iOS lock/unlock mid-session keeps audio + animation in phase on resume — diagnosis #1) by producing an executable manual UAT plan against a real iOS device. The architectural prereqs are locked by Plans 51-01 through 51-04; this plan turns those into a checklist the operator can run against an iPhone (or iPad) to confirm the end-user-observable behavior.

This plan is `autonomous: false` because the work requires an iOS device + the operator's hands-on testing — there is no CLI / API path for "lock the iPhone screen and verify audio resumes in phase". The checkpoint pauses execution until the operator reports UAT outcomes.

Purpose: Final gate for CLOCK-04. Closes the architectural milestone by confirming the diagnosis #1 behavior is fixed in production. Surfaces any regressions for `--gaps` planning if needed.

Output:
- `.planning/phases/51-master-clock-unification/51-IOS-UAT.md` — executable manual test plan.
- A checkpoint where the operator reports UAT outcomes.
- A SUMMARY that records the operator's PASS/FAIL marks per scenario.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/51-master-clock-unification/51-CONTEXT.md
@.planning/phases/51-master-clock-unification/51-01-PLAN.md
@.planning/phases/51-master-clock-unification/51-02-PLAN.md
@.planning/phases/51-master-clock-unification/51-03-PLAN.md
@.planning/phases/51-master-clock-unification/51-04-PLAN.md
@.planning/notes/audio-animation-three-clocks-diagnosis.md
@.planning/phases/49-ios-speaker-route-fix/49-02-PLAN.md

<interfaces>
<!-- Phase 49-02-PLAN.md is the closest in-repo prior art for a Phase-level iOS UAT plan (autonomous: false, checkpoint structure). Use its shape as a template — same checkpoint task type, same PASS/FAIL framing per scenario. -->

Existing PASS / FAIL framing patterns from `.planning/phases/49-ios-speaker-route-fix/49-02-PLAN.md`:
- Scenario blocks with explicit setup steps, action steps, and observable expectations.
- Resume-signal phrasing: "Operator types 'approved' to continue, or describes failures."
- Scope exclusion notes that reference STATE.md's Deferred Items table for the v2.x carry-forwards.

Test devices the operator has access to (per STATE.md + v2.x carry-forward):
- iOS device available: yes (the operator's primary device — exact model + iOS version is captured by the operator in the UAT doc at run time).
- Android device: NOT available (S2 Android Chrome wake-lock UAT deferred per STATE.md L101).
- iOS PWA-standalone install on iOS < 18.4: NOT available; explicitly out of scope (STATE.md L99).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the iOS UAT plan document</name>
  <files>.planning/phases/51-master-clock-unification/51-IOS-UAT.md</files>
  <read_first>
    - `.planning/phases/49-ios-speaker-route-fix/49-02-PLAN.md` (template for the document structure — copy the section ordering: header → device matrix → scenario list → PASS/FAIL framing → scope exclusions → operator notes).
    - `.planning/phases/51-master-clock-unification/51-CONTEXT.md` D-07 (practice-time semantics — relevant for scenario expectations: "audio is silent during lock; resume picks up where left off"), D-10/D-11 (reanchor flow — scenario (f) verifies elapsed preservation across recovery).
    - `.planning/ROADMAP.md` Phase 51 success criteria at lines 148-153 (the 4 success criteria the UAT MUST cover).
    - `.planning/notes/audio-animation-three-clocks-diagnosis.md` §"How each bug falls out" lines 28-32 (the diagnosis #1 + #2 framing that motivated the milestone; the UAT scenarios are the end-user-observable manifestation).
    - `.planning/STATE.md` L99-101 (the v2.x carry-forward scope-exclusion items that must be explicitly out-of-scope in this UAT).
    - `src/hooks/useAudioCues.ts` L249-262 (visibilitychange + optimistic resume path — relevant for the brief-lock scenario) and L383-486 (reconstruction path — relevant for scenario (f)).
  </read_first>
  <action>
    Create `.planning/phases/51-master-clock-unification/51-IOS-UAT.md` with the following sections:

    **Section 1 — Header + scope**: Title ("Phase 51 iOS UAT: Master Clock Unification — Lock/Unlock Sync"); date stub for the operator to fill in; cross-references to ROADMAP Phase 51 success criteria #1-#4, REQUIREMENTS CLOCK-01 + CLOCK-03 + CLOCK-04 + CLOCK-05 (CLOCK-02 is satisfied by construction per CONTEXT and needs no UAT), CONTEXT D-07 + D-10 + D-11.

    **Section 2 — Device matrix**: A small table the operator fills in at run time with: iOS device model, iOS version, browser (Safari / Chrome on iOS / other), headphones-connected status if applicable. Mention that the test SHOULD run on Safari first (the iOS default + diagnosis target) and optionally on iOS Chrome (which uses the same WebKit engine but a different audio session lifecycle).

    **Section 3 — Pre-flight**: Operator builds + deploys current `main` to a hostable URL (e.g., the existing `lucindo.github.io/hrv/` versioned-deploy mechanism per `project_versioned_pages_deploy` memory rule); confirms the app loads on the iOS device; confirms audio plays on the speaker (the Phase 49 + 49.1 silent-loop posture is preserved at Phase 51 — verify by listening for a cue with silent switch ON).

    **Section 4 — Scenario (a) HRV brief lock**: Start a 5-min HRV resonant session; let it run for ~1 minute (so the user has a clear sense of the rhythm); lock the iOS device (press the power button); wait 5-15 seconds; unlock the device; observe. PASS: audio resumes in phase with the breathing orb (the orb's expansion/contraction is matched by the cue tones); no audible burst of catch-up cues; the session elapsed counter on the page shows time that is SLIGHTLY LESS than the wall-clock-elapsed (because the locked interval is excluded per D-07 practice-time semantics — the operator should observe this and mark it as expected, NOT a regression). FAIL: audio leads or lags the orb noticeably; cues fire in rapid succession on resume; the orb bursts forward to catch up.

    **Section 5 — Scenario (b) Stretch brief lock**: Same as (a) but with the Stretch practice instead of HRV. Verify the ramp position is preserved across the lock (the orb continues from where it left off — does not jump to a "wall-clock-correct" position). PASS/FAIL framing mirrors (a).

    **Section 6 — Scenario (c) Navi Kriya brief lock**: Same as (a) but with a Navi Kriya session. Verify NK stats recorded post-session excludes the lock duration (operator inspects stats afterward by navigating to a stats surface OR by reading the session's "completed in X minutes" indicator at completion — if the stats UI is removed per v2.0 Phase 37, the operator can verify via console / localStorage inspection; document this caveat in the scenario). PASS: stats `elapsedMs` excludes the locked interval (matches the D-08 invariant). FAIL: stats include the locked interval as if the AC kept running.

    **Section 7 — Scenario (d) HRV long lock**: Start an open-ended HRV session; let it run for 1 minute; lock for 5+ minutes; unlock. PASS: audio resumes in phase; no AC kill / recovery affordance appears (the lock was short enough that the AC stayed `'suspended'` and resumed cleanly via the optimistic `engine.resume()` path); session continues. FAIL: any of the above conditions are violated.

    **Section 8 — Scenario (e) HRV very-brief lock**: Lock for < 5 seconds (a quick tap-and-release on the power button). PASS: audio resumes near-instantly with no perceptual gap; orb continues smoothly. FAIL: any audible discontinuity.

    **Section 9 — Scenario (f) AC kill + recovery affordance**: Start an HRV session; lock the device for long enough that iOS kills the AC entirely (this duration is device-dependent — operator may need to lock for 10+ minutes OR use a different trigger like a phone call to force AC death; the trigger that reliably kills the AC on the operator's device is recorded at run time). Unlock. PASS: the morphing MuteToggle / `audioStatus === 'needs-resume'` recovery affordance appears in the UI per Phase 5.1; tapping it triggers `useAudioCues.resume()` → `reconstructEngine()`; audio comes back; the session elapsed counter shows the SAME value it showed pre-lock (D-10 reanchor preserved elapsed across the AC swap — this is the Phase 51-specific invariant on top of the existing Phase 5.1 recovery path). FAIL: the affordance does not appear; OR after tapping it, audio does not return; OR the session elapsed jumped to 0 (reanchor failed — session-clock was not preserved across the recovery).

    **Section 10 — Scenario (g) Foreground long-run smoke** (cross-reference to Plan 51-04 B7): Start an open-ended HRV session; let it run uninterrupted for 5+ minutes WITHOUT locking the device or backgrounding the tab. PASS: audio and orb remain in phase throughout; no drift, no burst, no perceptual mismatch. FAIL: any observable drift. This scenario complements the deterministic Plan 51-04 B7 jsdom smoke test — the real-device pass confirms the architectural guarantee holds in production.

    **Section 11 — Scope exclusions**: Bulleted list explicitly out-of-scope for this UAT, referencing STATE.md L99-101:
    - S2 Android Chrome wake-lock real-device UAT (physical device unavailable).
    - iOS standalone-PWA Wake Lock < 18.4 detect-and-warn (product decision pending).
    - iOS Safari Pitfall 6 — phone-call interrupted state (deferred to a future audio milestone with real-device UAT).
    - iOS Chrome / iOS Firefox parity beyond the optional Section 2 device-matrix entry (the diagnosis #1 fix is for Safari + WebKit; other iOS browsers use the same engine but have their own AC lifecycle quirks; document any observations the operator chooses to capture, but they are not pass/fail gates).

    **Section 12 — Operator notes section**: A blank section the operator fills in during the UAT with observations, screenshots / video clips if captured, device + iOS version captured at run time, exact lock durations used, and any anomalies that don't cleanly fit scenarios (a)-(g).

    **Section 13 — Outcome summary**: A table the operator fills in at the end:

    | Scenario | PASS / FAIL | Notes |
    |----------|-------------|-------|
    | (a) HRV brief lock | | |
    | (b) Stretch brief lock | | |
    | (c) NK brief lock | | |
    | (d) HRV long lock | | |
    | (e) HRV very-brief lock | | |
    | (f) AC kill + recovery affordance | | |
    | (g) Foreground long-run smoke | | |

    Followed by a free-form "Overall verdict" paragraph the operator writes.

    DO NOT modify any production source file in this task. This is a documentation-only task.

    DO NOT include time estimates or human-team-coordination patterns (per the GSD planner anti-enterprise guidance) — the document is for one operator + one device.
  </action>
  <verify>
    <automated>test -f .planning/phases/51-master-clock-unification/51-IOS-UAT.md && wc -l .planning/phases/51-master-clock-unification/51-IOS-UAT.md 2>&1 | tail -3</automated>
    Source assertions:
    - File exists at `.planning/phases/51-master-clock-unification/51-IOS-UAT.md`.
    - File line count ≥80 lines.
    - `grep -cE "PASS|FAIL" .planning/phases/51-master-clock-unification/51-IOS-UAT.md` returns ≥14 (≥7 scenarios × 2 verdicts at minimum).
    - `grep -cE "CLOCK-04|CLOCK-05|ROADMAP Phase 51" .planning/phases/51-master-clock-unification/51-IOS-UAT.md` returns ≥3 (cross-references present).
    - `grep -cE "D-07|D-10|D-11" .planning/phases/51-master-clock-unification/51-IOS-UAT.md` returns ≥3.
    - `grep -cE "Scenario \\([a-g]\\)" .planning/phases/51-master-clock-unification/51-IOS-UAT.md` returns ≥7.
    - `grep -cE "scope exclusion|out of scope|Out of Scope|out-of-scope" .planning/phases/51-master-clock-unification/51-IOS-UAT.md` returns ≥1 (Section 11 scope-exclusion section present).
  </verify>
  <acceptance_criteria>
    - File created at exact path `.planning/phases/51-master-clock-unification/51-IOS-UAT.md`.
    - ≥80 lines total.
    - All 7 scenarios (a-g) covered, each with explicit PASS / FAIL framing.
    - Cross-references to ROADMAP success criteria, REQUIREMENTS CLOCK-NN, and CONTEXT D-07/D-10/D-11 present.
    - Scope-exclusion section explicitly out-of-scopes the three STATE.md deferred items.
    - Outcome summary table present with all 7 scenario rows.
    - No production source files modified.
    - QUAL-01: `pnpm typecheck && pnpm lint && pnpm test:run` exits 0 at commit (no production code touched; tests baseline holds).
    - DEPS-01: `git diff package.json` shows zero changes.
  </acceptance_criteria>
  <done>
    The UAT plan document is committed and ready for the operator to run against an iOS device. Task 2 is the checkpoint where the operator reports outcomes.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Operator runs iOS UAT + reports outcomes</name>
  <what-built>
    Plans 51-01 through 51-04 land the master clock unification: a swappable proxy `SessionClock` (Plan 01), HRV / Stretch wiring through `useAudioCues.clock` + reanchor callback (Plan 02), NK wiring through `useNaviKriyaAudio.clock` (Plan 03), and behavioral tests covering D-07 / D-08 / D-09 / CLOCK-05 + reanchor invariants (Plan 04). The UAT document at `.planning/phases/51-master-clock-unification/51-IOS-UAT.md` covers the only remaining requirement, CLOCK-04, which can ONLY be verified against a real iOS device.
  </what-built>
  <how-to-verify>
    1. Build + deploy current `main` to a URL accessible from the iOS device (e.g., trigger the existing versioned-deploy GitHub Actions workflow per the `project_versioned_pages_deploy` memory rule, or run a temporary local dev server with iOS device on the same Wi-Fi network).
    2. Open the UAT document at `.planning/phases/51-master-clock-unification/51-IOS-UAT.md`.
    3. Fill in Section 2 (Device matrix) with the iOS device model + version being tested.
    4. Run scenarios (a) through (g) in order. For each, mark PASS or FAIL in Section 13's outcome summary table. If FAIL, capture details in the per-scenario notes column AND in Section 12 operator notes.
    5. Write an "Overall verdict" paragraph at the bottom of Section 13.
    6. If ANY scenario FAILED, the operator's resume signal includes the list of failing scenarios + any context the planner needs for a `/gsd:plan-phase 51 --gaps` follow-up. If all PASS, the resume signal is simply "all UAT pass" or equivalent.
    7. The operator commits the filled-in UAT document (the file modification is part of the operator's manual step — the agent does NOT auto-commit the UAT result; the operator owns the integrity of those marks).
  </how-to-verify>
  <resume-signal>
    Operator types either:
    - `all UAT pass` — all scenarios PASSED; Phase 51 milestone goal confirmed end-to-end against a real iOS device.
    - `UAT failures: <list>` — one or more scenarios FAILED; the failure list becomes input to `/gsd:plan-phase 51 --gaps` for gap closure.
    - `UAT skipped: <reason>` — operator chose to defer the UAT (e.g., no iOS device on hand at this moment); Phase 51 ships with CLOCK-04 marked as "pending real-device UAT" in the SUMMARY, to be re-run at milestone close or whenever the device is available.
  </resume-signal>
</task>

</tasks>

<threat_model>
No new threat surface — this plan only adds a markdown document + a manual-test checkpoint. No production code changes; no new I/O; no new deps. Existing iOS audio recovery threat surface (Phase 5.1) is the SUBJECT of scenario (f) — the UAT verifies the existing recovery affordance still works in combination with the new reanchor flow.
</threat_model>

<verification>
- `.planning/phases/51-master-clock-unification/51-IOS-UAT.md` exists and is ≥80 lines.
- All grep-based source assertions pass (see Task 1 `<verify>`).
- `pnpm typecheck && pnpm lint && pnpm test:run` exits 0 (no production code change).
- Operator's UAT outcomes are recorded in the document.
- Checkpoint resume signal received.
</verification>

<success_criteria>
- CLOCK-04 is closed (all UAT scenarios PASS) OR explicit gap items are captured for `--gaps` planning.
- ROADMAP Phase 51 success criterion #1 (iOS lock/unlock sync) is confirmed end-to-end against a real iOS device.
- ROADMAP success criteria #2 (foreground drift), #3 (single-source elapsed), #4 (no regression across practices) are confirmed at the user-observable level (the architectural guarantees were locked in Plan 51-04; this plan confirms they hold in production).
- DEPS-01 + QUAL-01 hold.
</success_criteria>

<output>
Create `.planning/phases/51-master-clock-unification/51-05-SUMMARY.md` when done. Record:
- Truths satisfied (the UAT document exists; scenarios covered).
- Operator UAT outcomes verbatim (scenario PASS / FAIL marks + the overall verdict paragraph).
- If any UAT failed, list the failing scenarios + any context for `/gsd:plan-phase 51 --gaps`.
- If UAT was skipped, document the skip reason and the milestone-close re-run plan.
- Phase 51 closure status: which CLOCK-NN requirements are fully verified, which (if any) are deferred to gap closure or milestone-close re-run.
</output>
