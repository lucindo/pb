---
phase: 17-visual-variants
plan: 02
type: execute
wave: 2
depends_on:
  - 17-01
files_modified:
  - src/components/OrbShape.tsx
  - src/components/OrbShape.test.tsx
  - src/components/shapeConstants.ts
  - src/components/BreathingShape.tsx
  - src/components/BreathingShape.test.tsx
autonomous: true
requirements:
  - VARIANT-02
  - VARIANT-05
user_setup: []
tags:
  - extraction
  - phase-17
  - orb-shape

must_haves:
  truths:
    - "`OrbShape` is a new exported function in `src/components/OrbShape.tsx` whose Body + LeadIn render output is byte-identical (modulo the post-Plan-01 className rename) to the pre-Plan-02 `BreathingShape` Body + LeadIn — VARIANT-02 zero-regression"
    - "`OrbShape({ frame, leadInDigit })` returns `<OrbLeadIn digit={leadInDigit} />` when `leadInDigit != null`, else `<OrbBody frame={frame!} />` — the idle null-return is OWNED BY the dispatcher (Plan 05), not OrbShape (D-04)"
    - "`OrbShape` renders a `data-variant='orb'` attribute on BOTH the Body root and the LeadIn root (D-16 — render-local)"
    - "`MIN_SCALE`, `MAX_SCALE`, `MID_SCALE` are exported from a shared module (`src/components/shapeConstants.ts`) so Square + Ring (Plan 03) can re-import without duplicating the constants — single source of truth"
    - "The Phase 5.1 D-20/D-22 four-edge anchoring + `-1.5px` outer-marker offsets + `translate3d(0,0,0) scale(...)` GPU promotion are preserved verbatim on both render sites"
    - "VARIANT-05: `OrbLeadIn` renders the lead-in digit (3/2/1) at `text-7xl/8xl` with `var(--color-orb-in-text)` color — identical to the pre-extraction BreathingShape behavior"
    - "`tsc && lint && build && test` exits 0 after each commit"
  artifacts:
    - path: "src/components/shapeConstants.ts"
      provides: "Exported `MIN_SCALE`, `MAX_SCALE`, `MID_SCALE` numeric constants (0.58, 1.0, 0.79) with sync-with-theme.css comments"
      contains: "export const MIN_SCALE"
    - path: "src/components/OrbShape.tsx"
      provides: "`OrbShape` exported function (top-level dispatch on leadInDigit), plus internal `OrbBody` and `OrbLeadIn` functions; identical render output to pre-Plan-02 BreathingShape Body + LeadIn except for added `data-variant='orb'` attribute on the two roots"
      contains: "export function OrbShape"
    - path: "src/components/OrbShape.test.tsx"
      provides: "Migrated body + lead-in tests + Phase 5.1 WR-03 structural contract block, all targeting `OrbShape` (not BreathingShape); class queries use `.shape-marker--*` (post-Plan-01)"
      contains: "OrbShape"
    - path: "src/components/BreathingShape.tsx"
      provides: "Intermediate state — Body + LeadIn function definitions REMOVED; dispatcher temporarily delegates to OrbShape for ALL invocations (variant prop NOT yet added — that lands in Plan 05). Net result: `BreathingShape` now imports and forwards to `OrbShape` unconditionally."
      contains: "import { OrbShape } from './OrbShape'"
    - path: "src/components/BreathingShape.test.tsx"
      provides: "Test file slimmed of Body + LeadIn structural assertions (those moved to OrbShape.test.tsx). Keeps the idle null-return test and the lead-in-priority test. WR-03 structural block REMOVED here (lives in OrbShape.test.tsx now). Plan 05 adds variant-dispatch tests."
      contains: "renders null when both frame and leadInDigit are absent"
  key_links:
    - from: "src/components/OrbShape.tsx (OrbBody)"
      to: "src/components/shapeConstants.ts"
      via: "named import"
      pattern: "import \\{ MIN_SCALE, MAX_SCALE, MID_SCALE \\} from './shapeConstants'"
    - from: "src/components/BreathingShape.tsx (dispatcher)"
      to: "src/components/OrbShape.tsx (OrbShape)"
      via: "named import + delegate render"
      pattern: "import \\{ OrbShape \\} from './OrbShape'"
    - from: "src/components/OrbShape.test.tsx"
      to: "src/components/OrbShape.tsx rendered DOM"
      via: "RTL render + getByRole + querySelector"
      pattern: "render\\(<OrbShape"
---

<objective>
Extract `BreathingShapeBody` + `BreathingShapeLeadIn` from `src/components/BreathingShape.tsx` into a NEW `src/components/OrbShape.tsx` file VERBATIM (modulo identifier rename and a `data-variant="orb"` attribute on the 2 root divs). Move the MIN/MAX/MID scale constants to a new shared module `src/components/shapeConstants.ts` so Plan 03 (Square + Ring) can import without circular deps.

Per CONTEXT.md D-02: zero-regression provable via git diff — orb code is unchanged except (a) function rename Body→OrbBody, LeadIn→OrbLeadIn, (b) the `.shape-marker--*` className rename from Plan 01, (c) `data-variant="orb"` added on both roots, (d) constants moved to `shapeConstants.ts` and re-imported.

Per CONTEXT.md D-04: `OrbShape` does NOT own the idle null-return guard — that moves to the `BreathingShape` dispatcher in Plan 05. In Plan 02, `BreathingShape` is left as a still-functional pass-through that forwards every call to `OrbShape` (no variant switch yet). This keeps `tsc && lint && build && test` green at each commit boundary.

Output: `OrbShape.tsx` (the new home for the orb body + lead-in), `OrbShape.test.tsx` (migrated body + lead-in tests + Phase 5.1 WR-03 structural contract block), `shapeConstants.ts` (3 exported numeric constants), trimmed `BreathingShape.tsx` (~30 LOC pass-through), trimmed `BreathingShape.test.tsx` (smoke + idle-null + lead-in-priority only).
</objective>

<execution_context>
@$HOME/.claude-personal-personal/get-shit-done/workflows/execute-plan.md
@$HOME/.claude-personal-personal/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/17-visual-variants/17-CONTEXT.md
@.planning/phases/17-visual-variants/17-PATTERNS.md
@.planning/phases/17-visual-variants/17-UI-SPEC.md
@.planning/phases/17-visual-variants/17-VALIDATION.md
@src/components/BreathingShape.tsx
@src/components/BreathingShape.test.tsx

<interfaces>
<!-- Post-Plan-01 state of the affected files (selector rename already applied). -->

src/components/BreathingShape.tsx (post-Plan-01 — pre-Plan-02):
  - Lines 1–11: imports + BreathingShapeProps interface (frame + leadInDigit). KEEP imports of SessionFrame + usePrefersReducedMotion in OrbShape.tsx, REMOVE from BreathingShape.tsx (after delegating).
  - Lines 13–20: MIN_SCALE / MAX_SCALE / MID_SCALE constants. MOVE to shapeConstants.ts.
  - Lines 33–41: dispatcher (idle null-return + Body/LeadIn delegate). REWRITE in Plan 02 to import + delegate to OrbShape (single call site, no variant switch yet). The idle null-return STAYS in the dispatcher per D-04.
  - Lines 43–147: BreathingShapeBody. EXTRACT verbatim → OrbBody in OrbShape.tsx. Add `data-variant="orb"` on root div (line 56 area in new file). KEEP all four-edge anchoring (lines 90–103), `-1.5px` outer offsets (line 76), `translate3d(0,0,0) scale(...)` (line 102), MID_SCALE substitution (line 51).
  - Lines 160–224: BreathingShapeLeadIn. EXTRACT verbatim → OrbLeadIn in OrbShape.tsx. Add `data-variant="orb"` on root div (line 163 area in new file). KEEP all four-edge anchoring (lines 189–195), `-1.5px` outer offsets (line 177), GPU promotion (line 194).

  After this plan, BreathingShape.tsx becomes ~25 LOC:
    ```
    import type { SessionFrame } from '../domain/sessionMath'
    import { OrbShape } from './OrbShape'

    export interface BreathingShapeProps {
      frame: SessionFrame | null
      leadInDigit?: 3 | 2 | 1 | null
    }

    export function BreathingShape({ frame, leadInDigit }: BreathingShapeProps) {
      if (frame === null && leadInDigit == null) {
        return null
      }
      return <OrbShape frame={frame} leadInDigit={leadInDigit} />
    }
    ```
  (Plan 05 will replace the single-delegate body with a 3-way variant switch.)

src/components/BreathingShape.test.tsx (post-Plan-01 — pre-Plan-02):
  - Keep: `'renders null when both frame and leadInDigit are absent'`, `'lead-in wins when both frame and leadInDigit are set (D-14 priority)'` (because these are dispatcher-level concerns).
  - REMOVE: the entire `'Phase 5.1 Plan 04 WR-03 structural contract'` describe block (lines 89–272). It MIGRATES to OrbShape.test.tsx verbatim.
  - REMOVE: the in-orb body + lead-in render tests that assert specific structural details (`.orb` class queries, `text-7xl` queries, `scale(0.79)` queries). MIGRATE to OrbShape.test.tsx.
  - KEEP: top-level rendering smoke tests (`renders the BreathingShapeBody when frame is provided…` style, `renders the lead-in digit in the orb area…` style) — these are dispatcher smoke and stay here.

OrbShape.test.tsx (NEW — migration target):
  - Receive ALL the structural Body/LeadIn assertions that previously lived in BreathingShape.test.tsx (the full Phase 5.1 WR-03 block).
  - Update class-name queries to the post-Plan-01 `.shape-marker--{outer,inner}` selectors.
  - Update `render(<BreathingShape …/>)` → `render(<OrbShape …/>)`.
  - Add 2 new tests: `data-variant='orb'` attribute on Body root + LeadIn root (RESEARCH §"Common Pitfalls — Pitfall 1").

shapeConstants.ts (NEW):
  - Three exports: `MIN_SCALE = 0.58`, `MAX_SCALE = 1.0`, `MID_SCALE = (MIN_SCALE + MAX_SCALE) / 2`.
  - Preserve the "keep in sync with --orb-scale-{min,max,mid}" comment block verbatim from BreathingShape.tsx:13–20.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create `src/components/shapeConstants.ts` with extracted MIN/MAX/MID scale constants + comment block</name>
  <files>src/components/shapeConstants.ts</files>
  <read_first>
    - src/components/BreathingShape.tsx lines 13–20 (the source of the comment block + numeric constants)
    - .planning/phases/17-visual-variants/17-PATTERNS.md §"`src/components/OrbShape.tsx` (component, request-response)" / "MIN/MID/MAX scale constants"
  </read_first>
  <action>
    Create new file `src/components/shapeConstants.ts` with EXACTLY this shape (replicating BreathingShape.tsx:13–20 comment + constants verbatim, plus an export for each):

    - File-leading JSDoc comment summarizing role: "Shared scale constants for Phase 17 visual variants (OrbShape / SquareShape / RingShape). MIN/MAX/MID values are kept in sync with --orb-scale-{min,max,mid} in src/styles/theme.css. If you edit one side, edit the other (IN-01)."
    - `export const MIN_SCALE = 0.58` — comment `// keep in sync with --orb-scale-min`
    - `export const MAX_SCALE = 1.0` — comment `// keep in sync with --orb-scale-max`
    - `export const MID_SCALE = (MIN_SCALE + MAX_SCALE) / 2` — comment `// 0.79 — D-06 reduced-motion fixed size; keep in sync with --orb-scale-mid`

    No other exports, no React imports (this is a pure-constants module so it can be imported by component + test files without test-side React-environment costs).
  </action>
  <verify>
    <automated>node -e "const m = require('./src/components/shapeConstants.ts'); /* tsx requires a loader; rely on tsc instead */" 2&gt;/dev/null; npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - File `src/components/shapeConstants.ts` exists.
    - `grep -cE "^export const (MIN|MAX|MID)_SCALE" src/components/shapeConstants.ts` returns exactly 3.
    - `npx tsc --noEmit` exits 0.
    - The file contains the literal substring `keep in sync with --orb-scale` (sync-with-css comment preserved).
  </acceptance_criteria>
  <done>
    `MIN_SCALE`, `MAX_SCALE`, `MID_SCALE` are importable as named exports from `./shapeConstants`; sync-with-css comment block is present; no React or test imports in the file.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Create `src/components/OrbShape.tsx` by verbatim-extracting Body + LeadIn from BreathingShape.tsx, adding `data-variant='orb'` on both roots</name>
  <files>src/components/OrbShape.tsx</files>
  <read_first>
    - src/components/BreathingShape.tsx (full file — lines 1–11 (imports + props), 33–41 (current dispatcher), 43–147 (Body), 160–224 (LeadIn) are the extraction source; post-Plan-01 className renames are baked in)
    - src/components/shapeConstants.ts (just created in Task 1)
    - .planning/phases/17-visual-variants/17-PATTERNS.md §"`src/components/OrbShape.tsx` (component, request-response)"
    - .planning/phases/17-visual-variants/17-CONTEXT.md §"Implementation Decisions" D-02 (verbatim extraction), D-04 (no idle guard in OrbShape), D-08 (typography identical), D-16 (data-variant render-local)
  </read_first>
  <action>
    Create new file `src/components/OrbShape.tsx` with the following structure (treat all references to specific lines below as source-of-truth pointers into the existing BreathingShape.tsx — copy the code verbatim where indicated):

    1. Imports:
       - `import type { SessionFrame } from '../domain/sessionMath'`
       - `import { usePrefersReducedMotion } from '../hooks/usePrefersReducedMotion'`
       - `import { MIN_SCALE, MAX_SCALE, MID_SCALE } from './shapeConstants'`

    2. Exported interface:
       - `export interface OrbShapeProps { frame: SessionFrame | null; leadInDigit?: 3 | 2 | 1 | null }`

    3. Exported function `OrbShape({ frame, leadInDigit }: OrbShapeProps)`:
       - Top-level dispatch:
         - If `leadInDigit != null`: `return <OrbLeadIn digit={leadInDigit} />`
         - Else: `return <OrbBody frame={frame!} />` (non-null assertion is safe — per D-04 the caller dispatcher ensures `frame !== null` when `leadInDigit == null`)
       - Add a leading comment noting D-04: "OrbShape does NOT own the idle null-return — the BreathingShape dispatcher (Plan 05) guards that and never invokes OrbShape with both frame=null AND leadInDigit=null."

    4. Internal function `OrbBody({ frame }: { frame: SessionFrame })` — extract VERBATIM from BreathingShape.tsx:43–147 (post-Plan-01 selector rename baked in). Identical to current source EXCEPT:
       - Add `data-variant="orb"` attribute on the root div (between `data-progress` and `className` — i.e. the same div that already carries `role="img"`, `aria-label`, `data-phase`, `data-progress`).
       - All other attributes, `style`, `className`, marker spans, inline gradients, phase-label span — VERBATIM. Preserve every comment block (Phase 5.1 D-20 four-edge-anchoring explanation, 260510-tc9 Bug 1 inner-ring explanation, etc.) verbatim.
       - The reduced-motion `liveScale` / `orbScale = reducedMotion ? MID_SCALE : liveScale` lines stay verbatim.
       - Keep the four-edge anchoring on the `.orb` div: `style={{ left: 0, right: 0, top: 0, bottom: 0, transform: 'translate3d(0,0,0) scale(${orbScale})' }}` (Phase 5.1 D-20 — NOT `inset-0`).
       - Keep the `-1.5px` four-edge offsets on the `.shape-marker--outer` span (Phase 5.1 D-12).
       - Keep `.shape-marker--inner` width/height set inline to `${(MIN_SCALE * 100).toFixed(2)}%` (260510-tc9 Bug 1 stacking order).
       - Keep the phase-label span's `text-5xl sm:text-6xl font-semibold tracking-tight z-10` classes + `color: frame.phase === 'in' ? 'var(--color-orb-in-text)' : 'var(--color-orb-out-text)'` inline style (D-08).

    5. Internal function `OrbLeadIn({ digit }: { digit: 1 | 2 | 3 })` — extract VERBATIM from BreathingShape.tsx:160–224 (post-Plan-01 selector rename baked in). Identical to current source EXCEPT:
       - Add `data-variant="orb"` attribute on the root div (next to `role="img"` and `aria-label`).
       - All other attributes, `style`, `className`, marker spans, inline `MID_SCALE` lock — VERBATIM.
       - Keep the digit span's `text-7xl sm:text-8xl font-semibold tracking-tight z-10` + `color: 'var(--color-orb-in-text)'` (D-08).

    Do NOT export `OrbBody` or `OrbLeadIn` — they remain module-private. Only `OrbShape` and `OrbShapeProps` are exported.

    Do NOT introduce any new color tokens or any hardcoded color classes — every color reference comes through `var(--color-*)` tokens just like the existing BreathingShape source (D-23 — THEME-UI-01 stays green).
  </action>
  <verify>
    <automated>npx tsc --noEmit &amp;&amp; npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - File `src/components/OrbShape.tsx` exists with `export function OrbShape` and `export interface OrbShapeProps`.
    - `grep -c "data-variant=\"orb\"" src/components/OrbShape.tsx` returns exactly 2 (one in OrbBody root, one in OrbLeadIn root).
    - `grep -c "shape-marker--outer\|shape-marker--inner" src/components/OrbShape.tsx` returns at least 4 (Body × 2 markers + LeadIn × 2 markers).
    - `grep -c "translate3d(0,0,0) scale" src/components/OrbShape.tsx` returns at least 2 (Body + LeadIn — GPU promotion preserved).
    - `grep -c "left: 0" src/components/OrbShape.tsx` returns at least 2 (Body + LeadIn `.orb` div four-edge anchoring).
    - `grep -c "left: '-1.5px'" src/components/OrbShape.tsx` returns at least 2 (Body + LeadIn outer-marker offset).
    - `grep -nE "text-(white|black|slate-|teal-)" src/components/OrbShape.tsx` returns ZERO matches (D-23 hardcoded-class guard).
    - `npx tsc --noEmit` exits 0.
    - `npm run lint` exits 0.
  </acceptance_criteria>
  <done>
    OrbShape.tsx is a verbatim extraction of the pre-Plan-02 Body + LeadIn with a single addition (`data-variant="orb"` on each root) and a single removal (idle null-return — owned by dispatcher in Plan 05). Functions OrbBody and OrbLeadIn are module-private; OrbShape + OrbShapeProps are the only exports.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Migrate Body + LeadIn + WR-03 structural tests from BreathingShape.test.tsx into new OrbShape.test.tsx; slim BreathingShape.test.tsx to dispatcher concerns; update BreathingShape.tsx to delegate to OrbShape</name>
  <files>src/components/OrbShape.test.tsx, src/components/BreathingShape.test.tsx, src/components/BreathingShape.tsx</files>
  <read_first>
    - src/components/BreathingShape.test.tsx (full file — every assertion will be classified migrate-vs-keep)
    - src/components/OrbShape.tsx (just created in Task 2 — confirms post-extraction render shape)
    - src/components/BreathingShape.tsx (post-Plan-01 source to be slimmed)
    - .planning/phases/17-visual-variants/17-PATTERNS.md §"#### `src/components/OrbShape.test.tsx` (NEW)" + §"#### `src/components/BreathingShape.test.tsx` (EDIT — slim to dispatch-only)"
    - .planning/phases/17-visual-variants/17-CONTEXT.md §"Implementation Decisions" D-02 (test migration), D-04 (dispatcher owns null-return)
  </read_first>
  <action>
    1. **Create `src/components/OrbShape.test.tsx`** by migrating the relevant blocks from BreathingShape.test.tsx:

       - Copy header imports: `import '@testing-library/jest-dom/vitest'`, `import { render, screen } from '@testing-library/react'`, `import { describe, expect, it } from 'vitest'`, plus `import { OrbShape } from './OrbShape'` (substituting BreathingShape → OrbShape), `import type { SessionFrame } from '../domain/sessionMath'`.
       - Copy the `sampleFrame` constant verbatim from BreathingShape.test.tsx:12–20.
       - Migrate the body-rendering smoke tests (the "renders the BreathingShapeBody" + "renders the lead-in digit X" + "lead-in keeps the orb at MID_SCALE" + "lead-in does not have data-phase or data-progress" + "lead-in renders digit with text-7xl class" tests at lines 28–86) into OrbShape.test.tsx. For each:
         - Change `render(<BreathingShape …/>)` → `render(<OrbShape …/>)`.
         - Drop the test "renders null when both frame and leadInDigit are absent" — that is now a DISPATCHER concern and stays in BreathingShape.test.tsx. OrbShape's caller guarantees one of `frame` or `leadInDigit` is non-null.
         - Drop the test "lead-in wins when both frame and leadInDigit are set (D-14 priority)" — that is ALSO a DISPATCHER concern (the dispatcher routes to OrbLeadIn before OrbBody). The internal lead-in priority inside OrbShape is structurally identical (top-level `if leadInDigit != null return LeadIn`) so a single smoke "renders lead-in digit when leadInDigit is set" already covers it.
       - Migrate the entire `describe('BreathingShape — Phase 5.1 Plan 04 WR-03 structural contract (D-24)', () => { ... })` block (lines 89–272) into OrbShape.test.tsx verbatim, EXCEPT:
         - Rename describe label to `describe('OrbShape — Phase 5.1 Plan 04 WR-03 structural contract (D-24, carried into OrbShape)', () => { ... })`.
         - Change every `render(<BreathingShape …/>)` → `render(<OrbShape …/>)`.
         - Class selectors are already `.shape-marker--*` post-Plan-01.
       - **Add new tests** for the `data-variant="orb"` attribute (per RESEARCH §"Common Pitfalls → Pitfall 1"):
         - `it('Body root carries data-variant="orb"', () => { const { container } = render(<OrbShape frame={sampleFrame} />); const root = container.querySelector('[role="img"]'); expect(root).toHaveAttribute('data-variant', 'orb') })`
         - `it('LeadIn root carries data-variant="orb"', () => { const { container } = render(<OrbShape frame={null} leadInDigit={2} />); const root = container.querySelector('[role="img"]'); expect(root).toHaveAttribute('data-variant', 'orb') })`

    2. **Slim `src/components/BreathingShape.test.tsx`** to only the dispatcher-level smoke:
       - KEEP: `'renders null when both frame and leadInDigit are absent'` (the idle null-return — Plan 05 will assert this with `variant="orb"` once the variant prop lands; in Plan 02 the BreathingShape signature still has only `{ frame, leadInDigit }` so this test stays as-is).
       - KEEP: `'lead-in wins when both frame and leadInDigit are set (D-14 priority)'` (dispatcher routes to OrbLeadIn first — the test stays valid because BreathingShape passes leadInDigit through to OrbShape, which itself routes lead-in first).
       - KEEP: one smoke each for `'renders the breathing body when frame is provided'` (use `screen.getByRole('img', { name: 'Breathing shape: In' })`) and `'renders the lead-in digit'` (use `screen.getByRole('img', { name: 'Lead-in: 3' })`).
       - REMOVE: all `'lead-in keeps the orb at MID_SCALE'`, `'lead-in does not have data-phase or data-progress'`, `'lead-in renders digit with text-7xl class'` tests (migrated to OrbShape.test.tsx).
       - REMOVE: the entire `'Phase 5.1 Plan 04 WR-03 structural contract'` describe block (migrated to OrbShape.test.tsx).

    3. **Refactor `src/components/BreathingShape.tsx`** to delegate to OrbShape:

       Replace the entire file body (post-Plan-01 state, ~225 LOC) with this slimmed pass-through shape (~25 LOC):

       - Imports:
         - `import type { SessionFrame } from '../domain/sessionMath'`
         - `import { OrbShape } from './OrbShape'`
       - Interface: `export interface BreathingShapeProps { frame: SessionFrame | null; leadInDigit?: 3 | 2 | 1 | null }`
       - Function:
         ```
         export function BreathingShape({ frame, leadInDigit }: BreathingShapeProps) {
           if (frame === null && leadInDigit == null) {
             return null
           }
           return <OrbShape frame={frame} leadInDigit={leadInDigit} />
         }
         ```

       Preserve the file-leading comment block that explains the dispatcher's role (idle null-return owned here per D-04, all rendering delegated). The MIN/MAX/MID constants are now imported from `shapeConstants.ts` — they are NOT used in this file post-slim, but mentioning them in a comment for the IN-01 sync-with-css rule is fine.

       After this refactor:
       - BreathingShape.tsx is ~25 LOC.
       - It does NOT use `usePrefersReducedMotion` directly (OrbShape's body owns the subscription).
       - It does NOT use the scale constants directly (OrbShape's body imports them from shapeConstants.ts).
       - It does NOT yet have the `variant` prop — Plan 05 adds that.

    Per D-17 per-commit green-gate: this 3-file edit (OrbShape.test.tsx creation, BreathingShape.test.tsx slim, BreathingShape.tsx slim) lands in a single commit so the suite stays green.
  </action>
  <verify>
    <automated>npm test -- --run OrbShape BreathingShape &amp;&amp; npx tsc --noEmit &amp;&amp; npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - File `src/components/OrbShape.test.tsx` exists with `import { OrbShape } from './OrbShape'`.
    - `grep -c "render(<OrbShape" src/components/OrbShape.test.tsx` returns at least 8 (5 smoke tests + 2 data-variant tests + WR-03 block × multiple — exact count depends on migrated suite size; the WR-03 block alone contains ≥10 render calls).
    - `grep -cE "describe\(.Phase 5.1 Plan 04 WR-03" src/components/OrbShape.test.tsx` returns exactly 1 (WR-03 block migrated successfully).
    - `grep -cE "describe\(.Phase 5.1 Plan 04 WR-03" src/components/BreathingShape.test.tsx` returns exactly 0 (WR-03 block removed from BreathingShape.test.tsx).
    - `grep -cE "data-variant.*orb" src/components/OrbShape.test.tsx` returns at least 2 (one assertion each for Body + LeadIn root).
    - `wc -l src/components/BreathingShape.tsx` is ≤ 40 (slimmed from 224 LOC; ~25 LOC body + comment block + imports).
    - `grep -c "import { OrbShape } from './OrbShape'" src/components/BreathingShape.tsx` returns exactly 1.
    - `npm test -- --run OrbShape` passes (the migrated suite is green against the new OrbShape.tsx).
    - `npm test -- --run BreathingShape` passes (the slimmed BreathingShape.test.tsx is green against the new pass-through BreathingShape.tsx).
    - `npx tsc --noEmit && npm run lint && npm run build && npm test -- --run` exits 0 at commit boundary (D-17 per-commit green-gate).
  </acceptance_criteria>
  <done>
    OrbShape.tsx owns the orb rendering (Body + LeadIn + the data-variant='orb' attribute), OrbShape.test.tsx owns the structural assertions (including the full Phase 5.1 WR-03 contract block), BreathingShape.tsx is a ~25-LOC delegate to OrbShape, BreathingShape.test.tsx contains only dispatcher-level smoke (idle null-return + lead-in-priority). Full suite green at commit boundary.
  </done>
</task>

</tasks>

<verification>
**Overall green-gate after Plan 02:** `npx tsc --noEmit && npm run lint && npm run build && npm test -- --run` exits 0.

**Carry-forward guard checks:**
- `src/styles/theme.contrast.test.ts` — THEME-05 ≥ 1.5 contrast guard stays green (no token edits).
- `src/styles/theme.no-hardcoded-classes.test.ts` — THEME-UI-01 guard stays green (OrbShape is a verbatim extraction; same token bindings).

**VARIANT-02 zero-regression diff check:** git diff between the pre-Plan-02 BreathingShape Body/LeadIn output and the post-Plan-02 OrbShape Body/LeadIn output shows ONLY:
- Function-name renames (`BreathingShapeBody` → `OrbBody`, `BreathingShapeLeadIn` → `OrbLeadIn`).
- Constants moved to shapeConstants.ts and re-imported.
- `data-variant="orb"` added on the two root divs.
- (Plan 01 already applied the `.shape-marker--*` className rename.)

Visually: a user who never opens the picker sees pixel-identical orb behavior.
</verification>

<success_criteria>
- `src/components/shapeConstants.ts` exports `MIN_SCALE`, `MAX_SCALE`, `MID_SCALE` with sync-with-css comments.
- `src/components/OrbShape.tsx` exports `OrbShape` (function) + `OrbShapeProps` (interface); contains module-private `OrbBody` + `OrbLeadIn`.
- OrbShape Body + LeadIn output is byte-identical to pre-Plan-02 BreathingShape Body + LeadIn EXCEPT for the `data-variant="orb"` attribute additions.
- `src/components/OrbShape.test.tsx` exists with the migrated body + lead-in + Phase 5.1 WR-03 structural assertions; class selectors are `.shape-marker--*`.
- `src/components/BreathingShape.tsx` is ≤ 40 LOC and delegates every non-idle call to OrbShape.
- `src/components/BreathingShape.test.tsx` contains only dispatcher-level smoke tests.
- `tsc && lint && build && test` exits 0 at commit boundary.
</success_criteria>

<output>
After completion, create `.planning/phases/17-visual-variants/17-02-SUMMARY.md` noting:
- The shapeConstants.ts extraction (3 numeric exports + sync-with-css comment).
- The verbatim OrbShape.tsx creation (Body + LeadIn + the single `data-variant="orb"` addition per root).
- The migration of body + lead-in + Phase 5.1 WR-03 tests into OrbShape.test.tsx.
- The pass-through BreathingShape.tsx slim (~225 LOC → ~25 LOC).
- Confirmation that THEME-UI-01 and THEME-05 guards stayed green.
- Confirmation that no `variant` prop was added in this plan (Plan 05 owns that).
</output>
