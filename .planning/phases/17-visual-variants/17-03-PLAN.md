---
phase: 17-visual-variants
plan: 03
type: execute
wave: 3
depends_on:
  - 17-02
files_modified:
  - src/components/SquareShape.tsx
  - src/components/SquareShape.test.tsx
  - src/components/RingShape.tsx
  - src/components/RingShape.test.tsx
  - src/styles/theme.css
autonomous: true
requirements:
  - VARIANT-01
  - VARIANT-04
  - VARIANT-05
user_setup: []
tags:
  - shape-variant
  - phase-17
  - css-override

must_haves:
  truths:
    - "`SquareShape({ frame, leadInDigit })` renders a rounded-square (border-radius 18%) body that scales 0.58 → 1.0 over phase progress, identical kinematics envelope to OrbShape, with `data-variant='square'` on both Body and LeadIn roots"
    - "`RingShape({ frame, leadInDigit })` renders an annulus (hollow center via radial-gradient at 35% inner stop on `.orb-layer--in/--out`) that scales 0.58 → 1.0 over phase progress, with `data-variant='ring'` on both Body and LeadIn roots"
    - "Both Square and Ring re-use `MIN_SCALE`, `MAX_SCALE`, `MID_SCALE` from `./shapeConstants` (no constant duplication)"
    - "VARIANT-04: Both variants apply reduced-motion via the SAME `usePrefersReducedMotion` hook + MID_SCALE substitution as OrbShape; the `.shape-marker--inner { display: none }` reduced-motion suppression rule applies automatically through class reuse (no per-variant @media block)"
    - "VARIANT-05: Both variants render the 3-2-1 lead-in digit through the existing `leadInDigit` prop path at `text-7xl/8xl` size with `var(--color-orb-in-text)` color — identical to OrbShape lead-in"
    - "theme.css contains 4 new CSS rules: `[data-variant='square'] .shape-marker--outer`, `[data-variant='square'] .shape-marker--inner`, `[data-variant='ring'] .shape-marker--outer`, `[data-variant='ring'] .shape-marker--inner`, plus 2 radial-gradient overrides `[data-variant='ring'] .orb-layer--in` and `[data-variant='ring'] .orb-layer--out`"
    - "Zero new CSS variables / color tokens (D-13) — `--color-orb-in/out-*` reused verbatim across all 3 variants"
    - "Phase 5.1 D-20/D-22 four-edge anchoring + `-1.5px` outer offsets + `translate3d(0,0,0) scale(...)` GPU promotion preserved on every Body and LeadIn render site"
    - "`tsc && lint && build && test` exits 0 after each commit"
  artifacts:
    - path: "src/components/SquareShape.tsx"
      provides: "SquareShape function + SquareShapeProps interface; Body + LeadIn renders with `data-variant='square'`, `rounded-[18%]` on .orb div, `rounded-[inherit]` on gradient layers; same kinematics + reduced-motion lock as OrbShape"
      contains: "export function SquareShape"
    - path: "src/components/SquareShape.test.tsx"
      provides: "Body + LeadIn render smoke + scale-interpolation + reduced-motion + leadInDigit + `data-variant='square'` attribute presence + four-edge anchoring assertions"
      contains: "SquareShape"
    - path: "src/components/RingShape.tsx"
      provides: "RingShape function + RingShapeProps interface; Body + LeadIn renders with `data-variant='ring'`, `rounded-full` host + `rounded-full` gradient layers (radial-gradient override comes from CSS); same kinematics + reduced-motion lock as OrbShape"
      contains: "export function RingShape"
    - path: "src/components/RingShape.test.tsx"
      provides: "Body + LeadIn render smoke + scale-interpolation + reduced-motion + leadInDigit + `data-variant='ring'` attribute presence + four-edge anchoring assertions"
      contains: "RingShape"
    - path: "src/styles/theme.css"
      provides: "Six new CSS rules covering `[data-variant='square'] .shape-marker--{outer,inner}` border-radius overrides and `[data-variant='ring'] .shape-marker--{outer,inner}` border-width + `.orb-layer--{in,out}` radial-gradient overrides; existing `@media reduced-motion` block unchanged"
      contains: "[data-variant='square'] .shape-marker--outer"
  key_links:
    - from: "src/components/SquareShape.tsx (SquareBody root div)"
      to: "src/styles/theme.css ([data-variant='square'] selectors)"
      via: "data-variant HTML attribute"
      pattern: "data-variant=\"square\""
    - from: "src/components/RingShape.tsx (RingBody .orb-layer--in/out spans)"
      to: "src/styles/theme.css ([data-variant='ring'] .orb-layer--in/--out radial-gradient override)"
      via: "data-variant on ancestor div + class on descendant span"
      pattern: "\\[data-variant='ring'\\] \\.orb-layer--(in|out)"
    - from: "src/components/{Square,Ring}Shape.tsx"
      to: "src/components/shapeConstants.ts"
      via: "named import of MIN_SCALE/MAX_SCALE/MID_SCALE"
      pattern: "import \\{ MIN_SCALE, MAX_SCALE, MID_SCALE \\} from './shapeConstants'"
---

<objective>
Create the two alternate variant render components (`SquareShape`, `RingShape`) that mirror OrbShape's prop interface and reuse OrbShape's kinematics + reduced-motion contract via shared CSS classes. Add the per-variant CSS overrides in `src/styles/theme.css` that give Square its rounded-corner geometry and give Ring its hollow center.

Per D-05: Square = scale-only kinematics with fixed `border-radius: 18%` (planner picks 18% as the starting value per UI-SPEC §"Per-variant geometry"; operator may revise after dev-server smoke test in Plan 06 phase-close UAT).

Per D-06: Ring = scale-only kinematics with hollow center via radial-gradient on `.orb-layer--in/--out` (Option D per RESEARCH — `radial-gradient(circle at center, transparent 0%, transparent 35%, var(--color-orb-{in,out}-from) 36%, var(--color-orb-{in,out}-to) 100%)`).

Per D-13: Zero new CSS variables / color tokens. Square + Ring reuse `--color-orb-in/out-*` verbatim. Per-variant differentiation is GEOMETRIC ONLY (border-radius for Square, inner-stop for Ring).

Per D-14: Reduced-motion contract is identical across all 3 variants via class reuse — the `@media (prefers-reduced-motion: reduce) .shape-marker--inner { display: none }` rule applies automatically; no new per-variant `@media` blocks are added.

Output: Two new shape components + their tests + six new CSS rules in theme.css (4 marker overrides + 2 radial-gradient overrides for Ring). All 5 palettes continue to satisfy THEME-05 ≥ 1.5 contrast.
</objective>

<execution_context>
@$HOME/.claude-personal-personal/get-shit-done/workflows/execute-plan.md
@$HOME/.claude-personal-personal/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/17-visual-variants/17-CONTEXT.md
@.planning/phases/17-visual-variants/17-PATTERNS.md
@.planning/phases/17-visual-variants/17-UI-SPEC.md
@.planning/phases/17-visual-variants/17-VALIDATION.md
@src/components/OrbShape.tsx
@src/components/shapeConstants.ts
@src/styles/theme.css

<interfaces>
<!-- Post-Plan-02 OrbShape.tsx exposes the analog structure SquareShape + RingShape must mirror: -->

OrbShape.tsx (post-Plan-02):
  - `export interface OrbShapeProps { frame: SessionFrame | null; leadInDigit?: 3 | 2 | 1 | null }`
  - `export function OrbShape({ frame, leadInDigit }: OrbShapeProps)` — top-level dispatch (lead-in vs body), no idle null-return
  - Module-private `OrbBody({ frame })`:
    - Imports `usePrefersReducedMotion` + `MIN_SCALE/MAX_SCALE/MID_SCALE` from shapeConstants
    - `progress = clamp01(frame.phaseProgress)`
    - `liveScale = frame.phase === 'in' ? MIN_SCALE + progress * (MAX_SCALE - MIN_SCALE) : MAX_SCALE - progress * (MAX_SCALE - MIN_SCALE)`
    - `orbScale = reducedMotion ? MID_SCALE : liveScale`
    - Root div: `role="img" aria-label data-variant="orb" data-phase={frame.phase} data-progress={progress.toFixed(3)} className="relative mx-auto my-12 grid place-items-center" style={{ width: 'var(--orb-size)', height: 'var(--orb-size)' }}`
    - Outer marker span: `className="shape-marker--outer absolute rounded-full border-solid" style={{ left:'-1.5px', top:'-1.5px', right:'-1.5px', bottom:'-1.5px' }}`
    - `.orb` host div: `className="orb absolute rounded-full motion-reduce:transition-none" style={{ left:0, right:0, top:0, bottom:0, transform: 'translate3d(0,0,0) scale(${orbScale})' }}` (NOT inset-0 per D-20)
    - Two gradient layers: `className="orb-layer--in absolute inset-0 rounded-full" aria-hidden="true"` and same for `.orb-layer--out`
    - Inner marker span: `className="shape-marker--inner absolute left-1/2 top-1/2 -translate-x-1/2 -translate-y-1/2 rounded-full border-solid" style={{ width: '${(MIN_SCALE*100).toFixed(2)}%', height: '${(MIN_SCALE*100).toFixed(2)}%' }}`
    - Phase label span: `className="relative z-10 text-5xl font-semibold tracking-tight text-[var(--color-breathing-accent-strong)] sm:text-6xl" style={{ color: frame.phase === 'in' ? 'var(--color-orb-in-text)' : 'var(--color-orb-out-text)' }}`
  - Module-private `OrbLeadIn({ digit })`: analogous structure, MID_SCALE locked, `.orb-layer--in` only (no `.orb-layer--out`), digit at `text-7xl sm:text-8xl` with `color: 'var(--color-orb-in-text)'`

Square deltas vs Orb (per RESEARCH §"Square body recipe" + UI-SPEC §"Per-variant geometry"):
  1. Replace `data-variant="orb"` → `data-variant="square"` on both roots.
  2. Replace `.orb` className `rounded-full` → `rounded-[18%]`.
  3. Replace `.orb-layer--in` className `rounded-full` → `rounded-[inherit]` (inherits the 18% from parent .orb).
  4. Replace `.orb-layer--out` className `rounded-full` → `rounded-[inherit]`.
  5. Remove `rounded-full` from `.shape-marker--outer` and `.shape-marker--inner` spans — leave only `absolute border-solid` (etc.); the `[data-variant='square'] .shape-marker--{outer,inner} { border-radius: 18% }` CSS rule will set the radius.
  Everything else (style props, MIN/MID/MAX kinematics, reduced-motion lock, four-edge anchoring, `-1.5px` offsets, GPU `translate3d`, phase-label classes/colors, lead-in digit sizes/colors) — VERBATIM.

Ring deltas vs Orb (per RESEARCH §"Ring body recipe" + UI-SPEC §"Per-variant geometry" + Pitfall 8):
  1. Replace `data-variant="orb"` → `data-variant="ring"` on both roots.
  2. KEEP `.orb` className `rounded-full` (ring outer edge is circular).
  3. KEEP `.orb-layer--in` and `.orb-layer--out` classNames `rounded-full` (circular layers; the radial-gradient that creates the hollow center is applied entirely via the `[data-variant='ring'] .orb-layer--in/--out { background: radial-gradient(...) }` CSS override — no TSX `background` inline override).
  4. KEEP `rounded-full` on `.shape-marker--outer` and `.shape-marker--inner` (ring markers are concentric thin circles). The `[data-variant='ring'] .shape-marker--{outer,inner} { border-width: 1px }` CSS rule will thin them.
  Everything else (style props, MIN/MID/MAX kinematics, reduced-motion lock, four-edge anchoring, `-1.5px` offsets, GPU `translate3d`, phase-label classes/colors, lead-in digit sizes/colors) — VERBATIM.

theme.css additions (append AFTER the existing `[data-phase='out'] .orb-ring--inner` / now `.shape-marker--inner` rule near line 388, and BEFORE the `@media (prefers-reduced-motion: reduce)` block at line 430):
  ```
  /* Phase 17 D-05/D-15: Square variant — rounded-corner marker outlines */
  [data-variant='square'] .shape-marker--outer { border-radius: 18%; }
  [data-variant='square'] .shape-marker--inner { border-radius: 18%; }

  /* Phase 17 D-06/D-15: Ring variant — thinner concentric markers + hollow-center body */
  [data-variant='ring'] .shape-marker--outer { border-width: 1px; }
  [data-variant='ring'] .shape-marker--inner { border-width: 1px; }
  [data-variant='ring'] .orb-layer--in {
    background: radial-gradient(
      circle at center,
      transparent 0%, transparent 35%,
      var(--color-orb-in-from) 36%, var(--color-orb-in-to) 100%
    );
  }
  [data-variant='ring'] .orb-layer--out {
    background: radial-gradient(
      circle at center,
      transparent 0%, transparent 35%,
      var(--color-orb-out-from) 36%, var(--color-orb-out-to) 100%
    );
  }
  ```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create SquareShape.tsx + SquareShape.test.tsx with rounded-corner geometry deltas; reuse OrbShape's kinematics + reduced-motion contract verbatim</name>
  <files>src/components/SquareShape.tsx, src/components/SquareShape.test.tsx</files>
  <behavior>
    - Test 1: SquareShape renders Body when frame is provided and leadInDigit is null; root `[role="img"]` has `data-variant='square'`, `data-phase` = frame.phase.
    - Test 2: SquareShape renders LeadIn when leadInDigit is set; root has `data-variant='square'`, no `data-phase`, digit text matches.
    - Test 3: Body `.orb` div has class `rounded-[18%]` (NOT `rounded-full`), four-edge anchoring `left:0 right:0 top:0 bottom:0` (NOT `inset-0`), and `style.transform` contains `translate3d(0,0,0) scale(...)`.
    - Test 4: Body `.orb-layer--in` and `.orb-layer--out` spans have class `rounded-[inherit]` (NOT `rounded-full`).
    - Test 5: Body inner marker `.shape-marker--inner` does NOT have `rounded-full` class (it inherits border-radius from CSS `[data-variant='square']` rule); width/height are `${(MIN_SCALE*100).toFixed(2)}%`.
    - Test 6: Body outer marker `.shape-marker--outer` has explicit four-edge offsets `left:'-1.5px' top:'-1.5px' right:'-1.5px' bottom:'-1.5px'` (Phase 5.1 D-12 mirror).
    - Test 7: Reduced-motion mock returns true → `.orb` style transform contains `scale(0.79)` (MID_SCALE substitution); `liveScale` interpolation is bypassed.
    - Test 8: VARIANT-05 — LeadIn renders the digit in a `text-7xl` (mobile) span with `color: var(--color-orb-in-text)` style.
    - Test 9: VARIANT-04 — Body and LeadIn root divs both carry `motion-reduce:transition-none` class on the `.orb` host (preserves Phase 5.1 reduced-motion contract).
  </behavior>
  <read_first>
    - src/components/OrbShape.tsx (post-Plan-02 — the structural template to mirror)
    - src/components/shapeConstants.ts (imports source)
    - src/components/OrbShape.test.tsx (post-Plan-02 — test scaffolding template)
    - .planning/phases/17-visual-variants/17-PATTERNS.md §"`src/components/SquareShape.tsx`" + §"`src/components/SquareShape.test.tsx` + `RingShape.test.tsx`"
    - .planning/phases/17-visual-variants/17-CONTEXT.md §"Implementation Decisions" D-05 (Square kinematics), D-07 (markers per variant), D-13 (token reuse), D-14 (reduced-motion class reuse), D-16 (data-variant render-local)
    - .planning/phases/17-visual-variants/17-UI-SPEC.md §"Per-variant geometry" table (Square row)
  </read_first>
  <action>
    1. Create `src/components/SquareShape.tsx`:

       - Imports identical to OrbShape: `import type { SessionFrame } from '../domain/sessionMath'`, `import { usePrefersReducedMotion } from '../hooks/usePrefersReducedMotion'`, `import { MIN_SCALE, MAX_SCALE, MID_SCALE } from './shapeConstants'`.
       - Interface: `export interface SquareShapeProps { frame: SessionFrame | null; leadInDigit?: 3 | 2 | 1 | null }`.
       - Function `SquareShape({ frame, leadInDigit })`:
         - Top-level dispatch identical to OrbShape: `if (leadInDigit != null) return <SquareLeadIn digit={leadInDigit} />`; else `return <SquareBody frame={frame!} />`. No idle null-return (D-04 — dispatcher in Plan 05 owns it).
       - Module-private `SquareBody({ frame })`: copy OrbBody's body verbatim from OrbShape.tsx with the following 5 deltas (and ONLY these 5):
         1. Root div: `data-variant="square"` instead of `data-variant="orb"`.
         2. `.orb` host div className: `"orb absolute rounded-[18%] motion-reduce:transition-none"` (was `rounded-full`).
         3. `.orb-layer--in` span className: `"orb-layer--in absolute inset-0 rounded-[inherit]"` (was `rounded-full`).
         4. `.orb-layer--out` span className: `"orb-layer--out absolute inset-0 rounded-[inherit]"` (was `rounded-full`).
         5. `.shape-marker--outer` span className: `"shape-marker--outer absolute border-solid"` (DROP `rounded-full` — CSS attribute selector handles the radius). Same for `.shape-marker--inner`: `"shape-marker--inner absolute left-1/2 top-1/2 -translate-x-1/2 -translate-y-1/2 border-solid"` (DROP `rounded-full`).
         All other lines (style props, scale math, phase-label span + its classes + token-bound color, comments documenting D-20/D-22/D-12) — VERBATIM from OrbShape.tsx.
       - Module-private `SquareLeadIn({ digit })`: copy OrbLeadIn's body verbatim with the SAME 5 deltas applied to the lead-in render site (D-22 — both render sites must match). `data-variant="square"` on root, `rounded-[18%]` on `.orb` host, `rounded-[inherit]` on `.orb-layer--in` (LeadIn does NOT render `.orb-layer--out` per Phase 3 D-14 — preserve that), drop `rounded-full` from markers. Digit span stays at `text-7xl sm:text-8xl` with `color: 'var(--color-orb-in-text)'`.

       Do NOT export `SquareBody` or `SquareLeadIn` — module-private. Only `SquareShape` + `SquareShapeProps`.

       Do NOT introduce any hardcoded color classes (D-23). All color references stay token-bound.

    2. Create `src/components/SquareShape.test.tsx`:

       - Header imports mirror OrbShape.test.tsx: `import '@testing-library/jest-dom/vitest'`, `import { render, screen } from '@testing-library/react'`, `import { afterEach, describe, expect, it, vi } from 'vitest'`, `import { SquareShape } from './SquareShape'`, `import type { SessionFrame } from '../domain/sessionMath'`, `import * as prm from '../hooks/usePrefersReducedMotion'`.
       - `sampleFrame` constant verbatim from OrbShape.test.tsx (the SessionFrame stub).
       - One `describe('SquareShape', () => { ... })` block with:
         - Smoke: `'renders the SquareBody when frame is provided'` (assert `screen.getByRole('img', { name: 'Breathing shape: In' })` is visible).
         - Smoke: `'renders the lead-in digit 3 / 2 / 1'` (3 separate `it()` or one `it.each`).
         - Attribute: `'Body root carries data-variant="square"'`.
         - Attribute: `'LeadIn root carries data-variant="square"'`.
         - Geometry: `'.orb div has rounded-[18%] class (not rounded-full)'` — assert via `container.querySelector('.orb').classList.contains('rounded-[18%]')` is true and `classList.contains('rounded-full')` is false.
         - Geometry: `'.orb-layer--in has rounded-[inherit] class'` and `'.orb-layer--out has rounded-[inherit] class'`.
         - Geometry: `'.shape-marker--outer does NOT have rounded-full class (CSS [data-variant=\"square\"] selector handles radius)'`.
         - Geometry: `'.shape-marker--inner does NOT have rounded-full class'`.
         - Anchoring: `'.shape-marker--outer has explicit four-edge offsets (-1.5px)'` — mirror OrbShape.test.tsx's WR-03 outer-ring offset assertion verbatim with class selector rename.
         - Anchoring: `'.orb div uses four-edge anchoring (left:0 right:0 top:0 bottom:0), not inset-0'` — mirror OrbShape.test.tsx's WR-03 `.orb` four-edge assertion.
         - Kinematics: `'.orb style.transform contains translate3d(0,0,0) scale(...) at idle frame'`.
         - Reduced-motion: `'reduced-motion locks orb at MID_SCALE = 0.79'` — `vi.spyOn(prm, 'usePrefersReducedMotion').mockReturnValue(true)`, then assert `container.querySelector('.orb').style.transform` contains `scale(0.79)`. Restore mock in `afterEach`.
         - VARIANT-04: `'lead-in keeps the orb at MID_SCALE (scale(0.79)) regardless of reduced-motion state'` — mirror OrbShape.test.tsx.
         - VARIANT-05: `'lead-in digit renders at text-7xl with color var(--color-orb-in-text)'`.

       Use `it.each(['orb', 'square'])` — actually, do NOT cross-parameterize; this is the SquareShape suite. Per-variant `it.each` parameterization belongs to a hypothetical cross-variant test file which is NOT in scope here.

  </action>
  <verify>
    <automated>npm test -- --run SquareShape</automated>
  </verify>
  <acceptance_criteria>
    - File `src/components/SquareShape.tsx` exists with `export function SquareShape` and `export interface SquareShapeProps`.
    - `grep -c "data-variant=\"square\"" src/components/SquareShape.tsx` returns exactly 2 (Body root + LeadIn root).
    - `grep -c "rounded-\\[18%\\]" src/components/SquareShape.tsx` returns at least 2 (Body `.orb` + LeadIn `.orb`).
    - `grep -c "rounded-\\[inherit\\]" src/components/SquareShape.tsx` returns at least 3 (Body `.orb-layer--in` + Body `.orb-layer--out` + LeadIn `.orb-layer--in`).
    - `grep -c "shape-marker--outer absolute border-solid" src/components/SquareShape.tsx` returns at least 2 (Body + LeadIn — no `rounded-full`).
    - `grep -c "translate3d(0,0,0) scale" src/components/SquareShape.tsx` returns at least 2 (Body + LeadIn GPU promotion preserved).
    - `grep -c "left: 0" src/components/SquareShape.tsx` returns at least 2 (Body + LeadIn `.orb` four-edge anchoring).
    - `grep -c "left: '-1.5px'" src/components/SquareShape.tsx` returns at least 2 (Body + LeadIn outer-marker offsets).
    - `grep -nE "text-(white|black|slate-|teal-)" src/components/SquareShape.tsx` returns ZERO (D-23 hardcoded-class guard).
    - `npm test -- --run SquareShape` exits 0 (all SquareShape tests pass).
    - `npx tsc --noEmit && npm run lint` exits 0.
  </acceptance_criteria>
  <done>
    SquareShape.tsx implements the 5 geometric deltas vs OrbShape; SquareShape.test.tsx asserts geometry, attribute presence, kinematics, reduced-motion lock, and lead-in digit rendering. Full SquareShape suite green.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create RingShape.tsx + RingShape.test.tsx with hollow-center via radial-gradient (CSS-only); reuse OrbShape's kinematics + markers</name>
  <files>src/components/RingShape.tsx, src/components/RingShape.test.tsx</files>
  <behavior>
    - Test 1: RingShape renders Body when frame is provided; root `[role="img"]` has `data-variant='ring'`, `data-phase` = frame.phase.
    - Test 2: RingShape renders LeadIn when leadInDigit is set; root has `data-variant='ring'`, no `data-phase`, digit text matches.
    - Test 3: Body `.orb` div has class `rounded-full` (ring outer edge is circular — UNLIKE Square).
    - Test 4: Body `.orb-layer--in` and `.orb-layer--out` spans have `rounded-full` class (the radial-gradient hollow center comes from CSS `[data-variant='ring']` overrides — no inline override).
    - Test 5: Body markers `.shape-marker--outer` and `.shape-marker--inner` retain `rounded-full` class (concentric circular thin rings — distinct from Square's marker treatment).
    - Test 6: Body inner marker `.shape-marker--inner` width/height are `${(MIN_SCALE*100).toFixed(2)}%` (same as Orb).
    - Test 7: Body outer marker has explicit four-edge `-1.5px` offsets.
    - Test 8: `.orb` div uses four-edge anchoring (`left: 0; right: 0; top: 0; bottom: 0`), NOT `inset-0`.
    - Test 9: Reduced-motion mock true → `.orb.style.transform` contains `scale(0.79)`.
    - Test 10: VARIANT-05 — LeadIn digit at `text-7xl sm:text-8xl` with `color: var(--color-orb-in-text)`.
    - Test 11: VARIANT-04 — `.orb` host has `motion-reduce:transition-none` class on both Body and LeadIn.
  </behavior>
  <read_first>
    - src/components/OrbShape.tsx (post-Plan-02 — structural template)
    - src/components/SquareShape.tsx (just created in Task 1 — reference for delta application style)
    - src/components/shapeConstants.ts
    - .planning/phases/17-visual-variants/17-PATTERNS.md §"`src/components/RingShape.tsx`"
    - .planning/phases/17-visual-variants/17-CONTEXT.md §"Implementation Decisions" D-06 (Ring kinematics + hollow center), D-07 (Ring markers stay circular thin), D-13 (token reuse), D-15 (Ring `border-width: 1px` override only)
    - .planning/phases/17-visual-variants/17-UI-SPEC.md §"Per-variant geometry" table (Ring row) + §"Inline Shape Swatches" (Pitfall 8)
  </read_first>
  <action>
    1. Create `src/components/RingShape.tsx`:

       - Imports identical to OrbShape + SquareShape.
       - Interface: `export interface RingShapeProps { frame: SessionFrame | null; leadInDigit?: 3 | 2 | 1 | null }`.
       - Function `RingShape({ frame, leadInDigit })`: top-level dispatch identical to OrbShape/SquareShape.
       - Module-private `RingBody({ frame })`: copy OrbBody's body verbatim with the following SINGLE delta (the radial-gradient hollow center is applied entirely in CSS, NOT TSX, so the TSX deltas are minimal):
         1. Root div: `data-variant="ring"` instead of `data-variant="orb"`.
         All other classNames stay UNCHANGED — `.orb` stays `rounded-full`; `.orb-layer--in`/`--out` stay `rounded-full`; `.shape-marker--outer`/`--inner` stay `rounded-full border-solid`; markers stay at width/height = MIN_SCALE * 100% for inner; outer marker stays at `-1.5px` four-edge offsets. The hollow-center effect comes from the `[data-variant='ring'] .orb-layer--in/--out { background: radial-gradient(...) }` CSS override added in Task 3.
         The `[data-variant='ring'] .shape-marker--outer/--inner { border-width: 1px }` CSS override (Task 3) thins the markers — no TSX change needed.
       - Module-private `RingLeadIn({ digit })`: same single delta on the LeadIn root — `data-variant="ring"`. All other TSX is verbatim from OrbLeadIn.

       Do NOT inline-override `background` on the gradient layers in TSX — CSS handles it (RESEARCH §"Ring body recipe Option D"). The TSX file should NOT contain any `background:` style references for the layers; it should NOT contain any `radial-gradient` literal — that lives in theme.css.

       Do NOT export RingBody/RingLeadIn — only RingShape + RingShapeProps.

       Do NOT introduce any hardcoded color classes (D-23).

    2. Create `src/components/RingShape.test.tsx`:

       - Header imports + sampleFrame constant — identical to SquareShape.test.tsx (substituting `SquareShape` → `RingShape`).
       - One `describe('RingShape', () => { ... })` block with tests mirroring Task 1's behavior list:
         - Smoke: renders Body / renders LeadIn 3 / 2 / 1.
         - Attribute: `'Body root carries data-variant="ring"'`, `'LeadIn root carries data-variant="ring"'`.
         - Geometry: `'.orb div retains rounded-full class (ring outer edge is circular)'`.
         - Geometry: `'.orb-layer--in retains rounded-full class (radial-gradient applied via CSS, not classes)'` and same for `--out`.
         - Geometry: `'.shape-marker--outer retains rounded-full class (concentric circular thin ring)'` and same for `--inner`.
         - Anchoring: `.shape-marker--outer` has `-1.5px` four-edge offsets (D-12 mirror).
         - Anchoring: `.orb` uses `left: 0; right: 0; top: 0; bottom: 0` (D-20 mirror).
         - Kinematics: `.orb` style.transform contains `translate3d(0,0,0) scale(...)`.
         - Reduced-motion: scale(0.79) under `usePrefersReducedMotion` mock returning true.
         - VARIANT-05: lead-in digit at `text-7xl sm:text-8xl` with `var(--color-orb-in-text)` color.
         - VARIANT-04: `motion-reduce:transition-none` on `.orb` host (Body + LeadIn).
         - (DO NOT test the actual radial-gradient background — that lives in CSS and would require a computed-style check in jsdom which is unreliable; the CSS-side coverage lives in Task 3's lint + visual UAT.)

       - One additional assertion confirming the TSX did NOT inline-override background (negative assertion — catch accidental drift):
         - `'no inline background style on .orb-layer--in/--out (CSS owns the radial-gradient)'` — `expect(container.querySelector('.orb-layer--in').style.background).toBe('')`.
  </action>
  <verify>
    <automated>npm test -- --run RingShape</automated>
  </verify>
  <acceptance_criteria>
    - File `src/components/RingShape.tsx` exists with `export function RingShape` and `export interface RingShapeProps`.
    - `grep -c "data-variant=\"ring\"" src/components/RingShape.tsx` returns exactly 2 (Body + LeadIn roots).
    - `grep -c "rounded-full" src/components/RingShape.tsx` returns at least 8 (Body: `.orb` + 2 gradient layers + 2 markers = 5; LeadIn: `.orb` + 1 gradient layer + 2 markers = 4; minimum 5 verified — but exact count depends on layout. Acceptable floor: ≥ 7).
    - `grep -nE "radial-gradient|background:" src/components/RingShape.tsx` returns ZERO matches (CSS owns the override).
    - `grep -c "translate3d(0,0,0) scale" src/components/RingShape.tsx` returns at least 2.
    - `grep -c "left: 0" src/components/RingShape.tsx` returns at least 2.
    - `grep -c "left: '-1.5px'" src/components/RingShape.tsx` returns at least 2.
    - `grep -nE "text-(white|black|slate-|teal-)" src/components/RingShape.tsx` returns ZERO.
    - `npm test -- --run RingShape` exits 0.
    - `npx tsc --noEmit && npm run lint` exits 0.
  </acceptance_criteria>
  <done>
    RingShape.tsx applies a single TSX delta vs OrbShape (`data-variant="ring"` on both roots); all other geometry is identical (Ring's hollow center comes from CSS in Task 3). Full RingShape suite green.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Add 6 `[data-variant='square'|'ring']` CSS overrides in theme.css; verify THEME-05 contrast guard + THEME-UI-01 hardcoded-class guard both stay green</name>
  <files>src/styles/theme.css</files>
  <read_first>
    - src/styles/theme.css (full file — focus on lines 344–438; new rules append after line ~390 and before line 430)
    - src/components/SquareShape.tsx (Task 1 output — confirms TSX expects `[data-variant='square']` border-radius overrides to set 18%)
    - src/components/RingShape.tsx (Task 2 output — confirms TSX expects `[data-variant='ring']` border-width override and radial-gradient override)
    - .planning/phases/17-visual-variants/17-PATTERNS.md §"`src/styles/theme.css` (EDIT — rename + per-variant overrides)" — the exact 6 CSS rules to add are listed verbatim there
    - .planning/phases/17-visual-variants/17-UI-SPEC.md §"Per-variant geometry" + §"CSS classes (rename + reuse, zero net-new color classes)"
    - .planning/phases/17-visual-variants/17-CONTEXT.md §"Implementation Decisions" D-13 (no new tokens), D-15 (per-variant border-radius + border-width via `[data-variant]`)
  </read_first>
  <action>
    Edit `src/styles/theme.css`. Append the following 6 rules immediately AFTER the existing `[data-phase='out'] .shape-marker--inner { opacity: 1; }` rule (around line 388–390) and BEFORE the `@media (prefers-reduced-motion: reduce) { ... }` block (around line 430). Group with a section comment:

    ```
    /* ============================================================
       Phase 17 — Visual Variants
       Per-variant geometric overrides on shared `.shape-marker--*`
       and `.orb-layer--*` classes (D-13 token reuse + D-15 single
       class hierarchy). Variant identity is geometric only — no
       new color tokens (--color-orb-in/out-* reused across all 3
       variants). The reduced-motion @media block below this section
       owns the cross-variant suppression rules (.shape-marker--inner
       display: none + .orb-layer crossfade preserved).
       ============================================================ */

    /* D-05/D-15: Square variant — rounded-corner marker outlines
       (planner-picked 18%; operator may revise after dev-server smoke). */
    [data-variant='square'] .shape-marker--outer {
      border-radius: 18%;
    }
    [data-variant='square'] .shape-marker--inner {
      border-radius: 18%;
    }

    /* D-06/D-15: Ring variant — thinner concentric markers
       (1px vs orb's 1.5px so the rings do not compete with the body annulus). */
    [data-variant='ring'] .shape-marker--outer {
      border-width: 1px;
    }
    [data-variant='ring'] .shape-marker--inner {
      border-width: 1px;
    }

    /* D-06: Ring variant — hollow center via radial-gradient on the
       shared `.orb-layer--in/--out` classes (Option D per RESEARCH).
       The inner stop at 35% creates a transparent core; the body
       annulus occupies 35%-100% of the body radius. Color tokens
       --color-orb-in/out-from/to are reused VERBATIM from the linear
       gradient — D-13 zero new tokens. */
    [data-variant='ring'] .orb-layer--in {
      background: radial-gradient(
        circle at center,
        transparent 0%,
        transparent 35%,
        var(--color-orb-in-from) 36%,
        var(--color-orb-in-to) 100%
      );
    }
    [data-variant='ring'] .orb-layer--out {
      background: radial-gradient(
        circle at center,
        transparent 0%,
        transparent 35%,
        var(--color-orb-out-from) 36%,
        var(--color-orb-out-to) 100%
      );
    }
    ```

    Do NOT touch:
    - The `@theme` block at lines 1–82 (no new tokens added — D-13).
    - Any existing `[data-theme='...']` palette block (lines 132–342).
    - The `.orb-layer--in/--out` linear-gradient declarations at lines 344–353 (Orb + Square still use the linear gradient; only Ring overrides to radial).
    - The `[data-phase='out'] .orb-layer--in/--out` opacity-crossfade rules at lines 356–362 (cross-variant — all 3 variants use this opacity mechanism via class reuse).
    - The `.shape-marker--outer` / `.shape-marker--inner` base declarations at lines 364–386 (per-variant rules ADD to these via cascade).
    - The `@media (prefers-reduced-motion: reduce)` block at lines 430–438 (cross-variant — `.shape-marker--inner { display: none }` applies to all 3 variants automatically).
    - The CR-01 inline-style invariant comment block (lines 419–424).

    Confirm the new rules sit in cascade order: shared `.orb-layer--*` linear-gradient → `[data-variant='ring'] .orb-layer--*` radial-gradient override (the override wins for Ring because the attribute selector adds specificity).
  </action>
  <verify>
    <automated>npm test -- --run theme.contrast theme.no-hardcoded-classes &amp;&amp; npx tsc --noEmit &amp;&amp; npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE "\\[data-variant='(square|ring)'\\]" src/styles/theme.css` returns at least 6 (4 marker overrides + 2 radial-gradient overrides).
    - `grep -cE "\\[data-variant='square'\\] \\.shape-marker--(outer|inner) \\{ border-radius: 18%" src/styles/theme.css` returns exactly 2 (or equivalent multi-line regex confirming both Square marker rules exist).
    - `grep -cE "\\[data-variant='ring'\\] \\.shape-marker--(outer|inner)" src/styles/theme.css` returns at least 2 (both Ring marker border-width rules exist).
    - `grep -cE "\\[data-variant='ring'\\] \\.orb-layer--(in|out) \\{" src/styles/theme.css` returns at least 2 (both Ring radial-gradient override rules exist).
    - `grep -cE "radial-gradient" src/styles/theme.css` returns at least 2 (the two Ring overrides; theme.css did not previously contain radial-gradient).
    - `grep -cE "--color-(orb|breathing)-" src/styles/theme.css` returns the SAME match count as before this task — i.e. no new CSS variable declarations or references introduced (D-13 zero new tokens).
    - `npm test -- --run theme.contrast` exits 0 (THEME-05 ≥ 1.5 contrast guard across all 5 palettes — no token edits, so no change expected).
    - `npm test -- --run theme.no-hardcoded-classes` exits 0 (THEME-UI-01 hardcoded-class guard — no .tsx changes in this task).
    - `npx tsc --noEmit && npm run lint && npm run build && npm test -- --run` exits 0 at commit boundary.
  </acceptance_criteria>
  <done>
    theme.css contains the 6 per-variant overrides grouped under a Phase 17 section comment, all inside the existing structure (after the marker rules + `[data-phase='out']` rule, before the `@media reduced-motion` block); zero new CSS variables; THEME-05 and THEME-UI-01 guards both green; full suite green at commit boundary.
  </done>
</task>

</tasks>

<verification>
**Overall green-gate after Plan 03:** `npx tsc --noEmit && npm run lint && npm run build && npm test -- --run` exits 0.

**Carry-forward guard checks:**
- `src/styles/theme.contrast.test.ts` — THEME-05 ≥ 1.5 contrast guard stays green (token reuse — no token edits).
- `src/styles/theme.no-hardcoded-classes.test.ts` — THEME-UI-01 guard stays green (SquareShape + RingShape are token-bound via Tailwind v4 arbitrary syntax + var() references inherited from OrbShape's pattern).

**Renderable smoke (post-Plan-03, pre-Plan-05):** SquareShape and RingShape are exported but NOT yet wired into BreathingShape — they are unreachable from the UI until Plan 05 (BreathingShape dispatcher). This is intentional: Plan 03 + Plan 04 (hooks) can land in parallel as wave 3.

**No regression to OrbShape:** Plan 03 does not modify OrbShape.tsx, OrbShape.test.tsx, or shapeConstants.ts — only the new Square + Ring files + CSS additions.
</verification>

<success_criteria>
- `src/components/SquareShape.tsx` exports `SquareShape` + `SquareShapeProps`; renders rounded-square Body + LeadIn with the 5 documented deltas vs OrbShape.
- `src/components/RingShape.tsx` exports `RingShape` + `RingShapeProps`; renders annulus Body + LeadIn with a single TSX delta (`data-variant='ring'`); hollow center comes entirely from CSS in Task 3.
- `src/components/SquareShape.test.tsx` and `RingShape.test.tsx` cover Body + LeadIn smoke, geometry, four-edge anchoring, kinematics, reduced-motion MID_SCALE lock, lead-in digit (VARIANT-05), `data-variant` attribute presence.
- `src/styles/theme.css` gains 6 new rules under a Phase 17 section comment: 2 Square marker `border-radius: 18%` overrides + 2 Ring marker `border-width: 1px` overrides + 2 Ring `.orb-layer--in/--out` radial-gradient overrides.
- Zero new CSS variables / color tokens (D-13).
- THEME-05 contrast guard + THEME-UI-01 hardcoded-class guard both stay green.
- `tsc && lint && build && test` exits 0 at every commit boundary.
</success_criteria>

<output>
After completion, create `.planning/phases/17-visual-variants/17-03-SUMMARY.md` noting:
- SquareShape.tsx + .test.tsx — Body + LeadIn with 5 geometric deltas vs OrbShape.
- RingShape.tsx + .test.tsx — Body + LeadIn with single TSX delta; hollow center via CSS.
- 6 new CSS rules in theme.css under Phase 17 section comment.
- Confirmation: zero new CSS variables, THEME-05 + THEME-UI-01 guards green, full suite green.
- Note that Square + Ring are not yet UI-reachable (Plan 05 wires the dispatcher).
</output>
