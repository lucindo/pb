---
phase: 17-visual-variants
plan: 05
type: execute
wave: 4
depends_on:
  - 17-02
  - 17-03
  - 17-04
files_modified:
  - src/components/BreathingShape.tsx
  - src/components/BreathingShape.test.tsx
  - src/components/VariantPicker.tsx
  - src/components/VariantPicker.test.tsx
autonomous: true
requirements:
  - VARIANT-01
  - VARIANT-02
  - VARIANT-04
  - VARIANT-05
  - VARIANT-06
user_setup: []
tags:
  - dispatcher
  - picker
  - phase-17

must_haves:
  truths:
    - "`BreathingShape` accepts a new OPTIONAL `variant?: VisualVariantId` prop alongside existing `frame` and `leadInDigit` — defaults to `'orb'` so App.tsx can be wired in Plan 06 without breaking the green-gate in Plan 05"
    - "`BreathingShape` owns the SINGLE idle null-return guard: `if (frame === null && leadInDigit == null) return null` — D-04 dispatcher contract"
    - "`BreathingShape` switches on `variant` and delegates to `<OrbShape | SquareShape | RingShape frame leadInDigit />`; the `orb` case is the default branch so any unknown variant falls back to Orb (defense in depth for cross-tab pollution)"
    - "Per D-03: in Plan 06, App.tsx will explicitly pass `variant={sessionVariantRef.current ?? liveVariant}` — the optional + default-`'orb'` shape in Plan 05 is a transitional bridge ONLY for the Plan 05 commit boundary. Plan 06 will tighten the interface as needed; the dispatcher's default branch is the runtime safety net regardless."
    - "`BreathingShape.test.tsx` covers: idle null-return guard, dispatch-by-variant smoke (each of 3 values renders the correct child with the correct `data-variant` attribute), lead-in priority preserved through the dispatcher path, variant prop is OPTIONAL and defaults to `'orb'`"
    - "`VariantPicker` becomes a full radiogroup over `VARIANT_OPTIONS` mirroring `ThemePicker.tsx` posture verbatim: same baseClasses / selectedClasses / unselectedClasses, same `aria-labelledby`/`aria-disabled`/`role='radio'`/`aria-checked` attributes, same 44×44 hit area (`min-h-12`), same `focus-visible:ring-breathing-accent` ring"
    - "Each `VariantPicker` button stacks an inline shape swatch (Option A: CSS-only mini render for Orb + Square via `.orb-layer--in` span with `borderRadius: '50%'` or `'18%'`; Option B: inline `<svg>` circle stroke for Ring per UI-SPEC Pitfall 8) above the variant label — `flex flex-col items-center gap-1` on each button"
    - "`VariantPicker` self-reads/writes via `useVariantChoice()` (Plan 04) — `{ disabled }` is the ONLY prop per Phase 15 D-02 + CONTEXT.md D-21"
    - "VARIANT-06: 44×44 hit area + `focus-visible:ring-2 focus-visible:ring-breathing-accent focus-visible:ring-offset-2` honored on every option button"
    - "Plan 05 does NOT touch `SettingsDialog.tsx` (D-20), does NOT touch `src/domain/settings.ts` (D-19), does NOT touch `src/storage/prefs.ts` (D-19), does NOT touch `App.tsx` (Plan 06 owns that — the optional `variant?` prop with default `'orb'` lets App.tsx compile unchanged through Plan 05's commit boundary)"
    - "VARIANT-02 zero-regression: With BreathingShape's `variant` prop defaulting to `'orb'`, App.tsx renders BreathingShape exactly as before (no prop passed) and the dispatcher's `case 'orb' / default` branch routes to OrbShape — byte-identical to pre-Phase-17 orb behavior for users who never open the picker"
    - "`tsc && lint && build && test` exits 0 after each commit"
  artifacts:
    - path: "src/components/BreathingShape.tsx"
      provides: "Final ~30-LOC dispatcher: imports OrbShape + SquareShape + RingShape + VisualVariantId; owns idle null-return; switches on `variant?: VisualVariantId = 'orb'` prop with `orb` as default. Preserves dispatcher-leading comment block explaining D-04 and the optional-prop transitional bridge."
      contains: "switch (variant)"
    - path: "src/components/BreathingShape.test.tsx"
      provides: "Test suite for dispatcher: idle null-return (with optional variant prop omitted), default-to-orb when no variant passed, lead-in-priority preserved, `it.each` over 3 variants asserting `data-variant` attribute on rendered child, unknown-variant fallback to Orb"
      contains: "BreathingShape"
    - path: "src/components/VariantPicker.tsx"
      provides: "Full radiogroup body — mirrors ThemePicker.tsx posture verbatim with inline shape swatches; consumes useVariantChoice() for state + write path"
      contains: "role=\"radiogroup\""
    - path: "src/components/VariantPicker.test.tsx"
      provides: "Replacement test suite (was stub) — mirrors ThemePicker.test.tsx structure: radiogroup behavior, disabled gating, savePrefs round-trip, CustomEvent dispatch, 44×44 hit area, focus-visible, swatch presence per option"
      contains: "VariantPicker"
  key_links:
    - from: "src/components/BreathingShape.tsx (dispatcher)"
      to: "src/components/OrbShape.tsx / SquareShape.tsx / RingShape.tsx"
      via: "switch on variant prop"
      pattern: "case 'square': return <SquareShape"
    - from: "src/components/VariantPicker.tsx"
      to: "src/hooks/useVariantChoice.ts"
      via: "named import + hook call"
      pattern: "useVariantChoice\\(\\)"
    - from: "src/components/VariantPicker.tsx option buttons"
      to: "src/styles/theme.css (`.orb-layer--in` swatch background)"
      via: "className on inline swatch span"
      pattern: "className=\"orb-layer--in"
---

<objective>
Two parallel completions that close the variant rendering loop, ready for Plan 06's App.tsx wire-up:

1. **`BreathingShape` becomes the real 3-way dispatcher.** Add an OPTIONAL `variant?: VisualVariantId` prop that defaults to `'orb'` (D-03 + transitional bridge). Own the idle null-return guard (D-04). Switch on variant and delegate to the correct sibling component with `orb` as the default branch (defense-in-depth fallback for unknown values). Slim test file covers dispatcher concerns only — Body/LeadIn structural assertions live in OrbShape/SquareShape/RingShape test files.

   **Why optional with default `'orb'`:** App.tsx currently calls `<BreathingShape frame={...} leadInDigit={...} />` without a variant prop. Making `variant` REQUIRED would break the TypeScript build at Plan 05's commit boundary (App.tsx not yet updated). Making it OPTIONAL with default `'orb'` preserves byte-identical orb behavior for App.tsx callers in Plan 05 AND lets Plan 06 add the explicit `variant={sessionVariantRef.current ?? liveVariant}` prop without re-editing BreathingShape. Phase 14 D-05 already locks `DEFAULT_VARIANT = 'orb'`, so the default value is consistent with the rest of the system.

2. **`VariantPicker` becomes the real radiogroup.** Replace the Phase 15 stub (`Variant: orb` read-only label) with the full ThemePicker-mirror body: 3 buttons, selected/unselected styling, inline shape swatches stacked above labels, 44×44 hit area, focus-visible ring, disabled gating, savePrefs + CustomEvent on click via `useVariantChoice`. VariantPicker.test.tsx is REPLACED wholesale (the 3 stub tests are obsolete).

Per CONTEXT.md D-20: `SettingsDialog.tsx` is NOT edited (Phase 15 D-01 invariant). Per D-19: `src/domain/settings.ts` and `src/storage/prefs.ts` are NOT edited. Per D-21: VariantPicker accepts ONLY `{ disabled: boolean }` (no value prop).

Plan 06 owns the final wire-up in `App.tsx`: hook invocation (useVisualVariant), sessionVariantRef declaration, snapshot at startSession, clear on session end, and passing `variant={sessionVariantRef.current ?? liveVariant}` to BreathingShape.

Output: dispatcher with full variant switch + slimmed test, full picker body + replaced test suite. After Plan 05, the user can still NOT see variant changes propagate to the running shape (App.tsx still renders BreathingShape with no `variant` prop, which defaults to `'orb'`) — that's Plan 06.
</objective>

<execution_context>
@$HOME/.claude-personal-personal/get-shit-done/workflows/execute-plan.md
@$HOME/.claude-personal-personal/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/17-visual-variants/17-CONTEXT.md
@.planning/phases/17-visual-variants/17-PATTERNS.md
@.planning/phases/17-visual-variants/17-UI-SPEC.md
@.planning/phases/17-visual-variants/17-VALIDATION.md
@src/components/OrbShape.tsx
@src/components/SquareShape.tsx
@src/components/RingShape.tsx
@src/components/ThemePicker.tsx
@src/components/ThemePicker.test.tsx
@src/components/VariantPicker.tsx
@src/components/BreathingShape.tsx
@src/components/BreathingShape.test.tsx
@src/hooks/useVariantChoice.ts
@src/domain/settings.ts

<interfaces>
<!-- Inputs from prior plans: -->

src/components/OrbShape.tsx (Plan 02):
  - `export interface OrbShapeProps { frame: SessionFrame | null; leadInDigit?: 3 | 2 | 1 | null }`
  - `export function OrbShape(props: OrbShapeProps)` — no idle null-return, dispatcher owns that.

src/components/SquareShape.tsx (Plan 03):
  - `export interface SquareShapeProps { frame: SessionFrame | null; leadInDigit?: 3 | 2 | 1 | null }`
  - `export function SquareShape(props: SquareShapeProps)` — same contract as OrbShape.

src/components/RingShape.tsx (Plan 03):
  - `export interface RingShapeProps { frame: SessionFrame | null; leadInDigit?: 3 | 2 | 1 | null }`
  - `export function RingShape(props: RingShapeProps)` — same contract.

src/hooks/useVariantChoice.ts (Plan 04):
  - `export function useVariantChoice(): { variant: VisualVariantId; setVariant: (next: VisualVariantId) => void }`

src/components/ThemePicker.tsx (Phase 16 — verbatim mirror template for VariantPicker body):
  - Identical structure described in PATTERNS.md §"Cross-cutting: Radiogroup picker UI (44×44 hit area + focus-visible + token-bound)"
  - Class strings (do NOT alter — copy verbatim):
    - `baseClasses = 'min-h-12 rounded-full px-3 py-2 text-sm font-semibold shadow-sm transition motion-reduce:transition-none focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-breathing-accent focus-visible:ring-offset-2 disabled:cursor-not-allowed disabled:opacity-45'`
    - `selectedClasses = 'border-2 border-[var(--color-breathing-accent)] bg-[var(--color-breathing-bg-soft)] text-[var(--color-breathing-accent-strong)]'`
    - `unselectedClasses = 'border border-[var(--color-breathing-accent)] bg-[var(--color-breathing-surface)] text-[var(--color-breathing-accent-strong)] hover:bg-[var(--color-breathing-bg-soft)] active:bg-[var(--color-breathing-bg-soft)]'`
  - For VariantPicker, append ` flex flex-col items-center gap-1` to baseClasses so swatch + label stack vertically (UI-SPEC §"Inline Shape Swatches" — swatch on top, label below).

src/components/BreathingShape.tsx (post-Plan-02 — current state):
  - Currently ~25 LOC pass-through:
    ```
    import type { SessionFrame } from '../domain/sessionMath'
    import { OrbShape } from './OrbShape'
    export interface BreathingShapeProps { frame: SessionFrame | null; leadInDigit?: 3 | 2 | 1 | null }
    export function BreathingShape({ frame, leadInDigit }: BreathingShapeProps) {
      if (frame === null && leadInDigit == null) return null
      return <OrbShape frame={frame} leadInDigit={leadInDigit} />
    }
    ```
  - Plan 05 EVOLVES this into the full 3-way dispatcher with OPTIONAL `variant` prop (default `'orb'`).

src/domain/settings.ts (locked Phase 14):
  - `VARIANT_OPTIONS = ['orb', 'square', 'ring'] as const`, `VisualVariantId`, `DEFAULT_VARIANT = 'orb'`, `isValidVariant`.

UI-SPEC §"Inline Shape Swatches" — swatch primitive split:
  - Orb: `<span className="block w-6 h-6 relative" aria-hidden="true"><span className="orb-layer--in absolute inset-0" style={{ borderRadius: '50%' }} /></span>`
  - Square: same wrapper, inner span `style={{ borderRadius: '18%' }}`.
  - Ring: `<svg width="24" height="24" viewBox="0 0 24 24" aria-hidden="true"><circle cx="12" cy="12" r="9" fill="none" stroke="var(--color-orb-in-from)" strokeWidth="4" /></svg>`. Pitfall 8 mitigation — radial-gradient at 24px is invisible; SVG circle-stroke is the only legible primitive at swatch size.

VariantPicker stub (current state — to be REPLACED in Task 2):
  - ~26 LOC: reads `loadPrefs()` and renders `Variant: orb` as read-only label. Replaced wholesale.

VariantPicker.test.tsx stub (current state — to be REPLACED in Task 2):
  - 3 stub tests assert the `Variant: <value>` literal label format. Obsolete after Plan 05.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Evolve BreathingShape.tsx into the full 3-way dispatcher with OPTIONAL `variant?: VisualVariantId` prop (default `'orb'`); update BreathingShape.test.tsx to cover dispatch + idle + lead-in priority + default-to-orb</name>
  <files>src/components/BreathingShape.tsx, src/components/BreathingShape.test.tsx</files>
  <behavior>
    - Test 1: `<BreathingShape frame={null} />` returns null (idle null-return — D-04; works even without variant prop, default `'orb'` is irrelevant since render is short-circuited).
    - Test 2: `<BreathingShape variant="orb" frame={null} leadInDigit={null} />` returns null (both null).
    - Test 3: `<BreathingShape variant="square" frame={null} />` returns null (idle null-return applies regardless of variant).
    - Test 4: Default-to-orb: `<BreathingShape frame={sampleFrame} />` (no variant prop) renders with `data-variant="orb"` on the root.
    - Test 5: `it.each(['orb', 'square', 'ring'])`: `<BreathingShape variant={v} frame={sampleFrame} />` renders the corresponding child — assert `container.querySelector('[role="img"]')` has `data-variant === v`.
    - Test 6: `it.each(['orb', 'square', 'ring'])`: `<BreathingShape variant={v} frame={null} leadInDigit={3} />` renders the lead-in branch of the correct sibling — assert `[role="img"]` has `data-variant === v` AND no `data-phase`, AND `screen.getByText('3')` is visible.
    - Test 7: Lead-in priority: `<BreathingShape variant="orb" frame={sampleFrame} leadInDigit={2} />` renders the LeadIn (`aria-label="Lead-in: 2"`), NOT the body (`Breathing shape: In` is NOT in the DOM).
    - Test 8: Unknown variant falls back to Orb (defense in depth — cross-tab pollution / forward-compat): `<BreathingShape variant={'unknown' as VisualVariantId} frame={sampleFrame} />` renders an element with `data-variant="orb"`.
  </behavior>
  <read_first>
    - src/components/BreathingShape.tsx (post-Plan-02 pass-through state)
    - src/components/BreathingShape.test.tsx (post-Plan-02 slimmed state)
    - src/components/OrbShape.tsx + SquareShape.tsx + RingShape.tsx (all exported now)
    - src/domain/settings.ts (VARIANT_OPTIONS, VisualVariantId, DEFAULT_VARIANT = 'orb')
    - .planning/phases/17-visual-variants/17-PATTERNS.md §"`src/components/BreathingShape.tsx` (EDIT — becomes ~20-line dispatcher)" + §"#### `src/components/BreathingShape.test.tsx` (EDIT — slim to dispatch-only)"
    - .planning/phases/17-visual-variants/17-CONTEXT.md §"Implementation Decisions" D-01 (sibling files + thin dispatcher), D-03 (variant prop passed by App), D-04 (dispatcher owns idle guard)
  </read_first>
  <action>
    1. Replace `src/components/BreathingShape.tsx` body with the full dispatcher (OPTIONAL `variant` prop, default `'orb'`):

       - Imports:
         - `import type { SessionFrame } from '../domain/sessionMath'`
         - `import type { VisualVariantId } from '../domain/settings'`
         - `import { OrbShape } from './OrbShape'`
         - `import { SquareShape } from './SquareShape'`
         - `import { RingShape } from './RingShape'`
       - File-leading comment block explaining role: "Phase 17 dispatcher. Owns the single idle null-return guard (D-04). Switches on the `variant` prop (D-03 — App.tsx passes the captured-at-Start frozen value in Plan 06). The prop is OPTIONAL with default `'orb'` so callers that pre-date Phase 17 (e.g. App.tsx prior to Plan 06's wire-up) compile and render unchanged — VARIANT-02 zero-regression. Phase 14 D-05 locks DEFAULT_VARIANT = 'orb', consistent with this default. Each sibling owns its own lead-in vs Body internal dispatch. Unknown variant falls back to Orb (default switch branch — defense in depth against cross-tab pollution of Envelope.prefs.variant, though Phase 14 D-17 coerceVariant is the primary guard)."
       - Interface (variant is OPTIONAL):
         ```
         export interface BreathingShapeProps {
           variant?: VisualVariantId
           frame: SessionFrame | null
           leadInDigit?: 3 | 2 | 1 | null
         }
         ```
       - Function body — use ES6 default-parameter syntax to apply the `'orb'` default at the destructuring site:
         ```
         export function BreathingShape({ variant = 'orb', frame, leadInDigit }: BreathingShapeProps) {
           if (frame === null && leadInDigit == null) {
             return null
           }
           switch (variant) {
             case 'square':
               return <SquareShape frame={frame} leadInDigit={leadInDigit} />
             case 'ring':
               return <RingShape frame={frame} leadInDigit={leadInDigit} />
             case 'orb':
             default:
               return <OrbShape frame={frame} leadInDigit={leadInDigit} />
           }
         }
         ```

       The `case 'orb'` and `default` MUST share the same `return` branch (TypeScript exhaustiveness — `variant: VisualVariantId` covers exactly `'orb' | 'square' | 'ring'`, but a `default` branch is required at runtime for cross-tab pollution defense).

       Do NOT touch App.tsx in this plan — the optional prop + default `'orb'` is the bridge that preserves the green-gate without requiring App.tsx edits.

    2. Update `src/components/BreathingShape.test.tsx` to cover the new dispatcher behavior:

       - Imports stay similar to post-Plan-02 state: `'@testing-library/jest-dom/vitest'`, `render, screen` from RTL, `describe, expect, it` from vitest, `BreathingShape` from './BreathingShape', `SessionFrame` type from '../domain/sessionMath'. ADD: `VisualVariantId` type from '../domain/settings'.
       - Keep `sampleFrame` constant verbatim.
       - Rewrite the `describe('BreathingShape', () => { ... })` block to cover the 8 behaviors above:
         - Idle null-return tests (Behaviors 1–3): include the case `<BreathingShape frame={null} />` (no variant) AND `<BreathingShape variant="orb" frame={null} leadInDigit={null} />` AND `<BreathingShape variant="square" frame={null} />`.
         - Default-to-orb test (Behavior 4):
           ```
           it('defaults variant to "orb" when no variant prop is passed', () => {
             const { container } = render(<BreathingShape frame={sampleFrame} />)
             const root = container.querySelector('[role="img"]')
             expect(root).toHaveAttribute('data-variant', 'orb')
           })
           ```
         - Dispatch-by-variant tests (Behaviors 5–6) using `it.each<VisualVariantId>(['orb', 'square', 'ring'])`:
           - Body render variant: assert `container.querySelector('[role="img"]')` has `data-variant === variant`.
           - LeadIn render variant: assert `[role="img"]` has `data-variant === variant`, no `data-phase` attribute, and the digit "3" is in the DOM.
         - Lead-in priority (Behavior 7): same shape as before, with `variant="orb"` added.
         - Unknown-variant fallback (Behavior 8):
           ```
           it('falls back to OrbShape when variant is an unknown value (defense in depth)', () => {
             const { container } = render(<BreathingShape variant={'unknown' as VisualVariantId} frame={sampleFrame} />)
             const root = container.querySelector('[role="img"]')
             expect(root).toHaveAttribute('data-variant', 'orb')
           })
           ```

       - DROP redundant smoke tests if they duplicate the `it.each` dispatch coverage.

    Per D-17 per-commit green-gate: BreathingShape.tsx + BreathingShape.test.tsx land in a single commit. The OPTIONAL `variant?` prop with default `'orb'` ensures App.tsx (which calls `<BreathingShape frame=... leadInDigit=... />` with no variant) continues to compile AND continues to render orb behavior unchanged — VARIANT-02 zero-regression preserved.
  </action>
  <verify>
    <automated>npm test -- --run BreathingShape &amp;&amp; npx tsc --noEmit &amp;&amp; npm run lint &amp;&amp; npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "switch (variant)" src/components/BreathingShape.tsx` returns exactly 1.
    - `grep -cE "case 'square'|case 'ring'|case 'orb'" src/components/BreathingShape.tsx` returns exactly 3.
    - `grep -c "default:" src/components/BreathingShape.tsx` returns at least 1 (paired with `case 'orb'`).
    - `grep -c "if (frame === null && leadInDigit == null)" src/components/BreathingShape.tsx` returns exactly 1 (single idle-guard site — D-04).
    - `grep -cE "variant\\?: VisualVariantId|variant = 'orb'" src/components/BreathingShape.tsx` returns at least 2 (interface field as `variant?:` AND destructured default `variant = 'orb'`).
    - `wc -l src/components/BreathingShape.tsx` returns ≤ 55 (target ~35 LOC including imports + interface + comment block + function body).
    - `grep -n "VariantPicker\\|variant=" src/app/App.tsx` returns NO new BreathingShape variant prop (App.tsx unchanged in this plan).
    - `npm test -- --run BreathingShape` exits 0 (all 8 dispatcher behaviors green).
    - `npx tsc --noEmit && npm run lint && npm run build && npm test -- --run` exits 0 at commit boundary (full suite green — App.tsx compiles because variant is optional).
  </acceptance_criteria>
  <done>
    BreathingShape.tsx is the final 3-way dispatcher with OPTIONAL `variant?` prop defaulting to `'orb'` (D-04 + transitional bridge for App.tsx). BreathingShape.test.tsx covers 8 behaviors: idle, default-to-orb, lead-in priority, `it.each` dispatch over 3 variants for body + lead-in, unknown-variant fallback. App.tsx is unchanged.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Replace VariantPicker.tsx body with the full radiogroup + inline shape swatches; replace VariantPicker.test.tsx wholesale with ThemePicker.test.tsx-mirrored coverage</name>
  <files>src/components/VariantPicker.tsx, src/components/VariantPicker.test.tsx</files>
  <behavior>
    - Test 1: Renders 3 radio buttons with `aria-checked` matching the seeded variant (e.g. seed `variant: 'square'` → Square button has `aria-checked="true"`, Orb + Ring have `aria-checked="false"`).
    - Test 2: Renders a section header `<p id="variant-picker-label">Variant</p>` above the radiogroup (UI-SPEC §"Copywriting Contract" — locked English).
    - Test 3: `aria-disabled={disabled}` on the radiogroup wrapper + native `disabled={disabled}` on each button.
    - Test 4: Clicking an unselected button calls `useVariantChoice().setVariant(id)` — verified via savePrefs round-trip (read `localStorage[STATE_KEY]`).
    - Test 5: Clicking dispatches `'hrv:prefs-changed'` CustomEvent with `detail = { key: 'variant', value: <clicked id> }`.
    - Test 6: When `disabled=true`, clicking does NOT trigger setVariant; localStorage envelope unchanged; no CustomEvent fires.
    - Test 7: When `disabled=true`, the selected option button retains `aria-checked="true"` and the selected visual style (selection state is preserved but non-interactive).
    - Test 8: 44×44 hit area — every button has `min-h-12` class AND `px-3` class (per `baseClasses`).
    - Test 9: focus-visible — every button has `focus-visible:ring-2 focus-visible:ring-breathing-accent focus-visible:ring-offset-2` classes.
    - Test 10: Swatch primitive per variant — Orb option button contains a `.orb-layer--in` span with inline `borderRadius: '50%'`; Square option button contains a `.orb-layer--in` span with inline `borderRadius: '18%'`; Ring option button contains an `<svg>` element with a `<circle>` child (Pitfall 8 mitigation).
  </behavior>
  <read_first>
    - src/components/ThemePicker.tsx (verbatim mirror template — copy structure, substitute labels)
    - src/components/ThemePicker.test.tsx (verbatim mirror test template — copy 7 cases, substitute identifiers)
    - src/components/VariantPicker.tsx (Phase 15 stub — to be REPLACED wholesale)
    - src/components/VariantPicker.test.tsx (Phase 15 stub tests — to be REPLACED wholesale)
    - src/hooks/useVariantChoice.ts (Plan 04 — hook contract)
    - src/domain/settings.ts (VARIANT_OPTIONS, VisualVariantId)
    - .planning/phases/17-visual-variants/17-PATTERNS.md §"`src/components/VariantPicker.tsx`" + §"#### `src/components/VariantPicker.test.tsx` (REPLACE)"
    - .planning/phases/17-visual-variants/17-UI-SPEC.md §"Visual Contract" + §"Inline Shape Swatches" + §"Sketch — VariantPicker resting state" + §"Sketch — Disabled state" + §"A11y Contract"
    - .planning/phases/17-visual-variants/17-CONTEXT.md §"Implementation Decisions" D-12 (inline swatches; no live idle preview), D-21 (picker prop contract preserved), D-24 (VARIANT-06 a11y floor)
  </read_first>
  <action>
    1. Replace `src/components/VariantPicker.tsx` body wholesale:

       - File-leading comment block: explain Phase 17 role (radiogroup body for variant selection; mirrors ThemePicker.tsx posture verbatim with inline shape swatches; consumes useVariantChoice from Plan 04; honors VARIANT-06 a11y floor + Phase 15 D-02 picker prop contract).
       - Imports:
         - `import { VARIANT_OPTIONS, type VisualVariantId } from '../domain/settings'`
         - `import { useVariantChoice } from '../hooks/useVariantChoice'`
       - Interface (preserve Phase 15 D-02 contract verbatim — per CONTEXT.md D-21):
         ```
         export interface VariantPickerProps {
           disabled: boolean
         }
         ```
       - Function body — mirror `ThemePicker.tsx` lines 17–53 structurally, with the following substitutions:
         - `theme` → `variant` identifiers, `setTheme` → `setVariant`, `THEME_OPTIONS` → `VARIANT_OPTIONS`, `ThemeId` → `VisualVariantId`.
         - Heading id: `theme-picker-label` → `variant-picker-label`.
         - Heading text: `Theme` → `Variant` (locked English — UI-SPEC §"Copywriting Contract").
         - Class strings (`baseClasses`, `selectedClasses`, `unselectedClasses`) — copy ThemePicker.tsx:32–34 VERBATIM. APPEND ` flex flex-col items-center gap-1` to `baseClasses` so swatch + label stack vertically.
         - Inside each button's children — replace `{label}` text node with a 2-child stack:
           - Swatch (top child):
             - For `id === 'ring'`: render `<svg width="24" height="24" viewBox="0 0 24 24" aria-hidden="true"><circle cx="12" cy="12" r="9" fill="none" stroke="var(--color-orb-in-from)" strokeWidth="4" /></svg>`
             - For `id === 'orb' || id === 'square'`: render `<span className="block w-6 h-6 relative" aria-hidden="true"><span className="orb-layer--in absolute inset-0" style={{ borderRadius: id === 'square' ? '18%' : '50%' }} /></span>`
           - Label (bottom child): `<span>{label}</span>` where `label = id.charAt(0).toUpperCase() + id.slice(1)` (same mapping logic as ThemePicker).

         The button structure is:
         ```
         <button key={id} type="button" role="radio" aria-checked={selected} disabled={disabled}
                 onClick={() => { setVariant(id) }}
                 className={`${baseClasses} ${selected ? selectedClasses : unselectedClasses}`}>
           {/* swatch primitive per variant */}
           {id === 'ring' ? (<svg .../>) : (<span ...><span ... /></span>)}
           <span>{label}</span>
         </button>
         ```

       Do NOT introduce any hardcoded color classes (D-23). All color references go through `var(--color-*)` tokens or token-bound Tailwind shorthand utilities like `text-breathing-accent`, `ring-breathing-accent` (token-bound via the Tailwind v4 `@theme` block, permitted per PATTERNS §"Cross-cutting: THEME-UI-01 token-binding guard" — ThemePicker.tsx precedent). The SVG circle stroke uses `var(--color-orb-in-from)` — token-bound (UI-SPEC §"Inline Shape Swatches" rationale).

    2. Replace `src/components/VariantPicker.test.tsx` wholesale:

       - Imports mirror ThemePicker.test.tsx: `'@testing-library/jest-dom/vitest'`, `render, screen` from RTL, `userEvent` from `@testing-library/user-event`, `afterEach, beforeEach, describe, expect, it, vi` from vitest, `VariantPicker` from './VariantPicker', `STATE_KEY` from '../storage', `VisualVariantId` from '../domain/settings'.
       - `seedVariant(variant: VisualVariantId)` helper writing the envelope to localStorage with default theme/timbre/locale + the variable variant.
       - `beforeEach`: `window.localStorage.clear()`. `afterEach`: `vi.restoreAllMocks()`.
       - 10 test cases per the `<behavior>` list above (mirror ThemePicker.test.tsx's 7 cases + 3 additions for swatch / disabled-state-preserved / hit-area-class-presence).

       Critical test details:
       - **CustomEvent assertion (Test 5):** attach `window.addEventListener('hrv:prefs-changed', (e) => { capturedDetail = (e as CustomEvent).detail })`; call `userEvent.click(squareButton)`; assert `capturedDetail.key === 'variant'` and `capturedDetail.value === 'square'`. (Mirror ThemePicker.test.tsx's CustomEvent test.)
       - **Disabled-click no-op (Test 6):** seed `variant: 'orb'`; render with `disabled={true}`; attempt `userEvent.click(squareButton)`; assert `localStorage[STATE_KEY]` envelope is unchanged AND no `hrv:prefs-changed` event was dispatched.
       - **Swatch presence (Test 10):** assert that the Orb button contains a span with className containing `orb-layer--in`; assert that the Square button contains a span with className `orb-layer--in` AND inline `style.borderRadius === '18%'`; assert that the Ring button contains an `<svg>` element with a `<circle>` child.

       DROP all 3 Phase 15 stub tests (the `Variant: <value>` literal-text assertions are obsolete after Plan 05).
  </action>
  <verify>
    <automated>npm test -- --run VariantPicker</automated>
  </verify>
  <acceptance_criteria>
    - File `src/components/VariantPicker.tsx` has been replaced (line count grew from ~26 to ~70+).
    - `grep -c "role=\"radiogroup\"" src/components/VariantPicker.tsx` returns exactly 1.
    - `grep -c "role=\"radio\"" src/components/VariantPicker.tsx` returns exactly 1 (single template within `.map`).
    - `grep -c "id=\"variant-picker-label\"" src/components/VariantPicker.tsx` returns exactly 1.
    - `grep -c "useVariantChoice()" src/components/VariantPicker.tsx` returns exactly 1.
    - `grep -c "min-h-12" src/components/VariantPicker.tsx` returns at least 1 (44×44 hit area — VARIANT-06).
    - `grep -c "focus-visible:ring-breathing-accent" src/components/VariantPicker.tsx` returns at least 1 (focus-visible — VARIANT-06).
    - `grep -c "flex flex-col items-center gap-1" src/components/VariantPicker.tsx` returns at least 1 (swatch + label stack — UI-SPEC).
    - `grep -c "orb-layer--in" src/components/VariantPicker.tsx` returns at least 1 (swatch primitive for orb + square).
    - `grep -c "viewBox=\"0 0 24 24\"" src/components/VariantPicker.tsx` returns at least 1 (Ring SVG swatch — Pitfall 8 mitigation).
    - `grep -nE "text-(white|black|slate-|teal-)" src/components/VariantPicker.tsx` returns ZERO (D-23 hardcoded-class guard).
    - File `src/components/VariantPicker.test.tsx` has been REPLACED (the 3 stub-format tests are gone; new test count is ≥ 10).
    - `npm test -- --run VariantPicker` exits 0.
    - `npx tsc --noEmit && npm run lint && npm run build && npm test -- --run` exits 0 at commit boundary (full suite green — includes SettingsDialog.test.tsx which mounts VariantPicker inside the dialog).
  </acceptance_criteria>
  <done>
    VariantPicker.tsx is the full radiogroup body: 3 buttons with inline shape swatches stacked over labels, ThemePicker-mirror classes for selected/unselected/disabled visuals, 44×44 hit area, focus-visible ring, useVariantChoice-backed write path. VariantPicker.test.tsx covers all 10 contract behaviors. Full suite green.
  </done>
</task>

</tasks>

<verification>
**Overall green-gate after Plan 05:** `npx tsc --noEmit && npm run lint && npm run build && npm test -- --run` exits 0.

**Carry-forward guard checks:**
- `src/styles/theme.contrast.test.ts` — green (no token edits).
- `src/styles/theme.no-hardcoded-classes.test.ts` — green (VariantPicker uses only token-bound Tailwind arbitrary syntax + `text-breathing-accent` / `ring-breathing-accent` shorthand utilities per ThemePicker precedent).

**Plan 05 → Plan 06 handoff:** App.tsx is UNCHANGED in Plan 05. The OPTIONAL `variant?` prop with default `'orb'` lets App.tsx's existing `<BreathingShape frame=... leadInDigit=... />` call site continue to compile and render orb behavior unchanged. Plan 06's task 1 will:
1. Add the `useVisualVariant()` hook invocation near `useTheme()` to provide `liveVariant`.
2. Add the `sessionVariantRef = useRef<VisualVariantId | null>(null)` declaration.
3. Wire the snapshot at the `startSession` site + clear it on session end.
4. Pass `variant={sessionVariantRef.current ?? liveVariant}` to BreathingShape.

**VARIANT-02 zero-regression preserved:** With the dispatcher's default `'orb'` falling through to OrbShape (which is the byte-identical extraction from pre-Phase-17 BreathingShape Body + LeadIn modulo the .shape-marker class rename + data-variant attribute), the user-visible orb behavior is unchanged for users who never open the picker. The picker is now functional and writes to localStorage, but App.tsx is not yet reading the live variant. Plan 06 lights up the full path.
</verification>

<success_criteria>
- `src/components/BreathingShape.tsx` is the final 3-way dispatcher with OPTIONAL `variant?: VisualVariantId` prop (default `'orb'`), single idle null-return guard, and switch + Orb default fallback. ≤ 55 LOC.
- `src/components/BreathingShape.test.tsx` covers idle null-return (all variants + omitted-prop case), default-to-orb, lead-in priority, `it.each` dispatch over 3 variants for both body + lead-in rendering, unknown-variant fallback.
- `src/components/VariantPicker.tsx` is the full radiogroup body (replaces the Phase 15 stub) with inline shape swatches per UI-SPEC (Option A for Orb/Square, Option B SVG for Ring), 44×44 hit area, focus-visible ring, ThemePicker-mirror classes, `useVariantChoice` write path.
- `src/components/VariantPicker.test.tsx` is replaced wholesale (10 cases).
- `src/app/App.tsx` is UNCHANGED in this plan (Plan 06 owns the wire-up; the optional `variant?` prop + `'orb'` default preserves compilation + behavior).
- Phase 15 `SettingsDialog.tsx` NOT modified (D-20). Phase 14 `settings.ts` + `prefs.ts` NOT modified (D-19).
- THEME-UI-01 + THEME-05 guards both stay green.
- `tsc && lint && build && test` exits 0 at every commit boundary.
</success_criteria>

<output>
After completion, create `.planning/phases/17-visual-variants/17-05-SUMMARY.md` noting:
- BreathingShape.tsx evolution to full dispatcher (OPTIONAL variant prop + default 'orb' + switch + idle guard).
- BreathingShape.test.tsx covering 8 behaviors via `it.each` over 3 variants.
- VariantPicker.tsx wholesale replacement — ThemePicker-mirror radiogroup with inline shape swatches (Option A for Orb/Square, Option B SVG for Ring per Pitfall 8).
- VariantPicker.test.tsx wholesale replacement (10 cases).
- App.tsx NOT touched — Plan 06 owns the hook/snapshot/prop wire-up. The optional `variant?` prop with default `'orb'` preserved the green-gate without requiring transitional App.tsx edits.
- Confirmation: SettingsDialog.tsx + settings.ts + prefs.ts unchanged; THEME-UI-01 + THEME-05 guards green; full suite green at commit boundary.
</output>
