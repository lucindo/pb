---
phase: 26-pt-br-native-speaker-review
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/content/strings.ts
  - src/content/learnContent.ts
  - src/content/content.no-review-markers.test.ts
autonomous: false
requirements: [I18N-07]

must_haves:
  truths:
    - "Every machine-translated pt-BR string in src/content/ has been reviewed by the operator (PT-BR native speaker) and corrected where needed"
    - "Zero `// TODO: native-speaker review` markers remain anywhere in src/content/"
    - "The strings.ts header comment (line 9) no longer quotes the literal marker string"
    - "pt-BR bpmLabel reads RPM (D-07) and HRV terms read VFC (D-06), consistent across both catalogs"
    - "A marker-guard test fails the build if the review marker ever reappears in src/content/"
    - "lockedCopy.ts and its byte-equality guard are untouched and green"
    - "strings.test.ts and learnContent.test.ts (type completeness / catalog shape) stay green"
  artifacts:
    - path: "src/content/strings.ts"
      provides: "UiStrings catalog with reviewed pt-BR values, all 85 trailing markers removed, header line 9 rewritten"
      contains: "satisfies"
    - path: "src/content/learnContent.ts"
      provides: "LearnContent catalog with reviewed pt-BR values, all 12 markers removed"
      contains: "satisfies"
    - path: "src/content/content.no-review-markers.test.ts"
      provides: "fs-scan drift-guard asserting the review marker is absent from src/content/"
      exports: []
      min_lines: 25
  key_links:
    - from: "src/content/content.no-review-markers.test.ts"
      to: "src/content/*.ts (non-test)"
      via: "readFileSync + substring scan"
      pattern: "readFileSync"
    - from: "src/content/strings.ts pt-BR bpmLabel"
      to: "SettingsDialog"
      via: "RPM label rendered in existing layout"
      pattern: "bpmLabel"
---

<objective>
Review every machine-translated pt-BR string in the two translatable content
catalogs (`src/content/strings.ts`, `src/content/learnContent.ts`), correct each
to natural Brazilian Portuguese where needed, and remove all 98
`// TODO: native-speaker review` markers — gating the corrections behind operator
(PT-BR native speaker) approval.

Purpose: Satisfy I18N-07. The pt-BR catalogs currently ship machine-translated
copy flagged for native review by Phases 19/24/25. This phase is the planned
single review sweep that resolves every marker and locks the done-state with a
drift-guard test.

Output:
- `strings.ts` — reviewed pt-BR values, 85 trailing markers removed, header line 9 rewritten.
- `learnContent.ts` — reviewed pt-BR values, all 12 markers removed.
- `content.no-review-markers.test.ts` — new fs-scan drift-guard (D-12).
</objective>

<execution_context>
@$HOME/.claude-personal-personal/get-shit-done/workflows/execute-plan.md
@$HOME/.claude-personal-personal/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/26-pt-br-native-speaker-review/26-CONTEXT.md
@.planning/phases/26-pt-br-native-speaker-review/26-PATTERNS.md

@src/content/strings.ts
@src/content/learnContent.ts
@src/styles/theme.no-hardcoded-classes.test.ts
@src/styles/favicon.sync.test.ts

<interfaces>
<!-- Catalog shape — value-only edits MUST preserve this typing and key structure. -->
<!-- strings.ts -->
UI_STRINGS satisfies Readonly<Record<LocaleId, UiStrings>>  // LocaleId = 'en' | 'pt-BR'
  - Only the 'pt-BR' branch values change. The 'en' branch is frozen — never edit it.
  - Keys, nesting, and template-fn entries (D-15) keep their exact shape.

<!-- learnContent.ts -->
LEARN_CONTENT satisfies Readonly<Record<LocaleId, LearnContent>>
  - Fixed explainer key order ['hrv','timing','forrest'] — do not reorder.
  - learnContent.test.ts lines ~30-46 assert the clinical-verbs blacklist
    /\b(melhora|trata|cura|diagnostica|avalia)\b/i is ABSENT from pt-BR explainer bodies.
    Any pt-BR body correction MUST avoid those five verbs.

<!-- Marker shapes (differ per catalog) -->
strings.ts:      trailing inline  ->  value: 'texto', // TODO: native-speaker review   (85 real markers)
strings.ts L9:   header reference ->  // "// TODO: native-speaker review" per I18N-07. (NOT a marker — rewrite it)
learnContent.ts: standalone line  ->  // TODO: native-speaker review                  (10 markers, lines 103-139)
learnContent.ts: trailing inline  ->  label: '...', // TODO: native-speaker review     (2 markers, lines 166,170)
</interfaces>

<must_not_break>
- `src/content/lockedCopy.ts` + `src/content/lockedCopy.test.ts` — frozen-EN byte-equality guard. Do NOT touch lockedCopy.ts.
- `src/content/strings.test.ts` — Record<LocaleId, UiStrings> exhaustiveness + non-empty values. No pt-BR value may become empty; no key removed.
- `src/content/learnContent.test.ts` — structural contract + clinical-verbs blacklist on pt-BR bodies.
- The EN (`en`) branch of both catalogs — never edited, only `pt-BR` values change.
</must_not_break>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Produce the two pt-BR review tables (all 98 marked strings)</name>
  <files>.planning/phases/26-pt-br-native-speaker-review/26-REVIEW-TABLES.md</files>
  <read_first>
    - src/content/strings.ts (every pt-BR entry carrying a trailing marker — 85 of them)
    - src/content/learnContent.ts (every pt-BR entry near a marker — 12 markers, lines 103-139, 166, 170)
    - .planning/phases/26-pt-br-native-speaker-review/26-CONTEXT.md (D-01..D-09 — register, glossary, delivery format)
    - .planning/phases/26-pt-br-native-speaker-review/26-PATTERNS.md (marker mechanics per catalog)
  </read_first>
  <action>
    Write `26-REVIEW-TABLES.md` containing exactly TWO Markdown tables — one per catalog
    — covering ALL 98 marked strings (D-02, D-03): 86 from strings.ts and 12 from
    learnContent.ts. Note strings.ts has 86 raw grep hits; line 9 is a header-comment
    reference, not a string — the strings.ts table covers the 85 real marked entries.
    Confirm the count with `grep -n "native-speaker review" src/content/strings.ts`
    (expect 86 lines: line 9 + 85 entries) and
    `grep -n "native-speaker review" src/content/learnContent.ts` (expect 12).

    Each table has these columns in order: `Key/Path` · `EN` · `Current PT-BR` ·
    `Proposed PT-BR` · `CHANGED|KEPT` · `Note`.

    For each marked string, propose a native-quality Brazilian Portuguese value following:
    - D-05 register: informal `você` with NO direct address — implied você, imperatives
      where natural (e.g. `Iniciar sessão`). Calm, neutral, quiet app tone. No warm/personal phrasing.
    - D-06 glossary: HRV -> VFC (Variabilidade da Frequência Cardíaca).
    - D-07 glossary: BPM -> RPM (Respirações Por Minuto) in pt-BR ONLY. The `bpmLabel`
      pt-BR value becomes `RPM` — flag this row CHANGED, note it as a real native-speaker correction.
    - D-08: `"Resonant Breathing"` and `"Forrest Knutson"` stay in English, untranslated.
    - D-09: apply glossary terms identically across BOTH tables.
    For each row set the CHANGED|KEPT flag to your judgment (CHANGED if Proposed differs
    from Current PT-BR, KEPT if your review judges Current already correct). The Note
    column explains non-obvious choices or flags layout concerns (e.g. RPM fit in SettingsDialog).

    For any proposed correction to a `learnContent.ts` explainer BODY, do NOT use the
    words melhora, trata, cura, diagnostica, or avalia — `learnContent.test.ts` asserts
    that blacklist absent and the build fails otherwise.

    Do NOT edit strings.ts or learnContent.ts in this task — this task only produces
    the review document for operator markup.
  </action>
  <verify>
    <automated>test $(grep -c '^|' .planning/phases/26-pt-br-native-speaker-review/26-REVIEW-TABLES.md) -ge 98 &amp;&amp; grep -q 'RPM' .planning/phases/26-pt-br-native-speaker-review/26-REVIEW-TABLES.md</automated>
  </verify>
  <acceptance_criteria>
    - `26-REVIEW-TABLES.md` exists with two Markdown tables (one labeled strings.ts, one labeled learnContent.ts).
    - The strings.ts table has exactly 85 data rows; the learnContent.ts table has exactly 12 data rows (98 total marked strings).
    - Every row has all six columns populated: Key/Path, EN, Current PT-BR, Proposed PT-BR, CHANGED|KEPT, Note.
    - The `bpmLabel` row is present, Proposed PT-BR = `RPM`, flagged CHANGED.
    - HRV-related rows propose VFC; no `learnContent.ts` body proposal contains melhora/trata/cura/diagnostica/avalia.
    - strings.ts and learnContent.ts are byte-identical to their pre-task state (no edits applied yet).
  </acceptance_criteria>
  <done>Two-table review document covering all 98 marked strings exists; no catalog file edited.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Operator native-speaker review of the proposed corrections</name>
  <what-built>
    A two-table Markdown review document (`26-REVIEW-TABLES.md`) listing all 98
    machine-translated pt-BR strings with Claude's proposed native-quality
    corrections, each flagged CHANGED or KEPT.
  </what-built>
  <how-to-verify>
    1. Open `.planning/phases/26-pt-br-native-speaker-review/26-REVIEW-TABLES.md`.
    2. As the PT-BR native speaker, review every row in both tables.
    3. For each row, do one of: accept the Proposed PT-BR as-is, edit the Proposed
       PT-BR cell with your preferred wording, or change the CHANGED|KEPT flag.
    4. Confirm glossary calls land: HRV -> VFC, BPM -> RPM (D-06/D-07), and that
       "Resonant Breathing" / "Forrest Knutson" remain English (D-08).
    5. Confirm the register stays informal-você with no direct address (D-05).
    6. Mark up the file directly (edit cells / flags) and save.
  </how-to-verify>
  <resume-signal>Type "approved" once the tables are marked up, or describe the rows you want reworked.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Apply approved corrections and remove all 98 markers from both catalogs</name>
  <files>src/content/strings.ts, src/content/learnContent.ts</files>
  <read_first>
    - .planning/phases/26-pt-br-native-speaker-review/26-REVIEW-TABLES.md (operator-marked — the source of truth for every applied value)
    - src/content/strings.ts (full file — 85 trailing markers + header line 9)
    - src/content/learnContent.ts (full file — 12 markers, lines 103-139, 166, 170)
    - src/content/strings.test.ts (non-empty value + Record exhaustiveness assertions)
    - src/content/learnContent.test.ts (structural contract + clinical-verbs blacklist)
    - .planning/phases/26-pt-br-native-speaker-review/26-PATTERNS.md (Edit rule D-11; substring-vs-regex decision)
  </read_first>
  <action>
    Apply every operator-approved correction from `26-REVIEW-TABLES.md` to the two
    catalog files in one pass. Edit rule (D-11): for each marked entry, set the
    `pt-BR` value to the operator-approved Proposed PT-BR and delete the
    `// TODO: native-speaker review` marker — for KEPT rows the value is unchanged
    but the marker is STILL deleted (I18N-07: every marker removed).

    strings.ts: each marker is a trailing inline comment — delete the trailing
    ` // TODO: native-speaker review` so the line ends after the value. Touch ONLY
    the `pt-BR` branch; never edit `en` values. Resolve all 85 real markers.

    strings.ts line 9 — rewrite the header comment so it no longer contains the
    literal substring `// TODO: native-speaker review`. The current line documents a
    now-resolved hand-off; replace it with a sentence stating the pt-BR values were
    native-speaker reviewed in Phase 26 per I18N-07 (worded so the literal marker
    string does not appear). This is the chosen approach (PATTERNS.md §Substring
    option 1): it lets Task 4 use a plain substring guard, the most robust form.

    learnContent.ts: 10 markers are standalone comment lines (lines 103-139) above
    their entry — delete the whole comment line. 2 markers are trailing inline
    (lines 166, 170) — delete the trailing comment. Apply approved pt-BR body/title
    values; no `pt-BR` explainer body may contain melhora/trata/cura/diagnostica/avalia.
    Do NOT remove the `// NOTE: "inspirado nos ensinamentos..."` comment near the
    forrest body — that is a different comment, not a review marker.

    Do not touch lockedCopy.ts. Do not modify any *.test.ts file in this task.

    Verify after editing: `grep -c "native-speaker review" src/content/strings.ts`
    and `... learnContent.ts` both return 0; `grep -rl "native-speaker review" src/`
    returns nothing except possibly the new guard test (added in Task 4).
  </action>
  <verify>
    <automated>test $(grep -rl "native-speaker review" src/content/strings.ts src/content/learnContent.ts | wc -l) -eq 0 &amp;&amp; npx tsc -b &amp;&amp; npx vitest run src/content/strings.test.ts src/content/learnContent.test.ts src/content/lockedCopy.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "native-speaker review" src/content/strings.ts` returns 0.
    - `grep -c "native-speaker review" src/content/learnContent.ts` returns 0.
    - strings.ts line 9 no longer contains the literal substring `native-speaker review`.
    - The `en` branch of both catalogs is byte-identical to its pre-task state.
    - `src/content/lockedCopy.ts` is unmodified.
    - `npx tsc -b` exits 0 (catalogs still satisfy Record<LocaleId, ...>).
    - `strings.test.ts`, `learnContent.test.ts`, `lockedCopy.test.ts` all pass.
    - The applied pt-BR values match the operator-approved cells in `26-REVIEW-TABLES.md`.
  </acceptance_criteria>
  <done>All 98 markers removed, every approved correction applied, header line 9 rewritten, EN + lockedCopy untouched, catalog/byte-guard tests green.</done>
</task>

<task type="auto">
  <name>Task 4: Add the marker-guard drift-test for src/content/</name>
  <files>src/content/content.no-review-markers.test.ts</files>
  <read_first>
    - src/styles/theme.no-hardcoded-classes.test.ts (primary analog — banned-pattern fs-scan guard: walker + scan + toEqual([]))
    - src/styles/favicon.sync.test.ts (secondary analog — node-type triple-slash reference + header-comment convention)
    - .planning/phases/26-pt-br-native-speaker-review/26-PATTERNS.md (§marker-guard test — walker/scan/assert excerpts, naming convention)
  </read_first>
  <action>
    Create a new standalone Vitest fs-scan guard test at
    `src/content/content.no-review-markers.test.ts` (D-12). A standalone file is the
    chosen approach (PATTERNS.md recommendation): the existing `src/content/*.test.ts`
    files are pure in-memory import tests; an fs-scan needs `node:fs` + the Node
    triple-slash type reference, which should not contaminate those clean unit files.

    Structure, adapting `theme.no-hardcoded-classes.test.ts`:
    - Open with a header block comment naming Phase 26, decision D-12, what it guards
      (the `TODO: native-speaker review` marker absent from `src/content/`), and the
      analog file — matching the repo's guard-test header convention.
    - Add `/// <reference types="node" />` then import `describe, it, expect` from
      `vitest` and `readFileSync, readdirSync, statSync` from `node:fs`, `resolve, join`
      from `node:path`.
    - Collect the `src/content/` files: a recursive walker (or flat `readdirSync`
      filter — the folder is flat) that picks files ending `.ts` and EXCLUDES files
      ending `.test.ts`. Excluding `.test.ts` is load-bearing — this guard file itself
      contains the literal marker substring and must not flag itself.
    - Use a plain substring scan: const marker string `TODO: native-speaker review`;
      collect every non-test `src/content/` file whose contents include it; assert the
      hit array `toEqual([])` with a diagnostic message listing offending files.
    - Base directory: `resolve(__dirname)` (= `src/content`).

    This test must be GREEN immediately because Task 3 removed all markers.
  </action>
  <verify>
    <automated>npx vitest run src/content/content.no-review-markers.test.ts &amp;&amp; npx tsc -b &amp;&amp; npx eslint src/content/content.no-review-markers.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/content/content.no-review-markers.test.ts` exists, opens with a Phase 26 / D-12 header comment naming the analog.
    - The test uses `node:fs` via the `/// <reference types="node" />` triple-slash directive.
    - The file collector excludes `*.test.ts` files (so the guard does not flag its own source).
    - `npx vitest run src/content/content.no-review-markers.test.ts` passes (green — no markers remain).
    - `npx tsc -b` exits 0 and `npx eslint` reports no errors on the new file.
    - `grep "native-speaker review" src/` returns matches ONLY inside this guard test file (the const + header), nowhere else.
  </acceptance_criteria>
  <done>Standalone marker-guard test exists and is green; the I18N-07 done-state is locked against regression.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none introduced) | This phase adds no trust boundary — no new input handling, no API, no auth, no schema, no runtime data flow. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-01 | Tampering | `src/content/strings.ts` / `learnContent.ts` pt-BR values | accept | Edits are static string literals consumed by existing, already-shipped render paths. No new code path, no interpolation of untrusted data. Template-fn entries (D-15) keep their existing shape; `tsc -b` enforces type integrity. Risk is below ASVS L1 threshold. |
| T-26-02 | Tampering | frozen-EN `LOCKED_COPY` (`lockedCopy.ts`) | mitigate | Phase scope explicitly forbids touching `lockedCopy.ts`; `lockedCopy.test.ts` byte-equality `.toBe()` guard runs in the per-commit green-gate and fails the build on any drift. Guard remains untouched. |
| T-26-03 | Information Disclosure | pt-BR Learn explainer bodies | accept | Translated copy is product content with no PII and no medical/diagnostic claims; `learnContent.test.ts` clinical-verbs blacklist (`melhora\|trata\|cura\|diagnostica\|avalia`) is asserted absent and runs every commit, preventing accidental claim-shaped phrasing. |
| T-26-04 | Tampering | regression of resolved markers | mitigate | New `content.no-review-markers.test.ts` fs-scan guard fails the build if the review marker reappears anywhere in `src/content/`, locking the I18N-07 done-state. |

**Note:** The realistic threat surface for this phase is minimal — the work is value-only
edits to two PT-BR string catalogs plus one new fs-scan test. Translated string values are
static literals consumed by existing render paths; no new input handling, API, auth, or
schema is introduced. No `high`-severity threat exists; the phase is ASVS-L1 clear.
</threat_model>

<verification>
Phase-level done-gate (D-13), run after all four tasks:
- `grep "native-speaker review" src/` returns matches only inside
  `src/content/content.no-review-markers.test.ts` (the const + header) — 0 elsewhere.
- `src/content/lockedCopy.test.ts` byte-equality guard green.
- `src/content/strings.test.ts` and `src/content/learnContent.test.ts` green.
- New `src/content/content.no-review-markers.test.ts` green.
- Per-commit green-gate holds: `npx tsc -b`, `npx eslint .`, `npm run build`,
  `npx vitest run` all exit 0.
</verification>

<success_criteria>
- All 98 `// TODO: native-speaker review` markers removed from `src/content/` (85 in strings.ts, 12 in learnContent.ts).
- Every machine-translated pt-BR string was reviewed and approved by the operator (PT-BR native speaker).
- Glossary applied consistently: HRV -> VFC, BPM -> RPM (pt-BR only), "Resonant Breathing" / "Forrest Knutson" kept English.
- `strings.ts` header line 9 rewritten so the literal marker substring no longer appears.
- `content.no-review-markers.test.ts` exists, green, and guards against marker regression.
- EN values, `lockedCopy.ts`, and all existing catalog/byte-equality tests untouched and green.
- `Record<LocaleId, UiStrings>` / `Record<LocaleId, LearnContent>` type completeness intact (`tsc -b` exits 0).
</success_criteria>

<output>
After completion, create `.planning/phases/26-pt-br-native-speaker-review/26-01-SUMMARY.md`
</output>
