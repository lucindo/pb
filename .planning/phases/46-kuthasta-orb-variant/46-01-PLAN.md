---
phase: 46-kuthasta-orb-variant
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/featureFlags.ts
  - src/featureFlags.test.ts
autonomous: true
requirements:
  - KUTH-01
tags:
  - feature-flag
  - parser
  - alias-list

must_haves:
  truths:
    - "URL parser maps ?breathingShape=spiritual-eye, ?breathingShape=kuthasta, ?breathingShape=star (and case-insensitive / whitespace-trimmed variants) to the canonical 'spiritual-eye' value, and the BreathingShapeVariant union exports the new literal (D-08)."
    - "Unrecognized ?breathingShape values continue to fall back to the production default 'orb-halo'."
    - "Existing aliases for 'orb-halo' and 'minimal-rings' are unchanged — no regressions."
    - "The BreathingShapeVariant union exports the new 'spiritual-eye' literal so downstream code (Plan 03 OrbShape, future Phase 47 PREFS coerceSettings) can reference it as a typed value."
  artifacts:
    - path: src/featureFlags.ts
      provides: "BreathingShapeVariant union extended; BREATHING_SHAPE_FLAG.parse recognises the spiritual-eye alias list."
      contains: "'spiritual-eye'"
    - path: src/featureFlags.test.ts
      provides: "Alias-list coverage for spiritual-eye / kuthasta / star + case + whitespace + unrecognized fallback."
      contains: "spiritual-eye"
  key_links:
    - from: src/featureFlags.ts BREATHING_SHAPE_FLAG.parse
      to: BreathingShapeVariant union
      via: "TypeScript return-type narrowing — the new clause must return the literal 'spiritual-eye', which must exist in the union."
      pattern: "spiritual-eye"
    - from: src/featureFlags.ts readFeatureFlags
      to: BREATHING_SHAPE_FLAG.parse
      via: "Indirect via readQueryFeatureFlag (no edit needed at the aggregator — generic in the spec type)."
      pattern: "readQueryFeatureFlag.*BREATHING_SHAPE_FLAG"
---

<objective>
Extend the BreathingShapeVariant union by one literal — `'spiritual-eye'` — and add the corresponding alias clause to `BREATHING_SHAPE_FLAG.parse` so the operator can request the spike-012 V5 Halo Flame orb via `?breathingShape=spiritual-eye` (or aliases `kuthasta` / `star`). Cover the new behaviour with vitest cases in the established featureFlags.test.ts shape.

Purpose: This is the type-system + parser foundation Plans 02 and 03 depend on. Plan 03 cannot branch on `variant === 'spiritual-eye'` until that literal exists in the discriminated union exported here.

Output: One source file edit, one test file edit. Roughly +1 line in `BreathingShapeVariant` (one new union member), +1 line in `BREATHING_SHAPE_FLAG.parse` (one new alias clause), and ~10–15 new vitest cases mirroring the existing orb-halo / minimal-rings coverage.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/46-kuthasta-orb-variant/46-CONTEXT.md
@.planning/phases/46-kuthasta-orb-variant/46-PATTERNS.md
@src/featureFlags.ts
@src/featureFlags.test.ts

<interfaces>
<!-- The contract this plan extends, extracted verbatim from src/featureFlags.ts. -->
<!-- After this plan: the union has a third member and the parser has a third clause. -->

Current (`src/featureFlags.ts:7`):
```typescript
export type BreathingShapeVariant = 'orb-halo' | 'minimal-rings'
```

After this plan:
```typescript
export type BreathingShapeVariant = 'orb-halo' | 'minimal-rings' | 'spiritual-eye'
```

Current parser (`src/featureFlags.ts:64-73`):
```typescript
const BREATHING_SHAPE_FLAG = {
  queryParam: 'breathingShape',
  defaultValue: 'orb-halo' as BreathingShapeVariant,
  parse(rawValue: string): BreathingShapeVariant | null {
    const v = rawValue.trim().toLowerCase()
    if (v === 'orb-halo' || v === 'orb' || v === 'halo') return 'orb-halo'
    if (v === 'minimal-rings' || v === 'minimal' || v === 'rings') return 'minimal-rings'
    return null
  },
} satisfies QueryFeatureFlagSpec<BreathingShapeVariant>
```

After this plan, a third clause is inserted before `return null` (per CONTEXT.md D-08):
```typescript
if (v === 'spiritual-eye' || v === 'kuthasta' || v === 'star') return 'spiritual-eye'
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend BreathingShapeVariant union and add spiritual-eye alias clause to BREATHING_SHAPE_FLAG.parse</name>
  <files>src/featureFlags.ts, src/featureFlags.test.ts</files>
  <read_first>
    - src/featureFlags.ts (full file — 107 lines)
    - src/featureFlags.test.ts (full file — 144 lines)
    - .planning/phases/46-kuthasta-orb-variant/46-CONTEXT.md §`<decisions>` "Feature flag (aliases, parser, default)" (D-08, around L85-89)
    - .planning/phases/46-kuthasta-orb-variant/46-PATTERNS.md §"`src/featureFlags.ts` (config, request-response)" (lines 22–70 — the verbatim union + parser pattern and the new clause to add)
  </read_first>
  <behavior>
    - parse('spiritual-eye') === 'spiritual-eye'
    - parse('kuthasta') === 'spiritual-eye'
    - parse('star') === 'spiritual-eye'
    - Case-insensitive: parse('KUTHASTA') === 'spiritual-eye'; parse('Spiritual-Eye') === 'spiritual-eye'
    - Whitespace trimmed: parse(' star ') === 'spiritual-eye'; %20-encoded URL query reads space and is trimmed
    - Unrecognized falls back to default: readFeatureFlags('?breathingShape=garbage').breathingShape === 'orb-halo'
    - Existing aliases unchanged: parse('orb-halo'), parse('orb'), parse('halo') → 'orb-halo'; parse('minimal-rings'), parse('minimal'), parse('rings') → 'minimal-rings'
    - Empty search yields the default: readFeatureFlags('').breathingShape === 'orb-halo' (this test already exists at L36–43; no new assertion needed — guard it does not regress)
  </behavior>
  <action>
    Step 1 (src/featureFlags.ts L7) — replace the `BreathingShapeVariant` union with the three-member form:

    ```
    export type BreathingShapeVariant = 'orb-halo' | 'minimal-rings' | 'spiritual-eye'
    ```

    Step 2 (src/featureFlags.ts inside `BREATHING_SHAPE_FLAG.parse`, between L70 and L71) — insert exactly one new clause **before** the `return null` line, following the verbatim shape of L69–70 (canonical first, aliases in lexicographic order):

    ```
    if (v === 'spiritual-eye' || v === 'kuthasta' || v === 'star') return 'spiritual-eye'
    ```

    Do not touch `readFeatureFlags` (L99–106), `readQueryFeatureFlag` (L48–56), or any other flag spec (`SWITCHER_ICON_FLAG`, `ORB_IDLE_FLAG`, `RING_CUE_FLAG`). The aggregator is generic in the spec type and inherits the new variant automatically.

    Step 3 (src/featureFlags.test.ts) — add a new `describe('readFeatureFlags', () => { ... })` block (or extend the existing block — operator discretion; the existing block already groups breathingShape cases at L61–84) covering the alias matrix above. Follow the verbatim shape of the existing minimal-rings test at L65–69:

    - One `it('parses spiritual-eye and its aliases', ...)` that asserts `readFeatureFlags('?breathingShape=spiritual-eye').breathingShape`, `'?breathingShape=kuthasta'`, and `'?breathingShape=star'` all resolve to `'spiritual-eye'`.
    - One `it('spiritual-eye is case-insensitive and trims whitespace', ...)` that asserts `'?breathingShape=KUTHASTA'`, `'?breathingShape=Spiritual-Eye'`, and `'?breathingShape=%20Star%20'` all resolve to `'spiritual-eye'`. The existing test at L77–80 is the verbatim shape model.
    - **Do not** add a new "falls back to default for invalid breathingShape values" test — the existing one at L82–84 already covers the unrecognized→`'orb-halo'` path generically; adding a duplicate would be a regression-anchor and violates `[[feedback_no_design_locking]]`.

    Per `[[feedback_no_design_locking]]`: tests assert the alias→canonical mapping and the unrecognized-fallback contract, NOT specific hex / Tailwind / pixel values. No comment in the test or the source file should duplicate the spike's hex values — those live in `theme.css` only (Plan 02).

    Per `[[feedback_spike_is_design_not_features]]`: Plan 01 adds the third union member + parser clause + test coverage and **nothing else** — no new flag, no new spec object, no new alias surface beyond spiritual-eye / kuthasta / star.

    Per D-08 in CONTEXT.md: aliases are exactly `spiritual-eye` / `kuthasta` / `star` (canonical first). No additional aliases are sanctioned by the spike or by CONTEXT.md — do not invent additions like `'spiritualeye'`, `'kut'`, or `'thirdeye'`.
  </action>
  <acceptance_criteria>
    - `src/featureFlags.ts` L7 reads exactly: `export type BreathingShapeVariant = 'orb-halo' | 'minimal-rings' | 'spiritual-eye'` (single-quoted literals, pipe-separated, no trailing pipe).
    - `src/featureFlags.ts` `BREATHING_SHAPE_FLAG.parse` body contains the literal substring `if (v === 'spiritual-eye' || v === 'kuthasta' || v === 'star') return 'spiritual-eye'` on its own line, positioned **after** the `'minimal-rings'` clause and **before** `return null`.
    - The existing two clauses (orb-halo, minimal-rings) are byte-identical to their pre-plan content (use git diff to verify).
    - `npx vitest run src/featureFlags.test.ts` passes; the test count increases by exactly the new `it(...)` blocks added (typically +2).
    - `npx tsc --noEmit` passes — the new union literal type-checks against the parse return type.
    - `grep -n "spiritual-eye" src/featureFlags.ts | wc -l` returns at least 4 (one in the union, three in the parser clause — canonical + two `return` literals).
    - No new helper file is created (per `[[feedback_spike_is_design_not_features]]` — no new infrastructure).
    - No additional aliases beyond `spiritual-eye` / `kuthasta` / `star` appear in the parser clause.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/featureFlags.test.ts && npx tsc --noEmit</automated>
  </verify>
  <done>
    Type union extended to three members; parser clause added in canonical position; ~2 new vitest cases pass covering aliases + case-insensitivity + whitespace; existing tests unchanged; tsc clean.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| URL query string → typed flag value | The browser's URL `?breathingShape=…` is untrusted input; `BREATHING_SHAPE_FLAG.parse` is the validation boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-46-01 | Tampering | URL query `?breathingShape=...` | mitigate | Existing allow-list parser (`if (v === '...') return ...; return null`) returns `null` on any unknown value; `readQueryFeatureFlag` falls back to the production default `'orb-halo'`. New clause follows the same allow-list shape — no eval, no dynamic dispatch. |
| T-46-02 | Information Disclosure | URL query (shareable) reveals user preference | accept | The query string is per-tab developer-toggle only; not a secret, not PII, low risk. Phase 47 will introduce persistence but that is out of scope here. |
| T-46-03 | Denial of Service | Malformed/huge URL query | accept | `URLSearchParams` + `String.trim().toLowerCase()` are O(n) on a bounded URL length; no DoS surface beyond standard `URL` parsing. ASVS L1 V5.1.3 input-validation covered by the existing parser shape. |

## ASVS L1 Applicability

- **V5.1.3 (Input Validation)** — covered by the existing allow-list parser pattern; new clause inherits the same `null`-on-unrecognized fallback. No new validation required beyond the new alias clause.
- **V5.3 (Output Encoding)** — N/A; this plan does not emit any user-controlled string to a sink.

## Blocking severity

None. Low-stakes URL-readable feature flag extension; existing allow-list parser is the single mitigation surface.
</threat_model>

<verification>
1. `npx vitest run src/featureFlags.test.ts` — all tests pass; alias matrix asserts spiritual-eye / kuthasta / star → `'spiritual-eye'`, case-insensitive, whitespace-trimmed.
2. `npx tsc --noEmit` — clean. New union literal type-checks; downstream files (`src/components/OrbShape.tsx`) compile against the wider union (the new literal is currently un-handled in `OrbContainer`'s ternary, but TypeScript's exhaustiveness in ternary expressions is permissive — Plan 03 closes that gap).
3. `git diff src/featureFlags.ts` — exactly two lines changed (union L7, new clause inside parse), no whitespace churn elsewhere.
4. Manual smoke: `npm run dev` then visit `http://localhost:5173/?breathingShape=spiritual-eye` — DevTools console: `vm` (or equivalent feature-flags surface) returns `breathingShape: 'spiritual-eye'`. Visual appearance defaults to the still-unhandled branch (Plan 03 wires the visual); for Plan 01 the contract is the **typed value**, not the render.
</verification>

<success_criteria>
- KUTH-01 satisfied at the parser level: `?breathingShape=spiritual-eye` (+ aliases `kuthasta` / `star`) reaches a typed `BreathingShapeVariant` value of `'spiritual-eye'`; unrecognized values fall back to `'orb-halo'`.
- The union literal `'spiritual-eye'` is exported, unblocking Plan 03's variant-branching code.
- No regressions in the existing two-variant alias matrix or any other feature flag.
</success_criteria>

<output>
Create `.planning/phases/46-kuthasta-orb-variant/46-01-SUMMARY.md` when done. Include the exact diff lines from `src/featureFlags.ts` and the new test names.
</output>
</content>
</invoke>