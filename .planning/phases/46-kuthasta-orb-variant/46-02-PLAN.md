---
phase: 46-kuthasta-orb-variant
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/styles/theme.css
autonomous: true
requirements:
  - KUTH-04
tags:
  - theme
  - tokens
  - spike-locked

must_haves:
  truths:
    - "Light theme defines five new --color-*-spiritual-eye tokens (halo-1, halo-2, disc, disc-strong, star-fill + star-stroke pair) backing the spike-012 V5 Halo Flame palette — refines D-06 from 4 tokens per theme (8 total) to 5 per theme (10 total) because the spike-locked dark star fill #fafafe cannot ride currentColor from --color-breathing-on-accent (PATTERNS.md open item #2)."
    - "Dark theme defines parallel tokens with the spike-locked dark-palette values (verbatim from spike-012 README 'Locked V5 values' table). Naming follows the spike-proposed slug spiritual-eye + -strong suffix for the NK back-phase variant, mirroring the production accent / accent-strong convention (D-07)."
    - "The --color-orb-disc-spiritual-eye-strong token (per theme) is an off-spike derivation by HSL-darkening each stop of the front-phase indigo by ~12% lightness, applied uniformly; the spike-locked front-phase indigo stays verbatim (D-03 — operator may re-spec at Plan 03 UAT checkpoint if the NK front/back distinction reads too subtle)."
    - "All themable tokens remain --color-* prefixed and live inside @theme (light) or [data-theme='dark']:root (dark), per the existing convention at theme.css L23."
    - "No existing tokens are modified — halo-1/-2/-3 production values stay byte-identical (halo-3 is intentionally reused for spiritual-eye per the spike)."
  artifacts:
    - path: src/styles/theme.css
      provides: "10 new --color-orb-*-spiritual-eye tokens (5 light, 5 dark) backing the spiritual-eye disc / halos / star."
      contains: "--color-orb-disc-spiritual-eye"
  key_links:
    - from: src/styles/theme.css
      to: src/components/OrbShape.tsx (Plan 03 consumer)
      via: "CSS custom-property reference — `background: var(--color-orb-disc-spiritual-eye)` and `background: var(--color-orb-halo-1-spiritual-eye)` etc."
      pattern: "--color-orb-(disc|halo-[12]|star-(fill|stroke))-spiritual-eye"
---

<objective>
Add the spike-012 V5 Halo Flame token set to `src/styles/theme.css` — five new `--color-*-spiritual-eye` tokens per theme (× 2 = 10 declarations total), inserted into the existing `@theme` (light) and `[data-theme='dark']:root` (dark) blocks. Values are transcribed verbatim from spike-012 README "Locked V5 values" with one explicit derivation (the NK back-phase darker indigo gradient `--color-orb-disc-spiritual-eye-strong`, ~12% lightness darker per HSL stop).

Purpose: Theme tokens are the design-contract surface for Plan 03's OrbContainer variant branch. Plan 03 references these tokens by name (no hex / rgba hardcoded in TSX). Plans 01 and 02 are independent (no shared files) and run in parallel as Wave 1.

Output: ~22 lines added to `src/styles/theme.css` (10 token declarations + 2 block-header comments). No existing line is touched.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/46-kuthasta-orb-variant/46-CONTEXT.md
@.planning/phases/46-kuthasta-orb-variant/46-PATTERNS.md
@.planning/spikes/012-spiritual-eye-orb/README.md
@src/styles/theme.css

<interfaces>
<!-- The exact spike-locked V5 values that go into theme.css. Source: -->
<!-- .planning/spikes/012-spiritual-eye-orb/README.md "Locked V5 values" L165-178 -->
<!-- + CONTEXT.md "Spike-012 V5 locked values (verbatim — no re-tuning)" L62-82. -->
<!-- The two -strong values are derived (D-03), not spike-locked; method below. -->

LIGHT block tokens (insert near L20, after `--color-orb-halo-3`):
- `--color-orb-halo-1-spiritual-eye: rgba(202, 166, 98, 0.48);`
- `--color-orb-halo-2-spiritual-eye: rgba(168, 148, 116, 0.44);`
- `--color-orb-disc-spiritual-eye: radial-gradient(circle at 50% 42%, #4a5a96 0%, #34406f 60%, #2a356a 100%);`
- `--color-orb-disc-spiritual-eye-strong: radial-gradient(circle at 50% 42%, #36416d 0%, #202845 60%, #191f3e 100%);` (D-03 derivation — see below)
- `--color-orb-star-fill-spiritual-eye: #ffffff;`
- `--color-orb-star-stroke-spiritual-eye: rgba(255, 255, 255, 0.20);`

DARK block tokens (insert near L70, after `--color-orb-halo-3`):
- `--color-orb-halo-1-spiritual-eye: rgba(206, 168, 100, 0.45);`
- `--color-orb-halo-2-spiritual-eye: rgba(170, 156, 122, 0.46);`
- `--color-orb-disc-spiritual-eye: radial-gradient(circle at 50% 42%, #6c7cb6 0%, #4a5a96 60%, #38477e 100%);`
- `--color-orb-disc-spiritual-eye-strong: radial-gradient(circle at 50% 42%, #4c5d99 0%, #36416d 60%, #252f54 100%);` (D-03 derivation)
- `--color-orb-star-fill-spiritual-eye: #fafafe;`
- `--color-orb-star-stroke-spiritual-eye: rgba(255, 255, 255, 0.12);`

**D-03 darker-indigo derivation (LOCKED in this plan, operator may re-spec at UAT):**
Each stop of the front-phase indigo gradient is darkened uniformly by ~12% HSL lightness using the canonical HSL-darken procedure (`colorsys.rgb_to_hls` → `L -= 0.12` → `hls_to_rgb`). Resulting stops:

| Theme | Front (spike-locked, unchanged) | Strong (this plan, derived) |
|---|---|---|
| Light | `#4a5a96 → #34406f → #2a356a` | `#36416d → #202845 → #191f3e` |
| Dark  | `#6c7cb6 → #4a5a96 → #38477e` | `#4c5d99 → #36416d → #252f54` |

This refines CONTEXT.md D-03 "planner / executor proposes a uniform-darkening (~10–15%)" with concrete hex stops the executor types verbatim. Per `[[feedback_spike_locked_values]]`: the front-phase indigo stays spike-verbatim; the `-strong` token is a derivation for an NK back-phase state the spike did not cover, NOT a re-tuning.

**Refinement of CONTEXT.md D-06 (this plan, not in CONTEXT.md):**
CONTEXT.md D-06 declared 8 tokens total (4 per theme: halo-1, halo-2, disc, disc-strong). PATTERNS.md "Analog 2: CheckmarkGlyph → StarGlyph" L216–219 noted that `currentColor` from `--color-breathing-on-accent` does NOT match the spike's dark star fill (`#1a1d24` vs spike-locked `#fafafe`). Open item #2 in the planning context resolves this by **adding 2 more tokens per theme** (star-fill, star-stroke), bringing the total to **10 tokens (5 per theme)**. Plan 03's StarGlyph reads these tokens directly instead of inheriting via `currentColor`. This is a refinement driven by pattern mapping — not a re-tuning of any spike value (the hex strings remain verbatim).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add five spiritual-eye --color-* tokens to the light @theme block</name>
  <files>src/styles/theme.css</files>
  <read_first>
    - src/styles/theme.css L1-50 (the @theme block — the light token home)
    - src/styles/theme.css L18-20 (the existing orb-halo light tokens — the precedent block)
    - .planning/phases/46-kuthasta-orb-variant/46-CONTEXT.md §`<decisions>` "Theme tokens" (D-06, around L53-60) AND §"Spike-012 V5 locked values (verbatim — no re-tuning)" (L62-82)
    - .planning/phases/46-kuthasta-orb-variant/46-PATTERNS.md §"`src/styles/theme.css` (config, declarative)" (lines 238-289 — the verbatim placement pattern)
    - .planning/spikes/012-spiritual-eye-orb/README.md §"Locked V5 values" (L163-178 — the design contract; values transcribed verbatim)
  </read_first>
  <action>
    Locate the existing light-block orb-halo section in `src/styles/theme.css` at L18-20:

    ```
    --color-orb-halo-1: rgba(168, 173, 183, 0.22);
    --color-orb-halo-2: rgba(120, 128, 140, 0.32);
    --color-orb-halo-3: rgba(93, 104, 119, 0.50);
    ```

    Immediately after L20 (before the `--color-modal-backdrop` declaration at L21), insert a header comment + five new token declarations. Use the exact text below (the values are spike-locked per `[[feedback_spike_locked_values]]` — do NOT round, re-tune, or re-name):

    ```
    /* Spike 012 V5 spiritual-eye tokens (light).
       Source: .planning/spikes/012-spiritual-eye-orb/README.md "Locked V5 values".
       Halo-3 reuses the existing --color-orb-halo-3 above (unchanged per spike).
       --color-orb-disc-spiritual-eye-strong is derived (NK back-phase, ~12% HSL
       darker per stop); operator may re-spec at UAT. */
    --color-orb-halo-1-spiritual-eye: rgba(202, 166, 98, 0.48);
    --color-orb-halo-2-spiritual-eye: rgba(168, 148, 116, 0.44);
    --color-orb-disc-spiritual-eye: radial-gradient(circle at 50% 42%, #4a5a96 0%, #34406f 60%, #2a356a 100%);
    --color-orb-disc-spiritual-eye-strong: radial-gradient(circle at 50% 42%, #36416d 0%, #202845 60%, #191f3e 100%);
    --color-orb-star-fill-spiritual-eye: #ffffff;
    --color-orb-star-stroke-spiritual-eye: rgba(255, 255, 255, 0.20);
    ```

    Constraints:
    - All token names are `--color-*` prefixed (matches the existing convention noted at theme.css L23).
    - All declarations live inside the `@theme { … }` block (i.e. before the closing `}` at ~L50).
    - The existing `--color-orb-halo-3` at L20 is **not** re-declared — the spike explicitly reuses it (CONTEXT.md L57–60, spike README L168-169).
    - Do not introduce a `--color-orb-halo-3-spiritual-eye` alias — the symmetric name would collide conceptually with the "reuse" decision and create a maintenance trap.
    - Do not touch any other token in the file. `git diff src/styles/theme.css` should show this contiguous insertion only.

    Per `[[feedback_spike_implementation_fidelity]]` + `[[feedback_spike_locked_values]]`: the rgba and hex values above are not decisions to revisit. They are spike-locked design contracts. The only off-spike value is the `-strong` derivation (D-03), explicitly noted in the header comment.

    Per `[[feedback_no_design_locking]]`: the header comment cites the spike source path; it does NOT duplicate the spike's hex table (avoid the maintenance trap of two sources of truth for the same value). The values appear once, in the token declarations themselves.
  </action>
  <acceptance_criteria>
    - `grep -nE "color-orb-(halo-[12]|disc|star-(fill|stroke))-spiritual-eye" src/styles/theme.css | grep -v "^.*:[[:space:]]*\*" | wc -l` returns exactly 6 declaration lines for the light block (insertion check).
    - The literal substring `rgba(202, 166, 98, 0.48)` appears exactly once in the file (light halo-1 spiritual-eye).
    - The literal substring `rgba(168, 148, 116, 0.44)` appears exactly once (light halo-2 spiritual-eye).
    - The literal substring `radial-gradient(circle at 50% 42%, #4a5a96 0%, #34406f 60%, #2a356a 100%)` appears exactly once (light disc spiritual-eye, spike-verbatim).
    - The literal substring `radial-gradient(circle at 50% 42%, #36416d 0%, #202845 60%, #191f3e 100%)` appears exactly once (light disc-strong derivation).
    - The literal substring `#ffffff` appears in the file (star fill light); pre-existing `#ffffff` occurrences (L10 `--color-breathing-surface`, L14 `--color-breathing-on-accent`) remain untouched.
    - `rgba(255, 255, 255, 0.20)` appears exactly once (light star stroke).
    - The pre-existing `--color-orb-halo-1`, `--color-orb-halo-2`, `--color-orb-halo-3` declarations at L18-20 are byte-identical to their pre-plan content.
    - All inserted lines are inside `@theme { … }` (verified by checking they appear before the first occurrence of `[data-theme='dark']:root`).
    - `npm run build` succeeds; no Tailwind / PostCSS error about unknown at-rules or malformed gradient.
  </acceptance_criteria>
  <verify>
    <automated>npm run build 2>&1 | tail -20</automated>
  </verify>
  <done>
    Five spiritual-eye tokens declared in the light `@theme` block with verbatim spike-locked values + the derived `-strong` gradient; build clean; existing tokens unchanged.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add five spiritual-eye --color-* tokens to the dark [data-theme='dark']:root block</name>
  <files>src/styles/theme.css</files>
  <read_first>
    - src/styles/theme.css L55-87 (the dark `[data-theme='dark']:root { … }` block — the dark token home)
    - src/styles/theme.css L68-70 (the existing orb-halo dark tokens — the precedent block)
    - .planning/phases/46-kuthasta-orb-variant/46-CONTEXT.md §"Spike-012 V5 locked values (verbatim — no re-tuning)" (L62-82 — Dark column of the table)
    - .planning/phases/46-kuthasta-orb-variant/46-PATTERNS.md §"`src/styles/theme.css`" "Dark block (L68-70)" (lines 249-285)
    - .planning/spikes/012-spiritual-eye-orb/README.md §"Locked V5 values" (L163-178 — Dark column)
  </read_first>
  <action>
    Locate the existing dark-block orb-halo section in `src/styles/theme.css` at L68-70:

    ```
    --color-orb-halo-1: rgba(74, 78, 88, 0.30);
    --color-orb-halo-2: rgba(108, 113, 124, 0.38);
    --color-orb-halo-3: rgba(150, 156, 168, 0.52);
    ```

    Immediately after L70 (before the `--color-modal-backdrop` declaration at L71), insert a header comment + five new token declarations with the spike-locked DARK palette:

    ```
    /* Spike 012 V5 spiritual-eye tokens (dark).
       Source: .planning/spikes/012-spiritual-eye-orb/README.md "Locked V5 values".
       Halo-3 reuses the existing --color-orb-halo-3 above (unchanged per spike). */
    --color-orb-halo-1-spiritual-eye: rgba(206, 168, 100, 0.45);
    --color-orb-halo-2-spiritual-eye: rgba(170, 156, 122, 0.46);
    --color-orb-disc-spiritual-eye: radial-gradient(circle at 50% 42%, #6c7cb6 0%, #4a5a96 60%, #38477e 100%);
    --color-orb-disc-spiritual-eye-strong: radial-gradient(circle at 50% 42%, #4c5d99 0%, #36416d 60%, #252f54 100%);
    --color-orb-star-fill-spiritual-eye: #fafafe;
    --color-orb-star-stroke-spiritual-eye: rgba(255, 255, 255, 0.12);
    ```

    Constraints (same as Task 1, applied to the dark block):
    - All declarations live inside `[data-theme='dark']:root { … }` (before the closing `}` at ~L87).
    - Token NAMES are byte-identical to the light block (only the VALUES differ — this is the per-theme token-swap pattern the rest of the file follows; e.g. light `--color-orb-halo-1` is a slate rgba, dark `--color-orb-halo-1` is a deeper slate rgba; they share the name).
    - Existing dark tokens at L68-70 stay byte-identical.
    - The star-fill in dark is `#fafafe` (NOT `#ffffff` — this is the spike-locked dark value per README "Locked V5 values" L172 and the reason Plan 02 ships a dedicated star-fill token instead of using `currentColor` from `--color-breathing-on-accent`, which is `#1a1d24` in dark — see PATTERNS.md L216-219 + this plan's `<interfaces>` block "Refinement of CONTEXT.md D-06").
    - No new selector / block is created; everything goes inside the existing `[data-theme='dark']:root { … }` braces.

    Per `[[feedback_spike_locked_values]]`: the dark values above are spike-verbatim. The `-strong` derivation uses the same uniform HSL-darken procedure as light (~12% lightness per stop).
  </action>
  <acceptance_criteria>
    - `grep -nE "color-orb-(halo-[12]|disc|star-(fill|stroke))-spiritual-eye" src/styles/theme.css | wc -l` returns exactly **12** total declaration lines (6 in light + 6 in dark; the comment headers do not match this pattern since they include `*` prefix).
    - The literal substring `rgba(206, 168, 100, 0.45)` appears exactly once (dark halo-1 spiritual-eye).
    - The literal substring `rgba(170, 156, 122, 0.46)` appears exactly once (dark halo-2 spiritual-eye).
    - The literal substring `radial-gradient(circle at 50% 42%, #6c7cb6 0%, #4a5a96 60%, #38477e 100%)` appears exactly once (dark disc spiritual-eye, spike-verbatim).
    - The literal substring `radial-gradient(circle at 50% 42%, #4c5d99 0%, #36416d 60%, #252f54 100%)` appears exactly once (dark disc-strong derivation).
    - The literal substring `#fafafe` appears exactly once (dark star fill — confirms it's NOT defaulted back to `#ffffff`).
    - `rgba(255, 255, 255, 0.12)` appears exactly once (dark star stroke).
    - All 6 new dark declarations appear **after** the line containing `[data-theme='dark']:root {` and **before** the next top-level `}` that closes that block.
    - The pre-existing `--color-orb-halo-1`, `--color-orb-halo-2`, `--color-orb-halo-3` declarations at L68-70 are byte-identical to their pre-plan content.
    - `npm run build` succeeds.
  </acceptance_criteria>
  <verify>
    <automated>npm run build 2>&1 | tail -20 && grep -c "spiritual-eye" src/styles/theme.css</automated>
  </verify>
  <done>
    Five spiritual-eye tokens declared in the dark `[data-theme='dark']:root` block with verbatim spike-locked dark values + the derived `-strong` gradient + per-theme star fill/stroke; build clean.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Stylesheet → rendered DOM | CSS is a build-time / load-time artifact; no untrusted input crosses this boundary in this plan. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-46-04 | Tampering | Theme tokens (build-time CSS) | accept | CSS custom properties are declarative and shipped as part of the build bundle; an attacker who can modify the bundle has already won. Standard SRI / deploy-pipeline trust applies; no additional mitigation needed for this plan. |
| T-46-05 | Information Disclosure | Token values reveal design palette | accept | Design tokens are inherently public (shipped to the browser); the spike-012 README that documents them is itself in the repo. No secret involved. |
| T-46-06 | Tampering (supply chain) | New token values introduced | accept | Plan 02 introduces no new dependencies — pure CSS edit. No npm install, no third-party package. The "Package Legitimacy Audit" gate is not triggered. |

## ASVS L1 Applicability

- **V14.1.1 (Security configuration)** — N/A for stylesheet-only changes.
- **V5.3 (Output Encoding)** — N/A; no user-controlled string enters the stylesheet.

## Blocking severity

None. Stylesheet-only token additions; no executable code, no input handling, no dependency change.
</threat_model>

<verification>
1. `npm run build` — clean. Tailwind / PostCSS does not fail on the new gradient or rgba syntax.
2. `grep -c "spiritual-eye" src/styles/theme.css` — at least 12 (10 token declarations + ≥ 2 comment-header lines).
3. `git diff src/styles/theme.css` — two contiguous insertions (one in @theme, one in [data-theme='dark']:root); no other line changed.
4. Manual smoke (Plan 03 will exercise the visual; this verification is token-only): in DevTools console with the app running, `getComputedStyle(document.documentElement).getPropertyValue('--color-orb-disc-spiritual-eye')` returns the light gradient string. Toggle dark theme and re-read — returns the dark gradient.
</verification>

<success_criteria>
- KUTH-04 satisfied at the token layer: light and dark themes each expose the spike-012-locked per-theme palette via `--color-orb-*-spiritual-eye` tokens.
- The token names form a consistent vocabulary (`-spiritual-eye` suffix; `-strong` for the NK back-phase variant; `-fill` / `-stroke` for the star) that Plan 03 references by name.
- No existing token is modified — `git diff` of L1-87 shows only insertions in two named locations.
</success_criteria>

<output>
Create `.planning/phases/46-kuthasta-orb-variant/46-02-SUMMARY.md` when done. Include the verbatim token blocks added (light + dark) and the D-03 derivation method confirmation.
</output>
</content>
</invoke>