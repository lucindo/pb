---
phase: 06-learning-claim-safe-positioning
plan: 2
type: execute
wave: 2
depends_on:
  - 06-01-PLAN.md
files_modified:
  - src/content/learnContent.ts
  - src/content/learnContent.test.ts
autonomous: true
requirements:
  - LEARN-01
  - LEARN-02
  - LEARN-03
  - LEARN-04

must_haves:
  truths:
    - "A single typed content module is the source of truth for the Phase 6 Learn modal copy and link set."
    - "The explainer contains three sections in fixed order: hrv, timing, forrest (D-08)."
    - "The forrest section body contains the verbatim phrase `inspired by Forrest's teachings` (D-11)."
    - "The timing section body paraphrases the three PROJECT.md rules: fewer than 7 breaths per minute, no pauses between inhale and exhale, exhale longer than inhale for asymmetric ratios."
    - "No explainer body uses forbidden clinical verbs: `improves`, `treats`, `cures`, `heals`, `diagnoses` (D-08 / D-14 claim-safe constraint)."
    - "Five labelled link entries exist: youtubeChannel, website, book, heroVideo, plus a keyVideos array (0–5 entries)."
    - "The book link URL is exactly `https://amzn.to/3RTAVqi` (D-12 item 3 locked)."
    - "Disclaimer copy is NOT in this asset — it stays inline in LearnDialog.tsx (CONTEXT.md §Established Patterns + UI-SPEC Content Asset Contract)."
  artifacts:
    - path: "src/content/learnContent.ts"
      provides: "LEARN_CONTENT export with explainer + links sub-records, plus LearnContent / ExplainerSection / LearnLink interfaces."
      contains: "export const LEARN_CONTENT"
      min_lines: 60
    - path: "src/content/learnContent.test.ts"
      provides: "Unit tests asserting locked phrase, forbidden-verbs absence, locked book URL, key videos count bounds."
      contains: "describe('LEARN_CONTENT"
  key_links:
    - from: "src/content/learnContent.ts"
      to: ".planning/phases/06-learning-claim-safe-positioning/06-URLS.md"
      via: "URLs inlined verbatim from the hand-off file"
      pattern: "https://amzn.to/3RTAVqi"
---

<objective>
Create the section-keyed, typed content asset `src/content/learnContent.ts` that is the single source of truth for the Phase 6 Learn modal's explainer prose and curated link set. The drafted explainer prose is committed into the file in this plan so the LearnDialog plan (Plan 03) can render against a stable contract.

Purpose: CONTEXT.md D-10 locks the content shape (i18n-ready, section-keyed, typed, no new dependency). D-09 says Claude drafts the explainer copy. D-11 locks the verbatim phrase `inspired by Forrest's teachings`. D-12 locks the link set and the book URL.
Output: `src/content/learnContent.ts` (typed record + drafted prose + inlined URLs from Plan 01) and `src/content/learnContent.test.ts` (asserts the locked content contract).
</objective>

<execution_context>
@$HOME/.claude-personal-personal/get-shit-done/workflows/execute-plan.md
@$HOME/.claude-personal-personal/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-learning-claim-safe-positioning/06-CONTEXT.md
@.planning/phases/06-learning-claim-safe-positioning/06-UI-SPEC.md
@.planning/phases/06-learning-claim-safe-positioning/06-PATTERNS.md
@.planning/phases/06-learning-claim-safe-positioning/06-URLS.md
@src/domain/settings.ts

<interfaces>
Pattern: `as const satisfies` typed exported record. Source: src/domain/settings.ts lines 10–43.
Required exports from src/content/learnContent.ts (names locked):
  - `export interface ExplainerSection { readonly title: string; readonly body: string }`
  - `export interface LearnLink { readonly label: string; readonly url: string }`
  - `export interface LearnContent { readonly explainer: { readonly hrv: ExplainerSection; readonly timing: ExplainerSection; readonly forrest: ExplainerSection }; readonly links: { readonly youtubeChannel: LearnLink; readonly website: LearnLink; readonly book: LearnLink; readonly heroVideo: LearnLink; readonly keyVideos: readonly LearnLink[] } }`
  - `export const LEARN_CONTENT: LearnContent` populated and closed with `as const satisfies LearnContent`.
</interfaces>

## Drafted Explainer Prose (locked at plan-phase; executor writes verbatim)

The executor MUST inline the following three section bodies into `LEARN_CONTENT.explainer.{hrv,timing,forrest}.body` exactly as written. Total length is ~155 words, within the D-09 120–200 target.

### Section 1 — title `What is HRV / resonance breathing` (key: `hrv`)
Body (verbatim):

```
HRV breathing is a calm practice of slow paced breaths, usually fewer than seven per minute. At that low rate your breath gently aligns with your heart's natural rhythm — a state sometimes called resonance breathing. This is a quiet practice, not a clinical procedure or a measurement of your heart.
```

### Section 2 — title `How this app times your breath` (key: `timing`)
Body (verbatim — paraphrases the three PROJECT.md rules):

```
This app guides one continuous inhale and exhale, with no pause held between them. You choose a slow rate under seven breaths per minute, and for uneven patterns the exhale is always the longer side. The on-screen orb and the optional bowl-like tones simply mark where you are in each breath — the timing itself does the work.
```

### Section 3 — title `Who is Forrest Knutson` (key: `forrest`)
Body (verbatim — MUST contain `inspired by Forrest's teachings`):

```
Forrest Knutson is a teacher who has shared HRV breathing practice for many years on YouTube. This is an independent web app inspired by Forrest's teachings, made so anyone can follow a calm paced breath from a browser. The links below point to his channel, his site, and a hand-picked starting video.
```

</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 06-URLS.md → source code | URLs from the hand-off file cross into a `.ts` source module that will be bundled and rendered in the browser. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-04 | Tampering | URL strings in LEARN_CONTENT | mitigate | Test `learnContent.test.ts` asserts every URL begins with `https://` and that none of `javascript:`, `data:`, `vbscript:` appears anywhere in the module. |
| T-06-05 | Information Disclosure | Inline explainer prose | mitigate | Test asserts the forbidden clinical verbs (`improves`, `treats`, `cures`, `heals`, `diagnoses`) do not appear in any explainer body — locks LEARN-04 framing into a CI-enforceable assertion. |
| T-06-06 | Repudiation | Locked book URL | mitigate | Test asserts `LEARN_CONTENT.links.book.url === 'https://amzn.to/3RTAVqi'` exactly. Drift would silently change the destination of the Book link. |

</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create src/content/learnContent.ts with typed record and drafted prose</name>
  <files>src/content/learnContent.ts</files>
  <behavior>
    - Module exports four symbols: `ExplainerSection` interface, `LearnLink` interface, `LearnContent` interface, `LEARN_CONTENT` constant.
    - `LEARN_CONTENT.explainer` has three keys in the order: `hrv`, `timing`, `forrest` (TS object key order is insertion order; test relies on this).
    - `LEARN_CONTENT.explainer.hrv.title === 'What is HRV / resonance breathing'`.
    - `LEARN_CONTENT.explainer.timing.title === 'How this app times your breath'`.
    - `LEARN_CONTENT.explainer.forrest.title === 'Who is Forrest Knutson'`.
    - `LEARN_CONTENT.explainer.forrest.body` contains the substring `inspired by Forrest's teachings`.
    - No explainer body contains any of: `improves`, `treats`, `cures`, `heals`, `diagnoses` (case-insensitive).
    - `LEARN_CONTENT.links.youtubeChannel.label === 'YouTube channel'`.
    - `LEARN_CONTENT.links.website.label === 'Website'`.
    - `LEARN_CONTENT.links.book.label === 'Book'`.
    - `LEARN_CONTENT.links.book.url === 'https://amzn.to/3RTAVqi'`.
    - `LEARN_CONTENT.links.heroVideo.label` and `.url` are non-empty strings; URL starts with `https://`.
    - `LEARN_CONTENT.links.keyVideos` is a readonly array of length 0–5, each item with non-empty `label` and `url` starting with `https://`.
    - No string property anywhere in `LEARN_CONTENT` contains `javascript:`, `data:`, or `vbscript:`.
  </behavior>
  <action>
    Read `.planning/phases/06-learning-claim-safe-positioning/06-URLS.md` and parse the locked URLs + titles. Use the Write tool to create `src/content/learnContent.ts`.

    Module structure (in this order from top to bottom):

    1. A leading comment block stating: source file authority is CONTEXT.md D-10; section keys are i18n-stable identifiers for v2 I18N-01; English-only in v1; disclaimer copy is intentionally inlined in `LearnDialog.tsx` per CONTEXT.md §Established Patterns (do NOT add disclaimer strings to this module).
    2. `export interface ExplainerSection { readonly title: string; readonly body: string }`.
    3. `export interface LearnLink { readonly label: string; readonly url: string }`.
    4. `export interface LearnContent { ... }` — `explainer` sub-record with `hrv`, `timing`, `forrest` (each `ExplainerSection`); `links` sub-record with `youtubeChannel`, `website`, `book`, `heroVideo` (each `LearnLink`) and `keyVideos: readonly LearnLink[]`.
    5. `export const LEARN_CONTENT: LearnContent = { ... } as const satisfies LearnContent` — populate every field using the drafted prose from the `<context>` block (verbatim, do not paraphrase) and the URLs/titles read from `06-URLS.md`. Use the `as const satisfies LearnContent` ending exactly as `src/domain/settings.ts` lines 24/26/43 do.

    The `links.book.url` field MUST contain the literal string `https://amzn.to/3RTAVqi` and the `links.book.label` field MUST be the literal `Book`. The `links.youtubeChannel.label` MUST be `YouTube channel`. The `links.website.label` MUST be `Website`. The `links.heroVideo.label` MUST be the title string the user supplied for the hero video (read from `06-URLS.md` row keyed `heroVideo`). The `links.keyVideos` array order MUST match the order in `06-URLS.md`.

    Forbidden in this file: any disclaimer string (`This is guided breathing practice — not medical advice.` and `Independent project. Not affiliated with Forrest Knutson.`) — those belong in `LearnDialog.tsx` per CONTEXT.md §Established Patterns and UI-SPEC §Content Asset Contract row "Disclaimer copy". Also forbidden: any `import` statement (this is a leaf data module with zero dependencies).
  </action>
  <read_first>
    - src/content/learnContent.ts (file does not yet exist — confirm with `ls src/content/ 2>/dev/null` returning either "No such file" or no `learnContent.ts` entry; if the directory must be created use the Write tool which creates it implicitly).
    - .planning/phases/06-learning-claim-safe-positioning/06-URLS.md (source of truth for URLs + titles)
    - .planning/phases/06-learning-claim-safe-positioning/06-CONTEXT.md (D-08, D-09, D-10, D-11, D-12 — explainer + link contract)
    - .planning/phases/06-learning-claim-safe-positioning/06-UI-SPEC.md (Content Asset Contract row; Copywriting Contract table for locked labels)
    - .planning/phases/06-learning-claim-safe-positioning/06-PATTERNS.md (§`src/content/learnContent.ts` — typed record + `as const satisfies` pattern)
    - src/domain/settings.ts (analog for `as const satisfies` literal-narrowing pattern)
  </read_first>
  <acceptance_criteria>
    - File exists at exact path `src/content/learnContent.ts`.
    - Module exports the four required symbols: `grep -c '^export interface ExplainerSection' src/content/learnContent.ts` returns `1`; same for `^export interface LearnLink`, `^export interface LearnContent`, `^export const LEARN_CONTENT`.
    - File ends `LEARN_CONTENT` declaration with `as const satisfies LearnContent` (assert via `grep -c 'as const satisfies LearnContent' src/content/learnContent.ts` returns `1`).
    - File contains the locked phrase: `grep -F "inspired by Forrest's teachings" src/content/learnContent.ts | grep -v '^[[:space:]]*//' | grep -c "inspired by Forrest's teachings"` returns `1`.
    - File contains locked book URL: `grep -c 'https://amzn.to/3RTAVqi' src/content/learnContent.ts` returns `1`.
    - File contains the three section titles verbatim: `grep -c "What is HRV / resonance breathing" src/content/learnContent.ts` returns `1`; same for `"How this app times your breath"` and `"Who is Forrest Knutson"`.
    - File does NOT contain any forbidden clinical verb in non-comment lines: `grep -v '^[[:space:]]*//' src/content/learnContent.ts | grep -iE '\b(improves|treats|cures|heals|diagnoses)\b'` returns no matches.
    - File does NOT contain disclaimer copy: `grep -c 'not medical advice' src/content/learnContent.ts` returns `0`; `grep -c 'Not affiliated with Forrest Knutson' src/content/learnContent.ts` returns `0`.
    - File has zero imports: `grep -c '^import ' src/content/learnContent.ts` returns `0`.
    - File does NOT contain dangerous URL schemes: `grep -E '(javascript:|data:|vbscript:)' src/content/learnContent.ts` returns no matches.
    - TypeScript compile succeeds: `npx tsc --noEmit -p tsconfig.app.json 2>&1 | grep -c learnContent` returns `0` (no errors mentioning learnContent).
  </acceptance_criteria>
  <verify>
    <automated>test -f src/content/learnContent.ts && grep -c 'as const satisfies LearnContent' src/content/learnContent.ts | grep -q '^1$' && grep -c 'https://amzn.to/3RTAVqi' src/content/learnContent.ts | grep -q '^1$' && grep -Fc "inspired by Forrest's teachings" src/content/learnContent.ts | grep -q '^1$' && ! grep -v '^[[:space:]]*//' src/content/learnContent.ts | grep -iqE '\b(improves|treats|cures|heals|diagnoses)\b' && ! grep -qE '(javascript:|data:|vbscript:)' src/content/learnContent.ts && npx tsc --noEmit -p tsconfig.app.json 2>&1 | (! grep -q learnContent) && echo PASS</automated>
  </verify>
  <done>`src/content/learnContent.ts` exists with all four exports, the three section titles verbatim, the locked phrase verbatim, the locked book URL, zero clinical verbs in prose, zero disclaimer copy (intentionally inline-in-dialog), zero imports, and `tsc --noEmit` succeeds.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create src/content/learnContent.test.ts asserting the locked content contract</name>
  <files>src/content/learnContent.test.ts</files>
  <behavior>
    - Test suite `LEARN_CONTENT structural contract` asserts:
      - `LEARN_CONTENT.explainer` has exactly three keys, in the order `['hrv', 'timing', 'forrest']` (via `Object.keys(LEARN_CONTENT.explainer)`).
      - Each section has a non-empty `title` and `body` string.
    - Test suite `LEARN_CONTENT locked copy contract` asserts:
      - `LEARN_CONTENT.explainer.forrest.body.includes("inspired by Forrest's teachings")` is true (D-11).
      - For each section body, none of the regex `/\b(improves|treats|cures|heals|diagnoses)\b/i` matches (D-08 / LEARN-04 claim-safety).
    - Test suite `LEARN_CONTENT link contract` asserts:
      - `LEARN_CONTENT.links.book.url === 'https://amzn.to/3RTAVqi'` (D-12 item 3 lock).
      - `LEARN_CONTENT.links.book.label === 'Book'`.
      - `LEARN_CONTENT.links.youtubeChannel.label === 'YouTube channel'` (LEARN-01).
      - `LEARN_CONTENT.links.website.label === 'Website'`.
      - For each of `youtubeChannel`, `website`, `book`, `heroVideo`: `.url.startsWith('https://')` is true (T-06-04 mitigation).
      - `LEARN_CONTENT.links.keyVideos.length` is between 0 and 5 inclusive (D-12 item 5 — LEARN-02).
      - Every `keyVideos[i].url.startsWith('https://')`.
      - No URL across the entire `links` sub-record matches `/^(javascript|data|vbscript):/i`.
  </behavior>
  <action>
    Use the Write tool to create `src/content/learnContent.test.ts`. Follow the existing vitest convention used by `src/components/ResetStatsDialog.test.tsx` for imports — use `describe`, `it`, `expect` from `vitest`. Do NOT import React/jsdom helpers (this is a pure-data module test).

    Structure: three `describe` blocks (`LEARN_CONTENT structural contract`, `LEARN_CONTENT locked copy contract`, `LEARN_CONTENT link contract`) implementing the assertions listed in `<behavior>`. Import the symbols under test with `import { LEARN_CONTENT } from './learnContent'`.

    For the section-key-order assertion, use `expect(Object.keys(LEARN_CONTENT.explainer)).toEqual(['hrv', 'timing', 'forrest'])`. For the forbidden-verb assertion, iterate `(['hrv','timing','forrest'] as const)` and for each section assert `expect(LEARN_CONTENT.explainer[key].body).not.toMatch(/\b(improves|treats|cures|heals|diagnoses)\b/i)`. For the dangerous-scheme assertion, gather all URL strings (`book.url`, `website.url`, `youtubeChannel.url`, `heroVideo.url`, `...keyVideos.map(v => v.url)`) into a single array and assert none match `/^(javascript|data|vbscript):/i`.
  </action>
  <read_first>
    - src/content/learnContent.ts (just-created from Task 1 — the symbols under test)
    - src/components/ResetStatsDialog.test.tsx (vitest convention for imports + describe/it/expect)
  </read_first>
  <acceptance_criteria>
    - File exists at exact path `src/content/learnContent.test.ts`.
    - Test suite runs and all assertions pass: `npx vitest run src/content/learnContent.test.ts --reporter=basic` exits 0.
    - Test file imports symbols from `./learnContent`: `grep -c "from './learnContent'" src/content/learnContent.test.ts` returns `1`.
    - Test file has the three describe blocks: `grep -cE "describe\\('LEARN_CONTENT (structural|locked copy|link) contract'" src/content/learnContent.test.ts` returns `3`.
    - All assertions reference the contract values directly (no skipped/xit/todo tests): `grep -cE '(xit|it\\.skip|it\\.todo)' src/content/learnContent.test.ts` returns `0`.
  </acceptance_criteria>
  <verify>
    <automated>test -f src/content/learnContent.test.ts && npx vitest run src/content/learnContent.test.ts --reporter=basic 2>&1 | grep -E '(passed|FAIL)' | head -5 && npx vitest run src/content/learnContent.test.ts --reporter=basic 2>&1 | grep -q 'Tests.*passed' && grep -cE "describe\('LEARN_CONTENT (structural|locked copy|link) contract'" src/content/learnContent.test.ts | grep -q '^3$' && ! grep -qE '(xit|it\.skip|it\.todo)' src/content/learnContent.test.ts && echo PASS</automated>
  </verify>
  <done>Test file exists, all assertions pass under vitest, no skipped/todo placeholders, asserts the locked phrase + locked book URL + clinical-verb absence + dangerous-scheme absence + keyVideos count bounds + section key order.</done>
</task>

</tasks>

<verification>
After both tasks, run `npx vitest run src/content/learnContent.test.ts --reporter=basic` and confirm all assertions pass. Run `npx tsc --noEmit -p tsconfig.app.json` and confirm zero new TypeScript errors. The content asset is now the locked source of truth that Plan 03 will import.
</verification>

<success_criteria>
- `src/content/learnContent.ts` exports `LEARN_CONTENT`, `LearnContent`, `LearnLink`, `ExplainerSection`.
- Verbatim locked phrase `inspired by Forrest's teachings` appears exactly once in the forrest section body.
- Verbatim locked book URL `https://amzn.to/3RTAVqi` appears exactly once.
- Three section titles match the UI-SPEC Copywriting Contract verbatim.
- No clinical verbs in any explainer body.
- No disclaimer copy in the content asset (it stays inline in the dialog per CONTEXT.md).
- `learnContent.test.ts` passes under vitest.
</success_criteria>

<output>
After completion, create `.planning/phases/06-learning-claim-safe-positioning/06-02-SUMMARY.md` summarising: content asset created with drafted prose + URLs from Plan 01; test file locks the contract; downstream consumer is Plan 03 LearnDialog.
</output>
