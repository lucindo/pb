---
phase: 06-learning-claim-safe-positioning
plan: 1
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/06-learning-claim-safe-positioning/06-URLS.md
autonomous: false
requirements:
  - LEARN-01
  - LEARN-02

must_haves:
  truths:
    - "Executor has YouTube channel URL captured (LEARN-01)."
    - "Executor has Forrest website URL captured."
    - "Executor has exactly one hero video URL + title captured (LEARN-02)."
    - "Executor has between 0 and 5 additional key video URLs + titles captured (LEARN-02; zero acceptable per CONTEXT.md D-12 item 5)."
    - "Book URL is already locked at https://amzn.to/3RTAVqi (no gather needed — D-12 item 3)."
    - "All gathered values land in a single source-of-truth file that the content asset plan can read."
  artifacts:
    - path: ".planning/phases/06-learning-claim-safe-positioning/06-URLS.md"
      provides: "Locked Forrest URLs + titles for Plan 02 content asset."
      contains: "# Phase 6 — Forrest URL Hand-off"
  key_links:
    - from: "06-URLS.md"
      to: "06-02-PLAN.md (content asset)"
      via: "Plan 02 read_first explicitly cites this file as the source of truth for link URLs"
      pattern: "06-URLS\\.md"
---

<objective>
Capture the deferred Forrest URLs from the user and write them into a single source-of-truth file (`06-URLS.md`) inside the phase directory. The orchestrator-level decision was to defer Forrest URL collection from research/discuss to execute-phase, but Plan 02 cannot build the content asset until these values exist. This plan is a single human-interaction task that runs before any code is touched.

Purpose: Plan 02 needs concrete URLs + titles to inline into `src/content/learnContent.ts`. The locked book URL (`https://amzn.to/3RTAVqi` — CONTEXT.md D-12 item 3) is the only URL pre-locked; all others must be gathered.
Output: `.planning/phases/06-learning-claim-safe-positioning/06-URLS.md`
</objective>

<execution_context>
@$HOME/.claude-personal-personal/get-shit-done/workflows/execute-plan.md
@$HOME/.claude-personal-personal/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-learning-claim-safe-positioning/06-CONTEXT.md
@.planning/phases/06-learning-claim-safe-positioning/06-UI-SPEC.md
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user → planner artifact | User-supplied URLs cross into a markdown file that downstream agents will inline into source. Untrusted text in URL position. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Tampering | URL hand-off file | mitigate | User confirms URL set before file is written; each URL must start with `https://` and target a known Forrest property (youtube.com, forrest's domain, amzn.to, or youtube watch URL). Reject any URL using `javascript:`, `data:`, or `vbscript:` schemes — none of these are valid for external link rendering. |
| T-06-02 | Information Disclosure | Affiliate parameters | accept | Forrest's published Amazon link is reused as-is (D-12). No new affiliate tag is appended. Already covered by D-14 affiliation micro-line. |
| T-06-03 | Spoofing | External-link recipients | mitigate (downstream) | Plan 03 enforces `target="_blank" rel="noopener noreferrer"` on every rendered link (CONTEXT.md D-07). This plan only records URLs; the security default is applied at render time. |

</threat_model>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Gather deferred Forrest URLs and write 06-URLS.md</name>
  <files>.planning/phases/06-learning-claim-safe-positioning/06-URLS.md</files>
  <read_first>
    - .planning/phases/06-learning-claim-safe-positioning/06-CONTEXT.md (D-12 link set; D-11 locked phrase; D-13 plain-text-label rule)
    - .planning/phases/06-learning-claim-safe-positioning/06-UI-SPEC.md (Copywriting Contract table — fixed labels for YouTube channel / Website / Book / Hero video / Key videos)
  </read_first>
  <action>
    Prompt the user for each of the following values, one prompt per line, in this exact order:

    1. Forrest Knutson YouTube channel URL (LEARN-01 — D-12 item 1). Expect a `https://www.youtube.com/...` or `https://youtube.com/@...` form.
    2. Forrest Knutson personal website URL (D-12 item 2). Expect any `https://` URL.
    3. Hero video URL — exactly one designated "start-here" Forrest video (D-12 item 4 — LEARN-02). Expect `https://www.youtube.com/watch?v=...` or shortform.
    4. Hero video title — the visible label string the user wants rendered for the hero video.
    5. Key video URLs + titles — between 0 and 5 additional curated videos (D-12 item 5 — LEARN-02). Ask the user to supply pairs of URL + title; if the user supplies zero, accept zero (CONTEXT.md D-12 item 5 explicitly permits a hero-only ship for LEARN-02 satisfaction).

    Do NOT prompt for the book URL — it is locked at `https://amzn.to/3RTAVqi` per CONTEXT.md D-12 item 3. Stamp it into the file as the locked value.

    For each user-supplied URL: reject inputs that do not start with `https://`, and reject inputs whose scheme is `javascript:`, `data:`, or `vbscript:` (T-06-01 threat mitigation). On a rejected input, re-prompt with the specific reason.

    Once all values are gathered, use the Write tool to create `.planning/phases/06-learning-claim-safe-positioning/06-URLS.md` with the following structure:

      - YAML frontmatter: `phase: 06-learning-claim-safe-positioning`, `gathered_at:` ISO timestamp, `status: locked`.
      - `# Phase 6 — Forrest URL Hand-off` H1.
      - Section `## Locked URLs` with a markdown table whose columns are `Key | Label | URL | Title (if applicable)`. Rows in this order: `youtubeChannel`, `website`, `book`, `heroVideo`, `keyVideos[0]`..`keyVideos[N-1]` (N may be 0). The `book` row's URL column MUST contain `https://amzn.to/3RTAVqi` verbatim. The `Label` column values MUST be exactly `YouTube channel`, `Website`, `Book`, and the hero/key video labels are the user-supplied titles.
      - Section `## Notes` containing a one-liner stating that the keys in the first column map 1:1 to the `LEARN_CONTENT.links.*` field names that Plan 02 will create in `src/content/learnContent.ts` (`youtubeChannel`, `website`, `book`, `heroVideo`, `keyVideos`).

    Output a short confirmation to the user listing all gathered URLs by label before pausing. Do not proceed if the user explicitly rejects the summary; re-prompt as needed.
  </action>
  <acceptance_criteria>
    - File exists at exact path `.planning/phases/06-learning-claim-safe-positioning/06-URLS.md` (verifiable by `test -f .planning/phases/06-learning-claim-safe-positioning/06-URLS.md && echo OK`).
    - File contains the literal string `https://amzn.to/3RTAVqi` exactly once (book row); verify with `grep -c 'https://amzn.to/3RTAVqi' .planning/phases/06-learning-claim-safe-positioning/06-URLS.md` returns `1`.
    - File contains rows labelled exactly `YouTube channel`, `Website`, `Book`; verify each with `grep -c '| YouTube channel |' 06-URLS.md`, `grep -c '| Website |' 06-URLS.md`, `grep -c '| Book |' 06-URLS.md` each returning `1`.
    - The `youtubeChannel`, `website`, `heroVideo` rows each have a URL column whose first 8 characters are `https://` (verifiable by reading the file and asserting per-row prefix).
    - The `keyVideos[*]` row count is between 0 and 5 inclusive.
    - No URL in any row begins with `javascript:`, `data:`, or `vbscript:` (verify with `grep -E '(javascript:|data:|vbscript:)' 06-URLS.md` returns no matches).
    - The file's YAML frontmatter `status` field equals `locked`.
  </acceptance_criteria>
  <verify>
    <automated>test -f .planning/phases/06-learning-claim-safe-positioning/06-URLS.md && grep -c 'https://amzn.to/3RTAVqi' .planning/phases/06-learning-claim-safe-positioning/06-URLS.md | grep -q '^1$' && grep -c '| YouTube channel |' .planning/phases/06-learning-claim-safe-positioning/06-URLS.md | grep -q '^1$' && grep -c '| Website |' .planning/phases/06-learning-claim-safe-positioning/06-URLS.md | grep -q '^1$' && grep -c '| Book |' .planning/phases/06-learning-claim-safe-positioning/06-URLS.md | grep -q '^1$' && ! grep -qE '(javascript:|data:|vbscript:)' .planning/phases/06-learning-claim-safe-positioning/06-URLS.md && echo PASS</automated>
  </verify>
  <resume-signal>Type "approved" once the URL summary is correct, or describe the change to make.</resume-signal>
  <done>06-URLS.md exists, contains the locked book URL exactly once, contains rows for YouTube channel + Website + Book + Hero video + (0–5) Key videos, every user-supplied URL begins with `https://`, no dangerous schemes present, user has approved the summary.</done>
</task>

</tasks>

<verification>
After the task completes, the file `.planning/phases/06-learning-claim-safe-positioning/06-URLS.md` exists with all five labelled URL rows (book row pre-locked, all other URLs user-supplied and validated against scheme blocklist). Plan 02's content asset can map its `LEARN_CONTENT.links.*` field names directly to the row keys.
</verification>

<success_criteria>
- 06-URLS.md exists at the expected path.
- Locked book URL `https://amzn.to/3RTAVqi` appears exactly once.
- YouTube channel + Website + Hero video URLs all use `https://` scheme.
- Key videos count is 0–5.
- No `javascript:` / `data:` / `vbscript:` schemes anywhere in the file.
- User has explicitly approved the gathered URL summary.
</success_criteria>

<output>
After completion, create `.planning/phases/06-learning-claim-safe-positioning/06-01-SUMMARY.md` summarising: URL gather completed, URLs land in `06-URLS.md`, downstream consumer is Plan 02 content asset task.
</output>
