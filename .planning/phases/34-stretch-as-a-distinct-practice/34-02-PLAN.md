---
phase: 34-stretch-as-a-distinct-practice
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - src/storage/storage.ts
  - src/storage/settings.ts
  - src/storage/practices.ts
  - src/storage/storage.test.ts
  - src/storage/settings.test.ts
  - src/storage/practices.test.ts
autonomous: true
requirements: [STRETCH-03, STRETCH-04, STRETCH-05]
must_haves:
  truths:
    - "STATE_VERSION is 3; a v2 envelope read by new code gains a practices.stretch slice"
    - "v2→v3 migration seeds practices.stretch.settings from the resonant raw blob and stretch stats from ZERO"
    - "practices.resonant settings/stats are left untouched by the migration"
    - "saveStretchSettings round-trips StretchSettings through practices.stretch.settings"
    - "recordStretchSession writes only practices.stretch.stats; resonant and naviKriya stats unchanged"
    - "coerceActivePractice('stretch') returns 'stretch'"
  artifacts:
    - path: "src/storage/storage.ts"
      provides: "STATE_VERSION=3 + v2→v3 migrateEnvelope step"
      contains: "fromVersion < 3"
    - path: "src/storage/practices.ts"
      provides: "PracticeId+'stretch', PracticeMap stretch slot, coerceStretchSettings, saveStretchSettings, recordStretchSession"
      contains: "coerceStretchSettings"
    - path: "src/storage/settings.ts"
      provides: "coerceSettings trimmed to 3 standard fields"
      contains: "durationMinutes"
  key_links:
    - from: "src/storage/practices.ts"
      to: "coerceStretchSettings"
      via: "coercePractices stretch slot"
      pattern: "coercePracticeSlice\\(r\\.stretch,\\s*coerceStretchSettings\\)"
    - from: "src/storage/storage.ts migrateEnvelope"
      to: "practices.stretch"
      via: "v2→v3 step seeds the slice"
      pattern: "stretch:\\s*\\{"
---

<objective>
Bump the storage envelope to version 3 and make the stretch practice a first-class slice in
the per-practice storage layer. Adds the v2→v3 `migrateEnvelope` step (lift resonant stretch
ramp fields into `practices.stretch.settings`, seed stretch stats with ZERO), the
`coerceStretchSettings` boundary coercer, `saveStretchSettings`, `recordStretchSession`, and
the `'stretch'` `PracticeId`. Trims `coerceSettings` to the new standard-only shape.

Purpose: D-03, D-04, D-05 — STRETCH-03/04/05. A returning user's saved stretch tuning
carries over; stretch stats start fresh; HRV and Navi data survive.
Output: Re-versioned storage with a stretch slice and its read/write functions.
</objective>

<execution_context>
@$HOME/.claude-personal-personal/get-shit-done/workflows/execute-plan.md
@$HOME/.claude-personal-personal/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/34-stretch-as-a-distinct-practice/34-CONTEXT.md
@.planning/phases/34-stretch-as-a-distinct-practice/34-RESEARCH.md
@.planning/phases/34-stretch-as-a-distinct-practice/34-PATTERNS.md
@.planning/phases/33-close-gap-practice-02-resonant-settings-read-write-split-bra/33-CONTEXT.md

<interfaces>
<!-- Depends on Plan 01: StretchSettings + DEFAULT_STRETCH_SETTINGS + validators now exist in domain/settings.ts. -->

src/storage/practices.ts (current — to be extended):
  export type PracticeId = 'resonant' | 'naviKriya'        # ADD 'stretch'
  export interface PracticeMap { resonant, naviKriya }      # ADD stretch slot
  asRecord(raw): Record<string, unknown>                    # prototype-pollution-safe guard — reuse
  coerceNaviKriyaSettings(raw): NaviKriyaSettings           # EXACT model for coerceStretchSettings
  coercePractices(raw): PracticeMap                         # ADD stretch slot
  coerceActivePractice(raw): PracticeId                     # ADD 'stretch'
  saveResonantSettings(settings, deps): void                # EXACT model for saveStretchSettings
  recordResonantSession(elapsedMs, isComplete, deps): PersistedStats  # EXACT model for recordStretchSession
  resetPracticeStats(practice, deps)                        # works unchanged once PracticeId widens

src/storage/storage.ts (current):
  export const STATE_VERSION = 2 as const                   # bump to 3
  migrateEnvelope(env, fromVersion): Envelope                # has v1→v2 step; ADD v2→v3

src/storage/settings.ts (current):
  coerceSettings(raw): SessionSettings                       # returns 9 fields incl. mode — TRIM to 3
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: STATE_VERSION 2→3 migration + trim coerceSettings</name>
  <read_first>
    - src/storage/storage.ts (STATE_VERSION, migrateEnvelope, the v1→v2 step at lines 90-107)
    - src/storage/settings.ts (coerceSettings — currently returns mode + ramp fields)
    - src/storage/storage.test.ts and src/storage/settings.test.ts (suites to extend)
    - .planning/phases/34-stretch-as-a-distinct-practice/34-PATTERNS.md (the storage.ts + settings.ts sections — exact v2→v3 step body and trimmed coercer)
    - .planning/phases/34-stretch-as-a-distinct-practice/34-RESEARCH.md (Pitfall 1 — circular import; Pitfall 7 — idempotency)
  </read_first>
  <behavior>
    - Test: a v2 envelope (`{ version: 2, practices: { resonant: { settings: {...ramp fields}, stats: {...} }, naviKriya: {...} } }`) migrated yields `practices.stretch` with `settings` = the resonant raw settings blob and `stats` = `{ totalSessions: 0, totalElapsedSeconds: 0, lastSessionAtMs: null, lastSessionDurationSeconds: null }`.
    - Test: after migration `practices.resonant` is byte-equal to its pre-migration value (untouched — orphan fields remain).
    - Test: re-running `migrateEnvelope` on the same v2 envelope twice produces the same result (idempotent — purely constructive).
    - Test: a v3 envelope (`fromVersion >= 3`) is returned unchanged.
    - Test: `coerceSettings` returns exactly `{ bpm, ratio, durationMinutes }` — drifted/extra fields like `mode` are not present on the result.
  </behavior>
  <action>
    In `storage.ts`: change `STATE_VERSION` to `3 as const`. Append a `if (fromVersion < 3)` block
    to `migrateEnvelope` after the existing `fromVersion < 2` block. The step reads the RAW
    `out.practices.resonant.settings` blob (via defensive `?? {}` casts to `Record<string, unknown>`),
    then spreads a new `stretch` slice into `out.practices`: `settings` = that raw resonant settings
    blob (coercers validate downstream), `stats` = an inline literal
    `{ totalSessions: 0, totalElapsedSeconds: 0, lastSessionAtMs: null, lastSessionDurationSeconds: null }`.
    Do NOT import `ZERO_STATS` (circular dep — `stats.ts` imports `storage.ts`). Do NOT modify or
    prune the resonant slice. In `settings.ts`: trim `coerceSettings` to return only `bpm`, `ratio`,
    `durationMinutes` (drop `mode`, `initialBpm`, `targetBpm`, `warmUpMinutes`, `coolDownMinutes`,
    `rampDurationMinutes`) and remove the now-unused imports (`isValidMode`, `DEFAULT_STRETCH_SETTINGS`,
    `isValidWarmUp`, `isValidCoolDown`, `isValidRampDuration`). Extend `storage.test.ts` with the v2→v3
    cases above and `settings.test.ts` with the trimmed-shape case.
  </action>
  <verify>
    <automated>npx vitest run src/storage/storage.test.ts src/storage/settings.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/storage/storage.test.ts src/storage/settings.test.ts` passes.
    - `grep -c "STATE_VERSION = 3" src/storage/storage.ts` returns 1.
    - `grep -c "fromVersion < 3" src/storage/storage.ts` returns 1.
    - `grep -n "ZERO_STATS" src/storage/storage.ts` returns no matches (inline literal used — no circular import).
    - `grep -n "isValidMode\|mode:" src/storage/settings.ts` returns no matches.
  </acceptance_criteria>
  <done>STATE_VERSION is 3; v2→v3 step seeds the stretch slice losslessly and idempotently; coerceSettings is standard-only.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Stretch practice slice — PracticeId, coercer, save/record functions</name>
  <read_first>
    - src/storage/practices.ts (full file — PracticeId, PracticeMap, asRecord, coerceNaviKriyaSettings, coercePractices, coerceActivePractice, saveResonantSettings, recordResonantSession, resetPracticeStats)
    - src/storage/practices.test.ts (suite to extend)
    - src/domain/settings.ts (StretchSettings, DEFAULT_STRETCH_SETTINGS, isValidRatio/isValidBpm/isValidWarmUp/isValidCoolDown/isValidRampDuration — produced by Plan 01)
    - .planning/phases/34-stretch-as-a-distinct-practice/34-PATTERNS.md (the `src/storage/practices.ts` section — exact coerceStretchSettings / saveStretchSettings / recordStretchSession bodies)
    - .planning/phases/34-stretch-as-a-distinct-practice/34-RESEARCH.md (Pitfall 8 — coerceActivePractice; Security Domain — asRecord guard)
  </read_first>
  <behavior>
    - Test: `coerceStretchSettings` returns a full `StretchSettings` for valid input; for a single drifted field (e.g. `initialBpm: 'x'`) it falls back per-field to `DEFAULT_STRETCH_SETTINGS` and keeps every other valid field — one bad field never discards the rest.
    - Test: `coerceStretchSettings(null)` / `coerceStretchSettings([])` / a `__proto__`-carrying object returns clean `DEFAULT_STRETCH_SETTINGS` values (asRecord guard).
    - Test: `saveStretchSettings(settings)` followed by `loadPractices().stretch.settings` round-trips the value.
    - Test: `recordStretchSession(elapsedMs, true)` increments `practices.stretch.stats.totalSessions` and leaves `practices.resonant.stats` and `practices.naviKriya.stats` unchanged.
    - Test: `resetPracticeStats('stretch')` zeros only the stretch stats slice.
    - Test: `coerceActivePractice('stretch')` returns `'stretch'`; an invalid value returns `'resonant'`.
  </behavior>
  <action>
    Widen `PracticeId` to `'resonant' | 'stretch' | 'naviKriya'`. Add a `stretch: PracticeSlice<StretchSettings>`
    slot to `PracticeMap`. Add `coerceStretchSettings(raw: unknown): StretchSettings` modeled exactly on
    `coerceNaviKriyaSettings` — `const r = asRecord(raw)` then per-field `isValidX(r.field) ? r.field : DEFAULT_STRETCH_SETTINGS.field`
    for `ratio`, `initialBpm`, `targetBpm`, `warmUpMinutes`, `rampDurationMinutes`, `coolDownMinutes`.
    Add the `stretch` slot to `coercePractices` (`coercePracticeSlice(r.stretch, coerceStretchSettings)`).
    Add `'stretch'` to the `coerceActivePractice` allow-list. Add `saveStretchSettings(settings: StretchSettings, deps)`
    modeled exactly on `saveResonantSettings` (spread `...practices`, override only `stretch`). Add
    `recordStretchSession(elapsedMs, isComplete, deps): PersistedStats` modeled exactly on
    `recordResonantSession` (NOT `recordNaviKriyaSession` — no `roundsCompleted` arg), reading/writing
    `practices.stretch.stats`. `resetPracticeStats` needs no change beyond the widened `PracticeId`.
    Import `StretchSettings` + `DEFAULT_STRETCH_SETTINGS` + the stretch validators from `../domain/settings`.
    Extend `practices.test.ts` with all behaviors above.
  </action>
  <verify>
    <automated>npx vitest run src/storage/practices.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/storage/practices.test.ts` passes.
    - `grep -c "'resonant' | 'stretch' | 'naviKriya'" src/storage/practices.ts` returns at least 1.
    - `practices.ts` exports `coerceStretchSettings`, `saveStretchSettings`, `recordStretchSession`.
    - `grep -n "roundsCompleted" src/storage/practices.ts` shows no `roundsCompleted` use inside `recordStretchSession`.
    - `coercePractices` and `coerceActivePractice` both handle the `stretch` key/id.
  </acceptance_criteria>
  <done>The stretch practice slice exists end to end in the storage layer — typed, coerced prototype-pollution-safe, with isolated save/record/reset.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| localStorage → app | The `hrv:state:v1` envelope is untrusted persisted data (user-editable, cross-version, drift-prone). Every read crosses this boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34-02 | Tampering | `coerceStretchSettings` reading named keys off `raw` | mitigate | `asRecord` guard rejects non-plain-objects / arrays / prototype-polluting payloads before any named-key read (V5 Input Validation; T-30-05 pattern carried forward). Per-field non-throwing coercion — one drifted field never discards the rest, never throws into the render path. |
| T-34-03 | Tampering | v2→v3 `migrateEnvelope` step reading `out.practices.resonant.settings` | mitigate | Defensive `?? {}` casts before named-key reads; the raw blob is passed through to `coerceStretchSettings` which validates it. The step is purely constructive (adds keys only) — no destructive write a tampered envelope could exploit. |
| T-34-04 | Tampering | `coerceActivePractice` | mitigate | Strict allow-list (`raw === 'resonant' \|\| 'stretch' \|\| 'naviKriya'`); any other value falls back to `'resonant'`. No reflection off untrusted input. |
</threat_model>

<verification>
- `npx vitest run src/storage/` is fully green.
- A v2 envelope read by the new code yields a populated `practices.stretch` slice with migrated settings + ZERO stats; `practices.resonant` is byte-identical to before.
</verification>

<success_criteria>
- STATE_VERSION is 3; the v2→v3 migration is lossless, idempotent, orphan-tolerant.
- `practices.stretch` is a fully-typed slice with `coerceStretchSettings`, `saveStretchSettings`, `recordStretchSession`.
- Storage test suite green.
</success_criteria>

<output>
After completion, create `.planning/phases/34-stretch-as-a-distinct-practice/34-02-SUMMARY.md`
</output>
