---
phase: 34-stretch-as-a-distinct-practice
plan: 11
type: execute
wave: 1
depends_on: []
gap_closure: true
files_modified:
  - src/components/PracticeToggle.tsx
  - src/components/PracticeToggle.test.tsx
autonomous: true
requirements: [STRETCH-05]
must_haves:
  truths:
    - "In switcher Treatment B the practice glyph renders inline with the label — icon and label on one baseline, centered in the pill — not detached at the top-left"
    - "The Stretch glyph is the spike 007 S-curve, not a plain descending diagonal line"
  artifacts:
    - path: "src/components/PracticeToggle.tsx"
      provides: "Treatment B pill button with flex inline layout; PracticeGlyph stretch branch using the spike S-curve path"
      contains: "M2 13 Q5.5 2 9 9 T16 5.5"
  key_links:
    - from: "src/components/PracticeToggle.tsx pill button"
      to: "PracticeGlyph + label span"
      via: "flex items-center justify-center gap-1 on the pill button"
      pattern: "items-center justify-center"
---

<objective>
Close UAT GAP 2 (minor) from Phase 34 testing: in switcher Treatment B (icon+label) the
practice glyph renders detached at the top-left of each pill instead of inline with the
label, and the Stretch glyph is a plain descending diagonal polyline that does not match
the icon shown in the spike. Two defects in `PracticeToggle.tsx`, both from plan 34-03.

OPERATOR DECISIONS (locked — do NOT re-open):
- Layout: add `flex items-center justify-center gap-1` to the Treatment B pill button and
  wrap the label text in a `<span>` so the glyph and label are sibling flex children on
  one baseline.
- Stretch glyph: match the spike — replace the Stretch glyph polyline with the spike 007
  S-curve path `M2 13 Q5.5 2 9 9 T16 5.5` on an 18x18 viewBox, `currentColor` stroke,
  stroke width ~2.

Purpose: STRETCH-05 — the three-practice switcher ships two label treatments behind a
developer-only build-time toggle; Treatment B must render the icon+label correctly and the
Stretch glyph must match what the operator saw in the spike harness.
Output: a Treatment B pill button with inline flex layout, a corrected Stretch S-curve
glyph, updated tests (the existing stretch-glyph test asserts a `polyline` and must move
to asserting a `path`), and a green verification run.
</objective>

<execution_context>
@$HOME/.claude-personal-personal/get-shit-done/workflows/execute-plan.md
@$HOME/.claude-personal-personal/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/34-stretch-as-a-distinct-practice/34-UAT.md
@.planning/debug/switcher-treatment-b-glyph-layout.md

<interfaces>
<!-- The GAP 2 fix touches PracticeToggle.tsx — already-shipped Phase 34 (plan 34-03) code. -->

src/components/PracticeToggle.tsx (current):
  // PracticeGlyph stretch branch (lines 38-44) — DEFECT 2: a plain diagonal polyline on a
  // 16x16 viewBox:
  //   <svg width="16" height="16" viewBox="0 0 16 16" aria-hidden="true">
  //     <polyline points="2,4 14,12" fill="none" stroke="currentColor" strokeWidth="1.5" .../>
  //   </svg>
  // resonant (circle, lines 30-37) and naviKriya (three circles, lines 46-53) glyphs are
  // CORRECT — do NOT touch them. Only the stretch branch changes.

  // pillClass (lines 82-88) — DEFECT 1: NO flex layout. The <button> renders
  //   {TREATMENT === 'B' && <PracticeGlyph id={id} />}
  //   {strings.practiceNames[id]}            // bare text node, NOT wrapped
  // (lines 99-101) as block-flow siblings, so the inline SVG lands top-left and the label
  // drops to the next line. `pillClass` starts with `'flex-1 rounded-full min-h-[44px] ...'`
  // — `flex-1` is the GRID sizing of the pill within the container, NOT a flex display on
  // the button itself.

src/components/PracticeToggle.test.tsx (current):
  // PracticeGlyph describe block (line ~160). The 'stretch glyph' test (lines ~170-177)
  // asserts `svg.querySelector('polyline')` is not null — this WILL FAIL once the stretch
  // glyph becomes a <path>; that test MUST be updated to assert a <path>.
  // The 'all glyphs use currentColor (no hardcoded hex)' test (lines ~187-200) must keep
  // passing — the new path uses currentColor, no hex.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: GAP 2 — inline flex layout for the Treatment B pill + spike S-curve Stretch glyph</name>
  <read_first>
    - src/components/PracticeToggle.tsx (lines 29-54 — PracticeGlyph, the stretch branch at 38-44 being replaced; lines 82-103 — pillClass and the pill <button> JSX where the flex layout and label <span> are added)
    - src/components/PracticeToggle.test.tsx (lines 13-26 — the strings fixture; lines 157-200 — the PracticeGlyph describe block, especially the stretch-glyph polyline test that must become a path test and the currentColor test that must keep passing; the existing pill-button render tests for how buttons are queried by accessible name)
    - .planning/debug/switcher-treatment-b-glyph-layout.md (the diagnosed two defects — pillClass lacks flex, stretch glyph is a plain polyline vs the spike S-curve path)
    - .planning/phases/34-stretch-as-a-distinct-practice/34-UAT.md (Gaps section — GAP 2 root_cause and the `missing` items, which carry the exact spike S-curve path and viewBox)
  </read_first>
  <behavior>
    - Test (glyph is the S-curve path): `PracticeGlyph id="stretch"` renders an aria-hidden SVG containing a `<path>` whose `d` attribute is exactly `M2 13 Q5.5 2 9 9 T16 5.5`; the SVG `viewBox` is `0 0 18 18`; the path stroke is `currentColor`. The stretch glyph no longer contains a `<polyline>`.
    - Test (other glyphs untouched): `PracticeGlyph id="resonant"` still renders a `<circle>` and `id="naviKriya"` still renders three `<circle>` elements — no regression.
    - Test (no hardcoded hex): the existing "all glyphs use currentColor" assertion still passes for all three glyphs including the new stretch path.
    - Test (inline layout): the Treatment B pill `<button>` className includes `flex`, `items-center`, `justify-center`, and a `gap-*` utility, and the label text is wrapped in a `<span>` (so the glyph and the label are two flex children on one baseline). Assert via the button element's className and that a `<span>` carrying the practice name is present inside the button.
  </behavior>
  <action>
    Two changes in `src/components/PracticeToggle.tsx`.

    DEFECT 2 — replace the Stretch glyph. In `PracticeGlyph`, the `id === 'stretch'` branch
    (lines 38-44), replace the `<svg ...><polyline .../></svg>` with the spike 007 S-curve:
    an `<svg width="18" height="18" viewBox="0 0 18 18" aria-hidden="true">` containing a
    single `<path d="M2 13 Q5.5 2 9 9 T16 5.5" fill="none" stroke="currentColor"
    strokeWidth="2" strokeLinecap="round" />`. Use the exact `d` string from the operator
    decision verbatim. Keep `fill="none"`, `stroke="currentColor"` (theme-token driven,
    no hex), and `strokeLinecap="round"` to match the other glyphs' rounded style. Do NOT
    touch the resonant or naviKriya branches — they are correct. Update the comment above
    the stretch branch from "descending diagonal line (BPM walk-down)" to describe the
    S-curve matching the spike 007 harness.

    DEFECT 1 — inline flex layout for the pill. In `pillClass` (lines 82-88), add `flex
    items-center justify-center gap-1` to the base class string (the first array element,
    currently `'flex-1 rounded-full min-h-[44px] border px-4 py-2 text-sm'`). `flex-1` sets
    the pill's WIDTH within the row; `flex` makes the button itself a flex container so its
    children lay out inline; `items-center` + `justify-center` center the glyph+label
    group on both axes; `gap-1` spaces the glyph from the label. Then in the pill `<button>`
    JSX (lines 90-102), wrap the label text node in a `<span>`: change the bare
    `{strings.practiceNames[id]}` (line 100) to `<span>{strings.practiceNames[id]}</span>`
    so the label is a single flex child rather than a loose text node. The
    `{TREATMENT === 'B' && <PracticeGlyph id={id} />}` line stays as-is — the glyph is
    already an element child; with the button now `flex` it sits inline before the label
    span. Treatment A (text-only) is unaffected: it renders only the label span, still
    centered. Add a brief comment on the pill button noting the flex layout keeps the
    Treatment B glyph inline with the label.

    Update `src/components/PracticeToggle.test.tsx`:
    - The existing 'stretch glyph renders an aria-hidden SVG with a polyline' test (lines
      ~170-177) WILL FAIL once the glyph is a `<path>`. Update it: assert
      `svg.querySelector('path')` is not null, the path's `d` attribute equals
      `M2 13 Q5.5 2 9 9 T16 5.5`, the path stroke is `currentColor`, and the svg `viewBox`
      is `0 0 18 18`. Rename the test accordingly (e.g. '… with an S-curve path').
    - Keep the resonant / naviKriya / currentColor glyph tests passing as-is.
    - Add a Treatment B layout regression test: render `PracticeToggle` (the strings
      fixture already exists) and assert each pill `<button>`'s className contains `flex`,
      `items-center`, `justify-center`, and a `gap-` utility, and that each button contains
      a `<span>` with the practice name text. (TREATMENT is a build-time constant defaulting
      to 'A'; assert the className/span layout, which holds for both treatments — the
      layout fix is treatment-independent and the glyph only renders under B.)
  </action>
  <verify>
    <automated>npx vitest run src/components/PracticeToggle.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/components/PracticeToggle.test.tsx` passes.
    - `grep -c "M2 13 Q5.5 2 9 9 T16 5.5" src/components/PracticeToggle.tsx` returns at least 1.
    - `grep -c "viewBox=\"0 0 18 18\"" src/components/PracticeToggle.tsx` returns at least 1.
    - `grep -vn '^\s*//' src/components/PracticeToggle.tsx | grep -c "polyline"` returns 0 (the stretch polyline is gone from executable code).
    - `grep -c "items-center justify-center" src/components/PracticeToggle.tsx` returns at least 1 (the pill button flex layout).
    - A test asserts the Stretch glyph is a `<path>` with `d="M2 13 Q5.5 2 9 9 T16 5.5"` on an `0 0 18 18` viewBox.
    - A test asserts each pill button carries `flex`/`items-center`/`justify-center`/`gap-*` and wraps the label in a `<span>`.
  </acceptance_criteria>
  <done>In switcher Treatment B the glyph renders inline with the label centered in the pill, and the Stretch glyph is the spike 007 S-curve path on an 18x18 viewBox.</done>
</task>

<task type="auto">
  <name>Task 2: Verification — run the affected suite, the full suite, and typecheck green</name>
  <read_first>
    - src/components/PracticeToggle.test.tsx (the suite carrying the Task 1 regression tests)
    - .planning/phases/34-stretch-as-a-distinct-practice/34-VERIFICATION.md (Behavioral Spot-Checks — the full-suite + tsc gate the phase uses)
  </read_first>
  <action>
    Confirm Task 1 lands cleanly with no regressions. Run
    `npx vitest run src/components/PracticeToggle.test.tsx` — the suite must be green. Run
    the full `npx vitest run` once to confirm no cross-suite regression — `PracticeToggle`
    is rendered by the App switcher suites. Run `npx tsc -b` — it must exit 0 with no
    errors. If any failure surfaces, fix the offending source or test within the scope of
    GAP 2 only. Do NOT broaden scope beyond this gap (the orb-freeze gap is plan 34-09, the
    session-overrun gap is plan 34-10 — leave both untouched).
  </action>
  <verify>
    <automated>npx vitest run src/components/PracticeToggle.test.tsx && npx vitest run && npx tsc -b</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/components/PracticeToggle.test.tsx` reports the suite passing.
    - `npx vitest run` (full suite) reports all tests passing.
    - `npx tsc -b` exits 0 with no type errors.
  </acceptance_criteria>
  <done>The PracticeToggle suite and the full test suite are green and the project typechecks — UAT GAP 2 is closed without regression.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new in this plan) | Both fixes are corrections to already-reviewed presentational code (an inline SVG path and CSS utility classes). No untrusted input crosses any boundary; the switcher treatment is a build-time constant. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34-20 | Information Disclosure | PracticeGlyph inline SVG path | accept | The glyph is a static decorative `aria-hidden` SVG with a hardcoded path string and `currentColor` stroke — no data, no user input, no theme leakage beyond the existing token-driven currentColor. |
</threat_model>

<verification>
- `npx vitest run src/components/PracticeToggle.test.tsx` — suite green (Task 2 gate).
- `npx vitest run` full suite green — confirms the layout and glyph changes cause no cross-suite regression in the App switcher suites.
- `npx tsc -b` passes — no type drift from the two edits.
</verification>

<success_criteria>
- UAT GAP 2 closed: in switcher Treatment B the practice glyph renders inline with the label, centered in the pill, no longer detached top-left.
- The Stretch glyph is the spike 007 S-curve path `M2 13 Q5.5 2 9 9 T16 5.5` on an 18x18 viewBox with a currentColor stroke.
- The existing stretch-glyph test is updated from polyline to path; a Treatment B layout regression test is added; the affected suite, the full suite, and `npx tsc -b` are green.
</success_criteria>

<output>
After completion, create `.planning/phases/34-stretch-as-a-distinct-practice/34-11-SUMMARY.md`
</output>
