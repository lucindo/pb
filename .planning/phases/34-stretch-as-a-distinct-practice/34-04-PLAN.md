---
phase: 34-stretch-as-a-distinct-practice
plan: 04
type: execute
wave: 3
depends_on: [01]
files_modified:
  - src/content/strings.ts
  - src/content/strings.test.ts
autonomous: true
requirements: [STRETCH-06]
must_haves:
  truths:
    - "Implements decisions: D-10"
    - "UiStrings.practice carries stretchName, stretchHeading, stretchHeader fields"
    - "Both EN and PT-BR locales provide non-empty values for all three stretch fields"
    - "The EN switcher label and practice heading are both 'Stretch'; PT-BR both 'Alongar'"
    - "The LOCKED_COPY byte-equality guard stays green (no locked copy touched)"
  artifacts:
    - path: "src/content/strings.ts"
      provides: "Stretch copy fields in UiStrings.practice for both locales"
      contains: "stretchName"
    - path: "src/content/strings.test.ts"
      provides: "Exhaustiveness coverage for the new stretch fields in both locales"
      contains: "stretchName"
  key_links:
    - from: "src/content/strings.ts UiStrings.practice"
      to: "UI_STRINGS.en + UI_STRINGS['pt-BR']"
      via: "stretchName/stretchHeading/stretchHeader present in both catalogs"
      pattern: "stretchName"
---

<objective>
Add the Stretch practice copy to the i18n catalog: the switcher label, the practice heading,
and the app header line — in English and native-quality PT-BR. Spike 007 confirmed "Stretch"
(EN) / "Alongar" (PT-BR) fit the 320px 3-practice switcher.

Purpose: D-10 — STRETCH-06. All new Stretch UI copy in both locales.
Output: Extended `UiStrings.practice` interface + both locale catalogs + test coverage.
</objective>

<execution_context>
@$HOME/.claude-personal-personal/get-shit-done/workflows/execute-plan.md
@$HOME/.claude-personal-personal/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/34-stretch-as-a-distinct-practice/34-CONTEXT.md
@.planning/phases/34-stretch-as-a-distinct-practice/34-RESEARCH.md
@.planning/phases/34-stretch-as-a-distinct-practice/34-PATTERNS.md

<interfaces>
<!-- strings.ts is self-contained config — depends on Plan 01 only for shared phase context. -->

src/content/strings.ts (current):
  interface UiStrings { ... practice: { toggleLabel, resonantName, naviKriyaName,
    resonantHeading, naviKriyaHeading, naviKriyaHeader, naviKriyaControlsPlaceholder,
    naviKriyaStatsEmptyBody, resetStatsTitle } ... }
  const UI_STRINGS: Record<LocaleId, UiStrings> = { en: {...}, 'pt-BR': {...} }
  # naviKriyaName / naviKriyaHeading / naviKriyaHeader is the exact triple pattern to mirror.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add stretch copy fields to UiStrings.practice for both locales</name>
  <read_first>
    - src/content/strings.ts (the UiStrings.practice interface ~lines 155-165, the EN practice block, the PT-BR practice block — mirror the naviKriya triple)
    - src/content/strings.test.ts (the exhaustiveness / completeness suite to extend)
    - src/content/lockedCopy.ts (LOCKED_COPY — confirm nothing here is touched)
    - .planning/phases/34-stretch-as-a-distinct-practice/34-PATTERNS.md (the strings.ts section — exact field names and EN/PT-BR values)
    - .planning/phases/34-stretch-as-a-distinct-practice/34-CONTEXT.md (D-10 — both label and heading are "Stretch"/"Alongar")
  </read_first>
  <behavior>
    - Test: `UI_STRINGS.en.practice.stretchName === 'Stretch'` and `UI_STRINGS.en.practice.stretchHeading === 'Stretch'` (D-10: same value).
    - Test: `UI_STRINGS['pt-BR'].practice.stretchName === 'Alongar'` and `UI_STRINGS['pt-BR'].practice.stretchHeading === 'Alongar'`.
    - Test: `stretchHeader` is a non-empty string in both locales.
    - Test: every `LocaleId` in `UI_STRINGS` has all three stretch fields present and non-empty (extend the existing completeness assertion).
  </behavior>
  <action>
    Extend the `UiStrings.practice` interface with three new `readonly` string fields:
    `stretchName` (short switcher label), `stretchHeading` (practice heading), `stretchHeader`
    (app header line) — modeled on the existing `naviKriyaName`/`naviKriyaHeading`/`naviKriyaHeader`
    triple. Add EN values: `stretchName: 'Stretch'`, `stretchHeading: 'Stretch'`,
    `stretchHeader: 'Stretch practice'`. Add PT-BR values: `stretchName: 'Alongar'`,
    `stretchHeading: 'Alongar'`, `stretchHeader: 'Prática de Alongar'`. Do not modify any
    `LOCKED_COPY` entry. Extend `strings.test.ts` so the completeness/exhaustiveness suite covers
    the three new fields in every locale.
  </action>
  <verify>
    <automated>npx vitest run src/content/strings.test.ts src/content/lockedCopy.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/content/strings.test.ts src/content/lockedCopy.test.ts` passes.
    - `grep -c "stretchName" src/content/strings.ts` returns at least 3 (interface + EN + PT-BR).
    - `grep -c "stretchHeading\|stretchHeader" src/content/strings.ts` returns at least 4.
    - `npx tsc --noEmit` passes — `Record<LocaleId, UiStrings>` is complete for both locales.
  </acceptance_criteria>
  <done>UiStrings.practice carries the three stretch fields; both locales provide non-empty values; LOCKED_COPY guard green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | Static compile-time copy catalog — no untrusted input, no runtime data flow. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34-07 | Tampering | `strings.ts` copy catalog | accept | Static const data compiled into the bundle; no user input path. The `LOCKED_COPY` byte-equality guard independently protects the legally-sensitive copy from accidental edits — verified green by this plan's test command. |
</threat_model>

<verification>
- `npx vitest run src/content/` is fully green.
- `npx tsc --noEmit` passes.
</verification>

<success_criteria>
- `UiStrings.practice` has `stretchName`/`stretchHeading`/`stretchHeader`; both locales populated.
- EN = Stretch, PT-BR = Alongar for label and heading; LOCKED_COPY guard green.
</success_criteria>

<output>
After completion, create `.planning/phases/34-stretch-as-a-distinct-practice/34-04-SUMMARY.md`
</output>
</output>
