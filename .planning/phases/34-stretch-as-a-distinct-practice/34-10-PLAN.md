---
phase: 34-stretch-as-a-distinct-practice
plan: 10
type: execute
wave: 2
depends_on: [34-09]
gap_closure: true
files_modified:
  - src/domain/stretchRamp.ts
  - src/domain/stretchRamp.test.ts
autonomous: true
requirements: [STRETCH-02]
must_haves:
  truths:
    - "A 5m/5m/5m stretch session's realized final endMs equals the requested whole-minute total exactly — the session runs for 15:00, not 15:03"
    - "The in-session REMAINING countdown reads 15:00 at the start of a 5/5/5 session and agrees with the Settings panel Duration readout"
    - "Warm-up and ramp-step segment boundaries still land on whole breath-cycle (Out→In) transitions — BPM never steps mid-breath"
  artifacts:
    - path: "src/domain/stretchRamp.ts"
      provides: "buildStretchSegments reworked so the final bounded cool-down segment absorbs the accumulated cycle-snapping residual and the last endMs equals the requested total"
      contains: "buildStretchSegments"
  key_links:
    - from: "src/domain/stretchRamp.ts buildStretchSegments"
      to: "computeStretchTotalMs"
      via: "the final cool-down segment endMs equals warmUp+ramp+coolDown requested total"
      pattern: "coolDownMinutes"
---

<objective>
Close UAT GAP 1 (minor) from Phase 34 testing: a 5m/5m/5m stretch session genuinely runs
~3s long (15:03, not the requested 15:00). buildStretchSegments snaps EACH segment
independently to a whole number of that segment's own breath cycles; because every segment
runs at a different BPM the per-segment rounding residuals do not cancel and sum to
+3220ms for the default 5/5/5 session. The in-session REMAINING countdown shows 15:03
while the Settings panel rounds the same total to "15 min", so the two displays disagree.

OPERATOR DECISION (locked — do NOT re-open): honor the exact requested whole-minute total.
Rework buildStretchSegments so the realized final endMs equals the requested total. Keep
the warm-up and ramp-step boundaries cycle-aligned exactly as today, but snap/absorb the
accumulated residual in the FINAL cool-down segment so the last endMs lands exactly on the
requested total.

Purpose: STRETCH-02 — the requested whole-minute total is the user-facing contract; a
display-only fix does not satisfy the UAT truth "runs for exactly 15:00". Once the engine
honors the exact total, the in-session countdown reads 15:00 with no display change and
the SettingsForm Math.round becomes a harmless no-op for whole-minute inputs.
Output: a reworked buildStretchSegments whose bounded-session final endMs equals the
requested total, regression tests for the exact total and the preserved cycle alignment,
and a green verification run — including an explicit check that the gap-1 rework does NOT
reintroduce the gap-3 orb freeze fixed in plan 34-09.
</objective>

<execution_context>
@$HOME/.claude-personal-personal/get-shit-done/workflows/execute-plan.md
@$HOME/.claude-personal-personal/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/34-stretch-as-a-distinct-practice/34-UAT.md
@.planning/debug/stretch-session-overruns-target.md
@.planning/phases/34-stretch-as-a-distinct-practice/34-09-SUMMARY.md

<interfaces>
<!-- The GAP 1 fix reworks buildStretchSegments in already-shipped Phase 34 code.
     This plan runs in WAVE 2, AFTER plan 34-09 (the orb-freeze fix) lands its
     getStretchFrame clamp change — both touch src/domain/stretchRamp.ts, so they
     are sequenced into separate waves to avoid a file-ownership conflict. -->

src/domain/stretchRamp.ts buildStretchSegments (current — lines 76-149):
  // makeSegment(bpm, requestedMs, stage) snaps EACH segment INDEPENDENTLY:
  //   cycleCount = Math.max(1, Math.round(requestedMs / cycleMs))
  //   durationMs = cycleCount * cycleMs
  // Step 1 warm-up: makeSegment(initialBpm, warmUpMinutes*60_000, 'hold-initial')
  // Step 2 ramp: numSteps segments, each makeSegment(stepBpm, stepRequestedMs, 'ramp')
  // Step 3 cool-down: makeSegment(targetBpm, coolDownMs, 'hold-target')
  //   coolDownMs = coolDownMinutes === 'open-ended' ? Infinity : coolDownMinutes*60_000
  // The per-segment Math.round residuals do NOT cancel — diagnosed 5/5/5 overrun +3220ms.

  // requested total (bounded session) =
  //   (warmUpMinutes + rampDurationMinutes + coolDownMinutes) * 60_000

src/domain/stretchRamp.ts computeStretchTotalMs (current — lines 240-244):
  // returns buildStretchSegments(settings).at(-1)!.endMs for a bounded session,
  // null for 'open-ended'. After this fix, for a bounded session it returns the
  // EXACT requested whole-minute total.

src/domain/stretchRamp.ts getStretchFrame (current — AS LEFT BY PLAN 34-09):
  // The DS-WR-03 clamp was narrowed by 34-09 to guard only the exact endMs
  // landing. It still relies on cycleInSegment = Math.floor(elapsedInSegment /
  // activeSeg.cycleMs). The final cool-down segment after THIS rework is no
  // longer a whole-cycle multiple — its span includes a partial residual cycle.
  // The fix below MUST keep activeSeg.cycleMs as the true breath-cycle length so
  // getStretchFrame's phase math is unchanged; only the cool-down segment's
  // endMs (its span) shifts to absorb the residual.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: GAP 1 — rework buildStretchSegments so the final cool-down absorbs the residual and the realized total equals the requested total</name>
  <read_first>
    - src/domain/stretchRamp.ts (lines 76-149 — the whole buildStretchSegments function; lines 99-125 — makeSegment, the per-segment snapping being reworked; lines 144-146 — the Step 3 cool-down construction; lines 165-228 — getStretchFrame, to confirm what consumes the segment table; lines 240-244 — computeStretchTotalMs)
    - src/domain/stretchRamp.test.ts (lines 12-25 — baseSettings / openEndedSettings fixtures; lines 27-176 — the buildStretchSegments suite, including the cycle-alignment and DEFAULT_STRETCH_SETTINGS tests; lines 178-260 — the getStretchFrame suite)
    - src/domain/settings.ts (DEFAULT_STRETCH_SETTINGS — the default 5/5/5 minute values, initialBpm/targetBpm/ratio used in the diagnosed overrun)
    - .planning/debug/stretch-session-overruns-target.md (Evidence + Resolution — the replicated 5/5/5 arithmetic showing the +3220ms residual, and Option A: let the cool-down absorb the residual)
    - .planning/phases/34-stretch-as-a-distinct-practice/34-UAT.md (Gaps section — GAP 1 root_cause and the `missing` items: keep warm-up/ramp cycle-aligned, snap the FINAL cool-down to absorb the residual)
    - .planning/phases/34-stretch-as-a-distinct-practice/34-09-SUMMARY.md (what plan 34-09 changed in getStretchFrame — confirm the clamp narrowing this rework must not break)
  </read_first>
  <behavior>
    - Test (exact total): for DEFAULT_STRETCH_SETTINGS (5/5/5 minutes) and for baseSettings, the final segment's `endMs` equals the requested whole-minute total exactly — `(warmUpMinutes + rampDurationMinutes + coolDownMinutes) * 60_000` — with no residual (assert strict equality, e.g. 900000 for 5/5/5). `computeStretchTotalMs` returns that exact total.
    - Test (cycle alignment preserved for warm-up + ramp): every segment boundary EXCEPT the final cool-down end still lands on a whole-cycle (Out→In) transition — for each non-final segment, `(seg.endMs - seg.startMs)` is a whole-number multiple of `seg.cycleMs` (within a tiny float epsilon). The warm-up and every ramp-step segment are unchanged from today's snapping.
    - Test (cool-down absorbs the residual): the final cool-down segment's span is whatever ms remains between the end of the last ramp segment and the requested total — its `cycleMs` is still `60_000 / targetBpm` (the true breath-cycle length for getStretchFrame's phase math), and its span need not be a whole multiple of that cycleMs.
    - Test (open-ended unchanged): for openEndedSettings (`coolDownMinutes: 'open-ended'`) the final cool-down segment still has `endMs === Infinity` and `computeStretchTotalMs` still returns null — the residual-absorption logic only applies to a bounded cool-down.
    - Test (orb-freeze NOT reintroduced — gap-3 guard): sample getStretchFrame across the final cycle of a bounded 5/5/5 session built by the reworked buildStretchSegments and assert phaseProgress still advances (is not frozen) up to isComplete — confirming the gap-1 rework does not reintroduce the GAP 3 freeze that plan 34-09 fixed.
    - Test (no inverted-ramp / RangeError regression): a valid down-ramp still builds a populated table; the existing `targetBpm >= initialBpm` RangeError guard (from plan 34-08) still throws.
  </behavior>
  <action>
    Rework `buildStretchSegments` in `src/domain/stretchRamp.ts` so the realized final
    `endMs` of a BOUNDED session equals the requested whole-minute total, while warm-up and
    ramp-step boundaries stay cycle-aligned exactly as today.

    Keep Step 1 (warm-up) and Step 2 (the ramp loop) untouched — they still call
    `makeSegment` with the current `Math.max(1, Math.round(requestedMs / cycleMs))` whole-
    cycle snapping, so every warm-up / ramp boundary stays on an Out→In transition. The
    rework is confined to Step 3, the cool-down segment.

    For Step 3, branch on `coolDownMinutes`:
    - `'open-ended'`: build the cool-down exactly as today — `makeSegment(targetBpm,
      Infinity, 'hold-target')` → `endMs = Infinity`. The residual-absorption logic does
      NOT apply; computeStretchTotalMs returns null for this case.
    - a bounded numeric `coolDownMinutes`: do NOT snap the cool-down to whole cycles.
      Instead compute the requested whole-minute total
      `requestedTotalMs = (warmUpMinutes + rampDurationMinutes + coolDownMinutes) * 60_000`
      and make the cool-down span exactly `requestedTotalMs - cursorMs` (where `cursorMs`
      is the end of the last ramp segment after Steps 1-2). Build the cool-down segment
      directly (not via the whole-cycle-snapping `makeSegment`, or via a `makeSegment`
      variant / extra parameter that skips the snap): set its `startMs = cursorMs`,
      `endMs = requestedTotalMs`, `bpm = targetBpm`, `cycleMs = 60_000 / targetBpm`,
      `inhaleMs` / `exhaleMs` from `ratioParts` exactly as `makeSegment` computes them,
      `stage = 'hold-target'`, and `cycleBaseIndex = cumulativeCycles` (the running cycle
      total after Steps 1-2 — keep the Pitfall-1 absolute-cycleIndex invariant intact).
      The cool-down's `cycleMs` MUST stay the true breath-cycle length (`60_000 /
      targetBpm`) so getStretchFrame's `Math.floor(elapsedInSegment / cycleMs)` phase math
      is unchanged — only the segment SPAN absorbs the residual, the cycle length does not.

    The executor may implement this by adding an optional "do not snap, use exact span"
    path to `makeSegment` (e.g. an extra arg) or by constructing the final segment inline —
    either is acceptable; pick the one that keeps the function readable. Whichever, the
    warm-up and ramp segments MUST keep the current snapping behavior unchanged.

    Update the Step 3 doc comment and the function-level doc comment (lines 60-75): record
    that warm-up and every ramp step are still snapped to whole cycles so BPM only ever
    steps on an Out→In boundary, but the FINAL bounded cool-down segment absorbs the
    accumulated cycle-snapping residual so the realized total equals the requested whole-
    minute total (operator decision — honor the exact total). Note that the cool-down
    cycleMs remains the true breath-cycle length, so getStretchFrame's phase math is
    unchanged; only the cool-down span shifts.

    In `src/domain/stretchRamp.test.ts`, add the regression tests from the behavior block
    to the `buildStretchSegments` / `getStretchFrame` suites: (a) the final endMs equals
    the requested total exactly for DEFAULT_STRETCH_SETTINGS and baseSettings; (b)
    `computeStretchTotalMs` returns that exact total; (c) every non-final segment is still
    a whole-cycle multiple; (d) the open-ended cool-down still has endMs Infinity and
    computeStretchTotalMs null; (e) the gap-3 guard — phaseProgress still advances through
    the final cycle of a reworked 5/5/5 table. Keep the existing buildStretchSegments tests
    green — if a pre-existing test asserted the cool-down was whole-cycle-snapped, update
    that specific assertion to the new exact-total contract and note why in a comment.
  </action>
  <verify>
    <automated>npx vitest run src/domain/stretchRamp.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/domain/stretchRamp.test.ts` passes.
    - A test asserts `buildStretchSegments(DEFAULT_STRETCH_SETTINGS).at(-1).endMs` equals the requested whole-minute total exactly (e.g. `=== 900000` for 5/5/5) — strict equality, no residual.
    - A test asserts `computeStretchTotalMs` for a bounded session returns the exact requested whole-minute total.
    - A test asserts every non-final segment's span is still a whole-number multiple of that segment's `cycleMs` (warm-up + ramp cycle alignment preserved).
    - A test asserts the open-ended cool-down still has `endMs === Infinity` and `computeStretchTotalMs` returns null.
    - A test asserts getStretchFrame's phaseProgress still advances through the final cycle of a reworked 5/5/5 table — the GAP 3 freeze fixed in plan 34-09 is NOT reintroduced.
  </acceptance_criteria>
  <done>buildStretchSegments produces a bounded session whose final endMs equals the requested whole-minute total exactly; warm-up and ramp boundaries stay cycle-aligned; the in-session countdown reads 15:00 for a 5/5/5 session and the gap-3 orb-freeze fix is preserved.</done>
</task>

<task type="auto">
  <name>Task 2: Verification — run the affected suite, the full suite, and typecheck green</name>
  <read_first>
    - src/domain/stretchRamp.test.ts (the suite carrying the Task 1 regression tests)
    - src/components/SettingsForm.stretch.test.tsx (the stretch Duration-readout suite — confirm the 34-07 Math.round on the now-exact total is a harmless no-op and the suite stays green)
    - src/app/App.session.test.tsx (the stretch-session suite — confirm the in-session countdown / completion tests still pass with the exact total)
    - .planning/phases/34-stretch-as-a-distinct-practice/34-VERIFICATION.md (Behavioral Spot-Checks — the full-suite + tsc gate the phase uses)
  </read_first>
  <action>
    Confirm Task 1 lands cleanly with no regressions. Run
    `npx vitest run src/domain/stretchRamp.test.ts` — the suite must be green. Run the full
    `npx vitest run` once — `buildStretchSegments` / `computeStretchTotalMs` /
    `getStretchFrame` are consumed by the useSessionEngine, sessionController, SettingsForm,
    and App session suites; all must stay green. In particular confirm the stretch Duration
    readout tests in `SettingsForm.stretch.test.tsx` still pass (the 34-07 `Math.round` now
    rounds an already-whole-minute total — a no-op) and the App stretch-session completion
    tests still pass. Run `npx tsc -b` — it must exit 0 with no errors. If any failure
    surfaces, fix the offending source or test within the scope of GAP 1 only. Do NOT
    broaden scope beyond this gap.
  </action>
  <verify>
    <automated>npx vitest run src/domain/stretchRamp.test.ts && npx vitest run && npx tsc -b</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/domain/stretchRamp.test.ts` reports the suite passing.
    - `npx vitest run` (full suite) reports all tests passing — including the SettingsForm stretch and App stretch-session suites.
    - `npx tsc -b` exits 0 with no type errors.
  </acceptance_criteria>
  <done>The stretchRamp suite and the full test suite are green and the project typechecks — UAT GAP 1 is closed without regression and the gap-3 fix from plan 34-09 still holds.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new in this plan) | The fix reworks segment-table arithmetic in a pure-domain function. No new untrusted input crosses any boundary; `buildStretchSegments` consumes a `StretchSettings` slice already coerced by `coerceStretchSettings` (hardened in plan 34-08). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34-18 | Tampering | `buildStretchSegments` cool-down residual-absorption arithmetic | accept | The cool-down span is derived from the requested whole-minute total of an already-coerced `StretchSettings` slice. The existing `rampDurationMinutes` and `targetBpm < initialBpm` RangeError guards (plan 34-08) stay in place; this rework adds no new input path. |
| T-34-19 | Denial of Service | a bounded cool-down span computed as `requestedTotalMs - cursorMs` going non-positive | accept | warmUp/ramp/coolDown minutes are each coerced to positive bounded values; the warm-up + ramp snapped spans cannot exceed the requested total by more than a few cycle-residual seconds, so `requestedTotalMs - cursorMs` stays positive for any valid 5/5/5-class configuration. The `targetBpm < initialBpm` guard already rejects the degenerate ramp. |
</threat_model>

<verification>
- `npx vitest run src/domain/stretchRamp.test.ts` — suite green (Task 2 gate).
- `npx vitest run` full suite green — confirms the buildStretchSegments rework causes no cross-suite regression in the engine / controller / SettingsForm / App session suites.
- `npx tsc -b` passes — no type drift from the rework.
</verification>

<success_criteria>
- UAT GAP 1 closed: a 5m/5m/5m stretch session's realized final endMs equals the requested 15:00 exactly — the session no longer overruns to 15:03, and the in-session countdown agrees with the Settings Duration readout.
- Warm-up and ramp-step segment boundaries still land on whole breath-cycle transitions — BPM never steps mid-breath.
- The open-ended cool-down path is unchanged; the gap-3 orb-freeze fix from plan 34-09 is not reintroduced.
- Dedicated regression tests cover the exact total, the preserved cycle alignment, and the gap-3 guard; the affected suite, the full suite, and `npx tsc -b` are green.
</success_criteria>

<output>
After completion, create `.planning/phases/34-stretch-as-a-distinct-practice/34-10-SUMMARY.md`
</output>
