---
phase: 34-stretch-as-a-distinct-practice
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/domain/settings.ts
  - src/domain/sessionController.ts
  - src/domain/stretchRamp.ts
  - src/domain/settings.test.ts
  - src/domain/sessionController.test.ts
  - src/domain/stretchRamp.test.ts
autonomous: true
requirements: [STRETCH-03]
must_haves:
  truths:
    - "SessionSettings carries only bpm, ratio, durationMinutes — no mode or ramp fields"
    - "StretchSettings is a standalone type with ratio + the five ramp fields, exported with DEFAULT_STRETCH_SETTINGS"
    - "buildStretchSegments and computeStretchTotalMs accept StretchSettings (ratio read internally), not SessionSettings"
    - "No SessionMode / MODE_OPTIONS / isValidMode symbols remain in the domain layer"
  artifacts:
    - path: "src/domain/settings.ts"
      provides: "Split SessionSettings + new StretchSettings type, defaults, validateStretchSettings"
      contains: "export interface StretchSettings"
    - path: "src/domain/stretchRamp.ts"
      provides: "buildStretchSegments/computeStretchTotalMs typed to StretchSettings"
      contains: "settings: StretchSettings"
    - path: "src/domain/sessionController.ts"
      provides: "startSession standard-only + new startStretchSession"
      contains: "startStretchSession"
  key_links:
    - from: "src/domain/sessionController.ts"
      to: "buildStretchSegments"
      via: "startStretchSession single-arg call"
      pattern: "buildStretchSegments\\(stretchSettings\\)"
---

<objective>
Split the settings domain model so Stretch is no longer an intra-resonant `mode`. Resonant
`SessionSettings` trims to standard-only fields (`bpm`, `ratio`, `durationMinutes`); a new
`StretchSettings` type carries the ramp knobs. The session engine's pure-function layer
(`sessionController.ts`, `stretchRamp.ts`) is re-typed to consume `StretchSettings` and the
`mode === 'stretch'` branches are removed.

Purpose: D-01 + D-02 — Stretch becomes a distinct practice; the `mode` concept retires from
the domain layer. This is the foundation every later plan builds on.
Output: Re-typed `src/domain/settings.ts`, `sessionController.ts`, `stretchRamp.ts` plus
their updated test files.
</objective>

<execution_context>
@$HOME/.claude-personal-personal/get-shit-done/workflows/execute-plan.md
@$HOME/.claude-personal-personal/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/34-stretch-as-a-distinct-practice/34-CONTEXT.md
@.planning/phases/34-stretch-as-a-distinct-practice/34-RESEARCH.md
@.planning/phases/34-stretch-as-a-distinct-practice/34-PATTERNS.md

<interfaces>
<!-- Current shapes the executor must transform. Read the files for full detail. -->

src/domain/settings.ts (current — to be split):
  export type SessionMode = 'standard' | 'stretch'        # REMOVE
  export const MODE_OPTIONS = [...]                        # REMOVE
  export interface SessionSettings { bpm, ratio, durationMinutes, mode, initialBpm,
    targetBpm, warmUpMinutes, coolDownMinutes, rampDurationMinutes }   # TRIM to 3 fields
  export const DEFAULT_STRETCH_SETTINGS = { ... } as const  # RE-TYPE as StretchSettings, ADD ratio
  export function isValidMode(v): v is SessionMode          # REMOVE
  export function validateSettings(settings): SessionSettings  # SPLIT — stretch branch out

src/domain/stretchRamp.ts (current signatures):
  buildStretchSegments(settings: SessionSettings, ratio: RatioLabel): StretchSegment[]
  computeStretchTotalMs(settings: SessionSettings): number | null

src/domain/sessionController.ts (current):
  startSession(selectedSettings: SessionSettings, nowMs): RunningSessionState  # branches on mode
  extendTimedSession(...)  # line 86 guard reads lockedSettings.mode === 'stretch'
  RunningSessionState carries `stretchSegments: StretchSegment[] | null`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Split SessionSettings and add the StretchSettings type</name>
  <read_first>
    - src/domain/settings.ts (full file — the type, defaults, validators being split)
    - src/domain/naviKriyaSettings.ts (the isolation pattern StretchSettings mirrors)
    - src/domain/settings.test.ts (the existing suite — mode/stretch cases to rework)
    - .planning/phases/34-stretch-as-a-distinct-practice/34-PATTERNS.md (the `src/domain/settings.ts` section — exact before/after shapes)
  </read_first>
  <behavior>
    - Test: `SessionSettings` type has exactly `bpm`, `ratio`, `durationMinutes` — a TS-level check or a `validateSettings` round-trip with only those fields succeeds.
    - Test: `validateSettings` throws `RangeError` for invalid `bpm`/`ratio`/`durationMinutes` and no longer references `mode`.
    - Test: `validateStretchSettings` accepts a valid `StretchSettings` and throws `RangeError` for out-of-range `initialBpm`, `targetBpm`, `warmUpMinutes`, `coolDownMinutes`, `rampDurationMinutes`.
    - Test: `DEFAULT_STRETCH_SETTINGS` is typed `StretchSettings`, includes `ratio: '40:60'`, and has all six ramp fields.
  </behavior>
  <action>
    Trim `SessionSettings` to `{ bpm: number; ratio: RatioLabel; durationMinutes: DurationOption }`
    — remove `mode`, `initialBpm`, `targetBpm`, `warmUpMinutes`, `coolDownMinutes`,
    `rampDurationMinutes`. Delete `SessionMode`, `MODE_OPTIONS`, `isValidMode`, and
    `DEFAULT_SETTINGS.mode`. Add a new exported `StretchSettings` interface with `ratio: RatioLabel`
    plus `initialBpm: number`, `targetBpm: number`, `warmUpMinutes: WarmUpMinutes`,
    `rampDurationMinutes: number`, `coolDownMinutes: CoolDownMinutes` — explicitly NOT
    `durationMinutes` (D-02: computed, not stored). Re-type `DEFAULT_STRETCH_SETTINGS` as
    `StretchSettings` and add `ratio: '40:60'`. Keep `isValidWarmUp`, `isValidCoolDown`,
    `isValidRampDuration`. Split `validateSettings`: it becomes 3-field standard-only (drop the
    `mode === 'stretch'` branch); add a new exported `validateStretchSettings(settings: StretchSettings): StretchSettings`
    carrying the former stretch-branch checks (`isValidBpm` on `initialBpm`/`targetBpm`, the ramp
    validators, the `targetBpm < initialBpm` ordering rule). Rework `settings.test.ts` to drop
    `mode`/`MODE_OPTIONS` cases and cover both `validateSettings` and `validateStretchSettings`.
  </action>
  <verify>
    <automated>npx vitest run src/domain/settings.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/domain/settings.test.ts` passes.
    - `grep -n "SessionMode\|MODE_OPTIONS\|isValidMode" src/domain/settings.ts` returns no matches.
    - `grep -c "export interface StretchSettings" src/domain/settings.ts` returns 1.
    - `src/domain/settings.ts` exports both `validateSettings` and `validateStretchSettings`.
  </acceptance_criteria>
  <done>SessionSettings is 3-field; StretchSettings exists with the six ramp fields; mode infrastructure deleted; both validators present and tested.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Re-type stretchRamp and split the session controller</name>
  <read_first>
    - src/domain/stretchRamp.ts (buildStretchSegments, computeStretchTotalMs, getStretchFrame)
    - src/domain/sessionController.ts (startSession, extendTimedSession, RunningSessionState)
    - src/domain/stretchRamp.test.ts and src/domain/sessionController.test.ts (suites to update)
    - src/domain/breathingPlan.ts (createBreathingPlan — startStretchSession builds a lead-in plan with it)
    - .planning/phases/34-stretch-as-a-distinct-practice/34-PATTERNS.md (the `sessionController.ts` + `stretchRamp.ts` sections — exact signatures)
  </read_first>
  <behavior>
    - Test: `buildStretchSegments(stretchSettings)` (single arg) returns the same segment table the old two-arg call produced for equivalent inputs.
    - Test: `computeStretchTotalMs(stretchSettings)` returns a finite ms total for valid `StretchSettings`.
    - Test: `startSession(standardSettings, now)` returns `status: 'running'` with `stretchSegments: null`.
    - Test: `startStretchSession(stretchSettings, now)` returns `status: 'running'` with a non-null `stretchSegments` table and a `lastFrame` from `getStretchFrame`.
    - Test: `extendTimedSession` extends only when `stretchSegments === null` (no `mode` read).
  </behavior>
  <action>
    In `stretchRamp.ts`: change `buildStretchSegments` to `(settings: StretchSettings): StretchSegment[]`
    — read `settings.ratio` internally for `RATIO_PARTS` lookup instead of a separate `ratio` param.
    Change `computeStretchTotalMs` to `(settings: StretchSettings)`. Replace the `SessionSettings`
    import with `StretchSettings`. In `sessionController.ts`: make `startSession` standard-only
    (remove `const isStretch = lockedSettings.mode === 'stretch'` and the stretch branch — it always
    sets `stretchSegments: null`). Add an exported `startStretchSession(stretchSettings: StretchSettings, nowMs: number): RunningSessionState`
    that builds a lead-in `createBreathingPlan` at `bpm: stretchSettings.initialBpm` with
    `durationMinutes: 'open-ended'`, computes `stretchSegments = buildStretchSegments(stretchSettings)`,
    and sets `lastFrame = getStretchFrame(stretchSegments, 0)`. In `extendTimedSession`, change the
    line-86 guard from `state.lockedSettings.mode === 'stretch' || state.stretchSegments !== null`
    to `state.stretchSegments !== null`. Update `stretchRamp.test.ts` and `sessionController.test.ts`
    to the new signatures and add `startStretchSession` coverage.
  </action>
  <verify>
    <automated>npx vitest run src/domain/stretchRamp.test.ts src/domain/sessionController.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/domain/stretchRamp.test.ts src/domain/sessionController.test.ts` passes.
    - `grep -n "mode" src/domain/sessionController.ts` returns no `=== 'stretch'` matches.
    - `grep -c "export function startStretchSession" src/domain/sessionController.ts` returns 1.
    - `buildStretchSegments` and `computeStretchTotalMs` signatures take a single `StretchSettings` argument.
  </acceptance_criteria>
  <done>stretchRamp + sessionController consume StretchSettings; startStretchSession exists; no mode branches remain in the domain layer.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none in this plan) | Pure domain layer — no untrusted input crosses here. Coercion at the storage boundary is Plan 02. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34-01 | Tampering | `validateStretchSettings` | accept | Domain validators are called on already-coerced data; the storage-boundary coercer (Plan 02) is the real input gate. `validateStretchSettings` throws on bad input — fail-closed, no silent acceptance. |
</threat_model>

<verification>
- `npx vitest run src/domain/` is fully green.
- No `mode`, `SessionMode`, `MODE_OPTIONS`, or `isValidMode` references remain anywhere under `src/domain/`.
</verification>

<success_criteria>
- `SessionSettings` is standard-only (3 fields); `StretchSettings` is a standalone exported type with the six ramp fields.
- `buildStretchSegments`/`computeStretchTotalMs` take `StretchSettings`; `startStretchSession` exists.
- Domain test suite green.
</success_criteria>

<output>
After completion, create `.planning/phases/34-stretch-as-a-distinct-practice/34-01-SUMMARY.md`
</output>
