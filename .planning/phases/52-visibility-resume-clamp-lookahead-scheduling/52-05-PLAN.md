---
phase: 52-visibility-resume-clamp-lookahead-scheduling
plan: "05"
type: execute
wave: 4
depends_on: ["52-01", "52-03", "52-04"]
files_modified:
  - src/hooks/useAudioCues.ts
  - src/hooks/useAudioCues.test.tsx
  - src/hooks/useBreathingSessionController.ts
  - src/hooks/useBreathingSessionController.test.tsx
  - src/audio/audioEngine.test.ts
autonomous: true
gap_closure: true
closes_verification: 52-VERIFICATION.md
closes_review: 52-REVIEW.md
requirements:
  - SCHED-02
  - SCHED-03
  - SCHED-04
  - SCHED-05
  - DEPS-01
  - QUAL-01
tags:
  - audio
  - scheduling
  - lookahead
  - cancellation
  - gap-closure
user_setup: []

must_haves:
  truths:
    - "CR-01-FIX: Controller top-up effect calls audioCancelFutureCues() before audioTopUpLookahead(cues) on every session.currentFrame change (cancel-then-reschedule, per SCHED-05 doctrine D-10)."
    - "CR-01-FIX: Two consecutive top-ups with overlapping audioTime values produce exactly the FINAL walk's cue count of scheduleInCueForTimbre/scheduleOutCueForTimbre invocations on the engine — never the sum of both walks."
    - "CR-01-FIX: useAudioCues exposes cancelFutureCues as a stable-identity facade (parallel to topUpLookahead) that delegates to engine.cancelFutureCues() with the same engineRef null-gate posture used by all clock-subscriber callbacks."
    - "WR-02-FIX: lastTopUpCuesRef.current is written ONLY AFTER the engineRef null-gate inside topUpLookahead — a no-engine call cannot poison the force-top-up cache."
    - "WR-02-FIX: stop() resets lastTopUpCuesRef.current = [] so a fast stop()→start() cycle cannot inherit cues from the prior session."
    - "CR-02-FIX: useAudioCues.start()'s scheduleLeadIn === null branch tears down clock subscriptions (clockUnsubsRef loop + clear), nulls engineRef, reverts the proxy clock source to a fresh wall clock, awaits engine.close(), and sets audioStatus='unavailable' before returning null — mirroring stop()'s teardown."
    - "WR-01-FIX (grouped with CR-02): both start() failure branches (L387 null-leadIn + L402-404 construction-catch) call setAudioStatus('unavailable') so MuteToggle.needsResume does not read healthy on a dead audio path."
    - "AH-WR-05 / AUDIO-04 / AH-WR-07 invariants preserved: cancel-loop snapshot iteration, idempotent try/catch on cancel closures, no setState-in-render."
  artifacts:
    - path: "src/hooks/useAudioCues.ts"
      provides: "cancelFutureCues facade; topUpLookahead cache-after-gate; null-leadIn teardown; setAudioStatus('unavailable') on both failure branches; stop() clears lastTopUpCuesRef"
      contains: "cancelFutureCues"
    - path: "src/hooks/useBreathingSessionController.ts"
      provides: "Cancel-then-reschedule wiring in the top-up effect — audioCancelFutureCues() called immediately before audioTopUpLookahead(cues)"
      contains: "audioCancelFutureCues"
    - path: "src/hooks/useAudioCues.test.tsx"
      provides: "Tightened AUDIO-03 null-leadIn assertion (engine.close + clockUnsubsRef invoked + audioStatus='unavailable'); WR-02 cache-after-gate test; cancelFutureCues facade test"
    - path: "src/audio/audioEngine.test.ts"
      provides: "CR-01 regression test: two consecutive topUpLookahead calls with overlapping audioTimes assert scheduler-spy call count equals the FINAL walk's cue count when paired with cancelFutureCues between calls"
      contains: "Phase 52 CR-01"
  key_links:
    - from: "src/hooks/useBreathingSessionController.ts top-up effect"
      to: "src/hooks/useAudioCues.ts audioCancelFutureCues facade"
      via: "destructured useAudioCues return + called pre-walk-dispatch"
      pattern: "audioCancelFutureCues\\(\\).*audioTopUpLookahead\\("
    - from: "src/hooks/useAudioCues.ts cancelFutureCues facade"
      to: "src/audio/audioEngine.ts cancelFutureCues method"
      via: "engineRef.current.cancelFutureCues()"
      pattern: "engine\\.cancelFutureCues\\(\\)"
    - from: "src/hooks/useAudioCues.ts start() null-leadIn branch"
      to: "stop() teardown template"
      via: "clockUnsubsRef loop + engineRef = null + proxy revert + engine.close + setAudioStatus('unavailable')"
      pattern: "setAudioStatus\\(['\"]unavailable['\"]\\)"
---

<objective>
Close the Phase 52 verification gaps (CR-01 BLOCKER + WR-02 + CR-02 + WR-01) as one coherent fix pass without re-opening prior plan contracts. Cancel-then-reschedule wiring in the controller top-up effect makes the lookahead queue idempotent under per-rAF top-ups; cache-after-gate posture and stop() reset hardens lastTopUpCuesRef; the null-leadIn branch in start() now mirrors stop()'s teardown so a degraded engine path cannot leak AudioContext + clock subscriptions.

Purpose: Reverse the CR-01 doubled-OscillatorNode regression that blocks SC-1, SC-2, SCHED-02, SCHED-03, SCHED-04. Close the secondary issues identified by VERIFICATION + REVIEW as part of the same fix pass per VERIFICATION instructions. Preserve every locked invariant from Plans 01/03/04.

Output:
- A `cancelFutureCues` facade on `useAudioCues` (parallel to `topUpLookahead`).
- A cancel-then-reschedule call ordering in the controller top-up effect.
- A hardened `topUpLookahead` cache-after-gate posture; `stop()` clears the cache.
- A teardown sequence in `start()`'s null-leadIn branch matching `stop()`.
- `setAudioStatus('unavailable')` on both start() failure branches.
- Regression coverage in `audioEngine.test.ts` (overlap-dedup via cancel-then-reschedule) and `useAudioCues.test.tsx` (tightened AUDIO-03; WR-02; facade exposure).
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/52-visibility-resume-clamp-lookahead-scheduling/52-CONTEXT.md
@.planning/phases/52-visibility-resume-clamp-lookahead-scheduling/52-VERIFICATION.md
@.planning/phases/52-visibility-resume-clamp-lookahead-scheduling/52-REVIEW.md
@.planning/phases/52-visibility-resume-clamp-lookahead-scheduling/52-PATTERNS.md
@.planning/phases/52-visibility-resume-clamp-lookahead-scheduling/52-01-PLAN.md
@.planning/phases/52-visibility-resume-clamp-lookahead-scheduling/52-03-PLAN.md
@.planning/phases/52-visibility-resume-clamp-lookahead-scheduling/52-04-PLAN.md
@.planning/phases/52-visibility-resume-clamp-lookahead-scheduling/52-01-SUMMARY.md
@.planning/phases/52-visibility-resume-clamp-lookahead-scheduling/52-03-SUMMARY.md
@.planning/phases/52-visibility-resume-clamp-lookahead-scheduling/52-04-SUMMARY.md
@CLAUDE.md

<interfaces>
<!-- Key contracts the executor needs. Extracted from the codebase. -->
<!-- Executor uses these directly — no codebase exploration needed for shape. -->

From src/audio/audioEngine.ts (Plan 01 surface — DO NOT MODIFY):
- `export const SAFE_LEAD_SEC = 0.005`
- `export const LOOKAHEAD_WINDOW_SEC = 6` / `LOOKAHEAD_MIN_CUES = 2` / `MAX_TICK_DELTA_SEC = 0.1`
- `interface AudioEngine` includes (Plan 01 additions):
  - `topUpLookahead(args: { cues: Array<{ audioTime: number; phaseDurationSec: number; kind: 'in' | 'out' }> }): void`
  - `cancelFutureCues(): void`  // already on the engine — Plan 05 only exposes the hook facade
- `setMuted(next)` already runs the same two-branch fade+cancel loop (D-10, Plan 04). The cancel-then-reschedule pattern Plan 05 wires in the controller mirrors this exactly.

From src/hooks/useAudioCues.ts (Plan 03/04 surface — facade to extend):
- `UseAudioCues` interface currently exposes:
  - `topUpLookahead(this: void, cues: Array<{ audioTime: number; phaseDurationSec: number; kind: 'in' | 'out' }>): void`
  - `notifyPhaseBoundary`, `setMuted`, `start`, `stop`, `resume`, `audioNow`, `playEndChord`, `audioStatus`
- Existing facade pattern (`topUpLookahead` at L627-635) is the model for the new `cancelFutureCues` facade.
- `lastTopUpCuesRef` exists (L187) and is written by `topUpLookahead` (currently BEFORE the engine null-gate — the WR-02 bug).
- `stop()` (L410-448) is the teardown template for the CR-02 null-leadIn branch fix.
- `start()` failure branches at L387 (null-leadIn) and L402-404 (construction catch) currently miss `setAudioStatus('unavailable')`.

From src/hooks/useBreathingSessionController.ts (Plan 03 top-up effect — modify):
- The top-up effect (L341-375) destructures `audioTopUpLookahead` from `useAudioCues`. Plan 05 destructures `audioCancelFutureCues` alongside it and calls `audioCancelFutureCues()` immediately before `audioTopUpLookahead(cues)`.
- Effect dep array currently: `[phase, session.currentFrame, audioTopUpLookahead, stretchSegmentsForTopUp]`. Plan 05 adds `audioCancelFutureCues` to this array (stable identity by useCallback empty deps).
</interfaces>
</context>

<gap_closure_summary>
## Closing These Gaps (from 52-VERIFICATION.md)

| Gap ID | Severity | Status | Truth Closed |
|--------|----------|--------|--------------|
| CR-01 | BLOCKER | open → closed | SC-1, SC-2 ("audio plays cleanly through lookahead window") |
| WR-02 | WARNING | open → closed | "lastTopUpCuesRef cache cannot poison force-top-up across stop/start" |
| CR-02 | WARNING (high impact) | open → closed | "start() null-leadIn branch tears down engine + AC + clock subs cleanly" |
| WR-01 | WARNING (grouped) | open → closed | "audioStatus reads 'unavailable' on both start() failure branches" |

## Approach Chosen (per VERIFICATION + REVIEW)

**Option A (cancel-then-reschedule)** — REVIEW.md and VERIFICATION.md both prefer this. It:
- Mirrors `setMuted(true)` D-10 doctrine (cancel future cues, then the next top-up refills) — same lock established in Plan 04.
- Matches SCHED-05 in CONTEXT.md ("cancel-then-reschedule cleanly").
- Reuses `engine.cancelFutureCues()` which is already shipped (Plan 01) — only the hook facade and controller wiring are missing.
- Localizes the fix at the hook + controller layer; no engine-layer change.

**Option B (engine-layer dedup)** — REJECTED. See `<threat_model>` for rationale recorded in T-52-NEW-C (so future plan-checkers do not re-propose it).

## Explicit Non-Scope (per VERIFICATION)

The following are explicitly OUT of this gap-closure plan:
- WR-03 (negative rawDelta clamp) — orthogonal.
- WR-04 (`reanchorSessionClock` input validation) — orthogonal.
- WR-05 (`walkFutureCues` `>=` vs `>` edge) — orthogonal.
- WR-06 (`playEndChord` prune) — orthogonal.
- IN-01..IN-04 (orphan API, dead code, threshold tuning) — orphan/retro candidates.
- SC-3 standalone behavioural test — deferred by design (SettingsDialog disabled in-session per Phase 14).

Do NOT touch these in Plan 05. They get separate phases or live in retro/refactor.
</gap_closure_summary>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add CR-01 regression test FIRST (RED) — overlap-dedup via cancel-then-reschedule</name>
  <files>src/audio/audioEngine.test.ts</files>

  <read_first>
    <!-- Executor MUST read these before writing the test. -->
    - `src/audio/audioEngine.test.ts` (look for the existing "Phase 52 D-04 topUpLookahead" suite around L1003-1081 and the "Phase 52 D-09/D-10 cancelFutureCues" suite that follows — Plan 05 adds tests near these).
    - `src/audio/audioEngine.ts` L483-494 (`topUpLookahead`), L496-511 (`cancelFutureCues`), L373-409 (`schedule`).
    - `.planning/phases/52-visibility-resume-clamp-lookahead-scheduling/52-VERIFICATION.md` — root-cause description and the missing test specification.
    - `.planning/phases/52-visibility-resume-clamp-lookahead-scheduling/52-PATTERNS.md` — "Test pattern — spy on per-cue scheduler primitives" (L617-650) and "No-design-locking" rule (L653-659).
  </read_first>

  <behavior>
    - Test name (grep target): "Phase 52 CR-01: cancelFutureCues() between two overlapping topUpLookahead calls yields scheduler-call-count equal to the SECOND walk only".
    - Construct a real engine via `await createAudioEngine({ timbre: 'bowl' })` (real implementation per project memory — no mock-heavy tests).
    - Spy on BOTH `scheduleInCueForTimbre` and `scheduleOutCueForTimbre` in `cueSynth` (mirror the spy pattern from existing D-04 tests at L1010-1023).
    - First top-up call: `engine.topUpLookahead({ cues: [...3 cues with audioTimes A, B, C, kinds in/out/in] })`. Capture combined call count after first call (sanity: equals 3).
    - Reset spy counts (`inSpy.mockClear(); outSpy.mockClear()`).
    - Second walk overlaps: `engine.cancelFutureCues()` THEN `engine.topUpLookahead({ cues: [...3 cues with audioTimes B, C, D — first two overlap with the prior walk, third is new] })`.
    - Assert: combined `inSpy.mock.calls.length + outSpy.mock.calls.length === 3` (the FINAL walk size, not 6 = sum of both walks). This is the load-bearing assertion that closes CR-01.
    - Add a sibling test name "Phase 52 CR-01: without cancelFutureCues, second overlapping topUpLookahead double-schedules overlapping cues (proves Option A is necessary)". This negative-control test calls `topUpLookahead` twice WITHOUT a `cancelFutureCues()` between and asserts the combined count equals 6 — locking the CR-01 invariant in both directions so a future refactor that "moves" the cancel responsibility cannot silently regress.
    - Both tests close the engine at the end (`await engine.close()`) — matches Phase 52 test cleanup posture.
  </behavior>

  <action>
    Append to `src/audio/audioEngine.test.ts` AFTER the existing "Phase 52 D-09/D-10 cancelFutureCues" describe block. Create a new `describe('Phase 52 CR-01 cancel-then-reschedule prevents overlap doubling', () => { ... })` suite containing the two `it` blocks defined in `<behavior>`.

    Spy on `cueSynth.scheduleInCueForTimbre` and `cueSynth.scheduleOutCueForTimbre` (already imported via `import * as cueSynth from './cueSynth'` at the top of the file — verify import exists; if missing, add it next to other module imports — do NOT add new top-level imports beyond what is required).

    No-design-locking: do NOT hard-code 3 (the walk size) into the assertion message; the assertion is `cuesIn.length + cuesOut.length === secondWalk.length` where `secondWalk` is the array literal you constructed at test setup. The literal sizes match the walks (3 cues each); the assertion is structural.

    Do NOT touch `src/audio/audioEngine.ts` in this task. Tests MUST go RED (the negative-control test will pass; the positive test will pass too because Plan 01 already shipped `cancelFutureCues` on the engine — RED for THIS PLAN is the **controller wiring** test in Task 4, not the engine spec test here). This task locks the engine contract; the actual user-facing wiring goes RED in Task 4.

    Commit message: `test(52-05): add CR-01 engine-level regression tests for cancel-then-reschedule overlap dedup`.
  </action>

  <verify>
    <automated>pnpm vitest run src/audio/audioEngine.test.ts -t "Phase 52 CR-01"</automated>
  </verify>

  <acceptance_criteria>
    - `src/audio/audioEngine.test.ts` contains `describe('Phase 52 CR-01` block with at least 2 `it` blocks: one that asserts `inSpy.mock.calls.length + outSpy.mock.calls.length === <secondWalk.length>` after `cancelFutureCues + topUpLookahead`, and a negative-control `it` that asserts the combined count equals the SUM of both walks when cancel is omitted.
    - `pnpm vitest run src/audio/audioEngine.test.ts -t "Phase 52 CR-01"` exits 0 (both tests pass — engine-side cancel + dispatch already work; Plan 05's engine surface is unchanged).
    - `pnpm vitest run src/audio/audioEngine.test.ts` exits 0 with 75 + 2 = 77 tests passing (no regression to the existing Phase 52 suites).
    - Test assertions use `secondWalk.length` (or equivalent symbolic reference), NOT a hard-coded literal like `3`.
  </acceptance_criteria>

  <done>
    Engine-level regression coverage for CR-01 exists, passes, and locks Option A (cancel-then-reschedule). The contract is now enforceable by the test suite.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Expose cancelFutureCues facade + harden topUpLookahead cache-after-gate + clear cache in stop()</name>
  <files>src/hooks/useAudioCues.ts, src/hooks/useAudioCues.test.tsx</files>

  <read_first>
    - `src/hooks/useAudioCues.ts` L75-110 (UseAudioCues interface — see existing JSDoc style for `topUpLookahead` to mirror), L183-187 (`lastTopUpCuesRef` declaration), L223-271 (handler defensive-gate posture used by every clock subscriber), L410-448 (`stop()` teardown — the cache-clear lands here), L627-635 (existing `topUpLookahead` facade — the cache-write reordering target).
    - `src/audio/audioEngine.ts` L496-511 (`cancelFutureCues` method on the engine — the facade delegates to this).
    - `.planning/phases/52-visibility-resume-clamp-lookahead-scheduling/52-04-PLAN.md` (Plan 04 established the facade pattern — Plan 05 adds a parallel one).
    - `.planning/phases/52-visibility-resume-clamp-lookahead-scheduling/52-04-SUMMARY.md` "What Was Built" — Task 2 description for the existing `topUpLookahead` facade is the structural model.
    - `src/hooks/useAudioCues.test.tsx` — find the Phase 52 D-04 forceTopUp tests (around the existing test suite) for the facade-test pattern.
  </read_first>

  <behavior>
    **Hook-side test plan (write FIRST — RED → GREEN):**

    Add a new `describe('Phase 52 CR-01-FIX: cancelFutureCues facade', () => { ... })` block in `useAudioCues.test.tsx`:

    - Test 1 (grep: "cancelFutureCues exposed on hook return"): Render `useAudioCues()`. Assert `typeof result.current.cancelFutureCues === 'function'`. Initially fails because the facade does not exist.
    - Test 2 (grep: "cancelFutureCues no-op when engine is null"): Before `start()`, call `result.current.cancelFutureCues()`. Assert no throw, no engine call. Verify defensive engineRef null-gate.
    - Test 3 (grep: "cancelFutureCues delegates to engine.cancelFutureCues after start"): Stub `createAudioEngine` similarly to the AUDIO-03 fake-engine pattern (lines ~1015-1033) but with `cancelFutureCues: vi.fn()` and a real `scheduleLeadIn: vi.fn(() => 3)`. Call `start()`, then `result.current.cancelFutureCues()`, assert `fakeEngine.cancelFutureCues` was called exactly once.

    Add a `describe('Phase 52 WR-02-FIX: topUpLookahead cache-after-gate', () => { ... })` block:

    - Test 1 (grep: "WR-02 topUpLookahead does NOT write lastTopUpCuesRef when engine is null"): Render `useAudioCues()`. Call `result.current.topUpLookahead([{audioTime: 999, phaseDurationSec: 4, kind: 'in'}])` BEFORE `start()`. Then `start()` with the fake engine (whose `clock.onResume` records the resume callback and provides a `fireResume()` helper — pattern from 52-04 tests). Fire resume. Assert `fakeEngine.topUpLookahead` was NOT called (cache was not poisoned by the pre-start call). The current code DOES call engine.topUpLookahead with the stale cue (the bug) — this test goes RED first.
    - Test 2 (grep: "WR-02 stop\\(\\) clears lastTopUpCuesRef"): After a `start()` + valid `topUpLookahead([validCues])` + `stop()` + fresh `start()` (with a new fake engine), fire the new engine's resume callback. Assert the new engine receives ZERO `topUpLookahead` invocations from the cached state of the prior session. RED first.

    **Implementation (after RED is locked):**

    1. Add `cancelFutureCues(this: void): void` to the `UseAudioCues` interface (mirror the JSDoc style of `topUpLookahead` at L87-90, citing Phase 52 CR-01-FIX).
    2. Add a new `useCallback` named `cancelFutureCues` near `topUpLookahead` (L627). Body: `const engine = engineRef.current; if (engine === null) return; engine.cancelFutureCues()`. Empty deps array (matches `topUpLookahead`).
    3. Return `cancelFutureCues` from the hook return at the bottom (mirrors `topUpLookahead` posture).
    4. Reorder `topUpLookahead` body (L627-635) so `lastTopUpCuesRef.current = cues` lands AFTER the `if (engine === null) return` guard, not before. WR-02 cache-after-gate fix.
    5. In `stop()` (L410-448), AFTER the `clockUnsubsRef` teardown loop and BEFORE the `firstInCueTimeRef.current = null` line, add `lastTopUpCuesRef.current = []`. Add an inline comment `// Phase 52 WR-02-FIX: clear cache so fast stop()→start() cannot replay stale cues into new engine.`.
  </behavior>

  <action>
    Step 1 (RED): add the two `describe` blocks above to `src/hooks/useAudioCues.test.tsx`. Use the AUDIO-03 fake-engine pattern from L1010-1050 as the template (NOTE: AUDIO-03 currently mocks an engine WITHOUT `cancelFutureCues` and `topUpLookahead` — extend the fake-engine fixture to include `cancelFutureCues: vi.fn()` and `topUpLookahead: vi.fn()`). For the resume-firing tests, capture the resume subscriber via `onResume: vi.fn((cb) => { resumeCb = cb; return () => undefined })`. Commit RED: `test(52-05): add failing tests for cancelFutureCues facade + WR-02 cache-after-gate`.

    Step 2 (GREEN): apply the four changes in the behavior block above to `src/hooks/useAudioCues.ts`. Stay surgical — no other edits. Commit GREEN: `feat(52-05): expose cancelFutureCues facade; cache-after-gate in topUpLookahead; clear cache in stop()`.

    Do NOT touch `useBreathingSessionController.ts` in this task — that wiring is Task 4 (so Task 4's RED can independently prove the controller-level fix).

    Do NOT modify the engine. The engine surface (Plan 01) is locked.
  </action>

  <verify>
    <automated>pnpm vitest run src/hooks/useAudioCues.test.tsx -t "CR-01-FIX|WR-02-FIX"</automated>
  </verify>

  <acceptance_criteria>
    - `src/hooks/useAudioCues.ts` interface declaration block contains `cancelFutureCues(this: void): void` with JSDoc citing Phase 52 CR-01-FIX.
    - `src/hooks/useAudioCues.ts` contains a `useCallback` named `cancelFutureCues` with body `const engine = engineRef.current; if (engine === null) return; engine.cancelFutureCues()` and empty deps array (`grep -n "engine.cancelFutureCues" src/hooks/useAudioCues.ts` returns at least one hit inside a useCallback body).
    - `src/hooks/useAudioCues.ts` `topUpLookahead` body has `lastTopUpCuesRef.current = cues` AFTER `if (engine === null) return` (manual read: the cache write must be the line after the null-gate, not before).
    - `src/hooks/useAudioCues.ts` `stop()` body contains `lastTopUpCuesRef.current = []` after the clockUnsubsRef teardown loop.
    - `src/hooks/useAudioCues.ts` hook return object includes `cancelFutureCues` (grep: in the return-object literal at the bottom of `useAudioCues`).
    - `pnpm vitest run src/hooks/useAudioCues.test.tsx -t "CR-01-FIX"` exits 0 (3 tests pass).
    - `pnpm vitest run src/hooks/useAudioCues.test.tsx -t "WR-02-FIX"` exits 0 (2 tests pass).
    - `pnpm vitest run src/hooks/useAudioCues.test.tsx` exits 0 with 52 + 5 = 57 tests passing.
    - `pnpm tsc --noEmit` exits 0.
  </acceptance_criteria>

  <done>
    `useAudioCues` exposes the cancel facade with stable identity and the cache is hardened against pre-start poisoning + cross-session leakage. Tests prove it.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Tighten AUDIO-03 + WR-01 — null-leadIn teardown mirrors stop() and sets audioStatus='unavailable'</name>
  <files>src/hooks/useAudioCues.ts, src/hooks/useAudioCues.test.tsx</files>

  <read_first>
    - `src/hooks/useAudioCues.ts` L340-408 (the `start()` callback body — focus L387 null-leadIn branch and L392-405 catch branch), L410-448 (`stop()` — teardown template to mirror).
    - `src/hooks/useAudioCues.test.tsx` L1009-1050 (existing AUDIO-03 test — the assertion floor to tighten).
    - `.planning/phases/52-visibility-resume-clamp-lookahead-scheduling/52-REVIEW.md` §CR-02 (full root cause + fix path), §WR-01 (audioStatus on failure branches).
  </read_first>

  <behavior>
    **Hook-side test plan (write FIRST — RED → GREEN):**

    Extend the existing AUDIO-03 test (do NOT delete the existing assertions — extend them):

    - The existing AUDIO-03 test asserts: `res === null`, `status === 'failed'`, `audioAvailable === false`.
    - Tighten by additionally asserting (each is a separate RED assertion since the current production code skips these):
      - `fakeEngine.close` was called exactly once. The current code does NOT close the fake engine on the null-leadIn branch (CR-02 leak).
      - The `onResume`, `onSuspend`, `onClose` unsubscribe functions returned by the fake `clock` mocks were each invoked (i.e., `clockUnsubsRef` was iterated and emptied). Pattern: each `onXxx` mock returns a tracked `vi.fn()` unsub; assert `.toHaveBeenCalledTimes(1)` on each unsub. There are FOUR subscriptions (Plan 04 added the 4th `onResume(handleForceTopUp)`), so assert four unsubs each fired once.
      - `result.current.audioStatus === 'unavailable'` after the start() returns (WR-01 fix — currently reads default `'ok'`).

    Add a sibling test "AUDIO-03 + WR-01: construction-failure branch (createAudioEngine throws) also sets audioStatus='unavailable'":
    - Stub `createAudioEngine` to throw (e.g., `mockRejectedValueOnce(new Error('iOS construction failed'))`). Render hook. Call `start()`. Assert `res === null`, `status === 'failed'`, `audioAvailable === false`, `audioStatus === 'unavailable'`.

    **Implementation (after RED is locked):**

    Rewrite the L387 null-leadIn branch in `start()` so it executes — IN THIS ORDER — before returning null:
    1. Run the existing `clockUnsubsRef` teardown loop (mirror `stop()` L420-421): `for (const off of clockUnsubsRef.current) off(); clockUnsubsRef.current = []`.
    2. Null `engineRef.current = null` (mirror `stop()` L427).
    3. Revert proxy clock source to fresh wall clock: `proxyMemoRef.current.setSource(createWallSessionClock())` (mirror `stop()` L437).
    4. Clear cached refs: `firstInCueTimeRef.current = null` and `lastTopUpCuesRef.current = []` (mirror `stop()` L438 + Task 2 cache-clear).
    5. `setAudioAvailable(false)` (preserved from prior code).
    6. `setAudioStatus('unavailable')` (WR-01 fix — NEW).
    7. `setStatus('failed')` (preserved from prior code).
    8. `void engine.close()` (fire-and-forget — matches the `iOS gesture preservation` posture used in `reconstructEngine`; no `await` here because we are in a sync-return branch and the engine is already known-bad). Use this exact form; do NOT substitute `await engine.close()` or `return engine.close().then(() => null)`.
    9. `return null`.

    In the catch branch (L392-405), additionally call `setAudioStatus('unavailable')` after `setAudioAvailable(false)` and before `setStatus('failed')`. No other catch-branch changes.

    Add inline comments citing CR-02-FIX and WR-01-FIX for both edits.
  </behavior>

  <action>
    Step 1 (RED): extend the AUDIO-03 test in `src/hooks/useAudioCues.test.tsx` with the four new assertions described in the behavior block. Add a new sibling `it` for the construction-catch branch. The fake-engine fixture for AUDIO-03 needs `close: vi.fn(async () => {})` (already has it) and the clock subscription mocks must return tracked unsubs (refactor the mock so each `onResume/onSuspend/onClose` returns a captured `vi.fn()` accessible from the test scope). Commit RED: `test(52-05): tighten AUDIO-03 to assert engine.close + clockUnsubs + audioStatus='unavailable' (CR-02 + WR-01)`.

    Step 2 (GREEN): apply the start() L387 + L402-404 edits in `src/hooks/useAudioCues.ts` per the behavior block. Surgical edits — do not touch the success path. Commit GREEN: `fix(52-05): null-leadIn branch tears down engine + audioStatus='unavailable' on both failure paths`.

    Do NOT add new imports unless `createWallSessionClock` is not already imported at the top of the file. Verify the existing import block.
  </action>

  <verify>
    <automated>pnpm vitest run src/hooks/useAudioCues.test.tsx -t "AUDIO-03"</automated>
  </verify>

  <acceptance_criteria>
    - Existing AUDIO-03 test still passes AND additionally asserts: `fakeEngine.close` called once; all 4 clock unsubs fired once each; `result.current.audioStatus === 'unavailable'`.
    - New construction-catch test passes and asserts `audioStatus === 'unavailable'` after a throwing `createAudioEngine`.
    - `src/hooks/useAudioCues.ts` L387-area null-leadIn branch contains all 8 teardown steps from the behavior block in order, with CR-02-FIX inline comment.
    - `src/hooks/useAudioCues.ts` catch branch contains `setAudioStatus('unavailable')` with WR-01-FIX inline comment.
    - `grep -n "setAudioStatus('unavailable')" src/hooks/useAudioCues.ts | wc -l` returns at least 3 (existing handleClose path + 2 new failure-branch paths).
    - `pnpm vitest run src/hooks/useAudioCues.test.tsx -t "AUDIO-03"` exits 0.
    - `pnpm vitest run src/hooks/useAudioCues.test.tsx` exits 0 (no regression to the 57 tests post-Task-2; should now be 58 tests with the new construction-catch test).
    - `pnpm tsc --noEmit` exits 0.
  </acceptance_criteria>

  <done>
    The null-leadIn path tears down the engine + AC + clock subs deterministically and surfaces `audioStatus='unavailable'` consistently with the construction-catch branch. AUDIO-03 test now proves it.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Wire audioCancelFutureCues into the controller top-up effect (CR-01 BLOCKER closure)</name>
  <files>src/hooks/useBreathingSessionController.ts, src/hooks/useBreathingSessionController.test.tsx</files>

  <read_first>
    - `src/hooks/useBreathingSessionController.ts` L330-376 (the top-up effect — the surgical edit site). NOTE: the effect destructures `audioTopUpLookahead` from the `useAudioCues` return — Plan 05 destructures `audioCancelFutureCues` alongside it from the same `useAudioCues` return value.
    - `src/hooks/useBreathingSessionController.ts` top of file (~L130-200) — find the existing `useAudioCues` destructure to add `cancelFutureCues: audioCancelFutureCues` to it.
    - `src/hooks/useAudioCues.ts` (Task 2 added the `cancelFutureCues` facade — confirm it exists before wiring).
    - `src/hooks/useBreathingSessionController.test.tsx` — find the existing top-up effect tests for the structural pattern to extend.
    - `.planning/phases/52-visibility-resume-clamp-lookahead-scheduling/52-03-PLAN.md` (Plan 03 wrote this effect — confirm Plan 05's edit preserves its dep-array and phase guards).
    - `.planning/phases/52-visibility-resume-clamp-lookahead-scheduling/52-VERIFICATION.md` — CR-01 root cause and "Either ... OR expose engine.cancelFutureCues through useAudioCues and call audioCancelFutureCues() before audioTopUpLookahead() in the controller effect" guidance.
  </read_first>

  <behavior>
    **Controller-side test plan (write FIRST — RED → GREEN):**

    Add a `describe('Phase 52 CR-01-FIX: controller top-up effect calls cancelFutureCues before topUpLookahead', () => { ... })` block in `src/hooks/useBreathingSessionController.test.tsx`:

    - Test 1 (grep: "CR-01-FIX top-up effect calls cancelFutureCues immediately before topUpLookahead"):
      - Mock `useAudioCues` (or use the existing test-double pattern in this file) so that the controller's `audioCancelFutureCues` and `audioTopUpLookahead` are tracked `vi.fn()`s.
      - Drive the controller through lead-in completion so `phase === 'running'` and `audioAnchorRef` is non-null.
      - Trigger a single `session.currentFrame` advance (the existing test harness's frame-advance helper — re-read `useBreathingSessionController.test.tsx` for the helper name; do NOT invent a new one).
      - Assert call order via `mock.invocationCallOrder`: `audioCancelFutureCues.mock.invocationCallOrder[0] < audioTopUpLookahead.mock.invocationCallOrder[0]`.
      - Assert `audioCancelFutureCues` was called once per frame change with no arguments.
      - Assert `audioTopUpLookahead` was called once per frame change with the walkFutureCues output as the argument.

    - Test 2 (grep: "CR-01-FIX two consecutive frame changes — cancel fires twice and dispatch fires twice"):
      - Same setup. Advance the controller through TWO frame changes. Assert `audioCancelFutureCues.mock.calls.length === 2` AND `audioTopUpLookahead.mock.calls.length === 2`. Assert pairwise call ordering for BOTH frames (cancel[0] < dispatch[0]; cancel[1] < dispatch[1]).

    - Test 3 (grep: "CR-01-FIX top-up effect does NOT call cancelFutureCues when audioAnchor is null"):
      - Drive controller into lead-in (audioAnchor still null). Assert `audioCancelFutureCues` was NOT called AND `audioTopUpLookahead` was NOT called. (Locks the existing guard at L347-349.)

    **Implementation (after RED is locked):**

    1. In the destructure of `useAudioCues(...)` in the controller, alias `cancelFutureCues` as `audioCancelFutureCues` alongside the existing `topUpLookahead: audioTopUpLookahead` alias.
    2. Inside the top-up effect (L341-375), immediately AFTER the `walkFutureCues({...})` call and BEFORE `audioTopUpLookahead(cues)`, add a single line: `audioCancelFutureCues()`. The cancel call has NO arguments and does NOT depend on `cues` — it cancels everything in `activeCues` with `scheduledAt > now`.
    3. Add `audioCancelFutureCues` to the effect's dep array (it is a stable-identity useCallback from Task 2, so dep-array inclusion is required by exhaustive-deps lint AND does not cause re-fire).
    4. Add an inline JSDoc comment at the top of the effect body citing CR-01-FIX + SCHED-05 doctrine + 52-VERIFICATION.md (one sentence).
  </behavior>

  <action>
    Step 1 (RED): write the three tests in `src/hooks/useBreathingSessionController.test.tsx`. The existing test file uses a mock-useAudioCues pattern — re-use it. Add `cancelFutureCues: vi.fn()` to the existing fake-useAudioCues fixture. Commit RED: `test(52-05): add failing tests for controller top-up effect calling audioCancelFutureCues before audioTopUpLookahead (CR-01-FIX)`.

    Step 2 (GREEN): apply the four changes in the behavior block to `src/hooks/useBreathingSessionController.ts`. Confirm `pnpm tsc --noEmit` passes AND no exhaustive-deps lint regression. Commit GREEN: `fix(52-05): controller top-up effect calls audioCancelFutureCues before audioTopUpLookahead (closes CR-01)`.

    Do NOT modify any other file in this task. The contract chain: Task 1 locks engine spec; Task 2 exposes facade; Task 3 fixes degraded engine path; Task 4 wires the runtime call ordering that closes CR-01.
  </action>

  <verify>
    <automated>pnpm vitest run src/hooks/useBreathingSessionController.test.tsx -t "CR-01-FIX"</automated>
  </verify>

  <acceptance_criteria>
    - `src/hooks/useBreathingSessionController.ts` destructures `cancelFutureCues: audioCancelFutureCues` from the `useAudioCues(...)` return.
    - `src/hooks/useBreathingSessionController.ts` top-up effect body contains `audioCancelFutureCues()` IMMEDIATELY BEFORE the `audioTopUpLookahead(cues)` line. `grep -n "audioCancelFutureCues" src/hooks/useBreathingSessionController.ts` returns at least 2 hits (destructure + effect body call).
    - Effect dep array includes `audioCancelFutureCues`.
    - `pnpm vitest run src/hooks/useBreathingSessionController.test.tsx -t "CR-01-FIX"` exits 0 with 3 tests passing.
    - `pnpm vitest run src/hooks/useBreathingSessionController.test.tsx` exits 0 (no regression; 9 + 3 = 12 tests passing).
    - `pnpm tsc --noEmit` exits 0.
    - `pnpm lint` does not report a new exhaustive-deps warning on the top-up effect (the existing pre-baseline lint floor is unchanged).
  </acceptance_criteria>

  <done>
    The CR-01 BLOCKER is closed at the runtime call-ordering layer. Two consecutive top-ups now cancel-then-reschedule. Tests prove ordering; engine-level test (Task 1) proves the resulting scheduler-call-count contract.
  </done>
</task>

<task type="auto">
  <name>Task 5: Full-suite green-gate + VERIFICATION audit</name>
  <files>n/a (no source edits)</files>

  <read_first>
    - `.planning/phases/52-visibility-resume-clamp-lookahead-scheduling/52-VERIFICATION.md` (gaps[].truth list — must all close after Plan 05).
    - `CLAUDE.md` (Tiger Style — make sure no new internal-module mocks were added beyond what was strictly required).
  </read_first>

  <action>
    Run, in order:
    1. `pnpm tsc --noEmit` — must exit 0.
    2. `pnpm vitest run src/audio/audioEngine.test.ts src/hooks/useAudioCues.test.tsx src/hooks/useBreathingSessionController.test.tsx` — every Phase 52 + Plan 05 test must pass.
    3. `pnpm test:run` — must show 1455 + ~11 tests passing (5 from Task 2 + 1 from Task 3 + 3 from Task 4 + 2 from Task 1 = 11 new tests; numbers approximate — accept any count >= baseline + 9).
    4. `pnpm build` — must exit 0 (tsc -b + vite emit; no dead-code warnings introduced).
    5. `pnpm lint` — pre-existing lint floor unchanged (the baseline carries 7 errors per 52-01-SUMMARY; do NOT regress).
    6. Manual VERIFICATION re-audit (no code change): re-read each `gaps[].truth` and `gaps[].missing` from `52-VERIFICATION.md`. For each CR/WR addressed by Plan 05, write a one-line confirmation in the SUMMARY indicating the file + line range that closes it.

    If any step fails: do NOT attempt to "fix" by editing other plan files. Investigate the failure source, document it in the SUMMARY's "Deviations" section per the standard executor protocol, and either (a) escalate to the user if it's a contract-level issue, or (b) auto-fix per Rule 1 (bug-introduced-by-this-plan) only.

    Commit: do NOT create a green-gate commit; the green-gate verifies the work of Tasks 1-4. The SUMMARY commit follows the executor's standard summary-write step.
  </action>

  <verify>
    <automated>pnpm tsc --noEmit && pnpm test:run && pnpm build</automated>
  </verify>

  <acceptance_criteria>
    - `pnpm tsc --noEmit` exits 0.
    - `pnpm vitest run src/audio/audioEngine.test.ts` shows 77+ tests passing (75 baseline + >= 2 new from Task 1).
    - `pnpm vitest run src/hooks/useAudioCues.test.tsx` shows 58+ tests passing (52 baseline + >= 6 new from Tasks 2 & 3).
    - `pnpm vitest run src/hooks/useBreathingSessionController.test.tsx` shows 12+ tests passing (9 baseline + 3 from Task 4).
    - `pnpm test:run` shows >= 1464 tests passing (1455 baseline + >= 9 net new). Per-step task contribution may exceed this floor; the floor is the minimum acceptable.
    - `pnpm build` exits 0.
    - SUMMARY contains a per-gap traceability list: "CR-01: closed by `useBreathingSessionController.ts` (cancel-then-reschedule wiring) + `useAudioCues.ts` (facade). Test: `audioEngine.test.ts` describe('Phase 52 CR-01') + `useBreathingSessionController.test.tsx` describe('Phase 52 CR-01-FIX')." (and similar for WR-02, CR-02, WR-01).
  </acceptance_criteria>

  <done>
    Full suite green; VERIFICATION-audited closure list documented. Phase 52 is ready for re-verification.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Controller effect <-> useAudioCues facade | Controller invokes facade methods on every rAF tick (~60Hz). The facade calls must be cheap; engine-side cancel is O(activeCues) snapshot iteration. |
| useAudioCues <-> AudioEngine | Hook reads `engineRef.current` once per call, defensively gates on null. AudioEngine guarantees `cancelFutureCues()` is closed-guarded + AH-WR-07 snapshot-iterate. |
| start() failure branch <-> surrounding effect machinery | A degraded start (null leadIn or construction throw) must leave NO dangling subscriptions, NO live engine, and a CORRECT `audioStatus` so downstream UI (MuteToggle.needsResume) reads consistently. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-52-NEW-A | Tampering (timing) | Controller top-up effect — cancel-then-reschedule window | accept (bounded) | The per-tick effect fires only on `session.currentFrame` change. Cancel and reschedule happen within the same effect run (synchronous). The window between `audioCancelFutureCues()` and `audioTopUpLookahead(cues)` is single-microtask; no rAF tick can interleave. If the rAF tick is dropped between two `session.currentFrame` updates, the effect simply does not fire — the prior tick's queue remains intact until the next frame change. Bounded by one rAF tick (~16ms). |
| T-52-NEW-B | Race condition | setMuted vs. controller top-up effect | accept (serialised) | Both `setMuted(true)` (which calls the same engine.cancelFutureCues snapshot loop inline) and the top-up effect run through React's effect runner / event handler queue; React serialises them. setMuted is a click handler effect; top-up is a useEffect on currentFrame. The DOM event loop and React's flush boundary serialise them — no concurrent mutation of `activeCues`. Confirmed by reading the engine code: `cancelFutureCues()` and `setMuted()` both use `[...activeCues]` snapshots (AH-WR-07), so even hypothetical re-entrancy would be safe. |
| T-52-NEW-C | Information disclosure / over-engineering | Engine-layer dedup (REJECTED Option B) | reject (Option A chosen) | Option B (Map<quantizedAudioTime+kind, CueHandle> dedup inside `topUpLookahead`) was rejected for three reasons documented in 52-REVIEW.md and 52-VERIFICATION.md: (a) duplicates intent already expressed by the existing `cancelFutureCues` method; (b) introduces a second invariant that must stay in sync with the `activeCues` Set; (c) does not match the existing `setMuted(true)` cancel-then-reschedule pattern documented as SCHED-05 doctrine in 52-CONTEXT.md D-10. This entry is recorded so a future plan-checker or refactor does not silently revive it. |
| T-52-NEW-D | Resource leak | start() null-leadIn branch (CR-02) | mitigate | Plan 05 Task 3 mirrors stop()'s teardown sequence (clockUnsubsRef loop, engineRef nulling, proxy revert, engine.close, cache clear) before returning null. Validated by tightened AUDIO-03 test asserting all 4 unsubs fired + engine.close called + audioStatus='unavailable'. |
| T-52-NEW-E | Stale data | lastTopUpCuesRef poisoning (WR-02) | mitigate | Plan 05 Task 2 moves the cache write AFTER the engine null-gate so a pre-start call cannot poison the cache. `stop()` clears the cache so fast stop()->start() cannot replay stale cues into a new engine. |
| T-52-08 (reversal) | Tampering | activeCues Set keyed by handle identity | re-confirm FALSE under Plan 05 | The Plan 03 threat-model entry T-52-08 ("Set keyed by handle identity is acceptable because repeated calls dispatch the SAME audioTimes for the SAME cues") was the root cause of CR-01. Under Plan 05's cancel-then-reschedule wiring, the only cues in `activeCues` at any top-up moment are the freshly scheduled ones (the prior tick's future cues have been cancelled). T-52-08 is RE-CONFIRMED FALSE as originally written; the new invariant is "Set keyed by handle identity is acceptable ONLY BECAUSE the controller cancel-then-reschedules on every top-up". Recorded so the dependency between controller wiring and engine bookkeeping is explicit. |
| T-52-NEW-SC | Tampering (supply chain) | No new packages installed | accept (no-op) | Plan 05 adds zero runtime dependencies and zero dev dependencies. DEPS-01 satisfied trivially. No package-legitimacy audit applicable. |

## Why Option A, Not Option B

Option A (cancel-then-reschedule at the controller layer) was selected per 52-VERIFICATION.md, 52-REVIEW.md, and 52-CONTEXT.md SCHED-05. It:

1. Reuses `engine.cancelFutureCues()` shipped in Plan 01 — no engine-layer change.
2. Mirrors `setMuted(true)` doctrine D-10 (Plan 04) — same cancel-then-refill pattern at the same lock-tier.
3. Documents SCHED-05 ("cancel and rescheduled cleanly") as runtime behavior, not just a deferred future-feature contract.
4. Localizes the fix at hook + controller layer, leaving the engine surface byte-stable.

Option B would have introduced engine-internal Map<audioTime+kind, CueHandle> dedup. Rejected as documented above and recorded in T-52-NEW-C.
</threat_model>

<verification>
## What this plan closes (cross-reference 52-VERIFICATION.md)

| Gap | Closed By | Test Lock |
|-----|-----------|-----------|
| CR-01 (BLOCKER) — duplicate OscillatorNode chains across consecutive top-ups | Task 4 (controller wiring) + Task 2 (facade) + Task 1 (engine-spec regression) | `audioEngine.test.ts` describe('Phase 52 CR-01') + `useBreathingSessionController.test.tsx` describe('Phase 52 CR-01-FIX') |
| SC-1 (FAILED — depends on CR-01) | CR-01 closure flows through | Manual verification per VERIFICATION.md human-verification §1 |
| SC-2 (FAILED — depends on CR-01) | CR-01 closure flows through | Manual verification per VERIFICATION.md human-verification §2 |
| SCHED-02 / SCHED-03 / SCHED-04 (BLOCKED by CR-01) | Same | Same |
| WR-02 — `lastTopUpCuesRef` cache-before-gate | Task 2 (cache-after-gate + stop() clear) | `useAudioCues.test.tsx` describe('Phase 52 WR-02-FIX') |
| CR-02 — null-leadIn engine + AC + sub leak | Task 3 (mirror stop() teardown) | `useAudioCues.test.tsx` tightened AUDIO-03 |
| WR-01 — `audioStatus` not 'unavailable' on failure | Task 3 (both failure branches) | `useAudioCues.test.tsx` tightened AUDIO-03 + new construction-catch test |
| SC-3 — mid-session BPM/timbre change | NOT IN SCOPE — deferred by design per CONTEXT §Deferred (SettingsDialog disabled in-session) | n/a |

## Human Verification (still required after Plan 05 ships)

Per 52-VERIFICATION.md, three browser tests remain (CR-01 fix needs real-browser WebAudio verification):

1. Tab background <= 6s (audio continuity, no cue doubling/phasing).
2. Tab background > 6s (clean stop on lookahead exhaustion).
3. Mute during active lookahead session (instant silence — no 6s tail of queued audio).

These are deferred to the gsd-verifier's human-verification step after Plan 05's automated suite is green.
</verification>

<success_criteria>
## Measurable Completion (Plan 05 only)

- [ ] `pnpm tsc --noEmit` exits 0.
- [ ] `pnpm test:run` shows >= 1464 tests passing (1455 baseline + >= 9 new).
- [ ] `pnpm vitest run src/audio/audioEngine.test.ts -t "Phase 52 CR-01"` exits 0 (>= 2 tests).
- [ ] `pnpm vitest run src/hooks/useAudioCues.test.tsx -t "CR-01-FIX|WR-02-FIX|AUDIO-03"` exits 0 (>= 6 tests).
- [ ] `pnpm vitest run src/hooks/useBreathingSessionController.test.tsx -t "CR-01-FIX"` exits 0 (>= 3 tests).
- [ ] `pnpm build` exits 0.
- [ ] `grep -n "audioCancelFutureCues" src/hooks/useBreathingSessionController.ts` returns >= 2 hits (destructure + effect call).
- [ ] `grep -n "engine.cancelFutureCues" src/hooks/useAudioCues.ts` returns >= 1 hit (inside the facade useCallback).
- [ ] `grep -c "setAudioStatus('unavailable')" src/hooks/useAudioCues.ts` returns >= 3 (handleClose + 2 new failure branches; note: `grep -c` counts lines, not file occurrences — sufficient for this check).
- [ ] `grep -n "lastTopUpCuesRef.current = \[\]" src/hooks/useAudioCues.ts` returns at least one hit inside `stop()`.
- [ ] AH-WR-05 / AUDIO-04 / AH-WR-07 invariants preserved — no setState-in-render added; cancel closures retain try/catch posture (no edits to cueSynth.ts / nkCueSynth.ts in this plan, so this is satisfied by non-modification).
- [ ] SUMMARY file written documenting the per-gap closure mapping table.
</success_criteria>

<output>
Create `.planning/phases/52-visibility-resume-clamp-lookahead-scheduling/52-05-SUMMARY.md` when done. Required sections:

- `requirements_completed`: SCHED-02, SCHED-03, SCHED-04, SCHED-05, DEPS-01, QUAL-01
- `tasks_completed` table: 5 tasks (RED/GREEN commits for Tasks 1-4; Task 5 is verification-only, no commit).
- `What Was Built`: per-task summary (Task 1 engine-spec regression, Task 2 facade + cache hardening, Task 3 null-leadIn teardown + WR-01, Task 4 controller wiring, Task 5 green-gate).
- `Verification Results`: full test counts and grep checks per success_criteria above.
- `Gap Closure Traceability`: table mapping CR-01, WR-02, CR-02, WR-01 to the files + tests that closed each.
- `Threat Model Updates`: confirm T-52-NEW-A..E disposition; record T-52-08 reversal.
- `Deviations from Plan`: any auto-fixes per Rule 1 / Rule 2.
- `Self-Check: PASSED` block at the bottom.
</output>
