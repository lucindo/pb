---
phase: 52-visibility-resume-clamp-lookahead-scheduling
plan: 06
type: execute
wave: 5
gap_closure: true
depends_on: [52-01, 52-02, 52-03, 52-04, 52-05]
files_modified:
  - src/hooks/useBreathingSessionController.ts
  - src/hooks/useBreathingSessionController.test.tsx
  - src/audio/audioEngine.ts
  - src/audio/audioEngine.test.ts
  - src/hooks/useAudioCues.ts
  - src/hooks/useAudioCues.test.tsx
  - src/domain/sessionAudio.ts
  - src/domain/sessionAudio.test.ts
  - src/hooks/useSessionEngine.ts
  - src/hooks/useSessionEngine.test.tsx
autonomous: true
requirements: [SCHED-01, SCHED-02, SCHED-03, SCHED-04, SCHED-05]

must_haves:
  truths:
    - "At every phase boundary, the boundary bowl cue is scheduled exactly once (no flam / double-strike), even when the rAF tick lags the audio clock across the boundary."
    - "walkFutureCues always terminates: a degenerate plan (cycleSec>0, inconsistent offsets, targetSec undefined) cannot hang the rAF tick."
    - "The clamp anchor (lastClockNowRef) advances only when the rebase that consumes it commits, so a status flip mid-updater never produces an unclamped elapsed jump on the next tick."
    - "On the reconstruction/reanchor path, stale cached cues with old-AC-origin audioTimes are never replayed (no collapsed-onto-one-instant stack of strikes)."
    - "reconstructEngine clears lastTopUpCuesRef so a later top-up cannot replay cues captured before reconstruction."
    - "Mute/unmute is symmetric and consistent with the locked Plan-04 unmute decision: no silent first-post-unmute skip and no double-handling while muted."
  artifacts:
    - path: "src/hooks/useBreathingSessionController.ts"
      provides: "Dispatch-site cue filtering against live audio clock + SAFE_LEAD_SEC; symmetric mute gating"
    - path: "src/audio/audioEngine.ts"
      provides: "audioNow() accessor + exported SAFE_LEAD_SEC symbol"
    - path: "src/domain/sessionAudio.ts"
      provides: "Hard iteration cap on walkFutureCues"
    - path: "src/hooks/useAudioCues.ts"
      provides: "Reconstruct-path top-up gating + lastTopUpCuesRef reset in reconstructEngine; audioNow facade"
    - path: "src/hooks/useSessionEngine.ts"
      provides: "Atomic clamp/rebase: anchor advance moved inside committed running-tick branch"
  key_links:
    - from: "src/hooks/useBreathingSessionController.ts"
      to: "src/audio/audioEngine.ts (audioNow, SAFE_LEAD_SEC) via useAudioCues facade"
      via: "filter cues to audioTime > audioNow() + SAFE_LEAD_SEC before dispatch"
      pattern: "audioNow|SAFE_LEAD_SEC"
    - from: "src/hooks/useAudioCues.ts (reconstructEngine)"
      to: "lastTopUpCuesRef"
      via: "reset to empty array alongside other ref resets"
      pattern: "lastTopUpCuesRef.current = \\[\\]"
---

<objective>
Second gap-closure pass for Phase 52. A code review (52-REVIEW.md) found that Plan 05's no-dedup fix is incomplete and surfaced five further correctness defects in the visibility-resume / clamp / lookahead-scheduling subsystem. This plan closes the BLOCKER (CR-01) plus WR-01..WR-05 from 52-REVIEW.md.

Authoritative gap source: `52-REVIEW.md` (newest artifact). `52-VERIFICATION.md` is STALE — it predates Plan 05 — and is used here only as background context; do not derive tasks from it.

Purpose: Restore the phase's headline guarantee — exactly one boundary cue per phase boundary — and eliminate the secondary infinite-loop, non-atomic-clamp, stale-cue-replay, and mute-asymmetry defects that compound it.

Output: Updated source + tests across the five named modules, each fix landed test-first (RED→GREEN) per the project TDD convention and Plan 05's commit pattern.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/52-visibility-resume-clamp-lookahead-scheduling/52-REVIEW.md
@.planning/phases/52-visibility-resume-clamp-lookahead-scheduling/52-CONTEXT.md
@.planning/phases/52-visibility-resume-clamp-lookahead-scheduling/52-PATTERNS.md
@.planning/phases/52-visibility-resume-clamp-lookahead-scheduling/52-05-SUMMARY.md
@.planning/phases/52-visibility-resume-clamp-lookahead-scheduling/52-04-SUMMARY.md
@CLAUDE.md

<conventions>
- Strictly test-first (TDD). Every task: write the failing test (RED) and confirm it fails for the stated reason, then implement (GREEN). Mirror Plan 05's explicit RED→GREEN commit sequencing (one `test(...)` commit, one `feat(...)`/`fix(...)` commit per fix).
- No design/value/line-number locking in tests or comments: assert on symbolic constants and behavior, never hard-coded cue counts or absolute line numbers. Reference constants by their exported symbol (SAFE_LEAD_SEC, LOOKAHEAD_WINDOW_SEC, LOOKAHEAD_MIN_CUES), never inline literals like 0.005.
- Per-commit green-gate (QUAL-01): `tsc --noEmit && lint && build && test` must pass on every commit.
- No new runtime dependencies (DEPS-01).
- Tiger Style: no magic numbers; new caps/leads are named module constants with a comment tying them to the governing window/lead constant, not an arbitrary value.
- Respect the Plan-04 setMuted D-10 cancel-then-reschedule doctrine and any locked unmute decision in 52-CONTEXT.md; do not invent new mute/unmute UX.
</conventions>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: CR-01 (BLOCKER) — eliminate race-window double-strike at phase boundaries</name>
  <files>src/audio/audioEngine.ts, src/audio/audioEngine.test.ts, src/hooks/useAudioCues.ts, src/hooks/useAudioCues.test.tsx, src/hooks/useBreathingSessionController.ts, src/hooks/useBreathingSessionController.test.tsx</files>
  <read_first>
    - 52-REVIEW.md (CR-01 section in full — the prescribed Option-a fix)
    - 52-CONTEXT.md (D-01..D-14, esp. the lookahead/SAFE_LEAD locked decisions)
    - 52-05-SUMMARY.md (what the no-dedup fix already changed; this builds directly on it)
    - src/audio/audioEngine.ts (cancelFutureCues, the now()/currentTime accessor, SAFE_LEAD_SEC definition — confirm whether it is already exported)
    - src/hooks/useAudioCues.ts (the cue dispatch path; whether an audioNow facade already exists)
    - src/hooks/useBreathingSessionController.ts (the top-up effect + the dispatch site that re-emits cues[0] for the just-crossed boundary)
    - src/audio/audioEngine.test.ts, src/hooks/useAudioCues.test.tsx, src/hooks/useBreathingSessionController.test.tsx (existing patterns; note WHY they pass today — they don't exercise the lagging-frame controller interaction)
  </read_first>
  <behavior>
    - Controller-level: when the session advances across a phase boundary with the rAF tick lagging the audio clock (boundary cue already in-flight, scheduledAt <= audioNow), scheduleInCueForTimbre for that boundary is called EXACTLY ONCE.
    - Cues whose audioTime is at or before audioNow() + SAFE_LEAD_SEC are not (re-)dispatched; only strictly-future cues dispatch.
    - audioNow() facade returns the live audio context time; SAFE_LEAD_SEC is consumed as an imported symbol, not a literal.
  </behavior>
  <action>
    Expose an `audioNow()` accessor from audioEngine.ts (returning the audio context current time) and export `SAFE_LEAD_SEC` as a named symbol if it is not already exported (do NOT hard-code 0.005). Surface `audioNow()` through useAudioCues as a facade so the controller can read the live audio clock without reaching into the engine. At the controller dispatch site in useBreathingSessionController.ts (the top-up effect that re-dispatches the new walk's cues[0] after cancelFutureCues), filter the cue list to those with `audioTime > audioNow() + SAFE_LEAD_SEC` BEFORE dispatch, so the already-in-flight just-crossed boundary cue is not re-emitted. Keep the cancelFutureCues cancel-then-reschedule doctrine intact (D-10) — this fix only drops the redundant re-dispatch of the in-flight boundary, it does not change cancellation semantics. RED: add a controller test that drives a boundary crossing with audioNow ahead of the rAF tick and asserts scheduleInCueForTimbre fires once for that boundary; note in the test comment that the pre-existing unit tests pass because they never advance the audio clock past the cue before the tick.
  </action>
  <verify>
    <automated>pnpm vitest run src/hooks/useBreathingSessionController.test.tsx src/hooks/useAudioCues.test.tsx src/audio/audioEngine.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - New controller test asserts the boundary's scheduleInCueForTimbre is called exactly once across a lagging-frame boundary crossing; it fails on the pre-fix code for the double-strike reason and passes after the dispatch-site filter.
    - audioNow() and SAFE_LEAD_SEC are referenced by symbol in the controller; no inline 0.005 literal is introduced.
    - cancelFutureCues semantics (D-10 cancel-then-reschedule) are unchanged; only the redundant re-dispatch is removed.
    - Full green-gate holds: `pnpm tsc --noEmit && pnpm lint && pnpm build && pnpm vitest run`.
  </acceptance_criteria>
  <done>Boundary cue scheduled exactly once across a lagging-frame crossing; SCHED-02/03/04 boundary guarantee restored; tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: WR-01 — hard iteration cap on walkFutureCues</name>
  <files>src/domain/sessionAudio.ts, src/domain/sessionAudio.test.ts</files>
  <read_first>
    - 52-REVIEW.md (WR-01 section; folds in IN-02's doc concern)
    - src/domain/sessionAudio.ts (walkFutureCues loop; LOOKAHEAD_WINDOW_SEC, LOOKAHEAD_MIN_CUES, cycleSec/targetSec handling)
    - src/domain/sessionAudio.test.ts (existing walk tests + assertion style)
  </read_first>
  <behavior>
    - A degenerate plan (cycleSec > 0, inconsistent phase offsets, targetSec === undefined) causes walkFutureCues to terminate at the cap instead of looping forever.
    - A well-formed plan produces the same cue sequence as before (cap not reached under normal lookahead windows).
  </behavior>
  <action>
    Add a named module constant capping walkFutureCues iterations, with a comment tying the value to LOOKAHEAD_WINDOW_SEC / LOOKAHEAD_MIN_CUES (e.g. derived as a safe multiple of the max cues a valid window can emit) — not a magic number. Break the walk loop when the cap is reached so a degenerate plan cannot hang the rAF tick. This also documents the loop bound, subsuming IN-02. RED: add a test feeding a degenerate plan (cycleSec>0, targetSec undefined, offsets that never satisfy the normal exit) and assert the function returns within the cap rather than hanging; add/keep a regression test that a normal plan's output is unchanged.
  </action>
  <verify>
    <automated>pnpm vitest run src/domain/sessionAudio.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Degenerate-plan test returns a bounded cue array (length <= cap) and completes without timeout; it would loop/hang on the pre-fix code.
    - Normal-plan regression test output is byte-for-byte unchanged.
    - The cap is a named constant whose comment references LOOKAHEAD_WINDOW_SEC/LOOKAHEAD_MIN_CUES; no bare numeric literal asserted in tests.
    - Full green-gate holds.
  </acceptance_criteria>
  <done>walkFutureCues provably terminates on degenerate input; normal output unchanged; tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: WR-03 — make clamp/rebase atomic in useSessionEngine</name>
  <files>src/hooks/useSessionEngine.ts, src/hooks/useSessionEngine.test.tsx</files>
  <read_first>
    - 52-REVIEW.md (WR-03 section)
    - src/hooks/useSessionEngine.ts (lastClockNowRef assignment relative to the setState updater; the committed-running-tick branch and the rebase that consumes the anchor)
    - src/hooks/useSessionEngine.test.tsx (existing clamp/rebase tests + status-transition harness)
  </read_first>
  <behavior>
    - When status flips to non-running between the ref-read and the state commit (updater short-circuits / discards the running branch), lastClockNowRef is NOT advanced.
    - On the next running tick, elapsed does not jump by the skipped interval (clamp still applies because the anchor only moved when the rebase committed).
  </behavior>
  <action>
    Move `lastClockNowRef.current = clockNowSec` from before the setState call to INSIDE the committed-running-tick branch of the updater, so the anchor advances only when the rebase that consumes it actually applies. Do not change the clamp math itself. RED: add a test that arranges the updater to short-circuit (status flips to non-running between ref-read and commit) and asserts no unclamped elapsed jump on the subsequent tick; confirm it fails on the pre-fix ordering.
  </action>
  <verify>
    <automated>pnpm vitest run src/hooks/useSessionEngine.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - New test: status flip mid-updater leaves lastClockNowRef unadvanced and produces no elapsed jump on the next running tick; fails on pre-fix code.
    - Existing clamp/rebase tests still pass (clamp math unchanged).
    - The anchor assignment appears only inside the committed running-tick branch.
    - Full green-gate holds.
  </acceptance_criteria>
  <done>Clamp anchor advances atomically with the rebase commit; no unclamped jump on aborted ticks; tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: WR-04 + WR-05 — reconstruction-path top-up gating and ref reset (shared useAudioCues.ts)</name>
  <files>src/hooks/useAudioCues.ts, src/hooks/useAudioCues.test.tsx</files>
  <read_first>
    - 52-REVIEW.md (WR-04 and WR-05 sections)
    - 52-CONTEXT.md (reconstruct / onReanchorRequired locked wiring)
    - src/hooks/useAudioCues.ts (handleForceTopUp, the onResume reconstruction path, onReanchorRequired wiring, reconstructEngine and its ref resets, lastTopUpCuesRef, stop()'s ref-reset pattern)
    - src/hooks/useAudioCues.test.tsx (reconstruct/reanchor + force-top-up test patterns)
  </read_first>
  <behavior>
    - WR-04: on the reconstruction onResume path, handleForceTopUp does NOT replay cached cues (whose absolute audioTimes belong to the old AC origin and would collapse onto one instant); reconstruction re-anchors via onReanchorRequired and the next boundary effect refills against the fresh anchor.
    - WR-05: reconstructEngine resets lastTopUpCuesRef to an empty array alongside its other ref resets, mirroring stop().
  </behavior>
  <action>
    WR-04: Gate handleForceTopUp so the reconstruct path does not replay cached cues — prefer matching the existing reconstruct/reanchor wiring (let onReanchorRequired + the next boundary effect refill from the fresh anchor) rather than recomputing inline; justify the chosen approach in the plan/summary against the actual wiring you read. WR-05: Inside reconstructEngine, set `lastTopUpCuesRef.current = []` alongside the other ref resets, mirroring stop()'s reset, so a later top-up cannot replay pre-reconstruction cues. RED: add two tests — (a) reconstruct onResume does NOT schedule cached old-origin cues (no collapsed stack at one instant), (b) after reconstructEngine, lastTopUpCuesRef is empty so a subsequent top-up does not replay stale cues. Both fail on pre-fix code.
  </action>
  <verify>
    <automated>pnpm vitest run src/hooks/useAudioCues.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - WR-04 test: on reconstruct onResume, no cached cues are scheduled (no multiple scheduleInCue calls collapsed at one instant); refill occurs only via the fresh-anchor boundary effect.
    - WR-05 test: lastTopUpCuesRef is empty after reconstructEngine; a following top-up does not replay pre-reconstruction cues.
    - reconstructEngine's lastTopUpCuesRef reset mirrors stop()'s reset pattern.
    - Existing reconstruct/reanchor tests still pass.
    - Full green-gate holds.
  </acceptance_criteria>
  <done>No stale-cue stack on reconstruction; lastTopUpCuesRef cleared on reconstruct; tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 5: WR-02 — resolve muted-gating asymmetry per locked decision</name>
  <files>src/hooks/useBreathingSessionController.ts, src/hooks/useBreathingSessionController.test.tsx, src/audio/audioEngine.ts, src/audio/audioEngine.test.ts</files>
  <read_first>
    - 52-REVIEW.md (WR-02 section)
    - 52-CONTEXT.md (locked unmute decision: "unmute waits for boundary" vs "refill on unmute" — implement whichever is locked)
    - 52-04-SUMMARY.md (setMuted D-10 cancel-then-reschedule doctrine)
    - src/hooks/useBreathingSessionController.ts (the cancelFutureCues call that runs unconditionally while muted; the top-up effect; mute state wiring)
    - src/audio/audioEngine.ts (setMuted, cancelFutureCues, any muted guard)
    - src/hooks/useBreathingSessionController.test.tsx, src/audio/audioEngine.test.ts (mute/unmute test patterns)
  </read_first>
  <behavior>
    - Determined by the locked CONTEXT.md decision:
      - If "gate both": while muted, neither cancelFutureCues nor top-up runs (symmetric no-op); on unmute behavior follows the locked rule.
      - If "refill on unmute": a muted guard prevents the silent-skip, and unmute drives a top-up so the first post-unmute cue is not dropped.
    - No silent first-post-unmute cue skip; no asymmetric handling while muted.
  </behavior>
  <action>
    Read 52-CONTEXT.md for the locked unmute behavior and implement THAT — do not invent new UX. Resolve the asymmetry where cancelFutureCues currently runs unconditionally while muted while top-up does not: either gate both cancel and top-up on the muted flag in the controller, or add a muted guard plus a top-up driven from the mute toggle, consistent with the locked decision and the D-10 cancel-then-reschedule doctrine. RED: add a test reproducing the silent first-post-unmute skip (or the asymmetric cancel-while-muted) and assert the symmetric/locked behavior; confirm it fails on pre-fix code. Cite the chosen locked decision in the test comment and the summary.
  </action>
  <verify>
    <automated>pnpm vitest run src/hooks/useBreathingSessionController.test.tsx src/audio/audioEngine.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - New test reproduces the pre-fix asymmetry (silent first-post-unmute skip or unconditional cancel-while-muted) and passes after the fix.
    - Implemented behavior matches the locked 52-CONTEXT.md unmute decision (cited by ID/quote in summary); no new UX invented.
    - D-10 cancel-then-reschedule doctrine remains satisfied.
    - Full green-gate holds.
  </acceptance_criteria>
  <done>Mute/unmute symmetric and consistent with the locked decision; no silent first-post-unmute skip; tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new trust boundary is introduced. All changes are local audio-timing / scheduling logic operating on in-process state (audio context clock, React refs, in-memory cue arrays). No external input, network, persistence, or privilege crossing is added or modified.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-52-06-01 | Tampering (timing race) | useBreathingSessionController dispatch site + audioEngine cancelFutureCues (CR-01) | mitigate | Filter cues against live audioNow() + SAFE_LEAD_SEC before dispatch so an in-flight boundary cue is never re-emitted; controller test asserts exactly-once scheduling across a lagging-frame crossing. |
| T-52-06-02 | Denial of Service (unbounded loop) | sessionAudio.walkFutureCues (WR-01) | mitigate | Named iteration cap tied to LOOKAHEAD_WINDOW_SEC/LOOKAHEAD_MIN_CUES prevents a degenerate plan from hanging the rAF tick (UI freeze). |
| T-52-06-03 | Tampering (stale state replay) | useAudioCues reconstruct path / lastTopUpCuesRef (WR-04/WR-05) | mitigate | Gate reconstruct-path top-up and clear lastTopUpCuesRef in reconstructEngine so old-AC-origin cues cannot be replayed. |
| T-52-06-SC | Tampering | npm/pnpm installs | accept | No new runtime dependencies added (DEPS-01); no package-manager install tasks in this plan, so the supply-chain checkpoint is N/A. |
</threat_model>

<verification>
- Full suite: `pnpm tsc --noEmit && pnpm lint && pnpm build && pnpm vitest run` passes after every commit (QUAL-01).
- CR-01 controller test asserts exactly-once boundary scheduling under a lagging-frame crossing.
- walkFutureCues degenerate-plan test terminates within the named cap; normal-plan output unchanged.
- useSessionEngine status-flip test shows no unclamped elapsed jump.
- useAudioCues reconstruct tests show no stale-cue stack and empty lastTopUpCuesRef post-reconstruct.
- Mute/unmute test matches the locked CONTEXT.md unmute decision.
- No new runtime dependency added (DEPS-01); no hard-coded magic numbers or line-number locks in tests/comments.
</verification>

<success_criteria>
- All 6 in-scope findings (CR-01, WR-01, WR-02, WR-03, WR-04, WR-05) closed with a failing-first test that passes after the fix.
- Phase headline guarantee restored: exactly one boundary cue per phase boundary across visibility-resume / lagging-frame crossings (SCHED-02/03/04).
- Each fix landed as a RED→GREEN commit pair; green-gate holds on every commit.
- IN-01, IN-02 (folded into WR-01 doc), IN-03 explicitly NOT touched (operator-deferred).
</success_criteria>

<output>
Create `.planning/phases/52-visibility-resume-clamp-lookahead-scheduling/52-06-SUMMARY.md` when done.
</output>
