---
phase: 52-visibility-resume-clamp-lookahead-scheduling
plan: 04
type: execute
wave: 3
depends_on:
  - 52-01
  - 52-03
files_modified:
  - src/audio/audioEngine.ts
  - src/audio/audioEngine.test.ts
  - src/hooks/useAudioCues.ts
  - src/hooks/useAudioCues.test.tsx
autonomous: true
requirements:
  - SCHED-05
  - SCHED-03
  - DEPS-01
  - QUAL-01
tags:
  - audio
  - scheduling
  - mute
  - resume

must_haves:
  truths:
    - "D-04: clock.onResume force-top-up subscription — useAudioCues subscribes handleForceTopUp to engine.clock.onResume (alongside the existing handleResume) so AC reconstruction / iOS unlock re-dispatches the cached cues before the next rAF tick fires (D-04's second half; the rAF-tick top-up half is Plan 03)."
    - "D-10: setMuted(true) runs applyMuteFadeOut on in-flight cues (scheduledAt <= now) AND cancelFutureCues() on future cues (scheduledAt > now) — both branches fire in the same call so the ~6s lookahead queue does not degrade mute UX."
    - "Engine setMuted(true) cancels future-queued cues via cancelFutureCues() (Plan 01) AND still fades in-flight cues via applyMuteFadeOut (Phase 18 D-08 + Phase 50 D-13 preserved)"
    - "Mute during lookahead does not leave 6 seconds of audible audio queued — user-perceptually instant mute"
    - "clock.onResume subscription fires engine.topUpLookahead with the current frame so AC reconstruction / iOS unlock force-tops up before the next rAF tick"
    - "All four clock subscriptions (onResume, onSuspend, onClose, onResume-for-top-up) survive engine reconstruction via the existing clockUnsubsRef teardown loop"
    - "Mid-session BPM/timbre change (SCHED-05 defensive path) results in no stale cues firing — settings transitions trigger cancelFutureCues + the next top-up re-queues from fresh state"
  artifacts:
    - path: "src/audio/audioEngine.ts"
      provides: "setMuted body extended: applyMuteFadeOut for in-flight cues AND cancelFutureCues() for future cues"
      contains: "cancelFutureCues()"
    - path: "src/hooks/useAudioCues.ts"
      provides: "New forceTopUp useCallback subscribed to clock.onResume; wired into clockUnsubsRef alongside the three existing subscriptions"
      contains: "handleForceTopUp"
  key_links:
    - from: "audioEngine.ts:setMuted(true)"
      to: "applyMuteFadeOut (in-flight) + cancelFutureCues (future)"
      via: "two iterations or single-loop branch on scheduledAt > now"
      pattern: "cancelFutureCues\\("
    - from: "useAudioCues.ts:handleForceTopUp"
      to: "engine.topUpLookahead via the cached lastTopUpCuesRef"
      via: "subscribed to clock.onResume; re-dispatches the last cues array passed to topUpLookahead"
      pattern: "clock\\.onResume\\(handleForceTopUp"
---

<objective>
Close the user-visible Phase 52 gap by:
1. Extending engine `setMuted(true)` to ALSO call `cancelFutureCues()` so future-queued cues from the lookahead window are stopped — without this, with Plan 03's deeper queue (~6s) a user pressing Mute would still hear up to 6 seconds of audio. D-10.
2. Wiring `clock.onResume` to fire a force-top-up via the new facade so post-AC-reconstruction (iOS unlock, engine swap) the queue refills BEFORE the next rAF tick. D-04 force-top-up half.

Purpose: Plan 01 added the engine-side `cancelFutureCues` + `topUpLookahead` methods. Plan 03 added the per-tick top-up trigger and the `audio.topUpLookahead` facade in useAudioCues. Plan 04 connects the two REMAINING wires: the mute-side cancel path (so the deeper queue doesn't degrade mute UX) and the onResume-side force-top-up (so iOS recovery / async resume doesn't leave a gap between when the AC resumes and when rAF fires).

Wave 3 (not Wave 2) because both `useAudioCues.ts` and `audioEngine.ts` are touched by upstream plans (Plan 03 adds the topUpLookahead facade in useAudioCues; Plan 01 added cancelFutureCues in audioEngine) — Plan 04 must serialize after both to avoid same-wave file-overlap conflicts.

Output: An extended `setMuted` body in `audioEngine.ts`, a new `handleForceTopUp` useCallback in `useAudioCues.ts` subscribed to `clock.onResume` and re-subscribed across reconstruction (cached-cues design — no cross-cutting modification of useBreathingSessionController.ts), plus ≥10 Vitest tests across both files.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/52-visibility-resume-clamp-lookahead-scheduling/52-CONTEXT.md
@.planning/phases/52-visibility-resume-clamp-lookahead-scheduling/52-PATTERNS.md
@.planning/phases/52-visibility-resume-clamp-lookahead-scheduling/52-01-PLAN.md
@.planning/phases/52-visibility-resume-clamp-lookahead-scheduling/52-03-PLAN.md
@.planning/phases/50-sessionclock-scheduler-abstraction/50-CONTEXT.md
@.planning/phases/51-master-clock-unification/51-CONTEXT.md

<interfaces>
<!-- Plan 04 reads Plan 01's cancelFutureCues + topUpLookahead AND Plan 03's audio.topUpLookahead facade. -->

From src/audio/audioEngine.ts (Plan 01 contracts):
- export const LOOKAHEAD_WINDOW_SEC: 6
- export const LOOKAHEAD_MIN_CUES: 2
- interface AudioEngine {
    topUpLookahead(args: { cues: Array<{...}> }): void  // Plan 01 Task 3
    cancelFutureCues(): void                            // Plan 01 Task 3
    setMuted(muted: boolean): void                      // existing — Plan 04 EXTENDS
    ...
  }

From src/audio/audioEngine.ts L458-475 (existing setMuted — Plan 04 EXTENDS):
- if (next && activeCues.size > 0): pruneExpiredCues + applyMuteFadeOut per cue. Plan 04 splits: in-flight branch (existing) + future branch (calls the new cancel path).

From src/hooks/useAudioCues.ts L213-246 (existing clock subscribers — Plan 04 ADDS a 4th):
- const handleResume = useCallback((): void => { const engine = engineRef.current; if (engine === null) return; void engine; visibilityResumeAttemptedRef.current = false; setAudioStatus('ok'); }, [])
- const handleSuspend = useCallback(...) — Pitfall 5 gate
- const handleClose = useCallback(...) — close handler

From src/hooks/useAudioCues.ts L341-344 (subscription registration — Plan 04 ADDS one more):
- const unsubResume = engine.clock.onResume(handleResume)
- const unsubSuspend = engine.clock.onSuspend(handleSuspend)
- const unsubClose = engine.clock.onClose(handleClose)
- clockUnsubsRef.current = [unsubResume, unsubSuspend, unsubClose]

From src/hooks/useAudioCues.ts L438-545 (existing reconstructEngine flow — Plan 04 ADDS the 4th subscription to the new-engine subscription block):
- After reconstructEngine builds newEngine, it re-subscribes the three existing handlers — Plan 04 adds the 4th forceTopUp subscription here too (mirror posture).

From src/hooks/useAudioCues.ts (Plan 03 ADDITIONS — read 52-03-PLAN.md Task 2 Part A):
- const topUpLookahead = useCallback((cues: Array<{...}>) => { ... engine.topUpLookahead({ cues }) }, [])
- Plan 04 ADDS a write to a new lastTopUpCuesRef inside this useCallback (cache the cues array).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend engine setMuted(true) to call cancelFutureCues after applyMuteFadeOut</name>
  <files>src/audio/audioEngine.ts, src/audio/audioEngine.test.ts</files>
  <read_first>
    - src/audio/audioEngine.ts L458-475 (current setMuted body — the EXTENSION TARGET)
    - src/audio/audioEngine.ts L169-192 (applyMuteFadeOut — UNCHANGED, just called as before)
    - src/audio/audioEngine.ts (the new cancelFutureCues helper added by Plan 01 Task 3)
    - .planning/phases/52-visibility-resume-clamp-lookahead-scheduling/52-CONTEXT.md §Decisions D-10
    - .planning/phases/52-visibility-resume-clamp-lookahead-scheduling/52-PATTERNS.md §"applyMuteFadeOut integration + new future-cue cancel path" (lines 105-127 — explicit "two branches: in-flight cues / future cues" guidance)
    - .planning/phases/50-sessionclock-scheduler-abstraction/50-CONTEXT.md §Decisions D-13 (per-cue mute fade PRESERVED — Plan 04 must not regress this)
  </read_first>
  <behavior>
    - Test 1: setMuted(true) with both in-flight cues (scheduledAt ≤ now) AND future cues (scheduledAt > now) in activeCues — in-flight cues get applyMuteFadeOut called on them (envelope.gain.cancelAndHoldAtTime / setTargetAtTime), future cues get cancel() called (osc.stop + disconnect). Both branches fire in the same setMuted call.
    - Test 2: setMuted(true) with only future cues — applyMuteFadeOut is NOT called (no in-flight cues to fade); cancelFutureCues IS called.
    - Test 3: setMuted(true) with only in-flight cues — applyMuteFadeOut called as today; cancelFutureCues is a no-op (no cues with scheduledAt > now).
    - Test 4: setMuted(false) — unchanged from existing behavior (no fade-in, no cue dispatch; "unmute waits for next boundary" per D-08, restated in CONTEXT D-10). Plan 04 does NOT alter unmute.
    - Test 5: setMuted(true) on closed engine — early return (closed-guard preserved); neither applyMuteFadeOut nor cancelFutureCues fires.
    - Test 6: future cues removed from activeCues after cancel — verify activeCues.size shrinks by the count of cancelled future cues (since cancelFutureCues calls activeCues.delete(cue) per Plan 01 Task 3).
  </behavior>
  <action>
    Per D-10 + PATTERNS.md §"applyMuteFadeOut integration + new future-cue cancel path":

    Extend the existing `setMuted(next: boolean)` body in `src/audio/audioEngine.ts` (currently L458-475).

    Two-step execution inside the `if (next && activeCues.size > 0)` branch:
    1. Existing loop modified to filter by `scheduledAt <= audioCtx.currentTime`:
       - `for (const cue of activeCues) { if (cue.scheduledAt <= audioCtx.currentTime) { applyMuteFadeOut(cue, audioCtx) } }`
       - This avoids redundant work on cues that are about to be cancelled (applyMuteFadeOut on a future cue would clobber the pre-scheduled envelope.gain automation BEFORE the strike fires; while not audible, it's wasted work).
    2. After the existing loop, call `cancelFutureCues()` — handles every cue with `scheduledAt > now` per Plan 01 Task 3 (cancels + removes from Set).

    Final structure:
    ```typescript
    setMuted(next: boolean): void {
      if (closed) {
        muted = next
        return
      }
      if (next && activeCues.size > 0) {
        // D-08 + WR-08 (PRESERVED): muting mid-cue applies fade-out to in-flight cues.
        // Phase 52 D-10: split iteration — in-flight branch fades, future branch cancels.
        pruneExpiredCues()
        const now = audioCtx.currentTime
        for (const cue of activeCues) {
          if (cue.scheduledAt <= now) {
            applyMuteFadeOut(cue, audioCtx)
          }
        }
        // Phase 52 D-10: future-queued cues from the lookahead window get hard-cancelled.
        // Without this, the deeper queue (~6s) would leave audible audio after mute.
        cancelFutureCues()
      }
      // D-08 (PRESERVED): unmute waits for next boundary (here: next top-up tick re-queues from
      // the fresh muted=false state). No make-up cue dispatched here.
      muted = next
    }
    ```

    **Important constraints:**
    - The CONTEXT D-10 phrasing "preserves today's D-08 behavior" is honored — in-flight cues still fade via applyMuteFadeOut (just gated on scheduledAt ≤ now).
    - cancelFutureCues is called AFTER the in-flight loop so the loop sees the original set (avoid mutating activeCues mid-iteration in the in-flight branch).
    - PRESERVE the `pruneExpiredCues()` call at the top — phantom-cue protection per the L308-315 comment.

    Tests in audioEngine.test.ts — new `describe('Phase 52 D-10 setMuted future-cue cancel', ...)` block. Extend `makeMockCueHandle` (already extended in Plan 01 Task 3 to include `cancel: vi.fn()`) — use the mock with deliberate `scheduledAt` values to cover both branches.
  </action>
  <verify>
    <automated>pnpm vitest run src/audio/audioEngine.test.ts -t "Phase 52 D-10" --reporter=verbose</automated>
    <!--
      SCHED-05 coverage note (defensive close per CONTEXT §Deferred):
      SCHED-05's "no stale cues fire on settings change" is behaviorally locked here by Test 1
      (setMuted(true) → cancelFutureCues path), but only along the MUTE path. SettingsDialog is
      disabled in-session per Phase 14 inSessionView contract (CONTEXT §Deferred), so a real
      BPM/timbre-change-mid-session test would require building scaffolding for a flow that is
      deliberately blocked. The cancelFutureCues mechanism is the same surface area that would
      service a future mid-session settings change. If a future phase enables mid-session
      settings UI, a settings-change behavioral test (simulate settings change → assert
      cancelFutureCues fires → assert next top-up produces cues with new settings) MUST be added
      at that time to upgrade SCHED-05 from defensive close to direct behavioral lock.
    -->
  </verify>
  <acceptance_criteria>
    - Source: setMuted body now contains BOTH applyMuteFadeOut call AND cancelFutureCues call — `awk '/setMuted\\(next: boolean\\): void {/,/^    }/' src/audio/audioEngine.ts | grep -cE "(applyMuteFadeOut|cancelFutureCues)"` returns ≥2.
    - Source: in-flight branch is gated by scheduledAt ≤ now — `awk '/setMuted\\(next: boolean\\): void {/,/^    }/' src/audio/audioEngine.ts | grep -c "scheduledAt <= now"` returns ≥1.
    - Source: D-10 comment cites the cancel-future rationale — `grep -B 1 "cancelFutureCues()" src/audio/audioEngine.ts | grep -c "D-10"` returns ≥1.
    - Source: D-08 PRESERVED comment present — `grep -c "D-08" src/audio/audioEngine.ts` returns ≥1 (existing comment intact OR re-asserted).
    - Source: pruneExpiredCues call still at top of mute branch — `awk '/setMuted\\(next: boolean\\): void {/,/^    }/' src/audio/audioEngine.ts | grep -c "pruneExpiredCues()"` returns ≥1.
    - Test: `pnpm vitest run src/audio/audioEngine.test.ts -t "Phase 52 D-10"` exits 0; ≥6 it() blocks pass.
    - Test: existing setMuted tests STILL pass — `pnpm vitest run src/audio/audioEngine.test.ts -t "setMuted"` exits 0; pre-Plan-04 baseline tests intact.
    - CLI: `pnpm typecheck` exits 0.
    - CLI: `pnpm lint` exits 0.
    - CLI: `pnpm test` exits 0.
  </acceptance_criteria>
  <done>Engine setMuted(true) cancels future cues via cancelFutureCues AND fades in-flight cues via applyMuteFadeOut; D-08 unmute-waits-for-boundary preserved; pre-Plan-04 setMuted tests unchanged; new D-10 tests lock the two-branch contract.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: clock.onResume → handleForceTopUp subscription in useAudioCues (cached-cues design); survives reconstruction</name>
  <files>src/hooks/useAudioCues.ts, src/hooks/useAudioCues.test.tsx</files>
  <read_first>
    - src/hooks/useAudioCues.ts (full file; especially L213-246 subscribers, L341-344 subscription block, L262-263 unmount teardown, L394-395 stop() teardown, L438-545 reconstructEngine flow with its new-engine subscription block around L515-545)
    - src/hooks/useAudioCues.ts AFTER Plan 03 lands — the new topUpLookahead useCallback added by Plan 03 Task 2 Part A is the WRITE site for the new lastTopUpCuesRef cache
    - src/hooks/useAudioCues.test.tsx (full file — engine mock + clock onResume firing harness)
    - .planning/phases/52-visibility-resume-clamp-lookahead-scheduling/52-CONTEXT.md §Decisions D-04
    - .planning/phases/52-visibility-resume-clamp-lookahead-scheduling/52-PATTERNS.md §"src/hooks/useAudioCues.ts" — "Existing handler shape — copy posture for forceTopUp handler" + "Subscription registration" + "Subscription teardown"
    - .planning/phases/52-visibility-resume-clamp-lookahead-scheduling/52-03-PLAN.md §Task 2 Part A (defines the audio.topUpLookahead facade Plan 04 hooks into)
    - .planning/phases/51-master-clock-unification/51-CONTEXT.md §Decisions D-04 (subscription forwarding survives source swap — the proxy makes Plan 04's onResume sub survive iOS reconstruction)
  </read_first>
  <behavior>
    - Test 1: clock.onResume fires → handleForceTopUp callback is invoked with NO error even if engineRef is null (defensive gate per the handleResume L214-217 pattern).
    - Test 2: clock.onResume fires after engine.start() AND after at least one topUpLookahead call has cached cues → handleForceTopUp re-dispatches those cached cues via engine.topUpLookahead — verify the engine.topUpLookahead spy is called with the SAME cues array.
    - Test 3: reconstruction (Phase 51 D-11 flow) — after reconstructEngine builds newEngine, fire newEngine.clock.onResume — handleForceTopUp still fires; uses the NEW engine's topUpLookahead (NOT the old one).
    - Test 4: unmount during running session — the forceTopUp subscription is in clockUnsubsRef and is torn down by the existing iteration loop (L262-263); no leaked listener.
    - Test 5: stop() teardown — same: the iteration at L394-395 handles the 4th subscription.
    - Test 6: forceTopUp before any topUpLookahead dispatched — lastTopUpCuesRef is empty array; handleForceTopUp early-returns (no engine call). No error.
    - Test 7: lastTopUpCuesRef is updated each time topUpLookahead is called — successive top-ups overwrite the cache; the latest array is what handleForceTopUp re-dispatches.
  </behavior>
  <action>
    Per D-04 + PATTERNS.md §"src/hooks/useAudioCues.ts":

    Plan 03 introduces `audio.topUpLookahead(cues)` as a useAudioCues facade method. Plan 04 needs a "force top-up NOW" capability subscribed to `clock.onResume`. The cue-walk computation lives in the controller (Plan 03 Task 2), but `useAudioCues` doesn't have access to the controller's frame/anchor/plan refs by design (separation of concerns).

    **Design choice: cached-cues indirection.** useAudioCues caches the LAST cues array passed to `topUpLookahead` in a ref, and `handleForceTopUp` re-dispatches that cached array via the engine. The controller's next regular tick will overwrite the cache with fresh cues. This avoids any modification to useBreathingSessionController.ts and avoids the file-edit conflict with Plan 03.

    Caveat: the cached cues may be slightly stale (≤ 1 rAF tick behind ≈ 16ms), but the engine's SAFE_LEAD_SEC callee clamp (Plan 01 topUpLookahead Task 3) handles that — cues with `audioTime < currentTime` are clamped forward to `currentTime + SAFE_LEAD_SEC`. Document this in the code comment.

    Implementation steps:

    1. Add a new ref near the existing handler declarations (around L177-201):
       ```typescript
       /** Phase 52 D-04: cache the last cues array dispatched via topUpLookahead.
        *  handleForceTopUp re-dispatches this on clock.onResume so AC reconstruction /
        *  iOS unlock force-tops up before the next rAF tick fires. Stale by ≤ 1 rAF
        *  (~16ms); engine's SAFE_LEAD_SEC callee clamp absorbs the drift. */
       const lastTopUpCuesRef = useRef<Array<{ audioTime: number; phaseDurationSec: number; kind: 'in' | 'out' }>>([])
       ```

    2. **Modify** the existing `topUpLookahead` useCallback (added by Plan 03 Task 2 Part A) to ALSO write to the ref:
       ```typescript
       const topUpLookahead = useCallback((cues: Array<{ audioTime: number; phaseDurationSec: number; kind: 'in' | 'out' }>): void => {
         lastTopUpCuesRef.current = cues  // Phase 52 D-04: cache for force-top-up on resume
         const engine = engineRef.current
         if (engine === null) return
         engine.topUpLookahead({ cues })
       }, [])
       ```

    3. Add the new handler `handleForceTopUp` mirroring handleResume's defensive gate posture (L213-222):
       ```typescript
       const handleForceTopUp = useCallback((): void => {
         // Phase 52 D-04: AC reconstruction / iOS unlock force-top-up. Re-dispatches
         // last cues so the queue refills before the next rAF tick fires.
         const engine = engineRef.current
         if (engine === null) return
         const cues = lastTopUpCuesRef.current
         if (cues.length === 0) return  // No prior dispatch (e.g., lead-in still running) — safe no-op.
         engine.topUpLookahead({ cues })
       }, [])
       ```

    4. Subscribe in the start-time subscription block (currently L341-344):
       ```typescript
       const unsubResume = engine.clock.onResume(handleResume)
       const unsubSuspend = engine.clock.onSuspend(handleSuspend)
       const unsubClose = engine.clock.onClose(handleClose)
       const unsubForceTopUp = engine.clock.onResume(handleForceTopUp)  // Phase 52 D-04
       clockUnsubsRef.current = [unsubResume, unsubSuspend, unsubClose, unsubForceTopUp]
       ```

    5. **Mirror inside reconstructEngine** (find the new-engine subscription block around L515-545; PATTERNS.md notes Phase 51 D-11 makes the proxy survive reconstruction, but the subscriptions are re-attached against `newEngine.clock` explicitly — Plan 04 adds the 4th subscription here too).

    6. Teardown — UNCHANGED: the existing iteration loops at L262-263 (unmount), L394-395 (stop), L465-466 (reconstructEngine) all iterate `clockUnsubsRef.current` as an array, so the 4th member is automatically cleaned up. No new teardown code.

    7. Tests in useAudioCues.test.tsx — new `describe('Phase 52 D-04 forceTopUp on clock.onResume', ...)` block. Use the existing engine-mock harness. Fire `clock.onResume` via the existing FakeAudioContext mechanism (`_fireStateChange` with state='running' from prior 'suspended' — or via the Phase 51 swappable proxy mock if used in this test).
  </action>
  <verify>
    <automated>pnpm vitest run src/hooks/useAudioCues.test.tsx -t "Phase 52 D-04" --reporter=verbose</automated>
  </verify>
  <acceptance_criteria>
    - Source: `grep -n "handleForceTopUp" src/hooks/useAudioCues.ts` returns ≥2 lines (declaration + subscription).
    - Source: `grep -n "lastTopUpCuesRef" src/hooks/useAudioCues.ts` returns ≥3 lines (declaration + write in topUpLookahead + read in handleForceTopUp).
    - Source: 4th subscription wired alongside the existing three — `grep -c "unsubForceTopUp" src/hooks/useAudioCues.ts` returns ≥2 (once at start, once at reconstructEngine — verify both paths re-attach the 4th sub).
    - Source: handleForceTopUp uses the defensive gate posture — `grep -A 6 "handleForceTopUp = useCallback" src/hooks/useAudioCues.ts | grep -c "engineRef.current"` returns ≥1.
    - Source: D-04 comment present near handleForceTopUp — `grep -B 3 "handleForceTopUp = useCallback" src/hooks/useAudioCues.ts | grep -cE "D-04"` returns ≥1.
    - Source: topUpLookahead caches the cues array — `awk '/topUpLookahead = useCallback/,/}, \\[\\])/' src/hooks/useAudioCues.ts | grep -c "lastTopUpCuesRef"` returns ≥1.
    - Test: `pnpm vitest run src/hooks/useAudioCues.test.tsx -t "Phase 52 D-04"` exits 0; ≥7 it() blocks pass.
    - Test: existing useAudioCues subscription teardown tests STILL pass — `pnpm vitest run src/hooks/useAudioCues.test.tsx -t "subscription"` exits 0.
    - Test: full file passes — `pnpm vitest run src/hooks/useAudioCues.test.tsx` exits 0.
    - CLI: `pnpm typecheck` exits 0.
    - CLI: `pnpm lint` exits 0.
    - CLI: `pnpm build` exits 0.
    - CLI: `pnpm test` exits 0 — full suite green.
  </acceptance_criteria>
  <done>useAudioCues subscribes a handleForceTopUp handler to clock.onResume that re-dispatches the last-known cues; subscription survives reconstruction via the existing teardown-loop pattern; no file conflict with Plan 03 (no modification to useBreathingSessionController.ts); ≥7 tests lock the contract.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User mute action → engine.setMuted(true) | User-triggered, infrequent (clicks-per-session). Cancel iterates activeCues; bounded. |
| clock.onResume → handleForceTopUp | Browser-fired (AC resume from suspend / iOS unlock / engine reconstruction). Can fire repeatedly if AC bounces; each fire dispatches the last-known cues to the engine. |
| handleForceTopUp re-dispatch with stale cues | Cached cues may be ≤ 1 rAF stale (~16ms); engine SAFE_LEAD_SEC clamp absorbs the drift. Worst case: cue audioTime < audioCtx.currentTime → engine clamps to currentTime + SAFE_LEAD_SEC (no audible break). |
| Reconstruction re-subscribes 4 handlers | Phase 51 D-04 makes the proxy survive source swaps; explicit re-subscription in reconstructEngine ensures the 4th sub is attached against newEngine.clock. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-52-12 | Tampering | setMuted iteration order — applyMuteFadeOut after cancelFutureCues mutates Set | mitigate | Task 1 action explicitly orders: applyMuteFadeOut FIRST (over original activeCues snapshot), then cancelFutureCues. cancelFutureCues uses the snapshot-iterate pattern from Plan 01 Task 3 (`for (const cue of [...activeCues])`). No iteration-during-mutation hazard. |
| T-52-13 | Denial of Service | clock.onResume firing rapidly → forceTopUp dispatches stale cues 60+ times | accept | onResume is browser-fired on AC state transitions (suspend → running); these are rare (iOS lock/unlock, tab visibility transitions). Each dispatch is bounded by the cached cues array length (≤ ~12 cues per Plan 03 walk). Engine's pruneExpiredCues + activeCues Set semantics keep the queue bounded. Cost negligible. |
| T-52-14 | Information Disclosure | stale cues with old audioTime values dispatched on resume | mitigate | Engine's SAFE_LEAD_SEC callee-side clamp (Plan 01 Task 3, mirrors scheduleNextCue L439) ensures every cue dispatched has `audioTime ≥ audioCtx.currentTime + SAFE_LEAD_SEC`. Stale cues with audioTime < currentTime are clamped forward. No silent missed cues. Documented in topUpLookahead code comment. |
| T-52-15 | Tampering | reconstruction misses the 4th subscription | mitigate | Acceptance criterion: `grep -c "unsubForceTopUp" src/hooks/useAudioCues.ts` returns ≥2 — explicit verification that the 4th sub is re-attached in BOTH the start path AND the reconstructEngine path. Test 3 verifies behaviorally (post-reconstruction onResume fires forceTopUp on newEngine). |
| T-52-16 | Elevation of Privilege | mute-during-mute reentrance | accept | setMuted is idempotent — second `setMuted(true)` call enters the `next && activeCues.size > 0` branch but pruneExpiredCues + the scheduledAt ≤ now gate handle the empty/already-cancelled state cleanly. No recursion, no privilege change. |
| T-52-SC | Tampering | npm/pip/cargo installs | n/a | DEPS-01: NO new runtime dependencies in Plan 04. Reuses Plan 01 cancelFutureCues + Plan 03 topUpLookahead. Legitimacy gate not required. |

**Note:** No new I/O, no new auth surface, no new network. The cancel-future path is in-process and bounded by `activeCues.size`; the onResume forceTopUp path is bounded by the cached cues array length.
</threat_model>

<verification>
- `pnpm vitest run src/audio/audioEngine.test.ts` exits 0.
- `pnpm vitest run src/hooks/useAudioCues.test.tsx` exits 0.
- `pnpm typecheck` exits 0.
- `pnpm lint` exits 0.
- `pnpm build` exits 0.
- `pnpm test` exits 0 — total test count ≥ baseline + Plans 01+02+03+04 additions.
- Mute UX: a mid-session mute press cancels the lookahead queue + fades the in-flight cue — verified via Task 1 Test 1.
- iOS recovery + force-top-up: post-reconstruction clock.onResume re-dispatches the last cues — verified via Task 2 Test 3.
- Reconstruction subscription survival: 4th subscription explicitly re-attached in reconstructEngine path.
- SCHED-05 (defensive cancel/reschedule on settings change): the SettingsDialog-during-session is disabled (Phase 14 inSessionView contract per CONTEXT §Deferred), so this is a defensive close — the cancel paths from Plan 01 D-09 + Plan 04 D-10 provide the surface area without a user-facing trigger.
</verification>

<success_criteria>
- D-10: engine setMuted(true) cancels future-queued cues via cancelFutureCues() AND fades in-flight cues via applyMuteFadeOut — the deeper queue (~6s) does NOT degrade mute UX.
- D-04 force-top-up half: clock.onResume → handleForceTopUp re-dispatches the last-known cues so AC reconstruction / iOS unlock refills the queue before the next rAF tick fires.
- D-08 (Phase 18 + Phase 50 D-13) PRESERVED: unmute waits for next boundary; per-cue mute fade applies to in-flight cues.
- Phase 51 D-04 (subscription forwarding across source swap) PRESERVED: 4th subscription survives reconstructEngine via the existing teardown-loop pattern.
- SCHED-05 (mid-session BPM/timbre change → no stale cues): **defensive close.** SettingsDialog is disabled in-session per the Phase 14 inSessionView contract (CONTEXT §Deferred), so no user-facing trigger for a mid-session settings change currently exists. The cancel mechanism is behaviorally locked by Task 1 Test 1 (setMuted(true) → cancelFutureCues path) — the SAME surface area that would service a settings-change reset. If a future phase enables mid-session settings UI, a settings-change behavioral test (simulate settings change → assert cancelFutureCues fires → assert next top-up produces cues with new settings) MUST be added at that time to upgrade SCHED-05 from defensive close to direct behavioral lock.
- SCHED-03 (audio continues through hidden window): combined Plans 01+02+03+04 satisfy this — top-up keeps queue full, clamp suppresses catch-up, onResume re-fills if AC reconstructs.
- Foreground mute behavior baseline parity: pre-Plan-04 setMuted tests still pass; per-cue fade behavior unchanged for in-flight cues.
- QUAL-01 honored: every commit in this plan passes `pnpm tsc && pnpm lint && pnpm build && pnpm test`.
- DEPS-01 honored: zero new entries in package.json `dependencies`.
</success_criteria>

<output>
Create `.planning/phases/52-visibility-resume-clamp-lookahead-scheduling/52-04-SUMMARY.md` when done.
</output>
</content>
