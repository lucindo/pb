---
phase: 47-persistable-feature-flag-preferences
plan: 04
type: execute
wave: 3
depends_on:
  - 47-02
files_modified:
  - src/hooks/useBreathingShapeChoice.ts
  - src/hooks/useRingCueChoice.ts
  - src/hooks/useOrbIdleChoice.ts
  - src/hooks/useSwitcherIconChoice.ts
  - src/hooks/useBreathingShapeChoice.test.ts
  - src/hooks/useRingCueChoice.test.ts
  - src/hooks/useOrbIdleChoice.test.ts
  - src/hooks/useSwitcherIconChoice.test.ts
autonomous: true
requirements:
  - PREFS-03
user_setup: []

must_haves:
  truths:
    - "Four picker-side choice hooks exist — `useBreathingShapeChoice`, `useRingCueChoice`, `useOrbIdleChoice`, `useSwitcherIconChoice` — each shaped like `useTimbreChoice` (PREFS-03 setter API foundation; D-09)"
    - "Each setter writes through `savePrefs({ ...loadPrefs(), [field]: next })`, preserving the other 7 prefs fields per the Phase 14 D-17 envelope-merge contract"
    - "Each setter dispatches `new CustomEvent('hrv:prefs-changed', { detail: { key: '<field>', value: next } })` with the field-specific `detail.key` per D-10"
    - "Each setter calls its `setState` setter BEFORE dispatching the CustomEvent — picker reflects change optimistically without waiting for any round-trip (D-09 verbatim from `useTimbreChoice`)"
    - "Each setter has stable identity across re-renders (`useCallback` with empty deps — mirrors `useTimbreChoice` line 34)"
    - "Each hook's initial state seeds from `loadPrefs().<field>` via `useState(() => loadPrefs().<field>)` (D-09)"
    - "The four hooks consume `loadPrefs()` / `savePrefs()` from Plan 02's 8-field UserPrefs without surprising the other 4 prefs fields"
  artifacts:
    - path: "src/hooks/useBreathingShapeChoice.ts"
      provides: "Picker-side choice hook for `breathingShape`"
      contains: "loadPrefs"
      contains: "savePrefs"
      contains: "hrv:prefs-changed"
      contains: "breathingShape"
    - path: "src/hooks/useRingCueChoice.ts"
      provides: "Picker-side choice hook for `ringCue`"
      contains: "ringCue"
    - path: "src/hooks/useOrbIdleChoice.ts"
      provides: "Picker-side choice hook for `orbIdle`"
      contains: "orbIdle"
    - path: "src/hooks/useSwitcherIconChoice.ts"
      provides: "Picker-side choice hook for `switcherIcon` (boolean variant)"
      contains: "switcherIcon"
    - path: "src/hooks/useBreathingShapeChoice.test.ts"
      provides: "Round-trip + envelope-merge + CustomEvent dispatch + setter-identity coverage"
    - path: "src/hooks/useRingCueChoice.test.ts"
      provides: "Same as above for ringCue"
    - path: "src/hooks/useOrbIdleChoice.test.ts"
      provides: "Same as above for orbIdle"
    - path: "src/hooks/useSwitcherIconChoice.test.ts"
      provides: "Same as above for switcherIcon (boolean values)"
  key_links:
    - from: "src/hooks/useBreathingShapeChoice.ts:setBreathingShape"
      to: "src/storage/prefs.ts:savePrefs"
      via: "spread-merge envelope write"
      pattern: "savePrefs\\(\\{ \\.\\.\\.current, breathingShape: next \\}\\)"
    - from: "src/hooks/useRingCueChoice.ts:setRingCue"
      to: "src/storage/prefs.ts:savePrefs"
      via: "spread-merge envelope write"
      pattern: "savePrefs\\(\\{ \\.\\.\\.current, ringCue: next \\}\\)"
    - from: "src/hooks/useOrbIdleChoice.ts:setOrbIdle"
      to: "src/storage/prefs.ts:savePrefs"
      via: "spread-merge envelope write"
      pattern: "savePrefs\\(\\{ \\.\\.\\.current, orbIdle: next \\}\\)"
    - from: "src/hooks/useSwitcherIconChoice.ts:setSwitcherIcon"
      to: "src/storage/prefs.ts:savePrefs"
      via: "spread-merge envelope write"
      pattern: "savePrefs\\(\\{ \\.\\.\\.current, switcherIcon: next \\}\\)"
---

<objective>
Add the four picker-side companion hooks that Phase 48 will bind to its segmented pickers and toggle rows. Each hook is a 20-line paste-and-rename of `useTimbreChoice.ts` per CONTEXT D-09 and PATTERNS.md §"`src/hooks/useBreathingShapeChoice.ts`": seed local state from `loadPrefs().<field>`, write through `savePrefs({ ...loadPrefs(), <field>: next })`, then dispatch `hrv:prefs-changed` with the field-specific `detail.key`.

Per the post-Plan-02 `UserPrefs` shape (8 fields), the spread-merge form `{ ...current, <field>: next }` correctly preserves all 7 sibling fields — including the 3 other Phase-47 flags — so a user toggling `breathingShape` does not stomp on their previously-set `ringCue`.

Purpose: closes the PREFS-03 setter-API half (setters that persist + dispatch). The hooks are NOT bound to any UI in this phase — they are data-layer plumbing that Phase 48 binds to its Appearance pickers. Phase 47 proves them via Vitest only (D-12 — no operator-facing dev surface).

Output: four new hook files + four new test files. No changes to existing files.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/47-persistable-feature-flag-preferences/47-CONTEXT.md
@.planning/phases/47-persistable-feature-flag-preferences/47-PATTERNS.md
@.planning/phases/47-persistable-feature-flag-preferences/47-02-PLAN.md
@src/hooks/useTimbreChoice.ts
@src/hooks/useTimbreChoice.test.ts
@src/hooks/useThemeChoice.ts

<interfaces>
<!-- Plan 02 has shipped; the following symbols are now available. -->

From src/storage/prefs.ts (post-Plan-02):
- `export function loadPrefs(deps?: StorageDeps): UserPrefs`
- `export function savePrefs(prefs: UserPrefs, deps?: StorageDeps): void`
- `export interface UserPrefs` — 8 fields including `breathingShape: BreathingShapeVariant`, `ringCue: RingCueStyle`, `orbIdle: OrbIdleBehavior`, `switcherIcon: boolean`.
- `export const DEFAULT_PREFS: UserPrefs` (8-field literal).

From src/featureFlags.ts (post-Plan-01):
- `export type BreathingShapeVariant = 'orb-halo' | 'minimal-rings' | 'spiritual-eye'`
- `export type RingCueStyle = 'outer-inner' | 'progress-arc'`
- `export type OrbIdleBehavior = 'still' | 'ambient'`

From src/hooks/useTimbreChoice.ts — the verbatim template:
- 20-line module.
- `import { useCallback, useState } from 'react'`
- `import { loadPrefs, savePrefs } from '../storage/prefs'`
- `import type { TimbreId } from '../domain/settings'`
- Function signature: `export function useTimbreChoice(): { timbre: TimbreId; setTimbre: (next: TimbreId) => void }`
- Body: `useState(() => loadPrefs().timbre)`, then a `useCallback` setter that does `const current = loadPrefs(); savePrefs({ ...current, timbre: next }); setTimbreState(next); window.dispatchEvent(new CustomEvent('hrv:prefs-changed', { detail: { key: 'timbre', value: next } }))`.

From src/hooks/useTimbreChoice.test.ts — the verbatim test template (6 it-blocks):
- `seedPrefs(prefs: UserPrefs)` helper writes `STATE_KEY` envelope.
- Tests: initial state, optimistic local-state update, savePrefs write-through, envelope-merge preservation, CustomEvent dispatch with correct detail shape, setter identity stability.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create the four choice-hook source files</name>
  <files>src/hooks/useBreathingShapeChoice.ts, src/hooks/useRingCueChoice.ts, src/hooks/useOrbIdleChoice.ts, src/hooks/useSwitcherIconChoice.ts</files>
  <read_first>
    - src/hooks/useTimbreChoice.ts (the whole file — 49 lines; this is the verbatim template)
    - src/hooks/useThemeChoice.ts (for cross-comparison — confirm the `useCallback` empty-deps + `setThemeState(next)` before `dispatchEvent` order)
    - src/featureFlags.ts (post-Plan-01 — confirm the three type aliases and their canonical values)
    - src/storage/prefs.ts (post-Plan-02 — confirm the 8-field UserPrefs and the loadPrefs / savePrefs signatures)
    - .planning/phases/47-persistable-feature-flag-preferences/47-PATTERNS.md §"`src/hooks/useBreathingShapeChoice.ts` (Choice hook — new)" — the full template
    - .planning/phases/47-persistable-feature-flag-preferences/47-CONTEXT.md §"Setter API + hook split" (D-09 + D-10)
  </read_first>
  <behavior>
    - **`useBreathingShapeChoice()`** returns `{ breathingShape: BreathingShapeVariant; setBreathingShape: (next: BreathingShapeVariant) => void }`.
    - **`useRingCueChoice()`** returns `{ ringCue: RingCueStyle; setRingCue: (next: RingCueStyle) => void }`.
    - **`useOrbIdleChoice()`** returns `{ orbIdle: OrbIdleBehavior; setOrbIdle: (next: OrbIdleBehavior) => void }`.
    - **`useSwitcherIconChoice()`** returns `{ switcherIcon: boolean; setSwitcherIcon: (next: boolean) => void }`.
    - Each hook seeds its local state from `loadPrefs().<field>` lazily via `useState(() => loadPrefs().<field>)`.
    - Each setter:
      1. Reads the current envelope (`const current = loadPrefs()`).
      2. Writes the spread-merged envelope (`savePrefs({ ...current, <field>: next })`).
      3. Calls `set<Field>State(next)` for optimistic UI.
      4. Dispatches `new CustomEvent('hrv:prefs-changed', { detail: { key: '<field>', value: next } })`.
    - The setter is wrapped in `useCallback(..., [])` so its identity is stable across re-renders (mirrors `useTimbreChoice.ts:34`).
    - The `<field>` and `detail.key` strings match exactly: `'breathingShape'`, `'ringCue'`, `'orbIdle'`, `'switcherIcon'`. No abbreviations.
    - Type imports for the three string-union hooks come from `'../featureFlags'` (not `'../domain/settings'` — these types live in `featureFlags.ts`, NOT `domain/settings.ts`). The boolean hook needs no type import.
  </behavior>
  <action>
    Create four new files. Each file is a 20-line paste-and-rename of `src/hooks/useTimbreChoice.ts:24-49` (skipping the file-header comment block — keep the new files concise with a one-line top-of-file comment naming the phase and citing the analog).

    **File 1 — `src/hooks/useBreathingShapeChoice.ts`:**
    - Top-of-file comment: `// src/hooks/useBreathingShapeChoice.ts — Phase 47 Plan 04 (D-09): paste-and-rename of useTimbreChoice for the breathingShape field. Dispatches hrv:prefs-changed with detail.key === 'breathingShape' per D-10.`
    - Imports: `import { useCallback, useState } from 'react'`, `import { loadPrefs, savePrefs } from '../storage/prefs'`, `import type { BreathingShapeVariant } from '../featureFlags'`.
    - Export the hook function with signature `export function useBreathingShapeChoice(): { breathingShape: BreathingShapeVariant; setBreathingShape: (next: BreathingShapeVariant) => void }`.
    - Body identical to `useTimbreChoice` with `breathingShape` substituted for `timbre` and `BreathingShapeVariant` substituted for `TimbreId`. The `detail.key` string is exactly `'breathingShape'`.

    **File 2 — `src/hooks/useRingCueChoice.ts`:**
    - Top-of-file comment cites Phase 47 Plan 04 (D-09) and `useTimbreChoice` as the analog.
    - Imports: `import { useCallback, useState } from 'react'`, `import { loadPrefs, savePrefs } from '../storage/prefs'`, `import type { RingCueStyle } from '../featureFlags'`.
    - Export signature: `export function useRingCueChoice(): { ringCue: RingCueStyle; setRingCue: (next: RingCueStyle) => void }`.
    - `detail.key` is exactly `'ringCue'`.

    **File 3 — `src/hooks/useOrbIdleChoice.ts`:**
    - Top-of-file comment cites Phase 47 Plan 04 (D-09).
    - Imports: `import { useCallback, useState } from 'react'`, `import { loadPrefs, savePrefs } from '../storage/prefs'`, `import type { OrbIdleBehavior } from '../featureFlags'`.
    - Export signature: `export function useOrbIdleChoice(): { orbIdle: OrbIdleBehavior; setOrbIdle: (next: OrbIdleBehavior) => void }`.
    - `detail.key` is exactly `'orbIdle'`.

    **File 4 — `src/hooks/useSwitcherIconChoice.ts`:**
    - Top-of-file comment cites Phase 47 Plan 04 (D-09) and notes the boolean type swap.
    - Imports: `import { useCallback, useState } from 'react'`, `import { loadPrefs, savePrefs } from '../storage/prefs'`. NO type import — `boolean` is a primitive.
    - Export signature: `export function useSwitcherIconChoice(): { switcherIcon: boolean; setSwitcherIcon: (next: boolean) => void }`.
    - `useState&lt;boolean&gt;(() => loadPrefs().switcherIcon)` — explicit `&lt;boolean&gt;` generic to prevent TS inferring from a literal `true`/`false` default.
    - `detail.key` is exactly `'switcherIcon'`.

    For all four files: the `useCallback` deps array is exactly `[]` (empty). The local-state setter (`setBreathingShapeState` / `setRingCueState` / `setOrbIdleState` / `setSwitcherIconState`) is called BEFORE `window.dispatchEvent` — matches `useTimbreChoice.ts:40-44` order verbatim. No `useEffect`, no `useMemo`, no other hooks.

    Do NOT export any helper or shared type. Do NOT create a `FeatureFlagChoice&lt;T&gt;` abstraction (per CONTEXT D-09 Claude's Discretion — the existing four choice hooks are independent today; we mirror that).
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `ls src/hooks/useBreathingShapeChoice.ts src/hooks/useRingCueChoice.ts src/hooks/useOrbIdleChoice.ts src/hooks/useSwitcherIconChoice.ts` returns four lines (all four files exist).
    - `grep -nE "^export function useBreathingShapeChoice\\(\\): \\{ breathingShape: BreathingShapeVariant; setBreathingShape: \\(next: BreathingShapeVariant\\) => void \\}" src/hooks/useBreathingShapeChoice.ts` returns one line.
    - `grep -nE "^export function useRingCueChoice\\(\\): \\{ ringCue: RingCueStyle; setRingCue: \\(next: RingCueStyle\\) => void \\}" src/hooks/useRingCueChoice.ts` returns one line.
    - `grep -nE "^export function useOrbIdleChoice\\(\\): \\{ orbIdle: OrbIdleBehavior; setOrbIdle: \\(next: OrbIdleBehavior\\) => void \\}" src/hooks/useOrbIdleChoice.ts` returns one line.
    - `grep -nE "^export function useSwitcherIconChoice\\(\\): \\{ switcherIcon: boolean; setSwitcherIcon: \\(next: boolean\\) => void \\}" src/hooks/useSwitcherIconChoice.ts` returns one line.
    - `grep -n "import type { BreathingShapeVariant } from '../featureFlags'" src/hooks/useBreathingShapeChoice.ts` returns one line.
    - `grep -n "import type { RingCueStyle } from '../featureFlags'" src/hooks/useRingCueChoice.ts` returns one line.
    - `grep -n "import type { OrbIdleBehavior } from '../featureFlags'" src/hooks/useOrbIdleChoice.ts` returns one line.
    - `grep -c "import type" src/hooks/useSwitcherIconChoice.ts` returns 0 (no type import needed for boolean).
    - `grep -nE "savePrefs\\(\\{ \\.\\.\\.current, breathingShape: next \\}\\)" src/hooks/useBreathingShapeChoice.ts` returns one line.
    - `grep -nE "savePrefs\\(\\{ \\.\\.\\.current, ringCue: next \\}\\)" src/hooks/useRingCueChoice.ts` returns one line.
    - `grep -nE "savePrefs\\(\\{ \\.\\.\\.current, orbIdle: next \\}\\)" src/hooks/useOrbIdleChoice.ts` returns one line.
    - `grep -nE "savePrefs\\(\\{ \\.\\.\\.current, switcherIcon: next \\}\\)" src/hooks/useSwitcherIconChoice.ts` returns one line.
    - `grep -nE "key: 'breathingShape'" src/hooks/useBreathingShapeChoice.ts` returns one line (the dispatch).
    - `grep -nE "key: 'ringCue'" src/hooks/useRingCueChoice.ts` returns one line.
    - `grep -nE "key: 'orbIdle'" src/hooks/useOrbIdleChoice.ts` returns one line.
    - `grep -nE "key: 'switcherIcon'" src/hooks/useSwitcherIconChoice.ts` returns one line.
    - `grep -cE "useCallback\\(" src/hooks/useBreathingShapeChoice.ts src/hooks/useRingCueChoice.ts src/hooks/useOrbIdleChoice.ts src/hooks/useSwitcherIconChoice.ts | awk -F: '{ s+=$2 } END { print s }'` returns 4 (one per file).
    - `grep -cE "useEffect" src/hooks/useBreathingShapeChoice.ts src/hooks/useRingCueChoice.ts src/hooks/useOrbIdleChoice.ts src/hooks/useSwitcherIconChoice.ts | awk -F: '{ s+=$2 } END { print s }'` returns 0 (choice hooks are write-only — no effects).
    - `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>Four new choice hook files exist, each a paste-and-rename of `useTimbreChoice` with the correct field name, type, and `detail.key`. Type-clean against the post-Plan-02 8-field UserPrefs.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create the four matching test files</name>
  <files>src/hooks/useBreathingShapeChoice.test.ts, src/hooks/useRingCueChoice.test.ts, src/hooks/useOrbIdleChoice.test.ts, src/hooks/useSwitcherIconChoice.test.ts</files>
  <read_first>
    - src/hooks/useTimbreChoice.test.ts (the whole file — 112 lines; verbatim template)
    - src/hooks/useBreathingShapeChoice.ts / useRingCueChoice.ts / useOrbIdleChoice.ts / useSwitcherIconChoice.ts (just-created in Task 1 — confirm the exact hook return shape and `detail.key` strings)
    - src/storage/prefs.ts (post-Plan-02 — confirm `DEFAULT_PREFS` is 8 fields; tests use `{ ...DEFAULT_PREFS, &lt;overrides&gt; }` for seedPrefs)
    - .planning/phases/47-persistable-feature-flag-preferences/47-PATTERNS.md §"`src/hooks/useBreathingShapeChoice.test.ts`, `useRingCueChoice.test.ts`, `useOrbIdleChoice.test.ts`, `useSwitcherIconChoice.test.ts` (Tests — new)" — the full 6-it-block template
  </read_first>
  <behavior>
    Each test file has exactly six `it()` cases inside one `describe('use&lt;Field&gt;Choice')` block:
    - **Initial state**: with the field pre-seeded to a non-default value, `result.current.&lt;field&gt;` equals the seeded value at mount.
    - **Optimistic local-state update**: calling `result.current.set&lt;Field&gt;(newValue)` inside `act()` updates `result.current.&lt;field&gt;` synchronously to `newValue`.
    - **savePrefs write-through**: after the setter call, reading `localStorage[STATE_KEY]` directly shows the new value at `raw.prefs.&lt;field&gt;`.
    - **Envelope-merge preservation**: pre-seed an envelope with non-default values across all 8 fields (incl. theme/timbre/cue/locale + the other 3 Phase-47 flags); call the setter; assert ALL 7 sibling fields are preserved verbatim in the post-setter disk state. This is the critical assertion that proves the spread-merge form works against the 8-field UserPrefs.
    - **CustomEvent dispatch**: a spy attached to `'hrv:prefs-changed'` receives exactly one `CustomEvent` with `event.detail.key === '&lt;field&gt;'` and `event.detail.value === newValue`.
    - **Setter identity stability**: across a `rerender()`, `result.current.set&lt;Field&gt;` references the same function instance (proves `useCallback([])` contract).
  </behavior>
  <action>
    Create four new test files. Each is a paste-and-rename of `useTimbreChoice.test.ts:1-111`. The boilerplate (imports, beforeEach/afterEach, seedPrefs helper) is identical; the body substitutes the field name, the hook name, the canonical values, and the `detail.key` string.

    **Shared helpers (replicated in each file):**
    - `import { afterEach, beforeEach, describe, expect, it, vi } from 'vitest'`
    - `import { renderHook, act } from '@testing-library/react'`
    - `import { use&lt;Field&gt;Choice } from './use&lt;Field&gt;Choice'`
    - `import { STATE_KEY } from '../storage'`
    - `import { DEFAULT_PREFS, type UserPrefs } from '../storage/prefs'`
    - `function seedPrefs(prefs: UserPrefs): void { window.localStorage.setItem(STATE_KEY, JSON.stringify({ version: 1, prefs })) }`
    - `beforeEach(() => { window.localStorage.clear() })`
    - `afterEach(() => { window.localStorage.clear(); vi.restoreAllMocks() })`

    **File 1 — `src/hooks/useBreathingShapeChoice.test.ts`:**

    Six `it()` cases inside `describe('useBreathingShapeChoice', () => { ... })`:

    a. `it('initial state matches loadPrefs().breathingShape when localStorage is pre-seeded', () => { seedPrefs({ ...DEFAULT_PREFS, breathingShape: 'spiritual-eye' }); const { result } = renderHook(() => useBreathingShapeChoice()); expect(result.current.breathingShape).toBe('spiritual-eye') })`.

    b. `it('setBreathingShape updates local state optimistically', () => { seedPrefs({ ...DEFAULT_PREFS, breathingShape: 'orb-halo' }); const { result } = renderHook(() => useBreathingShapeChoice()); act(() => { result.current.setBreathingShape('spiritual-eye') }); expect(result.current.breathingShape).toBe('spiritual-eye') })`.

    c. `it('setBreathingShape writes the new value to disk via savePrefs', () => { seedPrefs({ ...DEFAULT_PREFS, breathingShape: 'orb-halo' }); const { result } = renderHook(() => useBreathingShapeChoice()); act(() => { result.current.setBreathingShape('minimal-rings') }); const raw = JSON.parse(window.localStorage.getItem(STATE_KEY)!) as { version: number; prefs: UserPrefs }; expect(raw.prefs.breathingShape).toBe('minimal-rings') })` (use the eslint-disable comment for the non-null assertion exactly as `useTimbreChoice.test.ts:57-58`).

    d. `it('setBreathingShape preserves all other 7 prefs fields — envelope merge contract (8-field UserPrefs)', () => { ... })` — seed `{ theme: 'dark', timbre: 'bell', cue: 'arrow', locale: 'pt-BR', breathingShape: 'orb-halo', ringCue: 'outer-inner', orbIdle: 'still', switcherIcon: true }`; call `setBreathingShape('spiritual-eye')`; read disk; assert `raw.prefs.breathingShape === 'spiritual-eye'` AND `raw.prefs.theme === 'dark'` AND `raw.prefs.timbre === 'bell'` AND `raw.prefs.cue === 'arrow'` AND `raw.prefs.locale === 'pt-BR'` AND `raw.prefs.ringCue === 'outer-inner'` AND `raw.prefs.orbIdle === 'still'` AND `raw.prefs.switcherIcon === true`. This is the 7-other-fields preservation assertion that proves the spread-merge contract against the 8-field UserPrefs.

    e. `it('setBreathingShape dispatches hrv:prefs-changed CustomEvent with correct detail shape', () => { ... })` — attach a `vi.fn()` spy to `'hrv:prefs-changed'`, call the setter, detach spy, assert `spy.toHaveBeenCalledTimes(1)`, assert `event instanceof CustomEvent`, assert `event.detail.key === 'breathingShape'`, assert `event.detail.value === 'spiritual-eye'`.

    f. `it('setBreathingShape identity is stable across re-renders (useCallback empty deps contract)', () => { ... })` — render, capture initial setter reference, call `rerender()`, assert `result.current.setBreathingShape === initialSetter`.

    **File 2 — `src/hooks/useRingCueChoice.test.ts`:** same 6 cases for `ringCue`. Canonical values: seed `'progress-arc'`, set to `'outer-inner'`. `detail.key` is `'ringCue'`. The 7-other-fields preservation test uses seed `{ ...non-defaults, ringCue: 'progress-arc' }` then sets to `'outer-inner'`.

    **File 3 — `src/hooks/useOrbIdleChoice.test.ts`:** same 6 cases for `orbIdle`. Canonical values: seed `'ambient'`, set to `'still'`. `detail.key` is `'orbIdle'`. The 7-other-fields preservation test uses seed `{ ...non-defaults, orbIdle: 'ambient' }` then sets to `'still'`.

    **File 4 — `src/hooks/useSwitcherIconChoice.test.ts`:** same 6 cases for `switcherIcon`. Canonical values: seed `false`, set to `true`. `detail.key` is `'switcherIcon'`. The 7-other-fields preservation test uses seed `{ ...non-defaults, switcherIcon: false }` then sets to `true`. The CustomEvent assertion checks `event.detail.value === true` (a boolean, not a string).

    Use the verbatim eslint-disable comments from `useTimbreChoice.test.ts` (`// eslint-disable-next-line @typescript-eslint/no-non-null-assertion` before the JSON.parse non-null assertion lines). Mirror the `as CustomEvent&lt;{ key: string; value: string }&gt;` cast pattern at line 96 — for `useSwitcherIconChoice.test.ts` use `as CustomEvent&lt;{ key: string; value: boolean }&gt;`.

    Do NOT create a shared test-helper module. Each test file is self-contained. The boilerplate duplication is intentional — matches the existing precedent across `useTimbreChoice.test.ts`, `useThemeChoice.test.ts`, etc.
  </action>
  <verify>
    <automated>npx vitest run src/hooks/useBreathingShapeChoice.test.ts src/hooks/useRingCueChoice.test.ts src/hooks/useOrbIdleChoice.test.ts src/hooks/useSwitcherIconChoice.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `ls src/hooks/useBreathingShapeChoice.test.ts src/hooks/useRingCueChoice.test.ts src/hooks/useOrbIdleChoice.test.ts src/hooks/useSwitcherIconChoice.test.ts` returns four lines.
    - In each of the four test files: `grep -cE "^  it\\('" &lt;file&gt;` returns at least 6 (six `it()` cases minimum).
    - In `useBreathingShapeChoice.test.ts`: `grep -nE "detail\\.key\\)\\.toBe\\('breathingShape'\\)" src/hooks/useBreathingShapeChoice.test.ts` returns at least one line.
    - In `useRingCueChoice.test.ts`: `grep -nE "detail\\.key\\)\\.toBe\\('ringCue'\\)" src/hooks/useRingCueChoice.test.ts` returns at least one line.
    - In `useOrbIdleChoice.test.ts`: `grep -nE "detail\\.key\\)\\.toBe\\('orbIdle'\\)" src/hooks/useOrbIdleChoice.test.ts` returns at least one line.
    - In `useSwitcherIconChoice.test.ts`: `grep -nE "detail\\.key\\)\\.toBe\\('switcherIcon'\\)" src/hooks/useSwitcherIconChoice.test.ts` returns at least one line and `grep -nE "detail\\.value\\)\\.toBe\\(true\\)" src/hooks/useSwitcherIconChoice.test.ts` returns at least one line.
    - In each test file: `grep -nE "envelope merge contract" &lt;file&gt;` returns at least one line (the 7-other-fields preservation test is identifiable by its title).
    - In each test file: `grep -nE "raw\\.prefs\\.(theme|timbre|cue|locale)\\)\\.toBe" &lt;file&gt;` returns at least 4 lines (all 4 non-Phase-47 fields are asserted preserved).
    - In each test file: `grep -nE "raw\\.prefs\\.(breathingShape|ringCue|orbIdle|switcherIcon)\\)\\.toBe" &lt;file&gt;` returns at least 4 lines (3 sibling Phase-47 fields are asserted preserved, plus the field-under-test asserted to be the new value).
    - In each test file: `grep -nE "identity is stable across re-renders" &lt;file&gt;` returns one line.
    - `npx vitest run src/hooks/useBreathingShapeChoice.test.ts src/hooks/useRingCueChoice.test.ts src/hooks/useOrbIdleChoice.test.ts src/hooks/useSwitcherIconChoice.test.ts` exits 0.
    - `npx tsc --noEmit` exits 0.
    - `npx vitest run` (full suite) exits 0 — no regression in `useTimbreChoice.test.ts`, `useFeatureFlags.test.ts`, or any other suite.
  </acceptance_criteria>
  <done>Four new test files exist, each with six `it()` cases covering initial state, optimistic update, write-through, 8-field envelope-merge preservation, CustomEvent dispatch, and setter identity. All pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Picker UI (Phase 48) → `set<Field>` setter argument | Phase 48 will type the picker's `onChange` against the union (`BreathingShapeVariant` etc.) so the value is already narrowed before reaching the setter. Phase 47 ships the hook only; the boundary is the hook signature itself. |
| `set<Field>` → `savePrefs` → `writeEnvelope` localStorage write | Untrusted persisted blob is never CREATED by these hooks — they write a typed `UserPrefs` literal whose 8 fields are all union members or booleans by type. The downstream `readEnvelope` → `coercePrefs` boundary handles the next read. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-47-04-01 | Tampering | `set<Field>` argument type | mitigate | The TypeScript signatures `setBreathingShape: (next: BreathingShapeVariant) => void`, `setRingCue: (next: RingCueStyle) => void`, `setOrbIdle: (next: OrbIdleBehavior) => void`, `setSwitcherIcon: (next: boolean) => void` block any non-union value at the call site. Proof: `npx tsc --noEmit` would reject a call like `setBreathingShape('junk')`. No runtime validation needed — the type system is the trust boundary. |
| T-47-04-02 | Tampering | `savePrefs({ ...current, ... })` spread of fresh `loadPrefs()` | accept | The spread is over `loadPrefs()`, which returns a typed `UserPrefs` produced by `coercePrefs` — the prototype-pollution mitigation in `coercePrefs` (Plan 02, byte-identical to the v1.1 pattern) guarantees the spread source has no unknown keys. Mitigated transitively. |
| T-47-04-03 | Spoofing | A malicious script in the page dispatches `hrv:prefs-changed` with a forged `detail.value` | accept | The `useFeatureFlags` listener (Plan 03) discards `detail.value` and re-reads `loadPrefs()` from disk. The choice hooks' `setBreathingShapeState(next)` writes only to the hook's LOCAL state — not to disk via the event. The disk is updated only by the actual `savePrefs` call inside the setter. A forged event cannot cause a disk write. |
| T-47-04-04 | Denial of Service | A consumer mounts the hook 100 times | accept | Each hook instance owns one `useState` and one `useCallback`. No `useEffect` — no listeners attached, no leak. Bounded resource usage proportional to consumer count. In Phase 48, each hook is bound to a single picker / toggle row instance. |

**Preserved mitigations (named explicitly):**
- **Prototype-pollution mitigation T-14-01** — transitively via `loadPrefs()` → `coercePrefs` (Plan 02 byte-identical).
- **Non-throwing per-field coerce-and-fallback (Phase 14 D-10 / D-17)** — transitively; any future read of the persisted blob (by `useFeatureFlags` Plan 03, or by `useTimbreChoice`, etc.) goes through coercer fallback.
- **Envelope-merge contract (Phase 14 D-17 / `useTimbreChoice` precedent)** — preserved verbatim; spread-merge form preserves all sibling fields.
- **Per-flag `detail.key` contract (Phase 16 D-22 / D-10)** — each setter dispatches the field-specific key; Phase 47's `useFeatureFlags` listener (Plan 03) filters on the 4-key set, and existing `useTheme` / `useTimbre` / `useCue` / `useLocale` listeners ignore the 4 new keys (their filters check `=== 'theme'` etc.).
- **No `STATE_VERSION` bump** — these hooks write through `savePrefs`, which writes a typed `UserPrefs` into the existing `Envelope.prefs` subtree at the existing `STATE_VERSION`. No version mutation.
</threat_model>

<verification>
- `npx vitest run src/hooks/useBreathingShapeChoice.test.ts src/hooks/useRingCueChoice.test.ts src/hooks/useOrbIdleChoice.test.ts src/hooks/useSwitcherIconChoice.test.ts` exits 0.
- `npx vitest run` (full suite) exits 0 — no regression in `useTimbreChoice.test.ts` / `useTheme.test.ts` / `useFeatureFlags.test.ts` / `prefs.test.ts`.
- `npx tsc --noEmit` exits 0.
- `grep -cE "^export function use(BreathingShape|RingCue|OrbIdle|SwitcherIcon)Choice" src/hooks/useBreathingShapeChoice.ts src/hooks/useRingCueChoice.ts src/hooks/useOrbIdleChoice.ts src/hooks/useSwitcherIconChoice.ts | awk -F: '{ s+=$2 } END { print s }'` returns 4 (one per file).
- `grep -cE "useEffect" src/hooks/useBreathingShapeChoice.ts src/hooks/useRingCueChoice.ts src/hooks/useOrbIdleChoice.ts src/hooks/useSwitcherIconChoice.ts | awk -F: '{ s+=$2 } END { print s }'` returns 0 (write-only hooks; no listeners).
</verification>

<success_criteria>
- Four new picker-side choice hooks exist, each matching the `useTimbreChoice` shape with the correct field-specific name, type, and `detail.key`.
- Each setter preserves all 7 sibling prefs fields via the spread-merge contract (proven against the 8-field UserPrefs from Plan 02).
- Each setter dispatches `hrv:prefs-changed` with the field-specific `detail.key` per D-10, and Plan 03's `useFeatureFlags` listener re-reads `loadPrefs()` in response.
- Each setter identity is stable across re-renders (useCallback empty deps).
- 24 new test cases (6 per hook × 4 hooks) all pass.
- No new abstraction (no `FeatureFlagChoice<T>` helper); each hook is independent, mirroring the existing precedent.
- No UI surface introduced — the four hooks are data-layer plumbing for Phase 48 to bind to.
</success_criteria>

<output>
Create `.planning/phases/47-persistable-feature-flag-preferences/47-04-SUMMARY.md` when done — list the four new hook files, the four new test files (with their 6-case structure), the spread-merge contract evidence (the 7-other-fields preservation test per hook), and note that no UI surface was introduced (Phase 48 will bind these to pickers).
</output>
