---
phase: 19-language-switching
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/content/learnContent.ts
  - src/content/learnContent.test.ts
  - src/content/lockedCopy.ts
  - src/content/lockedCopy.test.ts
  - src/components/LearnDialog.tsx
  - src/components/LearnDialog.test.tsx
autonomous: true
requirements:
  - I18N-05
  - I18N-06
  - I18N-07
must_haves:
  truths:
    - "LEARN_CONTENT shape is Readonly<Record<LocaleId, LearnContent>> with en + pt-BR entries"
    - "LEARN_CONTENT.en.explainer.forrest.body does NOT contain the substring inspired by Forrest's teachings"
    - "LEARN_CONTENT['pt-BR'].explainer.forrest.body does NOT contain its lockedCopy.inspiredByForrest substring"
    - "LOCKED_COPY exposes 3 claim-safe entries (inspiredByForrest, medicalAdviceLine, affiliationLine) per locale"
    - "Frozen-EN snapshot test asserts byte-equality of all 3 EN values via .toBe() (D-02)"
    - "PT-BR link URLs are byte-identical to EN; only labels are translated"
    - "LearnDialog.tsx switches to LEARN_CONTENT.en + LOCKED_COPY.en direct access (stop-gap; Plan 08 swaps to prop-driven learnContent / lockedCopy from useLocale)"
    - "LearnDialog.tsx renders LOCKED_COPY.en.inspiredByForrest as a separate italicized closing paragraph after the forrest body"
    - "LearnDialog.tsx:171 affiliation literal is replaced with {LOCKED_COPY.en.affiliationLine}"
    - "LearnDialog.test.tsx:43-46 locked-phrase presence test stays green; line 48 medical-advice modal-absence guard stays green; line 57-62 affiliation byte-equality test stays green"
  artifacts:
    - path: "src/content/learnContent.ts"
      provides: "locale-keyed LEARN_CONTENT with EN substring stripped"
      contains: "Readonly<Record<LocaleId, LearnContent>>"
    - path: "src/content/lockedCopy.ts"
      provides: "LOCKED_COPY 3 entries × 2 locales"
      contains: "interface LockedCopy"
    - path: "src/content/lockedCopy.test.ts"
      provides: "frozen-EN byte-equality + PT-BR non-empty + substring-absence guard"
      contains: "LOCKED_COPY.en.inspiredByForrest"
    - path: "src/components/LearnDialog.tsx"
      provides: "stop-gap composition: LEARN_CONTENT.en / LOCKED_COPY.en direct access; separate locked-phrase paragraph; affiliation line via LOCKED_COPY.en.affiliationLine"
      contains: "LOCKED_COPY.en.inspiredByForrest"
    - path: "src/components/LearnDialog.test.tsx"
      provides: "test fixture migration to LEARN_CONTENT.en (preserves locked-phrase + affiliation + medical-advice-absence guards)"
      contains: "LEARN_CONTENT.en.links.keyVideos"
  key_links:
    - from: "src/content/lockedCopy.test.ts"
      to: "src/content/learnContent.ts"
      via: "substring-absence guard reads LEARN_CONTENT[locale].explainer.forrest.body"
      pattern: "LEARN_CONTENT\\[locale\\].explainer.forrest.body"
    - from: "src/content/lockedCopy.ts"
      to: "src/domain/settings.ts"
      via: "type LocaleId import"
      pattern: "import type \\{ LocaleId \\}"
    - from: "src/components/LearnDialog.tsx"
      to: "src/content/lockedCopy.ts"
      via: "LOCKED_COPY.en.inspiredByForrest + LOCKED_COPY.en.affiliationLine direct access (stop-gap)"
      pattern: "LOCKED_COPY.en.(inspiredByForrest|affiliationLine)"
    - from: "src/components/LearnDialog.tsx"
      to: "src/content/learnContent.ts"
      via: "LEARN_CONTENT.en direct access (stop-gap)"
      pattern: "LEARN_CONTENT.en"
---

<objective>
Convert `src/content/learnContent.ts` from EN-only singleton to `Readonly<Record<LocaleId, LearnContent>>` with EN content stripped of the locked Forrest phrase, plus a complete PT-BR translation. Create `src/content/lockedCopy.ts` housing the 3 D-12 claim-safe entries per locale. Create `src/content/lockedCopy.test.ts` enforcing the **frozen-EN byte-equality snapshot guard** (D-02) using `.toBe(...)` plus the **substring-absence guard** that proves `learnContent[locale].explainer.forrest.body` does NOT contain `lockedCopy[locale].inspiredByForrest`. Mutate `src/content/learnContent.test.ts` to (a) loop per locale, (b) INVERT the existing presence-of-substring guard at lines 27–31 into the substring-absence guard inside `lockedCopy.test.ts` (so the old test does not survive after Phase 19), and (c) assert PT-BR link URL byte-identity with EN.

Purpose: This is the I18N-06 guardrail — physical separation of locked claim-safe copy from translatable catalog, plus byte-equality enforcement of EN baseline values so future locale contributions cannot silently weaken D-12 positioning.

Output: 6 files (2 NEW, 4 EDIT — the 4 content files PLUS `src/components/LearnDialog.tsx` + `src/components/LearnDialog.test.tsx` stop-gap composition so the EN locked-phrase substring strip in Task 1 does not break the per-commit green-gate). All tests exit 0 (D-16).

**Why the LearnDialog stop-gap is in this plan (and not Plan 08):** Plan 03 mutates `LEARN_CONTENT` shape (`LearnContent → Readonly<Record<LocaleId, LearnContent>>`) AND strips the locked Forrest phrase from `forrest.body`. LearnDialog.tsx imports `LEARN_CONTENT` directly and destructures `.explainer` / `.links` at line 67; after Plan 03 those direct accesses fail TS compile and the `'inspired by Forrest\'s teachings'` substring presence test at LearnDialog.test.tsx:43–46 fails at runtime. To keep D-16 per-commit green-gate passing, Plan 03 ships the minimal LearnDialog stop-gap: switch to `LEARN_CONTENT.en` / `LOCKED_COPY.en` direct accesses (the EN fixture) + compose the locked phrase as a separate paragraph + migrate the affiliation literal to `LOCKED_COPY.en.affiliationLine`. Plan 08 then removes the `.en` stop-gap and replaces with prop-driven `learnContent` / `lockedCopy` resolved via `useLocale()`.
</objective>

<execution_context>
@$HOME/.claude-personal-personal/get-shit-done/workflows/execute-plan.md
@$HOME/.claude-personal-personal/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/19-language-switching/19-CONTEXT.md
@.planning/phases/19-language-switching/19-PATTERNS.md
@.planning/phases/19-language-switching/19-RESEARCH.md

<interfaces>
EN locked-copy baseline (byte-exact, per CONTEXT.md). All 3 values use ASCII apostrophe U+0027 except medical-advice which uses em-dash U+2014:

- inspiredByForrest  = inspired by Forrest's teachings
- medicalAdviceLine  = Guided breathing practice — not medical advice. (em-dash U+2014)
- affiliationLine    = Independent project. Not affiliated with Forrest Knutson.

From src/content/learnContent.ts (current shape — lines 8–32): ExplainerSection, LearnLink, LearnContent interfaces with `explainer.{hrv,timing,forrest}` and `links.{youtubeChannel,website,book,patreon,heroVideo,keyVideos}`.

Current EN `forrest.body` paragraph 2 reads: "This is an independent web app inspired by Forrest's teachings, made so anyone can follow a calm paced breath from a browser. The links below point to his channel, his site, and hand-picked starting videos."

After strip: replace the substring "inspired by Forrest's teachings, " (including the leading space before "inspired" and the trailing comma+space after "teachings") so the sentence becomes "This is an independent web app made so anyone can follow a calm paced breath from a browser. The links below point to his channel, his site, and hand-picked starting videos." The 2-paragraph structure stays intact.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Convert learnContent.ts to Record<LocaleId, LearnContent> with EN strip + PT-BR translation</name>
  <files>src/content/learnContent.ts</files>
  <read_first>
    - src/content/learnContent.ts (current EN-only file — entire 85 lines)
    - .planning/phases/19-language-switching/19-CONTEXT.md (D-04 strip semantics + I18N-07 markers)
    - .planning/phases/19-language-switching/19-PATTERNS.md (lines 588–639 — learnContent.ts mutation pattern)
  </read_first>
  <behavior>
    - `LEARN_CONTENT` export is typed `Readonly<Record<LocaleId, LearnContent>>` (not `LearnContent`).
    - `LEARN_CONTENT.en.explainer.forrest.body` is the original EN body MINUS the locked phrase plus its leading space and trailing comma.
    - `LEARN_CONTENT.en.explainer.hrv.body`, `.timing.body`, and the three section titles are byte-identical to the current values.
    - `LEARN_CONTENT.en.links` is byte-identical to the current `links` object — all URLs and labels preserved.
    - `LEARN_CONTENT['pt-BR'].explainer.{hrv,timing,forrest}.{title,body}` are populated with machine-translated PT-BR text, each accompanied by a `// TODO: native-speaker review` marker.
    - `LEARN_CONTENT['pt-BR'].links.{youtubeChannel,website,book,patreon,heroVideo}.url` are BYTE-IDENTICAL to the EN counterparts (D-12 URL invariant).
    - `LEARN_CONTENT['pt-BR'].links.*.label` are PT-BR translations with `// TODO: native-speaker review` markers.
    - `LEARN_CONTENT['pt-BR'].links.keyVideos` has the same length as EN (3 entries); each entry has byte-identical `url` and translated `label`.
    - `LEARN_CONTENT['pt-BR'].explainer.forrest.body` does NOT contain the substring `"inspirado nos ensinamentos do Forrest"` (the planned PT-BR `LOCKED_COPY` value — same strip pattern as EN).
  </behavior>
  <action>
    Edit `src/content/learnContent.ts` in place. Steps:

    1. Update the file header comment block (lines 1–6) to reflect locale-keyed shape per the PATTERNS.md pattern at lines 631–638. Cite Phase 19 I18N-01..07 and the substring strip rationale (phrase moved to lockedCopy.ts; composed at render time by LearnDialog.tsx per D-04).
    2. Add `import type { LocaleId } from '../domain/settings'` (no other imports needed).
    3. Keep `interface ExplainerSection`, `interface LearnLink`, `interface LearnContent` unchanged (lines 8–32).
    4. Replace the single `LEARN_CONTENT: LearnContent` export at line 34 with `LEARN_CONTENT: Readonly<Record<LocaleId, LearnContent>>`.
    5. Move the existing EN object (lines 35–84) under the `en:` key. Within `en.explainer.forrest.body`, locate the substring `"This is an independent web app inspired by Forrest's teachings, made so anyone can follow a calm paced breath from a browser."` and replace it with `"This is an independent web app made so anyone can follow a calm paced breath from a browser."` — the phrase + leading-space + trailing-comma are removed; everything else in that paragraph stays byte-identical. Other EN fields are NOT modified.
    6. Add a `'pt-BR':` entry next to `en:`. Populate every leaf with PT-BR machine translation. Each translated string gets a `// TODO: native-speaker review` marker on the line above OR end-of-line.

       PT-BR explainer candidates (executor may refine; substring `"inspirado nos ensinamentos do Forrest"` MUST NOT appear inside `forrest.body` — lockedCopy.ts owns that phrase):
       - hrv.title: `O que é HRV / respiração de ressonância`
       - hrv.body: PT-BR translation of "HRV breathing is a calm practice..." — keep clinical-verb-free per learnContent.test.ts:34 forbidden-words guard. Do not use any of: melhora/trata/cura/diagnostica/avalia.
       - timing.title: `Como este app cronometra sua respiração`
       - timing.body: PT-BR translation of the EN timing body — clinical-verb-free.
       - forrest.title: `Quem é Forrest Knutson`
       - forrest.body: two paragraphs separated by `\n\n`. Paragraph 1 = PT-BR translation of the biographic paragraph. Paragraph 2 = PT-BR equivalent of "This is an independent web app made so anyone can follow a calm paced breath from a browser. The links below point to his channel, his site, and hand-picked starting videos." **MUST NOT contain `inspirado nos ensinamentos do Forrest`** (the chosen PT-BR `LOCKED_COPY.inspiredByForrest` value selected in Task 2).

       PT-BR links candidates:
       - youtubeChannel.label: `Canal do YouTube`
       - youtubeChannel.url: BYTE-IDENTICAL to `en.links.youtubeChannel.url`.
       - website.label: `Site/Treinamentos`
       - book.label: `Livro "Mastering Meditation"` (book title stays in English — proper noun; only the word `book` translates to `Livro`).
       - patreon.label: `Patreon` (proper noun — kept verbatim, mark with `// TODO: native-speaker review` for confirmation).
       - heroVideo.label: PT-BR translation of "The Holy Trinity of Breath Induces HRV Resonance".
       - keyVideos: 3 entries with translated labels; URLs byte-identical to EN. The 3 EN labels are `The Meditation Magic of Sitting Very Still - SVS`, `4 Proofs of Meditation`, `Beginners Deep Meditation - Naturally - Clinical Mindfulness Technique`.
    7. Close with `} as const satisfies Readonly<Record<LocaleId, LearnContent>>` (D-10 exhaustiveness narrowing).

    Do NOT edit any test file in this task (Task 3 handles learnContent.test.ts). Do NOT touch `src/domain/settings.ts` (D-18). Do NOT touch `src/storage/prefs.ts` (D-18).
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20 && grep -c "inspired by Forrest" src/content/learnContent.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/content/learnContent.ts` exports `LEARN_CONTENT` with type `Readonly<Record<LocaleId, LearnContent>>`.
    - `npx tsc --noEmit` exits 0.
    - `grep -c "inspired by Forrest" src/content/learnContent.ts` returns 0 — the EN phrase has been stripped from the source.
    - `grep -c "'pt-BR':" src/content/learnContent.ts` returns 1 (second locale key present).
    - `grep -c "TODO: native-speaker review" src/content/learnContent.ts` returns >= 9 (PT-BR translatable entries flagged: 3 titles + 3 bodies + at least 3 labels minimum).
    - `grep -c "https://www.youtube.com/@ForrestKnutson" src/content/learnContent.ts` returns 2 (URL byte-identical between EN and PT-BR).
    - `grep -c "as const satisfies Readonly<Record<LocaleId, LearnContent>>" src/content/learnContent.ts` returns 1.
  </acceptance_criteria>
  <done>
    `learnContent.ts` is locale-keyed; the EN locked phrase is stripped from `en.forrest.body`; PT-BR translation is present with TODO markers; URLs are byte-identical across locales.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create src/content/lockedCopy.ts with LOCKED_COPY 3 entries × 2 locales</name>
  <files>src/content/lockedCopy.ts</files>
  <read_first>
    - .planning/phases/19-language-switching/19-CONTEXT.md (D-01..D-04 + "EN locked-copy baseline" table; lines 91–99 byte-exact EN values)
    - .planning/phases/19-language-switching/19-PATTERNS.md (lines 143–176 — lockedCopy.ts module shape)
    - src/content/learnContent.ts (the file just edited in Task 1 — referenced for the substring-absence design but NOT imported here)
    - src/domain/settings.ts (line 83 — LocaleId type)
    - src/app/App.tsx (line 686 — copy the em-dash U+2014 character verbatim from the existing medical-advice literal)
  </read_first>
  <behavior>
    - Exports `interface LockedCopy { readonly inspiredByForrest: string; readonly medicalAdviceLine: string; readonly affiliationLine: string }`.
    - Exports `const LOCKED_COPY: Readonly<Record<LocaleId, LockedCopy>>` with EN + PT-BR entries.
    - `LOCKED_COPY.en.inspiredByForrest` equals the string `inspired by Forrest's teachings` (ASCII apostrophe U+0027) — byte-exact.
    - `LOCKED_COPY.en.medicalAdviceLine` equals `Guided breathing practice — not medical advice.` (em-dash U+2014) — byte-exact.
    - `LOCKED_COPY.en.affiliationLine` equals `Independent project. Not affiliated with Forrest Knutson.` — byte-exact.
    - PT-BR entries are non-empty strings (selected per D-02 + CONTEXT.md `<specifics>` candidates).
    - Each PT-BR entry carries an inline `// LOCKED: back-translation = "..."` comment per D-02 (NOT `// TODO: native-speaker review` — locked-copy uses a separate back-translation comment style).
  </behavior>
  <action>
    Create `src/content/lockedCopy.ts` (NEW). Header comment block (style: see learnContent.ts:1–6) citing Phase 19 I18N-06 + D-01 separation rationale + D-02 frozen-EN snapshot guard + D-03 lock scope (3 entries) + D-04 composition design (LearnDialog + App.tsx render directly from this module).

    Imports: `import type { LocaleId } from '../domain/settings'`.

    Export the `LockedCopy` interface with three readonly string fields: `inspiredByForrest`, `medicalAdviceLine`, `affiliationLine`.

    Export `const LOCKED_COPY: Readonly<Record<LocaleId, LockedCopy>>` with these EXACT EN values (byte-exact, do not reformat or substitute punctuation):
    - `en.inspiredByForrest`: the string `inspired by Forrest's teachings` (ASCII apostrophe U+0027 between Forrest and s).
    - `en.medicalAdviceLine`: the string `Guided breathing practice — not medical advice.` (em-dash U+2014 — copy this character verbatim from `src/app/App.tsx:686`).
    - `en.affiliationLine`: the string `Independent project. Not affiliated with Forrest Knutson.`

    PT-BR values (planner picks from CONTEXT.md `<specifics>` candidates; each preceded by a `// LOCKED: back-translation = "..."` comment):
    - `'pt-BR'.inspiredByForrest`: `inspirado nos ensinamentos do Forrest` — back-translation comment: `inspired by Forrest's teachings`.
    - `'pt-BR'.medicalAdviceLine`: `Prática de respiração guiada — não é conselho médico.` (em-dash U+2014) — back-translation comment: `Guided breathing practice — not medical advice.`
    - `'pt-BR'.affiliationLine`: `Projeto independente. Não afiliado a Forrest Knutson.` — back-translation comment: `Independent project. Not affiliated with Forrest Knutson.`

    CRITICAL byte-exactness:
    - Verify the em-dash in BOTH EN and PT-BR `medicalAdviceLine` is U+2014 (em-dash), NOT U+2013 (en-dash) or U+002D (hyphen-minus). Copy from `src/app/App.tsx:686` directly.
    - Verify the apostrophe in EN `inspiredByForrest` is U+0027 (ASCII), NOT U+2019 (right single quote).

    Close with `} as const satisfies Readonly<Record<LocaleId, LockedCopy>>`.

    Do NOT add `// TODO: native-speaker review` markers here — `// LOCKED: back-translation` is the analog marker for locked copy (D-02 — PT-BR is not snapshot-locked but is back-translation-commented for reviewer audit).
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20 && grep -c "LOCKED: back-translation" src/content/lockedCopy.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/content/lockedCopy.ts` exists and exports `LockedCopy` interface + `LOCKED_COPY` constant.
    - `npx tsc --noEmit` exits 0.
    - `grep -c "LOCKED: back-translation" src/content/lockedCopy.ts` returns 3 (one per PT-BR entry).
    - `grep -c "Forrest's teachings" src/content/lockedCopy.ts` returns 1 (EN value present; PT-BR variant uses a different phrase).
    - `grep -c "Guided breathing practice" src/content/lockedCopy.ts` returns 1 (EN medicalAdviceLine present).
    - `grep -c "as const satisfies Readonly<Record<LocaleId, LockedCopy>>" src/content/lockedCopy.ts` returns 1.
    - `grep -c "—" src/content/lockedCopy.ts` returns >= 2 (em-dash U+2014 present in both EN and PT-BR medicalAdviceLine).
    - File contains NO `// TODO: native-speaker review` text (D-02 — uses back-translation comment style instead).
  </acceptance_criteria>
  <done>
    `lockedCopy.ts` exports the 3-entry × 2-locale catalog; EN values are byte-exact baselines; PT-BR values carry back-translation comments; em-dash U+2014 confirmed in source.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Create src/content/lockedCopy.test.ts with frozen-EN + PT-BR non-empty + substring-absence guards</name>
  <files>src/content/lockedCopy.test.ts</files>
  <read_first>
    - src/content/learnContent.test.ts (existing pattern — lines 1–24 describe + structural-contract style; lines 27–31 substring-presence guard that Plan 03 inverts)
    - src/content/lockedCopy.ts (the file just created in Task 2)
    - src/content/learnContent.ts (the file just mutated in Task 1)
    - .planning/phases/19-language-switching/19-PATTERNS.md (lines 179–230 — lockedCopy.test.ts pattern; CRITICAL: uses .toBe byte-equality NOT toMatchInlineSnapshot per RESEARCH.md line 11)
    - .planning/phases/19-language-switching/19-RESEARCH.md (Summary §3 — toBe vs toMatchInlineSnapshot rationale)
  </read_first>
  <behavior>
    - Three `describe` blocks (frozen-EN snapshot + PT-BR non-empty + substring-absence guard).
    - All assertions use `.toBe(...)` byte-equality for EN values — NOT `.toMatchInlineSnapshot()`. Snapshot auto-updates would silently weaken the D-12 lock.
    - Substring-absence guard iterates `LOCALE_OPTIONS` so adding a third locale (e.g. `'es'`) auto-extends the guard without code changes.
    - All tests pass under `npx vitest run src/content/lockedCopy.test.ts`.
  </behavior>
  <action>
    Create `src/content/lockedCopy.test.ts` (NEW).

    Imports: `describe`, `expect`, `it` from `'vitest'`; `LOCKED_COPY` from `'./lockedCopy'`; `LEARN_CONTENT` from `'./learnContent'`; `LOCALE_OPTIONS` from `'../domain/settings'`.

    `describe('LOCKED_COPY frozen-EN snapshot (D-02)')`:
    - `it('inspiredByForrest matches EN baseline byte-exact')` — `expect(LOCKED_COPY.en.inspiredByForrest).toBe("inspired by Forrest's teachings")` (ASCII apostrophe U+0027).
    - `it('medicalAdviceLine matches EN baseline byte-exact (em-dash U+2014)')` — `expect(LOCKED_COPY.en.medicalAdviceLine).toBe("Guided breathing practice — not medical advice.")` (em-dash U+2014 copied from source).
    - `it('affiliationLine matches EN baseline byte-exact')` — `expect(LOCKED_COPY.en.affiliationLine).toBe("Independent project. Not affiliated with Forrest Knutson.")`.

    `describe('LOCKED_COPY PT-BR non-empty')`:
    - `it('every PT-BR locked entry is non-empty')` — three `expect(...length).toBeGreaterThan(0)` checks for inspiredByForrest, medicalAdviceLine, affiliationLine.
    - `it('PT-BR medicalAdviceLine contains em-dash U+2014')` — `expect(LOCKED_COPY['pt-BR'].medicalAdviceLine.includes('—')).toBe(true)` (matches the em-dash convention from EN).

    `describe('LOCKED_COPY substring-absence guard (D-02 + D-04)')`:
    - `it('learnContent[locale].explainer.forrest.body does NOT contain lockedCopy[locale].inspiredByForrest as substring')` — for-loop over `LOCALE_OPTIONS`; assert `LEARN_CONTENT[locale].explainer.forrest.body.includes(LOCKED_COPY[locale].inspiredByForrest)` is `false`.

    CRITICAL: Use `.toBe(...)` byte-equality for the 3 frozen EN assertions. Do NOT use `.toMatchInlineSnapshot()` (per RESEARCH.md line 11 — snapshot auto-updates on diff and would defeat the lock).

    No `vi`, no `beforeEach`, no `localStorage` — pure data assertion file.
  </action>
  <verify>
    <automated>npx vitest run src/content/lockedCopy.test.ts 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `src/content/lockedCopy.test.ts` exists.
    - `npx vitest run src/content/lockedCopy.test.ts` exits 0.
    - Output reports >= 5 passing tests (3 frozen-EN + 2 PT-BR-non-empty + 1 substring-absence = 6 minimum).
    - `grep -c "toBe(" src/content/lockedCopy.test.ts` returns >= 3 (byte-equality assertions, NOT snapshots).
    - `grep -c "toMatchInlineSnapshot" src/content/lockedCopy.test.ts` returns 0 (D-02 forbids snapshot self-update).
    - `grep -c "for (const locale of LOCALE_OPTIONS)" src/content/lockedCopy.test.ts` returns >= 1 (substring-absence guard iterates locales).
    - `grep "inspired by Forrest" src/content/lockedCopy.test.ts | grep -c "Forrest's teachings"` returns 1 (the EN baseline literal is asserted exactly once).
  </acceptance_criteria>
  <done>
    `lockedCopy.test.ts` enforces frozen-EN byte-equality (no snapshot auto-update), PT-BR non-empty, and substring-absence across all locales.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Mutate learnContent.test.ts — loop per locale + invert substring-presence guard + assert PT-BR URL identity</name>
  <files>src/content/learnContent.test.ts</files>
  <read_first>
    - src/content/learnContent.test.ts (current file — entire 121 lines)
    - src/content/learnContent.ts (the file just mutated in Task 1)
    - src/content/lockedCopy.test.ts (the file just created in Task 3 — note that the substring-presence-INVERTED guard now lives there; do NOT duplicate in learnContent.test.ts)
    - .planning/phases/19-language-switching/19-PATTERNS.md (lines 642–673 — learnContent.test.ts mutation pattern)
  </read_first>
  <behavior>
    - DELETE the existing `describe('LEARN_CONTENT locked copy contract')` block at lines 26–50 — specifically the test at lines 27–31 (`'forrest body contains verbatim phrase ...'`). This guard is INVERTED in `lockedCopy.test.ts` (substring-absence). The 3 sibling tests (lines 33–50) about forbidden clinical verbs MOVE into the existing structural-contract loop where they iterate per locale.
    - WRAP the structural-contract assertions (existing lines 5–24) in a per-locale loop using `LOCALE_OPTIONS`. Each `it` becomes `it('[${locale}] ...')` and asserts against `LEARN_CONTENT[locale].explainer.{...}`.
    - WRAP the link-contract assertions (existing lines 52–121) similarly — replace `LEARN_CONTENT.links.{...}` with `LEARN_CONTENT.en.links.{...}` for the EN URL/label assertions (label assertions are EN-specific — the PT-BR labels are translated and don't need byte-equality). The URL `startsWith('https://')` and dangerous-scheme negative guards loop per locale.
    - ADD a new `describe('LEARN_CONTENT PT-BR URL identity (D-12 invariant)')` block that asserts every `links.{youtubeChannel,website,book,patreon,heroVideo}.url` is byte-identical between EN and PT-BR, plus the per-index `keyVideos[i].url` equality.
    - All tests pass.
  </behavior>
  <action>
    Edit `src/content/learnContent.test.ts` in place. Steps:

    1. Add `import { LOCALE_OPTIONS } from '../domain/settings'` to existing imports.
    2. Replace `describe('LEARN_CONTENT structural contract', ...)` with a loop: `for (const locale of LOCALE_OPTIONS) { describe(\`LEARN_CONTENT[${locale}] structural contract\`, () => { ... }) }`. Inside the loop, every assertion targets `LEARN_CONTENT[locale].explainer.{...}`. Keep all 4 structural tests (explainer keys ordering, hrv/timing/forrest non-empty title+body) per-locale.
    3. DELETE the existing `describe('LEARN_CONTENT locked copy contract')` block (lines 26–50) entirely. The substring-presence test (lines 27–31) is REPLACED by the substring-absence guard now living in `lockedCopy.test.ts`. The 3 forbidden-clinical-verbs tests are moved into a new per-locale block (next step) so PT-BR is also guarded.
    4. ADD a new per-locale block: `for (const locale of LOCALE_OPTIONS) { describe(\`LEARN_CONTENT[${locale}] clinical-verbs guard\`, () => { it('hrv body has no forbidden clinical verbs'), it('timing body has no forbidden clinical verbs'), it('forrest body has no forbidden clinical verbs') }) }`. For EN locale, keep the existing regex `/\\b(improves|treats|cures|heals|diagnoses)\\b/i`. For PT-BR locale, use the regex `/\\b(melhora|trata|cura|diagnostica|avalia)\\b/i` (PT-BR clinical-verb counterparts).
    5. Mutate `describe('LEARN_CONTENT link contract', ...)`: change every `LEARN_CONTENT.links.{...}` to `LEARN_CONTENT.en.links.{...}` for the EN URL/label byte-equality assertions (those are EN-specific contract anchors from v1.0).
    6. ADD a new `describe('LEARN_CONTENT PT-BR URL identity (D-12)')` block at the bottom:
       - `it('youtubeChannel URL identical across locales')` — assert `LEARN_CONTENT['pt-BR'].links.youtubeChannel.url === LEARN_CONTENT.en.links.youtubeChannel.url`.
       - Same per `website`, `book`, `patreon`, `heroVideo`.
       - `it('keyVideos URLs identical across locales')` — for each index 0..2, assert `LEARN_CONTENT['pt-BR'].links.keyVideos[i].url === LEARN_CONTENT.en.links.keyVideos[i].url`.
       - `it('keyVideos length identical across locales')` — assert `LEARN_CONTENT['pt-BR'].links.keyVideos.length === LEARN_CONTENT.en.links.keyVideos.length`.
    7. ADD `it('PT-BR forrest body does NOT contain its EN-baseline-equivalent locked phrase')` — assert `LEARN_CONTENT['pt-BR'].explainer.forrest.body.includes('inspirado nos ensinamentos do Forrest')` is `false`. (Belt-and-suspenders alongside the `lockedCopy.test.ts` substring-absence guard.)

    Do NOT touch any other test file. Do NOT touch `src/content/lockedCopy.test.ts`.
  </action>
  <verify>
    <automated>npx vitest run src/content/learnContent.test.ts src/content/lockedCopy.test.ts 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/content/learnContent.test.ts src/content/lockedCopy.test.ts` exits 0.
    - Original test `forrest body contains verbatim phrase "inspired by Forrest\'s teachings" (D-11)` is DELETED from learnContent.test.ts (`grep -c "verbatim phrase" src/content/learnContent.test.ts` returns 0).
    - New per-locale structural loop present: `grep -c "for (const locale of LOCALE_OPTIONS)" src/content/learnContent.test.ts` returns >= 2.
    - New `describe(.*PT-BR URL identity` block present (`grep -c "PT-BR URL identity" src/content/learnContent.test.ts` returns 1).
    - PT-BR substring-absence assertion present (`grep -c "inspirado nos ensinamentos do Forrest" src/content/learnContent.test.ts` returns 1).
    - PT-BR clinical-verbs regex present (`grep -c "melhora\\|trata\\|cura" src/content/learnContent.test.ts` returns >= 1).
    - All other previously-passing assertions migrated to per-locale form OR EN-prefixed form; no test removed except the inverted substring-presence guard.
  </acceptance_criteria>
  <done>
    `learnContent.test.ts` loops per locale, the substring-presence guard is removed (now lives inverted in lockedCopy.test.ts), PT-BR URL identity is enforced, PT-BR clinical-verbs guard is enforced.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 5: LearnDialog stop-gap composition — LEARN_CONTENT.en direct access + locked-phrase paragraph + affiliation migration</name>
  <files>src/components/LearnDialog.tsx, src/components/LearnDialog.test.tsx</files>
  <read_first>
    - src/components/LearnDialog.tsx (entire 187 lines — pay attention to line 3 import, line 67 destructure, lines 89–94 forrest section, line 171 affiliation literal)
    - src/components/LearnDialog.test.tsx (entire 157 lines — pay attention to line 43–46 locked-phrase presence guard, line 48 medical-advice absence guard, line 57–62 affiliation byte-equality, line 147 LEARN_CONTENT.links.keyVideos access)
    - src/content/learnContent.ts (the file just mutated in Task 1 — LEARN_CONTENT.en.* shape)
    - src/content/lockedCopy.ts (the file just created in Task 2 — LOCKED_COPY.en.* shape)
    - .planning/phases/19-language-switching/19-CONTEXT.md (D-04 composition rule + Claude's Discretion §"Planner picks JSX shape for the extracted Forrest phrase")
  </read_first>
  <behavior>
    - LearnDialog.tsx line 3 import unchanged in symbol set (still imports `LEARN_CONTENT` value, not type) — keep the value import for the stop-gap. Add a NEW value import: `import { LOCKED_COPY } from '../content/lockedCopy'`.
    - LearnDialog.tsx line 67 destructure changes shape from `const { explainer, links } = LEARN_CONTENT` to `const { explainer, links } = LEARN_CONTENT.en` (one-character delta — `.en` suffix). This is the stop-gap. Plan 08 swaps to prop-driven `learnContent` destructure when LearnDialog is widened with props.
    - LearnDialog.tsx forrest section (after the existing `explainer.forrest.body.split('\n\n').map(...)` paragraphs at lines 91–93): add a NEW `<p>` element rendering `{LOCKED_COPY.en.inspiredByForrest}` as an italicized closing paragraph (planner picks JSX option A per Claude's Discretion). Placement: INSIDE the same parent `<div>` that holds the forrest title + body paragraphs (the existing `<div>` opened at line 89, closed at line 94). The new `<p>` sits AFTER the `.map(...)` block, BEFORE the closing `</div>`.
    - LearnDialog.tsx line 171 affiliation literal `<p class=...>Independent project. Not affiliated with Forrest Knutson.</p>` becomes `<p class=...>{LOCKED_COPY.en.affiliationLine}</p>`. The outer `<p>` className + center alignment is preserved verbatim (D-19 minimal-diff invariant).
    - LearnDialog.test.tsx line 6 import unchanged in symbol set. Line 147 `const keyVideos = LEARN_CONTENT.links.keyVideos` becomes `const keyVideos = LEARN_CONTENT.en.links.keyVideos` (one-character delta).
    - LearnDialog.test.tsx existing tests at lines 43–46 (locked-phrase presence), 48 (medical-advice modal-absence), 57–62 (affiliation byte-equality) MUST stay green. The locked phrase appears in the modal via the new `<p>{LOCKED_COPY.en.inspiredByForrest}</p>` paragraph; the medical-advice line is not rendered in LearnDialog (lives in App.tsx); the affiliation byte-equality test reads the rendered DOM, which now reads from `LOCKED_COPY.en.affiliationLine` whose value is the EN baseline `'Independent project. Not affiliated with Forrest Knutson.'`.
    - All 17 existing LearnDialog tests pass after this task (no test additions in this plan — Plan 08 adds the PT-BR rendering smoke).
    - JSX shape choice for the new paragraph (italicized closing paragraph option A): className `text-base leading-6 italic text-[var(--color-breathing-muted)] [&:not(:first-of-type)]:mt-2` — mirrors the existing forrest body `<p>` className with an `italic` modifier added.
  </behavior>
  <action>
    Edit `src/components/LearnDialog.tsx`:
    1. Add `import { LOCKED_COPY } from '../content/lockedCopy'` immediately after the existing `import { LEARN_CONTENT } from '../content/learnContent'` line at line 3.
    2. Update the header comment block (lines 5–12) to note Phase 19 D-03 D-04 stop-gap: locked phrase + affiliation now read from `LOCKED_COPY.en`; Plan 08 will swap `.en` direct access for prop-driven `lockedCopy` resolved via `useLocale()`. Keep D-05/D-07/D-08/D-11/D-14/D-15 attribution lines verbatim.
    3. Line 67: change `const { explainer, links } = LEARN_CONTENT` to `const { explainer, links } = LEARN_CONTENT.en` (single `.en` suffix added).
    4. Lines 89–94 forrest section: after the closing `))}` of the `explainer.forrest.body.split('\n\n').map(...)` at line 93, INSIDE the same parent `<div>` (closes at line 94), add a new line: `<p className="text-base leading-6 italic text-[var(--color-breathing-muted)] [&:not(:first-of-type)]:mt-2">{LOCKED_COPY.en.inspiredByForrest}</p>`. This is the italicized closing paragraph (Claude's Discretion JSX option A).
    5. Line 171: change `Independent project. Not affiliated with Forrest Knutson.` to `{LOCKED_COPY.en.affiliationLine}` inside the existing `<p class="text-center text-xs text-[var(--color-breathing-muted)]">...</p>`. The outer `<p>` className stays verbatim per D-19 minimal-diff.

    Edit `src/components/LearnDialog.test.tsx`:
    1. Line 147: change `const keyVideos = LEARN_CONTENT.links.keyVideos` to `const keyVideos = LEARN_CONTENT.en.links.keyVideos` (single `.en` suffix added).
    2. Verify all other test bodies remain unchanged — the rendered DOM should still contain `'About this practice'`, `'Close'`, `'inspired by Forrest\'s teachings'`, `'Independent project. Not affiliated with Forrest Knutson.'`, link `href` attributes byte-identical to the EN baseline. NO test additions in this plan.

    Do NOT widen `LearnDialogProps` in this task. Do NOT add `learnContent` / `lockedCopy` / `strings` props yet. Do NOT touch the LearnDialog `useEffect`, `dialogRef`, `closeButtonRef`, `handleCancel`, `handleBackdropClick`, or any other primitive — only the import addition + the 3 LearnDialog body deltas + 1 LearnDialog.test.tsx delta.
  </action>
  <verify>
    <automated>npx vitest run src/components/LearnDialog.test.tsx 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "import { LOCKED_COPY }" src/components/LearnDialog.tsx` returns 1.
    - `grep -c "LEARN_CONTENT.en" src/components/LearnDialog.tsx` returns 1 (line 67 destructure).
    - `grep -c "LEARN_CONTENT.en" src/components/LearnDialog.test.tsx` returns 1 (line 147 keyVideos access).
    - `grep -c "LOCKED_COPY.en.inspiredByForrest" src/components/LearnDialog.tsx` returns 1 (new paragraph).
    - `grep -c "LOCKED_COPY.en.affiliationLine" src/components/LearnDialog.tsx` returns 1 (affiliation migration).
    - `grep -c "Independent project. Not affiliated with Forrest Knutson." src/components/LearnDialog.tsx` returns 0 (inline literal removed; the EN baseline value still flows through LOCKED_COPY.en so the rendered DOM and test assertions match).
    - `npx vitest run src/components/LearnDialog.test.tsx` exits 0 — all 17 existing tests pass; especially:
      - line 43–46 locked-phrase presence (composed from LOCKED_COPY.en.inspiredByForrest paragraph)
      - line 48 medical-advice modal-absence (still absent — App.tsx owns that literal)
      - line 57–62 affiliation byte-equality (now sourced from LOCKED_COPY.en.affiliationLine)
      - line 145–155 keyVideos security attrs (LEARN_CONTENT.en.links.keyVideos iteration)
    - JSX shape (italicized closing paragraph option A) verified: `grep -c "italic text-\[var(--color-breathing-muted)\]" src/components/LearnDialog.tsx` returns >= 1.
    - LearnDialogProps interface NOT widened (Plan 08 owns that): `grep -c "learnContent: LearnContent\|lockedCopy: LockedCopy\|strings: UiStrings" src/components/LearnDialog.tsx` returns 0.
  </acceptance_criteria>
  <done>
    LearnDialog.tsx + LearnDialog.test.tsx use `.en` direct access stop-gap; locked phrase paragraph composed; affiliation migrated; all 17 existing tests pass. Plan 08 will remove the `.en` stop-gap when LearnDialogProps is widened.
  </done>
</task>

<task type="auto">
  <name>Task 6: Per-commit green-gate (D-16)</name>
  <files>(no source edit — verification only)</files>
  <read_first>
    - .planning/phases/19-language-switching/19-CONTEXT.md (D-16)
  </read_first>
  <action>
    Run `npx tsc --noEmit && npm run lint && npm run build && npm test -- --run`. After exit 0, commit `feat(19-03): locale-keyed learnContent + lockedCopy + frozen-EN guard + LearnDialog stop-gap` containing only the six files in `files_modified`. Single atomic commit (D-16 allows multiple commits per plan, but logical atomicity REQUIRES one commit here: the LearnDialog stop-gap depends on the LEARN_CONTENT shape change AND the LOCKED_COPY module creation — splitting would leave intermediate commits with broken TS compile).
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run lint && npm run build && npm test -- --run 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - All 4 green-gate steps exit 0.
    - Commit `feat(19-03): locale-keyed learnContent + lockedCopy + frozen-EN guard + LearnDialog stop-gap` created.
    - `git diff HEAD~1 --stat` shows exactly these 6 files: `src/content/learnContent.ts`, `src/content/learnContent.test.ts`, `src/content/lockedCopy.ts`, `src/content/lockedCopy.test.ts`, `src/components/LearnDialog.tsx`, `src/components/LearnDialog.test.tsx`.
  </acceptance_criteria>
  <done>
    Green-gate passes; commit landed; no collateral file changes.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none active) | Plan 03 ships static-data + tests. No runtime input, no DOM write. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-06 | Tampering | LOCKED_COPY EN baseline values | mitigate | D-02 frozen-EN snapshot — Task 3 asserts byte-equality via `.toBe(...)`. `.toMatchInlineSnapshot` explicitly forbidden (auto-update would silently weaken D-12 positioning). |
| T-19-07 | Tampering | LEARN_CONTENT forrest.body locked-phrase substring | mitigate | D-04 strip pattern + Task 3 substring-absence guard iterates LOCALE_OPTIONS so a re-inline (e.g. accidental copy-paste during native-speaker review) breaks the test for any locale. |
| T-19-08 | Information Disclosure | PT-BR machine-translated entries | accept | Per D-13 + I18N-07 ship with `// TODO: native-speaker review`. No PII or secrets. v1.x carry-forward already tracked in REQUIREMENTS.md. |
| T-19-09 | Tampering | LearnContent PT-BR URLs (D-12 URL identity) | mitigate | New `describe(PT-BR URL identity)` block asserts byte-identity between EN and PT-BR; future locale contributions cannot redirect users to unverified URLs. |
</threat_model>

<verification>
- `npx tsc --noEmit && npm run lint && npm run build && npm test -- --run` exits 0.
- `grep -c "inspired by Forrest" src/content/learnContent.ts` returns 0 (D-04 substring strip confirmed).
- `grep -c "verbatim phrase" src/content/learnContent.test.ts` returns 0 (presence-guard removed).
- `grep -c "for (const locale of LOCALE_OPTIONS)" src/content/lockedCopy.test.ts` returns >= 1 (substring-absence iterates locales).
- `grep -c "toMatchInlineSnapshot" src/content/lockedCopy.test.ts` returns 0 (snapshot self-update forbidden).
- `grep -c "LOCKED_COPY.en.inspiredByForrest\|LOCKED_COPY.en.affiliationLine" src/components/LearnDialog.tsx` returns 2 (stop-gap composition + affiliation migration).
- `grep -c "Independent project. Not affiliated with Forrest Knutson." src/components/LearnDialog.tsx` returns 0 (inline literal removed; LOCKED_COPY.en.affiliationLine carries the same EN baseline string so rendered DOM is byte-identical).
- All 17 existing LearnDialog tests pass (locked-phrase presence + medical-advice absence + affiliation byte-equality + keyVideos security attrs).
- No edits to `src/domain/settings.ts` or `src/storage/prefs.ts` (D-18).
</verification>

<success_criteria>
1. `LEARN_CONTENT` is `Readonly<Record<LocaleId, LearnContent>>` with EN substring stripped and PT-BR translation present.
2. `LOCKED_COPY` exposes 3 entries × 2 locales with byte-exact EN baseline values.
3. Frozen-EN snapshot guard uses `.toBe(...)` byte-equality, not `.toMatchInlineSnapshot()`.
4. Substring-absence guard iterates LOCALE_OPTIONS so it scales to future locales.
5. PT-BR link URLs are byte-identical to EN; only labels are translated.
6. PT-BR clinical-verbs negative guard parallels the EN regex.
7. LearnDialog.tsx stop-gap composition lands in the same commit as the shape change: `LEARN_CONTENT.en` / `LOCKED_COPY.en` direct access; locked phrase rendered as italicized closing paragraph; affiliation migrated to `LOCKED_COPY.en.affiliationLine`; all 17 existing LearnDialog tests pass.
8. Green-gate exits 0 after the commit.
</success_criteria>

<output>
After completion, create `.planning/phases/19-language-switching/19-03-SUMMARY.md` capturing: 6 files touched (4 content + 2 LearnDialog stop-gap), the exact EN→stripped substring delta in `forrest.body`, the PT-BR phrase chosen for `LOCKED_COPY['pt-BR'].inspiredByForrest`, total `// TODO: native-speaker review` count added, total `// LOCKED: back-translation` count added, JSX shape chosen for the locked-phrase paragraph (italicized closing — option A), confirmation that Plan 08 will remove the `.en` direct-access stop-gap and replace with prop-driven `learnContent` / `lockedCopy` resolved via `useLocale()`.
</output>
