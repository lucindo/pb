---
phase: 19-language-switching
plan: 07
type: execute
wave: 3
depends_on: ["19-01"]
files_modified:
  - src/components/SettingsForm.tsx
  - src/components/SettingsStepper.tsx
  - src/components/SessionControls.tsx
  - src/components/SessionControls.test.tsx
  - src/components/StatsFooter.tsx
  - src/components/StatsFooter.test.tsx
  - src/components/SessionReadout.tsx
  - src/components/SessionReadout.test.tsx
  - src/components/BreathingShape.tsx
  - src/components/BreathingShape.test.tsx
  - src/components/OrbShape.tsx
  - src/components/OrbShape.test.tsx
  - src/components/SquareShape.tsx
  - src/components/SquareShape.test.tsx
  - src/components/DiamondShape.tsx
  - src/components/DiamondShape.test.tsx
autonomous: true
requirements:
  - I18N-04
must_haves:
  truths:
    - "SettingsForm accepts strings: UiStrings['settingsForm'] and drills stepper slice + per-field labels to 3 SettingsStepper instances"
    - "SettingsStepper accepts strings: { fieldAriaLabel, decreaseLabel, increaseLabel } template-fn slice + label string"
    - "SessionControls accepts strings: UiStrings['controls'] for Start/End button labels (both legacy + new-props branches)"
    - "StatsFooter accepts strings: UiStrings['stats'] with template-fn entries for sessionsCount + totalMinutes + lastSessionPrefix"
    - "SessionReadout accepts strings: UiStrings['readout'] for elapsed/remaining + aria-labels + sessionComplete message"
    - "BreathingShape accepts strings: UiStrings['breathing'] and pass-through-drills to OrbShape/SquareShape/DiamondShape; shapes consume strings.inhale / strings.exhale instead of frame.phaseLabel"
  artifacts:
    - path: "src/components/SettingsStepper.tsx"
      provides: "template-fn aria-label binding per D-15"
      contains: "strings.fieldAriaLabel\\|strings.decreaseLabel\\|strings.increaseLabel"
    - path: "src/components/BreathingShape.tsx"
      provides: "strings pass-through to shape variants"
      contains: "strings: UiStrings\\['breathing'\\]"
  key_links:
    - from: "src/components/SettingsForm.tsx"
      to: "src/components/SettingsStepper.tsx"
      via: "strings.stepper + per-field label drill"
      pattern: "strings={strings.stepper}"
    - from: "src/components/BreathingShape.tsx"
      to: "src/components/OrbShape.tsx"
      via: "strings prop pass-through"
      pattern: "strings={strings}"
---

<objective>
Edit 8 components (form + stepper + controls + footer + readout + breathing + 3 shape variants) to accept a typed `strings` prop. Replace inline English literals with `strings.*` lookups. SettingsStepper consumes the template-fn slice (D-15). BreathingShape pass-through-drills to OrbShape/SquareShape/DiamondShape; the shape components consume `strings.inhale` / `strings.exhale` instead of the `frame.phaseLabel` field per the JSX-layer wedge in PATTERNS.md (D-18 file-split invariant on `src/domain/sessionMath.ts`).

Purpose: Plan 07 is the "session UI" half of the component prop-drill — it pairs with Plan 06 (chrome/dialogs/pickers). After Plan 06 + 07, App.tsx (Plan 08) drills `uiStrings` slices to every consumer.

Output: 8 source files + 8 test files updated. Green-gate exits 0.
</objective>

<execution_context>
@$HOME/.claude-personal-personal/get-shit-done/workflows/execute-plan.md
@$HOME/.claude-personal-personal/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/19-language-switching/19-CONTEXT.md
@.planning/phases/19-language-switching/19-PATTERNS.md
@.planning/phases/19-language-switching/19-01-SUMMARY.md

<interfaces>
From src/content/strings.ts (Plan 01 — Plan-07-relevant slices):
```typescript
settingsForm: {
  ariaLabel: string; bpmLabel: string; ratioLabel: string; durationLabel: string;
  openEndedLabel: string; bpmUnit: string; minutesUnit: string;
  stepper: {
    fieldAriaLabel: (label: string) => string;
    decreaseLabel: (label: string) => string;
    increaseLabel: (label: string) => string;
  }
}
controls: { startSession: string; endSession: string }
stats: {
  sessionsCount: (n: number) => string;
  totalMinutes: (seconds: number) => string;
  lastSessionPrefix: (date: string, duration: string) => string;
  totalSuffix: string; reset: string;
}
readout: {
  elapsed: string; remaining: string;
  readoutAriaLabel: string; announcementAriaLabel: string; sessionComplete: string;
}
breathing: {
  inhale: string; exhale: string;
  breathingShapeAriaLabel: string;
  leadInAriaLabel: (digit: number) => string;
}
```

Current literals to replace:
- SettingsForm.tsx:18–24 (formatBpm/formatDuration), :52 (aria-label), :55–57 (3 stepper labels)
- SettingsStepper.tsx:35,44,59 (aria-label bindings)
- SessionControls.tsx:55,72 (Start/End button labels — both branches)
- StatsFooter.tsx:37–38,50,56 (sessions count, total minutes, lastLine prefix, Reset button)
- format.ts:38,59 — UPSTREAM source of sessions-count + last-session strings; per D-19 minimal-diff invariant, planner picks Path A (translate at component layer); StatsFooter consumes strings template-fns directly INSTEAD OF format.ts helpers (path A — translate at component, leave format.ts EN-only). format.ts is NOT edited in this plan.
- SessionReadout.tsx:22,28,33,54,63,68,73 — placeholderLabel ternary, aria-labels, timeLabel ternary, message
- BreathingShape.tsx — pass-through to shape variants
- OrbShape.tsx:37,125 — phaseLabel + breathingShapeAriaLabel
- SquareShape.tsx and DiamondShape.tsx — same pattern as OrbShape
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: SettingsForm + SettingsStepper accept strings; D-15 template-fn binding</name>
  <files>src/components/SettingsForm.tsx, src/components/SettingsStepper.tsx</files>
  <read_first>
    - src/components/SettingsForm.tsx (entire 81 lines — formatBpm/formatDuration helpers + 3 SettingsStepper invocations)
    - src/components/SettingsStepper.tsx (entire 69 lines — aria-label bindings at lines 35, 44, 59)
    - src/content/strings.ts (Plan 01 — settingsForm slice with nested stepper template-fns)
    - .planning/phases/19-language-switching/19-PATTERNS.md (lines 865–942 — SettingsForm + SettingsStepper mutation pattern)
    - .planning/phases/19-language-switching/19-CONTEXT.md (D-15 template-fn entries + Claude's Discretion §"Planner picks SettingsForm prop-drill shape" — pass strings.stepper + per-field label)
  </read_first>
  <behavior>
    - SettingsForm accepts `strings: UiStrings['settingsForm']`. Top-level `<div>` aria-label uses `strings.ariaLabel`. Each `SettingsStepper` instance receives `strings={strings.stepper}` (template-fn slice) AND `label={strings.bpmLabel | strings.ratioLabel | strings.durationLabel}` (per-field label string).
    - The two local helper functions `formatBpm` and `formatDuration` move into the component body (closure-scoped) and consume `strings.bpmUnit`, `strings.minutesUnit`, `strings.openEndedLabel` to compose values. Alternative: keep helpers at module scope but pass them strings explicitly. Planner picks closure-scope (smaller diff, no extra parameter threading).
    - SettingsStepper Props gains `strings: { fieldAriaLabel: (l: string) => string; decreaseLabel: (l: string) => string; increaseLabel: (l: string) => string }`. The 3 aria-label bindings change:
      - Line 35: `aria-label={label}` → `aria-label={strings.fieldAriaLabel(label)}`.
      - Line 44: `aria-label={`Decrease ${label}`}` → `aria-label={strings.decreaseLabel(label)}`.
      - Line 59: `aria-label={`Increase ${label}`}` → `aria-label={strings.increaseLabel(label)}`.
    - `legend` text at line 39 still uses `{label}` directly (per-field label string is the same in both EN and PT-BR — `BPM` is unchanged in PT-BR; `Ratio` → `Proporção`; `Duration` → `Duração` — handled at the call site).
    - All token-bound classes (focus-visible, hit-area floors) UNCHANGED.
  </behavior>
  <action>
    Edit `src/components/SettingsForm.tsx`:
    1. Add `import type { UiStrings } from '../content/strings'`.
    2. Extend `SettingsFormProps` with `strings: UiStrings['settingsForm']`.
    3. Destructure `strings` in the component signature.
    4. Move `formatBpm` + `formatDuration` from module scope INTO the component body as closure-scoped consts (or `const formatBpm = (value: number) => `${String(value)} ${strings.bpmUnit}`` ; same shape for duration using `strings.openEndedLabel` and `strings.minutesUnit`).
    5. Replace `aria-label="Session settings"` at line 52 with `aria-label={strings.ariaLabel}`.
    6. Update the 3 `<SettingsStepper>` invocations (lines 55–78) to add `strings={strings.stepper}` and use `label={strings.bpmLabel}` / `label={strings.ratioLabel}` / `label={strings.durationLabel}` in place of the hardcoded `"BPM"` / `"Ratio"` / `"Duration"` literals.

    Edit `src/components/SettingsStepper.tsx`:
    1. Extend `SettingsStepperProps<T>` interface with `strings: { fieldAriaLabel: (l: string) => string; decreaseLabel: (l: string) => string; increaseLabel: (l: string) => string }`.
    2. Destructure `strings` in the function signature.
    3. Replace line 35 `aria-label={label}` with `aria-label={strings.fieldAriaLabel(label)}`.
    4. Replace line 44 `aria-label={`Decrease ${label}`}` with `aria-label={strings.decreaseLabel(label)}`.
    5. Replace line 59 `aria-label={`Increase ${label}`}` with `aria-label={strings.increaseLabel(label)}`.

    Tests: SettingsStepper has no dedicated test file (verified by file listing — there is no `SettingsStepper.test.tsx`). SettingsForm has no dedicated test file in the listing either; its tests live inside `App.session.test.tsx` and `App.settings.test.tsx`. **DO NOT** create new test files for these components in this plan — Plan 09 will add a locale-aware App-level test. The compile + lint check + the App-level integration tests cover the strings prop wiring transitively.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20 && npx vitest run src/app/App.settings.test.tsx 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "strings.fieldAriaLabel\\|strings.decreaseLabel\\|strings.increaseLabel" src/components/SettingsStepper.tsx` returns 3.
    - `grep -c "strings={strings.stepper}" src/components/SettingsForm.tsx` returns 3 (drilled to 3 stepper instances).
    - `grep -c "strings.ariaLabel" src/components/SettingsForm.tsx` returns 1.
    - `grep -c "aria-label=\"Session settings\"\\|aria-label={`Decrease \\|aria-label={`Increase " src/components/SettingsForm.tsx src/components/SettingsStepper.tsx` returns 0 (literals removed).
    - `npx tsc --noEmit` exits 0.
    - Existing App-level integration tests still pass (will fail until App.tsx wires the new props; that's Plan 08 — see verification note in Task 9 about temporary App-side stop-gap).
  </acceptance_criteria>
  <done>
    SettingsForm + SettingsStepper accept strings; D-15 template-fn bindings replace inline literals; token-bound a11y classes preserved.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: SessionControls accepts strings prop for Start/End button labels (both branches)</name>
  <files>src/components/SessionControls.tsx, src/components/SessionControls.test.tsx</files>
  <read_first>
    - src/components/SessionControls.tsx (entire 83 lines — legacy branch line 55, new-props branch line 72)
    - src/components/SessionControls.test.tsx (existing fixtures)
    - src/content/strings.ts (Plan 01 — `UiStrings['controls']`)
  </read_first>
  <behavior>
    - `SessionControlsProps` gains `strings: UiStrings['controls']`.
    - BOTH branches (legacy at line 55, new-props at line 72) render `{isRunning ? strings.endSession : strings.startSession}` instead of inline literals.
    - Token-bound classes + flex layout + MuteToggle invocation in new-props branch UNCHANGED.
    - Tests update fixtures to pass `strings={EN_STRINGS_FIXTURE.controls}`.
  </behavior>
  <action>
    Edit `src/components/SessionControls.tsx`:
    1. Add `import type { UiStrings } from '../content/strings'`.
    2. Extend `SessionControlsProps` with `strings: UiStrings['controls']`.
    3. Destructure `strings` in the signature.
    4. Replace `'End session' : 'Start session'` at line 55 (legacy branch) with `strings.endSession : strings.startSession`.
    5. Replace the same ternary at line 72 (new-props branch).

    Edit `src/components/SessionControls.test.tsx`:
    - Add `EN_STRINGS_FIXTURE = UI_STRINGS.en` import.
    - Update every `render(<SessionControls .../>)` to pass `strings={EN_STRINGS_FIXTURE.controls}`.
    - Existing assertions about `Start session` / `End session` text continue to match EN strings.
  </action>
  <verify>
    <automated>npx vitest run src/components/SessionControls.test.tsx 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "strings.endSession\\|strings.startSession" src/components/SessionControls.tsx` returns >= 2 (both branches).
    - `grep -c "'End session'\\|'Start session'" src/components/SessionControls.tsx` returns 0.
    - `grep -c "EN_STRINGS_FIXTURE" src/components/SessionControls.test.tsx` returns >= 1.
    - `npx vitest run src/components/SessionControls.test.tsx` exits 0.
  </acceptance_criteria>
  <done>
    SessionControls accepts strings; both branches translated; backwards-compat preserved for legacy callers via required prop (compile-time enforcement).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: StatsFooter accepts strings; Path A — translate at component layer (D-19 minimal-diff)</name>
  <files>src/components/StatsFooter.tsx, src/components/StatsFooter.test.tsx</files>
  <read_first>
    - src/components/StatsFooter.tsx (entire 61 lines — uses `formatSessionCount`, `formatTotalMinutes`, `formatLastSession` from storage)
    - src/storage/format.ts (lines 38, 59 — current EN formatters; NOT edited in this plan per D-19)
    - src/components/StatsFooter.test.tsx (existing fixtures)
    - src/content/strings.ts (Plan 01 — `UiStrings['stats']` with template-fn entries)
    - .planning/phases/19-language-switching/19-PATTERNS.md (lines 1067–1105 — StatsFooter Path A pattern)
  </read_first>
  <behavior>
    - `StatsFooterProps` gains `strings: UiStrings['stats']`.
    - Path A — translate at component layer per D-19 minimal-diff. The component computes its own translated strings using `strings.sessionsCount(stats.totalSessions)`, `strings.totalMinutes(stats.totalElapsedSeconds)`, and `strings.lastSessionPrefix(date, duration)`. The legacy `formatSessionCount` / `formatTotalMinutes` / `formatLastSession` helpers from `storage` are NO LONGER USED inside StatsFooter (verified by removing their imports).
    - `Reset` button text at line 56 becomes `{strings.reset}`.
    - `total` suffix at line 38 becomes `{strings.totalSuffix}`.
    - Last-session line composition: component reads `stats.lastSessionAtMs` + `stats.lastSessionDurationSeconds` directly. For date formatting, planner picks: (a) inline `new Date(ms).toLocaleDateString(locale)` (requires locale prop — adds complexity) OR (b) reuse the existing `formatLastSessionDate` helper from `format.ts` for the date portion only, then compose via `strings.lastSessionPrefix(date, duration)`. **Choose (b)**: simpler, single date format across locales (acceptable per D-11a — locale-aware date formatting deferred to v1.x carry-forward).
    - Token-bound classes + flex layout + 44×44 hit area preserved.
  </behavior>
  <action>
    Edit `src/components/StatsFooter.tsx`:
    1. Add `import type { UiStrings } from '../content/strings'`.
    2. Replace the imports from `'../storage'` (which currently include `formatSessionCount`, `formatTotalMinutes`, `formatLastSession`) with the subset needed: `import { formatLastSessionDate, formatLastSessionDuration } from '../storage/format'` (or whatever named exports exist for date/duration only — verify by `grep -n "export function" src/storage/format.ts`).
    3. Extend `StatsFooterProps` with `strings: UiStrings['stats']`.
    4. Destructure `strings` in the signature.
    5. Replace line 32 `const lastLine = formatLastSession(stats)` with a closure-scoped compose:
       - `const lastDate = stats.lastSessionAtMs !== null && stats.lastSessionDurationSeconds !== null ? formatLastSessionDate(stats.lastSessionAtMs) : null`
       - `const lastDuration = stats.lastSessionDurationSeconds !== null ? formatLastSessionDuration(stats.lastSessionDurationSeconds) : null`
       - `const lastLine = lastDate !== null && lastDuration !== null ? strings.lastSessionPrefix(lastDate, lastDuration) : null`
    6. Replace line 37–38 `{formatSessionCount(...)} · {formatTotalMinutes(...)} total` with `{strings.sessionsCount(stats.totalSessions)} · {strings.totalMinutes(stats.totalElapsedSeconds)} {strings.totalSuffix}`.
    7. Replace line 56 `Reset` literal with `{strings.reset}`.

    If `formatLastSessionDate` / `formatLastSessionDuration` are not separately exported from `src/storage/format.ts`, fall back to: keep using `formatLastSession(stats)` for the whole composed string but pass it as a single-string `lastLine` to render with `{lastLine && <span>{lastLine} ·</span>}` UNCHANGED. In that case, the only literal that changes is the `Reset` button text and the `total` suffix. Document the chosen path in the SUMMARY.

    Edit `src/components/StatsFooter.test.tsx`:
    - Add `EN_STRINGS_FIXTURE = UI_STRINGS.en` import.
    - Update fixtures to pass `strings={EN_STRINGS_FIXTURE.stats}`.
    - Existing assertions about `Reset` button + `sessions` + `min total` text continue to pass under EN strings.
  </action>
  <verify>
    <automated>npx vitest run src/components/StatsFooter.test.tsx 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "strings.sessionsCount\\|strings.totalMinutes\\|strings.totalSuffix\\|strings.reset" src/components/StatsFooter.tsx` returns >= 4.
    - `grep -c "'Reset'\\|>Reset<" src/components/StatsFooter.tsx` returns 0 (literal removed).
    - `npx vitest run src/components/StatsFooter.test.tsx` exits 0.
    - Token-bound classes preserved: `grep -c "min-h-\\[44px\\]" src/components/StatsFooter.tsx` >= 1.
    - `src/storage/format.ts` UNCHANGED (D-19 minimal-diff): `git diff src/storage/format.ts` is empty.
  </acceptance_criteria>
  <done>
    StatsFooter accepts strings; Path A — component layer translation; format.ts untouched per D-19; existing test assertions migrated to EN fixture.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: SessionReadout accepts strings prop</name>
  <files>src/components/SessionReadout.tsx, src/components/SessionReadout.test.tsx</files>
  <read_first>
    - src/components/SessionReadout.tsx (entire 89 lines — lines 22, 28, 33, 54, 63, 68, 73)
    - src/components/SessionReadout.test.tsx (existing fixtures)
    - src/content/strings.ts (Plan 01 — `UiStrings['readout']`)
    - .planning/phases/19-language-switching/19-PATTERNS.md (lines 979–1005 — SessionReadout mutation pattern)
  </read_first>
  <behavior>
    - `SessionReadoutProps` gains `strings: UiStrings['readout']`.
    - Line 22 placeholder `Elapsed`/`Remaining` ternary → `strings.elapsed` / `strings.remaining`.
    - Line 28 `aria-label="Session readout"` → `aria-label={strings.readoutAriaLabel}`.
    - Line 33 `aria-label="Session announcement"` → `aria-label={strings.announcementAriaLabel}`.
    - Line 54 `Elapsed`/`Remaining` timeLabel ternary → `strings.elapsed` / `strings.remaining`.
    - Line 63 + 68 aria-labels → `strings.readoutAriaLabel` / `strings.announcementAriaLabel`.
    - Line 73 message — if the current code receives `message` as a prop from upstream (which holds the literal `'Session complete'`), the prop value is what's rendered. The literal lives at the call site (App.tsx). Resolution: leave message prop as-is — Plan 08 (App.tsx) will pass `strings.readout.sessionComplete` instead of the inline `'Session complete'` literal.
  </behavior>
  <action>
    Edit `src/components/SessionReadout.tsx`:
    1. Add `import type { UiStrings } from '../content/strings'`.
    2. Extend `SessionReadoutProps` with `strings: UiStrings['readout']`.
    3. Destructure `strings` in signature.
    4. Replace the 7 inline literals at lines 22, 28, 33, 54, 63, 68 per the behavior list above.
    5. DO NOT touch line 73 — the `message` prop is rendered as-is.

    Edit `src/components/SessionReadout.test.tsx`:
    - Add `EN_STRINGS_FIXTURE = UI_STRINGS.en` import.
    - Update fixtures to pass `strings={EN_STRINGS_FIXTURE.readout}`.
    - Existing assertions about `Elapsed`, `Remaining`, `Session readout`, `Session announcement` text continue to match.
  </action>
  <verify>
    <automated>npx vitest run src/components/SessionReadout.test.tsx 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "strings.elapsed\\|strings.remaining" src/components/SessionReadout.tsx` returns >= 4 (two ternaries, each two branches).
    - `grep -c "strings.readoutAriaLabel\\|strings.announcementAriaLabel" src/components/SessionReadout.tsx` returns >= 4 (two aria-labels each).
    - `grep -c "Session readout\\|Session announcement" src/components/SessionReadout.tsx` returns 0 (literals removed; comments don't count).
    - `npx vitest run src/components/SessionReadout.test.tsx` exits 0.
  </acceptance_criteria>
  <done>
    SessionReadout accepts strings; 7 inline literals replaced; message prop pass-through preserved.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 5: BreathingShape + OrbShape + SquareShape + DiamondShape accept strings; Path A wedge for phase label</name>
  <files>src/components/BreathingShape.tsx, src/components/OrbShape.tsx, src/components/SquareShape.tsx, src/components/DiamondShape.tsx, src/components/BreathingShape.test.tsx, src/components/OrbShape.test.tsx, src/components/SquareShape.test.tsx, src/components/DiamondShape.test.tsx</files>
  <read_first>
    - src/components/BreathingShape.tsx (entire 40 lines — dispatcher pass-through)
    - src/components/OrbShape.tsx (entire 207 lines — focus on lines 37, 125 where `frame.phaseLabel` is consumed)
    - src/components/SquareShape.tsx (entire 220 lines — same pattern as OrbShape)
    - src/components/DiamondShape.tsx (entire 199 lines — same pattern as OrbShape)
    - src/content/strings.ts (Plan 01 — `UiStrings['breathing']`)
    - .planning/phases/19-language-switching/19-PATTERNS.md (lines 1138–1183 — BreathingShape Path A wedge for phase label)
    - .planning/phases/19-language-switching/19-CONTEXT.md (D-11a — stable-reference note for breathing strings prop)
  </read_first>
  <behavior>
    - `BreathingShapeProps` gains `strings: UiStrings['breathing']`. BreathingShape dispatches to OrbShape / SquareShape / DiamondShape and pass-through-drills `strings={strings}` to each.
    - OrbShape, SquareShape, DiamondShape Props gain `strings: UiStrings['breathing']`.
    - In each shape component, the existing usages of `frame.phaseLabel` (lines OrbShape:37, 125 and analogous in Square/Diamond) become a JSX-layer wedge: `const phaseLabel = frame.phase === 'in' ? strings.inhale : strings.exhale` (Path A — translate at JSX layer; do NOT edit `src/domain/sessionMath.ts` per D-18). The new `phaseLabel` const replaces every `frame.phaseLabel` consumption in the JSX.
    - aria-label at line 37 `Breathing shape: ${frame.phaseLabel}` becomes `${strings.breathingShapeAriaLabel}: ${phaseLabel}` (or equivalent translated string composition).
    - Lead-in countdown ARIA: if shape components reference `leadInDigit` with an aria-label, use `strings.leadInAriaLabel(digit)`. Otherwise no change.
    - Token-bound colors + clip-path + radial-gradient JSX completely UNCHANGED.
    - All shape test files update render fixtures to pass `strings={EN_STRINGS_FIXTURE.breathing}`; existing assertions about `In` / `Out` phase label text continue to match because EN `strings.inhale === 'In'` and `strings.exhale === 'Out'`.
  </behavior>
  <action>
    Edit `src/components/BreathingShape.tsx`:
    1. Add `import type { UiStrings } from '../content/strings'`.
    2. Extend the BreathingShape props interface with `strings: UiStrings['breathing']`.
    3. Pass-through-drill `strings={strings}` to each of OrbShape / SquareShape / DiamondShape in the variant-dispatch switch.

    For EACH of `src/components/OrbShape.tsx`, `src/components/SquareShape.tsx`, `src/components/DiamondShape.tsx`:
    1. Add `import type { UiStrings } from '../content/strings'`.
    2. Extend the Props interface with `strings: UiStrings['breathing']`.
    3. Destructure `strings`.
    4. Compute `const phaseLabel = frame.phase === 'in' ? strings.inhale : strings.exhale` (where `frame` exists; if the component does NOT receive `frame`, use whatever phase signal it already has — the executor must read each shape file in full to identify the source of `phaseLabel` and adapt).
    5. Replace every `{frame.phaseLabel}` occurrence with `{phaseLabel}`.
    6. Replace every `Breathing shape: ${frame.phaseLabel}` aria-label composition with `${strings.breathingShapeAriaLabel}: ${phaseLabel}` (use template-literal composition with the constant string prefix from `strings.breathingShapeAriaLabel`).
    7. If any lead-in aria-label exists referencing `leadInDigit`, change to `strings.leadInAriaLabel(leadInDigit)`.

    For each shape test file:
    - Add `EN_STRINGS_FIXTURE = UI_STRINGS.en` import.
    - Update render fixtures to pass `strings={EN_STRINGS_FIXTURE.breathing}`.
    - Existing assertions about `In` / `Out` rendered text continue to match.

    Stable-reference note (D-11a): App.tsx (Plan 08) MAY memoize `uiStrings.breathing` to avoid React reconciliation churn on non-locale-change renders. For THIS plan, no memoization is required — the prop is consumed at the leaf and React reconciles only on string-content change anyway.
  </action>
  <verify>
    <automated>npx vitest run src/components/BreathingShape.test.tsx src/components/OrbShape.test.tsx src/components/SquareShape.test.tsx src/components/DiamondShape.test.tsx 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "strings.inhale\\|strings.exhale" src/components/OrbShape.tsx src/components/SquareShape.tsx src/components/DiamondShape.tsx` returns >= 6 (each shape has at least 2 phase-label references).
    - `grep -c "frame.phaseLabel" src/components/OrbShape.tsx src/components/SquareShape.tsx src/components/DiamondShape.tsx` returns 0 (Path A wedge complete).
    - `grep -c "strings.breathingShapeAriaLabel" src/components/OrbShape.tsx src/components/SquareShape.tsx src/components/DiamondShape.tsx` returns >= 3.
    - `grep -c "strings={strings}" src/components/BreathingShape.tsx` returns >= 1 (pass-through drill to shape child).
    - `npx vitest run src/components/BreathingShape.test.tsx src/components/OrbShape.test.tsx src/components/SquareShape.test.tsx src/components/DiamondShape.test.tsx` exits 0.
    - `src/domain/sessionMath.ts` UNCHANGED (D-18): `git diff src/domain/sessionMath.ts` is empty.
  </acceptance_criteria>
  <done>
    BreathingShape pass-through-drills strings; OrbShape/SquareShape/DiamondShape consume strings.inhale/strings.exhale via Path A wedge; D-18 file-split invariant preserved (domain/sessionMath.ts untouched).
  </done>
</task>

<task type="auto">
  <name>Task 6: Per-commit green-gate (D-16)</name>
  <files>(no source edit — verification only)</files>
  <read_first>
    - .planning/phases/19-language-switching/19-CONTEXT.md (D-16, D-22)
  </read_first>
  <action>
    Run `npx tsc --noEmit && npm run lint && npm run build && npm test -- --run`. Same temporary App.tsx stop-gap note as Plan 06 applies — to keep tests green, the executor MAY need to add an `import { UI_STRINGS } from '../content/strings'` line to `src/app/App.tsx` and pass EN slices as required props for SettingsForm/SessionControls/StatsFooter/SessionReadout/BreathingShape until Plan 08 wires `useLocale()` properly. Document the stop-gap in the SUMMARY. After green, commit `feat(19-07): form + footer + controls + breathing accept strings slices` containing the 16 files in `files_modified` (and possibly the temporary App.tsx stop-gap line — if added, include `src/app/App.tsx` in the commit; otherwise leave App.tsx for Plan 08).
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run lint && npm run build && npm test -- --run 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - All 4 green-gate steps exit 0.
    - `npx vitest run src/styles/theme.no-hardcoded-classes.test.ts` exits 0 (THEME-UI-01).
    - Commit `feat(19-07): form + footer + controls + breathing accept strings slices` created.
    - `git diff HEAD~1 --stat` matches `files_modified` (plus possibly `src/app/App.tsx` if a stop-gap was applied).
    - `src/domain/sessionMath.ts` and `src/storage/format.ts` UNCHANGED (D-18 + D-19 minimal-diff invariants).
  </acceptance_criteria>
  <done>
    Green-gate passes; commit landed; D-18 + D-19 invariants preserved.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Template-fn entry call (e.g. strings.decreaseLabel(label)) | Function consumes a per-field label string; output is rendered in aria-label only. |
| App.tsx → BreathingShape strings prop | Render-only string fields; no DOM event chain. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-18 | Tampering | strings template-fn signatures | mitigate | Plan 01 strings.test.ts exhaustiveness covers function-typed entries; TypeScript surface narrows signatures. |
| T-19-19 | Information Disclosure | aria-label strings | accept | aria-labels are user-facing UI; translation is the requirement. |
</threat_model>

<verification>
- `npx tsc --noEmit && npm run lint && npm run build && npm test -- --run` exits 0.
- `npx vitest run src/styles/theme.no-hardcoded-classes.test.ts` exits 0 (D-22).
- `src/domain/sessionMath.ts` and `src/storage/format.ts` UNCHANGED (D-18 + D-19).
- 16 component files modified (8 source + 8 test); App.tsx MAY contain a temporary EN-fixture stop-gap until Plan 08 wires useLocale().
</verification>

<success_criteria>
1. SettingsForm + SettingsStepper accept strings; D-15 template-fn aria-labels.
2. SessionControls accepts strings; both layout branches translated.
3. StatsFooter accepts strings (Path A — component-layer translation; format.ts unchanged).
4. SessionReadout accepts strings; 7 aria-labels + ternary labels translated.
5. BreathingShape + OrbShape + SquareShape + DiamondShape accept strings; Path A wedge for phase label (frame.phaseLabel no longer referenced).
6. D-18 + D-19 invariants preserved (sessionMath.ts + format.ts not edited).
7. Green-gate exits 0.
</success_criteria>

<output>
After completion, create `.planning/phases/19-language-switching/19-07-SUMMARY.md` capturing: 16 files modified, per-component strings-slice mapping table, confirmation of Path A choice for StatsFooter + BreathingShape, whether App.tsx stop-gap was needed, whether `formatLastSessionDate` / `formatLastSessionDuration` are separately exported in format.ts (if not, document the StatsFooter compromise path chosen).
</output>
