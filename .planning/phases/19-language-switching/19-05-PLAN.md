---
phase: 19-language-switching
plan: 05
type: execute
wave: 2
depends_on: ["19-01", "19-02"]
files_modified:
  - src/components/LanguagePicker.tsx
  - src/components/LanguagePicker.test.tsx
autonomous: true
requirements:
  - I18N-01
  - I18N-02
must_haves:
  truths:
    - "LanguagePicker renders a 2-option radiogroup with native endonym labels (English, Português (Brasil))"
    - "Native endonyms render identically regardless of current UI locale (D-14)"
    - "Selecting an option writes to disk via savePrefs and dispatches hrv:prefs-changed with detail.key === 'locale'"
    - "When disabled=true, both buttons are disabled and the radiogroup has aria-disabled=true; clicks do not write"
    - "Picker chrome uses only var(--color-breathing-*) tokens — zero hardcoded text-slate / bg-teal / text-white classes (D-22)"
    - "Picker honors 44×44 hit area + focus-visible ring (D-23)"
  artifacts:
    - path: "src/components/LanguagePicker.tsx"
      provides: "real radiogroup body filling the Phase 15 stub"
      contains: "role=\"radiogroup\""
    - path: "src/components/LanguagePicker.test.tsx"
      provides: "radiogroup posture + endonym + save + dispatch + disabled-gate tests"
      contains: "Português (Brasil)"
  key_links:
    - from: "src/components/LanguagePicker.tsx"
      to: "src/hooks/useLocaleChoice.ts"
      via: "useLocaleChoice() picker setter"
      pattern: "useLocaleChoice"
    - from: "src/components/LanguagePicker.tsx"
      to: "src/content/strings.ts"
      via: "LOCALE_DISPLAY_NAMES import for native endonyms"
      pattern: "LOCALE_DISPLAY_NAMES"
    - from: "src/components/LanguagePicker.tsx"
      to: "src/domain/settings.ts"
      via: "LOCALE_OPTIONS + LocaleId imports"
      pattern: "LOCALE_OPTIONS"
---

<objective>
Replace the Phase 15 read-only stub body of `src/components/LanguagePicker.tsx` (current lines 18–28) with a real radiogroup mirror of `TimbrePicker.tsx`. Use `useLocaleChoice` (Plan 02) for state + write path. Render native endonyms from `LOCALE_DISPLAY_NAMES` (Plan 01) so labels are identical in EN and PT-BR UIs per D-14. Keep the `{ disabled: boolean }` prop contract (Phase 15 D-02 + Phase 19 D-20). Extend `LanguagePicker.test.tsx` from the Phase 15 stub coverage (3 read-only tests) to a Phase 19 radiogroup test suite mirroring `ThemePicker.test.tsx`.

Purpose: This is the user-visible Phase 19 affordance. After this plan lands, the SettingsDialog contains a working language switcher even before the App.tsx wiring (Plan 08) plumbs the resolved strings through the rest of the UI.

Output: Both files updated; all radiogroup + endonym + disabled-gate tests pass.
</objective>

<execution_context>
@$HOME/.claude-personal-personal/get-shit-done/workflows/execute-plan.md
@$HOME/.claude-personal-personal/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/19-language-switching/19-CONTEXT.md
@.planning/phases/19-language-switching/19-PATTERNS.md
@.planning/phases/19-language-switching/19-01-SUMMARY.md
@.planning/phases/19-language-switching/19-02-SUMMARY.md

<interfaces>
From src/components/TimbrePicker.tsx (verbatim mirror target — entire 62 lines; selected-class strings, base-class string, role=radiogroup/role=radio posture, min-h-12 hit area, focus-visible ring chain).

From src/components/LanguagePicker.tsx (current stub — lines 14–28; PropTypes signature `{ disabled: boolean }` preserved):
```typescript
export interface LanguagePickerProps {
  disabled: boolean
}
export function LanguagePicker({ disabled }: LanguagePickerProps) { ... }
```

From src/domain/settings.ts (line 85):
```typescript
export const LOCALE_OPTIONS = ['en', 'pt-BR'] as const satisfies readonly LocaleId[]
```

From src/content/strings.ts (Plan 01 output):
```typescript
export const LOCALE_DISPLAY_NAMES: Readonly<Record<LocaleId, string>>
// LOCALE_DISPLAY_NAMES.en === 'English'
// LOCALE_DISPLAY_NAMES['pt-BR'] === 'Português (Brasil)'
```

From src/hooks/useLocaleChoice.ts (Plan 02 output):
```typescript
export function useLocaleChoice(): { locale: LocaleId; setLocale: (next: LocaleId) => void }
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Replace LanguagePicker.tsx body with real radiogroup mirroring TimbrePicker</name>
  <files>src/components/LanguagePicker.tsx</files>
  <read_first>
    - src/components/LanguagePicker.tsx (current stub — entire 28 lines)
    - src/components/TimbrePicker.tsx (verbatim mirror source — entire 62 lines)
    - src/content/strings.ts (Plan 01 — LOCALE_DISPLAY_NAMES export)
    - src/hooks/useLocaleChoice.ts (Plan 02 — useLocaleChoice export)
    - src/domain/settings.ts (line 85 — LOCALE_OPTIONS)
    - .planning/phases/19-language-switching/19-PATTERNS.md (lines 441–513 — LanguagePicker pattern)
    - .planning/phases/19-language-switching/19-CONTEXT.md (D-14 native endonyms + D-20 prop contract + D-22 token-binding + D-23 a11y floor)
  </read_first>
  <behavior>
    - `LanguagePicker` accepts `{ disabled: boolean }` only (D-20 — no `strings` prop, no `sectionLabel` prop in this plan; section label stays the hardcoded literal `'Language'` until App-side prop drill plumbs `uiStrings.settings.languageLabel`. NOTE: SettingsDialog wiring of a translatable section label happens in Plan 06 Task 7 (LanguagePicker `sectionLabel` widening + SettingsDialog drill). For this plan we keep `'Language'` hardcoded — same as Phase 15 stub — so the picker is self-contained while the App-side wiring catches up).

      Resolution per D-20: the picker's section label stays the literal `Language` (matches Phase 15 D-18 hardcode + Phase 18 D-16 picker-self-contained rule). The plumbed section label is rolled into Plan 06 Task 7 (LanguagePicker `sectionLabel` widening + SettingsDialog drill), not Plan 05. This keeps the picker decoupled from upstream catalogs and mirrors how TimbrePicker hardcodes `'Timbre'` despite Phase 18 D-12 translation requirements (option-names translation happens later via App-side prop drill).

      To match the Phase 19 design strictly: option-button labels in LanguagePicker come from `LOCALE_DISPLAY_NAMES[id]` (NOT translated — D-14 native endonyms). The "section label translates while option labels stay native" split is intentional per D-12 + D-14.
    - Section label `Language` renders (hardcoded literal at this plan boundary; translated in Plan 07 via App-side wiring).
    - 2 option buttons render with `LOCALE_DISPLAY_NAMES[id]` labels: `English` for `'en'`, `Português (Brasil)` for `'pt-BR'`.
    - Native endonyms are SAME in BOTH UI locales (D-14 — labels do not flow through `UI_STRINGS`).
    - Selected option has `aria-checked="true"`; others `aria-checked="false"`.
    - `disabled` prop disables all option buttons + sets `aria-disabled={disabled}` on the radiogroup container.
    - Clicking an enabled option calls `setLocale(id)` from `useLocaleChoice`.
    - Token-binding (D-22): zero hardcoded `text-slate-*` / `bg-teal-*` / `text-white` / `bg-white` classes. Uses `var(--color-breathing-*)` tokens verbatim from TimbrePicker.
    - A11y (D-23): `min-h-12` hit area, `focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-breathing-accent focus-visible:ring-offset-2`, `motion-reduce:transition-none`.
  </behavior>
  <action>
    Edit `src/components/LanguagePicker.tsx` in place. Steps:

    1. Update the header comment block (lines 1–10) to describe Phase 19 body fill. Cite Phase 19 I18N-01..02 + D-08 useLocaleChoice owner + D-14 native endonyms + D-20 prop contract + D-22 token-binding + D-23 a11y floor.
    2. Replace the import `import { loadPrefs } from '../storage/prefs'` (line 12) with the Plan 02 + Plan 01 imports:
       - `import { LOCALE_OPTIONS, type LocaleId } from '../domain/settings'`
       - `import { useLocaleChoice } from '../hooks/useLocaleChoice'`
       - `import { LOCALE_DISPLAY_NAMES } from '../content/strings'`
    3. Keep the `LanguagePickerProps` interface signature unchanged (just `{ disabled: boolean }` — D-20).
    4. Replace the function body with the radiogroup mirror of TimbrePicker.tsx:18–62:
       - `const { locale, setLocale } = useLocaleChoice()`
       - Outer `<div>` with no className.
       - Section label `<p id="language-picker-label" className="text-sm font-semibold text-[var(--color-breathing-accent-strong)]">Language</p>` (hardcoded literal per the rationale above; Plan 06 Task 7 will replace with `{sectionLabel}` (driven by SettingsDialog drill of `strings.settings.languageLabel`)).
       - Container `<div role="radiogroup" aria-labelledby="language-picker-label" aria-disabled={disabled} className="mt-2 grid grid-cols-2 gap-2">`. Use `grid-cols-2` for horizontal layout of 2 options (matches TimbrePicker geometry for 4 options halved).
       - `.map()` over `LOCALE_OPTIONS`, returning a `<button>` per id. For each button: `key={id}`, `type="button"`, `role="radio"`, `aria-checked={selected}`, `disabled={disabled}`, `onClick={() => { setLocale(id) }}`, `className={...selected ? selectedClasses : unselectedClasses}`.
       - Inside the button body: `const label = LOCALE_DISPLAY_NAMES[id]` (NOT the id-capitalization fallback used in TimbrePicker).
       - Reuse the exact `baseClasses`, `selectedClasses`, `unselectedClasses` strings from TimbrePicker.tsx:41–43 verbatim — D-22 token-binding is verbatim TimbrePicker chrome.
    5. NO new dependencies, NO new tokens, NO new className utilities. The diff against TimbrePicker.tsx should be exactly: imports (LOCALE_OPTIONS/useLocaleChoice/LOCALE_DISPLAY_NAMES instead of TIMBRE_OPTIONS/useTimbreChoice), label source (`LOCALE_DISPLAY_NAMES[id]` instead of `id.charAt(0)...`), section label text (`Language` instead of `Timbre`), section-label `id` attribute (`language-picker-label` instead of `timbre-picker-label`), state field rename (`locale` / `setLocale` instead of `timbre` / `setTimbre`).
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -10 && grep -c "LOCALE_DISPLAY_NAMES" src/components/LanguagePicker.tsx && grep -c "role=\"radio" src/components/LanguagePicker.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/LanguagePicker.tsx` no longer imports `loadPrefs` directly (picker self-contains via `useLocaleChoice`).
    - `grep -c "useLocaleChoice" src/components/LanguagePicker.tsx` returns >= 2 (import + invocation).
    - `grep -c "LOCALE_DISPLAY_NAMES" src/components/LanguagePicker.tsx` returns >= 2 (import + label lookup).
    - `grep -c "role=\"radio" src/components/LanguagePicker.tsx` returns 2 (`role="radiogroup"` and `role="radio"`).
    - `grep -c "aria-checked" src/components/LanguagePicker.tsx` returns 1.
    - `grep -c "aria-disabled={disabled}" src/components/LanguagePicker.tsx` returns 1.
    - `grep -c "min-h-12" src/components/LanguagePicker.tsx` returns 1 (D-23 a11y floor).
    - `grep -c "focus-visible:ring-breathing-accent" src/components/LanguagePicker.tsx` returns 1.
    - `grep -E "text-slate-|bg-slate-|text-teal-|bg-teal-|border-teal-|text-white|bg-white" src/components/LanguagePicker.tsx` matches NOTHING (D-22 — zero hardcoded utility colors).
    - `npx vitest run src/styles/theme.no-hardcoded-classes.test.ts` exits 0 (THEME-UI-01 guard stays green).
    - `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>
    `LanguagePicker.tsx` body is a radiogroup mirroring TimbrePicker; native endonyms from LOCALE_DISPLAY_NAMES; useLocaleChoice owns state + write; zero hardcoded utility-color classes; THEME-UI-01 guard green.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Rewrite LanguagePicker.test.tsx for the radiogroup body</name>
  <files>src/components/LanguagePicker.test.tsx</files>
  <read_first>
    - src/components/LanguagePicker.test.tsx (current Phase 15 stub tests — entire 30 lines)
    - src/components/ThemePicker.test.tsx (radiogroup test pattern — entire file; mirror target)
    - src/components/LanguagePicker.tsx (the file just rewritten in Task 1)
    - .planning/phases/19-language-switching/19-PATTERNS.md (lines 517–585 — LanguagePicker.test.tsx pattern)
    - .planning/phases/19-language-switching/19-CONTEXT.md (D-14 — native endonyms in BOTH UIs)
  </read_first>
  <behavior>
    - The Phase 15 stub tests are REPLACED (the read-only `Language: en` text no longer exists; the body is now a radiogroup).
    - Test 1: renders the section label `Language`.
    - Test 2: renders exactly 2 radio buttons with labels `English` and `Português (Brasil)` (D-14 native endonyms).
    - Test 3: `aria-checked` reflects the stored locale — seeded `'pt-BR'` makes the PT-BR button `aria-checked="true"` and the EN button `aria-checked="false"`.
    - Test 4: clicking an enabled option writes the new locale to disk via savePrefs (verify by reading STATE_KEY).
    - Test 5: clicking an enabled option dispatches `hrv:prefs-changed` with `detail.key === 'locale'` and `detail.value === <new locale>`.
    - Test 6: when `disabled=true`, both buttons have `disabled` attribute AND the radiogroup has `aria-disabled="true"`.
    - Test 7: when `disabled=true`, clicking does NOT write to disk.
    - Test 8: selected option retains its `aria-checked="true"` highlight even when `disabled=true`.
    - Test 9 (D-14 cross-UI endonym invariant): seed `locale: 'pt-BR'`, render picker, assert the rendered button labels are STILL `English` and `Português (Brasil)` (endonyms do not flow through UI_STRINGS).
    - All tests pass.
  </behavior>
  <action>
    Replace the contents of `src/components/LanguagePicker.test.tsx` (current 30-line stub) with a new test suite modeled after `ThemePicker.test.tsx`:

    Imports:
    - `'@testing-library/jest-dom/vitest'`
    - `{ render, screen }` from `'@testing-library/react'`
    - `userEvent` from `'@testing-library/user-event'`
    - `{ afterEach, beforeEach, describe, expect, it, vi }` from `'vitest'`
    - `{ LanguagePicker }` from `'./LanguagePicker'`
    - `{ STATE_KEY }` from `'../storage'`
    - `type { LocaleId }` from `'../domain/settings'`

    Helper at top: `function seedLocale(locale: LocaleId): void` writing `{ version: 1, prefs: { theme: 'system', timbre: 'bowl', variant: 'orb', locale } }` to localStorage (mirror ThemePicker.test.tsx:11–17).

    `beforeEach` / `afterEach`: `window.localStorage.clear()` + `vi.restoreAllMocks()` (mirror ThemePicker.test.tsx:19–26).

    `describe('LanguagePicker — real radiogroup picker (Phase 19)')` containing 9 tests per the behavior list above. Use `userEvent.setup()` + `await user.click(...)` for write/dispatch tests. For the dispatch assertion (Test 5), spy on `window.addEventListener('hrv:prefs-changed', spy)`, click PT-BR button, assert `event.detail.key === 'locale'` and `event.detail.value === 'pt-BR'`.

    For Test 9 (D-14 cross-UI endonym invariant), the seeded locale doesn't change what labels render — the test just renders with seeded `'pt-BR'` and asserts `screen.getByRole('radio', { name: 'English' })` AND `screen.getByRole('radio', { name: 'Português (Brasil)' })` are both present.

    Use `// eslint-disable-next-line ...` with `// Reason:` annotations on the 1–2 non-null assertions needed (mirror ThemePicker.test.tsx:62–65, 102–104).
  </action>
  <verify>
    <automated>npx vitest run src/components/LanguagePicker.test.tsx 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/components/LanguagePicker.test.tsx` exits 0.
    - Output reports at least 9 passing tests.
    - `grep -c "it(" src/components/LanguagePicker.test.tsx` returns >= 9.
    - `grep -c "Português (Brasil)" src/components/LanguagePicker.test.tsx` returns >= 3 (used in Tests 2, 3, 5, 9 at minimum).
    - `grep -c "English" src/components/LanguagePicker.test.tsx` returns >= 2.
    - `grep -c "detail.key.*'locale'" src/components/LanguagePicker.test.tsx` returns 1 (Test 5 dispatch assertion).
    - `grep -c "Language: en" src/components/LanguagePicker.test.tsx` returns 0 (Phase 15 stub assertion removed).
    - `grep -c "aria-disabled.*true" src/components/LanguagePicker.test.tsx` returns >= 1 (Test 6 disabled-gate).
    - Test 9 asserts both endonyms present when seeded with `pt-BR` locale (D-14 invariant).
  </acceptance_criteria>
  <done>
    Phase 15 stub tests removed; 9 Phase 19 radiogroup tests pass; D-14 cross-UI endonym invariant covered.
  </done>
</task>

<task type="auto">
  <name>Task 3: Per-commit green-gate (D-16)</name>
  <files>(no source edit — verification only)</files>
  <read_first>
    - .planning/phases/19-language-switching/19-CONTEXT.md (D-16)
  </read_first>
  <action>
    Run `npx tsc --noEmit && npm run lint && npm run build && npm test -- --run`. After exit 0, commit `feat(19-05): LanguagePicker radiogroup body + endonym tests` containing only `src/components/LanguagePicker.tsx` and `src/components/LanguagePicker.test.tsx`.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run lint && npm run build && npm test -- --run 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - All 4 green-gate steps exit 0.
    - Commit `feat(19-05): LanguagePicker radiogroup body + endonym tests` created.
    - `git diff HEAD~1 --stat` shows only 2 files.
    - `npx vitest run src/styles/theme.no-hardcoded-classes.test.ts` exits 0 (THEME-UI-01 still green).
  </acceptance_criteria>
  <done>
    Green-gate passes; commit landed; THEME-UI-01 token-binding guard remains green.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user click → setLocale → savePrefs | User input crosses into storage. Trust input shape: `id` is enum-narrowed from `LOCALE_OPTIONS.map(...)` callback parameter (TypeScript `LocaleId`). |
| user click → hrv:prefs-changed dispatch | Same-origin window event with `{ key: 'locale', value: LocaleId }` payload. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-13 | Tampering | onClick → setLocale handler | mitigate | `id` parameter is `LocaleId` enum-narrowed by `LOCALE_OPTIONS.map((id: LocaleId) => ...)`. Phase 14 D-17 coerceLocale defends downstream reads. |
| T-19-14 | Elevation of Privilege | disabled prop bypass | mitigate | Button `disabled` attribute is set + `aria-disabled` on container; Test 7 + Test 8 cover the bypass attempt (click while disabled does not write). |
| T-19-15 | Information Disclosure | Native endonyms in cross-locale UI | accept | D-14 explicitly chooses native endonyms in both UI locales — standard i18n convention; not an information-disclosure concern. |
</threat_model>

<verification>
- `npx tsc --noEmit && npm run lint && npm run build && npm test -- --run` exits 0.
- `npx vitest run src/components/LanguagePicker.test.tsx` shows >= 9 passing tests.
- `npx vitest run src/styles/theme.no-hardcoded-classes.test.ts` exits 0 (THEME-UI-01 — D-22).
- LanguagePicker.tsx contains zero hardcoded utility-color classes (D-22 grep negation).
- LanguagePicker.tsx imports `useLocaleChoice` (Plan 02) and `LOCALE_DISPLAY_NAMES` (Plan 01).
- No edit to `src/components/SettingsDialog.tsx` (D-19 invariant — that change is in Plan 06 or Plan 07).
</verification>

<success_criteria>
1. LanguagePicker body is a 2-option radiogroup mirroring TimbrePicker chrome verbatim per D-22.
2. Native endonyms (`English`, `Português (Brasil)`) come from `LOCALE_DISPLAY_NAMES` (Plan 01).
3. Endonyms are identical in BOTH UI locales per D-14 (Test 9 covers this).
4. `useLocaleChoice` owns state + savePrefs write + CustomEvent dispatch.
5. `disabled` prop disables buttons and radiogroup container.
6. THEME-UI-01 token-binding guard remains green.
7. Green-gate exits 0 after the commit.
</success_criteria>

<output>
After completion, create `.planning/phases/19-language-switching/19-05-SUMMARY.md` capturing: 2 files modified, test count (9), confirmation that the picker section label `Language` remains hardcoded for this plan (translation deferred to Plan 06 Task 7 (sectionLabel widening via SettingsDialog drill)), THEME-UI-01 guard status.
</output>
