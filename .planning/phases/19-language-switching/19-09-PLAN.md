---
phase: 19-language-switching
plan: 09
type: execute
wave: 5
depends_on: ["19-08"]
files_modified:
  - src/app/App.locale.test.tsx
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
  - .planning/phases/19-language-switching/19-SUMMARY.md
  - .planning/phases/19-language-switching/19-UAT.md
autonomous: false
requirements:
  - I18N-01
  - I18N-02
  - I18N-03
  - I18N-07
must_haves:
  truths:
    - "App-level locale switch test asserts document.documentElement.lang === 'pt-BR' after clicking PT-BR option"
    - "App-level locale switch test asserts PT-BR idle-state strings render after the switch"
    - "Manual UAT step covers mid-session cross-tab locale swap (I18N-02 loop unaffected)"
    - "REQUIREMENTS.md flips I18N-01..07 from Pending to Done"
    - "ROADMAP.md flips Phase 19 to complete"
    - "STATE.md records v1.1 close candidate"
  artifacts:
    - path: "src/app/App.locale.test.tsx"
      provides: "Phase 19 integration smoke for locale switching"
      contains: "documentElement.lang"
    - path: ".planning/phases/19-language-switching/19-SUMMARY.md"
      provides: "phase outcomes + deviations"
    - path: ".planning/phases/19-language-switching/19-UAT.md"
      provides: "manual UAT log with cross-tab + PT-BR spot-check items"
  key_links:
    - from: "src/app/App.locale.test.tsx"
      to: "src/app/App.tsx"
      via: "render(<App />) integration smoke"
      pattern: "render\\(<App"
    - from: ".planning/REQUIREMENTS.md"
      to: ".planning/phases/19-language-switching/19-SUMMARY.md"
      via: "I18N-01..07 traceability flips"
      pattern: "I18N-0[1-7].*Done"
---

<objective>
Add a Phase 19 App-level integration test (`src/app/App.locale.test.tsx`) covering: (a) `document.documentElement.lang` write on mount, (b) clicking the PT-BR LanguagePicker option re-renders idle-state strings + writes `document.documentElement.lang === 'pt-BR'`, (c) the breath loop stays uninterrupted by a locale switch (rendered via mid-session cross-tab `'storage'` event injection, since the picker itself is disabled in-session). Add a manual UAT log capturing the cross-tab mid-session scenario (deferred-to-human per VALIDATION.md Manual-Only table). Flip REQUIREMENTS.md I18N-01..07 from Pending to Done. Update ROADMAP.md + STATE.md. Produce `19-SUMMARY.md` with phase outcomes + deviations + total test count.

Purpose: Close the phase. Validate the headline I18N-01..03 + I18N-07 success criteria via automated test + manual UAT, then update planning artifacts so the v1.1 milestone can be audited.

Output: 1 new test file + 4 updated planning files + 2 new phase artifacts (SUMMARY + UAT).
</objective>

<execution_context>
@$HOME/.claude-personal-personal/get-shit-done/workflows/execute-plan.md
@$HOME/.claude-personal-personal/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-language-switching/19-CONTEXT.md
@.planning/phases/19-language-switching/19-VALIDATION.md
@.planning/phases/19-language-switching/19-01-SUMMARY.md
@.planning/phases/19-language-switching/19-02-SUMMARY.md
@.planning/phases/19-language-switching/19-03-SUMMARY.md
@.planning/phases/19-language-switching/19-04-SUMMARY.md
@.planning/phases/19-language-switching/19-05-SUMMARY.md
@.planning/phases/19-language-switching/19-06-SUMMARY.md
@.planning/phases/19-language-switching/19-07-SUMMARY.md
@.planning/phases/19-language-switching/19-08-SUMMARY.md

<interfaces>
From src/app/App.tsx (Plan 08):
- `useLocale()` invoked; locale prefs hydrate from `loadPrefs().locale`.
- SettingsDialog opens via the SettingsAnchor gear control.
- LanguagePicker (inside SettingsDialog) renders 2 radio buttons: `English` and `Português (Brasil)`.
- Selecting PT-BR triggers `useLocaleChoice.setLocale('pt-BR')` → savePrefs + dispatch.
- useLocale picks up the event, re-reads, writes `documentElement.lang`.

Existing App test files for posture reference: src/app/App.settings.test.tsx, src/app/App.session.test.tsx, src/app/App.dialog.test.tsx (use `render(<App />)` + helpers).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create src/app/App.locale.test.tsx with locale-switch integration smoke</name>
  <files>src/app/App.locale.test.tsx</files>
  <read_first>
    - src/app/App.settings.test.tsx (pattern target: render(<App />) + helpers + beforeEach)
    - src/app/App.tsx (Plan 08 final — locale wiring at top of component)
    - src/app/App.session.test.tsx (pattern for session-state assertions if needed)
    - src/content/strings.ts (Plan 01 — to derive expected PT-BR labels for assertions)
    - .planning/phases/19-language-switching/19-PATTERNS.md (lines 1262–1301 — App-level smoke test pattern)
    - .planning/phases/19-language-switching/19-VALIDATION.md (Per-Task Verification Map — Wave 4 + Wave 5 integration test entries)
  </read_first>
  <behavior>
    - Test 1 (`writes documentElement.lang on mount with seeded EN locale`): seed `prefs.locale = 'en'`, render `<App />`, assert `document.documentElement.lang === 'en'`.
    - Test 2 (`writes documentElement.lang on mount with seeded PT-BR locale`): seed `prefs.locale = 'pt-BR'`, render `<App />`, assert `document.documentElement.lang === 'pt-BR'`.
    - Test 3 (`switches UI strings + documentElement.lang when LanguagePicker PT-BR is clicked`): seed EN, render, open SettingsDialog via gear, click PT-BR radio, assert `document.documentElement.lang === 'pt-BR'` AND assert at least one PT-BR idle-state string is rendered (e.g. the translated Start-session button label OR the translated settings title — fetch the expected text from `UI_STRINGS['pt-BR'].controls.startSession` or `.settings.title`).
    - Test 4 (`cross-tab storage event updates locale even when LanguagePicker is not interacted with`): seed EN, render, simulate cross-tab write by setting STATE_KEY to `'pt-BR'` envelope and dispatching `StorageEvent('storage', { key: STATE_KEY })`, assert `document.documentElement.lang === 'pt-BR'`.
    - Test 5 (`locale picker is disabled while inSessionView`): seed EN, render, start a session (click Start), then attempt to open SettingsDialog (note: per Phase 15 the gear may auto-close the dialog mid-session — adjust assertions to mirror existing Settings test behavior). Confirm the LanguagePicker buttons have `disabled` attribute when rendered while session is running.
    - All tests pass under `npx vitest run src/app/App.locale.test.tsx`.
  </behavior>
  <action>
    Create `src/app/App.locale.test.tsx` (NEW). Imports + setup:
    - `'@testing-library/jest-dom/vitest'`
    - `{ render, screen, act }` from `'@testing-library/react'`
    - `userEvent` from `'@testing-library/user-event'`
    - `{ afterEach, beforeEach, describe, expect, it }` from `'vitest'`
    - `{ App }` from `'./App'` (verify default vs named export by reading App.tsx export line — adjust import accordingly)
    - `{ STATE_KEY }` from `'../storage'`
    - `{ UI_STRINGS }` from `'../content/strings'`
    - `type { UserPrefs }` from `'../storage/prefs'`

    Helper at top: `function seedPrefs(prefs: UserPrefs): void` writing `{ version: 1, prefs }` to localStorage.

    Constant: `const DEFAULT_FULL_PREFS: UserPrefs = { theme: 'system', timbre: 'bowl', variant: 'orb', locale: 'en' }`.

    `beforeEach`: `window.localStorage.clear(); document.documentElement.lang = ''`.
    `afterEach`: `window.localStorage.clear(); document.documentElement.lang = ''`.

    `describe('App locale switching (Phase 19)')` containing 5 tests per the behavior list.

    For Test 3 specifically (interactive picker click):
    - `const user = userEvent.setup()`
    - `render(<App />)`
    - Open SettingsDialog: `await user.click(screen.getByLabelText(UI_STRINGS.en.anchors.settings))` (target the gear via translated aria-label — should be 'Settings' when seeded EN).
    - Find PT-BR option: `const ptBrButton = screen.getByRole('radio', { name: 'Português (Brasil)' })` — D-14 native endonym.
    - Click: `await user.click(ptBrButton)`.
    - Assert: `expect(document.documentElement.lang).toBe('pt-BR')`.
    - Assert PT-BR idle string: `expect(screen.getByRole('button', { name: UI_STRINGS['pt-BR'].controls.startSession })).toBeInTheDocument()` (the Start-session button is always present in idle view; use the role + accessible-name query).

    For Test 4 (cross-tab):
    - Render with seeded EN, get `result` from `render(<App />)`.
    - `act(() => { window.localStorage.setItem(STATE_KEY, JSON.stringify({ version: 1, prefs: { ...DEFAULT_FULL_PREFS, locale: 'pt-BR' } })); window.dispatchEvent(new StorageEvent('storage', { key: STATE_KEY, newValue: '...', oldValue: null })) })`.
    - Assert `document.documentElement.lang === 'pt-BR'`.

    For Test 5 (in-session disabled):
    - Render with seeded EN, render `<App />`, click Start (use translated label `UI_STRINGS.en.controls.startSession`).
    - Then attempt to open SettingsDialog via the gear; if Phase 15 auto-closes the dialog mid-session, this test will be hard to write. Alternative: seed prefs + render `<App />` while session is mock-running, query the LanguagePicker buttons indirectly. PLANNER NOTE: this test may end up being a unit-level assertion on the disabled prop value rather than a full E2E flow. The executor decides based on the App-test surface; if the in-session disable scenario is too brittle, document a SKIP with reference to the manual UAT step in Task 2.

    Mark any `// eslint-disable-next-line` annotations with `// Reason:` per Phase 7 D-04 if needed.
  </action>
  <verify>
    <automated>npx vitest run src/app/App.locale.test.tsx 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `src/app/App.locale.test.tsx` exists.
    - `npx vitest run src/app/App.locale.test.tsx` exits 0.
    - Output reports at least 4 passing tests (5 if the in-session disabled test was implementable).
    - `grep -c "documentElement.lang" src/app/App.locale.test.tsx` returns >= 6 (resetters + assertions).
    - `grep -c "Português (Brasil)" src/app/App.locale.test.tsx` returns >= 1 (Test 3 endonym query).
    - `grep -c "StorageEvent" src/app/App.locale.test.tsx` returns >= 1 (Test 4 cross-tab).
    - `grep -c "UI_STRINGS\\['pt-BR'\\]\\|UI_STRINGS\\[\"pt-BR\"\\]" src/app/App.locale.test.tsx` returns >= 1.
  </acceptance_criteria>
  <done>
    `App.locale.test.tsx` covers seed + locale-switch click + cross-tab storage paths; documentElement.lang assertions pass. **Commit:** after the test passes, commit `test(19-09): App-level locale-switch integration smoke` containing ONLY `src/app/App.locale.test.tsx`. This is a SEPARATE commit from Task 3's docs-only commit. Per D-16, each commit boundary runs `npx tsc --noEmit && npm run lint && npm run build && npm test -- --run` and exits 0.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Manual UAT — mid-session cross-tab locale swap + PT-BR translation spot-check</name>
  <files>.planning/phases/19-language-switching/19-UAT.md</files>
  <what-built>
    - Phase 19 language switching is fully wired through SettingsDialog.
    - LanguagePicker offers English + Português (Brasil) options with native endonyms.
    - Picker is disabled while a session is running (in-session contract).
    - Locale persists across reloads via `Envelope.prefs.locale`.
    - `document.documentElement.lang` reflects the active locale.
    - Locked claim-safe copy (inspiredByForrest + medicalAdviceLine + affiliationLine) routes through PT-BR via LOCKED_COPY['pt-BR'].
    - PT-BR translation strings are marked `// TODO: native-speaker review` per I18N-07.
  </what-built>
  <how-to-verify>
    Two open dev-server tabs (`npm run dev`):

    UAT-1 (Cross-tab mid-session locale swap — I18N-02 loop-unaffected):
    1. In Tab A, click Start to begin a session at any BPM.
    2. In Tab B (same origin), open SettingsDialog and switch language to Português (Brasil).
    3. Return to Tab A and observe the breathing ring — assert that the In/Out phase labels swap to PT-BR (`Entra` / `Sai` or whatever translation strings Plan 01 selected), AND that the breath timing visually continues without phase glitches, jumps, or stutters.
    4. If the breath loop pauses, jumps, or visibly stutters during the swap → FAIL; record details.
    5. If labels swap atomically without timing disruption → PASS.

    UAT-2 (PT-BR translation spot-check — I18N-07 carry-forward):
    1. In a fresh tab, switch language to Português (Brasil) via SettingsDialog.
    2. Read through every visible UI surface in idle state: Start button, mute toggle, gear/learn anchors, settings dialog (open it), each picker section label, each picker option label, stats footer (after at least 1 completed session), LearnDialog (open it; verify forrest section + locked-phrase paragraph + affiliation line + headings).
    3. Flag any PT-BR translation that is clearly incorrect or unnatural. Carry-forward note: these are expected to have minor errors per I18N-07; the goal is to flag egregious mistakes only.
    4. Specifically verify: medical-advice line is rendered in PT-BR on the main screen (App.tsx:686 migrated); affiliation line is rendered in PT-BR inside LearnDialog; locked-phrase paragraph is rendered in PT-BR inside LearnDialog's forrest section.

    UAT-3 (Persistence across reload):
    1. Switch to Português (Brasil).
    2. Hard-refresh the page (Cmd+Shift+R / Ctrl+Shift+R).
    3. Assert the page reloads with all PT-BR labels intact and `document.documentElement.lang === 'pt-BR'` (check via DevTools).

    UAT-4 (In-session disable):
    1. Start a session, then click the gear control.
    2. Observe the SettingsDialog — verify the LanguagePicker option buttons are visibly disabled (greyed) and that clicking them does NOT change the language mid-session.

    Record results in `.planning/phases/19-language-switching/19-UAT.md` with a status field (PASS/FAIL/PARTIAL) per UAT item + any operator notes/finding details.
  </how-to-verify>
  <resume-signal>Type "approved" once all 4 UAT items have a recorded outcome in 19-UAT.md, OR describe findings that block phase close.</resume-signal>
  <action>
    Pause execution and present the four UAT items to the operator. Operator runs the dev server in two tabs and walks through UAT-1..UAT-4 (cross-tab mid-session swap, PT-BR translation spot-check, persistence across reload, in-session disable). For each item, record the result (PASS / FAIL / PARTIAL) plus any operator-flagged findings into `.planning/phases/19-language-switching/19-UAT.md`. Do NOT proceed to Task 3 until the operator has typed "approved" or recorded a blocking finding.
  </action>
  <verify>
    <automated>test -f .planning/phases/19-language-switching/19-UAT.md && grep -c "UAT-" .planning/phases/19-language-switching/19-UAT.md</automated>
  </verify>
  <done>
    `.planning/phases/19-language-switching/19-UAT.md` exists, contains 4 UAT entries with recorded PASS/FAIL/PARTIAL status, operator has typed "approved" or documented blocking findings.
  </done>
</task>

<task type="auto">
  <name>Task 3: Update planning artifacts (REQUIREMENTS, ROADMAP, STATE) + create SUMMARY + UAT log</name>
  <files>.planning/REQUIREMENTS.md, .planning/ROADMAP.md, .planning/STATE.md, .planning/phases/19-language-switching/19-SUMMARY.md, .planning/phases/19-language-switching/19-UAT.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (current — lines 126–132 traceability rows for I18N-01..07 are `Pending`)
    - .planning/ROADMAP.md (lines 56, 221–232 Phase 19 entry + the Progress row at line 260)
    - .planning/STATE.md (entire file — phase status fields, performance metrics)
    - .planning/phases/19-language-switching/19-01-SUMMARY.md through 19-08-SUMMARY.md (deviation notes)
    - All Phase 19 plan SUMMARYs (final commit hashes, test counts, file deltas)
  </read_first>
  <behavior>
    - `REQUIREMENTS.md` lines 126–132: flip every I18N-0[1-7] row from `Pending` to `Done`.
    - REQUIREMENTS.md `Coverage` block (lines 134–137): unmapped count stays 0; if pending count tracked, update.
    - REQUIREMENTS.md footer (line 141): update last-updated line with Phase 19 close note.
    - ROADMAP.md line 56: change `- [ ] **Phase 19: Language Switching** ...` to `- [x] **Phase 19: Language Switching** ...` and append `(completed YYYY-MM-DD)`.
    - ROADMAP.md lines 221–232 Phase 19 detail block: update `**Plans:** TBD` to `**Plans:** 9 plans` and list each plan (19-01-PLAN.md ... 19-09-PLAN.md) with brief objective.
    - ROADMAP.md line 260 Progress row: change `0/?` to `9/9` and `Not started` to `Complete YYYY-MM-DD`.
    - STATE.md `stopped_at` field: update to `Phase 19 (Language Switching) complete; v1.1 close pending`.
    - STATE.md `progress.completed_phases` + `progress.completed_plans` + `progress.percent`: increment to reflect Phase 19 close (10/10 phases? 38+9=47 plans? — verify against the current 38/34 numbers and add Phase 19's 9 plans).
    - STATE.md Current Position + Performance Metrics by Phase rows: update Phase 19 row to `Complete` with plan count.
    - STATE.md Roadmap Evolution: add a bullet summarizing Phase 19 close (key outcomes: 9 plans across 5 waves, EN+PT-BR, frozen-EN guard via lockedCopy.test.ts byte-equality, total test count delta).
    - Create `19-SUMMARY.md` covering: goal recap, 5 SC outcomes (PASS/PARTIAL/FAIL per SC), file inventory (9 NEW + ~22 EDIT), test count delta, key deviations (e.g. if Plan 06/07 needed a temporary App.tsx stop-gap, whether StatsFooter used Path A or fell back to format.ts helpers, JSX-shape pick for locked-phrase paragraph), carry-forwards (I18N-07 native-speaker review remains v1.x), commit hashes for each plan.
    - Create `19-UAT.md` capturing the 4 UAT items from Task 2 with PASS/FAIL/PARTIAL status, operator notes, and any deferred findings.
  </behavior>
  <action>
    Sequential edits:
    1. Edit `.planning/REQUIREMENTS.md`:
       - Find lines 126–132 (I18N-01..07 rows). For each row, replace `Pending` in the `Status` column with `Done`.
       - Update footer line 141 to: `*Last updated: YYYY-MM-DD — Phase 19 (Language Switching) closed; I18N-01..07 flipped to Done; EN+PT-BR shipped with TODO: native-speaker review markers on PT-BR entries per I18N-07 carry-forward.*` (use today's date 2026-05-14 or current date at execution time).
    2. Edit `.planning/ROADMAP.md`:
       - Line 56: prefix the Phase 19 line with `[x]` and append `(completed YYYY-MM-DD)`.
       - Lines 221–232 detail block: update `**Plans:** TBD` to `**Plans:** 9 plans` and add a markdown checklist of 9 plan files with one-line objectives.
       - Line 260 Progress row: `19. Language Switching | v1.1 | 9/9 | Complete | YYYY-MM-DD`.
    3. Edit `.planning/STATE.md`:
       - `stopped_at` field: `Phase 19 (Language Switching) complete; v1.1 close pending`.
       - `last_updated` ISO timestamp: current.
       - `progress`: `total_phases: 10, completed_phases: 9, total_plans: 47, completed_plans: 43, percent: 91` (verify exact numbers against pre-Phase-19 state; Phase 19 has 9 plans, all complete).
       - Current Position: update phase + status.
       - Performance Metrics by Phase: flip Phase 19 to `9 | Complete`.
       - Roadmap Evolution: append bullet summarizing Phase 19 close.
    4. Create `.planning/phases/19-language-switching/19-SUMMARY.md` per the SUMMARY template (`@$HOME/.claude-personal-personal/get-shit-done/templates/summary.md`).
    5. Create `.planning/phases/19-language-switching/19-UAT.md` recording the 4 UAT items from Task 2.

    After all edits, commit `docs(19): close phase — flip I18N-01..07 + ROADMAP + STATE + SUMMARY + UAT` containing EXACTLY 5 files: `.planning/REQUIREMENTS.md`, `.planning/ROADMAP.md`, `.planning/STATE.md`, `.planning/phases/19-language-switching/19-SUMMARY.md`, `.planning/phases/19-language-switching/19-UAT.md`. The 6th file in `files_modified` (`src/app/App.locale.test.tsx`) was already committed as a SEPARATE `test(19-09):` commit at the end of Task 1 — it does NOT appear in this docs commit.
  </action>
  <verify>
    <automated>grep -E "I18N-0[1-7].*Done" .planning/REQUIREMENTS.md | wc -l && grep -c "\\[x\\] \\*\\*Phase 19" .planning/ROADMAP.md && test -f .planning/phases/19-language-switching/19-SUMMARY.md && test -f .planning/phases/19-language-switching/19-UAT.md && echo "ALL_ARTIFACTS_PRESENT"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -E "I18N-0[1-7].*Done" .planning/REQUIREMENTS.md | wc -l` returns 7.
    - `grep -c "\\[x\\] \\*\\*Phase 19" .planning/ROADMAP.md` returns 1.
    - `grep -c "Phase 19 (Language Switching) complete" .planning/STATE.md` returns 1.
    - `.planning/phases/19-language-switching/19-SUMMARY.md` exists with non-empty content.
    - `.planning/phases/19-language-switching/19-UAT.md` exists with 4 UAT entries.
    - Commit `docs(19): close phase — flip I18N-01..07 + ROADMAP + STATE + SUMMARY + UAT` created.
    - `git diff HEAD~1 --stat` shows EXACTLY 5 files: `.planning/REQUIREMENTS.md`, `.planning/ROADMAP.md`, `.planning/STATE.md`, `.planning/phases/19-language-switching/19-SUMMARY.md`, `.planning/phases/19-language-switching/19-UAT.md`.
    - `src/app/App.locale.test.tsx` was committed in a SEPARATE `test(19-09):` commit at the end of Task 1 (verify via `git log --oneline -3` — the docs commit is HEAD, the test commit is HEAD~1 or HEAD~2 depending on Task 2's UAT log update ordering).
    - `npx tsc --noEmit && npm run lint && npm run build && npm test -- --run` exits 0 (final green-gate).
  </acceptance_criteria>
  <done>
    All Phase 19 traceability flips applied; SUMMARY + UAT log created; final green-gate passes; v1.1 milestone audit-ready.
  </done>
</task>

<task type="auto">
  <name>Task 4: Final phase-close green-gate (D-16)</name>
  <files>(no source edit — verification only)</files>
  <read_first>
    - .planning/phases/19-language-switching/19-CONTEXT.md (D-16)
  </read_first>
  <action>
    Run `npx tsc --noEmit && npm run lint && npm run build && npm test -- --run` ONE final time after the docs commit. Verify all gates pass. Run `npx vitest run src/styles/theme.no-hardcoded-classes.test.ts` separately to confirm THEME-UI-01 invariant. Run `grep -c "TODO: native-speaker review" src/content/strings.ts src/content/learnContent.ts` to confirm I18N-07 markers persist. Run `npx vitest run src/content/lockedCopy.test.ts` to confirm the frozen-EN guard is green. Record the final test count and bundle size delta in `19-SUMMARY.md`.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run lint && npm run build && npm test -- --run 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - All 4 green-gate steps exit 0.
    - `npx vitest run src/styles/theme.no-hardcoded-classes.test.ts` exits 0.
    - `npx vitest run src/content/lockedCopy.test.ts` exits 0 (frozen-EN guard green).
    - `grep -c "TODO: native-speaker review" src/content/strings.ts src/content/learnContent.ts` returns >= 9 (I18N-07 markers).
    - Final test count >= 657 (Phase 18 baseline + Phase 19 additions).
    - No new external runtime dependencies added (`git diff HEAD~10 -- package.json | grep -E "^[+-].*dependencies" | wc -l` returns 0; OR `npm ls --depth=0` shows no new top-level deps vs. Phase 18 close).
  </acceptance_criteria>
  <done>
    Phase 19 complete; all invariants validated; v1.1 milestone audit-ready.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| App.locale.test.tsx → render(<App />) | Test environment only; no production runtime impact. |
| docs(19) commit → planning artifacts | Source-of-truth update; no runtime code change. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-22 | Repudiation | Phase 19 traceability claims | mitigate | REQUIREMENTS.md status flips backed by automated tests + 19-SUMMARY.md commit hashes provide auditable trail. |
| T-19-23 | Information Disclosure | Manual UAT log entries | accept | UAT findings are operator notes; no PII; deferred items are flagged for v1.x review. |
</threat_model>

<verification>
- `npx tsc --noEmit && npm run lint && npm run build && npm test -- --run` exits 0.
- 7/7 I18N requirement IDs flipped to Done in REQUIREMENTS.md.
- ROADMAP.md Phase 19 marked complete + 9 plans listed.
- STATE.md reflects Phase 19 close + v1.1 progress.
- 19-SUMMARY.md + 19-UAT.md exist and capture phase outcomes + manual verification.
- No new runtime dependencies (D-17 invariant).
- THEME-UI-01 + frozen-EN snapshot guards remain green.
- Manual UAT for I18N-02 mid-session cross-tab swap recorded (PASS/FAIL/PARTIAL).
</verification>

<success_criteria>
1. App-level integration test covers locale switch + documentElement.lang write + cross-tab storage path.
2. Manual UAT log captures mid-session cross-tab swap, PT-BR translation spot-check, persistence-across-reload, in-session disable.
3. REQUIREMENTS.md I18N-01..07 status all flipped to Done.
4. ROADMAP.md Phase 19 marked complete with 9-plan listing.
5. STATE.md reflects phase close + updated progress metrics.
6. 19-SUMMARY.md + 19-UAT.md created.
7. Final green-gate exits 0 after the docs commit.
8. v1.1 milestone audit-ready.
</success_criteria>

<output>
After completion, ensure `.planning/phases/19-language-switching/19-SUMMARY.md` is committed as part of Task 3. This is the final phase artifact — no additional plan-level summary needed.

Notify operator: "Phase 19 complete. Next: `/gsd-transition` for milestone close eligibility check, or `/gsd-complete-milestone v1.1` if all v1.1 phases are now closed."
</output>
