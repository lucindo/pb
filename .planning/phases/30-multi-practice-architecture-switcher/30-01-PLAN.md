---
phase: 30-multi-practice-architecture-switcher
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/domain/naviKriyaSettings.ts
  - src/domain/naviKriyaSettings.test.ts
autonomous: true
requirements: [PRACTICE-02]
must_haves:
  truths:
    - "NaviKriyaSettings type, defaults, and validators exist in the domain layer"
    - "isValidFrontCount rejects non-multiple-of-4 values"
    - "D-02: Navi Kriya settings data model (NaviKriyaSettings type, defaults, validators) is defined now in Phase 30 so PRACTICE-02 is satisfied for both practices"
  artifacts:
    - path: "src/domain/naviKriyaSettings.ts"
      provides: "NaviKriyaSettings type, DEFAULT_NK_SETTINGS, OM_LENGTH_OPTIONS, isValid* predicates"
      contains: "export interface NaviKriyaSettings"
    - path: "src/domain/naviKriyaSettings.test.ts"
      provides: "Validator + defaults unit coverage for PRACTICE-02"
      contains: "describe('isValidFrontCount"
  key_links:
    - from: "src/domain/naviKriyaSettings.ts"
      to: "src/domain/naviKriyaSettings.test.ts"
      via: "imports validators + defaults under test"
      pattern: "from './naviKriyaSettings'"
---

<objective>
Define the Navi Kriya settings data model in the domain layer — the `NaviKriyaSettings`
type, defaults, OM-length option set, and validator predicates — per locked decision D-02.

Purpose: D-02 requires Phase 30 to fully define the NK settings shape now so PRACTICE-02
(per-practice persistence) is satisfied for both practices at the end of this phase.
Phase 31 adds only the engine and controls UI; it does not define new settings shape.

Output: `src/domain/naviKriyaSettings.ts` (new) and `src/domain/naviKriyaSettings.test.ts`
(new). The domain file exports types, the option array, defaults, and `isValid*`
predicates only — the `coerceNaviKriyaSettings` storage coercer is built in plan 30-03,
matching the existing `domain/settings.ts` (predicates) vs `storage/settings.ts` (coercer)
split.
</objective>

<execution_context>
@$HOME/.claude-personal-personal/get-shit-done/workflows/execute-plan.md
@$HOME/.claude-personal-personal/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-multi-practice-architecture-switcher/30-RESEARCH.md
@.planning/phases/30-multi-practice-architecture-switcher/30-PATTERNS.md
@.claude/skills/spike-findings-hrv/references/navi-kriya-practice.md

<interfaces>
<!-- The new naviKriyaSettings.ts mirrors the existing settings.ts domain module. -->
<!-- Executor: replicate the exact structure of src/domain/settings.ts. -->

NaviKriyaSettings shape (from RESEARCH.md Code Examples, authoritative per D-02):
- frontCount: number   — base front OM count; backCount = frontCount / 4; MUST be a multiple of 4; default 100
- omLength: 'fast' | 'medium' | 'slow'   — default 'medium'
- rounds: number       — integer >= 1; default 3
- perOmCue: boolean    — audible per-OM tick; default true

Required exports from src/domain/naviKriyaSettings.ts:
- type OmLength = 'fast' | 'medium' | 'slow'
- const OM_LENGTH_OPTIONS = ['fast', 'medium', 'slow'] as const satisfies readonly OmLength[]
- interface NaviKriyaSettings { frontCount; omLength; rounds; perOmCue }
- const DEFAULT_NK_SETTINGS: NaviKriyaSettings = { frontCount: 100, omLength: 'medium', rounds: 3, perOmCue: true }
- function isValidFrontCount(v: unknown): v is number
- function isValidOmLength(v: unknown): v is OmLength
- function isValidRounds(v: unknown): v is number
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create the NaviKriyaSettings domain model</name>
  <files>src/domain/naviKriyaSettings.ts</files>
  <read_first>
    - src/domain/settings.ts (the exact analog — type + const-array + DEFAULT_* + isValid* predicate structure to replicate; this file has NO imports and the new file must also be self-contained)
    - .planning/phases/30-multi-practice-architecture-switcher/30-RESEARCH.md (Code Examples section — the authoritative NaviKriyaSettings interface, defaults, and validator bodies)
    - .claude/skills/spike-findings-hrv/references/navi-kriya-practice.md (4:1 ratio, default counts, OM-length roles — confirms D-02 values)
  </read_first>
  <behavior>
    - isValidFrontCount(100) === true; isValidFrontCount(4) === true (smallest valid multiple of 4)
    - isValidFrontCount(102) === false (positive integer but NOT a multiple of 4 — Pitfall 5)
    - isValidFrontCount(0) === false; isValidFrontCount(-4) === false; isValidFrontCount(4.5) === false
    - isValidFrontCount(NaN) === false; isValidFrontCount(Infinity) === false; isValidFrontCount('100') === false; isValidFrontCount(null) === false
    - isValidOmLength('fast'|'medium'|'slow') === true; isValidOmLength('FAST') === false; isValidOmLength(null) === false
    - isValidRounds(3) === true; isValidRounds(1) === true; isValidRounds(0) === false; isValidRounds(2.5) === false; isValidRounds(NaN) === false; isValidRounds('3') === false
    - DEFAULT_NK_SETTINGS equals { frontCount: 100, omLength: 'medium', rounds: 3, perOmCue: true }
  </behavior>
  <action>
    Create a new self-contained domain module with NO imports, mirroring src/domain/settings.ts.
    Export: the `OmLength` union type ('fast' | 'medium' | 'slow'); the `OM_LENGTH_OPTIONS`
    const array typed `as const satisfies readonly OmLength[]`; the `NaviKriyaSettings`
    interface with fields `frontCount: number`, `omLength: OmLength`, `rounds: number`,
    `perOmCue: boolean`; and `DEFAULT_NK_SETTINGS` with values frontCount 100, omLength
    'medium', rounds 3, perOmCue true (D-02).
    Export three type-predicate validators following the settings.ts `isValid*` pattern.
    `isValidFrontCount` MUST check: typeof number, Number.isFinite, Number.isInteger, v > 0,
    AND `v % 4 === 0` — the multiple-of-4 check is the critical Pitfall 5 guard so
    `backCount = frontCount / 4` is never fractional. `isValidOmLength` checks membership in
    the OM_LENGTH_OPTIONS set. `isValidRounds` checks typeof number, Number.isFinite,
    Number.isInteger, v >= 1. Do NOT add a coercer here — `coerceNaviKriyaSettings` belongs
    in src/storage/practices.ts (built in plan 30-03), matching the domain-vs-storage split.
  </action>
  <verify>
    <automated>npx tsc --noEmit && grep -v '^//' src/domain/naviKriyaSettings.ts | grep -c "v % 4 === 0"</automated>
    <note>Task 1 is `tdd="true"` but its verify intentionally runs only `tsc` + a grep gate, not a test command — the validator test file does not exist until Task 2 creates it. Full behavioral coverage for this module (including the Pitfall 5 `102→false` regression case) is delivered by Task 2's `npm run test:run -- src/domain/naviKriyaSettings.test.ts` in the same wave. The `<behavior>` block above is the explicit RED specification Task 2 implements against.</note>
  </verify>
  <acceptance_criteria>
    - `src/domain/naviKriyaSettings.ts` exists and `npx tsc --noEmit` passes
    - File exports `NaviKriyaSettings`, `OmLength`, `OM_LENGTH_OPTIONS`, `DEFAULT_NK_SETTINGS`, `isValidFrontCount`, `isValidOmLength`, `isValidRounds`
    - `isValidFrontCount` body contains the substring `v % 4 === 0` (multiple-of-4 enforcement)
    - File has zero `import` statements (self-contained domain module, like settings.ts)
    - No `coerce` function is defined in this file
  </acceptance_criteria>
  <done>NaviKriyaSettings type, defaults, OM_LENGTH_OPTIONS, and all three isValid* validators exist; tsc passes; frontCount validator enforces multiple-of-4.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add the NaviKriyaSettings validator test suite</name>
  <files>src/domain/naviKriyaSettings.test.ts</files>
  <read_first>
    - src/domain/settings.test.ts (the exact test-file analog — `describe` per validator, valid/invalid/edge-case `it` blocks, vitest import style)
    - src/domain/naviKriyaSettings.ts (the module under test — created in Task 1)
  </read_first>
  <behavior>
    - One `describe` block per validator: isValidFrontCount, isValidOmLength, isValidRounds
    - isValidFrontCount block: 4 and 100 -> true; 102 -> false (multiple-of-4 rejection); 0, -4, 4.5, NaN, Infinity, '100', null -> false
    - isValidOmLength block: 'fast'/'medium'/'slow' -> true; 'FAST', '', null, 5 -> false
    - isValidRounds block: 1 and 3 -> true; 0, -1, 2.5, NaN, Infinity, '3', null -> false
    - A `describe` block for DEFAULT_NK_SETTINGS asserting exact value { frontCount: 100, omLength: 'medium', rounds: 3, perOmCue: true }
  </behavior>
  <action>
    Create the test file using the vitest `import { describe, expect, it } from 'vitest'`
    header from settings.test.ts. Import the validators and `DEFAULT_NK_SETTINGS` from
    './naviKriyaSettings'. Write one `describe` per validator with `it` cases covering valid
    values, wrong-type values, and edge values (NaN, Infinity, negative, non-integer). The
    isValidFrontCount block MUST include an explicit case proving 102 (a positive integer
    that is NOT a multiple of 4) returns false — this is the Pitfall 5 regression guard.
    Add a `describe` asserting DEFAULT_NK_SETTINGS equals the exact D-02 object.
  </action>
  <verify>
    <automated>npm run test:run -- src/domain/naviKriyaSettings.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npm run test:run -- src/domain/naviKriyaSettings.test.ts` exits 0 with all tests passing
    - Test output shows a passing assertion that `isValidFrontCount(102)` returns false
    - Test output shows a passing assertion for `DEFAULT_NK_SETTINGS` exact equality
    - At least one `describe` block per validator (3 total) plus one for defaults
  </acceptance_criteria>
  <done>The NK settings validator suite passes; the 102-is-invalid case and the defaults equality case are both green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new in this plan) | This plan adds pure domain types + predicates; no data crosses a boundary here. The localStorage→app boundary is exercised by the coercer in plan 30-03. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-01 | Tampering | `isValidFrontCount` predicate | mitigate | Predicate rejects non-finite, non-integer, non-positive, and non-multiple-of-4 values so a tampered `frontCount` from localStorage cannot produce a fractional `backCount` in Phase 31 session arithmetic. Enforced and unit-tested in Task 1/2. |
| T-30-02 | Tampering | `isValidRounds` / `isValidOmLength` | accept | Predicates reject malformed input; values are low-risk display/loop counts with no injection surface. ASVS V5 satisfied at the coercer boundary (plan 30-03). |
</threat_model>

<verification>
- `npx tsc --noEmit` passes (new domain module type-checks).
- `npm run test:run -- src/domain/naviKriyaSettings.test.ts` passes.
- `src/domain/naviKriyaSettings.ts` has no imports and no coercer.
</verification>

<success_criteria>
- `NaviKriyaSettings`, `DEFAULT_NK_SETTINGS`, `OM_LENGTH_OPTIONS`, and the three `isValid*`
  predicates are exported and type-check.
- `isValidFrontCount` enforces `v % 4 === 0`; the 102→false case is unit-tested.
- The data model is ready to be wired into the per-practice persistence map by plan 30-03
  (PRACTICE-02).
</success_criteria>

<output>
After completion, create `.planning/phases/30-multi-practice-architecture-switcher/30-01-SUMMARY.md`
</output>
</content>
</invoke>
