---
phase: 30-multi-practice-architecture-switcher
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/PracticeToggle.tsx
  - src/components/PracticeToggle.test.tsx
  - src/content/strings.ts
  - src/content/strings.test.ts
autonomous: true
requirements: [PRACTICE-01, PRACTICE-03]
must_haves:
  truths:
    - "PracticeToggle renders two pills and clicking one fires onSwitch with that practice id"
    - "PracticeToggle is non-interactive when disabled (in-session lock)"
    - "Practice display names, toggle aria-label, control headings, and practice-named reset title exist as EN copy strings"
    - "D-05: switcher pills use full practice names ('Resonant Breathing', 'Navi Kriya'); 'Navi Kriya' stays untranslated"
    - "D-04: a control heading copy string naming the active practice exists (EN now)"
    - "D-08: a practice-named reset-dialog title copy string exists so the reset dialog can name the practice being wiped"
  artifacts:
    - path: "src/components/PracticeToggle.tsx"
      provides: "Top segmented-control practice switcher (presentational)"
      contains: "export function PracticeToggle"
    - path: "src/components/PracticeToggle.test.tsx"
      provides: "Unit coverage for PRACTICE-01 and PRACTICE-03"
      contains: "aria-pressed"
    - path: "src/content/strings.ts"
      provides: "practice copy strings (names, heading, toggle label, reset title fn)"
      contains: "practice:"
  key_links:
    - from: "src/components/PracticeToggle.tsx"
      to: "onSwitch prop"
      via: "button onClick handler"
      pattern: "onClick.*onSwitch"
---

<objective>
Build the `PracticeToggle` segmented-control component and add the EN copy strings the
multi-practice UI needs — practice display names (D-05), the toggle's aria-label, the
inline practice-controls heading (D-04), and a practice-named reset-dialog title (D-08).

Purpose: PRACTICE-01 (switch via a top segmented control) and PRACTICE-03 (switcher
disabled during a session) are delivered by this presentational component. The copy
strings are the EN-only foundation that plan 30-04 wires into the home screen and that
Phase 32 will localize to PT-BR.

Output: `src/components/PracticeToggle.tsx` + test (new), and extensions to
`src/content/strings.ts` + `src/content/strings.test.ts`. This plan does NOT wire the
component into `App.tsx` — that integration is plan 30-04 (Wave 3).
</objective>

<execution_context>
@$HOME/.claude-personal-personal/get-shit-done/workflows/execute-plan.md
@$HOME/.claude-personal-personal/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-multi-practice-architecture-switcher/30-RESEARCH.md
@.planning/phases/30-multi-practice-architecture-switcher/30-PATTERNS.md
@.planning/phases/30-multi-practice-architecture-switcher/30-UI-SPEC.md

<interfaces>
<!-- PracticeId is exported by src/storage/practices.ts (plan 30-03). This plan runs in -->
<!-- the same wave, so to avoid a cross-plan dependency, define PracticeId locally here. -->
<!-- Use a string-literal union type alias declared in PracticeToggle.tsx itself: -->
<!--   type PracticeId = 'resonant' | 'naviKriya'  -->
<!-- Plan 30-03 exports the canonical PracticeId from storage/practices.ts; plan 30-04 -->
<!-- reconciles imports so a single PracticeId source is used app-wide. -->

PracticeToggleProps (from RESEARCH Pattern 3 + UI-SPEC Component Inventory):
- active: PracticeId                          — currently selected practice
- disabled: boolean                            — true while a session is in progress (D-06)
- onSwitch(this: void, id: PracticeId): void    — fired on pill click
- strings: { toggleLabel: string; practiceNames: Record<PracticeId, string> }

Existing strings.ts shapes (from src/content/strings.ts):
- export interface UiStrings { ... }                  — nested sub-objects per feature (D-10)
- export const UI_STRINGS: Readonly<Record<LocaleId, UiStrings>>  — keyed 'en' and 'pt-BR'
- LocaleId is imported from '../domain/settings'; the two locales are 'en' and 'pt-BR'
- resetStatsDialog currently: { readonly title: string; confirm: string; cancel: string }
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add practice copy strings to the strings catalog</name>
  <files>src/content/strings.ts, src/content/strings.test.ts</files>
  <read_first>
    - src/content/strings.ts (the UiStrings interface lines 12-143, the UI_STRINGS const lines 146-420 with both 'en' and 'pt-BR' locale objects, and the resetStatsDialog sub-object at lines 27-31 / 162 / 298)
    - src/content/strings.test.ts (the exhaustiveness test pattern — `describe('UI_STRINGS exhaustiveness')` and the template-fn `describe` at line 101)
    - .planning/phases/30-multi-practice-architecture-switcher/30-UI-SPEC.md (Copywriting Contract — the exact EN copy values for every new string)
  </read_first>
  <action>
    Add one new nested sub-object `practice` to the `UiStrings` interface following the
    D-10 nested-sub-object convention. Its members:
    - `toggleLabel: string`
    - `resonantName: string`
    - `naviKriyaName: string`
    - `resonantHeading: string`
    - `naviKriyaHeading: string`
    - `naviKriyaControlsPlaceholder: string`
    - `naviKriyaStatsEmptyBody: string`
    - `resetStatsTitle: (practiceName: string) => string`
    Populate the new `practice` sub-object in BOTH locale entries of `UI_STRINGS` ('en' and
    'pt-BR'). EN values (from UI-SPEC Copywriting Contract): toggleLabel "Switch practice",
    resonantName "Resonant Breathing", naviKriyaName "Navi Kriya", resonantHeading
    "Resonant Breathing", naviKriyaHeading "Navi Kriya", naviKriyaControlsPlaceholder
    "Controls coming soon", naviKriyaStatsEmptyBody "Navi Kriya sessions will appear here
    after completing your first session.", resetStatsTitle a function returning
    `Reset ${practiceName} stats?`. For the 'pt-BR' entry, use the SAME EN string values
    as placeholders (PT-BR translation is Phase 32, I18N-08) — "Navi Kriya" stays
    untranslated in all locales per D-05; do NOT add review markers since Phase 32 owns
    the PT-BR pass. Keep `resetStatsDialog.title` unchanged so existing ResetStatsDialog
    tests still pass — the new `practice.resetStatsTitle` function is the practice-named
    title; plan 30-04 switches the dialog to use it.
    Add test cases to strings.test.ts: a `describe('Phase 30 practice string keys')` block
    asserting every `practice.*` key exists and is non-empty in both 'en' and 'pt-BR', and
    that `practice.resetStatsTitle('Resonant Breathing')` returns a non-empty string
    containing "Resonant Breathing".
  </action>
  <verify>
    <automated>npm run test:run -- src/content/strings.test.ts && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `UiStrings` interface contains a `practice` sub-object with all 8 listed members
    - `UI_STRINGS.en.practice` and `UI_STRINGS['pt-BR'].practice` are both fully populated and `npx tsc --noEmit` passes
    - `UI_STRINGS.en.practice.resetStatsTitle('Resonant Breathing')` returns the string `Reset Resonant Breathing stats?`
    - `UI_STRINGS.en.practice.naviKriyaName` equals `Navi Kriya`
    - `resetStatsDialog.title` is unchanged from its pre-existing value
    - `npm run test:run -- src/content/strings.test.ts` passes including the new Phase 30 block
  </acceptance_criteria>
  <done>The `practice` string sub-object exists in both locales with the exact EN copy values; tsc and the strings suite pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build the PracticeToggle component</name>
  <files>src/components/PracticeToggle.tsx, src/components/PracticeToggle.test.tsx</files>
  <read_first>
    - src/components/ModeToggle.tsx (the role-match analog — props interface, presentational segmented-toggle structure, CSS-variable color tokens, focus-ring classes)
    - .planning/phases/30-multi-practice-architecture-switcher/30-UI-SPEC.md (Component Inventory "New: PracticeToggle" — the exact layout, class names, ARIA, and disabled-state contract)
    - .planning/phases/30-multi-practice-architecture-switcher/30-RESEARCH.md (Pattern 3 — the verified spike-002 PracticeToggle JSX)
    - .claude/skills/spike-findings-hrv/sources/002-switcher-ux/index.html (the runnable spike source for the pill layout)
    - src/components/ModeToggle.tsx (read for the focus-visible ring class string to copy verbatim)
  </read_first>
  <behavior>
    - Renders exactly two `<button>` pills, labelled from `strings.practiceNames.resonant` and `strings.practiceNames.naviKriya`
    - The pill matching `active` has `aria-pressed="true"`; the other has `aria-pressed="false"`
    - Clicking the inactive pill calls `onSwitch` once with that pill's practice id ('resonant' or 'naviKriya')
    - When `disabled` is true: both `<button>` elements have the `disabled` attribute set, and clicking does NOT call `onSwitch`
    - When `disabled` is false: pills are interactive
    - The container `<div>` has `role="group"` and `aria-label={strings.toggleLabel}`
  </behavior>
  <action>
    Create a presentational component mirroring ModeToggle.tsx's structure. Declare a local
    `type PracticeId = 'resonant' | 'naviKriya'` alias and export `PracticeToggleProps` with
    `active: PracticeId`, `disabled: boolean`, `onSwitch(this: void, id: PracticeId): void`,
    and `strings: { toggleLabel: string; practiceNames: Record<PracticeId, string> }`.
    Render a wrapping `<div role="group" aria-label={strings.toggleLabel}>` with classes
    `flex rounded-full bg-[var(--color-breathing-surface)] p-1`, and append
    `opacity-50 cursor-not-allowed` to that container's className when `disabled` is true
    (D-06 advisory visual). Map over `['resonant', 'naviKriya']` rendering one `<button
    type="button">` per id with `flex-1 rounded-full min-h-[44px] px-4 py-2 text-sm`, the
    `disabled={disabled}` attribute, `aria-pressed={active === id}`, and `onClick={() =>
    onSwitch(id)}`. Active pill className: `bg-white shadow font-semibold
    text-[var(--color-breathing-accent-strong)]`. Inactive pill className: `font-normal
    text-[var(--color-breathing-muted)]`. Add the standard focus ring
    `focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-breathing-accent
    focus-visible:ring-offset-2` to each button (copy the exact class string from
    ModeToggle.tsx). Use ONLY `var(--color-breathing-*)` tokens — no raw hex. The
    `disabled` attribute on each `<button>` is the real interaction lock (PRACTICE-03); the
    container opacity is advisory only.
    Create PracticeToggle.test.tsx using @testing-library/react: render with stub strings;
    assert two pills render with correct labels; assert `aria-pressed` is true only on the
    active pill; assert clicking the inactive pill fires `onSwitch` with the right id;
    assert that with `disabled` true both buttons carry the `disabled` attribute and a
    click does not fire `onSwitch`.
  </action>
  <verify>
    <automated>npm run test:run -- src/components/PracticeToggle.test.tsx && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/PracticeToggle.tsx` exists, exports `PracticeToggle` and `PracticeToggleProps`, and `npx tsc --noEmit` passes
    - Component renders exactly two `<button>` elements
    - `npm run test:run -- src/components/PracticeToggle.test.tsx` passes
    - Test output includes a passing assertion that clicking an inactive pill calls `onSwitch` with its practice id
    - Test output includes a passing assertion that when `disabled` is true, clicking does NOT call `onSwitch`
    - `grep -c '#' src/components/PracticeToggle.tsx` finds no raw hex color literals (all colors are `var(--color-breathing-*)` tokens)
  </acceptance_criteria>
  <done>PracticeToggle renders two ARIA-correct pills, switches on click, and is fully non-interactive when disabled; the unit suite passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user click → PracticeToggle onSwitch | A pill click crosses into app state. Untrusted only in timing — a click during a session must not switch practice. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-03 | Tampering | PracticeToggle in-session lock | mitigate | The `disabled` attribute on each `<button>` is the real interaction lock (not just container opacity), so a click during a session cannot fire `onSwitch`. Plan 30-04 adds a defense-in-depth `inSessionView` guard inside the `onSwitchPractice` handler. Unit-tested in Task 2. |
| T-30-04 | Information disclosure | practice copy strings | accept | Strings are static UI copy with no user data or secrets; no disclosure risk. |
</threat_model>

<verification>
- `npm run test:run -- src/components/PracticeToggle.test.tsx src/content/strings.test.ts` passes.
- `npx tsc --noEmit` passes.
- PracticeToggle uses only CSS-variable color tokens (no raw hex).
- `resetStatsDialog.title` is unchanged; existing ResetStatsDialog tests still pass.
</verification>

<success_criteria>
- PracticeToggle is a working, ARIA-correct, presentational segmented control delivering
  PRACTICE-01 and PRACTICE-03 (disabled lock).
- The `practice` copy sub-object exists in both locales with exact EN values, ready for
  plan 30-04 to consume and Phase 32 to translate.
</success_criteria>

<output>
After completion, create `.planning/phases/30-multi-practice-architecture-switcher/30-02-SUMMARY.md`
</output>
</content>
</invoke>
