---
phase: 49.1-bypass-silent-mode-toggle
plan: 3
type: execute
wave: 2
depends_on: [49.1-01, 49.1-02]
files_modified:
  - src/content/strings.ts
  - src/content/strings.test.ts
  - src/content/content.no-review-markers.test.ts
  - src/app/pages/AppearancePage.tsx
  - src/app/pages/AppearancePage.test.tsx
  - src/app/pages/AdvancedPage.tsx
  - src/app/pages/AdvancedPage.test.tsx
  - src/app/ScreenRouter.tsx
  - src/app/ScreenRouter.test.tsx
  - src/app/useAppNavigation.ts
  - src/app/useAppNavigation.test.tsx
  - src/app/appControllerAdapters.test.ts
  - src/app/appViewModel.ts
  - src/app/appControllerAdapters.ts
  - src/app/pages/AppSettingsPage.tsx
  - src/app/pages/AppSettingsPage.test.tsx
  - src/app/useAppViewModel.ts
autonomous: true
requirements: [ADV-01, ADV-02, ADV-03]
tags: [atomic-rename, ui, i18n, settings, toggle, wire-up]
must_haves:
  truths:
    - "App Settings → trailing chevron lands on a page whose title reads 'Advanced' (EN) / 'Avançado' (PT-BR). The strings 'Appearance' / 'Aparência' no longer appear anywhere in src/content/strings.ts (ADV-01)"
    - "The Behavior section on the Advanced page has the header 'Behavior' (EN) / 'Comportamento' (PT-BR). The strings-tree key is advanced.sections.behavior; the old advanced.sections.visual key is absent (ADV-02)"
    - "A 'Bypass silent mode' / 'Ignorar modo silencioso' toggle is visible on the Advanced page below the two existing Behavior toggles; checked by default; persists across reloads (ADV-03)"
    - "AppScreen enum value 'appearance' is gone — replaced everywhere by 'advanced' (D-10)"
    - "Navigation callbacks renamed symmetrically (Option A): onAppearanceOpen → onAdvancedOpen, onBackFromAppearance → onBackFromAdvanced, returningFromAppearance → returningFromAdvanced. Applies in `useAppNavigation.ts` (declaration), `appViewModel.ts` (interface — lines 48, 51, 53), `appControllerAdapters.ts` (factory — lines 220, 223, 225), `useAppViewModel.ts` (consumer + wire-up), `ScreenRouter.tsx` (case render), `AppSettingsPage.tsx` (chevron handler) — D-10 spirit, PATTERNS.md Option A recommendation"
    - "All edits land in a single executor commit (D-11 atomic-rename invariant — per-commit green-gate would otherwise fail at any intermediate state because TypeScript would reject a half-renamed enum or strings tree)"
    - "useAppViewModel reads featureFlags.bypassSilentMode (Plan 01) and passes it into useBreathingSessionController (Plan 02 signature) — final end-to-end wire-up"
    - "Three operator-confirmed native-validated EN+PT-BR strings (advanced.title, advanced.sections.behavior, advanced.bypassSilentMode.label) plus the value-flipped advanced.rightChevronAriaOnSettings carry NO // TODO: native-speaker review markers. Three existing PT-BR markers above the OLD `title: 'Aparência'`, `rightChevronAriaOnSettings: 'Configurações de aparência'`, and `visual: 'Visual'` MUST be REMOVED because those values flip to operator-confirmed strings (D-04 forbids markers on operator-confirmed strings). The remaining markers under the renamed `advanced.*` tree (above `backChevron`, `orb.*`, `ringCue.*`, `breathingEffect.label`, `switcherIcons.label`, `sections.orbStyle`) stay unchanged — those Phase 48 PT-BR values are unchanged. The plan preserves the existing PT-BR marker count under the `advanced.*` tree on a pre/post comparison (minus the three required removals)"
    - "DEPS-01: package.json dependencies unchanged"
    - "QUAL-01: pnpm tsc && pnpm lint && pnpm build && pnpm test is green at commit time"
  artifacts:
    - path: "src/content/strings.ts"
      provides: "strings tree renamed appearance → advanced; sections.visual → sections.behavior; new bypassSilentMode.label key; readonly type updated to match; three obsolete markers removed at PT-BR lines 547/551/556 (pre-edit positions)"
      contains: "advanced:"
    - path: "src/app/pages/AdvancedPage.tsx"
      provides: "renamed page component + 3rd SettingsToggleRow for Bypass silent mode; useBypassSilentModeChoice hook bound; D-09 inline comment"
      exports: ["AdvancedPage", "AdvancedPageProps"]
    - path: "src/app/pages/AdvancedPage.test.tsx"
      provides: "renamed test file + 3 new tests covering ADV-03 (toggle order + default-on + envelope write)"
    - path: "src/app/useAppNavigation.ts"
      provides: "AppScreen union with 'advanced' (not 'appearance'); 3 callback names renamed symmetrically"
    - path: "src/app/appViewModel.ts"
      provides: "AppNavigationViewModel interface — 3 callback identifiers renamed symmetrically at lines 48/51/53 (Option A)"
    - path: "src/app/appControllerAdapters.ts"
      provides: "createAppNavigationViewModel() return-object keys + navigation.* reads renamed at lines 220/223/225 (Option A)"
    - path: "src/app/useAppViewModel.ts"
      provides: "featureFlags.bypassSilentMode passed into useBreathingSessionController + Option A callback identifiers renamed in consumer + wire-up sites"
  key_links:
    - from: "src/app/pages/AdvancedPage.tsx"
      to: "src/hooks/useBypassSilentModeChoice.ts (Plan 01)"
      via: "import + binding"
      pattern: "useBypassSilentModeChoice"
    - from: "src/app/useAppViewModel.ts"
      to: "src/hooks/useBreathingSessionController.ts (Plan 02 signature)"
      via: "props.bypassSilentMode = featureFlags.bypassSilentMode"
      pattern: "bypassSilentMode: featureFlags\\.bypassSilentMode|bypassSilentMode=\\{featureFlags\\.bypassSilentMode\\}"
    - from: "src/app/ScreenRouter.tsx"
      to: "src/app/pages/AdvancedPage.tsx"
      via: "case 'advanced' import + render"
      pattern: "case 'advanced'"
    - from: "src/app/pages/AppSettingsPage.tsx"
      to: "src/app/useAppNavigation.ts (onAdvancedOpen)"
      via: "trailing chevron handler"
      pattern: "onAdvancedOpen"
    - from: "src/app/appViewModel.ts (AppNavigationViewModel interface)"
      to: "src/app/appControllerAdapters.ts (createAppNavigationViewModel factory)"
      via: "shared callback identifiers — interface + factory rename in lockstep at lines 48/51/53 + 220/223/225"
      pattern: "onAdvancedOpen|onBackFromAdvanced|returningFromAdvanced"
---

<objective>
Close Phase 49.1 end-to-end by:
  (1) **Atomically renaming** Appearance → Advanced across the strings tree,
      the AppScreen enum, the page component file, the router case, the
      navigation callbacks (interface + factory + consumers), and every test
      (D-10 / D-11). Per the operator-locked D-11 invariant this MUST land
      as a single executor commit — any intermediate state would fail the
      per-commit green-gate because TypeScript would reject a half-renamed
      enum or strings tree.
  (2) Binding the new "Bypass silent mode" toggle on the Advanced page using
      Plan 01's `useBypassSilentModeChoice` hook (ADV-03 UI).
  (3) Wiring `useAppViewModel` to read `featureFlags.bypassSilentMode` (from
      Plan 01) and pass it down to `useBreathingSessionController` (Plan 02
      signature) — the final end-to-end leg from prefs/query-string to the
      `createAudioEngine` opts.bypassSilentMode gate.

Per the PATTERNS.md deferred-decision callout, **Option A** is chosen: rename
the three navigation callbacks symmetrically (`onAppearanceOpen` →
`onAdvancedOpen`, `onBackFromAppearance` → `onBackFromAdvanced`,
`returningFromAppearance` → `returningFromAdvanced`). Rationale: D-10's
stated principle is "rename end-to-end"; a half-rename is the worst of
both worlds (residual naming smell + incomplete grep gates). Option A
requires editing both `appViewModel.ts` (the `AppNavigationViewModel`
interface at lines 48/51/53) AND `appControllerAdapters.ts` (the
`createAppNavigationViewModel()` factory at lines 220/223/225) in the
atomic-rename commit — TypeScript would otherwise reject the interface vs
factory mismatch.

This plan absorbs the D-11-bound callback rename edits in
`useAppViewModel`, `AppSettingsPage`, `appViewModel.ts`, and
`appControllerAdapters.ts`, keeping the atomic-rename surface in one commit.

Purpose: ship the user-facing half of Phase 49.1 — the renamed page, the
new toggle, and the connection from preference to audio gate.

Output: a single atomic-rename + UI binding + wire-up commit that closes
ADV-01, ADV-02, ADV-03, and the runtime data path for ADV-04 + ADV-05.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@./CLAUDE.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/49.1-bypass-silent-mode-toggle/49.1-CONTEXT.md
@.planning/phases/49.1-bypass-silent-mode-toggle/49.1-PATTERNS.md

<interfaces>
<!-- Extracted from src/content/strings.ts:171-200 — current readonly type block -->
```typescript
readonly appearance: {
  readonly title: string
  readonly backChevron: string
  readonly rightChevronAriaOnSettings: string
  readonly sections: { readonly orbStyle: string; readonly visual: string }
  readonly orb: { /* ... */ }
  readonly ringCue: { /* ... */ }
  readonly breathingEffect: { readonly label: string }
  readonly switcherIcons: { readonly label: string }
  // NEW: readonly bypassSilentMode: { readonly label: string }
}
```

<!-- Extracted from src/app/pages/AppearancePage.tsx — current shape -->
```typescript
import { SettingsToggleRow } from '../../components/SettingsToggleRow'
import { SectionCard } from '../../components/primitives/SectionCard'
import { useSwitcherIconChoice } from '../../hooks/useSwitcherIconChoice'

export function AppearancePage(props): JSX.Element {
  const strings = useUiStrings().appearance  // ← strings.advanced after rename
  const { switcherIcon, setSwitcherIcon } = useSwitcherIconChoice()
  // ... two SectionCards: orbStyle + visual (rename to behavior)
  // Inside the Behavior SectionCard, append a 3rd SettingsToggleRow
}
```

<!-- Extracted from src/app/useAppNavigation.ts:3 — AppScreen union -->
```typescript
export type AppScreen = 'practice' | 'learn' | 'appSettings' | 'appearance'
// → 'practice' | 'learn' | 'appSettings' | 'advanced'
```

<!-- Extracted from src/app/appViewModel.ts:46-54 — AppNavigationViewModel interface (Option A rename target) -->
```typescript
export interface AppNavigationViewModel {
  appScreen: AppScreen
  returningFromAppearance: boolean              // ← line 48 → returningFromAdvanced
  onLearnOpen(this: void): void
  onSettingsOpen(this: void): void
  onAppearanceOpen(this: void): void            // ← line 51 → onAdvancedOpen
  onBackToPractice(this: void): void
  onBackFromAppearance(this: void): void        // ← line 53 → onBackFromAdvanced
}
```

<!-- Extracted from src/app/appControllerAdapters.ts:213-227 — createAppNavigationViewModel() factory (Option A rename target) -->
```typescript
export function createAppNavigationViewModel({
  navigation,
}: {
  navigation: AppNavigation
}): AppNavigationViewModel {
  return {
    appScreen: navigation.appScreen,
    returningFromAppearance: navigation.returningFromAppearance,    // ← line 220 → returningFromAdvanced: navigation.returningFromAdvanced
    onLearnOpen: navigation.onLearnOpen,
    onSettingsOpen: navigation.onSettingsOpen,
    onAppearanceOpen: navigation.onAppearanceOpen,                  // ← line 223 → onAdvancedOpen: navigation.onAdvancedOpen
    onBackToPractice: navigation.onBackToPractice,
    onBackFromAppearance: navigation.onBackFromAppearance,          // ← line 225 → onBackFromAdvanced: navigation.onBackFromAdvanced
  }
}
```

<!-- Plan 01 interface (already landed by Wave 1) -->
```typescript
// src/hooks/useBypassSilentModeChoice.ts
export function useBypassSilentModeChoice(): {
  bypassSilentMode: boolean
  setBypassSilentMode: (next: boolean) => void
}
```

<!-- Plan 02 interface (already landed by Wave 1) -->
```typescript
// src/hooks/useBreathingSessionController.ts — accepts optional bypassSilentMode
interface UseBreathingSessionControllerArgs {
  // ... existing props ...
  bypassSilentMode?: boolean  // ← passed down to useAudioCues.start
}
```
</interfaces>

<atomic_rename_grep_gates>
After this plan lands, all four of these greps MUST return zero hits in src/:

  grep -rn "'appearance'" src/                      # enum literal, route case
  grep -rn "\\.appearance\\b" src/                  # strings-tree access
  grep -rn "AppearancePage" src/                    # file/component name
  grep -rn "appearance\\.sections\\.visual" src/    # section-key access (covered by .appearance grep but listed for clarity)

The grep below MUST return zero hits if Option A is implemented correctly
(covers `appViewModel.ts` lines 48/51/53 + `appControllerAdapters.ts` lines
220/223/225 + `useAppNavigation.ts` + `useAppViewModel.ts` + `AppSettingsPage.tsx`
+ `ScreenRouter.tsx`):

  grep -rn "onAppearanceOpen\\|onBackFromAppearance\\|returningFromAppearance" src/

Exception (intentional): the Phase 48 describe block in src/content/strings.test.ts
titled `'Phase 48 appearance.* and theme rename'` references the historical
`appSettings.sections.appearance` key (line 247) — that key is part of the
Phase 48 historical test and references a key that itself was renamed to
`theme` in Phase 48 (the test asserts the rename happened). The describe-block
title MAY stay (it documents Phase 48 history); the assertions about the
current strings tree MUST flip to `.advanced` access.
</atomic_rename_grep_gates>

</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Atomic D-11 rename — strings tree + AppScreen enum + ScreenRouter + page-file rename + AppSettingsPage chevron + useAppNavigation callbacks + appViewModel interface + appControllerAdapters factory + useAppViewModel consumer + every consuming test (Option A symmetric callback rename) + REMOVE three obsolete PT-BR markers</name>
  <files>
    src/content/strings.ts,
    src/content/strings.test.ts,
    src/content/content.no-review-markers.test.ts,
    src/app/pages/AppearancePage.tsx,
    src/app/pages/AppearancePage.test.tsx,
    src/app/pages/AdvancedPage.tsx,
    src/app/pages/AdvancedPage.test.tsx,
    src/app/ScreenRouter.tsx,
    src/app/ScreenRouter.test.tsx,
    src/app/useAppNavigation.ts,
    src/app/useAppNavigation.test.tsx,
    src/app/appControllerAdapters.test.ts,
    src/app/appViewModel.ts,
    src/app/appControllerAdapters.ts,
    src/app/pages/AppSettingsPage.tsx,
    src/app/pages/AppSettingsPage.test.tsx,
    src/app/useAppViewModel.ts
  </files>
  <read_first>
    - src/content/strings.ts (locate readonly interface block at lines 171-200; EN tree at lines 369-378; PT-BR tree at lines 546-589 — note the EXACT pre-edit positions of the three markers that MUST be removed: line 547 above `title: 'Aparência'`, line 551 above `rightChevronAriaOnSettings: 'Configurações de aparência'`, line 556 above `visual: 'Visual'`)
    - src/content/strings.test.ts (locate the Phase 48 describe block at lines 241-275 that accesses UI_STRINGS.en.appearance and UI_STRINGS['pt-BR'].appearance)
    - src/content/content.no-review-markers.test.ts (locate line 97 with `const inAppearance = stack.includes('appearance')`; line 104 with the `if (!inAppearance && ...)` usage; header comments at lines 6-7, 14-16; assertion messages at lines 126, 134, 170, 184; the fixture-string `appearance: {` at lines 174-184)
    - src/app/pages/AppearancePage.tsx (lines 7, 10, 13, 21, 25 — imports + strings access + the `appearance` tree binding at line 25; the SectionCard at lines 67-82 with the two existing SettingsToggleRow elements)
    - src/app/pages/AppearancePage.test.tsx (lines 35, 42, 50, 64, 67, 74, 82, 100, 118 — all UI_STRINGS.en.appearance.* references; line 59 with the toHaveLength(2) assertion)
    - src/app/ScreenRouter.tsx (line 4 import; lines 41-42 case branch)
    - src/app/ScreenRouter.test.tsx (lines 17-19 vi.mock + lines 70-76 test block)
    - src/app/useAppNavigation.ts (line 3 AppScreen union; lines 8-12 callback declarations; line 30; lines 54-58 and 65-68 callback usages; line 56 with `setAppScreen('appearance')`)
    - src/app/useAppNavigation.test.tsx (lines 88, 98, 139, 151, 168 — string literal `'appearance'` + test titles + callback names)
    - src/app/appControllerAdapters.test.ts (line 259 with `appScreen: 'appearance'`; lines 256-272 with callback refs if Option A)
    - src/app/appViewModel.ts (lines 46-54 — `AppNavigationViewModel` interface declaration; the three callback identifiers at line 48 `returningFromAppearance: boolean`, line 51 `onAppearanceOpen(this: void): void`, line 53 `onBackFromAppearance(this: void): void` — verified anchors for Option A symmetric rename)
    - src/app/appControllerAdapters.ts (lines 213-227 — `createAppNavigationViewModel()` factory; the three return-object keys + `navigation.*` reads at line 220 `returningFromAppearance: navigation.returningFromAppearance,`, line 223 `onAppearanceOpen: navigation.onAppearanceOpen,`, line 225 `onBackFromAppearance: navigation.onBackFromAppearance,` — verified anchors for Option A symmetric rename. Atomic-commit invariant: this file's edits + `appViewModel.ts`'s edits MUST land in the same commit as the rest of the rename group)
    - src/app/pages/AppSettingsPage.tsx (line 42 with `allStrings.appearance`; line 78 with `strings.appearance.rightChevronAriaOnSettings`; lines 16, 32, 79 for the callback prop if Option A)
    - src/app/pages/AppSettingsPage.test.tsx (lines 91, 99 with UI_STRINGS.en.appearance.rightChevronAriaOnSettings; renderPage helper signature if Option A)
    - src/app/useAppViewModel.ts (read the file and grep for any references to `onAppearanceOpen`, `onBackFromAppearance`, `returningFromAppearance`, `onAdvancedOpen` — note these may be CONSUMED here as part of Option A; the consumer rename is required to keep the file compiling against the renamed `AppNavigationViewModel` interface. ALSO locate the `featureFlags.bypassSilentMode` wire-up site near line 55-63 where `const featureFlags = useFeatureFlags()` is in scope and `useBreathingSessionController` is called — this is the end-to-end wire-up site for ADV-04 + ADV-05)
    - .planning/phases/49.1-bypass-silent-mode-toggle/49.1-PATTERNS.md (every per-file subsection from line 542 onward; the Pattern F atomic-rename invariant; the `src/app/appViewModel.ts` and `src/app/appControllerAdapters.ts` per-file subsections that document the verified anchor lines for Option A)
  </read_first>
  <behavior>
    - After the rename, `grep -rn "'appearance'" src/` returns zero hits (every enum literal flipped to 'advanced').
    - After the rename, `grep -rn "\\.appearance\\b" src/` returns zero hits (every strings-tree access flipped to .advanced).
    - After the rename, `grep -rn "AppearancePage" src/` returns zero hits (file renamed; all imports updated).
    - After the rename, `grep -rn "onAppearanceOpen\\|onBackFromAppearance\\|returningFromAppearance" src/` returns zero hits (Option A symmetric callback rename complete — covers `appViewModel.ts` interface lines 48/51/53 + `appControllerAdapters.ts` factory lines 220/223/225 + all consumers).
    - The Phase 48 strings.test.ts describe block (lines 241-275) — its title can stay (documents history); its assertions about the CURRENT strings tree MUST read UI_STRINGS.en.advanced and UI_STRINGS['pt-BR'].advanced.
    - The content.no-review-markers.test.ts guard still PASSES: the variable rename `inAppearance` → `inAdvanced` and the stack literal `'appearance'` → `'advanced'` is a pure rename (not extend); the allowlist's structural scope is unchanged.
    - The renamed AdvancedPage.tsx renders three SettingsToggleRow elements in the Behavior section (Task 2 adds the third; this task just renames + repositions).
    - All existing strings.test.ts assertions about the appearance.* keys flip to advanced.* and continue to pass.
    - Per D-04: zero new // TODO: native-speaker review markers are added to the strings tree; three OBSOLETE markers (above the OLD `title: 'Aparência'`, `rightChevronAriaOnSettings: 'Configurações de aparência'`, and `visual: 'Visual'` values, at pre-edit lines 547/551/556) MUST be REMOVED because those values flip to operator-confirmed strings; the remaining markers under the renamed tree's pre-existing keys (above `backChevron`, `orb.*`, `ringCue.*`, `breathingEffect.label`, `switcherIcons.label`, `sections.orbStyle`) remain unchanged.
  </behavior>
  <action>
    **CRITICAL D-11 INVARIANT: this entire task lands as ONE git commit.** Splitting any subset across commits breaks per-commit green-gate (`tsc && lint && build && test`) because TypeScript would reject a half-renamed enum or strings tree at any intermediate state. The executor MUST stage all edits, run the full green gate, and commit atomically. **The Option A interface (`appViewModel.ts`) + factory (`appControllerAdapters.ts`) rename pair is a hard atomic subset within the larger commit** — TypeScript would reject a half-rename of the interface vs the factory.

    Mirror the per-file subsections in PATTERNS.md (the "Pattern Assignments" section starting at line 78). Edit each anchor exactly as PATTERNS.md spells out:

    **(1) src/content/strings.ts** (Pattern: rename + add key + REMOVE three obsolete markers)
      - Readonly interface block (lines 171-200): rename `appearance:` → `advanced:`; rename `sections.visual` → `sections.behavior`; add `readonly bypassSilentMode: { readonly label: string }`.
      - EN tree (lines 369-378): rename `appearance:` → `advanced:`; flip `title: 'Appearance'` → `title: 'Advanced'`; flip `rightChevronAriaOnSettings: 'Appearance settings'` → `'Advanced settings'`; flip `sections.visual: 'Visual'` → `sections.behavior: 'Behavior'`; add `bypassSilentMode: { label: 'Bypass silent mode' }`.
      - PT-BR tree (lines 546-589): rename `appearance:` → `advanced:`; flip `title: 'Aparência'` → `'Avançado'` (operator-confirmed, NO marker per D-04); flip `rightChevronAriaOnSettings: 'Configurações de aparência'` → `'Configurações avançadas'` (operator-confirmed, NO marker per D-04); flip `sections.visual: 'Visual'` → `sections.behavior: 'Comportamento'` (operator-confirmed, NO marker per D-04); add `bypassSilentMode: { label: 'Ignorar modo silencioso' }` (operator-confirmed, NO marker per D-04).
      - **REMOVE three obsolete markers (D-04 — operator-confirmed strings MUST NOT carry the review marker):**
        - REMOVE the `// TODO: native-speaker review` comment line at **pre-edit line 547** (the line directly above `title: 'Aparência'` at pre-edit line 548) — the flipped value `'Avançado'` is operator-confirmed.
        - REMOVE the `// TODO: native-speaker review` comment line at **pre-edit line 551** (the line directly above `rightChevronAriaOnSettings: 'Configurações de aparência'` at pre-edit line 552) — the flipped value `'Configurações avançadas'` is operator-confirmed.
        - REMOVE the `// TODO: native-speaker review` comment line at **pre-edit line 556** (the line directly above `visual: 'Visual'` at pre-edit line 557 — the key itself is replaced by `behavior: 'Comportamento'`) — the new key + value are both operator-confirmed.
      - The remaining `// TODO: native-speaker review` markers under the renamed tree's pre-existing keys (`backChevron`, `orb.*`, `ringCue.*`, `breathingEffect.label`, `switcherIcons.label`, `sections.orbStyle`) STAY in place verbatim — they cover Phase 48 strings whose VALUES are unchanged.

    **(2) src/content/strings.test.ts** (Phase 48 describe block, lines 241-275)
      - The describe block title `'Phase 48 appearance.* and theme rename'` MAY stay (documents Phase 48 history). The historical assertion about `appSettings.sections.appearance` being absent (line 247) stays unchanged (that key was renamed to `theme` in Phase 48 — the assertion still holds).
      - Every assertion accessing UI_STRINGS.en.appearance (line 252) or UI_STRINGS['pt-BR'].appearance (line 270) MUST flip to .advanced.
      - Every assertion of literal copy values (`'Appearance'`, `'Aparência'`, `'Visual'`, etc.) MUST flip to the new values (`'Advanced'`, `'Avançado'`, `'Behavior'` / `'Comportamento'`). The test acts as the value-locking gate for D-01/D-02.

    **(3) src/content/content.no-review-markers.test.ts** (rename, not extend)
      - Line 97: rename `const inAppearance = stack.includes('appearance')` → `const inAdvanced = stack.includes('advanced')`.
      - Line 104 (or wherever the variable is used): rename `if (!inAppearance && ...)` → `if (!inAdvanced && ...)`.
      - Header comments at lines 6-7, 14-16: replace `appearance` text references with `advanced`.
      - Assertion messages at lines 126, 134, 170, 184: replace `appearance` text references with `advanced`.
      - Fixture-string `appearance: {` in synthetic test inputs (lines 174-184): rename to `advanced: {` and flip the test title to `'allows a marker inside advanced.* (any descendant)'`.

    **(4) RENAME src/app/pages/AppearancePage.tsx → src/app/pages/AdvancedPage.tsx** (via `git mv` to preserve history)
      - Inside the renamed file:
        - `export function AppearancePage(...)` → `export function AdvancedPage(...)`
        - `export interface AppearancePageProps` → `export interface AdvancedPageProps`
        - `useUiStrings().appearance` (line 25) → `useUiStrings().advanced`
        - The Behavior SectionCard's section-header label `strings.sections.visual` (line 66) → `strings.sections.behavior`
      - Task 2 will then add the 3rd SettingsToggleRow + the useBypassSilentModeChoice hook binding in this same file.

    **(5) RENAME src/app/pages/AppearancePage.test.tsx → src/app/pages/AdvancedPage.test.tsx** (via `git mv`)
      - Line 10 import: rename `AppearancePage` → `AdvancedPage`.
      - Lines 14-17 render call: rename `AppearancePage` → `AdvancedPage`.
      - Every `UI_STRINGS.en.appearance.*` reference (lines 35, 42, 50, 64, 67, 74, 82, 100, 118) → `UI_STRINGS.en.advanced.*`.
      - Task 2 will then update the toggle-count assertion + add 3 new ADV-03 tests.

    **(6) src/app/ScreenRouter.tsx**
      - Line 4 import: rename `AppearancePage` → `AdvancedPage`; path stays consistent with the renamed file.
      - Lines 41-42 case branch: `case 'appearance'` → `case 'advanced'`; `<AppearancePage onBack={vm.navigation.onBackFromAppearance} />` → `<AdvancedPage onBack={vm.navigation.onBackFromAdvanced} />` (Option A callback rename).

    **(7) src/app/ScreenRouter.test.tsx**
      - Lines 17-19: rename `vi.mock('./pages/AppearancePage', ...)` → `vi.mock('./pages/AdvancedPage', ...)`; rename test-id `appearance-page` → `advanced-page`; rename mock-component name `AppearancePage` → `AdvancedPage`.
      - Lines 70-76: rename test title `'renders AppearancePage when appScreen=appearance'` → `'renders AdvancedPage when appScreen=advanced'`; rename `makeVmForScreen('appearance')` → `makeVmForScreen('advanced')`; rename `getByTestId('appearance-page')` → `getByTestId('advanced-page')`.

    **(8) src/app/useAppNavigation.ts**
      - Line 3: rename `AppScreen` union literal `'appearance'` → `'advanced'`.
      - Option A callback renames: `onAppearanceOpen` → `onAdvancedOpen`; `onBackFromAppearance` → `onBackFromAdvanced`; `returningFromAppearance` → `returningFromAdvanced`; `setReturningFromAppearance` → `setReturningFromAdvanced` (lines 8-12, 30, 54-58, 65-68 per PATTERNS.md anchor list).
      - Line 56: `setAppScreen('appearance')` → `setAppScreen('advanced')`.

    **(9) src/app/useAppNavigation.test.tsx**
      - Lines 88, 98, 139, 151, 168: rename string literal `'appearance'` → `'advanced'` AND rename test titles that mention "appearance".
      - Option A: rename callback identifiers (`onAppearanceOpen` → `onAdvancedOpen`, etc.) wherever they appear.

    **(10) src/app/appControllerAdapters.test.ts**
      - Line 259: `appScreen: 'appearance'` → `appScreen: 'advanced'`.
      - Option A: lines 256-272 — rename callback identifiers symmetrically.

    **(11) src/app/appViewModel.ts** (Option A — interface declaration)
      - Line 48: `returningFromAppearance: boolean` → `returningFromAdvanced: boolean` (on the `AppNavigationViewModel` interface).
      - Line 51: `onAppearanceOpen(this: void): void` → `onAdvancedOpen(this: void): void`.
      - Line 53: `onBackFromAppearance(this: void): void` → `onBackFromAdvanced(this: void): void`.
      - These three identifiers are the interface contract that `appControllerAdapters.ts` produces and `useAppViewModel.ts` / `ScreenRouter.tsx` / `AppSettingsPage.tsx` consume. **TypeScript will reject the build unless these flip in lockstep with the factory in (12) — they MUST be in the same commit.**

    **(12) src/app/appControllerAdapters.ts** (Option A — factory implementation)
      - Line 220: `returningFromAppearance: navigation.returningFromAppearance,` → `returningFromAdvanced: navigation.returningFromAdvanced,`
      - Line 223: `onAppearanceOpen: navigation.onAppearanceOpen,` → `onAdvancedOpen: navigation.onAdvancedOpen,`
      - Line 225: `onBackFromAppearance: navigation.onBackFromAppearance,` → `onBackFromAdvanced: navigation.onBackFromAdvanced,`
      - The `navigation` source object is the return value of `useAppNavigation()` — which is also renamed in (8). **TypeScript will reject the build unless these flip in lockstep with the interface in (11) AND with `useAppNavigation.ts` in (8) — they MUST be in the same commit.**

    **(13) src/app/pages/AppSettingsPage.tsx**
      - Line 42: rename `allStrings.appearance` → `allStrings.advanced`.
      - Line 78: rename `strings.appearance.rightChevronAriaOnSettings` → `strings.advanced.rightChevronAriaOnSettings`.
      - Option A: lines 16 + 32 + 79 — rename callback prop `onAppearanceOpen` → `onAdvancedOpen` (interface declaration + call site).

    **(14) src/app/pages/AppSettingsPage.test.tsx**
      - Lines 91 + 99: `UI_STRINGS.en.appearance.rightChevronAriaOnSettings` → `UI_STRINGS.en.advanced.rightChevronAriaOnSettings`.
      - Option A: rename renderPage helper signature props to use `onAdvancedOpen`.

    **(15) src/app/useAppViewModel.ts**
      - Option A callback renames: any consumer reference to the renamed identifiers from (11) — `onAppearanceOpen`, `onBackFromAppearance`, `returningFromAppearance` — flips to the Advanced counterpart. Grep first to enumerate every consumer site in this file before editing.
      - **End-to-end wire-up (the data path for ADV-04 + ADV-05):** locate where `useBreathingSessionController` is called (around line 57-63 per PATTERNS.md), and add `bypassSilentMode: featureFlags.bypassSilentMode` (or pass via prop bag — match the existing pattern in this file). `featureFlags` is already in scope at line 55 (`const featureFlags = useFeatureFlags()`).

    **Hint for LSP-based renames:** per the user's standing rule, prefer LSP rename over sed/perl/regex for symbol/property renames across files. The atomic-rename group above has both string-literal renames (route enum, vi.mock paths, test-id strings, fixture strings — these MUST be text edits because LSP cannot rename literals) AND symbol renames (callback identifiers, component names, interface members — these SHOULD use LSP rename). Strategy: (a) use LSP rename for `AppearancePage` → `AdvancedPage`, for each of the three callback identifiers (LSP rename triggered on the interface declarations at `appViewModel.ts:48/51/53` will automatically propagate to the factory at `appControllerAdapters.ts:220/223/225` and to every consumer), and for the `appearance` tree access pattern via the readonly interface field; (b) use targeted text edits for the string literals and the marker-removal lines in `strings.ts`.

    **Verification step before commit:** run all four grep gates listed in atomic_rename_grep_gates above. All four MUST return zero hits in src/ before staging. Then verify the three required marker removals via `grep -nE "// TODO: native-speaker review" src/content/strings.ts` — the count should be reduced by exactly 3 from the pre-edit count. Then run the full green gate: `pnpm tsc --noEmit && pnpm lint && pnpm build && pnpm test`. Only after all five gates are green, stage everything and commit as a single commit.
  </action>
  <verify>
    <automated>
    pnpm tsc --noEmit &&
    pnpm lint &&
    pnpm build &&
    pnpm test &&
    test "$(grep -rn \"'appearance'\" src/ | grep -v '^#' | wc -l)" -eq 0 &&
    test "$(grep -rn \"\\.appearance\\b\" src/ | grep -v '^#' | wc -l)" -eq 0 &&
    test "$(grep -rn \"AppearancePage\" src/ | grep -v '^#' | wc -l)" -eq 0 &&
    test "$(grep -rn \"onAppearanceOpen\\|onBackFromAppearance\\|returningFromAppearance\" src/ | grep -v '^#' | wc -l)" -eq 0
    </automated>
  </verify>
  <acceptance_criteria>
    - File system state: src/app/pages/AppearancePage.tsx and src/app/pages/AppearancePage.test.tsx DO NOT EXIST. src/app/pages/AdvancedPage.tsx and src/app/pages/AdvancedPage.test.tsx EXIST. (Verifiable via `test -f src/app/pages/AdvancedPage.tsx && ! test -f src/app/pages/AppearancePage.tsx`.)
    - grep -rn "'appearance'" src/ returns zero hits (D-10 enum-literal invariant; ADV-01 negative gate).
    - grep -rn "\\.appearance\\b" src/ returns zero hits (D-10 strings-tree-access invariant).
    - grep -rn "AppearancePage" src/ returns zero hits (file-rename invariant).
    - grep -rn "onAppearanceOpen\\|onBackFromAppearance\\|returningFromAppearance" src/ returns zero hits (Option A invariant — covers `appViewModel.ts` lines 48/51/53 + `appControllerAdapters.ts` lines 220/223/225 + every consumer).
    - grep -nE "onAdvancedOpen|onBackFromAdvanced|returningFromAdvanced" src/app/appViewModel.ts returns ≥ 3 hits (the three renamed interface members at former lines 48/51/53).
    - grep -nE "onAdvancedOpen|onBackFromAdvanced|returningFromAdvanced" src/app/appControllerAdapters.ts returns ≥ 6 hits (the three return-object keys + the three `navigation.*` reads at former lines 220/223/225).
    - grep -nE "title: 'Advanced'" src/content/strings.ts shows the EN copy lock; grep -nE "title: 'Avançado'" src/content/strings.ts shows the PT-BR copy lock (D-01 / ADV-01).
    - grep -nE "behavior: 'Behavior'" src/content/strings.ts shows EN section header; grep -nE "behavior: 'Comportamento'" src/content/strings.ts shows PT-BR (D-02 / ADV-02).
    - grep -nE "bypassSilentMode: \\{ label: 'Bypass silent mode' \\}" src/content/strings.ts shows the new EN key; grep -nE "bypassSilentMode: \\{ label: 'Ignorar modo silencioso' \\}" src/content/strings.ts shows PT-BR (D-03).
    - grep -nE "appearance\\.sections\\.visual" src/ returns zero hits (D-02 negative gate).
    - **D-04 marker removal verification (Warning #5 — non-negotiable):** the three obsolete markers MUST be removed. Verify each:
      - Capture the pre-edit count via `git show HEAD:src/content/strings.ts | grep -c '// TODO: native-speaker review'`; the post-edit count via `grep -c '// TODO: native-speaker review' src/content/strings.ts` MUST be exactly `pre_count - 3` (one removed marker per flipped value at pre-edit lines 547/551/556).
      - Direct positional check: the post-edit line immediately above `title: 'Avançado'` MUST NOT contain `// TODO: native-speaker review` — verify via `grep -B1 "title: 'Avançado'" src/content/strings.ts | head -2 | grep -c '// TODO: native-speaker review'` returns 0.
      - Same for `rightChevronAriaOnSettings: 'Configurações avançadas'`: `grep -B1 "rightChevronAriaOnSettings: 'Configurações avançadas'" src/content/strings.ts | head -2 | grep -c '// TODO: native-speaker review'` returns 0.
      - Same for `behavior: 'Comportamento'`: `grep -B1 "behavior: 'Comportamento'" src/content/strings.ts | head -2 | grep -c '// TODO: native-speaker review'` returns 0.
    - The 3 newly-added strings (advanced.title, advanced.sections.behavior, advanced.bypassSilentMode.label) have NO `// TODO: native-speaker review` marker above them in the PT-BR tree (D-04 — covered by the marker-removal assertions above).
    - useAppViewModel.ts passes `bypassSilentMode: featureFlags.bypassSilentMode` (or equivalent prop-bag entry) into useBreathingSessionController — verify via `grep -nE "bypassSilentMode.*featureFlags" src/app/useAppViewModel.ts`.
    - content.no-review-markers.test.ts STILL PASSES after the rename — pnpm test src/content/content.no-review-markers.test.ts is green (the structural allowlist scope is preserved; only the name flipped, and the three marker removals are within the allowlisted tree so they don't trip any guard).
    - pnpm tsc --noEmit && pnpm lint && pnpm build && pnpm test ALL pass — D-11 per-commit green-gate invariant.
  </acceptance_criteria>
  <done>The Appearance → Advanced rename is complete end-to-end in a single commit; all four grep gates return zero; `appViewModel.ts` (lines 48/51/53) + `appControllerAdapters.ts` (lines 220/223/225) interface/factory pair flip in lockstep; three obsolete PT-BR markers at pre-edit lines 547/551/556 are removed; tsc + lint + build + test pass; useAppViewModel wires bypassSilentMode through to useBreathingSessionController (Plan 02 entry point).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add Bypass-silent-mode toggle row to AdvancedPage + 3 ADV-03 tests (binds Plan 01's useBypassSilentModeChoice hook)</name>
  <files>src/app/pages/AdvancedPage.tsx, src/app/pages/AdvancedPage.test.tsx</files>
  <read_first>
    - src/app/pages/AdvancedPage.tsx (after Task 1's rename; locate the Behavior SectionCard with the two existing SettingsToggleRow elements; locate the existing useSwitcherIconChoice import + binding as the structural analog)
    - src/hooks/useBypassSilentModeChoice.ts (Plan 01 — confirm exact import path and the return-value destructuring shape)
    - src/components/SettingsToggleRow (confirm the props interface — label, ariaLabel, checked, onChange — by reading its export signature)
    - src/app/pages/AdvancedPage.test.tsx (after Task 1's rename; locate the existing "writes switcherIcon=true to the prefs envelope when Switcher icons is toggled on" test at lines 109-125 as the analog for the new envelope-write test; locate line 59 with the `toHaveLength(2)` assertion that must flip to 3)
    - .planning/phases/49.1-bypass-silent-mode-toggle/49.1-PATTERNS.md (AppearancePage.tsx subsection lines 672+ for the exact insertion point + the D-09 inline-comment requirement; AppearancePage.test.tsx subsection for the 3 new test shapes)
  </read_first>
  <behavior>
    - Test 1 (ADV-03 order): renderPage(); screen.getAllByRole('switch') returns 3 elements; switches[2] has accessible name UI_STRINGS.en.advanced.bypassSilentMode.label ('Bypass silent mode'); confirms the new toggle is positioned BELOW the existing breathingEffect + switcherIcons toggles (D-03 order constraint).
    - Test 2 (ADV-03 default ON): renderPage() with empty localStorage; the bypass-silent-mode switch is `toBeChecked()` (default-on per D-05; reads via Plan 01's hook which reads Plan 01's DEFAULT_PREFS).
    - Test 3 (ADV-03 envelope write): renderPage(); userEvent.click(the bypass-silent-mode switch); read localStorage; the envelope's `prefs.bypassSilentMode === false` (toggle from default true to false writes the new value via Plan 01's setBypassSilentMode → savePrefs).
    - All existing AdvancedPage tests (renamed from AppearancePage.test.tsx by Task 1) continue to pass — the 2-switch assertion is updated to 3.
  </behavior>
  <action>
    Two anchor edits in src/app/pages/AdvancedPage.tsx (file already renamed + string-tree-access updated by Task 1):

    (1) Add import next to the existing `useSwitcherIconChoice` import (around line 13):
        `import { useBypassSilentModeChoice } from '../../hooks/useBypassSilentModeChoice'`

    (2) Bind the hook at the top of the function body (immediately after the existing `const { switcherIcon, setSwitcherIcon } = useSwitcherIconChoice()` line):
        `const { bypassSilentMode, setBypassSilentMode } = useBypassSilentModeChoice()`

    (3) Append a third SettingsToggleRow below the existing two in the Behavior SectionCard (after the SwitcherIcons row at line 76-81). Use the shape:
        - `label={strings.bypassSilentMode.label}`
        - `ariaLabel={strings.bypassSilentMode.label}`
        - `checked={bypassSilentMode}`
        - `onChange={setBypassSilentMode}`

    (4) Per CONTEXT.md D-09, add a brief inline JSX comment immediately above the new toggle explaining that mid-session toggles apply on the next engine construction (NOT live). The comment text should reference D-09 and explain that the audio engine reads the flag at start time + reconstruct time via the captured `bypassSilentModeRef` (Plan 02).

    In src/app/pages/AdvancedPage.test.tsx:

    (5) Update the existing toggle-count assertion at line 59 from `expect(switches).toHaveLength(2)` to `expect(switches).toHaveLength(3)`; update the test title to reference three switches.

    (6) Add 3 new tests as a new describe block titled `'Phase 49.1 D-03 — Bypass silent mode toggle (ADV-03)'`. Mirror the existing switcherIcon envelope-write test at lines 109-125 for the third test. The exact shapes are described in the <behavior> block above. Use `UI_STRINGS.en.advanced.bypassSilentMode.label` for accessible-name assertions.
  </action>
  <verify>
    <automated>pnpm test src/app/pages/AdvancedPage.test.tsx -- --reporter=verbose</automated>
  </verify>
  <acceptance_criteria>
    - grep -n "useBypassSilentModeChoice" src/app/pages/AdvancedPage.tsx returns ≥ 2 (import + binding).
    - grep -nE "<SettingsToggleRow[^/]*bypassSilentMode" src/app/pages/AdvancedPage.tsx returns ≥ 1 (the new toggle row).
    - grep -nE "checked=\\{bypassSilentMode\\}" src/app/pages/AdvancedPage.tsx returns 1.
    - grep -nE "onChange=\\{setBypassSilentMode\\}" src/app/pages/AdvancedPage.tsx returns 1.
    - grep -n "D-09" src/app/pages/AdvancedPage.tsx returns ≥ 1 (the inline comment is present).
    - AdvancedPage.test.tsx contains a new describe block matching /Phase 49\.1 D-03/ with exactly 3 it-blocks.
    - One of the new tests asserts `expect(envelope.prefs.bypassSilentMode).toBe(false)` after a click on the bypass toggle (D-03 persistence gate).
    - The toggle-count assertion in the existing test reads `toHaveLength(3)` (not 2).
    - pnpm test src/app/pages/AdvancedPage.test.tsx passes; verbose output shows all new tests pass.
  </acceptance_criteria>
  <done>The third Bypass silent mode SettingsToggleRow renders below the existing two in the Behavior SectionCard; defaults checked; click persists `bypassSilentMode: false` to the prefs envelope; 3 new ADV-03 tests pass; D-09 inline comment present.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User → AdvancedPage toggle | click event flows through useBypassSilentModeChoice → savePrefs → localStorage |
| useAppViewModel → useBreathingSessionController | featureFlags.bypassSilentMode flows from query/persisted prefs into the controller props |
| controller → useAudioCues.start → createAudioEngine | end-to-end data path completes here (Plan 02 owns the gate predicate) |
| atomic-rename commit boundary | per-commit green-gate (D-11) — any intermediate state would fail tsc |
| `appViewModel.ts` interface ↔ `appControllerAdapters.ts` factory | Option A symmetric rename — interface lines 48/51/53 + factory lines 220/223/225 MUST flip in lockstep within the same commit |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-49.1.03-01 | Tampering | Atomic-rename half-state (e.g., enum flipped but strings tree not) reaches main | mitigate | D-11 invariant enforced: all edits in one commit; pre-commit hooks would also catch via tsc/lint. Acceptance criteria require all 4 grep gates green BEFORE commit |
| T-49.1.03-02 | Spoofing | Wrong tree accessed (e.g., `strings.appearance` lingers, returns undefined → runtime crash) | mitigate | TypeScript catches this at tsc time because the readonly interface block is renamed in lockstep; the grep gate catches lingering `.appearance` text access |
| T-49.1.03-03 | Information disclosure | New PT-BR string `Ignorar modo silencioso` exposed to non-PT-BR users via mis-localization | accept | i18n routing is unchanged from Phase 48; the new key is registered in both locale trees with operator-confirmed values per D-04 |
| T-49.1.03-04 | Tampering | featureFlags.bypassSilentMode reaches useBreathingSessionController as undefined (Plan 01 not landed) | mitigate | depends_on: [49.1-01, 49.1-02] enforces wave ordering; tsc would error if FeatureFlags.bypassSilentMode is missing |
| T-49.1.03-05 | Repudiation | Click on toggle does not persist to localStorage | mitigate | Task 2 Test 3 asserts envelope.prefs.bypassSilentMode === false after a click — closes the gap |
| T-49.1.03-06 | Denial of service | content.no-review-markers.test.ts guard breaks after rename, allowing markers to leak into the renamed tree | mitigate | Task 1 acceptance criterion requires this test to pass post-rename; rename of `inAppearance` → `inAdvanced` preserves the guard's structural scope |
| T-49.1.03-07 | Tampering | `AppNavigationViewModel` interface (`appViewModel.ts`) and `createAppNavigationViewModel()` factory (`appControllerAdapters.ts`) drift apart — interface renamed but factory not (or vice versa) | mitigate | Both files added to atomic-commit surface; TypeScript will reject the build immediately. Acceptance criterion asserts ≥ 6 hits in `appControllerAdapters.ts` and ≥ 3 hits in `appViewModel.ts` for the renamed identifiers, plus zero hits for the old names anywhere in src/ |
| T-49.1.03-08 | Repudiation | Obsolete PT-BR markers above flipped values (pre-edit lines 547/551/556) silently retained → leaks deprecated review markers on operator-confirmed strings, violating D-04 | mitigate | Task 1 action explicitly requires REMOVAL of the three markers; acceptance criteria include both a marker-count delta assertion AND three positional `grep -B1` checks that verify no marker comment sits directly above each flipped value |
| T-49.1.03-SC | Tampering | npm install (no new packages this plan) | mitigate | DEPS-01: git diff package.json is empty at plan close |
</threat_model>

<verification>
- Both tasks pass their individual pnpm test commands.
- Full per-commit green gate: pnpm tsc --noEmit && pnpm lint && pnpm build && pnpm test green at every commit.
- All four atomic-rename grep gates return zero hits in src/ (see <atomic_rename_grep_gates> block).
- The three obsolete PT-BR markers at pre-edit lines 547/551/556 are removed (count delta verified; positional `grep -B1` checks all return 0).
- git diff package.json is empty (DEPS-01).
- Manual operator check (deferrable to phase-VERIFICATION pass): open the app, navigate Settings → trailing chevron, confirm the page title reads "Advanced", confirm three switches in the Behavior section, toggle bypass off, reload, confirm persistence.
</verification>

<success_criteria>
- ADV-01 (full): App Settings → trailing chevron → page titled "Advanced" (EN) / "Avançado" (PT-BR); `grep -rn "'Appearance'\\|'Aparência'" src/content/strings.ts` returns zero hits.
- ADV-02 (full): Behavior section header reads "Behavior" / "Comportamento"; key `advanced.sections.behavior` present; `advanced.sections.visual` absent.
- ADV-03 (full): third SettingsToggleRow labeled Bypass silent mode / Ignorar modo silencioso, default checked, persists across reloads via Plan 01's hook + persistence.
- ADV-04 + ADV-05 (full end-to-end data path): useAppViewModel passes featureFlags.bypassSilentMode → useBreathingSessionController → useAudioCues.start → createAudioEngine opts (Plan 02 gate). Plan 02 owns the audio-side verification; this plan owns the wire-up.
- D-11 atomic commit: all edits land in one git commit; per-commit green-gate holds. `appViewModel.ts` interface (lines 48/51/53) + `appControllerAdapters.ts` factory (lines 220/223/225) Option A rename pair flips in lockstep.
- D-04 i18n parity: zero new // TODO: native-speaker review markers on the operator-confirmed strings; three obsolete markers at pre-edit lines 547/551/556 REMOVED; the remaining markers under the renamed `advanced.*` tree stay unchanged on a pre/post comparison (minus the three required removals).
- DEPS-01 + QUAL-01: package.json untouched; full green gate at commit time.
</success_criteria>

<output>
Create `.planning/phases/49.1-bypass-silent-mode-toggle/49.1-03-SUMMARY.md` when done.
</output>
