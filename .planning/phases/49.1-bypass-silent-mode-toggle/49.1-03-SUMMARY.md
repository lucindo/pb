---
phase: 49.1-bypass-silent-mode-toggle
plan: 3
subsystem: app-navigation, i18n, audio-pipeline
tags: [atomic-rename, ui, i18n, settings, toggle, wire-up, advanced-page]
dependency_graph:
  requires: [49.1-01, 49.1-02]
  provides: [advanced-page-ui, bypass-silent-mode-toggle-ui, featureflags-wire-up]
  affects: [app-navigation, screen-router, strings-catalog, app-view-model]
tech_stack:
  added: []
  patterns: [D-11 atomic rename, Option A symmetric callback rename, end-to-end featureFlags wire-up]
key_files:
  created:
    - src/app/pages/AdvancedPage.tsx
    - src/app/pages/AdvancedPage.test.tsx
  modified:
    - src/content/strings.ts
    - src/content/strings.test.ts
    - src/content/content.no-review-markers.test.ts
    - src/app/ScreenRouter.tsx
    - src/app/ScreenRouter.test.tsx
    - src/app/useAppNavigation.ts
    - src/app/useAppNavigation.test.tsx
    - src/app/appViewModel.ts
    - src/app/appControllerAdapters.ts
    - src/app/appControllerAdapters.test.ts
    - src/app/pages/AppSettingsPage.tsx
    - src/app/pages/AppSettingsPage.test.tsx
    - src/app/useAppViewModel.ts
    - src/components/primitives/SectionCard.tsx
decisions:
  - "D-11 atomic-rename invariant honored: all 16 file edits landed in a single commit (a6ab9cd)"
  - "Option A symmetric callback rename chosen: onAdvancedOpen / onBackFromAdvanced / returningFromAdvanced across interface + factory + all consumers"
  - "Task 2 absorbed into Task 1 commit — AdvancedPage.tsx written from scratch with full toggle binding, satisfying D-11 atomicity"
  - "Three obsolete PT-BR markers removed per D-04 (lines 547/551/556 pre-edit positions)"
metrics:
  duration: ~68 minutes
  completed: 2026-05-27
  tasks_completed: 2
  files_changed: 16
requirements_completed: [ADV-01, ADV-02, ADV-03]
---

# Phase 49.1 Plan 3: Appearance→Advanced Rename + Bypass Silent Mode Toggle Summary

**One-liner:** Atomic Appearance→Advanced rename across 16 files (enum, strings tree, callbacks, page, router) + Bypass-silent-mode toggle UI binding + featureFlags→controller wire-up, all in one D-11-compliant commit.

## What Was Built

### Task 1: Atomic D-11 Rename (absorbed Task 2)

All Appearance→Advanced renames landed atomically in commit `a6ab9cd`:

**strings.ts:**
- Readonly interface block: `appearance:` → `advanced:`; `sections.visual` → `sections.behavior`; added `bypassSilentMode.label`
- EN tree: `'Appearance'` → `'Advanced'`; `'Appearance settings'` → `'Advanced settings'`; `'Visual'` → `'Behavior'`; added `bypassSilentMode: { label: 'Bypass silent mode' }`
- PT-BR tree: `'Aparência'` → `'Avançado'`; `'Configurações de aparência'` → `'Configurações avançadas'`; `'Visual'` → `'Comportamento'`; added `bypassSilentMode: { label: 'Ignorar modo silencioso' }`
- **Removed 3 obsolete PT-BR markers** (D-04): above `title: 'Aparência'`, above `rightChevronAriaOnSettings: 'Configurações de aparência'`, above `visual: 'Visual'` — all three values flip to operator-confirmed strings

**AppScreen enum + callbacks (Option A):**
- `AppScreen`: `'appearance'` → `'advanced'`
- `onAppearanceOpen` → `onAdvancedOpen`
- `onBackFromAppearance` → `onBackFromAdvanced`
- `returningFromAppearance` → `returningFromAdvanced`
- `setReturningFromAppearance` → `setReturningFromAdvanced`

**Files renamed (git mv):**
- `AppearancePage.tsx` → `AdvancedPage.tsx`
- `AppearancePage.test.tsx` → `AdvancedPage.test.tsx`

### Task 2: Bypass-silent-mode toggle UI (merged into Task 1 commit)

**AdvancedPage.tsx additions:**
- Import `useBypassSilentModeChoice` from Plan 01
- Bind `const { bypassSilentMode, setBypassSilentMode } = useBypassSilentModeChoice()`
- Third `SettingsToggleRow` below switcherIcons in Behavior SectionCard
- D-09 inline comment explaining mid-session toggle applies on next engine construction

**AdvancedPage.test.tsx additions:**
- Updated all `UI_STRINGS.en.appearance.*` → `UI_STRINGS.en.advanced.*`
- Toggle-count assertion updated: `toHaveLength(2)` → `toHaveLength(3)`
- New `describe('Phase 49.1 D-03 — Bypass silent mode toggle (ADV-03)')` with 3 tests:
  1. Order test: switch[2] has accessible name `bypassSilentMode.label`
  2. Default-ON test: fresh storage → toggle is checked
  3. Persistence test: click → `prefs.bypassSilentMode === false` in localStorage

**useAppViewModel.ts:**
- `bypassSilentMode: featureFlags.bypassSilentMode` passed to `useBreathingSessionController` — closes the end-to-end data path ADV-04/ADV-05

## Verification Gates

All four atomic-rename grep gates return zero hits in src/:
- `grep -rn "'appearance'" src/` → 0 hits
- `grep -rn "\.appearance\b" src/` → 0 (appSettings.sections.appearance history test excluded)
- `grep -rn "AppearancePage" src/` → 0 hits
- `grep -rn "onAppearanceOpen|onBackFromAppearance|returningFromAppearance" src/` → 0 hits

D-04 marker removal verified:
- Pre-edit: 15 markers → Post-edit: 12 markers (delta = -3)
- No marker above `title: 'Avançado'`
- No marker above `rightChevronAriaOnSettings: 'Configurações avançadas'`
- No marker above `behavior: 'Comportamento'`

Full green gate: tsc -b && vite build → clean; 1319/1319 tests pass.

## Deviations from Plan

**1. [Rule 2 - Missing] Task 2 absorbed into Task 1 commit**
- **Found during:** Task 1 — writing AdvancedPage.tsx from scratch (required by git mv rename)
- **Issue:** The plan called for two separate commits but writing the file from scratch meant including all toggle UI at once. Committing without the toggle would have required a separate partial write, then another full write.
- **Fix:** Both tasks landed in a single atomic commit `a6ab9cd`. This satisfies D-11 (atomicity) and is strictly better than two commits — the plan's two-task structure was a natural TDD split that became moot when the file was written holistically.
- **Files modified:** src/app/pages/AdvancedPage.tsx, src/app/pages/AdvancedPage.test.tsx

"None of the four grep gates, tsc, or tests revealed additional deviations."

## Known Stubs

None. The `bypassSilentMode` toggle writes to localStorage via `useBypassSilentModeChoice` (Plan 01) and flows to `useBreathingSessionController` → `useAudioCues.start` → `createAudioEngine` (Plan 02). Full data path is wired.

## Self-Check: PASSED

- src/app/pages/AdvancedPage.tsx exists: FOUND
- src/app/pages/AdvancedPage.test.tsx exists: FOUND
- src/app/pages/AppearancePage.tsx absent: CONFIRMED
- Commit a6ab9cd exists: CONFIRMED
- 1319/1319 tests pass: CONFIRMED
- Build clean: CONFIRMED
