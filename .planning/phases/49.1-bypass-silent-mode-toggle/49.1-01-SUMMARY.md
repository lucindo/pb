---
phase: 49.1-bypass-silent-mode-toggle
plan: 1
subsystem: feature-flags
tags: [feature-flag, storage, hook, settings, bypass-silent-mode]
dependency_graph:
  requires: []
  provides:
    - BYPASS_SILENT_MODE_FLAG (featureFlags.ts)
    - bypassSilentMode field on FeatureFlags interface
    - bypassSilentMode field on UserPrefs + DEFAULT_PREFS
    - coerceBypassSilentMode coercer (prefs.ts)
    - useBypassSilentModeChoice hook
    - 5-key event filter in useFeatureFlags
  affects:
    - src/featureFlags.ts
    - src/storage/prefs.ts
    - src/hooks/useBypassSilentModeChoice.ts
    - src/hooks/useFeatureFlags.ts
tech_stack:
  added: []
  patterns:
    - Pattern B: _FLAG spec + per-field coercer + readFeatureFlags row (mirroring SWITCHER_ICON_FLAG)
    - Pattern A: boolean choice hook paste-and-rename (mirroring useSwitcherIconChoice)
key_files:
  created:
    - src/hooks/useBypassSilentModeChoice.ts
    - src/hooks/useBypassSilentModeChoice.test.ts
  modified:
    - src/featureFlags.ts
    - src/featureFlags.test.ts
    - src/storage/prefs.ts
    - src/storage/prefs.test.ts
    - src/hooks/useFeatureFlags.ts
    - src/hooks/useFeatureFlags.test.ts
decisions:
  - D-05: defaultValue true preserves Phase 49 shipped iOS-bypass posture for users who never toggle
  - D-06: 2-arg readFeatureFlags(search, persisted) signature kept invariant; query > persisted > default
  - D-08: detail.key === 'bypassSilentMode' is the same-tab event key
  - No STATE_VERSION bump: per-field corrupt-field coercer handles missing/corrupt fields backward-compatibly
metrics:
  duration: 501s
  completed: "2026-05-27"
  tasks_completed: 4
  tasks_total: 4
  files_modified: 6
  files_created: 2
requirements_completed: [ADV-03, ADV-05]
---

# Phase 49.1 Plan 1: Bypass Silent Mode — Data Layer Foundation Summary

## One-Liner

bypassSilentMode boolean flag wired through featureFlags + prefs coercer + choice hook + useFeatureFlags 5-key event bus pipeline, defaulting true per D-05.

## What Was Built

This plan lands the data-layer foundation for the `bypassSilentMode` feature flag. Every change mirrors the Phase 47 pattern that added the four `switcherIcon / breathingShape / ringCue / orbIdle` flags — no novel patterns introduced.

**Task 1: featureFlags.ts + featureFlags.test.ts**
- Extended `FeatureFlags` interface with 5th field `bypassSilentMode: boolean`
- Added `BYPASS_SILENT_MODE_FLAG` const (`queryParam: 'bypassSilentMode'`, `defaultValue: true`)
- Extended `readFeatureFlags` to a 5-field resolver with the bypassSilentMode row
- Added 5 new tests: FLAG export spec, 4 resolver tests (default, query-wins, persisted-wins, garbage-falls-through)

**Task 2: storage/prefs.ts + storage/prefs.test.ts**
- Extended `UserPrefs` interface to 9 fields with `bypassSilentMode: boolean`
- Extended `DEFAULT_PREFS` with `bypassSilentMode: BYPASS_SILENT_MODE_FLAG.defaultValue` (= true)
- Imported `BYPASS_SILENT_MODE_FLAG` from featureFlags
- Added `coerceBypassSilentMode` (mirror of `coerceSwitcherIcon`, default true)
- Extended `coercePrefs` with 9th row `bypassSilentMode: coerceBypassSilentMode(r.bypassSilentMode)`
- Added 5-test `coerceBypassSilentMode` describe block; extended corrupt-field tests to 9-field seeds

**Task 3: useBypassSilentModeChoice.ts + test**
- New hook (paste-and-rename from `useSwitcherIconChoice`): `useBypassSilentModeChoice()` returns `{ bypassSilentMode, setBypassSilentMode }`
- Writes via `savePrefs`; dispatches `hrv:prefs-changed` with `detail.key === 'bypassSilentMode'`
- 7 tests: default-on-empty, seeded false, optimistic state, disk write, 9-field envelope merge, event shape, setter stability

**Task 4: useFeatureFlags.ts + test**
- Added 5th `||` branch `detail.key === 'bypassSilentMode'` to same-tab event filter
- Added 5th `bypassSilentMode: persisted.bypassSilentMode` to slim projection
- Updated header comments: "4-key" → "5-key", "Phase 47 4-key set" → "Phase 47 + Phase 49.1 5-key set"
- Added 2 new tests: bypassSilentMode event-filter triggers re-read, slim projection seeding

## Tests

- Before: 1289 tests
- After: 1309 tests (+20)
- All 1309 pass

## Commits

| Task | Hash | Description |
|------|------|-------------|
| 1 | 83fb8f0 | feat(49.1-01): extend FeatureFlags with BYPASS_SILENT_MODE_FLAG (5th flag) |
| 2 | 3006b29 | feat(49.1-01): extend UserPrefs with bypassSilentMode (9-field, default true) |
| 3 | 89bcac7 | feat(49.1-01): add useBypassSilentModeChoice hook (paste-and-rename from useSwitcherIconChoice) |
| 4 | 5fe2d5f | feat(49.1-01): extend useFeatureFlags 4-key filter → 5-key + 5-field slim projection |

## Verification

- `pnpm tsc --noEmit`: CLEAN (zero errors)
- `pnpm test`: 1309/1309 pass
- `git diff package.json`: empty (DEPS-01 satisfied)
- `BYPASS_SILENT_MODE_FLAG` appears in featureFlags.ts (2 refs for const + resolver), featureFlags.test.ts (4 refs), prefs.ts (4 refs for import + DEFAULT_PREFS + coercer + coercePrefs)
- No references to `switcherIcon` in `useBypassSilentModeChoice.ts` (grep returns 0)
- `detail.key === 'bypassSilentMode'` present in both hook and filter

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Updated toEqual assertion in featureFlags.test.ts**
- **Found during:** Task 1 GREEN
- **Issue:** Existing test `'returns defaults for empty search'` deep-compared FeatureFlags without `bypassSilentMode`, causing failure after the interface was extended to 5 fields.
- **Fix:** Added `bypassSilentMode: true` to the expected object in the deep-equal assertion.
- **Files modified:** `src/featureFlags.test.ts`
- **Commit:** 83fb8f0

**2. [Rule 1 - Bug] Updated 8-field round-trip test in prefs.test.ts to 9 fields**
- **Found during:** Task 2 GREEN
- **Issue:** Existing test `'round-trips an 8-field UserPrefs...'` failed TypeScript check when `UserPrefs` was extended to 9 fields, since the object literal was missing `bypassSilentMode`.
- **Fix:** Added `bypassSilentMode: false` (non-default) to the `fullPrefs` object; renamed test to "9-field".
- **Files modified:** `src/storage/prefs.test.ts`
- **Commit:** 3006b29

**3. [Rule 1 - Bug] Updated Phase 47 RED section in prefs.test.ts for 5th flag**
- **Found during:** Task 2 GREEN
- **Issue:** The Phase 47 RED describe block tested "4 new flag defaults" and "8-field UserPrefs" — stale with the 5th flag added.
- **Fix:** Added `bypassSilentMode: true` assertion and `Object.keys(DEFAULT_PREFS).toHaveLength(9)` check; updated counts.
- **Files modified:** `src/storage/prefs.test.ts`
- **Commit:** 3006b29

## Known Stubs

None. All data flows are wired end-to-end through the flag pipeline. Plan 02 (audio engine gate) and Plan 03 (UI rename + toggle binding) are future consumers.

## Threat Flags

None. All trust boundaries from the plan's threat model were mitigated:
- T-49.1.01-01 (localStorage tampering): `coerceBypassSilentMode` returns `true` for non-boolean/non-parseable input — verified by corrupt-field tests.
- T-49.1.01-02 (URL query-string garbage): `parseQueryBoolean` returns null → falls through to persisted — verified by resolver test.
- T-49.1.01-03 (CustomEvent info disclosure): accepted per plan (no PII, same-origin same-tab).
- T-49.1.01-SC (npm install): DEPS-01 — package.json unchanged.

## Self-Check: PASSED
