---
phase: 49.1-bypass-silent-mode-toggle
plan: 2
subsystem: audio
tags: [audio-engine, hook, gating, ios-bypass, bypassSilentMode, feature-flag]

# Dependency graph
requires:
  - phase: 49.1-bypass-silent-mode-toggle
    provides: "Plan 01 feature flag + prefs infrastructure (BYPASS_SILENT_MODE_FLAG, coerceBypassSilentMode)"
provides:
  - "AudioEngineOptions.bypassSilentMode?: boolean field"
  - "Gate: if (opts.bypassSilentMode !== false) wrapping silent-loop construction block"
  - "UseAudioCues.start 3rd arg bypassSilentMode?: boolean (hook signature unchanged)"
  - "bypassSilentModeRef capture-and-replay in useAudioCues (mirrors timbreRef pattern)"
  - "UseBreathingSessionControllerArgs.bypassSilentMode?: boolean prop + pass-through to audioStart"
affects: [49.1-03-plan, useAppViewModel, useBreathingSessionController callers]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "bypassSilentMode !== false gate predicate (not === true) — undefined coerces to construct"
    - "bypassSilentModeRef capture-and-replay mirrors existing timbreRef D-08/D-11 lifecycle"
    - "Optional prop default-undefined → D-07 coerce → Phase 49 v3 behavior (wave gap safety)"

key-files:
  created: []
  modified:
    - src/audio/audioEngine.ts
    - src/audio/audioEngine.test.ts
    - src/hooks/useAudioCues.ts
    - src/hooks/useAudioCues.test.tsx
    - src/hooks/useBreathingSessionController.ts

key-decisions:
  - "Gate predicate is opts.bypassSilentMode !== false (not === true) — undefined coerces to construct, preserving Phase 49 v3 behavior for all pre-49.1 callers"
  - "bypassSilentMode is placed on UseAudioCues.start (3rd arg), NOT on the hook signature — mirrors timbre arg placement, keeps hook signature unchanged"
  - "bypassSilentModeRef ref capture-and-replay in reconstructEngine (D-09 no-mid-session-rebuild) — symmetric with timbreRef"
  - "bypassSilentMode added to startOrCancel useCallback dep array in useBreathingSessionController to satisfy react-hooks/exhaustive-deps"

patterns-established:
  - "Pattern C applied: gate construction block with !== false; leave teardown null-guards untouched"
  - "Pattern E applied: bypassSilentModeRef mirrors timbreRef (declaration + capture-in-start + replay-in-reconstruct)"

requirements-completed: [ADV-04]

# Metrics
duration: 7min
completed: 2026-05-27
---

# Phase 49.1 Plan 02: Audio Engine bypassSilentMode Gate Summary

**ADV-04 success gate: createAudioEngine({ bypassSilentMode: false }) skips new Audio(...) entirely, verified by SpyAudio stub asserting constructor count 0; true/undefined constructs with Phase 49 v3 attribute parity; flag threaded through useAudioCues.start (3rd arg + bypassSilentModeRef) and useBreathingSessionController prop**

## Performance

- **Duration:** 7 min
- **Started:** 2026-05-27T20:53:20Z
- **Completed:** 2026-05-27T21:00:30Z
- **Tasks:** 3 (all TDD)
- **Files modified:** 5

## Accomplishments

- AudioEngineOptions.bypassSilentMode?: boolean field added with `!== false` gate predicate (undefined → construct preserved)
- 3 new SpyAudio tests covering ADV-04 success gate (skip-false), Phase 49 parity (construct-true), and backward-compat (construct-undefined)
- useAudioCues.start signature extended with optional 3rd arg (hook signature `useAudioCues(initialMuted?, onReanchorRequired?)` unchanged)
- bypassSilentModeRef capture-in-start + replay-in-reconstructEngine (D-09 no-mid-session-rebuild, mirrors timbreRef)
- 4 new tests covering bypassSilentMode forwarding via createAudioEngine spy (false, true, undefined, reconstruct reuse)
- useBreathingSessionController.UseBreathingSessionControllerArgs gains optional bypassSilentMode prop; forwarded to audioStart line 185
- Full test suite: 1289 → 1296 tests passing (+7 new)

## Task Commits

1. **Task 1: Gate silent-loop construction on opts.bypassSilentMode (ADV-04)** - `5c429fd` (feat)
2. **Task 2: Thread bypassSilentMode through useAudioCues.start (3rd arg + ref capture)** - `534f3f5` (feat)
3. **Task 3: Thread bypassSilentMode through useBreathingSessionController (wiring)** - `9e146df` (feat)

## Files Created/Modified

- `src/audio/audioEngine.ts` - Added bypassSilentMode?: boolean to AudioEngineOptions; gated new Audio(...) block with `if (opts.bypassSilentMode !== false)`; initialization changed from `new Audio(SILENT_WAV_DATA_URL)` to `null`
- `src/audio/audioEngine.test.ts` - Added describe block "Phase 49.1 D-07 — bypassSilentMode option gating (ADV-04)" with 3 SpyAudio tests
- `src/hooks/useAudioCues.ts` - Added bypassSilentModeRef; extended UseAudioCues.start interface and implementation with 3rd arg; capture in start(); replay in reconstructEngine()
- `src/hooks/useAudioCues.test.tsx` - Added 4 new tests for bypassSilentMode forwarding (false/true/undefined/reconstruct-reuse)
- `src/hooks/useBreathingSessionController.ts` - Added bypassSilentMode?: boolean to UseBreathingSessionControllerArgs; destructured in hook; forwarded to audioStart; added to useCallback dep array

## Decisions Made

- Gate predicate is `!== false` (NOT `=== true`): ensures undefined coerces to "construct", preserving Phase 49 shipped posture for any pre-49.1 caller that omits the field (ADV-04 requirement + D-07 invariant)
- bypassSilentMode placed on `start(plan, timbre, bypassSilentMode?)` not on `useAudioCues(...)` hook signature — mirrors existing timbre arg placement exactly (Phase 18 D-08 symmetric placement rationale)
- bypassSilentMode added to useBreathingSessionController startOrCancel dep array — react-hooks/exhaustive-deps compliance; correct because the flag is read only at start() time so dep-array update doesn't affect mid-session behavior (D-09 invariant still holds via bypassSilentModeRef)

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 2 - Missing Critical] Added bypassSilentMode to startOrCancel useCallback dep array**
- **Found during:** Task 3 (useBreathingSessionController wiring)
- **Issue:** ESLint `react-hooks/exhaustive-deps: error` flagged missing dependency. The `bypassSilentMode` prop from destructuring was used inside `startOrCancel` but not listed in the dep array.
- **Fix:** Added `bypassSilentMode` to the `useCallback` dep array for `startOrCancel`. Functionally safe: the flag is only read at start() time (via bypassSilentModeRef capture inside useAudioCues), so callback identity change on prop change is correct behavior.
- **Files modified:** src/hooks/useBreathingSessionController.ts
- **Committed in:** 9e146df (Task 3 commit)

---

**Total deviations:** 1 auto-fixed (Rule 2 - missing critical for lint compliance)
**Impact on plan:** Auto-fix necessary for per-commit green-gate compliance. No scope creep; the dep array addition is correct behavior per exhaustive-deps rules.

## Issues Encountered

None - plan executed smoothly. Pre-existing lint errors in storage.ts, sessionPresentation.ts, useAudioCues.ts (lines 190, 280), and useWakeLock.ts were pre-existing and not caused by this plan's changes.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- ADV-04 audio half complete: audioEngine gates construction; data path from useAudioCues.start → createAudioEngine carries bypassSilentMode; reconstruct replays captured value (D-09)
- useBreathingSessionController accepts bypassSilentMode prop (optional, defaults undefined → Phase 49 v3 behavior during wave-1 → wave-2 gap)
- Plan 03 (useAppViewModel wire-up) can now supply featureFlags.bypassSilentMode → useBreathingSessionController → audioStart → createAudioEngine end-to-end

## Threat Surface Scan

No new security-relevant surface introduced. All changes are internal to the audio engine option gating and hook wiring layers — no new network endpoints, auth paths, file access patterns, or schema changes. T-49.1.02-01 through T-49.1.02-SC threat mitigations verified:

- T-49.1.02-01 (Tampering): gate predicate `!== false` implemented — only exact boolean false skips; undefined/null/strings all fall through to construct
- T-49.1.02-02 (DoS): Test 3 explicitly covers undefined → construct (ADV-04 backward-compat)
- T-49.1.02-04 (Repudiation): `await engine.close()` called in each new test — null-guards verified

## Self-Check: PASSED

- src/audio/audioEngine.ts: EXISTS
- src/audio/audioEngine.test.ts: EXISTS
- src/hooks/useAudioCues.ts: EXISTS
- src/hooks/useAudioCues.test.tsx: EXISTS
- src/hooks/useBreathingSessionController.ts: EXISTS
- Commit 5c429fd: EXISTS (Task 1)
- Commit 534f3f5: EXISTS (Task 2)
- Commit 9e146df: EXISTS (Task 3)
- All 1296 tests pass
- pnpm tsc --noEmit: PASSED
- git diff package.json: EMPTY (DEPS-01)

---
*Phase: 49.1-bypass-silent-mode-toggle*
*Completed: 2026-05-27*
