---
phase: 37-stats-ui-removal
plan: 03
type: execute
wave: 3
depends_on:
  - 37-01
  - 37-02
files_modified:
  - src/content/content.no-stats-ui.test.ts
autonomous: true
requirements:
  - STATS-05
must_haves:
  truths:
    - "A new vitest test file at src/content/content.no-stats-ui.test.ts fails if any forbidden stats-UI token reappears in src/components/ or src/app/."
    - "The drift-guard scans every non-test .ts and .tsx file under both roots."
    - "Forbidden tokens are: 'StatsFooter', 'ResetStatsDialog' (plain substring, case-sensitive), and the four case-insensitive regex tokens 'MIN TODAY', 'STREAK', 'TOTAL TIME', plus the required-uppercase token 'SESSIONS'."
    - "The drift-guard test currently PASSES because Plan 01 + Plan 02 cleaned all forbidden tokens from the scanned roots."
    - "The test is the lock — future re-introduction phase explicitly deletes this file with rationale (CONTEXT D-11)."
    - "D-09: An automated fs-scan vitest case in the spirit of src/content/content.no-review-markers.test.ts fails CI if forbidden tokens appear in any non-test file under src/components/ or src/app/."
    - "D-10: Forbidden token list — component names 'StatsFooter' and 'ResetStatsDialog' (plain case-sensitive substring), plus regex tokens 'MIN TODAY' / 'STREAK' / 'TOTAL TIME' (case-insensitive) and word-boundary uppercase 'SESSIONS'."
    - "D-12: Surface coverage — the drift-guard scans both src/components/ and src/app/, which together host every render path for Idle, Running, Complete, Learn, and App Settings; test-file filename filter excludes *.test.ts and *.test.tsx."
  artifacts:
    - path: src/content/content.no-stats-ui.test.ts
      provides: "fs-scan drift-guard that fails CI if any forbidden stats-UI token reappears in src/components/ or src/app/ — Phase 37 long-term anti-gamification invariant lock."
      contains: "STATS-05"
      min_lines: 40
  key_links:
    - from: src/content/content.no-stats-ui.test.ts
      to: src/components/
      via: "node:fs recursive readdirSync collectFiles helper (copied from content.no-review-markers.test.ts analog)"
      pattern: "collectFiles\\(.*components"
    - from: src/content/content.no-stats-ui.test.ts
      to: src/app/
      via: "node:fs recursive readdirSync collectFiles helper"
      pattern: "collectFiles\\(.*app"
---

<objective>
Add the STATS-05 drift-guard test — the load-bearing artifact of Phase 37 per CONTEXT §specifics. Mirror the structure of `src/content/content.no-review-markers.test.ts` (the exact analog per PATTERNS) with three differences: (a) scan two roots `src/components/` and `src/app/` instead of `src/content/`, (b) filter `.tsx` test files as well as `.test.ts`, (c) check a multi-token list per CONTEXT D-10. Implements REQUIREMENTS STATS-05 and locks the anti-gamification stance (`.planning/spikes/MANIFEST.md` Decisions/Anti-gamification) against future regressions.

Purpose: this test is how the spike-010 invariant survives future contributions (CONTEXT §specifics line 117). Re-introduction of a calm stats display is a deliberate future-phase decision that explicitly deletes this test with rationale (CONTEXT D-11, REQUIREMENTS STATSDISPLAY-01).

Output: one new file at `src/content/content.no-stats-ui.test.ts` (PATTERNS recommendation — colocates with `content.no-review-markers.test.ts` so future readers find both drift-guards via the same `src/content/content.*` glob).
</objective>

<execution_context>
@$HOME/.claude-personal-personal/get-shit-done/workflows/execute-plan.md
@$HOME/.claude-personal-personal/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/37-stats-ui-removal/37-CONTEXT.md
@.planning/phases/37-stats-ui-removal/37-PATTERNS.md
@.planning/phases/37-stats-ui-removal/37-01-SUMMARY.md
@.planning/phases/37-stats-ui-removal/37-02-SUMMARY.md
@src/content/content.no-review-markers.test.ts

<interfaces>
<!-- The analog file the new drift-guard will mirror. -->

From src/content/content.no-review-markers.test.ts (analog — pattern to copy structurally):
- Triple-slash reference for node types: `/// <reference types="node" />`
- Imports: `import { describe, it, expect } from 'vitest'; import { readFileSync, readdirSync, statSync } from 'node:fs'; import { resolve, join } from 'node:path'`
- `collectFiles(dir: string, acc: string[] = []): string[]` recursive helper — accepts `.ts` files that do NOT end with `.test.ts`.
- Single describe with single it; per-file readFileSync + substring/regex check; failure message aggregates hits.

Adaptations for Phase 37 (per CONTEXT D-09/D-10):
- Two roots, not one: `resolve(__dirname, '..', 'components')` and `resolve(__dirname, '..', 'app')`.
- File filter accepts both `.ts` AND `.tsx`, rejects both `.test.ts` AND `.test.tsx` (the analog only handles `.ts`/`.test.ts`).
- Forbidden tokens are a list, not a single constant:
  - Plain substring (case-sensitive): `'StatsFooter'`, `'ResetStatsDialog'`
  - Regex case-insensitive: `/MIN TODAY/i`, `/STREAK/i`, `/TOTAL TIME/i`
  - Regex required-uppercase (no `i` flag, word-boundary): `/\bSESSIONS\b/`
- Per-hit aggregation: `${file}: ${tokenLabel}` so the failure message names both the file and which token tripped the guard.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create src/content/content.no-stats-ui.test.ts drift-guard</name>
  <files>src/content/content.no-stats-ui.test.ts</files>
  <read_first>
    - src/content/content.no-review-markers.test.ts (entire file — this is the structural analog; mirror imports, the collectFiles helper, and the describe/it/aggregation shape verbatim apart from the scope + token list changes documented in this plan's `<interfaces>` block)
    - .planning/phases/37-stats-ui-removal/37-PATTERNS.md (§"src/content/content.no-stats-ui.test.ts (NEW — STATS-05 drift-guard)" — pattern + adaptations)
    - .planning/phases/37-stats-ui-removal/37-CONTEXT.md (§decisions D-09, D-10, D-11, D-12)
    - .planning/phases/37-stats-ui-removal/37-01-SUMMARY.md (confirm StatsFooter / ResetStatsDialog deletions landed and the scanned roots are clean)
    - .planning/phases/37-stats-ui-removal/37-02-SUMMARY.md (confirm dead-code data-layer deletes landed)
  </read_first>
  <action>
    Create the new file `src/content/content.no-stats-ui.test.ts`. Mirror the analog (`src/content/content.no-review-markers.test.ts`) structurally — triple-slash node types reference, vitest + node:fs + node:path imports, recursive `collectFiles` helper, single describe with a small set of it cases. Apply the three adaptations spelled out in this plan's `<interfaces>` block:

    1. Header comment block (mirroring the analog's structure): name the file's purpose ("Phase 37 STATS-05 anti-gamification drift-guard"), name the two scanned roots, name the four forbidden token classes, and reference CONTEXT D-09/D-10/D-11 plus REQUIREMENTS STATSDISPLAY-01 — including the WHY note that this test is the lock; future re-introduction explicitly deletes it.

    2. `collectFiles(dir, acc=[])` helper — copy the analog's recursive readdirSync + statSync pattern. The single behavioral change: accept files where `entry.endsWith('.ts') || entry.endsWith('.tsx')` AND reject files where `entry.endsWith('.test.ts') || entry.endsWith('.test.tsx')`. (The analog only handles `.ts` vs `.test.ts`; Phase 37 must scan `.tsx` because the deleted components and their App consumers were TSX.)

    3. Define two scanned roots via `resolve(__dirname, '..', 'components')` and `resolve(__dirname, '..', 'app')`. Build a flat `SCAN_FILES: string[]` by concatenating `collectFiles(COMPONENTS_DIR)` and `collectFiles(APP_DIR)`.

    4. Define the forbidden tokens list as a typed array of `{ label: string, match: (text: string) => boolean }` entries:
       - `{ label: 'StatsFooter (component name)', match: (t) => t.includes('StatsFooter') }`
       - `{ label: 'ResetStatsDialog (component name)', match: (t) => t.includes('ResetStatsDialog') }`
       - `{ label: 'MIN TODAY (visual stats marker)', match: (t) => /MIN TODAY/i.test(t) }`
       - `{ label: 'STREAK (visual stats marker)', match: (t) => /STREAK/i.test(t) }`
       - `{ label: 'TOTAL TIME (visual stats marker)', match: (t) => /TOTAL TIME/i.test(t) }`
       - `{ label: 'SESSIONS (uppercase visual stats marker)', match: (t) => /\bSESSIONS\b/.test(t) }`
       The SESSIONS regex uses `\b...\b` (no `i` flag) per CONTEXT D-10 — only the uppercase form is forbidden (the lowercase word "sessions" appears in legitimate code; the visual stat readout uses the uppercase form).

    5. Single describe `describe('STATS-05 drift-guard (CONTEXT D-09 / D-10 / D-11)', () => { ... })` with one it case `it('no forbidden stats-UI token appears in src/components/ or src/app/', () => { ... })`. Inside the it, iterate every (file, token) pair, readFileSync(file, 'utf-8'), and if `token.match(text)` is true push `${file}: ${token.label}` to a `hits: string[]` array. End with `expect(hits, ...).toEqual([])` and a failure message that names both the file and the token (PATTERNS guidance: "so a future contributor sees what tripped the guard").

    6. Sanity-check by running the new test against the post-Plan-01/02 state: `npx vitest run src/content/content.no-stats-ui.test.ts` MUST exit 0. If it fails, the failure message will name the file + token; trace back to whichever Plan 01/02 deletion was incomplete and surface it (do NOT loosen the token list).

    Do NOT touch any other file in this task. Do NOT add the file to src/content/strings.ts re-exports — the test file is self-contained and discovered by vitest's globs.

    Commit message: `test(37): STATS-05 drift-guard for stats-UI tokens (CONTEXT D-09/D-10/D-11)`.
  </action>
  <verify>
    <automated>npx vitest run src/content/content.no-stats-ui.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - File `src/content/content.no-stats-ui.test.ts` exists (`test -f src/content/content.no-stats-ui.test.ts`).
    - File starts with `/// <reference types="node" />` somewhere in the first ~20 lines (mirroring analog).
    - `grep -n "import.*vitest" src/content/content.no-stats-ui.test.ts` and `grep -n "import.*node:fs" src/content/content.no-stats-ui.test.ts` both return matches.
    - `grep -n "STATS-05" src/content/content.no-stats-ui.test.ts` returns at least one match (in the describe name and/or the header comment).
    - `grep -nE "StatsFooter.*ResetStatsDialog|ResetStatsDialog.*StatsFooter" src/content/content.no-stats-ui.test.ts` returns matches OR both tokens appear separately in the file (both component names must be present as literal strings in the forbidden-token list).
    - `grep -nE "/MIN TODAY/i" src/content/content.no-stats-ui.test.ts` returns a match.
    - `grep -nE "/STREAK/i" src/content/content.no-stats-ui.test.ts` returns a match.
    - `grep -nE "/TOTAL TIME/i" src/content/content.no-stats-ui.test.ts` returns a match.
    - `grep -nE "\\\\bSESSIONS\\\\b" src/content/content.no-stats-ui.test.ts` returns a match (the word-boundary uppercase regex per CONTEXT D-10).
    - `grep -nE "\\.tsx" src/content/content.no-stats-ui.test.ts` returns matches (the file filter must explicitly mention `.tsx` since the analog only handles `.ts`).
    - `grep -nE "endsWith\\('.test.tsx'\\)" src/content/content.no-stats-ui.test.ts` returns a match (the filter rejects `.test.tsx` test files — Phase 37 has none after Plan 01, but a future contributor's test could trip a false positive without this filter).
    - `npx vitest run src/content/content.no-stats-ui.test.ts` exits 0 with 1 passing test (against the post-Plan-01/02 clean state).
    - `npx tsc --noEmit` exits 0.
    - File line count is reasonable (≥ 40 lines, ≤ ~120 lines — mirroring the analog's compact size; if substantially larger, the executor over-engineered).
  </acceptance_criteria>
  <done>The STATS-05 drift-guard test exists, passes against the current clean codebase, and will fail CI if any of the six forbidden tokens reappears in `src/components/` or `src/app/` (excluding test files).</done>
</task>

<task type="auto">
  <name>Task 2: Run the full green gate and validate the phase invariants</name>
  <files>(no source files modified — verification only)</files>
  <read_first>
    - .planning/phases/37-stats-ui-removal/37-01-SUMMARY.md
    - .planning/phases/37-stats-ui-removal/37-02-SUMMARY.md
    - .planning/phases/37-stats-ui-removal/37-CONTEXT.md (§decisions D-05, D-12)
  </read_first>
  <action>
    Run the full closing green gate for Phase 37. No file edits — this task verifies the cumulative state of Plans 01 + 02 + 03 and produces the data the Phase 37 SUMMARY/VERIFICATION steps will reference.

    1. `npx tsc --noEmit` — must exit 0.
    2. `npx vitest run` — must exit 0 across the entire suite.
    3. `npx vitest run src/content/content.no-stats-ui.test.ts -t "STATS-05"` — must exit 0 with 1 passing test.
    4. `npx vitest run src/storage/practices.test.ts -t "STATS-04"` — must exit 0 with 3 passing tests.
    5. `grep -rE "StatsFooter|ResetStatsDialog" src/ --include="*.ts" --include="*.tsx" | grep -v "src/content/content.no-stats-ui.test.ts"` — must return no matches (only the drift-guard test file is allowed to contain the literal token names).
    6. `grep -rE "resetPracticeStats|formatLastSession\\b" src/ --include="*.ts" --include="*.tsx"` — must return no matches.
    7. Confirm STATE_VERSION lock per CONTEXT D-05: `git diff main -- src/storage/storage.ts` must show no change to the STATE_VERSION constant.
    8. Confirm Phase 37 D-12 surface coverage: the drift-guard scans src/components/ + src/app/, which together host every render path for Idle, Running, Complete, Learn, and App Settings. The vitest run in step 2 is the empirical coverage proof — if any of those surfaces accidentally re-rendered a forbidden token, step 3 would fail.

    Record each command's output (exit code + summary) for the Phase 37 SUMMARY. No commit is created by this task — it is verification only. The Plan 03 commit landed in Task 1.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx vitest run</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` exits 0.
    - `npx vitest run` exits 0 across the whole suite.
    - `npx vitest run src/content/content.no-stats-ui.test.ts -t "STATS-05"` exits 0 with 1 passing test.
    - `npx vitest run src/storage/practices.test.ts -t "STATS-04"` exits 0 with 3 passing tests.
    - `grep -rE "StatsFooter|ResetStatsDialog" src/ --include="*.ts" --include="*.tsx" | grep -v "src/content/content.no-stats-ui.test.ts"` returns no matches.
    - `grep -rE "resetPracticeStats" src/ --include="*.ts" --include="*.tsx"` returns no matches.
    - `grep -rE "formatLastSession\\b" src/ --include="*.ts" --include="*.tsx"` returns no matches.
    - `git diff main -- src/storage/storage.ts | grep -nE "^[+-].*STATE_VERSION"` returns no matches (D-05 lock — STATE_VERSION constant is untouched across all three plans).
    - The Phase 37 ROADMAP success criteria are observably TRUE: SC1 (StatsFooter + ResetStatsDialog gone + Reset stats affordance gone — verified by steps 1, 2, 5), SC2 (STATS-04 regression test passes — step 4), SC3 (surface audit returns no forbidden tokens — step 3).
  </acceptance_criteria>
  <done>The full green gate (tsc + vitest) is clean, the STATS-04 invariant test passes, the STATS-05 drift-guard passes, no forbidden tokens survive in production code, and STATE_VERSION is locked. Phase 37 success criteria 1–3 (per ROADMAP) are observable.</done>
</task>

</tasks>

<verification>
After both tasks complete:
- `npx tsc --noEmit` — exit 0.
- `npx vitest run` — exit 0.
- The new drift-guard test file `src/content/content.no-stats-ui.test.ts` exists and the drift-guard test passes.
- All STATS-01..05 requirements are observably closed; D-01..D-12 are all referenced by Plan 01, Plan 02, or Plan 03.
</verification>

<success_criteria>
- src/content/content.no-stats-ui.test.ts exists, mirrors the no-review-markers.test.ts analog structurally, scans `src/components/` + `src/app/`, filters `*.test.ts` AND `*.test.tsx`, checks six forbidden tokens (2 component names + 4 visual-stat regexes per CONTEXT D-10).
- The drift-guard test passes against the post-Plan-01/02 clean state.
- The full vitest suite passes; tsc is green.
- STATS-05 is observably closed (CI fails if any forbidden token returns).
- STATE_VERSION unchanged across the phase.
</success_criteria>

<output>
After completion, create `.planning/phases/37-stats-ui-removal/37-03-SUMMARY.md` summarising:
- The new drift-guard file path, line count, and key structural choices (mirrors analog; two roots; .tsx filter; six tokens).
- The exact forbidden token list with the rationale link to CONTEXT D-10.
- Confirmation that the test passes against the post-Plan-01/02 clean state.
- Closing the phase: STATS-01..05 are all observable, D-01..D-12 are all referenced across the three plans, STATE_VERSION is locked.
- Pointer to the SUMMARY exit clause (CONTEXT D-11): future re-introduction phase explicitly deletes this file with rationale recorded in that phase's SUMMARY. The drift-guard is the lock; deleting it is the unlock.
</output>
