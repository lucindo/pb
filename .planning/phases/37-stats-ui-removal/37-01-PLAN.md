---
phase: 37-stats-ui-removal
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/StatsFooter.tsx
  - src/components/StatsFooter.test.tsx
  - src/components/ResetStatsDialog.tsx
  - src/components/ResetStatsDialog.test.tsx
  - src/components/SettingsDialog.tsx
  - src/app/App.tsx
  - src/app/App.dialog.test.tsx
  - src/app/App.persistence.test.tsx
  - src/content/strings.ts
autonomous: true
requirements:
  - STATS-01
  - STATS-02
  - STATS-03
must_haves:
  truths:
    - "The Idle screen renders no StatsFooter (no 'N sessions' / 'N min total' / 'Reset' button)."
    - "The Reset stats confirmation dialog cannot be opened (the affordance is removed from Practice Settings)."
    - "Auto-closing dialog tests (WR-09) no longer reference ResetStatsDialog."
    - "The persisted-stats branches in App.persistence.test.tsx (LOCL-02 + LOCL-03) are deleted; STORAGE-03 is also removed because its sole consumer (per-practice stats state) is gone."
    - "EN + PT-BR i18n catalogs contain no stats / resetStatsDialog / resetStatsTitle keys."
    - "tsc --noEmit exits 0 — the type system has no orphan consumer reference to UiStrings.stats, uiStrings.resetStatsDialog, or uiStrings.practice.resetStatsTitle."
    - "The full vitest suite passes after the deletions (no stats-render tests remain)."
    - "D-01: UiStrings.stats / practice.resetStatsTitle / resetStatsDialog are deleted from the UiStrings type AND from both EN and PT-BR catalogs in src/content/strings.ts — clean cut, no orphan keys."
    - "D-04: App.tsx activeStats subscription and the activeStats.totalSessions > 0 render gate are pruned; loadStats stays exported (test surface + internal use)."
    - "D-06: StatsFooter.test.tsx and ResetStatsDialog.test.tsx are git-rm'd in the same commit as their components (delete-with-component policy)."
    - "D-07: Reset-stats interaction branches are surgically stripped from src/app/App.dialog.test.tsx and src/app/App.persistence.test.tsx; the rest of those files stays intact."
  artifacts:
    - path: src/app/App.tsx
      provides: "Stats-UI-free App shell with no StatsFooter or ResetStatsDialog imports, state, render, or callbacks."
      forbids: "import.*(StatsFooter|ResetStatsDialog), useState<boolean>\\(.*resetDialogOpen, activeStats\\b, onResetClick, confirmReset, cancelReset, resetPracticeStats"
    - path: src/content/strings.ts
      provides: "UiStrings type and EN+PT-BR catalogs with the stats / resetStatsDialog / resetStatsTitle keys removed."
      forbids: "readonly stats:|readonly resetStatsDialog:|resetStatsTitle:"
  key_links:
    - from: src/app/App.tsx
      to: src/components/StatsFooter.tsx
      via: "non-existent (import was removed; component file deleted)"
      pattern: "MUST NOT contain 'StatsFooter'"
    - from: src/app/App.tsx
      to: src/components/ResetStatsDialog.tsx
      via: "non-existent (import was removed; component file deleted)"
      pattern: "MUST NOT contain 'ResetStatsDialog'"
    - from: src/app/App.persistence.test.tsx
      to: "STORAGE-03 storage-event listener"
      via: "deleted in App.tsx step 4; matching test describe block deleted here"
      pattern: "MUST NOT contain 'STORAGE-03 — cross-tab stats refresh'"
---

<objective>
Strip every user-visible stats surface and its supporting App-level wiring + tests + i18n in a single coordinated cut, so `tsc --noEmit && vitest run` stays green at the end of the plan. Closes STATS-01 (StatsFooter removed), STATS-02 (ResetStatsDialog removed), STATS-03 (Reset stats affordance gone). Resolves the PATTERNS.md storage-event-listener caveat by verifying that the listener has no non-stats consumer and removing it together with the per-practice stats useState setters it feeds.

Purpose: implement the spike-010 anti-gamification stance (per `.planning/spikes/MANIFEST.md` Decisions/Anti-gamification, locked in CONTEXT.md D-01..D-07 and D-12) without bumping `STATE_VERSION` (D-05) or rewriting the orphan i18n strings (D-01 clean cut). Re-introduction is owned by a future deliberate decision (D-11 / REQUIREMENTS.md STATSDISPLAY-01).

Output: 4 deleted files (StatsFooter.tsx, StatsFooter.test.tsx, ResetStatsDialog.tsx, ResetStatsDialog.test.tsx), 5 surgically edited files (App.tsx, App.dialog.test.tsx, App.persistence.test.tsx, strings.ts, SettingsDialog.tsx).
</objective>

<execution_context>
@$HOME/.claude-personal-personal/get-shit-done/workflows/execute-plan.md
@$HOME/.claude-personal-personal/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/37-stats-ui-removal/37-CONTEXT.md
@.planning/phases/37-stats-ui-removal/37-PATTERNS.md
@.planning/spikes/MANIFEST.md

<interfaces>
<!-- Key types and call sites the executor will be excising. Pulled from the codebase so no exploration is needed. -->

From src/content/strings.ts (type block to delete):
- UiStrings.stats: { sessionsCount(n), totalMinutes(seconds), lastSessionPrefix(date, duration), totalSuffix, reset, roundsCompletedLabel } at L114-123
- UiStrings.resetStatsDialog: { title, confirm, cancel } at L27-31
- UiStrings.practice.resetStatsTitle: (practiceName: string) => string at L161

Catalog blocks to delete (verified line numbers):
- EN resetStatsDialog at L209-213
- EN stats at L296-306
- EN practice.resetStatsTitle at L345
- PT-BR resetStatsDialog at L390-394
- PT-BR stats at L477-488
- PT-BR practice.resetStatsTitle at L527

From src/app/App.tsx (sites to delete — verified):
- L13: `import { StatsFooter } from '../components/StatsFooter'`
- L14: `import { ResetStatsDialog } from '../components/ResetStatsDialog'`
- L68: `resetPracticeStats,` (line inside the multi-import block from `../storage` at L51-70)
- L129-132: WHY-comment that references StatsFooter / reset dialog
- L134-135: `const [resonantStats, setResonantStats] = useState<PersistedStats>(...)` and `const [naviKriyaStats, setNaviKriyaStats] = useState<PersistedStats>(...)`
- L140-142: `const [stretchStats, setStretchStats] = useState<PersistedStats>(...)`
- L143: `const [resetDialogOpen, setResetDialogOpen] = useState<boolean>(false)`
- L220-264: STORAGE-03 useEffect that registers the `storage` event listener (its only writes are to setResonantStats/setStretchStats/setNaviKriyaStats — the listener is dead after step above)
- L299-304: `activeStats` selector
- L505-523: WR-09 useEffect — `setResetDialogOpen(false)` must be removed from L519; the surrounding effect (Learn + Settings auto-close on inSessionView) stays
- L675-695: `onResetClick`, `confirmReset` (incl. `resetPracticeStats(activePractice)` + 3 setStats branches), `cancelReset`
- L807, L812, L916: the three `setResonantStats(updated)` / `setStretchStats(updated)` / `setNaviKriyaStats(updated)` writes inside the recordSession effects — DEAD after L134-142 useStates are gone (recordXSession persists via WR-07 single-read in src/storage/stats.ts; no React state consumer remains)
- L1270-1278: render block `{!inSessionView && !nkSessionActive && activeStats.totalSessions > 0 && (<StatsFooter ... />)}`
- L1304-1310: `<ResetStatsDialog ... />` element

From src/app/App.dialog.test.tsx (single it block to delete):
- L332-362: `it('auto-closes ResetStatsDialog when the session starts underneath it (WR-09)', ...)` — 30 lines, single it inside an existing describe; surrounding describe stays intact.

From src/app/App.persistence.test.tsx (three describe blocks to delete):
- L45: line-comment `// env.practices.resonant.stats — recordResonantSession / resetPracticeStats` (remove the `/ resetPracticeStats` clause)
- L227-265: `describe('LOCL-02 — footer gating (D-09 / D-10)', ...)` (3 it cases)
- L270-344: `describe('LOCL-03 — reset clears stats only (D-11 / D-12)', ...)` (3 it cases)
- L349-450+: `describe('STORAGE-03 — cross-tab stats refresh', ...)` — entire describe block, because the App.tsx storage listener is being deleted (its sole purpose was to refresh the now-deleted StatsFooter render gate)

From src/components/SettingsDialog.tsx (comment lines to clean up):
- L20-21: D-13 comment — keep the first half ("NO explicit focus call on open — SettingsDialog has no destructive default; native focus-return contract"); delete the trailing `; differs from ResetStatsDialog which focuses Keep.`
- L25-28: "Three structural deltas from ResetStatsDialog" comment block — delete wholesale (Tiger Style WHY-only: no live sibling means no purpose)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete StatsFooter + ResetStatsDialog components and excise App.tsx consumers</name>
  <files>src/components/StatsFooter.tsx, src/components/StatsFooter.test.tsx, src/components/ResetStatsDialog.tsx, src/components/ResetStatsDialog.test.tsx, src/app/App.tsx</files>
  <read_first>
    - src/app/App.tsx (entire file — to verify storage-listener consumers and snapshot every site listed below)
    - src/components/StatsFooter.tsx (the file being deleted — read so the executor confirms what it imports from format.ts before deletion)
    - src/components/ResetStatsDialog.tsx (the file being deleted)
    - .planning/phases/37-stats-ui-removal/37-PATTERNS.md (§"Modify src/app/App.tsx — strip every stats-UI consumer" rows 1–10, the analog row for StatsFooter/ResetStatsDialog deletes, and §"Shared Patterns / Multi-file delete commit shape")
    - .planning/phases/37-stats-ui-removal/37-CONTEXT.md (§decisions D-01..D-07, §code_context "Integration Points")
  </read_first>
  <action>
    Delete four files outright via `git rm`: src/components/StatsFooter.tsx, src/components/StatsFooter.test.tsx, src/components/ResetStatsDialog.tsx, src/components/ResetStatsDialog.test.tsx. Implements STATS-01 + STATS-02 per CONTEXT D-06 (delete-with-component policy; precedent: Phase 36-08).

    Edit src/app/App.tsx — execute every excision listed in this plan's `<interfaces>` block precisely:
    1. Remove the two component imports at L13 and L14.
    2. Remove only the `resetPracticeStats,` line inside the `from '../storage'` multi-import block at L68 (keep all other names — `recordResonantSession`, `recordStretchSession`, `recordNaviKriyaSession`, `STATE_KEY`, etc. — they remain in use elsewhere).
    3. Delete the WHY-comment at L129-132 (references StatsFooter and the reset dialog — Tiger Style WHY-only: no purpose without live sibling).
    4. Delete the three per-practice stats useState declarations at L134-135 and L140-142 (`resonantStats`/`setResonantStats`, `naviKriyaStats`/`setNaviKriyaStats`, `stretchStats`/`setStretchStats`). Keep `stretchSettings`/`setStretchSettings` at L137-139 — those drive the live engine, not stats.
    5. Delete `resetDialogOpen`/`setResetDialogOpen` at L143.
    6. **Verify and delete the STORAGE-03 useEffect at L220-264.** Confirm via grep that `setResonantStats`, `setStretchStats`, `setNaviKriyaStats` are the only mutations the listener performs and that after step 4 there are no other call sites in App.tsx (the listener has no non-stats consumer per PATTERNS caveat 1). Delete the entire useEffect including the surrounding WHY-comments at L220-247 (they describe a now-deleted behavior).
    7. Delete the `activeStats` selector at L302-304 and the WHY-comment at L299-301 that introduces it. Keep `activePracticeName` (L308-311) and `appHeader`/`appTitle` (L315-322) — those remain in use.
    8. In the WR-09 useEffect at L514-523, remove only the `setResetDialogOpen(false)` line at L519 and update the surrounding WHY-comment at L505-513 to drop the ResetStatsDialog reference. Keep the rest of the effect intact (Learn + Settings auto-close on inSessionView).
    9. Delete `onResetClick` (L675-677), `confirmReset` (L679-691), and `cancelReset` (L693-695) wholesale.
    10. In the recordSession dispatch effect, delete the three setState writes at L807 (`setStretchStats(updated)`), L812 (`setResonantStats(updated)`), L916 (`setNaviKriyaStats(updated)`). The recordXSession calls themselves stay (D-05 invariant): `recordResonantSession`, `recordStretchSession`, `recordNaviKriyaSession` keep their signatures and persistence behavior. Per CONTEXT D-04, recordXSession persists via the WR-07 single-read pattern in src/storage/stats.ts so removing the setState writes is safe — the disk truth survives, just no React state mirror.
    11. Delete the render gate at L1270-1278 (the entire `{!inSessionView && !nkSessionActive && activeStats.totalSessions > 0 && (<StatsFooter ... />)}` JSX expression — the surrounding `<section>` and the InstallBanner block stay).
    12. Delete the `<ResetStatsDialog ... />` element at L1304-1310 wholesale.

    Do not bump STATE_VERSION (CONTEXT D-05). Do not touch `recordResonantSession`/`recordStretchSession`/`recordNaviKriyaSession` callsites. Do not delete `loadStats`/`STATE_KEY`/`ZERO_STATS`/`PersistedStats` from the storage barrel — `loadStats` stays exported per D-04, `STATE_KEY` is used by other tests, `ZERO_STATS` survives confirmReset deletion because nothing else writes it (verify post-delete via grep).

    After edits, run `npx tsc --noEmit` — every dangling consumer surfaces as a compile error. Resolve each by deleting the relevant code (do NOT add stub data). The compiler is the linter for clean-cut deletion per CONTEXT code_context line 105.

    Commit message: `chore(37): delete StatsFooter + ResetStatsDialog components and App consumers (STATS-01/02/03 D-01..D-07)` — single combined commit because the App.tsx surgery and the component deletes are tightly coupled (PATTERNS §"Multi-file delete commit shape" / Phase 36-08 precedent).
  </action>
  <verify>
    <automated>npx tsc --noEmit && grep -r "StatsFooter\|ResetStatsDialog" src/ --include="*.ts" --include="*.tsx" -l | grep -v "src/content/content.no-stats-ui.test.ts" || true</automated>
  </verify>
  <acceptance_criteria>
    - File src/components/StatsFooter.tsx does NOT exist (`test ! -f src/components/StatsFooter.tsx`).
    - File src/components/StatsFooter.test.tsx does NOT exist.
    - File src/components/ResetStatsDialog.tsx does NOT exist.
    - File src/components/ResetStatsDialog.test.tsx does NOT exist.
    - `grep -nE "StatsFooter|ResetStatsDialog" src/app/App.tsx` returns no matches.
    - `grep -nE "resonantStats|naviKriyaStats|stretchStats|setResonantStats|setNaviKriyaStats|setStretchStats|resetDialogOpen|setResetDialogOpen|activeStats|onResetClick|confirmReset|cancelReset|resetPracticeStats" src/app/App.tsx` returns no matches.
    - `grep -n "STORAGE-03\|addEventListener.'storage'" src/app/App.tsx` returns no matches (entire listener deleted per PATTERNS caveat 1 verification).
    - `grep -n "^import.*loadPractices" src/app/App.tsx` still returns a match (loadPractices is still used at mount via `initialPractices`).
    - `grep -n "recordResonantSession\\|recordStretchSession\\|recordNaviKriyaSession" src/app/App.tsx` still returns matches at the three dispatch sites (signatures unchanged per D-05).
    - `grep -nE "STATE_VERSION" src/storage/*.ts` returns the same value as on git main (`git show main:src/storage/storage.ts | grep STATE_VERSION` — D-05 no bump).
    - `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>StatsFooter and ResetStatsDialog components and all their App.tsx consumers are removed, the storage event listener is deleted because its sole consumers (per-practice stats setters) are gone, and tsc is green.</done>
</task>

<task type="auto">
  <name>Task 2: Excise stats i18n strings and prune App test branches</name>
  <files>src/content/strings.ts, src/app/App.dialog.test.tsx, src/app/App.persistence.test.tsx</files>
  <read_first>
    - src/content/strings.ts (entire file — to confirm the three type-level blocks and the four catalog blocks before editing)
    - src/app/App.dialog.test.tsx (the file being pruned)
    - src/app/App.persistence.test.tsx (entire file — to confirm LOCL-02 / LOCL-03 / STORAGE-03 boundaries and the L45 line-comment)
    - .planning/phases/37-stats-ui-removal/37-PATTERNS.md (§"Modify src/content/strings.ts — typed-catalog block deletion", §"Modify src/app/App.dialog.test.tsx", §"Modify src/app/App.persistence.test.tsx")
    - .planning/phases/37-stats-ui-removal/37-CONTEXT.md (§decisions D-01, D-07; §code_context "Established Patterns / Typed catalog with frozen-EN guard")
  </read_first>
  <action>
    Edit src/content/strings.ts — clean-cut deletion per CONTEXT D-01 (no orphan keys, no rot, no unused-export lint noise). Execute each site listed in this plan's `<interfaces>` block:
    1. Delete the type declarations at L27-31 (`resetStatsDialog`), L114-123 (`stats`), and L161 (`practice.resetStatsTitle`).
    2. Delete the EN catalog blocks at L209-213 (`resetStatsDialog`), L296-306 (`stats`), and L345 (`practice.resetStatsTitle` row).
    3. Delete the PT-BR catalog blocks at L390-394 (`resetStatsDialog`), L477-488 (`stats`), and L527 (`practice.resetStatsTitle` row).
    4. Run `npx tsc --noEmit` — the type system will surface any consumer of `UiStrings.stats` / `UiStrings.resetStatsDialog` / `UiStrings.practice.resetStatsTitle` that was missed in Task 1. The expected residue is zero (App.tsx callsites at L1274 / L1308 / L1309 are deleted in Task 1's L1270-1278 + L1304-1310 excisions). If tsc surfaces anything, delete the orphan callsite — do not stub it.

    Edit src/app/App.dialog.test.tsx — delete the single `it('auto-closes ResetStatsDialog when the session starts underneath it (WR-09)', ...)` block at L332-362. Keep the surrounding `describe` block intact (it likely contains other WR-09 / dialog tests).

    Edit src/app/App.persistence.test.tsx — execute the three deletions per CONTEXT D-07 (surgical strip, keep the rest of the file intact):
    1. Update the line-comment at L45: remove only the `/ resetPracticeStats` clause so the comment becomes `// env.practices.resonant.stats — recordResonantSession`.
    2. Delete `describe('LOCL-02 — footer gating (D-09 / D-10)', ...)` at L227-265 wholesale.
    3. Delete `describe('LOCL-03 — reset clears stats only (D-11 / D-12)', ...)` at L270-344 wholesale.
    4. Delete `describe('STORAGE-03 — cross-tab stats refresh', ...)` at L349 through the end of its block — both `it` cases — because the App.tsx storage listener it asserts on is deleted in Task 1 (per PATTERNS §"Modify src/app/App.persistence.test.tsx" planner-decision branch: if the listener is being deleted, drop the entire describe block).
    5. If any imports become unused after the deletions (e.g. `STATE_KEY` if it had no other consumer in the file, `within`, etc.), remove them — `tsc` will flag the unused-import.

    Commit message: `chore(37): excise stats i18n + reset-stats test branches (STATS-01..03 D-01 D-07)`.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx vitest run src/app/App.dialog.test.tsx src/app/App.persistence.test.tsx src/content/</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE "readonly stats:|readonly resetStatsDialog:|resetStatsTitle:|^\\s+stats:\\s*\\{|resetStatsDialog:\\s*\\{" src/content/strings.ts` returns no matches.
    - `grep -nE "uiStrings\\.stats\\b|uiStrings\\.resetStatsDialog|uiStrings\\.practice\\.resetStatsTitle" src/` returns no matches.
    - `grep -n "auto-closes ResetStatsDialog" src/app/App.dialog.test.tsx` returns no matches.
    - `grep -nE "LOCL-02|LOCL-03|STORAGE-03" src/app/App.persistence.test.tsx` returns no matches.
    - `grep -n "/ resetPracticeStats" src/app/App.persistence.test.tsx` returns no matches.
    - `npx tsc --noEmit` exits 0.
    - `npx vitest run src/app/App.dialog.test.tsx src/app/App.persistence.test.tsx` exits 0 with no `LOCL-02 / LOCL-03 / STORAGE-03 / ResetStatsDialog` test names in the output.
    - The `LOCKED_COPY` byte-equality guard (in src/content/) still passes — none of the deleted strings are in LOCKED_COPY (CONTEXT confirms at code_context line 103).
  </acceptance_criteria>
  <done>EN + PT-BR catalogs and the UiStrings type carry no stats / resetStatsDialog / resetStatsTitle keys; App.dialog.test.tsx and App.persistence.test.tsx carry no reset-stats / footer-gating / cross-tab-stats branches; the rest of those test files is untouched and passing.</done>
</task>

<task type="auto">
  <name>Task 3: Clean SettingsDialog comment debt and run the full green gate</name>
  <files>src/components/SettingsDialog.tsx</files>
  <read_first>
    - src/components/SettingsDialog.tsx (entire file — to confirm L20-21 and L25-28 verbatim before editing)
    - .planning/phases/37-stats-ui-removal/37-PATTERNS.md (§"Modify src/components/SettingsDialog.tsx — comment debt")
    - .planning/phases/37-stats-ui-removal/37-CONTEXT.md (§code_context "Integration Points / SettingsDialog.tsx comment debt")
  </read_first>
  <action>
    Edit src/components/SettingsDialog.tsx — execute the two comment edits per PATTERNS Option B recommendation (delete dangling references; do NOT rewrite as standalone documentation):
    1. At L20-21, change the D-13 comment from `// D-13: NO explicit focus call on open — SettingsDialog has no destructive default;\n//        native focus-return contract; differs from ResetStatsDialog which focuses Keep.` to `// D-13: NO explicit focus call on open — SettingsDialog has no destructive default;\n//        native focus-return contract.` (drop only the trailing `; differs from ResetStatsDialog which focuses Keep.` clause).
    2. At L25-28, delete the entire `// Three structural deltas from ResetStatsDialog: ...(a)/(b)/(c)` comment block — including the leading blank-comment line if present (Tiger Style WHY-only: when the WHY refers to a now-deleted live sibling, the WHY no longer informs).

    Do not touch any code (the JSX, hooks, useEffects in SettingsDialog stay byte-identical — this is a comments-only diff).

    Then run the full green gate to lock the Plan 01 cut: `npx tsc --noEmit && npx vitest run` — both must exit 0. The vitest run must show the full suite passing minus the deleted tests (StatsFooter.test.tsx, ResetStatsDialog.test.tsx are gone; LOCL-02 / LOCL-03 / STORAGE-03 / "auto-closes ResetStatsDialog" describe and it blocks are gone). No new test failures.

    Commit message: `docs(37): drop ResetStatsDialog references from SettingsDialog comments` (Tiger Style docs-only commit; precedent: PATTERNS §"Shared Patterns / Conventional-commit scope").
  </action>
  <verify>
    <automated>grep -nE "ResetStatsDialog" src/components/SettingsDialog.tsx; test $? -eq 1 && npx tsc --noEmit && npx vitest run</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE "ResetStatsDialog" src/components/SettingsDialog.tsx` returns no matches (exit code 1).
    - `grep -n "D-13: NO explicit focus call on open" src/components/SettingsDialog.tsx` still returns a match — the surviving half of the D-13 comment is intact.
    - `grep -n "Three structural deltas" src/components/SettingsDialog.tsx` returns no matches.
    - SettingsDialog code (everything after the comment block) is byte-identical to the prior version — `git diff src/components/SettingsDialog.tsx` shows only comment lines changed/deleted.
    - `npx tsc --noEmit` exits 0.
    - `npx vitest run` exits 0 across the entire suite.
    - `grep -rE "StatsFooter|ResetStatsDialog" src/ --include="*.ts" --include="*.tsx"` returns no matches in non-test, non-content paths — only the as-yet-uncreated `src/content/content.no-stats-ui.test.ts` from Plan 03 will eventually carry the literal token names, and that file is excluded from drift-scan via the `*.test.ts` filter.
  </acceptance_criteria>
  <done>SettingsDialog has no references to the deleted ResetStatsDialog; the full vitest suite passes; tsc is green; STATS-01, STATS-02, STATS-03 are observably closed (no visible stats UI, no Reset stats affordance, no dangling i18n or comment references).</done>
</task>

</tasks>

<verification>
After all three tasks complete:
- `git status` — only deletions + the listed edits; no stray files.
- `npx tsc --noEmit` — exit 0.
- `npx vitest run` — exit 0, the full suite passes minus the deleted blocks.
- `grep -rE "StatsFooter|ResetStatsDialog" src/ --include="*.ts" --include="*.tsx"` — no matches outside test scaffolding (none should exist in production code).
- Manual sanity check (optional): `npm run dev` and visit the Idle screen — no StatsFooter footer band, no "N sessions · N min total" text, no Reset button on the Practice Settings.
</verification>

<success_criteria>
- All four target component files (StatsFooter.tsx, StatsFooter.test.tsx, ResetStatsDialog.tsx, ResetStatsDialog.test.tsx) are gone.
- src/app/App.tsx contains no `StatsFooter`, `ResetStatsDialog`, `resetPracticeStats`, `activeStats`, `resonantStats`/`naviKriyaStats`/`stretchStats` state, `resetDialogOpen`, `onResetClick`/`confirmReset`/`cancelReset`, or STORAGE-03 storage-event listener.
- The recordSession dispatch effects still call `recordResonantSession` / `recordStretchSession` / `recordNaviKriyaSession` with their original signatures (D-05 invariant — Plan 02 will lock this with a regression test).
- src/content/strings.ts (UiStrings type + EN + PT-BR catalogs) has no stats / resetStatsDialog / resetStatsTitle keys.
- App.dialog.test.tsx and App.persistence.test.tsx are surgically pruned (D-07), rest of file intact.
- SettingsDialog.tsx has no comment references to the deleted ResetStatsDialog.
- STATE_VERSION is unchanged (D-05 lock).
- `npx tsc --noEmit && npx vitest run` exits 0.
</success_criteria>

<output>
After completion, create `.planning/phases/37-stats-ui-removal/37-01-SUMMARY.md` summarising:
- Files deleted (4 components) and files edited (5).
- Whether the STORAGE-03 storage-event listener verification confirmed no non-stats consumer (it should — listener writes only to setResonantStats/setStretchStats/setNaviKriyaStats; all three setters and their useState declarations are gone) — note explicitly in the SUMMARY that the listener was deleted, with a one-liner WHY.
- Decisions implemented: D-01 (i18n clean cut), D-04 (activeStats subscription + per-practice useState removal), D-05 (no STATE_VERSION bump confirmed), D-06 (delete-with-component), D-07 (surgical test strip), D-12 (Idle/Running/Complete/Learn/AppSettings surface coverage).
- Confirmation that STATS-01, STATS-02, STATS-03 are closed (no visible UI surface, no Reset stats affordance).
- Pre-handoff for Plan 02: list of any compile errors that surfaced from the dead-code data layer (`resetPracticeStats` from `src/storage/practices.ts` should now be unreferenced from src/app/ but still referenced from src/storage/practices.test.ts — Plan 02 closes that).
</output>
