---
phase: 32-learn-localization
plan: 03
type: execute
wave: 3
depends_on: ["32-01", "32-02"]
files_modified:
  - src/content/strings.ts
  - src/content/learnContent.ts
autonomous: false
requirements: [I18N-08]

must_haves:
  truths:
    - "Every v1.5 pt-BR string (practice, nkReadout, nkControls, stats, controls, learn) is operator-reviewed to native quality"
    - "The new Navi Kriya Learn pt-BR copy is operator-reviewed to native quality"
    - "All // TODO: native-speaker review markers are removed from src/content/"
    - "The content.no-review-markers fs-scan test passes — done-state is locked"
  artifacts:
    - path: "src/content/strings.ts"
      provides: "Finalized native-quality pt-BR for all v1.5 keys; zero review markers"
      contains: "pt-BR"
    - path: "src/content/learnContent.ts"
      provides: "Finalized native-quality pt-BR Navi Kriya Learn copy; zero review markers"
      contains: "naviKriya"
  key_links:
    - from: "src/content/content.no-review-markers.test.ts"
      to: "src/content/ (strings.ts, learnContent.ts)"
      via: "fs-scan for the review marker"
      pattern: "TODO: native-speaker review"
---

<objective>
Finalize all v1.5 Brazilian Portuguese copy to native quality through the operator-as-native-reviewer flow (Phase 26 precedent, D-09/D-10/D-11). Every pt-BR string that shipped as an executor draft in Phases 30/31 — plus the net-new Navi Kriya Learn copy from Plan 01 — is presented to the operator for review. The operator approves or corrects each string; Claude applies corrections; the `// TODO: native-speaker review` markers are removed only after operator sign-off.

Purpose: I18N-08 — the user can read all new Navi Kriya and multi-practice UI copy in both English and native-quality PT-BR.
Output: Finalized pt-BR in `strings.ts` and `learnContent.ts`, zero review markers, the `content.no-review-markers` drift-guard test passing. Full suite green.
</objective>

<execution_context>
@$HOME/.claude-personal-personal/get-shit-done/workflows/execute-plan.md
@$HOME/.claude-personal-personal/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32-learn-localization/32-CONTEXT.md
@.planning/phases/32-learn-localization/32-RESEARCH.md
@.planning/phases/32-learn-localization/32-PATTERNS.md
@.planning/phases/32-learn-localization/32-01-SUMMARY.md
@.planning/phases/32-learn-localization/32-02-SUMMARY.md

<interfaces>
<!-- The pt-BR review surface — keys enumerated from the codebase. -->

v1.5 pt-BR draft keys in src/content/strings.ts (D-09 review scope — ~31 strings):
- `practice.*`: toggleLabel, resonantName, naviKriyaName, resonantHeading, naviKriyaHeading, naviKriyaHeader, naviKriyaControlsPlaceholder, naviKriyaStatsEmptyBody, resetStatsTitle (template fn) — 9 entries
- `nkReadout.*`: statusLabel, readoutAriaLabel, phaseLabel, front, back, roundLabel, countLabel, roundOf, countOf — 9 entries
- `nkControls.*`: roundsLabel, frontCountLabel, omLengthLabel, omLengthFast, omLengthMedium, omLengthSlow, perOmCueLabel, perOmCueOn, perOmCueOff, estimatedDuration — 10 entries
- `stats.roundsCompletedLabel` — 1 entry
- `controls.pause`, `controls.resume` — 2 entries
- `learn.naviKriyaVideosHeading`, `learn.naviKriyaDescriptionSection1Title`, `learn.naviKriyaDescriptionSection2Title` — 3 entries (new in Plan 01)

New pt-BR Navi Kriya Learn copy in src/content/learnContent.ts (D-11 review scope):
- `practices.naviKriya.description.section1.title`, `section1.body`, `section2.title`, `section2.body` — 4 entries

From src/content/content.no-review-markers.test.ts:
- `REVIEW_MARKER = 'TODO: native-speaker review'` — fs-scan over all non-`.test.ts` files in `src/content/`. Test fails if any marker survives.

Locked / untranslatable:
- "Navi Kriya" — Sanskrit proper noun, never translated (Phase 30 D-05)
- Video titles "The Guardian In Meditation" / "Navi Kriya Walkthrough" — stay English in pt-BR (D-12)
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Operator native-speaker review of all v1.5 pt-BR copy</name>
  <what-built>
    Claude has assembled the full v1.5 pt-BR review surface for native-speaker review (D-09, D-10):

    Group A — `strings.ts` Phase 30/31 draft keys (~31 strings): `practice.*` (9), `nkReadout.*` (9), `nkControls.*` (10), `stats.roundsCompletedLabel`, `controls.pause`, `controls.resume`.

    Group B — `strings.ts` new Learn heading keys (3 strings, from Plan 01): `learn.naviKriyaVideosHeading`, `learn.naviKriyaDescriptionSection1Title`, `learn.naviKriyaDescriptionSection2Title`.

    Group C — `learnContent.ts` new Navi Kriya Learn copy (4 strings, from Plan 01): `practices.naviKriya.description.section1` title + body, `section2` title + body.

    All Group B and Group C strings currently carry `// TODO: native-speaker review` markers. Group A strings shipped without markers in Phases 30/31 — D-09 brings them into review scope as drafts.
  </what-built>
  <how-to-verify>
    1. Claude presents each pt-BR string as: key path — EN source — current pt-BR draft. Strings are grouped A / B / C.
    2. For each string the operator either approves the draft as-is or gives the exact corrected pt-BR text.
    3. Watch for: "Navi Kriya" must NOT be translated (D-05); the two video titles stay English (D-12 — these are in `learnContent.ts` `videos[].label`, not under review); template functions (`practice.resetStatsTitle`, `nkControls.estimatedDuration`) must keep their `${...}` placeholders intact.
    4. The pt-BR description bodies in Group C must stay claim-safe and non-medical — no clinical verbs (melhora, trata, cura, diagnostica, avalia).
    5. The operator works through all three groups. Claude records every approval/correction.
  </how-to-verify>
  <resume-signal>Type "approved" when every v1.5 pt-BR string has been reviewed, or provide corrections per string. Claude proceeds to apply corrections and remove markers in Task 2.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Apply operator pt-BR corrections and remove all review markers</name>
  <files>src/content/strings.ts, src/content/learnContent.ts</files>
  <read_first>
    - src/content/strings.ts (pt-BR block — practice.*, nkReadout.*, nkControls.*, stats.*, controls.*, learn.*)
    - src/content/learnContent.ts (pt-BR practices.naviKriya block)
    - src/content/content.no-review-markers.test.ts (REVIEW_MARKER string, scan scope)
    - src/content/learnContent.test.ts (clinical-verbs guard, URL identity tests)
    - src/content/strings.test.ts (exhaustiveness checks)
    - .planning/phases/32-learn-localization/32-PATTERNS.md (PT-BR Drift-Guard Marker section)
  </read_first>
  <action>
    Apply every pt-BR correction the operator provided in Task 1 — for Group A (`strings.ts` `practice.*`, `nkReadout.*`, `nkControls.*`, `stats.roundsCompletedLabel`, `controls.pause`, `controls.resume`), Group B (`strings.ts` `learn.naviKriya*` heading keys), and Group C (`learnContent.ts` `practices.naviKriya.description` titles and bodies). Where the operator approved a draft as-is, leave the text unchanged.

    Remove every `// TODO: native-speaker review` comment from `strings.ts` and `learnContent.ts` — these markers exist only while copy is unreviewed; the operator's Task 1 sign-off clears them. Remove the marker comment text; keep the string values and any other comments (e.g. the "Video title kept in English" comment on the pt-BR NK video entries stays — it is not a review marker).

    Do not change any EN values, any English video labels, the structural shape, or the `as const satisfies` constraints. "Navi Kriya" stays untranslated in pt-BR (D-05). Preserve all `${...}` placeholders in template-function strings.

    After applying, confirm zero markers remain across `src/content/` so the `content.no-review-markers` fs-scan test passes.
  </action>
  <verify>
    <automated>grep -rc "TODO: native-speaker review" src/content/ --include='*.ts' | grep -v ':0$' | grep -v '.test.ts' || echo "NO MARKERS"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rl "TODO: native-speaker review" src/content/ --include='*.ts' | grep -v '.test.ts'` returns nothing (no non-test content file contains the marker).
    - `npx vitest run src/content/content.no-review-markers.test.ts` exits 0 (drift-guard passes).
    - `npx tsc --noEmit` exits 0 — pt-BR corrections did not break the `as const satisfies` constraint.
    - The pt-BR `practices.naviKriya.description.section1.body` and `section2.body` match no clinical verb: `npx vitest run src/content/learnContent.test.ts` passes (the clinical-verbs guard added in Plan 01 covers these).
    - EN values are unchanged: `git diff src/content/strings.ts src/content/learnContent.ts` shows changes only on pt-BR string values and removed marker comments.
  </acceptance_criteria>
  <done>All operator pt-BR corrections applied, every review marker removed, drift-guard test passes, tsc green.</done>
</task>

<task type="auto">
  <name>Task 3: Full suite + build verification gate</name>
  <files>src/content/strings.ts, src/content/learnContent.ts</files>
  <read_first>
    - src/content/strings.test.ts (exhaustiveness coverage)
    - src/content/learnContent.test.ts (structural + clinical-verbs + URL identity)
    - .planning/phases/32-learn-localization/32-RESEARCH.md (Validation Architecture — Sampling Rate, phase gate)
  </read_first>
  <action>
    Run the full validation gate for the phase: `npx vitest run` (the complete 78-file / 1131-test suite) and `npm run build` (tsc + vite production build).

    If any test fails, diagnose and fix the root cause in `strings.ts` or `learnContent.ts` (this task owns only those two files). Do not weaken or skip any test to make it pass. A likely failure mode: a `strings.test.ts` or `learnContent.test.ts` non-empty assertion catching a pt-BR value the operator left empty or whitespace-only — fix the content, not the test.

    Sanity-check the two Navi Kriya video URLs still resolve to the intended YouTube videos (`...M3t7gY_yak8` and `...A4BGQCIp9fI`) — both were operator-confirmed in CONTEXT.md D-06; this is a final pre-ship check.
  </action>
  <verify>
    <automated>npx vitest run && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run` exits 0 — full suite green, no skipped or failing tests.
    - `npm run build` exits 0 — tsc typecheck and vite production build both succeed.
    - `grep -rl "TODO: native-speaker review" src/content/ --include='*.ts' | grep -v '.test.ts'` returns nothing (final confirmation no marker survived).
  </acceptance_criteria>
  <done>Full suite green, production build succeeds, zero review markers — phase 32 is verifiable-complete.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Authored content → runtime UI | All v1.5 pt-BR copy is static authored data compiled into the bundle; no user input crosses into it |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-32-05 | Tampering | pt-BR string values in `strings.ts` / `learnContent.ts` | accept | All copy is static authored constants reviewed by the operator. No interpolation of untrusted input. Template-function strings use fixed `${...}` placeholders bound to typed numeric/string args, not user free-text. |
| T-32-06 | Repudiation | Review-marker removal without operator sign-off | mitigate | The `content.no-review-markers` fs-scan test enforces that no `// TODO: native-speaker review` marker survives — but markers are only removed in Task 2 AFTER the Task 1 operator checkpoint. The checkpoint gate makes the review an explicit, blocking step. |

No high-severity threats. This plan is pure static-content finalization — no network calls, no user input, no auth, no new persistence.
</threat_model>

<verification>
- `npx vitest run` — full 78-file suite green.
- `npx vitest run src/content/content.no-review-markers.test.ts` passes — zero markers in `src/content/`.
- `npm run build` succeeds.
- `npx tsc --noEmit` exits 0.
- Operator has reviewed every v1.5 pt-BR string (Task 1 checkpoint).
- Both Navi Kriya video URLs resolve.
</verification>

<success_criteria>
- Every v1.5 pt-BR string (practice, nkReadout, nkControls, stats, controls, learn) is operator-reviewed to native quality (I18N-08).
- The new Navi Kriya Learn pt-BR copy in `learnContent.ts` is operator-reviewed to native quality.
- Zero `// TODO: native-speaker review` markers remain in `src/content/`; the drift-guard test passes.
- "Navi Kriya" stays untranslated; video titles stay English; template placeholders intact.
- Full suite green; production build succeeds.
</success_criteria>

<output>
After completion, create `.planning/phases/32-learn-localization/32-03-SUMMARY.md`
</output>
