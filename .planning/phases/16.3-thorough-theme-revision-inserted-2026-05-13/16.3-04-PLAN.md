---
phase: 16.3
plan: 04
type: execute
wave: 3
depends_on: [16.3-03]
files_modified:
  - src/styles/theme.css
autonomous: false
requirements: []
honored_constraints: [THEME-05, THEME-UI-01]
tags: [theme, palette-redesign, css-only, interactive, moss-palette]

must_haves:
  truths:
    - "Operator names an open-source design system + flavor for the Moss palette at execution time (Moss has a green/teal personality — Catppuccin Latte 'green', Rosé Pine Dawn 'pine'/'foam', Solarized green/cyan, Gruvbox Light 'aqua' all natural fits)"
    - "Agent fetches source's canonical hex table via gh api or raw.githubusercontent.com (RESEARCH Pitfall 8)"
    - "Agent applies RESEARCH §Pattern 2 mapping rubric to [data-theme='moss']:root block — Moss is a LIGHT-flavor palette (bg in the f2fbf7 / fff8 family) so use a Light source flavor (Catppuccin Latte, Rosé Pine Dawn, Solarized Light, Gruvbox Light)"
    - "The Moss redesign naturally cleans up the 16.2-01 deferred ring-inner harmonization (RESEARCH §Anti-Pattern 'Forgetting derived rgb()/alpha tokens' + 16.2-01-SUMMARY-deferred-items): the current --color-ring-inner uses #3b82f6 (blue-500) channels which DON'T MATCH the post-16.2 emerald orb-out family. This plan recomputes ring-inner from the new orb-out-from channels per rubric"
    - "Pre-edit contrast calculator validates THEME-05 + D-01 + polarity BEFORE write"
    - "ONLY [data-theme='moss']:root edited. Light (post-Plan-02), Dark (post-Plan-03), Slate, Dusk all byte-identical to their respective baselines"
    - "Per-palette UAT (1-row verdict for Moss) approves BEFORE commit"
    - "Atomic B1 commit; full diff = src/styles/theme.css"
    - "Per-commit green-gate: tsc + lint + build + test exits 0"
  artifacts:
    - path: "src/styles/theme.css"
      provides: "[data-theme='moss']:root block redesigned from a named source; 17 tokens updated including ring-inner harmonization carry-forward closed"
      contains: "[data-theme='moss']:root"
  key_links:
    - from: "src/styles/theme.css [data-theme='moss']:root block"
      to: "THEME-05 + D-01 contrast guards 'theme=moss' iterations"
      via: "midpoints + accent-strong/on-accent contrast ≥ 1.5"
      pattern: "data-theme='moss'"
    - from: "Moss ring-inner harmonization (16.2-01-SUMMARY deferred-items)"
      to: "This plan's rubric derived-token step"
      via: "ring-inner channels recomputed from new orb-out-from family (RESEARCH Pitfall 2)"
      pattern: "--color-ring-inner"
---

<objective>
Wave 3 / Plan 04: Redesign the Moss palette `[data-theme='moss']:root` block in `src/styles/theme.css` from an operator-named open-source design system. Interactive workflow identical to Plans 02/03 in shape. Additionally CLOSES the 16.2-01 deferred ring-inner harmonization carry-forward (RESEARCH §Anti-Patterns: 16.2's Moss `--color-ring-inner: rgb(59 130 246 / 0.35)` uses the OLD blue-500 channels even though 16.2-01 retuned orb-out to emerald — this plan rewrites ring-inner from the new orb-out-from channels per rubric Pattern 2 derived-token recompute step).

Cadence: B1. Output: 1 commit, src/styles/theme.css only.
</objective>

<execution_context>
@$HOME/.claude-personal-personal/get-shit-done/workflows/execute-plan.md
@$HOME/.claude-personal-personal/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/16.3-thorough-theme-revision-inserted-2026-05-13/16.3-RESEARCH.md
@.planning/phases/16.3-thorough-theme-revision-inserted-2026-05-13/16.3-PATTERNS.md
@.planning/phases/16.3-thorough-theme-revision-inserted-2026-05-13/16.3-01-SUMMARY.md
@.planning/phases/16.3-thorough-theme-revision-inserted-2026-05-13/16.3-03-PLAN.md
@.planning/phases/16.2-palette-aesthetic-refresh-inserted-2026-05-13/16.2-01-SUMMARY.md
@src/styles/theme.css
@src/styles/theme.contrast.test.ts

<interfaces>
The Moss palette's 17-token surface (canonical order; selector block at `[data-theme='moss']:root` — block boundaries ~ lines 64–90 of `src/styles/theme.css`):

Current Moss palette state (pre-plan, post-16.2):

  --color-breathing-bg:            #f2fbf7;   (custom green-tinted near-white)
  --color-breathing-bg-soft:       #e4f6ef;   (custom)
  --color-breathing-bg-edge:       #f8fffc;   (custom)
  --color-breathing-surface:       #ffffff;   (white)
  --color-breathing-accent:        #0f766e;   (teal-700)
  --color-breathing-accent-strong: #115e59;   (teal-800)
  --color-breathing-muted:         #64748b;   (slate-500)
  --color-breathing-on-accent:     #ffffff;   (white)
  --color-orb-in-from:             #99f6e4;   (teal-200)
  --color-orb-in-to:               #ecfdf5;   (emerald-50)
  --color-orb-out-from:             #047857;  (emerald-700 — set in 16.2 F4 retune)
  --color-orb-out-to:              #6ee7b7;   (emerald-300 — set in 16.2 F4 retune)
  --color-orb-in-text:             #134e4a;   (teal-900)
  --color-orb-out-text:            #022c22;   (emerald-950 — set in 16.2 F4)
  --color-ring-outer:              rgb(153 246 228 / 0.6);   (teal-200 channels)
  --color-ring-inner:              rgb(59 130 246 / 0.35);   (blue-500 channels — STALE per 16.2-01 deferred item; should be emerald-700 channels after F4 retune)
  --color-modal-backdrop:          rgb(15 23 42 / 0.30);     (slate-900 channels)

Pre-phase contrast baseline (from 16.3-01-SUMMARY.md):
  Moss THEME-05: 2.36
  Moss D-01:     (captured in 16.3-01 — teal-800 #115e59 vs white #fff is ~9+)

Catalog of Moss-suitable Light-flavor sources (Moss has a green/teal personality):

| Source | Flavor | Why Moss-suitable | Canonical fetch |
|--------|--------|-------------------|-----------------|
| Catppuccin Latte | base #eff1f5; green #40a02b; teal #179299 | green/teal natural anchors | catppuccin.com/palette OR gh api |
| Rosé Pine Dawn | base #faf4ed; pine #286983; foam #56949f | pine + foam read as moss greens | raw rose-pine-palette CSS |
| Solarized Light | base3 #fdf6e3; green #859900; cyan #2aa198 | classic moss + cyan accent | ethanschoonover.com/solarized |
| Gruvbox Light | light0 #fbf1c7; green #98971a; aqua #689d6a | natural earthy green | gh api morhetz/gruvbox |

Moss-specific rubric notes:
  - The chrome anchor (`--color-breathing-accent`) wants to land in source's teal/pine/aqua family (NOT pure green — the personality is moss-green WITH SOME COOL UNDERTONE).
  - `accent-strong` wants to be a DEEPER teal/pine — e.g. Latte's `teal #179299` for accent + a custom deeper green or source's darker accent for accent-strong.
  - `orb-in-from/-to` light pole — source's lightest green/teal pair (Latte `teal #179299` + `green #40a02b` blended toward a light pole? Or actually source's lightest surface tier — be careful: Moss orb-in is meant to be LIGHT-pole, currently teal-200/emerald-50. Stay in light territory).
  - `orb-out-from/-to` dark pole — source's deeper green family. CAUTION (RESEARCH §Pattern 2): avoid landing too close to chrome accent (current accent-strong #115e59 teal-800) — slight hue separation (e.g. emerald vs teal, pine vs forest) is preferred. The 16.2-01 retune already established this discipline.
  - `--color-ring-inner` — RECOMPUTE from the new orb-out-from channels (closes 16.2-01 carry-forward).
  - `--color-ring-outer` — recompute from new orb-in-from channels (proactive — keep families coherent).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Moss palette redesign — prompt operator → fetch source → apply rubric (with ring-inner harmonization carry-forward) → edit [data-theme='moss']:root block → guards → UAT → commit</name>
  <files>src/styles/theme.css</files>
  <read_first>
    - src/styles/theme.css (full file — edit ONLY [data-theme='moss']:root block at lines ~64–90)
    - src/styles/theme.contrast.test.ts (the 'theme=moss' iteration is the gate)
    - .planning/phases/16.3-thorough-theme-revision-inserted-2026-05-13/16.3-RESEARCH.md §"Pattern 2: Mapping rubric"
    - .planning/phases/16.3-thorough-theme-revision-inserted-2026-05-13/16.3-RESEARCH.md §"Common Pitfalls > Pitfall 2 (derived rgb/alpha drift — Moss is the literal example: 16.2 left ring-inner stale)"
    - .planning/phases/16.3-thorough-theme-revision-inserted-2026-05-13/16.3-PATTERNS.md §"Pattern 7: Per-palette UAT prompt shape"
    - .planning/phases/16.3-thorough-theme-revision-inserted-2026-05-13/16.3-01-SUMMARY.md (Moss pre-phase baseline ratios + md5)
    - .planning/phases/16.2-palette-aesthetic-refresh-inserted-2026-05-13/16.2-01-SUMMARY.md (16.2-01 deferred-items section: Moss + Slate ring-inner stale — Moss is closed by this plan)
    - The previous palette plan's commit body (Plan 03 Dark — for cross-palette ratio drift sanity)
  </read_first>
  <action>
    Interactive workflow same as Plans 02/03 Task 1. `autonomous: false`.

    Step 1 — PROMPT THE OPERATOR for the Moss source design system. Surface the catalog (Catppuccin Latte, Rosé Pine Dawn, Solarized Light, Gruvbox Light) and wait for `<chosen-source>`.

    Step 2 — FETCH canonical hex table per RESEARCH Pitfall 8.

    Step 3 — APPLY rubric (RESEARCH §Pattern 2) — Moss-specific routing:
      - Chrome: bg/bg-soft/bg-edge from source's lightest surface tiers (often source's lightest 2–3 tones blended toward green tint). Surface = source's surface0 OR custom near-white. Accent = source's teal/pine/aqua. Accent-strong = source's deeper green or teal. Muted = source's subtext. On-accent = source's base (light text on dark accent-strong) OR a near-white (matches current).
      - Orb: In = source's light teal/aqua pair. Out = source's deeper green family — AVOID landing too close to accent (slight hue separation: emerald vs teal, or pine vs jungle).
      - Orb text: in-text = source's darkest green/teal text. out-text = source's darkest available (emerald-950 family stays a safe default).
      - **Derived rgb/alpha (RESEARCH Pitfall 2 — the 16.2 carry-forward):**
        - `--color-ring-outer` = rgb(<new orb-in-from channels>) / 0.5..0.7
        - `--color-ring-inner` = rgb(<new orb-out-from channels>) / 0.35..0.45 **← THIS is the 16.2-01 carry-forward fix; the current Moss ring-inner uses blue-500 channels even though orb-out is emerald.**
        - `--color-modal-backdrop` = rgb(<deepest neutral channels>) / 0.30..0.50

    Step 4 — RUN pre-edit contrast calculator. Verify polarity + THEME-05 ≥ 1.5 + D-01 ≥ 1.5. Iterate Step 3+4 until all three pass.

    Step 5 — EDIT `src/styles/theme.css` `[data-theme='moss']:root` block ONLY (~lines 64–90; specifically the 17 tokens at lines ~72–89). UPDATE the comment block above the Moss selector to reference the new source + note the ring-inner harmonization closure: e.g. `/* Source: <chosen-source> (<owner>/<repo> MIT) — <flavor>. Closes 16.2-01-SUMMARY deferred ring-inner harmonization: ring-inner channels recomputed from new orb-out-from family. */`

    DO NOT TOUCH: @theme (Light post-Plan-02), [data-theme='dark']:root (post-Plan-03), [data-theme='slate']:root (pre-phase md5), [data-theme='dusk']:root (pre-phase md5), .orb rules, @media reduced-motion block, any .tsx, any test file.

    Step 6 — RUN guards (same as Plans 02/03): contrast + hardcoded-classes + sentinels 30/40 + scope guard + full chain.

    Step 7 — PER-PALETTE UAT for Moss (3-row verdict table this time — operator should observe (a) orb visual integration, (b) lead-in digit legibility, (c) active phase label legibility, AND (d) ring-inner now blends with the new orb-out family rather than carrying blue):
      ```
      Phase 16.3 Plan 04 — Moss palette UAT.

      1. Open http://localhost:5173/hrv/ in Chrome.
      2. Open SettingsDialog → select "Moss" → close dialog.
      3. Observe orb at idle — In gradient integrated with green-tinted chrome?
      4. Start session → 3-2-1 lead-in digit legibility on In gradient?
      5. Watch ≥2 In→Out→In transitions:
         - Out gradient integrated with chrome (not jarring blue from old ring-inner)?
         - Active phase label legibility on both phases?
         - In and Out perceptually distinct?
         - **NEW for Moss: inner reference ring (visible during Out phase) — does it blend with the new orb-out family, NOT show a foreign blue?**
      6. Stop session.

      Capture verdict row:
      | 3 | Moss | moss | <chosen-source> | bg <hex>, surface <hex>, accent <hex> | <in family> | <out family> | approved/rejected | pass/fail | pass/fail | source citation; ring-inner harmonized |

      Reply `approved (Moss; ring-inner harmonized)` + row, OR `rejected: <observation>`.
      Loop bound: ≥3 distinct-source-system rejections → `phase-blocked`.
      ```

      Wait for approval.

    Step 8 — COMMIT atomically:
      `feat(16.3-04): redesign Moss palette from <chosen-source> (<flavor> flavor); close ring-inner harmonization carry-forward`

      Body: source citation + ratios + explicit note "closes 16.2-01-SUMMARY deferred-items: Moss ring-inner channels migrated from #3b82f6 (blue-500) to <new emerald-family> channels per rubric Pattern 2 derived-token step".
  </action>
  <verify>
    <automated>npm test -- src/styles/theme.contrast.test.ts src/styles/theme.no-hardcoded-classes.test.ts --run && npx tsc --noEmit && npm run lint && npm run build && npm test -- --run && [ "$(grep -cE '^  --color-orb-' src/styles/theme.css)" -eq 30 ] && [ "$(grep -cE '^  --color-breathing-' src/styles/theme.css)" -eq 40 ] && [ "$(git diff --name-only HEAD~1 HEAD)" = "src/styles/theme.css" ]</automated>
  </verify>
  <acceptance_criteria>
    - `[data-theme='moss']:root` block redesigned from a named open-source design system (operator-chosen).
    - At least one of `--color-breathing-bg`, `--color-breathing-accent`, `--color-orb-in-from`, `--color-orb-out-from` differs from pre-plan value.
    - All 8 chrome + 6 orb tokens are hex literals; 3 derived tokens use `rgb(R G B / alpha)`.
    - `--color-ring-inner` channels DIFFER from `59 130 246` (the stale blue-500 channels from 16.2) — verified explicit: `grep '\-\-color-ring-inner: rgb(59 130 246' src/styles/theme.css` must NOT match in the Moss block (closes 16.2-01 carry-forward).
    - Light (post-Plan-02) / Dark (post-Plan-03) / Slate (pre-phase) / Dusk (pre-phase) blocks have md5 byte-identical to their respective baselines.
    - `grep -cE "^  --color-orb-" src/styles/theme.css` returns 30; `grep -cE "^  --color-breathing-" src/styles/theme.css` returns 40.
    - `git diff --name-only HEAD~1 HEAD` returns exactly `src/styles/theme.css`.
    - `npm test -- src/styles/theme.contrast.test.ts --run` exits 0.
    - `npx tsc --noEmit && npm run lint && npm run build && npm test -- --run` exits 0.
    - Polarity invariant (Moss inMidLum > outMidLum) holds.
    - Per-palette UAT: operator returned `approved (Moss; ring-inner harmonized)` before commit.
    - Commit subject matches pattern; commit body notes ring-inner harmonization closure.
  </acceptance_criteria>
  <done>
    Moss palette redesigned from named source; 16.2-01 ring-inner harmonization carry-forward closed (ring-inner channels migrated from stale blue-500 to new orb-out-family channels). All invariants hold. Other 4 blocks byte-identical. UAT approved.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| no new boundaries | Same as Plans 02/03 — pure CSS value substitution within one selector block. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16.3-04-01 | Spoofing (a11y) | Moss THEME-05 or D-01 floor breached | mitigate | Pre-edit calculator + theme.contrast.test.ts double-gate. |
| T-16.3-04-02 | Tampering | Ring-inner left at stale blue-500 channels (the 16.2-01 carry-forward) | mitigate | Acceptance criterion explicitly forbids `59 130 246` channels in the Moss `--color-ring-inner`. Rubric Step 3 recomputes ring-inner from new orb-out-from channels (RESEARCH Pitfall 2). UAT Step 7 visually verifies inner ring blends. |
| T-16.3-04-03 | Tampering | Cross-palette regression | mitigate | Md5 cross-check + scope guard. |
| T-16.3-04-04 | Tampering | Accent-strong vs accent-Out gradient color collision (Moss-specific risk per RESEARCH §Pattern 2 note) | mitigate | Rubric Step 3 explicitly avoids landing orb-out family too close to chrome accent — slight hue separation (emerald vs teal). |
| T-16.3-04-05 | Spoofing (a11y) | Orb-text legibility on new Moss gradients | mitigate | UAT step verifies lead-in digit + active phase label. |
| T-16.3-04-06 | Denial of Service | UAT rejection loop spins | mitigate | W3 fix: 3-rejection bound. |
</threat_model>

<verification>
After Task 1 completes (same shape as Plans 02/03):

```bash
# Specific Moss ring-inner harmonization check (the 16.2-01 carry-forward closure):
awk "/^\[data-theme='moss'\]:root \{/,/^\}$/" src/styles/theme.css | grep -E "^  --color-ring-inner"
# Expected: NOT `rgb(59 130 246 / 0.35)` — channels should match the new orb-out-from family.

# Standard chain (same as Plans 02/03):
npm test -- src/styles/theme.contrast.test.ts --run
npm test -- src/styles/theme.no-hardcoded-classes.test.ts --run
[ "$(grep -cE '^  --color-orb-' src/styles/theme.css)" -eq 30 ]
[ "$(grep -cE '^  --color-breathing-' src/styles/theme.css)" -eq 40 ]
[ "$(git diff --name-only HEAD~1 HEAD)" = "src/styles/theme.css" ]
npx tsc --noEmit && npm run lint && npm run build && npm test -- --run
```
</verification>

<success_criteria>
1. [data-theme='moss']:root block redesigned from an operator-named open-source design system.
2. THEME-05 ≥ 1.5; D-01 ≥ 1.5; polarity invariant holds.
3. `--color-ring-inner` channels migrated from stale `59 130 246` (blue-500) to the new orb-out-from family — closes 16.2-01-SUMMARY deferred-items.
4. Light (post-Plan-02), Dark (post-Plan-03), Slate (pre-phase), Dusk (pre-phase) blocks byte-identical to their baselines.
5. Token-name contract: 30 orb + 40 chrome.
6. theme.no-hardcoded-classes.test.ts exits 0; full chain exits 0.
7. Per-palette UAT for Moss approved BEFORE commit.
8. Atomic B1 commit; subject `feat(16.3-04): redesign Moss palette from <source> (<flavor>); close ring-inner harmonization carry-forward`.
</success_criteria>

<output>
After Task 1, src/styles/theme.css has Moss redesigned + ring-inner harmonization carry-forward closed. Optional `16.3-04-SUMMARY.md` may be written.
</output>
