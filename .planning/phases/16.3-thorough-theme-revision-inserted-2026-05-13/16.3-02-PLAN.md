---
phase: 16.3
plan: 02
type: execute
wave: 1
depends_on: [16.3-01]
files_modified:
  - src/styles/theme.css
autonomous: false
requirements: []
honored_constraints: [THEME-05, THEME-UI-01]
tags: [theme, palette-redesign, css-only, interactive, light-palette]

must_haves:
  truths:
    - "Operator names an open-source design system + flavor for the Light palette at execution time (interactive prompt — NOT pre-locked at plan time)"
    - "Agent fetches the source's canonical hex table via gh api or raw.githubusercontent.com (per RESEARCH Pitfall 8, NOT WebFetch on swatch pages)"
    - "Agent applies the RESEARCH §Pattern 2 mapping rubric to route source role slots onto OUR 17-token surface for the Light @theme block"
    - "Pre-edit contrast calculator validates THEME-05 floor (in-mid vs out-mid ≥ 1.5) AND D-01 floor (accent-strong vs on-accent ≥ 1.5) AND polarity invariant (in-mid lum > out-mid lum) BEFORE writing src/styles/theme.css"
    - "The Light @theme block is the ONLY block edited in this plan. The 4 other palette blocks (Dark/Moss/Slate/Dusk) remain byte-identical to their pre-phase md5 from 16.3-01-SUMMARY.md"
    - "Up to 17 Light token values change (8 chrome + 4 orb gradient + 2 orb-text + 3 derived rgb/alpha). Token NAMES locked from 16.1 — NO new token names added"
    - "Per-palette UAT (5-row verdict for Light only) approves visual integration + lead-in digit legibility + active phase label legibility BEFORE the commit"
    - "Atomic commit (B1 cadence): one palette, one commit. git diff --name-only HEAD~1 HEAD equals exactly src/styles/theme.css"
    - "Token-count sentinels still hold (30 orb, 40 chrome) — no token names added or dropped"
    - "Per-commit green-gate: tsc + lint + build + test exits 0"
  artifacts:
    - path: "src/styles/theme.css"
      provides: "Light @theme block redesigned from a named open-source design system (operator-chosen at execution); 17 tokens updated; other 4 blocks byte-identical"
      contains: "@theme {"
  key_links:
    - from: "src/styles/theme.css @theme block (Light)"
      to: "THEME-05 contrast guard 'theme=light' iteration in theme.contrast.test.ts"
      via: "contrastRatio(midpoint(orb-in-from, orb-in-to), midpoint(orb-out-from, orb-out-to)) >= 1.5"
      pattern: "--color-orb-(in|out)-(from|to)"
    - from: "src/styles/theme.css @theme block (Light)"
      to: "D-01 contrast guard 'theme=light' iteration in theme.contrast.test.ts"
      via: "contrastRatio(accent-strong, on-accent) >= 1.5"
      pattern: "--color-breathing-(accent-strong|on-accent)"
    - from: "Operator UAT verdict (this plan)"
      to: "Phase 17.3-SUMMARY.md aggregate 5/5 verdict table (built by Plan 07)"
      via: "Light row of the 5-row UAT verdict table"
      pattern: "Light"
---

<objective>
Wave 1 / Plan 02: Redesign the Light palette in `src/styles/theme.css` from an operator-named open-source design system. Interactive workflow — operator names the source at execution time; agent fetches canonical hex table, applies RESEARCH §Pattern 2 mapping rubric, validates via pre-edit calculator, edits the @theme block, runs guards, runs per-palette UAT, commits atomically.

Purpose: Replace the Light palette's chrome + orb + derived rgb/alpha tokens (currently sourced from Tailwind defaults — see `src/styles/theme.css` lines 4–26) with a deliberately-curated palette grounded in a named open-source design system. This is the first of 5 per-palette redesigns; Light is chosen first because its `@theme` block is also the Tailwind v4 baseline (so any rubric quirks specific to `@theme` resolution surface here before they propagate to `[data-theme]:root` blocks).

Cadence rule (B1 — inherited from 16.2-01): one palette = one task = one commit. The full repo diff for this plan's commit must equal exactly `src/styles/theme.css`. Bisection becomes trivial — any later contrast or visual regression bisects to this single commit if Light is the affected palette.

Output: 1 file modified (`src/styles/theme.css` — Light @theme block only). 1 atomic commit. Per-palette UAT verdict row for Light captured in the commit body. No per-palette SUMMARY file is written — atomic B1 keeps the commit pure (theme.css only); Plan 07 phase SUMMARY aggregates all 5 verdict rows.
</objective>

<execution_context>
@$HOME/.claude-personal-personal/get-shit-done/workflows/execute-plan.md
@$HOME/.claude-personal-personal/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/16.3-thorough-theme-revision-inserted-2026-05-13/16.3-RESEARCH.md
@.planning/phases/16.3-thorough-theme-revision-inserted-2026-05-13/16.3-PATTERNS.md
@.planning/phases/16.3-thorough-theme-revision-inserted-2026-05-13/16.3-VALIDATION.md
@.planning/phases/16.3-thorough-theme-revision-inserted-2026-05-13/16.3-01-SUMMARY.md
@.planning/phases/16.2-palette-aesthetic-refresh-inserted-2026-05-13/16.2-SUMMARY.md
@src/styles/theme.css
@src/styles/theme.contrast.test.ts

<interfaces>
The Light palette's 17-token surface to redesign (canonical order per PATTERNS Pattern 3):

  @theme {
    --color-breathing-bg:            <hex>;   /* page background (largest surface) */
    --color-breathing-bg-soft:       <hex>;   /* card/secondary tint */
    --color-breathing-bg-edge:       <hex>;   /* edge/highlight tint */
    --color-breathing-surface:       <hex>;   /* card/dialog inner surface */
    --color-breathing-accent:        <hex>;   /* secondary brand hue */
    --color-breathing-accent-strong: <hex>;   /* primary action bg — D-01 contrast pair */
    --color-breathing-muted:         <hex>;   /* secondary text + disabled fg */
    --color-breathing-on-accent:     <hex>;   /* fg on accent-strong — D-01 contrast pair */
    --color-orb-in-from:             <hex>;   /* In gradient — light pole */
    --color-orb-in-to:               <hex>;
    --color-orb-out-from:            <hex>;   /* Out gradient — dark pole */
    --color-orb-out-to:              <hex>;
    --color-orb-in-text:             <hex>;   /* lead-in digit + active phase label color on In */
    --color-orb-out-text:            <hex>;   /* lead-in digit + active phase label color on Out */
    --color-ring-outer:              rgb(<R> <G> <B> / <alpha>);  /* derived from orb-in-from family */
    --color-ring-inner:              rgb(<R> <G> <B> / <alpha>);  /* derived from orb-out-from family */
    --color-modal-backdrop:          rgb(<R> <G> <B> / <alpha>);  /* derived from deepest neutral */
    /* Non-themable layout/sizing tokens BELOW (DO NOT TOUCH per D-02):
       --orb-size, --orb-scale-min, --orb-scale-max, --orb-scale-mid, --shadow-breathing-card */
  }

Current Light palette state (pre-plan, post-16.2 close — for "before" column in this plan's SUMMARY hex table):

  --color-breathing-bg:            #f8fafc;   (slate-50)
  --color-breathing-bg-soft:       #f1f5f9;   (slate-100)
  --color-breathing-bg-edge:       #ffffff;   (white)
  --color-breathing-surface:       #ffffff;   (white)
  --color-breathing-accent:        #64748b;   (slate-500)
  --color-breathing-accent-strong: #475569;   (slate-600)
  --color-breathing-muted:         #94a3b8;   (slate-400)
  --color-breathing-on-accent:     #ffffff;   (white)
  --color-orb-in-from:             #e0f2fe;   (sky-100)
  --color-orb-in-to:               #f0f9ff;   (sky-50)
  --color-orb-out-from:            #f59e0b;   (amber-500 — set in 16.2 F1 retune)
  --color-orb-out-to:              #fcd34d;   (amber-300 — set in 16.2 F1 retune)
  --color-orb-in-text:             #0c4a6e;   (sky-900)
  --color-orb-out-text:            #7c2d12;   (orange-900)
  --color-ring-outer:              rgb(224 242 254 / 0.6);   (sky-100 channels)
  --color-ring-inner:              rgb(100 116 139 / 0.35);  (slate-500 channels)
  --color-modal-backdrop:          rgb(15 23 42 / 0.30);     (slate-900 channels)

Pre-phase contrast baseline (from 16.3-01-SUMMARY.md — verify before editing):
  Light THEME-05: 1.59
  Light D-01:     1.81

Catalog of MIT-licensed hex-publishing source design systems (for operator reference at the prompt step):

| Source | Flavors (Light-suitable in bold) | Canonical fetch source |
|--------|----------------------------------|------------------------|
| Catppuccin | **Latte**, Frappé, Macchiato, Mocha | `gh api repos/catppuccin/catppuccin` or catppuccin.com/palette |
| Rosé Pine | Main, Moon, **Dawn** | raw.githubusercontent.com/rose-pine/rose-pine-palette/main/dist/css/rose-pine.css |
| Solarized | Dark, **Light** | ethanschoonover.com/solarized/ |
| Gruvbox | Dark, **Light** (soft/medium/hard) | github.com/morhetz/gruvbox/blob/master/colors/gruvbox.vim |
| Nord | (Polar Night dark / **Snow Storm light** elements) | nordtheme.com/docs/colors-and-palettes |
| Tokyo Night | Storm, Night (both dark — less suitable for Light) | github.com/enkia/tokyo-night-vscode-theme |

Likely strongest matches for the Light palette: Catppuccin Latte, Rosé Pine Dawn, Solarized Light, Gruvbox Light. Operator choice at execution.

The pre-edit contrast calculator (RESEARCH §"Code Examples" lines 420–480) — mirror theme.contrast.test.ts:20–90 WCAG math exactly:

  inMidLum  = relLum(midpoint(hexToRgb(orb-in-from), hexToRgb(orb-in-to)))
  outMidLum = relLum(midpoint(hexToRgb(orb-out-from), hexToRgb(orb-out-to)))
  THEME-05  = (max(inMidLum, outMidLum) + 0.05) / (min(inMidLum, outMidLum) + 0.05)
  D-01      = (max(relLum(accent-strong), relLum(on-accent)) + 0.05) / (min(...) + 0.05)

  Polarity invariant: inMidLum > outMidLum (In is the LIGHT pole).
  THEME-05 floor:     THEME-05 ≥ 1.5
  D-01 floor:         D-01 ≥ 1.5

Anti-patterns to refuse (from RESEARCH §"Anti-Patterns to Avoid"):
  - Ad-hoc Tailwind-default picking (superseded by named-source mandate)
  - Multi-palette commits (violates B1 — only Light edited here)
  - Adding new tokens (NAMES are locked — only VALUES change)
  - Touching `.tsx` files (THEME-UI-01 guard contract — full diff must equal src/styles/theme.css only)
  - Inverting In/Out polarity (RESEARCH Pitfall 1 — if rubric output flips polarity, flip role assignments not gradient direction)
  - Forgetting derived rgb()/alpha tokens (RESEARCH Pitfall 2 — recompute ring-outer/ring-inner/modal-backdrop from new In/Out/bg families)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Light palette redesign — prompt operator → fetch source → apply rubric → edit @theme block → guards → UAT → commit</name>
  <files>src/styles/theme.css</files>
  <read_first>
    - src/styles/theme.css (full file — 273 lines; this task edits ONLY the @theme block at lines ~1–38; rest of file is read-only context)
    - src/styles/theme.contrast.test.ts (full file — to confirm the WCAG math the guards apply; specifically the midpoint() + contrastRatio() + parseColorToRgb helpers)
    - .planning/phases/16.3-thorough-theme-revision-inserted-2026-05-13/16.3-RESEARCH.md §"Pattern 2: Mapping rubric" (the per-slot rubric table — the load-bearing methodology)
    - .planning/phases/16.3-thorough-theme-revision-inserted-2026-05-13/16.3-RESEARCH.md §"Pattern 3: Pre-edit contrast calculator" (the inline node calculator pattern)
    - .planning/phases/16.3-thorough-theme-revision-inserted-2026-05-13/16.3-RESEARCH.md §"Common Pitfalls" (1 polarity / 2 derived rgb-alpha drift / 3 D-01 regression / 5 hex-only parser / 8 swatch summarization)
    - .planning/phases/16.3-thorough-theme-revision-inserted-2026-05-13/16.3-PATTERNS.md §"Pattern 7: Per-palette UAT prompt shape" (the 5-row verdict table — Light row to populate)
    - .planning/phases/16.3-thorough-theme-revision-inserted-2026-05-13/16.3-01-SUMMARY.md (pre-phase Light baseline ratios — Light THEME-05 1.59, Light D-01 1.81; per-block md5 for verification)
    - .planning/phases/16.2-palette-aesthetic-refresh-inserted-2026-05-13/16.2-SUMMARY.md (immediate prior phase outcome — for "before" column reference and ring-inner Light hue context)
  </read_first>
  <action>
    This task is INTERACTIVE. Plan frontmatter has `autonomous: false`. The executor MUST surface a prompt to the operator at Step 1 below and wait for the operator's reply before proceeding. Do NOT pick a source design system unilaterally.

    Step 1 — PROMPT THE OPERATOR (the interactive checkpoint):
      Display the following prompt verbatim and wait for the operator's reply:

      ```
      Phase 16.3 Plan 02 — Light palette redesign.

      Which open-source design system should source the Light palette?
      Suggested Light-suitable sources (MIT-licensed, hex-publishing — see 16.3-RESEARCH.md):
        • Catppuccin Latte        (e.g. base #eff1f5, mantle #e6e9ef, blue #1e66f5, peach #fe640b)
        • Rosé Pine Dawn          (e.g. base #faf4ed, foam #56949f, gold #ea9d34)
        • Solarized Light         (e.g. base3 #fdf6e3, base2 #eee8d5, blue #268bd2, orange #cb4b16)
        • Gruvbox Light (medium)  (e.g. light0 #fbf1c7, blue #458588, orange #d65d0e)

      Please respond with the source name AND flavor (e.g. "Catppuccin Latte" or
      "Solarized Light"). The agent will fetch the canonical hex table from the
      source's canonical URL and apply the mapping rubric.
      ```

      Capture the operator's reply as `<chosen-source>` (e.g. "Catppuccin Latte"). DO NOT proceed past this step until the operator names a source.

    Step 2 — FETCH the canonical hex table for `<chosen-source>`. Per RESEARCH Pitfall 8 (WebFetch summarizes swatch pages — hex values lost), prefer `gh api` or raw.githubusercontent.com over WebFetch:
      - Catppuccin: `gh api repos/catppuccin/catppuccin/contents/docs/style-guide.md` OR `WebFetch https://catppuccin.com/palette` (text-rendered page — usable)
      - Rosé Pine: WebFetch raw.githubusercontent.com/rose-pine/rose-pine-palette/main/dist/css/rose-pine.css (raw CSS — usable)
      - Solarized: WebFetch https://ethanschoonover.com/solarized/ (text-rendered hex table — usable)
      - Gruvbox: `gh api repos/morhetz/gruvbox/contents/colors/gruvbox.vim` (raw vim file — usable)

      Capture all hex slots needed for the rubric (typically ~12–16 hex values per source: bg/surface/edge tier × 3–4, accent + accent-strong, muted/text, plus 4–6 named accents like blue/peach/yellow/mauve/lavender/green).

    Step 3 — APPLY the mapping rubric (RESEARCH §Pattern 2). For the Light palette specifically, treat the source as a LIGHT-flavor source:
      - `--color-breathing-bg`: source's base / lightest surface tier
      - `--color-breathing-bg-soft`: source's mantle / one-tier-deeper surface
      - `--color-breathing-bg-edge`: source's crust / deepest available light surface OR pure white if source lacks
      - `--color-breathing-surface`: source's surface0 / dialog-inner surface
      - `--color-breathing-accent`: source's primary semantic-blue accent (Catppuccin blue, Rosé Pine foam, Solarized blue, Gruvbox blue)
      - `--color-breathing-accent-strong`: source's darker/saturated accent (Catppuccin mauve or sapphire, Rosé Pine pine/iris, Solarized base02 or magenta) — MUST clear D-01 ≥ 1.5 vs on-accent
      - `--color-breathing-muted`: source's subtext / overlay mid-tone
      - `--color-breathing-on-accent`: source's lightest tone (typically base) IF accent-strong is dark; OR source's darkest text IF accent-strong is light
      - `--color-orb-in-from` / `-in-to`: a LIGHT POLE pair in source's cool family (sky/blue/lavender) — luminance ~0.85–0.95 for Light palette to keep In as the airy pole
      - `--color-orb-out-from` / `-out-to`: a DARK POLE pair in source's warm family (peach/orange/yellow/mauve) — luminance ~0.35–0.55 to clear THEME-05 floor vs the unchanged-bright In
      - `--color-orb-in-text`: source's darkest text — must clear WCAG AA 3.0 (large text) vs In midpoint
      - `--color-orb-out-text`: source's darkest text in warm family OR same as orb-in-text if Out midpoint is light enough
      - `--color-ring-outer`: rgb(<orb-in-from channels> / 0.5–0.7)
      - `--color-ring-inner`: rgb(<orb-out-from channels> / 0.35–0.45)
      - `--color-modal-backdrop`: rgb(<deepest source neutral channels> / 0.30–0.50)

    Step 4 — RUN the pre-edit contrast calculator (RESEARCH §"Code Examples"). Inline via `node -e '...'` is fine; alternatively save as scratch script. Verify three invariants BEFORE writing src/styles/theme.css:
      a. Polarity: inMidLum > outMidLum (in is LIGHTER pole for Light palette).
      b. THEME-05 floor: contrastRatio(inMid, outMid) ≥ 1.5.
      c. D-01 floor: contrastRatio(accent-strong, on-accent) ≥ 1.5.

      If polarity fails → flip orb-in/orb-out role assignments per RESEARCH Pitfall 1.
      If THEME-05 fails → iterate hex picks (lighten In family further OR deepen Out family further) and re-run calculator. Document iteration count in the SUMMARY/commit body.
      If D-01 fails → swap on-accent to a deeper source tone (typically source's darkest text). Or swap accent-strong to a deeper source accent. Re-run calculator.

      Repeat Steps 3+4 until all three invariants pass.

    Step 5 — EDIT `src/styles/theme.css` @theme block ONLY (lines ~1–38; specifically lines 4–26 which hold the 17 themable tokens). Write the validated mapping:
      - 8 chrome tokens (lines ~4–11)
      - 6 orb tokens (lines ~18–23: orb-in-from, orb-in-to, orb-out-from, orb-out-to, orb-in-text, orb-out-text)
      - 3 derived rgb/alpha tokens (lines ~24–26: ring-outer, ring-inner, modal-backdrop)

      DO NOT TOUCH:
      - Lines 28–37 (layout/sizing tokens: --orb-size, --orb-scale-min/max/mid, --shadow-breathing-card) — D-02 non-themable
      - The 4 other palette selector blocks ([data-theme='dark']:root, ='moss', ='slate', ='dusk') — they MUST remain byte-identical to their pre-phase md5 from 16.3-01-SUMMARY.md
      - Any `.tsx` file (THEME-UI-01 guard)
      - The two test files (theme.contrast.test.ts, theme.no-hardcoded-classes.test.ts) — frozen guards

      UPDATE the comment block above the Light palette tokens (currently lines 1–3 + 13–17) to reference the new source: e.g. replace the 16.2 F1 retune comment with a one-line attribution: `/* Source: <chosen-source> (<owner>/<repo> MIT) — <flavor> flavor */` plus a 2–3 line note describing the rubric application (mauve as accent-strong, peach as orb-out-from, etc.).

    Step 6 — RUN guards (the automated gate):
      - `npm test -- src/styles/theme.contrast.test.ts src/styles/theme.no-hardcoded-classes.test.ts --run` exits 0 (10 it() pass for theme.contrast — Light passes the new ratios; other 4 palettes pass by construction since they weren't touched; theme.no-hardcoded-classes passes 10 banned-pattern checks — green by construction since no .tsx touched).

      Sentinels:
      - `grep -cE "^  --color-orb-" src/styles/theme.css` returns 30 (no orb tokens added/dropped)
      - `grep -cE "^  --color-breathing-" src/styles/theme.css` returns 40 (no chrome tokens added/dropped)
      - `git diff --name-only` returns exactly `src/styles/theme.css` (the FULL repo diff — no .tsx, no test, no config, no docs files in this commit)
      - Per-block md5 for Dark/Moss/Slate/Dusk matches the pre-phase md5 from 16.3-01-SUMMARY.md (their selector blocks must be byte-identical).

      Full chain:
      - `npx tsc --noEmit && npm run lint && npm run build && npm test -- --run` exits 0.

    Step 7 — PER-PALETTE UAT (the perceptual gate — PATTERNS Pattern 7). This is the operator-input checkpoint:
      a. Run `npm run dev`. Wait for "Local: http://localhost:5173/hrv/" line.
      b. Surface this prompt to the operator:

      ```
      Phase 16.3 Plan 02 — Light palette UAT.

      1. Open http://localhost:5173/hrv/ in Chrome.
      2. Open SettingsDialog (gear icon).
      3. Select "Light" in the ThemePicker, close the dialog.
      4. Observe the orb at idle. Verify the In gradient feels integrated with the new chrome (bg, surface, accent).
      5. Click "Start". Watch the 3-2-1 lead-in countdown — verify the digit is legible (renders in --color-orb-in-text on the In gradient).
      6. Watch at least 2 full In→Out→In transitions. Verify:
         - Out gradient feels integrated with the chrome.
         - Active phase label ("Inhale"/"Exhale") is legible on its phase.
         - In and Out are perceptually distinct (the math guarantees ratio ≥ 1.5, but verify).
      7. Stop the session.

      Capture the verdict in this row (one row of the eventual 5-row phase verdict table):

      | # | Palette | data-theme attr | Source design system | Chrome anchors | Orb In family | Orb Out family | Visual integration | Lead-in digit legibility | Active phase label legibility | Notes |
      |---|---------|-----------------|----------------------|----------------|---------------|----------------|-------------------|--------------------------|-------------------------------|-------|
      | 1 | Light   | (no attr / @theme baseline) | <chosen-source> | bg <hex>, surface <hex>, accent <hex> | <in family> | <out family> | approved / rejected | pass / fail | pass / fail | source citation |

      Reply with `approved (Light)` + the completed row.

      OR reply with `rejected: <observation>` — agent will iterate (return to Step 3 / Step 4 with a different hex pick from <chosen-source>'s palette, or operator may name a different source).

      Loop bound (W3): ≥3 distinct-source-system rejections of the Light palette → STOP and return `phase-blocked` to the operator.
      ```

      Wait for operator approval. Do NOT commit until 1/1 Light approval is returned.

      On rejection: return to Step 3 (re-apply rubric with different hex picks from the same source, OR ask operator to name a different source), then Step 4 → Step 5 → Step 6 → Step 7 again. Track the rejection count; on the 3rd rejection of Light with 3 distinct source-system attempts, return `phase-blocked`.

    Step 8 — COMMIT atomically. Subject line shape from PATTERNS Pattern 1:

      `feat(16.3-02): redesign Light palette from <chosen-source> (<flavor> flavor)`

      Body:
      ```
      Maps <chosen-source> base/surface/accent/text role slots onto our Light chrome + orb tokens
      per RESEARCH §Pattern 2 mapping rubric.

      THEME-05 contrast: <X.XX> (≥ 1.5 floor; was 1.59 pre-phase).
      D-01 accent-strong vs on-accent: <X.XX>:1 (≥ 1.5 floor; was 1.81 pre-phase).

      Source: <owner>/<repo> (MIT) — <flavor> flavor.
      URL: <canonical-palette-url>
      ```
  </action>
  <verify>
    <automated>npm test -- src/styles/theme.contrast.test.ts src/styles/theme.no-hardcoded-classes.test.ts --run && npx tsc --noEmit && npm run lint && npm run build && npm test -- --run && [ "$(grep -cE '^  --color-orb-' src/styles/theme.css)" -eq 30 ] && [ "$(grep -cE '^  --color-breathing-' src/styles/theme.css)" -eq 40 ] && [ "$(git diff --name-only HEAD~1 HEAD)" = "src/styles/theme.css" ]</automated>
  </verify>
  <acceptance_criteria>
    - The `@theme { ... }` block in `src/styles/theme.css` contains tokens deliberately mapped from a named open-source design system (operator-chosen at execution; recorded in commit body).
    - At least one of `--color-breathing-bg`, `--color-breathing-accent`, `--color-breathing-accent-strong`, `--color-orb-in-from`, `--color-orb-out-from` differs from its pre-phase value (i.e. the palette is actually redesigned, not a no-op).
    - `--color-breathing-bg`, `--color-breathing-bg-soft`, `--color-breathing-bg-edge`, `--color-breathing-surface`, `--color-breathing-accent`, `--color-breathing-accent-strong`, `--color-breathing-muted`, `--color-breathing-on-accent` are all defined as 6-or-7-char hex literals (`#rrggbb` or `#rgb`) — NOT `oklch()` or `hsl()` (RESEARCH Pitfall 5).
    - `--color-orb-in-from`, `--color-orb-in-to`, `--color-orb-out-from`, `--color-orb-out-to`, `--color-orb-in-text`, `--color-orb-out-text` are all hex literals.
    - `--color-ring-outer`, `--color-ring-inner`, `--color-modal-backdrop` use `rgb(R G B / alpha)` form (not hex; recomputed from the new In/Out/bg families per RESEARCH Pitfall 2).
    - The 4 other palette selector blocks (Dark/Moss/Slate/Dusk) have md5 byte-identical to the pre-phase md5 captured in 16.3-01-SUMMARY.md. NO cross-palette edits in this plan.
    - The layout/sizing tokens at @theme lines 28–37 (--orb-size, --orb-scale-*, --shadow-breathing-card) are byte-identical to pre-phase (D-02 non-themable).
    - `grep -cE "^  --color-orb-" src/styles/theme.css` returns 30.
    - `grep -cE "^  --color-breathing-" src/styles/theme.css` returns 40.
    - `git diff --name-only HEAD~1 HEAD` returns exactly `src/styles/theme.css` (FULL repo diff for the Light commit).
    - `npm test -- src/styles/theme.contrast.test.ts --run` exits 0 (all 5 palettes pass — Light passes the new mapping, other 4 pass by virtue of being untouched).
    - `npm test -- src/styles/theme.no-hardcoded-classes.test.ts --run` exits 0.
    - `npx tsc --noEmit && npm run lint && npm run build && npm test -- --run` exits 0.
    - Per-palette UAT: operator returned `approved (Light)` along with the completed verdict row before commit.
    - Commit subject matches pattern `feat(16.3-02): redesign Light palette from <source> (<flavor> flavor)` and commit body cites the source repo + flavor + canonical URL + post-redesign THEME-05 and D-01 ratios.
  </acceptance_criteria>
  <done>
    Light palette redesigned from an operator-named open-source design system. All 17 Light tokens mapped per RESEARCH §Pattern 2. THEME-05 ≥ 1.5 + D-01 ≥ 1.5 + polarity invariant all hold. Other 4 palettes byte-identical. Per-palette UAT approved before commit. Atomic B1 commit boundary: if any later regression surfaces, bisects directly to this commit.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| no new boundaries | Pure CSS hex/rgb value substitution within a single selector block. No new runtime computation, no new event handlers, no new auth/data surface, no new dependencies. Web-fetch of source design-system hex tables is INFORMATION INGRESS to the planning corpus, not runtime ingress. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16.3-02-01 | Spoofing (a11y) | Light THEME-05 floor or D-01 floor breached | mitigate | Pre-edit contrast calculator (Step 4) validates BOTH floors BEFORE src/styles/theme.css is written. theme.contrast.test.ts (Step 6) re-validates in jsdom after the write. Both gates must pass before commit. |
| T-16.3-02-02 | Spoofing (a11y) | Orb-in-text / orb-out-text legibility regression on new gradient midpoints | mitigate | Rubric specifies orb-in-text/orb-out-text routes from source's darkest text/base. Step 7 per-palette UAT verifies legibility perceptually (lead-in digit + active phase label). If UAT rejects on legibility, Step 7 loops back to rubric iteration before commit. |
| T-16.3-02-03 | Tampering | Polarity inversion (in midpoint darker than out midpoint) | mitigate | Step 4a polarity check explicit: if inMidLum < outMidLum, flip the role assignments (RESEARCH Pitfall 1). The pre-edit calculator surfaces this before write. |
| T-16.3-02-04 | Tampering | Cross-palette regression (Dark/Moss/Slate/Dusk md5 changes) | mitigate | Acceptance criterion asserts per-block md5 for Dark/Moss/Slate/Dusk equals pre-phase md5 from 16.3-01-SUMMARY.md. Scope guard (full diff = src/styles/theme.css only) is necessary; per-block md5 cross-check is sufficient. |
| T-16.3-02-05 | Tampering | Derived rgb()/alpha tokens left stale (ring-outer/ring-inner/modal-backdrop) | mitigate | Rubric step explicitly recomputes the 3 derived tokens from the new In/Out/bg families (RESEARCH Pitfall 2). UAT step verifies ring hues read as belonging to the new orb gradient. |
| T-16.3-02-06 | Tampering | Stray .tsx edit breaks THEME-UI-01 guard | mitigate | `[ "$(git diff --name-only HEAD~1 HEAD)" = "src/styles/theme.css" ]` scope guard runs in verify. theme.no-hardcoded-classes.test.ts re-runs the 10 banned-pattern scan against src/**/*.tsx after the commit. |
| T-16.3-02-07 | Denial of Service | UAT rejection loop spins indefinitely on a perceptually-impossible Light source | mitigate | W3 fix from 16.2: Step 7 enforces 3-distinct-source-system rejection bound. On 3rd rejection across 3 sources, executor returns `phase-blocked` for operator decision. |
</threat_model>

<verification>
After Task 1 completes:

```bash
# Contrast guard — all 5 palettes pass:
npm test -- src/styles/theme.contrast.test.ts --run
# Expected: 10 it() pass (5 THEME-05 + 5 D-01). Light shows new ratios; other 4 unchanged.

# Hardcoded-classes guard — green by construction (no .tsx touched):
npm test -- src/styles/theme.no-hardcoded-classes.test.ts --run
# Expected: 10 it() pass.

# Sentinels:
[ "$(grep -cE '^  --color-orb-' src/styles/theme.css)" -eq 30 ] && echo "orb tokens OK"
[ "$(grep -cE '^  --color-breathing-' src/styles/theme.css)" -eq 40 ] && echo "chrome tokens OK"

# Scope guard:
[ "$(git diff --name-only HEAD~1 HEAD)" = "src/styles/theme.css" ] && echo "scope OK"

# Per-block md5 cross-check (Dark/Moss/Slate/Dusk unchanged):
for theme in dark moss slate dusk; do
  current=$(awk "/^\[data-theme='$theme'\]:root \{/,/^\}$/" src/styles/theme.css | md5 -q)
  echo "$theme: $current — must match 16.3-01-SUMMARY.md"
done

# Full chain:
npx tsc --noEmit && npm run lint && npm run build && npm test -- --run
```
</verification>

<success_criteria>
1. Light @theme block in `src/styles/theme.css` redesigned from an operator-named open-source design system.
2. THEME-05 contrast ratio (Light orb in-mid vs out-mid) ≥ 1.5 post-redesign.
3. D-01 contrast ratio (Light accent-strong vs on-accent) ≥ 1.5 post-redesign.
4. Polarity invariant holds: Light orb-in midpoint luminance > orb-out midpoint luminance.
5. The 4 other palette blocks (Dark/Moss/Slate/Dusk) have md5 byte-identical to their pre-phase md5 from 16.3-01-SUMMARY.md.
6. Token-name contract holds: grep counts return 30 orb + 40 chrome (no token names added/dropped).
7. `theme.no-hardcoded-classes.test.ts` exits 0 (zero .tsx changes — full repo diff = src/styles/theme.css only).
8. Per-commit green-gate: `tsc + lint + build + test` exit 0.
9. Per-palette UAT for Light: operator returned `approved (Light)` with completed verdict row BEFORE commit.
10. Atomic commit with subject `feat(16.3-02): redesign Light palette from <source> (<flavor> flavor)` and body citing source repo + flavor + URL + post-redesign ratios.
</success_criteria>

<output>
After Task 1 completes, src/styles/theme.css has the Light palette redesigned from a named source. The commit body carries the per-token mapping rationale, contrast ratios, and the per-palette UAT verdict row. No per-palette `16.3-02-SUMMARY.md` is written — the atomic B1 commit scope-guard requires `git diff --name-only HEAD~1 HEAD = src/styles/theme.css` (a separate SUMMARY would violate it). Plan 07 (phase close) reads the per-palette commit bodies and aggregates all 5 verdicts into the single phase SUMMARY.
</output>
