---
phase: 12-assets-content-hygiene-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/REQUIREMENTS.md
  - REVIEW.md
  - src/storage/format.ts
  - public/favicon.svg
  - index.html
  - src/content/learnContent.ts
  - src/content/learnContent.test.ts
  - src/components/LearnDialog.test.tsx
  - src/domain/settings.ts
  - src/storage/settings.ts
  - src/domain/settings.test.ts
autonomous: true
requirements:
  - ASSETS-01
  - CONTENT-01
  - HYGIENE-01
  - HYGIENE-02
  - HYGIENE-03

must_haves:
  truths:
    - "HYGIENE-01 row in REQUIREMENTS.md traceability table reads 'Overtaken (by Phase 9 AUDIO-02)' (per CONTEXT D-01). Phase 12 does NOT remove audioNow from the hook — Phase 9 AUDIO-02 caller-side clamp at App.tsx:200, :342, :546 depends on it. ROADMAP Phase 12 Success Criterion #3 (pruning audioNow) is satisfied via this docs-only flip, NOT by literal code removal."
    - "REVIEW.md §IN-02 ends with a '[2026-05-12 update] Overtaken by Phase 9 AUDIO-02 …' line citing App.tsx:549 and 12-CONTEXT.md (per CONTEXT D-02)."
    - "src/storage/format.ts:42 has a single-line `/** @param now Test-only seam — … */` JSDoc immediately above the `formatLastSessionDate` signature (per CONTEXT D-11)."
    - "public/favicon.svg exists, ~150–200 bytes, contains exactly one `<circle>` with the resolved `--color-breathing-accent` hex `#0f766e`, and has no `<script>` / `onload=` / `<foreignObject>` / external refs (per CONTEXT D-03)."
    - "index.html:5 favicon `<link>` href is `%BASE_URL%favicon.svg` (per CONTEXT D-04). `npm run build` produces a `dist/index.html` whose favicon href is `/hrv/favicon.svg`."
    - "src/content/learnContent.ts:60 book.url is the verbatim Forrest YouTube-description Amazon URL (per CONTEXT D-05): `https://www.amazon.com/Mastering-Meditation-Eight-Steps-Beginner-ebook/dp/B0CCFWP4W8?sr=8-1&linkId=1a5a2958fc89bdb6769b54d0bc9a4d17&language=en_US`. The string `amzn.to/3RTAVqi` no longer appears in src/."
    - "LearnDialog disclaimer at src/components/LearnDialog.tsx:171 (\"Independent project. Not affiliated with Forrest Knutson.\") is NOT edited (per CONTEXT D-06 — the linkId is Forrest's tag, not ours)."
    - "src/domain/settings.ts exports `isValidBpm`, `isValidRatio`, `isValidDuration` with `(v: unknown): v is <DomainType>` signatures (per CONTEXT D-08). `validateSettings` body calls these predicates and preserves the verbatim `RangeError` throw class + `` `Unsupported BPM: ${String(...)}` `` message format (per CONTEXT D-09)."
    - "src/storage/settings.ts no longer defines local `isValidBpm`/`isValidRatio`/`isValidDuration` functions; the names are imported from `../domain/settings`; the import block has no unused names (per CONTEXT D-09). `coerceSettings` body at lines 35–44 is byte-identical for fallback policy."
    - "src/domain/settings.test.ts exists, imports the three predicates, and contains 6–9 `it()` cases across three `describe` blocks (per CONTEXT D-10) covering happy/invalid/wrong-type per predicate."
    - "Every commit boundary in Phase 12 passes `npx tsc --noEmit`, `npm run lint`, `npm run build`, and the full Vitest suite (per CONTEXT D-15). No new `react-hooks/*` ESLint disables are introduced (per CONTEXT D-14)."
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "HYGIENE-01 traceability row flipped to 'Overtaken (by Phase 9 AUDIO-02)'"
      contains: "Overtaken (by Phase 9 AUDIO-02)"
    - path: "REVIEW.md"
      provides: "[2026-05-12 update] addendum to §IN-02"
      contains: "[2026-05-12 update] Overtaken by Phase 9 AUDIO-02"
    - path: "src/storage/format.ts"
      provides: "Single-line JSDoc above formatLastSessionDate documenting test-only `now` seam"
      contains: "Test-only seam"
    - path: "public/favicon.svg"
      provides: "Single-color filled-orb favicon glyph (~150–200 bytes)"
      contains: "<circle"
    - path: "index.html"
      provides: "Vite %BASE_URL% favicon link"
      contains: "%BASE_URL%favicon.svg"
    - path: "src/content/learnContent.ts"
      provides: "book.url = canonical amazon.com /dp/B0CCFWP4W8 URL"
      contains: "/dp/B0CCFWP4W8"
    - path: "src/content/learnContent.test.ts"
      provides: "URL assertion updated to canonical amazon.com URL"
      contains: "/dp/B0CCFWP4W8"
    - path: "src/components/LearnDialog.test.tsx"
      provides: "Book link href assertion updated to canonical amazon.com URL"
      contains: "/dp/B0CCFWP4W8"
    - path: "src/domain/settings.ts"
      provides: "Shared predicates `isValidBpm`/`isValidRatio`/`isValidDuration` + predicate-call rewrite of validateSettings"
      contains: "export function isValidBpm"
    - path: "src/storage/settings.ts"
      provides: "Imports shared predicates from domain; local declarations removed"
      contains: "isValidBpm,"
    - path: "src/domain/settings.test.ts"
      provides: "6–9 cases locking the three predicates at the domain layer (structural gap-fill — CONTEXT D-10)"
      contains: "describe('isValidBpm"
  key_links:
    - from: "src/storage/settings.ts"
      to: "src/domain/settings.ts"
      via: "named import of isValidBpm/isValidRatio/isValidDuration"
      pattern: "from '\\.\\./domain/settings'"
    - from: "index.html"
      to: "public/favicon.svg"
      via: "Vite %BASE_URL% HTML substitution (vite.config.ts base: '/hrv/')"
      pattern: "%BASE_URL%favicon\\.svg"
    - from: "dist/index.html"
      to: "/hrv/favicon.svg"
      via: "Vite build-time substitution of %BASE_URL% → '/hrv/'"
      pattern: "/hrv/favicon\\.svg"
    - from: "src/components/LearnDialog.tsx (existing render — NOT EDITED per D-06)"
      to: "src/content/learnContent.ts book.url"
      via: "anchor href via LEARN_CONTENT.links.book.url"
      pattern: "/dp/B0CCFWP4W8"
    - from: ".planning/REQUIREMENTS.md HYGIENE-01 row"
      to: "REVIEW.md §IN-02 addendum"
      via: "cross-cited 'Overtaken by Phase 9 AUDIO-02' rationale"
      pattern: "Overtaken"
---

<objective>
Ship the five Phase 12 cleanup items in a single plan, single wave, in CONTEXT D-13 risk-ascending order: (1) HYGIENE-01 docs-only row flip + REVIEW.md addendum (closes the misleading "Pending" status before any code edits — HYGIENE-01 is overtaken by the Phase 9 AUDIO-02 caller-side clamp that depends on `audio.audioNow`); (2) HYGIENE-03 one-line JSDoc on `formatLastSessionDate`'s test-only `now` seam (zero-behaviour); (3) ASSETS-01 — new `public/favicon.svg` filled-orb glyph + `index.html` `%BASE_URL%` swap (closes the `/favicon.svg` 404 under Vite `base: '/hrv/'`); (4) CONTENT-01 — swap `amzn.to/3RTAVqi` to Forrest's verbatim canonical `amazon.com/.../dp/B0CCFWP4W8?…` URL plus update the two existing test assertions that lock the old string; (5) HYGIENE-02 — relocate `isValidBpm`/`isValidRatio`/`isValidDuration` from `src/storage/settings.ts` to `src/domain/settings.ts`, rewrite `validateSettings` to call them (preserving throw class + message format verbatim), and add a new `src/domain/settings.test.ts` (~6–9 cases) as a structural gap-fill.

Purpose: Close the last v1.0.1 patch phase. Each task lands on the strict baseline + per-commit green-gate from Phase 7 D-09 / Phase 11 D-17 with no behavioural delta beyond the favicon asset materialising and the book URL becoming honest in hover-preview.

Output: 5 task-group commits; +1 new asset (`public/favicon.svg`); +1 new test file (`src/domain/settings.test.ts` with 6–9 cases); ~1 unused-import prune in `src/storage/settings.ts`; +2 docs edits; 0 net behaviour delta in app code.
</objective>

<execution_context>
@$HOME/.claude-personal-personal/get-shit-done/workflows/execute-plan.md
@$HOME/.claude-personal-personal/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/12-assets-content-hygiene-cleanup/12-CONTEXT.md
@.planning/phases/12-assets-content-hygiene-cleanup/12-PATTERNS.md
@REVIEW.md

<!-- Source-of-truth files under edit (read these directly; do not re-derive from analogs) -->
@index.html
@vite.config.ts
@src/content/learnContent.ts
@src/content/learnContent.test.ts
@src/components/LearnDialog.test.tsx
@src/domain/settings.ts
@src/storage/settings.ts
@src/storage/format.ts
@src/styles/theme.css

<interfaces>
<!-- Key contracts the executor needs. Extracted from codebase 2026-05-12. -->

From src/domain/settings.ts (current, before edit):
```typescript
export type RatioLabel = '50:50' | '40:60' | '30:70' | '20:80'
export type DurationOption = number | 'open-ended'
export interface SessionSettings { bpm: number; ratio: RatioLabel; durationMinutes: DurationOption }
export const BPM_OPTIONS: readonly number[]            // line 10
export const RATIO_OPTIONS: readonly RatioLabel[]      // line 26
export const DURATION_OPTIONS: readonly DurationOption[]  // line 28
export const DEFAULT_SETTINGS: SessionSettings         // line 44
export function validateSettings(settings: SessionSettings): SessionSettings  // line 50; THROWS RangeError
```

From src/storage/settings.ts (current, before edit) — the three predicates to relocate verbatim:
```typescript
// Lines 20–33 — file-private (function, not export function). No external importers (grep confirms).
function isValidBpm(v: unknown): v is number {
  return typeof v === 'number' && Number.isFinite(v) && (BPM_OPTIONS as readonly number[]).includes(v)
}
function isValidRatio(v: unknown): v is RatioLabel {
  return typeof v === 'string' && (RATIO_OPTIONS as readonly string[]).includes(v)
}
function isValidDuration(v: unknown): v is DurationOption {
  if (v === 'open-ended') return true
  return typeof v === 'number'
    && Number.isFinite(v)
    && (DURATION_OPTIONS as readonly DurationOption[]).includes(v)
}
```

From src/storage/format.ts (current, lines 41–42):
```typescript
// D-05: "May 7" current year; "May 7, 2025" other year.
export function formatLastSessionDate(atMs: number, now: () => number = Date.now): string {
```

From src/app/App.tsx (the Phase 9 AUDIO-02 caller-side clamp consumers of audio.audioNow — DO NOT EDIT, cited for D-01/D-02 rationale):
- Line 200: `const audioAudioNow = audio.audioNow`
- Line 342: comment cites `audioNow() inside this setTimeout callback`
- Line 546: `// AUDIO-02 D-01/D-02 caller-side clamp. Pitfall 5: audio.audioNow() returns …`

From src/styles/theme.css line 7:
```
--color-breathing-accent: #0f766e;
```
(Tailwind teal-700; literal hex for the favicon `fill` per CONTEXT D-03.)

From vite.config.ts:
```typescript
base: '/hrv/'  // line 7 — production base path; %BASE_URL% expands to '/hrv/'
```
</interfaces>

<!-- TEST-FILE PRE-FLIGHT FACT (read before Task 4) -->
<!-- CONTEXT D-07 says "no test asserts on the literal amzn.to/3RTAVqi". -->
<!-- Reality check (grep -rn 'amzn.to/3RTAVqi' src/) at plan time found TWO assertions: -->
<!--   1. src/content/learnContent.test.ts:53–54  expect(LEARN_CONTENT.links.book.url).toBe('https://amzn.to/3RTAVqi') -->
<!--   2. src/components/LearnDialog.test.tsx:120  expect(link).toHaveAttribute('href', 'https://amzn.to/3RTAVqi') -->
<!-- Task 4 MUST update both assertions when swapping the source string; D-07's "no test changes" -->
<!-- forecast is superseded by these existing locks. README.md:37 also references the old URL but -->
<!-- is documentation, not asserted in tests — it is OUT OF SCOPE per files_modified scope. -->
</context>

<tasks>

<task type="auto">
  <name>Task 1: HYGIENE-01 — flip REQUIREMENTS.md HYGIENE-01 row to "Overtaken (by Phase 9 AUDIO-02)" + append [2026-05-12 update] addendum to REVIEW.md §IN-02 (docs-only)</name>
  <files>.planning/REQUIREMENTS.md, REVIEW.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (full file — table at lines 122–150, totals line at 152)
    - REVIEW.md lines 385–395 (§IN-02 block ending with the existing "Fix:" paragraph)
    - .planning/phases/12-assets-content-hygiene-cleanup/12-CONTEXT.md (D-01 + D-02 — verbatim addendum string and rationale)
    - src/app/App.tsx lines 198–202, 542–552 (the Phase 9 AUDIO-02 caller-side clamp that REQUIRES `audio.audioNow` to remain — proof HYGIENE-01 is overtaken, not pending)
  </read_first>
  <action>
    Per CONTEXT D-01: in .planning/REQUIREMENTS.md, edit the HYGIENE-01 traceability-table row (currently line 148: `| HYGIENE-01 | Phase 12 — Assets, Content & Hygiene Cleanup | Pending |`). Change the Status cell from `Pending` to `Overtaken (by Phase 9 AUDIO-02)` — keep the 3-column shape (per PATTERNS.md "REQUIREMENTS.md row" analysis — Phase 12 introduces the first `Overtaken` cell; do NOT add a fourth Notes column). Leave every other row and the `**Phase totals:**` line at 152 unchanged (HYGIENE-01 still counts in Phase 12's total of 5; only its status changes).

    Per CONTEXT D-02: in REVIEW.md §IN-02 (the block beginning `### IN-02:` near line 385), append the addendum on a new paragraph immediately after the existing `**Fix:** Remove from the return tuple …` line that ends with `App.tsx doesn't actually call it).` Preserve a blank line before the addendum. The addendum text is VERBATIM:

    `[2026-05-12 update] Overtaken by Phase 9 AUDIO-02 — `audio.audioNow()` is the documented seam for the caller-side past-time clamp at App.tsx:549. HYGIENE-01 closed-no-op in 12-CONTEXT.md.`

    Use backticks around `audio.audioNow()` and `App.tsx:549` exactly as shown. Do NOT edit any other section of REVIEW.md; the file is the frozen 2026-05-11 deep review and this is the first dated addendum.

    Commit message: `docs(12): HYGIENE-01 overtaken by Phase 9 AUDIO-02 — REQUIREMENTS row flip + REVIEW.md §IN-02 addendum` and run the per-commit green-gate (`npx tsc --noEmit && npm run lint && npm run build && npm test`) before committing per CONTEXT D-15. (All four should pass trivially — docs-only change.)
  </action>
  <verify>
    <automated>grep -c '| HYGIENE-01 | Phase 12 — Assets, Content & Hygiene Cleanup | Overtaken (by Phase 9 AUDIO-02) |' .planning/REQUIREMENTS.md | grep -q '^1$' && ! grep -q '| HYGIENE-01 | Phase 12 — Assets, Content & Hygiene Cleanup | Pending |' .planning/REQUIREMENTS.md && grep -c '\[2026-05-12 update\] Overtaken by Phase 9 AUDIO-02' REVIEW.md | grep -q '^1$' && grep -q 'App.tsx:549' REVIEW.md && grep -q '12-CONTEXT.md' REVIEW.md && grep -c '\*\*Phase totals:\*\* 7 → 3 / 8 → 3 / 9 → 7 / 10 → 5 / 11 → 4 / 12 → 5  =  27' .planning/REQUIREMENTS.md | grep -q '^1$' && npx tsc --noEmit && npm run lint && npm run build && npm test -- --run --reporter=basic 2>&1 | tail -5 | grep -qE 'Test Files.*passed' && echo PASS</automated>
  </verify>
  <done>
    REQUIREMENTS.md HYGIENE-01 row shows `Overtaken (by Phase 9 AUDIO-02)`; the old `Pending` cell is gone; totals line unchanged. REVIEW.md §IN-02 ends with the verbatim `[2026-05-12 update] …` line (single match in the whole file). All four green-gate commands exit 0.
  </done>
</task>

<task type="auto">
  <name>Task 2: HYGIENE-03 — add one-line JSDoc above formatLastSessionDate documenting the test-only `now` seam (zero-behaviour)</name>
  <files>src/storage/format.ts</files>
  <read_first>
    - src/storage/format.ts (full file — pay attention to lines 41–42 where the `// D-05:` comment sits above the `formatLastSessionDate` signature; lines 1–4 and lines 18–27 confirm the existing comment style is `//` and JSDoc is new here)
    - src/storage/format.test.ts (verify it already exercises the same-year vs other-year branches via the `now` seam — Task 2 does not touch this file; reading it confirms the JSDoc's claim is accurate)
    - .planning/phases/12-assets-content-hygiene-cleanup/12-CONTEXT.md (D-11 — verbatim wording suggestion)
    - .planning/phases/12-assets-content-hygiene-cleanup/12-PATTERNS.md (lines ~290–321 — JSDoc-single-line pattern from src/audio/audioEngine.ts:39–50)
  </read_first>
  <action>
    Per CONTEXT D-11: insert a single new JSDoc line BETWEEN the existing `// D-05: "May 7" current year; "May 7, 2025" other year.` comment at src/storage/format.ts:41 and the `export function formatLastSessionDate(...)` declaration at line 42. Preserve the existing `// D-05:` comment verbatim above. Insert this exact JSDoc as line 42 (existing function declaration becomes line 43):

    `/** @param now Test-only seam — production callers always omit this; tests pass a pinned `() => number` to drive the same-year vs other-year branch coverage in `format.test.ts`. */`

    Use the compact one-line JSDoc form `/** ... */` (PATTERNS.md cites `src/audio/audioEngine.ts:39–50` as the canonical compact-style precedent — same shape: opener, single line of prose, closer, all on one physical line). Do NOT add JSDoc to `formatTotalMinutes`, `formatSessionCount`, `formatLastSessionDuration`, or `formatLastSession` — HYGIENE-03 scope is `formatLastSessionDate` only. Do NOT touch `src/storage/format.test.ts` — the test already exercises the seam and is the proof-of-test-only-intent the JSDoc cites.

    Commit message: `docs(12): HYGIENE-03 — document formatLastSessionDate test-only `now` seam` and run the per-commit green-gate before committing per D-15.
  </action>
  <verify>
    <automated>grep -nE '^/\*\* @param now Test-only seam' src/storage/format.ts | grep -q '^42:' && grep -c '@param now Test-only seam' src/storage/format.ts | grep -q '^1$' && grep -nE '^// D-05: "May 7" current year' src/storage/format.ts | grep -q '^41:' && grep -nE '^export function formatLastSessionDate' src/storage/format.ts | grep -q '^43:' && grep -c 'export function format' src/storage/format.ts | grep -q '^5$' && npx tsc --noEmit && npm run lint && npm run build && npm test -- --run --reporter=basic 2>&1 | tail -5 | grep -qE 'Test Files.*passed' && echo PASS</automated>
  </verify>
  <done>
    src/storage/format.ts line 41 is the existing `// D-05: …` comment; line 42 is the new single-line JSDoc starting `/** @param now Test-only seam`; line 43 is `export function formatLastSessionDate(...)`. Exactly one `@param now Test-only seam` in the file. Total `export function format` count unchanged (5 — `formatTotalMinutes`, `formatSessionCount`, `formatLastSessionDate`, `formatLastSessionDuration`, `formatLastSession`). All four green-gate commands exit 0.
  </done>
</task>

<task type="auto">
  <name>Task 3: ASSETS-01 — create public/favicon.svg filled-orb glyph + swap index.html:5 favicon href to `%BASE_URL%favicon.svg`</name>
  <files>public/favicon.svg, index.html</files>
  <read_first>
    - index.html (full file — 13 lines; line 5 is the current `<link rel="icon" … href="/favicon.svg" />`)
    - vite.config.ts (line 7 confirms `base: '/hrv/'` — production `%BASE_URL%` expands to `/hrv/`)
    - src/styles/theme.css line 7 (confirms `--color-breathing-accent: #0f766e;` — the literal hex to inline in the SVG; CSS custom properties do not apply inside an external SVG loaded as a `<link rel="icon">` resource, per CONTEXT D-03 — so the hex MUST be inlined)
    - .planning/phases/12-assets-content-hygiene-cleanup/12-CONTEXT.md (D-03 + D-04 — design brief and href swap)
    - .planning/phases/12-assets-content-hygiene-cleanup/12-PATTERNS.md (the `public/favicon.svg` section — "No Analog Found" notes the `public/` directory does not exist yet; Vite copies `public/*` to `dist/` verbatim under `base`)
  </read_first>
  <action>
    Step A — create `public/favicon.svg` (the `public/` directory does not exist yet; the Write tool will create both). The file body MUST be exactly this single-line SVG (no trailing newline preferred; one trailing newline acceptable — both pass the byte-size gate):

    `<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 32 32"><circle cx="16" cy="16" r="14" fill="#0f766e"/></svg>`

    Notes on the literal hex `#0f766e`: this is the resolved value of `--color-breathing-accent` from `src/styles/theme.css:7` (Tailwind teal-700). PATTERNS.md speculatively cited `#0d9488` (teal-600); the planner verified the actual theme value at plan time and `#0f766e` is correct. CONTEXT D-03 instructs the planner to pick the literal hex matching the resolved theme — `#0f766e` matches `--color-breathing-accent` exactly. Single `<circle>` only; do NOT include `<script>`, `<foreignObject>`, `onload=`, `xlink:href`, external `href`, gradients, filters, or animation elements (threat-model carve-out per Phase 12 `<threat_model>` ASSETS-01 row).

    Step B — edit `index.html:5`. Current line:

    `    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />`

    Replace with (per CONTEXT D-04 — keep the same 4-space indent, same attribute order):

    `    <link rel="icon" type="image/svg+xml" href="%BASE_URL%favicon.svg" />`

    `%BASE_URL%` is Vite's documented HTML substitution; it expands to the configured `base` value (`/hrv/` per `vite.config.ts:7`) during the index.html build transform. No plugin, no codegen, no `vite.config.ts` edit required. Do NOT touch any other line of `index.html` (lines 1–4, 6–13 are unchanged). Do NOT change the favicon `<link>` to relative `./favicon.svg` — CONTEXT D-04 explicitly chose `%BASE_URL%` over `./` because it survives any future `base` change without an HTML edit.

    Production-build smoke check (in `<verify>`): run `npm run build`, then grep `dist/index.html` for the resolved `/hrv/favicon.svg` href (substituted from `%BASE_URL%`). Confirm `dist/favicon.svg` was copied verbatim from `public/favicon.svg`.

    Commit message: `feat(12): ASSETS-01 — ship favicon.svg + %BASE_URL% href in index.html` and run the per-commit green-gate before committing per D-15.
  </action>
  <verify>
    <automated>test -f public/favicon.svg && test "$(wc -c < public/favicon.svg)" -ge 100 && test "$(wc -c < public/favicon.svg)" -le 250 && grep -q '<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 32 32"><circle cx="16" cy="16" r="14" fill="#0f766e"/></svg>' public/favicon.svg && ! grep -qE '<script|<foreignObject|onload=|xlink:href|<animate' public/favicon.svg && grep -c 'href="%BASE_URL%favicon.svg"' index.html | grep -q '^1$' && ! grep -q 'href="/favicon.svg"' index.html && npx tsc --noEmit && npm run lint && npm run build && grep -q 'href="/hrv/favicon.svg"' dist/index.html && test -f dist/favicon.svg && diff -q public/favicon.svg dist/favicon.svg && npm test -- --run --reporter=basic 2>&1 | tail -5 | grep -qE 'Test Files.*passed' && echo PASS</automated>
  </verify>
  <done>
    `public/favicon.svg` exists (100–250 bytes), contains exactly the single-line SVG above with `fill="#0f766e"`, has no script-execution surface. `index.html:5` references `%BASE_URL%favicon.svg`; the old `/favicon.svg` href is gone. `npm run build` produces `dist/index.html` with `href="/hrv/favicon.svg"` and `dist/favicon.svg` byte-identical to `public/favicon.svg`. All four green-gate commands exit 0.
  </done>
</task>

<task type="auto">
  <name>Task 4: CONTENT-01 — swap learnContent.ts book.url from amzn.to short URL to Forrest's verbatim canonical amazon.com /dp/B0CCFWP4W8 URL + update the two existing test assertions that lock the old string</name>
  <files>src/content/learnContent.ts, src/content/learnContent.test.ts, src/components/LearnDialog.test.tsx</files>
  <read_first>
    - src/content/learnContent.ts (full file — line 60 is `url: 'https://amzn.to/3RTAVqi',` inside the `book` LearnLink entry; sibling URLs on lines 52, 56, 64, 68 use full-domain canonical URLs as analogs)
    - src/content/learnContent.test.ts lines 52–60 (`book URL is exactly the locked value …` assertion at lines 53–54)
    - src/components/LearnDialog.test.tsx lines 115–125 (`the Book link has target="_blank" … and locked href (D-12)` assertion at line 120)
    - src/components/LearnDialog.tsx line 171 (the "Independent project. Not affiliated with Forrest Knutson." disclaimer — confirm it exists; NOT EDITED per D-06)
    - .planning/phases/12-assets-content-hygiene-cleanup/12-CONTEXT.md (D-05 + D-06 + D-07)
    - .planning/phases/12-assets-content-hygiene-cleanup/12-PATTERNS.md (the `src/content/learnContent.ts:60` section — full canonical URL string + the verbatim disclaimer JSX)
  </read_first>
  <action>
    Step A — `src/content/learnContent.ts:60`. Replace exactly this string (preserve 6-space indent and trailing comma):

    `      url: 'https://amzn.to/3RTAVqi',`

    with (per CONTEXT D-05 — verbatim Forrest YouTube-description URL; do NOT strip the `linkId`/`sr`/`language` query params — they are Forrest's own Amazon Associates tag, not ours):

    `      url: 'https://www.amazon.com/Mastering-Meditation-Eight-Steps-Beginner-ebook/dp/B0CCFWP4W8?sr=8-1&linkId=1a5a2958fc89bdb6769b54d0bc9a4d17&language=en_US',`

    Step B — `src/content/learnContent.test.ts` lines 53–54. The current assertion is:

    `  it('book URL is exactly the locked value https://amzn.to/3RTAVqi (D-12 item 3)', () => {`
    `    expect(LEARN_CONTENT.links.book.url).toBe('https://amzn.to/3RTAVqi')`

    Update BOTH the `it()` description string AND the `expect().toBe()` argument to the new canonical URL. The new `it()` description should read `book URL is the canonical amazon.com /dp/B0CCFWP4W8 URL (CONTEXT-01 D-05)` — replace `(D-12 item 3)` with `(CONTEXT-01 D-05)` so the traceability ID matches Phase 12 instead of Phase 6 D-12. The `expect().toBe()` argument becomes the full canonical URL above. Do NOT add a new test; the existing locked-URL test is the right home for the swap (D-07 said no new geography, and the existing test moves with the source string verbatim).

    Step C — `src/components/LearnDialog.test.tsx:120`. The current assertion is:

    `    expect(link).toHaveAttribute('href', 'https://amzn.to/3RTAVqi')`

    Update the second argument to the new canonical URL. The surrounding `it(...)` description and the `target`/`rel` assertions on lines 117–119 are NOT changed. Do NOT touch the disclaimer-related tests in this file (the disclaimer is NOT edited per D-06; tests that assert on `Independent project. Not affiliated with Forrest Knutson.` stay green automatically).

    Step D — `src/components/LearnDialog.tsx:171` is NOT EDITED. Per CONTEXT D-06: the disclaimer remains accurate because (a) the `linkId` is Forrest's Amazon Associates tag, not ours, (b) the disclaimer scope is "us-vs-Forrest" affiliation, not "us-vs-Amazon" link routing, and (c) we have zero financial relationship with either party.

    OUT OF SCOPE: `README.md:37` also references the old `amzn.to/3RTAVqi` URL. README.md is project-level documentation, not a runtime artifact and not in this plan's `files_modified`. CONTEXT.md does not list README.md under "Source under edit". Leave README.md unchanged in Phase 12; it can be updated as a docs-housekeeping pass in a future quick task if desired.

    Commit message: `feat(12): CONTENT-01 — book.url → canonical amazon.com /dp/B0CCFWP4W8 (+ test assertion sync)` and run the per-commit green-gate before committing per D-15.
  </action>
  <verify>
    <automated>grep -c "url: 'https://www.amazon.com/Mastering-Meditation-Eight-Steps-Beginner-ebook/dp/B0CCFWP4W8?sr=8-1&linkId=1a5a2958fc89bdb6769b54d0bc9a4d17&language=en_US'" src/content/learnContent.ts | grep -q '^1$' && ! grep -rqE 'amzn\.to/3RTAVqi' src/ && grep -c "expect(LEARN_CONTENT.links.book.url).toBe('https://www.amazon.com/Mastering-Meditation-Eight-Steps-Beginner-ebook/dp/B0CCFWP4W8?sr=8-1&linkId=1a5a2958fc89bdb6769b54d0bc9a4d17&language=en_US')" src/content/learnContent.test.ts | grep -q '^1$' && grep -c "expect(link).toHaveAttribute('href', 'https://www.amazon.com/Mastering-Meditation-Eight-Steps-Beginner-ebook/dp/B0CCFWP4W8?sr=8-1&linkId=1a5a2958fc89bdb6769b54d0bc9a4d17&language=en_US')" src/components/LearnDialog.test.tsx | grep -q '^1$' && grep -c "Independent project. Not affiliated with Forrest Knutson." src/components/LearnDialog.tsx | grep -q '^1$' && npx tsc --noEmit && npm run lint && npm run build && npm test -- --run --reporter=basic 2>&1 | tail -5 | grep -qE 'Test Files.*passed' && echo PASS</automated>
  </verify>
  <done>
    `src/content/learnContent.ts:60` is the new canonical URL. `amzn.to/3RTAVqi` no longer appears anywhere under `src/` (verified by recursive grep). `src/content/learnContent.test.ts` and `src/components/LearnDialog.test.tsx` both assert on the new canonical URL. `src/components/LearnDialog.tsx` still contains the unchanged disclaimer. All four green-gate commands exit 0.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 5: HYGIENE-02 — relocate isValidBpm/isValidRatio/isValidDuration from storage/settings.ts to domain/settings.ts as exports + rewrite validateSettings to call them (preserve throw class + message verbatim) + delete storage-side locals + prune unused imports + new src/domain/settings.test.ts (6–9 cases as structural gap-fill)</name>
  <files>src/domain/settings.ts, src/domain/settings.test.ts, src/storage/settings.ts</files>
  <read_first>
    - src/domain/settings.ts (full file — 64 lines; lines 50–64 are the `validateSettings` body to rewrite; lines 10–42 hold `BPM_OPTIONS`/`RATIO_OPTIONS`/`DURATION_OPTIONS` already in this file; types `RatioLabel`/`DurationOption` on lines 1–2)
    - src/storage/settings.ts (full file — 67 lines; lines 8–16 are the import block to extend + prune; lines 20–33 are the three predicates to DELETE; lines 35–44 are the `coerceSettings` body that remains UNCHANGED — keep byte-identical fallback behaviour)
    - src/storage/settings.test.ts (existing — verify it still passes with no source-side change; do NOT edit; planner confirmed predicates were file-private — `function` not `export function` — so no external importers break)
    - .planning/phases/12-assets-content-hygiene-cleanup/12-CONTEXT.md (D-08 + D-09 + D-10)
    - .planning/phases/12-assets-content-hygiene-cleanup/12-PATTERNS.md (lines ~23–96 — the verbatim predicate bodies + the validateSettings rewrite shape + the new-test-file pattern)
  </read_first>
  <behavior>
    The relocation MUST be observably behaviour-preserving:
    - `validateSettings({ bpm: 5.5, ratio: '40:60', durationMinutes: 10 })` → returns `{ bpm: 5.5, ratio: '40:60', durationMinutes: 10 }` (existing happy-path callers unchanged).
    - `validateSettings({ bpm: 99, ratio: '40:60', durationMinutes: 10 })` → throws `RangeError` with message exactly `Unsupported BPM: 99` (caller catch sites in `loadSettings`/`saveSettings`/UI handlers unchanged).
    - Parallel for ratio: `validateSettings({ ... ratio: '99:1' as RatioLabel ... })` → throws `RangeError("Unsupported ratio: 99:1")`. Parallel for duration: throws `RangeError("Unsupported duration: 99")`.
    - `coerceSettings({ bpm: 99 })` → returns `{ bpm: 5.5, ratio: '40:60', durationMinutes: 10 }` (DEFAULT_SETTINGS fallback). `coerceSettings({ bpm: 5.5, ratio: '40:60', durationMinutes: 10 })` → returns the input verbatim. (Existing `src/storage/settings.test.ts` cases lock this.)
    - The new `src/domain/settings.test.ts` MUST cover (TDD-style — write tests first, then verify they pass against the relocated predicates):
      * Test G1 — `isValidBpm`: returns true for `5.5`; false for `0`, `7.5`, `'5'`, `null`, `NaN`, `Infinity`.
      * Test G2 — `isValidRatio`: returns true for `'40:60'`; false for `'40-60'`, `''`, `60`.
      * Test G3 — `isValidDuration`: returns true for `10` and `'open-ended'`; false for `7`, `'forever'`, `null`.
      * Group these into three `describe('isValid<X> (HYGIENE-02 D-08)', () => { … })` blocks, 6–9 `it()` cases total (the PATTERNS.md skeleton at lines 168–222 is the recommended exact shape — minor case-grouping flexibility allowed as long as ≥ 6 cases and ≤ 9 cases).
  </behavior>
  <action>
    Step A — TDD: write `src/domain/settings.test.ts` FIRST as a new file (structural gap-fill — `src/domain/settings.ts` has no existing test file; per CONTEXT D-10 + Phase 10 D-20 + Phase 11 D-16 exception, a new test file is correct here). At this step the test imports `isValidBpm`/`isValidRatio`/`isValidDuration` from `'./settings'`, which do not yet exist as exports — the file will fail to type-check / fail tests. Use the PATTERNS.md skeleton (12-PATTERNS.md lines 156–222) as the starting structure: three `describe` blocks, 6–9 `it` cases total, tagged `(HYGIENE-02 D-08)`. Imports stanza is exactly:

    `import { describe, expect, it } from 'vitest'`
    blank line
    `import { isValidBpm, isValidRatio, isValidDuration } from './settings'`

    Step B — `src/domain/settings.ts`: add the three predicates as new `export function` declarations BETWEEN `DEFAULT_SETTINGS` (line 44–48) and `validateSettings` (line 50). Bodies are VERBATIM the file-private declarations from `src/storage/settings.ts:20–33` with `function` → `export function`. No imports change in `src/domain/settings.ts` — `BPM_OPTIONS`/`RATIO_OPTIONS`/`DURATION_OPTIONS` and the types are already in-file.

    Step C — `src/domain/settings.ts:50–64` rewrite `validateSettings`. Replace each inline `.includes()` check with a `!isValid<X>(settings.<field>)` call. The throw class (`RangeError`), the message templates (`` `Unsupported BPM: ${String(settings.bpm)}` `` and parallel `Unsupported ratio: ${settings.ratio}` and `Unsupported duration: ${String(settings.durationMinutes)}`), and the return shape (`{ ...settings }`) MUST be byte-for-byte identical to today. Use the PATTERNS.md skeleton (lines 77–93) for the exact target form.

    Step D — `src/storage/settings.ts`:
    1. Edit the import block at lines 8–16. After the relocation, the only names this file still uses from `'../domain/settings'` are `DEFAULT_SETTINGS`, `SessionSettings` (type), plus the three new predicates `isValidBpm`/`isValidRatio`/`isValidDuration`. PRUNE `BPM_OPTIONS`, `RATIO_OPTIONS`, `DURATION_OPTIONS`, `RatioLabel`, `DurationOption` — these are no longer referenced in `src/storage/settings.ts` after Step E below. Final import block (planner-final shape):
    ```
    import {
      DEFAULT_SETTINGS,
      isValidBpm,
      isValidRatio,
      isValidDuration,
      type SessionSettings,
    } from '../domain/settings'
    ```
    Lint will catch any prune miss — if it complains about an unused name, remove it. If it complains about a still-needed name being missing, restore it (this should NOT happen).
    2. DELETE lines 20–33 (the three `function isValid<X>(v: unknown): v is <T>` declarations). The blank lines before/after may collapse — any consistent whitespace is fine; lint will normalise.
    3. `coerceSettings` body (currently lines 35–44, becomes lines ~21–30 after deletion) is UNCHANGED. The bare-identifier calls `isValidBpm(r.bpm)` etc. now resolve to the imported names. `coerceMute` (line 46), `loadSettings` (line 50), `saveSettings` (line 54), `loadMute` (line 59), `saveMute` (line 63) are all UNCHANGED. The leading file comments at lines 1–6 are UNCHANGED.

    Step E — verification loop: `npm test` now passes the new `src/domain/settings.test.ts` cases (the predicates exist as exports at the path the test imports from) AND the existing `src/storage/settings.test.ts` cases (storage-side coercer behaviour unchanged — same allowlists, same fallback policy). Total test count goes from 400 to ~406–409.

    Step F — final green-gate per CONTEXT D-15: `npx tsc --noEmit && npm run lint && npm run build && npm test`. If lint complains about an unused import in `src/storage/settings.ts`, that is the prune-miss case from Step D.1 — fix and re-run.

    Commit message: `refactor(12): HYGIENE-02 — relocate isValid<X> predicates to domain/settings + new domain test`. Single commit covers Steps A–E (the new test file lands together with the predicates so the suite is green at the commit boundary — TDD red-step is internal to this task and not committed independently per CONTEXT D-15's "every commit boundary green" rule).

    OUT OF SCOPE: no schema changes to `BPM_OPTIONS`/`RATIO_OPTIONS`/`DURATION_OPTIONS`; no new predicate variants; no changes to `coerceMute`, `loadSettings`, `saveSettings`, or the `validateSettings` throw policy (no new `Error` subclass, no new message format). Per CONTEXT D-14: this task touches zero hook code, so no new `react-hooks/*` ESLint disables are introduced.
  </action>
  <verify>
    <automated>grep -c '^export function isValidBpm(v: unknown): v is number' src/domain/settings.ts | grep -q '^1$' && grep -c '^export function isValidRatio(v: unknown): v is RatioLabel' src/domain/settings.ts | grep -q '^1$' && grep -c '^export function isValidDuration(v: unknown): v is DurationOption' src/domain/settings.ts | grep -q '^1$' && grep -c 'if (!isValidBpm(settings.bpm))' src/domain/settings.ts | grep -q '^1$' && grep -c 'if (!isValidRatio(settings.ratio))' src/domain/settings.ts | grep -q '^1$' && grep -c 'if (!isValidDuration(settings.durationMinutes))' src/domain/settings.ts | grep -q '^1$' && grep -c 'throw new RangeError(`Unsupported BPM: ${String(settings.bpm)}`)' src/domain/settings.ts | grep -q '^1$' && grep -c 'throw new RangeError(`Unsupported ratio: ${settings.ratio}`)' src/domain/settings.ts | grep -q '^1$' && grep -c 'throw new RangeError(`Unsupported duration: ${String(settings.durationMinutes)}`)' src/domain/settings.ts | grep -q '^1$' && ! grep -E '^function isValid(Bpm|Ratio|Duration)' src/storage/settings.ts && grep -c "isValidBpm," src/storage/settings.ts | grep -q '^1$' && grep -c "isValidRatio," src/storage/settings.ts | grep -q '^1$' && grep -c "isValidDuration," src/storage/settings.ts | grep -q '^1$' && ! grep -E '^\s*BPM_OPTIONS,' src/storage/settings.ts && ! grep -E '^\s*RATIO_OPTIONS,' src/storage/settings.ts && ! grep -E '^\s*DURATION_OPTIONS,' src/storage/settings.ts && test -f src/domain/settings.test.ts && grep -c "from './settings'" src/domain/settings.test.ts | grep -q '^1$' && TOTAL_IT=$(grep -cE "^\s*it\(" src/domain/settings.test.ts) && test "$TOTAL_IT" -ge 6 && test "$TOTAL_IT" -le 9 && grep -cE "describe\('isValidBpm \(HYGIENE-02 D-08\)" src/domain/settings.test.ts | grep -q '^1$' && grep -cE "describe\('isValidRatio \(HYGIENE-02 D-08\)" src/domain/settings.test.ts | grep -q '^1$' && grep -cE "describe\('isValidDuration \(HYGIENE-02 D-08\)" src/domain/settings.test.ts | grep -q '^1$' && npx tsc --noEmit && npm run lint && npm run build && npm test -- --run --reporter=basic 2>&1 | tail -5 | grep -qE 'Test Files.*passed' && echo PASS</automated>
  </verify>
  <done>
    `src/domain/settings.ts` exports `isValidBpm`/`isValidRatio`/`isValidDuration`; `validateSettings` body calls them with the verbatim `RangeError` throw and message format. `src/storage/settings.ts` no longer defines the predicates locally and imports them by name; unused `BPM_OPTIONS`/`RATIO_OPTIONS`/`DURATION_OPTIONS`/`RatioLabel`/`DurationOption` imports pruned. `src/domain/settings.test.ts` exists with 6–9 `it()` cases across the three `describe('isValid<X> (HYGIENE-02 D-08)', …)` blocks. All four green-gate commands exit 0; total Vitest count is in the 406–409 range (baseline 400 + 6–9 new cases).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user-agent ↔ Vite static asset server (dev + production `dist/`) | Favicon SVG bytes cross from disk to browser; rendered inside `<link rel="icon">` (favicon context, NOT inline `<img src="data:…">` or inline `<svg>`). |
| LearnDialog `<a>` → outbound `amazon.com` URL | Outbound link from in-app render to a third-party domain; new-tab navigation. |
| Storage envelope → `coerceSettings`/`validateSettings` predicates | Untrusted JSON (`unknown` values from localStorage / cross-tab `StorageEvent`) crosses into the domain layer via `(v: unknown)` predicates. |
| `.planning/REQUIREMENTS.md` + `REVIEW.md` docs edits | No runtime trust boundary — docs-only artifacts read by humans and the GSD planner; no code execution. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-12-01 | Tampering / XSS (S+T) | `public/favicon.svg` (NEW asset, ASSETS-01) | low | mitigate | SVG body restricted to a single `<svg>` root + a single `<circle>` element. NO `<script>`, `<foreignObject>`, `onload=`/`onclick=` attributes, `xlink:href`/`href` to external resources, `<image>` tag, `<animate>`/`<set>` tags, gradients, or filters. Loaded as `<link rel="icon">` (favicon context — browsers do NOT parse this resource as a document; even if hostile content were present it would not execute). Verified by negative grep in Task 3's `<verify>` (`! grep -qE '<script\|<foreignObject\|onload=\|xlink:href\|<animate' public/favicon.svg`). |
| T-12-02 | Information disclosure / open-redirect (I+E) | `src/content/learnContent.ts` book link → outbound `amazon.com` URL (CONTENT-01) | low | mitigate | URL is a hardcoded constant in source — not user-controlled. `LearnDialog.tsx` renders the link with `target="_blank"` and `rel="noopener noreferrer"` (already locked by the existing assertion at `src/components/LearnDialog.test.tsx:117–119` — unchanged by Task 4). Destination domain is `amazon.com` (canonical product page) — the previous `amzn.to` short URL resolved to the same destination per CONTEXT D-05; Task 4 makes the hover-preview honest without changing the destination or routing tag (the `linkId` is Forrest's Amazon Associates tag, unchanged). No open-redirect surface (no dynamic URL construction). |
| T-12-03 | Tampering / type-confusion (T) | `isValidBpm`/`isValidRatio`/`isValidDuration` at the storage→domain boundary (HYGIENE-02) | low | accept | Predicates use `Number.isFinite` + readonly-allowlist `.includes()` paths only. No property access by user-controlled key (no `obj[userKey]` lookup); no `eval`/`Function`/dynamic require. The relocation is byte-for-byte VERBATIM from the existing `src/storage/settings.ts:20–33` shape — no mitigation delta needed; Phase 12 introduces no new attack surface here, only a module move. `coerceSettings` already gates on `(raw !== null && typeof raw === 'object' && !Array.isArray(raw))` before any property read; that gate is preserved unchanged. |
| T-12-04 | N/A | HYGIENE-01 (REQUIREMENTS.md row flip), HYGIENE-03 (JSDoc), `index.html` `%BASE_URL%` swap | info | accept | Docs-only / build-time-substitution-only changes. No runtime attack surface. `%BASE_URL%` is a documented Vite HTML transform (no plugin, no codegen); resolves to the static string `/hrv/` at build time per `vite.config.ts:7`. Not user-controlled, not network-derived. |

**Block-on-high gate:** zero `high` severity threats in Phase 12 (per CONTEXT.md `<threat_model_requirement>` — phase is expected to enumerate `low`/`info` only). Plan proceeds.
</threat_model>

<verification>
The plan-level verification (running as one pass after Task 5 commits) confirms every must-have truth holds simultaneously:

1. **HYGIENE-01 docs-only flip** — `grep -c '| HYGIENE-01 .* | Overtaken (by Phase 9 AUDIO-02) |' .planning/REQUIREMENTS.md` returns 1; `grep -c '\[2026-05-12 update\] Overtaken by Phase 9 AUDIO-02' REVIEW.md` returns 1; `audio.audioNow` is STILL exported from `src/hooks/useAudioCues.ts:58, :390` (confirms the SC#3 carve-out: docs flip, not literal removal; per D-01/D-02).
2. **HYGIENE-03 JSDoc** — `grep -c '@param now Test-only seam' src/storage/format.ts` returns 1; `src/storage/format.test.ts` still green.
3. **ASSETS-01** — `test -f public/favicon.svg` true; `grep -q '%BASE_URL%favicon.svg' index.html`; `npm run build` then `grep -q 'href="/hrv/favicon.svg"' dist/index.html` and `test -f dist/favicon.svg`. SC #1 satisfied.
4. **CONTENT-01** — `! grep -rqE 'amzn\.to/3RTAVqi' src/`; `grep -q '/dp/B0CCFWP4W8' src/content/learnContent.ts`; disclaimer at `src/components/LearnDialog.tsx:171` unchanged. SC #2 satisfied via the canonical-URL path (D-05); disclaimer unchanged is correct (D-06).
5. **HYGIENE-02** — three new exports in `src/domain/settings.ts`; predicate-call rewrite of `validateSettings` preserves `RangeError` + message format byte-identically; locals deleted from `src/storage/settings.ts` and unused imports pruned; new `src/domain/settings.test.ts` with 6–9 cases; existing `src/storage/settings.test.ts` still green. SC #4 satisfied.
6. **HYGIENE-03 JSDoc test-only seam** — already covered above. SC #5 satisfied.
7. **Per-commit green-gate (D-15)** — every commit boundary passed `tsc --noEmit && npm run lint && npm run build && npm test`. No new `react-hooks/*` disables (D-14 — Phase 12 touches zero hook code).

Cross-phase invariant: `audio.audioNow` STILL exists (validates D-01 — Phase 9 AUDIO-02 caller-side clamp at `App.tsx:200, :342, :546` continues to function); the milestone audit reading HYGIENE-01 should resolve via the `Overtaken (by Phase 9 AUDIO-02)` cell + the REVIEW.md `[2026-05-12 update]` addendum, NOT via a code-removal step.
</verification>

<success_criteria>
ROADMAP Phase 12 Success Criteria (with CONTEXT.md D-01/D-02 carve-out for SC #3 — docs-only flip, NOT literal code removal):

1. **(ASSETS-01)** `dist/index.html` after `npm run build` shows `href="/hrv/favicon.svg"`; `dist/favicon.svg` is the byte-identical copy of `public/favicon.svg`; the source `index.html:5` uses `%BASE_URL%favicon.svg`. Closes the `/favicon.svg` 404 todo (`.planning/todos/pending/2026-05-11-missing-favicon-404-in-console.md` can be moved to `completed/` after verify-phase per CONTEXT.md "Reviewed Todos / folded").
2. **(CONTENT-01)** `src/content/learnContent.ts:60` is the canonical `amazon.com/.../dp/B0CCFWP4W8?…` URL (Forrest's verbatim YouTube-description URL — `linkId` preserved per D-05); the LearnDialog disclaimer at `src/components/LearnDialog.tsx:171` remains "Independent project. Not affiliated with Forrest Knutson." (D-06 carve-out — `linkId` is Forrest's tag, not ours); existing test assertions at `src/content/learnContent.test.ts:54` and `src/components/LearnDialog.test.tsx:120` updated to the new URL.
3. **(HYGIENE-01 — DOCS-ONLY FLIP per D-01/D-02)** ROADMAP wording "`useAudioCues` no longer returns `audioNow`; the `UseAudioCues` interface and return tuple are pruned" is SATISFIED HERE via the `.planning/REQUIREMENTS.md` HYGIENE-01 row flip to `Overtaken (by Phase 9 AUDIO-02)` plus the `REVIEW.md` §IN-02 `[2026-05-12 update]` addendum. CONTEXT D-01 establishes that Phase 9 AUDIO-02 (shipped 2026-05-11) introduced a caller-side clamp at `src/app/App.tsx:200, :342, :546` that DEPENDS on `audio.audioNow()`. Literal removal would break the AUDIO-02 contract — risk/reward inverted for a docs-only finding. The cross-phase reality (later than ROADMAP's draft) is recorded in two artifacts; the milestone audit reads them as closure.
4. **(HYGIENE-02)** `isValidBpm`/`isValidRatio`/`isValidDuration` exported from `src/domain/settings.ts`; consumed by both `validateSettings` (throwing, verbatim `RangeError` + message format) and `coerceSettings` (silent-fallback, unchanged); duplicated allow-list logic in `src/storage/settings.ts` deleted; new `src/domain/settings.test.ts` with 6–9 cases as structural gap-fill (D-10).
5. **(HYGIENE-03)** Single-line JSDoc above `formatLastSessionDate` at `src/storage/format.ts:42` documents the `now: () => number = Date.now` parameter as test-only; full Vitest suite passes (target ~406–409 cases after Task 5's additions).

**Milestone invariant (PROJECT.md):** zero net user-facing behaviour change beyond (a) the favicon stops 404'ing in the console (ROADMAP SC#1's intended outcome) and (b) the LearnDialog book-link hover-preview becomes honest text (`amzn.to/3RTAVqi` → `amazon.com/.../dp/B0CCFWP4W8?…` — same destination, same routing tag). 363/363 → 400/400 → 406–409/406–409 Vitest progression preserved with every commit green.

**Milestone closeout posture (D-17):** Phase 12 is the LAST phase of v1.0.1. After `/gsd-verify-phase 12` + any UAT, `/gsd-complete-milestone` handles archival and v1.1 onboarding. This plan does NOT pre-empt that flow (no milestone-archival edits, no v1.1 placeholder phases).
</success_criteria>

<output>
After completion, create `.planning/phases/12-assets-content-hygiene-cleanup/12-01-SUMMARY.md` summarising the 5 commits (one per task group, in CONTEXT D-13 risk-ascending order), the test-count delta (400 → ~406–409), and the HYGIENE-01 docs-only-flip rationale (cross-link to CONTEXT D-01/D-02 and to REVIEW.md §IN-02 `[2026-05-12 update]` addendum).
</output>
