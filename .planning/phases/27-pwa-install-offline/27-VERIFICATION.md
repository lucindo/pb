---
phase: 27-pwa-install-offline
verified: 2026-05-16T03:10:00Z
status: passed
score: 5/5 must-haves verified
overrides_applied: 0
re_verification:
  previous_status: human_needed
  previous_score: 4/5
  gaps_closed:
    - "Success Criterion 4 — iOS UAT device version captured (iOS 18.7.9 >= 18.4); Wake Lock confirmed via normal path; all six iOS scenarios PASS"
  gaps_remaining: []
  regressions: []
---

# Phase 27: PWA Install & Offline Verification Report

**Phase Goal:** The HRV breathing app is an installable, offline-capable PWA — a Web App Manifest makes it addable to home screen/desktop, a service worker precaches the app shell so a started session runs fully offline, and updates roll out without serving a stale shell or interrupting a running session.

**Verified:** 2026-05-16T03:10:00Z
**Status:** passed
**Re-verification:** Yes — after gap closure (iOS 18.7.9 device version recorded, Success Criterion 4 confirmed)

---

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | A user can install the app to their home screen/desktop — Web App Manifest has correct start_url/scope for the /hrv/ base path, maskable icons, and an Apple touch icon | VERIFIED | `dist/manifest.webmanifest` confirmed: `name=HRV Breathing`, `scope=/hrv/`, `start_url=/hrv/`, four icon entries (two `any`, two `maskable`) all with relative srcs. `apple-touch-icon.png` (180x180) exists in `public/` and is linked in `index.html` via `%BASE_URL%apple-touch-icon.png`. |
| 2 | A user can run a started breathing session fully offline — a service worker precaches the app shell and all static assets | VERIFIED | `dist/sw.js` generated by Workbox in `generateSW` mode. Precache manifest contains 17 content-hashed entries: `index.html`, `registerSW.js`, `favicon.svg`, `apple-touch-icon.png`, all four icon PNGs, both maskable PNGs, `manifest.webmanifest`, CSS bundle, and JS bundle. `workbox.globPatterns: ['**/*.{js,wasm,css,html,png,svg}']` covers the app shell. |
| 3 | The installed app updates to the latest deployed version without serving a stale app shell, and an update never interrupts a running session | VERIFIED | `registerType: 'autoUpdate'` + `cleanupOutdatedCaches: true` confirmed in `vite.config.ts`. `dist/sw.js` contains `cleanupOutdatedCaches`. `dist/registerSW.js` contains zero instances of `location.reload()`. No `virtual:pwa-register` import anywhere in `src/` (grep count = 0). |
| 4 | Real-device iOS standalone-mode UAT confirms install, offline session, audio, and Wake Lock behavior — named deliverable; iOS < 18.4 Wake Lock limitation documented per WebKit bug 254545 | VERIFIED | `27-03-iOS-UAT.md` records all six iOS-1..iOS-6 scenarios as PASS with operator sign-off on 2026-05-16. Test device: iPhone (notch-class), **iOS 18.7.9**. Since 18.7.9 >= 18.4, Wake Lock is available in standalone PWA mode — the WebKit bug 254545 limitation (screen dims on iOS < 18.4) does not apply to this device; iOS-3 passed on the normal Wake Lock path. Device version is explicitly captured; the gap from the initial verification is closed. |
| 5 | The zero-net-new-runtime-deps invariant holds: dependencies stays react + react-dom; vite-plugin-pwa enters as a build-time dependency only | VERIFIED | `package.json` `dependencies` contains exactly `react: ^19.2.5` and `react-dom: ^19.2.5`. `vite-plugin-pwa: ^1.3.0` is in `devDependencies`. Confirmed by direct inspection of package.json. |

**Score:** 5/5 truths verified

---

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `package.json` | vite-plugin-pwa in devDependencies; dependencies unchanged | VERIFIED | `"vite-plugin-pwa": "^1.3.0"` in devDependencies; dependencies block contains only `react` and `react-dom` |
| `public/pwa-192x192.png` | Standard PWA icon, 192x192 PNG | VERIFIED | Exists, PNG format, 192x192 |
| `public/pwa-512x512.png` | Standard PWA icon, 512x512 PNG | VERIFIED | Exists, PNG format, 512x512 |
| `public/pwa-maskable-192x192.png` | Maskable PWA icon, 192x192 PNG | VERIFIED | Exists, PNG format, 192x192 |
| `public/pwa-maskable-512x512.png` | Maskable PWA icon, 512x512 PNG | VERIFIED | Exists, PNG format, 512x512 |
| `public/apple-touch-icon.png` | iOS install icon, 180x180 PNG | VERIFIED | Exists, PNG format, 180x180 |
| `vite.config.ts` | VitePWA() plugin with generateSW + autoUpdate + manifest + workbox config | VERIFIED | All required fields present: `strategies: 'generateSW'`, `registerType: 'autoUpdate'`, `injectRegister: 'auto'`, manifest with `HRV Breathing` name, four icon entries with relative srcs (no leading `/`), maskable icons, `cleanupOutdatedCaches: true` |
| `index.html` | Apple-touch-icon link + iOS standalone meta tags, no hand-added manifest link | VERIFIED | All five tags present: `apple-touch-icon`, `apple-mobile-web-app-capable`, `apple-mobile-web-app-title`, `apple-mobile-web-app-status-bar-style`, `theme-color`. Uses `%BASE_URL%apple-touch-icon.png`. Source has zero `rel="manifest"` hand-authored links. |
| `dist/manifest.webmanifest` | Web App Manifest: name=HRV Breathing, scope=/hrv/, start_url=/hrv/, maskable icons | VERIFIED | All fields confirmed: name, scope, start_url, display=standalone, theme_color=#5e81ac, background_color=#eceff4, four icon entries (two maskable), all icon srcs relative |
| `dist/sw.js` | Workbox-generated service worker with cleanupOutdatedCaches | VERIFIED | Exists, contains `cleanupOutdatedCaches`, 17 content-hashed precache entries |
| `dist/registerSW.js` | Plugin-injected SW registration, no location.reload() | VERIFIED | Exists, zero `location.reload()` occurrences |
| `README.md` | iOS < 18.4 standalone Wake Lock limitation documented (Wake Lock, 18.4, WebKit bug 254545) | VERIFIED | "Known Limitations" section present with exact text mentioning WebKit bug 254545, iOS 18.4 threshold, screen may dim, progressive enhancement framing. No runtime detection or warning UI described. |
| `.planning/phases/27-pwa-install-offline/27-03-iOS-UAT.md` | Six iOS-1..iOS-6 scenarios with PASS/FAIL results; device iOS version captured | VERIFIED | All six scenarios recorded as PASS with operator sign-off. Test device iOS version recorded as **18.7.9** (>= 18.4). iOS-3 Wake Lock scenario passed on normal path; WebKit bug 254545 < 18.4 limitation explicitly noted as not applicable to this device. |

---

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `vite.config.ts VitePWA()` | `dist/manifest.webmanifest + dist/sw.js` | `vite build` runs the plugin | WIRED | Build confirmed: `dist/manifest.webmanifest` (0.57 kB) and `dist/sw.js` emitted; build output shows `PWA v1.3.0 mode generateSW precache 17 entries` |
| `index.html` | `public/apple-touch-icon.png` | `<link rel=apple-touch-icon href=%BASE_URL%apple-touch-icon.png>` | WIRED | `apple-touch-icon.png` exists in `public/`; `dist/index.html` contains `/hrv/apple-touch-icon.png` (resolved `%BASE_URL%`) |
| `27-03-iOS-UAT.md` | ROADMAP Phase 27 Success Criterion 4 | Named real-device UAT deliverable with iOS-1..iOS-6 | WIRED | All six scenarios PASS; device iOS version 18.7.9 captured; Wake Lock behavior confirmed for >= 18.4 path |

---

### Data-Flow Trace (Level 4)

Not applicable — this phase produces build-time configuration, static assets, and documentation artifacts. No dynamic data rendering components were created. The PWA layer is explicitly invisible to users (D-08: zero net-new UI).

---

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| `npm run build` produces manifest, sw.js, registerSW.js | `npm run build` | Exit 0; `dist/manifest.webmanifest`, `dist/sw.js`, `dist/registerSW.js` all present | PASS |
| manifest.webmanifest has /hrv/-scoped start_url and scope | `node -e` JSON parse of dist/manifest.webmanifest | `scope=/hrv/`, `start_url=/hrv/` | PASS |
| manifest icons are all relative (no leading `/`) | `node -e` check of icons array | 0 absolute icon srcs | PASS |
| manifest has maskable icon entries | `node -e` check | `has maskable icon: true` | PASS |
| sw.js contains cleanupOutdatedCaches | `grep` dist/sw.js | 1 match | PASS |
| registerSW.js has no location.reload() | `grep` dist/registerSW.js | 0 matches | PASS |
| No virtual:pwa-register in src/ | `grep -r` src/ | 0 matches | PASS |
| 959/959 test suite passes | `npx vitest run` | 65 files, 959 tests passed | PASS |
| README contains Wake Lock, 18.4, 254545 | `node -e` regex checks | All three present | PASS |

---

### Probe Execution

No conventional `scripts/*/tests/probe-*.sh` probes declared or present for this phase. The build verification above serves as the equivalent functional check.

---

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|------------|-------------|--------|----------|
| PWA-01 | 27-01-PLAN, 27-02-PLAN, 27-03-PLAN | User can install the app — Web App Manifest with correct start_url/scope, maskable icons, Apple touch icon | SATISFIED | Manifest emitted with `/hrv/` scope, four icons (two maskable), apple-touch-icon in public/ and linked in index.html |
| PWA-02 | 27-01-PLAN, 27-02-PLAN, 27-03-PLAN | User can run started session fully offline — service worker precaches app shell and static assets | SATISFIED | Workbox `generateSW` mode produces sw.js precaching 17 content-hashed entries covering all static assets |
| PWA-03 | 27-02-PLAN, 27-03-PLAN | Installed app updates without stale shell; update never interrupts running session | SATISFIED | `autoUpdate` + `cleanupOutdatedCaches`; no `location.reload()` in registerSW.js; no `virtual:pwa-register` import |

No orphaned requirements: PWA-01, PWA-02, PWA-03 are the only requirements mapped to Phase 27 in REQUIREMENTS.md, and all three are claimed by at least one plan.

---

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| `index.html` | 10 | `apple-mobile-web-app-capable` without standards-track `mobile-web-app-capable` | Warning (code review WR-02) | Forward-compatibility gap if a future iOS WebKit drops the Apple-prefixed legacy meta; Android/Chrome installs unaffected (manifest `display: standalone` covers them). Advisory — does not block current functionality. |
| `public/favicon.svg` | 1 | Redesigned two-tone orb-glow diverges from runtime tab favicon in `faviconPalette.ts:22` | Warning (code review WR-01) | Visual inconsistency between home-screen install icon (dark orb-glow) and browser tab favicon (flat single-color circle). Stale sync comment at `faviconPalette.ts:22`. No automated test catches the geometry drift. Advisory — session functionality unaffected. |

No `TBD`, `FIXME`, or `XXX` debt markers found in any of the files modified by this phase (vite.config.ts, index.html, README.md, public/*.png, 27-03-iOS-UAT.md). Code review found 0 critical, 2 warnings, 3 info — all advisory.

---

### Human Verification Required

None. The single pending item from the initial verification (iOS device version not captured) is now closed: `27-03-iOS-UAT.md` records the test device as running **iOS 18.7.9**, which is >= 18.4. Wake Lock is available on this device via the normal path; the WebKit bug 254545 < 18.4 screen-dim limitation does not apply. All six iOS scenarios are recorded as PASS with operator sign-off.

---

### Gaps Summary

No gaps. All five must-haves are verified:

1. Web App Manifest correctly scoped to `/hrv/` with maskable icons and Apple touch icon.
2. Service worker precaches 17 content-hashed entries covering the full app shell.
3. `autoUpdate` + `cleanupOutdatedCaches` + no `location.reload()` ensures non-disruptive updates.
4. Real-device iOS UAT: all six scenarios PASS on iOS 18.7.9; Wake Lock confirmed; device version captured.
5. Zero-net-new-runtime-deps invariant holds: `vite-plugin-pwa` is devDep only.

The two code-review warnings (WR-01 favicon geometry drift, WR-02 missing standards-track meta) are advisory and do not block PWA functionality or phase close.

---

_Verified: 2026-05-16T03:10:00Z_
_Re-verified: 2026-05-16 (gap closure — iOS 18.7.9 device version recorded)_
_Verifier: Claude (gsd-verifier)_
