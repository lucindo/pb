---
phase: 27-pwa-install-offline
plan: 03
type: execute
wave: 3
depends_on: ["27-02"]
files_modified:
  - README.md
  - .planning/phases/27-pwa-install-offline/27-03-iOS-UAT.md
autonomous: false
requirements: [PWA-01, PWA-02, PWA-03]
nyquist_compliant: true

must_haves:
  truths:
    - "The README documents that Wake Lock is unavailable in iOS standalone PWA mode on iOS < 18.4 (WebKit bug 254545)"
    - "A real iOS device confirms the app installs to the home screen via Add to Home Screen"
    - "A real iOS device confirms a started breathing session runs fully offline (airplane mode) with audio and visuals"
    - "A real iOS device confirms an SW update applies on next launch without serving a stale shell or interrupting a running session"
  artifacts:
    - path: "README.md"
      provides: "documented iOS < 18.4 standalone Wake Lock limitation"
      contains: "Wake Lock"
    - path: ".planning/phases/27-pwa-install-offline/27-03-iOS-UAT.md"
      provides: "recorded real-device iOS standalone-mode UAT results (iOS-1..iOS-6)"
  key_links:
    - from: "27-03-iOS-UAT.md"
      to: "ROADMAP Phase 27 Success Criterion 4"
      via: "named real-device UAT deliverable"
      pattern: "iOS-[1-6]"
---

<objective>
Document the iOS < 18.4 standalone Wake Lock limitation in the README, then run and record the named real-device iOS standalone-mode UAT — the first-class Phase 27 deliverable that covers install, offline, audio, update, and Wake Lock behavior that cannot be tested in jsdom.

Purpose: Phase 27 Success Criterion 4 makes the real-device iOS UAT a NAMED deliverable, not a deferred carry-forward (STATE.md `[PWA real-device UAT is first-class]` decision, mirroring the v1.0 audio/wake-lock real-device lesson). PWA-01/02/03 each have runtime behavior — install flow, offline serving, SW update lifecycle — that no automated test can reach (27-VALIDATION.md §Manual-Only Verifications). This plan closes that gap with a structured, recorded operator UAT. The README documentation discharges D-09: the iOS < 18.4 Wake Lock gap is handled as a documented known limitation, with no runtime detection or warning UI.

Output: A README section documenting the Wake Lock limitation; a recorded `27-03-iOS-UAT.md` capturing pass/fail for the six iOS UAT scenarios.
</objective>

<execution_context>
@$HOME/.claude-personal-personal/get-shit-done/workflows/execute-plan.md
@$HOME/.claude-personal-personal/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/27-pwa-install-offline/27-CONTEXT.md
@.planning/phases/27-pwa-install-offline/27-RESEARCH.md
@.planning/phases/27-pwa-install-offline/27-VALIDATION.md
@.planning/phases/27-pwa-install-offline/27-02-SUMMARY.md

<interfaces>
<!-- The six iOS UAT scenarios — verbatim from 27-RESEARCH.md §Real-Device iOS UAT Plan. -->

| #     | Scenario                                                              | Pass Criteria |
|-------|-----------------------------------------------------------------------|---------------|
| iOS-1 | Install via Safari Share → Add to Home Screen                         | App icon appears correctly; opens in standalone mode (no browser chrome) |
| iOS-2 | Launch from Home Screen, disable network, start session              | Session runs; audio cues play; orb animates — fully offline |
| iOS-3 | Run a 2-min session, then put device to sleep                         | Session resumes correctly on wake if iOS ≥ 18.4; screen dims on iOS < 18.4 (documented behavior) |
| iOS-4 | Check app icon on Home Screen                                         | Correct orb icon, not a screenshot thumbnail |
| iOS-5 | Check status bar in standalone mode                                   | Theme color visible; no browser address bar |
| iOS-6 | Verify app name on Home Screen                                        | Shows "HRV Breathing" |

Wake Lock facts (RESEARCH §iOS Standalone PWA Specifics — for the README text):
  - iOS < 16.4: Wake Lock API unavailable in any context.
  - iOS 16.4–18.3: Wake Lock available in Safari browser but NOT in standalone (Home Screen) PWA mode — WebKit bug 254545.
  - iOS ≥ 18.4: Wake Lock works in standalone mode (fix shipped 2025-03-31).
  - D-09: document only — no runtime detection, no warning UI. The app already treats Wake Lock as progressive enhancement.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Document the iOS < 18.4 standalone Wake Lock limitation in README</name>
  <read_first>
    - README.md (current content — find the existing section structure to place the note coherently)
    - .planning/phases/27-pwa-install-offline/27-RESEARCH.md (§iOS Standalone PWA Specifics — iOS Wake Lock subsection)
    - .planning/phases/27-pwa-install-offline/27-CONTEXT.md (§decisions D-09 — document-only handling)
  </read_first>
  <files>README.md</files>
  <action>
    Add a short subsection to `README.md` documenting the iOS standalone-PWA Wake Lock limitation. Place it near the existing install / browser-support content if such a section exists; otherwise add it as a small "Known limitations" subsection. State the concrete facts (see `<interfaces>`): the Screen Wake Lock API does not work in installed (Home Screen) PWA mode on iOS before 18.4 due to WebKit bug 254545, so the screen may dim during a session on those devices; iOS 18.4 and later, and Safari browser tabs on iOS 16.4+, are unaffected. Frame it as a known limitation consistent with the app's existing stance that Wake Lock is a progressive enhancement — the session itself still runs correctly. Do NOT describe any runtime detection or in-app warning (D-09: documentation only). Keep it concise (2-4 sentences) and claim-safe in tone, consistent with the rest of the README.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const r=fs.readFileSync('README.md','utf8'); if(!/Wake Lock/i.test(r)) throw new Error('README missing Wake Lock note'); if(!/18\.4/.test(r)) throw new Error('README missing iOS 18.4 version reference'); if(!/254545/.test(r)) throw new Error('README missing WebKit bug 254545 reference'); console.log('OK README wake-lock note');"</automated>
  </verify>
  <acceptance_criteria>
    - `README.md` contains a subsection mentioning `Wake Lock`, the iOS `18.4` threshold, and WebKit bug `254545`.
    - The text states the screen may dim during a session in standalone PWA mode on iOS < 18.4 and frames Wake Lock as a progressive enhancement.
    - The text does NOT describe any in-app runtime detection or warning UI (D-09 compliance).
    - `tsc -b && eslint . && vite build && vitest run` exits 0 (README change is doc-only; the green gate must remain clean).
  </acceptance_criteria>
  <done>README.md documents the iOS < 18.4 standalone Wake Lock limitation as a known limitation, with no mention of runtime detection or warning UI.</done>
</task>

<task type="auto">
  <name>Task 2: Build a production bundle and scaffold the iOS UAT record</name>
  <read_first>
    - .planning/phases/27-pwa-install-offline/27-RESEARCH.md (§Real-Device iOS UAT Plan — the six iOS-1..iOS-6 scenarios)
    - .planning/phases/27-pwa-install-offline/27-VALIDATION.md (§Manual-Only Verifications)
    - .planning/phases/27-pwa-install-offline/27-02-SUMMARY.md (what the PWA wiring produced)
  </read_first>
  <files>.planning/phases/27-pwa-install-offline/27-03-iOS-UAT.md</files>
  <action>
    Produce a clean production build with `tsc -b && vite build` and confirm the PWA artifacts are present in `dist/` (`sw.js`, `registerSW.js`, `manifest.webmanifest`, and the five icon PNGs) — this is the bundle the operator will deploy/serve for the device UAT.

    Then create `.planning/phases/27-pwa-install-offline/27-03-iOS-UAT.md` as the UAT record. It must contain: a short header naming the build under test and how to serve it (e.g. `vite preview` or the deployed `/hrv/` host over HTTPS — note that a service worker requires HTTPS or localhost); a results table with one row per scenario iOS-1 through iOS-6 (copy the scenario text and pass criteria verbatim from the `<interfaces>` block), each row with empty `Result` and `Notes` columns for the operator to fill in; and an explicit row or note recording the device's iOS version (so the iOS-3 Wake Lock expectation — dims on < 18.4, holds on ≥ 18.4 — can be judged correctly). Leave all result cells unmarked; the next task is the operator checkpoint that fills them.
  </action>
  <verify>
    <automated>tsc -b && vite build && node -e "const fs=require('fs'); for(const f of ['dist/sw.js','dist/registerSW.js','dist/manifest.webmanifest']){ fs.accessSync(f); } const u=fs.readFileSync('.planning/phases/27-pwa-install-offline/27-03-iOS-UAT.md','utf8'); for(const s of ['iOS-1','iOS-2','iOS-3','iOS-4','iOS-5','iOS-6']){ if(!u.includes(s)) throw new Error('UAT record missing scenario '+s); } console.log('OK build + UAT scaffold');"</automated>
  </verify>
  <acceptance_criteria>
    - `tsc -b && vite build` exits 0 and `dist/` contains `sw.js`, `registerSW.js`, and `manifest.webmanifest`.
    - `.planning/phases/27-pwa-install-offline/27-03-iOS-UAT.md` exists and references all six scenarios `iOS-1` through `iOS-6` with their pass criteria.
    - The UAT record has empty result/notes cells and a place to record the test device's iOS version.
    - The record notes that the service worker requires HTTPS (or localhost) to register.
  </acceptance_criteria>
  <done>A production build with PWA artifacts is confirmed in dist/; the iOS UAT record is scaffolded with all six scenarios ready for operator results.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Real-device iOS standalone-mode UAT (Phase 27 Success Criterion 4)</name>
  <what-built>
    A complete installable, offline-capable PWA: a Web App Manifest (`/hrv/`-scoped, maskable icons, Apple touch icon), a Workbox service worker that precaches the app shell, silent `autoUpdate` with `cleanupOutdatedCaches`, and iOS standalone meta tags. The production build in `dist/` is ready to serve over HTTPS for device testing.
  </what-built>
  <how-to-verify>
    Serve the `dist/` build over HTTPS at the `/hrv/` path (deploy to the GitHub Pages host, or use an HTTPS tunnel / `vite preview` over localhost — a service worker will NOT register over plain `http://` on a remote host). On a real iOS device, run the six scenarios and record each result in `.planning/phases/27-pwa-install-offline/27-03-iOS-UAT.md`:

    1. iOS-1 — In Safari, open the app, use Share → Add to Home Screen. Expect: the orb app icon appears; tapping it opens the app in standalone mode with no browser chrome.
    2. iOS-2 — Launch the app from the Home Screen, enable Airplane Mode (network fully off), start a breathing session. Expect: the session runs fully offline — audio cues play and the orb animates.
    3. iOS-3 — Start a ~2-minute session, then let the device sleep / lock. Expect: on iOS ≥ 18.4 the session continues / resumes correctly on wake; on iOS < 18.4 the screen may dim mid-session (this is the documented WebKit bug 254545 limitation — a PASS, not a regression). Record the device's iOS version.
    4. iOS-4 — Check the Home Screen icon. Expect: the correct orb icon, not a screenshot thumbnail.
    5. iOS-5 — In standalone mode, check the status bar. Expect: theme color visible, no browser address bar.
    6. iOS-6 — Check the app name on the Home Screen. Expect: it reads "HRV Breathing".

    Fill in the result and notes columns in `27-03-iOS-UAT.md`, including the tested iOS version. If a real iOS device is genuinely unavailable, record that explicitly in the UAT file and describe the closest substitute attempted (e.g. iOS Simulator) — do not silently skip; the operator decides whether to accept a partial UAT at phase close.
  </how-to-verify>
  <resume-signal>Type "approved" once the six iOS scenarios are recorded in 27-03-iOS-UAT.md, or describe any failures found.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HTTPS host → installed iOS PWA | The service worker only registers over HTTPS/localhost; install + offline behavior is exercised across this boundary |
| documentation → user expectation | The README sets the user's accurate expectation about the iOS < 18.4 Wake Lock gap |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-08 | Information Disclosure | undocumented iOS < 18.4 Wake Lock gap leaving users with a silently dimming screen | mitigate | Task 1 documents the limitation explicitly in the README (D-09); the user is informed rather than surprised. The session itself is unaffected — only the screen-dim behavior differs. |
| T-27-09 | Tampering | service worker registering over insecure transport during UAT | accept | The UAT instructions require HTTPS (or localhost); browsers refuse SW registration over plain remote `http://` by spec. No residual risk — the platform enforces this boundary. |

**Residual risk:** LOW. This plan adds documentation and runs a verification UAT; it introduces no new code paths, no user input, and no network surface beyond the already-static PWA shell.
</threat_model>

<verification>
- Task 1 automated check: README mentions Wake Lock, iOS 18.4, and WebKit bug 254545.
- Task 2 automated check: production build emits the PWA artifacts; the UAT record scaffolds all six scenarios.
- Task 3 (checkpoint): all six iOS-1..iOS-6 scenarios recorded with results in `27-03-iOS-UAT.md`, including the tested iOS version.
- Green gate: `tsc -b && eslint . && vite build && vitest run` exits 0 at every commit.
</verification>

<success_criteria>
- README documents the iOS < 18.4 standalone Wake Lock limitation as a known limitation (D-09).
- A production build with `dist/sw.js`, `dist/registerSW.js`, `dist/manifest.webmanifest`, and the five icon PNGs is confirmed.
- `27-03-iOS-UAT.md` records real-device results for all six iOS scenarios (or an explicit operator-acknowledged substitute) — satisfying Phase 27 Success Criterion 4, the named real-device UAT deliverable.
</success_criteria>

<output>
After completion, create `.planning/phases/27-pwa-install-offline/27-03-SUMMARY.md`
</output>
</invoke>
