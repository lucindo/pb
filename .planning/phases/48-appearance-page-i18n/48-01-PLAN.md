---
phase: 48-appearance-page-i18n
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/content/strings.ts
  - src/content/strings.test.ts
  - src/content/content.no-review-markers.test.ts
  - src/components/SettingsPanelBody.tsx
autonomous: true
requirements:
  - I18N-01
  - I18N-02
  - I18N-03
must_haves:
  truths:
    - "User switching locale to PT-BR sees all new Appearance strings rendered with PT-BR drafts (D-09)."
    - "EN catalog has the renamed `appSettings.sections.theme` key (D-01) and a new top-level `appearance.*` namespace per D-03..D-08."
    - "PT-BR catalog has parallel entries for every new EN key with `// TODO: native-speaker review` markers per D-09 + Phase 26 D-01 workflow."
    - "The LOCKED_COPY byte-equality guard remains green (new strings live outside LOCKED_COPY)."
    - "The drift-guard at `content.no-review-markers.test.ts` accepts the new `appearance.*` markers without flipping green to red."
    - "D-04: Ring cue picker label is `Ring cue` with options `Arc` (`progress-arc`) and `Rings` (`outer-inner`); EN + PT-BR catalogs ship these exact strings."
  artifacts:
    - path: "src/content/strings.ts"
      provides: "UiStrings type extension + EN + PT-BR catalog entries; renamed sections.theme key"
      contains: "appearance:"
    - path: "src/content/strings.test.ts"
      provides: "Per-phase describe block 'Phase 48 appearance.* and theme rename'"
      contains: "Phase 48"
    - path: "src/content/content.no-review-markers.test.ts"
      provides: "Adapted drift-guard that allows markers within the appearance.* namespace per D-18 path (a)"
      contains: "appearance"
    - path: "src/components/SettingsPanelBody.tsx"
      provides: "Updated single consumer of the renamed key"
      contains: "sections.theme"
  key_links:
    - from: "src/content/strings.ts"
      to: "src/components/SettingsPanelBody.tsx"
      via: "strings.appSettings.sections.theme"
      pattern: "strings\\.appSettings\\.sections\\.theme"
    - from: "src/content/strings.ts"
      to: "as const satisfies Readonly<Record<LocaleId, UiStrings>>"
      via: "compile-time type-completeness guard"
      pattern: "satisfies Readonly<Record<LocaleId, UiStrings>>"
---

<objective>
Extend the EN + PT-BR string catalogs with the new `appearance.*` namespace, rename `appSettings.sections.appearance` → `appSettings.sections.theme` (D-01), update the single consumer in `SettingsPanelBody.tsx`, and adapt the `content.no-review-markers.test.ts` drift-guard so the new `// TODO: native-speaker review` markers under `appearance.*` are accepted while non-`appearance` markers still fail (D-18 path a).

Purpose: This is the foundation layer the other Phase 48 plans bind to. Without it, downstream picker labels, page title, section headers, and ARIA labels have no copy source; the type-completeness `satisfies Readonly<Record<LocaleId, UiStrings>>` guarantees every locale ships every key in one atomic edit (RESEARCH §Pitfall 2). The drift-guard adaptation enables the rest of the phase to ship without flipping the existing marker test red.

Output: `UiStrings.appearance` namespace + EN + PT-BR entries (with markers); renamed `sections.theme` key; one-line consumer update; adapted drift-guard with structural allowlist; new `strings.test.ts` describe block.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/48-appearance-page-i18n/48-CONTEXT.md
@.planning/phases/48-appearance-page-i18n/48-RESEARCH.md
@.planning/phases/48-appearance-page-i18n/48-PATTERNS.md
@.planning/phases/48-appearance-page-i18n/48-UI-SPEC.md

<interfaces>
<!-- Concrete keys/values locked by CONTEXT.md D-01, D-03..D-09 — copied verbatim so the executor needs zero interpretation. -->

EN copy (locked verbatim per D-01, D-03..D-08):
- `appSettings.sections.theme` (renamed from `appSettings.sections.appearance`): `'Theme'`
- `appearance.title`: `'Appearance'`
- `appearance.backChevron`: `'Back to Settings'`
- `appearance.rightChevronAriaOnSettings`: `'Appearance settings'`
- `appearance.sections.orbStyle`: `'Orb Style'`
- `appearance.sections.visual`: `'Visual'`
- `appearance.orb.label`: `'Orb'`
- `appearance.orb.options.halo`: `'Halo'`
- `appearance.orb.options.minimal`: `'Minimal'`
- `appearance.orb.options.kuthasta`: `'Kuthasta'`
- `appearance.ringCue.label`: `'Ring cue'`
- `appearance.ringCue.options.arc`: `'Arc'`
- `appearance.ringCue.options.rings`: `'Rings'`
- `appearance.breathingEffect.label`: `'Breathing effect'`
- `appearance.switcherIcons.label`: `'Switcher icons'`

PT-BR drafts (verbatim per D-09; each ships with `// TODO: native-speaker review` on the line above the value):
- `appSettings.sections.theme`: `'Tema'`
- `appearance.title`: `'Aparência'`
- `appearance.backChevron`: `'Voltar para Configurações'`
- `appearance.rightChevronAriaOnSettings`: `'Configurações de aparência'`
- `appearance.sections.orbStyle`: `'Estilo do orbe'`
- `appearance.sections.visual`: `'Visual'`
- `appearance.orb.label`: `'Orbe'`
- `appearance.orb.options.halo`: `'Halo'`
- `appearance.orb.options.minimal`: `'Mínimo'`
- `appearance.orb.options.kuthasta`: `'Kuthasta'`
- `appearance.ringCue.label`: `'Sinal do anel'`
- `appearance.ringCue.options.arc`: `'Arco'`
- `appearance.ringCue.options.rings`: `'Anéis'`
- `appearance.breathingEffect.label`: `'Efeito de respiração'`
- `appearance.switcherIcons.label`: `'Ícones do alternador'`

Type-completeness sentinel: `as const satisfies Readonly<Record<LocaleId, UiStrings>>` at the end of `UI_STRINGS` (existing at line 522 — do not move it; let it fail loudly if a locale forgets a key).

Sole consumer of the renamed key: `src/components/SettingsPanelBody.tsx` line 139 (verified by RESEARCH §"Renaming sed" pitfall + PATTERNS.md analysis — the only production site).

Drift-guard structural-allowlist analog: `src/content/content.no-removed-keys.test.ts:59-79` uses `Array<{ label, pattern: RegExp }>` and scans each file's text. The adapted `content.no-review-markers.test.ts` follows the same shape: an `ALLOWED_MARKER_CONTEXTS` array whose `RegExp`s match the line above an allowed key path.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 01-01: Extend UiStrings + EN catalog + PT-BR drafts; rename appearance → theme key</name>
  <files>src/content/strings.ts</files>
  <read_first>
    - src/content/strings.ts (the full file — read interface at lines 22-187 and both locale entries; the rename touches the interface + EN entry + PT-BR entry in one atomic edit per RESEARCH §Pitfall 2)
    - src/content/lockedCopy.ts (verify `LOCKED_COPY` does NOT reference `appSettings.sections.appearance`; new strings stay outside LOCKED_COPY)
    - .planning/phases/48-appearance-page-i18n/48-PATTERNS.md §`src/content/strings.ts` (lines 750-913 of this PATTERNS.md — has the verbatim before/after for the interface + EN + PT-BR shapes)
    - .planning/phases/48-appearance-page-i18n/48-UI-SPEC.md §Copywriting Contract (the locked EN + PT-BR rows)
  </read_first>
  <behavior>
    - The `UiStrings` interface gains a top-level readonly `appearance` object with the shape declared in the `<interfaces>` block (title, backChevron, rightChevronAriaOnSettings, sections.{orbStyle, visual}, orb.{label, options.{halo, minimal, kuthasta}}, ringCue.{label, options.{arc, rings}}, breathingEffect.label, switcherIcons.label) — all `readonly string`.
    - `UiStrings.appSettings.sections.appearance` is RENAMED to `theme` (key name change only; the surrounding `sections` shape keeps `language`, `audio`, `about`).
    - `UI_STRINGS.en` reflects the renamed key (`theme: 'Theme'`) and adds the new `appearance:` top-level entry with the EN values from the `<interfaces>` block.
    - `UI_STRINGS['pt-BR']` mirrors the EN structure with the D-09 drafts; every newly added string value AND the renamed `theme: 'Tema'` value carries a `// TODO: native-speaker review` comment on the line directly above its value (the line-above placement is what the adapted drift-guard recognises per D-18 path a).
    - The existing `as const satisfies Readonly<Record<LocaleId, UiStrings>>` at the end of `UI_STRINGS` stays put and remains green after this edit.
  </behavior>
  <action>
    Edit `src/content/strings.ts` in three coordinated edits (must land in ONE commit per RESEARCH §Pitfall 2):

    (1) Interface: in `UiStrings.appSettings.sections`, rename the key `appearance: string` → `theme: string` per D-01. Keep `language`, `audio`, `about` unchanged. Then add a new top-level readonly `appearance: { ... }` member with the exact nested shape from the `<interfaces>` block. Use `readonly` on every leaf per the file's existing convention (verified at lines 22-187).

    (2) EN catalog (`UI_STRINGS.en`): replace `appearance: 'Appearance'` inside `sections` with `theme: 'Theme'` (D-01); then add a top-level `appearance: { ... }` entry positioned alphabetically near the existing `appSettings` block (or wherever the existing locale-entry ordering convention places it — match the EN entry's existing key-order). Use the EN copy values from the `<interfaces>` block verbatim. Do NOT add any `// TODO: native-speaker review` markers to EN entries — EN is the locked source.

    (3) PT-BR catalog (`UI_STRINGS['pt-BR']`): mirror the EN structure shift — rename `appearance: 'Aparência'` → `theme: 'Tema'` inside `sections`, and add the parallel `appearance: { ... }` top-level entry with the PT-BR draft values from the `<interfaces>` block. CRITICAL — drift-guard compatibility (D-18 path a): place `// TODO: native-speaker review` on the line directly above each newly added string value AND above the renamed `theme: 'Tema'` value. Format: `    // TODO: native-speaker review\n    title: 'Aparência',` — the comment immediately precedes the value line. Do NOT place markers on lines containing only structural braces or commas.

    Do NOT modify `src/content/lockedCopy.ts` (LOCKED_COPY byte-equality guard must stay green per D-18 hard constraint). Do NOT use sed/perl/regex for the rename per `[[feedback_use_lsp_for_renames]]` — manual edits only at the two known sites in this file.
  </action>
  <verify>
    <automated>npm run test:run -- src/content/strings.test.ts src/content/lockedCopy.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `tsc -b` succeeds (the `as const satisfies Readonly<Record<LocaleId, UiStrings>>` guard implicitly enforces both EN + PT-BR have every newly added key — `grep -n "satisfies Readonly<Record<LocaleId, UiStrings>>" src/content/strings.ts` returns a hit at the end of UI_STRINGS).
    - `grep -n "sections\\.appearance" src/content/strings.ts` returns 0 hits (the old key is fully gone from this file).
    - `grep -nE "^\\s*theme:\\s*'Theme'" src/content/strings.ts` returns at least 1 hit.
    - `grep -nE "^\\s*theme:\\s*'Tema'" src/content/strings.ts` returns at least 1 hit.
    - `grep -c "appearance:" src/content/strings.ts` returns at least 3 (interface declaration + EN entry + PT-BR entry).
    - `grep -nE "title:\\s*'Appearance'" src/content/strings.ts` returns at least 1 hit (EN page title).
    - `grep -nE "title:\\s*'Aparência'" src/content/strings.ts` returns at least 1 hit (PT-BR page title).
    - `grep -c "TODO: native-speaker review" src/content/strings.ts` returns ≥ 15 (one per PT-BR string added under `appearance.*` + the renamed `theme: 'Tema'`).
    - LOCKED_COPY guard remains green: `npm run test:run -- src/content/lockedCopy.test.ts` exits 0 (no production change to `lockedCopy.ts`).
  </acceptance_criteria>
  <done>The `UiStrings` interface declares `appearance.*` and renames `sections.appearance → sections.theme`; both EN and PT-BR `UI_STRINGS` entries match the locked copy from the `<interfaces>` block; PT-BR strings under `appearance.*` and the renamed `theme: 'Tema'` carry line-above `// TODO: native-speaker review` markers; `tsc -b` compiles; LOCKED_COPY test still green.</done>
</task>

<task type="auto">
  <name>Task 01-02: Update the single consumer of the renamed key in SettingsPanelBody.tsx</name>
  <files>src/components/SettingsPanelBody.tsx</files>
  <read_first>
    - src/components/SettingsPanelBody.tsx (the full file — verify the lone consumer site at line 139; confirm no other `appSettings.sections.appearance` reference exists in this file)
    - .planning/phases/48-appearance-page-i18n/48-PATTERNS.md §`src/components/SettingsPanelBody.tsx` (the single-line rename pattern + the Pitfall 6 note)
  </read_first>
  <action>
    Change the one line at `SettingsPanelBody.tsx:139` from `<SettingsSectionHeader label={strings.appSettings.sections.appearance} />` to `<SettingsSectionHeader label={strings.appSettings.sections.theme} />` per D-01.

    Do NOT use sed/perl/regex per `[[feedback_use_lsp_for_renames]]` + RESEARCH §"Renaming sed" pitfall. Manual edit only — there is exactly one consumer in this file. Do NOT touch any surrounding spacing, ordering, or the existing `Appearance`/`Theme` distinction in other section labels (`Language`, `Audio`, `About` stay unchanged).
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "appSettings\\.sections\\.appearance" src/components/SettingsPanelBody.tsx` returns 0 hits.
    - `grep -n "appSettings\\.sections\\.theme" src/components/SettingsPanelBody.tsx` returns exactly 1 hit at the location of the section header label.
    - `npm run build` succeeds (typecheck green — the field now resolves against the renamed interface key).
    - `grep -rn "appSettings\\.sections\\.appearance" src/` returns 0 hits across the whole `src/` tree (rename is complete; the old key is dead).
  </acceptance_criteria>
  <done>The single consumer of the renamed key reads `strings.appSettings.sections.theme`; build passes; no stale references to the old key remain anywhere in `src/`.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 01-03: Adapt content.no-review-markers.test.ts drift-guard with appearance.* allowlist (D-18 path a)</name>
  <files>src/content/content.no-review-markers.test.ts</files>
  <read_first>
    - src/content/content.no-review-markers.test.ts (the full current file — 47 lines; understand the current fs-scan that fails on ANY occurrence of `TODO: native-speaker review`)
    - src/content/content.no-removed-keys.test.ts (the structural-allowlist analog at lines 59-79 + 90-107 — the `Array<{ label, pattern: RegExp }>` shape is what we mirror, inverted from "forbidden" to "allowed-context")
    - src/content/strings.ts (after Task 01-01 lands — the new file contains the markers we are allowlisting; verify their line-above placement is consistent)
    - .planning/phases/48-appearance-page-i18n/48-PATTERNS.md §`src/content/content.no-review-markers.test.ts` (the D-18 path discussion + Path (a) details)
    - .planning/phases/48-appearance-page-i18n/48-UI-SPEC.md §Copywriting Contract last paragraph (the LOCKED_COPY hard constraint)
  </read_first>
  <behavior>
    - The guard continues to fail on any `// TODO: native-speaker review` marker that is NOT immediately above an `appearance.*` key path.
    - The guard accepts every marker placed immediately above a value-line for a key under the `appearance: { ... }` namespace in either `UI_STRINGS.en` or `UI_STRINGS['pt-BR']` (in practice all are in `pt-BR` for Phase 48; the allowlist permits both for future use).
    - The accepted-context detection is structural (line-N-1 contains the marker, line-N contains one of the allowed `appearance.*` key identifiers), not "appears anywhere in a file named strings.ts" — restrict the allowlist as tightly as feasible so other files cannot smuggle markers through.
    - When the I18N-04 future requirement closes the markers, removing them lets the test stay green without further code changes (the allowlist is a relaxation, not a requirement).
    - LOCKED_COPY byte-equality guard (`src/content/lockedCopy.test.ts`) is NOT touched and stays green.
  </behavior>
  <action>
    Rewrite the test body in `src/content/content.no-review-markers.test.ts` keeping the existing imports (`readFileSync`/`readdirSync`/`statSync`, `resolve`/`join`), the existing `collectFiles` helper, the existing `CONTENT_DIR`/`CONTENT_FILES` constants, and the existing `REVIEW_MARKER` constant verbatim — only the test body changes. Replace the single `it('no "// TODO: native-speaker review" marker remains ...')` assertion with a per-line scanner that:

    1. Defines `ALLOWED_KEY_PATTERNS: RegExp[]` — patterns matching the structural shape of an `appearance.*` value line. Suggested list (planner discretion on exact regex; favour readability):
       - `/^\\s*title:\\s*'/`
       - `/^\\s*backChevron:\\s*'/`
       - `/^\\s*rightChevronAriaOnSettings:\\s*'/`
       - `/^\\s*orbStyle:\\s*'/`
       - `/^\\s*visual:\\s*'/`
       - `/^\\s*label:\\s*'/`
       - `/^\\s*halo:\\s*'/`
       - `/^\\s*minimal:\\s*'/`
       - `/^\\s*kuthasta:\\s*'/`
       - `/^\\s*arc:\\s*'/`
       - `/^\\s*rings:\\s*'/`
       - `/^\\s*theme:\\s*'/`  ← include this for the D-01 renamed key (PT-BR `Tema` carries a marker per D-09)
    2. Iterates each content file (existing `CONTENT_FILES`), splits the text into lines, and for every line that contains `REVIEW_MARKER` checks whether the NEXT line matches one of `ALLOWED_KEY_PATTERNS`. If yes, the marker is allowlisted; if no, the marker is a hit and the test fails.
    3. The failure message lists each offending location as `${file}:${lineNumber}` for debuggability.
    4. Add an explanatory comment above `ALLOWED_KEY_PATTERNS` referencing CONTEXT.md D-18 + REQUIREMENTS.md I18N-02 + I18N-04 future requirement (so a future maintainer reading the test knows why the relaxation exists and what closes it).

    Do NOT scope the allowlist by namespace string (e.g., "appears inside the literal `appearance: {`" block) — that requires brace-balance tracking and is fragile. The line-above-value-shape approach is simple, structural, and per `[[feedback_no_design_locking]]` does not pin Tailwind classes / hex values / deleted-code references.

    Do NOT touch `src/content/lockedCopy.ts` or `src/content/lockedCopy.test.ts` per the D-18 hard constraint.
  </action>
  <verify>
    <automated>npm run test:run -- src/content/content.no-review-markers.test.ts src/content/lockedCopy.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - After Task 01-01 lands AND this task lands: `npm run test:run -- src/content/content.no-review-markers.test.ts` exits 0 (markers added under `appearance.*` are accepted via the allowlist).
    - Negative-control: inserting a stray `// TODO: native-speaker review` line ABOVE a non-allowed key (e.g., above `language: 'Idioma'`) makes the test fail — verify mentally by reading the code, not via a destructive grep on the catalog.
    - LOCKED_COPY guard remains green: `npm run test:run -- src/content/lockedCopy.test.ts` exits 0.
    - `grep -n "REVIEW_MARKER" src/content/content.no-review-markers.test.ts` returns ≥ 1 hit (the constant name is preserved).
    - `grep -n "ALLOWED_KEY_PATTERNS" src/content/content.no-review-markers.test.ts` returns ≥ 1 hit (the new allowlist constant exists with the suggested name).
    - `grep -nE "D-18|I18N-04" src/content/content.no-review-markers.test.ts` returns ≥ 1 hit (the explanatory comment cites the decision + the close-out gate).
  </acceptance_criteria>
  <done>The drift-guard scans line-by-line, allows markers immediately above value lines whose key matches one of the `appearance.*` patterns (including the renamed `theme` key), fails on any other marker; LOCKED_COPY test stays green; the test cites D-18 + I18N-04 in a comment.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 01-04: Extend strings.test.ts with a Phase 48 appearance.* describe block</name>
  <files>src/content/strings.test.ts</files>
  <read_first>
    - src/content/strings.test.ts (full file — locate the existing per-phase describe-block convention; RESEARCH cites "Phase 32 new learn.* heading keys" at lines 219-251 as the analog)
    - src/content/strings.ts (after Tasks 01-01..01-03 — the keys to assert on exist)
    - .planning/phases/48-appearance-page-i18n/48-RESEARCH.md §"Phase Requirements → Test Map" (the I18N-01 + I18N-02 rows specify the exact assertions)
    - .planning/phases/48-appearance-page-i18n/48-UI-SPEC.md §Copywriting Contract (the EN + PT-BR copy tables — assertion values come from here)
  </read_first>
  <behavior>
    - A new `describe('Phase 48 appearance.* and theme rename', ...)` block in `strings.test.ts` asserts that every EN string under `appearance.*` matches its locked copy (D-03..D-08) byte-for-byte.
    - The same block asserts the PT-BR `appearance.*` values are non-empty strings (covers I18N-02 type-completeness behavior).
    - The renamed key is asserted in both locales: `UI_STRINGS.en.appSettings.sections.theme === 'Theme'`; `UI_STRINGS['pt-BR'].appSettings.sections.theme === 'Tema'`.
    - The block does NOT assert exact PT-BR strings — they ship as drafts subject to the I18N-04 native-speaker pass, so pinning them would create a future maintenance gate without value.
    - The block does NOT assert that PT-BR markers exist (that is the drift-guard's job in Task 01-03, not this one's).
  </behavior>
  <action>
    Add a new `describe('Phase 48 appearance.* and theme rename', () => { ... })` block to `src/content/strings.test.ts`, positioned alongside the existing per-phase describe blocks (PATTERNS / RESEARCH cite the Phase 32 block at lines 219-251 as the placement analog — place this block adjacent to that or at the end, matching the existing file's ordering convention).

    Inside the block, add `it(...)` cases:

    1. `it('renames appSettings.sections.appearance to appSettings.sections.theme in EN and PT-BR')` — assert `UI_STRINGS.en.appSettings.sections.theme === 'Theme'` and `UI_STRINGS['pt-BR'].appSettings.sections.theme === 'Tema'`; also assert via `expect((UI_STRINGS.en.appSettings.sections as Record<string, unknown>).appearance).toBeUndefined()` that the old EN key is fully gone.

    2. `it('declares EN appearance.* copy locked by D-03..D-08')` — single test with multiple `expect(...).toBe(...)` lines against the EN values from the `<interfaces>` block at the top of this PLAN: title='Appearance', backChevron='Back to Settings', rightChevronAriaOnSettings='Appearance settings', sections.orbStyle='Orb Style', sections.visual='Visual', orb.label='Orb', orb.options.halo='Halo', orb.options.minimal='Minimal', orb.options.kuthasta='Kuthasta', ringCue.label='Ring cue', ringCue.options.arc='Arc', ringCue.options.rings='Rings', breathingEffect.label='Breathing effect', switcherIcons.label='Switcher icons'.

    3. `it('declares PT-BR appearance.* draft values as non-empty strings (I18N-02)')` — for each PT-BR `appearance.*` leaf, assert `typeof value === 'string'` and `value.length > 0`. Iterate via a flat list of paths or via explicit per-leaf expects — match the existing file's idiom. Do NOT assert exact PT-BR strings.

    Use the existing test file's imports (`import { UI_STRINGS } from './strings'` style). Follow the existing per-phase block's tone — concise, no extra commentary.
  </action>
  <verify>
    <automated>npm run test:run -- src/content/strings.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE "describe\\(['\"]Phase 48 appearance" src/content/strings.test.ts` returns ≥ 1 hit (the new describe block exists, scoped to Phase 48 — not a generic `it(` count that could match unrelated blocks).
    - `grep -nE "Phase 48 appearance.+(theme rename|catalog completeness|locked copy)" src/content/strings.test.ts` returns ≥ 1 hit (the describe block's title names the Phase-48 scope; this catches a no-op rename where someone changed only the body of an unrelated describe block).
    - `npm run test:run -- src/content/strings.test.ts` exits 0 with all new `it(...)` cases green.
    - The new describe block contains at least 3 `it(...)` cases — verify with `awk` or by inspection that the count of `it(...)` lines BETWEEN the new `describe('Phase 48 appearance...'` opener and its matching closer is ≥ 3. (A loose grep like `grep -c "it("` is NOT sufficient because it counts pre-existing `it()` cases from unrelated phases.)
    - `grep -n "appSettings.sections.theme" src/content/strings.test.ts` returns at least 2 hits (one for EN, one for PT-BR assertion).
    - `grep -n "'Halo'" src/content/strings.test.ts` returns at least 1 hit (verifies the EN locked-copy assertion landed — `'Halo'` is uniquely tied to D-03 and would not appear in unrelated test blocks).
    - `grep -n "appSettings.sections.theme" src/content/strings.test.ts` returns at least 2 hits (one for EN, one for PT-BR assertion).
    - No assertion pins a PT-BR string value verbatim (sanity-check by inspection: no `'Aparência'` literal inside an `expect(...).toBe(...)` line under this describe block).
  </acceptance_criteria>
  <done>A new per-phase describe block titled `'Phase 48 appearance.* and theme rename'` (or equivalent Phase-48-scoped title) in `strings.test.ts` asserts the EN locked copy verbatim, asserts both locales have the renamed `sections.theme` key, asserts PT-BR `appearance.*` leaves are non-empty strings without pinning exact draft values; all cases pass under `npm run test:run`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| compile-time string catalog → runtime UI render | EN + PT-BR `UI_STRINGS` are static literals defined at module load; React 19 auto-escapes during JSX interpolation. No user input flows into these strings. |
| test fs-scan → source files | The adapted drift-guard reads files with `readFileSync`; it does NOT execute or eval their contents. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-48-01-01 | Tampering | New i18n string values in `strings.ts` | accept | Strings are static module-load literals; no user input mixes in. React 19 auto-escapes all `{string}` interpolations in JSX. ASVS V5 not applicable (no input surface introduced). |
| T-48-01-02 | Information Disclosure | PT-BR draft markers `// TODO: native-speaker review` shipping in source | accept | Source comments are visible to anyone reading the open-source repository by design; no secret content exposed. I18N-04 future requirement closes the markers. |
| T-48-01-03 | Repudiation | Drift-guard allowlist could be silently widened to allowlist non-appearance markers | mitigate | The allowlist is a fixed `RegExp[]` of explicit `appearance.*` key shapes (not a free-form wildcard); the acceptance criteria require the constant name `ALLOWED_KEY_PATTERNS` + cited decision IDs in a comment, both grep-visible. Any future widening must be a code change with a commit, not a silent test mutation. |
| T-48-01-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan; package.json untouched. RESEARCH.md §"Package Legitimacy Audit" — N/A (zero npm packages added across Phase 48). |
</threat_model>

<verification>
- `npm run test:run -- src/content/strings.test.ts src/content/lockedCopy.test.ts src/content/content.no-review-markers.test.ts` — all three green.
- `npm run build` — typecheck green (type-completeness `as const satisfies Readonly<Record<LocaleId, UiStrings>>` covers EN + PT-BR atomic update).
- `grep -rn "appSettings\\.sections\\.appearance" src/` — 0 hits (rename is total).
</verification>

<success_criteria>
1. `UiStrings` interface declares `appearance: { ... }` per the locked shape; `appSettings.sections` has `theme` not `appearance`.
2. EN catalog ships the verbatim copy from D-03..D-08.
3. PT-BR catalog ships D-09 drafts with line-above `// TODO: native-speaker review` markers on every new value AND on the renamed `theme: 'Tema'`.
4. `SettingsPanelBody.tsx:139` consumes `strings.appSettings.sections.theme` (not `.appearance`).
5. The adapted `content.no-review-markers.test.ts` passes with the new markers in place and would still fail if a marker leaked outside the `appearance.*` allowlist.
6. The LOCKED_COPY byte-equality guard remains green (no edits to `lockedCopy.ts` or its test).
7. The type-completeness `satisfies Readonly<Record<LocaleId, UiStrings>>` guard remains green.
</success_criteria>

<output>
Create `.planning/phases/48-appearance-page-i18n/48-01-SUMMARY.md` when done.
</output>
