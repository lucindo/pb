---
phase: 48-appearance-page-i18n
plan: 04
type: execute
wave: 2
depends_on:
  - 48-01
  - 48-02
  - 48-03
files_modified:
  - src/app/pages/AppearancePage.tsx
  - src/app/pages/AppearancePage.test.tsx
  - src/app/pages/AppSettingsPage.tsx
  - src/app/pages/AppSettingsPage.test.tsx
  - src/app/ScreenRouter.tsx
  - src/app/ScreenRouter.test.tsx
autonomous: false
requirements:
  - APPEAR-01
  - APPEAR-02
  - APPEAR-04
  - APPEAR-05
  - APPEAR-06
user_setup: []
must_haves:
  truths:
    - "User on App Settings sees a `>` right-chevron in the header trailing slot and can tap it to navigate to the new Appearance page."
    - "User on the Appearance page sees title `Appearance`, a back-chevron labeled `Back to Settings`, an Orb Style section containing the Orb and Ring cue pickers, and a Visual section containing the Breathing effect and Switcher icons toggles."
    - "User toggling Breathing effect maps to `orbIdle: 'still' ↔ 'ambient'` via `useOrbIdleChoice`; toggling Switcher icons maps to `switcherIcon: false ↔ true` via `useSwitcherIconChoice`; changes apply live across the app via the Phase 47 `hrv:prefs-changed` event."
    - "User returning from Appearance via the back-chevron sees focus restored to the App Settings right-chevron (sentinel=true → chevronButtonRef.focus); fresh entry to App Settings (sentinel=false) keeps focus on the back-chevron (existing behaviour)."
    - "User in both Light and Dark themes sees the Appearance page render with the same Mono Zen chrome as App Settings — `PageShell` + `TopAppBar` + `SectionCard` + the two pickers + the two toggles — via existing primitives only."
  artifacts:
    - path: "src/app/pages/AppearancePage.tsx"
      provides: "New Appearance page surface — chrome + two sections + two pickers + two toggles"
      contains: "AppearancePage"
    - path: "src/app/pages/AppearancePage.test.tsx"
      provides: "Integration test covering D-15 — title, back-chevron, both pickers, both toggles, mount-focus"
      contains: "AppearancePage"
    - path: "src/app/pages/AppSettingsPage.tsx"
      provides: "Trailing right-chevron IconButton + conditional mount-focus based on returningFromAppearance"
      contains: "chevronButtonRef"
    - path: "src/app/pages/AppSettingsPage.test.tsx"
      provides: "Focus-restoration assertions D-17 — sentinel=true focuses chevron, sentinel=false focuses back"
      contains: "returningFromAppearance"
    - path: "src/app/ScreenRouter.tsx"
      provides: "Fourth case `'appearance'` dispatching <AppearancePage> + propagation of new props to AppSettingsPage"
      contains: "case 'appearance'"
    - path: "src/app/ScreenRouter.test.tsx"
      provides: "Fourth-case render assertion + stale-install-field cleanup (RESEARCH Pitfall 5)"
      contains: "appearance-page"
  key_links:
    - from: "src/app/pages/AppearancePage.tsx"
      to: "src/components/OrbPicker.tsx + src/components/RingCuePicker.tsx + src/components/SettingsToggleRow.tsx"
      via: "JSX composition with strings prop slices"
      pattern: "OrbPicker|RingCuePicker|SettingsToggleRow"
    - from: "src/app/pages/AppSettingsPage.tsx"
      to: "src/components/icons/ChevronRightIcon.tsx"
      via: "trailing IconButton on TopAppBar"
      pattern: "ChevronRightIcon"
    - from: "src/app/ScreenRouter.tsx"
      to: "src/app/pages/AppearancePage.tsx"
      via: "case 'appearance' dispatch"
      pattern: "<AppearancePage"
---

<objective>
Ship the user-visible surfaces — the new `AppearancePage`, the right-chevron added to `AppSettingsPage`, and the `ScreenRouter` fourth case — that compose the foundations laid by Plans 01 (strings), 02 (navigation), and 03 (pickers) into a working flow: App Settings → Appearance → back. Bind the two Visual-section toggles inline on `AppearancePage` to `useOrbIdleChoice` (boolean ↔ `'ambient'/'still'` adapter) and `useSwitcherIconChoice` (direct boolean) per D-10. Add the conditional mount-focus on `AppSettingsPage` driven by the `returningFromAppearance` sentinel per D-13. Extend the matching test files per D-15 + D-17. Ship a blocking human verification checkpoint to UAT the page in both themes and viewports.

Purpose: This is the user-visible end of the v2.1 settings-switches story. Plans 01-03 produced foundation; this plan turns them into a navigable page that consumes the locked strings, the new pickers, and the routing/sentinel contract. The blocking human-verify checkpoint covers APPEAR-06 (visual chrome in both themes) which has no automated assertion possible (UI-SPEC §Checker Sign-Off row 2).

Output: 1 new page + 1 new page test; 2 modified pages + 1 modified page test; 1 modified router + 1 modified router test. End-to-end manual UAT: open App Settings, tap right-chevron, change pickers/toggles, observe live update, tap back, observe focus on chevron.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/48-appearance-page-i18n/48-CONTEXT.md
@.planning/phases/48-appearance-page-i18n/48-RESEARCH.md
@.planning/phases/48-appearance-page-i18n/48-PATTERNS.md
@.planning/phases/48-appearance-page-i18n/48-UI-SPEC.md

# Prior-plan summaries — depends on the contracts they shipped
@.planning/phases/48-appearance-page-i18n/48-01-SUMMARY.md
@.planning/phases/48-appearance-page-i18n/48-02-SUMMARY.md
@.planning/phases/48-appearance-page-i18n/48-03-SUMMARY.md

<interfaces>
<!-- Concrete identifiers + signatures extracted verbatim from existing source + Plans 01–03's outputs so the executor pastes deterministically. -->

UiStrings.appearance shape (locked by Plan 01 — Phase 48 Plan 01 §Task 01-01):
- `strings.appearance.title` → 'Appearance'
- `strings.appearance.backChevron` → 'Back to Settings'
- `strings.appearance.rightChevronAriaOnSettings` → 'Appearance settings'
- `strings.appearance.sections.orbStyle` → 'Orb Style'
- `strings.appearance.sections.visual` → 'Visual'
- `strings.appearance.orb` → `{ label: 'Orb', options: { halo, minimal, kuthasta } }` (passed as `strings={strings.appearance.orb.options}` to `OrbPicker`)
- `strings.appearance.ringCue` → `{ label: 'Ring cue', options: { arc, rings } }` (passed as `strings={strings.appearance.ringCue.options}` to `RingCuePicker`)
- `strings.appearance.breathingEffect.label` → 'Breathing effect'
- `strings.appearance.switcherIcons.label` → 'Switcher icons'

Phase 47 choice hooks (already shipped — direct imports for the inline toggles):
- `useOrbIdleChoice()` → `{ orbIdle: 'still' | 'ambient', setOrbIdle(next): void }`. Boolean adapter: `checked={orbIdle === 'ambient'}`, `onChange={(next) => setOrbIdle(next ? 'ambient' : 'still')}`.
- `useSwitcherIconChoice()` → `{ switcherIcon: boolean, setSwitcherIcon(next): void }`. Direct binding: `checked={switcherIcon}`, `onChange={setSwitcherIcon}`.

AppNavigation extension (shipped by Plan 02):
- `vm.dialogs.onAppearanceOpen()` — wired to App Settings trailing chevron `onClick`.
- `vm.dialogs.onBackToAppSettings()` — wired to Appearance back-chevron `onClick` via `<AppearancePage onBack={...}/>` from `ScreenRouter`.
- `vm.dialogs.returningFromAppearance: boolean` — passed as a prop into `<AppSettingsPage>` so its mount `useEffect` can decide which button to focus.

Existing TopAppBar trailing slot (`src/components/primitives/TopAppBar.tsx`): already accepts a `trailing` prop. No primitive change — just pass an `IconButton` into it on `AppSettingsPage`.

Existing icons (already shipped):
- `import { ChevronBackIcon, ChevronRightIcon } from '../../components/icons'` (the barrel exports both).

Existing primitives (no edits needed):
- `IconButton` accepts a `buttonRef: RefObject<HTMLButtonElement>` prop used today by `AppSettingsPage` for back-button focus.
- `PageShell`, `TopAppBar`, `SettingsSectionHeader`, `SettingsToggleRow` — all consumed by reference.

SectionCard inline helper (verbatim from `SettingsPanelBody.tsx:36-55` — copy into `AppearancePage.tsx` per RESEARCH OQ#4):
- Props `{ padding: string; children: ReactNode }`
- Renders `<div style={{ background: 'var(--color-breathing-surface)', border: '1px solid var(--color-border-soft)', borderRadius: 20, padding }}>{children}</div>`
- Use `padding="16px"` for both Phase 48 cards (matches App Settings precedent).
- Do NOT export from `SettingsPanelBody.tsx` to share — duplicate the 14-line helper inline (RESEARCH OQ#4 — "inline duplicate matches D-10's 'compose existing primitives, don't extract new shared ones unnecessarily'").

ScreenRouter Pitfall 5 cleanup (RESEARCH §Pitfall 5 — PATTERNS §`src/app/ScreenRouter.test.tsx`):
- The existing `makeVmForScreen` helper in `ScreenRouter.test.tsx:31-32` constructs a fake `install` slice with `showBanner: false` and `onDismiss: () => {}` — both fields removed in J18.4 and forbidden by `content.no-removed-keys.test.ts:73`. The cast through `as unknown as AppViewModel` masks this drift today. Plan 04 extends this fixture for the new `'appearance'` case AND removes the stale fields in the same edit.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 04-01: Create AppearancePage.tsx — chrome + two sections + two pickers + two toggles</name>
  <files>src/app/pages/AppearancePage.tsx</files>
  <read_first>
    - src/app/pages/AppSettingsPage.tsx (paste-and-rename analog — full file; the page chrome template per PATTERNS §`src/app/pages/AppearancePage.tsx`)
    - src/app/pages/LearnPage.tsx (secondary page-chrome precedent; mount-focus pattern)
    - src/components/SettingsPanelBody.tsx lines 36-55 (the `SectionCard` inline helper to copy verbatim)
    - src/components/OrbPicker.tsx (after Plan 03 lands — the picker this page composes)
    - src/components/RingCuePicker.tsx (after Plan 03 lands)
    - src/components/SettingsToggleRow.tsx (the toggle primitive — verify the `{ label, ariaLabel, checked, onChange }` prop shape)
    - src/components/SettingsSectionHeader.tsx (section header chrome — verify the `label` prop)
    - src/hooks/useOrbIdleChoice.ts + src/hooks/useSwitcherIconChoice.ts (verify return shapes per `<interfaces>`)
    - src/hooks/useUiStringsContext.tsx (verify `useUiStrings()` export)
    - .planning/phases/48-appearance-page-i18n/48-PATTERNS.md §`src/app/pages/AppearancePage.tsx` (the body composition listing)
    - .planning/phases/48-appearance-page-i18n/48-UI-SPEC.md §Layout Contract B (the locked layout tree)
  </read_first>
  <behavior>
    - `AppearancePage` is a named functional component accepting `{ onBack(this: void): void }` only — no other props.
    - Component reads the full UI strings via `useUiStrings()` then slices `appearance` once: `const strings = useUiStrings().appearance` (per RESEARCH Shared Pattern F — LearnPage style for single-namespace pages).
    - Mount-focus: `useRef<HTMLButtonElement>(null)` named `backButtonRef`; `useEffect(() => backButtonRef.current?.focus({ preventScroll: true }), [])` — always focuses the back-chevron on mount (the sentinel governs `AppSettingsPage`, not this page).
    - Chrome: `<PageShell>` wrapping a `<TopAppBar title={strings.title} leading={<IconButton ... back chevron .../>} />` and then `<div className="w-full text-left">` containing the two sections.
    - Section 1 (`Orb Style`): `<SettingsSectionHeader label={strings.sections.orbStyle} />` then `<SectionCard padding="16px"><div className="grid gap-4"><OrbPicker .../><RingCuePicker .../></div></SectionCard>`. The `grid gap-4` (16px) matches the Audio card's `CuePicker`+`TimbrePicker` inter-control spacing (UI-SPEC §Spacing Scale).
    - Section 2 (`Visual`): `<SettingsSectionHeader label={strings.sections.visual} />` then `<SectionCard padding="16px">` containing two `<SettingsToggleRow>` instances stacked (no spacer between — `SettingsToggleRow`'s composed `SettingsRow` provides a `border-t` divider between rows).
    - `OrbPicker` props: `disabled={false}`, `sectionLabel={strings.orb.label}`, `sectionLabelHidden={false}` (D-10 + UI-SPEC §Layout Contract B picker label visibility), `strings={strings.orb.options}`.
    - `RingCuePicker` props: `disabled={false}`, `sectionLabel={strings.ringCue.label}`, `sectionLabelHidden={false}`, `strings={strings.ringCue.options}`.
    - Breathing effect toggle: `<SettingsToggleRow label={strings.breathingEffect.label} ariaLabel={strings.breathingEffect.label} checked={orbIdle === 'ambient'} onChange={(next) => setOrbIdle(next ? 'ambient' : 'still')} />` per the boolean adapter (`<interfaces>` block).
    - Switcher icons toggle: `<SettingsToggleRow label={strings.switcherIcons.label} ariaLabel={strings.switcherIcons.label} checked={switcherIcon} onChange={setSwitcherIcon} />` per direct boolean binding.
    - Page-level toggle hook calls live near the top of the component body (after `useUiStrings`): `const { orbIdle, setOrbIdle } = useOrbIdleChoice()` and `const { switcherIcon, setSwitcherIcon } = useSwitcherIconChoice()`. The pickers manage their own hooks internally (Plan 03).
    - Inline `SectionCard` helper copied verbatim from `SettingsPanelBody.tsx:36-55` into a private (non-exported) declaration inside `AppearancePage.tsx`. Do NOT export it; do NOT modify `SettingsPanelBody.tsx` to share (RESEARCH OQ#4).
  </behavior>
  <action>
    Create `src/app/pages/AppearancePage.tsx`. Mirror `src/app/pages/AppSettingsPage.tsx`'s structure (imports → exported `AppearancePageProps` interface → exported `AppearancePage` function → return JSX) and apply the body composition from PATTERNS §`src/app/pages/AppearancePage.tsx` + UI-SPEC §Layout Contract B verbatim.

    Imports:
    - From `'react'`: `useEffect`, `useRef`, `type ReactElement`, `type ReactNode`.
    - `import { ChevronBackIcon } from '../../components/icons'`.
    - `import { IconButton } from '../../components/primitives/IconButton'`.
    - `import { PageShell } from '../../components/primitives/PageShell'`.
    - `import { TopAppBar } from '../../components/primitives/TopAppBar'`.
    - `import { OrbPicker } from '../../components/OrbPicker'`.
    - `import { RingCuePicker } from '../../components/RingCuePicker'`.
    - `import { SettingsSectionHeader } from '../../components/SettingsSectionHeader'`.
    - `import { SettingsToggleRow } from '../../components/SettingsToggleRow'`.
    - `import { useUiStrings } from '../../hooks/useUiStringsContext'`.
    - `import { useOrbIdleChoice } from '../../hooks/useOrbIdleChoice'`.
    - `import { useSwitcherIconChoice } from '../../hooks/useSwitcherIconChoice'`.

    Props interface: `export interface AppearancePageProps { onBack(this: void): void }`.

    Inline `SectionCard` helper (private, NOT exported) — copy verbatim from `SettingsPanelBody.tsx:36-55`. Use `function SectionCard({ padding, children }: { padding: string; children: ReactNode }): ReactElement { return (<div style={{ background: 'var(--color-breathing-surface)', border: '1px solid var(--color-border-soft)', borderRadius: 20, padding }}>{children}</div>) }`.

    Function body composition (per UI-SPEC §Layout Contract B):
    1. `const strings = useUiStrings().appearance` — single subscription.
    2. `const { orbIdle, setOrbIdle } = useOrbIdleChoice()`.
    3. `const { switcherIcon, setSwitcherIcon } = useSwitcherIconChoice()`.
    4. `const backButtonRef = useRef<HTMLButtonElement>(null)`.
    5. `useEffect(() => { backButtonRef.current?.focus({ preventScroll: true }) }, [])`.
    6. Return the JSX tree: `<PageShell>` → `<TopAppBar title={strings.title} leading={<IconButton icon={<ChevronBackIcon />} label={strings.backChevron} onClick={onBack} buttonRef={backButtonRef} />} />` → `<div className="w-full text-left">` containing the two section blocks.

    Section 1 JSX:
    - `<SettingsSectionHeader label={strings.sections.orbStyle} />`
    - `<SectionCard padding="16px"><div className="grid gap-4"><OrbPicker disabled={false} sectionLabel={strings.orb.label} sectionLabelHidden={false} strings={strings.orb.options} /><RingCuePicker disabled={false} sectionLabel={strings.ringCue.label} sectionLabelHidden={false} strings={strings.ringCue.options} /></div></SectionCard>`

    Section 2 JSX:
    - `<SettingsSectionHeader label={strings.sections.visual} />`
    - `<SectionCard padding="16px"><SettingsToggleRow label={strings.breathingEffect.label} ariaLabel={strings.breathingEffect.label} checked={orbIdle === 'ambient'} onChange={(next) => { setOrbIdle(next ? 'ambient' : 'still') }} /><SettingsToggleRow label={strings.switcherIcons.label} ariaLabel={strings.switcherIcons.label} checked={switcherIcon} onChange={setSwitcherIcon} /></SectionCard>`

    Do NOT add a `trailing` slot to the `TopAppBar` (UI-SPEC §Layout Contract B leaves trailing empty for title centering).
    Do NOT include any new design tokens, Tailwind classes outside the locked spike-010 set, or hex literals per `[[project_v16_visual_locks]]` + `[[feedback_no_design_locking]]`.
    Do NOT use ad-hoc `dispatchEvent` calls anywhere; rely on the choice-hook setters.
    Do NOT export `SectionCard`; keep it private to this file per RESEARCH OQ#4.
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `test -f src/app/pages/AppearancePage.tsx` exits 0.
    - `grep -n "export function AppearancePage" src/app/pages/AppearancePage.tsx` returns exactly 1 hit.
    - `grep -n "useUiStrings().appearance" src/app/pages/AppearancePage.tsx` returns exactly 1 hit.
    - `grep -n "useOrbIdleChoice" src/app/pages/AppearancePage.tsx` returns ≥ 2 hits (import + call).
    - `grep -n "useSwitcherIconChoice" src/app/pages/AppearancePage.tsx` returns ≥ 2 hits.
    - `grep -n "OrbPicker" src/app/pages/AppearancePage.tsx` returns ≥ 2 hits (import + JSX).
    - `grep -n "RingCuePicker" src/app/pages/AppearancePage.tsx` returns ≥ 2 hits.
    - `grep -c "SettingsToggleRow" src/app/pages/AppearancePage.tsx` returns ≥ 3 (import + 2 JSX usages).
    - `grep -c "SettingsSectionHeader" src/app/pages/AppearancePage.tsx` returns ≥ 3 (import + 2 JSX usages).
    - `grep -n "function SectionCard" src/app/pages/AppearancePage.tsx` returns exactly 1 hit (inline helper).
    - `grep -n "export.*SectionCard\\|export\\s\\+.*SectionCard" src/app/pages/AppearancePage.tsx` returns 0 hits (SectionCard NOT exported).
    - `grep -n "dispatchEvent\\|savePrefs\\|loadPrefs" src/app/pages/AppearancePage.tsx` returns 0 hits.
    - `grep -n "next ? 'ambient' : 'still'" src/app/pages/AppearancePage.tsx` returns exactly 1 hit (boolean→union adapter).
    - `grep -n "backButtonRef\\.current?\\.focus" src/app/pages/AppearancePage.tsx` returns ≥ 1 hit (mount-focus).
    - `npm run build` succeeds.
  </acceptance_criteria>
  <done>`AppearancePage.tsx` exists with the locked imports, the inline private `SectionCard` helper, the two sections holding the two pickers + two toggles via the boolean adapter for `useOrbIdleChoice`, mount-focus on `backButtonRef`, and zero ad-hoc dispatch/persistence calls; `npm run build` typechecks.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 04-02: Create AppearancePage.test.tsx integration test (D-15)</name>
  <files>src/app/pages/AppearancePage.test.tsx</files>
  <read_first>
    - src/app/pages/AppSettingsPage.test.tsx (the analog harness — RTL + UiStringsProvider; PATTERNS Shared Pattern E)
    - src/app/pages/AppearancePage.tsx (after Task 04-01 — the component under test)
    - src/content/strings.ts (after Plan 01 — verify `UI_STRINGS.en.appearance` exists with the locked values; the test will assert against these)
    - src/hooks/useUiStringsContext.tsx (verify `UiStringsProvider` export and prop shape)
    - .planning/phases/48-appearance-page-i18n/48-PATTERNS.md §`src/app/pages/AppearancePage.test.tsx` (the assertion list for D-15)
    - .planning/phases/48-appearance-page-i18n/48-RESEARCH.md §Phase Requirements → Test Map (the APPEAR-02 + APPEAR-04 + APPEAR-05 rows)
  </read_first>
  <behavior>
    - Test file mounts `<AppearancePage onBack={vi.fn()} />` inside `<UiStringsProvider value={UI_STRINGS.en}>` (mirrors the `AppSettingsPage.test.tsx` harness).
    - Suite asserts: page title `Appearance` rendered as `h1`; back-chevron exists with `aria-label='Back to Settings'`; clicking back-chevron invokes the supplied `onBack`; two radiogroups rendered (Orb + Ring cue); two switches rendered (Breathing effect + Switcher icons); back-chevron has focus after mount; clicking an orb option writes the correct `breathingShape` to localStorage; toggling Breathing effect writes `orbIdle: 'ambient'` to localStorage.
    - Uses real `localStorage` per RESEARCH OQ#5 — same convention as the picker tests in Plan 03.
  </behavior>
  <action>
    Create `src/app/pages/AppearancePage.test.tsx` mirroring `src/app/pages/AppSettingsPage.test.tsx`'s harness:

    Imports: `@testing-library/jest-dom/vitest`, `render`/`screen` from `@testing-library/react`, `userEvent` from `@testing-library/user-event`, `afterEach`/`beforeEach`/`describe`/`expect`/`it`/`vi` from `vitest`. `import { UI_STRINGS } from '../../content/strings'`. `import { UiStringsProvider } from '../../hooks/useUiStringsContext'`. `import { AppearancePage } from './AppearancePage'`. `import { STATE_KEY } from '../../storage'`. `import { DEFAULT_PREFS } from '../../storage/prefs'` (verify exact path against Plan 03 picker tests).

    Helper `renderPage(props?: Partial<{ onBack: () => void }>)`: returns `render(<UiStringsProvider value={UI_STRINGS.en}><AppearancePage onBack={onBack ?? vi.fn()} /></UiStringsProvider>)` together with the `onBack` reference.

    `beforeEach`: clear localStorage. `afterEach`: clear localStorage + `vi.restoreAllMocks()`.

    `describe('AppearancePage', () => { ... })` with these `it(...)` cases (mirroring PATTERNS §`src/app/pages/AppearancePage.test.tsx`):

    1. `it('renders the locked page title Appearance')` — assert `screen.getByRole('heading', { level: 1, name: UI_STRINGS.en.appearance.title }).toBeInTheDocument()`.

    2. `it('renders a back-chevron with the locked aria-label')` — assert `screen.getByRole('button', { name: UI_STRINGS.en.appearance.backChevron }).toBeInTheDocument()`.

    3. `it('invokes onBack when back-chevron is clicked')` — render with a `vi.fn()` onBack; click the back-chevron via `userEvent`; assert `onBack` called exactly once.

    4. `it('renders two radiogroups for Orb and Ring cue')` — assert `screen.getAllByRole('radiogroup').length === 2`.

    5. `it('renders two switches for Breathing effect and Switcher icons')` — assert `screen.getAllByRole('switch').length === 2`. Optionally narrow each by name (`{ name: UI_STRINGS.en.appearance.breathingEffect.label }` etc.).

    6. `it('focuses the back-chevron on mount')` — assert `screen.getByRole('button', { name: UI_STRINGS.en.appearance.backChevron }).toHaveFocus()`.

    7. `it('writes breathingShape to the prefs envelope when an orb option is clicked')` — click the radio with name `UI_STRINGS.en.appearance.orb.options.kuthasta`; parse `STATE_KEY` from localStorage; assert `prefs.breathingShape === 'spiritual-eye'`.

    8. `it('writes orbIdle=ambient to the prefs envelope when Breathing effect is toggled on')` — initial state is `still`; click the Breathing effect switch; parse `STATE_KEY`; assert `prefs.orbIdle === 'ambient'`.

    9. `it('writes switcherIcon=true to the prefs envelope when Switcher icons is toggled on')` — initial state is `false`; click the Switcher icons switch; parse `STATE_KEY`; assert `prefs.switcherIcon === true`.

    Do NOT mock the choice hooks (RESEARCH OQ#5). Do NOT assert on visual styling, Tailwind classes, hex colors, or any spike-010 token values per `[[feedback_no_design_locking]]`. Do NOT pin PT-BR copy in assertions (the EN catalog is the test fixture via `UiStringsProvider value={UI_STRINGS.en}`).
  </action>
  <verify>
    <automated>npm run test:run -- src/app/pages/AppearancePage.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `test -f src/app/pages/AppearancePage.test.tsx` exits 0.
    - `grep -c "it(" src/app/pages/AppearancePage.test.tsx` returns ≥ 9.
    - `grep -n "UiStringsProvider" src/app/pages/AppearancePage.test.tsx` returns ≥ 2 hits (import + JSX).
    - `grep -n "UI_STRINGS.en.appearance" src/app/pages/AppearancePage.test.tsx` returns ≥ 3 hits (multiple assertions reading from the locked catalog).
    - `grep -n "STATE_KEY" src/app/pages/AppearancePage.test.tsx` returns ≥ 2 hits.
    - `grep -n "vi\\.mock" src/app/pages/AppearancePage.test.tsx` returns 0 hits.
    - `grep -n "getAllByRole('radiogroup')" src/app/pages/AppearancePage.test.tsx` returns ≥ 1 hit.
    - `grep -n "getAllByRole('switch')" src/app/pages/AppearancePage.test.tsx` returns ≥ 1 hit.
    - `npm run test:run -- src/app/pages/AppearancePage.test.tsx` exits 0; all 9+ tests green.
  </acceptance_criteria>
  <done>The integration test file exists with 9+ `it(...)` cases asserting title, back-chevron, onBack invocation, two radiogroups, two switches, mount-focus, picker click writes envelope, both toggle clicks write envelope; runs against `UiStringsProvider` with `UI_STRINGS.en`; no hook mocks; all green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 04-03: Extend AppSettingsPage.tsx — trailing right-chevron + conditional mount-focus</name>
  <files>src/app/pages/AppSettingsPage.tsx</files>
  <read_first>
    - src/app/pages/AppSettingsPage.tsx (the existing file — verify the current props interface, the current single-ref mount-focus pattern, the existing imports)
    - src/components/icons/index.ts (verify `ChevronRightIcon` is exported by the barrel; if not, import directly from `../../components/icons/ChevronRightIcon`)
    - src/components/primitives/TopAppBar.tsx (verify the `trailing` slot prop already exists — UI-SPEC §Layout Contract A confirms it does)
    - src/components/primitives/IconButton.tsx (verify the `buttonRef` prop signature — used today by AppSettingsPage for the back-chevron)
    - .planning/phases/48-appearance-page-i18n/48-PATTERNS.md §`src/app/pages/AppSettingsPage.tsx` (the additive extension)
    - .planning/phases/48-appearance-page-i18n/48-UI-SPEC.md §Layout Contract A (the trailing-slot composition + focus management on mount rule)
  </read_first>
  <behavior>
    - `AppSettingsPageProps` gains two new fields: `onAppearanceOpen(this: void): void` and `returningFromAppearance: boolean`.
    - The component declares a second `useRef<HTMLButtonElement>(null)` named `chevronButtonRef`.
    - The existing single mount-focus `useEffect` becomes conditional: `if (returningFromAppearance) chevronButtonRef.current?.focus({ preventScroll: true }); else backButtonRef.current?.focus({ preventScroll: true })`. Dependency array: `[returningFromAppearance]` (so re-mounting App Settings via a different navigation path re-runs the focus logic).
    - The strings slice expands to include `appearance` (for the trailing chevron's aria-label): `const strings = { appSettings: allStrings.appSettings, install: allStrings.install, appearance: allStrings.appearance }`.
    - `<TopAppBar>` invocation gains a `trailing={<IconButton icon={<ChevronRightIcon />} label={strings.appearance.rightChevronAriaOnSettings} onClick={onAppearanceOpen} buttonRef={chevronButtonRef} />}` prop alongside the existing `leading` and `title`.
    - The chevron's default `IconButton` size (md = 40px) mirrors the back-chevron's size per UI-SPEC §Layout Contract A.
    - The chevron is always present on App Settings (UI-SPEC §Layout Contract A — "the chevron remains visually present and tappable").
  </behavior>
  <action>
    Edit `src/app/pages/AppSettingsPage.tsx` per PATTERNS §`src/app/pages/AppSettingsPage.tsx` + UI-SPEC §Layout Contract A:

    1. Add `ChevronRightIcon` to the existing icon import: `import { ChevronBackIcon, ChevronRightIcon } from '../../components/icons'` (combine with the existing `ChevronBackIcon` line if they share the same source).

    2. Extend the `AppSettingsPageProps` interface to add `onAppearanceOpen(this: void): void` and `returningFromAppearance: boolean` (preserve `this: void` modifier convention).

    3. Destructure the two new props in the function signature alongside the existing destructured props.

    4. Expand the strings slice: replace `const strings = { appSettings: allStrings.appSettings, install: allStrings.install }` with `const strings = { appSettings: allStrings.appSettings, install: allStrings.install, appearance: allStrings.appearance }`.

    5. Add a second ref next to the existing one: `const chevronButtonRef = useRef<HTMLButtonElement>(null)`.

    6. Replace the existing mount-focus `useEffect` body with the conditional branch from `<interfaces>` / RESEARCH Pattern 4: focus the chevron if `returningFromAppearance`, otherwise focus the back-chevron. Change the dependency array from `[]` to `[returningFromAppearance]`.

    7. Inside the `<TopAppBar ...>` JSX, after the existing `leading={...}` prop, add `trailing={<IconButton icon={<ChevronRightIcon />} label={strings.appearance.rightChevronAriaOnSettings} onClick={onAppearanceOpen} buttonRef={chevronButtonRef} />}`. Do NOT pass a `size` prop — let it default to `md` per UI-SPEC §Layout Contract A.

    Do NOT change any other JSX in the file (body, install button, About section). Do NOT touch the back-chevron's `IconButton` properties. Do NOT modify the existing `useUiStrings()` call site.
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "ChevronRightIcon" src/app/pages/AppSettingsPage.tsx` returns ≥ 2 hits (import + JSX in the trailing slot).
    - `grep -n "onAppearanceOpen" src/app/pages/AppSettingsPage.tsx` returns ≥ 3 hits (interface member, function param, JSX onClick wiring).
    - `grep -n "returningFromAppearance" src/app/pages/AppSettingsPage.tsx` returns ≥ 3 hits (interface member, function param, useEffect branch).
    - `grep -n "chevronButtonRef" src/app/pages/AppSettingsPage.tsx` returns ≥ 3 hits (useRef declaration, JSX `buttonRef={}` assignment, useEffect focus call).
    - `grep -n "trailing=" src/app/pages/AppSettingsPage.tsx` returns ≥ 1 hit (the new TopAppBar trailing prop).
    - `grep -nE "useEffect\\([^)]+\\[returningFromAppearance\\]" src/app/pages/AppSettingsPage.tsx` OR `grep -n "[returningFromAppearance]" src/app/pages/AppSettingsPage.tsx` returns ≥ 1 hit (the useEffect dependency array includes the sentinel).
    - `grep -n "strings\\.appearance\\.rightChevronAriaOnSettings" src/app/pages/AppSettingsPage.tsx` returns exactly 1 hit (the IconButton label).
    - `grep -n "allStrings\\.appearance" src/app/pages/AppSettingsPage.tsx` returns ≥ 1 hit (the strings slice extension).
    - `npm run build` succeeds.
  </acceptance_criteria>
  <done>AppSettingsPage props expand by two; second ref `chevronButtonRef` declared; mount-focus useEffect is conditional on `returningFromAppearance` with `[returningFromAppearance]` deps; TopAppBar `trailing` slot holds the right-chevron IconButton with the locked aria-label and the supplied `onAppearanceOpen` handler; build typechecks.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 04-04: Extend AppSettingsPage.test.tsx — focus-restoration assertions (D-17)</name>
  <files>src/app/pages/AppSettingsPage.test.tsx</files>
  <read_first>
    - src/app/pages/AppSettingsPage.test.tsx (the existing test file — locate the `renderPage` helper around lines 10-33 and the existing `focuses the back button on mount` test at line 77-80)
    - src/app/pages/AppSettingsPage.tsx (after Task 04-03 — verify the prop shape the harness must satisfy)
    - src/content/strings.ts (after Plan 01 — verify `UI_STRINGS.en.appearance.rightChevronAriaOnSettings` exists for the chevron-name assertion)
    - .planning/phases/48-appearance-page-i18n/48-PATTERNS.md §`src/app/pages/AppSettingsPage.test.tsx` (D-17 verbatim test bodies)
  </read_first>
  <behavior>
    - `renderPage` helper is extended to accept `onAppearanceOpen` (default `vi.fn()`) and `returningFromAppearance` (default `false`).
    - The existing `'focuses the back button on mount'` test is split into TWO scoped cases per D-17: one for `returningFromAppearance: false` (default mount-focus → back-chevron) and one for `returningFromAppearance: true` (mount-focus → right-chevron).
    - The new chevron-related cases also assert the chevron exists and its click invokes the supplied `onAppearanceOpen` handler.
  </behavior>
  <action>
    Edit `src/app/pages/AppSettingsPage.test.tsx`:

    1. Update the `renderPage` helper to accept and pass through the two new props with sensible defaults. Suggested shape: extend its `props` argument type with `onAppearanceOpen?: () => void` and `returningFromAppearance?: boolean`; create `const onAppearanceOpen = props.onAppearanceOpen ?? vi.fn()` and `const returningFromAppearance = props.returningFromAppearance ?? false`; pass both into the rendered `<AppSettingsPage ...>` element; return them in the helper's return shape so tests can assert on `onAppearanceOpen`.

    2. Replace the existing `it('focuses the back button on mount', ...)` test (line 77-80) with TWO new tests:

       (a) `it('focuses the back button on mount when returningFromAppearance=false', () => { renderPage({ returningFromAppearance: false }); expect(screen.getByRole('button', { name: UI_STRINGS.en.appSettings.close })).toHaveFocus() })`.

       (b) `it('focuses the right-chevron on mount when returningFromAppearance=true', () => { renderPage({ returningFromAppearance: true }); expect(screen.getByRole('button', { name: UI_STRINGS.en.appearance.rightChevronAriaOnSettings })).toHaveFocus() })`.

    3. Add a new test for APPEAR-01 chevron presence + click handling: `it('renders the right-chevron and invokes onAppearanceOpen when clicked', async () => { ... })` — render via `renderPage`; locate `getByRole('button', { name: UI_STRINGS.en.appearance.rightChevronAriaOnSettings })`; call `userEvent.click(...)`; assert the `onAppearanceOpen` returned from `renderPage` was called exactly once.

    Do NOT delete any other existing test. Do NOT change the unrelated install-section tests. Do NOT pin Tailwind classes or hex values.
  </action>
  <verify>
    <automated>npm run test:run -- src/app/pages/AppSettingsPage.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE "it\\(['\"]focuses the back button on mount when returningFromAppearance=false" src/app/pages/AppSettingsPage.test.tsx` returns ≥ 1 hit.
    - `grep -nE "it\\(['\"]focuses the right-chevron on mount when returningFromAppearance=true" src/app/pages/AppSettingsPage.test.tsx` returns ≥ 1 hit.
    - `grep -nE "it\\(['\"]renders the right-chevron and invokes onAppearanceOpen" src/app/pages/AppSettingsPage.test.tsx` returns ≥ 1 hit.
    - `grep -n "UI_STRINGS\\.en\\.appearance\\.rightChevronAriaOnSettings" src/app/pages/AppSettingsPage.test.tsx` returns ≥ 2 hits (focus assertion + click assertion).
    - `grep -n "onAppearanceOpen" src/app/pages/AppSettingsPage.test.tsx` returns ≥ 2 hits (helper + click assertion).
    - `npm run test:run -- src/app/pages/AppSettingsPage.test.tsx` exits 0; the original `'focuses the back button on mount'` test is no longer present (replaced by the two scoped versions); all other existing tests still pass.
  </acceptance_criteria>
  <done>renderPage helper accepts the two new props with defaults; three new focus-/chevron-related `it(...)` cases land; the original single mount-focus test is replaced; all tests in the file pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 04-05: Extend ScreenRouter.tsx (fourth case) + ScreenRouter.test.tsx (case + Pitfall 5 cleanup)</name>
  <files>src/app/ScreenRouter.tsx, src/app/ScreenRouter.test.tsx</files>
  <read_first>
    - src/app/ScreenRouter.tsx (the full 42-line file — the existing 3-case switch is the analog to extend)
    - src/app/ScreenRouter.test.tsx (the full file — note the `makeVmForScreen` helper at line ~25 + the stale `install` fixture fields at lines 31-32 per RESEARCH §Pitfall 5)
    - src/app/pages/AppearancePage.tsx (after Task 04-01 — the page being routed to)
    - src/app/appViewModel.ts (after Plan 02 — verify the `AppDialogsViewModel` interface declares the three new fields the test fixture must populate)
    - src/content/content.no-removed-keys.test.ts (verify the J18.4 entry forbidding `showBanner` and `onDismiss` — line 73 region; this is why removing them in the fixture is mandatory, not optional)
    - .planning/phases/48-appearance-page-i18n/48-PATTERNS.md §`src/app/ScreenRouter.tsx` + §`src/app/ScreenRouter.test.tsx` (the verbatim additive 4th case + the fixture cleanup specification)
  </read_first>
  <behavior>
    - `ScreenRouter.tsx` gains an `import { AppearancePage } from './pages/AppearancePage'`.
    - The switch gains a fourth `case 'appearance':` returning `<AppearancePage onBack={vm.dialogs.onBackToAppSettings} />`.
    - The existing `case 'appSettings':` JSX is extended to pass two new props: `onAppearanceOpen={vm.dialogs.onAppearanceOpen}` and `returningFromAppearance={vm.dialogs.returningFromAppearance}`.
    - `ScreenRouter.test.tsx` gains a `vi.mock('./pages/AppearancePage', () => ({ AppearancePage: () => <div data-testid="appearance-page" /> }))` declaration alongside the existing mocks for the other pages.
    - The `makeVmForScreen` helper is extended to include the three new `dialogs` fields (`onAppearanceOpen`, `onBackToAppSettings`, `returningFromAppearance`) AND to REMOVE the stale `install.showBanner` and `install.onDismiss` fields per RESEARCH §Pitfall 5.
    - A new `it('renders AppearancePage when appScreen=appearance', ...)` test asserts the 4th case behaviour.
    - All existing tests continue to pass (the AppSettings case test still finds `app-settings-page` testid, etc.).
  </behavior>
  <action>
    Edit BOTH files in a coordinated change:

    (a) `src/app/ScreenRouter.tsx` (per PATTERNS §`src/app/ScreenRouter.tsx`):
    - Add `import { AppearancePage } from './pages/AppearancePage'` alongside the existing page imports.
    - Inside the switch, after the existing `case 'appSettings':` block, add a new `case 'appearance':` block that returns `<AppearancePage onBack={vm.dialogs.onBackToAppSettings} />`. Position it before the `case 'practice':` / `default:` fall-through.
    - Inside the existing `case 'appSettings':` `<AppSettingsPage ...>` invocation, add two new prop assignments: `onAppearanceOpen={vm.dialogs.onAppearanceOpen}` and `returningFromAppearance={vm.dialogs.returningFromAppearance}` (alongside the existing props). Order them after the existing `onBack={vm.dialogs.onBackToPractice}`.

    (b) `src/app/ScreenRouter.test.tsx` (per PATTERNS §`src/app/ScreenRouter.test.tsx`):
    - Add a `vi.mock('./pages/AppearancePage', () => ({ AppearancePage: () => <div data-testid="appearance-page" /> }))` declaration alongside the existing mocks for the other pages.
    - Extend the `makeVmForScreen(appScreen)` helper fixture (around line 25 in the current file). In the `dialogs` slice of the returned `AppViewModel`, ADD `onAppearanceOpen: () => {}`, `onBackToAppSettings: () => {}`, `returningFromAppearance: false`. In the `install` slice (lines 31-32 region), REMOVE the stale `showBanner: false` and `onDismiss: () => {}` entries per RESEARCH §Pitfall 5 (these fields no longer exist on the `AppInstallViewModel` shape per the J18.4 drift-guard).
    - Add a new `it('renders AppearancePage when appScreen=appearance', () => { render(<ScreenRouter vm={makeVmForScreen('appearance')} />); expect(screen.getByTestId('appearance-page')).toBeInTheDocument(); /* assert other testids absent */ })`.
    - Existing per-case tests should be reviewed for any remaining references to the removed install fields; if `getByTestId('app-settings-page')` etc. tests pass without those fields, no further edit is needed (the cast through `as unknown as AppViewModel` previously masked the absence — now the fixture is honest).

    Do NOT change the order of the existing switch cases. Do NOT modify the `default:` fall-through to `<PracticeScreen vm={vm} />`. Do NOT touch any other test fixtures unrelated to the install / dialogs slices.
  </action>
  <verify>
    <automated>npm run test:run -- src/app/ScreenRouter.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "import { AppearancePage }" src/app/ScreenRouter.tsx` returns exactly 1 hit.
    - `grep -n "case 'appearance':" src/app/ScreenRouter.tsx` returns exactly 1 hit.
    - `grep -n "<AppearancePage onBack={vm.dialogs.onBackToAppSettings}" src/app/ScreenRouter.tsx` returns ≥ 1 hit.
    - `grep -n "onAppearanceOpen={vm.dialogs.onAppearanceOpen}" src/app/ScreenRouter.tsx` returns exactly 1 hit (the AppSettings-case prop).
    - `grep -n "returningFromAppearance={vm.dialogs.returningFromAppearance}" src/app/ScreenRouter.tsx` returns exactly 1 hit.
    - `grep -n "appearance-page" src/app/ScreenRouter.test.tsx` returns ≥ 2 hits (mock declaration + assertion).
    - `grep -n "onBackToAppSettings" src/app/ScreenRouter.test.tsx` returns ≥ 1 hit (fixture extension).
    - `grep -n "returningFromAppearance" src/app/ScreenRouter.test.tsx` returns ≥ 1 hit (fixture extension).
    - `grep -n "showBanner" src/app/ScreenRouter.test.tsx` returns 0 hits (Pitfall 5 cleanup verified).
    - `grep -n "onDismiss" src/app/ScreenRouter.test.tsx` returns 0 hits (Pitfall 5 cleanup verified).
    - `npm run test:run -- src/app/ScreenRouter.test.tsx` exits 0; all per-case tests including the new appearance-case test green.
    - `npm run test:run -- src/content/content.no-removed-keys.test.ts` exits 0 (the drift-guard's J18.4 entry stays green now that the fixture doesn't reference `showBanner`/`onDismiss`).
  </acceptance_criteria>
  <done>ScreenRouter has a 4th case for `'appearance'`; AppSettings case forwards `onAppearanceOpen` + `returningFromAppearance`; ScreenRouter.test fixture includes the three new dialogs fields and is purged of the stale install fields; the new appearance-case test passes; the drift-guard for removed keys remains green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 04-06: Human visual verification — Light + Dark themes, mobile + desktop viewports (APPEAR-06)</name>
  <files>(verification-only — no files written by the executor; operator inspects running app)</files>
  <action>
    The executor pauses here. The operator runs the dev server, opens the URLs and viewports listed under `<how-to-verify>`, walks through the 14 confirmation steps, and either replies with `approved` to finalize Phase 48 or with iteration notes that the executor uses to refine Tasks 04-01 through 04-05 (or, for string regressions, Plan 01). The executor MUST NOT auto-advance through this checkpoint — APPEAR-06 visual chrome verification has no automated assertion possible and `[[project_dark_theme_token_collapse]]` + `[[project_v16_visual_locks]]` require operator eyes on the rendered design in both themes + viewports + locales before sign-off.
  </action>
  <what-built>
    A complete end-to-end Appearance flow: App Settings now shows a right-chevron in the trailing header slot; tapping it navigates to a new Appearance page with an Orb Style section (Orb + Ring cue segmented pickers) and a Visual section (Breathing effect + Switcher icons toggles); changes apply live across the app (orb variant changes in practice surface, switcher labels update, ring cue swaps, breathing effect changes idle behaviour); the back-chevron returns to App Settings with focus restored to the source right-chevron. PT-BR strings ship as drafts with `// TODO: native-speaker review` markers (operator pass deferred to I18N-04).
  </what-built>
  <how-to-verify>
    1. Run `npm run dev` and open the dev URL in a desktop browser at default width.
    2. Open App Settings via the existing Settings icon. CONFIRM: the section previously labeled `Appearance` is now labeled `Theme`. CONFIRM: a `>` right-chevron is visible in the header trailing slot.
    3. Tap the right-chevron. CONFIRM: navigation to the Appearance page; header reads `Appearance`; back-chevron labeled `Back to Settings` is present in the leading slot; two sections render with headers `Orb Style` and `Visual`.
    4. CONFIRM: Orb Style card contains two pickers labeled `Orb` (with options `Halo`, `Minimal`, `Kuthasta`) and `Ring cue` (with options `Arc`, `Rings`). Visual card contains two toggle rows labeled `Breathing effect` and `Switcher icons`.
    5. Tap `Kuthasta` on the Orb picker. CONFIRM: no error. Navigate back to practice (or have a second tab on practice) and CONFIRM: the practice orb is now the Kuthasta variant; refresh the page and CONFIRM the orb is still Kuthasta (persistence).
    6. Tap `Rings` on the Ring cue picker. CONFIRM the ring-cue visualization changes accordingly on the practice surface.
    7. Toggle `Breathing effect` on/off. CONFIRM the idle orb behaviour changes on the practice surface (still vs ambient).
    8. Toggle `Switcher icons` on/off. CONFIRM the bottom practice switcher chrome changes between text-only and icon+label.
    9. Tap the back-chevron on Appearance. CONFIRM: navigation back to App Settings AND keyboard focus visibly lands on the right-chevron (the source), not the back-chevron. Use Tab to confirm focus position if the visual indicator is subtle.
    10. From App Settings, hit the back-chevron back to Practice. Re-enter Settings via the practice Settings icon. CONFIRM: focus is on the BACK-chevron (fresh entry, sentinel cleared per the design).
    11. SWITCH TO DARK THEME via the existing Theme picker in App Settings → Theme section. Navigate to Appearance. CONFIRM: both section cards render with visible 1px `border-soft` borders, the segmented controls and toggles read as discrete surfaces, no chrome looks flat or invisible (this is the `[[project_dark_theme_token_collapse]]` verification — UI-SPEC §Color "Dark-theme verification (mandatory)").
    12. RESIZE viewport to mobile width (< 640px) — use browser devtools responsive mode. CONFIRM: page chrome adapts (full-width content), safe-area inset is preserved, no horizontal scroll, no clipped chevrons. Test both Light and Dark themes at mobile width.
    13. Switch locale to `Português` via the Language picker in App Settings. Re-enter Appearance. CONFIRM: page title shows `Aparência`, sections show `Estilo do orbe` / `Visual`, pickers/toggles show their PT-BR drafts (`Orbe`, `Sinal do anel`, `Efeito de respiração`, `Ícones do alternador`, etc.). The strings are drafts (no operator pass yet) — visual quality is what matters here, not native-speaker accuracy.
    14. Sanity-check the test suite: `npm run test:run` exits 0; `npm run build` succeeds; `npm run lint` is clean.

    Issues to call out if observed:
    - Any spike-010 token regression (different shades, missing borders, off-spec spacing).
    - Any focus-restoration failure (back from Appearance lands on the wrong button).
    - Any picker click that does NOT live-update the practice surface.
    - Any string that reads wrong in either locale.
    - Any console error or warning.
  </how-to-verify>
  <resume-signal>Type `approved` if every confirmation in steps 1-14 holds. Otherwise describe each issue precisely (which step, what observed, screenshot/console output if helpful) so the next iteration can address it.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user gesture on chevron/pickers/toggles → React state + persistence | User clicks/keystrokes are handled by typed React event handlers; all writes go through the typed Phase 47 choice-hook setters; no direct envelope writes. |
| `vm.dialogs.*` callbacks → `useAppNavigation` state transitions | The callbacks are produced by `createAppDialogsViewModel` (Plan 02) from the `AppNavigation` hook; no user input value flows in. |
| string catalog → JSX render | Strings are static `UI_STRINGS` literals; React 19 auto-escapes during interpolation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-48-04-01 | Tampering | AppearancePage exposing an additional persistence write surface | mitigate | All four flag writes go through the Phase 47 choice hooks; `AppearancePage` itself does not call `savePrefs`/`loadPrefs`/`dispatchEvent` (verified by `grep -n "savePrefs\\|loadPrefs\\|dispatchEvent" src/app/pages/AppearancePage.tsx` = 0 hits in acceptance criteria). The choice hooks remain the single legitimate write surface. |
| T-48-04-02 | Information Disclosure | Right-chevron aria-label `Appearance settings` revealing destination | accept | Standard UX disclosure — accessibility tools announce destinations by design. No sensitive data leaks (the destination is a public preference page). |
| T-48-04-03 | Spoofing | Inline ad-hoc `dispatchEvent('hrv:prefs-changed', ...)` on the page | mitigate | Acceptance criteria forbid `dispatchEvent` references in `AppearancePage.tsx`; the two toggles call the typed Phase 47 hook setters directly. Same Pitfall-3 mitigation as Plan 03. |
| T-48-04-04 | Denial of Service | Focus-restoration sentinel could land on a stale button reference if the component re-mounts mid-transition | mitigate | The `chevronButtonRef` is declared inside `AppSettingsPage`'s component scope (re-created on each mount); the `useEffect` reads its current value on the same mount cycle. The `[returningFromAppearance]` dependency array ensures the effect re-runs if the sentinel changes (e.g., a session-start while on AppSettings clears it and re-runs to focus back-chevron). RESEARCH §Pitfall 4 — sentinel-clearing in every non-back transition (handled in Plan 02) ensures the sentinel cannot stick. |
| T-48-04-05 | Repudiation | Drift-guard `content.no-removed-keys.test.ts` could be silently bypassed if the ScreenRouter fixture re-introduces `showBanner`/`onDismiss` | mitigate | Pitfall 5 cleanup is enforced by acceptance criteria (`grep -n showBanner|onDismiss src/app/ScreenRouter.test.tsx` = 0 hits) AND by the drift-guard test itself (the J18.4 entries at `content.no-removed-keys.test.ts:73` actively scan for these field references). |
| T-48-04-06 | Information Disclosure | PT-BR draft strings shipping with markers visible in source | accept | Open-source repo; comments are public by design. The drift-guard from Plan 01 keeps the markers structurally scoped to `appearance.*` keys. |
| T-48-04-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan; all in-tree primitives are first-party (UI-SPEC §Registry Safety — `none` for shadcn / third-party registries). |
</threat_model>

<verification>
- `npm run test:run` — full suite green (including the new tests in this plan + Plan 01/02/03 outputs).
- `npm run build` — typecheck green end-to-end (the cross-file contracts from Plans 02/04 now bind together).
- `npm run lint` — clean.
- Human verification per Task 04-06 — Light/Dark themes, mobile/desktop viewports, locale switch.
- `grep -rn "appSettings\\.sections\\.appearance" src/` — 0 hits (rename complete).
- `grep -rn "dispatchEvent" src/components/OrbPicker.tsx src/components/RingCuePicker.tsx src/app/pages/AppearancePage.tsx` — 0 hits (Pitfall 3 invariant holds across this plan + Plan 03).
</verification>

<success_criteria>
1. `AppearancePage` renders title `Appearance`, back-chevron with label `Back to Settings`, two sections (`Orb Style`, `Visual`), two pickers (`OrbPicker`, `RingCuePicker`), two toggles (`Breathing effect`, `Switcher icons`).
2. `AppSettingsPage` shows a right-chevron in the trailing slot; clicking it invokes `onAppearanceOpen`; the section previously named `Appearance` is now `Theme`.
3. `AppearancePage` mount-focuses the back-chevron; `AppSettingsPage` conditionally mount-focuses the chevron (sentinel=true) or back-chevron (sentinel=false).
4. `ScreenRouter` dispatches `<AppearancePage>` on `appScreen='appearance'` and forwards the new props to `<AppSettingsPage>` on `appScreen='appSettings'`.
5. Picker clicks and toggle changes propagate live to the practice surface via the Phase 47 hook contract; persistence holds across page reload.
6. Tests for `AppearancePage` (9+ cases), `AppSettingsPage` (3 new focus/chevron cases), `ScreenRouter` (4th-case test) all green; `content.no-removed-keys` test stays green after the Pitfall-5 fixture cleanup.
7. Human verification (Task 04-06) signs off Light + Dark + mobile + desktop + locale-switched rendering.
</success_criteria>

<output>
Create `.planning/phases/48-appearance-page-i18n/48-04-SUMMARY.md` when done.
</output>
