---
phase: 33-close-gap-practice-02-resonant-settings-read-write-split-brain
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/App.tsx
  - src/storage/settings.ts
  - src/storage/settings.test.ts
  - src/storage/stats.test.ts
  - src/app/App.persistence.test.tsx
autonomous: true
requirements: [PRACTICE-02]

must_haves:
  truths:
    - "A fresh-v2 user's saved resonant BPM/ratio/duration/mode survive a page reload"
    - "A v1-migrated user with a stale flat env.settings sees the practices.resonant.settings value after reload, not the stale flat value"
    - "loadSettings and saveSettings no longer exist anywhere in src/"
    - "The full test suite passes with no test importing a deleted symbol"
  artifacts:
    - path: "src/app/App.tsx"
      provides: "Resonant settings read seeded from the per-practice envelope"
      contains: "initialPractices.resonant.settings"
    - path: "src/storage/settings.ts"
      provides: "Mute storage + coerceSettings only; loadSettings/saveSettings removed"
      contains: "loadMute"
    - path: "src/app/App.persistence.test.tsx"
      provides: "D-05 regression tests proving resonant settings survive remount"
      contains: "PRACTICE-02"
  key_links:
    - from: "src/app/App.tsx:110 initialSettings"
      to: "initialPractices.resonant.settings (from loadPractices)"
      via: "useMemo seed"
      pattern: "initialSettings.*initialPractices\\.resonant\\.settings"
    - from: "src/app/App.tsx persistedSetSettings"
      to: "practices.resonant.settings"
      via: "saveResonantSettings (unchanged write path)"
      pattern: "saveResonantSettings"
---

<objective>
Restore resonant-settings persistence across page reloads (requirement PRACTICE-02).

The Phase 31 CR-01 fix moved the resonant-settings **write** path to
`saveResonantSettings()` → `practices.resonant.settings`, but left the **read**
path on the abandoned flat `env.settings` field. `App.tsx:110` calls
`loadSettings()` (reads flat `env.settings`, never written after Phase 31), so
resonant BPM / ratio / duration / mode revert on every reload.

Purpose: Re-deliver PRACTICE-02 — a milestone-audit BLOCKER. The app's primary
practice currently loses its settings on every reload.

Output:
- `App.tsx:110` reads resonant settings from the per-practice envelope.
- Dead `loadSettings`/`saveSettings` removed from `src/storage/settings.ts`.
- Dependent test files reworked off the deleted symbols.
- Regression tests in `App.persistence.test.tsx` covering both revert scenarios.
</objective>

<execution_context>
@$HOME/.claude-personal-personal/get-shit-done/workflows/execute-plan.md
@$HOME/.claude-personal-personal/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/v1.5-MILESTONE-AUDIT.md
@.planning/phases/33-close-gap-practice-02-resonant-settings-read-write-split-bra/33-CONTEXT.md
@.planning/phases/33-close-gap-practice-02-resonant-settings-read-write-split-bra/33-PATTERNS.md

<interfaces>
<!-- The correct per-practice storage API. Use directly — no exploration needed. -->

From src/storage/practices.ts:
```typescript
export function loadPractices(deps?: StorageDeps): { resonant: { settings: SessionSettings; stats: PersistedStats }; naviKriya: { settings: NaviKriyaSettings; stats: PersistedStats } };
export function saveResonantSettings(settings: SessionSettings, deps?: StorageDeps): void;
```

From src/storage/settings.ts (post-change surface):
```typescript
export function coerceSettings(raw: unknown): SessionSettings;  // KEEP — used by practices coercer
export function coerceMute(raw: unknown): boolean;              // KEEP
export function loadMute(deps?: StorageDeps): boolean;          // KEEP
export function saveMute(muted: boolean, deps?: StorageDeps): void;  // KEEP
// loadSettings — REMOVED by this plan
// saveSettings — REMOVED by this plan
```

App.tsx mount reads (lines 108-116), useMemo([])-once synchronous pattern — preserve:
```typescript
const initialSettings  = useMemo<SessionSettings>(() => ..., [...])  // SOURCE changes, timing does not
const initialMute      = useMemo<boolean>(() => loadMute(), [])
const initialPractices = useMemo(() => loadPractices(), [])          // already present — the source to reuse
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Retarget the resonant-settings read path to the per-practice envelope</name>
  <files>src/app/App.tsx</files>
  <read_first>
    - src/app/App.tsx (lines 45-160 — the import block at 49-68 and the mount-time useMemo reads at 108-150, including the NK seed analog at 144-146)
    - src/storage/practices.ts (loadPractices return shape, saveResonantSettings)
    - .planning/phases/33-close-gap-practice-02-resonant-settings-read-write-split-bra/33-PATTERNS.md (App.tsx section — the exact fix pattern)
  </read_first>
  <behavior>
    - On mount with a v2 envelope whose practices.resonant.settings is non-default, the session engine is seeded with that saved value (not DEFAULT_SETTINGS).
    - On mount with a v2 envelope carrying BOTH a stale flat env.settings AND a newer practices.resonant.settings, the engine is seeded from practices.resonant.settings.
    - The write path (persistedSetSettings → saveResonantSettings) is unchanged and still targets practices.resonant.settings.
  </behavior>
  <action>
    Change `App.tsx:110` so `initialSettings` is seeded from the per-practice
    envelope instead of the flat field. Currently:
    `const initialSettings = useMemo<SessionSettings>(() => loadSettings(), [])`.
    Replace the body to derive resonant settings from `initialPractices`
    (already computed at line ~115 via `loadPractices()`): seed it as
    `useMemo<SessionSettings>(() => initialPractices.resonant.settings, [initialPractices])`.
    This mirrors the established Phase 31 NK symmetry — NK settings seed from
    `initialPractices.naviKriya.settings` at line ~145. Note `initialPractices`
    is declared below line 110; if a JS use-before-declaration ordering issue
    arises, either move the `initialPractices` memo above line 110 or use the
    equivalent `useMemo(() => loadPractices().resonant.settings, [])` form —
    `useMemo([])` runs synchronously so both are correct; prefer reusing the
    existing `initialPractices` memo to avoid a second `loadPractices()` call.
    Update the comment on line ~108 to reflect that the source is now the
    per-practice envelope (per D-01).
    Remove `loadSettings` from the storage import block (line ~50) — keep
    `loadMute`, `loadPractices`, `saveResonantSettings`, and all other current
    imports. Do not touch `persistedSetSettings` / `saveResonantSettings`
    (the write path at lines ~411-417 stays as-is).
  </action>
  <verify>
    <automated>grep -n "initialPractices.resonant.settings" src/app/App.tsx && ! grep -q "loadSettings" src/app/App.tsx && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `src/app/App.tsx` contains the string `initialPractices.resonant.settings` in the `initialSettings` memo.
    - `grep -c 'loadSettings' src/app/App.tsx` returns 0 (import and call both gone).
    - `npx tsc --noEmit` reports no type errors.
    - The `persistedSetSettings` callback still calls `saveResonantSettings` (write path unchanged).
  </acceptance_criteria>
  <done>App.tsx seeds resonant settings from the per-practice envelope; no reference to loadSettings remains; project type-checks clean.</done>
</task>

<task type="auto">
  <name>Task 2: Remove dead loadSettings/saveSettings and rework dependent tests</name>
  <files>src/storage/settings.ts, src/storage/settings.test.ts, src/storage/stats.test.ts</files>
  <read_first>
    - src/storage/settings.ts (current full file — confirm exact line ranges of loadSettings/saveSettings vs. coerceSettings/loadMute/saveMute)
    - src/storage/settings.test.ts (the `loadSettings / saveSettings round-trip` describe block and the `loadMute / saveMute round-trip` block; the import block)
    - src/storage/stats.test.ts (the import line and the `resetStats` describe block that uses saveSettings/loadSettings)
    - src/storage/practices.ts (saveResonantSettings, loadPractices — the replacement seeding API)
    - src/storage/index.ts (barrel — confirm `export * from './settings'` is a wildcard)
  </read_first>
  <action>
    Per D-02/D-03: delete `loadSettings` and `saveSettings` from
    `src/storage/settings.ts`. Keep `coerceSettings` (consumed by
    `coercePracticeSlice` in practices.ts), `coerceMute`, `loadMute`, and
    `saveMute` unchanged.

    `src/storage/index.ts`: no edit needed — the `export * from './settings'`
    wildcard automatically stops re-exporting the deleted symbols. Confirm by
    grep after deletion.

    `src/storage/settings.test.ts`: delete the entire
    `loadSettings / saveSettings round-trip` describe block. In the surviving
    `loadMute / saveMute round-trip` block, the "preserves settings + stats
    fields" test seeds via `saveSettings(...)` — reseed it instead via
    `saveResonantSettings(...)` imported from `./practices` (or a direct
    `localStorage.setItem` of a v2 envelope), and assert the resonant settings
    via the practices subtree rather than flat `env.settings`. Prune
    `loadSettings`/`saveSettings` from the import block; keep `coerceSettings`,
    `coerceMute`, `loadMute`, `saveMute`.

    `src/storage/stats.test.ts`: change the import line from
    `import { saveSettings, saveMute, loadSettings, loadMute } from './settings'`
    to `import { saveMute, loadMute } from './settings'` plus
    `import { saveResonantSettings, loadPractices } from './practices'`.
    In the `resetStats` describe block, replace `saveSettings(savedSettings)`
    with `saveResonantSettings(savedSettings)` and replace
    `expect(loadSettings()).toEqual(savedSettings)` with
    `expect(loadPractices().resonant.settings).toEqual(savedSettings)`.
    Apply the same substitution to every other `saveSettings`/`loadSettings`
    call in this file (e.g. the line-112 `saveSettings(DEFAULT_SETTINGS)`).
    Envelope co-existence coverage (settings + stats surviving together) must
    remain — keep it expressed against the per-practice envelope.

    Leave the stale flat `env.settings` field in localStorage untouched (D-04 —
    no pruning logic).
  </action>
  <verify>
    <automated>! grep -rn "loadSettings\|saveSettings" src/ && npx vitest run src/storage/settings.test.ts src/storage/stats.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn 'loadSettings\|saveSettings' src/` returns no matches (zero callers, zero definitions).
    - `src/storage/settings.ts` still exports `coerceSettings`, `coerceMute`, `loadMute`, `saveMute`.
    - `npx vitest run src/storage/settings.test.ts src/storage/stats.test.ts` passes with no import/resolution errors.
    - `src/storage/stats.test.ts` still asserts settings + stats co-existence, expressed via `loadPractices().resonant.settings`.
    - `src/storage/index.ts` is unchanged (wildcard re-export needs no edit).
  </acceptance_criteria>
  <done>loadSettings/saveSettings deleted; no src/ file references them; storage test files pass against the per-practice envelope API.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add D-05 regression tests — resonant settings survive remount</name>
  <files>src/app/App.persistence.test.tsx</files>
  <read_first>
    - src/app/App.persistence.test.tsx (full file — the `seedEnvelope` helper at lines 26-56, the subtree extractors `resonantSettingsOf`, the LOCL-01 restoration tests at lines 89-141, and the `beforeEach`/`afterEach` fake-timer setup at lines 76-84)
    - src/storage/storage.ts (STATE_KEY, migrateEnvelope — confirm version: 2 envelopes skip the v1→v2 ladder)
    - .planning/phases/33-close-gap-practice-02-resonant-settings-read-write-split-bra/33-PATTERNS.md (the App.persistence.test.tsx section — exact test bodies and the seedV2Envelope helper shape)
  </read_first>
  <behavior>
    - Test 1 (fresh-v2 user): a v2 envelope with practices.resonant.settings = non-default BPM/ratio/duration and NO flat env.settings — after `render(<App />)`, the saved values render on screen.
    - Test 2 (v1-migrated user): a v2 envelope with a stale flat env.settings (e.g. BPM 6) AND a newer practices.resonant.settings (e.g. BPM 4) — after `render(<App />)`, the practices-subtree value (4 BPM) renders and the stale flat value (6 BPM) does not.
  </behavior>
  <action>
    Per D-05/D-06: add a new `describe('PRACTICE-02 — resonant settings survive
    remount', ...)` block to `src/app/App.persistence.test.tsx`.

    Add a `seedV2Envelope(resonantSettings)` helper alongside the existing
    `seedEnvelope` helper: it writes a `version: 2` envelope to `STATE_KEY`
    with `practices.resonant.settings = resonantSettings`,
    `practices.resonant.stats = null`, `practices.naviKriya = { settings: null,
    stats: null }`, `activePractice: 'resonant'`, and NO flat `settings` field.
    `version: 2` ensures `migrateEnvelope` skips the v1→v2 ladder so the test
    exercises the post-fix read path directly.

    Test 1 — fresh-v2 user: call `seedV2Envelope({ bpm: 4, ratio: '50:50',
    durationMinutes: 5 })`, `render(<App />)`, then assert the saved values are
    visible on screen (match the on-screen BPM / ratio / duration display the
    way the existing LOCL-01 tests do — reuse their exact query strategy).

    Test 2 — v1-migrated user: `localStorage.setItem(STATE_KEY, ...)` directly
    with a `version: 2` envelope carrying BOTH a stale flat `settings`
    (`{ bpm: 6, ratio: '40:60', durationMinutes: 10 }`) AND a newer
    `practices.resonant.settings` (`{ bpm: 4, ratio: '50:50',
    durationMinutes: 5 }`). `render(<App />)`, then assert the 4 BPM value is
    present and the 6 BPM value is absent (`queryByText(...).not.toBeInTheDocument()`).

    Both tests are mount-only assertions — no session is started, so no
    `startAndAdvancePastLeadIn` is needed; `render(<App />)` plus synchronous
    `screen` queries suffices (matching the LOCL-01 restoration tests). Reuse
    the existing `beforeEach`/`afterEach` fake-timer + localStorage-clear setup.
    Use real `SessionSettings`-typed fixture values so the test fails RED
    against the pre-Task-1 code and passes GREEN after it.
  </action>
  <verify>
    <automated>npx vitest run src/app/App.persistence.test.tsx -t "PRACTICE-02"</automated>
  </verify>
  <acceptance_criteria>
    - `src/app/App.persistence.test.tsx` contains a `describe` titled with `PRACTICE-02`.
    - The file defines a `seedV2Envelope` helper that writes a `version: 2` envelope with no flat `settings` field.
    - The fresh-v2 test asserts the seeded BPM/ratio/duration are visible after `render(<App />)`.
    - The v1-migrated test asserts the practices-subtree value is shown and the stale flat value is not.
    - `npx vitest run src/app/App.persistence.test.tsx -t "PRACTICE-02"` passes; both tests would FAIL if Task 1's read-path change were reverted.
  </acceptance_criteria>
  <done>Two regression tests prove resonant settings survive a remount for both fresh-v2 and v1-migrated users; the suite is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| localStorage → App mount | Persisted prefs envelope (untrusted: user-editable / cross-tab-writable) is read at mount |

This phase introduces NO new trust boundary. It changes the *source field*
within the same already-trusted-boundary read (one localStorage envelope read
at mount). No new network path, no new auth surface, no new user-input field.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-01 | Tampering | `practices.resonant.settings` read at App.tsx:110 | mitigate | The read flows through `loadPractices` → `coercePractices` → `coercePracticeSlice` → `coerceSettings`, which per-field validates and falls back to defaults for any drifted/malicious value. No raw localStorage value reaches the session engine unvalidated. Existing guard — unchanged. |
| T-33-02 | Denial of Service | Malformed v2 envelope JSON in localStorage | accept | `readEnvelope` already null-guards and `coerceSettings` returns `DEFAULT_SETTINGS` for non-object input; worst case the user sees default settings, no crash. Pre-existing behavior; not in scope to harden further. |
| T-33-03 | Information Disclosure | Orphan flat `env.settings` left in localStorage (D-04) | accept | The orphan field holds only breathing-pace numbers (BPM/ratio/duration) — no PII, no secrets. A few stale bytes; zero disclosure value. Pruning deliberately skipped per D-04. |

No high-severity threats. Existing localStorage coercion guards remain the
mitigation; this phase neither adds nor weakens them.
</threat_model>

<verification>
- `npx tsc --noEmit` — clean (no type errors after the read-path retarget and symbol removal).
- `grep -rn "loadSettings\|saveSettings" src/` — zero matches.
- `npx vitest run` — full suite green, including the two new PRACTICE-02 regression tests.
- `npm run build` — production build succeeds.
- Manual sanity (optional): change a resonant BPM in the running app, reload the page, confirm the changed BPM persists.
</verification>

<success_criteria>
- A fresh-v2 user's resonant BPM/ratio/duration/mode survive a page reload (PRACTICE-02 restored).
- A v1-migrated user sees `practices.resonant.settings` after reload, not the stale flat `env.settings`.
- `loadSettings` and `saveSettings` no longer exist anywhere in `src/`.
- No test file imports a deleted symbol; envelope co-existence coverage remains.
- Full test suite and production build pass.
</success_criteria>

<output>
After completion, create `.planning/phases/33-close-gap-practice-02-resonant-settings-read-write-split-bra/33-01-SUMMARY.md`
</output>
