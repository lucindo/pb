---
phase: 29-settings-install-entry-localization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/IosInstallSteps.tsx
  - src/components/IosInstallSteps.test.tsx
  - src/components/InstallBanner.tsx
  - src/content/strings.ts
autonomous: true
requirements: [INSTALL-06, INSTALL-07]

must_haves:
  truths:
    - "The iOS Share-to-Home-Screen step list renders from a single shared component used by both InstallBanner and (later) the Settings install row (D-06)"
    - "InstallBanner still renders the iOS steps with no visual or behavioral change after extraction (D-06)"
    - "UiStrings.install exposes a settingsLabel key with EN and PT-BR values (D-03)"
  artifacts:
    - path: "src/components/IosInstallSteps.tsx"
      provides: "Shared iOS install steps component (numbered ol + IOsShareIcon) accepting id + strings props"
      exports: ["IosInstallSteps", "IosInstallStepsProps"]
      min_lines: 30
    - path: "src/components/IosInstallSteps.test.tsx"
      provides: "Unit tests for the shared iOS steps component"
    - path: "src/components/InstallBanner.tsx"
      provides: "InstallBanner refactored to delegate iOS steps to IosInstallSteps"
      contains: "IosInstallSteps"
    - path: "src/content/strings.ts"
      provides: "UiStrings.install interface + both locale entries extended with settingsLabel"
      contains: "settingsLabel"
  key_links:
    - from: "src/components/InstallBanner.tsx"
      to: "src/components/IosInstallSteps.tsx"
      via: "import + render with id=\"install-ios-steps\""
      pattern: "IosInstallSteps"
    - from: "src/content/strings.ts UiStrings.install interface"
      to: "en.install + pt-BR.install locale entries"
      via: "as const satisfies exhaustiveness check"
      pattern: "settingsLabel"
---

<objective>
Extract the iOS install-steps block out of `InstallBanner.tsx` into a new shared `IosInstallSteps` component (CONTEXT D-06), refactor `InstallBanner` to consume it, and extend the `UiStrings.install` string catalog with the new `settingsLabel` key (CONTEXT D-03) so the Phase 29 Settings install row (Plan 02) can mount against a single source of truth.

Purpose: D-06 requires one source of truth for the iOS steps so the banner and the Settings entry never drift apart. This is interface-first work — Plan 02 imports the component and the new string key created here.

Output: New `IosInstallSteps.tsx` + `IosInstallSteps.test.tsx`, a refactored `InstallBanner.tsx` with no behavior change, and `strings.ts` extended with `settingsLabel` in both locales.
</objective>

<execution_context>
@$HOME/.claude-personal-personal/get-shit-done/workflows/execute-plan.md
@$HOME/.claude-personal-personal/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-settings-install-entry-localization/29-CONTEXT.md
@.planning/phases/29-settings-install-entry-localization/29-RESEARCH.md
@.planning/phases/29-settings-install-entry-localization/29-PATTERNS.md
@.planning/phases/29-settings-install-entry-localization/29-UI-SPEC.md

<interfaces>
<!-- Current UiStrings.install interface — src/content/strings.ts (8 fields). -->
<!-- Phase 29 adds exactly one key: settingsLabel. -->

From src/content/strings.ts (UiStrings.install interface):
```typescript
readonly install: {
  readonly regionLabel: string
  readonly bannerText: string
  readonly installButton: string
  readonly iosStepsButton: string
  readonly dismiss: string
  readonly iosStep1: string
  readonly iosStep2: string
  readonly iosStep3: string
}
```

From src/components/InstallBanner.tsx (component props — unchanged by this plan):
```typescript
export interface InstallBannerProps {
  isIOS: boolean
  onInstall(this: void): Promise<void>
  onDismiss(this: void): void
  strings: UiStrings['install']
}
```

The iOS steps block to extract is InstallBanner.tsx lines 75-89 (the
`{isIOS && iosExpanded && (...)}` block) and the `IOsShareIcon` helper
function at lines 96-117.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create the shared IosInstallSteps component</name>
  <files>src/components/IosInstallSteps.tsx</files>
  <read_first>
    - CONTEXT D-06: the iOS step content (numbered Share → Add to Home Screen steps + inline Share glyph) is extracted into a shared component that both InstallBanner and the Settings entry render — one source of truth.
    - src/components/InstallBanner.tsx (lines 74-117 — the iOS steps block and IOsShareIcon function being extracted)
    - src/content/strings.ts (UiStrings.install interface — the strings shape consumed)
    - .planning/phases/29-settings-install-entry-localization/29-PATTERNS.md (section "src/components/IosInstallSteps.tsx" — exact extraction pattern)
    - .planning/phases/29-settings-install-entry-localization/29-UI-SPEC.md (section "IosInstallSteps Component Contract")
  </read_first>
  <behavior>
    - Renders an outer `<div>` whose `id` attribute equals the `id` prop value (no hardcoded id — Pitfall 3 mitigation).
    - Outer div carries `aria-live="polite"` and className `pt-4 text-sm leading-6`.
    - Renders an `<ol className="list-decimal pl-5">` with exactly three `<li>` items: step 1 text + inline Share glyph, step 2 text, step 3 text.
    - First `<li>` carries className `text-[var(--color-breathing-accent-strong)]` and renders `strings.iosStep1` followed by a space and the iOS Share SVG.
    - The Share SVG is `aria-hidden="true"`.
  </behavior>
  <action>
    Per CONTEXT D-06, create `src/components/IosInstallSteps.tsx` by extracting the iOS steps markup verbatim from `InstallBanner.tsx` lines 74-89. Define and export `IosInstallStepsProps` with two fields: `id: string` and `strings: UiStrings['install']` (import `UiStrings` as a type from `../content/strings`, mirroring InstallBanner.tsx line 8). Export a named function component `IosInstallSteps({ id, strings }: IosInstallStepsProps)`.

    The component returns the outer `<div id={id} aria-live="polite" className="pt-4 text-sm leading-6">` containing the `<ol className="list-decimal pl-5">` with three `<li>` items. First `<li>`: `className="text-[var(--color-breathing-accent-strong)]"`, content `{strings.iosStep1}{' '}<IOsShareIcon />`. Second `<li>`: `{strings.iosStep2}`. Third `<li>`: `{strings.iosStep3}`. The `id` MUST come from the prop — do NOT hardcode `"install-ios-steps"`.

    Move the `IOsShareIcon` helper function verbatim from `InstallBanner.tsx` lines 96-117 into this file as an internal (non-exported) function below the component. Keep its inline-SVG convention comment. This co-location avoids the circular-dependency risk noted in RESEARCH.md Pattern 2.

    Do not add `useState` — the `iosExpanded` toggle stays in each consumer, not in this presentational component.
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.app.json</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/IosInstallSteps.tsx` exists and exports a named `IosInstallSteps` function and an `IosInstallStepsProps` interface.
    - `grep -c 'install-ios-steps' src/components/IosInstallSteps.tsx` returns 0 (no hardcoded id literal in the shared component).
    - The file contains the `IOsShareIcon` function and an `<svg ... aria-hidden="true">` element.
    - `npx tsc --noEmit -p tsconfig.app.json` exits 0.
  </acceptance_criteria>
  <done>IosInstallSteps.tsx exists, exports the component + props type, takes `id` as a prop, and type-checks clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Write IosInstallSteps unit tests and refactor InstallBanner to consume the shared component</name>
  <files>src/components/IosInstallSteps.test.tsx, src/components/InstallBanner.tsx</files>
  <read_first>
    - CONTEXT D-06: InstallBanner must delegate its iOS steps to the shared IosInstallSteps component with no behavior drift after extraction.
    - src/components/IosInstallSteps.tsx (the component created in Task 1)
    - src/components/InstallBanner.tsx (the file being refactored — full file)
    - src/components/InstallBanner.test.tsx (existing test patterns and render helper to mirror)
    - .planning/phases/29-settings-install-entry-localization/29-PATTERNS.md (sections "src/components/IosInstallSteps.test.tsx" and "src/components/InstallBanner.tsx (edit ...)")
  </read_first>
  <behavior>
    - IosInstallSteps test: renders all three step texts (`UI_STRINGS.en.install.iosStep1/2/3`) into the document.
    - IosInstallSteps test: the container `<div>` uses the exact `id` value passed via the `id` prop (assert with `container.querySelector('#settings-ios-steps')` when `id="settings-ios-steps"` is passed).
    - IosInstallSteps test: an `svg[aria-hidden="true"]` element is present (the Share glyph).
    - InstallBanner regression: with `isIOS={true}`, clicking the "How to install" toggle still expands and shows `iosStep1` text — behavior unchanged after extraction.
  </behavior>
  <action>
    Create `src/components/IosInstallSteps.test.tsx` mirroring `InstallBanner.test.tsx` header and imports: import `'@testing-library/jest-dom/vitest'`, `{ render, screen }` from `@testing-library/react`, `{ describe, expect, it }` from `vitest`, `{ IosInstallSteps }` from `./IosInstallSteps`, and `{ UI_STRINGS }` from `../content/strings`. Add a `renderSteps(id = 'test-ios-steps')` helper that renders `<IosInstallSteps id={id} strings={UI_STRINGS.en.install} />`. Write a `describe('IosInstallSteps', ...)` block with three `it` cases covering the behaviors above: all three step texts present; container div uses the provided `id` prop (call `renderSteps('settings-ios-steps')` and assert `container.querySelector('#settings-ios-steps')` is non-null); an `svg[aria-hidden="true"]` is present.

    Per CONTEXT D-06, refactor `src/components/InstallBanner.tsx`: add `import { IosInstallSteps } from './IosInstallSteps'` after the existing imports. Replace the inline iOS steps block (lines 75-89, the `{isIOS && iosExpanded && (<div id="install-ios-steps" ...>...</div>)}`) with `{isIOS && iosExpanded && (<IosInstallSteps id="install-ios-steps" strings={strings} />)}`. Delete the now-unused `IOsShareIcon` function (lines 96-117) from InstallBanner.tsx — it lives in IosInstallSteps.tsx now. Keep `aria-controls="install-ios-steps"` on the iOS toggle button unchanged; it must match the `id` passed to IosInstallSteps. Leave `DismissIcon`, the banner row, the `iosExpanded` state, and all other InstallBanner code untouched.
  </action>
  <verify>
    <automated>npm test -- --run src/components/IosInstallSteps.test.tsx src/components/InstallBanner.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `npm test -- --run src/components/IosInstallSteps.test.tsx src/components/InstallBanner.test.tsx` passes with zero failures.
    - `grep -c 'IOsShareIcon' src/components/InstallBanner.tsx` returns 0 (helper removed from InstallBanner).
    - `grep -c 'IosInstallSteps' src/components/InstallBanner.tsx` returns 2 or more (import line + render usage).
    - InstallBanner.tsx still contains `aria-controls="install-ios-steps"` and the render passes `id="install-ios-steps"` to IosInstallSteps.
    - `npx tsc --noEmit -p tsconfig.app.json` exits 0.
  </acceptance_criteria>
  <done>IosInstallSteps has passing unit tests; InstallBanner renders the shared component with no behavior regression and no duplicate IOsShareIcon.</done>
</task>

<task type="auto">
  <name>Task 3: Add settingsLabel key to UiStrings.install in both locales</name>
  <files>src/content/strings.ts</files>
  <read_first>
    - CONTEXT D-03: the Settings install row section label copy direction is "Install for offline use" — a benefit-describing, banner-style one-liner; final EN wording is locked here.
    - src/content/strings.ts (the `UiStrings.install` interface block, the `en.install` block, and the `pt-BR.install` block)
    - .planning/phases/29-settings-install-entry-localization/29-PATTERNS.md (section "src/content/strings.ts" — exact key placement)
    - .planning/phases/29-settings-install-entry-localization/29-UI-SPEC.md (section "Strings Contract")
  </read_first>
  <action>
    Add exactly one new key, `settingsLabel: string`, to the `UiStrings.install` interface as `readonly settingsLabel: string` (place it last, after `iosStep3`). Per CONTEXT D-03, this is the benefit-describing section label for the Settings install row.

    Add `settingsLabel` to BOTH locale entries in the SAME edit (Pitfall 4: the `as const satisfies Readonly<Record<LocaleId, UiStrings>>` exhaustiveness check fails the build if either locale is missing the key):
    - `en.install`: `settingsLabel: 'Install for offline use'` (final EN copy per D-03).
    - `pt-BR.install`: `settingsLabel: 'Instalar para uso offline'` — this is a DRAFT value. Do NOT add a review marker in this plan; the PT-BR review-marker workflow and operator sign-off happen in Plan 02. Leave the existing `// DRAFT: Phase 29 will finalize PT-BR install copy` comment above the `pt-BR.install` block in place — Plan 02 replaces it with the review-marker workflow.

    Do not change any existing string values. Do not touch any other string block.
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.app.json && npm test -- --run src/components/InstallBanner.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'settingsLabel' src/content/strings.ts` returns 3 (interface declaration + en value + pt-BR value).
    - `grep -c "settingsLabel: 'Install for offline use'" src/content/strings.ts` returns 1.
    - `grep -c "settingsLabel: 'Instalar para uso offline'" src/content/strings.ts` returns 1.
    - `npx tsc --noEmit -p tsconfig.app.json` exits 0 (the `as const satisfies` check confirms both locales have the key).
    - `npm test -- --run src/components/InstallBanner.test.tsx` passes (no regression in string consumers).
  </acceptance_criteria>
  <done>`UiStrings.install` has a `settingsLabel` key; both `en` and `pt-BR` locales provide a value; the build type-checks clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → React render | The `beforeinstallprompt` event and `isIOS`/`isStandalone` detection are browser-controlled inputs; they reach the UI as plain booleans. This plan touches none of that — it is pure presentational extraction + a static string key. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-01 | Tampering | IosInstallSteps `id` prop | accept | `id` is a static string literal supplied by the parent component (`"install-ios-steps"` / `"settings-ios-steps"`), never user input. It is used only as a DOM `id`/`aria-controls` target — no injection surface. No external mutation path. |
| T-29-02 | Information disclosure | strings.ts `settingsLabel` | accept | Static localized UI copy; no PII, no secrets, no user data. Rendered as React text content (auto-escaped), not `dangerouslySetInnerHTML`. No XSS surface. |

No high-severity threats. This plan is UI-only composition with no server, no auth, no user-text input, and no new storage — same posture as Phase 28 (T-28-08).
</threat_model>

<verification>
- `npm test -- --run src/components/IosInstallSteps.test.tsx src/components/InstallBanner.test.tsx` — all green.
- `npx tsc --noEmit -p tsconfig.app.json` — exits 0.
- `npm test -- --run` — full suite green (no regression).
- Manual grep confirms `IOsShareIcon` no longer exists in `InstallBanner.tsx`.
</verification>

<success_criteria>
- `IosInstallSteps` component exists, is unit-tested, and takes `id` + `strings` props.
- `InstallBanner` renders `IosInstallSteps` with `id="install-ios-steps"`, no behavior change, no duplicate icon helper.
- `UiStrings.install.settingsLabel` exists in interface and both locale entries; build type-checks clean.
- Full test suite remains green.
</success_criteria>

<output>
After completion, create `.planning/phases/29-settings-install-entry-localization/29-01-SUMMARY.md`
</output>
</content>
</invoke>
