---
phase: 49-ios-speaker-route-fix
verified: 2026-05-27T15:55:00Z
status: passed
score: 5/5 must-haves verified (IOS-04 partial — operator-accepted deferral)
overrides_applied: 0
re_verification: null
gaps: []
deferred:
  - truth: "IOS-04 — Android Chrome arm (Scenario E) not run on real hardware"
    addressed_in: "Operator follow-up when Android device is available"
    evidence: "49-02-DEVICE-VALIDATION.md Scenario E marked 'deferred — no device'; 49-02-SUMMARY.md disposition row marks IOS-04 'partially verified — F desktop passes; E Android deferred (no device)'; operator signal 'approved'"
human_verification: []
---

# Phase 49: iOS speaker route fix — Verification Report

**Phase Goal:** Coerce iOS Safari audio session category from "ambient" to "playback" so cue audio routes through the device speaker when the silent switch is ON and no headphones are connected, via a silent-loop HTMLAudioElement wired into `createAudioEngine()` on the user-gesture chain. Must not regress: iOS-with-headphones, iOS-with-silent-OFF, non-iOS, or the existing Phase 5.1 'interrupted'/'needs-resume' background/lock-screen flow.

**Verified:** 2026-05-27T15:55:00Z
**Status:** passed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths (merged: ROADMAP success criteria + PLAN 01/02 must_haves)

| #  | Truth                                                                                                                                    | Status     | Evidence                                                                                                                                                                                                                                                          |
| -- | ---------------------------------------------------------------------------------------------------------------------------------------- | ---------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1  | iOS Safari, silent switch ON, no headphones → cue audio plays through device speaker (ROADMAP SC#1, IOS-01)                              | ✓ VERIFIED | `49-02-DEVICE-VALIDATION.md` Scenario A = `pass` on operator's iPhone. Required 3 PCM revisions (v1→v2→v3 16-bit -90 dBFS) to land an inaudible loop; final state on `c49765d` is approved on real hardware.                                                       |
| 2  | iOS Safari, headphones connected → audio continues to route through headphones (ROADMAP SC#2 part 1, IOS-02)                              | ✓ VERIFIED | `49-02-DEVICE-VALIDATION.md` Scenario B = `pass` ("Routes through headphones; no speaker bleed").                                                                                                                                                                  |
| 3  | iOS Safari, silent switch OFF → audio plays normally, no regression (ROADMAP SC#2 part 2, IOS-03)                                         | ✓ VERIFIED | `49-02-DEVICE-VALIDATION.md` Scenario C = `pass` ("Normal speaker playback; no regression").                                                                                                                                                                       |
| 4  | Non-iOS platforms (Android Chrome + ≥1 desktop browser) → no audible or measurable performance regression (ROADMAP SC#3, IOS-04)          | ◐ PARTIAL  | Scenario F (desktop macOS) = `pass` ("Normal playback, no audible artifact, no perceptible CPU impact"); Scenario E (Android) = `deferred — no device`. Operator signal `approved` with explicit deferral acknowledgement. Treated as deferred per task brief.    |
| 5  | No autoplay on page load — silent `<audio playsInline>` only starts via the user-gesture chain that constructs `AudioContext`, and is torn down on AC close (ROADMAP SC#4, IOS-05) | ✓ VERIFIED | `audioEngine.ts:174-193` — `new Audio(...)` + `.play()` is inside `createAudioEngine()` sync gesture head between `new AudioContext()` (L169) and the first `await audioCtx.resume()` (L208). `audioEngine.ts:388-392` — teardown inside `close()` body. Test "sync-construct order" (D-04) asserts `['AudioContext', 'Audio']` invariant. No module-top-level Audio instance constructed. |
| 6  | iOS background + lock-screen behavior unchanged — Phase 5.1 'interrupted'/'needs-resume' flow not regressed (ROADMAP SC#5)               | ✓ VERIFIED | `49-02-DEVICE-VALIDATION.md` Scenario D = `pass` ("Behavior unchanged from pre-Phase-49 … Phase 49 introduces no regression"). `git diff a003211 -- src/hooks/useAudioCues.ts` empty — Phase 5.1 reconstruct path code unchanged.                                  |
| 7  | engine.close() pauses element + clears src + drops reference before audioCtx.close() (D-08 teardown)                                     | ✓ VERIFIED | `audioEngine.ts:388-392` — `silentLoopElement.pause(); silentLoopElement.removeAttribute('src'); silentLoopElement = null` inside `if (silentLoopElement !== null)` guard, runs after end-chord tail wait and BEFORE the disconnect loop. Tests "engine.close() pauses … (D-08 teardown)" and "engine.close() is idempotent" both pass.                                                              |
| 8  | A silent-loop play() rejection does not propagate — createAudioEngine still resolves (D-09 silent-absorb)                                 | ✓ VERIFIED | `audioEngine.ts:193` — `void silentLoopElement.play()?.catch(() => undefined)`. Test "createAudioEngine resolves even when silent-loop element.play() rejects (D-09 silent-absorb)" passes via `RejectingAudio` stub returning `Promise.reject(new DOMException('autoplay denied', 'NotAllowedError'))`. |
| 9  | Construct order is AudioContext → Audio (D-04 sync-first invariant)                                                                       | ✓ VERIFIED | Test "sync-construct order: new AudioContext() precedes new Audio() in createAudioEngine (D-04)" asserts `callOrder === ['AudioContext', 'Audio']` via ordered stub. Audit of source between L169 and L206 shows no `await` between AC and Audio construct sites. |
| 10 | CR-01 fix: silent-loop element torn down on AC.resume() reject path (no leak when caller never gets an engine handle)                     | ✓ VERIFIED | `audioEngine.ts:206-218` — catch block on `audioCtx.resume()` invokes element teardown sequence (pause + removeAttribute + null) BEFORE `audioCtx.close()` and `throw err`. Test "createAudioEngine tears down silent-loop element when AudioContext.resume() rejects (CR-01)" passes — commit `b623477`.                                                                                                  |

**Score:** 5/5 ROADMAP success criteria verified (with SC#3 partial — IOS-04 Android arm deferred per operator acknowledgement).

### Deferred Items

| # | Item                                                                                                | Addressed In                                  | Evidence                                                                                                                                                              |
| - | --------------------------------------------------------------------------------------------------- | --------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1 | IOS-04 Android Chrome arm (Scenario E) — not validated on real Android hardware at phase close      | Operator follow-up; no specific later phase   | `49-02-DEVICE-VALIDATION.md` Scenario E = `deferred — no device`; Phase 49 SUMMARY follow-ups section: "Android verification (IOS-04 completion): when operator gains access to an Android device, re-run Scenario E"; operator resume signal = `approved` with explicit acknowledgement. |

### Required Artifacts

| Artifact                                                                                       | Expected                                                                              | Status     | Details                                                                                                                                                                                                                                                                                            |
| ---------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------- | ---------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `src/audio/audioEngine.ts`                                                                     | `SILENT_WAV_DATA_URL` constant + silent `<audio>` element wiring + `close()` teardown | ✓ VERIFIED | All three insertion sites present: L120 constant block, L174-193 sync gesture head construct, L388-392 close() teardown. Plus CR-01 fix at L206-218 (teardown on AC.resume reject). 5 grep-anchored assertions all match. File compiles, file is lint-clean (verified per-file in plan SUMMARY).    |
| `src/audio/audioEngine.test.ts`                                                                | Tests for D-04/D-05/D-08/D-09 + idempotency (5 new) + CR-01 regression                | ✓ VERIFIED | 30 `it()` blocks total (24 baseline + 5 Phase 49 + 1 CR-01). All five Phase 49 test titles present verbatim. Section header `// Phase 49: iOS silent-loop element` at L453.                                                                                                                                                |
| `.planning/phases/49-ios-speaker-route-fix/49-02-DEVICE-VALIDATION.md`                         | Per-device test matrix capturing operator observations across 6 scenarios             | ✓ VERIFIED | File exists, contains 6-row matrix (Scenarios A-F), each row populated with Result/Notes. Iteration log captures three PCM revisions. Root-cause analysis explains iOS-Safari `.volume` no-op finding. Operator signal `approved` in frontmatter.                                                       |

### Key Link Verification

| From                                                                              | To                                                              | Via                                                                                                | Status   | Details                                                                                                                                                                                                                                                                                                                                                                    |
| --------------------------------------------------------------------------------- | --------------------------------------------------------------- | -------------------------------------------------------------------------------------------------- | -------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `audioEngine.ts` sync gesture head                                                | iOS Safari audio session category coercion                      | `new Audio(SILENT_WAV_DATA_URL).play()` between `new AudioContext()` and `await audioCtx.resume()` | ✓ WIRED  | Source: L169 `new AudioContext()` → L174 `new Audio(SILENT_WAV_DATA_URL)` → L179-182 attribute assignments → L193 `void silentLoopElement.play()?.catch()` → L206 first `await`. Zero `await` between L169 and L206. D-04 sync-construct order test asserts `['AudioContext', 'Audio']`. Device-validated against real iPhone hardware (Scenario A pass on `c49765d`). |
| `audioEngine.close()` body                                                        | Silent-loop element released (decoded buffer freed, GC eligible) | `pause() + removeAttribute('src') + null reference` inside `if (closed) return` idempotency guard  | ✓ WIRED  | Source: L357 `if (closed) return` guard at top of close() → L388-392 element teardown block. Test "engine.close() is idempotent" asserts `pause` + `removeAttribute` each called exactly once across two close() calls.                                                                                                                                                                                                                                |
| `audioCtx.resume()` reject catch block                                            | Silent-loop element released on failure path (CR-01)             | Inline teardown before `audioCtx.close()` + `throw err`                                            | ✓ WIRED  | Source: L209-217. Test "CR-01" asserts pause + removeAttribute called once before the factory rejects with the AC failure. Closes the leak that would otherwise occur because the caller never gets an engine handle to invoke close().                                                                                                                                                                                                              |

### Data-Flow Trace (Level 4)

Skipped at the conventional sense — Phase 49 does not render dynamic data. The "data" is a static 444-byte base64 WAV constant whose flow path is `SILENT_WAV_DATA_URL` (L120) → `new Audio(SILENT_WAV_DATA_URL)` (L174) → DOM audio decoder. Static-by-design; not a hollow-prop or empty-store anti-pattern.

### Behavioral Spot-Checks

| Behavior                                                                            | Command                                                                            | Result                                                                                                  | Status  |
| ----------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------- | ------- |
| Build compiles                                                                      | `npm run build`                                                                    | exit 0; `dist/assets/index-BUeIIEmB.js 313.44 kB │ gzip: 92.95 kB`; PWA SW generated                    | ✓ PASS  |
| Full test suite passes at phase-49 baseline                                         | `npm run test:run`                                                                 | exit 0; 114 test files, **1289 tests** (1283 v2.1 baseline + 5 Phase 49 + 1 CR-01)                      | ✓ PASS  |
| audioEngine tests pass                                                              | `npm run test:run -- src/audio/audioEngine.test.ts`                                | exit 0; **30 tests passing** (24 baseline + 5 Phase 49 + 1 CR-01)                                       | ✓ PASS  |
| SILENT_WAV_DATA_URL is a real decodable WAV with RIFF header                        | `node -e "..." (decode base64, check RIFF magic + chunk size)`                     | First 4 bytes `RIFF`; RIFF chunk size field = 436 = decoded.byteLength(444) - 8 — header is consistent | ✓ PASS  |
| Zero React imports in audioEngine.ts                                                | `grep -cE "from ['\"]react" src/audio/audioEngine.ts`                              | 0                                                                                                       | ✓ PASS  |
| package.json `dependencies` unchanged across phase                                  | `git diff a003211 -- package.json`                                                 | 0 lines (DEPS-01)                                                                                       | ✓ PASS  |
| useAudioCues.ts / useAudioCues.test.tsx / vitest.setup.ts unchanged across phase    | `git diff a003211 -- src/hooks/useAudioCues.ts src/hooks/useAudioCues.test.tsx vitest.setup.ts` | 0 lines (D-04 NO-CHANGE directive)                                                                      | ✓ PASS  |

### Probe Execution

Not applicable — Phase 49 does not declare or imply probe-based verification. No `scripts/*/tests/probe-*.sh` exists in this repo, and the PLAN files use Vitest + `npm run test:run` as the automated-verification driver (already covered in Behavioral Spot-Checks above).

### Requirements Coverage

| Requirement | Source Plan      | Description                                                                                                                              | Status              | Evidence                                                                                                                                                                                                                                                                                          |
| ----------- | ---------------- | ---------------------------------------------------------------------------------------------------------------------------------------- | ------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| IOS-01      | 49-01, 49-02     | User on iOS Safari with silent switch ON and no headphones hears app audio through the device speaker.                                   | ✓ SATISFIED         | Plan 01 wired the code; Plan 02 Scenario A (`49-02-DEVICE-VALIDATION.md`) = `pass` on operator's iPhone, build `c49765d`.                                                                                                                                                                          |
| IOS-02      | 49-02            | User on iOS Safari with headphones connected continues to route audio through headphones (no regression).                                | ✓ SATISFIED         | Plan 02 Scenario B = `pass` ("Routes through headphones; no speaker bleed").                                                                                                                                                                                                                       |
| IOS-03      | 49-02            | User on iOS Safari with silent switch OFF hears audio normally (no regression).                                                          | ✓ SATISFIED         | Plan 02 Scenario C = `pass` ("Normal speaker playback; no regression").                                                                                                                                                                                                                            |
| IOS-04      | 49-02            | User on non-iOS platforms experiences no audible regression and no measurable performance regression from the silent-loop element.       | ◐ PARTIAL (accepted) | Scenario F desktop = `pass`; Scenario E Android = `deferred — no device`. Operator signal `approved` with explicit deferral acknowledgement. Recorded in deferred items above; documented in 49-02-SUMMARY.md "Deferred items" section.                                                            |
| IOS-05      | 49-01            | The silent `<audio playsInline>` element starts only via the same user-gesture chain that constructs `AudioContext` and is torn down on AC close. | ✓ SATISFIED         | Source assertions: element constructed at L174 inside `createAudioEngine()` body (no module-top `new Audio`), torn down inside `close()` at L388-392 + on AC.resume reject path at L209-217. Tests "sync-construct order (D-04)" and "engine.close() pauses … (D-08 teardown)" both pass.            |

All 5 requirement IDs accounted for. No ORPHANED requirements detected against REQUIREMENTS.md.

### Anti-Patterns Found

Scanned `src/audio/audioEngine.ts` and `src/audio/audioEngine.test.ts` (the only two files modified by this phase).

| File                            | Line     | Pattern                                                                                                              | Severity      | Impact                                                                                                                                                                                                                                                                                              |
| ------------------------------- | -------- | -------------------------------------------------------------------------------------------------------------------- | ------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `src/audio/audioEngine.ts`      | 107-116  | Comment-vs-data mismatch (REVIEW WR-02 / WR-03): comments describe "sine cycle" / "444 bytes / 592 base64 chars" while actual data is pulse-pair shape / 444 bytes / 592 base64 chars. WR-02 is documentation-only (no behavior impact). WR-03's numbers ARE correct (verified 444 bytes / 592 base64 chars). | ℹ️ Info       | WR-02 is a docstring honesty issue — does not affect runtime behavior, decoder compatibility, or test outcomes. WR-03's claim is in fact accurate (independent re-decode confirms 444 decoded bytes, 592 base64 chars; the SUMMARY's "366 chars" was a stale Plan 01 v1 number, not the v3 final). |
| `src/audio/audioEngine.ts`      | n/a      | No `TBD`/`FIXME`/`XXX` debt markers in Phase-49-modified lines                                                       | (clean)       | grep returned 0 hits for unresolved debt markers in the Phase 49 surface.                                                                                                                                                                                                                          |
| `src/audio/audioEngine.test.ts` | n/a      | No empty handlers, no `return null` stubs, no `placeholder` strings, no `console.log`-only impls                     | (clean)       | All five new tests execute substantive assertions (attribute reads, mock call counts, ordered-callstring equality, rejection propagation).                                                                                                                                                          |

Code review report (`49-REVIEW.md`) identified 1 Critical (CR-01) + 3 Warnings (WR-01..WR-03) + 3 Info items. CR-01 was fixed in commit `b623477` with a regression test ("CR-01" in audioEngine.test.ts). WR-02/WR-03 are documentation-honesty defects with no behavior impact and are out of scope for this verification pass (verifier scope is whether the phase GOAL was achieved; the WR-* items do not gate goal achievement). WR-01 (RIFF chunk size mismatch) is contradicted by independent re-decode in this verification — the header field is internally consistent (436 = 444 - 8). The REVIEW finding appears to have measured a different artifact; the current `main` build is header-conformant.

### Human Verification Required

None outstanding. All five ROADMAP success criteria are device-validated on real hardware (with IOS-04 Android arm operator-accepted as deferred per task brief). The operator's `approved` signal in `49-02-DEVICE-VALIDATION.md` and `49-02-SUMMARY.md` closes the human-verification loop for this phase.

### Gaps Summary

No gaps. Phase 49 achieves its goal:

1. **iOS speaker routing is verified on real hardware** — operator's iPhone, build `c49765d`, Scenario A pass (the primary requirement IOS-01).
2. **No regression in any tested scenario** — Scenarios B, C, D, F all pass; Scenario E (Android) deferred with operator acknowledgement.
3. **Phase 5.1 reconstruct flow untouched** — `git diff a003211 -- src/hooks/useAudioCues.ts` is empty; D-04 NO-CHANGE directive held across all three PCM revisions and the CR-01 fix.
4. **DEPS-01 holds** — `git diff a003211 -- package.json` is empty.
5. **QUAL-01 holds at phase close** — `npm run build` exits 0; `npm run test:run` reports 1289 tests passing on 114 test files; `audioEngine.ts` + `audioEngine.test.ts` are lint-clean (per-file). Pre-existing lint errors in unrelated `sessionPresentation.ts` + `storage.ts` are documented in `deferred-items.md` and explicitly out of Phase 49 scope.
6. **Code review CR-01 closed** — silent-loop leak on AC.resume reject path was identified post-execution and fixed in `b623477` with a regression test.

**Notable side-effect findings worth surfacing to milestone retro (informational, not blockers):**

- **iOS Safari ignores `HTMLMediaElement.volume` entirely.** This was discovered during Plan 02 device validation and forced the v3 16-bit PCM revision (encoding attenuation into the samples instead of via `.volume`). Spec D-05's "volume near-zero" lock anticipated tuning but did not flag the no-op behavior. Worth a known-iOS-gotcha entry for future audio work.
- **Service worker cache invalidation friction.** Each PCM revision required manual SW unregister + cache wipe on the iPhone before the new build took effect. The PWA SW silently masking new builds is a recurring dev-validation friction point.
- **WR-02 / WR-03 documentation defects** (comment claims "sine cycle" / wrong byte counts) remain in the source per the verifier scope (goal achievement, not docstring honesty). A future opportunistic cleanup can rectify these without affecting behavior.

---

_Verified: 2026-05-27T15:55:00Z_
_Verifier: Claude (gsd-verifier)_
