---
phase: 49-ios-speaker-route-fix
plan: 02
type: execute
wave: 2
depends_on:
  - 49-01
files_modified: []
autonomous: false
requirements:
  - IOS-01
  - IOS-02
  - IOS-03
  - IOS-04
must_haves:
  truths:
    - "User on iOS Safari with silent switch ON and no headphones connected starts a session and hears cue audio through the device speaker (IOS-01)"
    - "User on iOS Safari with headphones connected continues to route audio through headphones — no regression (IOS-02)"
    - "User on iOS Safari with silent switch OFF hears audio normally — no regression (IOS-03)"
    - "User on non-iOS platforms (Android Chrome and at least one desktop browser) hears no audible regression and observes no measurable performance regression from the silent-loop element (IOS-04)"
    - "iOS background + lock-screen behavior remains unchanged — the existing 'interrupted'/'needs-resume' flow is not regressed by the new element (ROADMAP success criterion #5)"
  artifacts:
    - path: ".planning/phases/49-ios-speaker-route-fix/49-02-DEVICE-VALIDATION.md"
      provides: "Per-device test matrix with pass/fail observations per scenario, captured by the operator during the checkpoint"
      contains: "iOS Safari"
  key_links:
    - from: "Plan 01 silent-loop element wiring"
      to: "iOS Safari audio session category coercion"
      via: "user-gesture-attached new Audio(SILENT_WAV_DATA_URL).play()"
      pattern: "(speaker|playback|ambient)"
---

<objective>
Device-validate the Phase 49 silent-loop fix against the five ROADMAP success criteria — none of which are verifiable in jsdom (no real iOS Safari, no real headphones, no silent switch, no Android Chrome, no real device autoplay policy). Covers IOS-01..IOS-04 plus the iOS background/lock-screen non-regression check from ROADMAP success criterion #5.

Purpose: Confirm Plan 01's code change actually produces the intended iOS audio-session-category coercion on real hardware, and produces no audible or measurable regression on iOS-with-headphones, iOS-with-silent-switch-OFF, or non-iOS platforms. Without this checkpoint, Plan 01 is only "code-shaped correctly" — not "shipping the requirement."

Output:
- A new `.planning/phases/49-ios-speaker-route-fix/49-02-DEVICE-VALIDATION.md` file capturing the operator's per-scenario observations across a fixed test matrix.
- Operator approval (or a documented failure → revision loop back to Plan 01) before the phase is closed.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/49-ios-speaker-route-fix/49-CONTEXT.md
@.planning/phases/49-ios-speaker-route-fix/49-01-PLAN.md
@.planning/phases/49-ios-speaker-route-fix/49-01-SUMMARY.md
@src/audio/audioEngine.ts
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Device validation — iOS speaker routing + non-regression + non-iOS non-regression</name>
  <read_first>
    - .planning/phases/49-ios-speaker-route-fix/49-CONTEXT.md (D-02 always-on rationale; D-05 attribute lock — confirm the deployed build matches)
    - .planning/phases/49-ios-speaker-route-fix/49-01-SUMMARY.md (Plan 01 close-out — confirms which build is under test)
    - .planning/ROADMAP.md "Phase 49: iOS speaker route fix" Success Criteria (5 items — this checkpoint closes 4 of them; the 5th is composed of automated + observation per the matrix below)
    - .planning/REQUIREMENTS.md §"iOS Audio Routing (Phase 49 — iOS speaker route fix)" IOS-01..IOS-04 verbatim
  </read_first>
  <files>.planning/phases/49-ios-speaker-route-fix/49-02-DEVICE-VALIDATION.md</files>
  <action>Manual device-test checkpoint — operator-driven. Detailed scenario script lives in &lt;how-to-verify&gt; below. Output: the operator creates .planning/phases/49-ios-speaker-route-fix/49-02-DEVICE-VALIDATION.md capturing per-scenario Device / OS Version / Browser Version / Result / Notes for Scenarios A (IOS-01, silent ON + no HP), B (IOS-02, headphones), C (IOS-03, silent OFF), D (ROADMAP criterion #5 — lock + background), E (IOS-04 Android Chrome), F (IOS-04 desktop). Resume signal locked to one of: "approved" / "revise plan 01: &lt;desc&gt;" / "defer device validation: &lt;reason&gt;". No code changes in this plan.</action>
  <verify>
    <human-check>Operator completes all six scenarios from &lt;how-to-verify&gt; and records pass/fail in 49-02-DEVICE-VALIDATION.md. Operator types the resume signal.</human-check>
  </verify>
  <what-built>
    Plan 01 wired a silent looping `<audio playsInline loop muted=false volume=0.0001>` element inside `createAudioEngine()` on the user-gesture chain, torn down inside `engine.close()`. The Vitest suite confirms the code-shaped contract (attribute wiring, sync construct order, silent-absorb, close() teardown). What Vitest CANNOT verify: whether the deployed build actually coerces iOS Safari from "ambient" to "playback" audio session category on a real iPhone with the silent switch ON and no headphones connected.

    This checkpoint validates the five ROADMAP success criteria on real hardware:
    1. iOS Safari, silent switch ON, no headphones — audio plays through speaker (IOS-01).
    2. iOS Safari, headphones connected — audio routes through headphones (IOS-02).
    3. iOS Safari, silent switch OFF, no headphones — audio plays normally (IOS-03).
    4. Non-iOS — no audible regression, no measurable performance regression (IOS-04).
    5. iOS background + lock-screen — existing `'interrupted'`/`'needs-resume'` flow unchanged (ROADMAP criterion #5).
  </what-built>
  <how-to-verify>
    PRE-CHECK — deploy the build under test:
    1. Confirm Plan 01 is merged on `main` and `npm run build` exits 0.
    2. Serve a local build to the iPhone (Vite preview over LAN, or push a preview deploy — operator's choice). Capture the URL used.

    SCENARIO A — iOS Safari, silent switch ON, no headphones (IOS-01 — the primary requirement):
    1. On an iPhone running iOS Safari (not Chrome iOS, not Firefox iOS — Safari specifically).
    2. Flip the physical silent switch ON (orange visible).
    3. Disconnect / undock any headphones, AirPods, Bluetooth audio.
    4. Open the served build URL.
    5. Start an HRV Breathing session (Start button — single tap).
    6. EXPECTED: lead-in countdown ticks + first In bowl cue are audible through the iPhone speaker.
    7. RECORD: pass/fail. If FAIL, capture (a) iOS version, (b) Safari version, (c) any console error you can capture via Web Inspector connected to the iPhone, and (d) whether audio is silent OR routed elsewhere (e.g., Bluetooth picked up despite no headphones).

    SCENARIO B — iOS Safari, headphones connected (IOS-02 — no headphone regression):
    1. Same iPhone, same build URL.
    2. Connect wired headphones (or AirPods).
    3. Reload the page.
    4. Start an HRV Breathing session.
    5. EXPECTED: audio routes through headphones; speaker is silent.
    6. RECORD: pass/fail. If FAIL, headphone bypass logic has changed — escalate; this is a hard regression.

    SCENARIO C — iOS Safari, silent switch OFF, no headphones (IOS-03 — no silent-OFF regression):
    1. Same iPhone, same build URL.
    2. Flip silent switch OFF (orange hidden).
    3. Disconnect headphones.
    4. Reload the page.
    5. Start an HRV Breathing session.
    6. EXPECTED: audio plays through speaker at normal volume; behavior is indistinguishable from pre-Phase-49.
    7. RECORD: pass/fail. If FAIL, the silent-loop interfered with the normal path — escalate.

    SCENARIO D — iOS Safari, background + lock-screen behavior (ROADMAP criterion #5 — no regression of Phase 5.1 `'interrupted'`/`'needs-resume'` flow):
    1. Same iPhone, same build URL, headphones disconnected, silent switch state irrelevant.
    2. Start an HRV Breathing session.
    3. Mid-session, press the side button to lock the screen.
    4. Wait ~10 seconds.
    5. Unlock.
    6. EXPECTED: existing iOS Phase 5.1 affordance still operates — either audio resumes automatically, OR the morphing `MuteToggle` surfaces a tappable affordance that resumes cleanly. Behavior is identical to pre-Phase-49.
    7. Also test: background the Safari tab (swipe up / switch apps), wait ~10 seconds, return to the tab. Confirm the same Phase 5.1 recovery posture.
    8. RECORD: pass/fail per sub-step. If the affordance fails to appear OR audio silently dies without recovery, escalate — Phase 49 must NOT regress Phase 5.1.

    SCENARIO E — Android Chrome (IOS-04 — non-iOS no-regression, primary):
    1. On any Android device running Chrome (Pixel, Samsung, etc.).
    2. Open the served build URL.
    3. Start an HRV Breathing session at default volume.
    4. EXPECTED: audio plays normally; no clicks, no distortion, no perceptible delay vs. the v2.1 baseline.
    5. RECORD: pass/fail + Android version + Chrome version.

    SCENARIO F — Desktop browser (IOS-04 — non-iOS no-regression, secondary):
    1. On at least ONE desktop browser of your choice — Chrome OR Firefox OR Safari (macOS).
    2. Open the served build URL.
    3. Start an HRV Breathing session.
    4. EXPECTED: audio plays normally.
    5. OPTIONAL perf check: open DevTools → Performance → record a 60-second session → confirm no new sustained CPU spike traceable to the silent loop (a near-silent looping `<audio>` should register as ~negligible additional render work; if you see a >5% CPU baseline shift vs. v2.1, RECORD it — the D-02 always-on decision documents UA-gating as the fallback if regression appears).
    6. RECORD: pass/fail + browser + version + perf observation if measured.

    CAPTURE — at the end of all scenarios, create `.planning/phases/49-ios-speaker-route-fix/49-02-DEVICE-VALIDATION.md` with a table:

    | Scenario | Device | OS Version | Browser Version | Result | Notes |
    |----------|--------|------------|-----------------|--------|-------|
    | A (IOS-01, silent ON + no HP) | iPhone … | iOS … | Safari … | pass/fail | … |
    | B (IOS-02, headphones) | iPhone … | iOS … | Safari … | pass/fail | … |
    | C (IOS-03, silent OFF) | iPhone … | iOS … | Safari … | pass/fail | … |
    | D (criterion #5, lock + background) | iPhone … | iOS … | Safari … | pass/fail | … |
    | E (IOS-04 Android) | … | Android … | Chrome … | pass/fail | … |
    | F (IOS-04 desktop) | … | … | … /… | pass/fail | perf obs … |

    A pass on Scenarios A + B + C + D + E + (at least one of) F closes the checkpoint. Scenario A FAIL is a Phase 49 implementation failure — escalate to a Plan 01 revision (likely candidates: SILENT_WAV_DATA_URL not a "real enough" track for current iOS Safari, `volume` value needs tuning, or autoplay policy intercepting `.play()` despite the gesture chain). Scenarios B/C/D/E FAIL is a regression — escalate immediately.

    DEVICE AVAILABILITY note: if a physical iPhone is unavailable for any scenario, document the unavailability in the table (Result column = "deferred — no device") and surface the gap in the checkpoint resume signal so the operator can decide whether to ship Plan 01 on automated-test-only confidence or defer the phase close until device access is available.
  </how-to-verify>
  <acceptance_criteria>
    - Artifact existence: `.planning/phases/49-ios-speaker-route-fix/49-02-DEVICE-VALIDATION.md` exists and contains a Markdown table with at minimum 6 rows (Scenarios A through F).
    - Source assertion: `grep -cE "^\| [A-F] \(" .planning/phases/49-ios-speaker-route-fix/49-02-DEVICE-VALIDATION.md` returns ≥ 6.
    - Source assertion: every row's Result column is one of `pass`, `fail`, or `deferred — no device`.
    - Behavior assertion (recorded in the file, not auto-checkable): Scenario A reports `pass` OR the file documents a Plan 01 revision request with the specific failure mode captured.
    - Behavior assertion (recorded): Scenarios B, C, D, E all report `pass` (or `deferred — no device` with operator acknowledgement that IOS-02/IOS-03/IOS-04 are unverified at phase close).
    - Behavior assertion (recorded): Scenario F reports `pass` for at least one desktop browser.
    - Operator-signal: the checkpoint resume-signal is one of the documented choices below; no implicit advance.
    - CLI output: `git status` after the checkpoint shows `49-02-DEVICE-VALIDATION.md` as either tracked (committed) or staged (ready for the SUMMARY commit).
  </acceptance_criteria>
  <resume-signal>
    Type one of:
    - `approved` — all in-scope scenarios passed (or any `deferred — no device` rows are explicitly acknowledged); proceed to Plan 02 SUMMARY + phase close.
    - `revise plan 01: <description>` — Scenario A or another scenario failed in a way that requires a Plan 01 code change; orchestrator routes back to revision mode with the failure description.
    - `defer device validation: <reason>` — a required device is unavailable; operator chooses to ship Plan 01 on automated-test-only confidence; document the unverified requirements in the phase SUMMARY and the milestone deferred items table.
  </resume-signal>
  <done>
    `49-02-DEVICE-VALIDATION.md` exists with all 6 scenario rows recorded. Operator has resumed with one of `approved`, `revise plan 01: …`, or `defer device validation: …`. Phase 49 SUMMARY can now be written with the device-validation outcome captured.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → device under test | Operator manually triggers the user gesture (Start button tap) on a real iPhone / Android / desktop; the device's audio stack is the verification surface. |
| device audio session ↔ OS routing | iOS Safari's audio-session-category decision (ambient vs. playback) is the device-level boundary this phase is targeting. |

## STRIDE Threat Register

Plan 02 is a manual validation checkpoint — no new code, no new deps, no automated test. The threat surface is the documentation accuracy and the operator's recording fidelity.

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-49-02-01 | Tampering | `49-02-DEVICE-VALIDATION.md` test record | accept | The validation record is operator-authored; the only "tampering" mitigation is the existing git-history audit trail. No additional control is appropriate for an operator-driven manual test. |
| T-49-02-02 | Information Disclosure | LAN-served build URL exposed during device testing | accept | Vite preview servers bind to localhost or LAN by default; build under test contains no secrets (the SILENT_WAV_DATA_URL is the only Phase 49 addition and is inert). No new disclosure surface vs. existing dev workflow. |
| T-49-02-SC | Tampering | npm/pip/cargo installs | mitigate | DEPS-01 invariant: this plan installs nothing. `git diff package.json` returns empty after Plan 02. |

ASVS L1 mapping: no applicable controls — Plan 02 surface is operator documentation only.

</threat_model>

<verification>
## Plan-level verification

1. `.planning/phases/49-ios-speaker-route-fix/49-02-DEVICE-VALIDATION.md` exists with the required 6-row table.
2. `git diff package.json` empty (DEPS-01).
3. `npm run build && npm run lint && npm run test:run` still green at the post-Plan-01 baseline (QUAL-01 — no regression from documentation-only plan).
4. Operator has signalled `approved` OR `revise plan 01: …` OR `defer device validation: …`.

## Source-of-truth verification

- Every row in the validation table has Device, OS Version, Browser Version, Result, and Notes columns populated (or explicitly marked `n/a` for the Notes column).
- IOS-01 (Scenario A) result is documented unambiguously.
- IOS-02 / IOS-03 / IOS-04 (Scenarios B/C/E/F) results are documented unambiguously.
- ROADMAP success criterion #5 (Scenario D) result is documented unambiguously.
</verification>

<success_criteria>
- All Task 1 acceptance criteria pass.
- IOS-01, IOS-02, IOS-03, IOS-04 either each report `pass` in the validation file OR have a documented deferral with operator acknowledgement.
- ROADMAP success criterion #5 (iOS background + lock-screen) reports `pass` or documented deferral.
- QUAL-01 green-gate holds (no regression introduced by this plan — it adds documentation only).
- DEPS-01 holds (`package.json` unchanged).
- Phase 49 can be closed with `/gsd:close-phase 49` after this plan resumes with `approved`.
</success_criteria>

<output>
Create `.planning/phases/49-ios-speaker-route-fix/49-02-SUMMARY.md` when done. Capture:
- Final scenario-by-scenario validation table (or a summary pointing to `49-02-DEVICE-VALIDATION.md`).
- IOS-01..IOS-04 + ROADMAP criterion #5 disposition per scenario.
- Operator's resume signal (`approved` / `revise plan 01: …` / `defer device validation: …`).
- Confirmation that DEPS-01 + QUAL-01 still hold.
- Any follow-up items (e.g., if `defer device validation` was chosen, link to the milestone deferred items table entry).
</output>
</content>
</invoke>