---
phase: 49-ios-speaker-route-fix
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/audio/audioEngine.ts
  - src/audio/audioEngine.test.ts
autonomous: true
requirements:
  - IOS-01
  - IOS-05
must_haves:
  truths:
    - "createAudioEngine() constructs a silent <audio> element inside the same sync gesture head as new AudioContext()"
    - "The element is wired with playsInline=true, loop=true, muted=false, volume=0.0001 and an inline data: URL source"
    - "engine.close() pauses the element and clears its src before audioCtx.close()"
    - "A silent-loop play() rejection does not propagate — createAudioEngine still resolves (D-09 silent-absorb)"
    - "Construct order is AudioContext → Audio (D-04 sync-first invariant), and the element teardown lives inside the existing close() idempotency guard"
  artifacts:
    - path: "src/audio/audioEngine.ts"
      provides: "SILENT_WAV_DATA_URL constant + silent <audio> element wiring + close() teardown"
      contains: "SILENT_WAV_DATA_URL"
    - path: "src/audio/audioEngine.test.ts"
      provides: "Tests for element construction, attribute wiring, close() teardown, silent-absorb on play reject, and construct order"
  key_links:
    - from: "src/audio/audioEngine.ts:129"
      to: "src/audio/audioEngine.ts:136"
      via: "sync gesture head — new AudioContext() → new Audio(SILENT_WAV_DATA_URL) → void audioElement.play().catch(...) → if (audioCtx.state === 'suspended') await audioCtx.resume()"
      pattern: "new Audio\\(SILENT_WAV_DATA_URL\\)"
    - from: "src/audio/audioEngine.ts:282 (closed guard)"
      to: "src/audio/audioEngine.ts:317 (cue disconnect loop)"
      via: "element teardown — audioElement.pause() + audioElement.removeAttribute('src') + null reference, BEFORE the disconnect loop, BEFORE await audioCtx.close()"
      pattern: "audioElement\\.(pause|removeAttribute)"
---

<objective>
Implement the iOS silent-loop fix inside `createAudioEngine()` per the canonical spec (`.planning/todos/2026-05-27-ios-speaker-route-audio-element-fix.md`) and the locked decisions D-01..D-09 in `49-CONTEXT.md`. Single-file surgery on `src/audio/audioEngine.ts` plus companion tests in `src/audio/audioEngine.test.ts`.

Purpose: Coerce iOS Safari's audio session category from "ambient" to "playback" so users with the silent switch ON and no headphones connected hear app cue audio through the device speaker — without UA sniffing, without new runtime deps, without leaking React into `audioEngine.ts`, and without disturbing the iOS Phase 5.1 reconstruct path.

Output:
- `src/audio/audioEngine.ts` modified at three insertion points (constants block, sync gesture head between L129 and L136, close() teardown before L317).
- `src/audio/audioEngine.test.ts` extended with five tests covering attribute wiring, element construction count, close() teardown, silent-absorb on play() reject, and sync-construct order.
- All existing tests still pass (QUAL-01 green gate); no new runtime dep added (DEPS-01).
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/49-ios-speaker-route-fix/49-CONTEXT.md
@.planning/phases/49-ios-speaker-route-fix/49-PATTERNS.md
@.planning/todos/2026-05-27-ios-speaker-route-audio-element-fix.md
@src/audio/audioEngine.ts
@src/audio/audioEngine.test.ts
@src/hooks/useAudioCues.ts

<interfaces>
<!-- Engine factory signature and the existing module-level constants the new constant joins.
     Extracted from src/audio/audioEngine.ts so the executor does not need to re-explore. -->

From src/audio/audioEngine.ts (file-header invariant L1): "Zero React imports."

Public factory at L125:
```typescript
export async function createAudioEngine(opts: AudioEngineOptions): Promise<AudioEngine>
```

Module-level constants block (L78-96 — SILENT_WAV_DATA_URL + SILENT_LOOP_VOLUME join this block):
```typescript
const MUTE_FADE_TIME_CONSTANT = 0.05
const MIN_GAIN_VALUE = 0.0001                       // ← DO NOT couple SILENT_LOOP_VOLUME to this
export const LEAD_IN_TICK_INTERVAL_SEC = 1.0
export const LEAD_IN_DURATION_SEC = 3.0
export const LEAD_IN_TICK_INTERVAL_MS = LEAD_IN_TICK_INTERVAL_SEC * 1000
export const LEAD_IN_DURATION_MS = LEAD_IN_DURATION_SEC * 1000
export const SAFE_LEAD_SEC = 0.005
```

Sync gesture head invariant (L125-143):
```typescript
const audioCtx = new AudioContext()                 // L129 — D-04 sync anchor
// ← INSERT silent-loop construct + attribute wiring + .play() HERE (Task 1).
if (audioCtx.state === 'suspended') {               // L136 — first await follows
  try { await audioCtx.resume() } catch (err) { … throw err }
}
```

Per-engine closure state region (L165-177): `activeCues`, `muted`, `sessionTimbre`, `closed` declared here. The silent-loop element joins this region as a `let audioElement: HTMLAudioElement | null` declaration referencing the const created in the gesture head.

close() teardown (L281-325):
```typescript
async close(): Promise<void> {
  if (closed) return                                // L282 — idempotency guard
  closed = true
  audioCtx.removeEventListener('statechange', onStateChange)   // L286
  // … end-chord tail wait L302-307 …
  // ← INSERT element teardown HERE (Task 1) — BEFORE the disconnect loop.
  for (const cue of [...activeCues]) { … }          // L317 — disconnect loop
  activeCues.clear()
  await audioCtx.close()                            // L324
}
```

Test stub mechanism (audioEngine.test.ts L57-60 + L73-85 + L202-226):
- `afterEach(() => { vi.unstubAllGlobals(); vi.restoreAllMocks() })`
- Per-test `class ProbeAC { … }` + `vi.stubGlobal('AudioContext', ProbeAC)` already established for AC stubbing.
- Task 2 extends with `vi.stubGlobal('Audio', SpyAudio)` per the same mechanism.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wire SILENT_WAV_DATA_URL + silent &lt;audio&gt; element inside createAudioEngine() and tear it down inside engine.close()</name>
  <files>src/audio/audioEngine.ts</files>
  <read_first>
    - src/audio/audioEngine.ts (full file — focus L1, L78-96, L125-143, L165-177, L281-325)
    - .planning/phases/49-ios-speaker-route-fix/49-CONTEXT.md (D-01..D-09 implementation directives, verbatim)
    - .planning/phases/49-ios-speaker-route-fix/49-PATTERNS.md (Patterns A, B, C, D — exact line anchors and "what to copy" guidance)
    - .planning/todos/2026-05-27-ios-speaker-route-audio-element-fix.md (canonical spec — spike-lock)
    - src/hooks/useAudioCues.ts L296-309 (Plan 06 Task 8 sync-first reconstruct invariant — context only; D-04 extends this but NO edit to this file is permitted)
  </read_first>
  <behavior>
    - The factory exposes (file-locally or as an `export const` — keep file-local unless Task 2 imports it) a `SILENT_WAV_DATA_URL` constant whose value is a `data:audio/wav;base64,` URL encoding an 8000 Hz mono 8-bit PCM WAV of ≤ 500 bytes raw, ~25 ms duration, with sample values oscillating in {127, 128} so it is near-zero amplitude AND a real decodable track (NOT digital silence). The encoded URL string MUST fit within ~400 base64 characters.
    - The factory exposes a file-local `const SILENT_LOOP_VOLUME = 0.0001` declared in the same constants block as `MIN_GAIN_VALUE` but NOT aliased to it (Pattern A separation directive — they are unrelated invariants).
    - Inside `createAudioEngine()`, between the existing `const audioCtx = new AudioContext()` at L129 and the `if (audioCtx.state === 'suspended')` block starting at L136, the factory synchronously constructs `const audioElement = new Audio(SILENT_WAV_DATA_URL)`, assigns the four locked attributes (`playsInline = true`, `loop = true`, `muted = false`, `volume = SILENT_LOOP_VOLUME`), and fires `void audioElement.play().catch(() => undefined)`. The entire block contains NO `await` — the order `new AudioContext() → new Audio(...) → audioElement.play()` is sync per D-04/D-06.
    - The closure-state region around L165-177 adds a `let audioElement: HTMLAudioElement | null = audioElement` (renamed if needed to avoid shadowing — e.g. introduce the local as `const silentLoopElement` in the head and capture as `let silentLoopElementRef: HTMLAudioElement | null = silentLoopElement` in the closure block, or use a single `let` declared in the head). The reference is closure-captured per Pattern C, NOT exposed on the returned `engine` object (no new member on the `AudioEngine` interface at L29-63).
    - Inside `engine.close()`, between the end-chord tail wait at L302-307 and the in-flight cue disconnect loop at L317, the teardown executes `if (silentLoopElementRef !== null) { silentLoopElementRef.pause(); silentLoopElementRef.removeAttribute('src'); silentLoopElementRef = null }` — sync, no try/catch, no new await. Lives strictly inside the existing `if (closed) return` guard at L282 so a second `close()` short-circuits before reaching it.
    - File-header "Zero React imports" invariant at L1 remains intact. No import of `react`, `react-dom`, or any `*.tsx` file. `new Audio(...)` is a DOM API.
    - The `AudioEngine` interface at L29-63 gains NO new members (the element is invisible at the hook seam per D-04; integration-points doc).
  </behavior>
  <action>
    Modify `src/audio/audioEngine.ts` at three insertion sites — preserve every existing line outside these inserts; no refactors, no comment churn outside the new blocks.

    Insertion 1 — module-level constants block (anchor `MIN_GAIN_VALUE` at L83):
    - Add `SILENT_WAV_DATA_URL` const after `MIN_GAIN_VALUE` (or grouped with the new `SILENT_LOOP_VOLUME` immediately above the `LEAD_IN_TICK_INTERVAL_SEC` block at L88). The value is a `data:audio/wav;base64,` string. Construct the WAV: RIFF/WAVE header (44 bytes) for 8000 Hz mono 8-bit PCM, ~200 sample bytes (each byte in {127, 128} — half at 127, half at 128, alternating to break any pure-silence detection while staying near-zero amplitude), totalling ≤ 244 raw bytes → ≤ 328 base64 chars. Total `data:` URL string ≤ 400 chars. Verify byte length at execute time with `console.log(SILENT_WAV_DATA_URL.length)` once during local dev (REMOVE the log before commit). Lead the constant with a comment block: purpose (iOS audio-session coercion), provenance (Phase 49, references `.planning/phases/49-ios-speaker-route-fix/49-CONTEXT.md` D-03), and the "real decodable WAV — not digital silence" constraint.
    - Add `const SILENT_LOOP_VOLUME = 0.0001` immediately under `SILENT_WAV_DATA_URL` with a one-line comment: "Phase 49 D-05: near-zero non-zero. NOT coupled to MIN_GAIN_VALUE — separate invariants (49-PATTERNS.md Pattern A)."
    - Keep both constants file-local (no `export`) — Task 2 stubs `Audio` globally and does not need to import the URL.

    Insertion 2 — sync gesture head, between `const audioCtx = new AudioContext()` (L129) and the `if (audioCtx.state === 'suspended')` block (L136):
    - Add a comment header: "Phase 49 D-01/D-04/D-06: silent looping <audio> element constructed inside the engine, on the sync gesture head, BEFORE any await. Coerces iOS audio session from 'ambient' to 'playback'. Always-on per D-02 (no UA gating). See 49-CONTEXT.md."
    - Declare `let silentLoopElement: HTMLAudioElement | null = new Audio(SILENT_WAV_DATA_URL)` (use `let` so the close() teardown can null it).
    - Assign four attributes on individual lines: `silentLoopElement.playsInline = true`, `silentLoopElement.loop = true`, `silentLoopElement.muted = false`, `silentLoopElement.volume = SILENT_LOOP_VOLUME`. Per D-05: `muted` MUST be `false`; the element MUST be a "real" track.
    - Fire `void silentLoopElement.play().catch(() => undefined)` with a comment: "D-09 silent-absorb: a .play() rejection (autoplay policy regression, codec issue) does NOT propagate. Session continues; iOS silent-switch users simply do not get speaker routing — no worse than pre-Phase-49. Critically different from the AC resume() reject below at L139-143, which DOES re-throw — the silent loop is sub-essential infrastructure (49-PATTERNS.md 'Silent-absorb on resource-acquisition failures')."
    - VERIFY: no `await` between L129 (`new AudioContext()`) and L136 (`if (audioCtx.state === 'suspended')`) after this insert — only sync property assignments and a `void`-ed fire-and-forget promise.

    Insertion 3 — `engine.close()` body, between the end-chord tail wait (ends L307) and the in-flight cue disconnect loop (starts L317):
    - Add a comment header: "Phase 49 D-08: silent-loop element teardown. pause() + clear src releases the decoded buffer; null reference drops the closure handle for GC. Sync, no await, no try/catch — the engine owns the element exclusively (Pattern D). Inside the existing `if (closed) return` guard at L282 — second close() short-circuits before this runs."
    - Add the teardown block: `if (silentLoopElement !== null) { silentLoopElement.pause(); silentLoopElement.removeAttribute('src'); silentLoopElement = null }`.

    After the three insertions:
    - Run `npm run build` (executes `tsc -b && vite build`) — TypeScript MUST compile cleanly. Common breakage: shadowing `audioElement` vs `silentLoopElement` — keep names consistent across sites.
    - Run `npm run lint` — no new ESLint errors.
    - Run `npm run test:run -- src/audio/audioEngine.test.ts` — all 19 existing tests in this file MUST still pass (Task 2 adds 5 more; verify pre-Task-2 baseline first).
    - Commit per the QUAL-01 green-gate convention: `feat(49-01): wire silent-loop audio element inside createAudioEngine (iOS speaker route fix)`.

    DO NOT touch: `src/hooks/useAudioCues.ts` (D-04 extends the sync invariant transitively through `createAudioEngine()` — no hook edit), `vitest.setup.ts` (no new polyfill needed; jsdom provides `Audio` natively), `package.json` (DEPS-01).
  </action>
  <verify>
    <automated>npm run build &amp;&amp; npm run lint &amp;&amp; npm run test:run -- src/audio/audioEngine.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `grep -n "SILENT_WAV_DATA_URL" src/audio/audioEngine.ts` returns at least 2 hits (declaration + use in `new Audio(...)`).
    - Source assertion: `grep -n "SILENT_LOOP_VOLUME" src/audio/audioEngine.ts` returns at least 2 hits (declaration + assignment).
    - Source assertion: `grep -nE "new Audio\(SILENT_WAV_DATA_URL\)" src/audio/audioEngine.ts` returns exactly 1 hit.
    - Source assertion: `grep -nE "(playsInline|loop|muted|volume)\s*=\s*" src/audio/audioEngine.ts` shows the four locked attributes assigned on the silent-loop element (alongside the existing `muted` declarations in the closure state).
    - Source assertion: `grep -nE "silentLoopElement\.(pause|removeAttribute)" src/audio/audioEngine.ts` returns at least 2 hits (pause + removeAttribute in close()).
    - Source assertion: between L129 (`new AudioContext()`) and the first `await audioCtx.resume()`, there is no `await` keyword — verify with `awk '/new AudioContext\(\)/,/audioCtx\.state === .suspended./' src/audio/audioEngine.ts | grep -c "await"` returning 0.
    - Source assertion: file-header "Zero React imports" invariant holds — `grep -nE "from ['\"]react" src/audio/audioEngine.ts` returns 0 hits.
    - Source assertion: `grep -nE "^(export )?interface AudioEngine" src/audio/audioEngine.ts` shows no new members added (interface body at L29-63 unchanged outside docstrings).
    - Source assertion: `data:audio/wav;base64,` literal appears in the SILENT_WAV_DATA_URL value.
    - Behavior assertion: `node -e "const url = require('fs').readFileSync('src/audio/audioEngine.ts', 'utf8').match(/SILENT_WAV_DATA_URL\s*=\s*['\"]([^'\"]+)['\"]/)[1]; console.log('len:', url.length); process.exit(url.length <= 400 ? 0 : 1)"` exits 0 (data URL fits within 400 chars).
    - Test command: `npm run test:run -- src/audio/audioEngine.test.ts` reports green at the 19-test pre-Task-2 baseline (no existing tests regressed).
    - Test command: `npm run build` exits 0.
    - Test command: `npm run lint` exits 0.
    - CLI output: `git diff --stat src/audio/audioEngine.ts` shows insertions only (no large deletions outside the three insertion sites).
    - CLI output: `git diff src/hooks/useAudioCues.ts` is empty (no hook edit).
    - CLI output: `git diff package.json` shows no changes to the `dependencies` object (DEPS-01).
  </acceptance_criteria>
  <done>
    `src/audio/audioEngine.ts` contains SILENT_WAV_DATA_URL, SILENT_LOOP_VOLUME, the sync gesture-head construct + attribute + .play() block, and the close() teardown — all wired per D-01..D-09. `npm run build`, `npm run lint`, and the existing 19 audioEngine tests all pass green. No new runtime dep, no React import, no hook edit, no new AudioEngine interface members.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add five Phase 49 tests covering element construction, attribute wiring, close() teardown + idempotency, silent-absorb on play() reject, and sync-construct order</name>
  <files>src/audio/audioEngine.test.ts</files>
  <read_first>
    - src/audio/audioEngine.test.ts (full file — focus L1-60 setup, L62-71 Pattern E, L73-85 Pattern G inverse, L202-250 Pattern F + idempotency, L294-318 ProbeAC class shape)
    - src/audio/audioEngine.ts (after Task 1 — for the actual attribute names + values to assert)
    - .planning/phases/49-ios-speaker-route-fix/49-PATTERNS.md (Patterns E, F, G, H — verbatim test sketches under each)
    - .planning/phases/49-ios-speaker-route-fix/49-CONTEXT.md (D-04, D-05, D-08, D-09 — the contracts these tests lock)
    - vitest.setup.ts (confirm no `Audio` polyfill exists — tests must stub per-test)
  </read_first>
  <behavior>
    - Test 1 (attribute wiring, D-05 + Pattern E): `createAudioEngine` constructs an `Audio` element exactly once, and that element has `playsInline === true`, `loop === true`, `muted === false`, `volume === 0.0001`. Asserts via `vi.stubGlobal('Audio', SpyAudio)` capturing the constructed instance.
    - Test 2 (close() teardown, D-08 + Pattern F): `engine.close()` invokes `pause()` exactly once and `removeAttribute('src')` exactly once on the silent-loop element. Uses a `SpyAudio` class exposing per-instance `pause` and `removeAttribute` mocks.
    - Test 3 (idempotency, mirrors L228-250 close() idempotency test): Calling `engine.close()` twice in sequence still invokes `pause` and `removeAttribute` at most once total — the existing `if (closed) return` guard at audioEngine.ts L282 covers the new teardown for free.
    - Test 4 (silent-absorb on .play() reject, D-09 + Pattern G inverse): When the stubbed `Audio.play()` returns a rejected promise (`DOMException('autoplay denied', 'NotAllowedError')`), `createAudioEngine({ timbre: 'bowl' })` still resolves with a defined engine — the rejection MUST NOT propagate. Verifies the inverse of the AC-rejection test at L73-85.
    - Test 5 (sync-construct order, D-04 + Pattern H): Using two ordered stubs that push to a shared `callOrder: string[]` array on construction, after `createAudioEngine({ timbre: 'bowl' })` the array equals `['AudioContext', 'Audio']` — confirming `new AudioContext()` precedes `new Audio(...)` in the factory head.
  </behavior>
  <action>
    Edit `src/audio/audioEngine.test.ts` — APPEND the five new tests inside the existing `describe('audioEngine', ...)` block. Do NOT modify any existing test, do NOT change the `afterEach` cleanup at L57-60, do NOT add top-level setup outside the describe.

    Test sketches are in `49-PATTERNS.md` Patterns E, F, G, H — copy their shape verbatim. Group the five new tests at the END of the describe block (after the final existing `it(...)` at L350+) under a `// Phase 49: iOS silent-loop element` section header comment.

    Test 1 — title: `'createAudioEngine constructs a silent-loop <audio> element with locked attributes (D-05)'`
    - Define `class SpyAudio` with public-property fields initialized to safe defaults (`playsInline = false; loop = false; muted = true; volume = 1`), `pause = vi.fn()`, `removeAttribute = vi.fn()`, `play = vi.fn(async () => {})`, and a constructor `constructor(_src?: string) {}` (suppress the unused-param lint per Pattern E sketch).
    - Capture the constructed instance: track via `let constructed: SpyAudio | null = null` inside the test, set it in the constructor body via `constructed = this` (in the `constructor`, assign to an outer `let` — vitest pattern is fine).
    - `vi.stubGlobal('Audio', SpyAudio)` BEFORE `createAudioEngine`.
    - After `await createAudioEngine({ timbre: 'bowl' })`, assert `constructed !== null`, `constructed.playsInline === true`, `constructed.loop === true`, `constructed.muted === false`, `constructed.volume === 0.0001`.
    - Always `await engine.close()` at the end of the test body (mirror existing tests).

    Test 2 — title: `'engine.close() pauses the silent-loop element and clears its src (D-08 teardown)'`
    - Use a `SpyAudio` class with `pause = vi.fn()` and `removeAttribute = vi.fn()` as per-instance mocks. Same shape as Test 1.
    - `vi.stubGlobal('Audio', SpyAudio)`.
    - `const engine = await createAudioEngine({ timbre: 'bowl' }); await engine.close()`.
    - Capture the instance via the same outer-`let` pattern from Test 1, then assert `pauseSpy.mock.calls.length === 1` (or equivalent) and `removeAttributeSpy.toHaveBeenCalledWith('src')`. Use either a captured reference (`constructed.pause` is the `vi.fn()`) or a static factory pattern — both fit existing conventions.

    Test 3 — title: `'engine.close() is idempotent for silent-loop teardown — pause and removeAttribute called at most once across two close() calls'`
    - Same `SpyAudio` setup as Test 2.
    - `const engine = await createAudioEngine({ timbre: 'bowl' }); await engine.close(); await engine.close()`.
    - Assert `constructed.pause` called exactly 1 time, `constructed.removeAttribute` called exactly 1 time.

    Test 4 — title: `'createAudioEngine resolves even when silent-loop element.play() rejects (D-09 silent-absorb)'`
    - Use a `RejectingAudio` class with `play = vi.fn(async () => { throw new DOMException('autoplay denied', 'NotAllowedError') })` and the same default attribute/method shape as `SpyAudio`. Constructor `constructor(_src?: string) {}`.
    - `vi.stubGlobal('Audio', RejectingAudio)`.
    - `const engine = await createAudioEngine({ timbre: 'bowl' })`.
    - Assert `engine !== undefined` (the engine MUST be returned) and `typeof engine.close === 'function'`.
    - `await engine.close()` to clean up.
    - Critically: do NOT use `await expect(...).rejects` — that is the AC-reject test at L73-85; THIS test verifies the inverse (D-09 absorbs).

    Test 5 — title: `'sync-construct order: new AudioContext() precedes new Audio() in createAudioEngine (D-04)'`
    - Define `const callOrder: string[] = []` inside the test body.
    - Define `class OrderedAC` matching the existing `ProbeAC` shape at L202-220 (state, sampleRate, destination, currentTime, resume, close, createOscillator, createGain, createBiquadFilter, addEventListener, removeEventListener — all as `vi.fn()`) with `constructor() { callOrder.push('AudioContext') }`.
    - Define `class OrderedAudio` matching the Test 1 `SpyAudio` shape with `constructor(_src?: string) { callOrder.push('Audio') }`.
    - `vi.stubGlobal('AudioContext', OrderedAC); vi.stubGlobal('Audio', OrderedAudio)`.
    - `const engine = await createAudioEngine({ timbre: 'bowl' })`.
    - Assert `callOrder` equals `['AudioContext', 'Audio']` (D-04 sync invariant — AudioContext first, then Audio, both before any await).
    - `await engine.close()` to clean up.

    After appending all five tests:
    - Run `npm run test:run -- src/audio/audioEngine.test.ts` — all 24 tests (19 existing + 5 new) MUST pass.
    - Run `npm run lint` — silence per-test `_src` unused-param + `useless-constructor` per the existing convention shown in the Pattern E/F/G/H sketches (`// eslint-disable-next-line @typescript-eslint/no-useless-constructor, @typescript-eslint/no-unused-vars` immediately above the constructor line).
    - Run the full suite once: `npm run test:run` — total count is the v2.1 baseline + 5 new (no other test file changed, no regressions).
    - Commit per QUAL-01: `test(49-01): cover silent-loop element wiring, teardown, silent-absorb, and sync-construct order`.

    DO NOT touch: `src/hooks/useAudioCues.test.tsx` (NO-CHANGE directive per 49-PATTERNS.md — element is invisible at hook seam, D-04), `vitest.setup.ts` (no new `Audio` polyfill — per-test stubs only).
  </action>
  <verify>
    <automated>npm run test:run -- src/audio/audioEngine.test.ts &amp;&amp; npm run test:run &amp;&amp; npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `grep -cE "^\s*it\(" src/audio/audioEngine.test.ts` returns 24 (19 existing + 5 new).
    - Source assertion: `grep -nE "Phase 49: iOS silent-loop" src/audio/audioEngine.test.ts` returns at least 1 hit (section header).
    - Source assertion: each of the five test titles appears verbatim — `grep -F "'createAudioEngine constructs a silent-loop <audio> element with locked attributes (D-05)'"`, `grep -F "'engine.close() pauses the silent-loop element and clears its src (D-08 teardown)'"`, `grep -F "'engine.close() is idempotent for silent-loop teardown"`, `grep -F "'createAudioEngine resolves even when silent-loop element.play() rejects (D-09 silent-absorb)'"`, `grep -F "'sync-construct order: new AudioContext() precedes new Audio() in createAudioEngine (D-04)'"` — each returns ≥ 1 hit.
    - Source assertion: `grep -nE "vi\.stubGlobal\(['\"]Audio['\"]" src/audio/audioEngine.test.ts` returns ≥ 5 hits (one per new test).
    - Source assertion: `grep -nE "playsInline\s*===\s*true|playsInline.*toBe\(true\)" src/audio/audioEngine.test.ts` returns ≥ 1 hit (Test 1 attribute assertion).
    - Source assertion: `grep -nE "volume.*0\.0001|toBe\(0\.0001\)" src/audio/audioEngine.test.ts` returns ≥ 1 hit (Test 1 volume assertion).
    - Source assertion: `grep -nE "removeAttribute.*toHaveBeenCalledWith\(['\"]src['\"]" src/audio/audioEngine.test.ts` returns ≥ 1 hit (Test 2 src clear).
    - Source assertion: `grep -nE "NotAllowedError" src/audio/audioEngine.test.ts` returns ≥ 1 hit (Test 4 silent-absorb).
    - Source assertion: `grep -nE "callOrder.*toEqual.*AudioContext.*Audio" src/audio/audioEngine.test.ts` returns ≥ 1 hit OR equivalent verifying `['AudioContext', 'Audio']` order (Test 5).
    - Test command: `npm run test:run -- src/audio/audioEngine.test.ts` exits 0 with all 24 tests green.
    - Test command: `npm run test:run` exits 0 with total = v2.1 baseline + 5 (no other suite regressed).
    - Test command: `npm run lint` exits 0.
    - CLI output: `git diff src/hooks/useAudioCues.test.tsx` is empty (no edit to the hook tests — D-04 NO-CHANGE directive).
    - CLI output: `git diff vitest.setup.ts` is empty (no polyfill added — DEPS-01 in spirit + scope discipline).
    - CLI output: `git diff package.json` is empty (DEPS-01 — no new dep).
  </acceptance_criteria>
  <done>
    `src/audio/audioEngine.test.ts` contains the five new Phase 49 tests covering attribute wiring (D-05), close() teardown (D-08), idempotency, silent-absorb on .play() reject (D-09), and sync-construct order (D-04). Full test suite green at v2.1 baseline + 5. Lint clean. No edit to `useAudioCues.test.tsx`, `vitest.setup.ts`, or `package.json`.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user gesture → engine factory | Click handler in `App.tsx` → `useAudioCues.start()` → `createAudioEngine()`; gesture activation token is consumed inside the factory head. The new `<audio>` element rides this same gesture chain. |
| engine factory ↔ browser DOM (Audio + AudioContext) | All silent-loop side effects (autoplay policy decision, audio routing) happen inside browser/DOM scope. The page does not transmit, persist, or render the audio data. |
| (no other boundary) | No network calls, no auth, no user-supplied input feeds the silent-loop element (source is a hard-coded base64 constant). No new file written, no new IPC. |

## STRIDE Threat Register

Phase 49 surface is trivial — no new runtime deps, no network, no auth, no user input, no persistence. Source is a hard-coded base64 data URL of ~244 bytes. The honest threat surface is small.

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-49-01 | Tampering | `SILENT_WAV_DATA_URL` constant in `audioEngine.ts` | accept | Constant is bundled into the immutable JS output; tampering requires either source-tree write access (covered by the existing per-commit green-gate QUAL-01 + git history) or a CDN/build-pipeline compromise (out of Phase 49 scope; Vite build integrity covered at platform layer). No additional mitigation needed. |
| T-49-02 | Denial of Service | Silent `<audio>` element runs always-on (D-02, no UA gating) on every successful `createAudioEngine()` call | mitigate | Element is constructed inside the engine factory only (D-07 — no autoplay on page load) and torn down inside `engine.close()` (D-08). One element per engine instance, one engine instance per session. CPU/memory cost of a looping ~25 ms near-silent buffer is negligible. Non-iOS regression is validated by the device-test plan (Plan 02 — IOS-04). If a measurable regression appears, UA gating is the documented follow-up (49-CONTEXT.md D-02 + deferred-ideas). |
| T-49-03 | Information Disclosure | Silent `<audio>` element with `loop=true` could in principle be inspected by other browser-level code (extensions, etc.) | accept | Element is a programmatically-constructed `Audio()` instance held in closure inside the engine factory — never attached to the DOM tree, never exposed via the `AudioEngine` interface (Pattern C — closure-captured, no getter/setter). No information disclosure surface beyond what `new Audio()` itself exposes to any extension on any page using `<audio>`. |
| T-49-04 | Elevation of Privilege | `.play()` rejection silently absorbed (D-09) could mask an underlying autoplay-policy regression | accept | D-09 is the explicit posture: silent-loop failure is non-essential — session continues, iOS users without speaker routing are no worse than pre-Phase-49 state. AC `resume()` rejection (the privileged failure mode that DOES affect session essential infra) keeps its re-throw behavior at L139-143; THAT path is unchanged by Phase 49. |
| T-49-SC | Tampering | npm/pip/cargo installs | mitigate | DEPS-01 invariant: NO new runtime deps added in this phase. `dependencies` in `package.json` unchanged (verified by `git diff package.json` returning empty). No package-legitimacy audit needed because no installs occur. |

ASVS L1 mapping: Phase surface does not touch authentication (V2), session management (V3), access control (V4), input validation (V5 — no user input feeds the silent loop), error handling/logging (V7 — silent-absorb is the documented posture), data protection (V8 — no PII, no secrets). The relevant ASVS L1 control for this phase is V14 (configuration / DOS) which T-49-02 addresses.

</threat_model>

<verification>
## Plan-level verification

Run as part of the executor's close-out (mirrors the QUAL-01 per-commit green-gate):

1. `npm run build` — TypeScript compiles, Vite build succeeds.
2. `npm run lint` — ESLint clean.
3. `npm run test:run` — total test count = v2.1 baseline (1283) + 5 new = 1288 (or current baseline + 5 if other phases have shifted the count). All green.
4. `git diff package.json` empty (DEPS-01 — no new runtime dep).
5. `git diff src/hooks/useAudioCues.ts src/hooks/useAudioCues.test.tsx vitest.setup.ts` empty (no incidental edits outside the two target files).

## Source-of-truth verification

- `audioEngine.ts` retains the L1 file-header "Zero React imports" invariant.
- `audioEngine.ts` SILENT_WAV_DATA_URL length ≤ 400 base64 chars (≤ 500 raw bytes after decode — D-03 + spike-lock constraint).
- `audioEngine.ts` between L129 and the first `await` contains only sync code (D-04/D-06).
- `engine.close()` element teardown lives inside the existing `if (closed) return` guard (Pattern D idempotency).
</verification>

<success_criteria>
- All Task 1 and Task 2 acceptance criteria pass.
- `npm run build && npm run lint && npm run test:run` exits 0 (QUAL-01 green-gate).
- `package.json` `dependencies` field is byte-identical to pre-Phase-49 state (DEPS-01).
- Two atomic commits land on `main` (impl, tests) — both pass the green-gate.
- IOS-05 is automatically verified (sync gesture chain + close() teardown source assertions + tests).
- IOS-01 is partially verified (code is in place — device validation lands in Plan 02).
</success_criteria>

<output>
Create `.planning/phases/49-ios-speaker-route-fix/49-01-SUMMARY.md` when done. Capture:
- Final SILENT_WAV_DATA_URL length (chars).
- Final test count (this file + total suite).
- Final QUAL-01 gate output (build/lint/test exit codes).
- Confirmation that DEPS-01 holds (`package.json` diff empty).
- Confirmation that no edit landed in `useAudioCues.ts`, `useAudioCues.test.tsx`, or `vitest.setup.ts`.
- Handoff note for Plan 02: device-validation checkpoint (IOS-01..IOS-04) is now unblocked.
</output>
</content>
</invoke>