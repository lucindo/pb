---
phase: 38-variant-removal
plan: 02
type: execute
wave: 2
depends_on:
  - 38-01
files_modified:
  - src/domain/settings.ts
  - src/storage/prefs.ts
  - src/storage/prefs.test.ts
  - src/content/strings.ts
  - src/content/strings.test.ts
  - src/components/SettingsDialog.tsx
autonomous: true
requirements:
  - VAR-03
  - VAR-05
must_haves:
  truths:
    - "src/domain/settings.ts no longer exports VisualVariantId, VARIANT_OPTIONS, isValidVariant, or DEFAULT_VARIANT (per D-01)."
    - "src/storage/prefs.ts no longer declares prefs.variant on UserPrefs, no longer seeds DEFAULT_PREFS.variant, no longer exports coerceVariant, and no longer reads r.variant inside coercePrefs (per D-01)."
    - "src/content/strings.ts no longer declares UiStrings.variants (3-key block) or UiStrings.settings.variantLabel; EN catalog no longer carries the `variants: { orb, square, diamond }` block or the `variantLabel: 'Variant'` row; PT-BR catalog no longer carries the `variants: { orb: 'Esfera', square: 'Quadrado', diamond: 'Losango' }` block or the `variantLabel: 'Variante'` row (per D-08)."
    - "src/components/SettingsDialog.tsx Pick<UiStrings, ...> union no longer contains 'variants' at L29."
    - "src/storage/prefs.test.ts no longer imports coerceVariant / DEFAULT_VARIANT / VARIANT_OPTIONS / VisualVariantId; all variant-axis test cases are deleted or surgically stripped; remaining coercer tests for theme / timbre / cue / locale pass against the 4-field UserPrefs (per D-10)."
    - "src/content/strings.test.ts no longer imports VARIANT_OPTIONS; the 'every locale has variants entries' test (L46-52) is deleted; the settings-label test no longer asserts on settings.variantLabel."
    - "pnpm tsc --noEmit exits 0 after this plan's commits — the type system has no orphan consumer of VisualVariantId or UiStrings.variants."
    - "pnpm test exits 0 across the full suite (Plan 03's App.tsx state strip + cross-cutting test fixtures land in Plan 03; this plan's diff stays clean against the current App.tsx state because Plan 01 already removed the BreathingShape JSX prop reads and the App test fixtures with `variant: 'orb'` are tolerated by the persisted-envelope Phase 8 D-01 spread-then-override pattern — see <interfaces>)."
    - "D-01 satisfied: prefs.variant is deleted from UserPrefs / DEFAULT_PREFS / coercePrefs; coerceVariant export deleted; persisted unknown key tolerated via Phase 8 D-01 envelope spread."
    - "D-08 satisfied: clean cut — variants block + variantLabel deleted from UiStrings type + both EN + PT-BR catalogs. No orphan strings."
    - "VAR-03 closed: VariantPicker is gone from SettingsDialog (Plan 01) AND the type-level prop surface (`'variants'` in the Pick union) is gone (this plan)."
    - "VAR-05 closed implicitly: with `prefs.variant` deleted, a returning user with persisted `{prefs: {variant: 'square'}}` reads via coercePrefs which ignores unknown keys. The render path is always OrbShape (Plan 01). No STATE_VERSION bump."
  artifacts:
    - path: src/domain/settings.ts
      provides: "Settings module without the VisualVariantId / VARIANT_OPTIONS / isValidVariant / DEFAULT_VARIANT 9-line block (L117-125 in pre-Phase-38 state)."
      forbids: "VisualVariantId|VARIANT_OPTIONS|isValidVariant|DEFAULT_VARIANT"
    - path: src/storage/prefs.ts
      provides: "UserPrefs shrunk to 4 fields (theme, timbre, cue, locale); coercePrefs reads only those 4 keys; coerceVariant export deleted."
      forbids: "variant:|coerceVariant|DEFAULT_VARIANT|VisualVariantId|isValidVariant"
    - path: src/content/strings.ts
      provides: "UiStrings type with the variants block and settings.variantLabel removed; EN and PT-BR catalogs aligned to the new type."
      forbids: "variants:\\s*\\{|variantLabel"
    - path: src/components/SettingsDialog.tsx
      provides: "Pick<UiStrings, ...> union without 'variants' (Plan 01 already removed the VariantPicker import + render and the Theme→Variant→Cue→Timbre comment)."
      forbids: "'variants'"
  key_links:
    - from: src/storage/prefs.ts
      to: src/domain/settings.ts
      via: "the import block at L7-23 — after this plan, no `DEFAULT_VARIANT` / `isValidVariant` / `VisualVariantId` names appear in the import names"
      pattern: "import.*from\\s+'\\.\\./domain/settings'"
    - from: src/content/strings.ts
      to: src/components/SettingsDialog.tsx
      via: "the Pick<UiStrings, 'settings' | 'themes' | 'cue' | 'timbres' | 'install'> type narrowing — once strings.ts drops the variants key, the Pick union must drop 'variants' in the same commit boundary"
      pattern: "Pick<UiStrings,\\s*'settings'\\s*\\|\\s*'themes'\\s*\\|\\s*'cue'"
---

<objective>
Strip the variant axis from the data layer and the i18n surface in one coordinated cut. Delete the `VisualVariantId` type and its three siblings (`VARIANT_OPTIONS`, `isValidVariant`, `DEFAULT_VARIANT`) from `src/domain/settings.ts`; delete the `variant` field from `UserPrefs` + `DEFAULT_PREFS` + the `coerceVariant` import/export inside `coercePrefs` in `src/storage/prefs.ts`; delete the `UiStrings.variants` block + `UiStrings.settings.variantLabel` from `src/content/strings.ts` (type + EN + PT-BR catalogs); remove the `'variants'` token from the `SettingsDialog.tsx` Pick union; and strip variant-axis cases from `prefs.test.ts` + `strings.test.ts`. Closes VAR-03 (type-level half — Plan 01 closed the render half) and VAR-05 (persisted-pref coercion drop — the field disappears entirely; Phase 8 D-01 envelope tolerance handles the unknown key on read; no STATE_VERSION bump).

Purpose: collapse the data + i18n + type surface so any future contributor reading the codebase sees a single shape system (OrbShape) with no residual variant scaffolding. Tiger Style atomic-commit-per-logical-change: 3 sequential commits inside this plan (domain + storage, i18n, test fixtures) so a future bisect can name the layer that introduced any regression.

Output: 5 surgically edited files + 1 test fixture strip (prefs.test.ts) + 1 test fixture strip (strings.test.ts).

CONTEXT D-01 wording correction: REQUIREMENTS.md VAR-05 says "coerced via `coerceSettings`" but the actual coercer was `coerceVariant` inside `coercePrefs` (src/storage/prefs.ts). The intent — "forward-compat read, no STATE_VERSION bump" — is satisfied by the field-deletion path (Phase 8 D-01 envelope tolerance preserves the persisted unknown key; coercePrefs ignores it on read; the only render path is OrbShape per Plan 01). The planner does NOT surface a sidecar REQUIREMENTS.md edit in this plan — that is deferred (CONTEXT §deferred wording-fix bullet).
</objective>

<execution_context>
@$HOME/.claude-personal-personal/get-shit-done/workflows/execute-plan.md
@$HOME/.claude-personal-personal/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/38-variant-removal/38-CONTEXT.md
@.planning/phases/38-variant-removal/38-PATTERNS.md
@.planning/phases/38-variant-removal/38-01-SUMMARY.md
@.planning/phases/37-stats-ui-removal/37-01-PLAN.md
@.planning/phases/37-stats-ui-removal/37-LEARNINGS.md

<interfaces>
<!-- The exact source blocks to delete. Pulled from the codebase at plan time so the executor performs precise excisions, not semantic guesses. -->

From src/domain/settings.ts (L117-125 — the 9-line block to delete):
```typescript
export type VisualVariantId = 'orb' | 'square' | 'diamond'

export const VARIANT_OPTIONS = ['orb', 'square', 'diamond'] as const satisfies readonly VisualVariantId[]

export function isValidVariant(v: unknown): v is VisualVariantId {
  return typeof v === 'string' && (VARIANT_OPTIONS as readonly string[]).includes(v)
}

export const DEFAULT_VARIANT: VisualVariantId = 'orb'
```

After: entire 9-line block deleted. No replacement. The surrounding coercer block layout (isValidTheme, isValidTimbre, isValidCue, isValidLocale) is unchanged.

From src/storage/prefs.ts (verified line numbers — confirm with grep before edits since prior plans may have shifted):
- L10: `  DEFAULT_VARIANT,` — DELETE the named import.
- L15: `  isValidVariant,` — DELETE the named import.
- L20: `  type VisualVariantId,` — DELETE the named import.
- L30: `  variant: VisualVariantId` — DELETE the field from `UserPrefs`.
- L38: `  variant: DEFAULT_VARIANT,` — DELETE the field from `DEFAULT_PREFS`.
- L50: comment line `// (same kind of value-level coercion as coerceVariant's pre-swap 'ring' remap).` — DELETE the parenthetical (or the whole comment if it no longer informs).
- L57-59: the standalone `coerceVariant` export — DELETE the entire 3-line function:
  ```typescript
  export function coerceVariant(raw: unknown): VisualVariantId {
    return isValidVariant(raw) ? raw : DEFAULT_VARIANT
  }
  ```
- L78: `    variant: coerceVariant(r.variant),` — DELETE the line from `coercePrefs`'s return object.
- L70 (approximate) — UPDATE the WHY-comment about "we only read five known keys" to "we only read four known keys" (theme + timbre + cue + locale).

After D-01: `coercePrefs` reads only `r.theme`, `r.timbre`, `r.cue`, `r.locale`. `UserPrefs` has 4 fields. A persisted object with `{prefs: {variant: 'square'|'diamond'|'orb', ...}}` passes through Phase 8 D-01 envelope spread-then-override — the unknown `variant` key is harmlessly preserved on disk (unless overwritten in a future write) and ignored at read time.

From src/content/strings.ts (verified line numbers — confirm with grep):
- L31 (UiStrings.settings interface): `    readonly variantLabel: string` — DELETE.
- L44-48 (UiStrings.variants block):
  ```typescript
    readonly variants: {
      readonly orb: string
      readonly square: string
      readonly diamond: string
    }
  ```
  DELETE entire 5-line block.
- L195 (EN catalog, settings): `      variantLabel: 'Variant',` — DELETE.
- L208-212 (EN catalog, variants):
  ```typescript
      variants: {
        orb: 'Orb',
        square: 'Square',
        diamond: 'Diamond',
      },
  ```
  DELETE entire 5-line block.
- L357 (PT-BR catalog, settings): `      variantLabel: 'Variante',` — DELETE.
- L370-374 (PT-BR catalog, variants):
  ```typescript
      variants: {
        orb: 'Esfera',
        square: 'Quadrado',
        diamond: 'Losango',
      },
  ```
  DELETE entire 5-line block.

LOCKED_COPY: variant display strings are NOT in LOCKED_COPY (verified Phase 38 CONTEXT code_context line 128, Phase 19 byte-equality guard untouched).

From src/components/SettingsDialog.tsx L29 (Pick union):
```typescript
strings: Pick<UiStrings, 'settings' | 'themes' | 'variants' | 'cue' | 'timbres' | 'install'>
```
becomes:
```typescript
strings: Pick<UiStrings, 'settings' | 'themes' | 'cue' | 'timbres' | 'install'>
```
(The 'variants' token disappears; the surrounding Pick keys stay in the order verified at plan time — `settings`, `themes`, `cue`, `timbres`, `install`.)

From src/storage/prefs.test.ts (verified line numbers — confirm with grep; primary excision targets):
- L7: `  coerceVariant,` — DELETE from the imports list.
- L19: `  DEFAULT_VARIANT,` — DELETE from the imports list.
- L24: `  VARIANT_OPTIONS,` — DELETE from the imports list.
- L52 fixture: `const valid: UserPrefs = { theme: 'dark', timbre: 'bell', variant: 'square', cue: 'nose', locale: 'pt-BR' }` — REMOVE the `variant: 'square'` field.
- L56-58 it() "falls back PER FIELD when theme is invalid (D-17) — keeps timbre + variant + cue + locale" — UPDATE the title to "keeps timbre + cue + locale"; UPDATE the fixture object input and `.toEqual()` expected object to drop `variant`.
- L61-63 it() "falls back PER FIELD when timbre is invalid" — same fixture-and-title update.
- L66-68 it() "falls back PER FIELD when variant is invalid" — DELETE the entire it() block (no variant field to test).
- L71-73 it() "falls back PER FIELD when cue is invalid — keeps theme + timbre + variant + locale" — UPDATE title + fixtures to drop variant.
- L76-78 it() "falls back PER FIELD when locale is invalid (D-17) — keeps theme + timbre + variant + cue" — UPDATE title + fixtures.
- L83-84 it() (the omit-locale fallback case) — UPDATE the fixture + `.toEqual()` to drop variant.
- L91 fixture: `const polluted: unknown = JSON.parse('{"theme":"system","timbre":"bowl","variant":"orb","cue":"labels","locale":"en","__proto__":{"polluted":true}}')` — UPDATE the JSON string to drop `"variant":"orb",` so the proto-pollution test now uses a 4-field envelope (the assertion that __proto__ is NOT spread is unaffected by the missing variant).
- L98 describe() "coerceTheme / coerceTimbre / coerceVariant / coerceCue / coerceLocale (D-10 per-field)" — UPDATE the title to "coerceTheme / coerceTimbre / coerceCue / coerceLocale".
- L119 comment "the bowl default. coerceVariant's 'ring'→default pattern is NOT equivalent here because..." — UPDATE to drop the coerceVariant reference (or rewrite to use coerceTheme or another surviving coercer as the analog).
- L143-149 (the fixture asserting full prefs object with `variant: 'square'`) — DELETE the `variant: 'square'` field; UPDATE the `result.variant` assertion at L149 (`expect(result.variant).toBe('square')`) by DELETING that line entirely.
- L154-160 it() "coerceVariant accepts all VARIANT_OPTIONS members and rejects invalid values" — DELETE the entire it() block.
- L164-169 it() "coerceVariant('ring') → DEFAULT_VARIANT ('orb') — forward-compat for pre-swap localStorage values" — DELETE the entire it() block.

From src/content/strings.test.ts (verified line numbers):
- L6: `  VARIANT_OPTIONS,` — DELETE from the imports list.
- L26 it() "every locale has non-empty settings.title, settings.close, settings.themeLabel, settings.variantLabel, settings.cueLabel, settings.timbreLabel, settings.languageLabel" — UPDATE the title to drop "settings.variantLabel"; DELETE the `expect(UI_STRINGS[locale].settings.variantLabel.length).toBeGreaterThan(0)` assertion at L31.
- L46-52 it() "every locale has variants entries for every VARIANT_OPTIONS id" — DELETE the entire it() block.

Compiler cascade (Plan 01 already ran):
- App.tsx no longer destructures `liveVariant` from `useVisualVariant()` (Plan 01 replaced L224 with a `liveVariant: VisualVariantId | null = null` shim — Plan 02 does NOT touch that shim; Plan 03 deletes it together with the rest of the sessionVariantRef + capture/clear thread).
- BreathingShape.tsx is gone (Plan 01); BreathingShape.test.tsx is gone (Plan 01); VariantPicker.tsx is gone (Plan 01); useVisualVariant + useVariantChoice are gone (Plan 01); SettingsDialog.tsx no longer renders <VariantPicker /> (Plan 01).
- App.session.test.tsx still imports `type VisualVariantId` (L9) and references `seedVariant` (L346 helper) and the VARIANT-03 + VARIANT-02 describe blocks (L342+) — these are Plan 03's cross-cutting test strip scope; this plan does NOT touch them. However, deleting `VisualVariantId` from src/domain/settings.ts WILL break the App.session.test.tsx L9 import in this plan's commit boundary.
- Resolution: this plan also strips `VisualVariantId` from the App.session.test.tsx L9 import line and DELETES the entire VARIANT-03 + VARIANT-02 describe blocks in this plan's domain-settings commit boundary so tsc stays green. (Note: this overlaps with Plan 03's stated scope; Plan 03 handles the App.session.test.tsx surgical strip of the seedVariant helper + the cross-cutting Variant references in App.session.test.tsx's beforeEach + other shared fixtures. The VARIANT-03/VARIANT-02 blocks themselves are Plan 02's because the type deletion in Plan 02 forces them to die in the same commit boundary.)

Phase 37 LEARNINGS reminder: when stripping i18n catalog entries, delete the type-level block FIRST → run `pnpm tsc --noEmit` → fix every fixture the compiler flags → delete the EN + PT-BR catalog entries. Reverse order works too but leaves a window where the catalog entries are typed-orphans.
</interfaces>
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| disk → loadPrefs() | Returning user with persisted `{prefs: {variant: 'square'|'diamond'|'orb', ...}}` — handled by Phase 8 D-01 envelope tolerance (unknown key ignored on read); the only render path is OrbShape (Plan 01) |
| build-time → runtime | type-removal cascade — `tsc --noEmit` is the linter for every orphan consumer of `VisualVariantId`; the App.session.test.tsx VARIANT-03 block must die in the same commit as the type to avoid a broken intermediate state |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-38-06 | T (tampering — persisted-data compatibility) | localStorage `prefs.variant` for returning users | accept | Phase 8 D-01 envelope tolerance preserves unknown keys on disk; coercePrefs ignores `r.variant` on read (the key no longer matches any UserPrefs field); render path is always OrbShape per Plan 01 — no behavioral regression. **Not a vulnerability** — explicit CONTEXT D-01 design decision. |
| T-38-07 | I (information disclosure — orphan i18n string) | strings.ts catalogs | mitigate | Clean cut per CONTEXT D-08: delete type block first → tsc flags orphan consumers → delete catalog entries. VAR-06 drift-guard (Plan 04) catches future re-entry. |
| T-38-08 | T (tampering — orphan coercer) | coerceVariant export | mitigate | Single-commit deletion: import-strip + UserPrefs field + DEFAULT_PREFS field + coercePrefs return object + coerceVariant function — all five sites in the same commit. tsc --noEmit at commit-end is the safety net. |
| T-38-09 | D (denial — broken test suite mid-plan) | App.session.test.tsx VARIANT-03 + VARIANT-02 blocks | mitigate | This plan's domain-settings commit DELETES those describe blocks atomically with the type removal — no intermediate broken state. (Plan 03's separate App.session.test.tsx work covers the seedVariant helper + other cross-cutting variant residue.) |
| T-38-10 | I (info — VAR-05 wording drift) | REQUIREMENTS.md VAR-05 says `coerceSettings` but actual is `coerceVariant` inside `coercePrefs` | accept | Field-deletion path satisfies the VAR-05 intent ("forward-compat, no STATE_VERSION bump"); REQUIREMENTS.md tidy is explicitly deferred (CONTEXT §deferred). Not a regression. |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Delete VisualVariantId / VARIANT_OPTIONS / isValidVariant / DEFAULT_VARIANT from src/domain/settings.ts; strip the variant field + coerceVariant from src/storage/prefs.ts; surgical-strip variant cases from src/storage/prefs.test.ts; delete VARIANT-03 + VARIANT-02 describe blocks from src/app/App.session.test.tsx (force-killed by the type deletion in this commit boundary)</name>
  <files>
    - src/domain/settings.ts
    - src/storage/prefs.ts
    - src/storage/prefs.test.ts
    - src/app/App.session.test.tsx
  </files>
  <read_first>
    - src/domain/settings.ts L117-125 (the 9-line VisualVariantId block — see <interfaces> for exact verbatim)
    - src/storage/prefs.ts (all variant references: L7-23 imports, L30 UserPrefs field, L38 DEFAULT_PREFS, L50 comment, L57-59 coerceVariant function, L78 coercePrefs return)
    - src/storage/prefs.test.ts L1-180 (all variant references: L7/L19/L24 imports, L52/L57/L62/L67/L72/L77/L83/L91 fixtures, L98 describe title, L119 comment, L143-149 + L154-160 + L164-169 it() blocks)
    - src/app/App.session.test.tsx L1-50 (line 9: `import type { CueStyleId, TimbreId, VisualVariantId } from '../domain/settings'`)
    - src/app/App.session.test.tsx L340-450 (the VARIANT-03 capture-at-session-start describe block + the VARIANT-02 zero-regression it() — Plan 03's seedVariant helper at L346 is ALSO inside this scope; this task deletes the entire describe block including the helper)
    - .planning/phases/38-variant-removal/38-PATTERNS.md §"src/domain/settings.ts" + §"src/storage/prefs.ts (surgical edit — delete variant field + coercer)" + §"src/storage/prefs.test.ts (surgical edit — strip variant cases)"
    - .planning/phases/37-stats-ui-removal/37-01-PLAN.md (Phase 37 strings.ts type-first pattern is the analog for this plan's commit ordering)
  </read_first>
  <action>
    Order: domain → storage → storage tests → App.session.test.tsx VARIANT-03 strip. Run `pnpm tsc --noEmit` after each file edit; only commit at the end of Task 1 when tsc + the prefs test suite are both green.

    1. **`src/domain/settings.ts`** — DELETE the L117-125 block (9 lines: `export type VisualVariantId`, blank, `export const VARIANT_OPTIONS`, blank, `export function isValidVariant`, function body, closing brace, blank, `export const DEFAULT_VARIANT`). Use `grep -n "VisualVariantId\\|VARIANT_OPTIONS\\|isValidVariant\\|DEFAULT_VARIANT" src/domain/settings.ts` to confirm exact line numbers before the edit. Verify with `grep -nc "VisualVariantId\\|VARIANT_OPTIONS\\|isValidVariant\\|DEFAULT_VARIANT" src/domain/settings.ts` returning 0 after.

    2. **`src/storage/prefs.ts`** — apply the 7-symbol strip per <interfaces>:
       - DELETE L10 `  DEFAULT_VARIANT,`
       - DELETE L15 `  isValidVariant,`
       - DELETE L20 `  type VisualVariantId,`
       - DELETE L30 `  variant: VisualVariantId` (the field on `UserPrefs`)
       - DELETE L38 `  variant: DEFAULT_VARIANT,` (the field on `DEFAULT_PREFS`)
       - DELETE the L50 parenthetical `// (same kind of value-level coercion as coerceVariant's pre-swap 'ring' remap).` (or rewrite the surrounding comment so the WHY no longer references coerceVariant)
       - DELETE L57-59 — the standalone `coerceVariant` export (3 lines)
       - DELETE L78 `    variant: coerceVariant(r.variant),`
       - UPDATE the L70 (approximate) WHY-comment from "we only read five known keys... raw is never spread" to "we only read four known keys" — verify exact wording with grep before edit.

       Verify with `grep -n "variant\\|VisualVariant\\|coerceVariant\\|DEFAULT_VARIANT\\|isValidVariant" src/storage/prefs.ts` returning 0 results.

    3. **`src/storage/prefs.test.ts`** — apply the surgical strip per <interfaces>:
       - DELETE L7 `  coerceVariant,`
       - DELETE L19 `  DEFAULT_VARIANT,`
       - DELETE L24 `  VARIANT_OPTIONS,`
       - For each `it()` listed in <interfaces> L52..L83: UPDATE the fixture object + the title + the `.toEqual()` expected object to drop `variant: ...` (titles drop the "variant" word; fixtures drop the variant field).
       - DELETE the L66-68 it() "falls back PER FIELD when variant is invalid (D-17)" — entire block.
       - DELETE the L143-149 fixture+assertion pair (`variant: 'square'` in fixture; `expect(result.variant).toBe('square')` assertion).
       - DELETE the L154-160 it() "coerceVariant accepts all VARIANT_OPTIONS members..." — entire block.
       - DELETE the L164-169 it() "coerceVariant('ring') → DEFAULT_VARIANT ('orb')..." — entire block.
       - UPDATE L98 describe() title from "coerceTheme / coerceTimbre / coerceVariant / coerceCue / coerceLocale (D-10 per-field)" to "coerceTheme / coerceTimbre / coerceCue / coerceLocale (D-10 per-field)".
       - UPDATE the L119 comment to drop the coerceVariant reference (or rewrite so the analog uses coerceCue).
       - UPDATE the L91 JSON-pollution fixture: `'{"theme":"system","timbre":"bowl","variant":"orb","cue":"labels","locale":"en","__proto__":{"polluted":true}}'` → `'{"theme":"system","timbre":"bowl","cue":"labels","locale":"en","__proto__":{"polluted":true}}'`.

       Verify with `grep -nE "variant|VARIANT|coerceVariant|DEFAULT_VARIANT|VisualVariantId" src/storage/prefs.test.ts` returning 0 results.

    4. **`src/app/App.session.test.tsx`** — strip the type import + the variant-axis describe block:
       - L9: `import type { CueStyleId, TimbreId, VisualVariantId } from '../domain/settings'` → `import type { CueStyleId, TimbreId } from '../domain/settings'` (drop the `, VisualVariantId` token).
       - DELETE L342-? (the entire `describe('VARIANT-03 capture-at-session-start', () => { ... })` block including the L346 `function seedVariant(...)` helper inside it). Use the `describe` opening brace at L354 and the matching closing `})` (the entire block ends after the L432-440 `VARIANT-02` it() and the L442-... lead-in countdown it()) as the deletion boundaries. Use `grep -n "describe.*VARIANT-0\\|seedVariant" src/app/App.session.test.tsx` to confirm scope before deletion. After deletion, verify `grep -n "VisualVariantId\\|seedVariant\\|VARIANT-0[23]" src/app/App.session.test.tsx` returns 0.

    5. **Verify**:
       - `pnpm tsc --noEmit` — must exit 0.
       - `pnpm test src/storage/prefs.test.ts --run` — must exit 0.
       - `pnpm test src/app/App.session.test.tsx --run` — must exit 0 (Plan 03's remaining App.session work doesn't break this run).

    6. **Commit** with: `chore(38): delete VisualVariantId / VARIANT_OPTIONS / coerceVariant + prefs.variant field; strip variant cases from prefs.test + App.session.test (VAR-05; D-01)`.
  </action>
  <verify>
    <automated>pnpm tsc --noEmit && pnpm test src/storage src/app/App.session.test.tsx --run</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "VisualVariantId\\|VARIANT_OPTIONS\\|isValidVariant\\|DEFAULT_VARIANT" src/domain/settings.ts` returns 0 results.
    - `grep -n "VisualVariantId\\|coerceVariant\\|DEFAULT_VARIANT\\|isValidVariant" src/storage/prefs.ts` returns 0 results.
    - `grep -nE "^\\s+variant:" src/storage/prefs.ts` returns 0 results (no `variant: VisualVariantId` field, no `variant: DEFAULT_VARIANT` field, no `variant: coerceVariant(...)` line).
    - `grep -nE "coerceVariant|VARIANT_OPTIONS|DEFAULT_VARIANT|VisualVariantId" src/storage/prefs.test.ts` returns 0 results.
    - `grep -n "VisualVariantId" src/app/App.session.test.tsx` returns 0 results.
    - `grep -n "seedVariant\\|VARIANT-0[23]" src/app/App.session.test.tsx` returns 0 results.
    - `pnpm tsc --noEmit` exits 0.
    - `pnpm test src/storage/prefs.test.ts --run` exits 0; surviving test cases for coerceTheme / coerceTimbre / coerceCue / coerceLocale all pass.
    - The UserPrefs interface in src/storage/prefs.ts has exactly 4 fields (theme, timbre, cue, locale) — verify with `grep -cE "readonly\\s+(theme|timbre|cue|locale):" src/storage/prefs.ts` returning 4.
    - `git diff --stat` for this commit shows 4 files modified (domain/settings.ts, storage/prefs.ts, storage/prefs.test.ts, app/App.session.test.tsx).
  </acceptance_criteria>
  <done>Domain + storage + storage-test + App.session-test variant residue is gone. UserPrefs is 4 fields. coercePrefs reads 4 fields. Persisted `prefs.variant` lands harmlessly via Phase 8 D-01 envelope tolerance. VAR-05 is closed implicitly (the field disappears; no STATE_VERSION bump). The L9 + L342-? App.session.test.tsx strip lands here because Plan 02's type deletion would otherwise create a broken commit boundary; Plan 03's seedVariant-residue work is independent.</done>
</task>

<task type="auto">
  <name>Task 2: Delete UiStrings.variants block + UiStrings.settings.variantLabel from src/content/strings.ts (type + EN + PT-BR catalogs); strip the 'variants' token from SettingsDialog.tsx Pick union; strip variant cases from src/content/strings.test.ts</name>
  <files>
    - src/content/strings.ts
    - src/content/strings.test.ts
    - src/components/SettingsDialog.tsx
  </files>
  <read_first>
    - src/content/strings.ts L1-50 (UiStrings type — locate `settings.variantLabel` at L31 and the `variants` block at L44-48)
    - src/content/strings.ts L190-220 (EN catalog — locate `variantLabel: 'Variant',` at L195 and the `variants: { orb: 'Orb', square: 'Square', diamond: 'Diamond' },` block at L208-212)
    - src/content/strings.ts L350-380 (PT-BR catalog — locate `variantLabel: 'Variante',` at L357 and the `variants: { orb: 'Esfera', square: 'Quadrado', diamond: 'Losango' },` block at L370-374)
    - src/content/strings.test.ts L1-60 (locate the L6 `VARIANT_OPTIONS,` import, the L26 settings-label test, and the L46-52 variants-exhaustiveness it())
    - src/components/SettingsDialog.tsx L1-40 (locate the L29 Pick<UiStrings, ...> union with the 'variants' token; verify Plan 01's L8 + L95 + L93 + L16 edits landed)
    - .planning/phases/38-variant-removal/38-PATTERNS.md §"src/content/strings.ts (surgical edit — typed-catalog block deletion)" + §"Frozen-catalog deletion (Phase 19/26 I18N pattern)"
    - .planning/phases/37-stats-ui-removal/37-01-PLAN.md (the strings.ts deletion order: type block → tsc → catalog blocks)
  </read_first>
  <action>
    Order: SettingsDialog.tsx Pick union (kills the only type-level consumer) → strings.ts type block → run `tsc` (must exit 0) → strings.ts EN catalog → PT-BR catalog → strings.test.ts strip. This order keeps `tsc` green at every intermediate point.

    1. **`src/components/SettingsDialog.tsx`** — strip the `'variants'` token from the L29 Pick union:
       - From: `strings: Pick<UiStrings, 'settings' | 'themes' | 'variants' | 'cue' | 'timbres' | 'install'>`
       - To:   `strings: Pick<UiStrings, 'settings' | 'themes' | 'cue' | 'timbres' | 'install'>`
       - Verify with `grep -n "'variants'" src/components/SettingsDialog.tsx` returning 0 results.

    2. **`src/content/strings.ts` — Type block** — DELETE the L31 `readonly variantLabel: string` line from the `settings` interface AND DELETE the L44-48 `readonly variants: { ... }` block (5 lines). Verify with `grep -n "variantLabel\\|variants:" src/content/strings.ts` returning matches only inside the EN + PT-BR catalogs (3 lines left at L195/208 + L357/370 area before step 3 lands).

    3. **Run `pnpm tsc --noEmit`** — must exit 0. The EN + PT-BR catalog entries STILL exist (they will violate the type until step 4) — but TypeScript object literal excess-property checking is lenient on satisfies/cast expressions; verify the actual error surface. **If tsc reports errors on the EN/PT-BR catalog lines**, proceed to step 4 immediately (do not commit the intermediate broken state).

    4. **`src/content/strings.ts` — EN catalog** — DELETE the L195 `variantLabel: 'Variant',` line AND DELETE the L208-212 `variants: { orb: 'Orb', square: 'Square', diamond: 'Diamond' },` block (5 lines).

    5. **`src/content/strings.ts` — PT-BR catalog** — DELETE the L357 `variantLabel: 'Variante',` line AND DELETE the L370-374 `variants: { orb: 'Esfera', square: 'Quadrado', diamond: 'Losango' },` block (5 lines).

    6. **Verify**: `grep -nE "variantLabel|variants:|'Square'|'Diamond'|'Quadrado'|'Losango'" src/content/strings.ts` returns 0 results. `pnpm tsc --noEmit` exits 0.

    7. **`src/content/strings.test.ts`** — strip the variant-exhaustiveness test cases per <interfaces>:
       - DELETE L6 `  VARIANT_OPTIONS,` from the imports.
       - DELETE L31 `expect(UI_STRINGS[locale].settings.variantLabel.length).toBeGreaterThan(0)` and UPDATE the L26 it() title to drop "settings.variantLabel" from the comma-separated list.
       - DELETE L46-52 (the entire `it('every locale has variants entries for every VARIANT_OPTIONS id', ...)` block).
       - Verify with `grep -nE "VARIANT_OPTIONS|variants|variantLabel" src/content/strings.test.ts` returning 0 results.

    8. **Final verify**:
       - `pnpm tsc --noEmit` — must exit 0.
       - `pnpm test src/content --run` — must exit 0.
       - `pnpm test src/components/SettingsDialog.test.tsx --run` — must exit 0 (Plan 01 already stripped VariantPicker references; the fixture in SettingsDialog.test.tsx that supplies `strings={...}` must already NOT contain a `variants` key after Plan 01 OR this plan must update it).

       Audit SettingsDialog.test.tsx fixtures with `grep -n "variants\\|variantLabel" src/components/SettingsDialog.test.tsx` — IF the test fixture still includes `variants: { orb: ..., square: ..., diamond: ... }` or `variantLabel: '...'`, **this task ALSO deletes those fixture entries** so tsc + the test suite pass (Plan 01 was scoped to delete the `<VariantPicker />` render but did not touch test fixtures; Plan 02 owns i18n-shaped fixture data in test files). If grep returns 0 results, no edit needed.

    9. **Commit** with: `chore(38): strip variant i18n surface (UiStrings.variants + variantLabel; EN + PT-BR catalogs; SettingsDialog Pick union; strings.test variant-exhaustiveness) (VAR-03; D-08)`.
  </action>
  <verify>
    <automated>pnpm tsc --noEmit && pnpm test src/content src/components/SettingsDialog.test.tsx --run</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE "variantLabel|variants:\\s*\\{" src/content/strings.ts` returns 0 results.
    - `grep -nE "'Square'|'Diamond'|'Quadrado'|'Losango'" src/content/strings.ts` returns 0 results.
    - `grep -n "'variants'" src/components/SettingsDialog.tsx` returns 0 results.
    - `grep -nE "VARIANT_OPTIONS|variants|variantLabel" src/content/strings.test.ts` returns 0 results.
    - `grep -nE "variants:\\s*\\{|variantLabel" src/components/SettingsDialog.test.tsx` returns 0 results (whether by Plan 01 or this plan's fixture cleanup).
    - `pnpm tsc --noEmit` exits 0.
    - `pnpm test src/content --run` exits 0.
    - `pnpm test src/components/SettingsDialog.test.tsx --run` exits 0.
    - The EN catalog still defines all 4 surviving settings keys (themeLabel, cueLabel, timbreLabel, languageLabel); the PT-BR catalog parallels.
  </acceptance_criteria>
  <done>UiStrings.variants block and settings.variantLabel are gone from the type + both catalogs; the SettingsDialog.tsx Pick union drops 'variants'; strings.test.ts variant-exhaustiveness it() is gone; the variantLabel assertion in the settings-label it() is gone; SettingsDialog.test.tsx fixtures are aligned. VAR-03 is fully closed (Plan 01 removed the render, Plan 02 removed the type surface). VAR-05 is closed (field disappeared in Task 1; no rendered consumer remains).</done>
</task>

</tasks>

<verification>
After both tasks land (2 commits):
- `pnpm tsc --noEmit` exits 0.
- `pnpm test src/content src/storage src/components/SettingsDialog.test.tsx src/app/App.session.test.tsx --run` exits 0.
- `git grep -E "VisualVariantId|VARIANT_OPTIONS|isValidVariant|DEFAULT_VARIANT|coerceVariant" src/` returns 0 results.
- `git grep -E "variantLabel|variants:\\s*\\{" src/` returns 0 results.
- `git grep "'variants'" src/components/SettingsDialog.tsx` returns 0 results.
- The 4-field UserPrefs interface is the new canonical shape (theme + timbre + cue + locale).
</verification>

<success_criteria>
- VisualVariantId / VARIANT_OPTIONS / isValidVariant / DEFAULT_VARIANT all deleted from src/domain/settings.ts.
- UserPrefs has 4 fields; coercePrefs reads 4 keys; coerceVariant export deleted.
- UiStrings type has no variants block and no settings.variantLabel; EN + PT-BR catalogs align.
- SettingsDialog.tsx Pick union drops 'variants'.
- prefs.test.ts coerceVariant-axis it() blocks deleted; remaining coercer tests pass.
- strings.test.ts variant-exhaustiveness it() deleted; remaining locale-exhaustiveness tests pass.
- App.session.test.tsx VARIANT-03 + VARIANT-02 describe blocks deleted (forced by VisualVariantId removal — same commit boundary).
- VAR-03 (picker removed from SettingsDialog — render via Plan 01, type via this plan) and VAR-05 (persisted-pref coercion drop) are observably closed.
- `pnpm tsc --noEmit` + `pnpm test` (scoped to touched files) both exit 0.
</success_criteria>

<output>
After completion, create `.planning/phases/38-variant-removal/38-02-SUMMARY.md` summarising:
- The 9-line block deleted from src/domain/settings.ts (VisualVariantId + VARIANT_OPTIONS + isValidVariant + DEFAULT_VARIANT).
- The 7-symbol strip from src/storage/prefs.ts (3 import names + 2 field declarations + 1 standalone function + 1 coercePrefs return field; plus the L70 + L50 comment updates).
- The full strings.ts diff (type block + EN catalog + PT-BR catalog — 3 sites × 2 deletion classes).
- The SettingsDialog.tsx L29 Pick union token strip.
- The prefs.test.ts surgical strip (3 imports + 5 it()-body fixture updates + 3 it() deletions + 1 describe title update + 1 comment update).
- The strings.test.ts strip (1 import + 1 it()-body assertion + 1 it() deletion).
- The App.session.test.tsx VARIANT-03 + VARIANT-02 + seedVariant deletion (forced by the VisualVariantId type removal).
- Confirmation that Phase 8 D-01 envelope tolerance handles the unknown persisted `prefs.variant` key (VAR-05 satisfied implicitly — no STATE_VERSION bump).
- Confirmation that `pnpm tsc --noEmit` + scoped `pnpm test` exit 0.
- Pointer forward to Plan 03 (App.tsx state strip — L224 shim, L292-293 ref+state, L487/491-492/670-671/819/869-870/938 capture/clear sites, L557/L927 dep arrays; theme.css CSS block deletion; cross-cutting App test strips for App.test.tsx + App.locale.test.tsx variant fixtures + App.session.test.tsx remaining residue).
- Pointer forward to Plan 04 (VAR-06 drift-guard).
- Tiger Style commit hygiene: 2 atomic commits this plan (domain+storage+tests; i18n+SettingsDialog+strings-test), both scoped `(38)`.
- Optional sidecar note: REQUIREMENTS.md VAR-05 wording fix (coerceSettings → coerceVariant) explicitly deferred per CONTEXT §deferred.
</output>
