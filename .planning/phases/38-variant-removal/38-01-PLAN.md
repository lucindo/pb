---
phase: 38-variant-removal
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/SquareShape.tsx
  - src/components/SquareShape.test.tsx
  - src/components/DiamondShape.tsx
  - src/components/DiamondShape.test.tsx
  - src/components/VariantPicker.tsx
  - src/components/VariantPicker.test.tsx
  - src/components/BreathingShape.tsx
  - src/components/BreathingShape.test.tsx
  - src/hooks/useVisualVariant.ts
  - src/hooks/useVisualVariant.test.ts
  - src/hooks/useVariantChoice.ts
  - src/hooks/useVariantChoice.test.ts
  - src/components/NKShape.tsx
  - src/components/NKShape.test.tsx
  - src/components/shapeConstants.ts
  - src/app/App.tsx
autonomous: true
requirements:
  - VAR-01
  - VAR-02
  - VAR-04
must_haves:
  truths:
    - "src/components/SquareShape.tsx and src/components/SquareShape.test.tsx no longer exist on disk."
    - "src/components/DiamondShape.tsx and src/components/DiamondShape.test.tsx no longer exist on disk."
    - "src/components/VariantPicker.tsx and src/components/VariantPicker.test.tsx no longer exist on disk."
    - "src/components/BreathingShape.tsx and src/components/BreathingShape.test.tsx no longer exist on disk."
    - "src/hooks/useVisualVariant.ts / useVisualVariant.test.ts no longer exist (D-02)."
    - "src/hooks/useVariantChoice.ts / useVariantChoice.test.ts no longer exist (D-02)."
    - "App.tsx renders <OrbShape /> directly at all 3 prior <BreathingShape /> call sites (~L1011, L1021 nkStarting branch, L1030) — no `variant=` prop, no BreathingShape import (D-03)."
    - "NKShape.tsx no longer imports SquareShape / DiamondShape / VisualVariantId, no longer accepts a variant prop, and always renders the OrbShape branch."
    - "pnpm tsc --noEmit exits 0 after this plan's commits (the component/hook/picker deletions + NKShape collapse are coherent — no orphan imports remain in the deleted-component scope)."
    - "pnpm test src/components and pnpm test src/hooks pass (no test imports a deleted symbol)."
    - "D-02 satisfied: both hooks are deleted in the same commit as their test twins (delete-with-component policy)."
    - "D-03 satisfied: BreathingShape is gone; OrbShape is the only shape the App renders directly; NKShape is reduced to OrbShape-only."
    - "D-09 satisfied: each component + test pair lands in the same commit via git rm."
  artifacts:
    - path: src/app/App.tsx
      provides: "App shell that imports OrbShape (not BreathingShape) and renders <OrbShape cue={...} frame={...} leadInDigit={...} strings={...} /> at the 3 prior BreathingShape sites; no variant prop threaded."
      forbids: "import.*(BreathingShape|VariantPicker|SquareShape|DiamondShape|useVisualVariant|useVariantChoice)"
    - path: src/components/NKShape.tsx
      provides: "Collapsed NKShape that always renders OrbShape — no imports of SquareShape / DiamondShape / VisualVariantId, no variant prop on NKShapeProps, no variant === 'orb' ? ... : ... dispatcher."
      forbids: "SquareShape|DiamondShape|VisualVariantId|variant:\\s*VisualVariantId"
    - path: src/components/shapeConstants.ts
      provides: "Constants file retained (consumed by OrbShape for breathing math); the docstring no longer enumerates SquareShape / DiamondShape."
      forbids: "SquareShape|DiamondShape"
  key_links:
    - from: src/app/App.tsx
      to: src/components/OrbShape.tsx
      via: "direct <OrbShape ... /> JSX render at the 3 prior BreathingShape call sites; no dispatcher wrapper"
      pattern: "<OrbShape\\s+cue="
    - from: src/components/NKShape.tsx
      to: src/components/OrbShape.tsx
      via: "always-OrbShape render path (no variant dispatch)"
      pattern: "<OrbShape\\s+frame=\\{null\\}\\s+nkPhase="
---

<objective>
Delete every shape-variant component, hook, and wrapper that depends on the multi-variant model, then collapse the 3 App.tsx `<BreathingShape variant={...} />` call sites to direct `<OrbShape />` renders, and collapse NKShape's dispatcher so it always renders OrbShape. Closes VAR-01 (Square removed from BreathingShape + Variant union), VAR-02 (Diamond removed from BreathingShape + Variant union), and the rendering half of VAR-04 (sessionVariant ?? liveVariant prop disappears from JSX — the state-strip half lands in Plan 03).

Purpose: enforce the spike-010 "Orb-only" user-facing surface (CONTEXT.md §domain) so the Phase 42 orb rewrite has exactly one shape to dispatch to. Tiger Style — no premature abstraction kept for Phase 42's benefit (CONTEXT D-03). This is the highest-impact deletion plan; downstream plans (data-layer, CSS, drift-guard) become safe to run once every consumer of `VisualVariantId` / `BreathingShape` / `VariantPicker` is gone from the render path.

Output: 14 files git-rm'd (6 component + 4 hook + 4 component-test pairs; SquareShape/DiamondShape/VariantPicker/BreathingShape + their .test twins; useVisualVariant + useVariantChoice + their .test twins). 3 files surgically edited (App.tsx call-site collapse + variant-import strip; NKShape.tsx dispatcher collapse + variant-prop drop; NKShape.test.tsx + shapeConstants.ts comment-debt).

Note: This plan does NOT touch `src/storage/prefs.ts` or `src/domain/settings.ts` (Plan 02's scope) or the App.tsx `sessionVariantRef` / `sessionVariant` state declarations (Plan 03's scope). The hook-and-import deletions here will trigger `tsc` errors on `liveVariant` reads inside App.tsx — Plan 03 closes those by deleting the remaining `sessionVariantRef` + `setSessionVariant(null)` sites. Plan 01 keeps the surviving usages of `liveVariant` and `sessionVariant` in dep arrays + tidy-clear sites untouched so the App.tsx diff stays focused on call-site collapse; Plan 03 deletes the state declarations + their consumers in one coherent sweep.

CORRECTION TO BOUNDARY: After re-reading App.tsx, the `useVisualVariant` import + destructure CANNOT survive without the type import (`VisualVariantId`), and removing the hook destructure removes `liveVariant`. To keep `tsc` green within Plan 01, this plan removes:
- L3 `import { BreathingShape } ...` (component is being deleted)
- L35 `import { useVisualVariant } ...` (hook is being deleted in this plan)
- L65 `VisualVariantId` from the `import type` line (no consumer left after L292-293 strip... but those state declarations are Plan 03's scope)

Resolution: Plan 01 removes ONLY the L3 BreathingShape import + the 3 JSX call sites. The hook + type imports + the sessionVariantRef state + the capture/clear sites stay until Plan 03. Plan 01 therefore must commit the App.tsx call-site collapse SEPARATELY from the hook + picker file deletes — otherwise `tsc` would fail mid-plan with "cannot find module './hooks/useVisualVariant'". Commit sequence below honors this; see <tasks>.

This means Plan 01 leaves the **deleted** hook files on disk via `git rm` in a SINGLE commit that also strips the L35 import and L224 destructure from App.tsx. Same applies to NKShape's variant prop — NKShape's caller (App.tsx L1021) currently passes `variant={sessionVariant ?? liveVariant}`. To collapse cleanly, we drop that prop from the NKShape call site in App.tsx at the same time we drop the variant prop from NKShapeProps.

See Task 2 below for the precise App.tsx diff that lands in the same commit as the hook deletions.
</objective>

<execution_context>
@$HOME/.claude-personal-personal/get-shit-done/workflows/execute-plan.md
@$HOME/.claude-personal-personal/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/38-variant-removal/38-CONTEXT.md
@.planning/phases/38-variant-removal/38-PATTERNS.md
@.planning/phases/37-stats-ui-removal/37-CONTEXT.md
@.planning/phases/37-stats-ui-removal/37-LEARNINGS.md

<interfaces>
<!-- The 3 BreathingShape call sites in App.tsx — verbatim from the codebase so the executor performs an exact replacement, not a semantic guess. -->

From src/app/App.tsx (3 call sites — verified line numbers):

L1011-1017 (nkStarting branch — under nkSessionActive):
```tsx
<BreathingShape
  variant={sessionVariant ?? liveVariant}
  cue={sessionCue ?? liveCue}
  frame={null}
  leadInDigit={nkLeadInDigit}
  strings={uiStrings.breathing}
/>
```
becomes:
```tsx
<OrbShape
  cue={sessionCue ?? liveCue}
  frame={null}
  leadInDigit={nkLeadInDigit}
  strings={uiStrings.breathing}
/>
```

L1019-1027 (NKShape branch — under nkSessionActive && !nkStarting):
```tsx
<NKShape
  key={`nk-${String(nkCount)}`}
  variant={sessionVariant ?? liveVariant}
  count={nkCount}
  phase={nkPhase === 'back' ? 'back' : 'front'}
  isPaused={!nkRunning}
  strings={uiStrings.breathing}
  nkReadoutStrings={uiStrings.nkReadout}
/>
```
becomes:
```tsx
<NKShape
  key={`nk-${String(nkCount)}`}
  count={nkCount}
  phase={nkPhase === 'back' ? 'back' : 'front'}
  isPaused={!nkRunning}
  strings={uiStrings.breathing}
  nkReadoutStrings={uiStrings.nkReadout}
/>
```
(The `variant=` prop is dropped because NKShapeProps no longer accepts it after the collapse below.)

L1030-1036 (HRV/Stretch branch — under !nkSessionActive):
```tsx
<BreathingShape
  variant={sessionVariant ?? liveVariant}
  cue={sessionCue ?? liveCue}
  frame={appPhase === 'running' ? session.liveFrame : null}
  leadInDigit={appPhase === 'lead-in' ? leadInDigit : null}
  strings={uiStrings.breathing}
/>
```
becomes:
```tsx
<OrbShape
  cue={sessionCue ?? liveCue}
  frame={appPhase === 'running' ? session.liveFrame : null}
  leadInDigit={appPhase === 'lead-in' ? leadInDigit : null}
  strings={uiStrings.breathing}
/>
```

From src/components/BreathingShape.tsx (entire file — being deleted):
- Default export shape: `BreathingShape({ variant = 'orb', cue = 'labels', frame, strings, leadInDigit }: BreathingShapeProps)` (L35)
- Dispatcher: returns `<SquareShape>` when `variant === 'square'`, `<DiamondShape>` when `variant === 'diamond'`, else `<OrbShape>` (the default). After D-03 the dispatcher disappears entirely; App.tsx renders OrbShape directly.
- `BreathingShapeProps`: variant?, cue?, frame, strings, leadInDigit (L21-29).

From src/components/NKShape.tsx (collapsing in-place):
- L1: `import type { VisualVariantId } from '../domain/settings'` — DELETE.
- L5-7: imports of `OrbShape`, `SquareShape`, `DiamondShape` — DELETE SquareShape/DiamondShape lines; keep OrbShape line.
- L24: `variant: VisualVariantId` field on `NKShapeProps` — DELETE.
- L34: `variant,` destructure in the function param list — DELETE.
- L94: `data-variant={variant}` on the outer wrapper div — REPLACE with `data-variant="orb"` (NKShape historically emitted this for theme.css gradient targeting; OrbShape's body already emits `data-variant="orb"` at L62/L178 — keeping the wrapper's `data-variant="orb"` preserves any CSS that targets the NK wrapper specifically).
- L111-129: the `variant === 'orb' ? <OrbShape ... /> : variant === 'square' ? <SquareShape ... /> : <DiamondShape ... />` dispatcher — REPLACE with a single `<OrbShape frame={null} nkPhase={phase} strings={strings} />` render (the OrbShape branch verbatim).
- D-01 comment (L9-21) updates: "NKShape is a thin wrapper that renders OrbShape in NK mode" — drop the variant enumeration.

From src/components/NKShape.test.tsx:
- L17: `variant: 'orb'` in `defaults: NKShapeProps` — DELETE the line (prop no longer exists on type).
- L27-31: `it('D-01: variant="orb" produces data-variant="orb"')` — KEEP (still asserts the wrapper carries data-variant="orb" after collapse; update the it() title to drop "variant=\"orb\"" or rename to "wrapper carries data-variant=\"orb\"").
- L33-37: `it('D-01: variant="square" ...')` — DELETE wholesale.
- L39-43: `it('D-01: variant="diamond" ...')` — DELETE wholesale.
- All other tests survive (count rendering, phase rendering, reduced-motion, aria-label).

From src/components/shapeConstants.ts:
- Docstring references "OrbShape / SquareShape / DiamondShape" — verify exact wording with `grep -n "SquareShape\\|DiamondShape" src/components/shapeConstants.ts` and update to "OrbShape" (or drop the enumeration entirely). MIN_SCALE / MAX_SCALE / MID_SCALE constants stay (OrbShape consumes them).

Pattern reference (D-09 delete-with-component, copied from Phase 37 PATTERNS.md):
"git rm each component and its test file in the same commit. Never leave a component deleted while its test file survives (or vice versa)."
</interfaces>
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| disk → loadPrefs() | Previously-persisted `prefs.variant: 'square' \| 'diamond' \| 'orb'` lands in this plan's runtime path (App.tsx renders OrbShape always; the persisted key is harmlessly ignored on read; field deletion + coercer-drop is Plan 02's scope) |
| build-time → runtime | `tsc --noEmit` is the only consumer-detection mechanism for the deleted component / hook / type symbols; the green-gate at the end of this plan is the safety net |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-38-01 | T (tampering, future regression) | Deleted component / hook surface | mitigate | VAR-06 drift-guard test (Plan 04) — fs-scan that fails CI if `SquareShape` / `DiamondShape` / `VariantPicker` / `useVisualVariant` / `useVariantChoice` re-enter src/components, src/app, src/content, or src/styles |
| T-38-02 | I (information disclosure, comment debt) | shapeConstants.ts docstring | mitigate | docstring update: drop "SquareShape / DiamondShape" enumeration so stale WHY does not mislead a future reader |
| T-38-03 | D (denial of service, broken render) | NKShape collapse | mitigate | NKShape always renders OrbShape; existing tests for NK count + phase + reduced-motion stay; new variant-axis test cases (L33-43 NKShape.test.tsx) deleted in same commit |
| T-38-04 | E (elevation of privilege — not applicable) | n/a | n/a | deletion phase, no auth surface |
| T-38-05 | I (info — orphan import) | App.tsx import block | mitigate | tsc --noEmit at end of plan exits 0 — any remaining `BreathingShape` import or `useVisualVariant` import would fail compilation |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Delete all 12 variant-axis component + hook files (with their tests) and update shapeConstants docstring</name>
  <files>
    - src/components/SquareShape.tsx (DELETE)
    - src/components/SquareShape.test.tsx (DELETE)
    - src/components/DiamondShape.tsx (DELETE)
    - src/components/DiamondShape.test.tsx (DELETE)
    - src/components/VariantPicker.tsx (DELETE)
    - src/components/VariantPicker.test.tsx (DELETE)
    - src/components/BreathingShape.tsx (DELETE)
    - src/components/BreathingShape.test.tsx (DELETE)
    - src/hooks/useVisualVariant.ts (DELETE)
    - src/hooks/useVisualVariant.test.ts (DELETE)
    - src/hooks/useVariantChoice.ts (DELETE)
    - src/hooks/useVariantChoice.test.ts (DELETE)
    - src/components/shapeConstants.ts (EDIT — comment-only)
  </files>
  <read_first>
    - src/components/SquareShape.tsx (confirm dead code — has no non-BreathingShape consumer after the call-site collapse in Task 2)
    - src/components/DiamondShape.tsx (same)
    - src/components/VariantPicker.tsx (same — only consumer is SettingsDialog.tsx L8, removed in Plan 02; before Plan 02 lands, no other code imports VariantPicker since BreathingShape gets deleted in Task 2 simultaneously — but SettingsDialog.tsx still has the L8 import. Resolution: this task's commit MUST also strip the SettingsDialog.tsx L8 + L95 lines so tsc stays green; see <action> step 2)
    - src/components/BreathingShape.tsx (confirm the dispatcher does what PATTERNS.md describes)
    - src/components/SettingsDialog.tsx L1-100 (the L8 VariantPicker import and L95 <VariantPicker /> render that MUST be stripped in this same commit so tsc stays green)
    - src/hooks/useVisualVariant.ts (single export: useVisualVariant(): { variant, setVariant })
    - src/hooks/useVariantChoice.ts (single export: useVariantChoice(): { variant, setVariant })
    - src/components/shapeConstants.ts (locate any docstring listing SquareShape / DiamondShape)
    - .planning/phases/38-variant-removal/38-PATTERNS.md §"DELETE files" — confirms the same-commit delete-with-component policy
    - .planning/phases/37-stats-ui-removal/37-01-SUMMARY.md (Phase 37 precedent — single coordinated deletion commit kept tsc green throughout)
  </read_first>
  <action>
    BOUNDARY NOTE: The L8 + L95 SettingsDialog.tsx VariantPicker references are INSIDE this commit (not Plan 02) because Plan 02 scopes domain/settings.ts + prefs.ts + the strings.ts i18n surface. Removing the VariantPicker render (a tsc dependency on the deleted file) belongs here so `tsc --noEmit` stays green across the commit boundary. The remaining SettingsDialog.tsx + strings.ts work (the `variantLabel` and `variants` block in the strings type + EN + PT-BR catalogs, the `'variants'` from the Pick union at L29) lands in Plan 02.

    1. **Delete the 12 files via `git rm`** in a single coordinated cut (PATTERNS.md §Delete-with-component policy; Phase 37 precedent):
       ```
       git rm src/components/SquareShape.tsx src/components/SquareShape.test.tsx
       git rm src/components/DiamondShape.tsx src/components/DiamondShape.test.tsx
       git rm src/components/VariantPicker.tsx src/components/VariantPicker.test.tsx
       git rm src/components/BreathingShape.tsx src/components/BreathingShape.test.tsx
       git rm src/hooks/useVisualVariant.ts src/hooks/useVisualVariant.test.ts
       git rm src/hooks/useVariantChoice.ts src/hooks/useVariantChoice.test.ts
       ```

    2. **In `src/components/SettingsDialog.tsx`** — strip the VariantPicker dependency so tsc stays green at this commit boundary:
       - DELETE L8: `import { VariantPicker } from './VariantPicker'`
       - DELETE L95: the full `<VariantPicker disabled={inSessionView} strings={strings.variants} sectionLabel={strings.settings.variantLabel} />` JSX line
       - DO NOT touch L29 (`Pick<UiStrings, 'settings' | 'themes' | 'variants' | 'cue' | 'timbres' | 'install'>`) yet — `'variants'` stays in the union until Plan 02 removes the `UiStrings.variants` type block (removing it now leaves the SettingsDialog tests' strings fixture with an unread but harmless `variants` field; deleting BOTH at the same commit boundary creates a wider blast radius without test-coverage benefit).
       - L93 comment (`{/* D-10 ... Theme → Variant → Cue → Timbre → Language order ... */}`) — UPDATE to drop "→ Variant" so the WHY matches the new render order: `{/* D-10 (updated Phase 25 Plan 04 / Phase 38): Theme → Cue → Timbre → Language order (Landmine 7: each receives disabled={inSessionView}) */}`
       - L16 file-header comment (`// D-10: inner layout single column, Theme → Variant → Timbre → Language order.`) — UPDATE to `// D-10: inner layout single column, Theme → Cue → Timbre → Language order. (Phase 38: Variant slot removed.)`

    3. **In `src/components/shapeConstants.ts`** — comment-only edit (D-03 / PATTERNS §shapeConstants.ts):
       - Run `grep -n "SquareShape\\|DiamondShape" src/components/shapeConstants.ts` to locate the exact lines.
       - Update the docstring so the shape enumeration reads "OrbShape" (or drop the enumeration entirely if it adds no value post-collapse).
       - Constants (MIN_SCALE / MAX_SCALE / MID_SCALE) are untouched — OrbShape still consumes them for breathing math.

    4. **Verify the commit boundary with `tsc --noEmit`** — this MUST exit 0 BEFORE moving to Task 2:
       - The `BreathingShape` import at App.tsx L3 is still present (Task 2 removes it).
       - The `useVisualVariant` import at App.tsx L35 is still present (Task 2 removes it).
       - The `VariantPicker` import in SettingsDialog.tsx is GONE (this task removed it).
       - tsc will FAIL with "cannot find module" for the L3 + L35 App.tsx imports IF this task is committed BEFORE Task 2's App.tsx edits land. Resolution: **Task 1 and Task 2 ship in the SAME commit** — see <verify>. Run Task 2 immediately after Task 1's file-rm operations, then run tsc once before staging the combined diff.

    5. Stage the combined diff (12 file deletions + SettingsDialog.tsx + shapeConstants.ts + the full App.tsx + NKShape changes from Task 2) and commit with message: `chore(38): delete shape components + hooks + VariantPicker; collapse BreathingShape call sites to OrbShape (VAR-01 VAR-02 VAR-04 partial; D-02 D-03 D-09)`.

    DO NOT touch: src/domain/settings.ts, src/storage/prefs.ts (Plan 02 scope); the L292-293 sessionVariantRef + sessionVariant state declarations, the capture/clear sites at L487/491/492 / L670-671 / L819 / L869-870 / L938, the L557 + L927 dep arrays (Plan 03 scope); src/content/strings.ts (Plan 02 scope); src/styles/theme.css (Plan 03 scope).
  </action>
  <verify>
    <automated>pnpm tsc --noEmit && pnpm test src/components src/hooks --run</automated>
  </verify>
  <acceptance_criteria>
    - `test ! -e src/components/SquareShape.tsx && test ! -e src/components/SquareShape.test.tsx` — both gone.
    - `test ! -e src/components/DiamondShape.tsx && test ! -e src/components/DiamondShape.test.tsx` — both gone.
    - `test ! -e src/components/VariantPicker.tsx && test ! -e src/components/VariantPicker.test.tsx` — both gone.
    - `test ! -e src/components/BreathingShape.tsx && test ! -e src/components/BreathingShape.test.tsx` — both gone.
    - `test ! -e src/hooks/useVisualVariant.ts && test ! -e src/hooks/useVisualVariant.test.ts` — both gone.
    - `test ! -e src/hooks/useVariantChoice.ts && test ! -e src/hooks/useVariantChoice.test.ts` — both gone.
    - `grep -n "VariantPicker" src/components/SettingsDialog.tsx` returns zero results (L8 import + L95 JSX both deleted).
    - `grep -n "SquareShape\\|DiamondShape" src/components/shapeConstants.ts` returns zero results (docstring updated).
    - `pnpm tsc --noEmit` exits 0 AFTER Task 1 + Task 2 land together in the same commit.
    - `git ls-files --deleted` returns empty (deletions are staged, not lingering as unstaged).
  </acceptance_criteria>
  <done>All 12 variant-axis source files (6 component + 4 hook + 4 component-test pairs) are removed from the working tree; SettingsDialog.tsx no longer imports or renders VariantPicker; shapeConstants.ts docstring no longer enumerates SquareShape / DiamondShape. Tasks 1 + 2 ship together in a single commit. Closes VAR-01 (Square deletion) + VAR-02 (Diamond deletion) + the render-layer half of VAR-03 (VariantPicker render removed from SettingsDialog). Plan 02 closes the remaining type-level half of VAR-03 by removing the `UiStrings.variants` block.</done>
</task>

<task type="auto">
  <name>Task 2: Collapse 3 App.tsx call sites to <OrbShape />; strip BreathingShape + useVisualVariant imports; collapse NKShape to OrbShape-only</name>
  <files>
    - src/app/App.tsx
    - src/components/NKShape.tsx
    - src/components/NKShape.test.tsx
  </files>
  <read_first>
    - src/app/App.tsx L1-65 (import block — locate L3 `BreathingShape` import and L35 `useVisualVariant` import; locate L65 `VisualVariantId` type import — DO NOT remove yet, Plan 03 handles it after the state declarations go)
    - src/app/App.tsx L220-230 (locate L224 `const { variant: liveVariant } = useVisualVariant() ...` destructure — this will fail to compile after the hook deletion)
    - src/app/App.tsx L1000-1040 (the 3 prior BreathingShape / 1 NKShape call sites — see this plan's <interfaces> block for the verbatim before/after diff)
    - src/components/NKShape.tsx (lines 1-138 — collapsing in place; see <interfaces> for line-by-line edits)
    - src/components/NKShape.test.tsx L15-43 (variant-axis test cases to strip)
    - src/components/OrbShape.tsx (verify prop signature — accepts `frame`, `nkPhase`, `strings`, `cue`, `leadInDigit`; the call-site collapse uses these exact props verbatim)
    - .planning/phases/38-variant-removal/38-PATTERNS.md §"src/app/App.tsx (surgical edit ...)" — the 10-thread surgical-strip ordering
    - .planning/phases/38-variant-removal/38-PATTERNS.md §"BreathingShape call-site collapse (D-03)" — the canonical before/after JSX
  </read_first>
  <action>
    This task ships in the SAME COMMIT as Task 1 (tsc-boundary requirement — the L3 + L35 imports must vanish in the same commit as the deleted modules). Order the edits in App.tsx + NKShape.tsx as below; run `pnpm tsc --noEmit` after both files are edited (BEFORE committing) to confirm green.

    **A. `src/app/App.tsx` — surgical edits (anchor by current line numbers; the executor verifies with grep before each edit since edits shift line numbers):**

    A1. DELETE L3 import: `import { BreathingShape } from '../components/BreathingShape'`

    A2. DELETE L35 import: `import { useVisualVariant } from '../hooks/useVisualVariant'`

    A3. DELETE L224 hook destructure: `const { variant: liveVariant } = useVisualVariant() // Phase 17 VARIANT-01..07: live state + cross-tab/same-tab sync (no global attribute write — D-16)`

       After this delete, `liveVariant` reads at L491, L492, L557 (dep array), L869, L870, L927 (dep array) are now `tsc` errors. **DO NOT fix them in this plan** — Plan 03 deletes the entire sessionVariantRef + sessionVariant + capture/clear sites + dep array entries in one coherent sweep. To keep `tsc` green within Task 2's commit boundary, we leave the L224 destructure in place IF AND ONLY IF Plan 03 ships in the same wave.

       **Resolution (CONTEXT D-03 spirit — Tiger Style "no intermediate broken state"):** The destructure at L224 + the L491/492/869/870/927/557 reads + the L292/293 state + the L487/670/819/938 clear sites + the L1012/1021/1031 JSX reads form a single semantic unit. Task 2 here drops THE WHOLE UNIT in one edit set. Specifically:

       Replace L224 with: `const liveVariant: undefined = undefined as const // Phase 38: shim — Plan 03 deletes this line plus the L292-293 state + the L487/670/819/938 clear sites. Keeps tsc green within Plan 01's commit boundary.`

       This shim makes `liveVariant` typed as `undefined`, so `sessionVariantRef.current = liveVariant` at L491 becomes `sessionVariantRef.current = undefined` — but `sessionVariantRef` is declared `useRef<VisualVariantId | null>(null)` (L292), so assigning `undefined` is a type error.

       **REVISED RESOLUTION:** Keep `liveVariant` typed properly to match `sessionVariantRef`'s type — declare it as `const liveVariant: VisualVariantId | null = null` (matches the type of `sessionVariantRef.current = null`). All capture sites (`sessionVariantRef.current = liveVariant`) now assign `null` instead of a real variant value. The captured-state path becomes a no-op until Plan 03 deletes the ref + state entirely.

       Replace L224 with:
       ```
       const liveVariant: VisualVariantId | null = null // Phase 38 Plan 01 shim: hook deleted; Plan 03 will delete this line + sessionVariantRef + setSessionVariant + the L487/491-492/670-671/819/869-870/938 capture/clear sites in one coherent sweep.
       ```

       Add WHY-only comment so a future reader (or Plan 03's executor) understands the shim's lifetime.

       **Update dep arrays L557 and L927** — `liveVariant` is now a stable `null` const; the dep array entries no longer constrain re-render behavior, but ESLint's react-hooks plugin will demand the entries stay (or be removed with an explicit comment). Leave the dep arrays untouched (they reference `liveVariant` by name which still binds to the new const) — Plan 03 deletes both entries when it deletes the shim.

    A4. Replace L1011-1017 BreathingShape JSX (nkStarting branch) with the OrbShape form per <interfaces>. Specifically:
       ```
       <BreathingShape
         variant={sessionVariant ?? liveVariant}
         cue={sessionCue ?? liveCue}
         frame={null}
         leadInDigit={nkLeadInDigit}
         strings={uiStrings.breathing}
       />
       ```
       becomes
       ```
       <OrbShape
         cue={sessionCue ?? liveCue}
         frame={null}
         leadInDigit={nkLeadInDigit}
         strings={uiStrings.breathing}
       />
       ```

    A5. Replace L1019-1027 NKShape JSX (NKShape branch) by dropping the `variant={sessionVariant ?? liveVariant}` line (NKShapeProps no longer accepts variant after edit C below):
       ```
       <NKShape
         key={`nk-${String(nkCount)}`}
         variant={sessionVariant ?? liveVariant}
         count={nkCount}
         ...
       />
       ```
       becomes
       ```
       <NKShape
         key={`nk-${String(nkCount)}`}
         count={nkCount}
         ...
       />
       ```

    A6. Replace L1030-1036 BreathingShape JSX (HRV/Stretch branch) with the OrbShape form per <interfaces>. Mirror of A4 except `frame` and `leadInDigit` use the active-running computed values.

    A7. L1001-1002 comment block — UPDATE to drop the Phase 17 D-09 WHY about variant capture (it no longer applies after Plan 03; this plan's edit removes the obsolete half of the comment so it does not mislead a future reader):
       ```
       {/* Phase 3 D-14: lead-in numeral takes over the orb area when appPhase==='lead-in' */}
       {/* Phase 25 D-09: sessionCue is the captured-at-Start frozen value (T-25-09); liveCue is the fallback at idle. */}
       {/* Phase 31: an active Navi Kriya session replaces the resonant breathing shape with the OM-counting NKShape. The key restarts the nk-om-pulse animation on every OM (NKShape caller contract). */}
       ```
       (the Phase 17 D-09 sessionVariant/liveVariant line is DELETED; the cue + NK comments stay because those subsystems survive)

    **B. `src/components/NKShape.tsx` — collapse the dispatcher (per <interfaces>):**

    B1. DELETE L1: `import type { VisualVariantId } from '../domain/settings'`

    B2. DELETE L6 + L7: the `SquareShape` and `DiamondShape` imports. KEEP L5: `import { OrbShape } from './OrbShape'`.

    B3. UPDATE L9-21 comment block — drop the "NKShape is a thin wrapper that renders the user's chosen variant shape (OrbShape / SquareShape / DiamondShape) in NK mode" enumeration. New WHY: "NKShape renders OrbShape in NK mode — count number centered, shape locked at MID_SCALE (no scale crossfade), Front/Back drives the In/Out gradient via OrbShape's nkPhase prop." Keep the caller-responsibility note about `key={`nk-${String(count)}`}`.

    B4. DELETE L24: `variant: VisualVariantId      // 'orb' | 'square' | 'diamond'` from NKShapeProps.

    B5. DELETE L34: `variant,` from the destructured function params. Resulting signature:
       ```typescript
       export function NKShape({
         count,
         phase,
         isPaused = false,
         strings,
         nkReadoutStrings,
       }: NKShapeProps) {
       ```

    B6. UPDATE L94 wrapper div: `data-variant={variant}` → `data-variant="orb"` (literal — OrbShape's inner body emits `data-variant="orb"` at L62/L178; the NK wrapper keeps the attribute as a fixed `"orb"` so any CSS that targets the NK wrapper specifically still matches).

    B7. UPDATE L73-90 ARCHITECTURE comment — drop the "(OrbShape/SquareShape/DiamondShape)" enumeration; reword to "the OrbShape component" / "OrbShape's LeadIn structure".

    B8. REPLACE L111-129 dispatcher block (`{variant === 'orb' ? <OrbShape ... /> : variant === 'square' ? <SquareShape ... /> : <DiamondShape ... />}`) with the OrbShape branch only:
       ```typescript
       <OrbShape
         frame={null}
         nkPhase={phase}
         strings={strings}
       />
       ```

    **C. `src/components/NKShape.test.tsx` — strip variant-axis test cases (per <interfaces>):**

    C1. DELETE L17 from `defaults`: `variant: 'orb',` (NKShapeProps no longer accepts it).

    C2. RENAME L27 it title from `'D-01: variant="orb" produces data-variant="orb"'` to `'NK wrapper carries data-variant="orb" (single-shape collapse — Phase 38 VAR-01/02)'`. Keep the body — it still passes (`renderShape({})` instead of `renderShape({ variant: 'orb' })`).

    C3. DELETE L33-37 wholesale: `it('D-01: variant="square" produces data-variant="square"', () => { ... })` — variant prop no longer exists.

    C4. DELETE L39-43 wholesale: `it('D-01: variant="diamond" produces data-variant="diamond"', () => { ... })` — variant prop no longer exists.

    C5. All other NKShape tests (count, phase, reduced-motion, aria-label) survive untouched.

    **D. Verify and commit:**
    - Run `pnpm tsc --noEmit` — must exit 0.
    - Run `pnpm test src/components src/hooks --run` — must exit 0 (component-test suite passes against the collapsed NKShape + deleted shape components).
    - Stage the combined diff from Task 1 + Task 2 and commit with the message from Task 1 step 5.
  </action>
  <verify>
    <automated>pnpm tsc --noEmit && pnpm test src/components --run</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "BreathingShape" src/app/App.tsx` returns zero results.
    - `grep -n "useVisualVariant" src/app/App.tsx` returns zero results.
    - `grep -nc "<OrbShape" src/app/App.tsx` returns at least 2 (the 2 BreathingShape sites that became OrbShape — the NKShape branch stays as <NKShape ...>).
    - `grep -n "<BreathingShape" src/app/App.tsx` returns zero results.
    - `grep -n "variant=" src/app/App.tsx | grep -v "data-variant"` returns zero results in JSX context (the `variant={sessionVariant ?? liveVariant}` props at L1012/1021/1031 are gone). NOTE: `liveVariant` and `sessionVariant` identifiers themselves are still present (Plan 03 strips them); only the JSX prop reads are gone.
    - `grep -n "SquareShape\\|DiamondShape\\|VisualVariantId" src/components/NKShape.tsx` returns zero results.
    - `grep -n "variant" src/components/NKShape.tsx` returns at most: one occurrence inside `data-variant="orb"` on L94 and any prose comments (no `variant: VisualVariantId` field, no `variant,` destructure, no `variant === 'orb'` dispatcher).
    - `grep -nE "variant=['\"](square|diamond)['\"]" src/components/NKShape.test.tsx` returns zero results.
    - `pnpm tsc --noEmit` exits 0.
    - `pnpm test src/components --run` exits 0 (NKShape's remaining tests pass; deleted shape tests' files do not exist; OrbShape tests are untouched).
    - `git diff --cached --stat` for the combined commit shows: 12 deletions, 3 modifications (App.tsx, NKShape.tsx, NKShape.test.tsx), 1 modification (SettingsDialog.tsx from Task 1), 1 modification (shapeConstants.ts from Task 1).
  </acceptance_criteria>
  <done>App.tsx renders <OrbShape /> at all 3 prior BreathingShape sites with no variant prop; NKShape always renders OrbShape (no dispatcher, no variant prop); NKShape.test.tsx has the variant-axis cases stripped and the remaining count/phase/reduced-motion tests passing. Tasks 1 + 2 ship as a single commit `chore(38): delete shape components + hooks + VariantPicker; collapse BreathingShape call sites to OrbShape (VAR-01 VAR-02 VAR-04 partial; D-02 D-03 D-09)`. Closes VAR-01, VAR-02, and the render-layer half of VAR-04.</done>
</task>

</tasks>

<verification>
After both tasks land (single commit):
- `pnpm tsc --noEmit` exits 0.
- `pnpm test src/components src/hooks --run` exits 0.
- `git grep -E "SquareShape|DiamondShape|VariantPicker|BreathingShape|useVisualVariant|useVariantChoice" src/components src/hooks src/app` returns at most the shim comment in App.tsx L224 (a planned residue cleaned up in Plan 03) and any prose comments scheduled for Plan 03's WHY-comment audit. No type imports, no JSX, no module imports, no hook destructures.
- The single commit message scope is `(38)` and lists VAR-01, VAR-02, VAR-04 (partial) + D-02 D-03 D-09 references.
</verification>

<success_criteria>
- 12 source files deleted (6 component + 4 hook + 4 component-test pairs).
- App.tsx renders <OrbShape /> directly at 3 prior BreathingShape sites — no BreathingShape import, no <BreathingShape ...> JSX, no `variant=` JSX prop on the 3 sites.
- NKShape collapsed to always-OrbShape (no SquareShape / DiamondShape imports, no variant prop on type, no dispatcher).
- SettingsDialog.tsx no longer imports or renders VariantPicker (the L29 Pick union retains `'variants'` for Plan 02 to remove).
- shapeConstants.ts docstring no longer enumerates SquareShape / DiamondShape.
- `pnpm tsc --noEmit` + `pnpm test src/components src/hooks` both exit 0.
- VAR-01 (Square removed from BreathingShape + Variant union) and VAR-02 (Diamond removed from BreathingShape + Variant union) are observably closed — the BreathingShape file is gone, so the union is gone with it.
- VAR-04's render half is closed (the JSX no longer reads sessionVariant ?? liveVariant).
</success_criteria>

<output>
After completion, create `.planning/phases/38-variant-removal/38-01-SUMMARY.md` summarising:
- The 12 files deleted (paths + line-counts removed).
- The App.tsx surgical-edit diff (BreathingShape → OrbShape at 3 sites; useVisualVariant + BreathingShape imports removed; L224 shim with WHY-comment pointing forward to Plan 03).
- The NKShape collapse (5-symbol strip: VisualVariantId import, SquareShape/DiamondShape imports, variant prop, variant destructure, dispatcher → single OrbShape render).
- The NKShape.test.tsx strip (2 variant-axis cases deleted, defaults `variant: 'orb'` removed).
- The SettingsDialog.tsx VariantPicker import/render strip (the L29 Pick union `'variants'` token deferred to Plan 02).
- The shapeConstants.ts comment-only docstring update.
- Confirmation that `pnpm tsc --noEmit` + `pnpm test src/components src/hooks --run` exit 0.
- Pointer forward to Plan 02 (data-layer + i18n cleanup — domain/settings.ts + storage/prefs.ts + content/strings.ts + the L29 Pick union token + prefs.test.ts strip).
- Pointer forward to Plan 03 (App.tsx state strip — the L224 shim + L292-293 ref+state + L487/491-492/670-671/819/869-870/938 capture/clear sites + L557/L927 dep array entries; CSS theme.css cleanup; cross-cutting App test strips).
- Pointer forward to Plan 04 (VAR-06 drift-guard).
- Tiger Style commit hygiene note: single combined commit scope `(38)`.
</output>
