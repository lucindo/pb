---
phase: 25-labels-vs-icons-cue-toggle
plan: 03
type: execute
wave: 2
depends_on: ["25-01"]
files_modified:
  - src/components/CueGlyph.tsx
  - src/components/CueGlyph.test.tsx
  - src/components/BreathingShape.tsx
  - src/components/BreathingShape.test.tsx
  - src/components/OrbShape.tsx
  - src/components/OrbShape.test.tsx
  - src/components/SquareShape.tsx
  - src/components/SquareShape.test.tsx
  - src/components/DiamondShape.tsx
  - src/components/DiamondShape.test.tsx
autonomous: true
requirements: [CUE-01, CUE-03]
must_haves:
  truths:
    - "In 'labels' mode the in-orb phase label renders exactly as today — the localized word in the text-5xl/sm:text-6xl span (zero regression)"
    - "In 'arrow' mode the phase slot renders an aria-hidden chevron SVG (up for In, down for Out) plus visually-hidden localized In/Out text"
    - "In 'nose' mode the phase slot renders an aria-hidden nose+arrows SVG (arrows up for In, down for Out) plus visually-hidden localized In/Out text"
    - "A screen reader still announces the localized In/Out word in arrow and nose modes via the visually-hidden text"
    - "The shape root role=img aria-label and the lead-in countdown digit are unchanged in all 3 cue modes"
    - "Each cue mode renders correctly in all 3 variants (Orb, Square, Diamond)"
  artifacts:
    - path: "src/components/CueGlyph.tsx"
      provides: "Cue-mode renderer: labels span / arrow SVG / nose SVG + sr-only text"
      exports: ["CueGlyph"]
    - path: "src/components/OrbShape.tsx"
      provides: "OrbBody branches the phase slot on the cue prop"
      contains: "CueGlyph"
    - path: "src/components/SquareShape.tsx"
      provides: "SquareBody branches the phase slot on the cue prop"
      contains: "CueGlyph"
    - path: "src/components/DiamondShape.tsx"
      provides: "DiamondBody branches the phase slot on the cue prop"
      contains: "CueGlyph"
  key_links:
    - from: "src/components/BreathingShape.tsx"
      to: "OrbShape / SquareShape / DiamondShape"
      via: "cue prop threading"
      pattern: "cue=\\{cue\\}"
    - from: "src/components/OrbShape.tsx (and Square/Diamond)"
      to: "src/components/CueGlyph.tsx"
      via: "phase-slot render"
      pattern: "<CueGlyph"
---

<objective>
Build the in-orb cue rendering. Create one shared `CueGlyph` component that owns all three cue modes — `labels` (today's text span, zero regression), `arrow` (mockup candidate F soft-solid chevron), `nose` (mockup candidate D2 nose outline + straight up/down arrows) — including the CUE-03 accessibility pattern (aria-hidden SVG + visually-hidden localized In/Out text). Thread an optional `cue` prop through `BreathingShape` → all three shape components → their `*Body` phase-label slot.

Purpose: A single `CueGlyph` keeps the SVG path data and the sr-only a11y pattern in one place — the three shapes each call it identically, so the per-variant legibility tuning happens once. The chevron and nose SVGs have no in-repo analog (PATTERNS.md "No Analog Found"); path data is sourced from `25-cue-icon-mockup.html`.
Output: One new component + test, three modified shape components + tests, the dispatcher + test.
</objective>

<execution_context>
@$HOME/.claude-personal-personal/get-shit-done/workflows/execute-plan.md
@$HOME/.claude-personal-personal/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-labels-vs-icons-cue-toggle/25-CONTEXT.md
@.planning/phases/25-labels-vs-icons-cue-toggle/25-PATTERNS.md
@.planning/phases/25-labels-vs-icons-cue-toggle/25-cue-icon-mockup.html

<interfaces>
<!-- Plan 01 (25-01) added CueStyleId. Consume it directly. -->

From src/domain/settings.ts (added by Plan 01):
```typescript
export type CueStyleId = 'labels' | 'arrow' | 'nose'
```

Today's phase-label slot — IDENTICAL across all 3 shapes (OrbShape lines 122-133, SquareShape lines 129-140, DiamondShape lines 119-130):
```typescript
<span
  className="relative z-10 text-5xl font-semibold tracking-tight text-[var(--color-breathing-accent-strong)] sm:text-6xl"
  style={{ color: frame.phase === 'in' ? 'var(--color-orb-in-text)' : 'var(--color-orb-out-text)' }}
>
  {phaseLabel}
</span>
```
`phaseLabel` derives in each *Body as `frame.phase === 'in' ? strings.inhale : strings.exhale` (OrbShape line 39, SquareShape line 37, DiamondShape line 50).

Mockup SVG path data (from 25-cue-icon-mockup.html, viewBox 0 0 100 100):
- Arrow candidate F (In, up): `M50 28 Q54 28 57 32 L82 64 Q84 68 80 70 Q76 72 73 68 L50 44 L27 68 Q24 72 20 70 Q16 68 18 64 L43 32 Q46 28 50 28 Z` (filled).
- Arrow candidate F (Out, down): `M50 72 Q46 72 43 68 L18 36 Q16 32 20 30 Q24 28 27 32 L50 56 L73 32 Q76 28 80 30 Q84 32 82 36 L57 68 Q54 72 50 72 Z` (filled).
- Nose candidate D2 (In): nose paths + up arrows — lines/polylines from the mockup `D2 — Nose + straight arrows` In orb.
- Nose candidate D2 (Out): same nose paths + down arrows — from the D2 Out orb.

The shape root `role="img"` aria-label `${strings.breathingShapeAriaLabel}: ${phaseLabel}` (OrbShape line 44 and equivalents) stays UNCHANGED. The lead-in (OrbLeadIn / Square/Diamond equivalents) does NOT receive `cue` — CONTEXT D-07.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create the CueGlyph component (labels / arrow / nose)</name>
  <files>src/components/CueGlyph.tsx, src/components/CueGlyph.test.tsx</files>
  <read_first>
    - .planning/phases/25-labels-vs-icons-cue-toggle/25-cue-icon-mockup.html (the SVG path data — candidate F arrow In/Out, candidate D2 nose In/Out, and the "D2 across the 3 variants" row)
    - .planning/phases/25-labels-vs-icons-cue-toggle/25-PATTERNS.md ("a11y: aria-hidden glyph + visually-hidden text" + "Token-only colors" + "MODIFY src/components/OrbShape.tsx" target-pattern sections)
    - src/components/OrbShape.tsx (the phase-label span at lines 122-133 — the labels-mode output must match this exactly)
    - src/domain/settings.ts (CueStyleId — Plan 01 output)
  </read_first>
  <behavior>
    - CueGlyph with cue='labels' renders a single span containing phaseLabel, with class `relative z-10 text-5xl font-semibold tracking-tight text-[var(--color-breathing-accent-strong)] sm:text-6xl` and inline color `var(--color-orb-in-text)` for phase 'in' / `var(--color-orb-out-text)` for phase 'out' — byte-identical to today's slot
    - CueGlyph with cue='arrow', phase='in' renders an svg with aria-hidden="true" containing the up-chevron path; with phase='out' the down-chevron path
    - CueGlyph with cue='nose', phase='in' renders an aria-hidden svg with the nose + up-arrows; phase='out' renders nose + down-arrows
    - In arrow and nose modes the component also renders a visually-hidden span (Tailwind `sr-only`) containing phaseLabel — query by text finds the In/Out word even though the SVG is aria-hidden
    - In arrow and nose modes the SVG fill/stroke resolves to `var(--color-orb-in-text)` for 'in' and `var(--color-orb-out-text)` for 'out' (no hardcoded hex)
    - The SVG is static — no animation classes, no <animate> elements
  </behavior>
  <action>
    Create src/components/CueGlyph.tsx exporting `CueGlyph` with props `{ cue: CueStyleId; phase: 'in' | 'out'; phaseLabel: string }`. Branch on `cue`: for `'labels'` return the exact phase-label `<span>` from OrbShape lines 122-133 (same className, same inline `color` token branch) wrapping `phaseLabel` — zero regression. For `'arrow'` and `'nose'` render a wrapper `<span className="relative z-10">` containing (a) an `<svg aria-hidden="true" viewBox="0 0 100 100">` sized to the text-5xl/sm:text-6xl footprint (CONTEXT D-08 — set width/height so it visually matches the word slot; use a Tailwind size class plus `sm:` step), and (b) a visually-hidden `<span className="sr-only">{phaseLabel}</span>` (CUE-03 — screen readers announce the word). The SVG color: phase 'in' → `var(--color-orb-in-text)`, 'out' → `var(--color-orb-out-text)`; apply via the SVG element's `color` style + `fill="currentColor"` (chevron) or `stroke="currentColor"` (nose), never a hex. Arrow mode uses candidate F filled path data; up path for 'in', down path for 'out' (Up = In per CONTEXT D-03). Nose mode uses candidate D2: the nose outline paths (identical for both phases) plus straight arrows pointing up for 'in' / down for 'out' (CONTEXT D-05); use `stroke-linecap="round" stroke-linejoin="round"` with `fill="none"` for the nose lines. The glyph is STATIC — no animation, no reduced-motion branch (CONTEXT D-08). Add a file-header comment crediting `25-cue-icon-mockup.html` candidates F and D2 as the design source. Create src/components/CueGlyph.test.tsx covering the behavior above for all 3 modes × both phases, asserting aria-hidden on the SVG and the sr-only text presence/content.
  </action>
  <verify>
    <automated>npx vitest run src/components/CueGlyph.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/components/CueGlyph.test.tsx` exits 0 covering 3 modes × 2 phases
    - `grep -E "#[0-9a-fA-F]{3,6}" src/components/CueGlyph.tsx` returns nothing (zero hardcoded hex — token-only colors, D-23)
    - `grep -n "sr-only" src/components/CueGlyph.tsx` matches (visually-hidden text present)
    - `grep -En "animate|@keyframes|transition-" src/components/CueGlyph.tsx | grep -v '^#'` returns nothing (static glyph, D-08)
    - `grep -c 'aria-hidden="true"' src/components/CueGlyph.tsx` is at least 1 (decorative SVG)
  </acceptance_criteria>
  <done>CueGlyph renders labels/arrow/nose; arrow & nose carry aria-hidden SVG + sr-only localized text; token-only colors; static; tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Thread the cue prop through BreathingShape and the three shapes</name>
  <files>src/components/BreathingShape.tsx, src/components/BreathingShape.test.tsx, src/components/OrbShape.tsx, src/components/OrbShape.test.tsx, src/components/SquareShape.tsx, src/components/SquareShape.test.tsx, src/components/DiamondShape.tsx, src/components/DiamondShape.test.tsx</files>
  <read_first>
    - src/components/BreathingShape.tsx (the dispatcher being modified — read the variant prop threading, lines 18-42)
    - src/components/OrbShape.tsx (being modified — OrbShapeProps lines 6-10, OrbShape→OrbBody signatures lines 15-26, phase-label span lines 122-133, OrbLeadIn lines 149-214)
    - src/components/SquareShape.tsx (being modified — SquareShapeProps, SquareBody signature line 26, phase-label span lines 129-140)
    - src/components/DiamondShape.tsx (being modified — DiamondShapeProps, DiamondBody signature, phase-label span lines 119-130)
    - src/components/CueGlyph.tsx (Task 1 output — the component each shape now calls)
    - .planning/phases/25-labels-vs-icons-cue-toggle/25-PATTERNS.md (the BreathingShape / OrbShape / SquareShape / DiamondShape MODIFY sections)
    - src/components/BreathingShape.test.tsx, OrbShape.test.tsx, SquareShape.test.tsx, DiamondShape.test.tsx (the test files being modified)
  </read_first>
  <behavior>
    - BreathingShape accepts an optional `cue` prop defaulting to 'labels'; with cue omitted every shape renders today's text label (zero regression)
    - BreathingShape passes `cue` to whichever of Orb/Square/Diamond it dispatches to
    - OrbShape / SquareShape / DiamondShape each accept an optional `cue` prop (default 'labels'), thread it to their *Body, and *Body renders the phase slot via CueGlyph
    - With cue='arrow' an Orb/Square/Diamond renders the chevron SVG + sr-only In/Out text in the phase slot; the lead-in countdown digit is unchanged regardless of cue
    - The shape root role=img aria-label still resolves to `${breathingShapeAriaLabel}: ${phaseLabel}` in all 3 cue modes
  </behavior>
  <action>
    In src/components/BreathingShape.tsx: add `cue?: CueStyleId` to `BreathingShapeProps` (optional, default 'labels' — same zero-regression rationale comment as `variant`); extend the `'../domain/settings'` import to include `type CueStyleId`; destructure `cue = 'labels'` in the function signature; pass `cue={cue}` to `<SquareShape>`, `<DiamondShape>`, and `<OrbShape>` in the switch. In src/components/OrbShape.tsx: add `cue?: CueStyleId` to `OrbShapeProps`; thread `cue` from `OrbShape` to `OrbBody` (extend both signatures, default `'labels'`); in `OrbBody` replace the phase-label `<span>` (lines 122-133) with `<CueGlyph cue={cue} phase={frame.phase} phaseLabel={phaseLabel} />`; import `CueGlyph` from `'./CueGlyph'` and `type CueStyleId` from `'../domain/settings'`. `OrbLeadIn` does NOT receive `cue` — leave it untouched (CONTEXT D-07). Apply the IDENTICAL change to src/components/SquareShape.tsx (SquareBody, phase-label span lines 129-140, SquareLeadIn untouched) and src/components/DiamondShape.tsx (DiamondBody, phase-label span lines 119-130, DiamondLeadIn untouched). Update each shape's test file: extend existing tests so the default (no cue / cue='labels') still asserts today's text label, and add a case per shape rendering with cue='arrow' asserting the SVG is present and the sr-only In/Out text is queryable, plus a case asserting the lead-in digit is unchanged when cue='arrow'. Update BreathingShape.test.tsx to assert `cue` is forwarded to the dispatched shape.
  </action>
  <verify>
    <automated>npx vitest run src/components/BreathingShape.test.tsx src/components/OrbShape.test.tsx src/components/SquareShape.test.tsx src/components/DiamondShape.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/components/BreathingShape.test.tsx src/components/OrbShape.test.tsx src/components/SquareShape.test.tsx src/components/DiamondShape.test.tsx` exits 0
    - `grep -c "<CueGlyph" src/components/OrbShape.tsx src/components/SquareShape.tsx src/components/DiamondShape.tsx` shows exactly 1 per file (the *Body phase slot)
    - `grep -n "cue" src/components/OrbShape.tsx | grep -i leadin` returns nothing AND OrbLeadIn's signature has no `cue` param (D-07 — lead-in untouched)
    - `grep -c "cue=" src/components/BreathingShape.tsx` is 3 (one per shape in the switch)
    - `npx tsc -b` exits 0
  </acceptance_criteria>
  <done>cue threads BreathingShape → all 3 shapes → CueGlyph; lead-in untouched; root aria-label unchanged; default is zero-regression; tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `cue` prop → CueGlyph render | The `cue` value originates from `coerceCue` (Plan 01) — already whitelist-constrained to `CueStyleId`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-05 | Tampering | An out-of-range `cue` value reaching CueGlyph | accept | TypeScript constrains the prop to `CueStyleId`; at runtime the value already passed `coerceCue` (Plan 01). CueGlyph's branch falls through to the `labels` default for any unexpected value — fail-safe to zero-regression text. |
| T-25-06 | Information Disclosure | The aria-hidden SVG hiding the In/Out cue from assistive tech | mitigate | CUE-03: arrow/nose modes pair the decorative `aria-hidden` SVG with a visually-hidden `sr-only` span carrying the localized `phaseLabel`; the shape root `role="img"` aria-label is unchanged. Screen-reader announcement parity is asserted in CueGlyph.test.tsx and the per-shape tests. |

Pure presentational render of a typed prop — no network, no storage write, no auth, no PII. Spoofing / Repudiation / DoS / Elevation-of-Privilege not applicable.
</threat_model>

<verification>
- `npx tsc -b` exits 0
- `npx eslint src/components/CueGlyph.tsx src/components/BreathingShape.tsx src/components/OrbShape.tsx src/components/SquareShape.tsx src/components/DiamondShape.tsx` exits 0
- `npx vitest run src/components/CueGlyph.test.tsx src/components/BreathingShape.test.tsx src/components/OrbShape.test.tsx src/components/SquareShape.test.tsx src/components/DiamondShape.test.tsx` exits 0
- No hardcoded hex colors in CueGlyph.tsx
- OrbLeadIn / SquareLeadIn / DiamondLeadIn signatures carry no `cue` param (D-07)
</verification>

<success_criteria>
- 'labels' mode renders byte-identical to today's phase label (zero regression)
- 'arrow' mode renders the candidate-F chevron (up=In, down=Out); 'nose' mode renders candidate-D2 nose + up/down arrows
- Arrow & nose modes carry aria-hidden SVG + sr-only localized In/Out text; root aria-label unchanged (CUE-03)
- Lead-in countdown digit unchanged in all 3 cue modes (D-07)
- cue threads through all 3 variants; green gate passes
</success_criteria>

<output>
After completion, create `.planning/phases/25-labels-vs-icons-cue-toggle/25-03-SUMMARY.md`
</output>
