---
phase: 25-labels-vs-icons-cue-toggle
plan: 05
type: execute
wave: 4
depends_on: ["25-02", "25-03", "25-04"]
files_modified:
  - src/app/App.tsx
  - src/app/App.test.tsx
autonomous: false
requirements: [CUE-01, CUE-03]
must_haves:
  decisions_covered:
    - "D-14: App consumes useVisualCue and passes the chosen cue to BreathingShape alongside variant (App-side half of the hook pair)"
  truths:
    - "App reads the live cue via useVisualCue and captures it at session Start (sessionCueRef), mirroring sessionVariantRef"
    - "App passes the captured-or-live cue to BreathingShape alongside variant"
    - "A user who picks Arrow or Nose in SettingsDialog and starts a session sees the chosen cue glyph in the running orb"
    - "Changing the cue mid-session does not change the running cue (captured at Start); the new cue applies to the next session"
    - "The operator has visually approved the Arrow (candidate F) and Nose (candidate D2) glyphs across all 3 variants (Orb, Square, Diamond) before phase close"
  artifacts:
    - path: "src/app/App.tsx"
      provides: "useVisualCue consumption + sessionCueRef capture-at-Start + cue prop to BreathingShape"
      contains: "useVisualCue"
  key_links:
    - from: "src/app/App.tsx"
      to: "src/components/BreathingShape.tsx"
      via: "cue prop"
      pattern: "cue=\\{sessionCue \\?\\? liveCue\\}"
    - from: "src/app/App.tsx"
      to: "src/hooks/useVisualCue.ts"
      via: "live cue read"
      pattern: "useVisualCue\\(\\)"
---

<objective>
Complete the end-to-end wiring: consume `useVisualCue` in `App.tsx`, capture the cue at session Start into a `sessionCueRef` (mirroring `sessionVariantRef` — variant precedent is capture-at-Start, CONTEXT Claude's Discretion), and pass the captured-or-live cue to `BreathingShape` alongside `variant`. Then run an operator visual-review checkpoint: render the chosen Arrow (candidate F) and Nose (candidate D2) glyphs across all 3 variants for approval before lock.

Purpose: This is the last link — without it the picker writes the envelope but the running orb never reads it. The checkpoint honors the operator's explicit request (CONTEXT `<specifics>`) for a visual-review loop on the final SVGs before lock, the same loop used for the palette redesigns.
Output: `App.tsx` wired + test updated, plus operator sign-off on the glyphs.
</objective>

<execution_context>
@$HOME/.claude-personal-personal/get-shit-done/workflows/execute-plan.md
@$HOME/.claude-personal-personal/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-labels-vs-icons-cue-toggle/25-CONTEXT.md
@.planning/phases/25-labels-vs-icons-cue-toggle/25-PATTERNS.md
@.planning/phases/25-labels-vs-icons-cue-toggle/25-cue-icon-mockup.html

<interfaces>
<!-- The variant dimension's App-side wiring is the verbatim analog. -->

From src/hooks/useVisualCue.ts (Plan 02): `useVisualCue()` returns `{ cue, setCue }`.
From src/components/BreathingShape.tsx (Plan 03): accepts an optional `cue?: CueStyleId` prop, default 'labels'.

App.tsx variant-dimension analog sites (mirror each for cue):
- import + consume: `import { useVisualVariant }` (line 18), `const { variant: liveVariant } = useVisualVariant()` (line 170)
- capture-at-Start ref: `sessionVariantRef` + `sessionVariant` state pair (lines 211-212), captured `liveVariant` at `onStartClick` (lines 365-366), cleared on session end (lines 348-349, 537-538)
- pass to shape: `<BreathingShape variant={sessionVariant ?? liveVariant} ... />` (lines 669-670)
- SettingsDialog receives `strings={uiStrings}` (line 743) — the `cue` group is already inside `uiStrings`, NO change needed there.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wire useVisualCue + capture-at-Start into App.tsx</name>
  <files>src/app/App.tsx, src/app/App.test.tsx</files>
  <read_first>
    - src/app/App.tsx (the file being modified — read the useVisualVariant import (line 18) + consumption (line 170), the sessionVariantRef / sessionVariant pair (lines 211-212), onStartClick capture (lines 365-366), session-end clears (lines 348-349, 537-538), and the BreathingShape render (lines 669-670))
    - src/app/App.test.tsx (the test file being modified — read existing session-start / BreathingShape-prop assertions)
    - src/hooks/useVisualCue.ts (Plan 02 output — the hook consumed here)
    - src/components/BreathingShape.tsx (Plan 03 output — the cue prop target)
    - .planning/phases/25-labels-vs-icons-cue-toggle/25-PATTERNS.md (the "ALSO: App-side wiring" section — the exact mirror sites)
  </read_first>
  <behavior>
    - App calls useVisualCue() and holds the live cue
    - At onStartClick the live cue is captured into a sessionCueRef and a sessionCue state value (mirrors sessionVariant)
    - BreathingShape receives cue={sessionCue ?? liveCue}
    - On session end sessionCue / sessionCueRef are cleared (back to using liveCue)
    - A cue change made mid-session does not alter the running BreathingShape's cue prop; it takes effect on the next session start
    - With the default prefs (cue='labels') BreathingShape renders today's text label — zero regression
  </behavior>
  <action>
    In src/app/App.tsx, mirror the variant-dimension wiring exactly for cue: (1) add `import { useVisualCue } from '../hooks/useVisualCue'` next to the useVisualVariant import; consume `const { cue: liveCue } = useVisualCue()` next to `liveVariant`. (2) Add a `sessionCueRef` ref + `sessionCue` state pair mirroring `sessionVariantRef` / `sessionVariant` (lines 211-212). (3) In `onStartClick` capture `liveCue` into both, mirroring lines 365-366. (4) At every session-end site that clears `sessionVariant` / `sessionVariantRef` (lines 348-349 and 537-538), clear `sessionCue` / `sessionCueRef` the same way. (5) Add `cue={sessionCue ?? liveCue}` to the `<BreathingShape>` render alongside `variant={sessionVariant ?? liveVariant}` (lines 669-670). Do NOT touch the SettingsDialog `strings={uiStrings}` prop — the `cue` group already rides inside `uiStrings`. In src/app/App.test.tsx add/extend tests: (a) starting a session with cue='arrow' seeded passes cue='arrow' to BreathingShape; (b) changing the cue mid-session leaves the running BreathingShape's cue unchanged; (c) the default (cue='labels') renders the text label — zero regression. If App.test.tsx mocks BreathingShape, assert the `cue` prop value passed.
  </action>
  <verify>
    <automated>npx vitest run src/app/App.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/app/App.test.tsx` exits 0 including the new cue cases
    - `grep -n "useVisualCue" src/app/App.tsx` matches both the import and the consumption
    - `grep -n "sessionCueRef" src/app/App.tsx` matches (capture-at-Start ref present)
    - `grep -En "cue=\{sessionCue" src/app/App.tsx` matches the BreathingShape render
    - `grep -c "sessionCue" src/app/App.tsx` is at least 4 (ref + state + capture + at-least-one clear)
    - `npx tsc -b` exits 0
  </acceptance_criteria>
  <done>App reads liveCue, captures at Start into sessionCueRef, passes captured-or-live cue to BreathingShape, clears on session end; tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Full green gate + build verification</name>
  <files>(no source edits — verification only)</files>
  <read_first>
    - package.json (the build / test / lint scripts: `build` = `tsc -b && vite build`, `test:run` = `vitest run`, `lint` = `eslint .`)
  </read_first>
  <action>
    Run the full per-commit green gate end-to-end across the whole repo to confirm Phase 25's changes integrate cleanly with every existing test: `npx tsc -b`, then `npx eslint .`, then `npm run build`, then `npx vitest run`. All four must exit 0. This is the D-09/D-15 invariant carried forward (STATE.md "Per-commit green-gate"). If any command fails, fix the cause before proceeding to the checkpoint — do not advance with a red gate.
  </action>
  <verify>
    <automated>npx tsc -b && npx eslint . && npm run build && npx vitest run</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc -b` exits 0
    - `npx eslint .` exits 0
    - `npm run build` exits 0 (production bundle builds)
    - `npx vitest run` exits 0 (entire suite green — no regressions in the existing variant/theme/timbre/locale tests)
  </acceptance_criteria>
  <done>The full repo green gate (tsc + lint + build + test) passes with Phase 25 changes integrated.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Operator visual review of the Arrow + Nose glyphs across all 3 variants</name>
  <files>(no source edits — operator verification of CueGlyph.tsx rendering; any requested tweak applies to src/components/CueGlyph.tsx)</files>
  <action>This is a blocking human-verify checkpoint — Claude does not edit source here unless the operator requests an SVG tweak. Start the dev server, present the rendered Arrow (candidate F) and Nose (candidate D2) glyphs across all 3 variants, and follow the how-to-verify steps below. If the operator requests path-data or sizing changes, apply them to src/components/CueGlyph.tsx and re-present until approved.</action>
  <verify>Operator types "approved" after confirming the Arrow + Nose glyphs across Orb, Square, and Diamond per the acceptance criteria below.</verify>
  <done>Operator has signed off on candidate F + D2 across all 3 variants; any requested SVG tweaks applied to CueGlyph.tsx and re-verified.</done>
  <what-built>
    The cue picker (CuePicker) and the in-orb cue rendering (CueGlyph) are fully wired end-to-end:
    - SettingsDialog has a 5th picker "Cue style" (Text / Arrow / Nose) between Variant and Timbre.
    - Arrow mode renders mockup candidate F (soft solid chevron — up=In, down=Out).
    - Nose mode renders mockup candidate D2 (nose outline + straight up/down arrows).
    - Both render across all 3 variants (Orb, Square, Diamond) with token-driven colors and a visually-hidden In/Out label for screen readers.
    The dev server is running so the operator can see the actual rendered SVGs in the real app — this is the visual-review loop the operator explicitly requested (CONTEXT `<specifics>`), the same loop used for the palette redesigns.
  </what-built>
  <how-to-verify>
    1. Start the dev server: `npm run dev` — open the printed localhost URL (note the `/hrv/` base path).
    2. Open SettingsDialog (the settings affordance). Confirm a 5th picker "Cue style" appears between "Variant" and "Timbre", with three options Text / Arrow / Nose, each showing a small preview glyph.
    3. For each of the 3 variants (use the Variant picker — Orb, then Square, then Diamond):
       a. Select cue = Arrow. Start a session. Watch a full In→Out cycle. Confirm the chevron points UP on the In phase and DOWN on the Out phase, reads cleanly, and is well-sized inside the shape.
       b. Select cue = Nose. Start a session. Confirm the nose drawing + arrows render legibly (arrows up on In, down on Out) — pay attention to Diamond, whose clip-path makes the center slot tightest.
       c. Select cue = Text. Confirm the In/Out word renders exactly as before (zero regression).
    4. Confirm the lead-in 3-2-1 countdown digit looks unchanged in all 3 cue modes.
    5. Optionally toggle a couple of the 5 palettes to confirm the glyph colors track the theme tokens, and enable OS reduced-motion to confirm the static glyph is unaffected.
    6. Cross-reference against the design intent in `.planning/phases/25-labels-vs-icons-cue-toggle/25-cue-icon-mockup.html` (candidates F and D2).

    If the Arrow or Nose SVG needs path-data or sizing tweaks for legibility on any variant (final SVG tuning is Claude's Discretion per CONTEXT D-03/D-05), describe the change — Claude will adjust CueGlyph.tsx and re-present.
  </how-to-verify>
  <acceptance_criteria>
    - The operator confirms the Arrow (F) and Nose (D2) glyphs render acceptably across Orb, Square, and Diamond.
    - Up=In / Down=Out direction semantics are correct in both Arrow and Nose modes.
    - Text mode is a confirmed zero-regression.
    - The lead-in countdown digit is unchanged in all 3 modes.
    - Any requested SVG tweaks are applied to CueGlyph.tsx and re-verified before sign-off.
  </acceptance_criteria>
  <resume-signal>Type "approved" to lock the glyphs, or describe the SVG tweaks needed.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| useVisualCue → App render | The live cue crosses from the prefs layer into the running session render; already whitelist-coerced (Plan 01). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-09 | Tampering | A cross-tab `storage` event mutating the cue mid-session | mitigate | Capture-at-Start: `sessionCueRef` freezes the cue at `onStartClick`; `useVisualCue` re-reads only feed `liveCue`, and `cue={sessionCue ?? liveCue}` uses the frozen value during a session. A mid-session envelope change cannot alter the running cue — it applies to the next session. Asserted in App.test.tsx. |
| T-25-10 | Denial of Service | A malformed envelope crashing the session render | accept | `useVisualCue` re-reads through `loadPrefs()` → `coerceCue` (Plan 01) — any bad value resolves to `DEFAULT_CUE = 'labels'`, the safe text render. No crash path. |

End-to-end client-side wiring — no network, no auth, no server, no PII introduced. Spoofing / Repudiation / Information Disclosure / Elevation-of-Privilege not applicable.
</threat_model>

<verification>
- `npx tsc -b` exits 0
- `npx eslint .` exits 0
- `npm run build` exits 0
- `npx vitest run` exits 0 (full suite, no regressions)
- Operator sign-off recorded on the Arrow + Nose glyphs across all 3 variants
</verification>

<success_criteria>
- App reads liveCue via useVisualCue, captures it at Start, passes captured-or-live cue to BreathingShape
- A picked Arrow/Nose cue renders in the running orb; mid-session changes apply only to the next session
- Default 'labels' is a confirmed zero-regression
- Full repo green gate passes
- Operator has visually approved candidate F + D2 across Orb, Square, Diamond
</success_criteria>

<output>
After completion, create `.planning/phases/25-labels-vs-icons-cue-toggle/25-05-SUMMARY.md`
</output>
</output>
