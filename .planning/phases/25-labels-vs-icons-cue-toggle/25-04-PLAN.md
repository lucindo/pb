---
phase: 25-labels-vs-icons-cue-toggle
plan: 04
type: execute
wave: 3
depends_on: ["25-01", "25-02", "25-03"]
files_modified:
  - src/components/CuePicker.tsx
  - src/components/CuePicker.test.tsx
  - src/components/SettingsDialog.tsx
  - src/components/SettingsDialog.test.tsx
autonomous: true
requirements: [CUE-01]
must_haves:
  decisions_covered:
    - "D-10: CuePicker component mirrors VariantPicker posture verbatim — role=radiogroup, { disabled, strings, sectionLabel } prop contract, token-only colors, 44px hit area, optimistic-UI local state"
    - "D-11: CuePicker placed directly after VariantPicker — SettingsDialog order Theme, Variant, Cue, Timbre, Language"
  truths:
    - "SettingsDialog shows a 5th picker, Cue style, positioned directly after Variant and before Timbre"
    - "The Cue picker offers three options labelled Text / Arrow / Nose, each a radiogroup radio"
    - "Clicking a cue option selects it, updates the radiogroup aria-checked state, and persists the choice (via useCueChoice)"
    - "Each cue option button shows a small preview glyph above its label"
    - "The Cue picker is disabled when a session is in view (inSessionView threads in as disabled)"
  artifacts:
    - path: "src/components/CuePicker.tsx"
      provides: "Radiogroup picker for the cue dimension"
      exports: ["CuePicker"]
    - path: "src/components/SettingsDialog.tsx"
      provides: "CuePicker rendered between VariantPicker and TimbrePicker"
      contains: "CuePicker"
  key_links:
    - from: "src/components/CuePicker.tsx"
      to: "src/hooks/useCueChoice.ts"
      via: "state + write path"
      pattern: "useCueChoice"
    - from: "src/components/SettingsDialog.tsx"
      to: "src/components/CuePicker.tsx"
      via: "render placement after VariantPicker"
      pattern: "<CuePicker"
---

<objective>
Create the `CuePicker` radiogroup component — a verbatim posture clone of `VariantPicker` — and wire it into `SettingsDialog` directly after `VariantPicker`, giving the new dialog order Theme → Variant → Cue → Timbre → Language (CONTEXT D-11). Each cue option button stacks a small preview glyph (the actual Text / Arrow / Nose rendering) above the option label.

Purpose: This is the user-facing control for CUE-01 — the only way a user changes their cue style. It consumes `useCueChoice` (Plan 02) for state + persistence and reuses the `CueGlyph` rendering (Plan 03) for the in-button previews.
Output: One new picker component + test, `SettingsDialog` wired + test updated.
</objective>

<execution_context>
@$HOME/.claude-personal-personal/get-shit-done/workflows/execute-plan.md
@$HOME/.claude-personal-personal/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-labels-vs-icons-cue-toggle/25-CONTEXT.md
@.planning/phases/25-labels-vs-icons-cue-toggle/25-PATTERNS.md

<interfaces>
<!-- VariantPicker is the verbatim posture analog. CuePicker clones it. -->

From src/components/VariantPicker.tsx — picker prop contract (Phase 15 D-02: NO value prop):
```typescript
export interface VariantPickerProps { disabled: boolean; strings: UiStrings['variants']; sectionLabel: string }
export function VariantPicker({ disabled, strings, sectionLabel }: VariantPickerProps) {
  const { variant, setVariant } = useVariantChoice()
  // <div role="radiogroup" aria-labelledby="variant-picker-label" aria-disabled={disabled}
  //      className="mt-2 grid grid-cols-3 gap-2"> ... per-option <button role="radio" aria-checked> ...
}
```
The token-bound class strings `baseClasses` / `selectedClasses` / `unselectedClasses` (VariantPicker lines 37-39) are copied verbatim — they are token-only (D-23). `baseClasses` already includes `flex flex-col items-center gap-1` for the stacked glyph-above-label layout.

From Plan 01 — domain: `CUE_OPTIONS` (readonly ['labels','arrow','nose']), `type CueStyleId`.
From Plan 02 — hook: `useCueChoice()` returns `{ cue, setCue }`.
From Plan 03 — `CueGlyph` component: `{ cue, phase, phaseLabel }`.
From Plan 01 — strings: `UiStrings['cue']` is `{ labels, arrow, nose }`; `strings.settings.cueLabel`.

SettingsDialog (src/components/SettingsDialog.tsx): VariantPicker is rendered at line 85; the strings prop is typed `Pick<UiStrings, 'settings' | 'themes' | 'variants' | 'timbres'>` at line 32.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create the CuePicker radiogroup component</name>
  <files>src/components/CuePicker.tsx, src/components/CuePicker.test.tsx</files>
  <read_first>
    - src/components/VariantPicker.tsx (the verbatim posture analog — clone the entire file)
    - src/components/VariantPicker.test.tsx (the verbatim test analog — note the seedVariant() envelope-seeding fixture and UI_STRINGS.en usage)
    - src/components/CueGlyph.tsx (Plan 03 — reused for the in-button preview glyphs)
    - src/hooks/useCueChoice.ts (Plan 02 — state + write path)
    - src/domain/settings.ts (Plan 01 — CUE_OPTIONS, CueStyleId)
    - .planning/phases/25-labels-vs-icons-cue-toggle/25-PATTERNS.md (the "NEW src/components/CuePicker.tsx" section + "Test fixtures" shared pattern)
  </read_first>
  <behavior>
    - CuePicker renders a `role="radiogroup"` with `aria-labelledby="cue-picker-label"` and `aria-disabled` reflecting the disabled prop
    - It renders exactly 3 `role="radio"` buttons labelled Text, Arrow, Nose (from strings.labels/arrow/nose)
    - The currently-selected cue's button has `aria-checked="true"`; the others `aria-checked="false"`
    - Clicking the Arrow button calls setCue('arrow') — the seeded localStorage envelope's cue becomes 'arrow' and aria-checked moves to the Arrow button
    - Each button shows a preview glyph above the text label (the CueGlyph rendering for that option)
    - When disabled, every button is disabled and the radiogroup is aria-disabled
    - The section-label `<p>` carries `id="cue-picker-label"` (distinct from variant-picker-label — no DOM id collision)
  </behavior>
  <action>
    Create src/components/CuePicker.tsx as a clone of src/components/VariantPicker.tsx with: prop type `CuePickerProps { disabled: boolean; strings: UiStrings['cue']; sectionLabel: string }`; `const { cue, setCue } = useCueChoice()`; map over `CUE_OPTIONS` with `(id: CueStyleId)`; `selected = cue === id`; `label = strings[id]`; `onClick={() => { setCue(id) }}`. Copy `baseClasses`/`selectedClasses`/`unselectedClasses` verbatim. Rename the section-label id and `aria-labelledby` from `variant-picker-label` to `cue-picker-label` (avoids the DOM id collision with VariantPicker). For each option's preview, render a small `<CueGlyph cue={id} phase="in" phaseLabel={label} />` (or a deliberately small wrapper) stacked above `<span>{label}</span>` — note CueGlyph's 'labels' mode renders text-5xl; for the picker preview wrap it so it renders at a small swatch size (e.g. a fixed-size container with a scale or an explicitly small width — the picker preview must be visibly small, not the full in-orb display size). Keep the `grid grid-cols-3 gap-2` radiogroup layout and the `flex flex-col items-center gap-1` per-button stacking from `baseClasses`. Imports: `CUE_OPTIONS, type CueStyleId` from `'../domain/settings'`, `useCueChoice` from `'../hooks/useCueChoice'`, `CueGlyph` from `'./CueGlyph'`, `type UiStrings` from `'../content/strings'`. Adapt the file-header comment to the cue dimension (cite Phase 15 D-02 prop contract, D-23 token-only). Create src/components/CuePicker.test.tsx as a clone of VariantPicker.test.tsx: a `seedCue()` helper writing a known envelope (whose `prefs` object includes the new `cue` field — PATTERNS.md "Test fixtures") to localStorage under STATE_KEY, `UI_STRINGS.en.cue` as the strings fixture, beforeEach/afterEach clearing storage, covering the behavior above.
  </action>
  <verify>
    <automated>npx vitest run src/components/CuePicker.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/components/CuePicker.test.tsx` exits 0
    - `grep -n 'id="cue-picker-label"' src/components/CuePicker.tsx` matches AND `grep -c "variant-picker-label" src/components/CuePicker.tsx` returns 0 (no id collision)
    - `grep -n "useCueChoice" src/components/CuePicker.tsx` matches the state hook
    - `grep -E "#[0-9a-fA-F]{3,6}" src/components/CuePicker.tsx` returns nothing (token-only colors, D-23)
    - The rendered radiogroup contains exactly 3 role=radio buttons (asserted in test)
  </acceptance_criteria>
  <done>CuePicker renders a 3-option radiogroup with preview glyphs, consumes useCueChoice, token-only colors, distinct DOM id; tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire CuePicker into SettingsDialog</name>
  <files>src/components/SettingsDialog.tsx, src/components/SettingsDialog.test.tsx</files>
  <read_first>
    - src/components/SettingsDialog.tsx (the file being modified — read the import block, the strings prop type at line 32, the picker render block lines 83-87, and the D-10 picker-order comment at line 83)
    - src/components/SettingsDialog.test.tsx (the test file being modified — read existing picker-presence and ordering assertions)
    - src/components/CuePicker.tsx (Task 1 output — the component being wired)
    - .planning/phases/25-labels-vs-icons-cue-toggle/25-PATTERNS.md (the "MODIFY src/components/SettingsDialog.tsx" section — three edit sites)
  </read_first>
  <behavior>
    - SettingsDialog renders 5 pickers in DOM order: Theme, Variant, Cue, Timbre, Language
    - The CuePicker receives `disabled={inSessionView}`, `strings={strings.cue}`, `sectionLabel={strings.settings.cueLabel}`
    - The Cue section label text is "Cue style" (EN)
    - When inSessionView is true the CuePicker is disabled along with the other pickers
  </behavior>
  <action>
    In src/components/SettingsDialog.tsx: (1) add `import { CuePicker } from './CuePicker'` after the VariantPicker import. (2) Extend the strings prop type from `Pick<UiStrings, 'settings' | 'themes' | 'variants' | 'timbres'>` to include `'cue'`. (3) Insert `<CuePicker disabled={inSessionView} strings={strings.cue} sectionLabel={strings.settings.cueLabel} />` directly after the `<VariantPicker .../>` line and before `<TimbrePicker .../>`. Update the D-10 picker-order comment at line 83 to read "Theme → Variant → Cue → Timbre → Language order". In src/components/SettingsDialog.test.tsx: update any picker-count / picker-order assertions to expect the 5-picker order Theme → Variant → Cue → Timbre → Language; add an assertion that the Cue section label "Cue style" is present and that the CuePicker is disabled when inSessionView is true. Ensure the test's strings fixture supplies the `cue` group (use UI_STRINGS.en if the test already imports it).
  </action>
  <verify>
    <automated>npx vitest run src/components/SettingsDialog.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/components/SettingsDialog.test.tsx` exits 0
    - `grep -n "<CuePicker" src/components/SettingsDialog.tsx` matches, positioned between the VariantPicker and TimbrePicker render lines
    - `grep -n "'cue'" src/components/SettingsDialog.tsx` matches inside the strings `Pick<...>` type
    - `grep -n "Variant → Cue → Timbre" src/components/SettingsDialog.tsx` matches (D-10 comment updated)
    - `npx tsc -b` exits 0
  </acceptance_criteria>
  <done>CuePicker rendered between VariantPicker and TimbrePicker; strings prop type extended; picker-order comment updated; tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user click → useCueChoice write | A radio click triggers a localStorage envelope write via `useCueChoice` (covered by Plan 02 / Plan 01). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-07 | Tampering | The picker writing an out-of-set cue value | accept | The button `onClick` can only pass an `id` drawn from the typed `CUE_OPTIONS` array — there is no free-text input. `useCueChoice` writes through `savePrefs`; a subsequent read re-coerces via `coerceCue` (Plan 01). No injection surface. |
| T-25-08 | Denial of Service | The picker enabled mid-session causing a cue change during a live session | accept | `disabled={inSessionView}` blocks interaction during a session — the established picker contract (PATTERNS.md "Picker prop contract"). Asserted in SettingsDialog.test.tsx. |

Client-side UI control only — no network, no auth, no server, no PII. Spoofing / Repudiation / Information Disclosure / Elevation-of-Privilege not applicable.
</threat_model>

<verification>
- `npx tsc -b` exits 0
- `npx eslint src/components/CuePicker.tsx src/components/SettingsDialog.tsx src/components/CuePicker.test.tsx src/components/SettingsDialog.test.tsx` exits 0
- `npx vitest run src/components/CuePicker.test.tsx src/components/SettingsDialog.test.tsx` exits 0
- No hardcoded hex colors in CuePicker.tsx
- SettingsDialog DOM order: Theme, Variant, Cue, Timbre, Language
</verification>

<success_criteria>
- CuePicker is a 3-option radiogroup (Text/Arrow/Nose) with preview glyphs, consuming useCueChoice
- SettingsDialog shows the Cue picker between Variant and Timbre with section label "Cue style"
- The Cue picker is disabled in-session; selecting an option persists the choice
- Green gate passes
</success_criteria>

<output>
After completion, create `.planning/phases/25-labels-vs-icons-cue-toggle/25-04-SUMMARY.md`
</output>
</output>
