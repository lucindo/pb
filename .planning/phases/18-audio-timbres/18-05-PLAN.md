---
phase: 18-audio-timbres
plan: 05
type: execute
wave: 4
depends_on:
  - 18-02
files_modified:
  - src/components/TimbrePicker.tsx
  - src/components/TimbrePicker.test.tsx
autonomous: true
requirements:
  - TIMBRE-01
  - TIMBRE-04
tags:
  - ui
  - picker
  - radiogroup
must_haves:
  truths:
    - "TimbrePicker renders a 4-option radiogroup (Bowl / Bell / Sine / Chime) using `useTimbreChoice` (Plan 02) for read + write"
    - "Picker UI is a verbatim mirror of ThemePicker.tsx (D-06 + PATTERNS.md substitution table) — same chrome tokens, same a11y posture, same selected/unselected button classes"
    - "Capitalization uses `id.charAt(0).toUpperCase() + id.slice(1)` (D-06 — name-only label, no descriptor text, no SVG glyph, no audio preview button)"
    - "All color references use var(--color-breathing-*) tokens — THEME-UI-01 guard test stays green (D-19)"
    - "`role='radiogroup'` + `aria-labelledby` + `aria-disabled` on the container; `role='radio'` + `aria-checked` + `disabled` on each option button — D-20 a11y posture identical to ThemePicker"
    - "44×44 minimum hit area via min-h-12 + padding (Phase 2 + Phase 17 D-24 carry-forward)"
    - "Phase 15 D-02 picker prop contract preserved — TimbrePicker accepts ONLY { disabled: boolean }; no new props at SettingsDialog call site"
    - "Phase 15 D-01 invariant preserved — SettingsDialog.tsx is NOT edited"
    - "Phase 14 D-09 file-split preserved — domain/settings.ts and storage/prefs.ts are NOT edited"
    - "tsc && lint && build && test exit 0 (D-13); theme.no-hardcoded-classes.test.ts stays green (D-19)"
  artifacts:
    - path: "src/components/TimbrePicker.tsx"
      provides: "Filled-in radiogroup body — Bowl/Bell/Sine/Chime selection wired through useTimbreChoice"
      contains: "useTimbreChoice"
      contains_also: "TIMBRE_OPTIONS"
      contains_also: "role=\"radiogroup\""
      contains_also: "role=\"radio\""
    - path: "src/components/TimbrePicker.test.tsx"
      provides: "Radiogroup behavior + disabled gating + savePrefs + custom-event dispatch test coverage"
      contains: "setTimbre"
      contains_also: "aria-checked"
  key_links:
    - from: "src/components/TimbrePicker.tsx"
      to: "src/hooks/useTimbreChoice.ts"
      via: "import { useTimbreChoice }"
      pattern: "useTimbreChoice"
    - from: "src/components/TimbrePicker.tsx"
      to: "src/domain/settings.ts"
      via: "import { TIMBRE_OPTIONS, type TimbreId }"
      pattern: "TIMBRE_OPTIONS"
---

<objective>
Fill the Phase 15 `TimbrePicker.tsx` stub body with a real radiogroup, verbatim mirror of `ThemePicker.tsx` (D-06): four buttons (Bowl / Bell / Sine / Chime) bound to `useTimbreChoice` (Plan 02), name-only labels via the standard capitalize formula, token-bound chrome via `var(--color-breathing-*)` (D-19 THEME-UI-01 guard), and the radiogroup a11y posture (D-20). The picker self-reads `loadPrefs().timbre` indirectly via `useTimbreChoice`'s mount-time read and self-owns the write path — Phase 15 D-01 invariant (SettingsDialog.tsx NOT edited) and D-02 prop contract (`{ disabled: boolean }` only) preserved.

Purpose: Land the user-facing selection surface for TIMBRE-01. After this plan, opening SettingsDialog → Timbre section shows a 4-button radiogroup; selecting an option immediately writes to storage and updates local UI, but the change does NOT affect a running session (TIMBRE-03 — picker is disabled while `inSessionView`; Plan 06 closes the loop by capturing at Start).

Output: Edits to `src/components/TimbrePicker.tsx` (~30 LOC of net delta — stub body replaced) + edits to `src/components/TimbrePicker.test.tsx` (~80 LOC of new test coverage — current stub test is minimal per Phase 15 D-04).
</objective>

<execution_context>
@$HOME/.claude-personal-personal/get-shit-done/workflows/execute-plan.md
@$HOME/.claude-personal-personal/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md

@.planning/phases/18-audio-timbres/18-CONTEXT.md
@.planning/phases/18-audio-timbres/18-PATTERNS.md
@.planning/phases/18-audio-timbres/18-02-SUMMARY.md

@src/components/TimbrePicker.tsx
@src/components/TimbrePicker.test.tsx
@src/components/ThemePicker.tsx
@src/hooks/useTimbreChoice.ts
@src/domain/settings.ts

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from existing source. -->

From `src/components/ThemePicker.tsx` (full file — verbatim mirror template):

  export interface ThemePickerProps { disabled: boolean }

  export function ThemePicker({ disabled }: ThemePickerProps) {
    const { theme, setTheme } = useThemeChoice()
    return (
      <div>
        <p id="theme-picker-label" className="text-sm font-semibold text-[var(--color-breathing-accent-strong)]">Theme</p>
        <div
          role="radiogroup"
          aria-labelledby="theme-picker-label"
          aria-disabled={disabled}
          className="mt-2 grid grid-cols-3 gap-2"
        >
          {THEME_OPTIONS.map((id: ThemeId) => {
            const selected = theme === id
            const label = id.charAt(0).toUpperCase() + id.slice(1)
            const selectedClasses = '...'
            const unselectedClasses = '...'
            const baseClasses = 'min-h-12 ...'
            return (
              <button key={id} type="button" role="radio" aria-checked={selected}
                disabled={disabled} onClick={() => { setTheme(id) }}
                className={`${baseClasses} ${selected ? selectedClasses : unselectedClasses}`}
              >{label}</button>
            )
          })}
        </div>
      </div>
    )
  }

From `src/hooks/useTimbreChoice.ts` (Plan 02 output):

  export function useTimbreChoice(): { timbre: TimbreId; setTimbre: (next: TimbreId) => void }

From `src/domain/settings.ts` (Phase 14 locked):

  export const TIMBRE_OPTIONS = ['bowl', 'bell', 'sine', 'chime'] as const
  export type TimbreId = typeof TIMBRE_OPTIONS[number]

TimbrePicker substitutions from the ThemePicker mirror (D-06):

- `theme-picker-label` → `timbre-picker-label`
- `Theme` (label text) → `Timbre`
- `THEME_OPTIONS` → `TIMBRE_OPTIONS`; `ThemeId` → `TimbreId`
- `{ theme, setTheme } = useThemeChoice()` → `{ timbre, setTimbre } = useTimbreChoice()`
- `theme === id` (selected check) → `timbre === id`
- `setTheme(id)` → `setTimbre(id)`
- `grid-cols-3` (6 themes) → `grid-cols-2` (4 timbres fit 2×2 — D-06 starting point; could also be grid-cols-4 1×4. Pick grid-cols-2 for native-dialog mobile width)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Replace `src/components/TimbrePicker.tsx` stub body with verbatim ThemePicker mirror</name>
  <read_first>
    src/components/ThemePicker.tsx (full file — 53 lines — the verbatim mirror template),
    src/components/TimbrePicker.tsx (current Phase 15 stub — 26 lines — to be replaced),
    src/hooks/useTimbreChoice.ts (Plan 02 output — the hook to consume),
    src/domain/settings.ts (TIMBRE_OPTIONS + TimbreId — Phase 14 locked),
    .planning/phases/18-audio-timbres/18-CONTEXT.md (D-06 name-only radiogroup; D-17 prop contract; D-19 THEME-UI-01 guard; D-20 a11y floor),
    .planning/phases/18-audio-timbres/18-PATTERNS.md (§"`src/components/TimbrePicker.tsx` (EDIT)" — full substitution rules + invariants list)
  </read_first>
  <behavior>
    - The file is rewritten with the ThemePicker-mirror body. The existing `import { loadPrefs } from '../storage/prefs'` is REMOVED (the stub used it; useTimbreChoice now owns the read).
    - New imports: `import { TIMBRE_OPTIONS, type TimbreId } from '../domain/settings'` and `import { useTimbreChoice } from '../hooks/useTimbreChoice'`.
    - The exported component signature is unchanged: `export function TimbrePicker({ disabled }: TimbrePickerProps)`. The `TimbrePickerProps` interface is unchanged (`{ disabled: boolean }` — D-02/D-17).
    - Body structure: `<div>` outer → `<p id="timbre-picker-label" class="...accent-strong">Timbre</p>` → `<div role="radiogroup" aria-labelledby="timbre-picker-label" aria-disabled={disabled} className="mt-2 grid grid-cols-2 gap-2">` containing the 4 `<button>` elements from `TIMBRE_OPTIONS.map`.
    - Each button: `<button key={id} type="button" role="radio" aria-checked={selected} disabled={disabled} onClick={() => { setTimbre(id) }} className={...}>{label}</button>` where `label = id.charAt(0).toUpperCase() + id.slice(1)` (capitalize formula — D-06 verbatim from ThemePicker.tsx:31).
    - selectedClasses / unselectedClasses / baseClasses copied VERBATIM from ThemePicker.tsx:32-34 — every `var(--color-breathing-*)` token reference is unchanged (D-19 THEME-UI-01 guard).
    - The leading file comment is updated: replace the Phase 15 stub explanation with "Phase 18 (TIMBRE-01..05): verbatim mirror of ThemePicker.tsx (D-06 name-only radiogroup) with theme→timbre substitutions. useTimbreChoice owns read + write. SettingsDialog.tsx NOT edited (D-16). Phase 14 D-09 file-split preserved — domain/settings.ts + storage/prefs.ts NOT edited."
    - `grid-cols-2` for 4 timbres (2×2 grid — D-06 starting point; mobile-comfortable in the native dialog width). If smoke-test reveals 4 buttons too wide for the dialog max-w, the executor may swap to `grid-cols-4` (1×4) — record the choice in the SUMMARY.
  </behavior>
  <action>
    Edit `src/components/TimbrePicker.tsx`. (1) REPLACE the entire file content (preserve the export shape but replace the body verbatim from the ThemePicker mirror). (2) Update the leading comment block to reference Phase 18 + D-06 + D-16 + D-17 + D-19 + D-20 (a single comment block, ~10 lines, mirroring the ThemePicker.tsx:1-8 header style). (3) Imports block: `import { TIMBRE_OPTIONS, type TimbreId } from '../domain/settings'` + `import { useTimbreChoice } from '../hooks/useTimbreChoice'`. REMOVE `import { loadPrefs } from '../storage/prefs'` (no longer needed — useTimbreChoice owns the read). (4) `TimbrePickerProps` interface block stays IDENTICAL (`{ disabled: boolean }` only). (5) Component body: copy ThemePicker.tsx lines 17-53 verbatim, apply the substitution table from `<interfaces>` (theme→timbre / Theme→Timbre / THEME_OPTIONS→TIMBRE_OPTIONS / ThemeId→TimbreId / useThemeChoice→useTimbreChoice / setTheme→setTimbre / theme-picker-label→timbre-picker-label / grid-cols-3→grid-cols-2). (6) Do NOT add any descriptor text below each button (D-06 name-only). Do NOT add SVG glyph or audio preview button (D-06 explicit exclusions). Do NOT introduce any hardcoded `text-slate-*` / `bg-teal-*` / `text-white` / `bg-white` / `text-black` / `bg-black` Tailwind classes (D-19 guard). Do NOT touch SettingsDialog.tsx (D-16) or any non-TimbrePicker file.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&amp;1 | grep "TimbrePicker\.tsx" ; test $? -ne 0 &amp;&amp; echo "tsc clean"</automated>
    <automated>grep -q "useTimbreChoice" src/components/TimbrePicker.tsx &amp;&amp; echo "useTimbreChoice wired"</automated>
    <automated>grep -q 'role="radiogroup"' src/components/TimbrePicker.tsx &amp;&amp; echo "radiogroup posture present"</automated>
    <automated>grep -q 'role="radio"' src/components/TimbrePicker.tsx &amp;&amp; echo "radio buttons present"</automated>
    <automated>grep -q "aria-checked" src/components/TimbrePicker.tsx &amp;&amp; echo "aria-checked binding present"</automated>
    <automated>grep -q "TIMBRE_OPTIONS" src/components/TimbrePicker.tsx &amp;&amp; echo "TIMBRE_OPTIONS iterated"</automated>
    <automated>grep -q "id.charAt(0).toUpperCase()" src/components/TimbrePicker.tsx &amp;&amp; echo "Capitalize formula present (D-06 mirror)"</automated>
    <automated>! grep -E "loadPrefs" src/components/TimbrePicker.tsx &amp;&amp; echo "loadPrefs import removed (useTimbreChoice owns read)"</automated>
    <automated>! grep -E "(text-slate-[0-9]|bg-slate-[0-9]|text-teal-[0-9]|bg-teal-[0-9]|text-white|bg-white|text-black|bg-black)" src/components/TimbrePicker.tsx &amp;&amp; echo "D-19 THEME-UI-01 guard: zero hardcoded color utilities"</automated>
    <automated>grep -q "min-h-12" src/components/TimbrePicker.tsx &amp;&amp; echo "44×44 hit area via min-h-12 (D-20)"</automated>
  </verify>
  <done>
    `src/components/TimbrePicker.tsx` compiles green; renders a 4-option radiogroup bound to `useTimbreChoice`; uses `var(--color-breathing-*)` tokens throughout (THEME-UI-01 guard); a11y posture matches ThemePicker verbatim; capitalize formula present; `loadPrefs` import removed.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend `src/components/TimbrePicker.test.tsx` with radiogroup + dispatch coverage</name>
  <read_first>
    src/components/TimbrePicker.test.tsx (current — 27 lines — minimal stub-era test; will grow to cover the full radiogroup),
    src/components/ThemePicker.test.tsx (existing — full file — read first to mirror the test posture; the executor uses this as the test scaffolding template),
    src/components/TimbrePicker.tsx (Task 1 output — the SUT),
    src/hooks/useTimbreChoice.ts + .test.ts (for understanding the dispatch contract),
    .planning/phases/18-audio-timbres/18-CONTEXT.md (D-17 prop contract; D-18 CustomEvent contract; D-20 a11y)
  </read_first>
  <behavior>
    - Test posture mirrors `ThemePicker.test.tsx` — Vitest + @testing-library/react + the existing localStorage-seeding helper (or inline equivalent).
    - Tests to add (mirror the ThemePicker.test.tsx coverage; do NOT add tests beyond what ThemePicker has — parity, not expansion):
      - Renders 4 buttons with capitalized labels (Bowl, Bell, Sine, Chime).
      - The button matching the stored `prefs.timbre` has `aria-checked="true"`; others have `aria-checked="false"`.
      - Clicking an unselected button (e.g. Bell) calls `setTimbre('bell')` — verifiable by reading localStorage after the click or by spying on `dispatchEvent`.
      - The click also dispatches a `'hrv:prefs-changed'` CustomEvent with `detail: { key: 'timbre', value: 'bell' }`.
      - When `disabled={true}`, the radiogroup's `aria-disabled` attribute is `"true"`, each button is `disabled`, and clicking a button does NOT invoke setTimbre (storage unchanged + no dispatch).
      - The radiogroup container has `aria-labelledby="timbre-picker-label"` pointing at the visible label paragraph.
      - The `min-h-12` hit-area class is present on each button (D-20 a11y floor).
    - Replace the existing stub test that asserts `Timbre: bowl` read-only text (no longer valid — the picker now renders 4 buttons, not "Timbre: bowl" text).
  </behavior>
  <action>
    Edit `src/components/TimbrePicker.test.tsx`. (1) Read `src/components/ThemePicker.test.tsx` first to mirror its test posture (imports, render helper, seeding). (2) Replace the Phase 15 stub test entirely with the new test suite covering the behaviors in `<behavior>`. Mirror ThemePicker.test.tsx's structural choices: same `describe` block, same beforeEach/afterEach pattern, same localStorage seeding helper, same way of capturing CustomEvent dispatches. (3) Use the test values 'bell' and 'sine' / 'chime' for click scenarios (NOT 'bowl' as the click target — 'bowl' is the default; pick a non-default to ensure the click actually changes state). (4) Do NOT add tests for the underlying setTimbre logic (that's covered by `useTimbreChoice.test.ts` from Plan 02). (5) Do NOT introduce a new test infrastructure file. (6) Use `screen.getByRole('radio', { name: /Bell/i })` or similar role-based queries (mirror the ThemePicker test patterns).
  </action>
  <verify>
    <automated>npm test -- src/components/TimbrePicker.test.tsx --run 2>&amp;1 | tail -20</automated>
    <automated>grep -q "aria-checked" src/components/TimbrePicker.test.tsx &amp;&amp; echo "aria-checked assertion present"</automated>
    <automated>grep -q "hrv:prefs-changed" src/components/TimbrePicker.test.tsx &amp;&amp; echo "CustomEvent dispatch assertion present"</automated>
    <automated>grep -q "disabled" src/components/TimbrePicker.test.tsx &amp;&amp; echo "disabled gating asserted"</automated>
    <automated>! grep -q "Timbre: bowl" src/components/TimbrePicker.test.tsx &amp;&amp; echo "Stale Phase 15 stub assertion removed"</automated>
  </verify>
  <done>
    `src/components/TimbrePicker.test.tsx` passes green; coverage at parity with ThemePicker.test.tsx; capitalized labels asserted; aria-checked / aria-disabled / CustomEvent dispatch / disabled gate all covered; stale Phase 15 stub assertion replaced.
  </done>
</task>

<task type="auto">
  <name>Task 3: Global green-gate and commit</name>
  <read_first>
    .planning/phases/18-audio-timbres/18-CONTEXT.md (D-13 green-gate; D-16 SettingsDialog NOT edited; D-19 THEME-UI-01 guard)
  </read_first>
  <action>
    Run `npx tsc --noEmit && npm run lint && npm run build && npm test --run`. All four MUST exit 0 — including `theme.no-hardcoded-classes.test.ts` (D-19 guard for TimbrePicker.tsx). Verify NO touch to `src/components/SettingsDialog.tsx` (D-16), no touch to other component files, no touch to `useAudioCues.ts` / `App.tsx` (those are Plan 06 + Plan 04 respectively). Stage `src/components/TimbrePicker.tsx` + `src/components/TimbrePicker.test.tsx` and commit with message `feat(18-05): fill TimbrePicker radiogroup body (TIMBRE-01/04)`.
  </action>
  <verify>
    <automated>npx tsc --noEmit &amp;&amp; npm run lint &amp;&amp; npm run build &amp;&amp; npm test --run 2>&amp;1 | tail -5</automated>
    <automated>npm test -- src/styles/theme.no-hardcoded-classes.test.ts --run 2>&amp;1 | tail -10 ; echo "D-19 THEME-UI-01 guard check"</automated>
    <automated>git log --oneline -1 | grep -E "feat\(18-05\)" &amp;&amp; echo "Commit landed"</automated>
    <automated>git show --stat HEAD | grep -E "TimbrePicker\.(tsx|test\.tsx)" | wc -l | awk '$1==2 {print "Exactly 2 files in commit"}'</automated>
    <automated>! git show --stat HEAD | grep -E "(SettingsDialog|App\.tsx|useAudioCues|cueSynth|audioEngine|prefs|settings\.ts)" &amp;&amp; echo "No out-of-scope file touches (D-15/D-16 preserved)"</automated>
  </verify>
  <done>
    Four gates green; theme.no-hardcoded-classes guard remains green (D-19); single commit touching exactly the two TimbrePicker files; SettingsDialog.tsx, useAudioCues.ts, App.tsx, audio/, storage/, domain/ all untouched.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User click → setTimbre | The click handler `() => { setTimbre(id) }` is invoked with `id: TimbreId` — `id` comes from iterating `TIMBRE_OPTIONS` (locked at Phase 14 D-01), so the value is always a valid `TimbreId`. No XSS path: `label = id.charAt(0).toUpperCase() + id.slice(1)` operates on a TS-typed literal. |
| TimbrePicker → useTimbreChoice → localStorage | Same as Plan 02 — `savePrefs({ ...current, timbre: next })` writes through Phase 14 D-10 storage layer. No new ingress. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-05-01 | Cross-Site Scripting | The `label` string is rendered inside `<button>` text content | mitigate | React's default text-node rendering escapes all string values. `label` is derived from TS-typed `TimbreId` (compile-time enum, not user input). No `dangerouslySetInnerHTML` used. |
| T-18-05-02 | Tampering | A future contributor could add a hardcoded `text-white` class and silently break THEME-UI-01 token binding | mitigate | `theme.no-hardcoded-classes.test.ts` (Phase 16.1-07) guard runs as part of green-gate (Task 3 explicit verify). All new color references go through `var(--color-breathing-*)` tokens. |
| T-18-05-03 | A11y regression (Spoofing accessibility tree) | A future change could drop `aria-checked` or `aria-labelledby` and degrade screen reader UX | mitigate | Task 2 tests assert `aria-checked` and `aria-labelledby` explicitly. Phase 17 D-24 a11y floor carry-forward. |
| T-18-05-04 | Information Disclosure | n/a — no PII rendered | accept | |
| T-18-05-05 | Repudiation | n/a — local-only | accept | |
| T-18-05-06 | Denial of Service | Rapidly clicking different timbre options could spam dispatch events | accept | The picker is `disabled` during sessions (TIMBRE-03 + Phase 15 D-08). Idle-state spam is bounded by user hand speed; no rate-limit needed (`setTimbre` is synchronous + no async work). |
| T-18-05-07 | Elevation of Privilege | n/a — no privilege boundaries in local client | accept | |
| T-18-05-08 | Supply chain | New npm dependency | mitigate | D-14 zero-deps; no new imports beyond intra-project (`useTimbreChoice`, `TIMBRE_OPTIONS`). |
</threat_model>

<verification>
- `npx tsc --noEmit && npm run lint && npm run build && npm test --run` exit 0.
- `theme.no-hardcoded-classes.test.ts` (THEME-UI-01 guard) explicitly verified green after Task 1.
- TimbrePicker.test.tsx covers: 4 labeled buttons; aria-checked matches stored timbre; click changes storage + dispatches event; disabled state gates both clicks and aria; aria-labelledby + 44×44 hit area present.
- Manual grep verifies: zero touches to SettingsDialog.tsx, useAudioCues.ts, App.tsx, audio/, storage/, domain/.
- The radiogroup is now functional in the SettingsDialog (user-facing — opening Settings → Timbre section shows the 4-button picker).
</verification>

<success_criteria>
- [ ] `src/components/TimbrePicker.tsx` renders a 4-option radiogroup (Bowl/Bell/Sine/Chime).
- [ ] Uses `useTimbreChoice` from Plan 02 for read + write.
- [ ] `loadPrefs` import removed; `useTimbreChoice` owns the read path.
- [ ] Capitalize formula `id.charAt(0).toUpperCase() + id.slice(1)` present (D-06 mirror).
- [ ] All color refs use `var(--color-breathing-*)` tokens — `theme.no-hardcoded-classes.test.ts` stays green (D-19).
- [ ] `role="radiogroup"` + `aria-labelledby` + `aria-disabled` on container; `role="radio"` + `aria-checked` + `disabled` on each button (D-20).
- [ ] `min-h-12` hit-area floor present (44×44 + Phase 17 D-24).
- [ ] `TimbrePickerProps` interface unchanged (`{ disabled: boolean }` only) — D-02/D-17.
- [ ] `TimbrePicker.test.tsx` covers: 4 labels, aria-checked, click dispatch, disabled gate, aria-labelledby, hit-area.
- [ ] `tsc && lint && build && test` exit 0 (D-13).
- [ ] No touches to SettingsDialog.tsx (D-16), useAudioCues.ts, App.tsx, audio/, storage/, domain/.
- [ ] Single commit: `feat(18-05): fill TimbrePicker radiogroup body (TIMBRE-01/04)`.
</success_criteria>

<output>
After completion, create `.planning/phases/18-audio-timbres/18-05-SUMMARY.md` capturing:
- Files modified: `src/components/TimbrePicker.tsx`, `src/components/TimbrePicker.test.tsx`.
- Final grid choice (grid-cols-2 vs grid-cols-4 — note if smoke-test-driven swap occurred).
- Test count delta.
- D-19 THEME-UI-01 guard green confirmation.
- Commit hash.
- Forward-decl: Plan 06 (App integration) is the last plan — wires the picker selection into `audio.start(plan, prefs.timbre)` at session start.
</output>
