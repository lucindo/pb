---
phase: 55-comment-de-archaeology
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/hooks/**/*.ts
  - src/hooks/**/*.tsx
autonomous: true
requirements: [COMMENT-01, COMMENT-02, TEST-01, BEHAVIOR-01, QUAL-01]

must_haves:
  truths:
    - "A maintainer reading any src/hooks file sees present-tense invariants, zero archaeology tags, and zero stale line-references."
    - "Every changed line in src/hooks is inside a comment region — no executable token (identifier, literal, operator, JSX) changed."
    - "Build, lint, and the full test suite remain green; package.json is byte-identical."
  artifacts:
    - path: "src/hooks/useAudioCues.ts"
      provides: "Densest de-archaeology target (150 hits); JSDoc D-10 block at :37-116 rephrased to present-tense invariants"
      contains: "// "
  key_links:
    - from: "src/hooks comment regions"
      to: "present-tense invariant prose (D-03 keep) or deletion (D-04/D-05 history/parity)"
      via: "keep-vs-cut decision tree from 55-CONTEXT.md"
      pattern: "comment-only diff"
---

<objective>
De-archaeologize all comments in `src/hooks/**` (~390 comment lines across 28 files — the densest judgment layer). Strip every archaeology taxonomy token, delete pure-history/parity/modeling comment lines, and rephrase keep-worthy comments into present-tense invariants. NO executable token changes.

Purpose: A maintainer opening any hook reads only what is true about the code now, not its development history (COMMENT-01), and never a stale `L###` pointer (COMMENT-02).
Output: Comment-only diffs across src/hooks; build/lint/test stay green.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/55-comment-de-archaeology/55-CONTEXT.md
@.planning/phases/55-comment-de-archaeology/55-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: De-archaeologize the dense hooks (useAudioCues.ts and other high-hit hooks)</name>
  <read_first>
    - src/hooks/useAudioCues.ts (and every other file under src/hooks/ this task edits)
    - .planning/phases/55-comment-de-archaeology/55-CONTEXT.md (D-01..D-09 decision tree — read first)
    - .planning/phases/55-comment-de-archaeology/55-RESEARCH.md (per-site landmine notes for src/hooks, keep-vs-cut tree, rephrasing examples, grep patterns)
  </read_first>
  <action>
    Edit comments only. For every comment in the hooks files (start with the densest: useAudioCues.ts at 150 hits), apply the 55-CONTEXT keep-vs-cut decision tree:
    - STRIP every taxonomy token: `D-xx`, `WR-xx`/`DS-WR-xx`, `Phase NN`, `Plan NN`, `Blocker #N`, `Pitfall N`, `spike NNN`, dated kitchen-sink references (COMMENT-01).
    - STRIP every line-reference archaeology: all `L###`, `formerly at ...`, `mirror L###`. Treat ALL `L###` as presumptively stale — delete the reference, NEVER "fix" the number (COMMENT-02).
    - KEEP-and-rephrase (D-03): a comment that states a real invariant becomes present-tense prose with the archaeology stripped.
    - DELETE the whole comment line (D-04/D-05) when it is pure history / parity-with-old-code / data-modeling narration.
    Named landmines in this layer (see RESEARCH for full detail): useAudioCues.ts:431 trailing `setAudioStatus('unavailable')` comment; :267 the lying `L213-222` reference; the JSDoc D-10 density at :37-116 (rephrase the block to present-tense invariants).
    Do NOT change any executable token (identifier, literal, operator, JSX). Do NOT delete or alter code. Do NOT create new files. Do NOT paste implementations — consult RESEARCH for per-site detail.
  </action>
  <verify>
    <automated>git grep -nE '\b(D-[0-9]+|(DS-)?WR-[0-9]+|Phase [0-9]+|Plan [0-9]+|Blocker|Pitfall|spike[ -]?[0-9]+|kitchen.?sink)' -- 'src/hooks/**' | grep -v '^Binary' ; test -z "$(git grep -nE '\b(D-[0-9]+|(DS-)?WR-[0-9]+|Phase [0-9]+|Plan [0-9]+|Blocker|Pitfall|spike[ -]?[0-9]+|kitchen.?sink)' -- 'src/hooks/**')"</automated>
  </verify>
  <acceptance_criteria>
    - `git grep -nE '\b(D-[0-9]+|(DS-)?WR-[0-9]+|Phase [0-9]+|Plan [0-9]+|Blocker|Pitfall|spike[ -]?[0-9]+|kitchen.?sink)' -- 'src/hooks/**'` returns empty (COMMENT-01).
    - `git grep -nE '(\bL[0-9]{2,}|formerly at|mirror .*L[0-9])' -- 'src/hooks/**'` returns empty (COMMENT-02).
    - For every changed file, `git diff <file>` shows every `+`/`-` hunk inside a comment region — no executable token changed (BEHAVIOR-01 / D-09).
  </acceptance_criteria>
  <done>Hooks comment regions carry present-tense invariants only; both grep gates empty; all diffs comment-only.</done>
</task>

<task type="auto">
  <name>Task 2: Green gate — build, lint, test, package.json unchanged</name>
  <read_first>
    - .planning/phases/55-comment-de-archaeology/55-VALIDATION.md (comment-only-diff invariant + green gate)
    - package.json (scripts + dependencies)
  </read_first>
  <action>
    Run the green gate over the changes from Task 1. If any check fails, the only legal fix is to a comment region — investigate whether a comment edit accidentally crossed into executable code and revert that token. Confirm `dependencies` stays exactly `react` + `react-dom` (QUAL-01).
  </action>
  <verify>
    <automated>npm run build && npm run lint && npm run test:run && test -z "$(git diff package.json)"</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` exits 0; `npm run lint` exits 0; `npm run test:run` passes (BEHAVIOR-01 / QUAL-01).
    - `git diff package.json` is empty; `dependencies` is still `react` + `react-dom` (QUAL-01).
  </acceptance_criteria>
  <done>All three gates green and package.json byte-identical.</done>
</task>

</tasks>

<threat_model>
No new threat surface — comment-only text edits; no executable token changes. One adjacent care: any comment documenting a security-relevant invariant (e.g. "no raw stack to user-facing surfaces") is KEPT and rephrased per D-03, never deleted.

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-55-01 | Tampering | comment edit crossing into executable token | mitigate | comment-only-diff invariant + green gate (Task 2) catches any token change |
</threat_model>

<verification>
- Both grep gates (COMMENT-01, COMMENT-02) empty over `src/hooks/**`.
- Every diff hunk inside a comment region (BEHAVIOR-01 / D-09).
- Green gate passes; package.json unchanged (QUAL-01).
</verification>

<success_criteria>
src/hooks comments read as present-tense invariants with zero archaeology tags and zero stale line-refs; behavior and dependencies unchanged.
</success_criteria>

<output>
Create `.planning/phases/55-comment-de-archaeology/55-01-SUMMARY.md` when done.
</output>
