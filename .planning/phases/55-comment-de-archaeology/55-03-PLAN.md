---
phase: 55-comment-de-archaeology
plan: 03
type: execute
wave: 3
depends_on: []
files_modified:
  - src/domain/**/*.ts
  - src/domain/**/*.tsx
autonomous: true
requirements: [COMMENT-01, COMMENT-02, TEST-01, BEHAVIOR-01, QUAL-01]

must_haves:
  truths:
    - "A maintainer reading any src/domain file sees present-tense invariants, zero archaeology tags, zero stale line-refs."
    - "Every changed line in src/domain is inside a comment region — no executable token changed."
    - "Build, lint, and the full test suite remain green; package.json is byte-identical."
  artifacts:
    - path: "src/domain"
      provides: "Domain comments de-archaeologized (~56 lines / 10 files)"
      contains: "// "
  key_links:
    - from: "src/domain comment regions"
      to: "present-tense invariant prose (D-03) or deletion (D-04/D-05)"
      via: "keep-vs-cut decision tree from 55-CONTEXT.md"
      pattern: "comment-only diff"
---

<objective>
De-archaeologize all comments in `src/domain/**` (~56 comment lines across 10 files). Strip taxonomy tokens and stale line-refs; keep-rephrase real invariants; delete pure history/parity/modeling lines. NO executable token changes.

Purpose: COMMENT-01 + COMMENT-02 over the domain layer.
Output: Comment-only diffs across src/domain; build/lint/test green.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/55-comment-de-archaeology/55-CONTEXT.md
@.planning/phases/55-comment-de-archaeology/55-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: De-archaeologize src/domain comments</name>
  <read_first>
    - every file under src/domain/ this task edits
    - .planning/phases/55-comment-de-archaeology/55-CONTEXT.md (D-01..D-09 decision tree — read first)
    - .planning/phases/55-comment-de-archaeology/55-RESEARCH.md (per-site notes for src/domain, keep-vs-cut tree, rephrasing examples, grep patterns)
  </read_first>
  <action>
    Edit comments only. Apply the 55-CONTEXT keep-vs-cut tree:
    - STRIP taxonomy tokens: `D-xx`, `WR-xx`/`DS-WR-xx`, `Phase NN`, `Plan NN`, `Blocker #N`, `Pitfall N`, `spike NNN`, dated kitchen-sink (COMMENT-01).
    - STRIP `L###`, `formerly at`, `mirror L###` — all `L###` presumptively stale, delete never re-number (COMMENT-02).
    - KEEP-rephrase (D-03) real invariants to present tense; DELETE (D-04/D-05) pure history/parity/modeling lines.
    No executable-token changes. No new files. Consult RESEARCH for per-site detail.
  </action>
  <verify>
    <automated>test -z "$(git grep -nE '\b(D-[0-9]+|(DS-)?WR-[0-9]+|Phase [0-9]+|Plan [0-9]+|Blocker|Pitfall|spike[ -]?[0-9]+|kitchen.?sink)' -- 'src/domain/**')" && test -z "$(git grep -nE '(\bL[0-9]{2,}|formerly at|mirror .*L[0-9])' -- 'src/domain/**')"</automated>
  </verify>
  <acceptance_criteria>
    - taxonomy grep over `src/domain/**` empty (COMMENT-01).
    - line-ref grep over `src/domain/**` empty (COMMENT-02).
    - every `git diff <file>` hunk inside a comment region (BEHAVIOR-01 / D-09).
  </acceptance_criteria>
  <done>src/domain comments present-tense; both grep gates empty; diffs comment-only.</done>
</task>

<task type="auto">
  <name>Task 2: Green gate — build, lint, test, package.json unchanged</name>
  <read_first>
    - .planning/phases/55-comment-de-archaeology/55-VALIDATION.md (green gate)
    - package.json
  </read_first>
  <action>Run the green gate over Task 1's changes; fix only by reverting accidental token changes. Confirm `dependencies` = `react` + `react-dom`.</action>
  <verify>
    <automated>npm run build && npm run lint && npm run test:run && test -z "$(git diff package.json)"</automated>
  </verify>
  <acceptance_criteria>
    - build/lint exit 0; test:run passes (BEHAVIOR-01 / QUAL-01).
    - `git diff package.json` empty (QUAL-01).
  </acceptance_criteria>
  <done>All gates green; package.json byte-identical.</done>
</task>

</tasks>

<threat_model>
No new threat surface — comment-only text edits; no executable token changes. A comment documenting a security-relevant invariant is KEPT/rephrased per D-03, never deleted.

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-55-03 | Tampering | comment edit crossing into executable token | mitigate | comment-only-diff invariant + green gate (Task 2) |
</threat_model>

<verification>
- Both grep gates empty over `src/domain/**`; diffs comment-only; green gate passes.
</verification>

<success_criteria>
src/domain comments read present-tense with zero archaeology and zero stale line-refs; behavior unchanged.
</success_criteria>

<output>
Create `.planning/phases/55-comment-de-archaeology/55-03-SUMMARY.md` when done.
</output>
