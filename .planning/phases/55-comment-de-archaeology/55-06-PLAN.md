---
phase: 55-comment-de-archaeology
plan: 06
type: execute
wave: 6
depends_on: []
files_modified:
  - "src/app/**/*.ts"
  - "src/app/**/*.tsx"
  - "src/content/**/*.ts"
  - "src/content/**/*.tsx"
  - "src/styles/**/*.ts"
  - "src/styles/**/*.tsx"
  - "src/main.tsx"
  - "src/featureFlags.ts"
  - "src/vite-env.d.ts"
  - "vite.config.ts"
  - "vitest.setup.ts"
autonomous: true
requirements: [COMMENT-01, COMMENT-02, TEST-01, BEHAVIOR-01, QUAL-01]

# Surface boundary (disjointness): this plan owns ONLY src/app, src/content,
# src/styles, the genuine src depth-1 files (main.tsx, featureFlags.ts,
# vite-env.d.ts), and the two root-level TS config/infra files (vite.config.ts,
# vitest.setup.ts). It does NOT own anything under src/hooks (plan 01),
# src/audio (02), src/domain (03), src/storage (04), src/components (05), or any
# *.test.* file (plans 07/08). Every pathspec is root-anchored — NO bare *.ts /
# *.tsx globs (those match at any depth and would overlap plans 01-05/07/08).
# Out of scope (CONSCIOUS): eslint.config.js — a .js root config whose only tag
# (BUILD-03 at :29) is NOT in the COMMENT-01 stripped taxonomy. Not owned by any
# Phase 55 plan; left untouched.

must_haves:
  truths:
    - "A maintainer reading any src/app, src/content, src/styles, the src depth-1 files (main.tsx, featureFlags.ts, vite-env.d.ts), or the two root TS infra files (vite.config.ts, vitest.setup.ts) sees present-tense invariants, zero archaeology tags, zero stale line-refs."
    - "The 12 `// TODO: native-speaker review` markers in src/content/strings.ts are UNTOUCHED (I18N-04 out of scope)."
    - "D-09: every changed line is comment-only — no token change to executable code/types/values; trailing-comment values (e.g. the two `configurable: true, // …` lines in vitest.setup.ts) left byte-identical, only the comment after `//` edited."
    - "D-06: no new doc or history file is created (no docs/audio-architecture.md or equivalent)."
    - "Build, lint, and the full test suite remain green; package.json is byte-identical."
  artifacts:
    - path: "src/content/strings.ts"
      provides: "Strings comments de-archaeologized while preserving the 12 native-speaker-review TODO markers"
      contains: "// TODO: native-speaker review"
    - path: "vitest.setup.ts"
      provides: "Test-infra setup comments de-archaeologized (Source:/Plan NN/D-NN block + trailing landmines); D-01 applies, D-02 preserves test behavior"
      contains: "configurable: true"
  key_links:
    - from: "src/app/content/styles + src depth-1 + root TS infra comment regions"
      to: "present-tense invariant prose (D-03) or deletion (D-04/D-05)"
      via: "keep-vs-cut decision tree from 55-CONTEXT.md, skipping I18N-04 markers"
      pattern: "comment-only diff"
---

<objective>
De-archaeologize comments in `src/app/**`, `src/content/**`, `src/styles/**`, the genuine `src` depth-1 files (`src/main.tsx`, `src/featureFlags.ts`, `src/vite-env.d.ts`), and the two root-level TS infra files (`vite.config.ts`, `vitest.setup.ts`) — standard strip. EXCEPTION: do NOT touch the 12 `// TODO: native-speaker review` markers in `src/content/strings.ts` (I18N-04, out of scope). NO executable token changes (D-09).

This plan's surface is DISJOINT from plans 01-05 (the per-layer source sweeps) and plans 07/08 (the test-file sweeps): every pathspec is root-anchored — no bare `*.ts`/`*.tsx`/`*.config.ts` glob — so it never reaches `src/hooks|audio|domain|storage|components` or any `*.test.*` file. `eslint.config.js` is consciously out of scope (see frontmatter note: `.js` root config, its only tag `BUILD-03` is not in the COMMENT-01 stripped taxonomy).

Purpose: COMMENT-01 + COMMENT-02 over the remaining non-test source + the two root TS infra files, while leaving I18N-04 markers intact.
Output: Comment-only diffs; build/lint/test green; I18N markers preserved.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/55-comment-de-archaeology/55-CONTEXT.md
@.planning/phases/55-comment-de-archaeology/55-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: De-archaeologize src/app, src/content, src/styles, the src depth-1 files, and the two root TS infra files (skip I18N-04 markers)</name>
  <read_first>
    - every file under src/app/, src/content/, src/styles/, plus src/main.tsx, src/featureFlags.ts, src/vite-env.d.ts, vite.config.ts, and vitest.setup.ts (this task's exact surface)
    - .planning/phases/55-comment-de-archaeology/55-CONTEXT.md (D-01..D-09 decision tree — read first)
    - .planning/phases/55-comment-de-archaeology/55-RESEARCH.md (per-site notes for these dirs, the I18N-04 skip note, keep-vs-cut tree, grep patterns)
  </read_first>
  <action>
    Edit comments only. Apply the 55-CONTEXT keep-vs-cut tree across the EXACT surface listed in <read_first>:
    - STRIP taxonomy tokens: `D-xx`, `WR-xx`/`DS-WR-xx`, `Phase NN`, `Plan NN`, `Blocker #N`, `Pitfall N`, `spike NNN`, dated kitchen-sink (COMMENT-01).
    - STRIP `L###`, `formerly at`, `mirror L###` — all `L###` presumptively stale, delete never re-number (COMMENT-02).
    - KEEP-rephrase (D-03) real invariants; DELETE (D-04/D-05) pure history/parity/modeling lines.
    - HARD SKIP: leave the 12 `// TODO: native-speaker review` markers in `src/content/strings.ts` exactly as-is (I18N-04 out of scope). Do not strip, reword, or renumber them.
    - vitest.setup.ts is test-INFRA config (NOT a test): apply D-01 (strip tags from its comments). D-02 still binds — do NOT change any fake/polyfill behavior; editing the comments in this setup file is fine. Named landmines verified live: the `// Source: NN-RESEARCH.md …` block-comment headers (~:30/:88/:119/:139), the `// Plan 06 D-37/D-40`, `// Plan 01 D-14 (preserved)`, `// Plan 06 D-40 additions`, `// AUDIO-04 D-14 …` block comments, and TWO trailing-comment landmines on `configurable: true,` lines (~:274 AudioContext install and ~:309 wakeLock install). On those two lines edit ONLY the text after `//`; the `configurable: true,` token must stay byte-identical (D-09).
    - D-06: do NOT create any new doc or history file.
    No executable-token changes (D-09). No new files (D-06). Consult RESEARCH for per-site detail.
  </action>
  <verify>
    <automated>test -z "$(git grep -nE '\b(D-[0-9]+|(DS-)?WR-[0-9]+|Phase [0-9]+|Plan [0-9]+|Blocker|Pitfall|spike[ -]?[0-9]+|kitchen.?sink)' -- ':(glob)src/app/**' ':(glob)src/content/**' ':(glob)src/styles/**' src/main.tsx src/featureFlags.ts src/vite-env.d.ts vite.config.ts vitest.setup.ts ':(exclude)src/**/*.test.ts' ':(exclude)src/**/*.test.tsx')" && test -z "$(git grep -nE '(\bL[0-9]{2,}|formerly at|mirror .*L[0-9])' -- ':(glob)src/app/**' ':(glob)src/content/**' ':(glob)src/styles/**' src/main.tsx src/featureFlags.ts src/vite-env.d.ts vite.config.ts vitest.setup.ts ':(exclude)src/**/*.test.ts' ':(exclude)src/**/*.test.tsx')" && test "$(git grep -c 'TODO: native-speaker review' -- src/content/strings.ts | cut -d: -f2)" = "$(git show HEAD:src/content/strings.ts | grep -c 'TODO: native-speaker review')"</automated>
  </verify>
  <acceptance_criteria>
    - taxonomy grep over this plan's exact surface (root-anchored pathspecs + the named root/src files, excluding any *.test.* that the glob might reach) empty (COMMENT-01).
    - line-ref grep over the same surface empty (COMMENT-02).
    - I18N marker count in src/content/strings.ts is UNCHANGED vs HEAD (the gate compares working tree to `HEAD:src/content/strings.ts` — currently 12; a future legitimate drift in the source count won't false-fail, only an accidental edit will).
    - vitest.setup.ts: the two `configurable: true,` tokens (~:274/:309) byte-identical to HEAD; only their trailing comments changed.
    - every `git diff <file>` hunk inside a comment region (BEHAVIOR-01 / D-09).
  </acceptance_criteria>
  <done>app/content/styles + the named root/src files have present-tense comments; both grep gates empty over this disjoint surface; I18N marker count unchanged vs HEAD; vitest.setup.ts code tokens intact; diffs comment-only.</done>
</task>

<task type="auto">
  <name>Task 2: Green gate — build, lint, test, package.json unchanged</name>
  <read_first>
    - .planning/phases/55-comment-de-archaeology/55-VALIDATION.md (green gate)
    - package.json
  </read_first>
  <action>Run the green gate; fix only by reverting accidental token changes. Confirm `dependencies` = `react` + `react-dom`.</action>
  <verify>
    <automated>npm run build && npm run lint && npm run test:run && test -z "$(git diff package.json)"</automated>
  </verify>
  <acceptance_criteria>
    - build/lint exit 0; test:run passes (BEHAVIOR-01 / QUAL-01).
    - `git diff package.json` empty (QUAL-01).
  </acceptance_criteria>
  <done>All gates green; package.json byte-identical.</done>
</task>

</tasks>

<threat_model>
No new threat surface — comment-only text edits; no executable token changes. A comment documenting a security-relevant invariant is KEPT/rephrased per D-03, never deleted. vitest.setup.ts carries trailing-comment landmines on `configurable: true` lines — edit the comment, never the value (D-09).

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-55-06 | Tampering | comment edit crossing into executable token (esp. vitest.setup.ts `configurable: true` trailing comments) / stripping an I18N-04 marker | mitigate | comment-only-diff invariant + green gate (Task 2); I18N marker-count compared to HEAD keeps the markers intact; D-09 byte-identical check on the two `configurable: true` tokens |
</threat_model>

<verification>
- Both grep gates empty over the root-anchored, test-excluded surface; I18N marker count unchanged vs HEAD; vitest.setup.ts code tokens intact; diffs comment-only; green gate passes.
</verification>

<success_criteria>
app/content/styles + the named root/src files read present-tense with zero archaeology and zero stale line-refs; I18N-04 markers preserved; vitest.setup.ts behavior unchanged; eslint.config.js consciously excluded.
</success_criteria>

<output>
Create `.planning/phases/55-comment-de-archaeology/55-06-SUMMARY.md` when done.
</output>
