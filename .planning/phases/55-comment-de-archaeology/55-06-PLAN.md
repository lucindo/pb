---
phase: 55-comment-de-archaeology
plan: 06
type: execute
wave: 6
depends_on: []
files_modified:
  - src/app/**/*.ts
  - src/app/**/*.tsx
  - src/content/**/*.ts
  - src/content/**/*.tsx
  - src/styles/**/*.ts
  - src/styles/**/*.tsx
  - "*.ts"
  - "*.tsx"
  - "*.config.ts"
autonomous: true
requirements: [COMMENT-01, COMMENT-02, TEST-01, BEHAVIOR-01, QUAL-01]

must_haves:
  truths:
    - "A maintainer reading any src/app, src/content, src/styles, or root config file sees present-tense invariants, zero archaeology tags, zero stale line-refs."
    - "The 12 `// TODO: native-speaker review` markers in src/content/strings.ts are UNTOUCHED (I18N-04 out of scope)."
    - "Every changed line is inside a comment region — no executable token changed."
    - "Build, lint, and the full test suite remain green; package.json is byte-identical."
  artifacts:
    - path: "src/content/strings.ts"
      provides: "Strings comments de-archaeologized while preserving the 12 native-speaker-review TODO markers"
      contains: "// TODO: native-speaker review"
  key_links:
    - from: "src/app/content/styles + root config comment regions"
      to: "present-tense invariant prose (D-03) or deletion (D-04/D-05)"
      via: "keep-vs-cut decision tree from 55-CONTEXT.md, skipping I18N-04 markers"
      pattern: "comment-only diff"
---

<objective>
De-archaeologize all comments in `src/app/**`, `src/content/**`, `src/styles/**`, and root-level TS/config files (~39 comment lines — standard strip). EXCEPTION: do NOT touch the 12 `// TODO: native-speaker review` markers in `src/content/strings.ts` (I18N-04, out of scope). NO executable token changes.

Purpose: COMMENT-01 + COMMENT-02 over the remaining non-test source, while leaving I18N-04 markers intact.
Output: Comment-only diffs; build/lint/test green; I18N markers preserved.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/55-comment-de-archaeology/55-CONTEXT.md
@.planning/phases/55-comment-de-archaeology/55-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: De-archaeologize src/app, src/content, src/styles, and root config comments (skip I18N-04 markers)</name>
  <read_first>
    - every file under src/app/, src/content/, src/styles/, and the root TS/config files this task edits
    - .planning/phases/55-comment-de-archaeology/55-CONTEXT.md (D-01..D-09 decision tree — read first)
    - .planning/phases/55-comment-de-archaeology/55-RESEARCH.md (per-site notes for these dirs, the I18N-04 skip note, keep-vs-cut tree, grep patterns)
  </read_first>
  <action>
    Edit comments only. Apply the 55-CONTEXT keep-vs-cut tree:
    - STRIP taxonomy tokens: `D-xx`, `WR-xx`/`DS-WR-xx`, `Phase NN`, `Plan NN`, `Blocker #N`, `Pitfall N`, `spike NNN`, dated kitchen-sink (COMMENT-01).
    - STRIP `L###`, `formerly at`, `mirror L###` — all `L###` presumptively stale, delete never re-number (COMMENT-02).
    - KEEP-rephrase (D-03) real invariants; DELETE (D-04/D-05) pure history/parity/modeling lines.
    - HARD SKIP: leave the 12 `// TODO: native-speaker review` markers in `src/content/strings.ts` exactly as-is (I18N-04 out of scope). Do not strip, reword, or renumber them.
    No executable-token changes. No new files. Consult RESEARCH for per-site detail.
  </action>
  <verify>
    <automated>test -z "$(git grep -nE '\b(D-[0-9]+|(DS-)?WR-[0-9]+|Phase [0-9]+|Plan [0-9]+|Blocker|Pitfall|spike[ -]?[0-9]+|kitchen.?sink)' -- 'src/app/**' 'src/content/**' 'src/styles/**' '*.ts' '*.tsx' '*.config.ts')" && test -z "$(git grep -nE '(\bL[0-9]{2,}|formerly at|mirror .*L[0-9])' -- 'src/app/**' 'src/content/**' 'src/styles/**' '*.ts' '*.tsx' '*.config.ts')" && test "$(git grep -c 'TODO: native-speaker review' -- src/content/strings.ts | cut -d: -f2)" = "12"</automated>
  </verify>
  <acceptance_criteria>
    - taxonomy grep over the four globs empty (COMMENT-01).
    - line-ref grep over the four globs empty (COMMENT-02).
    - `git grep -c 'TODO: native-speaker review' -- src/content/strings.ts` still reports 12 (I18N-04 markers untouched).
    - every `git diff <file>` hunk inside a comment region (BEHAVIOR-01 / D-09).
  </acceptance_criteria>
  <done>app/content/styles/root comments present-tense; both grep gates empty; 12 I18N markers preserved; diffs comment-only.</done>
</task>

<task type="auto">
  <name>Task 2: Green gate — build, lint, test, package.json unchanged</name>
  <read_first>
    - .planning/phases/55-comment-de-archaeology/55-VALIDATION.md (green gate)
    - package.json
  </read_first>
  <action>Run the green gate; fix only by reverting accidental token changes. Confirm `dependencies` = `react` + `react-dom`.</action>
  <verify>
    <automated>npm run build && npm run lint && npm run test:run && test -z "$(git diff package.json)"</automated>
  </verify>
  <acceptance_criteria>
    - build/lint exit 0; test:run passes (BEHAVIOR-01 / QUAL-01).
    - `git diff package.json` empty (QUAL-01).
  </acceptance_criteria>
  <done>All gates green; package.json byte-identical.</done>
</task>

</tasks>

<threat_model>
No new threat surface — comment-only text edits; no executable token changes. A comment documenting a security-relevant invariant is KEPT/rephrased per D-03, never deleted.

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-55-06 | Tampering | comment edit crossing into executable token / stripping an I18N-04 marker | mitigate | comment-only-diff invariant + green gate (Task 2); marker-count gate keeps the 12 I18N TODOs intact |
</threat_model>

<verification>
- Both grep gates empty over the four globs; 12 I18N markers intact; diffs comment-only; green gate passes.
</verification>

<success_criteria>
app/content/styles/root comments read present-tense with zero archaeology and zero stale line-refs; I18N-04 markers preserved; behavior unchanged.
</success_criteria>

<output>
Create `.planning/phases/55-comment-de-archaeology/55-06-SUMMARY.md` when done.
</output>
