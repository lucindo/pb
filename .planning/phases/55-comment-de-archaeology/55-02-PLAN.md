---
phase: 55-comment-de-archaeology
plan: 02
type: execute
wave: 2
depends_on: []
files_modified:
  - src/audio/**/*.ts
  - src/audio/**/*.tsx
autonomous: true
requirements: [COMMENT-01, COMMENT-02, TEST-01, BEHAVIOR-01, QUAL-01]

must_haves:
  truths:
    - "A maintainer reading any src/audio file sees present-tense invariants (iOS gesture-token, TOCTOU, silent-WAV) with all archaeology tags stripped."
    - "Every changed line in src/audio is inside a comment region — no executable token changed."
    - "Build, lint, and the full test suite remain green; package.json is byte-identical."
  artifacts:
    - path: "src/audio"
      provides: "Highest per-line judgment layer; long invariant essays KEEP-rephrased per D-03"
      contains: "// "
  key_links:
    - from: "src/audio invariant essays (iOS gesture-token, TOCTOU, silent-WAV)"
      to: "present-tense invariant prose with archaeology stripped (D-03)"
      via: "keep-vs-cut decision tree from 55-CONTEXT.md"
      pattern: "comment-only diff"
---

<objective>
De-archaeologize all comments in `src/audio/**` (~265 comment lines across 8 files — highest per-line judgment density). The iOS gesture-token, TOCTOU, and silent-WAV essays are real invariants: KEEP and rephrase to present tense per D-03; strip every archaeology token and stale line-ref. NO executable token changes.

Purpose: COMMENT-01 (zero tags) and COMMENT-02 (zero stale refs) while preserving the load-bearing audio invariants as present-tense prose.
Output: Comment-only diffs across src/audio; build/lint/test stay green.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/55-comment-de-archaeology/55-CONTEXT.md
@.planning/phases/55-comment-de-archaeology/55-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: De-archaeologize src/audio comments (preserve invariant essays as present-tense prose)</name>
  <read_first>
    - every file under src/audio/ this task edits
    - .planning/phases/55-comment-de-archaeology/55-CONTEXT.md (D-01..D-09 decision tree — read first; D-03 keep-rephrase is the dominant rule here)
    - .planning/phases/55-comment-de-archaeology/55-RESEARCH.md (per-site landmine notes for src/audio: iOS gesture-token / TOCTOU / silent-WAV essays, keep-vs-cut tree, rephrasing examples, grep patterns)
  </read_first>
  <action>
    Edit comments only. Apply the 55-CONTEXT keep-vs-cut tree:
    - STRIP every taxonomy token: `D-xx`, `WR-xx`/`DS-WR-xx`, `Phase NN`, `Plan NN`, `Blocker #N`, `Pitfall N`, `spike NNN`, dated kitchen-sink references (COMMENT-01).
    - STRIP every `L###`, `formerly at`, `mirror L###`. ALL `L###` are presumptively stale — delete, never re-number (COMMENT-02).
    - KEEP-and-rephrase (D-03): the iOS gesture-token essay, the TOCTOU note, and the silent-WAV unlock essay are genuine invariants — rewrite them present-tense, archaeology removed, content preserved.
    - DELETE (D-04/D-05) any pure-history / parity / modeling comment line.
    A comment documenting a security-relevant invariant (e.g. "no raw stack to user-facing surfaces") is KEPT/rephrased, never deleted.
    Do NOT change any executable token. Do NOT create new files (D-06 — no docs/audio-architecture.md). Consult RESEARCH for per-site detail; do not paste implementations.
  </action>
  <verify>
    <automated>test -z "$(git grep -nE '\b(D-[0-9]+|(DS-)?WR-[0-9]+|Phase [0-9]+|Plan [0-9]+|Blocker|Pitfall|spike[ -]?[0-9]+|kitchen.?sink)' -- 'src/audio/**')" && test -z "$(git grep -nE '(\bL[0-9]{2,}|formerly at|mirror .*L[0-9])' -- 'src/audio/**')"</automated>
  </verify>
  <acceptance_criteria>
    - `git grep -nE '\b(D-[0-9]+|(DS-)?WR-[0-9]+|Phase [0-9]+|Plan [0-9]+|Blocker|Pitfall|spike[ -]?[0-9]+|kitchen.?sink)' -- 'src/audio/**'` returns empty (COMMENT-01).
    - `git grep -nE '(\bL[0-9]{2,}|formerly at|mirror .*L[0-9])' -- 'src/audio/**'` returns empty (COMMENT-02).
    - The iOS gesture-token, TOCTOU, and silent-WAV invariants still appear (as present-tense prose) — no invariant content lost.
    - For every changed file, `git diff <file>` shows every hunk inside a comment region (BEHAVIOR-01 / D-09).
  </acceptance_criteria>
  <done>src/audio invariants preserved as present-tense prose; both grep gates empty; all diffs comment-only; no new files.</done>
</task>

<task type="auto">
  <name>Task 2: Green gate — build, lint, test, package.json unchanged</name>
  <read_first>
    - .planning/phases/55-comment-de-archaeology/55-VALIDATION.md (comment-only-diff invariant + green gate)
    - package.json (scripts + dependencies)
  </read_first>
  <action>
    Run the green gate over Task 1's changes. Any failure is fixed only by reverting an accidental executable-token change inside a comment edit. Confirm `dependencies` stays exactly `react` + `react-dom`.
  </action>
  <verify>
    <automated>npm run build && npm run lint && npm run test:run && test -z "$(git diff package.json)"</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` / `npm run lint` exit 0; `npm run test:run` passes (BEHAVIOR-01 / QUAL-01).
    - `git diff package.json` empty (QUAL-01).
  </acceptance_criteria>
  <done>All gates green and package.json byte-identical.</done>
</task>

</tasks>

<threat_model>
No new threat surface — comment-only text edits; no executable token changes. Adjacent care: src/audio carries security-relevant invariants (gesture-token gating, no raw stack to user surfaces) — these are KEPT and rephrased per D-03, never deleted.

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-55-02 | Information disclosure | dropping a security invariant comment | mitigate | D-03 keep-rephrase rule preserves the invariant as present-tense prose; reviewer confirms presence |
</threat_model>

<verification>
- Both grep gates empty over `src/audio/**`.
- Invariant essays preserved as present-tense prose.
- Green gate passes; package.json unchanged.
</verification>

<success_criteria>
src/audio invariants read present-tense with zero archaeology and zero stale line-refs; behavior and dependencies unchanged.
</success_criteria>

<output>
Create `.planning/phases/55-comment-de-archaeology/55-02-SUMMARY.md` when done.
</output>
