---
phase: 55-comment-de-archaeology
plan: 05
type: execute
wave: 5
depends_on: []
files_modified:
  - src/components/**/*.ts
  - src/components/**/*.tsx
autonomous: true
requirements: [COMMENT-01, COMMENT-02, TEST-01, BEHAVIOR-01, QUAL-01]

must_haves:
  truths:
    - "A maintainer reading any src/components file sees present-tense invariants, zero archaeology tags, zero stale line-refs."
    - "Spike-geometry comments keep their geometry VALUE (D-08) and a what-it-controls note when non-obvious (D-07); the L###/spike/index.html provenance pointer is stripped."
    - "Every changed line in src/components is inside a comment region — no executable token changed."
    - "Build, lint, and the full test suite remain green; package.json is byte-identical."
  artifacts:
    - path: "src/components"
      provides: "Components comments de-archaeologized (~87 lines / 55 files); spike-geometry provenance handled in 26 files per RESEARCH Pitfall 5"
      contains: "// "
  key_links:
    - from: "spike-geometry comments in the 26 Pitfall-5 files"
      to: "geometry value preserved (D-08) + what-it-controls note if non-obvious (D-07), provenance pointer stripped"
      via: "55-RESEARCH Pitfall 5 file list + 55-CONTEXT D-07/D-08"
      pattern: "comment-only diff, value retained"
---

<objective>
De-archaeologize all comments in `src/components/**` (~87 comment lines across 55 files). The load-bearing subtlety: 26 files (RESEARCH Pitfall 5) carry spike-geometry provenance — STRIP the `L###` + `spike`/`index.html` pointer but KEEP the geometry value (D-08) and keep a what-it-controls note only when non-obvious (D-07). NO executable token changes.

Purpose: COMMENT-01 + COMMENT-02 over components without losing spike-locked geometry values.
Output: Comment-only diffs across src/components; build/lint/test green.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/55-comment-de-archaeology/55-CONTEXT.md
@.planning/phases/55-comment-de-archaeology/55-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: De-archaeologize src/components comments, handling spike-geometry provenance (D-07/D-08)</name>
  <read_first>
    - every file under src/components/ this task edits
    - .planning/phases/55-comment-de-archaeology/55-CONTEXT.md (D-01..D-09 decision tree — read first; D-07/D-08 spike-geometry rules are critical here)
    - .planning/phases/55-comment-de-archaeology/55-RESEARCH.md (Pitfall 5 — the full 26-file spike-geometry list, per-site notes, keep-vs-cut tree, grep patterns)
  </read_first>
  <action>
    Edit comments only. Apply the 55-CONTEXT keep-vs-cut tree across all 55 component files:
    - STRIP taxonomy tokens: `D-xx`, `WR-xx`/`DS-WR-xx`, `Phase NN`, `Plan NN`, `Blocker #N`, `Pitfall N`, `spike NNN`, dated kitchen-sink (COMMENT-01).
    - STRIP `L###`, `formerly at`, `mirror L###` — all `L###` presumptively stale, delete never re-number (COMMENT-02).
    - SPIKE-GEOMETRY (D-08/D-07): in each of the 26 Pitfall-5 files, strip the `L###` + `spike`/`index.html` provenance pointer but KEEP the geometry value verbatim; keep a brief what-it-controls note ONLY when the value's purpose is non-obvious (D-07) — otherwise leave just the value.
    - KEEP-rephrase (D-03) other genuine invariants; DELETE (D-04/D-05) pure history/parity/modeling lines.
    No executable-token changes (geometry values live in code, not comments — do not touch them in code regardless). No new files. Consult RESEARCH Pitfall 5 for the exact 26-file list and per-site detail.
  </action>
  <verify>
    <automated>test -z "$(git grep -nE '\b(D-[0-9]+|(DS-)?WR-[0-9]+|Phase [0-9]+|Plan [0-9]+|Blocker|Pitfall|spike[ -]?[0-9]+|kitchen.?sink)' -- 'src/components/**')" && test -z "$(git grep -nE '(\bL[0-9]{2,}|formerly at|mirror .*L[0-9])' -- 'src/components/**')" && test -z "$(git grep -niE '(spike|index\.html)' -- 'src/components/**')"</automated>
  </verify>
  <acceptance_criteria>
    - taxonomy grep over `src/components/**` empty (COMMENT-01).
    - line-ref grep over `src/components/**` empty (COMMENT-02).
    - `git grep -niE '(spike|index\.html)' -- 'src/components/**'` empty (provenance pointers stripped).
    - spike geometry VALUES still present in the 26 Pitfall-5 files (D-08) — no value deleted; spot-check against RESEARCH Pitfall 5 list.
    - every `git diff <file>` hunk inside a comment region (BEHAVIOR-01 / D-09).
  </acceptance_criteria>
  <done>Components comments present-tense; spike-geometry values preserved with provenance stripped; all grep gates empty; diffs comment-only.</done>
</task>

<task type="auto">
  <name>Task 2: Green gate — build, lint, test, package.json unchanged</name>
  <read_first>
    - .planning/phases/55-comment-de-archaeology/55-VALIDATION.md (green gate)
    - package.json
  </read_first>
  <action>Run the green gate; fix only by reverting accidental token changes. Confirm `dependencies` = `react` + `react-dom`.</action>
  <verify>
    <automated>npm run build && npm run lint && npm run test:run && test -z "$(git diff package.json)"</automated>
  </verify>
  <acceptance_criteria>
    - build/lint exit 0; test:run passes (BEHAVIOR-01 / QUAL-01).
    - `git diff package.json` empty (QUAL-01).
  </acceptance_criteria>
  <done>All gates green; package.json byte-identical.</done>
</task>

</tasks>

<threat_model>
No new threat surface — comment-only text edits; no executable token changes. A comment documenting a security-relevant invariant is KEPT/rephrased per D-03, never deleted.

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-55-05 | Tampering | comment edit crossing into executable token / dropping a spike geometry value | mitigate | comment-only-diff invariant + green gate (Task 2); D-08 value-retention spot-check against RESEARCH Pitfall 5 |
</threat_model>

<verification>
- All three grep gates empty over `src/components/**`; spike geometry values retained; diffs comment-only; green gate passes.
</verification>

<success_criteria>
src/components comments read present-tense with zero archaeology and zero stale line-refs; spike-locked geometry values intact; behavior unchanged.
</success_criteria>

<output>
Create `.planning/phases/55-comment-de-archaeology/55-05-SUMMARY.md` when done.
</output>
