---
phase: 55-comment-de-archaeology
plan: 04
type: execute
wave: 4
depends_on: []
files_modified:
  - src/storage/**/*.ts
  - src/storage/**/*.tsx
autonomous: true
requirements: [COMMENT-01, COMMENT-02, TEST-01, BEHAVIOR-01, QUAL-01]

must_haves:
  truths:
    - "A maintainer reading any src/storage file sees present-tense invariants only; parity/modeling narration is gone."
    - "Every changed line in src/storage is inside a comment region — no executable token changed."
    - "Build, lint, and the full test suite remain green; package.json is byte-identical."
  artifacts:
    - path: "src/storage"
      provides: "Storage comments de-archaeologized (~98 lines / 7 files — mostly delete: parity/modeling)"
      contains: "// "
  key_links:
    - from: "src/storage parity/modeling narration"
      to: "deletion (D-04/D-05)"
      via: "keep-vs-cut decision tree from 55-CONTEXT.md"
      pattern: "comment-only diff"
---

<objective>
De-archaeologize all comments in `src/storage/**` (~98 comment lines across 7 files — predominantly DELETE, since most are parity-with-old-code / data-modeling narration). Strip taxonomy tokens and stale line-refs; keep-rephrase the few real invariants; delete the rest. NO executable token changes.

Purpose: COMMENT-01 + COMMENT-02 over the storage layer.
Output: Comment-only diffs across src/storage; build/lint/test green.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/55-comment-de-archaeology/55-CONTEXT.md
@.planning/phases/55-comment-de-archaeology/55-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: De-archaeologize src/storage comments (mostly deletion of parity/modeling lines)</name>
  <read_first>
    - every file under src/storage/ this task edits
    - .planning/phases/55-comment-de-archaeology/55-CONTEXT.md (D-01..D-09 decision tree — read first; D-04/D-05 deletion is the dominant rule here)
    - .planning/phases/55-comment-de-archaeology/55-RESEARCH.md (per-site notes for src/storage, keep-vs-cut tree, grep patterns)
  </read_first>
  <action>
    Edit comments only. Apply the 55-CONTEXT keep-vs-cut tree, expecting mostly DELETE:
    - STRIP taxonomy tokens: `D-xx`, `WR-xx`/`DS-WR-xx`, `Phase NN`, `Plan NN`, `Blocker #N`, `Pitfall N`, `spike NNN`, dated kitchen-sink (COMMENT-01).
    - STRIP `L###`, `formerly at`, `mirror L###` — all `L###` presumptively stale, delete never re-number (COMMENT-02).
    - DELETE (D-04/D-05) parity-with-old-code and data-modeling narration outright (the bulk here).
    - KEEP-rephrase (D-03) only genuine present-tense invariants.
    No executable-token changes. No new files. Consult RESEARCH for per-site detail.
  </action>
  <verify>
    <automated>test -z "$(git grep -nE '\b(D-[0-9]+|(DS-)?WR-[0-9]+|Phase [0-9]+|Plan [0-9]+|Blocker|Pitfall|spike[ -]?[0-9]+|kitchen.?sink)' -- 'src/storage/**')" && test -z "$(git grep -nE '(\bL[0-9]{2,}|formerly at|mirror .*L[0-9])' -- 'src/storage/**')"</automated>
  </verify>
  <acceptance_criteria>
    - taxonomy grep over `src/storage/**` empty (COMMENT-01).
    - line-ref grep over `src/storage/**` empty (COMMENT-02).
    - every `git diff <file>` hunk inside a comment region (BEHAVIOR-01 / D-09).
  </acceptance_criteria>
  <done>src/storage parity/modeling narration removed; remaining comments present-tense; both grep gates empty; diffs comment-only.</done>
</task>

<task type="auto">
  <name>Task 2: Green gate — build, lint, test, package.json unchanged</name>
  <read_first>
    - .planning/phases/55-comment-de-archaeology/55-VALIDATION.md (green gate)
    - package.json
  </read_first>
  <action>Run the green gate; fix only by reverting accidental token changes. Confirm `dependencies` = `react` + `react-dom`.</action>
  <verify>
    <automated>npm run build && npm run lint && npm run test:run && test -z "$(git diff package.json)"</automated>
  </verify>
  <acceptance_criteria>
    - build/lint exit 0; test:run passes (BEHAVIOR-01 / QUAL-01).
    - `git diff package.json` empty (QUAL-01).
  </acceptance_criteria>
  <done>All gates green; package.json byte-identical.</done>
</task>

</tasks>

<threat_model>
No new threat surface — comment-only text edits; no executable token changes. A comment documenting a security-relevant invariant is KEPT/rephrased per D-03, never deleted.

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-55-04 | Tampering | comment edit crossing into executable token | mitigate | comment-only-diff invariant + green gate (Task 2) |
</threat_model>

<verification>
- Both grep gates empty over `src/storage/**`; diffs comment-only; green gate passes.
</verification>

<success_criteria>
src/storage comments read present-tense with zero archaeology and zero stale line-refs; behavior unchanged.
</success_criteria>

<output>
Create `.planning/phases/55-comment-de-archaeology/55-04-SUMMARY.md` when done.
</output>
