---
phase: 55-comment-de-archaeology
plan: 08
type: execute
wave: 7
depends_on: []
files_modified:
  - "src/{hooks,storage,app,styles}/**/*.test.ts"
  - "src/{hooks,storage,app,styles}/**/*.test.tsx"
  - "src/*.test.ts"
  - "src/*.test.tsx"
autonomous: true
requirements: [COMMENT-01, COMMENT-02, TEST-01, BEHAVIOR-01, QUAL-01]

must_haves:
  truths:
    - "Test comments in the hooks/storage/app/styles + top-level test files carry zero archaeology tags and zero stale line-refs (D-01)."
    - "No test is deleted, skipped, or rewritten — only comment text inside tests is edited (D-02)."
    - "Every changed line is inside a comment region — no test code, assertion, describe/it title, or literal changed."
    - "Build, lint, and the full test suite remain green; package.json is byte-identical; test count unchanged."
  artifacts:
    - path: "src/hooks/**/*.test.ts"
      provides: "Test comments de-archaeologized without touching any test body"
      contains: "import"
  key_links:
    - from: "test-file comment regions (this half of the suite)"
      to: "tag-stripped comment text only (D-01)"
      via: "55-CONTEXT D-01/D-02 + 55-RESEARCH Pitfall 3 (no test asserts on comment text)"
      pattern: "comment-only diff, no test deleted/rewritten"
---

<objective>
Strip archaeology taxonomy tokens and stale line-refs from comments inside the test files under `src/hooks`, `src/storage`, `src/app`, `src/styles`, and any top-level `src/*.test.*` (second half of the ~797-line / 78-file test surface). Mechanical per D-01. Do NOT delete, skip, or rewrite any test (D-02). NO executable/test-code token changes.

Purpose: COMMENT-01 + COMMENT-02 + TEST-01 over this half of the test suite, behavior-preserving.
Output: Comment-only diffs in these test files; suite green; test count unchanged.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/55-comment-de-archaeology/55-CONTEXT.md
@.planning/phases/55-comment-de-archaeology/55-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Strip archaeology from comments in hooks/storage/app/styles + top-level test files (no test deletion/rewrite)</name>
  <read_first>
    - every *.test.ts(x) under src/hooks, src/storage, src/app, src/styles, and top-level src/*.test.* this task edits
    - .planning/phases/55-comment-de-archaeology/55-CONTEXT.md (D-01 strip tags in test comments; D-02 NO deletion/rewrite of any test — read first)
    - .planning/phases/55-comment-de-archaeology/55-RESEARCH.md (Pitfall 3: no test asserts on comment text + the REVIEW_MARKER / marker-guard caveat; grep patterns)
  </read_first>
  <action>
    Edit comment text inside test files only. Mechanical per D-01:
    - STRIP taxonomy tokens from comments: `D-xx`, `WR-xx`/`DS-WR-xx`, `Phase NN`, `Plan NN`, `Blocker #N`, `Pitfall N`, `spike NNN`, dated kitchen-sink (COMMENT-01).
    - STRIP `L###`, `formerly at`, `mirror L###` from comments — all `L###` presumptively stale, delete never re-number (COMMENT-02).
    - D-02 HARD RULE: do NOT delete, skip, `.skip`, `.only`, reorder, or rewrite any test, describe/it block, assertion, or test literal. Touch comment text only.
    - Per RESEARCH Pitfall 3: no test asserts on comment text, so comment edits are safe. If `content.no-review-markers.test.ts` falls in this set, do NOT touch its `REVIEW_MARKER` string literal (non-comment guard for I18N-04).
    No executable/test-code token changes. No new files. Consult RESEARCH for per-site detail.
  </action>
  <verify>
    <automated>test -z "$(git grep -nE '\b(D-[0-9]+|(DS-)?WR-[0-9]+|Phase [0-9]+|Plan [0-9]+|Blocker|Pitfall|spike[ -]?[0-9]+|kitchen.?sink)' -- 'src/hooks/**/*.test.*' 'src/storage/**/*.test.*' 'src/app/**/*.test.*' 'src/styles/**/*.test.*' 'src/*.test.*')" && test -z "$(git grep -nE '(\bL[0-9]{2,}|formerly at|mirror .*L[0-9])' -- 'src/hooks/**/*.test.*' 'src/storage/**/*.test.*' 'src/app/**/*.test.*' 'src/styles/**/*.test.*' 'src/*.test.*')"</automated>
  </verify>
  <acceptance_criteria>
    - taxonomy grep over these test globs empty (COMMENT-01).
    - line-ref grep over these test globs empty (COMMENT-02).
    - No test deleted/skipped/rewritten: `git diff` for each file shows only comment-region hunks; no change to any `describe(`, `it(`, `test(`, `expect(`, `.skip`, `.only`, or string literal (D-02 / TEST-01 / BEHAVIOR-01).
    - any `REVIEW_MARKER` literal untouched.
  </acceptance_criteria>
  <done>This half of the test suite has tag-free, ref-free comments; every test intact; diffs comment-only.</done>
</task>

<task type="auto">
  <name>Task 2: Green gate — build, lint, test, package.json + test count unchanged</name>
  <read_first>
    - .planning/phases/55-comment-de-archaeology/55-VALIDATION.md (green gate)
    - package.json
  </read_first>
  <action>Run the green gate. Confirm the suite still passes with the same number of tests as before (no test removed/added). Fix only by reverting accidental code/literal changes. Confirm `dependencies` = `react` + `react-dom`.</action>
  <verify>
    <automated>npm run build && npm run lint && npm run test:run && test -z "$(git diff package.json)"</automated>
  </verify>
  <acceptance_criteria>
    - build/lint exit 0; test:run passes with unchanged test count (BEHAVIOR-01 / TEST-01 / QUAL-01).
    - `git diff package.json` empty (QUAL-01).
  </acceptance_criteria>
  <done>All gates green; test count unchanged; package.json byte-identical.</done>
</task>

</tasks>

<threat_model>
No new threat surface — comment-only text edits in test files; no executable/test-code token changes. Any `REVIEW_MARKER` guard literal (I18N-04) is left untouched.

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-55-08 | Tampering | comment edit deleting/altering a test or the REVIEW_MARKER literal | mitigate | D-02 no-deletion rule + comment-only-diff invariant + green gate with unchanged test count (Task 2) |
</threat_model>

<verification>
- Both grep gates empty over this test half; no test deleted/rewritten; diffs comment-only; green gate passes with unchanged test count.
</verification>

<success_criteria>
Hooks/storage/app/styles + top-level test comments are tag-free and ref-free; all tests intact; behavior unchanged.
</success_criteria>

<output>
Create `.planning/phases/55-comment-de-archaeology/55-08-SUMMARY.md` when done.
</output>
