# Phase 5.1 UAT Log

Date: 2026-05-10
Tester: Renato Lucindo
App build: 5d54aa0

## Task 1 — Automated regression suite (D-18) — PASS

- `npx vitest run` exit 0 — **24 files, 280/280 tests pass**
- `npx tsc --noEmit -p .` exit 0
- New resume-path test count (`grep -c "visibility resume"` on `useAudioCues.test.tsx --reporter=verbose`): **4** (matches Plan 05.1-01 Task 4 spec — `resume-fires-when-visible-and-engine-alive`, `resume-not-fired-when-engineRef-null`, `resume-not-fired-when-hidden`, `resume-rejection-silently-absorbed`)
- No previously-green test is now red. Net delta = +4 tests (276 → 280).

## Task 2 — Real-device iOS Safari audio resume (D-17, AUDI-01 / AUDI-02) — **FAIL** (Plan 01 fix inadequate; root cause diagnosed 2026-05-10)

**Device:** iPhone (iOS Safari) — real-device UAT
**Session config:** active session with audio on
**Lock duration:** ~10s (well under 30s discard threshold)

**Result:** **FAIL** — audio does not resume after unlock.
**Observed audio behavior on unlock:** Session continues running visually; audio silent; mute toggle does NOT restore audio; only starting a new session restores audio (constructs fresh AudioContext).
**Mute toggle (AUDI-02 regression check):** Unchanged behavior — but does not serve as recovery path either (D-08 just flips muted flag; never calls resume()).
**End-session cleanup:** Clean.
**Notes / surprises:** Plan 01 spec was empirically wrong on two counts (see Diagnostic findings below).

### Diagnostic findings (Web Inspector console capture, 2026-05-10)

Temporary `console.log` instrumentation added to `useAudioCues.onVisibility` and `audioEngine.resume()` (later reverted; not committed). Mac+iPhone Safari Web Inspector session captured the following sequence on a single lock(~10s)/unlock cycle:

```
visibilitychange fired → visibilityState: "hidden",  hasEngine: true
                          ↓ (lock duration)
visibilitychange fired → visibilityState: "visible", hasEngine: true
engine.resume() entry  → closedFlag: false,  audioCtx.state: "interrupted",  currentTime: 21.152
engine.resume()        → REJECTED  InvalidStateError "Failed to start the audio device"
                                   audioCtx.state after reject: "suspended"
```

**Key facts:**

1. `visibilitychange` listener wires up and fires correctly on both hide and visible — D-01..D-09 gate logic is sound.
2. `audioCtx.state === "interrupted"` is a **WebKit-only non-standard state** not present in MDN's published AudioContext state enum (`suspended | running | closed`). Plan 01 spec assumed lock → `suspended`; reality is lock → `interrupted` → (after rejected resume()) `suspended`.
3. `audioCtx.resume()` invoked from a `visibilitychange` handler is **NOT considered a user gesture** by iOS Safari. The promise rejects with `InvalidStateError: "Failed to start the audio device"` even though the AC transitions to `suspended` (which is normally recoverable from a gesture). The D-09 silent-absorb pattern hides this rejection, so visuals continue while audio is dead — no surface to the user.
4. The mute-toggle "recovery path" the user instinctively tried does not work because `setMuted(false)` only flips a flag (`audioEngine.ts:38`); it never calls `resume()`. A user tap on the mute button IS a real user gesture, so this path could trivially be made to succeed if `setMuted(false)` invoked `resume()` synchronously.

**Implication for Plan 05.1-06 spec (input only; binding spec to be written via /gsd-plan-phase):**

| # | Direction |
|---|-----------|
| 1 | Treat `interrupted` symmetrically with `suspended` in any state-machine surface. Both are recoverable from a user gesture; neither is a terminal state. |
| 2 | The visibilitychange path cannot be the sole recovery — it lacks user-gesture context. UI must surface a "Tap to resume audio" affordance when post-unlock `resume()` rejects. |
| 3 | `setMuted(false)` is the cheap gesture-attached shortcut: have it call `resume()` synchronously so users who instinctively un-mute recover for free. |
| 4 | Use `audioCtx.addEventListener('statechange')` to drive the recovery flag transitions cleanly instead of polling. |
| 5 | Relax D-09 specifically for `InvalidStateError` on iOS — propagate to a status surface (e.g. `audioStatus: 'needs-resume'`) rather than swallowing. Keep silent-absorb for unknown errors. |
| 6 | Tests need a mock AC `'interrupted'` state value (jsdom default does not expose). ~6 new tests; existing D-01..D-09 cases stay green if the gate is widened additively. |

**Carry-forward (Plan 01 was not wasted):** D-01..D-09 gate scaffolding stands; Plan 06 widens it. The empirical failure here drove the diagnosis; the alternative (shipping a guessed engine-reconstruction Plan 06 preemptively) would have likely missed the user-gesture half of the bug entirely.

## Task 2 (Re-test post-Plan 06) — Real-device iOS Safari audio resume (D-43, AUDI-01 / AUDI-02) — **FAIL (iOS limitation confirmed; carry-forward to v1.x)**

**Device:** iPhone Xs Max iOS 18.7.8 Safari — real-device UAT
**App build:** 213c2c7 (Plan 06 + post-UAT kitchen-sink fix)
**Lock duration:** ~10 s
**Tester:** Renato Lucindo
**Date:** 2026-05-10

### Cycle 1 — first ship, no diagnostic instrumentation

**Result:** **FAIL** — no resume affordance morph appeared on unlock; mute toggle did not recover audio; no console errors.

### Cycle 2 — fix `c510d49` (read live `engine.state` to gate reconstruction) + diagnostic instrumentation

**Result:** **FAIL** — affordance appeared correctly on unlock (statechange D-38 + needs-resume D-34 wired right), but tapping it muted-flipped (D-31 dual-purpose intentional) and audio did NOT resume. Second tap (unmute) had no audio. Waited a full in/out cycle, silent.

**Diagnostic trace excerpt:**

```
engine.resume() called, state: suspended           ← gesture tap fires audio.resume()
engine.resume() resolved, state: running           ← AC clock should now advance
statechange: running                                ← state machine reports running
scheduleNextCue: out, audioTime: 32.14, AC.currentTime: 22.55, delta: 9.59
scheduleNextCue: in,  audioTime: 37.29, AC.currentTime: 22.55, delta: 14.74
scheduleNextCue: out, audioTime: 40.71, AC.currentTime: 22.55, delta: 18.16
scheduleNextCue: in,  audioTime: 45.86, AC.currentTime: 22.55, delta: 23.31
```

**Smoking gun:** AC.currentTime stuck at 22.55 across 30+ seconds of real time despite `state === 'running'`. Beep test (manual `__audioCtx.createOscillator()` from Web Inspector console post-recovery) silent. The audio thread never started.

**Two bugs identified post cycle 2 (kitchen-sink fix `213c2c7` shipped):**
- Reconstruction was creating new AC AFTER awaiting old `engine.close()` — iOS consumes gesture token at first await, so new AC ran outside gesture context.
- Re-anchor formula in App.tsx used `newAC.now()` (≈ 0) as `audioAnchor`, so boundary math evaluated to large future audioTime relative to new AC.

### Cycle 3 — fix `213c2c7` (always reconstruct on gesture; gesture-preserving order; anchor offset)

**Result:** **FAIL** — affordance still appears, tap still triggers reconstruction (new AC IS built inside gesture context now), new AC.state correctly transitions `suspended → running` via WR-06. **But new AC.currentTime stuck at 0 across 24+ seconds of real time:**

```
new AC state: suspended                            ← reconstruction kicked in (NEW AC inside gesture)
statechange: running                                ← new AC autoplay-resumed
handleStateChange: running, attempted: false       ← fresh state machine
scheduleNextCue: out, audioTime: 3.16,  AC.currentTime: 0, delta: 3.16
scheduleNextCue: in,  audioTime: 11.16, AC.currentTime: 0, delta: 11.16
scheduleNextCue: out, audioTime: 16.49, AC.currentTime: 0, delta: 16.49
scheduleNextCue: in,  audioTime: 24.49, AC.currentTime: 0, delta: 24.49
```

**Even a freshly-constructed AudioContext acquired inside iOS gesture context has its clock stuck at 0.** Web Audio state machine reports `running`; hardware audio thread never starts. The iOS audio session was permanently lost when the device entered the lock state and CANNOT be re-acquired within the same page session via any Web Audio API call. Plain `resume()`, in-place reconstruction, gesture-preserved or not — all blocked at the OS level.

**Root finding (cycles 2 + 3 combined):** **iOS Safari (this device / version) loses the audio session permanently on lock/unlock and does not grant a new session to any subsequent AudioContext within the same page lifetime.** The only known recovery path is the one `.continue-here.md` originally observed pre-Plan-06: **end the current session and start a new one** (the await in `stop() → engine.close()` runs to completion before the new gesture creates a fresh AC; only this temporal ordering releases the iOS audio session). Note this is fundamentally different from "tap to recover" — it requires a session-state transition (end → start), not just an AC reconstruction.

### Sign-off (carry-forward to v1.x)

Per user decision (2026-05-10), this is **scoped out of v1 as a documented iOS limitation, not a Plan 06 implementation defect**:

- Plan 06's affordance + statechange + reconstruction code IS retained and IS correct everywhere else (jsdom test suite 312/312 green; Chrome / Firefox / Safari Desktop do not enter `'interrupted'` state and behave normally per D-44 expectation).
- On iOS Safari post-lock, the affordance still appears (state surfaces correctly), the gesture-attached recovery still fires (semantically correct), but the actual audio output does not return because iOS holds the audio session closed.
- v1.x candidate scopes for full iOS lock recovery (require fresh discuss-phase + plan):
  1. Auto-detect dead AC.currentTime and force-restart session (loses cycle index continuity; replays lead-in).
  2. Two-tap recovery UX: first tap closes + awaits, second tap creates new AC.
  3. Page-reload-on-audio-loss (uses `location.reload()`; loses any unsaved state).
- v1 user-facing guidance to be added: "If audio goes silent after you lock/unlock the screen mid-session, end and start a new session to restore audio cues."

**Overall Task 2 (Re-test):** FAIL on iOS (carry-forward); behavior on non-iOS browsers confirmed unchanged from Plan 01 baseline by the structural test suite. Cross-browser D-44 smoke test deferred to Plan 03 Task 4 manual sweep.

## Task 3 — Safari desktop orb-to-outer-ring visual (D-17, GUID-01) — **PASS** (after re-fix)

> Originally captured as FAIL; remediated by Plan 05.1-04 + post-UAT hotfix sequence (commits 67c03fb → 4f4dea4 → 9e4889e → b717175 → 425fa5a). Final state below; FAIL diagnostic preserved beneath for traceability.

**Final UAT (post-remediation):**

| Browser | Result |
|---|---|
| Safari Desktop (macOS, Retina, private window) | **PASS** — at progress=0.984 → visible 357.6 px (transform scale 0.993) — orb fills outer ring at peak, no perceptible gap |
| Safari iOS (iPhone) | **PASS** — orb meets ring (already passed pre-remediation; no regression) |
| Chrome Desktop | **PASS** — smooth scaling, no flicker |
| Firefox Desktop | **PASS with caveat** — animation works, **slight flicker remains** during scale animation. Known issue. See Caveats below. |

**Caveats (carry-forward, not blocking sign-off):**
- **Firefox Desktop scale-animation flicker (FF-01).** Per-frame inline `transform` writes alongside `left/right/top/bottom` four-edge anchoring trigger residual paint flashing on Firefox. Mitigations attempted: `will-change: transform` (partial), `translate3d(0,0,0) scale(s)` (partial), `scale3d(s, s, 1) + backface-visibility: hidden` (kitchen-sink — reverted at user's request). Root remedy likely requires CSS-keyframe-driven animation instead of per-frame React transform writes (out of Phase 5.1 budget). Phase 5.1 success criterion #2 calls out Safari only; Firefox is not a primary target. Captured here for v1.x or Phase 6 follow-up.
- **D-23 (orb-layer children).** Default (b) was honored — `.orb-layer--in` / `.orb-layer--out` kept `inset-0` Tailwind. Verified visually that gradient layers render correctly inside the four-edge-anchored `.orb` parent.

**Remediation summary (commit chain — see git log for detail):**
1. `67c03fb` — `.orb` swapped from `width:100%; height:100%; left:0; top:0` (initial Plan 04 fix; Safari sized box correctly but froze the 200ms transition) to four-edge anchoring `left:0; right:0; top:0; bottom:0`.
2. `4f4dea4` — dropped `.orb { transition: transform 200ms ease-out }` from theme.css. Per-frame React updates (60fps via rAF) handle smoothing. Safari/Chromium/iOS Safari all smooth. Firefox flickered.
3. `9e4889e` — `will-change: transform` on `.orb` (Firefox compositor-promotion hint). Partial improvement.
4. `b717175` — inline `transform: translate3d(0,0,0) scale(s)`. Partial improvement.
5. `acea349` → reverted by `425fa5a` — kitchen-sink (`scale3d(s, s, 1) + backface-visibility: hidden`) gave only marginal further improvement; reverted at user's request to keep CSS surface minimal.

**Final inline `transform` form on `.orb`:** `translate3d(0,0,0) scale(orbScale)` (BreathingShapeBody) and `translate3d(0,0,0) scale(MID_SCALE)` (BreathingShapeLeadIn).

**Lead-in render check:** PASS — outer ring matches body via D-22 mirror invariant.
**Reduced-motion check:** PASS — orb at MID_SCALE (0.79), no animation, ring geometry unchanged. JS-controlled inline transform still wins per CR-01.

**Test surface:** vitest 294/294 pass; tsc exit 0. New `BreathingShape.test.tsx` D-24 structural assertions (14 cases) lock the post-fix DOM markup against silent regression on future refactors.

---

### Original FAIL diagnostic (preserved for history)


**Device:** macOS Safari Desktop (Retina display)
**Session config (initial):** BPM=4.5, Ratio=40:60 (≥5s In phase, ample time to peak)
**Diagnostic session:** BPM=2 (or current default), inspected at `data-progress="0.971"`

**Result at near-peak inhale:** **FAIL** — visible gap ≈ 75 px on each side between orb edge and outer ring inner edge.

**iOS Safari (iPhone) result:** **PASS** — orb meets ring; no gap. The bug is Safari Desktop only.

**Inspector data captured at progress=0.971:**

| Element | getBoundingClientRect().width | Expected |
|--------|-------------------------------|----------|
| `div[role="img"][data-phase]` (container) | 360.00 px | 360 px ✓ |
| `.orb-ring--outer` | 363.00 px | 363 px ✓ (`inset:-1.5px` resolves correctly) |
| `.orb` | **209.00 px** | 360 px ✗ — sized at 58% of parent (≈ MIN_SCALE × 360) |

The orb's `transform: scale(0.988016)` (matches `0.58 + 0.971 × 0.42`) is mathematically correct, but the underlying box is collapsed to 209 px on Safari Desktop instead of filling the 360 px container via `inset-0`.

**Root cause hypothesis (matches existing WR-03 comment in `BreathingShape.tsx:82`):**
Absolutely-positioned children of a `display: grid; place-items: center` parent are inconsistently sized on Safari Desktop. The inner ring already uses an explicit `left:50%; top:50%; width: 58%; height: 58%; -translate-{x,y}-1/2` pattern to dodge this. The `.orb` div and the outer-ring span still rely on `inset-0` / `inset: -1.5px` shorthand, which Safari Desktop honors for the outer ring (negative inset forces non-zero size) but NOT for `.orb` (where `inset: 0` collapses the box to 58% of parent, matching the inner ring's explicit size — most likely Safari is using sibling auto-track sizing).

**Plan 05.1-02 status:** addressed the 1.5px border-alignment surface symptom (real but secondary). The deeper WR-03-class root cause remains. Re-fix required.

**Lead-in render check:** _(deferred until Issue A is fixed — blocking)_
**Reduced-motion check:** _(deferred until Issue A is fixed — blocking)_
**Screenshots:** screenshot 1 (peak-inhale gap, BPM=4.5 Ratio=40:60), screenshot 3 (Web Inspector, progress=0.971, scale=0.988016) — both transient in chat; user can re-save to `screenshots/` as needed.
**Notes:** Bug is Safari Desktop-specific render path. iOS Safari + Chromium expected to render correctly. Next plan must add a Chromium regression check.

## Task 3b — Out-of-scope finding: inner-ring visibility during Out phase

User-reported (cross-browser, all engines): during the Out phase, the inner reference ring is not visible because the orb (still scaled above MIN_SCALE for most of the phase) covers it. Symmetry gap with the In phase, where the outer ring provides a "approaching boundary" cue. Phase 5.1 scope (D-11) explicitly preserved the inner ring untouched, so this is a new defect surface.

**Disposition:** track as Plan 05.1-05 candidate (inner-ring UX symmetry); does NOT block Phase 5.1 sign-off if scoped out, BUT user has chosen to expand Phase 5.1 to include it.

## Task 4 — Phase 1–5 regression sweep on real Safari (D-18)

**Device:** _(device + browser used)_
**Phase 1 timing + EndSessionDialog:** PASS / FAIL — _(notes)_
**Phase 2 orb + a11y:** PASS / FAIL — _(notes)_
**Phase 3 audio cues + mute:** PASS / FAIL — _(notes)_
**Phase 4 storage restore + stats:** PASS / FAIL — _(notes)_
**Phase 5 wake lock:** PASS / FAIL — _(notes)_

## Sign-off

Phase 5.1 acceptance: PASS / FAIL
All three success criteria from ROADMAP §Phase 5.1 are met / NOT met.
Outstanding gaps: _(none / list)_

---

Phase 5 S2 Android Chrome carry-forward (`05-04-UAT-LOG.md` Gap 1) is explicitly out of scope for Phase 5.1 sign-off per Plan 05.1-03.
