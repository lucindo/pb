# Phase 5.1: Hands-Off Resilience Polish - Context

**Gathered:** 2026-05-10
**Status:** Ready for planning

<domain>
## Phase Boundary

Phase 5.1 ships two carry-forward fixes from the Phase 5 manual UAT log (`05-04-UAT-LOG.md` Findings 1+2) and one Phase 2 visual carry-forward, all on real-device Safari surfaces:

1. **iOS Safari audio-resume on visibilitychange→visible.** When the user manually locks the phone mid-session and unlocks ≥30 s later, the AudioContext is left in a `suspended` state by iOS Safari and never resumes — audio cues stay silent for the rest of the session even though the wake-lock has re-acquired and the visual orb continues. Phase 5.1 mirrors the Phase 5 D-03 visibility-re-acquire pattern inside `useAudioCues` so the AC `resume()` path is symmetric with the wake-lock path: same listener placement, same ref-gated guard, same silent-fallback posture.

2. **Safari desktop breathing-orb max-scale visual gap.** At peak inhale the inner orb (`transform: scale(MAX_SCALE = 1.0)`) does not visually reach the dashed outer reference ring. The gap is sub-pixel/box-sizing arithmetic on Safari only — Chromium renders both edges coincident. Phase 5.1 compensates the outer ring sizing (negative inset OR explicit content-box width/height) without touching the orb scale constants or the Phase 2 reduced-motion contract.

The phase boundary is fixed by `.planning/ROADMAP.md` §Phase 5.1. **Out of scope:** Android Chrome S2 carry-forward UAT (tracked separately in `05-04-UAT-LOG.md` Gap 1 — closed by physical-device run, not by code work in this phase), MOBL-03 user-visible Wake Lock fallback explanation (v2), pause/resume mid-session (v2 SESS-06), volume control (v2 AUDI-03), Forrest learning content (Phase 6), any change to Phase 1 timing math, Phase 3 dual-anchor / scheduleNextCue contract, Phase 4 storage envelope, or Phase 5 useWakeLock surface.

</domain>

<decisions>
## Implementation Decisions

### Audio Resume Site
- **D-01:** The `visibilitychange` listener that drives audio resume is **owned by the `useAudioCues` hook**, mirroring Phase 5 D-04 (`useWakeLock` owns its own visibility listener). Listener installed in a `useEffect([])` on mount, removed in cleanup. Ref-gated guard inside the handler. Symmetry with the wake-lock pattern is the primary reason — future readers see the same shape twice and the pattern becomes obvious.
- **D-02:** Listener placement is in the **hook**, not in `audioEngine.ts`. The engine stays React-free / DOM-event-free (current invariant — engine module currently has zero `document.*` and zero `window.*` access). The hook is the React-side resource owner; the engine is the pure-imperative service composed by it. This is the same boundary Phase 3 D-09 → D-11 / Phase 5 D-11 → D-13 already established.

### Audio Resume Gating
- **D-03:** The visibility handler calls `audioCtx.resume()` **whenever an engine exists** (`engineRef.current !== null`). Mirrors Phase 5 D-04's `wasAcquiredRef`-gated guard pattern. Covers running and lead-in (any time the AudioContext was constructed and not yet closed). The hook does NOT need to know about App-level `appPhase` — the `engineRef` is a sufficient and self-owned signal.
- **D-04:** Gate is **`engineRef.current !== null`** specifically (not `audioCtx.state === 'suspended'`). The state read is unreliable on iOS during the display-sleep window and adds a polling-feel. The ref tells us "we own a live AC"; calling `resume()` on an already-running AC resolves silently — same idempotent posture as Phase 5 D-08.

### Make-Up Cue Policy
- **D-05:** After a successful resume mid-phase, **wait for the next phase boundary** — do NOT fire a make-up cue. Mirrors Phase 3 D-08 (unmute waits for boundary). The next In/Out cue fires from the existing `scheduleNextCue` dispatch driven by App.tsx Plan 03-04's boundary effect — no make-up scheduling code needed. Avoids audible "late strike" that would feel like a glitch and avoids touching the Plan 03-04 dual-anchor invariant (Phase 3 Pitfall 2).
- **D-06:** No re-anchoring of `audioAnchorRef` on resume. The original anchor captured at lead-in completion stays valid — the audio clock and the breathing plan offsets are unchanged across the suspend/resume cycle. Re-anchoring would cascade into App.tsx integration tests and risks misaligning subsequent cues.

### Engine Surface
- **D-07:** `audioEngine.ts` gains a single new method: **`resume(): Promise<void>`** that calls `audioCtx.resume()` inside a try/catch. Idempotent: calling `resume()` on an already-running AC resolves silently. No state read inside (mirrors D-04's posture — the caller decides whether to call). Failures absorbed silently per D-09 below. This is the minimal engine API change needed; everything else stays internal to the hook.
- **D-08:** `useAudioCues` gains no new public method. The visibility listener is fully internal — exactly the same pattern as `useWakeLock`'s D-12 (no `isAcquired` / no `isSupported` / no resume status exposed). The public hook surface (`status`, `audioAvailable`, `muted`, `start`, `stop`, `setMuted`, `notifyPhaseBoundary`, `audioNow`) is unchanged. App.tsx requires zero edits beyond the test files.

### Failure Posture
- **D-09:** **All resume failures are silently absorbed.** Concretely: `audioCtx.resume()` rejecting (browser veto, AC already closed, race against unmount), the listener firing after `engineRef.current` was nulled by `stop()`, any synchronous throw — all resolve with no user-facing artifact. No banner, no toast, no console warning required (single dev-only `console.debug` is acceptable but optional). Matches Phase 3 D-10 (AC fail → visuals-only) and Phase 5 D-09 (Wake Lock fail → silent). The session continues; if iOS rejects resume, the user is no worse off than the current Phase 5 baseline.

### Orb Scale Fix
- **D-10:** **Outer reference ring sizing compensated; orb scale constants UNCHANGED.** Concretely: in `BreathingShape.tsx` the outer ring `<span>` (currently `className="orb-ring--outer absolute inset-0 rounded-full border-solid"`) is updated so its visible edge matches the orb at `scale(MAX_SCALE = 1.0)` on Safari. Two acceptable mechanisms (planner picks based on cross-browser test):
  1. Explicit width/height: 100% with `box-sizing: content-box` on `.orb-ring--outer` so the 1.5 px border lives outside the 100% box, OR
  2. Negative inset compensation: replace `inset-0` with explicit `inset: -1.5px` (or the equivalent left/top/right/bottom -1.5px) so the border-box's outer edge sits 1.5 px beyond the container's 100% box.

  Neither approach changes `--orb-scale-min` / `--orb-scale-max` / `--orb-scale-mid` or the corresponding TS constants. The TS↔CSS contract documented in `BreathingShape.tsx:18-22` and `theme.css:14-20` (IN-01 sync rule) is preserved.
- **D-11:** **Inner reference ring is NOT touched.** UAT only flagged the outer ring (`05-04-UAT-LOG.md` Finding 1: "inner aqua orb does not visually meet the dashed outer guide ring"). The inner ring is already explicitly sized via `width: ${MIN_SCALE * 100}%` / `height: ${MIN_SCALE * 100}%` (`BreathingShape.tsx:80-83`) so it does NOT share the `inset-0` + border-box arithmetic that produces the outer-ring gap. Don't fix what isn't broken.
- **D-12:** **Lead-in render path receives the same outer-ring fix.** `BreathingShapeLeadIn` (`BreathingShape.tsx:126-171`) renders its own copy of the outer ring at `inset-0 + border-solid`. The fix applies to both render sites or the gap reappears during the 3-2-1 countdown.

### Reduced-Motion Compatibility
- **D-13:** **The Phase 2 reduced-motion contract is preserved verbatim.** `MID_SCALE = 0.79` is the average of `MIN_SCALE` and `MAX_SCALE`; if the planner had bumped MAX_SCALE in D-10 it would have shifted MID_SCALE and broken `theme.css:18-20` IN-01 sync + the Phase 2 D-06 fixed-mid-scale contract. Compensating the outer ring instead leaves all three scale constants and the reduced-motion render path untouched. The `@media (prefers-reduced-motion: reduce)` block in `theme.css:112-117` is unchanged.

### Test Infrastructure
- **D-14:** **`vitest.setup.ts` FakeAudioContext is extended with `state` + `resume()` + `_simulateSuspend()`.** Following Phase 5 D-13 precedent (FakeWakeLock added the same way). Concrete additions:
  - A `state` getter that returns `'running'` by default and `'suspended'` after `_simulateSuspend()` is called.
  - A `resume()` method that flips `state` back to `'running'` and resolves. Spy-able with `vi.spyOn(audioCtxInstance, 'resume')`.
  - A `_simulateSuspend()` non-standard hook (underscore-prefixed, test-only) that sets `state='suspended'` so tests can model the iOS lock-screen window.
  - The existing FakeAudioContext's other behaviors (currentTime, destination, close, scheduleTick/Cue) remain unchanged.
- **D-15:** **Hook tests dispatch a `visibilitychange` event** via `Object.defineProperty(document, 'visibilityState', { value: 'visible' })` + `document.dispatchEvent(new Event('visibilitychange'))`. Same pattern as Phase 5 D-13 wake-lock visibility tests in `useWakeLock.test.tsx`. Tests assert: (a) resume() is called when visibility→visible AND engineRef exists, (b) resume() is NOT called when engineRef is null (after stop()), (c) silent absorption when resume() rejects, (d) resume() is NOT called when document.visibilityState is 'hidden' (defensive).
- **D-16:** **No App-level integration test required for audio resume.** The hook test is sufficient because the visibility-resume path is fully internal (D-08: hook surface unchanged → App.tsx is unchanged → no integration seam to exercise). Mirrors the Phase 5 split where the wake-lock unit tests in `useWakeLock.test.tsx` carry most of the coverage and the App.wakeLock.test.tsx tests cover only the request/release call-site integration. The audio resume in 5.1 has no equivalent App-level call sites.

### UAT
- **D-17:** **Manual UAT runs on real Safari for both fixes.**
  - Audio resume: real iPhone Xs Max iOS 18.7.8 Safari, 5+ min running session, lock 30 s, unlock, audio cues resume within ≤1 phase boundary. Append to `05.1-UAT.md` (or equivalent name per planner) with device + iOS version + observed behavior.
  - Orb visual: Safari desktop screenshot at peak inhale (orb at MAX_SCALE) showing inner orb meeting outer ring. Compare against the existing `05-04-UAT-LOG.md` Finding 1 screenshot baseline. Optional: capture iOS Safari at peak inhale too — same fix, same render path.
- **D-18:** **Phase 1–5 automated tests must all stay green.** Success-criterion 3 ("All Phase 1–5 automated tests remain green; no regressions in Chromium, Firefox, or jsdom test runs") is a goal-backward gate. The new audio test additions are net-add only; the orb CSS change is constrained by D-13 to not affect existing render paths.

### Claude's Discretion
The following implementation details are explicitly left to research/planning. The decisions above bound the design space; the planner picks within those bounds:
- Exact form of `audioEngine.resume()` — whether the method short-circuits on `closed === true` (probably yes, mirrors `scheduleLeadIn`'s `if (closed) return`) and whether it returns `Promise<void>` or `Promise<boolean>` (either acceptable; D-08 says no public hook return surface, and D-04 says caller doesn't branch on it — return type is for testing ergonomics).
- Whether the outer-ring CSS fix uses option (1) content-box width/height or option (2) negative inset. Both satisfy D-10. Planner picks based on which works cleaner across Safari + Chromium + Firefox in the test suite, with explicit cross-browser checks (the Plan 02-02 test pattern is precedent for cross-browser CSS verification).
- Whether the test file is `useAudioCues.resume.test.tsx` (new file) or appended to the existing `useAudioCues.test.tsx`. Same decision Phase 5 made for `useWakeLock.test.tsx` (single file). Either is fine; planner picks based on file size growth.
- Whether `console.debug` lines are added in the resume failure path. D-09 makes this optional.
- Whether the visibility listener uses `document.visibilityState === 'visible'` or `!document.hidden`. Both equivalent; planner picks for symmetry with `useWakeLock.ts:82`.

</decisions>

<canonical_refs>
## Canonical References

**Downstream agents MUST read these before planning or implementing.**

### Product Scope And Constraints
- `.planning/PROJECT.md` — Audio constraint ("Browser audio should be generated or bundled locally for v1"), no-medical-claims and no-PWA bounds, hands-off mobile core-value statement.
- `.planning/ROADMAP.md` §Phase 5.1 — Defines Phase 5.1 goal, fixed scope boundary (`Depends on: Phase 5`), `Requirements: AUDI-01, AUDI-02, GUID-01 (carry-forward — no new requirement IDs)`, the three success criteria, and the "Origin" pointer to UAT findings.
- `.planning/STATE.md` — Notes "Phase 5 closed; Phase 5.1 inserted next" and Phase 5 carry-forward gap status.

### Requirements
- `.planning/REQUIREMENTS.md` — `AUDI-01` (User can hear generated soft gong/bowl-like cues aligned to inhale and exhale phase changes — currently breaks after iOS lock/unlock; Phase 5.1 restores it), `AUDI-02` (User can mute audio cues and still use the visual guide — unchanged but must not regress), `GUID-01` (User can follow a polished breathing animation synchronized to the current inhale/exhale phase — orb-to-ring visual gap is the carry-forward defect on Safari).

### Carrying Forward From Prior Phases
- `.planning/phases/02-visual-guide-accessible-responsive-interface/02-CONTEXT.md` — D-04 outer/inner reference rings at MAX/MIN_SCALE boundaries (the visual contract Phase 5.1 must preserve), D-06 reduced-motion fixed-mid-scale crossfade (must not regress), IN-01 TS↔CSS scale-constant sync rule.
- `.planning/phases/03-optional-generated-audio-cues/03-CONTEXT.md` — D-08 unmute-waits-for-boundary (the precedent for D-05 no-make-up-cue here), D-09 user-gesture AC construction (unchanged — resume does NOT need a gesture), D-10 silent visuals-only fallback (the precedent for D-09 silent absorb here), D-11 AC close on every End path (unchanged — resume doesn't add a new lifecycle path), D-13/D-14 dual-anchor + lead-in scheduling (must not be touched per D-06 above), Pitfall 2 dual-anchor invariant + Pitfall 9 cancelAndHoldAtTime fallback.
- `.planning/phases/05-mobile-hands-off-resilience/05-CONTEXT.md` — D-03 visibility re-acquire on `visibilitychange→visible` (the structural template for D-01 here), D-04 ref-gated re-acquire (the template for D-03 here), D-05 silent absorption of re-acquire failures (the template for D-09 here), D-08 idempotent release (the template for D-04's idempotent resume here), D-13 vitest.setup.ts polyfill pattern (the template for D-14 here).
- `.planning/phases/05-mobile-hands-off-resilience/05-04-UAT-LOG.md` Findings 1 + 2 — primary defect documentation. Finding 1 is the orb visual gap (D-10/D-11 resolution); Finding 2 is the iOS audio silent-after-unlock (D-01..D-09 resolution).

### Web APIs And Browser Behavior
- MDN `Web Audio API` — `AudioContext.state` ('suspended' | 'running' | 'closed'), `AudioContext.resume()` returns `Promise<void>`, behavior of suspended ACs (currentTime stops advancing, scheduled cues do NOT fire even when their scheduled instants pass — they're held until resume). iOS Safari auto-suspends AC on display sleep / page hidden; resume requires either a fresh user gesture OR (per WebKit changes 16.4+) a programmatic call from a visibility-handler in many cases.
- MDN `Page Visibility API` — same as Phase 5: `document.visibilityState`, `visibilitychange` event. Used here for resume on tab-visible. Same handler placement as Phase 5 D-03/D-04.

### Files Touched (For Verification)
- `src/audio/audioEngine.ts` — D-07 adds `resume()` method; everything else unchanged.
- `src/hooks/useAudioCues.ts` — D-01 / D-02 / D-03 / D-04 / D-05 add visibility listener; public surface (D-08) unchanged.
- `src/hooks/useAudioCues.test.tsx` (or sibling) — D-15 adds resume tests.
- `src/components/BreathingShape.tsx` — D-10 / D-12 update outer ring sizing in BOTH render sites (`BreathingShapeBody` line ~70 and `BreathingShapeLeadIn` line ~140).
- `src/styles/theme.css` — D-10 may extend `.orb-ring--outer` rule with the box-sizing/inset compensation. D-13: scale tokens unchanged.
- `vitest.setup.ts` — D-14 extends FakeAudioContext with `state`, `resume()`, `_simulateSuspend()`.

</canonical_refs>

<code_context>
## Existing Code Insights

### Reusable Assets
- `src/hooks/useWakeLock.ts` is the **structural template** for the audio visibility-resume listener (D-01). It demonstrates: `useEffect([])` install + cleanup, ref-gated guard inside the handler (`wasAcquiredRef.current` → use `engineRef.current !== null` here), `if (document.visibilityState !== 'visible') return` early-out, silent-absorb posture inside the inner call. Follow the same shape verbatim — same lines, just swap `request()` for `audioEngine.resume()` and the gate ref name.
- `src/audio/audioEngine.ts` is the structural template for D-07 `resume()`. The existing `close()` method (line 172-179) shows the closed-flag short-circuit + try/catch posture. The new `resume()` follows the same shape but swaps `audioCtx.close()` for `audioCtx.resume()` and the operation has no irreversible side effect (matches D-04's idempotent-on-already-running posture).
- `vitest.setup.ts` FakeAudioContext extension precedent: Phase 5 D-13 added `navigator.wakeLock` polyfill following the existing `Object.defineProperty(window, '...', ...)` pattern (vitest.setup.ts:90-185 per Phase 5 5-CONTEXT). D-14 extends FakeAudioContext the same way — add `state` getter + `resume()` + `_simulateSuspend()` to the existing class.
- `src/components/BreathingShape.tsx` outer ring lives in two render sites: `BreathingShapeBody` (line 68-71) and `BreathingShapeLeadIn` (line 137-141). Both are the same `<span aria-hidden="true" className="orb-ring--outer absolute inset-0 rounded-full border-solid" />` markup — the fix applies identically to both.

### Established Patterns
- **Imperative resource hooks own their own visibility listener** — Phase 5 D-04 (`useWakeLock`) is the precedent. Phase 5.1 D-01 extends this to a second resource. Future readers see the same pattern in two places and the convention is locked.
- **Silent-fallback posture for browser-API failures** — Phase 3 D-10 + Phase 4 D-16/D-17 + Phase 5 D-09 + Phase 5.1 D-09. Established project pattern; do not re-litigate.
- **Single test seam in vitest.setup.ts** — Phase 3 FakeAudioContext + Phase 4 Storage polyfill + Phase 5 wakeLock polyfill all live in the same file with the same shape (`Object.defineProperty(window/navigator, ...)` with `configurable: true` for per-test overrides). Phase 5.1 D-14 extends this same file.
- **Match-pair sentinel guard for browser-event listeners** — Phase 5's `if (sentinelRef.current === sentinel)` check (`useWakeLock.ts:50`) is the template for any similar guard if we add a release-style event listener here. The audio resume path probably doesn't need it (resume doesn't have a paired "freshly-suspended" event we listen for in Phase 5.1), but the pattern is available.

### Integration Points
- **`src/hooks/useAudioCues.ts` (existing file):** D-01 / D-02 / D-03 / D-04 / D-05 add a new `useEffect([])` immediately below the existing unmount cleanup effect (lines 73-82). The new effect installs a `visibilitychange` listener, the listener checks `engineRef.current !== null` + `document.visibilityState === 'visible'`, then calls a new `engineRef.current.resume()` method (D-07). Cleanup removes the listener. The existing public surface (lines 154-163) is unchanged (D-08).
- **`src/audio/audioEngine.ts` (existing file):** D-07 adds a `resume(): Promise<void>` method to the AudioEngine interface (line 27-42) and its implementation in the `engine` object literal (lines 115-180). Body: `if (closed) return; try { await audioCtx.resume() } catch { /* D-09 */ }`.
- **`src/components/BreathingShape.tsx` (existing file):** D-10 / D-12 update the outer-ring `<span>` markup in both `BreathingShapeBody` (line 68-71) and `BreathingShapeLeadIn` (line 137-141). The change is local to the `className` and/or inline `style` attributes; structure unchanged.
- **`src/styles/theme.css` (existing file):** D-10 may extend the existing `.orb-ring--outer` rule (lines 68-71) with a `box-sizing: content-box;` declaration if the planner picks option (1). Option (2) keeps the change purely in `BreathingShape.tsx`.
- **`vitest.setup.ts` (existing file):** D-14 extends the existing FakeAudioContext class with `state` getter + `resume()` + `_simulateSuspend()`. No new top-level constructs.
- **New test file (or extension):** D-15 adds resume tests to `useAudioCues.test.tsx` (extension) or a new sibling `useAudioCues.resume.test.tsx`. Planner discretion (D-Claude's-Discretion #3).

### NOT Touching
- `src/app/App.tsx` — D-08 keeps the public hook surface unchanged → App.tsx requires zero edits. (Possible exception: if planner discovers a real integration seam during research, escalate via plan-checker rather than silently expanding scope.)
- `src/hooks/useWakeLock.ts` and `src/hooks/useWakeLock.test.tsx` — Phase 5 surfaces are unchanged.
- `src/hooks/useSessionEngine.ts`, `src/domain/`, `src/storage/`, `src/components/SessionControls.tsx`, `src/components/MuteToggle.tsx`, `src/components/EndSessionDialog.tsx`, `src/components/ResetStatsDialog.tsx`, `src/components/StatsFooter.tsx`, `src/components/SettingsForm.tsx`, `src/components/SessionReadout.tsx` — none of these need to change for either fix.
- The Phase 3 dual-anchor invariant (App.tsx Plan 03-04 boundary effect) — D-06 explicitly preserves this.
- The Phase 2 reduced-motion contract — D-13 explicitly preserves this.

</code_context>

<specifics>
## Specific Ideas

- The user explicitly chose **`useAudioCues` hook ownership** over `audioEngine` ownership and over inline App.tsx (D-01 / D-02). Reason: structural symmetry with Phase 5 `useWakeLock`. Two hooks, same shape, future readers see the convention twice. The engine module currently has zero `document.*` access and adding a listener there would break that boundary.
- The user explicitly chose **`engineRef.current !== null` gating** over `appPhase`-based gating and over `audioCtx.state === 'suspended'` reading (D-03 / D-04). Reason: ref ownership matches Phase 5 D-04 pattern. Hook owns its own signal; doesn't depend on caller state.
- The user explicitly chose **wait-for-next-boundary** over make-up cue and over re-anchor (D-05 / D-06). Reason: exact Phase 3 D-08 mirror (unmute waits for boundary). No new scheduling code, no audible glitch, no risk to Phase 3 dual-anchor invariant.
- The user explicitly chose **outer-ring sizing compensation** over `MAX_SCALE` buffer and over SVG circle replacement (D-10). Reason: leaves the orb scale constants and the Phase 2 reduced-motion math untouched. The TS↔CSS sync contract documented in IN-01 stays intact. Minimal blast radius.
- The user explicitly chose **inner ring untouched** (D-11). Reason: UAT only flagged the outer ring, and the inner ring already uses an explicit `width/height` percent so it doesn't share the bug's root cause. Don't fix what isn't broken.
- The user explicitly chose **FakeAudioContext extension + jsdom test coverage** over manual-UAT-only and over a separate App-level integration test (D-14 / D-15 / D-16). Reason: every prior browser-API fallback has automated coverage (Phase 3 D-10, Phase 4 D-16/D-17, Phase 5 D-09); skipping here would be a regression. Hook-level test is sufficient because the public surface doesn't change.
- The user explicitly chose **manual Safari UAT for the orb fix** over Playwright/visual-regression tooling and over grep-only (D-17). Reason: the bug only manifests in Safari rendering; jsdom can't exercise box-model arithmetic. Adding Playwright for one cosmetic fix is out of proportion. Real-device screenshot is the cheapest deterministic verification.

</specifics>

<deferred>
## Deferred Ideas

- **Android Chrome S2 carry-forward UAT** — tracked in `05-04-UAT-LOG.md` Gap 1 + `05-UAT.md` Test 3 + `05-SECURITY.md` AR-05-01. Closed by physical-device run, not by Phase 5.1 code work. Append to the existing 05 logs when device available.
- **MOBL-03 user-visible Wake Lock fallback explanation** — explicitly v2 (per `REQUIREMENTS.md`). Phase 5 D-10 / Phase 5.1 D-09 keep the silent posture.
- **MOBL-04-style user-visible audio-resume failure explanation** — not in REQUIREMENTS.md, not requested. If iOS rejects `resume()` (some older iOS / locked-down profiles), the user sees no audio. Same silent posture as D-09; could be revisited in a future "audio fallback explanation" requirement if user feedback warrants.
- **Pause/resume during a session** — out of scope (`SESS-06` is v2). Phase 5.1 does not introduce a `pause()` API on the engine.
- **Volume control** — out of scope (`AUDI-03` is v2). Engine `setMuted` is unchanged.
- **Make-up cue on resume** — explicitly rejected for Phase 5.1 (D-05). May be revisited in v2 if user feedback shows the silent gap is too long. Would require integrating with the App.tsx boundary effect, which Phase 5.1 explicitly avoids.
- **Visual regression test infrastructure (Playwright / Chromatic / Percy)** — explicitly deferred (D-17 picked manual UAT). Could be revisited if Phase 6+ introduces more visual-fidelity work; for one cosmetic fix it's out of proportion.
- **AudioContext resume on user gesture (e.g., re-tap) instead of visibilitychange** — not the chosen path. iOS lock/unlock doesn't generate a user gesture in the page; the visibility-listener path is the only programmatic option that doesn't require UI changes.
- **Switching the orb scale animation to CSS keyframes / web-animations API** — not in scope. The current `transform: scale()` + `transition` combo is correct on Chromium and the Safari fix is structural (ring sizing), not animation-mechanism.
- **Forrest learning content / claim-safe positioning** — Phase 6, untouched.

</deferred>

---

*Phase: 05.1-hands-off-resilience-polish*
*Context gathered: 2026-05-10*
