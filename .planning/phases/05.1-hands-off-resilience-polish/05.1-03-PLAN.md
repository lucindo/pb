---
phase: 05.1-hands-off-resilience-polish
plan: 03
type: execute
wave: 2
depends_on:
  - 05.1-01
  - 05.1-02
files_modified:
  - .planning/phases/05.1-hands-off-resilience-polish/05.1-UAT.md
autonomous: false
requirements:
  - AUDI-01
  - AUDI-02
  - GUID-01
tags:
  - uat
  - manual
  - safari
  - regression
must_haves:
  truths:
    - "On a real iPhone Xs Max iOS 18.7.8 Safari, after starting a 5+ min running session, locking the device for ≥30s, and unlocking, audio cues resume within ≤1 phase boundary."
    - "On Safari desktop at peak inhale, the inner orb visually meets the dashed outer reference ring with no perceptible gap (compared against the existing 05-04-UAT-LOG.md Finding 1 baseline)."
    - "AUDI-02 mute behavior is intact on real Safari — toggling mute mid-session silences the next cue and unmute waits for the next phase boundary (no make-up cue)."
    - "Phase 1–5 success criteria are not regressed: the full automated test suite passes locally before the manual UAT begins, and the manual sweep confirms no UI/timing regressions on the target devices."
  artifacts:
    - path: ".planning/phases/05.1-hands-off-resilience-polish/05.1-UAT.md"
      provides: "Manual UAT log capturing real-Safari verification of both Phase 5.1 fixes (D-17) and a Phase 1–5 regression confirmation (D-18)."
      contains: "iPhone"
  key_links:
    - from: ".planning/phases/05.1-hands-off-resilience-polish/05.1-UAT.md"
      to: ".planning/phases/05-mobile-hands-off-resilience/05-04-UAT-LOG.md"
      via: "cross-references Findings 1+2 baseline"
      pattern: "05-04-UAT-LOG"
    - from: "05.1-UAT.md"
      to: "05.1-01-SUMMARY.md and 05.1-02-SUMMARY.md"
      via: "explicit reference to the implementation work being validated"
      pattern: "05\\.1-0[12]-SUMMARY"
---

<objective>
Validate the two Phase 5.1 fixes on real Safari devices (D-17) and confirm zero regressions in Phase 1–5 (D-18). The bug only manifests in real Safari rendering / iOS audio lifecycle — jsdom cannot exercise either the box-model arithmetic (orb fix) or the iOS lock-screen AC suspend (audio fix). This plan is the deterministic real-device verification step.

Purpose: Goal-backward closure of all three Phase 5.1 success criteria from ROADMAP.md §Phase 5.1:
1. Audio cues resume after a manual lock/unlock cycle on iOS Safari.
2. Orb visually meets the outer reference ring on Safari desktop.
3. Phase 1–5 automated tests stay green; no regressions.

Output: A `05.1-UAT.md` log appended (or created) with device/version metadata, observed behavior, and a regression-suite confirmation. Pass-through of all four `must_haves.truths` is the gate.
</objective>

<execution_context>
@$HOME/.claude-personal-personal/get-shit-done/workflows/execute-plan.md
@$HOME/.claude-personal-personal/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/05.1-hands-off-resilience-polish/05.1-CONTEXT.md
@.planning/phases/05.1-hands-off-resilience-polish/05.1-01-PLAN.md
@.planning/phases/05.1-hands-off-resilience-polish/05.1-02-PLAN.md
@.planning/phases/05-mobile-hands-off-resilience/05-04-UAT-LOG.md
@.planning/phases/05-mobile-hands-off-resilience/05-UAT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run the full automated test suite and confirm Phase 1–5 green (D-18)</name>
  <files>(no source files modified; this task gates the manual UAT)</files>
  <read_first>
    - .planning/phases/05.1-hands-off-resilience-polish/05.1-CONTEXT.md (D-18)
    - .planning/phases/05.1-hands-off-resilience-polish/05.1-01-SUMMARY.md (must exist — this task runs in Wave 2 after Plan 01)
    - .planning/phases/05.1-hands-off-resilience-polish/05.1-02-SUMMARY.md (must exist — this task runs in Wave 2 after Plan 02)
  </read_first>
  <action>
    Run the full project test suite from the repo root. The expected behavior:
    - `npx vitest run` exits 0.
    - The new resume-path tests (Plan 01 Task 4) appear in the output as 4 additional `✓` marks under `useAudioCues — visibility resume (Phase 5.1 D-01..D-09)`.
    - All Phase 1–5 tests (useSessionEngine, useWakeLock, audioEngine, App.audio, App.persistence, App.wakeLock, useAudioCues original cases, BreathingShape, EndSessionDialog, ResetStatsDialog, StatsFooter, MuteToggle, SessionControls, settings/storage units, etc.) still pass.
    - `npx tsc --noEmit -p .` exits 0.

    Capture exact counts in the UAT log:
    - Total test count BEFORE Phase 5.1 implementation (read from `git stash` of `npx vitest run --reporter=verbose | grep -c "✓"` against pre-plan HEAD, OR estimate from the prior summaries — the precise number is less important than "no test count went DOWN").
    - Total test count AFTER Phase 5.1 implementation = pre-count + 4 (the 4 new resume tests).
    - Any test that now fails is a regression — do NOT proceed to Task 2.

    If the regression suite fails, escalate via plan-checker before manual UAT. Do NOT attempt to "fix forward" on this task — capture the failing test name, the expected vs actual, and stop.
  </action>
  <verify>
    <automated>npx vitest run &amp;&amp; npx tsc --noEmit -p .</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run` exits 0
    - `npx tsc --noEmit -p .` exits 0
    - The 4 new resume-path tests are visible in the test output (verify with `npx vitest run src/hooks/useAudioCues.test.tsx --reporter=verbose 2>&amp;1 | grep -c "visibility resume"` >= 4)
    - No previously-green test from Phase 1–5 is now red
  </acceptance_criteria>
  <done>
    Full automated suite green; Phase 5.1 net-add tests visible; no Phase 1–5 regressions; ready for manual UAT.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Manual UAT — iOS Safari audio resume after lock/unlock (D-17)</name>
  <what-built>
    Phase 5.1 Plan 01 added a `visibilitychange→visible` listener inside `useAudioCues` that calls `audioEngine.resume()` whenever an engine is alive. Goal: AUDI-01 audio cues resume automatically after the user manually locks the iPhone for ≥30s and unlocks, mid-session. The fix was tested in jsdom (Plan 01 Task 4 — 4 unit tests pass) but the real iOS Safari AudioContext lifecycle can only be exercised on a physical device.
  </what-built>
  <how-to-verify>
    Real device required: **iPhone Xs Max iOS 18.7.8 Safari** (matches the original UAT baseline in `05-04-UAT-LOG.md` Finding 2). If the original device is unavailable, an iPhone running iOS ≥16.4 Safari is acceptable — record the substitute model + iOS version in the log.

    1. Pull the latest Phase 5.1 commits onto the dev server. Open the app at the project's local dev URL on the iPhone in Safari (Tailscale / local network / ngrok — same setup as Plan 05-04 UAT).
    2. Configure: BPM = 5, Ratio = 40:60, Duration = 5 min (these match the Plan 05-04 baseline; any setup is fine, but a 5-min session ensures you have time for the lock window).
    3. Tap **Start session**. Wait for the 3-2-1 lead-in to complete and the first **In** bowl cue to play. Confirm audio is audible at this baseline.
    4. Wait for at least one full inhale-exhale cycle so you hear both the In bowl and the Out bowl. Audio is confirmed working at t=0.
    5. **Manually lock the phone** (side button). Keep it locked for at least **30 seconds** (timer the gap with another device).
    6. **Unlock the phone** (face ID / passcode / side button). Safari should still be in the foreground (do NOT switch apps).
    7. Observe within the next phase boundary (≤ 1 inhale-exhale cycle, i.e., ≤ ~12 seconds at 5 BPM):
       - **Expected:** the next In or Out bowl cue plays automatically, no user interaction needed. The orb continues animating from where it logically should be (Phase 1 timing math is wall-clock based — orb position never paused).
       - **Bug-state baseline (pre-fix):** the orb continues but audio is silent for the rest of the session — this is what `05-04-UAT-LOG.md` Finding 2 captured.
    8. Continue the session for at least 30 more seconds to confirm cues keep playing on every boundary (not just one one-shot resume).
    9. Toggle Mute mid-session and confirm AUDI-02 still works: muting silences the next cue, unmute waits for the next boundary (no make-up cue per D-05). This is a regression check.
    10. End the session via the End/Reset control. Confirm the AC closes cleanly (no console error visible in remote-debug — optional if Safari devtools-bridge is connected).

    Append observations to `.planning/phases/05.1-hands-off-resilience-polish/05.1-UAT.md` using this structure:

    ```markdown
    # Phase 5.1 UAT Log

    Date: YYYY-MM-DD
    Tester: <name>
    App build: <git short SHA at the start of the session>

    ## Test 1 — iOS Safari audio resume after lock/unlock (D-17, AUDI-01 / AUDI-02)

    **Device:** iPhone Xs Max iOS 18.7.8 Safari (or substitute — record exactly)
    **Session config:** BPM=5, Ratio=40:60, Duration=5min
    **Lock duration:** 30s (or actual; record exactly)

    **Result:** PASS / FAIL
    **Observed audio behavior on unlock:** [exactly what happened in the next ≤1 phase boundary]
    **Mute toggle (AUDI-02 regression check):** [unchanged / regressed]
    **End-session cleanup:** [clean / errors observed]
    **Notes / surprises:** [anything else]
    ```

    Failure mode: If the next cue does NOT play within 1 phase boundary, this is a bug. Capture the exact silence duration, whether the orb continued advancing, and whether closing/reopening the tab fixes it. Then escalate via plan-checker — do NOT mark the phase complete.
  </how-to-verify>
  <resume-signal>Type "approved" with the device + iOS version in the UAT log filled in, OR describe failure mode in detail.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Manual UAT — Safari desktop orb-to-outer-ring visual gap (D-17)</name>
  <what-built>
    Phase 5.1 Plan 02 changed the outer reference ring's positioning in BOTH `BreathingShapeBody` and `BreathingShapeLeadIn` from `inset-0` to `inset: '-1.5px'` so the border-box outer edge sits 1.5px beyond the 100% container, matching the orb at `scale(MAX_SCALE = 1.0)` on Safari. Goal: GUID-01 polished animation — the inner orb visually meets the dashed outer reference ring at peak inhale on Safari desktop.
  </what-built>
  <how-to-verify>
    **Device:** Safari desktop (any recent macOS Safari ≥ 17). Optional secondary: real iOS Safari (same render path) and Chromium (regression check that the fix did not break previously-correct rendering).

    1. Open the app in Safari desktop at the local dev URL.
    2. Configure any session: BPM=5, Ratio=50:50 is convenient because peak-inhale is held for a long fraction of the cycle. (50:50 is supported per BREA-02.)
    3. Tap **Start session**. Wait for the 3-2-1 lead-in. During the lead-in:
       - **Expected:** the outer reference ring sits 1.5px outside the 100%-sized container. The lead-in orb (locked at MID_SCALE = 0.79) is visibly INSIDE both rings, with the outer-ring fix matching the body fix (D-12). No mid-countdown jitter as the lead-in transitions to the active orb.
    4. Wait for the first In phase. As the orb scales from MIN_SCALE to MAX_SCALE, watch the outer ring at peak inhale:
       - **Expected (PASS):** the orb's outer edge visually coincides with the dashed outer ring's INNER edge (or sits slightly inside, touching). NO 1.5–2px gap.
       - **Bug-state baseline (pre-fix):** a thin gap between the orb edge and the outer ring at peak inhale — this is what `05-04-UAT-LOG.md` Finding 1 captured.
    5. Take a screenshot at peak inhale. Save into `.planning/phases/05.1-hands-off-resilience-polish/screenshots/safari-desktop-peak-inhale-postfix.png` (create the screenshots dir if absent).
    6. Compare against the baseline screenshot referenced in `05-04-UAT-LOG.md` Finding 1 (if a baseline file path is recorded there) OR against the current Chromium rendering (open a second browser to the same session).
    7. **Optional but recommended:** run the same check on real iOS Safari at peak inhale. Same fix, same render path — the iPhone screenshot is a useful add to the log.
    8. Reduced-motion regression sweep (cheap to run):
       - In macOS System Settings → Accessibility → Display, enable **Reduce motion**.
       - Reload the app. Start a session. The orb should be locked at MID_SCALE (Phase 2 D-06 contract) and crossfade between In/Out gradients (Phase 2 D-07).
       - **Expected:** no scale animation; the orb sits inside both rings with no gap (the outer-ring fix is independent of the orb scale).
       - Disable reduce-motion when done.

    Append to `.planning/phases/05.1-hands-off-resilience-polish/05.1-UAT.md`:

    ```markdown
    ## Test 2 — Safari desktop orb-to-outer-ring visual (D-17, GUID-01)

    **Device:** macOS Safari <version>
    **Session config:** BPM=5, Ratio=50:50

    **Result at peak inhale:** PASS (orb meets ring) / FAIL (gap visible)
    **Lead-in render check:** [outer ring matches body — D-12 verified / mismatched]
    **Reduced-motion check:** [orb at MID_SCALE, no animation, no ring gap / regressed]
    **Screenshot:** [path]
    **Optional iOS Safari screenshot:** [path or N/A]
    **Notes:** [Chromium comparison, any cross-browser surprises]
    ```

    Failure mode: If a gap is still visible on Safari, capture the screenshot + browser version + zoom level. The fix arithmetic is `inset: -1.5px` paired with `border-width: 1.5px` in theme.css — if the gap is approximately 1.5px the discretion option (1) `box-sizing: content-box` may need to be tried instead. Escalate via plan-checker before re-implementing.
  </how-to-verify>
  <resume-signal>Type "approved" with the screenshot path and result recorded, OR describe failure mode and escalate.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Phase 1–5 regression sweep on real Safari + finalize UAT log (D-18)</name>
  <what-built>
    Phase 5.1 success-criterion 3 requires Phase 1–5 success criteria to remain intact. Task 1 confirmed automated tests are green; this task confirms the manual phase acceptance criteria are not regressed on real Safari.
  </what-built>
  <how-to-verify>
    Run a 10-minute manual smoke through the most regression-prone surfaces in real Safari (desktop OR iOS — pick one; iOS is more thorough because it covers both fixes simultaneously):

    1. **Phase 1 (timing):** Start a 5-minute timed session. Verify the BPM/Ratio steppers hide on start (Phase 2 D-???). Verify the timer counts down. Use the End-session dialog to abort early — confirm `<dialog>` opens, focus lands on **Keep going** (Phase 2 Plan 02-04), Esc cancels, backdrop click closes.
    2. **Phase 2 (visual):** Confirm the orb crossfades between In/Out gradients on phase change. Confirm the phase label is readable inside the orb. Tab through controls — focus rings visible.
    3. **Phase 3 (audio):** Toggle the MuteToggle on/off mid-session. Confirm muting silences cues; unmute waits for the next boundary (no make-up cue — Phase 3 D-08). End session — confirm AC closes (no audible tail beyond the natural ramp).
    4. **Phase 4 (storage):** Reload the app after step 3. Confirm BPM/Ratio/Duration/Mute restored to last values (LOCL-01). Open StatsFooter — confirm "1 session" or "N sessions" + total minutes (LOCL-02). Open Reset stats — confirm dialog (LOCL-03).
    5. **Phase 5 (wake lock):** Start a session. On a supported browser (Chrome desktop, iOS Safari ≥16.4), the screen should not auto-dim. End session — wake lock releases.
    6. **Phase 5.1 specific:** Re-confirm Test 1 (audio resume) and Test 2 (orb visual) PASS markers from earlier tasks.

    Append to `.planning/phases/05.1-hands-off-resilience-polish/05.1-UAT.md`:

    ```markdown
    ## Test 3 — Phase 1–5 regression sweep (D-18)

    **Device:** [device + browser]
    **Phase 1 timing + EndSessionDialog:** PASS / FAIL [notes]
    **Phase 2 orb + a11y:** PASS / FAIL [notes]
    **Phase 3 audio cues + mute:** PASS / FAIL [notes]
    **Phase 4 storage restore + stats:** PASS / FAIL [notes]
    **Phase 5 wake lock:** PASS / FAIL [notes]

    ## Sign-off

    Phase 5.1 acceptance: PASS / FAIL
    All three success criteria from ROADMAP §Phase 5.1 are met / NOT met.
    Outstanding gaps: [none / list]
    ```

    Document carry-forward acks (if any) in the same file. Phase 5.1 closes only when:
    - Test 1 = PASS (or documented Safari-version-specific blocker that user explicitly accepts as carry-forward).
    - Test 2 = PASS.
    - Test 3 = PASS for all phases.
    - Sign-off block written.

    Note: the Phase 5 S2 Android Chrome carry-forward (tracked in `05-04-UAT-LOG.md` Gap 1) is explicitly out of scope for Phase 5.1 — do NOT block Phase 5.1 sign-off on it.
  </how-to-verify>
  <resume-signal>Type "approved" with the sign-off block filled in, OR list any FAIL items for plan-checker escalation.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

This plan is documentation-only — appending observations to a UAT log file under `.planning/`. No source code, no production assets touched.

**No threat surface:**
- No new code paths, no DOM event handlers, no network calls, no storage writes (beyond a markdown log inside `.planning/` which is checked into the repo and reviewed in PR).
- The manual UAT exercises real-device behavior — those interactions are on the user's own device, with no privileged escalation.
- Risk profile inherits from the implementation plans (05.1-01 has audio-resume threats; 05.1-02 has none). This plan adds no new threats.

Per the security_gate in the planner context: "vitest.setup.ts: test-only code, never ships to prod" — analogous reasoning applies here. The UAT log is a verification artifact, not a runtime surface.
</threat_model>

<verification>
- `.planning/phases/05.1-hands-off-resilience-polish/05.1-UAT.md` exists.
- Test 1 (iOS audio resume), Test 2 (Safari orb visual), Test 3 (Phase 1–5 regression) sections all present, all marked PASS or with explicit user-acknowledged carry-forward.
- Sign-off block present and completed.
- `npx vitest run` and `npx tsc --noEmit -p .` both exit 0 from Task 1.
- All four `must_haves.truths` are demonstrably observed and recorded in the UAT log.
</verification>

<success_criteria>
- The three Phase 5.1 success criteria from ROADMAP.md §Phase 5.1 are demonstrated PASS on real Safari:
  1. Audio cues resume automatically after iOS Safari lock/unlock cycle ≥30s (no end+restart needed).
  2. Orb at peak inhale meets outer reference ring on Safari desktop with no perceptible gap.
  3. Phase 1–5 automated tests green AND manual regression sweep on real Safari shows no regressions.
- 05.1-UAT.md captures device + version metadata for reproducibility.
</success_criteria>

<output>
After completion, create `.planning/phases/05.1-hands-off-resilience-polish/05.1-03-SUMMARY.md` documenting:
- Devices used (iPhone model + iOS version, macOS Safari version).
- Test results: Test 1 / Test 2 / Test 3 — PASS / FAIL.
- Screenshot paths.
- Any carry-forward gaps acknowledged by the user.
- Phase 5.1 sign-off marker.

Then update `.planning/STATE.md`:
- Set `status: Phase 5.1 closed; Phase 6 next`
- Update `completed_phases` from 4 to 6 (Phase 5 was already closed; Phase 5.1 closes here — but verify count interpretation against the existing STATE.md format).
- Update `last_activity: YYYY-MM-DD -- Phase 5.1 closed; iOS audio resume + Safari orb fix shipped`

Then update `.planning/ROADMAP.md`:
- Mark Phase 5.1 as `[x]` complete in the phases list.
- Set the Phase 5.1 detail status row in the bottom table to `Complete | YYYY-MM-DD`.
- Mark all four Phase 5.1 plans as `[x]` in the Plans list.
</output>
</content>
</invoke>