---
phase: 36
plan: 05
type: execute
wave: 5
depends_on: [36-04]
files_modified:
  - .planning/phases/28-phone-install-banner/28-01-SUMMARY.md
  - .planning/phases/28-phone-install-banner/28-03-SUMMARY.md
autonomous: true
requirements: [HOUSE-08]
tags: [housekeeping, summary, drift-fix, git-recovery]
must_haves:
  truths:
    - "`28-01-SUMMARY.md` exists at `.planning/phases/28-phone-install-banner/` (recovered from `f81f08f^` per 36-PATTERNS §16-17)"
    - "`28-03-SUMMARY.md` exists at the same location"
    - "The `field count` drift in 28-01-SUMMARY.md is corrected against the canonical code (`src/storage/installDismissed.ts`, `src/content/strings.ts`)"
    - "The superseded `SafariNavigator` reference in 28-03-SUMMARY.md `decisions:` is replaced with the current iOS-detect symbol name (per `src/lib/iosDetect.ts` or wherever the canonical implementation lives)"
  artifacts:
    - path: ".planning/phases/28-phone-install-banner/28-01-SUMMARY.md"
      provides: "Phase 28 plan 01 summary — recovered from f81f08f^ and drift-corrected"
    - path: ".planning/phases/28-phone-install-banner/28-03-SUMMARY.md"
      provides: "Phase 28 plan 03 summary — recovered from f81f08f^ and drift-corrected"
  key_links:
    - from: "f81f08f^ (pre-deletion HEAD)"
      to: ".planning/phases/28-phone-install-banner/28-0{1,3}-SUMMARY.md"
      via: "git show recovery"
      pattern: "git show f81f08f\\^:.planning/phases/28-phone-install-banner/28-0[13]-SUMMARY.md"
---

<objective>
Fix the Phase 28 SUMMARY drift (HOUSE-08): recover the deleted `28-01-SUMMARY.md`
and `28-03-SUMMARY.md` from `f81f08f^` (per 36-PATTERNS §16-17, both files were
deleted in commit `f81f08f docs(phase-30): add security threat verification` and
are absent from both HEAD and the working tree), then apply the two drift fixes
flagged by CONTEXT canonical_refs — the `field count` mismatch in 28-01 and the
superseded `SafariNavigator` reference in 28-03's `decisions:` block. Code in
`main` is canonical; the SUMMARYs must reflect it.

Purpose: Close HOUSE-08. Phase 36 success criterion #2 demands the 28-01/28-03
SUMMARY drift is corrected.

Output: Two recovered + drift-corrected SUMMARY files placed at
`.planning/phases/28-phone-install-banner/` (per CONTEXT specifics line 141 and
36-PATTERNS §16-17 placement decision option-a — v1.4 archive backfill stays
deferred). Single commit per CONTEXT D-05 commit #5.
</objective>

<execution_context>
@$HOME/.claude-personal-personal/get-shit-done/workflows/execute-plan.md
@$HOME/.claude-personal-personal/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/36-housekeeping-bookkeeping-reset/36-CONTEXT.md
@.planning/phases/36-housekeeping-bookkeeping-reset/36-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Recover 28-01 and 28-03 SUMMARYs from f81f08f^</name>
  <files>
    .planning/phases/28-phone-install-banner/28-01-SUMMARY.md
    .planning/phases/28-phone-install-banner/28-03-SUMMARY.md
  </files>
  <read_first>
    .planning/phases/36-housekeeping-bookkeeping-reset/36-PATTERNS.md
  </read_first>
  <action>
    Both files are absent from HEAD and the working tree (deleted in commit `f81f08f`). Recover from the pre-deletion commit. Create the parent dir if missing: `mkdir -p .planning/phases/28-phone-install-banner`. Then write the recovered content into place:

    `git show f81f08f^:.planning/phases/28-phone-install-banner/28-01-SUMMARY.md > .planning/phases/28-phone-install-banner/28-01-SUMMARY.md`
    `git show f81f08f^:.planning/phases/28-phone-install-banner/28-03-SUMMARY.md > .planning/phases/28-phone-install-banner/28-03-SUMMARY.md`

    Confirm both files exist and contain non-empty content. Do NOT stage yet — Task 2 and Task 3 edit the recovered files before staging in Task 4. If `git show f81f08f^:…` errors (path mismatch), surface the error rather than guessing the path — re-run `git log --diff-filter=D --name-only --all -- '.planning/phases/28-*'` to locate the deletion commit and rerun.
  </action>
  <verify>
    <automated>[ -s .planning/phases/28-phone-install-banner/28-01-SUMMARY.md ] && [ -s .planning/phases/28-phone-install-banner/28-03-SUMMARY.md ]</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/28-phone-install-banner/28-01-SUMMARY.md` exists and is non-empty
    - `.planning/phases/28-phone-install-banner/28-03-SUMMARY.md` exists and is non-empty
    - Each file's content matches the `f81f08f^` blob byte-for-byte (verify with `git show f81f08f^:.planning/phases/28-phone-install-banner/28-01-SUMMARY.md | diff - .planning/phases/28-phone-install-banner/28-01-SUMMARY.md` returning empty)
    - Files are NOT yet staged
  </acceptance_criteria>
  <done>Both 28-01 and 28-03 SUMMARY files are recovered to disk from `f81f08f^` and pending drift fixes in Task 2/3.</done>
</task>

<task type="auto">
  <name>Task 2: Fix 28-01-SUMMARY.md `field count` drift</name>
  <files>.planning/phases/28-phone-install-banner/28-01-SUMMARY.md</files>
  <read_first>
    .planning/phases/28-phone-install-banner/28-01-SUMMARY.md
    src/storage/installDismissed.ts
    src/content/strings.ts
  </read_first>
  <action>
    Per 36-PATTERNS §16: the "field count" drift may refer to either (a) the SUMMARY's `metrics.files_changed:` value vs the actual count of files in `key_files.created` + `key_files.modified`, OR (b) a count of `UiStrings.install` strings/fields vs what was actually added. Read the recovered `28-01-SUMMARY.md` to identify which numeric claim is stale. Cross-check against the canonical implementation: count `UiStrings.install` entries in `src/content/strings.ts` and the actual exports in `src/storage/installDismissed.ts`. Edit ONLY the stale numeric claim to match the canonical code; do NOT rewrite unrelated sections. If both candidate counts already match (no drift exists), document that in the plan summary and leave the file as-recovered. If the drift is in a body paragraph rather than a frontmatter `metrics:` field, the edit may touch the body — that is allowed for HOUSE-08 (body content correction is the explicit deliverable, unlike HOUSE-05/07 which were frontmatter-only).
  </action>
  <verify>
    <automated>test -f .planning/phases/28-phone-install-banner/28-01-SUMMARY.md && [ -s .planning/phases/28-phone-install-banner/28-01-SUMMARY.md ]</automated>
  </verify>
  <acceptance_criteria>
    - `28-01-SUMMARY.md` exists with the field-count claim matching the canonical code in `src/storage/installDismissed.ts` and `src/content/strings.ts`
    - If `metrics.files_changed:` is the drifted field: it equals the sum of `key_files.created` + `key_files.modified` entries
    - If no drift exists, the file is unchanged from `f81f08f^` recovery and the SUMMARY documents the no-op outcome
    - No unrelated edits made to the file
  </acceptance_criteria>
  <done>28-01-SUMMARY.md's field-count claim matches the canonical code in `main` (or no drift was found and the no-op is documented).</done>
</task>

<task type="auto">
  <name>Task 3: Fix 28-03-SUMMARY.md superseded SafariNavigator reference</name>
  <files>.planning/phases/28-phone-install-banner/28-03-SUMMARY.md</files>
  <read_first>
    .planning/phases/28-phone-install-banner/28-03-SUMMARY.md
    src/lib/iosDetect.ts
    src/components/InstallBanner.tsx
  </read_first>
  <action>
    The SUMMARY's `decisions:` list contains a row referencing the now-superseded `SafariNavigator` type (per 36-PATTERNS §16-17). Confirm whether the name still exists by running `grep -rn "SafariNavigator" src/` — if absent, the SUMMARY's decision row is stale. Read `src/lib/iosDetect.ts` (or wherever the current iOS-detect symbol lives — locate via `grep -rn "isIOS\\|iosDetect\\|navigator.standalone" src/`) to identify the current canonical API name. Edit ONLY the stale `decisions:` row in the SUMMARY's frontmatter to cite the current API/hook name — rewriting the quoted string so it accurately reflects the implementation in `main`. Do not introduce any code changes. Do not touch other `decisions:` rows.
  </action>
  <verify>
    <automated>[ "$(grep -c "SafariNavigator" .planning/phases/28-phone-install-banner/28-03-SUMMARY.md)" = "0" ] || [ "$(grep -c "SafariNavigator" src/)" -gt "0" ]</automated>
  </verify>
  <acceptance_criteria>
    - If `grep -rn "SafariNavigator" src/` returns zero matches (the symbol is gone from code): `28-03-SUMMARY.md` no longer references `SafariNavigator` in any `decisions:` row
    - If the symbol still exists in code: the SUMMARY is left as-is and the SUMMARY notes "SafariNavigator still in code — no edit needed"
    - The edit is confined to the single `decisions:` row in frontmatter (no body changes; no other decision rows touched)
    - The replacement quoted string accurately describes the current iOS-detect implementation in `main`
  </acceptance_criteria>
  <done>28-03-SUMMARY.md's `decisions:` block cites the canonical current iOS-detect API (or documents that no edit was needed because the SafariNavigator symbol still exists in code).</done>
</task>

<task type="auto">
  <name>Task 4: Stage and commit the drift fix (D-05 commit #5)</name>
  <files>
    .planning/phases/28-phone-install-banner/28-01-SUMMARY.md
    .planning/phases/28-phone-install-banner/28-03-SUMMARY.md
  </files>
  <read_first>
    .planning/phases/36-housekeeping-bookkeeping-reset/36-CONTEXT.md
  </read_first>
  <action>
    Stage the two recovered + drift-corrected SUMMARY files by explicit path. Commit with subject `docs(36): correct 28-01/28-03 SUMMARY drift — field count + superseded SafariNavigator ref (HOUSE-08)` (use HEREDOC for the body). Body notes the recovery from `f81f08f^` and the specific drift fixes applied (or no-op outcomes). Do NOT amend. Phase 28's wider archive (move to `.planning/milestones/v1.4-phases/`) stays deferred per CONTEXT §Deferred — out of scope for this plan.
  </action>
  <verify>
    <automated>git log -1 --format=%s | grep -q "HOUSE-08" && [ "$(git diff HEAD~1 --name-only | grep -c "28-0[13]-SUMMARY.md$")" = "2" ]</automated>
  </verify>
  <acceptance_criteria>
    - HEAD commit subject matches `docs(36): correct 28-01/28-03 SUMMARY drift — field count + superseded SafariNavigator ref (HOUSE-08)`
    - HEAD commit touches exactly two files: `28-01-SUMMARY.md` and `28-03-SUMMARY.md`
    - HEAD commit is not an amend
    - `git ls-files .planning/phases/28-phone-install-banner/` shows the two SUMMARY files are tracked
  </acceptance_criteria>
  <done>Commit #5 lands cleanly with both Phase 28 SUMMARYs recovered, drift-corrected, and tracked.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | `.planning/` docs only. Read-only inspection of `src/` to verify canonical code state. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36-05-01 | Tampering | Recovered SUMMARY content | accept | Recovery uses `git show f81f08f^:…` which pulls the exact pre-deletion blob; Task 1 verify cross-checks byte-for-byte via `diff`. |
| T-36-05-02 | Spoofing | Drift-fix edits | mitigate | Task 2 and Task 3 are explicitly gated on reading the canonical `src/` files first; the SUMMARY's claim must match code, not the other way around. |
</threat_model>

<verification>
- `28-01-SUMMARY.md` and `28-03-SUMMARY.md` exist and are tracked (Task 1 + 4)
- Field-count drift fixed or documented as no-op (Task 2)
- SafariNavigator reference removed if symbol is gone from code; otherwise documented as no-op (Task 3)
- Single commit lands cleanly on top of HEAD
</verification>

<success_criteria>
- HOUSE-08 closed: 28-01 and 28-03 SUMMARYs exist in the working tree, match canonical code, and the deleted-blob → tracked-file transition is recorded in commit history
</success_criteria>

<output>
After completion, create `.planning/phases/36-housekeeping-bookkeeping-reset/36-05-SUMMARY.md`.
</output>
