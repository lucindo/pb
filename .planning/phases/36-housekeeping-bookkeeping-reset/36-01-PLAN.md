---
phase: 36
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/30-multi-practice-architecture-switcher/
  - .planning/phases/31-navi-kriya-engine-session/
  - .planning/phases/32-learn-localization/
  - .planning/phases/33-close-gap-practice-02-resonant-settings-read-write-split-bra/
  - .planning/phases/34-stretch-as-a-distinct-practice/
  - .planning/phases/35-flute-cue-timbre-replace-chime/
autonomous: true
requirements: []
tags: [housekeeping, git-restore, v1.5]
must_haves:
  truths:
    - "The 6 v1.5 phase directories (30/31/32/33/34/35) are present in the working tree (no longer ` D` in git status)"
    - "Each restored directory matches HEAD byte-for-byte (no incidental edits)"
  artifacts:
    - path: ".planning/phases/30-multi-practice-architecture-switcher/30-04-SUMMARY.md"
      provides: "restored Phase 30 final-plan summary"
    - path: ".planning/phases/31-navi-kriya-engine-session/31-VERIFICATION.md"
      provides: "restored Phase 31 verification (status: human_needed before re-flip in 36-03)"
    - path: ".planning/phases/31-navi-kriya-engine-session/31-HUMAN-UAT.md"
      provides: "operator UAT evidence underpinning HOUSE-05 re-flip"
    - path: ".planning/phases/32-learn-localization/32-VERIFICATION.md"
      provides: "restored Phase 32 verification (audit table source for HOUSE-06)"
    - path: ".planning/phases/33-close-gap-practice-02-resonant-settings-read-write-split-bra/33-01-SUMMARY.md"
      provides: "restored Phase 33 final-plan summary (HOUSE-06 target)"
    - path: ".planning/phases/34-stretch-as-a-distinct-practice/34-11-SUMMARY.md"
      provides: "restored Phase 34 final-plan summary (HOUSE-06 target)"
    - path: ".planning/phases/35-flute-cue-timbre-replace-chime/35-02-SUMMARY.md"
      provides: "restored Phase 35 final-plan summary (HOUSE-06 target)"
  key_links:
    - from: "working tree"
      to: "HEAD"
      via: "git restore"
      pattern: "git restore .planning/phases/3[0-5]-*"
---

<objective>
Restore the six v1.5 phase directories (30/31/32/33/34/35) from `HEAD` so the
downstream backfill plans (36-02..36-06) and the archive move (36-07) have files
to edit and `git mv`. The directories are currently in `working-tree-deleted`
state (status ` D`) — restoring them is a non-destructive `git restore` operation
that brings them back from `HEAD` without touching git history.

Purpose: Per CONTEXT D-01, the phase follows a single-batch flow — restore upfront,
backfill in place, archive at the end. This plan executes the restore step.
Output: 6 restored phase directories under `.planning/phases/3{0..5}-*` with all
artifacts present (PLAN/SUMMARY per plan plus phase-level VALIDATION/SECURITY/
VERIFICATION/CONTEXT/etc.).
</objective>

<execution_context>
@$HOME/.claude-personal-personal/get-shit-done/workflows/execute-plan.md
@$HOME/.claude-personal-personal/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/36-housekeeping-bookkeeping-reset/36-CONTEXT.md
@.planning/phases/36-housekeeping-bookkeeping-reset/36-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restore v1.5 phase directories from HEAD</name>
  <files>
    .planning/phases/30-multi-practice-architecture-switcher/
    .planning/phases/31-navi-kriya-engine-session/
    .planning/phases/32-learn-localization/
    .planning/phases/33-close-gap-practice-02-resonant-settings-read-write-split-bra/
    .planning/phases/34-stretch-as-a-distinct-practice/
    .planning/phases/35-flute-cue-timbre-replace-chime/
  </files>
  <read_first>
    .planning/phases/36-housekeeping-bookkeeping-reset/36-CONTEXT.md
    .planning/phases/36-housekeeping-bookkeeping-reset/36-PATTERNS.md
  </read_first>
  <action>
    Run `git restore .planning/phases/30-multi-practice-architecture-switcher/ .planning/phases/31-navi-kriya-engine-session/ .planning/phases/32-learn-localization/ .planning/phases/33-close-gap-practice-02-resonant-settings-read-write-split-bra/ .planning/phases/34-stretch-as-a-distinct-practice/ .planning/phases/35-flute-cue-timbre-replace-chime/` to undo the working-tree deletions for all six v1.5 phase directories. This pulls files back from `HEAD` (no merge, no conflict, no staging interaction — the `D` entries flip to absent in `git status`). Then run `git status --porcelain | grep -c '^ D .planning/phases/3[0-5]'` and confirm the count is 0 (every v1.5 ` D` entry is gone). Do NOT stage anything in this plan — the restore is a working-tree-only operation. Phase 28 is NOT restored here (Phase 28 is v1.4 and HOUSE-08 handles its own recovery in plan 36-05). Per CONTEXT D-01, the working tree is intentionally restored + dirty after this plan; it returns to clean only after plan 36-07's `git mv` to the archive.
  </action>
  <verify>
    <automated>test -d .planning/phases/30-multi-practice-architecture-switcher && test -d .planning/phases/31-navi-kriya-engine-session && test -d .planning/phases/32-learn-localization && test -d .planning/phases/33-close-gap-practice-02-resonant-settings-read-write-split-bra && test -d .planning/phases/34-stretch-as-a-distinct-practice && test -d .planning/phases/35-flute-cue-timbre-replace-chime && [ "$(git status --porcelain | grep -c '^ D .planning/phases/3[0-5]')" = "0" ] && [ -f .planning/phases/31-navi-kriya-engine-session/31-HUMAN-UAT.md ] && [ -f .planning/phases/34-stretch-as-a-distinct-practice/34-11-SUMMARY.md ]</automated>
  </verify>
  <acceptance_criteria>
    - All 6 directories `.planning/phases/3[0-5]-*` exist on disk
    - `git status --porcelain | grep -c '^ D .planning/phases/3[0-5]'` returns `0`
    - `.planning/phases/31-navi-kriya-engine-session/31-HUMAN-UAT.md` exists (HOUSE-05 evidence file)
    - `.planning/phases/34-stretch-as-a-distinct-practice/34-11-SUMMARY.md` exists (HOUSE-06 target)
    - `.planning/phases/35-flute-cue-timbre-replace-chime/35-02-SUMMARY.md` exists (HOUSE-06 target)
    - No files were modified by the restore (each file matches the tracked HEAD blob — `git diff` shows no staged or unstaged changes for restored paths)
    - No files outside `.planning/phases/3[0-5]-*` were touched
  </acceptance_criteria>
  <done>The six v1.5 phase directories are restored from HEAD and the working tree carries zero ` D` entries for paths under `.planning/phases/3[0-5]-*`.</done>
</task>

<task type="auto">
  <name>Task 2: Stage and commit the restore</name>
  <files>
    .planning/phases/30-multi-practice-architecture-switcher/
    .planning/phases/31-navi-kriya-engine-session/
    .planning/phases/32-learn-localization/
    .planning/phases/33-close-gap-practice-02-resonant-settings-read-write-split-bra/
    .planning/phases/34-stretch-as-a-distinct-practice/
    .planning/phases/35-flute-cue-timbre-replace-chime/
  </files>
  <read_first>
    .planning/phases/36-housekeeping-bookkeeping-reset/36-CONTEXT.md
  </read_first>
  <action>
    The `git restore` in Task 1 cleared the ` D` working-tree-delete entries. There is nothing to commit — the index already matches HEAD for these paths after the restore. Verify by running `git status --porcelain .planning/phases/3[0-5]-*` and confirming no output. Do NOT create a no-op commit. This task exists to make the no-commit decision explicit and traceable: per CONTEXT D-05 commit-granularity, commit #1 was conceptualized as `docs(36): restore v1.5 phase directories from git (HOUSE-10 prep)` — but `git restore` alone produces no diff, so the conceptual "commit 1" collapses into a no-op. The actual archive landing happens in plan 36-07 (`git mv` of all 6 dirs), which IS the real HOUSE-10 commit. Skip ahead.
  </action>
  <verify>
    <automated>[ -z "$(git status --porcelain .planning/phases/30-multi-practice-architecture-switcher/ .planning/phases/31-navi-kriya-engine-session/ .planning/phases/32-learn-localization/ .planning/phases/33-close-gap-practice-02-resonant-settings-read-write-split-bra/ .planning/phases/34-stretch-as-a-distinct-practice/ .planning/phases/35-flute-cue-timbre-replace-chime/)" ]</automated>
  </verify>
  <acceptance_criteria>
    - `git status --porcelain` shows no output for the 6 restored directories
    - No commit was created in this plan
    - The decision to skip commit #1 is recorded in the SUMMARY (the conceptual commit collapses to a no-op; HOUSE-10 lands at plan 36-07)
  </acceptance_criteria>
  <done>The restore is verified clean against HEAD and no commit was created. Subsequent plans can edit the restored files in place.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Plan touches only docs in `.planning/` — no code surface, no I/O, no network |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36-01-01 | Tampering | `.planning/phases/3[0-5]-*` | accept | `git restore` pulls byte-for-byte from `HEAD` — no executable surface, no user-visible state. If restored files diverge from `HEAD` blob, the verify-step grep catches it. |
| T-36-01-02 | Information Disclosure | git index | accept | Working-tree-only operation; nothing pushed; no secrets introduced. |
</threat_model>

<verification>
- `git status --porcelain` shows zero ` D` entries for paths under `.planning/phases/3[0-5]-*` after Task 1
- Six directories exist on disk (Task 1 verify)
- No commits created in this plan (Task 2 verify)
- Downstream plans 36-02..36-06 can read/edit the restored files
</verification>

<success_criteria>
- All six v1.5 phase directories are present in the working tree
- The restored files match `HEAD` exactly (no incidental edits)
- The working tree is intentionally "restored + dirty" as set up for the backfill plans (per CONTEXT D-01)
- HOUSE-10 (archive step) is set up but NOT completed in this plan — completion lives in 36-07
</success_criteria>

<output>
After completion, create `.planning/phases/36-housekeeping-bookkeeping-reset/36-01-SUMMARY.md`.
</output>
