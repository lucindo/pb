---
phase: 36
plan: 08
type: execute
wave: 8
depends_on: [36-07]
files_modified:
  - CLAUDE.md
  - .claude/skills/spike-findings-hrv/
  - .gitignore
autonomous: true
requirements: [HOUSE-11, HOUSE-12, HOUSE-13]
tags: [housekeeping, cleanup, gitignore, claude-config]
must_haves:
  truths:
    - "Root `CLAUDE.md` no longer exists in the repo"
    - "`.claude/skills/spike-findings-hrv/` directory and all its tracked contents (21 tracked files — verified via `git ls-files .claude/skills/spike-findings-hrv/ | wc -l` at revision time) are removed from the repo"
    - "`.gitignore` contains a `.claude/` entry on its own line"
    - "After this plan, an `ls .claude/` on disk may still show files (the skill is currently auto-loaded for this session and persists locally), but `git ls-files .claude/` returns empty (nothing tracked)"
    - "Future `.claude/`-rooted files cannot be re-committed by accident — `git status` no longer flags them as untracked"
  artifacts:
    - path: ".gitignore"
      provides: "gitignore with .claude/ entry"
      contains: ".claude/"
  key_links:
    - from: "git index"
      to: "CLAUDE.md + .claude/skills/spike-findings-hrv/"
      via: "git rm -r"
      pattern: "git rm.*CLAUDE\\.md|git rm.*-r.*spike-findings-hrv"
    - from: ".gitignore"
      to: ".claude/* per-project files"
      via: "single-line ignore entry"
      pattern: "^\\.claude/$"
---

<objective>
Drop dead `.claude/` content from the repo (HOUSE-11, HOUSE-12) and prevent
future per-project Claude Code files from being re-committed by accident
(HOUSE-13). This is the final docs/config commit before the green-gate + push
in plan 36-09.

Purpose: Close HOUSE-11, HOUSE-12, HOUSE-13. Phase 36 success criterion #4
demands all three changes land: `CLAUDE.md` removed, `.claude/skills/spike-findings-hrv/`
removed (21 tracked files — verified at revision time via
`git ls-files .claude/skills/spike-findings-hrv/ | wc -l = 21`; the original
CONTEXT said "22 tracked files" but actual count is 21), `.claude/` in `.gitignore`.

The spike-findings skill is currently auto-loaded for this session; the planner
read it during planning per the planning_context. Removing the skill is fine —
its content was either absorbed into v1.5 phases already or codified in
`.planning/spikes/MANIFEST.md` Requirements (per HOUSE-12 wording).
`./CLAUDE.md` only pointed to that skill, so it goes too (HOUSE-11).

CONTEXT D-05's commit-granularity proposes commit #8 as either a single combined
commit OR a split. Per CONTEXT Claude's Discretion: "planner picks based on diff
readability". This plan ships ONE combined commit because the three items are
tightly coupled (CLAUDE.md only existed to point at the removed skill; the
gitignore entry prevents the just-removed dir from getting re-committed) and a
single commit reads cleaner than three for a coordinated cleanup.

Output: One combined commit per CONTEXT D-05 commit #8.
</objective>

<execution_context>
@$HOME/.claude-personal-personal/get-shit-done/workflows/execute-plan.md
@$HOME/.claude-personal-personal/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/36-housekeeping-bookkeeping-reset/36-CONTEXT.md
@.planning/phases/36-housekeeping-bookkeeping-reset/36-PATTERNS.md
@.gitignore
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Append `.claude/` to .gitignore (HOUSE-13)</name>
  <files>.gitignore</files>
  <read_first>
    .gitignore
    .planning/phases/36-housekeeping-bookkeeping-reset/36-CONTEXT.md
  </read_first>
  <action>
    Read the current `.gitignore` to confirm `.claude/` is not already present. Then append the literal line `.claude/` on its own line at file tail (with a leading blank line if the existing tail is not blank-terminated, matching the existing block structure — the current file has section comments like `# Editor directories and files` followed by entries; the new entry can ship as a new section `# Claude Code per-project files` + `.claude/` for readability, or simply appended at tail). Confirm with `grep -c '^\.claude/$' .gitignore` returning `1`. Do NOT modify any other entries in `.gitignore`. Do NOT add the entry inline within an existing section (keep it cleanly separated). Note: appending `.gitignore` BEFORE the `git rm -r .claude/skills/spike-findings-hrv/` in Task 2 is the right order — once the dir is ignored, `git rm` still removes tracked entries from the index, but the working-tree copies stay (per the `.gitignore` rules), which is the desired outcome (local skill files remain available; tracked git entries go away).
  </action>
  <verify>
    <automated>[ "$(grep -c '^\.claude/$' .gitignore)" = "1" ]</automated>
  </verify>
  <acceptance_criteria>
    - `.gitignore` contains exactly one line that is `.claude/` (no trailing space, no `*` glob, no nested-only `.claude/*`)
    - All previously-present `.gitignore` entries are preserved
    - The new entry is on its own line (not inline-appended to another entry)
  </acceptance_criteria>
  <done>`.gitignore` carries `.claude/` on its own line; existing entries are preserved.</done>
</task>

<task type="auto">
  <name>Task 2: Remove tracked CLAUDE.md and .claude/skills/spike-findings-hrv/ (HOUSE-11, HOUSE-12)</name>
  <files>
    CLAUDE.md
    .claude/skills/spike-findings-hrv/
  </files>
  <read_first>
    CLAUDE.md
    .claude/skills/spike-findings-hrv/SKILL.md
    .planning/phases/36-housekeeping-bookkeeping-reset/36-CONTEXT.md
  </read_first>
  <action>
    First, sanity-check the file count: `git ls-files .claude/skills/spike-findings-hrv/ | wc -l` should return `21` (revision-time fact; if the count has drifted by execute time, update the Task 3 verify threshold to match the new count). Then run `git rm CLAUDE.md` (single-file removal — HOUSE-11). Then run `git rm -r .claude/skills/spike-findings-hrv/` to remove the entire skill directory and its 21 tracked files (HOUSE-12). Confirm both commands succeeded by running `git status --porcelain | grep -E '^D\\s+(CLAUDE\\.md|\\.claude/skills/spike-findings-hrv/)' | wc -l` — expect 22 entries (1 for CLAUDE.md, 21 for spike-findings-hrv files). The working-tree copies may or may not stay depending on `git rm` behavior with `.gitignore`-matched entries (the dir is now ignored per Task 1, but `git rm` removes both index and working tree by default unless `--cached` is used) — per HOUSE-12 wording the entire dir should be removed, so plain `git rm -r` (not `--cached`) is correct. Verify the spike-findings dir is gone from the filesystem after the `git rm -r`: `test ! -d .claude/skills/spike-findings-hrv` should pass.
  </action>
  <verify>
    <automated>[ ! -f CLAUDE.md ] && [ ! -d .claude/skills/spike-findings-hrv ] && [ "$(git status --porcelain | grep -E '^D\s+CLAUDE\.md$' | wc -l | tr -d ' ')" = "1" ]</automated>
  </verify>
  <acceptance_criteria>
    - `CLAUDE.md` does not exist in the working tree
    - `.claude/skills/spike-findings-hrv/` does not exist in the working tree
    - `git status --porcelain` shows one `D  CLAUDE.md` entry (staged delete)
    - `git status --porcelain | grep -c "^D.*\\.claude/skills/spike-findings-hrv"` returns at least 21 (the 21 tracked files in the skill dir)
    - No other files are staged for deletion
  </acceptance_criteria>
  <done>CLAUDE.md and the spike-findings-hrv skill directory are removed from the working tree and staged for deletion.</done>
</task>

<task type="auto">
  <name>Task 3: Stage .gitignore change and commit (D-05 commit #8)</name>
  <files>
    .gitignore
    CLAUDE.md
    .claude/skills/spike-findings-hrv/
  </files>
  <read_first>
    .planning/phases/36-housekeeping-bookkeeping-reset/36-CONTEXT.md
  </read_first>
  <action>
    Stage the `.gitignore` edit by explicit path: `git add .gitignore`. The `git rm`s in Task 2 already staged the deletes. Commit with subject `docs(36): drop root CLAUDE.md + .claude/skills/spike-findings-hrv/ and gitignore .claude/ (HOUSE-11..13)` (use HEREDOC for the body). Body lists: (1) `CLAUDE.md` removed because it pointed to the spike-findings skill which is removed alongside; (2) `.claude/skills/spike-findings-hrv/` removed (21 files) because the skill's content was absorbed into v1.5 phases or codified in `.planning/spikes/MANIFEST.md`; (3) `.claude/` in `.gitignore` to prevent future per-project Claude Code files from being re-committed. Do NOT amend. Do NOT use `git add -A` — stage only `.gitignore` (the `git rm`s already staged the deletes). Confirm `git diff HEAD~1 --stat` shows: 1 modified file (`.gitignore`), 1 deleted file (`CLAUDE.md`), and 21 deleted files under `.claude/skills/spike-findings-hrv/`.
  </action>
  <verify>
    <automated>git log -1 --format=%s | grep -q "HOUSE-11..13" && [ "$(git diff HEAD~1 --name-status | grep -c '^D')" -ge "22" ] && [ "$(git diff HEAD~1 --name-status | grep -c '^M\s*\.gitignore$')" = "1" ]</automated>
  </verify>
  <acceptance_criteria>
    - HEAD commit subject matches `docs(36): drop root CLAUDE.md + .claude/skills/spike-findings-hrv/ and gitignore .claude/ (HOUSE-11..13)`
    - HEAD commit shows `.gitignore` modified (1 file)
    - HEAD commit shows at least 22 deletions (1 for `CLAUDE.md` + 21 for skill files)
    - HEAD commit is not an amend
    - `git ls-files | grep -c '^CLAUDE\\.md$'` returns `0`
    - `git ls-files | grep -c '^\\.claude/skills/spike-findings-hrv/'` returns `0`
    - `git check-ignore -q .claude/` exits 0 (the dir is now gitignored)
  </acceptance_criteria>
  <done>Commit #8 lands cleanly: CLAUDE.md gone, spike-findings-hrv skill gone, `.claude/` in `.gitignore`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Local repo cleanup. The removed files are docs/markdown only — no executable surface, no runtime impact on the deployed app. `.claude/` is per-project Claude Code config; ignoring it removes a possible accidental-commit footgun but introduces no security surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36-08-01 | Information Disclosure | `.claude/skills/spike-findings-hrv/` content | accept | The skill's content was already public (committed in HEAD); removal does not change disclosure surface. Its content is now codified in `.planning/spikes/MANIFEST.md` for any future reference. |
| T-36-08-02 | Tampering | `.gitignore` rule | mitigate | The new `.claude/` ignore rule is a single-line append; Task 1 verifies it via `grep -c '^\\.claude/$'`. It cannot mask tracked files (gitignore is index-aware; it only affects untracked-file decisions). |
| T-36-08-03 | Repudiation | Removed-file history | accept | `git log --follow CLAUDE.md` and `git log --diff-filter=D` continue to show the historical content; nothing is destroyed beyond recovery. |
| T-36-08-04 | Information Disclosure | Future `.claude/settings.local.json` or `.claude/scheduled_tasks.lock` content | mitigate | The new `.gitignore` rule explicitly covers these (per CONTEXT D-13: "covers scheduled_tasks.lock, settings.local.json, worktrees/, and any future Claude Code per-project files"). |
</threat_model>

<verification>
- `.gitignore` carries `.claude/` (Task 1 verify)
- `CLAUDE.md` and `.claude/skills/spike-findings-hrv/` are gone from working tree and index (Task 2 verify)
- Single commit lands cleanly with 1 modify + 22 deletes (Task 3 verify)
- `git check-ignore -q .claude/` exits 0 (the rule is active)
</verification>

<success_criteria>
- HOUSE-11 closed: `CLAUDE.md` removed
- HOUSE-12 closed: `.claude/skills/spike-findings-hrv/` (21 tracked files) removed
- HOUSE-13 closed: `.claude/` in `.gitignore`
- Phase 36 success criterion #4 fully closed
- Working tree is clean except for any in-progress Phase 36 planning artifacts (36-CONTEXT.md, 36-PATTERNS.md, 36-DISCUSSION-LOG.md, 36-{01..09}-PLAN.md / SUMMARY.md), which land at phase open + executor cadence
</success_criteria>

<output>
After completion, create `.planning/phases/36-housekeeping-bookkeeping-reset/36-08-SUMMARY.md`.
</output>
</content>
</invoke>