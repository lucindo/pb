---
phase: 36
plan: 09
type: execute
wave: 9
depends_on: [36-01, 36-02, 36-03, 36-04, 36-05, 36-06, 36-07, 36-08]
files_modified: []
autonomous: false
requirements: [HOUSE-14]
tags: [housekeeping, green-gate, push, milestone-baseline]
must_haves:
  truths:
    - "The full local green-gate (`tsc -b && eslint . && vite build && vitest run`) exits 0 once before the push"
    - "All Phase 36 commits (commit #2 through commit #8 plus the phase-open commits) are pushed to `origin/main`"
    - "After push, `git status` is clean and `git log origin/main..HEAD` returns empty (local HEAD === remote HEAD)"
    - "The CLAUDE.md removal and the `.claude/skills/spike-findings-hrv/` removal are visible on `origin/main`"
    - "The v1.5 backfill commits (HOUSE-01..09) land publicly so the GSD baseline is reset for v2.0"
  artifacts: []
  key_links:
    - from: "local HEAD"
      to: "origin/main"
      via: "git push origin main"
      pattern: "git push origin main"
    - from: "green-gate"
      to: "push"
      via: "single sequential run per D-11"
      pattern: "tsc.*&&.*eslint.*&&.*vite build.*&&.*vitest run"
---

<objective>
Run the full local green-gate ONCE, then push all Phase 36 commits to
`origin/main`. Per CONTEXT D-11, a single green-gate run before push is
sufficient because only commit #6 (HOUSE-09 test) touches `src/`; commits #2-#5,
#7, #8 are docs-only. Per CONTEXT D-12, on green-gate failure: add a fix-up
commit (e.g., `fix(36): correct HOUSE-09 test fixture`) — do NOT amend. Per
CONTEXT D-13, the push command is plain `git push origin main` with no
post-push verification step.

Purpose: Close HOUSE-14. Phase 36 success criterion #5 demands all Phase 36
commits are pushed to `origin/main` at phase close — the GSD baseline reset is
publicly visible before any v2.0 build work starts (Phase 37 onward).

Output: A single green-gate run exits 0, followed by a single `git push origin main`.
No new commits in this plan unless D-12's fix-up path is triggered.
</objective>

<execution_context>
@$HOME/.claude-personal-personal/get-shit-done/workflows/execute-plan.md
@$HOME/.claude-personal-personal/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/36-housekeeping-bookkeeping-reset/36-CONTEXT.md
@.planning/phases/36-housekeeping-bookkeeping-reset/36-PATTERNS.md
@package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run the full local green-gate</name>
  <files></files>
  <read_first>
    package.json
    .planning/phases/36-housekeeping-bookkeeping-reset/36-CONTEXT.md
    .planning/phases/36-housekeeping-bookkeeping-reset/36-PATTERNS.md
  </read_first>
  <action>
    Run the four green-gate commands sequentially. Per `package.json` scripts, the exact commands are: `npx tsc -b` (note: `build` script chains `tsc -b && vite build`, so running `npm run build` after `npx tsc -b` is allowed redundancy that catches any typescript-build-only failure separately), `npm run lint` (ESLint), `npm run build` (Vite + tsc), `npm run test:run` (Vitest single-run, NOT `npm test` which is a watch mode). All four must exit 0 in sequence. Capture each command's exit code; if any fails, do NOT proceed to push — branch to Task 2 (fix-up commit) instead. Per CONTEXT D-11 this is a single sequential run, not a loop. The repo has no CI gates beyond this local green-gate (per D-13), so this run is the canonical pre-push assurance.
  </action>
  <verify>
    <automated>npx tsc -b && npm run lint && npm run build && npm run test:run 2>&1 | tail -10 | grep -q "passed"</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc -b` exits 0
    - `npm run lint` exits 0 with no errors (warnings acceptable if any, but ESLint config in this repo treats key issues as errors)
    - `npm run build` exits 0 (Vite build artifact produced under `dist/`)
    - `npm run test:run` exits 0 with all tests passing including the HOUSE-09 block from 36-06
    - No fix-up commit needed (if any command fails, skip to Task 2)
  </acceptance_criteria>
  <done>All four green-gate commands exit 0; the local tree is push-ready.</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 2: ON FAILURE ONLY — fix-up commit and re-run gate (D-12 fallback)</name>
  <decision>
    Reached ONLY if Task 1's green-gate fails. Decide which Phase 36 commit
    introduced the regression (likely commit #6 if the failure is test-related;
    likely a backfilled VALIDATION.md if the failure is in a gap-filled test) and
    create a fix-up commit. Do NOT amend any prior Phase 36 commit (CONTEXT
    D-12). Do NOT use `--no-verify` or skip-hook flags.
  </decision>
  <context>
    Per CONTEXT D-12, the fix-up commit subject follows the same `<type>(36): …`
    pattern, e.g., `fix(36): correct HOUSE-09 test fixture` or `fix(36): adjust
    Phase 12 SECURITY.md threat register row`. After the fix-up commit lands,
    re-run the full green-gate (Task 1 sequence again). Only proceed to Task 3
    (push) when the green-gate exits 0.
  </context>
  <options>
    <option id="option-a">
      <name>Skip — green-gate already passed in Task 1, proceed to Task 3</name>
      <pros>The standard success path.</pros>
      <cons>None — this is the expected outcome.</cons>
    </option>
    <option id="option-b">
      <name>Add a `fix(36): …` commit, re-run green-gate, then push</name>
      <pros>Honors D-12 amend-avoidance preference.</pros>
      <cons>Adds one or more extra commits to Phase 36's history.</cons>
    </option>
  </options>
  <action>This task is the D-12 fallback. ONLY enter this task if Task 1's green-gate FAILED (any of tsc/lint/build/test exited non-zero). If Task 1 passed, select option-a and proceed directly to Task 3. If Task 1 failed: diagnose the failing command's output, identify the offending file (likely `src/storage/storage.test.ts` from 36-06, or a backfilled doc with a syntax issue), make the minimal correcting edit, stage and commit with subject `fix(36): {brief}` per CONTEXT D-12 (use HEREDOC for the body), then re-run the full green-gate from Task 1. Loop until the gate passes. NEVER amend a prior Phase 36 commit. NEVER use `--no-verify`. NEVER use `git push --force`.</action>
  <verify>
    <automated>echo "checkpoint task — verification is user resume signal, not automated"</automated>
  </verify>
  <done>Either Task 1's gate passed (option-a, no fix-up needed) OR a fix-up commit landed and a re-run green-gate exits 0 (option-b complete).</done>
  <resume-signal>Select: option-a (gate passed, proceed to push) or option-b (gate failed, fix-up needed — describe failure)</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Push Phase 36 commits to origin/main (D-13)</name>
  <files></files>
  <read_first>
    .planning/phases/36-housekeeping-bookkeeping-reset/36-CONTEXT.md
  </read_first>
  <action>
    Run `git push origin main`. Per CONTEXT D-13: no `--force`, no `--force-with-lease`, no `--no-verify`. The push is a plain fast-forward — the local `main` branch is ahead of `origin/main` by the Phase 36 commits (commit #2 through commit #8 from prior plans plus the phase-open commits) and no force-push or rebase is needed. After the push succeeds, confirm via `git log origin/main..HEAD` returning empty (local HEAD === remote HEAD) and `git status` showing nothing to commit, working tree clean. Per D-13, there is no post-push verification step beyond this — the repo has no CI; phase 36 closes when the push succeeds. If the push errors due to remote-ahead drift (non-fast-forward), do NOT force; surface the drift as a checkpoint and let the operator decide pull/rebase/merge strategy.
  </action>
  <verify>
    <automated>git fetch origin main 2>&1 && [ -z "$(git log origin/main..HEAD)" ] && [ -z "$(git status --porcelain)" ]</automated>
  </verify>
  <acceptance_criteria>
    - `git push origin main` exits 0
    - `git log origin/main..HEAD` returns empty after the push (local HEAD === remote HEAD)
    - `git status --porcelain` returns empty (working tree clean)
    - No `--force` or `--no-verify` flags used
  </acceptance_criteria>
  <done>All Phase 36 commits are visible on `origin/main`; local and remote are in sync; working tree is clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local repo → origin/main | `git push` crosses the local-to-remote boundary. The push exposes Phase 36's bookkeeping commits publicly. No new code surface ships — the only `src/` change is the HOUSE-09 test addition (pure migration regression; no runtime impact on the deployed app). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36-09-01 | Tampering | Push payload | mitigate | The single green-gate run in Task 1 (per D-11) verifies the entire local tree passes typecheck/lint/build/test before the push. The push is fast-forward only — no `--force` per D-13. |
| T-36-09-02 | Repudiation | Force-push history rewrite | mitigate | No `--force` / `--force-with-lease` per D-13. The fix-up-commit path (D-12) is also explicitly amend-avoiding — every adjustment is a new commit, not a rewrite of prior history. |
| T-36-09-03 | Spoofing | Hook bypass | mitigate | No `--no-verify` per D-13. If pre-push hooks exist, they run normally. |
| T-36-09-04 | Denial of Service | Network push failure | accept | Network errors surface to the operator via `git push`'s stderr; the plan halts at Task 3 if the push errors and waits for operator follow-up. No retry loop. |
| T-36-09-05 | Information Disclosure | Removed `.claude/skills/spike-findings-hrv/` content visible on origin | accept | The skill's content was already public (in HEAD); the push only changes the file's current-tree state. Removed content remains in git history for anyone with repo access — this is intentional. |
</threat_model>

<verification>
- Green-gate exits 0 once (Task 1 verify)
- `git push origin main` succeeds; local HEAD === origin/main HEAD (Task 3 verify)
- Working tree clean after push (Task 3 verify)
- If D-12 fix-up path triggered: re-run gate passes before push
</verification>

<success_criteria>
- HOUSE-14 closed: All Phase 36 commits visible on `origin/main`
- Phase 36 success criterion #5 fully closed
- Phase 36 closes when the push succeeds — per CONTEXT specifics, no Human UAT step for this phase (bookkeeping-only)
- The GSD baseline reset is publicly visible; Phase 37 (Stats UI removal) can start fresh
</success_criteria>

<output>
After completion, create `.planning/phases/36-housekeeping-bookkeeping-reset/36-09-SUMMARY.md`. This is the LAST plan of Phase 36 — its SUMMARY should also note the phase close (no separate `/gsd-verify-work` or `/gsd-secure-phase` for Phase 36 per CONTEXT specifics: "no new code surface — the only code change is a test").
</output>
