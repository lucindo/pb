---
phase: 36
plan: 02
type: execute
wave: 2
depends_on: [36-01]
files_modified:
  - .planning/milestones/v1.0.1-phases/12-assets-content-hygiene-cleanup/12-VALIDATION.md
  - .planning/milestones/v1.0.1-phases/12-assets-content-hygiene-cleanup/12-SECURITY.md
  - .planning/phases/33-close-gap-practice-02-resonant-settings-read-write-split-bra/33-VALIDATION.md
  - .planning/phases/35-flute-cue-timbre-replace-chime/35-VALIDATION.md
autonomous: true
requirements: [HOUSE-01, HOUSE-02, HOUSE-03, HOUSE-04]
tags: [housekeeping, validation, security, backfill]
must_haves:
  truths:
    - "Phase 12 has both `12-VALIDATION.md` and `12-SECURITY.md` files present (at the archive location)"
    - "Phase 33 has `33-VALIDATION.md` present in the restored phase dir"
    - "Phase 35 has `35-VALIDATION.md` present in the restored phase dir"
    - "All four backfilled artifacts carry frontmatter `status: verified` — the canonical terminal status emitted by the gsd-nyquist-auditor / gsd-security-auditor agents (cross-checked against `13-VALIDATION.md`, `34-VALIDATION.md`, `13-SECURITY.md`)"
  artifacts:
    - path: ".planning/milestones/v1.0.1-phases/12-assets-content-hygiene-cleanup/12-VALIDATION.md"
      provides: "Phase 12 Nyquist coverage table — backfilled retroactively"
      contains: "status: verified"
    - path: ".planning/milestones/v1.0.1-phases/12-assets-content-hygiene-cleanup/12-SECURITY.md"
      provides: "Phase 12 threat register — backfilled retroactively"
      contains: "status: verified"
    - path: ".planning/phases/33-close-gap-practice-02-resonant-settings-read-write-split-bra/33-VALIDATION.md"
      provides: "Phase 33 Nyquist coverage table — backfilled retroactively"
      contains: "status: verified"
    - path: ".planning/phases/35-flute-cue-timbre-replace-chime/35-VALIDATION.md"
      provides: "Phase 35 Nyquist coverage table — backfilled retroactively"
      contains: "status: verified"
  key_links:
    - from: "/gsd-validate-phase 12"
      to: "12-VALIDATION.md"
      via: "gsd-nyquist-auditor agent"
      pattern: "status: verified"
    - from: "/gsd-secure-phase 12"
      to: "12-SECURITY.md"
      via: "gsd-security-auditor agent"
      pattern: "status: verified"
---

<objective>
Backfill the four missing validation/security artifacts from the v1.x procedural
backlog: Phase 12 `VALIDATION.md`, Phase 12 `SECURITY.md`, Phase 33 `VALIDATION.md`,
Phase 35 `VALIDATION.md`. Each artifact is produced by invoking the canonical
orchestrator (`/gsd-validate-phase {N}` or `/gsd-secure-phase 12`) — the planner
does NOT hand-write the bodies; the orchestrator + auditor agents do (CONTEXT
D-08, D-09).

Purpose: Close HOUSE-01..04. Phase 36 success criterion #1 demands all three
VALIDATION.md files reach the canonical terminal status (`verified`) and Phase
12's SECURITY.md exists at `verified` with `threats_open: 0`.

**Status vocabulary note (planner-revised per checker feedback):** The gsd-nyquist-auditor
agent emits `status: draft → ready → verified` (with `approved` as the post-sign-off
end-state). It does NOT emit `status: passed`. Inspection of in-tree VALIDATION.md
files confirms this (`34-VALIDATION.md` uses `verified`; `13-VALIDATION.md` uses
`approved`; no file in the tree uses `passed`). The original CONTEXT D-10 wording
("`status: passed`") was an aspirational coinage, not a canonical auditor output —
this plan ships against the actual auditor vocabulary. The gate convention for
Phase 36 success criterion #1 is "the auditor has signed off" — `verified` is the
auditor's first-pass terminal state and satisfies the gate.

Output: 4 backfilled doc artifacts (2 under the v1.0.1 archive, 2 under the
restored v1.5 phase dirs), plus a single `docs(36): backfill Phase 12 VALIDATION
+ SECURITY and Phase 33/35 VALIDATION (HOUSE-01..04)` commit.
</objective>

<execution_context>
@$HOME/.claude-personal-personal/get-shit-done/workflows/execute-plan.md
@$HOME/.claude-personal-personal/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/36-housekeeping-bookkeeping-reset/36-CONTEXT.md
@.planning/phases/36-housekeeping-bookkeeping-reset/36-PATTERNS.md
@.planning/milestones/v1.0.1-phases/12-assets-content-hygiene-cleanup/12-01-PLAN.md
@.planning/milestones/v1.1-phases/13-inner-ring-ux-symmetry/13-VALIDATION.md
@.planning/milestones/v1.1-phases/13-inner-ring-ux-symmetry/13-SECURITY.md
@.planning/phases/34-stretch-as-a-distinct-practice/34-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Confirm orchestrator phase-dir resolution (pre-resolved decision)</name>
  <files></files>
  <read_first>
    .planning/phases/36-housekeeping-bookkeeping-reset/36-CONTEXT.md
    .planning/milestones/v1.0.1-phases/12-assets-content-hygiene-cleanup/12-01-PLAN.md
  </read_first>
  <action>
    Planner pre-resolved this at revision time (per W2 checker feedback): the
    `/gsd-validate-phase` and `/gsd-secure-phase` orchestrators resolve `phase_dir`
    via `gsd-sdk query init.phase-op {N}`, which transparently locates the phase
    directory regardless of whether it lives under `.planning/phases/{N}-*` OR
    `.planning/milestones/vX.Y-phases/{N}-*`. Verified by running
    `gsd-sdk query init.phase-op 12` → returns
    `phase_dir: .planning/milestones/v1.0.1-phases/12-assets-content-hygiene-cleanup`
    (i.e., the archive path is correctly picked up automatically — no flag, no
    temp restore, no hand-written fallback needed).

    Executor action in this task: run `gsd-sdk query init.phase-op 12` and confirm
    `phase_dir` resolves to the v1.0.1 archive path. If it does (expected), proceed
    to Tasks 2-5 using the standard `/gsd-validate-phase 12` and `/gsd-secure-phase 12`
    invocations — no plumbing tricks. If it does NOT (regression in the SDK), STOP
    and surface a checkpoint to the operator (this is the only remaining failure
    mode and is highly unlikely given the SDK's current behavior).
  </action>
  <verify>
    <automated>[ "$(gsd-sdk query init.phase-op 12 2>/dev/null | grep '"phase_dir"' | grep -c 'v1.0.1-phases/12-')" = "1" ]</automated>
  </verify>
  <acceptance_criteria>
    - `gsd-sdk query init.phase-op 12` returns JSON with `"phase_dir": ".planning/milestones/v1.0.1-phases/12-assets-content-hygiene-cleanup"`
    - No checkpoint pause needed (the path resolution is canonical; planner pre-confirmed)
    - Tasks 2-5 can invoke the standard orchestrators with phase number args only
  </acceptance_criteria>
  <done>The orchestrator path-resolution is confirmed canonical; downstream tasks invoke the standard commands.</done>
</task>

<task type="auto">
  <name>Task 2: Backfill Phase 12 VALIDATION.md (HOUSE-01)</name>
  <files>.planning/milestones/v1.0.1-phases/12-assets-content-hygiene-cleanup/12-VALIDATION.md</files>
  <read_first>
    .planning/milestones/v1.0.1-phases/12-assets-content-hygiene-cleanup/12-01-PLAN.md
    .planning/milestones/v1.1-phases/13-inner-ring-ux-symmetry/13-VALIDATION.md
    .planning/phases/36-housekeeping-bookkeeping-reset/36-CONTEXT.md
    .planning/phases/36-housekeeping-bookkeeping-reset/36-PATTERNS.md
  </read_first>
  <action>
    Invoke `/gsd-validate-phase 12` to generate `12-VALIDATION.md` at the archive path
    `.planning/milestones/v1.0.1-phases/12-assets-content-hygiene-cleanup/12-VALIDATION.md`
    (resolved automatically per Task 1). The auditor regenerates the Nyquist coverage
    table from `12-01-PLAN.md` must-haves. Frontmatter must carry `phase: 12`,
    `slug: assets-content-hygiene-cleanup`, `status: verified` (auditor's canonical
    terminal status — confirmed via grep of 34-VALIDATION.md and other in-tree
    files), `nyquist_compliant: true` (or `partial` with override block),
    `wave_0_complete: true`, `created: 2026-05-20` (per CONTEXT D-03). The body must
    open with the italic backfill notice
    `_Backfilled retroactively for Phase 12 (shipped 2026-05-12). Frontmatter `created: 2026-05-20` reflects backfill date; the audited code surface is the Phase 12 implementation present in `main`._`
    (verbatim format from 36-PATTERNS Shared Patterns §"Backfill body opener";
    ship date 2026-05-12 per v1.0.1 ROADMAP entry). Body section headings mirror
    `13-VALIDATION.md` (Test Infrastructure / Sampling Rate / Per-Task Verification
    Map / Wave 0 Requirements / Manual-Only Verifications / Validation Sign-Off).
    Per CONTEXT D-10 gap-fill policy: if the auditor surfaces uncovered Nyquist
    points, spawn `gsd-nyquist-auditor` with gap-filling enabled to write the
    missing test(s) and re-run validation in-phase. The final `12-VALIDATION.md`
    MUST end with `status: verified`. Note: gap-filling for Phase 12 means adding
    tests to `src/` — if that happens, the new test files are an additional staged
    change in this plan's commit; if no gap, commit is docs-only.
  </action>
  <verify>
    <automated>test -f .planning/milestones/v1.0.1-phases/12-assets-content-hygiene-cleanup/12-VALIDATION.md && grep -q "^status: verified$" .planning/milestones/v1.0.1-phases/12-assets-content-hygiene-cleanup/12-VALIDATION.md && grep -q "Backfilled retroactively for Phase 12" .planning/milestones/v1.0.1-phases/12-assets-content-hygiene-cleanup/12-VALIDATION.md</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/milestones/v1.0.1-phases/12-assets-content-hygiene-cleanup/12-VALIDATION.md` exists
    - Frontmatter contains `status: verified`
    - Frontmatter contains `created: 2026-05-20`
    - Frontmatter contains `phase: 12`
    - Body opens with the italic line `_Backfilled retroactively for Phase 12 (shipped 2026-05-12)…_`
    - Body carries the six section headings listed in `13-VALIDATION.md` (Test Infrastructure / Sampling Rate / Per-Task Verification Map / Wave 0 Requirements / Manual-Only Verifications / Validation Sign-Off)
    - If gap-filling added tests, those test files exist under `src/` and `npm run test:run` exits 0
  </acceptance_criteria>
  <done>Phase 12 `VALIDATION.md` exists at the archive path with `status: verified`, the backfill notice, and the canonical section structure.</done>
</task>

<task type="auto">
  <name>Task 3: Backfill Phase 12 SECURITY.md (HOUSE-02)</name>
  <files>.planning/milestones/v1.0.1-phases/12-assets-content-hygiene-cleanup/12-SECURITY.md</files>
  <read_first>
    .planning/milestones/v1.0.1-phases/12-assets-content-hygiene-cleanup/12-01-PLAN.md
    .planning/milestones/v1.1-phases/13-inner-ring-ux-symmetry/13-SECURITY.md
    .planning/phases/36-housekeeping-bookkeeping-reset/36-CONTEXT.md
    .planning/phases/36-housekeeping-bookkeeping-reset/36-PATTERNS.md
    src/content/learnContent.ts
    src/domain/settings.ts
  </read_first>
  <action>
    Invoke `/gsd-secure-phase 12` to generate `12-SECURITY.md` at the archive path
    (resolved automatically per Task 1). Per CONTEXT D-09, the auditor REGENERATES
    the threat model from the implemented Phase 12 code surface (assets/content/cleanup
    — favicon.svg, the `learnContent.ts` Amazon URL, the `settings.ts` predicate
    extraction); the inline threats in `12-01-PLAN.md` are advisory only — the
    regenerated register may surface threats the original PLAN missed. Frontmatter
    must carry `phase: 12`, `slug: assets-content-hygiene-cleanup`, `status: verified`,
    `threats_open: 0`, `asvs_level: 1`, `created: 2026-05-20`. Body opens with the
    italic backfill notice `_Backfilled retroactively for Phase 12 (shipped 2026-05-12)…_`.
    Sections mirror `13-SECURITY.md`: Trust Boundaries / Threat Register (STRIDE table) /
    Accepted Risks Log / Security Audit Trail / Sign-Off. The threat register entries
    must reference concrete components (e.g., `src/content/learnContent.ts:{line}` for
    the Amazon URL, `public/favicon.svg` for the asset surface, `src/domain/settings.ts`
    for the predicate). End-state MUST be `status: verified` with `threats_open: 0` —
    if the auditor surfaces unmitigated threats, escalate via checkpoint rather than
    ship a `status: open` file.
  </action>
  <verify>
    <automated>test -f .planning/milestones/v1.0.1-phases/12-assets-content-hygiene-cleanup/12-SECURITY.md && grep -q "^status: verified$" .planning/milestones/v1.0.1-phases/12-assets-content-hygiene-cleanup/12-SECURITY.md && grep -q "^threats_open: 0$" .planning/milestones/v1.0.1-phases/12-assets-content-hygiene-cleanup/12-SECURITY.md && grep -q "Backfilled retroactively for Phase 12" .planning/milestones/v1.0.1-phases/12-assets-content-hygiene-cleanup/12-SECURITY.md</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/milestones/v1.0.1-phases/12-assets-content-hygiene-cleanup/12-SECURITY.md` exists
    - Frontmatter contains `status: verified`
    - Frontmatter contains `threats_open: 0`
    - Frontmatter contains `asvs_level: 1`
    - Frontmatter contains `created: 2026-05-20`
    - Body opens with the italic backfill notice
    - Body carries section headings: Trust Boundaries / Threat Register / Accepted Risks Log / Security Audit Trail / Sign-Off (matching `13-SECURITY.md`)
    - Threat register references concrete Phase 12 components by file path (favicon.svg, learnContent.ts, settings.ts)
  </acceptance_criteria>
  <done>Phase 12 `SECURITY.md` exists at the archive path with `status: verified`, `threats_open: 0`, and a STRIDE register regenerated from the actual Phase 12 code surface.</done>
</task>

<task type="auto">
  <name>Task 4: Backfill Phase 33 VALIDATION.md (HOUSE-03)</name>
  <files>.planning/phases/33-close-gap-practice-02-resonant-settings-read-write-split-bra/33-VALIDATION.md</files>
  <read_first>
    .planning/phases/33-close-gap-practice-02-resonant-settings-read-write-split-bra/33-01-PLAN.md
    .planning/phases/34-stretch-as-a-distinct-practice/34-VALIDATION.md
    .planning/phases/36-housekeeping-bookkeeping-reset/36-CONTEXT.md
    .planning/phases/36-housekeeping-bookkeeping-reset/36-PATTERNS.md
  </read_first>
  <action>
    Invoke `/gsd-validate-phase 33` against the restored Phase 33 dir (restored by
    plan 36-01) to generate `33-VALIDATION.md`. The orchestrator finds the Phase 33
    dir via `gsd-sdk query init.phase-op 33`. Frontmatter: `phase: 33`,
    `slug: close-gap-practice-02-resonant-settings-read-write-split-bra`,
    `status: verified` (canonical terminal status), `nyquist_compliant: true`,
    `wave_0_complete: true`, `created: 2026-05-20`. Body opens with
    `_Backfilled retroactively for Phase 33 (shipped 2026-05-19). Frontmatter `created: 2026-05-20` reflects backfill date; the audited code surface is the Phase 33 implementation present in `main`._`.
    Body sections mirror `34-VALIDATION.md`. Per CONTEXT D-10: if uncovered Nyquist
    points surface, gap-fill in-phase; closing requires `status: verified`.
  </action>
  <verify>
    <automated>test -f .planning/phases/33-close-gap-practice-02-resonant-settings-read-write-split-bra/33-VALIDATION.md && grep -q "^status: verified$" .planning/phases/33-close-gap-practice-02-resonant-settings-read-write-split-bra/33-VALIDATION.md && grep -q "Backfilled retroactively for Phase 33" .planning/phases/33-close-gap-practice-02-resonant-settings-read-write-split-bra/33-VALIDATION.md</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/33-close-gap-practice-02-resonant-settings-read-write-split-bra/33-VALIDATION.md` exists
    - Frontmatter contains `status: verified`
    - Frontmatter contains `phase: 33`
    - Frontmatter contains `created: 2026-05-20`
    - Body opens with the italic backfill notice referencing "Phase 33 (shipped 2026-05-19)"
    - Body carries the same section headings as `34-VALIDATION.md`
  </acceptance_criteria>
  <done>Phase 33 `VALIDATION.md` exists in the restored phase dir with `status: verified`.</done>
</task>

<task type="auto">
  <name>Task 5: Backfill Phase 35 VALIDATION.md (HOUSE-04)</name>
  <files>.planning/phases/35-flute-cue-timbre-replace-chime/35-VALIDATION.md</files>
  <read_first>
    .planning/phases/35-flute-cue-timbre-replace-chime/35-01-PLAN.md
    .planning/phases/35-flute-cue-timbre-replace-chime/35-02-PLAN.md
    .planning/phases/34-stretch-as-a-distinct-practice/34-VALIDATION.md
    .planning/phases/36-housekeeping-bookkeeping-reset/36-CONTEXT.md
    .planning/phases/36-housekeeping-bookkeeping-reset/36-PATTERNS.md
  </read_first>
  <action>
    Invoke `/gsd-validate-phase 35` against the restored Phase 35 dir (slug:
    `flute-cue-timbre-replace-chime`). Frontmatter: `phase: 35`,
    `slug: flute-cue-timbre-replace-chime`, `status: verified` (canonical terminal
    status), `nyquist_compliant: true`, `wave_0_complete: true`, `created: 2026-05-20`.
    Body opens with `_Backfilled retroactively for Phase 35 (shipped 2026-05-19). Frontmatter `created: 2026-05-20` reflects backfill date; the audited code surface is the Phase 35 implementation present in `main`._`.
    Body sections mirror `34-VALIDATION.md`. Gap-fill in-phase if needed per D-10;
    closing requires `status: verified`.
  </action>
  <verify>
    <automated>test -f .planning/phases/35-flute-cue-timbre-replace-chime/35-VALIDATION.md && grep -q "^status: verified$" .planning/phases/35-flute-cue-timbre-replace-chime/35-VALIDATION.md && grep -q "Backfilled retroactively for Phase 35" .planning/phases/35-flute-cue-timbre-replace-chime/35-VALIDATION.md</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/35-flute-cue-timbre-replace-chime/35-VALIDATION.md` exists
    - Frontmatter contains `status: verified`
    - Frontmatter contains `phase: 35`
    - Frontmatter contains `created: 2026-05-20`
    - Body opens with the italic backfill notice referencing "Phase 35 (shipped 2026-05-19)"
    - Body carries the same section headings as `34-VALIDATION.md`
  </acceptance_criteria>
  <done>Phase 35 `VALIDATION.md` exists in the restored phase dir with `status: verified`.</done>
</task>

<task type="auto">
  <name>Task 6: Stage and commit the four backfills (D-05 commit #2)</name>
  <files>
    .planning/milestones/v1.0.1-phases/12-assets-content-hygiene-cleanup/12-VALIDATION.md
    .planning/milestones/v1.0.1-phases/12-assets-content-hygiene-cleanup/12-SECURITY.md
    .planning/phases/33-close-gap-practice-02-resonant-settings-read-write-split-bra/33-VALIDATION.md
    .planning/phases/35-flute-cue-timbre-replace-chime/35-VALIDATION.md
  </files>
  <read_first>
    .planning/phases/36-housekeeping-bookkeeping-reset/36-CONTEXT.md
  </read_first>
  <action>
    Stage exactly the four backfilled files (plus any gap-fill test files added under `src/` during Task 2/4/5, if any). Commit with subject `docs(36): backfill Phase 12 VALIDATION + SECURITY and Phase 33/35 VALIDATION (HOUSE-01..04)` and a body that lists the four HOUSE-IDs closed and notes the gap-fill outcome (gap-filled vs no gaps). Use HEREDOC format. Do NOT amend. Do NOT `git add -A` — stage explicit paths only. Confirm `git diff HEAD~1 --stat` shows only the 4 backfill files (+ optional gap-fill test files).
  </action>
  <verify>
    <automated>git log -1 --format=%s | grep -q "HOUSE-01..04" && git diff HEAD~1 --name-only | grep -c "VALIDATION\|SECURITY" | grep -q "^[34]$"</automated>
  </verify>
  <acceptance_criteria>
    - HEAD commit subject matches `docs(36): backfill Phase 12 VALIDATION + SECURITY and Phase 33/35 VALIDATION (HOUSE-01..04)`
    - HEAD commit touches exactly the four backfill files (+ any in-phase gap-fill test files under `src/`)
    - `git status --porcelain | grep -E '12-(VALIDATION|SECURITY)|33-VALIDATION|35-VALIDATION'` returns no output (all four staged and committed)
    - The commit is NOT an amend (it sits on top of the prior HEAD, not replacing it)
  </acceptance_criteria>
  <done>Commit #2 lands cleanly with the four HOUSE-01..04 artifacts, on top of the prior HEAD.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Plan touches `.planning/` docs only. The only `src/` touch is via the D-10 gap-fill path, which adds test files under `src/storage/` or equivalent — same trust surface as existing tests (Vitest in jsdom). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36-02-01 | Tampering | Backfilled VALIDATION/SECURITY artifacts | accept | Honest `created: 2026-05-20` + body `Backfilled retroactively…` notice prevents the artifact from being mistaken for an original phase-time validation; the git commit message is the audit trail. |
| T-36-02-02 | Spoofing | `status: verified` claim | mitigate | Per D-10, the file lands ONLY after the auditor confirms; in-phase gap-filling is required if the auditor surfaces uncovered Nyquist points or unmitigated threats — no manual `status:` flip without auditor sign-off. Verify commands explicitly grep for `^status: verified$` (canonical auditor terminal). |
| T-36-02-03 | Information Disclosure | Threat register in 12-SECURITY.md | accept | Regenerated against Phase 12's already-shipped code; references public files only (favicon.svg, learnContent.ts Amazon URL, settings.ts predicate). No secrets in scope. |
</threat_model>

<verification>
- Four backfilled files exist with the required frontmatter (per-task verify steps)
- All carry `created: 2026-05-20` and the italic backfill notice
- All four files carry `status: verified` (canonical auditor terminal); the SECURITY.md additionally carries `threats_open: 0`
- Single commit `docs(36): backfill Phase 12 VALIDATION + SECURITY and Phase 33/35 VALIDATION (HOUSE-01..04)` on top of HEAD
</verification>

<success_criteria>
- HOUSE-01 closed: Phase 12 VALIDATION.md exists with `status: verified`
- HOUSE-02 closed: Phase 12 SECURITY.md exists with `status: verified`, `threats_open: 0`
- HOUSE-03 closed: Phase 33 VALIDATION.md exists with `status: verified`
- HOUSE-04 closed: Phase 35 VALIDATION.md exists with `status: verified`
- Single commit lands cleanly without amending prior history
</success_criteria>

<output>
After completion, create `.planning/phases/36-housekeeping-bookkeeping-reset/36-02-SUMMARY.md`.
</output>
</content>
</invoke>