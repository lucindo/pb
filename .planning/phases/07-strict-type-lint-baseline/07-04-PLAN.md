---
phase: 07-strict-type-lint-baseline
plan: 04
type: execute
wave: 4
depends_on:
  - 07-01
  - 07-02
  - 07-03
files_modified:
  - eslint.config.js
  - src/app/App.tsx
  - src/hooks/usePrefersReducedMotion.ts
autonomous: true
requirements:
  - BUILD-03

must_haves:
  truths:
    - "eslint.config.js explicitly overrides react-hooks/exhaustive-deps to 'error' (upgrading from the recommended-default 'warn')"
    - "All surviving // eslint-disable-next-line react-hooks/* comments in src/ are preceded by a // Reason: ... line per D-04"
    - "App.tsx has exactly 2 react-hooks/set-state-in-effect disables — at lines ~205 and ~390 — each with its // Reason: annotation; the stale line 411 disable was removed in Plan 03 Task 1 Pass H"
    - "usePrefersReducedMotion.ts setReduced(mql.matches) call has either a refactor that eliminates the set-state-in-effect fire OR a justified // eslint-disable-next-line react-hooks/set-state-in-effect with a // Reason: annotation"
    - "npm run lint exits 0 against the eslint.config.js with exhaustive-deps at error level"
    - "tsc --noEmit on both projects exits 0"
    - "npm run build exits 0"
    - "npm run test -- --run reports 363/363 passing — D-09 behavior preservation"
    - "npx eslint --print-config src/app/App.tsx reports react-hooks/exhaustive-deps resolved to 'error' (or numeric 2)"
  artifacts:
    - path: "eslint.config.js"
      provides: "react-hooks/exhaustive-deps explicit error-level override"
      contains: "'react-hooks/exhaustive-deps': 'error'"
    - path: "src/app/App.tsx"
      provides: "Two surviving react-hooks/set-state-in-effect disables (~lines 205 and 390) each preceded by a // Reason: annotation per D-04"
    - path: "src/hooks/usePrefersReducedMotion.ts"
      provides: "Justified annotation OR refactor for the set-state-in-effect fire at the post-mount mql.matches re-seed"
  key_links:
    - from: "eslint.config.js rules block"
      to: "react-hooks/exhaustive-deps enforcement"
      via: "explicit 'error' override AFTER the ...reactHooks.configs.recommended.rules spread (later-wins ordering — Pitfall 6)"
      pattern: "react-hooks/exhaustive-deps.*error"
    - from: "Each surviving // eslint-disable-next-line react-hooks/* comment"
      to: "// Reason: annotation on the immediately preceding line"
      via: "D-04 annotation policy"
      pattern: "Reason:"
---

<objective>
Land BUILD-03 — the react-hooks enforcement audit — on top of Plan 03's lint-clean strictTypeChecked baseline (Plan 02 landed the preset upgrade + 48 production-rule-cluster fixes; Plan 03 closed the remaining 16 production singletons + 162 test-file errors + the App.tsx:411 stale disable). One surgical edit to eslint.config.js (add the `react-hooks/exhaustive-deps: 'error'` explicit override) and source-side audit edits (add `// Reason:` annotations to the two surviving App.tsx set-state-in-effect disables that Plans 02 and 03 left untouched, and resolve the unsuppressed set-state-in-effect fire on the setReduced(mql.matches) call in usePrefersReducedMotion.ts). The stale App.tsx:411 disable was already removed by Plan 03 Task 1 Pass H — do not attempt to remove it again.

Purpose: Make `react-hooks/exhaustive-deps` enforcement explicit and authoritative (per D-04: "every surviving disable MUST be preceded by a `// Reason:` annotation naming the invariant"). After this plan ships, future v1.0.1 phases (8-12) and v1.1+ work all lint under exhaustive-deps at error level with documented exceptions only.

Output: eslint.config.js with the explicit `'react-hooks/exhaustive-deps': 'error'` rule entry; App.tsx with two `// Reason:` annotations attached to the surviving set-state-in-effect disables; usePrefersReducedMotion.ts with a justified annotated disable for its post-mount re-seed setState; D-09 commit-boundary trio green.
</objective>

<execution_context>
@$HOME/.claude-personal-personal/get-shit-done/workflows/execute-plan.md
@$HOME/.claude-personal-personal/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-strict-type-lint-baseline/07-CONTEXT.md
@.planning/phases/07-strict-type-lint-baseline/07-RESEARCH.md
@.planning/phases/07-strict-type-lint-baseline/07-PATTERNS.md
@.planning/phases/07-strict-type-lint-baseline/07-VALIDATION.md
@.planning/phases/07-strict-type-lint-baseline/07-01-PLAN.md
@.planning/phases/07-strict-type-lint-baseline/07-02-PLAN.md
@.planning/phases/07-strict-type-lint-baseline/07-03-PLAN.md

<interfaces>
BUILD-03 has three concrete artifacts. Verified line numbers and post-edit shapes from PATTERNS.md "Fix Category 6: // Reason: Annotations on Surviving eslint-disable Comments".

eslint.config.js — add ONE rule override inside the existing rules block (which spreads ...reactHooks.configs.recommended.rules). After Plan 02 + Plan 03 the rules block looks like:
  rules: {
    ...reactHooks.configs.recommended.rules,
    'react-refresh/only-export-components': ['warn', { allowConstantExport: true }],
  }

Add `'react-hooks/exhaustive-deps': 'error',` between the spread and the react-refresh entry. Final shape:
  rules: {
    ...reactHooks.configs.recommended.rules,
    'react-hooks/exhaustive-deps': 'error',   // BUILD-03: upgrade from recommended-default 'warn'
    'react-refresh/only-export-components': ['warn', { allowConstantExport: true }],
  }

App.tsx surviving set-state-in-effect disables (PATTERNS.md "Fix Category 6"):
- Line ~205 (current). Effect that mirrors session.status to endDialogOpen. The current code wraps the setEndDialogOpen(false) call inside an if-guard with a `// eslint-disable-next-line react-hooks/set-state-in-effect` comment immediately above it but NO `// Reason:` annotation. Add a `// Reason: subscribe-and-reflect — endDialogOpen mirrors external session.status; setting local state from this trigger effect is the documented React pattern for "subscribe + reflect".` line directly above the existing eslint-disable line. Keep the eslint-disable line and the setEndDialogOpen line unchanged.

- Line ~390 (current). Effect tearing down audio/wakeLock and resetting appPhase to 'idle'. Two existing void-prefixed cleanup calls precede the eslint-disable line that suppresses the rule on `setAppPhase('idle')`. Add a `// Reason: subscribe-and-reflect — appPhase resets to 'idle' when session leaves running; this effect is the single write site per D-16 Phase 4 invariant.` line directly above the existing eslint-disable line. Keep all surrounding code unchanged.

App.tsx line ~411 (was the third disable per RESEARCH.md): ALREADY REMOVED by Plan 03 Task 1 Pass H. Plan 04 must NOT attempt to add an annotation. Verify the disable count for this file is exactly 2 after Plan 04 (matching the count Plan 03 left it at, plus the // Reason: lines added).

usePrefersReducedMotion.ts — new set-state-in-effect fire on the setReduced(mql.matches) call (PATTERNS.md "Fix Category 6 / usePrefersReducedMotion.ts"):
- The current code (post-Plan 03 — Plan 03 Task 1 Pass A removed the typeof-window dead checks but did NOT add the set-state-in-effect disable). Locate the `setReduced(mql.matches)` call inside the useEffect body that subscribes to the matchMedia change event.
- The existing IN-02 multi-line comment block already documents the WHY (re-seed from live MediaQueryList on mount to close the stale-initial-state window). Add a `// Reason: re-seed from live MediaQueryList on mount to close the stale-initial-state window; subsequent updates come from the change listener (MDN canonical pattern).` line directly above a NEW `// eslint-disable-next-line react-hooks/set-state-in-effect` line, both inserted between the existing IN-02 comment block and the setReduced(mql.matches) call.
- Do NOT remove the existing IN-02 multi-line comment — it provides additional context. The // Reason: line is the formal D-04-policy annotation; the IN-02 prose explains the WHY in more depth.
- Alternative (NOT recommended for D-08 minimal fix): refactor to use the addEventListener('change') callback exclusively without the post-mount re-seed. This would eliminate the rule fire but changes behavior (loses the stale-initial-state-window protection that IN-02 explicitly defends). Stick with the annotated disable.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add explicit react-hooks/exhaustive-deps error override to eslint.config.js</name>
  <files>eslint.config.js</files>
  <read_first>
    - eslint.config.js (post-Plan-03 shape — confirms the ...tseslint.configs.strictTypeChecked spread (added in Plan 02 Task 1), projectService wired in languageOptions, rules block spreads ...reactHooks.configs.recommended.rules then has the react-refresh/only-export-components entry)
    - .planning/phases/07-strict-type-lint-baseline/07-PATTERNS.md (the eslint.config.js Config Modification section, change #3 — exact shape for the new rule entry, ordering relative to the spread)
    - .planning/phases/07-strict-type-lint-baseline/07-RESEARCH.md (the react-hooks/exhaustive-deps section that confirms the recommended-config default ships warn, so an explicit error override is required)
    - .planning/phases/07-strict-type-lint-baseline/07-CONTEXT.md (D-04 annotation policy + the implicit assumption that exhaustive-deps fires at error level so any future undocumented disable is a CI failure)
  </read_first>
  <action>
    Add a single rule entry to the existing `rules:` block in `eslint.config.js`. Insert the literal line `'react-hooks/exhaustive-deps': 'error',` BETWEEN the `...reactHooks.configs.recommended.rules,` spread and the existing `'react-refresh/only-export-components': [...]` entry. This ordering is mandatory per Pitfall 6 (later-wins): the override MUST come after the spread to take effect. Add a brief trailing inline comment naming this as the BUILD-03 upgrade from the recommended-default warn level.

    Do NOT add any other rule entry. Do NOT change react-hooks/set-state-in-effect or any other react-hooks rule from its recommended default — only exhaustive-deps is the BUILD-03 contract. Do NOT introduce a per-file override block (no react-hooks rules need file-specific tuning at this baseline).

    After the edit, confirm the rule resolution by running `npx eslint --print-config src/app/App.tsx` and grepping for the rule key — the resolved value MUST be `'error'` (or numeric `2` — both representations are equivalent in ESLint flat-config output).

    Run `npm run lint`. Expected outcome: continues to exit 0 because Plan 03 Task 1 Pass H already fixed the single exhaustive-deps violation in App.tsx (the missing audio dep). If ANY new exhaustive-deps error appears, treat as either (a) a real regression in this plan's setup — investigate and fix the dep list inline, or (b) a Plan 03 gap — fix the dep list inline AND log it in the SUMMARY. Do NOT add a disable for any new exhaustive-deps fire in this task; disables on exhaustive-deps require a justified // Reason: annotation per D-04 and any new disable adds annotation work that must be enumerated, not ad-hoc.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const c=fs.readFileSync('eslint.config.js','utf8');if(!/react-hooks\/exhaustive-deps['\"]\s*:\s*['\"]error/.test(c)){console.error('FAIL: exhaustive-deps error override not found in eslint.config.js');process.exit(1)}console.log('OK: rule entry present')" &amp;&amp; npx eslint --print-config src/app/App.tsx > /tmp/eslint-resolved-config.json 2>/dev/null &amp;&amp; node -e "const j=require('/tmp/eslint-resolved-config.json');const r=j.rules['react-hooks/exhaustive-deps'];if(!r){console.error('FAIL: rule not present in resolved config');process.exit(1)}const sev=Array.isArray(r)?r[0]:r;if(sev!=='error'&amp;&amp;sev!==2){console.error('FAIL: severity is',sev,'expected error or 2');process.exit(1)}console.log('OK: resolved severity =',sev)" &amp;&amp; npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `eslint.config.js` contains the literal substring `react-hooks/exhaustive-deps` paired with `'error'` on the same source line (verified by node regex check in the verify command)
    - `npx eslint --print-config src/app/App.tsx` resolved-config output shows `rules['react-hooks/exhaustive-deps']` equal to `'error'` or numeric `2`
    - `npm run lint` exits 0 (the single exhaustive-deps fire from RESEARCH.md was fixed in Plan 03 Task 1 Pass H; no new fires expected)
    - `npx tsc --noEmit -p tsconfig.app.json` and `tsconfig.node.json` exit 0
    - `npm run build` exits 0
    - `npm run test -- --run` reports `363 passed`
    - The new override line is positioned AFTER `...reactHooks.configs.recommended.rules,` and BEFORE `'react-refresh/only-export-components'` (verified by manual diff review)
    - No other rule entry is added to the rules block by this task (verified by `git diff eslint.config.js | grep '^+' | grep -cE "':\s*'(error|warn|off)'"` returning exactly 1)
  </acceptance_criteria>
  <done>eslint.config.js carries the explicit `react-hooks/exhaustive-deps: 'error'` override, positioned correctly after the recommended.rules spread. Lint resolves the rule to error level. Lint, tsc, build, test all green.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Audit App.tsx and usePrefersReducedMotion.ts react-hooks disables — add // Reason: annotations per D-04</name>
  <files>src/app/App.tsx, src/hooks/usePrefersReducedMotion.ts</files>
  <read_first>
    - src/app/App.tsx (locate the two surviving `// eslint-disable-next-line react-hooks/set-state-in-effect` comments — RESEARCH.md and PATTERNS.md identify them at lines ~205 (above setEndDialogOpen(false) inside an if-guard) and ~390 (above setAppPhase('idle') inside the audio/wakeLock cleanup effect). Confirm Plan 03 Task 1 Pass H already removed the third stale disable that was at line ~411 — `grep -c "set-state-in-effect" src/app/App.tsx` should currently return 2.)
    - src/hooks/usePrefersReducedMotion.ts (full file — locate the setReduced(mql.matches) call inside the useEffect that subscribes to the matchMedia change event. The existing IN-02 multi-line comment block immediately above that call documents the canonical re-seed pattern. Plan 03 Task 1 Pass A removed the typeof-window dead checks; the file is shorter post-Plan 03 — re-locate the setReduced call by content, not by line number.)
    - .planning/phases/07-strict-type-lint-baseline/07-PATTERNS.md (the Fix Category 6 section: exact post-edit text for App.tsx:205 // Reason annotation, App.tsx:390 // Reason annotation, usePrefersReducedMotion.ts // Reason annotation; the App.tsx:411 entry is REMOVE-only and was already executed by Plan 03 Task 1 Pass H — do not re-execute)
    - .planning/phases/07-strict-type-lint-baseline/07-CONTEXT.md (D-04 — every surviving disable MUST be preceded by a // Reason: annotation naming the invariant being protected; D-05 — Phase 7 does NOT add a project-level enforcement rule for un-annotated disables; the policy is enforced via code review)
    - .planning/phases/07-strict-type-lint-baseline/07-RESEARCH.md (the BUILD-03 Disables section that lists each disable's location and prescribed action)
  </read_first>
  <action>
    Three surgical source-file edits — no behavior changes. Each edit inserts a single `// Reason:` comment line immediately above an existing or new `// eslint-disable-next-line react-hooks/set-state-in-effect` line. The exact // Reason: text is mandated by PATTERNS.md (matching word-for-word so a future grep against the codebase finds the same justification text used across the project for the same pattern).

    Edit A — App.tsx ~line 205 (the first surviving disable):
    Locate the existing `// eslint-disable-next-line react-hooks/set-state-in-effect` line that sits directly above a `setEndDialogOpen(false)` call inside a `useEffect` body's if-guard `if (state.status !== 'running' && endDialogOpen) { ... }`. Insert this exact line on the line IMMEDIATELY ABOVE the existing eslint-disable line:
      `// Reason: subscribe-and-reflect — endDialogOpen mirrors external session.status; setting local state from this trigger effect is the documented React pattern for "subscribe + reflect".`
    Use leading whitespace identical to the surrounding indentation level. Do NOT modify the eslint-disable line itself or the setEndDialogOpen call.

    Edit B — App.tsx ~line 390 (the second surviving disable):
    Locate the existing `// eslint-disable-next-line react-hooks/set-state-in-effect` line that sits directly above a `setAppPhase('idle')` call inside the cleanup-on-leave-running effect (the surrounding lines contain `void audioStop()` and `void wakeLockRelease()`). Insert this exact line on the line IMMEDIATELY ABOVE the existing eslint-disable line:
      `// Reason: subscribe-and-reflect — appPhase resets to 'idle' when session leaves running; this effect is the single write site per D-16 Phase 4 invariant.`
    Use leading whitespace identical to the surrounding indentation level. Do NOT modify the eslint-disable line, the setAppPhase call, or the surrounding void-prefixed cleanup calls.

    Edit C — usePrefersReducedMotion.ts (the new fire surfaced by react-hooks v7.1.1):
    Locate the `setReduced(mql.matches)` call inside the useEffect body. The existing multi-line comment block starting with `// IN-02:` immediately precedes this call and explains the canonical MDN re-seed pattern. Insert TWO new lines between the IN-02 comment block and the setReduced call (in this exact order):
      1. `// Reason: re-seed from live MediaQueryList on mount to close the stale-initial-state window; subsequent updates come from the change listener (MDN canonical pattern).`
      2. `// eslint-disable-next-line react-hooks/set-state-in-effect`
    Use leading whitespace matching the surrounding code (the body of the useEffect — typically 4 spaces inside the arrow function). Do NOT remove or modify the existing IN-02 comment block. Do NOT modify the setReduced call. Do NOT change any other line in the file.

    After all three edits run `npm run lint`. Outcome MUST be exit 0 (zero problems). If ANY react-hooks/* rule still fires, locate it and either (a) verify it falls under D-04 — add an annotated disable with a content-specific // Reason: line per the analog patterns, or (b) if the disable was inappropriate to begin with, refactor the code (Phase 7 prefers smallest behavior-preserving fix per D-08; refactor only if annotation is not honest).
  </action>
  <verify>
    <automated>npm run lint &amp;&amp; npx tsc --noEmit -p tsconfig.app.json &amp;&amp; npx tsc --noEmit -p tsconfig.node.json &amp;&amp; npm run build &amp;&amp; npm run test -- --run 2>&amp;1 | tail -5 | grep -E "363 passed" &amp;&amp; node -e "const fs=require('fs');function checkAnnotations(file){const lines=fs.readFileSync(file,'utf8').split('\n');let issues=[];for(let i=0;i<lines.length;i++){const m=lines[i].match(/eslint-disable-next-line\s+react-hooks\//);if(m){const prev=i>0?lines[i-1]:'';if(!/\/\/\s*Reason:/.test(prev)){issues.push(file+':'+(i+1)+' — react-hooks disable without // Reason: annotation on prior line');}}}return issues}const all=[].concat(checkAnnotations('src/app/App.tsx'),checkAnnotations('src/hooks/usePrefersReducedMotion.ts'));if(all.length){console.error('FAIL D-04 annotation policy:');all.forEach(i=>console.error('  '+i));process.exit(1)}console.log('OK: every react-hooks disable preceded by // Reason:')"</automated>
  </verify>
  <acceptance_criteria>
    - `npm run lint` exits 0 (zero problems — BUILD-02 baseline preserved AND BUILD-03 enforcement now active)
    - `npx tsc --noEmit -p tsconfig.app.json` and `tsconfig.node.json` exit 0
    - `npm run build` exits 0
    - `npm run test -- --run` reports `363 passed` (D-09 invariant — annotation-only edits cannot change runtime behavior)
    - `grep -c "set-state-in-effect" src/app/App.tsx` returns exactly 2 (the two surviving disables; the line ~411 stale disable was removed by Plan 03 Task 1 Pass H and stays removed)
    - `grep -c "set-state-in-effect" src/hooks/usePrefersReducedMotion.ts` returns exactly 1 (the new annotated disable on the setReduced re-seed call)
    - For App.tsx: `grep -B1 "eslint-disable-next-line react-hooks/" src/app/App.tsx | grep -c "Reason:"` returns >= 2 (each of the 2 disables has a // Reason: line on the immediately preceding line)
    - For usePrefersReducedMotion.ts: `grep -B1 "eslint-disable-next-line react-hooks/" src/hooks/usePrefersReducedMotion.ts | grep -c "Reason:"` returns >= 1 (the new disable has a // Reason: line on the immediately preceding line)
    - The node script in the verify command (which scans App.tsx + usePrefersReducedMotion.ts and reports any react-hooks disable NOT preceded by a // Reason: line) reports zero issues — this is the D-04 enforcement check
    - The exact // Reason: text used at App.tsx ~line 205 contains the substring "subscribe-and-reflect" and "endDialogOpen mirrors external session.status"
    - The exact // Reason: text used at App.tsx ~line 390 contains the substring "subscribe-and-reflect" and "appPhase resets to 'idle'"
    - The exact // Reason: text used at the usePrefersReducedMotion.ts setReduced site contains the substring "re-seed from live MediaQueryList on mount" and "MDN canonical pattern"
    - `git diff src/app/App.tsx` shows ONLY new // Reason: lines added; no existing line is modified, removed, or reflowed
    - `git diff src/hooks/usePrefersReducedMotion.ts` shows ONLY two new lines added (the // Reason: line and the // eslint-disable-next-line line); the existing IN-02 multi-line comment is untouched; the setReduced call is untouched
    - D-09 commit-boundary trio fully green at task end (`tsc && lint && build && test = 363 passed`)
  </acceptance_criteria>
  <done>App.tsx's two surviving react-hooks/set-state-in-effect disables are each preceded by a project-standard // Reason: annotation. usePrefersReducedMotion.ts's previously-unsuppressed set-state-in-effect fire is now suppressed by a justified annotated disable. The full D-04 audit is complete: every react-hooks disable in src/ has a // Reason: line on the prior line. tsc/lint/build/test trio green; behavior unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none introduced) | This plan modifies one build-toolchain config (eslint.config.js — adds one rule entry) and adds three comment-only annotations to two source files. No new endpoints, no new auth/authz logic, no new data flows, no new runtime dependencies. Comment-only changes cannot alter runtime semantics. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-04-01 | (n/a) | eslint.config.js + src/app/App.tsx + src/hooks/usePrefersReducedMotion.ts | accept | No runtime threat surface — config-only override of a lint rule severity, plus three // Reason: comment annotations on existing or new eslint-disable directives. typescript-eslint, eslint, and eslint-plugin-react-hooks are dev-only deps already installed (RESEARCH.md Environment Availability). Behavior-preservation invariant D-08/D-09 enforced by full Vitest suite (363/363) at the commit boundary. |
</threat_model>

<verification>
## Plan-Level Verification

Run at the end of this plan (before /gsd-verify-work):

1. `npm run lint` — exits 0 with zero problems (BUILD-02 baseline preserved AND BUILD-03 enforcement active)
2. `npx tsc --noEmit -p tsconfig.app.json && npx tsc --noEmit -p tsconfig.node.json` — both exit 0
3. `npm run build` — exits 0
4. `npm run test -- --run` — reports `363 passed` (D-09 invariant)
5. `npx eslint --print-config src/app/App.tsx` shows `rules['react-hooks/exhaustive-deps']` equal to `'error'` or `2`
6. Across `src/`, EVERY `// eslint-disable-next-line react-hooks/...` comment is preceded by a `// Reason:` line (verified by the node script in Task 2's verify command and by `grep -rn "eslint-disable-next-line react-hooks/" src/` followed by a manual scan of the three surviving sites: App.tsx ~205, App.tsx ~390, usePrefersReducedMotion.ts setReduced)
7. `grep -rc "eslint-disable-next-line react-hooks/" src/` returns total = 3 (2 in App.tsx, 1 in usePrefersReducedMotion.ts) — matching RESEARCH.md's enumeration: 2 surviving from prior phases plus 1 new for the usePrefersReducedMotion fire
8. Phase 7 done: BUILD-01 (Plan 01), BUILD-02 (Plans 02 + 03), BUILD-03 (this plan, Plan 04) — every requirement ID has a corresponding task; ROADMAP.md Phase 7 success criteria 1-4 are all satisfied
</verification>

<success_criteria>
- BUILD-03 satisfied: react-hooks/exhaustive-deps enforced at error level in eslint.config.js (verified by --print-config); every surviving react-hooks eslint-disable in src/ is preceded by a project-standard // Reason: annotation per D-04
- D-09 quad green: tsc (both projects), lint, build, test (363/363)
- Phase 7 complete: BUILD-01 (Plan 01) + BUILD-02 (Plans 02 + 03) + BUILD-03 (this plan, Plan 04) all landed; strict-mode TypeScript + strictTypeChecked ESLint + react-hooks enforcement is the new baseline for v1.0.1 phases 8-12
- No behavior change anywhere in this plan (annotation-only edits to source; one rule-severity bump in config)
</success_criteria>

<output>
After completion, create `.planning/phases/07-strict-type-lint-baseline/07-04-SUMMARY.md` per the standard summary template, including:
- Confirmation that `npx eslint --print-config src/app/App.tsx` resolves react-hooks/exhaustive-deps to error/2
- Final disable inventory: each surviving react-hooks/* eslint-disable in src/ with its file path, line number, exact // Reason: text, and the rule it suppresses
- Confirmation App.tsx has exactly 2 set-state-in-effect disables (the line 411 stale one was removed by Plan 03 Task 1 Pass H)
- Confirmation usePrefersReducedMotion.ts has exactly 1 set-state-in-effect disable (the new annotated one on the setReduced re-seed call)
- Final test pass count (363/363) and lint problem count (0)
- Note: Phase 7 milestone-end state — BUILD-01/02/03 all satisfied, baseline ready for Phases 8-12
</output>
