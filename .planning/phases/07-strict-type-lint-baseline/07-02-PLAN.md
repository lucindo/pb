---
phase: 07-strict-type-lint-baseline
plan: 02
type: execute
wave: 2
depends_on:
  - 07-01
files_modified:
  # Config (1)
  - eslint.config.js
  # Production sources affected by 64 ESLint errors (20 files)
  - src/app/App.tsx
  - src/main.tsx
  - src/audio/audioEngine.ts
  - src/audio/cueSynth.ts
  - src/hooks/useAudioCues.ts
  - src/components/SessionControls.tsx
  - src/components/EndSessionDialog.tsx
  - src/components/ResetStatsDialog.tsx
  - src/components/SettingsForm.tsx
  - src/components/SettingsStepper.tsx
  - src/components/LearnAnchor.tsx
  - src/components/LearnDialog.tsx
  - src/components/MuteToggle.tsx
  - src/components/StatsFooter.tsx
  - src/components/BreathingShape.tsx
  - src/storage/format.ts
  - src/storage/storage.ts
  - src/domain/settings.ts
  - src/domain/sessionMath.ts
  # Test/setup files affected by 162 ESLint test errors (representative; executor may edit additional test files surfaced by lint)
  - vitest.setup.ts
  - src/app/App.audio.test.tsx
  - src/app/App.wakeLock.test.tsx
  - src/app/App.persistence.test.tsx
  - src/audio/cueSynth.test.ts
  - src/audio/audioEngine.test.ts
  - src/hooks/useAudioCues.test.tsx
  - src/storage/settings.test.ts
  - src/storage/storage.test.ts
autonomous: true
requirements:
  - BUILD-02

must_haves:
  truths:
    - "eslint.config.js extends tseslint.configs.strictTypeChecked (replacing the previous tseslint.configs.recommended)"
    - "eslint.config.js wires parserOptions.projectService: true with tsconfigRootDir: import.meta.dirname"
    - "npm run lint exits 0 against the strict-type-checked preset (zero errors, zero warnings)"
    - "npx tsc --noEmit -p tsconfig.app.json and tsconfig.node.json continue to exit 0 (Plan 01 baseline preserved)"
    - "npm run build exits 0"
    - "npm run test -- --run reports 363/363 passing — D-09 behavior preservation"
    - "All 64 production-file ESLint errors are eliminated by inline fixes following the rule-batched pattern in PATTERNS.md (no rule-level disables in eslint.config.js, no global suppression — D-01)"
    - "All ~162 test-file ESLint errors are eliminated by inline fixes; surviving disables in vitest.setup.ts (require-await on FakeAudioContext methods) are preceded by '// Reason:' annotations per D-04"
  artifacts:
    - path: "eslint.config.js"
      provides: "Strict-type-checked ESLint baseline (D-01) with type-aware projectService parser (D-02)"
      contains: "tseslint.configs.strictTypeChecked"
    - path: "src/hooks/useAudioCues.ts"
      provides: "UseAudioCues interface with this: void on every method (start/stop/setMuted/etc.) — fixes unbound-method"
      contains: "this: void"
    - path: "src/hooks/useWakeLock.ts"
      provides: "UseWakeLock interface with this: void on request/release"
      contains: "this: void"
    - path: "src/components/SessionControls.tsx"
      provides: "Prop interface methods carry this: void"
    - path: "src/components/BreathingShape.tsx"
      provides: "CSS template literals format numbers via .toFixed() — fixes restrict-template-expressions"
    - path: "src/storage/format.ts"
      provides: "Numeric template-literal interpolations wrapped in String() — fixes restrict-template-expressions"
    - path: "src/main.tsx"
      provides: "document.getElementById('root') null-checked OR annotated with // Reason: — fixes no-non-null-assertion"
    - path: "vitest.setup.ts"
      provides: "FakeAudioContext suspend/close methods carry // Reason: + // eslint-disable-next-line require-await"
  key_links:
    - from: "eslint.config.js"
      to: "type-aware ESLint rules (no-floating-promises, no-misused-promises, restrict-template-expressions, etc.)"
      via: "languageOptions.parserOptions.projectService: true + tsconfigRootDir: import.meta.dirname"
      pattern: "projectService: true"
    - from: "tseslint.configs.strictTypeChecked spread position"
      to: "Custom rule overrides (react-refresh, future react-hooks/exhaustive-deps in Plan 03)"
      via: "Spread MUST come BEFORE the custom block per Pitfall 6 — flat-config later-wins ordering"
      pattern: '\.\.\.tseslint\.configs\.strictTypeChecked'
    - from: "Hook + component prop interfaces"
      to: "this: void method signatures"
      via: "Per PATTERNS.md §Fix Category 2 — interface declarations only, no implementation change"
      pattern: 'this:\s*void'
    - from: "JSX async-callback props (onStart={onStartClick} pattern)"
      to: "void wrapper in JSX"
      via: "onStart={() => { void onStartClick() }} per PATTERNS.md §Fix Category 4 (D-08 behavior-preserving Option A)"
      pattern: 'void\s+\w+Click'
---

<objective>
Upgrade `eslint.config.js` to `tseslint.configs.strictTypeChecked` with `parserOptions.projectService` (BUILD-02), then fix all 226 surfaced ESLint errors inline (64 production + ~162 test). Per RESEARCH.md the dominant rule fires are: `unbound-method` (24, all interface declarations), `restrict-template-expressions` (16, mostly CSS/format strings), `no-confusing-void-expression` (6, JSX arrow shorthand), `no-unnecessary-condition` (6, mix of dead SPA SSR guards and post-narrowing checks), `no-unnecessary-type-assertion` (5, audioEngine.ts WebKit casts), `no-misused-promises` (2, App.tsx async-handler JSX), plus 1-occurrence rules in production and ~162 test errors dominated by `no-non-null-assertion`, `no-unsafe-member-access`, `no-unsafe-assignment`, `require-await`, `no-extraneous-class`.

Purpose: Establish the linter-enforced baseline (D-01: full strictTypeChecked preset; no cherry-picking) so every v1.0.1 phase 8-12 writes code under the strict-type-checked rule set. Behavior is preserved (D-08, D-09) — every fix is type-narrowing, void-wrapping, or interface-method `this: void` annotation; no runtime semantic change.

Output: eslint.config.js with strictTypeChecked + projectService; 20 production files fixed by rule-grouped inline edits; ~16 test files fixed (test edits cluster around `JSON.parse` typing in persistence/settings tests and FakeAudioContext `require-await` annotations); D-09 commit-boundary trio green at every task commit.

Important scope boundary: This plan does NOT change `react-hooks/exhaustive-deps` to error level, does NOT add `// Reason:` annotations to the existing `react-hooks/set-state-in-effect` disables, and does NOT add the new `set-state-in-effect` disable in usePrefersReducedMotion.ts — those are Plan 03 (BUILD-03) scope. However, the 1 production `react-hooks/exhaustive-deps` violation surfaced in App.tsx (audio missing from useCallback deps per RESEARCH.md) IS in scope here because it surfaces under the recommended preset already (warn level) and may surface as error after Plan 03 — fix it now to unblock Plan 03.
</objective>

<execution_context>
@$HOME/.claude-personal-personal/get-shit-done/workflows/execute-plan.md
@$HOME/.claude-personal-personal/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-strict-type-lint-baseline/07-CONTEXT.md
@.planning/phases/07-strict-type-lint-baseline/07-RESEARCH.md
@.planning/phases/07-strict-type-lint-baseline/07-PATTERNS.md
@.planning/phases/07-strict-type-lint-baseline/07-VALIDATION.md
@.planning/phases/07-strict-type-lint-baseline/07-01-PLAN.md

<interfaces>
<!-- Verified post-edit shape for eslint.config.js (PATTERNS.md §"eslint.config.js — Config Modification"). Three surgical changes; no structural rewrite. -->

eslint.config.js — three surgical edits:
1. Line 10: replace `...tseslint.configs.recommended` with `...tseslint.configs.strictTypeChecked` (one-line preset swap; strictTypeChecked is a superset of recommended).
2. languageOptions block: add a `parserOptions` key with `projectService: true` and `tsconfigRootDir: import.meta.dirname`.
3. rules block: NO `react-hooks/exhaustive-deps` change in this plan (Plan 03 scope). Only fix react-refresh/only-export-components ordering if needed.

Spread ordering rule (Pitfall 6): `...tseslint.configs.strictTypeChecked` MUST come BEFORE the custom `{ files, languageOptions, plugins, rules }` block so the custom block's overrides win.

Interface this: void pattern (PATTERNS.md §"Fix Category 2"):
- For zero-arg methods: `onStart(this: void): void`
- For methods with args: `onChange(this: void, settings: SessionSettings): void`
- For methods returning Promise: `request(this: void): Promise<void>`
- Affected interfaces (exhaustive list per PATTERNS.md):
  * UseAudioCues (src/hooks/useAudioCues.ts:51,53,55) — start, stop, setMuted
  * UseWakeLock (src/hooks/useWakeLock.ts:24,27) — request, release
  * SessionControlsProps (src/components/SessionControls.tsx:6-7) — onStart, onEnd
  * EndSessionDialogProps (src/components/EndSessionDialog.tsx:5-6) — onConfirm, onCancel
  * ResetStatsDialogProps (src/components/ResetStatsDialog.tsx:5-6) — onConfirm, onCancel
  * SettingsFormProps (src/components/SettingsForm.tsx:14-15) — onChange, onExtendDuration
  * SettingsStepperProps (src/components/SettingsStepper.tsx:6) — onChange
  * LearnAnchorProps (src/components/LearnAnchor.tsx:14) — onClick
  * LearnDialogProps (src/components/LearnDialog.tsx:16) — onClose
  * MuteToggleProps (src/components/MuteToggle.tsx:17) — onToggle
  * StatsFooterProps (src/components/StatsFooter.tsx:23) — onResetClick

restrict-template-expressions fix patterns (PATTERNS.md §"Fix Category 5"):
- CSS scale/transform precision: `${orbScale}` → `${orbScale.toFixed(4)}` (BreathingShape.tsx:104)
- CSS percent: `${MIN_SCALE * 100}%` → `${(MIN_SCALE * 100).toFixed(2)}%` (BreathingShape.tsx:131,132,196,211)
- Plain numeric labels: `${minutes} min` → `${String(minutes)} min` (storage/format.ts, sessionMath.ts:48, App.tsx:440 cycleIndex, settings.ts error messages, SettingsForm.tsx labels)
- Existing analog already correct: `${hours.toFixed(1)} hours` (storage/format.ts:33)

no-confusing-void-expression fix pattern (PATTERNS.md §"Fix Category 3"):
- BEFORE: `onClick={() => changeBy(-1)}`  AFTER: `onClick={() => { changeBy(-1) }}`  (add braces on the arrow body)

no-misused-promises fix pattern (PATTERNS.md §"Fix Category 4" — D-08 Option A, no interface change):
- BEFORE: `onStart={onStartClick}` (where onStartClick is async)  AFTER: `onStart={() => { void onStartClick() }}`

JSON.parse in tests fix pattern (PATTERNS.md §"Fix Category 7"):
- Type readEnvelope return: `function readEnvelope(): Record<string, unknown> | null { … return raw ? (JSON.parse(raw) as Record<string, unknown>) : null }`
- Replace nested member-access assertions with `expect(env).toMatchObject({ … })` where semantically equivalent (existing analog: storage.test.ts:56)

require-await on FakeAudioContext (PATTERNS.md §"Fix Category 8"):
- Add `// Reason: async signature matches AudioContext.<method>() API contract; no real async work in the fake.` then `// eslint-disable-next-line @typescript-eslint/require-await` directly above EACH `vi.fn(async () => {...})` that has no await. Apply to `suspend` and `close` (NOT `resume` — it has conditional throw and is meaningfully async).

usePrefersReducedMotion no-unnecessary-condition fix (PATTERNS.md §"Fix Category 6 / Option A"):
- Remove the `typeof window === 'undefined'` checks at usePrefersReducedMotion.ts:7,14 (dead code in this Vite SPA — recommended approach in RESEARCH.md Open Question 2). Keep the `!window.matchMedia` guard. Do NOT add the `set-state-in-effect` disable here — that is Plan 03 scope.

audioEngine.ts no-unnecessary-type-assertion (PATTERNS.md §"Fix Category 9"):
- Three sites at audioEngine.ts:125,233,241 use `audioCtx.state as AudioContextState | 'interrupted'`. D-08 fix: KEEP the cast (it documents the WebKit widening per Phase 5.1 D-37) and prefix each with `// Reason: AudioContextState widened to include WebKit 'interrupted' extension (D-37); cast documents intent.` then `// eslint-disable-next-line @typescript-eslint/no-unnecessary-type-assertion`.

audioEngine.ts:232 no-unnecessary-condition: confirm at lint time whether `opts.onStateChange?.()` fires; if so, add `// Reason: …` + `// eslint-disable-next-line @typescript-eslint/no-unnecessary-condition`. Do NOT remove the optional chain (defensive intent).

cueSynth.ts:104 no-unnecessary-condition (PATTERNS.md §"Fix Category 9"):
- Restructure `if (needsSustain && phaseDurationSec !== undefined)` → `if (needsSustain) { … const dur = phaseDurationSec! /* Reason: phaseDurationSec is always defined when needsSustain is true per outer condition. */ }`

useAudioCues.ts:124 no-unnecessary-condition (PATTERNS.md §"Fix Category 9"):
- D-08 fix: keep the explicit `state === 'interrupted'` check, add `// Reason: explicit state check documents the WebKit-specific state-machine branch (D-37).` then `// eslint-disable-next-line @typescript-eslint/no-unnecessary-condition` directly above.

useAudioCues.ts:261 no-useless-assignment (PATTERNS.md §"Fix Category 9"):
- Restructure: `let newEngine: AudioEngine` (no initial null) and rely on the catch's `return` for definite assignment. OR change type to `let newEngine: AudioEngine | undefined` and remove the `= null` initializer. Either is fine; choose the option that does not trigger any other rule.

SettingsStepper.tsx:28 no-unnecessary-condition (PATTERNS.md §"Fix Category 9"):
- Replace `options[selectedIndex + offset]` with `options.at(selectedIndex + offset)` to make the `T | undefined` return type unambiguous to the linter.

main.tsx no-non-null-assertion (PATTERNS.md §"Fix Category 9 / main.tsx:6"):
- Inline narrowing per D-07: `const rootEl = document.getElementById('root'); if (rootEl === null) throw new Error('Root element #root not found in index.html'); createRoot(rootEl).render(...)`. Do NOT use the annotated-assertion fallback here — the inline narrowing is preferred per D-07.

App.tsx exhaustive-deps fix (RESEARCH.md §"Production Errors by Rule" / 1 occurrence):
- The single `react-hooks/exhaustive-deps` warning in App.tsx (audio missing from useCallback deps). Fix BY ADDING `audio` to the deps list — do NOT add a disable. Behavior is preserved because audio is the actual dependency the callback closes over (this is a real but harmless missing dep; adding it stabilizes the rule).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Upgrade eslint.config.js to strictTypeChecked + wire projectService</name>
  <files>eslint.config.js</files>
  <read_first>
    - eslint.config.js (full file — confirm current shape: spreads tseslint.configs.recommended on line 10; languageOptions has only ecmaVersion + globals; rules block spreads reactHooks.configs.recommended.rules + react-refresh override)
    - .planning/phases/07-strict-type-lint-baseline/07-PATTERNS.md (§"eslint.config.js — Config Modification" — exact three surgical edits with post-edit full-file shape)
    - .planning/phases/07-strict-type-lint-baseline/07-RESEARCH.md (§"Recommended `eslint.config.js` Shape", §"Pitfall 6: Ordering of strictTypeChecked Config Spread")
    - .planning/phases/07-strict-type-lint-baseline/07-CONTEXT.md (D-01 full preset, no cherry-pick; D-02 wire parserOptions; Claude's Discretion entry — projectService preferred over legacy project array)
    - tsconfig.app.json (confirm strict mode landed in Plan 01 — projectService requires this)
    - tsconfig.node.json (confirm strict mode landed in Plan 01)
    - tsconfig.json (root project-references shell — projectService discovers via this file)
  </read_first>
  <action>
    Apply three surgical edits to `eslint.config.js` per PATTERNS.md §"eslint.config.js — Config Modification":
    1. On the line that currently reads `...tseslint.configs.recommended,` replace it with `...tseslint.configs.strictTypeChecked,` (one-line preset swap; this is the D-01 implementation — no cherry-pick, full preset). The spread MUST remain BEFORE the custom block (Pitfall 6 — later-wins ordering).
    2. In the `languageOptions` object, add a `parserOptions` key with `projectService: true` and `tsconfigRootDir: import.meta.dirname`. Place this key after the existing `globals` key. This is the D-02 implementation.
    3. Do NOT add `react-hooks/exhaustive-deps: 'error'` here — that override is Plan 03 scope (BUILD-03). The current `...reactHooks.configs.recommended.rules` spread (which ships exhaustive-deps as `warn`) stays as-is for this plan.
    4. Do NOT add per-file overrides for `vite.config.ts` (RESEARCH.md confirms projectService resolves it via tsconfig.node.json automatically) and do NOT add an `ignores` entry for `eslint.config.js` itself (RESEARCH.md confirms `**/*.{ts,tsx}` glob does not match the .js config file).
    5. Do NOT add ANY rule-level `'off'` or downgrade entries to mute strictTypeChecked rules globally — D-01 forbids this. Every fire is fixed inline by Tasks 2-N.
    After the edit, run `npm run lint` once. The expected output is roughly 226 problems (RESEARCH.md baseline). Do NOT attempt to fix any of them in this task — Tasks 2-7 batch the fixes by rule. Verify the lint command actually invokes the strictTypeChecked rules by confirming at least one `restrict-template-expressions` or `unbound-method` error appears (these don't fire under `recommended`).
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.app.json &amp;&amp; npx tsc --noEmit -p tsconfig.node.json &amp;&amp; node -e "const fs=require('fs');const c=fs.readFileSync('eslint.config.js','utf8');if(!c.includes('strictTypeChecked')){console.error('missing strictTypeChecked');process.exit(1)}if(!c.includes('projectService')){console.error('missing projectService');process.exit(1)}if(c.includes('tseslint.configs.recommended')&amp;&amp;!c.includes('strictTypeChecked')){console.error('still using recommended');process.exit(1)}console.log('ok')" &amp;&amp; npm run lint 2>&amp;1 | grep -qE "(unbound-method|restrict-template-expressions|no-floating-promises)"</automated>
  </verify>
  <acceptance_criteria>
    - `eslint.config.js` contains the literal string `tseslint.configs.strictTypeChecked` AND no longer contains `tseslint.configs.recommended` (verified by grep / node read in verify command)
    - `eslint.config.js` contains the literal string `projectService` (set to `true`) AND `tsconfigRootDir` (set to `import.meta.dirname`)
    - `eslint.config.js` does NOT contain `'react-hooks/exhaustive-deps': 'error'` (Plan 03 scope) — verified by `grep -c "exhaustive-deps" eslint.config.js` returning 0
    - `eslint.config.js` does NOT contain any new rule entry set to `'off'` or `'warn'` for any strictTypeChecked rule (D-01: no cherry-picking)
    - `npm run lint` exit code is non-zero (errors present) AND its output mentions at least one of: `unbound-method`, `restrict-template-expressions`, `no-floating-promises` (proving type-aware rules are firing — not just the previous recommended set)
    - `npx tsc --noEmit -p tsconfig.app.json` and `tsconfig.node.json` continue to exit 0 (Plan 01 baseline preserved)
    - `npm run test -- --run` still reports `363 passed` (config change does not affect runtime tests)
    - `npm run build` exits 0
    - The pre-existing `{ ignores: ['dist'] }` block is preserved
    - Spread ordering: in the array passed to `tseslint.config(...)`, `...tseslint.configs.strictTypeChecked` appears BEFORE the `{ files: ['**/*.{ts,tsx}'], … }` block (verified by manual diff review or by `node -e` reading the source)
  </acceptance_criteria>
  <done>eslint.config.js extends strictTypeChecked with projectService wired correctly. Lint surfaces ~226 type-aware errors as a clean baseline for Tasks 2-7 to address. tsc + build + tests stay green.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Fix unbound-method (24 errors) by adding `this: void` to all hook + prop interface methods</name>
  <files>src/hooks/useAudioCues.ts, src/hooks/useWakeLock.ts, src/components/SessionControls.tsx, src/components/EndSessionDialog.tsx, src/components/ResetStatsDialog.tsx, src/components/SettingsForm.tsx, src/components/SettingsStepper.tsx, src/components/LearnAnchor.tsx, src/components/LearnDialog.tsx, src/components/MuteToggle.tsx, src/components/StatsFooter.tsx</files>
  <read_first>
    - src/hooks/useAudioCues.ts (lines 44-58 — the UseAudioCues interface declaration with `start(plan: BreathingPlan): Promise<number | null>`, `stop(): Promise<void>`, `setMuted(muted: boolean): void`)
    - src/hooks/useWakeLock.ts (lines 20-30 — UseWakeLock interface with `request(): Promise<void>`, `release(): Promise<void>`)
    - src/components/SessionControls.tsx (line 6-7 — SessionControlsProps with `onStart(): void`, `onEnd(): void`)
    - src/components/EndSessionDialog.tsx (lines 5-6 — EndSessionDialogProps)
    - src/components/ResetStatsDialog.tsx (lines 5-6 — ResetStatsDialogProps)
    - src/components/SettingsForm.tsx (lines 14-15 — SettingsFormProps)
    - src/components/SettingsStepper.tsx (line 6 — SettingsStepperProps)
    - src/components/LearnAnchor.tsx (line 14 — LearnAnchorProps)
    - src/components/LearnDialog.tsx (line 16 — LearnDialogProps)
    - src/components/MuteToggle.tsx (line 17 — MuteToggleProps)
    - src/components/StatsFooter.tsx (line 23 — StatsFooterProps)
    - .planning/phases/07-strict-type-lint-baseline/07-PATTERNS.md (§"Fix Category 2: unbound-method" — exact post-edit shape for every interface; complete affected-interface list)
    - .planning/phases/07-strict-type-lint-baseline/07-RESEARCH.md (§"unbound-method Rule Explanation and Fix Pattern")
  </read_first>
  <action>
    For every interface enumerated in PATTERNS.md §"Fix Category 2", add `this: void` as the FIRST parameter of EVERY method-shorthand declaration. The transformation is purely a type-signature change — no implementation changes. Per-interface edits (exhaustive — do not skip any):
    1. UseAudioCues: add `this: void` to start (before `plan`), stop (only param), setMuted (before `muted`). Apply to ALL methods declared on the interface, including any not listed here that you discover in the file.
    2. UseWakeLock: add `this: void` to request, release.
    3. SessionControlsProps: add `this: void` to onStart, onEnd.
    4. EndSessionDialogProps: add `this: void` to onConfirm, onCancel.
    5. ResetStatsDialogProps: add `this: void` to onConfirm, onCancel.
    6. SettingsFormProps: add `this: void` to onChange (before `settings: SessionSettings`), onExtendDuration (before `durationMinutes: number`).
    7. SettingsStepperProps: add `this: void` to onChange (before `value: T`).
    8. LearnAnchorProps: add `this: void` to onClick.
    9. LearnDialogProps: add `this: void` to onClose.
    10. MuteToggleProps: add `this: void` to onToggle.
    11. StatsFooterProps: add `this: void` to onResetClick.
    Do NOT change ANY useCallback / arrow-function implementation — implementations are arrow functions and have no `this` binding. Do NOT change call sites — `this: void` on the declaration does not require call-site changes. Do NOT use the alternative arrow-function-property syntax (`onStart: () => void`) as a workaround — RESEARCH.md notes both work but the per-interface analog uses method-shorthand consistently; preserve that style. After the edits, `npm run lint 2>&1 | grep -c "unbound-method"` MUST return 0.
  </action>
  <verify>
    <automated>npm run lint 2>&amp;1 | { grep -c "unbound-method" || echo 0; } | grep -qx 0 &amp;&amp; npx tsc --noEmit -p tsconfig.app.json &amp;&amp; npm run test -- --run 2>&amp;1 | tail -5 | grep -E "363 passed"</automated>
  </verify>
  <acceptance_criteria>
    - `npm run lint 2>&1 | grep -c "unbound-method"` returns `0` (all 24 unbound-method errors eliminated)
    - `npx tsc --noEmit -p tsconfig.app.json` exits 0 (no compile regression)
    - `npx tsc --noEmit -p tsconfig.node.json` exits 0
    - `npm run build` exits 0
    - `npm run test -- --run` reports `363 passed` (D-09 invariant — interface signature change does not affect runtime)
    - Every method declaration in the eleven listed interfaces (`UseAudioCues`, `UseWakeLock`, `SessionControlsProps`, `EndSessionDialogProps`, `ResetStatsDialogProps`, `SettingsFormProps`, `SettingsStepperProps`, `LearnAnchorProps`, `LearnDialogProps`, `MuteToggleProps`, `StatsFooterProps`) has `this: void` as its FIRST parameter — verified by `grep -E "^\s*(on[A-Z]\w*|request|release|start|stop|setMuted)\s*\(" src/hooks/useAudioCues.ts src/hooks/useWakeLock.ts src/components/{SessionControls,EndSessionDialog,ResetStatsDialog,SettingsForm,SettingsStepper,LearnAnchor,LearnDialog,MuteToggle,StatsFooter}.tsx | grep -v "this: void"` returning ZERO method-shorthand declarations missing `this: void`
    - No `useCallback` or arrow-function implementation was modified by this task (verify by `git diff src/hooks src/components` — only interface declaration lines are changed)
    - D-09 commit-boundary trio fully green at task end
  </acceptance_criteria>
  <done>All 24 unbound-method errors eliminated by `this: void` on the eleven enumerated interfaces. tsc/lint/build/test trio green. No implementation code touched.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Fix restrict-template-expressions (16 errors) + no-confusing-void-expression (6 errors) + no-misused-promises (2 errors) — string/JSX hygiene batch</name>
  <files>src/components/BreathingShape.tsx, src/storage/format.ts, src/components/SettingsForm.tsx, src/domain/settings.ts, src/app/App.tsx, src/domain/sessionMath.ts, src/components/SettingsStepper.tsx</files>
  <read_first>
    - src/components/BreathingShape.tsx (lines 100-220 — the 7 sites: line 104 `transform: scale(${orbScale})`, lines 131,132,196,211 `${MIN_SCALE * 100}%`, plus 2 others surfaced by lint)
    - src/storage/format.ts (full file — 3 sites at lines 32, 38, 52 with `${minutes} min`, `${count} sessions`, `${Math.floor(durationSeconds / 60)} min`; existing analog at line 33 `${hours.toFixed(1)} hours`)
    - src/components/SettingsForm.tsx (lines 60, 66 — onChange JSX shorthand; lines with `${value} BPM`-style template literals)
    - src/domain/settings.ts (lines 52, 60 — RangeError messages with `${settings.bpm}`, `${settings.durationMinutes}`)
    - src/app/App.tsx (line 440 — `${frame.cycleIndex}:${frame.phase}` and the 2 no-misused-promises sites where async handlers are passed to JSX props; note: per RESEARCH.md ~line 523 is the canonical `onStart={onStartClick}` site)
    - src/domain/sessionMath.ts (line 48 — `${minutes}:${seconds.toString().padStart(2, '0')}`)
    - src/components/SettingsStepper.tsx (lines 47, 62 — `onClick={() => changeBy(-1)}` / `onClick={() => changeBy(1)}`)
    - .planning/phases/07-strict-type-lint-baseline/07-PATTERNS.md (§"Fix Category 5: restrict-template-expressions" — fix patterns by precision class; §"Fix Category 3: no-confusing-void-expression" — add braces; §"Fix Category 4: no-misused-promises" — JSX void-wrapper)
    - .planning/phases/07-strict-type-lint-baseline/07-RESEARCH.md (§"restrict-template-expressions Rule Explanation", §"no-confusing-void-expression Fix Pattern", §"no-misused-promises in App.tsx")
  </read_first>
  <action>
    Three rule-batched fix passes — apply in order:

    Pass A — restrict-template-expressions (16 errors):
    - For CSS scale/transform values where precision matters: wrap the numeric expression with `.toFixed(N)`. BreathingShape.tsx:104 → `${orbScale.toFixed(4)}`. BreathingShape.tsx:131,132,196,211 → `${(MIN_SCALE * 100).toFixed(2)}%`. Match the existing analog style at storage/format.ts:33 (`${hours.toFixed(1)}`). Apply `.toFixed(2)` to percent dimensions and `.toFixed(4)` to raw scale values to maintain the precision the original numeric interpolation provided.
    - For plain integer/string interpolation (storage/format.ts lines 32, 38, 52; domain/sessionMath.ts:48; settings.ts:52, 60; App.tsx:440 cycleIndex; SettingsForm.tsx labels): wrap with `String(n)`. Examples: `${minutes} min` → `${String(minutes)} min`; `${frame.cycleIndex}:${frame.phase}` → `${String(frame.cycleIndex)}:${frame.phase}` (frame.phase is already a string-literal type and does not need wrapping).
    - Do NOT change `eslint.config.js` to set `allowNumber: true` (D-01 — no cherry-picking).
    - Do NOT introduce a shared formatting helper (D-07 — fix sites inline; the existing format.ts utilities are the right home for any future helper, but are out of Phase 7 scope).

    Pass B — no-confusing-void-expression (6 errors):
    - For each JSX arrow shorthand in SettingsStepper.tsx:47, SettingsStepper.tsx:62, App.tsx (the 2 sites — likely clearLeadInTimeouts() / similar — confirm at lint time), SettingsForm.tsx:60, SettingsForm.tsx:66 (and any others surfaced): add braces around the body. Pattern: `onClick={() => changeBy(-1)}` → `onClick={() => { changeBy(-1) }}`; `onChange={(bpm) => updateSettings({ bpm })}` → `onChange={(bpm) => { updateSettings({ bpm }) }}`.
    - Do NOT remove the arrow function and pass the bare callable — that would change argument-count semantics.

    Pass C — no-misused-promises (2 errors in App.tsx):
    - For each JSX prop where an async function is passed to a `() => void`-typed handler (e.g., `onStart={onStartClick}` at App.tsx ~line 523, plus the second site surfaced by lint), wrap with the void-expression pattern: `onStart={() => { void onStartClick() }}`. This matches the existing `void audioStop()` analog at App.tsx:234 and is D-08 Option A (no interface change).
    - Do NOT widen `SessionControlsProps.onStart` to `() => void | Promise<void>` (D-08 — interface change is Plan 11/Phase 11 scope if needed; for Phase 7 the JSX wrapper is the behavior-preserving fix).

    After all three passes, `npm run lint 2>&1 | grep -cE "(restrict-template-expressions|no-confusing-void-expression|no-misused-promises)"` MUST return 0.
  </action>
  <verify>
    <automated>npm run lint 2>&amp;1 | { grep -cE "(restrict-template-expressions|no-confusing-void-expression|no-misused-promises)" || echo 0; } | grep -qx 0 &amp;&amp; npx tsc --noEmit -p tsconfig.app.json &amp;&amp; npm run build &amp;&amp; npm run test -- --run 2>&amp;1 | tail -5 | grep -E "363 passed"</automated>
  </verify>
  <acceptance_criteria>
    - `npm run lint 2>&1 | grep -c "restrict-template-expressions"` returns `0` (all 16 fixed)
    - `npm run lint 2>&1 | grep -c "no-confusing-void-expression"` returns `0` (all 6 fixed)
    - `npm run lint 2>&1 | grep -c "no-misused-promises"` returns `0` (all 2 fixed)
    - `npx tsc --noEmit -p tsconfig.app.json` exits 0
    - `npm run build` exits 0 — CRITICAL: BreathingShape.tsx CSS values must not regress visually; the `.toFixed(N)` outputs are equivalent to the prior numeric interpolation up to precision N
    - `npm run test -- --run` reports `363 passed` — D-09 behavior preservation (any test that asserts on a CSS string output must still pass; if a test breaks, the precision N was wrong and must be widened)
    - The 2 App.tsx JSX async-handler sites use the pattern `() => { void <fnName>() }` (verified by `grep -cE "void\s+\w+Click\(\)" src/app/App.tsx` returning >= 2)
    - No `eslint.config.js` rule is set to `'off'` for any of restrict-template-expressions / no-confusing-void-expression / no-misused-promises (verified by `grep -cE "(restrict-template-expressions|no-confusing-void-expression|no-misused-promises).*off" eslint.config.js` returning 0)
    - No new helper module was created (D-07; verified by `git diff --stat` showing only the seven files in `<files>` were modified, no `src/utils/format.ts` or similar)
  </acceptance_criteria>
  <done>All 24 errors across the three rules eliminated by inline fixes following the pattern catalog in PATTERNS.md. CSS visual output preserved (toFixed precision matches original numeric interpolation). 363/363 tests pass; build green.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: Fix the remaining production single/few-occurrence rules — no-unnecessary-condition, no-unnecessary-type-assertion, no-non-null-assertion, no-useless-assignment, exhaustive-deps</name>
  <files>src/hooks/usePrefersReducedMotion.ts, src/audio/audioEngine.ts, src/audio/cueSynth.ts, src/components/SettingsStepper.tsx, src/hooks/useAudioCues.ts, src/main.tsx, src/app/App.tsx</files>
  <read_first>
    - src/hooks/usePrefersReducedMotion.ts (full file — lines 7 and 14 contain `typeof window === 'undefined'` checks; line 22 contains `setReduced(mql.matches)` which fires set-state-in-effect — Plan 03 scope, do NOT touch line 22 here)
    - src/audio/audioEngine.ts (lines 120-134 and 228-242 — three `audioCtx.state as AudioContextState | 'interrupted'` casts at lines 125, 233, 241; also line 232 `opts.onStateChange?.()` no-unnecessary-condition site)
    - src/audio/cueSynth.ts (lines 98-112 — line 104 `if (needsSustain && phaseDurationSec !== undefined)` post-narrowing redundant check)
    - src/hooks/useAudioCues.ts (lines 44-58 = interface — already touched by Task 2; lines 120-130 = the `state === 'interrupted'` check at ~line 124; lines 258-278 = the `let newEngine: AudioEngine | null = null` site at ~line 261)
    - src/components/SettingsStepper.tsx (lines 25-32 — `const nextValue = options[selectedIndex + offset]; if (nextValue !== undefined) { … }`)
    - src/main.tsx (full file — line 6 `createRoot(document.getElementById('root')!)`)
    - src/app/App.tsx (the single `react-hooks/exhaustive-deps` site where `audio` is missing from a useCallback deps list — locate via `npm run lint 2>&1 | grep -A2 exhaustive-deps`)
    - .planning/phases/07-strict-type-lint-baseline/07-PATTERNS.md (§"Fix Category 6: // Reason: Annotations" for usePrefersReducedMotion Option A; §"Fix Category 9: Remaining Isolated Fixes" for audioEngine, cueSynth, useAudioCues, SettingsStepper, main.tsx)
    - .planning/phases/07-strict-type-lint-baseline/07-RESEARCH.md (§"no-unnecessary-condition in Production (D-08 flag)", §Open Question 2 — recommends Option A for usePrefersReducedMotion: remove typeof window dead checks; §Open Question 3 — recommends restructuring useAudioCues let newEngine)
    - .planning/phases/07-strict-type-lint-baseline/07-CONTEXT.md (D-04: every surviving // eslint-disable preceded by // Reason:; D-07: prefer inline narrowing over helpers, but local non-null assertions WITH // Reason: are allowed; D-08: smallest behavior-preserving fix)
  </read_first>
  <action>
    Five rule-batched fix passes — apply in order:

    Pass A — usePrefersReducedMotion.ts no-unnecessary-condition (2 errors at lines 7,14):
    - Apply RESEARCH.md Open Question 2 Option A (recommended): remove the `typeof window === 'undefined'` checks entirely. Both are dead code in this Vite SPA. Keep the `!window.matchMedia` guard on its own (it returns falsy if matchMedia is absent, sufficient defensive check).
    - Specific edits: at line 7 (the useState initializer guard) change `if (typeof window === 'undefined' || !window.matchMedia) return false` to `if (!window.matchMedia) return false`. At line 14 (the useEffect early-return) change `if (typeof window === 'undefined' || !window.matchMedia) return` to `if (!window.matchMedia) return`.
    - Do NOT touch line 22 `setReduced(mql.matches)` here — that is the new `set-state-in-effect` error, Plan 03 scope.

    Pass B — audioEngine.ts no-unnecessary-type-assertion (3 errors at lines 125, 233, 241):
    - Apply D-08 KEEP-AND-ANNOTATE pattern (the casts document the WebKit widening per Phase 5.1 D-37; removing them would lose the documentation). For each of the three sites add the two-line preface:
      `// Reason: AudioContextState widened to include WebKit 'interrupted' extension (D-37 / Phase 5.1); cast documents intent even if TS DOM lib does not require it.`
      `// eslint-disable-next-line @typescript-eslint/no-unnecessary-type-assertion`
    - The disable line MUST be on the line IMMEDIATELY above the cast (D-04). Do NOT use a single block disable for all three sites — annotate each individually.

    Pass C — audioEngine.ts no-unnecessary-condition (1 error, line 232):
    - Confirm at lint time which precise expression fires (`opts.onStateChange?.()` per RESEARCH.md). Apply D-08 keep-and-annotate (defensive optional chain has documentation value): add `// Reason: defensive optional chain — onStateChange is typed optional in CreateAudioEngineOptions; the chain documents that intent even where the call site happens to always supply it.` then `// eslint-disable-next-line @typescript-eslint/no-unnecessary-condition`. Do NOT remove the `?.`.

    Pass D — cueSynth.ts no-unnecessary-condition (1 error, line 104):
    - Restructure per PATTERNS.md §"Fix Category 9 / cueSynth.ts:104": change `if (needsSustain && phaseDurationSec !== undefined) { … }` to `if (needsSustain) { /* Reason: phaseDurationSec is always defined when needsSustain is true per outer condition at line 88. */ const dur = phaseDurationSec!; … }` and use `dur` (or `phaseDurationSec!` directly with the // Reason: annotation above the use site) for the body. Add `// Reason:` line above the non-null assertion. Do NOT remove the `needsSustain` check — only the redundant `phaseDurationSec !== undefined` part.

    Pass E — useAudioCues.ts (3 fixes):
    - useAudioCues.ts no-unnecessary-condition at line 124 (`state === 'interrupted'`): apply D-08 keep-and-annotate. Add `// Reason: explicit state check documents the WebKit-specific 'interrupted' state-machine branch (D-37); TS narrowing here is incidental to the documentation purpose.` then `// eslint-disable-next-line @typescript-eslint/no-unnecessary-condition`.
    - useAudioCues.ts no-useless-assignment at line 261 (`let newEngine: AudioEngine | null = null`): restructure per PATTERNS.md §"Fix Category 9 / useAudioCues.ts:261". Recommended approach: change to `let newEngine: AudioEngine` (no initializer) and rely on the catch's `return` for definite assignment. Verify TypeScript narrows correctly. If TypeScript complains about definite assignment, use the alternative `let newEngine: AudioEngine | undefined`. Do NOT keep the `= null` initializer.
    - exhaustive-deps in App.tsx (1 error): locate via `npm run lint 2>&1 | grep -B1 -A3 exhaustive-deps`. Add the missing `audio` identifier to the affected useCallback's dependency array. Do NOT add a disable. The dep is a real dependency; adding it is behavior-preserving (the closure already references `audio`).

    Pass F — main.tsx no-non-null-assertion (1 error, line 6):
    - Apply PATTERNS.md §"Fix Category 9 / main.tsx:6" inline-narrowing pattern (NOT the annotated-assertion fallback — D-07 prefers inline narrowing for boilerplate sites). Replace `createRoot(document.getElementById('root')!).render(...)` with:
      `const rootEl = document.getElementById('root')`
      `if (rootEl === null) throw new Error('Root element #root not found in index.html')`
      `createRoot(rootEl).render(...)`
    - This adds a runtime check that throws with a descriptive message if `#root` is missing. Behavior is preserved for the happy path (tag exists in index.html); the error message is more informative than a silent null-deref.

    Pass G — SettingsStepper.tsx no-unnecessary-condition (1 error, line 28):
    - Replace `const nextValue = options[selectedIndex + offset]` with `const nextValue = options.at(selectedIndex + offset)`. This makes the `T | undefined` return type unambiguous to the linter. The downstream `if (nextValue !== undefined)` check then correctly narrows.

    After all seven passes: `npm run lint 2>&1 | grep -E "(no-unnecessary-condition|no-unnecessary-type-assertion|no-non-null-assertion|no-useless-assignment|exhaustive-deps)" | grep -v test` MUST return zero production matches.
  </action>
  <verify>
    <automated>npm run lint 2>&amp;1 | grep -E "(no-unnecessary-condition|no-unnecessary-type-assertion|no-non-null-assertion|no-useless-assignment|exhaustive-deps)" | grep -v "\.test\." | grep -v "vitest.setup" | wc -l | tr -d ' ' | grep -qx 0 &amp;&amp; npx tsc --noEmit -p tsconfig.app.json &amp;&amp; npm run build &amp;&amp; npm run test -- --run 2>&amp;1 | tail -5 | grep -E "363 passed"</automated>
  </verify>
  <acceptance_criteria>
    - `npm run lint 2>&1 | grep -E "(no-unnecessary-condition|no-unnecessary-type-assertion|no-non-null-assertion|no-useless-assignment|exhaustive-deps)" | grep -v "\.test\." | grep -v "vitest.setup"` returns ZERO lines (all production-file errors for these 5 rules eliminated)
    - `npx tsc --noEmit -p tsconfig.app.json` and `tsconfig.node.json` exit 0
    - `npm run build` exits 0
    - `npm run test -- --run` reports `363 passed` — D-09 behavior preservation
    - usePrefersReducedMotion.ts: line 22 `setReduced(mql.matches)` is UNCHANGED by this task (Plan 03 scope; verified by `git diff src/hooks/usePrefersReducedMotion.ts | grep -c "set-state-in-effect"` returning 0)
    - Every `// eslint-disable-next-line` added by this task is preceded by a `// Reason:` line on the immediately previous line (verified by `git diff` review; or by `awk` script that scans for any added eslint-disable line and confirms the prior line starts with `// Reason:`)
    - Every non-null assertion (`!`) added by this task is preceded by a `// Reason:` annotation
    - main.tsx: `createRoot(document.getElementById('root')!)` is REPLACED with the inline-narrowing pattern (`grep -c "rootEl === null" src/main.tsx` >= 1 AND `grep -c "getElementById('root')!" src/main.tsx` returns 0)
    - useAudioCues.ts:261 no longer initializes `newEngine = null` (verified by `grep -c "let newEngine.*=.*null" src/hooks/useAudioCues.ts` returning 0)
    - SettingsStepper.tsx:28 uses `.at(` not bracket access for `nextValue` (`grep -c "options.at(selectedIndex" src/components/SettingsStepper.tsx` >= 1)
    - App.tsx: the single exhaustive-deps fix added `audio` to a useCallback deps array (do NOT add a disable; verified by `git diff src/app/App.tsx | grep -c "exhaustive-deps"` returning 0 NEW disable additions)
    - D-09 commit-boundary trio fully green at task end
  </acceptance_criteria>
  <done>All ~17 production-side single/few-occurrence rule errors eliminated by the seven inline-fix passes per PATTERNS.md. SPA SSR dead-code in usePrefersReducedMotion removed. WebKit widening casts in audioEngine preserved with // Reason: documentation. main.tsx now has informative null-check error. useAudioCues let-newEngine restructured. App.tsx exhaustive-deps fix landed (audio added to deps). 363/363 tests; build green.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 5: Fix the 162 test-file ESLint errors — JSON.parse typing, mock non-null annotation, no-extraneous-class on test classes, FakeAudioContext require-await</name>
  <files>src/app/App.persistence.test.tsx, src/storage/settings.test.ts, src/storage/storage.test.ts, src/app/App.audio.test.tsx, src/app/App.wakeLock.test.tsx, src/audio/cueSynth.test.ts, src/audio/audioEngine.test.ts, src/hooks/useAudioCues.test.tsx, vitest.setup.ts</files>
  <read_first>
    - src/app/App.persistence.test.tsx (lines 30-50 — readEnvelope helper using JSON.parse; downstream env?.settings?.bpm member-access sites — this is the bulk of the unsafe-* errors)
    - src/storage/settings.test.ts (full file — JSON.parse + property-access sites; ~remainder of unsafe-* errors)
    - src/storage/storage.test.ts (lines 50-60 — has the existing `expect(JSON.parse(raw!)).toMatchObject({...})` analog at line 56; 2 errors)
    - src/app/App.audio.test.tsx (4 no-extraneous-class errors on FakeAudioContext variant classes; ~13 no-unnecessary-type-assertion sites)
    - src/app/App.wakeLock.test.tsx (require-await sites in async test helpers; non-null assertions added by Plan 01 may need annotation review)
    - src/audio/cueSynth.test.ts (require-await + non-null annotation review)
    - src/audio/audioEngine.test.ts (require-await on async test helpers)
    - src/hooks/useAudioCues.test.tsx (require-await sites)
    - vitest.setup.ts (lines 185-213 — FakeAudioContext class with `suspend = vi.fn(async () => { … })` at line 199 and `close = vi.fn(async () => { … })` at line 203; resume at line 187 has conditional throw and is NOT a require-await target)
    - .planning/phases/07-strict-type-lint-baseline/07-PATTERNS.md (§"Fix Category 7: no-unsafe-member-access on JSON.parse Results in Tests" — toMatchObject preferred; §"Fix Category 8: require-await on FakeAudioContext Methods")
    - .planning/phases/07-strict-type-lint-baseline/07-RESEARCH.md (§"Pitfall 4: Test Files Generating Bulk Unsafe-any Errors from Parsed JSON", §"Pitfall 5: require-await on FakeAudioContext Methods in Tests", §"Test Errors by Rule" — 162 errors with rule breakdown)
  </read_first>
  <action>
    Five test-file fix passes — apply in order:

    Pass A — readEnvelope JSON.parse typing in App.persistence.test.tsx (~37 unsafe-* errors clustered here):
    - Find the `readEnvelope()` helper (PATTERNS.md §"Fix Category 7" shows lines 30-50). Add explicit return type annotation: `function readEnvelope(): Record<string, unknown> | null { … return raw ? (JSON.parse(raw) as Record<string, unknown>) : null }`. Add `// Reason: test helper reads raw localStorage; structural shape validated by downstream test assertions.` directly above the `JSON.parse(...) as Record<string, unknown>` cast (the `as` cast may itself fire `no-unsafe-type-assertion`-class rules — annotate if so).
    - For downstream usage sites (`env?.settings?.bpm`, etc.) — REPLACE with `expect(env).toMatchObject({ settings: { bpm: <expected> } })` wherever the test is asserting on an expected value (this matches the existing analog at storage.test.ts:56 and eliminates the chained unsafe member access entirely). For sites that need the value for further computation (not just assertion), narrow incrementally: `const settings = env?.settings as Record<string, unknown> | undefined; expect(settings?.bpm).toBe(<value>)`.
    - Apply the same readEnvelope-typing pattern to settings.test.ts and storage.test.ts wherever they have a similar JSON.parse helper. The 2 errors in storage.test.ts may require minor adjustment around the existing `JSON.parse(raw!)` site.

    Pass B — non-null assertion annotations in test files (Plan 01 added `!` for tsc fixes; with strictTypeChecked the no-non-null-assertion rule fires on every test `!`):
    - For every `!` non-null assertion in test files (App.audio.test.tsx, App.wakeLock.test.tsx, cueSynth.test.ts, useAudioCues.test.tsx) — including the ones added in Plan 01 — add `// Reason:` line above the assertion AND add `// eslint-disable-next-line @typescript-eslint/no-non-null-assertion` directly above. Reason text examples: "Reason: length asserted by toHaveBeenCalledTimes(N) immediately above.", "Reason: array populated by FakeAudioContext constructor; length is test-invariant.", "Reason: mock.results[0] is the requested promise resolution; presence asserted by spy invocation count above."
    - Approximate count: 42 in tests per RESEARCH.md.
    - Alternative: where Option B narrowing (`.at(0)` + `expect(item).toBeDefined()`) was used in Plan 01, no `!` annotation is needed for the original site — but the post-narrowing usage `item!` still needs the annotation. Apply per actual lint output, not pre-emptive guessing.

    Pass C — no-unsafe-member-access / no-unsafe-assignment remainder (~16+37 spread across test files):
    - Wherever `JSON.parse(...)` returns are not yet covered by Pass A's readEnvelope helper, apply the same `as Record<string, unknown>` cast with `// Reason:` annotation, and prefer `toMatchObject` for assertions over chained property access.
    - Sites that are not JSON.parse but are accessing `any`-typed values from third-party APIs may need `as unknown` + narrowing — handle case by case per lint output. Do NOT use `as unknown as T` double-casts (RESEARCH.md anti-pattern).

    Pass D — no-extraneous-class on FakeAudioContext variants in App.audio.test.tsx (4 errors):
    - Per PATTERNS.md §"No Analog Found" guidance: classes are required for `new FakeAudioContext()` invocation in `vi.spyOn` / window.AudioContext substitution. Fix by adding `// Reason: test double for AudioContext API; class syntax required for new invocation in vi.spyOn / global polyfill context.` then `// eslint-disable-next-line @typescript-eslint/no-extraneous-class` directly above each affected class declaration.

    Pass E — require-await on FakeAudioContext methods in vitest.setup.ts (and any test helper async-without-await):
    - In vitest.setup.ts, the `suspend` (line 199) and `close` (line 203) methods declared as `vi.fn(async () => { … })` have no `await` inside. Add a two-line preface DIRECTLY ABOVE EACH of those two `vi.fn(async …)` lines:
      `// Reason: async signature matches AudioContext.<methodName>() API contract; no real async work in the fake.`
      `// eslint-disable-next-line @typescript-eslint/require-await`
    - Do NOT touch the `resume` method at line 187 — RESEARCH.md confirms it has conditional throw logic that is meaningfully async and does NOT fire require-await.
    - For other test files with `require-await` fires (App.audio.test.tsx, audioEngine.test.ts, useAudioCues.test.tsx — RESEARCH.md table shows ~14 occurrences total across these): if the helper has no real await and the async signature is required for caller `await`-style usage, apply the same disable+Reason pattern. If the `async` is unnecessary (no caller depends on the Promise return type), remove the `async` keyword instead.

    After all five passes, `npm run lint` MUST exit 0 (zero errors, zero warnings — note: BUILD-03 react-hooks/exhaustive-deps stale `set-state-in-effect` warning at App.tsx:411 is Plan 03 scope; for this plan a 1-warning result for THAT specific stale-disable is acceptable AT TASK END but Plan 03 must drive it to zero. To be safe in this plan, also remove the stale disable at App.tsx:411 per PATTERNS.md §"App.tsx:411 — REMOVE (stale disable)" since the lint check fires it as a warning here).

    Special handling for App.tsx:411 stale disable (overlap with Plan 03):
    - Plan 03 owns the App.tsx hook-disable annotation work (lines 205, 390, 411). However, the line 411 entry is a STALE disable that fires as a warning under strictTypeChecked. Per PATTERNS.md its only fix is REMOVAL (no annotation). To get Plan 02's lint to exit 0, REMOVE only the line 411 disable comment in this plan. Plan 03 will add the // Reason: annotations to the SURVIVING lines 205 and 390 disables (which do NOT fire as warnings under strictTypeChecked because they correctly suppress live errors). This narrow App.tsx edit is the only intentional file-overlap with Plan 03 and is required for the D-09 lint-clean invariant in this plan.
  </action>
  <verify>
    <automated>npm run lint 2>&amp;1 | tail -3 | grep -E "(0 problems|^$)" || (npm run lint; false) &amp;&amp; npx tsc --noEmit -p tsconfig.app.json &amp;&amp; npx tsc --noEmit -p tsconfig.node.json &amp;&amp; npm run build &amp;&amp; npm run test -- --run 2>&amp;1 | tail -5 | grep -E "363 passed"</automated>
  </verify>
  <acceptance_criteria>
    - `npm run lint` exits 0 (zero problems — this is the BUILD-02 gate)
    - `npx tsc --noEmit -p tsconfig.app.json` and `tsconfig.node.json` exit 0
    - `npm run build` exits 0
    - `npm run test -- --run` reports `363 passed` — D-09 (test edits must not break test semantics; toMatchObject substitutions preserve assertion intent)
    - vitest.setup.ts: `suspend` (line ~199) and `close` (line ~203) `vi.fn(async () => …)` declarations are EACH preceded by a `// Reason:` line and a `// eslint-disable-next-line @typescript-eslint/require-await` line; verified by `grep -B2 "vi.fn(async" vitest.setup.ts | grep -c "Reason:"` returning >= 2
    - vitest.setup.ts: the `resume` method at line ~187 is UNCHANGED (verified by `git diff vitest.setup.ts` not touching the resume implementation)
    - App.persistence.test.tsx: `readEnvelope` has explicit return type `Record<string, unknown> | null` (verified by `grep -c "function readEnvelope.*Record<string, unknown>" src/app/App.persistence.test.tsx` >= 1)
    - App.tsx:411 stale `react-hooks/set-state-in-effect` disable is REMOVED (verified by counting react-hooks disables in App.tsx before vs after this task — count drops by exactly 1; remaining 2 disables at lines 205 and 390 are UNCHANGED for Plan 03 to annotate)
    - Every `!` non-null assertion in the modified test files is preceded by `// Reason:` and `// eslint-disable-next-line @typescript-eslint/no-non-null-assertion` (verified by an awk/grep script that scans test files for `!` and checks the preceding two lines)
    - Every `// eslint-disable-next-line` added by this task is preceded by `// Reason:` (D-04 enforcement)
    - No new helper file, mock factory module, or shared test utility is added (verified by `git status` showing only existing files modified, no new files in this plan)
    - The total ESLint problem count goes from ~226 (post-Task-1 baseline) to 0
  </acceptance_criteria>
  <done>npm run lint exits 0. All 226 ESLint errors eliminated by Tasks 2-5 inline fixes. Behavior preserved (363/363 tests). Build green. Stale App.tsx:411 disable removed (Plan 03 will annotate the surviving 2 disables at 205/390 plus the new usePrefersReducedMotion.ts:22 disable).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none introduced) | This plan modifies build-toolchain config (eslint.config.js), production source (interface signatures, template-literal formatting, JSX void-wrappers, dead-SSR-guard removal in a SPA, inline narrowing of array access), and test-only narrowing/typing. No new endpoints, no new auth/authz logic, no new data flows, no new runtime dependencies. typescript-eslint and @typescript-eslint/parser are dev-only deps already installed and do not change runtime semantics. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-02-01 | (n/a) | eslint.config.js + 20 production source files + ~16 test/setup files | accept | No runtime threat surface — config-only changes plus type-narrowing inline fixes preserved by D-08. Indirect security tightening: no-unsafe-* rules now reject `any`-typed flows (V5 input-validation adjacent), and no-floating-promises forces explicit `void` markers on intentional fire-and-forget. Behavior-preservation invariant D-08/D-09 enforced by full Vitest suite (363/363) at every commit boundary. |
</threat_model>

<verification>
## Plan-Level Verification

Run at the end of this plan (before /gsd-verify-work):

1. `npm run lint` — exits 0 with zero problems (BUILD-02 gate)
2. `npx tsc --noEmit -p tsconfig.app.json && npx tsc --noEmit -p tsconfig.node.json` — both exit 0 (Plan 01 baseline preserved)
3. `npm run build` — exits 0
4. `npm run test -- --run` — reports `363 passed` (D-09 invariant)
5. `grep -c "tseslint.configs.strictTypeChecked" eslint.config.js` returns 1 (D-01 implementation)
6. `grep -c "projectService" eslint.config.js` returns >= 1 (D-02 implementation)
7. `grep -c "exhaustive-deps" eslint.config.js` returns 0 (Plan 03 owns this override)
8. Total file-modified count by this plan matches the `files_modified` frontmatter (no scope creep)
9. App.tsx still has exactly 2 surviving `react-hooks/set-state-in-effect` disable comments at lines 205 and 390 (Plan 03 will annotate them) — `grep -c "set-state-in-effect" src/app/App.tsx` returns 2 (was 3 before this plan removed the line 411 stale entry)
</verification>

<success_criteria>
- BUILD-02 satisfied: eslint.config.js extends strictTypeChecked + projectService; npm run lint exits 0
- All 226 surfaced ESLint errors eliminated by inline rule-batched fixes per PATTERNS.md
- D-09 quad green at task and plan boundaries: tsc, lint, build, test (363/363)
- App.tsx:411 stale disable removed; lines 205 + 390 preserved untouched for Plan 03 to annotate
- Plan 03 unblocked: ESLint baseline is now strict-clean, react-hooks/exhaustive-deps override and disable annotations are the only remaining BUILD-* work
</success_criteria>

<output>
After completion, create `.planning/phases/07-strict-type-lint-baseline/07-02-SUMMARY.md` per the standard summary template, including:
- Pre/post ESLint problem counts (226 → 0)
- Per-rule eliminated counts (unbound-method 24, restrict-template-expressions 16, no-confusing-void-expression 6, no-misused-promises 2, etc.)
- Test pass count (363/363)
- List of every file with the count of fixes applied
- List of every `// eslint-disable-next-line` added in this plan with its `// Reason:` text and rule name
- Confirmation App.tsx:411 stale disable removed and 205/390 disables UNTOUCHED
- Note any tasks where the actual lint fire-pattern differed from RESEARCH.md (executor judgment log)
</output>
