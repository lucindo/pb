---
phase: 07-strict-type-lint-baseline
plan: 02
type: execute
wave: 2
depends_on:
  - 07-01
files_modified:
  # Config (1)
  - eslint.config.js
  # Production sources affected by unbound-method (24 errors across 11 files)
  - src/hooks/useAudioCues.ts
  - src/hooks/useWakeLock.ts
  - src/components/SessionControls.tsx
  - src/components/EndSessionDialog.tsx
  - src/components/ResetStatsDialog.tsx
  - src/components/SettingsForm.tsx
  - src/components/SettingsStepper.tsx
  - src/components/LearnAnchor.tsx
  - src/components/LearnDialog.tsx
  - src/components/MuteToggle.tsx
  - src/components/StatsFooter.tsx
  # Production sources affected by string/JSX hygiene rules (restrict-template-expressions + no-confusing-void-expression + no-misused-promises — 24 errors across 7 files)
  - src/components/BreathingShape.tsx
  - src/storage/format.ts
  - src/domain/settings.ts
  - src/app/App.tsx
  - src/domain/sessionMath.ts
autonomous: true
requirements:
  - BUILD-02

must_haves:
  truths:
    - "eslint.config.js extends tseslint.configs.strictTypeChecked (replacing the previous tseslint.configs.recommended)"
    - "eslint.config.js wires parserOptions.projectService: true with tsconfigRootDir: import.meta.dirname"
    - "All 24 production-side unbound-method errors are eliminated by `this: void` annotations on the eleven affected interface declarations (no implementation changes)"
    - "All 16 production-side restrict-template-expressions errors are eliminated by inline String() / .toFixed(N) wrapping per PATTERNS.md (no allowNumber: true cherry-pick — D-01)"
    - "All 6 production-side no-confusing-void-expression errors are eliminated by JSX arrow-shorthand bracing"
    - "All 2 App.tsx no-misused-promises errors are eliminated by JSX void-wrapper pattern (no SessionControlsProps interface widening — D-08 Option A)"
    - "npx tsc --noEmit -p tsconfig.app.json and tsconfig.node.json continue to exit 0 (Plan 01 baseline preserved)"
    - "npm run build exits 0"
    - "npm run test -- --run reports 363/363 passing — D-09 behavior preservation"
    - "Net ESLint problem count after this plan equals (226 baseline) - (1 + 24 + 16 + 6 + 2) ≈ 177 remaining errors, ALL of which are isolated production rules (Plan 03 Pass A) or test-file errors (Plan 03 Pass B-F). No NEW lint regressions introduced by this plan."
    - "D-11: the planner exercised the split delegation — the empirical 226 ESLint-error blast radius (RESEARCH.md) exceeded the >100 threshold, so Phase 7 ships as four plans (07-01 tsconfig, 07-02 ESLint preset + dominant rule clusters, 07-03 isolated production rules + test-file pass, 07-04 hooks audit). This plan exists as a direct consequence of that D-11 split."
  artifacts:
    - path: "eslint.config.js"
      provides: "Strict-type-checked ESLint baseline (D-01) with type-aware projectService parser (D-02)"
      contains: "tseslint.configs.strictTypeChecked"
    - path: "src/hooks/useAudioCues.ts"
      provides: "UseAudioCues interface with this: void on every method (start/stop/setMuted) — fixes unbound-method"
      contains: "this: void"
    - path: "src/hooks/useWakeLock.ts"
      provides: "UseWakeLock interface with this: void on request/release"
      contains: "this: void"
    - path: "src/components/SessionControls.tsx"
      provides: "Prop interface methods carry this: void"
    - path: "src/components/BreathingShape.tsx"
      provides: "CSS template literals format numbers via .toFixed() — fixes restrict-template-expressions"
    - path: "src/storage/format.ts"
      provides: "Numeric template-literal interpolations wrapped in String() — fixes restrict-template-expressions"
    - path: "src/app/App.tsx"
      provides: "Two JSX async-handler sites use the void-wrapper pattern (D-08 Option A) — fixes no-misused-promises; cycleIndex template literal wrapped in String() — fixes restrict-template-expressions"
  key_links:
    - from: "eslint.config.js"
      to: "type-aware ESLint rules (no-floating-promises, no-misused-promises, restrict-template-expressions, etc.)"
      via: "languageOptions.parserOptions.projectService: true + tsconfigRootDir: import.meta.dirname"
      pattern: "projectService: true"
    - from: "tseslint.configs.strictTypeChecked spread position"
      to: "Custom rule overrides (react-refresh, future react-hooks/exhaustive-deps in Plan 04)"
      via: "Spread MUST come BEFORE the custom block per Pitfall 6 — flat-config later-wins ordering"
      pattern: '\.\.\.tseslint\.configs\.strictTypeChecked'
    - from: "Hook + component prop interfaces"
      to: "this: void method signatures"
      via: "Per PATTERNS.md §Fix Category 2 — interface declarations only, no implementation change"
      pattern: 'this:\s*void'
    - from: "JSX async-callback props (onStart={onStartClick} pattern)"
      to: "void wrapper in JSX"
      via: "onStart={() => { void onStartClick() }} per PATTERNS.md §Fix Category 4 (D-08 behavior-preserving Option A)"
      pattern: 'void\s+\w+Click'
---

<objective>
Upgrade `eslint.config.js` to `tseslint.configs.strictTypeChecked` with `parserOptions.projectService` (BUILD-02 — preset landing), then fix the production-side ESLint errors that fall into three large rule clusters: `unbound-method` (24 errors, all interface declarations), `restrict-template-expressions` (16 errors, mostly CSS/format strings), `no-confusing-void-expression` (6 errors, JSX arrow shorthand), and `no-misused-promises` (2 errors, App.tsx async-handler JSX). Total 48 of the 64 production errors plus the eslint.config.js upgrade itself.

Purpose: Establish the linter-enforced baseline (D-01: full strictTypeChecked preset; no cherry-picking) AND clear the largest, most mechanical production-side error clusters in a single plan. Behavior is preserved (D-08, D-09) — every fix is a type-signature annotation (`this: void`), template-literal hygiene (`String(n)` / `.toFixed(N)`), JSX bracing, or JSX void-wrapping. No runtime semantic change.

Output: eslint.config.js with strictTypeChecked + projectService; eleven interfaces carrying `this: void` on every method; seven files with template-literal / JSX / void-wrapper hygiene applied; D-09 commit-boundary trio (tsc + build + test 363/363) green at every task commit. Lint will still report ~177 remaining errors at the end of this plan (the isolated production single-occurrence rules + the 162 test-file errors + the App.tsx:411 stale disable warning) — Plan 03 closes those. The full D-09 lint-exit-0 invariant first becomes satisfied at the end of Plan 03.

**Scope split rationale (D-11 / checker-blocker resolution):** The original 5-task Plan 02 was split into this Plan 02 (eslint.config upgrade + production-side rule clusters: unbound-method + string/JSX hygiene; 3 tasks) and Plan 03 (remaining isolated production rules + 162 test-file errors + App.tsx:411 stale disable; 4-pass single task). The split bisects naturally at the production-vs-test boundary and keeps every task ≤ ~30% context. Both plans are mechanical PATTERNS.md compliance with zero judgment risk per fix; no code shape questions remain.

**Important scope boundary:** This plan does NOT change `react-hooks/exhaustive-deps` to error level, does NOT add `// Reason:` annotations to the existing `react-hooks/set-state-in-effect` disables, does NOT touch usePrefersReducedMotion.ts, does NOT touch any test file, does NOT touch the audioEngine/cueSynth/useAudioCues isolated production sites (no-unnecessary-condition, no-unnecessary-type-assertion, no-useless-assignment, no-non-null-assertion, exhaustive-deps), and does NOT remove the App.tsx:411 stale disable. ALL of those move to Plan 03 (BUILD-02 production cleanup + test-file pass) and Plan 04 (BUILD-03 hooks audit). The 1 production `react-hooks/exhaustive-deps` violation surfaced in App.tsx (audio missing from useCallback deps per RESEARCH.md) is also Plan 03 Pass A scope — NOT addressed in this plan.
</objective>

<execution_context>
@$HOME/.claude-personal-personal/get-shit-done/workflows/execute-plan.md
@$HOME/.claude-personal-personal/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-strict-type-lint-baseline/07-CONTEXT.md
@.planning/phases/07-strict-type-lint-baseline/07-RESEARCH.md
@.planning/phases/07-strict-type-lint-baseline/07-PATTERNS.md
@.planning/phases/07-strict-type-lint-baseline/07-VALIDATION.md
@.planning/phases/07-strict-type-lint-baseline/07-01-PLAN.md

<interfaces>
<!-- Verified post-edit shape for eslint.config.js (PATTERNS.md §"eslint.config.js — Config Modification"). Three surgical changes; no structural rewrite. -->

eslint.config.js — three surgical edits:
1. Line 10: replace `...tseslint.configs.recommended` with `...tseslint.configs.strictTypeChecked` (one-line preset swap; strictTypeChecked is a superset of recommended).
2. languageOptions block: add a `parserOptions` key with `projectService: true` and `tsconfigRootDir: import.meta.dirname`.
3. rules block: NO `react-hooks/exhaustive-deps` change in this plan (Plan 04 scope). Only fix react-refresh/only-export-components ordering if needed.

Spread ordering rule (Pitfall 6): `...tseslint.configs.strictTypeChecked` MUST come BEFORE the custom `{ files, languageOptions, plugins, rules }` block so the custom block's overrides win.

Interface this: void pattern (PATTERNS.md §"Fix Category 2"):
- For zero-arg methods: `onStart(this: void): void`
- For methods with args: `onChange(this: void, settings: SessionSettings): void`
- For methods returning Promise: `request(this: void): Promise<void>`
- Affected interfaces (exhaustive list per PATTERNS.md):
  * UseAudioCues (src/hooks/useAudioCues.ts:51,53,55) — start, stop, setMuted
  * UseWakeLock (src/hooks/useWakeLock.ts:24,27) — request, release
  * SessionControlsProps (src/components/SessionControls.tsx:6-7) — onStart, onEnd
  * EndSessionDialogProps (src/components/EndSessionDialog.tsx:5-6) — onConfirm, onCancel
  * ResetStatsDialogProps (src/components/ResetStatsDialog.tsx:5-6) — onConfirm, onCancel
  * SettingsFormProps (src/components/SettingsForm.tsx:14-15) — onChange, onExtendDuration
  * SettingsStepperProps (src/components/SettingsStepper.tsx:6) — onChange
  * LearnAnchorProps (src/components/LearnAnchor.tsx:14) — onClick
  * LearnDialogProps (src/components/LearnDialog.tsx:16) — onClose
  * MuteToggleProps (src/components/MuteToggle.tsx:17) — onToggle
  * StatsFooterProps (src/components/StatsFooter.tsx:23) — onResetClick

restrict-template-expressions fix patterns (PATTERNS.md §"Fix Category 5"):
- CSS scale/transform precision: `${orbScale}` → `${orbScale.toFixed(4)}` (BreathingShape.tsx:104)
- CSS percent: `${MIN_SCALE * 100}%` → `${(MIN_SCALE * 100).toFixed(2)}%` (BreathingShape.tsx:131,132,196,211)
- Plain numeric labels: `${minutes} min` → `${String(minutes)} min` (storage/format.ts, sessionMath.ts:48, App.tsx:440 cycleIndex, settings.ts error messages, SettingsForm.tsx labels)
- Existing analog already correct: `${hours.toFixed(1)} hours` (storage/format.ts:33)

no-confusing-void-expression fix pattern (PATTERNS.md §"Fix Category 3"):
- BEFORE: `onClick={() => changeBy(-1)}`  AFTER: `onClick={() => { changeBy(-1) }}`  (add braces on the arrow body)

no-misused-promises fix pattern (PATTERNS.md §"Fix Category 4" — D-08 Option A, no interface change):
- BEFORE: `onStart={onStartClick}` (where onStartClick is async)  AFTER: `onStart={() => { void onStartClick() }}`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Upgrade eslint.config.js to strictTypeChecked + wire projectService</name>
  <files>eslint.config.js</files>
  <read_first>
    - eslint.config.js (full file — confirm current shape: spreads tseslint.configs.recommended on line 10; languageOptions has only ecmaVersion + globals; rules block spreads reactHooks.configs.recommended.rules + react-refresh override)
    - .planning/phases/07-strict-type-lint-baseline/07-PATTERNS.md (§"eslint.config.js — Config Modification" — exact three surgical edits with post-edit full-file shape)
    - .planning/phases/07-strict-type-lint-baseline/07-RESEARCH.md (§"Recommended `eslint.config.js` Shape", §"Pitfall 6: Ordering of strictTypeChecked Config Spread")
    - .planning/phases/07-strict-type-lint-baseline/07-CONTEXT.md (D-01 full preset, no cherry-pick; D-02 wire parserOptions; Claude's Discretion entry — projectService preferred over legacy project array)
    - tsconfig.app.json (confirm strict mode landed in Plan 01 — projectService requires this)
    - tsconfig.node.json (confirm strict mode landed in Plan 01)
    - tsconfig.json (root project-references shell — projectService discovers via this file)
  </read_first>
  <action>
    Apply three surgical edits to `eslint.config.js` per PATTERNS.md §"eslint.config.js — Config Modification":
    1. On the line that currently reads `...tseslint.configs.recommended,` replace it with `...tseslint.configs.strictTypeChecked,` (one-line preset swap; this is the D-01 implementation — no cherry-pick, full preset). The spread MUST remain BEFORE the custom block (Pitfall 6 — later-wins ordering).
    2. In the `languageOptions` object, add a `parserOptions` key with `projectService: true` and `tsconfigRootDir: import.meta.dirname`. Place this key after the existing `globals` key. This is the D-02 implementation.
    3. Do NOT add `react-hooks/exhaustive-deps: 'error'` here — that override is Plan 04 scope (BUILD-03). The current `...reactHooks.configs.recommended.rules` spread (which ships exhaustive-deps as `warn`) stays as-is for this plan.
    4. Do NOT add per-file overrides for `vite.config.ts` (RESEARCH.md confirms projectService resolves it via tsconfig.node.json automatically) and do NOT add an `ignores` entry for `eslint.config.js` itself (RESEARCH.md confirms `**/*.{ts,tsx}` glob does not match the .js config file).
    5. Do NOT add ANY rule-level `'off'` or downgrade entries to mute strictTypeChecked rules globally — D-01 forbids this. Every fire is fixed inline by Tasks 2-3 (this plan) and Plan 03's task.
    After the edit, run `npm run lint` once. The expected output is roughly 226 problems (RESEARCH.md baseline). Do NOT attempt to fix any of them in this task — Tasks 2-3 batch the production fixes by rule cluster; Plan 03 closes the remaining production singletons + all 162 test errors. Verify the lint command actually invokes the strictTypeChecked rules by confirming at least one `restrict-template-expressions` or `unbound-method` error appears (these don't fire under `recommended`).
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.app.json &amp;&amp; npx tsc --noEmit -p tsconfig.node.json &amp;&amp; node -e "const fs=require('fs');const c=fs.readFileSync('eslint.config.js','utf8');if(!c.includes('strictTypeChecked')){console.error('missing strictTypeChecked');process.exit(1)}if(!c.includes('projectService')){console.error('missing projectService');process.exit(1)}if(c.includes('tseslint.configs.recommended')&amp;&amp;!c.includes('strictTypeChecked')){console.error('still using recommended');process.exit(1)}console.log('ok')" &amp;&amp; npm run lint 2>&amp;1 | grep -qE "(unbound-method|restrict-template-expressions|no-floating-promises)"</automated>
  </verify>
  <acceptance_criteria>
    - `eslint.config.js` contains the literal string `tseslint.configs.strictTypeChecked` AND no longer contains `tseslint.configs.recommended` (verified by grep / node read in verify command)
    - `eslint.config.js` contains the literal string `projectService` (set to `true`) AND `tsconfigRootDir` (set to `import.meta.dirname`)
    - `eslint.config.js` does NOT contain `'react-hooks/exhaustive-deps': 'error'` (Plan 04 scope) — verified by `grep -c "exhaustive-deps" eslint.config.js` returning 0
    - `eslint.config.js` does NOT contain any new rule entry set to `'off'` or `'warn'` for any strictTypeChecked rule (D-01: no cherry-picking)
    - `npm run lint` exit code is non-zero (errors present) AND its output mentions at least one of: `unbound-method`, `restrict-template-expressions`, `no-floating-promises` (proving type-aware rules are firing — not just the previous recommended set)
    - `npx tsc --noEmit -p tsconfig.app.json` and `tsconfig.node.json` continue to exit 0 (Plan 01 baseline preserved)
    - `npm run test -- --run` still reports `363 passed` (config change does not affect runtime tests)
    - `npm run build` exits 0
    - The pre-existing `{ ignores: ['dist'] }` block is preserved
    - Spread ordering: in the array passed to `tseslint.config(...)`, `...tseslint.configs.strictTypeChecked` appears BEFORE the `{ files: ['**/*.{ts,tsx}'], … }` block (verified by manual diff review or by `node -e` reading the source)
  </acceptance_criteria>
  <done>eslint.config.js extends strictTypeChecked with projectService wired correctly. Lint surfaces ~226 type-aware errors as a clean baseline for Tasks 2-3 (this plan) and Plan 03 to address. tsc + build + tests stay green.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Fix unbound-method (24 errors) by adding `this: void` to all hook + prop interface methods</name>
  <files>src/hooks/useAudioCues.ts, src/hooks/useWakeLock.ts, src/components/SessionControls.tsx, src/components/EndSessionDialog.tsx, src/components/ResetStatsDialog.tsx, src/components/SettingsForm.tsx, src/components/SettingsStepper.tsx, src/components/LearnAnchor.tsx, src/components/LearnDialog.tsx, src/components/MuteToggle.tsx, src/components/StatsFooter.tsx</files>
  <read_first>
    - src/hooks/useAudioCues.ts (lines 44-58 — the UseAudioCues interface declaration with `start(plan: BreathingPlan): Promise<number | null>`, `stop(): Promise<void>`, `setMuted(muted: boolean): void`)
    - src/hooks/useWakeLock.ts (lines 20-30 — UseWakeLock interface with `request(): Promise<void>`, `release(): Promise<void>`)
    - src/components/SessionControls.tsx (line 6-7 — SessionControlsProps with `onStart(): void`, `onEnd(): void`)
    - src/components/EndSessionDialog.tsx (lines 5-6 — EndSessionDialogProps)
    - src/components/ResetStatsDialog.tsx (lines 5-6 — ResetStatsDialogProps)
    - src/components/SettingsForm.tsx (lines 14-15 — SettingsFormProps)
    - src/components/SettingsStepper.tsx (line 6 — SettingsStepperProps)
    - src/components/LearnAnchor.tsx (line 14 — LearnAnchorProps)
    - src/components/LearnDialog.tsx (line 16 — LearnDialogProps)
    - src/components/MuteToggle.tsx (line 17 — MuteToggleProps)
    - src/components/StatsFooter.tsx (line 23 — StatsFooterProps)
    - .planning/phases/07-strict-type-lint-baseline/07-PATTERNS.md (§"Fix Category 2: unbound-method" — exact post-edit shape for every interface; complete affected-interface list)
    - .planning/phases/07-strict-type-lint-baseline/07-RESEARCH.md (§"unbound-method Rule Explanation and Fix Pattern")
  </read_first>
  <action>
    For every interface enumerated in PATTERNS.md §"Fix Category 2", add `this: void` as the FIRST parameter of EVERY method-shorthand declaration. The transformation is purely a type-signature change — no implementation changes. Per-interface edits (exhaustive — do not skip any):
    1. UseAudioCues: add `this: void` to start (before `plan`), stop (only param), setMuted (before `muted`). Apply to ALL methods declared on the interface, including any not listed here that you discover in the file.
    2. UseWakeLock: add `this: void` to request, release.
    3. SessionControlsProps: add `this: void` to onStart, onEnd.
    4. EndSessionDialogProps: add `this: void` to onConfirm, onCancel.
    5. ResetStatsDialogProps: add `this: void` to onConfirm, onCancel.
    6. SettingsFormProps: add `this: void` to onChange (before `settings: SessionSettings`), onExtendDuration (before `durationMinutes: number`).
    7. SettingsStepperProps: add `this: void` to onChange (before `value: T`).
    8. LearnAnchorProps: add `this: void` to onClick.
    9. LearnDialogProps: add `this: void` to onClose.
    10. MuteToggleProps: add `this: void` to onToggle.
    11. StatsFooterProps: add `this: void` to onResetClick.
    Do NOT change ANY useCallback / arrow-function implementation — implementations are arrow functions and have no `this` binding. Do NOT change call sites — `this: void` on the declaration does not require call-site changes. Do NOT use the alternative arrow-function-property syntax (`onStart: () => void`) as a workaround — RESEARCH.md notes both work but the per-interface analog uses method-shorthand consistently; preserve that style. After the edits, `npm run lint 2>&1 | grep -c "unbound-method"` MUST return 0.
  </action>
  <verify>
    <automated>npm run lint 2>&amp;1 | { grep -c "unbound-method" || echo 0; } | grep -qx 0 &amp;&amp; npx tsc --noEmit -p tsconfig.app.json &amp;&amp; npm run test -- --run 2>&amp;1 | tail -5 | grep -E "363 passed"</automated>
  </verify>
  <acceptance_criteria>
    - `npm run lint 2>&1 | grep -c "unbound-method"` returns `0` (all 24 unbound-method errors eliminated)
    - `npx tsc --noEmit -p tsconfig.app.json` exits 0 (no compile regression)
    - `npx tsc --noEmit -p tsconfig.node.json` exits 0
    - `npm run build` exits 0
    - `npm run test -- --run` reports `363 passed` (D-09 invariant — interface signature change does not affect runtime)
    - Every method declaration in the eleven listed interfaces (`UseAudioCues`, `UseWakeLock`, `SessionControlsProps`, `EndSessionDialogProps`, `ResetStatsDialogProps`, `SettingsFormProps`, `SettingsStepperProps`, `LearnAnchorProps`, `LearnDialogProps`, `MuteToggleProps`, `StatsFooterProps`) has `this: void` as its FIRST parameter — verified by `grep -E "^\s*(on[A-Z]\w*|request|release|start|stop|setMuted)\s*\(" src/hooks/useAudioCues.ts src/hooks/useWakeLock.ts src/components/{SessionControls,EndSessionDialog,ResetStatsDialog,SettingsForm,SettingsStepper,LearnAnchor,LearnDialog,MuteToggle,StatsFooter}.tsx | grep -v "this: void"` returning ZERO method-shorthand declarations missing `this: void`
    - No `useCallback` or arrow-function implementation was modified by this task (verify by `git diff src/hooks src/components` — only interface declaration lines are changed)
    - D-09 commit-boundary trio fully green at task end (lint count for unbound-method = 0; total lint count strictly decreases vs Task 1 baseline)
  </acceptance_criteria>
  <done>All 24 unbound-method errors eliminated by `this: void` on the eleven enumerated interfaces. tsc/build/test trio green; lint count for unbound-method now 0. No implementation code touched. Plan-end lint count reduced by exactly 24.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Fix restrict-template-expressions (16 errors) + no-confusing-void-expression (6 errors) + no-misused-promises (2 errors) — string/JSX hygiene batch</name>
  <files>src/components/BreathingShape.tsx, src/storage/format.ts, src/components/SettingsForm.tsx, src/domain/settings.ts, src/app/App.tsx, src/domain/sessionMath.ts, src/components/SettingsStepper.tsx</files>
  <read_first>
    - src/components/BreathingShape.tsx (lines 100-220 — the 7 sites: line 104 `transform: scale(${orbScale})`, lines 131,132,196,211 `${MIN_SCALE * 100}%`, plus 2 others surfaced by lint)
    - src/storage/format.ts (full file — 3 sites at lines 32, 38, 52 with `${minutes} min`, `${count} sessions`, `${Math.floor(durationSeconds / 60)} min`; existing analog at line 33 `${hours.toFixed(1)} hours`)
    - src/components/SettingsForm.tsx (lines 60, 66 — onChange JSX shorthand; lines with `${value} BPM`-style template literals)
    - src/domain/settings.ts (lines 52, 60 — RangeError messages with `${settings.bpm}`, `${settings.durationMinutes}`)
    - src/app/App.tsx (line 440 — `${frame.cycleIndex}:${frame.phase}` and the 2 no-misused-promises sites where async handlers are passed to JSX props; note: per RESEARCH.md ~line 523 is the canonical `onStart={onStartClick}` site)
    - src/domain/sessionMath.ts (line 48 — `${minutes}:${seconds.toString().padStart(2, '0')}`)
    - src/components/SettingsStepper.tsx (lines 47, 62 — `onClick={() => changeBy(-1)}` / `onClick={() => changeBy(1)}`)
    - .planning/phases/07-strict-type-lint-baseline/07-PATTERNS.md (§"Fix Category 5: restrict-template-expressions" — fix patterns by precision class; §"Fix Category 3: no-confusing-void-expression" — add braces; §"Fix Category 4: no-misused-promises" — JSX void-wrapper)
    - .planning/phases/07-strict-type-lint-baseline/07-RESEARCH.md (§"restrict-template-expressions Rule Explanation", §"no-confusing-void-expression Fix Pattern", §"no-misused-promises in App.tsx")
  </read_first>
  <action>
    Three rule-batched fix passes — apply in order:

    Pass A — restrict-template-expressions (16 errors):
    - For CSS scale/transform values where precision matters: wrap the numeric expression with `.toFixed(N)`. BreathingShape.tsx:104 → `${orbScale.toFixed(4)}`. BreathingShape.tsx:131,132,196,211 → `${(MIN_SCALE * 100).toFixed(2)}%`. Match the existing analog style at storage/format.ts:33 (`${hours.toFixed(1)}`). Apply `.toFixed(2)` to percent dimensions and `.toFixed(4)` to raw scale values to maintain the precision the original numeric interpolation provided.
    - For plain integer/string interpolation (storage/format.ts lines 32, 38, 52; domain/sessionMath.ts:48; settings.ts:52, 60; App.tsx:440 cycleIndex; SettingsForm.tsx labels): wrap with `String(n)`. Examples: `${minutes} min` → `${String(minutes)} min`; `${frame.cycleIndex}:${frame.phase}` → `${String(frame.cycleIndex)}:${frame.phase}` (frame.phase is already a string-literal type and does not need wrapping).
    - Do NOT change `eslint.config.js` to set `allowNumber: true` (D-01 — no cherry-picking).
    - Do NOT introduce a shared formatting helper (D-07 — fix sites inline; the existing format.ts utilities are the right home for any future helper, but are out of Phase 7 scope).

    Pass B — no-confusing-void-expression (6 errors):
    - For each JSX arrow shorthand in SettingsStepper.tsx:47, SettingsStepper.tsx:62, App.tsx (the 2 sites — likely clearLeadInTimeouts() / similar — confirm at lint time), SettingsForm.tsx:60, SettingsForm.tsx:66 (and any others surfaced): add braces around the body. Pattern: `onClick={() => changeBy(-1)}` → `onClick={() => { changeBy(-1) }}`; `onChange={(bpm) => updateSettings({ bpm })}` → `onChange={(bpm) => { updateSettings({ bpm }) }}`.
    - Do NOT remove the arrow function and pass the bare callable — that would change argument-count semantics.

    Pass C — no-misused-promises (2 errors in App.tsx):
    - For each JSX prop where an async function is passed to a `() => void`-typed handler (e.g., `onStart={onStartClick}` at App.tsx ~line 523, plus the second site surfaced by lint), wrap with the void-expression pattern: `onStart={() => { void onStartClick() }}`. This matches the existing `void audioStop()` analog at App.tsx:234 and is D-08 Option A (no interface change).
    - Do NOT widen `SessionControlsProps.onStart` to `() => void | Promise<void>` (D-08 — interface change is Plan 11/Phase 11 scope if needed; for Phase 7 the JSX wrapper is the behavior-preserving fix).

    After all three passes, `npm run lint 2>&1 | grep -cE "(restrict-template-expressions|no-confusing-void-expression|no-misused-promises)"` MUST return 0.
  </action>
  <verify>
    <automated>npm run lint 2>&amp;1 | { grep -cE "(restrict-template-expressions|no-confusing-void-expression|no-misused-promises)" || echo 0; } | grep -qx 0 &amp;&amp; npx tsc --noEmit -p tsconfig.app.json &amp;&amp; npm run build &amp;&amp; npm run test -- --run 2>&amp;1 | tail -5 | grep -E "363 passed"</automated>
  </verify>
  <acceptance_criteria>
    - `npm run lint 2>&1 | grep -c "restrict-template-expressions"` returns `0` (all 16 fixed)
    - `npm run lint 2>&1 | grep -c "no-confusing-void-expression"` returns `0` (all 6 fixed)
    - `npm run lint 2>&1 | grep -c "no-misused-promises"` returns `0` (all 2 fixed)
    - `npx tsc --noEmit -p tsconfig.app.json` exits 0
    - `npm run build` exits 0 — CRITICAL: BreathingShape.tsx CSS values must not regress visually; the `.toFixed(N)` outputs are equivalent to the prior numeric interpolation up to precision N
    - `npm run test -- --run` reports `363 passed` — D-09 behavior preservation (any test that asserts on a CSS string output must still pass; if a test breaks, the precision N was wrong and must be widened)
    - The 2 App.tsx JSX async-handler sites use the pattern `() => { void <fnName>() }` (verified by `grep -cE "void\s+\w+Click\(\)" src/app/App.tsx` returning >= 2)
    - No `eslint.config.js` rule is set to `'off'` for any of restrict-template-expressions / no-confusing-void-expression / no-misused-promises (verified by `grep -cE "(restrict-template-expressions|no-confusing-void-expression|no-misused-promises).*off" eslint.config.js` returning 0)
    - No new helper module was created (D-07; verified by `git diff --stat` showing only the seven files in `<files>` were modified, no `src/utils/format.ts` or similar)
    - Plan-end lint count: at this task end, total lint count = 226 (preset baseline) - 24 (Task 2) - 24 (this task: 16+6+2) = 178 remaining errors (all are isolated production singletons + 162 test errors + 1 stale disable warning, all of which Plan 03 closes). The count strictly decreases vs Task 2 end.
  </acceptance_criteria>
  <done>All 24 errors across the three rules eliminated by inline fixes following the pattern catalog in PATTERNS.md. CSS visual output preserved (toFixed precision matches original numeric interpolation). 363/363 tests pass; build green. Plan-end production rule clusters fully closed for unbound-method + restrict-template-expressions + no-confusing-void-expression + no-misused-promises (~48 of 64 production errors gone).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none introduced) | This plan modifies build-toolchain config (eslint.config.js — dev-only ESLint preset upgrade) and production source (interface signatures with `this: void`, template-literal hygiene with `String()` / `.toFixed()`, JSX braces, JSX void-wrappers). No new endpoints, no new auth/authz logic, no new data flows, no new runtime dependencies. typescript-eslint and @typescript-eslint/parser are dev-only deps already installed and do not change runtime semantics. All edits are behavior-preserving inline fixes per D-08. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-02-01 | (n/a) | eslint.config.js + 16 production source files (11 interface declarations + 7 string/JSX hygiene targets, with overlap so 16 unique files total) | accept | No runtime threat surface — dev-only ESLint config swap plus type-signature annotations and template-literal hygiene that preserve runtime output. Indirect security tightening: no-misused-promises now forces explicit `void` markers on intentional fire-and-forget JSX handlers (no silent unhandled-rejection paths). Behavior-preservation invariant D-08/D-09 enforced by full Vitest suite (363/363) at every commit boundary. |
</threat_model>

<verification>
## Plan-Level Verification

Run at the end of this plan (before Plan 03 begins):

1. `npx tsc --noEmit -p tsconfig.app.json && npx tsc --noEmit -p tsconfig.node.json` — both exit 0 (Plan 01 baseline preserved)
2. `npm run build` — exits 0
3. `npm run test -- --run` — reports `363 passed` (D-09 invariant)
4. `grep -c "tseslint.configs.strictTypeChecked" eslint.config.js` returns 1 (D-01 implementation)
5. `grep -c "projectService" eslint.config.js` returns >= 1 (D-02 implementation)
6. `grep -c "exhaustive-deps" eslint.config.js` returns 0 (Plan 04 owns this override)
7. `npm run lint 2>&1 | grep -c "unbound-method"` returns 0
8. `npm run lint 2>&1 | grep -cE "(restrict-template-expressions|no-confusing-void-expression|no-misused-promises)"` returns 0
9. `npm run lint 2>&1` total problem count is strictly less than 226 (the post-Task-1 baseline) and strictly greater than 0 (Plan 03 closes the rest). Approximate expected count: ~177-178 (RESEARCH.md predicts 226 - 48 = 178).
10. Total file-modified count by this plan matches the `files_modified` frontmatter (no scope creep into Plan 03 territory: usePrefersReducedMotion, audioEngine, cueSynth, useAudioCues isolated sites, main.tsx, test files, vitest.setup.ts, App.tsx:411 stale disable removal — all UNTOUCHED here)
</verification>

<success_criteria>
- BUILD-02 partial satisfaction: eslint.config.js extends strictTypeChecked + projectService (preset upgrade complete); 48 of 64 production errors eliminated by inline rule-batched fixes per PATTERNS.md (the 16 isolated production singletons + 162 test-file errors + App.tsx:411 stale disable remain for Plan 03 to close)
- D-09 partial-quad green at task and plan boundaries: tsc, build, test (363/363) all green at every commit; lint exit-code remains non-zero (~177 errors remaining) but lint problem count strictly decreases monotonically across Tasks 1 → 2 → 3. The full D-09 lint-exit-0 invariant first becomes binding at the end of Plan 03.
- Plan 03 unblocked: all rule-cluster fixes done; only single/few-occurrence production rules + bulk test errors + 1 stale disable remain
- App.tsx:411 stale disable preserved (untouched here — Plan 03 removes it as part of the lint-exit-0 push)
</success_criteria>

<output>
After completion, create `.planning/phases/07-strict-type-lint-baseline/07-02-SUMMARY.md` per the standard summary template, including:
- Pre/post ESLint problem counts (226 → ~178 expected)
- Per-rule eliminated counts (unbound-method 24, restrict-template-expressions 16, no-confusing-void-expression 6, no-misused-promises 2 = 48 total)
- Test pass count (363/363)
- List of every file with the count of fixes applied
- Confirmation Plan 03 territory was NOT touched (no usePrefersReducedMotion, no audioEngine, no cueSynth isolated sites, no useAudioCues line 124/261, no main.tsx, no test files, no vitest.setup.ts, no App.tsx:411 removal, no App.tsx audio-deps fix)
- Note any tasks where the actual lint fire-pattern differed from RESEARCH.md (executor judgment log)
</output>
</content>
</invoke>