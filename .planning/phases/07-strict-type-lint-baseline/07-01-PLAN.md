---
phase: 07-strict-type-lint-baseline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tsconfig.app.json
  - tsconfig.node.json
  - src/app/App.audio.test.tsx
  - src/app/App.wakeLock.test.tsx
  - src/audio/cueSynth.test.ts
  - src/hooks/useAudioCues.test.tsx
autonomous: true
requirements:
  - BUILD-01

must_haves:
  truths:
    - "tsconfig.app.json compilerOptions sets strict, noUncheckedIndexedAccess, noImplicitReturns to true (in addition to the preserved noFallthroughCasesInSwitch)"
    - "tsconfig.node.json compilerOptions sets the same three new flags"
    - "npx tsc --noEmit -p tsconfig.app.json exits 0 against the strict-mode-tightened config"
    - "npx tsc --noEmit -p tsconfig.node.json exits 0 against the strict-mode-tightened config"
    - "npm run build exits 0"
    - "npm run test -- --run reports 363/363 passing"
    - "All 12 surfaced TS2532/TS18048 errors in the four target test files are eliminated by inline narrowing fixes (no behavior change to test assertions)"
  artifacts:
    - path: "tsconfig.app.json"
      provides: "Strict TypeScript compile contract for src/ tree (production + tests, per D-03)"
      contains: '"strict": true'
    - path: "tsconfig.node.json"
      provides: "Strict TypeScript compile contract for vite.config.ts"
      contains: '"strict": true'
    - path: "src/app/App.audio.test.tsx"
      provides: "Audio test suite with mock.calls[N] accesses narrowed for noUncheckedIndexedAccess"
    - path: "src/app/App.wakeLock.test.tsx"
      provides: "Wake-lock test suite with mock.results[N].value accesses narrowed"
    - path: "src/audio/cueSynth.test.ts"
      provides: "Cue-synth test suite with oscillators[0] / filters[0] accesses narrowed"
    - path: "src/hooks/useAudioCues.test.tsx"
      provides: "useAudioCues test suite with reanchorSpy.mock.calls[0][0] narrowed"
  key_links:
    - from: "tsconfig.app.json"
      to: "tsc --noEmit pipeline + Plan 02 ESLint projectService"
      via: "compilerOptions.strict + compilerOptions.noUncheckedIndexedAccess"
      pattern: '"strict": true'
    - from: "tsconfig.node.json"
      to: "tsc --noEmit pipeline (vite.config.ts) + Plan 02 ESLint projectService for node-tier files"
      via: "compilerOptions.strict + compilerOptions.noUncheckedIndexedAccess"
      pattern: '"strict": true'
    - from: "test file mock.calls[N] / mock.results[N] sites"
      to: "TypeScript narrowing rules under noUncheckedIndexedAccess"
      via: "either ?. chain, .at() + toBeDefined, or non-null assertion preceded by '// Reason:' length-asserted-above annotation"
      pattern: 'mock\.(calls|results)\[[0-9]+\](\?|!)'
---

<objective>
Land strict TypeScript on both tsconfig.app.json and tsconfig.node.json (BUILD-01) and fix the 12 test-file compile errors that strict surfaces. This is the smallest, lowest-risk piece of Phase 7 — production code is already strict-clean per RESEARCH.md (12 errors total, ALL in 4 test files). Landing tsc strict first unblocks Plan 02's ESLint type-aware rules (parserOptions.projectService requires strict tsconfig to resolve correctly).

Purpose: Establish the compiler-enforced baseline (per D-06 — no staged rollout) so every subsequent v1.0.1 phase writes code under strict + noUncheckedIndexedAccess + noImplicitReturns. Behavior is preserved (D-08, D-09) — the 12 fixes are pure type-narrowing on test mock accesses.

Output: tsconfig.app.json + tsconfig.node.json with three new compilerOptions; four test files with mock-array narrowing applied; tsc + lint + build + 363/363 vitest all green at commit boundary.
</objective>

<d09_scope_note>
**D-09 lint-exit-0 invariant — phased application:** D-09 mandates that `npm run lint` exits 0 at every commit boundary. In Plan 07-01 (this plan), the strictTypeChecked preset is NOT yet active (the preset upgrade is Plan 07-02 Task 1 scope). Therefore `npm run lint` under the current `recommended` baseline will continue to surface the 226 pre-existing errors that strictTypeChecked would generate, plus zero new errors from this plan's edits. Plan 07-01 tasks gate on **lint-count-not-increased** (226 baseline) rather than **lint-exits-0**, because the preset that generates those 226 errors only lands in 07-02 Task 1.

The full D-09 lint-exit-0 invariant becomes binding from the Plan 07-02 Task 1 commit (the strictTypeChecked + projectService landing) onward. Plans 07-02 → 07-03 → 07-04 each carry the lint-exit-0 gate at their commit boundaries. The first commit at which `npm run lint` exits 0 across ALL files is Plan 07-03 (the test-file fix pass that drives the 162 test errors to zero).

This phased relaxation is the only departure from D-09 in Phase 7 and is logged here for traceability.
</d09_scope_note>

<execution_context>
@$HOME/.claude-personal-personal/get-shit-done/workflows/execute-plan.md
@$HOME/.claude-personal-personal/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-strict-type-lint-baseline/07-CONTEXT.md
@.planning/phases/07-strict-type-lint-baseline/07-RESEARCH.md
@.planning/phases/07-strict-type-lint-baseline/07-PATTERNS.md
@.planning/phases/07-strict-type-lint-baseline/07-VALIDATION.md

<interfaces>
<!-- Verified post-edit shapes for the two tsconfig files (PATTERNS.md §"tsconfig.app.json" and §"tsconfig.node.json"). Both are surgical inserts AFTER the existing noFallthroughCasesInSwitch line in the /* Linting */ block. D-10 preserves the existing four flags; do not rewrite the file. -->

tsconfig.app.json /* Linting */ block — post-edit shape:
- "noUnusedLocals": true,           (existing, preserve)
- "noUnusedParameters": true,       (existing, preserve)
- "erasableSyntaxOnly": true,       (existing, preserve)
- "noFallthroughCasesInSwitch": true, (existing, preserve — D-10)
- "strict": true,                   (NEW — BUILD-01)
- "noUncheckedIndexedAccess": true, (NEW — BUILD-01, D-06)
- "noImplicitReturns": true         (NEW — BUILD-01)

tsconfig.node.json /* Linting */ block — identical insert pattern.

Test-file mock-access narrowing patterns (PATTERNS.md §"Fix Category 1"):
- Option A (length asserted above): const x = arr[0]!  preceded by `// Reason: length asserted by toHaveBeenCalledTimes(N) immediately above.`
- Option B (.at + toBeDefined):     const item = arr.at(0); expect(item).toBeDefined(); then use item!.foo
- Option C (?. chain):              const stopArg = stopMock.mock.calls[0]?.[0] as number  (already in cueSynth.test.ts:70 — preferred for single-level access)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add strict + noUncheckedIndexedAccess + noImplicitReturns to both tsconfigs</name>
  <files>tsconfig.app.json, tsconfig.node.json</files>
  <read_first>
    - tsconfig.app.json (full file — confirm current /* Linting */ block ends with noFallthroughCasesInSwitch)
    - tsconfig.node.json (full file — confirm same shape minus jsx)
    - .planning/phases/07-strict-type-lint-baseline/07-PATTERNS.md (§"tsconfig.app.json — Config Modification" and §"tsconfig.node.json — Config Modification" — exact insertion point and post-edit shape)
    - .planning/phases/07-strict-type-lint-baseline/07-CONTEXT.md (D-06: enable noUncheckedIndexedAccess together with the rest of strict; D-10: preserve existing flags including noFallthroughCasesInSwitch)
  </read_first>
  <action>
    Surgical edit (per D-10 — preserve existing flags). In BOTH tsconfig.app.json and tsconfig.node.json, append three new keys at the END of the `/* Linting */` block, AFTER the existing `"noFallthroughCasesInSwitch": true` line and inside the same `compilerOptions` object. The keys are: `"strict": true`, `"noUncheckedIndexedAccess": true`, `"noImplicitReturns": true`. Maintain JSON syntax (add a trailing comma to the noFallthroughCasesInSwitch line). Do NOT add `"noFallthroughCasesInSwitch"` again (per RESEARCH.md and D-10 it is already present in both files). Do NOT touch tsconfig.json (root project-references shell needs no change). Do NOT add any other compilerOptions key. Do NOT change `include` arrays (per D-03, src/**/*.test.ts(x) stays inside tsconfig.app.json include — no test tsconfig split). After both edits, run `npx tsc --noEmit -p tsconfig.app.json` and `npx tsc --noEmit -p tsconfig.node.json` to surface the 12 expected errors that Task 2 will fix. Do NOT proceed to Task 2 until both tsc invocations show errors ONLY in the four test files named in Task 2.
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.node.json 2>&1 | { grep -c "error TS" || echo 0; } | grep -qx 0 &amp;&amp; node -e "const a=require('./tsconfig.app.json'),b=require('./tsconfig.node.json');const need=['strict','noUncheckedIndexedAccess','noImplicitReturns','noFallthroughCasesInSwitch'];for(const k of need){if(a.compilerOptions[k]!==true){console.error('app missing',k);process.exit(1)}if(b.compilerOptions[k]!==true){console.error('node missing',k);process.exit(1)}}console.log('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `tsconfig.app.json` `compilerOptions` contains exactly these four flags set to `true`: `strict`, `noUncheckedIndexedAccess`, `noImplicitReturns`, `noFallthroughCasesInSwitch` (verified by `node -e "..."` JSON read in the verify command above)
    - `tsconfig.node.json` `compilerOptions` contains the same four flags set to `true`
    - `tsconfig.app.json` `include` is unchanged (still `["src"]`)
    - `tsconfig.node.json` `include` is unchanged (still `["vite.config.ts"]`)
    - `tsconfig.json` (root) is NOT modified by this task
    - `npx tsc --noEmit -p tsconfig.node.json` exits 0 (vite.config.ts has no surfaced errors per RESEARCH.md)
    - `npx tsc --noEmit -p tsconfig.app.json` exits non-zero with errors ONLY in these four files: `src/app/App.audio.test.tsx`, `src/app/App.wakeLock.test.tsx`, `src/audio/cueSynth.test.ts`, `src/hooks/useAudioCues.test.tsx` (Task 2 fixes them)
    - No production source file (anything under src/ NOT ending in `.test.ts(x)`) appears in the tsc error output
  </acceptance_criteria>
  <done>Both tsconfig files have the three new strict-mode flags appended after noFallthroughCasesInSwitch with no other change. tsc against tsconfig.node.json is clean. tsc against tsconfig.app.json fails ONLY in the four named test files, surfacing exactly the 12 errors RESEARCH.md predicts. No production file regresses.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Narrow the 12 mock.calls[N] / mock.results[N] / array[0] accesses in the four affected test files</name>
  <files>src/app/App.audio.test.tsx, src/app/App.wakeLock.test.tsx, src/audio/cueSynth.test.ts, src/hooks/useAudioCues.test.tsx</files>
  <read_first>
    - src/app/App.audio.test.tsx (find the 2 sites where `mock.calls[0]` is accessed — surfaced as TS18048; current line numbers per RESEARCH.md §"Blast Radius: tsc")
    - src/app/App.wakeLock.test.tsx (find the 4 sites where `mock.results[0].value` is accessed; PATTERNS.md §"Fix Category 1" shows current site at line 64)
    - src/audio/cueSynth.test.ts (find the 4 sites — `oscillators[0]`, `filters[0]`, `mock.calls[0]`; PATTERNS.md shows existing analog at line 70 already using `?.` — match that style for new fixes)
    - src/hooks/useAudioCues.test.tsx (find the 2 sites where `reanchorSpy.mock.calls[0][0]` is accessed; PATTERNS.md shows site at line 647)
    - .planning/phases/07-strict-type-lint-baseline/07-PATTERNS.md (§"Fix Category 1: mock.calls[N] Narrowing in Test Files" — three fix options A/B/C with exact code shapes; §"Shared Patterns / Non-null assertion with length assertion")
    - .planning/phases/07-strict-type-lint-baseline/07-CONTEXT.md (D-04 annotation policy: every `// eslint-disable` MUST be preceded by `// Reason: …`; D-07: prefer inline narrowing over a `safeAt(arr, i)` helper — DO NOT introduce any helper; D-08: no behavior change)
  </read_first>
  <action>
    For each of the 12 surfaced TS2532/TS18048 errors in the four target test files, apply the smallest behavior-preserving narrowing per PATTERNS.md §"Fix Category 1". Selection rule (deterministic — no executor judgment):
    1. If the surrounding test already calls `expect(spy).toHaveBeenCalledTimes(N)` (or equivalent length assertion) on the same mock array IMMEDIATELY above the site — use Option A: insert a single-line `// Reason: length asserted by toHaveBeenCalledTimes(N) immediately above.` comment ABOVE the access, then change `arr[0]` to `arr[0]!`. The `// Reason:` comment is REQUIRED by D-04 even though `!` is not an `eslint-disable`; the annotation documents the invariant.
    2. If the access is a single-level subscript with no preceding length assertion (e.g., `stopMock.mock.calls[0]?.[0]`) — use Option C: replace `arr[0].x` with `arr[0]?.x` (optional chain). This matches the existing analog at `cueSynth.test.ts:70`.
    3. If the access is multi-level chained (e.g., `reanchorSpy.mock.calls[0][0]`) — use Option B: introduce a local `const call = arr.at(0); expect(call).toBeDefined();` then use `call![1]` for downstream access. Add `// Reason: toBeDefined assertion above narrows call to non-undefined for typecheck.`
    Apply ONE option per site (do NOT mix). Do NOT modify any test assertion semantics — `expect(...)` calls and their arguments must remain functionally identical. Do NOT add new `expect` calls except as part of Option B's `expect(call).toBeDefined()` pattern. Do NOT introduce a `safeAt` helper, a shared narrowing utility, or any new module (D-07 forbids). Do NOT modify production source (the 64 production ESLint errors are Plan 02 scope, not this plan; the 12 tsc errors are ALL in test files per RESEARCH.md). After all 12 sites are fixed, the D-09 commit-boundary trio MUST be green.
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.app.json &amp;&amp; npx tsc --noEmit -p tsconfig.node.json &amp;&amp; npm run build &amp;&amp; npm run test -- --run 2>&amp;1 | tail -5 | grep -E "Tests +363 passed|363 passed"</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit -p tsconfig.app.json` exits 0 (zero errors)
    - `npx tsc --noEmit -p tsconfig.node.json` exits 0
    - `npm run build` exits 0
    - `npm run test -- --run` reports `363 passed` (count must match the milestone-start baseline; D-09 invariant)
    - Every modified site uses ONE of the three fix patterns from PATTERNS.md §"Fix Category 1"; NO new helper module or shared utility is introduced (verify by `git diff --stat` showing only the four named test files modified, no new files added)
    - Every non-null assertion (`!`) added in this task is preceded by a `// Reason:` line on the line immediately above (verify by `grep -B1 "\!" src/app/App.audio.test.tsx src/app/App.wakeLock.test.tsx src/audio/cueSynth.test.ts src/hooks/useAudioCues.test.tsx | grep -c "Reason:"` returning a count >= the number of `!` non-null assertions added in this task)
    - No `expect(...)` argument value or matcher is changed in semantically meaningful ways (manual diff review — only narrowing-style additions like `expect(item).toBeDefined()` are allowed)
    - D-09 commit-boundary trio: `npx tsc --noEmit -p tsconfig.app.json && npx tsc --noEmit -p tsconfig.node.json && npm run lint && npm run test -- --run && npm run build` all exit 0 (note: `npm run lint` is expected to still report the 226 pre-existing ESLint errors that Plan 02 fixes — but this task itself MUST NOT introduce any NEW lint regression beyond that baseline; verify by counting lint errors before vs after this task — count must not increase)
  </acceptance_criteria>
  <done>All 12 strict-mode tsc errors in the four target test files are eliminated by Option A/B/C narrowing per PATTERNS.md. Test count is 363 passing. Build is green. No production source file was touched by this task. No new helper modules were created. Every non-null assertion added carries a `// Reason:` annotation per D-04.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none introduced) | This plan only modifies build-toolchain config (tsconfig.app.json, tsconfig.node.json) and test-only narrowing in four test files. No new endpoints, no new auth/authz logic, no new data flows, no new runtime dependencies. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01-01 | (n/a) | tsconfig.app.json + tsconfig.node.json + 4 test files | accept | No runtime threat surface — config-only changes plus test-file type narrowing. typescript and typescript-eslint are dev-only deps already installed (RESEARCH.md §Environment Availability). Behavior-preservation invariant D-08/D-09 enforced by full Vitest suite (363/363) at the commit boundary. |
</threat_model>

<verification>
## Plan-Level Verification

Run at the end of this plan (before /gsd-verify-work):

1. `npx tsc --noEmit -p tsconfig.app.json && npx tsc --noEmit -p tsconfig.node.json` — exit 0 (BUILD-01 gate part 1)
2. `npm run build` — exit 0 (BUILD-01 gate part 2)
3. `npm run test -- --run` — output contains `363 passed` (D-09 behavior-preservation gate)
4. `git diff --stat tsconfig.app.json tsconfig.node.json` — shows only the three new compilerOptions added in each, no other change
5. `git diff --stat src/` — shows ONLY the four named test files modified, NO production source files touched, NO new files added
6. `npm run lint 2>&1 | grep -c "problem"` — count must be `<=` the pre-Task-1 baseline (226 errors per RESEARCH.md). This plan must not introduce NEW lint regressions; the 226 pre-existing errors are Plan 02 scope.
</verification>

<success_criteria>
- BUILD-01 satisfied: tsconfig.app.json + tsconfig.node.json carry strict, noUncheckedIndexedAccess, noImplicitReturns (plus preserved noFallthroughCasesInSwitch); tsc --noEmit on both exits 0
- All 12 surfaced TS2532/TS18048 errors in the four test files are eliminated by inline narrowing (no helper module, no behavior change)
- D-09 quad green: `npx tsc --noEmit` (both projects), `npm run lint` (no new regressions vs baseline), `npm run build`, `npm run test -- --run` (363/363)
- Plan 02 unblocked: ESLint can now wire `parserOptions.projectService` against the strict tsconfig
</success_criteria>

<output>
After completion, create `.planning/phases/07-strict-type-lint-baseline/07-01-SUMMARY.md` per the standard summary template, including:
- Final tsc error counts (must be 0/0 across both tsconfigs)
- Final test pass count (must be 363/363)
- Pre- vs post-Task-1 ESLint error count (baseline 226 expected)
- Per-file fix log: which of Option A/B/C was applied at each of the 12 sites, and the `// Reason:` text used for each non-null assertion
- Confirmation no production source file was modified by this plan
</output>
