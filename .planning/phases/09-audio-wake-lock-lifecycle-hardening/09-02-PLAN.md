---
phase: 09-audio-wake-lock-lifecycle-hardening
plan: 02
type: execute
wave: 2
depends_on: ['01']
files_modified:
  - src/hooks/useAudioCues.ts
  - src/hooks/useAudioCues.test.tsx
  - src/app/App.tsx
  - src/app/App.audio.test.tsx
autonomous: true
requirements:
  - AUDIO-01
  - AUDIO-02
  - AUDIO-03
  - AUDIO-05
  - AUDIO-06

must_haves:
  truths:
    - "useAudioCues.reconstructEngine stamps a monotonic generation counter (reconstructGenerationRef) at the start, then post-await checks the captured local against the live ref — if mismatched, the new engine is closed and engineRef is left null."
    - "useAudioCues.stop() and the unmount cleanup BOTH bump reconstructGenerationRef in addition to the existing synchronous-null pattern (defense in depth — Pitfall 1)."
    - "useAudioCues.handleStateChange has a single null gate at the top of the handler — const engine = engineRef.current; if (engine === null) return — protecting ANY future branch that reads engineRef.current (D-04 / D-06). Per D-05 the larger reshape — moving addEventListener('statechange') to after the WR-06 resume completes (option b) — is explicitly deferred to v1.x to keep this phase surgical; a JSDoc cross-ref at the top of the new null-safe handler records the deferral."
    - "useAudioCues.start() handles a null return from engine.scheduleLeadIn by falling through to the existing failure path (setAudioAvailable(false); setStatus('failed'); return null)."
    - "useAudioCues no longer calls setStatus('starting') in start() — transition is 'idle' → 'lead-in' (success) or 'idle' → 'failed' directly (D-08). The state-machine docstring at the top of the file is rewritten to remove the dead-state explanation (D-09)."
    - "App.tsx boundary effect imports SAFE_LEAD_SEC from '../audio/audioEngine'; reads audio.audioNow() into a local; skips the schedule when audioNow is null; otherwise computes clampedAudioTime = Math.max(audioTime, liveAudioNow + SAFE_LEAD_SEC) and passes clampedAudioTime to audioNotifyPhaseBoundary. Implements D-01 (caller-side / App.tsx half of the belt-and-suspenders clamp pair — the App's explicit timing math anchor + boundaryStartMs / 1000 cannot ship a past-time value into the engine), D-02 (Math.max formula at both sites; do not subtract or transform after), and D-03 (import the named SAFE_LEAD_SEC export from audioEngine.ts — single source of truth, no duplicated literals)."
    - "Plan packaging follows D-11 (two plans, two waves; Wave 2 = hook-and-App-layer plan that consumes the Wave 1 API plus the caller-side clamp) and D-13 (Plan 02 ships AUDIO-01 reconstructEngine generation counter + bail-and-close on mismatch with stop/unmount bumping the same counter, AUDIO-05 handleStateChange null-safe end-to-end per D-04, AUDIO-03 hook-side null propagation in useAudioCues.start returning number | null, AUDIO-06 hook-side cleanup dropping setStatus('starting') + rewriting the docstring per D-08/D-09, AUDIO-02 caller-side clamp at App.tsx:504 importing SAFE_LEAD_SEC from audioEngine; files modified: src/hooks/useAudioCues.ts, src/hooks/useAudioCues.test.tsx, src/app/App.tsx, src/app/App.audio.test.tsx; depends_on: ['01']). Test geography follows D-14: new contract assertions co-locate in each file's existing *.test.{ts,tsx} neighbor — no new test files."
  artifacts:
    - path: "src/hooks/useAudioCues.ts"
      provides: "reconstructGenerationRef declaration + bumps in stop()/unmount/reconstructEngine + post-await bail; handleStateChange single null gate; start() null-return propagation; setStatus('starting') removal; docstring rewrite."
      contains: "reconstructGenerationRef"
    - path: "src/hooks/useAudioCues.test.tsx"
      provides: "AUDIO-01 stop-during-reconstruct test; AUDIO-05 synthetic statechange after stop test; AUDIO-03 hook-side null-propagation test; AUDIO-06 hook-side cleanup test (no transient 'starting' render) + union exhaustive-switch lock."
      contains: "AUDIO-01"
    - path: "src/app/App.tsx"
      provides: "SAFE_LEAD_SEC import; caller-side clamp at the boundary effect (line ~504 region) with audio.audioNow() null gate."
      contains: "SAFE_LEAD_SEC"
    - path: "src/app/App.audio.test.tsx"
      provides: "AUDIO-02 caller-side clamp test using installTrackedAC + cueSynth.scheduleOutCue spy."
      contains: "SAFE_LEAD_SEC"
  key_links:
    - from: "src/hooks/useAudioCues.ts reconstructEngine"
      to: "newEngine.close() orphan path"
      via: "post-await gen !== reconstructGenerationRef.current bail check"
      pattern: "gen !== reconstructGenerationRef\\.current"
    - from: "src/hooks/useAudioCues.ts handleStateChange"
      to: "early return on engineRef.current === null"
      via: "const engine = engineRef.current; if (engine === null) return at top of handler"
      pattern: "if \\(engine === null\\) return"
    - from: "src/hooks/useAudioCues.ts start()"
      to: "setStatus('failed') / setAudioAvailable(false) / return null"
      via: "null check on firstInCueTime after engine.scheduleLeadIn"
      pattern: "if \\(firstInCueTime === null\\)"
    - from: "src/app/App.tsx boundary effect"
      to: "audioNotifyPhaseBoundary with clampedAudioTime"
      via: "Math.max(audioTime, liveAudioNow + SAFE_LEAD_SEC) after audio.audioNow() null gate"
      pattern: "Math\\.max\\(audioTime, liveAudioNow \\+ SAFE_LEAD_SEC\\)"
---

<objective>
Plan 02 lands the Wave 2 hook+App-layer changes for Phase 9 against the Plan 01 (Wave 1) engine-layer API contracts. This plan delivers:

- AUDIO-01: `useAudioCues.reconstructEngine` gains a `reconstructGenerationRef` integer generation counter; `stop()` and the unmount cleanup bump the same counter; the post-await branch in `reconstructEngine` bails (closes the orphaned new engine) on counter mismatch. The existing synchronous-null pattern is preserved on top of the counter (Pitfall 1 — defense in depth).
- AUDIO-05: `useAudioCues.handleStateChange` adds a single null gate at the top of the handler (`const engine = engineRef.current; if (engine === null) return`) per D-04 / D-06. Matches the existing Plan 06 WR-06 visibility-resume single-gate pattern at `useAudioCues.ts:156–158`.
- AUDIO-03 (hook half): `useAudioCues.start()` propagates the `null` return from `engine.scheduleLeadIn` (now `number | null` after Plan 01) up to its existing `number | null` return — falls through to the existing failure path on null.
- AUDIO-06 (hook half): Delete `setStatus('starting')` call at `useAudioCues.ts:192`; rewrite the state-machine docstring at the top of the file per D-08 / D-09; add a Vitest case using exhaustive switch / `satisfies` to lock the tightened union shape (D-10).
- AUDIO-02 (caller half): Add `SAFE_LEAD_SEC` to the `App.tsx` import from `'../audio/audioEngine'`; introduce the caller-side clamp at the boundary effect (~line 504) with the Pitfall-5 null gate on `audio.audioNow()`.

Purpose: consume the Plan 01 tightened API contracts; close the imperative-resource races and dead-code paths in the hook/App layer; ship Phase 9 with all six Roadmap success criteria observable.

Output: 4 modified files; new co-located test cases per D-14; the hook + App layer fully consume the Wave 1 engine-layer API.
</objective>

<execution_context>
@$HOME/.claude-personal-personal/get-shit-done/workflows/execute-plan.md
@$HOME/.claude-personal-personal/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/09-audio-wake-lock-lifecycle-hardening/09-CONTEXT.md
@.planning/phases/09-audio-wake-lock-lifecycle-hardening/09-RESEARCH.md
@.planning/phases/09-audio-wake-lock-lifecycle-hardening/09-VALIDATION.md
@.planning/phases/09-audio-wake-lock-lifecycle-hardening/09-PATTERNS.md
@.planning/phases/09-audio-wake-lock-lifecycle-hardening/09-01-PLAN.md
@.planning/phases/07-strict-type-lint-baseline/07-CONTEXT.md
@.planning/phases/08-storage-forward-compat-cross-tab-ui-sync/08-CONTEXT.md

<interfaces>
<!-- Contracts produced by Plan 01 (Wave 1) that this plan consumes. -->

From src/audio/audioEngine.ts (post-Plan-01 surface):

  export const SAFE_LEAD_SEC: number                              // 0.005 (Plan 01 Task 1)
  export type AudioStatus = 'idle' | 'lead-in' | 'failed'         // 'starting' removed (Plan 01 Task 1)
  scheduleLeadIn(startAudioTime: number, plan: BreathingPlan): number | null   // returns null when closed (Plan 01 Task 1)

From src/hooks/useAudioCues.ts (existing — read for context, then modify per tasks below):

  // Canonical generation-counter precedent from src/app/App.tsx (Plan 04 startGenerationRef pattern):
  const startGenerationRef = useRef(0)   // bumped in cancel + start paths, captured into local, compared post-await
  // This plan introduces an analogous reconstructGenerationRef inside useAudioCues.

  // Existing synchronous-null pattern preserved (Pitfall 1 — do NOT replace):
  //   reconstructEngine: engineRef.current = null BEFORE await createAudioEngine
  //   stop():            engineRef.current = null BEFORE await engine.close()
  //   unmount cleanup:   engineRef.current = null BEFORE void engine.close()

  // Existing single-gate-on-engineRef precedent (visibility-resume effect, lines 156–158):
  //   if (engineRef.current === null) return — applied verbatim to handleStateChange.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: AUDIO-01 reconstructGenerationRef + AUDIO-05 handleStateChange null gate + AUDIO-06 hook-side cleanup + AUDIO-03 hook-side null propagation in src/hooks/useAudioCues.ts and its test</name>
  <files>src/hooks/useAudioCues.ts, src/hooks/useAudioCues.test.tsx</files>

  <read_first>
    - src/hooks/useAudioCues.ts (read fully — the entire file, paying attention to: docstring at L5–7, handleStateChange at L116–150, stop() at L188–230, start() at L188–218, reconstructEngine at L254–299, unmount cleanup at L141–150, visibility-resume single gate at L156–158, setStatus('starting') call at L192)
    - src/hooks/useAudioCues.test.tsx (read selectively — focus on: start success case at L35–54, SpyableAC pattern at L447–503, SlowCloseAC pattern at L248–289, D-41 (c) at L558–579, D-41 (d) discriminating assertion at L581–625)
    - src/app/App.tsx lines 152–158 + 266–309 (the canonical startGenerationRef precedent — bump-then-stamp-then-compare across cancel + start paths)
    - .planning/phases/09-audio-wake-lock-lifecycle-hardening/09-RESEARCH.md §Recommended Code Shapes §A (full reconstructEngine + stop + unmount skeleton), §C (hook-side null propagation from scheduleLeadIn), §E (handleStateChange null gate), §F (AudioStatus union test), §Open Question 4 (test infrastructure for stop-during-reconstruct), §Pitfall 1 (do NOT remove synchronous-null), §Pitfall 6 (no-unnecessary-condition annotation)
    - .planning/phases/09-audio-wake-lock-lifecycle-hardening/09-PATTERNS.md §"src/hooks/useAudioCues.ts" (Patterns H1–H7 — integer generation counter, single-gate, 'starting' deletion + docstring rewrite, hook-side null propagation, SlowCloseAC pending-promise control, D-41 (d) discriminating assertion, AudioStatus exhaustive switch)
    - .planning/phases/09-audio-wake-lock-lifecycle-hardening/09-VALIDATION.md (rows AUDIO-01 race, AUDIO-01 counter, AUDIO-03 hook, AUDIO-05, AUDIO-06 no-transient, AUDIO-06 union)
    - .planning/phases/09-audio-wake-lock-lifecycle-hardening/09-CONTEXT.md §Decisions D-04, D-06, D-07, D-08, D-09, D-10
  </read_first>

  <behavior>
    - A new `reconstructGenerationRef` of type `useRef<number>` initialized at `0` lives alongside the existing refs (near the top of `useAudioCues`, in the same region as `engineRef`, `firstInCueTimeRef`, `visibilityResumeAttemptedRef`).
    - `reconstructEngine` (the async useCallback at L254–299): the FIRST line of the body captures `const gen = ++reconstructGenerationRef.current` BEFORE the existing synchronous-null block. After the `await createAudioEngine(...)` settles, BEFORE the existing `engineRef.current = newEngine` assignment, check `if (gen !== reconstructGenerationRef.current)` — if mismatched, call `void newEngine.close()` (and `if (oldEngine !== null) void oldEngine.close()` to clean up the captured old engine that was synchronously nulled but not yet closed), then `return` without assigning.
    - `stop()`: insert `++reconstructGenerationRef.current` as the FIRST sync mutation, BEFORE the existing `const engine = engineRef.current; engineRef.current = null` block.
    - Unmount cleanup: insert `++reconstructGenerationRef.current` as the FIRST sync mutation, BEFORE the existing `engineRef.current = null` line.
    - **CRITICAL — Pitfall 1:** the existing synchronous-null at `useAudioCues.ts:260-261` (inside reconstructEngine) and the synchronous-null at L223–224 (inside stop) MUST remain present. The generation counter is layered ON TOP, not in place of. Pitfall 1 anti-pattern check: a diff that deletes `engineRef.current = null` from `reconstructEngine` is wrong.
    - `handleStateChange` (the useCallback at L116–150): the FIRST two lines of the body become `const engine = engineRef.current` and `if (engine === null) return`. Every branch in the body remains otherwise unchanged. Tag the comment `// AUDIO-05 D-04 / D-06: defensive single gate at top — protects ANY future branch that reads engineRef.current. Deferred reshape (D-05 → v1.x).` If Phase 7 `@typescript-eslint/no-unnecessary-condition` fires on the null check (Pitfall 6), annotate with `// Reason: ref read is not narrowable across statements; gate documents the invariant for branches that may be added in v1.x` per Phase 7 D-04, with the `// eslint-disable-next-line @typescript-eslint/no-unnecessary-condition` on the line immediately above the `if`.
    - `start()` (around L200–208): after the existing `const firstInCueTime = engine.scheduleLeadIn(startAudioTime, plan)`, insert `if (firstInCueTime === null) { setAudioAvailable(false); setStatus('failed'); return null }` BEFORE the existing `firstInCueTimeRef.current = firstInCueTime` line. The existing success path (`firstInCueTimeRef.current = firstInCueTime; setStatus('lead-in'); setAudioAvailable(true); return firstInCueTime`) follows the null branch unchanged.
    - **AUDIO-06 hook-side cleanup (D-08 / D-09):**
      - Delete the `setStatus('starting')` call at `useAudioCues.ts:192` (the line right before `try { const engine = await createAudioEngine(...) ... }`). The `try { ... }` block is preserved; transition is now `'idle' → 'lead-in'` directly (or `'idle' → 'failed'` via catch).
      - Rewrite the state-machine docstring at `useAudioCues.ts:5–7` to a single line: `// State machine: 'idle' → 'lead-in' (success) | 'failed' (D-10 Plan 06).` Drop the dead-state explanation entirely.
  </behavior>

  <action>
    Modify `src/hooks/useAudioCues.ts`:

    1. **Docstring rewrite (D-09 / AUDIO-06):** At lines 5–7, replace the existing multi-line state-machine docstring (`// State machine: 'idle' → 'starting' → 'lead-in' (success) | 'failed' (D-10).` and the bullet explanation) with the single line `// State machine: 'idle' → 'lead-in' (success) | 'failed' (D-10 Plan 06).` per RESEARCH §F + PATTERNS Pattern H3.

    2. **Declare `reconstructGenerationRef` (AUDIO-01):** Near the other refs (line 83 region per PATTERNS Pattern H1; same region as `engineRef`, `firstInCueTimeRef`), add `const reconstructGenerationRef = useRef<number>(0)` with comment `// AUDIO-01: monotonic counter; bumped on every reconstruct, stop, and unmount; checked post-await to detect cancellation.`

    3. **handleStateChange single null gate (AUDIO-05):** Modify the useCallback body at lines 116–136 region per PATTERNS Pattern H2. The FIRST two lines of the body become `const engine = engineRef.current` and `if (engine === null) return`. Preserve every existing branch (`if (state === 'running') { ... } else if (state === 'closed') { ... } else if ((state === 'suspended' || state === 'interrupted') && visibilityResumeAttemptedRef.current) { ... }`). Tag the comment `// AUDIO-05 D-04 / D-06: defensive single gate at top — protects ANY future branch that reads engineRef.current. Deferred reshape (D-05 → v1.x).` If Phase 7 lint fires `no-unnecessary-condition` on the gate, add the `// Reason: ref read is not narrowable across statements; gate documents the invariant for branches that may be added in v1.x` annotation per Phase 7 D-04 + RESEARCH Pitfall 6.

    4. **start() null propagation (AUDIO-03):** Inside `start()` at the line right after `const firstInCueTime = engine.scheduleLeadIn(startAudioTime, plan)` (around L204), insert:
       - `if (firstInCueTime === null) { setAudioAvailable(false); setStatus('failed'); return null }`
       - Tag the comment `// AUDIO-03: closed engine — defense-in-depth, fall through to failure.`
       - Keep the subsequent existing success path (`firstInCueTimeRef.current = firstInCueTime; setStatus('lead-in'); setAudioAvailable(true); return firstInCueTime`) unchanged after the new null branch.

    5. **Delete setStatus('starting') (AUDIO-06 D-08):** Remove the line `setStatus('starting')` at `useAudioCues.ts:192` (the line immediately before the `try { const engine = await createAudioEngine(...) ... }` block). The `try { ... }` block stays.

    6. **stop() generation bump (AUDIO-01):** Inside the `stop` useCallback (line 220 region per PATTERNS Pattern H1), insert `++reconstructGenerationRef.current` as the FIRST line of the body, BEFORE the existing `const engine = engineRef.current; engineRef.current = null` block. Tag `// AUDIO-01: invalidate any in-flight reconstruct.` Preserve every existing line of stop().

    7. **Unmount cleanup generation bump (AUDIO-01):** Inside the cleanup returned from the useEffect (line 141–150 region per PATTERNS Pattern H1), insert `++reconstructGenerationRef.current` as the FIRST sync mutation, BEFORE the existing `engineRef.current = null` line. Tag `// AUDIO-01: invalidate any in-flight reconstruct.` Preserve every existing cleanup line.

    8. **reconstructEngine generation stamp + post-await bail (AUDIO-01):** Inside reconstructEngine (line 254 region), follow PATTERNS Pattern H1 + RESEARCH §A:
       - FIRST line of the function body: `const gen = ++reconstructGenerationRef.current` with comment `// AUDIO-01: stamp this reconstruct's generation token.`
       - Preserve the existing synchronous-null block immediately after (`const oldEngine = engineRef.current`, `const currentMuted = muted`, `engineRef.current = null`, `firstInCueTimeRef.current = null`) — DO NOT REMOVE per Pitfall 1.
       - After `newEngine = await createAudioEngine({ onStateChange: handleStateChange })` settles (around L265), BEFORE the existing assignment branch (`if (oldEngine !== null) void oldEngine.close(); engineRef.current = newEngine; ...`), insert the bail check:
         ```
         // AUDIO-01: bail if stop() / unmount / a newer reconstruct ran during the await.
         if (gen !== reconstructGenerationRef.current) {
           void newEngine.close()
           if (oldEngine !== null) void oldEngine.close()
           return
         }
         ```
       - Leave the existing post-bail assignment branch (assign newEngine, setMuted, onReanchorRequiredRef call, setAudioStatus, setAudioAvailable) unchanged.

    Modify `src/hooks/useAudioCues.test.tsx`:

    9. **AUDIO-01 stop-during-reconstruct test:** Add a new case in the existing `'reconstruction'` / `'audioStatus state machine + reconstruction'` describe block (after L625 per Shared Pattern 3 in PATTERNS.md). Name: `'AUDIO-01: stop() during in-flight reconstructEngine closes the orphaned new engine and does not assign to engineRef'`. Structure per RESEARCH §A + Open Question 4 + PATTERNS Pattern H5:
       - Use the existing SpyableAC scaffolding (L447–503) as the constructor stub.
       - Drive the hook to needs-resume via the D-41 (c) prelude (L558–579 — `start()`, dispatch a synthetic 'suspended' or 'interrupted' statechange to set visibilityResumeAttemptedRef).
       - To park `createAudioEngine` mid-await: stub the second AudioContext instance (constructed during reconstruct) such that its `resume()` returns a controllable pending promise (capture `resolveFn` in outer scope). The WR-06 path inside `createAudioEngine` (`audioEngine.ts:113-118`) awaits `audioCtx.resume()` — parking that promise parks the entire `createAudioEngine` factory.
       - Fire `result.current.resume()` WITHOUT awaiting — enters `reconstructEngine`, parks on the resume await.
       - Synchronously call `result.current.stop()` — bumps `reconstructGenerationRef` and synchronously nulls engineRef.
       - Resolve `resolveFn(undefined)` — `createAudioEngine` resolves with the new engine.
       - `await act(async () => { await Promise.resolve(); await Promise.resolve() })` to flush microtasks.
       - Assert: tracker shows construction count rose by 1 from the reconstruct attempt; the new AC's `close()` was called (via tracker.instances or a close-spy on the new instance); `engineRef.current` is still null (verified indirectly via `audioStatus === 'ok'` set by stop and NOT flipped); the reanchor-required callback was NOT called.

    10. **AUDIO-05 statechange-after-stop test:** Add a new case adjacent to the D-41 (d) case at L581–625 per PATTERNS Pattern H6. Name: `'AUDIO-05: synthetic statechange dispatch AFTER stop() nulled engineRef does not throw and does not call setAudioStatus'`. Structure per RESEARCH §E + Reference F:
       - Use the existing SpyableAC + prototype-level `dispatchStateChange` spy pattern from D-41 (d).
       - After `start()`, call `result.current.stop()` (synchronously nulls engineRef).
       - Manually trigger `SpyableAC.lastInstance.dispatchStateChange()` with `state = 'closed'` to dispatch a synthetic statechange AFTER engineRef was nulled.
       - `await act(async () => { await Promise.resolve(); await Promise.resolve() })`.
       - Assert: no exception thrown; `result.current.audioStatus === 'ok'` (set by stop, NOT flipped to 'unavailable' because the null gate returned early).

    11. **AUDIO-03 hook-side null propagation test:** Add a new case after the existing `start(plan) on success transitions to lead-in` case at L35–54. Name: `'AUDIO-03: start() returns null and sets status to failed when engine.scheduleLeadIn returns null'`. Structure:
       - Stub the AudioContext so that the created engine's `scheduleLeadIn` returns null (achievable by stubbing `audioEngine.createAudioEngine` to return a fake engine whose `scheduleLeadIn` is `vi.fn(() => null)`, OR by closing the engine immediately before start in a way that triggers the in-engine null branch). The cleaner path: spy on `audioEngineModule.createAudioEngine` to return an engine where `scheduleLeadIn` is replaced with a vi.fn returning null.
       - Call `result.current.start(samplePlan)`.
       - `await act` flush.
       - Assert: the returned promise resolves to `null`; `result.current.audioStatus` ends in `'unavailable'` and `result.current.status === 'failed'` (or whatever the hook's external observable name is for the inner status).

    12. **AUDIO-06 union exhaustive-switch test (D-10):** Add a new case near the initial-state cases at L35 region per PATTERNS Pattern H7. Name: `'AUDIO-06: AudioStatus union excludes "starting" (D-07)'`. Body per RESEARCH §F:
       - Import `type { AudioStatus } from '../audio/audioEngine'`.
       - Declare `const exhaustive: AudioStatus = 'idle'`.
       - `switch (exhaustive) { case 'idle': case 'lead-in': case 'failed': expect(true).toBe(true); break }`.
       - The compiler will refuse to compile if `'starting'` is reintroduced (TS narrowing requires either branch coverage or `default: assertNever(exhaustive)`).

    13. **AUDIO-06 no-transient-render test:** Add a case asserting `start()`'s status transitions skip `'starting'`. Name: `'AUDIO-06: start() success transitions status idle → lead-in directly (no transient starting)'`. Structure: render the hook with an observable wrapper around `result.current.status` that captures every render's value into an array (or use `result.all` from `renderHook`). Call `start(samplePlan)`, `await act`, assert the array contains `'lead-in'` and NEVER contains `'starting'`.

    All new code must pass Phase 7 strict baseline. `void` prefix on fire-and-forget `.close()`/`.release()` per `no-floating-promises`. Any new `// eslint-disable` carries a `// Reason:` annotation per Phase 7 D-04. No console.warn per Phase 8 D-03.
  </action>

  <verify>
    <automated>npm run test -- src/hooks/useAudioCues.test.tsx &amp;&amp; tsc --noEmit &amp;&amp; npm run lint</automated>
  </verify>

  <acceptance_criteria>
    - Source assertion: `grep -n "reconstructGenerationRef" src/hooks/useAudioCues.ts` returns >= 5 matches (declaration + stamp in reconstructEngine + bump in stop + bump in unmount + post-await comparison).
    - Source assertion: `grep -nE "if \\(gen !== reconstructGenerationRef\\.current\\)" src/hooks/useAudioCues.ts` returns exactly 1 match (post-await bail check in reconstructEngine).
    - Source assertion: the existing synchronous-null pattern `engineRef.current = null` remains present inside `reconstructEngine` AFTER the new `gen` stamp line. Run `grep -nB2 "engineRef.current = null" src/hooks/useAudioCues.ts` and verify the inside-reconstructEngine occurrence (around L260 region) is intact (Pitfall 1).
    - Source assertion: `grep -c "setStatus('starting')" src/hooks/useAudioCues.ts` returns 0 (D-08).
    - Source assertion: the first line of the file (or the first lines of the existing block comment cluster) match the new single-line docstring `// State machine: 'idle' → 'lead-in' (success) | 'failed' (D-10 Plan 06).` — verified by `grep -nE "State machine: 'idle' → 'lead-in'" src/hooks/useAudioCues.ts` returning at least 1 match AND `grep -c "'starting'" src/hooks/useAudioCues.ts` returning 0.
    - Source assertion: `handleStateChange` body's first two effective statements are `const engine = engineRef.current` and `if (engine === null) return`. Verify with `grep -nA5 "const handleStateChange" src/hooks/useAudioCues.ts` showing the gate before any other branch.
    - Source assertion: `start()` contains the null-propagation branch — `grep -nE "if \\(firstInCueTime === null\\)" src/hooks/useAudioCues.ts` returns exactly 1 match, and the next-following lines call `setStatus('failed')` and `return null`.
    - Behavior assertion: new test `'AUDIO-01: stop() during in-flight reconstructEngine closes the orphaned new engine ...'` exists and passes — tracker shows the new AC was constructed AND its `close()` was called, `engineRef` was never assigned.
    - Behavior assertion: new test `'AUDIO-05: synthetic statechange dispatch AFTER stop() ...'` exists and passes — no throw, `audioStatus` remains at the post-stop value.
    - Behavior assertion: new test `'AUDIO-03: start() returns null and sets status to failed ...'` exists and passes — start resolves to null, status transitions to failed.
    - Behavior assertion: new test `'AUDIO-06: AudioStatus union excludes "starting" (D-07)'` compiles and passes.
    - Behavior assertion: new test `'AUDIO-06: start() success transitions status idle → lead-in directly (no transient starting)'` exists and passes — no intermediate `'starting'` observed across renders.
    - CLI: `npm run test -- src/hooks/useAudioCues.test.tsx` exits 0.
    - CLI: `tsc --noEmit` exits 0; `npm run lint` exits 0.
    - No regressions in `useAudioCues.test.tsx` — all previously-passing cases still green.
  </acceptance_criteria>

  <done>
    All seven source/grep assertions hold; all five new test cases pass; the existing synchronous-null pattern is intact (Pitfall 1 self-check); `tsc --noEmit` + `npm run lint` exit 0; previously-passing tests remain green.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: AUDIO-02 caller-side clamp in src/app/App.tsx boundary effect + co-located test in App.audio.test.tsx</name>
  <files>src/app/App.tsx, src/app/App.audio.test.tsx</files>

  <read_first>
    - src/app/App.tsx (read selectively — lines 1–60 region for imports, lines 195–200 for the audioNotifyPhaseBoundary hoist pattern, lines 480–520 for the boundary scheduling effect — per PATTERNS Patterns A1 + A2)
    - src/app/App.audio.test.tsx (read selectively — focus on installTrackedAC scaffolding at L358–379, existing boundary-cue precedent at L128–154)
    - src/hooks/useAudioCues.ts (read selectively — confirm `audio.audioNow` is a stable useCallback([]) returning `number | null` at L333–335 region)
    - src/audio/audioEngine.ts (post-Plan-01) — confirm `SAFE_LEAD_SEC` is exported (this plan imports it)
    - .planning/phases/09-audio-wake-lock-lifecycle-hardening/09-RESEARCH.md §Recommended Code Shapes §B (App.tsx revised boundary effect), §Pitfall 5 (caller-side clamp depends on nullable audio.audioNow), §Assumption A2 (Phase 12 HYGIENE-01 cross-phase note)
    - .planning/phases/09-audio-wake-lock-lifecycle-hardening/09-PATTERNS.md §"src/app/App.tsx" (Patterns A1 + A2 + A3 — import block, boundary effect, installTrackedAC scaffolding)
    - .planning/phases/09-audio-wake-lock-lifecycle-hardening/09-VALIDATION.md (row AUDIO-02 caller)
    - .planning/phases/09-audio-wake-lock-lifecycle-hardening/09-CONTEXT.md §Decisions D-01, D-02, D-03 (caller-side clamp lives at App.tsx)
  </read_first>

  <behavior>
    - `App.tsx` imports `SAFE_LEAD_SEC` from `'../audio/audioEngine'` alongside the existing `LEAD_IN_DURATION_MS` and `LEAD_IN_TICK_INTERVAL_MS` imports (per PATTERNS Pattern A1).
    - The boundary scheduling effect at App.tsx lines ~480–515 (the `useEffect` that fires `audioNotifyPhaseBoundary` on `session.currentFrame` change) inserts the caller-side clamp between the existing `const audioTime = audioAnchor + boundaryStartMs / 1000` computation and the existing `audioNotifyPhaseBoundary({ ..., audioTime })` call (per PATTERNS Pattern A2).
    - Pitfall 5 null gate: BEFORE computing `clampedAudioTime`, read `const liveAudioNow = audio.audioNow()`. If `liveAudioNow === null` (the engine is closed or unavailable mid-frame), `return` from the effect without scheduling the boundary. This is correct because the engine-side clamp (Plan 01) is the safety net, AND the next frame's effect will re-evaluate.
    - When `liveAudioNow` is non-null, compute `const clampedAudioTime = Math.max(audioTime, liveAudioNow + SAFE_LEAD_SEC)` and pass `clampedAudioTime` (not `audioTime`) as the `audioTime` field to `audioNotifyPhaseBoundary`.
    - The existing `if (audioAnchor === null || plan === null) return` short-circuit at L493 is preserved (it covers a different case — anchor not yet established).
    - **useEffect dep list:** the boundary effect currently has `[appPhase, session.currentFrame, audioNotifyPhaseBoundary]`. Adding `audio.audioNow()` reads `audio.audioNow` which is a stable `useCallback([])` (verified per PATTERNS Pattern A2 + RESEARCH §B). Hoist `audio.audioNow` into a stable reference at the top of `App` next to the existing `audioNotifyPhaseBoundary` hoist (App.tsx:195–197 region), then add THAT stable reference to the dep list. Do NOT add the raw `audio` object — that would re-fire the effect every render.
    - **Cross-phase note (Pitfall 5 / Assumption A2):** Phase 12 HYGIENE-01 plans to REMOVE `audioNow` from the hook surface. Plan 02's commit message must reference HYGIENE-01 / IN-02 so Phase 12's planner sees the new consumer.
  </behavior>

  <action>
    Modify `src/app/App.tsx`:

    1. **Import SAFE_LEAD_SEC:** At the existing import block from `'../audio/audioEngine'` (App.tsx:17–20 region per PATTERNS Pattern A1), add `SAFE_LEAD_SEC` alongside `LEAD_IN_DURATION_MS` and `LEAD_IN_TICK_INTERVAL_MS`. Final import shape:
       ```
       import {
         LEAD_IN_DURATION_MS,
         LEAD_IN_TICK_INTERVAL_MS,
         SAFE_LEAD_SEC,
       } from '../audio/audioEngine'
       ```

    2. **Hoist audio.audioNow into a stable reference:** At App.tsx:195–197 region (near the existing `const audioNotifyPhaseBoundary = audio.notifyPhaseBoundary` hoist pattern), add `const audioAudioNow = audio.audioNow` (or a similarly named local). This reads the stable useCallback once per render so the boundary effect's dep list does not depend on the entire `audio` object. Tag the comment `// Phase 9 AUDIO-02: stable reference for the boundary effect's caller-side clamp.`

    3. **Insert caller-side clamp in the boundary effect:** Inside the boundary effect at App.tsx:480–515 region (per PATTERNS Pattern A2), BETWEEN the existing `const audioTime = audioAnchor + boundaryStartMs / 1000` line and the existing `audioNotifyPhaseBoundary({ newPhase: frame.phase, audioTime, phaseDurationSec })` call, insert:
       ```
       // AUDIO-02 D-01/D-02 caller-side clamp. Pitfall 5: audio.audioNow() returns
       // number | null — null in the window between engine close and audioAnchor clear.
       // Skip the schedule entirely when null (engine-side clamp is the safety net at the callee).
       const liveAudioNow = audioAudioNow()
       if (liveAudioNow === null) return
       const clampedAudioTime = Math.max(audioTime, liveAudioNow + SAFE_LEAD_SEC)
       ```
       Then modify the `audioNotifyPhaseBoundary({ ... })` call to pass `clampedAudioTime` (not `audioTime`) as the `audioTime` field. The `phaseDurationSec` computation and the rest of the call are unchanged.

    4. **Update the effect's dep array:** Append `audioAudioNow` to the existing `[appPhase, session.currentFrame, audioNotifyPhaseBoundary]` dep array (per PATTERNS Pattern A2). Phase 7's `react-hooks/exhaustive-deps` rule will surface a missing-dep lint error if this is omitted.

    Modify `src/app/App.audio.test.tsx`:

    5. **AUDIO-02 caller-side clamp test:** Add a new case in the existing `describe('App — audio cues (Phase 3)', ...)` block alongside the existing boundary-cue cases (around L128–154 per PATTERNS Pattern A3). Name: `'AUDIO-02: App boundary effect clamps audioTime to audio.audioNow() + SAFE_LEAD_SEC (caller-side)'`. Structure per RESEARCH §B + PATTERNS Pattern A3:
       - Wrap the test with the existing `installTrackedAC` scaffolding at L358–379.
       - Render `<App />`, run `startAndAdvancePastLeadIn()` (or the equivalent helper) to drive the session past the lead-in.
       - Before or during a phase advance, manipulate the tracked AC's `currentTime` to a value LARGER than the natural `audioAnchor + boundaryStartMs / 1000` would produce — making `audioTime` a past value relative to `currentTime + SAFE_LEAD_SEC`. The cleanest mechanism: `Object.defineProperty(tracker.instances[0], 'currentTime', { get: () => N })` where N is chosen to be larger than the computed `audioTime`.
       - Spy on `cueSynth.scheduleOutCue` (matches the existing precedent at L128–154 which already spies `cueSynth.scheduleOutCue`).
       - Advance fake timers to fire the first Out boundary.
       - Read the 2nd argument of the first `scheduleOutCue` call: `const audioTimeArg = scheduleOutSpy.mock.calls[0]?.[1] as number`.
       - Assert `audioTimeArg >= currentTime + SAFE_LEAD_SEC - 1e-9` (floating-point safe). Equivalently `expect(audioTimeArg).toBeGreaterThanOrEqual(currentTime + SAFE_LEAD_SEC)`.

    6. **Paired no-clamp case (optional but recommended):** Add a paired case `'AUDIO-02: App boundary effect passes audioTime verbatim when already > now() + SAFE_LEAD_SEC'`. Same structure, but with a small `currentTime` (e.g., 0 or 1) so the natural audioTime sails past the clamp threshold; assert the 2nd arg to `scheduleOutCue` equals the un-clamped value.

    All new code must pass Phase 7 strict baseline. `react-hooks/exhaustive-deps` MUST be satisfied on the modified dep array. Any new `// eslint-disable` carries a `// Reason:` annotation per Phase 7 D-04.
  </action>

  <verify>
    <automated>npm run test -- src/app/App.audio.test.tsx &amp;&amp; tsc --noEmit &amp;&amp; npm run lint</automated>
  </verify>

  <acceptance_criteria>
    - Source assertion: `grep -n "SAFE_LEAD_SEC" src/app/App.tsx` returns >= 2 matches (1 import line + 1 usage inside the boundary effect).
    - Source assertion: the import block at App.tsx imports `SAFE_LEAD_SEC` from `'../audio/audioEngine'` (verify with `grep -nB1 -A3 "LEAD_IN_DURATION_MS" src/app/App.tsx` showing SAFE_LEAD_SEC alongside).
    - Source assertion: `grep -nE "Math\\.max\\(audioTime, liveAudioNow \\+ SAFE_LEAD_SEC\\)" src/app/App.tsx` returns exactly 1 match.
    - Source assertion: `grep -nE "if \\(liveAudioNow === null\\) return" src/app/App.tsx` returns exactly 1 match (Pitfall 5 null gate).
    - Source assertion: the existing `audioNotifyPhaseBoundary({...})` call inside the boundary effect now passes `clampedAudioTime`, not `audioTime`, as the `audioTime` field. Verify with `grep -nA3 "audioNotifyPhaseBoundary" src/app/App.tsx` showing `audioTime: clampedAudioTime`.
    - Behavior assertion: new test `'AUDIO-02: App boundary effect clamps audioTime to audio.audioNow() + SAFE_LEAD_SEC (caller-side)'` exists in `src/app/App.audio.test.tsx` and passes.
    - Behavior assertion: in the clamp case, the value passed to `scheduleOutCue` as the 2nd arg is `>= currentTime + SAFE_LEAD_SEC`.
    - Behavior assertion: in the paired no-clamp case (if added), the value passed is the un-clamped audioTime verbatim.
    - CLI: `npm run test -- src/app/App.audio.test.tsx` exits 0.
    - CLI: `tsc --noEmit` exits 0; `npm run lint` exits 0 — verifies `react-hooks/exhaustive-deps` is satisfied on the modified dep array (the hoisted `audioAudioNow` is listed).
    - No regressions in `App.audio.test.tsx` — all previously-passing cases still green.
  </acceptance_criteria>

  <done>
    All five source assertions hold; the new AUDIO-02 caller-side test passes (clamp value reaches `scheduleOutCue`); the dep-list update passes `react-hooks/exhaustive-deps`; `tsc --noEmit` + `npm run lint` exit 0; previously-passing App.audio.test.tsx cases remain green. Plan 02 commit message references Phase 12 HYGIENE-01 / IN-02 per RESEARCH Pitfall 5 cross-phase note + Assumption A2.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

This phase introduces no new trust boundaries. Phase 9 is a defensive lifecycle hardening patch on existing client-side imperative resources (Web Audio + Screen Wake Lock); no new authentication, session, access-control, cryptography, or input-validation surface.

| Boundary | Description |
|----------|-------------|
| (none new) | No new trust boundary crosses introduced by this plan. |

## STRIDE Threat Register

**No applicable security threats for this defensive lifecycle hardening patch.** ASVS V2 (Authentication), V3 (Session Management), V4 (Access Control), V5 (Input Validation), V6 (Cryptography) categories do not apply (single-user local app, no auth, no crypto, no server sessions). The clamp at AUDIO-02 is a numeric range guard, not security-motivated input validation.

The only STRIDE pattern that maps to this plan (informational) is:

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-03 | Denial of service (self) | `useAudioCues.reconstructEngine` orphan-engine path | mitigate | AUDIO-01 — `reconstructGenerationRef` post-await check closes the orphaned new engine if `stop()` / unmount / a newer reconstruct ran during the await. Prevents engineRef pointing at a disposable orphan + leaked unclosed AudioContexts. |
| T-09-04 | Denial of service (self) | `useAudioCues.handleStateChange` null-deref | mitigate | AUDIO-05 — single null gate at top of handler prevents any future branch from dereferencing `engineRef.current` after `stop()`/unmount nulled it. |
| T-09-05 | Denial of service (self) | App boundary effect past-time schedule | mitigate | AUDIO-02 caller-side — `Math.max(audioTime, audio.audioNow() + SAFE_LEAD_SEC)` ensures every cue is scheduled with positive lead time relative to the live audio clock. Engine-side clamp (Plan 01) is the safety net. |
</threat_model>

<verification>
- `npm run test` (full Vitest suite) exits 0 after both tasks land. Baseline preserved: prior-passing tests (366 cases per CONTEXT.md `<scope_lock>`) remain green; new cases for AUDIO-01 (stop-during-reconstruct), AUDIO-05 (statechange-after-stop), AUDIO-03 (hook-side null propagation), AUDIO-06 (union exhaustive switch + no-transient-render), and AUDIO-02 (caller-side clamp + paired no-clamp) added.
- `tsc --noEmit` exits 0 after every task commit. Verifies: the AudioStatus union tightening from Plan 01 holds at the hook layer; `scheduleLeadIn`'s `number | null` return is correctly narrowed in `start()`; the dep array on the boundary effect satisfies `react-hooks/exhaustive-deps`.
- `npm run lint` exits 0 after every task commit. Verifies: `no-floating-promises` on `void newEngine.close()`; `react-hooks/exhaustive-deps` on the boundary effect's modified dep list; `// Reason:` annotations on any new `eslint-disable` per Phase 7 D-04.
- `npm run build` exits 0 (Phase 7 D-09 invariant) at the wave-merge boundary.
- Source-level invariants (per acceptance criteria of each task):
  - The synchronous-null pattern inside `reconstructEngine` (Pitfall 1) remains present after Task 1 — proves the generation counter was layered on top, not in place of.
  - `grep -c "'starting'" src/hooks/useAudioCues.ts` returns 0 after Task 1 — proves the hook-side dead-state cleanup is complete.
  - `audioNotifyPhaseBoundary` is invoked with `clampedAudioTime` (not raw `audioTime`) in the boundary effect — proves the caller-side clamp is wired through, not just declared.
- Cross-phase commit note: Plan 02 commit message references Phase 12 HYGIENE-01 / IN-02 per RESEARCH Pitfall 5 (the new `audio.audioNow()` consumer in App.tsx must survive — or be removed in tandem — when Phase 12 prunes the unused `audioNow` from the hook surface).
</verification>

<success_criteria>
This plan succeeds when all six must-haves are observable in the running codebase:

1. **`reconstructGenerationRef` is stamped + checked end-to-end:** declared at the hook top; bumped in `stop()` and unmount cleanup; stamped at the top of `reconstructEngine`; post-await comparison bails with `void newEngine.close()` when mismatched (AUDIO-01, Roadmap Success Criterion #1).
2. **Synchronous-null pattern is preserved (Pitfall 1):** `engineRef.current = null` lines inside `reconstructEngine` (L260 region) and `stop()` (L223 region) remain present AFTER the new generation-counter additions.
3. **`handleStateChange` single null gate is in place:** the first two effective statements of the handler body are `const engine = engineRef.current` and `if (engine === null) return`; the rest of the handler is unchanged (AUDIO-05, Roadmap Success Criterion #4).
4. **`'starting'` is fully removed from the hook layer:** `grep -c "'starting'" src/hooks/useAudioCues.ts` returns 0; the state-machine docstring at the top of the file is rewritten to the single-line form per D-09; `start()` no longer calls `setStatus('starting')` per D-08 (AUDIO-06, Roadmap Success Criterion #4).
5. **`start()` propagates `null` from `scheduleLeadIn`:** the new `if (firstInCueTime === null) { setAudioAvailable(false); setStatus('failed'); return null }` branch is present immediately after the `engine.scheduleLeadIn` call (AUDIO-03, Roadmap Success Criterion #3 hook-side).
6. **Caller-side clamp at App.tsx is wired:** `SAFE_LEAD_SEC` is imported from `'../audio/audioEngine'`; the boundary effect reads `audio.audioNow()` (via stable hoist `audioAudioNow`) into `liveAudioNow`; the Pitfall-5 null gate `if (liveAudioNow === null) return` is present; `clampedAudioTime = Math.max(audioTime, liveAudioNow + SAFE_LEAD_SEC)` is passed to `audioNotifyPhaseBoundary` (AUDIO-02 caller-side, Roadmap Success Criterion #2).

Full Vitest suite green (366 + new cases from both Plan 01 and Plan 02). `tsc --noEmit` and `npm run lint` exit 0. All seven Phase 9 REQ-IDs (AUDIO-01..06, WAKELOCK-01) are observable in the running codebase with co-located Vitest cases locking each contract.
</success_criteria>

<output>
After completion, create `.planning/phases/09-audio-wake-lock-lifecycle-hardening/09-02-SUMMARY.md` documenting:
- Files modified with line ranges.
- New tests added (per file) with their describe-block neighbors.
- Pitfall 1 self-check: confirm the synchronous-null inside `reconstructEngine` and `stop()` remains present after the generation-counter additions.
- `'starting'` removal self-check: confirm `grep -c "'starting'" src/hooks/useAudioCues.ts` returns 0 and that the docstring rewrite matches D-09.
- Cross-phase note: explicitly cross-reference Phase 12 HYGIENE-01 / IN-02 in the commit message — App.tsx's new `audio.audioNow()` consumer must be considered when Phase 12 prunes the unused `audioNow` from the hook surface.
- Phase 9 closure: all seven Phase 9 REQ-IDs (AUDIO-01..06, WAKELOCK-01) covered; Roadmap success criteria 1–6 observable; baseline preserved at 366 + new cases.
</output>
