---
phase: 40-timbre-preview-cue
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/audio/previewContext.ts
  - src/audio/previewContext.test.ts
autonomous: true
requirements: [PREV-02, PREV-05]
requirements_addressed: [PREV-02, PREV-05]
tags: [audio, web-audio, audiocontext, singleton, preview]

must_haves:
  truths:
    - "playInhalePreview(timbre) routes through the existing scheduleInCueForTimbre dispatch (PREV-02)"
    - "First-tap creates a module-level AudioContext singleton attached to the user gesture (D-01)"
    - "Subsequent taps reuse the same AudioContext instance (D-01 latency target)"
    - "ctx.resume() is invoked when the singleton AudioContext is suspended (D-02 iOS / auto-suspend)"
    - "scheduleInCueForTimbre is called synchronously in the same microtask as playInhalePreview (PREV-05 contract / D-12)"
    - "phaseDurationSec is omitted from the dispatch call (D-03 natural decay)"
  artifacts:
    - path: "src/audio/previewContext.ts"
      provides: "Pure audio module exporting playInhalePreview(timbre: TimbreId): void"
      exports: ["playInhalePreview"]
      contains: "import { scheduleInCueForTimbre } from './cueSynth'"
    - path: "src/audio/previewContext.test.ts"
      provides: "Unit coverage under FakeAudioContext polyfill (D-10 a–d)"
      contains: "playInhalePreview"
  key_links:
    - from: "src/audio/previewContext.ts"
      to: "src/audio/cueSynth.ts (scheduleInCueForTimbre)"
      via: "named import"
      pattern: "import\\s+\\{[^}]*scheduleInCueForTimbre[^}]*\\}\\s+from\\s+['\"]\\./cueSynth['\"]"
    - from: "src/audio/previewContext.ts"
      to: "TimbreId type"
      via: "type-only named import"
      pattern: "import\\s+type\\s+\\{[^}]*TimbreId[^}]*\\}\\s+from\\s+['\"]\\.\\./domain/settings['\"]"
---

<objective>
Create the pure audio module `src/audio/previewContext.ts` exporting a single bare function `playInhalePreview(timbre: TimbreId): void`, plus the unit test file `src/audio/previewContext.test.ts` covering D-10(a–d).

Purpose: Establishes the audio surface that Phase 40 ships. PREV-02 (reuse cueSynth scheduler — zero duplicated DSP) is satisfied by importing and calling `scheduleInCueForTimbre` verbatim. PREV-05 (≤100 ms latency) is satisfied structurally by (i) a module-level AudioContext singleton that pays the construction cost once on first tap (D-01) and (ii) a synchronous resume + schedule call path with no awaits (D-12).

Output: New module + new unit test file. No UI consumer yet (Plan 03 wires it). No structural drift-guard yet (Plan 02 locks PREV-03).
</objective>

<execution_context>
@$HOME/.claude-personal-personal/get-shit-done/workflows/execute-plan.md
@$HOME/.claude-personal-personal/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/40-timbre-preview-cue/40-CONTEXT.md
@.planning/phases/40-timbre-preview-cue/40-PATTERNS.md

<!-- READ-ONLY pattern source files; do not edit. -->
@src/audio/cueSynth.ts
@src/audio/audioEngine.ts
@src/audio/cueSynth.test.ts
@vitest.setup.ts

<interfaces>
<!-- Key contracts the executor consumes. Extracted from the codebase. Do not re-derive. -->

From src/audio/cueSynth.ts:198-207 (the load-bearing API):
  export function scheduleInCueForTimbre(
    audioCtx: AudioContext,
    when: number,
    destination: AudioNode,
    timbre: TimbreId,
    phaseDurationSec?: number,
  ): CueHandle

From src/domain/settings.ts:
  export type TimbreId = 'bowl' | 'bell' | 'sine' | 'flute'
  export const TIMBRE_OPTIONS: readonly TimbreId[]

From src/audio/audioEngine.ts:123-143 (lifecycle pattern — adapt, do not copy verbatim):
  - `new AudioContext()` MUST be called from a user-gesture path
  - When state === 'suspended', call `ctx.resume()` (audioEngine awaits; Phase 40 does NOT await per D-02 / D-12)
  - audioEngine.ts:160 holds `let muted` in the closure — previewContext.ts MUST NOT reach into this closure (locked structurally in Plan 02)

From vitest.setup.ts FakeAudioContext polyfill (Phase 3, global install):
  - `new AudioContext()` inside test code transparently constructs a FakeAudioContext
  - Instance method `_simulateSuspend(): void` transitions state → 'suspended' and fires statechange
  - `resume()` is a `vi.fn()`-style spy by default
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create `src/audio/previewContext.ts` — singleton + lazy-create + resume-if-suspended + dispatch</name>
  <files>src/audio/previewContext.ts</files>
  <read_first>
    - src/audio/cueSynth.ts (read header comment 1-15 + lines 194-207 for the dispatch signature)
    - src/audio/audioEngine.ts (read lines 1-3 for the pure-module header style + lines 123-143 for the lifecycle pattern)
    - src/audio/timbres.ts (lines 1-30 — confirm TimbreId / TIMBRE_PRESETS shape; READ-ONLY)
    - src/domain/settings.ts (TimbreId type definition; READ-ONLY)
    - .planning/phases/40-timbre-preview-cue/40-CONTEXT.md (D-01, D-02, D-03, D-06)
    - .planning/phases/40-timbre-preview-cue/40-PATTERNS.md (`src/audio/previewContext.ts` section — header WHY-only comment pattern, singleton helper shape, public surface)
  </read_first>
  <behavior>
    - playInhalePreview('bowl') invoked once → scheduleInCueForTimbre called exactly once with the singleton ctx, ctx.currentTime, ctx.destination, 'bowl', undefined
    - playInhalePreview called N times → AudioContext constructor invoked exactly once (singleton reuse)
    - Singleton ctx.state === 'suspended' at entry → ctx.resume() invoked (no await), then scheduleInCueForTimbre runs in the same microtask
    - Singleton ctx.state === 'running' at entry → ctx.resume() NOT invoked
    - The exported function returns void; no Promise leaks; no awaits anywhere in the call chain
  </behavior>
  <action>
    Create `src/audio/previewContext.ts` as a pure audio module mirroring the `src/audio/cueSynth.ts` style.

    Module shape (per 40-PATTERNS.md `src/audio/previewContext.ts` section):
    - File header comment: pure-Web-Audio purity declaration mirroring cueSynth.ts:1 (`// Pure Web Audio synthesis builders. Zero React imports.`), followed by WHY-only block citing CONTEXT D-01, D-02, D-03 (singleton-reuse rationale for PREV-05 latency target; resume-on-every-tap rationale for iOS/auto-suspend; omit-phaseDurationSec for natural decay). Tiger Style WHY-only — no narration of WHAT.
    - Imports (exact two lines, no others):
      - Named import of `scheduleInCueForTimbre` from `'./cueSynth'`
      - Type-only import of `TimbreId` from `'../domain/settings'`
    - Module-level state: `let ctx: AudioContext | null = null` (the singleton holder per D-01).
    - Module-private helper `ensurePreviewContext(): AudioContext` (or planner-chosen synonym from D-15 Claude's Discretion) — if `ctx === null`, set `ctx = new AudioContext()` and return it; otherwise return the existing `ctx`. Not exported.
    - Single exported function: `export function playInhalePreview(timbre: TimbreId): void`. Body, in order:
      1. const ctx = ensurePreviewContext()
      2. if (ctx.state === 'suspended') { void ctx.resume() } — fire-and-forget, no await (D-02 + D-12)
      3. scheduleInCueForTimbre(ctx, ctx.currentTime, ctx.destination, timbre) — phaseDurationSec omitted per D-03

    Hard invariants:
    - Zero React imports.
    - Zero imports from `./audioEngine` or any module that re-exports the engine's muted state (Plan 02 will enforce structurally).
    - No exported helpers, no class, no default export, no test-only escape hatches.
    - No await anywhere in `playInhalePreview` (the synchronous-call-path contract IS the PREV-05 lock per D-12).
    - No NEVER place fenced code blocks inside action; the PATTERNS file already carries the exact shape — consume it.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -v '^$' | head -20 ; echo --- ; npx vitest run src/audio/previewContext.ts --reporter=basic 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - Source: `src/audio/previewContext.ts` exists at the repo path.
    - Source: file contains `export function playInhalePreview(timbre: TimbreId): void`.
    - Source: file contains `import { scheduleInCueForTimbre } from './cueSynth'`.
    - Source: file contains `import type { TimbreId } from '../domain/settings'`.
    - Source: file does NOT contain the substring `audioEngine` (case-sensitive; the Plan 02 drift-guard will assert this independently — same-plan early failure caught here).
    - Source: file does NOT contain `await` as a JS keyword (verify via `grep -nE '(^|[^A-Za-z_])await([^A-Za-z_]|$)' src/audio/previewContext.ts | grep -v '^[0-9]*://'` returning zero non-comment matches; the synchronous-call-path contract IS the PREV-05 lock per D-12).
    - Source: file contains `let ctx: AudioContext | null = null` (singleton holder, D-01).
    - Source: file does NOT contain `class ` or `export default` (bare-function-export style per D-06).
    - Source: file does NOT import from `'react'` or `'react-dom'` (zero React imports per cueSynth.ts:1 invariant).
    - CLI: `npx tsc --noEmit` exits 0 (type-checks clean against the existing cueSynth.ts dispatch signature).
  </acceptance_criteria>
  <done>
    `src/audio/previewContext.ts` exists, type-checks, exports the single bare function `playInhalePreview`, holds a module-level AudioContext singleton, resumes when suspended, and calls `scheduleInCueForTimbre` synchronously with `phaseDurationSec` omitted. Zero audioEngine imports.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create `src/audio/previewContext.test.ts` — unit coverage (D-10 a–d) under FakeAudioContext</name>
  <files>src/audio/previewContext.test.ts</files>
  <read_first>
    - src/audio/previewContext.ts (the module under test, just created in Task 1)
    - src/audio/cueSynth.test.ts (lines 1-17 for the scaffold; lines 405-454 for the `it.each(TIMBRE_OPTIONS)` per-timbre coverage pattern)
    - vitest.setup.ts (lines 138-277 — FakeAudioContext polyfill + the `_simulateSuspend` test hook)
    - src/domain/settings.ts (TIMBRE_OPTIONS readonly array)
    - .planning/phases/40-timbre-preview-cue/40-PATTERNS.md (`src/audio/previewContext.test.ts` section — scaffold, per-timbre coverage pattern, singleton-reuse assertion, suspend-resume assertion)
    - .planning/phases/40-timbre-preview-cue/40-CONTEXT.md (D-10 a/b/c/d)
  </read_first>
  <behavior>
    - Case (a): For each TimbreId in TIMBRE_OPTIONS, calling playInhalePreview(timbre) invokes scheduleInCueForTimbre exactly once with arguments (ctx, ctx.currentTime, ctx.destination, timbre, undefined). The fifth positional arg (phaseDurationSec) is asserted to be undefined — locks D-03 natural decay.
    - Case (b): When the singleton AudioContext is in state 'suspended' on entry, playInhalePreview invokes ctx.resume() exactly once before scheduling the cue.
    - Case (c): Calling playInhalePreview N consecutive times constructs exactly one AudioContext instance (singleton reuse — locks D-01 PREV-05 latency rationale).
    - Case (d): The scheduleInCueForTimbre call happens synchronously in the same microtask as the playInhalePreview entry (locks D-12 PREV-05 synchronous-call-path contract). Verified by the spy being called before any await/microtask boundary in the test.
    - Per-test isolation: vi.resetModules() + dynamic re-import of './previewContext' inside each test, so the module-level singleton is fresh between cases (planner picks per 40-PATTERNS.md recommendation option 1).
  </behavior>
  <action>
    Create `src/audio/previewContext.test.ts` as the unit test file for the new module. Mirror the scaffold from `src/audio/cueSynth.test.ts:1-17` (imports + describe block structure) and the per-timbre coverage pattern from `cueSynth.test.ts:412-437`.

    Top-of-file scaffold:
    - Imports: `describe, expect, it, vi, beforeEach` from `'vitest'`; `TIMBRE_OPTIONS` from `'../domain/settings'`. Do NOT statically import `./previewContext` at the top — use dynamic `await import('./previewContext')` inside each `it` for singleton isolation per the D-10 PATTERNS recommendation (option 1).
    - `beforeEach(() => { vi.resetModules() })` resets the previewContext singleton between cases.

    Required test cases (one `describe` block, four `it` blocks — or `it.each` + standalone `it` per D-10):
    1. D-10(a) — per-timbre dispatch correctness (`it.each(TIMBRE_OPTIONS)`):
       - Use `vi.spyOn(await import('./cueSynth'), 'scheduleInCueForTimbre')` to intercept the dispatch before importing previewContext.
       - Call `(await import('./previewContext')).playInhalePreview(timbre)`.
       - Assert the spy was called exactly once with arguments matching: (any AudioContext, any number, any AudioDestinationNode, the specific TimbreId, undefined).
       - The `undefined` fifth-arg assertion is the D-03 natural-decay lock.
    2. D-10(b) — suspend → resume:
       - First call: `playInhalePreview('bowl')` to construct the singleton.
       - Capture the singleton AudioContext instance (planner picks the cleanest capture method — `vi.spyOn(window, 'AudioContext')` per 40-PATTERNS.md, OR a spy injected via vi.fn impl).
       - Call `(ctx as unknown as { _simulateSuspend: () => void })._simulateSuspend()` to drive state to 'suspended'.
       - Reset the resume spy: `(ctx.resume as ReturnType<typeof vi.fn>).mockClear()`.
       - Second call: `playInhalePreview('bowl')`.
       - Assert `ctx.resume` was called exactly once.
    3. D-10(c) — singleton reuse across N taps:
       - Spy on the AudioContext constructor: `const acCtor = vi.spyOn(window, 'AudioContext')`.
       - Call `playInhalePreview` four times with different TimbreIds (bowl, bell, sine, flute).
       - Assert `acCtor.mock.instances.length` or equivalent === 1.
    4. D-10(d) — synchronous-call-path contract (PREV-05 lock per D-12):
       - Spy on scheduleInCueForTimbre.
       - Call `playInhalePreview('bowl')`.
       - Assert the spy was called BEFORE any `await` in the test (i.e., the spy.mock.calls.length === 1 IMMEDIATELY after the synchronous playInhalePreview call, no `await Promise.resolve()` needed between).

    Hard invariants:
    - Use the existing FakeAudioContext polyfill — no new mock infrastructure.
    - Reset modules + restore mocks between tests; do not leak the singleton across cases.
    - No NEVER place fenced code blocks inside this action — the PATTERNS file carries shape examples.
  </action>
  <verify>
    <automated>npx vitest run src/audio/previewContext.test.ts --reporter=basic 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - Source: `src/audio/previewContext.test.ts` exists at the repo path.
    - Source: file contains `import { TIMBRE_OPTIONS } from '../domain/settings'`.
    - Source: file contains a per-timbre case using `it.each(TIMBRE_OPTIONS)` OR equivalent per-timbre iteration covering all 4 TimbreIds.
    - Source: file contains assertions on `scheduleInCueForTimbre` being called with `undefined` as its fifth positional argument (D-03 natural-decay lock).
    - Source: file contains `_simulateSuspend` (D-10(b) coverage).
    - Source: file contains `beforeEach` invoking `vi.resetModules()` (singleton isolation).
    - Test: `npx vitest run src/audio/previewContext.test.ts` exits 0.
    - Test: the run reports at minimum 4 tests passing (D-10 a, b, c, d) — count via `--reporter=basic` output.
    - CLI: `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>
    `src/audio/previewContext.test.ts` exists, exercises D-10(a–d) under the FakeAudioContext polyfill with module-singleton isolation between cases, and the full test file passes.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user-gesture → AudioContext creation | Click handler invokes a synchronous chain that constructs `new AudioContext()` on first tap. The browser autoplay-policy gate is the trust boundary — only gesture-attached construction is permitted. |
| previewContext module → cueSynth module | Function call across two pure-audio modules in the same trust domain (no external input, no untrusted serialization). Boundary is structural (import graph), not security-relevant. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-40-01 | T (Tampering) | previewContext.ts singleton | accept | The `let ctx` module variable is module-private; ES module encapsulation prevents external reassignment. No exported setter. No risk surface beyond what every module-private state in this codebase already carries (cueSynth, audioEngine, useTimbreChoice all use the same pattern). |
| T-40-02 | I (Information disclosure) | playInhalePreview(timbre) input | accept | The `timbre` argument is a TS-narrowed `TimbreId` union (`'bowl' \| 'bell' \| 'sine' \| 'flute'`). TypeScript enforces the narrow type at every call site; the dispatch table `TIMBRE_PRESETS[timbre]` only contains keys for the four enumerated values. No PII, no remote data, no untrusted string flows through. |
| T-40-03 | D (Denial of service) | rapid-tap → polyphonic overlap | accept | Per CONTEXT D-08, brief polyphonic overlap during rapid auditioning is by design. cueSynth's per-call oscillator graph + envelope + `ended` cleanup (Phase 9 AUDIO-04 explicit-disconnect contract) bounds the resource footprint. Web Audio engines bound oscillator count; pathological tap-storms degrade gracefully. No new attack vector. |
| T-40-04 | E (Elevation of privilege) | new AudioContext() | accept | AudioContext construction is gated by the browser autoplay policy; the only call site is inside the user-gesture chain. No way to escalate from this surface; matches the Phase 5/5.1 / Phase 9 audio-creation invariant already shipped. |

Severity ceiling: all dispositions are **accept** with rationale (no PII, no remote data, no untrusted input, no escalation surface). No `high`-severity threats — gate passes.
</threat_model>

<verification>
- `src/audio/previewContext.ts` exists with `export function playInhalePreview(timbre: TimbreId): void`.
- `npx tsc --noEmit` exits 0.
- `npx vitest run src/audio/previewContext.test.ts` exits 0 with at least 4 passing assertions covering D-10(a–d).
- File does not import `'./audioEngine'`, any audioEngine re-export, or `'react'`/`'react-dom'`.
- No `await` token in `src/audio/previewContext.ts` outside comments (synchronous-call-path contract per D-12).
- The module exports exactly one symbol (`playInhalePreview`) — no other named exports.
</verification>

<success_criteria>
- Module file created with the singleton + lazy-create + resume-if-suspended + dispatch pipeline.
- Unit test file passes under FakeAudioContext polyfill covering D-10(a–d).
- PREV-02 satisfied: the dispatch is `scheduleInCueForTimbre` from `./cueSynth` (reuse, not duplication).
- PREV-05 satisfied structurally: singleton + synchronous-call-path contract (no await between resume and schedule).
- Atomic commit `feat(40): add previewContext module + unit tests` per Tiger Style PATTERNS precedent.
- Per-commit green gate: `tsc && lint && build && test` exit 0.
</success_criteria>

<output>
After completion, create `.planning/phases/40-timbre-preview-cue/40-01-SUMMARY.md` per the SUMMARY template — record: files added, exact lines added per file, the D-10 test case count, any deviations from CONTEXT decisions (none expected), and the green-gate evidence.
</output>
