---
phase: 40-timbre-preview-cue
plan: 02
type: execute
wave: 2
depends_on: [40-01]
files_modified:
  - src/audio/previewContext.no-audioengine-import.test.ts
autonomous: true
requirements: [PREV-03]
requirements_addressed: [PREV-03]
tags: [audio, drift-guard, structural-test, import-graph, prev-03]

must_haves:
  truths:
    - "src/audio/previewContext.ts cannot import './audioEngine' without failing CI (PREV-03 structural lock)"
    - "src/audio/previewContext.ts cannot import any module that re-exports the audioEngine muted state (Claude's Discretion ban-list extension per D-15)"
    - "The drift-guard runs against the actual source text on disk (not the transpiled output) so future refactors fail loudly"
    - "Deleting this file is the intentional unlock — recorded as the documented future-phase mechanism"
  artifacts:
    - path: "src/audio/previewContext.no-audioengine-import.test.ts"
      provides: "Structural-drift-guard locking PREV-03 at the import-graph level"
      contains: "readFileSync"
  key_links:
    - from: "src/audio/previewContext.no-audioengine-import.test.ts"
      to: "src/audio/previewContext.ts (read as plain text via fs.readFileSync)"
      via: "file-I/O + regex scan"
      pattern: "readFileSync\\([^)]*previewContext\\.ts"
---

<objective>
Create the structural drift-guard test `src/audio/previewContext.no-audioengine-import.test.ts` that reads `src/audio/previewContext.ts` as plain text and asserts it imports neither `./audioEngine` nor any module known to re-export the engine's muted state. This is the load-bearing lock for PREV-03 ("preview plays when muted") — proving the import absence proves the preview cannot be muted by `setMuted`, since `let muted` lives only inside the audioEngine.ts:160 closure (no exported setter).

Purpose: PREV-03 is satisfied structurally, not by a runtime branch. Per CONTEXT D-11, this is treated as a first-class deliverable equal in weight to the actual preview code — it is the mechanism that keeps PREV-03 alive across Phase 41+ refactors.

Output: One new test file. Lineage: Phase 26 I18N-07 → 37 STATS-05 → 38 VAR-06 → 39 THM-01..03 → 40 PREV-03 (drift-guard-as-lock pattern extended to the import-graph level).
</objective>

<execution_context>
@$HOME/.claude-personal-personal/get-shit-done/workflows/execute-plan.md
@$HOME/.claude-personal-personal/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/40-timbre-preview-cue/40-CONTEXT.md
@.planning/phases/40-timbre-preview-cue/40-PATTERNS.md
@.planning/phases/40-timbre-preview-cue/40-01-PLAN.md

<!-- READ-ONLY analog files for the drift-guard-as-lock pattern. -->
@src/content/content.no-removed-themes.test.ts
@src/content/content.no-variants.test.ts
@src/content/content.no-stats-ui.test.ts
@src/audio/previewContext.ts

<interfaces>
<!-- Key contracts for the drift-guard. Extracted from the codebase. -->

From src/content/content.no-removed-themes.test.ts (Phase 39 — full structural twin):
  /// <reference types="node" />
  import { describe, it, expect } from 'vitest'
  import { readFileSync } from 'node:fs'
  import { resolve } from 'node:path'

  - Pattern: read production source with readFileSync, scan with regex, assert no forbidden tokens.
  - Header WHY-only comment block citing the requirement ID, the phase decision, and the "delete this file = intentional unlock" mechanism.

From src/audio/audioEngine.ts:160:
  - `let muted` lives inside the createAudioEngine closure. No module-level export. No setter on any other module.
  - The only way to reach this closure is through the engine instance returned by createAudioEngine().

From src/audio/previewContext.ts (created in Plan 01):
  - Imports ONLY `./cueSynth` and `../domain/settings` (no audioEngine, no hooks).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create the structural drift-guard `previewContext.no-audioengine-import.test.ts`</name>
  <files>src/audio/previewContext.no-audioengine-import.test.ts</files>
  <read_first>
    - src/content/content.no-removed-themes.test.ts (full file — the exact structural twin; copy the file-header WHY block style and the readFileSync + regex shape)
    - src/content/content.no-variants.test.ts (cross-reference for the ban-list-as-array idiom)
    - src/content/content.no-stats-ui.test.ts (cross-reference)
    - src/audio/previewContext.ts (the file under scan — confirm the imports the drift-guard will assert against)
    - src/audio/audioEngine.ts (lines 155-165 — confirm `let muted` location and absence of any exported setter; this is the WHY the structural lock works)
    - .planning/phases/40-timbre-preview-cue/40-CONTEXT.md (D-11, D-15 Claude's Discretion on ban-list scope)
    - .planning/phases/40-timbre-preview-cue/40-PATTERNS.md (`src/audio/previewContext.no-audioengine-import.test.ts` section — header pattern + ban-list shape)
  </read_first>
  <behavior>
    - Scanning `src/audio/previewContext.ts` (as it currently exists from Plan 01) PASSES the drift-guard.
    - If a future refactor adds `import { ... } from './audioEngine'` to previewContext.ts, the drift-guard FAILS with a message naming the forbidden import.
    - If a future refactor adds `import { ... } from '../hooks/useAudioCues'` to previewContext.ts, the drift-guard FAILS (Claude's Discretion ban-list extension per D-15 — covers the indirect "hook re-exports muted state" path).
    - The error message lists every forbidden import that hit, so the failure mode is immediately diagnosable.
    - The test reads the file via `readFileSync` so it scans the actual source on disk (NOT the transpiled module graph) — captures import statements regardless of whether they would actually be reachable at runtime.
  </behavior>
  <action>
    Create `src/audio/previewContext.no-audioengine-import.test.ts` as a structural drift-guard, mirroring the shape of `src/content/content.no-removed-themes.test.ts` and the header pattern from PATTERNS.md `src/audio/previewContext.no-audioengine-import.test.ts` section.

    Top-of-file (in order):
    - `/// <reference types="node" />` triple-slash directive (verbatim from content.no-removed-themes.test.ts:47 — adds node:fs type coverage for this test-only file without touching tsconfig.app.json).
    - WHY-only header comment block (per PATTERNS.md "header pattern" subsection) citing:
      (1) The phase decision ID (CONTEXT D-11).
      (2) The requirement being locked (PREV-03 — "preview plays when muted").
      (3) The mechanism (audioEngine.ts:160 `let muted` is closure-only, no exported setter; proving the import absence proves the unmutability).
      (4) The unlock contract ("Deleting this file is the intentional unlock — record rationale in that phase's SUMMARY").
    - Imports: `describe, it, expect` from `'vitest'`; `readFileSync` from `'node:fs'`; `resolve` from `'node:path'`.

    Test body:
    - Define `const PREVIEW_PATH = resolve(__dirname, 'previewContext.ts')`.
    - Define `FORBIDDEN_IMPORTS: Array<{ label: string; pattern: RegExp }>` covering at minimum (Claude's Discretion per D-15 — pick the cleaner ban-list at PATTERNS time):
      1. `import ... from './audioEngine'` — `/from\s+['"]\.\/audioEngine['"]/`. **Primary lock for PREV-03.**
      2. `import ... from '../audio/audioEngine'` — `/from\s+['"]\.\.\/audio\/audioEngine['"]/`. Covers relative-path variants if previewContext.ts ever moves.
      3. `import ... from '../hooks/useAudioCues'` — `/from\s+['"]\.\.\/hooks\/useAudioCues['"]/`. Wider net per D-15 — the hook re-exports mute state if it ever gets refactored.
    - One `describe` block named `'Phase 40 drift-guard: previewContext.ts must not import audioEngine (PREV-03 structural lock)'`.
    - One `it` block: read the file via `readFileSync(PREVIEW_PATH, 'utf-8')`, iterate the ban-list, collect any matches into a `hits: string[]` array, assert `hits` equals `[]` with a message listing every hit label on failure.

    Hard invariants:
    - Use `node:fs` / `node:path` (the node: prefixed module specifiers — required by the existing tsconfig.app.json + jsdom test env).
    - Do NOT add any code that mutates global state.
    - Do NOT scan beyond `src/audio/previewContext.ts` — Plan 02's scope is the previewContext module only (the Plan 03 wiring is a separate concern; the import-graph lock for the PREVIEW file is the load-bearing artifact).
    - No NEVER place fenced code blocks inside this action.
  </action>
  <verify>
    <automated>npx vitest run src/audio/previewContext.no-audioengine-import.test.ts --reporter=basic 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - Source: `src/audio/previewContext.no-audioengine-import.test.ts` exists at the repo path.
    - Source: file starts with `/// <reference types="node" />` triple-slash directive.
    - Source: file contains a WHY-only header comment block referencing `D-11` and `PREV-03` (substring match for both tokens).
    - Source: file contains `readFileSync` and `resolve` imports from `node:fs` and `node:path` respectively.
    - Source: ban-list contains at minimum the three forbidden imports listed in `<action>` (`./audioEngine`, `../audio/audioEngine`, `../hooks/useAudioCues`).
    - Test: `npx vitest run src/audio/previewContext.no-audioengine-import.test.ts` exits 0 (file as written in Plan 01 passes the guard).
    - Behavior: Manually verify (no automation needed for this acceptance — covered by the next bullet) that an editor adding `import { setMuted } from './audioEngine'` to previewContext.ts causes the guard to fail with a non-empty `hits` array.
    - Behavior: The failure message (assertion description on the `expect(hits, ...).toEqual([])` call) explicitly names which forbidden import(s) were detected.
    - CLI: `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>
    `src/audio/previewContext.no-audioengine-import.test.ts` exists, passes against the current `src/audio/previewContext.ts`, and rejects any future change that re-introduces an audioEngine import (or any module known to re-export muted state). PREV-03 is structurally locked.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test file → source file (file-I/O) | Test reads the source file at scan time via `readFileSync`. Trust boundary is internal (both files live in the same repo, under version control); no untrusted input flows in. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-40-05 | T (Tampering) | previewContext.ts source text | mitigate | This test IS the mitigation. Tampering with `previewContext.ts` to add an audioEngine import causes the drift-guard to fail the per-commit green gate. The PREV-03 invariant is enforced via the regex ban-list. |
| T-40-06 | I (Information disclosure) | readFileSync call | accept | The path scanned is a hard-coded sibling file (`resolve(__dirname, 'previewContext.ts')`); no user input, no path traversal surface. No risk. |
| T-40-07 | E (Elevation of privilege) | drift-guard bypass via comment | accept | A future contributor could in principle add `// import { ... } from './audioEngine'` (commented out) which the regex would still hit, OR add the import inside a string literal which the regex would still hit. Both are false-positive-leaning, not false-negative-leaning — the test errs toward over-locking, which is the desired posture. Deleting the test is the documented unlock; bypassing it via syntax tricks is detectable in code review. |

Severity ceiling: all dispositions are **mitigate** or **accept** with rationale. No `high`-severity threats. Gate passes.
</threat_model>

<verification>
- `src/audio/previewContext.no-audioengine-import.test.ts` exists with the `/// <reference types="node" />` directive and the WHY-only header.
- The test passes against the current `src/audio/previewContext.ts` (no forbidden imports present after Plan 01).
- The ban-list covers at minimum the three import patterns listed in `<action>`.
- `npx tsc --noEmit` exits 0; `npx vitest run src/audio/previewContext.no-audioengine-import.test.ts` exits 0.
</verification>

<success_criteria>
- Structural drift-guard exists and passes.
- PREV-03 is locked at the import-graph level — future "helpfully wire engine into preview" refactors will fail the per-commit green gate.
- Drift-guard-as-lock pattern lineage extended from Phase 39 (THM) to Phase 40 (PREV-03) at the import-graph level.
- Atomic commit `test(40): lock PREV-03 via structural import-graph guard` per Tiger Style PATTERNS precedent.
- Per-commit green gate: `tsc && lint && build && test` exit 0.
</success_criteria>

<output>
After completion, create `.planning/phases/40-timbre-preview-cue/40-02-SUMMARY.md` per the SUMMARY template — record: file added, ban-list contents, the WHY-only header text, the unlock contract documented in the header, and the green-gate evidence.
</output>
