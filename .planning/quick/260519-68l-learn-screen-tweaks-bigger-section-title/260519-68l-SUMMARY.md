---
phase: quick-260519-68l
plan: "01"
subsystem: learn-dialog
tags: [ui, typography, learn-dialog, locked-copy]
dependency_graph:
  requires: []
  provides: [enlarged-section-h3-headings, tagline-above-affiliation]
  affects: [src/components/LearnDialog.tsx]
tech_stack:
  added: []
  patterns: [first-letter:uppercase CSS utility for display-capitalisation without string mutation]
key_files:
  created: []
  modified:
    - src/components/LearnDialog.tsx
decisions:
  - "first-letter:uppercase Tailwind utility capitalizes displayed first letter without modifying the locked lowercase string — works for both EN ('inspired...' -> 'Inspired...') and pt-BR ('inspirado...' -> 'Inspirado...')"
  - "Tagline moved to immediately above affiliationLine rather than remaining in the Forrest explainer section, giving it a clearer attribution home near the affiliation micro-line"
  - "lockedCopy.ts D-04 comment left intentionally untouched (file is byte-frozen; touching it would risk the equality test)"
metrics:
  duration: "~8 minutes"
  completed: "2026-05-19"
  tasks_completed: 3
  files_changed: 1
---

# Phase quick-260519-68l Plan 01: Learn Screen Tweaks — Bigger Section Title Summary

**One-liner:** Enlarged six Learn dialog section h3 headings from text-base (16px) to text-xl (20px) and relocated the inspiredByForrest tagline from the Forrest explainer section to directly above the affiliation micro-line, restyled centered/italic/bold/text-xs with CSS first-letter:uppercase display-capitalisation.

## Tasks Completed

| Task | Name | Commit | Files |
|------|------|--------|-------|
| 1 | Enlarge six section h3 headings to text-xl | a481f87 | src/components/LearnDialog.tsx |
| 2 | Relocate and restyle Forrest tagline | db502ba | src/components/LearnDialog.tsx |
| 3 | Validate types and reconcile LearnDialog tests | (no commit — no test changes needed) | — |

## Verification

- `npx tsc -b`: exits 0 (clean)
- `npx vitest run src/components/LearnDialog.test.tsx`: 32/32 tests passed
- `grep -c 'text-xl font-semibold text-[var(--color-breathing-accent-strong)]' src/components/LearnDialog.tsx`: 6
- `grep -c 'text-base font-semibold text-[var(--color-breathing-accent-strong)]' src/components/LearnDialog.tsx`: 1 (Close button only — expected)
- `grep -n 'inspiredByForrest' src/components/LearnDialog.tsx`: single render site at line 224, directly above affiliationLine
- `git diff --stat src/content/lockedCopy.ts`: empty (byte-frozen, untouched)

## Deviations from Plan

None — plan executed exactly as written.

## Known Stubs

None.

## Intentionally Left Stale

`src/content/lockedCopy.ts` contains a D-04 comment (line ~25 area) that refers to the Forrest phrase living in the dialog. This comment was NOT updated because `lockedCopy.ts` is byte-frozen by a locked-copy equality test (`lockedCopy.test.ts` uses `.toBe()` on the string values). The D-04 comment in `lockedCopy.ts` is now slightly stale with respect to the tagline's new render location, but the content string itself is correct and immutable. Future maintainers should be aware this comment does not reflect the tag line's visual position.

## Threat Flags

None — no new network endpoints, auth paths, or trust boundary changes.

## Self-Check: PASSED

- src/components/LearnDialog.tsx: FOUND
- Commit a481f87: FOUND (feat: enlarge six section h3 headings)
- Commit db502ba: FOUND (feat: relocate and restyle Forrest tagline)
- lockedCopy.ts unchanged: CONFIRMED
- tsc -b: clean
- vitest: 32/32 green
