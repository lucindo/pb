---
phase: quick-260519-68l
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/LearnDialog.tsx
  - src/components/LearnDialog.test.tsx
autonomous: true
requirements: []
must_haves:
  truths:
    - "All six Learn dialog section headings render at 20px (text-xl)"
    - "The 24px <h2> dialog title 'About this practice' is unchanged"
    - "The 'inspired by Forrest's teachings' tagline no longer renders inside the Forrest explainer section"
    - "The tagline renders directly above the affiliation micro-line, centered/italic/bold, at text-xs, with first letter displayed capitalized"
    - "src/content/lockedCopy.ts is not modified (locked string preserved byte-for-byte)"
    - "LearnDialog test file passes and tsc -b is clean"
  artifacts:
    - path: "src/components/LearnDialog.tsx"
      provides: "Enlarged section headings + relocated tagline"
      contains: "first-letter:uppercase"
  key_links:
    - from: "src/components/LearnDialog.tsx tagline <p>"
      to: "lockedCopy.affiliationLine <p>"
      via: "sibling paragraph rendered immediately above"
      pattern: "inspiredByForrest"
---

<objective>
Two visual tweaks to the shared Learn dialog (`src/components/LearnDialog.tsx`):

1. Enlarge the six `<h3>` section titles from 16px (`text-base`) to 20px (`text-xl`).
2. Move the "inspired by Forrest's teachings" tagline out of the Forrest explainer
   section and place it directly above the affiliation micro-line, restyled
   (centered, italic, bold, `text-xs`, display-capitalized first letter).

Purpose: Improve visual hierarchy of section headings and give the Forrest
attribution tagline a clearer home next to the affiliation line.
Output: Updated `LearnDialog.tsx`; `LearnDialog.test.tsx` assertions reconciled if
they break; stale comments corrected.
</objective>

<execution_context>
@$HOME/.claude-personal-personal/get-shit-done/workflows/execute-plan.md
@$HOME/.claude-personal-personal/get-shit-done/templates/summary.md
</execution_context>

<context>
@src/components/LearnDialog.tsx
@src/components/LearnDialog.test.tsx
@src/content/lockedCopy.ts

<interfaces>
Locked copy strings (DO NOT MODIFY src/content/lockedCopy.ts):
- `lockedCopy.inspiredByForrest` — EN: "inspired by Forrest's teachings" (lowercase i),
  pt-BR: "inspirado nos ensinamentos do Forrest". Byte-frozen by lockedCopy.test.ts `.toBe()`.
- `lockedCopy.affiliationLine` — EN: "Independent project. Not affiliated with Forrest Knutson."

Current relevant LearnDialog.tsx lines:
- Six section <h3> elements at lines 111, 115, 125, 143, 152, 196 — all use
  `className="text-base font-semibold text-[var(--color-breathing-accent-strong)]"`.
- <h2> dialog title at line 105 uses `text-2xl` — MUST stay unchanged.
- Tagline <p> at line 147 (inside the Forrest explainer div):
  `<p className="text-base leading-6 italic text-[var(--color-breathing-muted)] [&:not(:first-of-type)]:mt-2">{lockedCopy.inspiredByForrest}</p>`
- Affiliation <p> at lines 221-223:
  `<p className="text-center text-xs text-[var(--color-breathing-muted)]">{lockedCopy.affiliationLine}</p>`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enlarge the six section <h3> headings to text-xl</name>
  <files>src/components/LearnDialog.tsx</files>
  <action>
    In every one of the six `<h3>` section-heading elements (lines ~111, ~115, ~125,
    ~143, ~152, ~196), change the leading class token `text-base` to `text-xl`.
    The resulting className for each is
    `text-xl font-semibold text-[var(--color-breathing-accent-strong)]`.
    Keep `font-semibold` and the `text-[var(--color-breathing-accent-strong)]` color
    token unchanged. Do NOT touch the `<h2>` dialog title at line ~105 (stays
    `text-2xl`). Do NOT change any `<p>` body copy or `<a>` link classes.
    User decision: 20px / `text-xl` was explicitly chosen (midpoint of 16px h3 and
    24px h2).
  </action>
  <verify>
    <automated>grep -c 'text-xl font-semibold text-\[var(--color-breathing-accent-strong)\]' src/components/LearnDialog.tsx</automated>
  </verify>
  <done>All six section &lt;h3&gt; use `text-xl font-semibold`; exactly zero `<h3>` retains `text-base`; the &lt;h2&gt; title still uses `text-2xl`.</done>
</task>

<task type="auto">
  <name>Task 2: Relocate and restyle the Forrest tagline</name>
  <files>src/components/LearnDialog.tsx</files>
  <action>
    Two edits in LearnDialog.tsx:

    (a) REMOVE the tagline `<p>` at line ~147 from inside the "Who is Forrest
    Knutson" explainer div — the line rendering `{lockedCopy.inspiredByForrest}`
    with class `text-base leading-6 italic text-[var(--color-breathing-muted)]
    [&:not(:first-of-type)]:mt-2`. The explainer div then ends after the
    `forrest.body.split('\n\n').map(...)` paragraphs.

    (b) ADD a new `<p>` directly ABOVE the affiliation-line `<p>` (lines ~221-223).
    The new paragraph renders `{lockedCopy.inspiredByForrest}` with className
    `text-center text-xs font-bold italic first-letter:uppercase text-[var(--color-breathing-muted)]`.
    The `first-letter:uppercase` Tailwind utility capitalizes the displayed first
    letter at render time without modifying the locked lowercase string — works for
    both EN ("inspired..." -> "Inspired...") and pt-BR ("inspirado..." -> "Inspirado...").
    Do NOT modify `src/content/lockedCopy.ts` — the string stays byte-frozen.

    (c) Correct stale comments in LearnDialog.tsx: the top-of-file comment block
    (lines ~8-20) and the explainer-section comment should no longer describe the
    tagline as living inside the Forrest explainer. Update any line that pins the
    tagline's old location to reflect it now sits immediately above the affiliation
    micro-line. Do NOT touch lockedCopy.ts's D-04 comment — leave it and note the
    staleness in the SUMMARY instead (lockedCopy.ts is locked; touching it risks the
    equality test).
  </action>
  <verify>
    <automated>grep -n 'inspiredByForrest' src/components/LearnDialog.tsx</automated>
  </verify>
  <done>Exactly one `inspiredByForrest` render site remains, located immediately above the affiliation `<p>`, with class `text-center text-xs font-bold italic first-letter:uppercase text-[var(--color-breathing-muted)]`; the Forrest explainer div no longer renders the tagline; stale LearnDialog.tsx comments corrected.</done>
</task>

<task type="auto">
  <name>Task 3: Validate types and reconcile LearnDialog tests</name>
  <files>src/components/LearnDialog.test.tsx</files>
  <action>
    Run `npx tsc -b` and `npx vitest run src/components/LearnDialog.test.tsx`.

    Existing tagline assertions are presence-only and should still pass after the
    move:
    - Line ~58: `screen.getByText(/inspired by Forrest's teachings/)` — the locked
      string still renders, so this passes (the regex matches the locked lowercase
      string; `first-letter:uppercase` is a CSS-only display transform that does not
      change the DOM text node).
    - Line ~176: `screen.getByText(LOCKED_COPY['pt-BR'].inspiredByForrest)` — same,
      still passes.
    - Line ~146/~272: "every <a> has target/rel" — unaffected (no links changed).

    If any assertion breaks because it pins the OLD location (e.g. asserts the
    tagline appears within the Forrest explainer section) or the OLD styling
    (`text-base`, position after the explainer body), update that assertion to the
    new location/styling. If nothing breaks, make NO test changes. Do NOT weaken
    presence assertions. Do NOT modify lockedCopy.test.ts or lockedCopy.ts.
  </action>
  <verify>
    <automated>npx tsc -b && npx vitest run src/components/LearnDialog.test.tsx</automated>
  </verify>
  <done>`npx tsc -b` is clean; `npx vitest run src/components/LearnDialog.test.tsx` passes with all assertions green; any breakage caused only by the move/restyle was fixed without weakening presence checks.</done>
</task>

</tasks>

<verification>
- `npx tsc -b` exits 0.
- `npx vitest run src/components/LearnDialog.test.tsx` passes.
- `grep -c 'text-base font-semibold' src/components/LearnDialog.tsx` shows the
  section headings no longer use `text-base` (the Close button keeps its own
  `text-base font-semibold` — that is the button, not an `<h3>`; verify the six
  `<h3>` specifically use `text-xl`).
- `src/content/lockedCopy.ts` is unchanged (`git diff --stat src/content/lockedCopy.ts` empty).
</verification>

<success_criteria>
- Six Learn-dialog section `<h3>` headings render at 20px (`text-xl`); `<h2>` title unchanged.
- "inspired by Forrest's teachings" tagline renders once, directly above the
  affiliation micro-line, centered + italic + bold + `text-xs`, first letter shown
  capitalized via `first-letter:uppercase`.
- Tagline removed from the Forrest explainer section.
- `lockedCopy.ts` untouched; locked-copy equality test still passes.
- tsc clean; LearnDialog tests green.
</success_criteria>

<output>
After completion, create `.planning/quick/260519-68l-learn-screen-tweaks-bigger-section-title/260519-68l-SUMMARY.md`.
Note in the SUMMARY any remaining stale comment in `src/content/lockedCopy.ts`
(D-04 comment) that was intentionally left untouched because lockedCopy.ts is locked.
</output>
