---
phase: quick-260519-9mi
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/domain/settings.ts
  - src/domain/settings.test.ts
  - src/storage/prefs.test.ts
  - src/app/App.test.tsx
  - src/app/App.session.test.tsx
autonomous: true
requirements: [QUICK-260519-9mi]
must_haves:
  truths:
    - "A new user (no/invalid persisted cue) gets the 'arrow' cue style by default"
    - "A new user (no/invalid persisted timbre) gets the 'sine' timbre by default"
    - "Theme, Variant, Language defaults are unchanged ('system' / 'orb' / 'en')"
    - "A returning user with a valid persisted cue/timbre keeps their choice (coercer fallback only)"
    - "npm run build is clean and npm run test:run has no NEW failures"
  artifacts:
    - path: "src/domain/settings.ts"
      provides: "DEFAULT_CUE = 'arrow', DEFAULT_TIMBRE = 'sine'"
      contains: "DEFAULT_CUE"
  key_links:
    - from: "src/storage/prefs.ts"
      to: "src/domain/settings.ts"
      via: "coerceCue / coerceTimbre import DEFAULT_CUE / DEFAULT_TIMBRE by reference"
      pattern: "DEFAULT_CUE|DEFAULT_TIMBRE"
---

<objective>
Apply the two operator-confirmed Settings default changes and fix the stale comment, then update every test assertion tied to the OLD defaults.

Purpose: The operator reviewed all 5 app Settings defaults one-by-one. Two changed (Cue, Timbre); three are explicitly confirmed unchanged. This task mechanically applies the locked spec.

Output: `DEFAULT_CUE = 'arrow'` and `DEFAULT_TIMBRE = 'sine'` in `src/domain/settings.ts`, a corrected `DEFAULT_CUE` comment, and updated test fixtures so the suite reflects the new defaults.
</objective>

<execution_context>
@$HOME/.claude-personal-personal/get-shit-done/workflows/execute-plan.md
@$HOME/.claude-personal-personal/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/quick/260519-9mi-set-default-values-for-app-settings-item/260519-9mi-CONTEXT.md
@src/domain/settings.ts

<interfaces>
<!-- Locked spec from CONTEXT.md. Apply exactly — do not re-derive. -->

Current state in src/domain/settings.ts:
- Line 115: `export const DEFAULT_TIMBRE: TimbreId = 'bowl'`
- Line 135: `export const DEFAULT_CUE: CueStyleId = 'labels'  // FIXED per CONTEXT D-01 / success criterion 5`

UNCHANGED (do NOT touch): DEFAULT_THEME = 'system', DEFAULT_VARIANT = 'orb', DEFAULT_LOCALE = 'en'.
CUE_OPTIONS and TIMBRE_OPTIONS are UNCHANGED — only the default pointer moves.
No migration code needed: changing the constant only affects new users and users with no/invalid persisted value. Coercers (`coerceCue` / `coerceTimbre` in src/storage/prefs.ts) and `src/hooks/useAudioCues.ts` consume the defaults by reference and pick up new values automatically — no edits there.

Default-DEPENDENT test sites that MUST change (resolved default / coercer fallback):
- src/domain/settings.test.ts:163-164 — `it('DEFAULT_CUE is "labels" ...')` asserting `expect(DEFAULT_CUE).toBe('labels')`
- src/storage/prefs.test.ts:202-203 — `it('DEFAULT_PREFS.cue === "labels" ...')` asserting `expect(DEFAULT_PREFS.cue).toBe('labels')`
- src/app/App.test.tsx:116-129 — `it('default cue=labels renders the visible In/Out text — zero regression')`: localStorage is cleared so `loadPrefs().cue` resolves to DEFAULT_CUE. With DEFAULT_CUE now 'arrow', the shape renders an aria-hidden SVG glyph, NOT visible In/Out text.
- src/app/App.session.test.tsx:547-562 — `it('TIMBRE-02 zero-regression ... DEFAULT_TIMBRE "bowl" ...')`: localStorage cleared so `loadPrefs()` resolves to DEFAULT_TIMBRE, now 'sine'.

Test sites that are NOT default-dependent — LEAVE ALONE (they exercise a specific value, or already use DEFAULT_* by reference):
- src/storage/prefs.test.ts:57-58, 62-63 — pass explicit non-default cue/timbre values in the input AND output; unrelated to the default.
- src/storage/prefs.test.ts:81-85 — `pre-Phase-25 envelope` test already asserts `cue: DEFAULT_CUE` by reference; passes automatically.
- src/storage/prefs.test.ts:128 — `coerceTimbre('bowl')` exercises the specific 'bowl' value, not the default.
- src/domain/settings.test.ts:93-94, 107, 134-135, 154, 159-160 — `isValidTimbre`/`isValidCue` membership + CUE_OPTIONS shape; options are unchanged.
- src/app/App.test.tsx:143-199 — mid-session cross-tab tests `seedCue('arrow')` then write explicit `'labels'`; they exercise specific values, not the default.
- src/app/App.session.test.tsx mid-session timbre cross-tab tests using explicit seeded timbres.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Change DEFAULT_CUE and DEFAULT_TIMBRE, fix stale comment, update default-dependent tests</name>
  <files>src/domain/settings.ts, src/domain/settings.test.ts, src/storage/prefs.test.ts, src/app/App.test.tsx, src/app/App.session.test.tsx</files>
  <action>
In src/domain/settings.ts:
1. Line 115: change `export const DEFAULT_TIMBRE: TimbreId = 'bowl'` to `export const DEFAULT_TIMBRE: TimbreId = 'sine'`.
2. Line 135: change `export const DEFAULT_CUE: CueStyleId = 'labels'` to `export const DEFAULT_CUE: CueStyleId = 'arrow'`. Replace the stale trailing comment `// FIXED per CONTEXT D-01 / success criterion 5` with a comment noting the default is 'arrow', set via quick task 260519-9mi (2026-05-19), superseding the earlier CONTEXT D-01 "labels (fixed)" decision. Exact wording is your discretion.

Then update ONLY the default-dependent test assertions listed in the <interfaces> block — do NOT touch tests that exercise a specific non-default value:
3. src/domain/settings.test.ts — update the `DEFAULT_CUE` test (~line 163-164): change the assertion to `expect(DEFAULT_CUE).toBe('arrow')` and update the `it(...)` title and any "FIXED per CONTEXT D-01" wording so it reads as the 'arrow' default set by this quick task.
4. src/storage/prefs.test.ts — update the `DEFAULT_PREFS.cue` test (~line 202-203): assertion to `expect(DEFAULT_PREFS.cue).toBe('arrow')` and the `it(...)` title accordingly.
5. src/app/App.test.tsx — the `default cue=labels renders the visible In/Out text` test (~line 116-129): with DEFAULT_CUE now 'arrow' and localStorage cleared, the shape renders an aria-hidden SVG glyph instead of visible text. Update the test to assert arrow-cue behavior: rename the title (e.g. "default cue=arrow renders the SVG glyph — zero regression"), update the comment about coercing to DEFAULT_CUE, and flip the body to assert the aria-hidden SVG IS present (mirror the existing `arrow cue seeded before Start` test at ~line 131-141). Drop the `toHaveTextContent('In')` visible-text assertion.
6. src/app/App.session.test.tsx — the `TIMBRE-02 zero-regression` test (~line 547-562): with DEFAULT_TIMBRE now 'sine', update the `it(...)` title, the comment, and the assertion `expect(firstCallArgs![3]).toBe('bowl')` to `'sine'`.

After editing, grep the repo once more for any remaining test assertion that depends on the OLD default value to confirm nothing was missed: `grep -rn "DEFAULT_CUE\|DEFAULT_TIMBRE\|DEFAULT_PREFS.cue" src --include='*.test.ts' --include='*.test.tsx'` and inspect each hit. Update any additional default-dependent assertion found; leave specific-value tests alone.
  </action>
  <verify>
    <automated>npm run build && npm run test:run</automated>
  </verify>
  <done>`src/domain/settings.ts` has `DEFAULT_CUE = 'arrow'` (with corrected comment) and `DEFAULT_TIMBRE = 'sine'`; DEFAULT_THEME/VARIANT/LOCALE unchanged. `npm run build` is clean. `npm run test:run` shows no NEW failures — the only failures are the 3 pre-existing `App.persistence.test.tsx` LOCL-03 failures (known/unrelated, not counted). All four default-dependent test sites assert the new defaults.</done>
</task>

</tasks>

<verification>
- `npm run build` exits 0 with no TypeScript errors.
- `npm run test:run` (NOT `npm test` — that is watch mode): the only failing tests are the 3 known pre-existing `App.persistence.test.tsx` LOCL-03 failures. Any other failure is a regression and must be fixed.
- `grep -n "DEFAULT_CUE\|DEFAULT_TIMBRE" src/domain/settings.ts` shows `'arrow'` and `'sine'`.
</verification>

<success_criteria>
- DEFAULT_CUE = 'arrow' and DEFAULT_TIMBRE = 'sine' in src/domain/settings.ts.
- The stale `// FIXED per CONTEXT D-01 / success criterion 5` comment is replaced.
- DEFAULT_THEME, DEFAULT_VARIANT, DEFAULT_LOCALE untouched.
- All four default-dependent test sites updated; specific-value tests untouched.
- Build clean; no new test failures (3 known LOCL-03 failures excluded).
</success_criteria>

<output>
After completion, create `.planning/quick/260519-9mi-set-default-values-for-app-settings-item/260519-9mi-SUMMARY.md`
</output>
