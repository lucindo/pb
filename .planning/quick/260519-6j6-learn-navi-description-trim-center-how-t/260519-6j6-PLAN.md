---
phase: 260519-6j6-learn-navi-description-trim-center-how-t
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/content/learnContent.ts
  - src/components/SettingsDialog.tsx
autonomous: true
requirements: [QUICK-260519-6j6]

must_haves:
  truths:
    - "The Navi Kriya 'What is Navi Kriya' EN description no longer contains the clause 'moving attention along the spine'"
    - "The Navi Kriya 'O que é Navi Kriya' pt-BR description no longer contains the clause 'movendo a atenção pela coluna'"
    - "The iOS-only 'How to install' button is horizontally centered in its row"
    - "The non-iOS 'Install' button is unchanged (not centered)"
    - "tsc and the two affected test files stay green"
  artifacts:
    - path: "src/content/learnContent.ts"
      provides: "Trimmed Navi Kriya descriptions in EN and pt-BR"
      contains: "of the body. This is a quiet seated practice"
    - path: "src/components/SettingsDialog.tsx"
      provides: "Centered iOS install button"
      contains: "mx-auto block"
  key_links:
    - from: "src/components/SettingsDialog.tsx"
      to: "iOS install button className"
      via: "mx-auto block added to button's own className"
      pattern: "mx-auto block"
---

<objective>
Two small, independent UI copy/layout tweaks:
1. Trim the Navi Kriya Learn description in both locales — remove the clause ", moving attention along the spine" (EN) and its pt-BR parallel ", movendo a atenção pela coluna".
2. Center the iOS-only "How to install" button in the Settings dialog without affecting the non-iOS "Install" button.

Purpose: Tighten Learn copy and improve Settings layout balance.
Output: Edited `src/content/learnContent.ts` and `src/components/SettingsDialog.tsx`, with `tsc` clean and affected tests green.
</objective>

<execution_context>
@$HOME/.claude-personal-personal/get-shit-done/workflows/execute-plan.md
@$HOME/.claude-personal-personal/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md

@src/content/learnContent.ts
@src/components/SettingsDialog.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Trim Navi Kriya description in both locales</name>
  <files>src/content/learnContent.ts</files>
  <action>
In the `naviKriya` practice description ONLY (do NOT touch the resonant practice description):

EN — line 118, `section1.body`: delete exactly the substring ", moving attention along the spine" (the leading comma plus the clause). The text "front and back of the body, moving attention along the spine. This is a quiet seated practice" becomes "front and back of the body. This is a quiet seated practice".

pt-BR — line 211, `section1.body`: delete exactly the substring ", movendo a atenção pela coluna" (the leading comma plus the clause). The text "parte frontal e dorsal do corpo, movendo a atenção pela coluna. Esta é uma prática silenciosa" becomes "parte frontal e dorsal do corpo. Esta é uma prática silenciosa".

Make no other edits. The remaining sentence in each body must read with a single space after the period ("of the body. This"; "do corpo. Esta").
  </action>
  <verify>
    <automated>grep -c "moving attention along the spine" src/content/learnContent.ts | grep -qx 0 && grep -c "movendo a atenção pela coluna" src/content/learnContent.ts | grep -qx 0 && grep -q "of the body. This is a quiet seated practice" src/content/learnContent.ts && grep -q "do corpo. Esta é uma prática silenciosa" src/content/learnContent.ts && echo PASS</automated>
  </verify>
  <done>Neither removed clause appears anywhere in the file; both trimmed sentences are present in EN and pt-BR; resonant description untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Center the iOS "How to install" button</name>
  <files>src/components/SettingsDialog.tsx</files>
  <action>
On the iOS-only `<button>` (lines 115-124, the one rendering `{strings.install.iosStepsButton}`), add `mx-auto block` to the START of its `className` string. `block` makes the button a block-level element; `mx-auto` centers it at its natural content width.

CRITICAL CONSTRAINT: Do NOT add centering to the shared `<div className="mt-2">` wrapper (line 112) — that wrapper also contains the non-iOS "Install" button branch, which must stay unchanged. Edit ONLY the iOS button's own className. Do NOT touch the non-iOS Install button (lines 128-135) or its className.
  </action>
  <verify>
    <automated>grep -q "mx-auto block min-h-\[44px\] text-sm font-semibold text-\[var(--color-breathing-accent-strong)\] focus-visible:outline-none" src/components/SettingsDialog.tsx && echo PASS</automated>
  </verify>
  <done>iOS button className begins with "mx-auto block"; the `mt-2` wrapper div and the non-iOS Install button are unchanged.</done>
</task>

<task type="auto">
  <name>Task 3: Validate types and affected tests</name>
  <files>(no file changes — validation only)</files>
  <action>
Run TypeScript build and the two affected test files to confirm nothing broke. If any check fails, return to the relevant task and fix before completing.
  </action>
  <verify>
    <automated>npx tsc -b && npx vitest run src/content/learnContent.test.ts src/components/SettingsDialog.test.tsx</automated>
  </verify>
  <done>`npx tsc -b` exits clean; both test files pass with zero failures.</done>
</task>

</tasks>

<verification>
- `npx tsc -b` is clean.
- `npx vitest run src/content/learnContent.test.ts src/components/SettingsDialog.test.tsx` is green.
- Removed clauses do not appear in `learnContent.ts`.
- iOS button className starts with `mx-auto block`; non-iOS Install button and `mt-2` wrapper unchanged.
</verification>

<success_criteria>
- Navi Kriya EN and pt-BR descriptions no longer contain the spine/coluna clause.
- iOS "How to install" button renders horizontally centered; non-iOS "Install" button visually unchanged.
- tsc clean and both affected test files pass.
</success_criteria>

<output>
After completion, create `.planning/quick/260519-6j6-learn-navi-description-trim-center-how-t/260519-6j6-01-SUMMARY.md`
</output>
