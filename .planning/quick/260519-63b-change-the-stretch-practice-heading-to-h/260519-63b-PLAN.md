---
phase: quick-260519-63b
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/content/strings.ts
  - src/content/strings.test.ts
autonomous: true
requirements: [QUICK-260519-63b]
must_haves:
  truths:
    - "The large Stretch practice heading reads 'HRV Stretch' in EN"
    - "The large Stretch practice heading reads 'Alongar VFC' in PT-BR"
    - "The switcher pill label still reads 'Stretch' / 'Alongar' (unchanged)"
    - "The small-caps eyebrow still reads 'Stretch practice' / 'Prática de Alongar' (unchanged)"
    - "strings.test.ts passes with the new heading values"
  artifacts:
    - path: "src/content/strings.ts"
      provides: "Updated stretchHeading values for both locales"
      contains: "stretchHeading: 'HRV Stretch'"
    - path: "src/content/strings.test.ts"
      provides: "Test assertions pinned to the new heading values"
  key_links:
    - from: "src/app/App.tsx"
      to: "stretchHeading"
      via: "consumes practice.stretchHeading at App.tsx:310,321"
      pattern: "stretchHeading"
---

<objective>
Change ONLY the large Stretch practice heading from "Stretch" to "HRV Stretch" (EN)
and "Alongar" to "Alongar VFC" (PT-BR). The switcher pill label (`stretchName`) and
the small-caps eyebrow (`stretchHeader`) must remain unchanged.

Purpose: The operator wants the practice title to clarify it is an HRV-driven stretch.
Output: Updated `stretchHeading` values in `strings.ts` and matching assertions in `strings.test.ts`.

Note: This deliberately breaks the D-10 invariant ("heading same value as name"). The
type comment at strings.ts:168 and the EN/PT-BR value comments (lines 351, 535-536)
that assert "label and heading are both 'Stretch'/'Alongar'" must be corrected so they
no longer claim an invariant that no longer holds.
</objective>

<execution_context>
@$HOME/.claude-personal-personal/get-shit-done/workflows/execute-plan.md
@$HOME/.claude-personal-personal/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@src/content/strings.ts
@src/content/strings.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update stretchHeading values and correct invariant comments</name>
  <files>src/content/strings.ts</files>
  <action>
  In src/content/strings.ts make exactly two value changes and three comment corrections.
  Value changes:
  - Line 353 (EN): `stretchHeading: 'Stretch',` → `stretchHeading: 'HRV Stretch',`
  - Line 538 (PT-BR): `stretchHeading: 'Alongar',` → `stretchHeading: 'Alongar VFC',`
  Comment corrections (the D-10 "same value as name" invariant is now intentionally broken):
  - Line 168 type comment: change `// practice heading (D-10: same value as stretchName)` to
    `// practice heading (260519-63b: 'HRV Stretch' / 'Alongar VFC', distinct from stretchName)`
  - Line 351 EN comment: change the line that reads `// Phase 34 STRETCH-06 / D-10: EN values — label and heading are both 'Stretch'`
    to `// Phase 34 STRETCH-06 / D-10: EN — label is 'Stretch'; heading 'HRV Stretch' per 260519-63b`
  - Lines 535-536 PT-BR comment: change `// Phase 34 STRETCH-06 / D-10: PT-BR values — label and heading are both 'Alongar'`
    to `// Phase 34 STRETCH-06 / D-10: PT-BR — label is 'Alongar'; heading 'Alongar VFC' per 260519-63b`
  Do NOT touch `stretchName` or `stretchHeader` in either locale. Leave the Spike 007
  switcher-fit comment (about 'Alongar' fitting the 320px switcher) intact — it still
  applies to `stretchName`.
  </action>
  <verify>
    <automated>grep -n "stretchHeading" src/content/strings.ts | grep -E "HRV Stretch|Alongar VFC" | wc -l | grep -q 2 && grep -n "stretchName: 'Stretch'" src/content/strings.ts && grep -n "stretchName: 'Alongar'" src/content/strings.ts</automated>
  </verify>
  <done>strings.ts has `stretchHeading: 'HRV Stretch'` (EN) and `stretchHeading: 'Alongar VFC'` (PT-BR); `stretchName` and `stretchHeader` unchanged; the three D-10 comments corrected to reflect the broken invariant.</done>
</task>

<task type="auto">
  <name>Task 2: Update strings.test.ts assertions for the new heading values</name>
  <files>src/content/strings.test.ts</files>
  <action>
  In src/content/strings.test.ts, in the `Phase 34 stretch practice string keys (STRETCH-06)`
  describe block, update the two assertions that pin the old `stretchHeading` values:
  - Lines 226-228 (EN): change the `it` title from
    `'UI_STRINGS.en.practice.stretchHeading equals "Stretch" (D-10: same as label)'` to
    `'UI_STRINGS.en.practice.stretchHeading equals "HRV Stretch"'`, and change the
    assertion `expect(UI_STRINGS.en.practice.stretchHeading).toBe('Stretch')` to `.toBe('HRV Stretch')`.
  - Lines 234-236 (PT-BR): change the assertion
    `expect(UI_STRINGS['pt-BR'].practice.stretchHeading).toBe('Alongar')` to `.toBe('Alongar VFC')`
    (and the `it` title if it references the old value).
  Leave the `stretchName` assertions (lines 222-224, 230-232) unchanged. Optionally, the
  EN `stretchName` test title at line 222 still says `(D-10: label same as heading)` — that
  parenthetical is now inaccurate; correct it to `(D-10: switcher pill label)` so the suite
  documentation stays truthful. Do not change the assertion value itself ('Stretch'/'Alongar').
  The `stretchHeader` non-empty test and the completeness test need no changes.
  </action>
  <verify>
    <automated>npx vitest run src/content/strings.test.ts</automated>
  </verify>
  <done>strings.test.ts passes; EN/PT-BR `stretchHeading` assertions expect 'HRV Stretch'/'Alongar VFC'; `stretchName` assertions still expect 'Stretch'/'Alongar'.</done>
</task>

</tasks>

<verification>
- `npx vitest run src/content/strings.test.ts` passes (all stretch key tests green).
- `npx tsc --noEmit` reports no type errors (string literals only, no shape change).
- App.tsx:310,321 consume `stretchHeading` and require no edits — the heading renders the new value automatically.
</verification>

<success_criteria>
- The large practice heading shows "HRV Stretch" (EN) / "Alongar VFC" (PT-BR).
- The switcher pill label and small-caps eyebrow are unchanged.
- Test suite green; type check clean.
- D-10 invariant comments corrected so they no longer claim heading === name.
</success_criteria>

<output>
After completion, create `.planning/quick/260519-63b-change-the-stretch-practice-heading-to-h/260519-63b-SUMMARY.md`
</output>
