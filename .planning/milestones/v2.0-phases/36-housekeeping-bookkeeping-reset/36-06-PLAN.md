---
phase: 36
plan: 06
type: execute
wave: 6
depends_on: [36-05]
files_modified:
  - src/storage/storage.test.ts
autonomous: true
requirements: [HOUSE-09]
tags: [housekeeping, test, migration, regression, tdd]
must_haves:
  truths:
    - "Decision references: D-05 (this plan emits commit #6 of the 7-8 logical-group commits — `test(36): add v1→v3 chained migrateEnvelope regression (HOUSE-09)`), D-06 (HOUSE-09 test lives as a new `describe('migrateEnvelope v1→v3 chained (HOUSE-09)', …)` block in `src/storage/storage.test.ts` alongside the existing v1→v2 and v2→v3 blocks; asserts v1 flat envelope → v3 envelope via `migrateEnvelope(env, 1)` with resonant slice populated losslessly, stretch slice seeded, version === 3, idempotent on re-migration), D-07 (commit prefix `test(36):` — the only test-prefixed commit in Phase 36)."
    - "D-06: A new `describe('migrateEnvelope v1→v3 chained (HOUSE-09)', …)` block exists in `src/storage/storage.test.ts`, appended after the existing v2→v3 block"
    - "D-06: The block exercises the full v1 → v3 chained ladder in a SINGLE `migrateEnvelope(env, 1)` call"
    - "D-06: The block asserts: `practices.resonant.{settings,stats}` populated losslessly from flat v1 fields; `practices.stretch.{settings,stats}` seeded with stretch settings from the resonant blob plus `ZERO_STATS_LITERAL`; `migrated.version === 3`; idempotency on re-migration"
    - "The block does NOT import `ZERO_STATS` from `src/storage/stats.ts` (circular-dep guard from `storage.ts:112-113`); it inlines the literal"
    - "D-06 wording resolution: The block does NOT assert naviKriya seeding directly on `migrateEnvelope` output — naviKriya defaults are supplied downstream by `coercePractices`, not by `migrateEnvelope` (per 36-PATTERNS §1 strategy (a))"
    - "`npm run test:run -- src/storage/storage.test.ts` exits 0 with the new block included"
    - "The full green-gate (`tsc -b && eslint . && vite build && vitest run`) exits 0"
  artifacts:
    - path: "src/storage/storage.test.ts"
      provides: "new describe block 'migrateEnvelope v1→v3 chained (HOUSE-09)' with ≥3 it() cases"
      min_lines: 40
      contains: "migrateEnvelope v1→v3 chained (HOUSE-09)"
  key_links:
    - from: "v1 flat envelope fixture"
      to: "migrateEnvelope(env, 1)"
      via: "single-call chained ladder"
      pattern: "migrateEnvelope\\(.*, 1\\)"
    - from: "test assertions"
      to: "v3 envelope shape (resonant + stretch + version=3)"
      via: "structural cast + expect.toEqual"
      pattern: "expect\\(.*practices.*\\)"
---

<objective>
Add the v1→v3 chained `migrateEnvelope` regression test (HOUSE-09): a returning
user with a v1 flat envelope migrates losslessly across both ladder steps
(v1→v2 then v2→v3) in a SINGLE `migrateEnvelope(env, 1)` call.

Purpose: Close HOUSE-09. Phase 36 success criterion #3 calls for a storage test
exercising the v1 → v3 chained ladder. This is the only `src/` change in
Phase 36 — every other plan touches `.planning/` docs only.

Resolves the CONTEXT D-06 / 36-PATTERNS wording mismatch: D-06 describes
"practices.navi seeded with defaults", but `migrateEnvelope` does NOT seed
naviKriya — naviKriya defaults are supplied downstream by `coercePractices` (see
`src/storage/storage.ts:88`). Per 36-PATTERNS §1 recommended strategy (a), the
test asserts ONLY what `migrateEnvelope` actually produces: resonant + stretch +
version=3, omitting naviKriya. This matches the existing v1→v2 block (line
154-167) which also does not assert naviKriya. The mismatch is recorded in the
SUMMARY so future readers see the resolution.

Output: One new `describe(…)` block appended to `src/storage/storage.test.ts`
(file tail, after line 298 — end of the existing v2→v3 block). Single commit
per CONTEXT D-05 commit #6, with `test(36):` prefix per D-07.
</objective>

<execution_context>
@$HOME/.claude-personal-personal/get-shit-done/workflows/execute-plan.md
@$HOME/.claude-personal-personal/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/36-housekeeping-bookkeeping-reset/36-CONTEXT.md
@.planning/phases/36-housekeeping-bookkeeping-reset/36-PATTERNS.md
@src/storage/storage.ts
@src/storage/storage.test.ts

<interfaces>
<!-- Key contracts from src/storage/storage.ts (lines 80-180). Use these — no further codebase exploration needed. -->

From src/storage/storage.ts:
- export function migrateEnvelope(env: Envelope, fromVersion: number): Envelope
  - When `fromVersion < 2`: sets `out.practices.resonant = { settings: out.settings, stats: out.stats }` and `out.activePractice = 'resonant'`. naviKriya is INTENTIONALLY OMITTED — coercePractices supplies defaults downstream.
  - When `fromVersion < 3`: adds `out.practices.stretch = { settings: <copy of resonant blob>, stats: ZERO_STATS_LITERAL }` to the existing practices. resonant slice untouched (orphan fields fine).
- export const STATE_VERSION = 3
- export const STATE_KEY = 'hrv:state:v1'

From src/storage/storage.test.ts (already imported at top of file):
- import { afterEach, beforeEach, describe, expect, it, vi } from 'vitest'
- import { readEnvelope, writeEnvelope, migrateEnvelope, STATE_KEY, STATE_VERSION } from './storage'

Existing analog blocks (DO NOT MODIFY — append new block AFTER line 298):
- Lines 143-218: `describe('migrateEnvelope v1→v2 (PRACTICE-04)')` — fixture shape source for V1_SETTINGS / V1_STATS
- Lines 220-298: `describe('migrateEnvelope v2→v3 (Phase 34 STRETCH-03)')` — fixture shape source for ZERO_STATS_LITERAL (lines 254-259)

Circular-dep guard (storage.ts:112-113): DO NOT import `ZERO_STATS` from `src/storage/stats.ts` — stats.ts imports from storage.ts, creating a circular dep. Use the inline literal.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Append v1→v3 chained describe block to storage.test.ts</name>
  <files>src/storage/storage.test.ts</files>
  <read_first>
    src/storage/storage.test.ts
    src/storage/storage.ts
    .planning/phases/36-housekeeping-bookkeeping-reset/36-PATTERNS.md
  </read_first>
  <behavior>
    The new `describe('migrateEnvelope v1→v3 chained (HOUSE-09)', …)` block must contain at least these three `it(…)` cases (per 36-PATTERNS §1 "Core assertion pattern"):

    1. `it('folds a v1 flat envelope all the way to v3 in one call', …)`:
       - Input: `migrateEnvelope({ version: 1, settings: V1_SETTINGS, stats: V1_STATS }, 1)`
       - Where `V1_SETTINGS = { bpm: 4, ratio: '40:60', durationMinutes: 10 }` and `V1_STATS = { totalSessions: 7, totalElapsedSeconds: 4200, lastSessionAtMs: 1_700_000_000_000, lastSessionDurationSeconds: 600 }`
       - Assert: `practices.resonant.settings` equals `V1_SETTINGS` (lossless v1→v2 fold)
       - Assert: `practices.resonant.stats` equals `V1_STATS` (lossless v1→v2 fold)
       - Assert: `migrated.activePractice === 'resonant'` (v1→v2 step)
       - Assert: `practices.stretch.settings` equals `V1_SETTINGS` (v2→v3 step: stretch settings carry from resonant blob)
       - Assert: `practices.stretch.stats` equals the inline `ZERO_STATS_LITERAL` `{ totalSessions: 0, totalElapsedSeconds: 0, lastSessionAtMs: null, lastSessionDurationSeconds: null }` (v2→v3 step)

    2. `it('is idempotent on re-migration (running v1→v3 twice yields the same envelope)', …)`:
       - Call `migrateEnvelope({ version: 1, settings: V1_SETTINGS, stats: V1_STATS }, 1)` twice
       - Assert: the two return values are `toEqual` (structural equality across the full envelope)

    3. `it('STATE_VERSION is 3 (ladder terminal)', …)`:
       - Assert: `STATE_VERSION === 3` (locks the test against silent ladder extension — if a future v3→v4 step ships, this assertion will fail and force the regression to be re-evaluated)

    Critical contract assertions per CONTEXT D-06, with the planner-resolved wording mismatch:
    - Resonant slice populated losslessly (D-06 item 1) — asserted in case 1
    - Stretch slice seeded with stretch settings from resonant blob + ZERO_STATS_LITERAL (D-06 item 2) — asserted in case 1
    - **naviKriya seeding is NOT asserted** (D-06 wording mismatch resolved per 36-PATTERNS strategy (a)): `migrateEnvelope` does not seed naviKriya; defaults are supplied downstream by `coercePractices`. The v1→v2 analog block at lines 154-167 also omits naviKriya assertions. The HOUSE-09 plan and SUMMARY both document this resolution so the wording mismatch in D-06 is closed in writing.
    - `migrated.version` is NOT asserted equal to 3 in the chained block (`migrateEnvelope` does not mutate `version`; the disk-version override happens upstream in `readEnvelope`). This matches the v1→v2 / v2→v3 analog blocks which also do not assert `migrated.version`. The terminal-version invariant is captured by case 3's `STATE_VERSION === 3` assertion instead.
  </behavior>
  <action>
    Append a new `describe('migrateEnvelope v1→v3 chained (HOUSE-09)', () => { … })` block AT THE END of `src/storage/storage.test.ts` (immediately after the closing `})` of the existing v2→v3 block on line 298). The new block declares its own scoped consts `V1_SETTINGS`, `V1_STATS`, `ZERO_STATS_LITERAL` using the exact values from the `<behavior>` section (block-scoped — no top-level addition; matches the v1→v2 and v2→v3 blocks). Cast pattern for `migrated.practices`: `const practices = migrated.practices as { resonant: { settings: unknown; stats: unknown }; stretch: { settings: unknown; stats: unknown } }` — copied verbatim from the v2→v3 block at lines 263-264, 270-271. Do NOT add new imports — `describe`, `it`, `expect`, `migrateEnvelope`, `STATE_VERSION` are all already in scope from lines 1-3. Do NOT import `ZERO_STATS` from `src/storage/stats.ts` — inline the literal per the circular-dep guard at `src/storage/storage.ts:112-113`. Do NOT modify the existing v1→v2 or v2→v3 blocks. Do NOT add a top-level helper function. The new block is fully self-contained, mirrors the analog structure, and the test file tail looks like: `… [v2→v3 block]\n})\n\ndescribe('migrateEnvelope v1→v3 chained (HOUSE-09)', () => { … })\n`. Implement to pass the three `it()` cases described in `<behavior>`. Run `npx vitest run src/storage/storage.test.ts` after writing — expect 3 new passing tests (and the prior test count + 3).

    **Verify scope hardening (per W4 checker feedback):** the `>= 3 it() cases` check is scoped to the new describe block via awk (extracting lines from the new `describe('migrateEnvelope v1→v3 chained (HOUSE-09)'` opener to the file's last `})`). The whole-file `grep -c 'it('` would be trivially satisfied by the >> 3 existing matches in the prior blocks.
  </action>
  <verify>
    <automated>npx vitest run src/storage/storage.test.ts && grep -q "migrateEnvelope v1→v3 chained (HOUSE-09)" src/storage/storage.test.ts && [ "$(awk '/describe\(.migrateEnvelope v1→v3 chained \(HOUSE-09\)./,EOF' src/storage/storage.test.ts | grep -c 'it(')" -ge "3" ] && [ "$(grep -c "ZERO_STATS" src/storage/storage.test.ts)" -ge "1" ]</automated>
  </verify>
  <acceptance_criteria>
    - `src/storage/storage.test.ts` contains the literal string `migrateEnvelope v1→v3 chained (HOUSE-09)` exactly once
    - The NEW describe block (extracted via awk from its opener to EOF) contains at least 3 `it(` cases (covering happy path, idempotency, terminal version) — scoped check, not whole-file
    - The new block declares its own scoped `V1_SETTINGS`, `V1_STATS`, `ZERO_STATS_LITERAL` consts
    - The block does NOT contain any new `import` statement (imports already exist at lines 1-3)
    - The block does NOT contain the string `from './stats'` or `from '@/storage/stats'` (no ZERO_STATS import — circular-dep guard)
    - The block does NOT assert `migrated.practices.naviKriya` directly (per the D-06 wording resolution)
    - `npx vitest run src/storage/storage.test.ts` exits 0 and reports all tests in the file passing
    - `npm run test:run` (full Vitest suite) exits 0
    - The existing v1→v2 and v2→v3 blocks (lines 143-218 and 220-298) are unchanged byte-for-byte
  </acceptance_criteria>
  <done>The new HOUSE-09 describe block is appended at the file tail with 3 passing it() cases (scoped check), full Vitest suite exits 0.</done>
</task>

<task type="auto">
  <name>Task 2: Run the full green-gate locally to confirm no regression</name>
  <files></files>
  <read_first>
    package.json
  </read_first>
  <action>
    Run the four green-gate commands sequentially (this is a pre-push sanity check; the full D-11 green-gate also runs once in plan 36-09 immediately before push). The commands map to package.json scripts: `npx tsc -b` (typecheck), `npm run lint` (ESLint), `npm run build` (Vite build — note `build` script is `tsc -b && vite build`), `npm run test:run` (Vitest single-run). All four must exit 0. If any fails: fix the issue in this plan via a follow-up edit on `src/storage/storage.test.ts` before staging — do NOT defer to a later plan, and do NOT use `--no-verify` or skip-hook flags. Per W1 fix, the verify relies on chained exit codes only (no fragile output greps).
  </action>
  <verify>
    <automated>npx tsc -b && npm run lint && npm run build && npm run test:run</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc -b` exits 0
    - `npm run lint` exits 0
    - `npm run build` exits 0
    - `npm run test:run` exits 0 with all tests reported passing (including the new HOUSE-09 block)
  </acceptance_criteria>
  <done>All four green-gate commands exit 0 with the HOUSE-09 test addition in the tree.</done>
</task>

<task type="auto">
  <name>Task 3: Stage and commit the test addition (D-05 commit #6)</name>
  <files>src/storage/storage.test.ts</files>
  <read_first>
    .planning/phases/36-housekeeping-bookkeeping-reset/36-CONTEXT.md
  </read_first>
  <action>
    Stage `src/storage/storage.test.ts` by explicit path (do NOT use `git add -A`). Commit with subject `test(36): add v1→v3 chained migrateEnvelope regression (HOUSE-09)` (use HEREDOC for the body). Body summarizes the three it() cases and explicitly records the D-06 wording resolution (naviKriya not asserted because `migrateEnvelope` doesn't seed it). Note `test(36):` prefix per CONTEXT D-07 — this is the only test-prefixed commit in Phase 36. Do NOT amend.
  </action>
  <verify>
    <automated>git log -1 --format=%s | grep -q "^test(36): add v1→v3 chained migrateEnvelope regression (HOUSE-09)$" && [ "$(git diff HEAD~1 --name-only)" = "src/storage/storage.test.ts" ]</automated>
  </verify>
  <acceptance_criteria>
    - HEAD commit subject matches `test(36): add v1→v3 chained migrateEnvelope regression (HOUSE-09)`
    - HEAD commit touches exactly one file: `src/storage/storage.test.ts`
    - HEAD commit is not an amend
    - The commit body explicitly records the D-06 wording resolution (naviKriya not seeded by migrateEnvelope)
  </acceptance_criteria>
  <done>Commit #6 lands cleanly with the only `src/` change in Phase 36; green-gate passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Test file only. No production code changes. The test exercises pure migration logic — no I/O, no network, no DOM. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36-06-01 | Tampering | Test fixture values | accept | The test uses literal-value fixtures (V1_SETTINGS / V1_STATS) — no external input, no parsing, no untrusted source. |
| T-36-06-02 | Denial of Service | Vitest runtime | accept | The new block adds 3 `it()` cases that exercise pure-function migration; each `it()` runs in <1ms (no async, no I/O). Negligible runtime impact. |
| T-36-06-03 | Spoofing | naviKriya seeding claim | mitigate | The plan explicitly resolves the D-06 wording mismatch in writing — naviKriya is NOT asserted (because `migrateEnvelope` doesn't seed it; `coercePractices` does, downstream). Future readers reading both D-06 and the test see the resolution recorded. |
</threat_model>

<verification>
- `npx vitest run src/storage/storage.test.ts` exits 0 (Task 1 + 2 verify)
- Full green-gate (`tsc && lint && build && test:run`) exits 0 (Task 2 verify; chained exit codes only)
- The new describe block is committed in a single `test(36):`-prefixed commit (Task 3 verify)
- The D-06 / PATTERNS wording mismatch is resolved in writing — both the plan and the eventual SUMMARY document strategy (a)
</verification>

<success_criteria>
- HOUSE-09 closed: `src/storage/storage.test.ts` carries the new `migrateEnvelope v1→v3 chained (HOUSE-09)` describe block
- The test asserts the contract per CONTEXT D-06 with the naviKriya wording mismatch explicitly resolved
- Green-gate exits 0 with the new block in place
- Phase 36 success criterion #3 partially closed (HOUSE-09 done; HOUSE-10 archive still pending in 36-07)
</success_criteria>

<output>
After completion, create `.planning/phases/36-housekeeping-bookkeeping-reset/36-06-SUMMARY.md` and explicitly record the D-06 → strategy (a) resolution in its body.
</output>
