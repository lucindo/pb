---
phase: 36
plan: 07
type: execute
wave: 7
depends_on: [36-06]
files_modified:
  - .planning/phases/30-multi-practice-architecture-switcher/
  - .planning/phases/31-navi-kriya-engine-session/
  - .planning/phases/32-learn-localization/
  - .planning/phases/33-close-gap-practice-02-resonant-settings-read-write-split-bra/
  - .planning/phases/34-stretch-as-a-distinct-practice/
  - .planning/phases/35-flute-cue-timbre-replace-chime/
  - .planning/milestones/v1.5-phases/30-multi-practice-architecture-switcher/
  - .planning/milestones/v1.5-phases/31-navi-kriya-engine-session/
  - .planning/milestones/v1.5-phases/32-learn-localization/
  - .planning/milestones/v1.5-phases/33-close-gap-practice-02-resonant-settings-read-write-split-bra/
  - .planning/milestones/v1.5-phases/34-stretch-as-a-distinct-practice/
  - .planning/milestones/v1.5-phases/35-flute-cue-timbre-replace-chime/
autonomous: true
requirements: [HOUSE-10]
tags: [housekeeping, archive, git-mv, v1.5]
must_haves:
  truths:
    - "Decision references: D-01 (this plan executes the ARCHIVE step of the single-batch restore→backfill→archive flow — closes the loop opened by 36-01's restore; working tree returns to clean here), D-02 (archive shape mirrors `.planning/milestones/v1.1-phases/` convention — full artifact carry-through), D-05 (this plan emits commit #7 of the 7-8 logical-group commits — `docs(36): re-archive v1.5 phase dirs to .planning/milestones/v1.5-phases/ (HOUSE-10)`), D-07 (commit prefix `docs(36):`)."
    - "D-01: All 6 v1.5 phase dirs have been moved from `.planning/phases/` to `.planning/milestones/v1.5-phases/` (archive step of the single-batch flow)"
    - "Git rename history is preserved (each dir shows as a rename, not delete+add)"
    - "D-02: Each archived dir carries the full artifact set: PLAN/SUMMARY per plan + phase-level VALIDATION/SECURITY/VERIFICATION/RESEARCH/REVIEW/UI-SPEC/PATTERNS/HUMAN-UAT/DISCUSSION-LOG/CONTEXT/.continue-here.md where present"
    - "D-01: The working tree is clean after the archive commit (zero ` D` or `??` entries for paths under `.planning/phases/3[0-5]-*`) — single-batch flow closes here"
    - "Phase 33's restored `33-VALIDATION.md` (from 36-02) and Phase 35's restored `35-VALIDATION.md` (from 36-02) and Phase 31's re-flipped `31-VERIFICATION.md` (from 36-03) and the four populated SUMMARYs from 36-04 all travel with the archive — the prior plans' edits are preserved in the moved dirs"
  artifacts:
    - path: ".planning/milestones/v1.5-phases/30-multi-practice-architecture-switcher/30-04-SUMMARY.md"
      provides: "Phase 30 archived"
    - path: ".planning/milestones/v1.5-phases/31-navi-kriya-engine-session/31-VERIFICATION.md"
      provides: "Phase 31 archived with passed status from 36-03"
      contains: "status: passed"
    - path: ".planning/milestones/v1.5-phases/32-learn-localization/32-03-SUMMARY.md"
      provides: "Phase 32 archived with requirements-completed from 36-04"
      contains: "requirements-completed:"
    - path: ".planning/milestones/v1.5-phases/33-close-gap-practice-02-resonant-settings-read-write-split-bra/33-VALIDATION.md"
      provides: "Phase 33 archived with VALIDATION from 36-02"
      contains: "status: verified"
    - path: ".planning/milestones/v1.5-phases/34-stretch-as-a-distinct-practice/34-11-SUMMARY.md"
      provides: "Phase 34 archived with requirements-completed from 36-04"
      contains: "requirements-completed:"
    - path: ".planning/milestones/v1.5-phases/35-flute-cue-timbre-replace-chime/35-VALIDATION.md"
      provides: "Phase 35 archived with VALIDATION from 36-02"
      contains: "status: verified"
  key_links:
    - from: ".planning/phases/3[0-5]-*/"
      to: ".planning/milestones/v1.5-phases/3[0-5]-*/"
      via: "git mv"
      pattern: "git mv .planning/phases/3[0-5]-\\* .planning/milestones/v1.5-phases/"
---

<objective>
Re-archive the six v1.5 phase directories to `.planning/milestones/v1.5-phases/`
in a single `git mv` operation. This is the canonical archive landing per CONTEXT
D-01 (single-batch flow: restore upfront → backfill in place → archive at end)
and D-02 (archive shape mirrors `v1.0/v1.0.1/v1.1-phases/` — full artifact
carry-through).

Purpose: Close HOUSE-10. Phase 36 success criterion #3 calls for the v1.5 phase
directories to be re-archived to `.planning/milestones/v1.5-phases/` with a
clean working tree.

Output: Six directory renames (git rename-tracked), one commit per CONTEXT D-05
commit #7. After this plan, the working tree is clean for all `.planning/phases/3[0-5]-*`
paths — the Phase 36 backfill is now archived alongside Phase 30-35.

**Status vocabulary note (revised per checker feedback):** This plan's verify
commands assert `status: verified` (not `status: passed`) on the VALIDATION.md
files that traveled with the archive move. This matches the corrected status
vocabulary set by plan 36-02 (canonical gsd-nyquist-auditor terminal status).
The Phase 31 `31-VERIFICATION.md` continues to carry `status: passed` because
VERIFICATION.md uses a different status vocabulary (`passed` / `human_needed`)
than VALIDATION.md (`draft` / `ready` / `verified` / `approved`) — verified by
grepping the in-tree files.
</objective>

<execution_context>
@$HOME/.claude-personal-personal/get-shit-done/workflows/execute-plan.md
@$HOME/.claude-personal-personal/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/36-housekeeping-bookkeeping-reset/36-CONTEXT.md
@.planning/phases/36-housekeeping-bookkeeping-reset/36-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create archive parent directory and move all six v1.5 dirs</name>
  <files>
    .planning/milestones/v1.5-phases/30-multi-practice-architecture-switcher/
    .planning/milestones/v1.5-phases/31-navi-kriya-engine-session/
    .planning/milestones/v1.5-phases/32-learn-localization/
    .planning/milestones/v1.5-phases/33-close-gap-practice-02-resonant-settings-read-write-split-bra/
    .planning/milestones/v1.5-phases/34-stretch-as-a-distinct-practice/
    .planning/milestones/v1.5-phases/35-flute-cue-timbre-replace-chime/
  </files>
  <read_first>
    .planning/phases/36-housekeeping-bookkeeping-reset/36-CONTEXT.md
    .planning/phases/36-housekeeping-bookkeeping-reset/36-PATTERNS.md
  </read_first>
  <action>
    Create the target parent: `mkdir -p .planning/milestones/v1.5-phases`. Then move each of the six restored v1.5 phase dirs in a single command:

    `git mv .planning/phases/30-multi-practice-architecture-switcher .planning/phases/31-navi-kriya-engine-session .planning/phases/32-learn-localization .planning/phases/33-close-gap-practice-02-resonant-settings-read-write-split-bra .planning/phases/34-stretch-as-a-distinct-practice .planning/phases/35-flute-cue-timbre-replace-chime .planning/milestones/v1.5-phases/`

    `git mv` preserves rename history — `git log --follow` on any moved file will still trace through the move. After the move, `git status --porcelain` shows six rename entries (`R` lines pairing each source path to its new archive path). Run `ls .planning/phases/ | grep '^3[0-5]-'` and confirm zero output (the source dirs are gone from the working tree). Run `ls .planning/milestones/v1.5-phases/ | grep '^3[0-5]-' | wc -l` and confirm `6`. Phase 28 SUMMARYs (created in 36-05) STAY at `.planning/phases/28-phone-install-banner/` — they are NOT included in this move (CONTEXT specifics line 141 + 36-PATTERNS §16-17 placement option-a: v1.4 archive backfill stays deferred).
  </action>
  <verify>
    <automated>[ "$(ls .planning/phases/ 2>/dev/null | grep -c '^3[0-5]-')" = "0" ] && [ "$(ls .planning/milestones/v1.5-phases/ 2>/dev/null | grep -c '^3[0-5]-')" = "6" ] && [ -f .planning/milestones/v1.5-phases/31-navi-kriya-engine-session/31-VERIFICATION.md ] && grep -q "^status: passed$" .planning/milestones/v1.5-phases/31-navi-kriya-engine-session/31-VERIFICATION.md && grep -q "^status: verified$" .planning/milestones/v1.5-phases/33-close-gap-practice-02-resonant-settings-read-write-split-bra/33-VALIDATION.md && grep -q "^status: verified$" .planning/milestones/v1.5-phases/35-flute-cue-timbre-replace-chime/35-VALIDATION.md</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/` contains zero entries matching `^3[0-5]-` (all six dirs moved out)
    - `.planning/milestones/v1.5-phases/` contains exactly six dirs matching `^3[0-5]-` (all six dirs moved in)
    - `git status --porcelain` shows six `R` (rename) entries pairing each phase source path to its archive destination
    - Phase 31's archived `31-VERIFICATION.md` still carries `status: passed` (VERIFICATION vocabulary; the 36-03 edit traveled with the move)
    - Phase 33's archived `33-VALIDATION.md` still carries `status: verified` (VALIDATION vocabulary; the 36-02 backfill traveled)
    - Phase 35's archived `35-VALIDATION.md` still carries `status: verified` (VALIDATION vocabulary; the 36-02 backfill traveled)
    - Phase 32/33/34/35 archived SUMMARYs still carry `requirements-completed:` (the 36-04 edits traveled)
    - `.planning/phases/28-phone-install-banner/` is UNCHANGED (not part of the move)
  </acceptance_criteria>
  <done>All six v1.5 phase dirs are moved to the archive via `git mv`; backfilled artifacts from prior plans (36-02/03/04) travel with the move.</done>
</task>

<task type="auto">
  <name>Task 2: Stage and commit the archive move (D-05 commit #7)</name>
  <files>
    .planning/milestones/v1.5-phases/
    .planning/phases/
  </files>
  <read_first>
    .planning/phases/36-housekeeping-bookkeeping-reset/36-CONTEXT.md
  </read_first>
  <action>
    `git mv` already stages the rename entries. Verify by running `git status --porcelain | grep -c '^R'` — should be at least 6 (one rename entry per top-level moved dir, though git may report per-file renames depending on rename-detection settings; either form is acceptable). Commit with subject `docs(36): re-archive v1.5 phase dirs to .planning/milestones/v1.5-phases/ (HOUSE-10)` (use HEREDOC for the body). Body lists the six phases and references the v1.0.1 / v1.1 archives as the precedent shape (CONTEXT D-02). Do NOT amend. Do NOT use `git add -A` — the `git mv` already staged the right paths. Confirm `git status --porcelain` after commit shows clean for all `.planning/phases/3[0-5]-*` paths and all `.planning/milestones/v1.5-phases/` paths.
  </action>
  <verify>
    <automated>git log -1 --format=%s | grep -q "HOUSE-10" && [ -z "$(git status --porcelain | grep -E '^( D|R |\\?\\?).+(\\.planning/phases/3[0-5]-|\\.planning/milestones/v1\\.5-phases/3[0-5]-)')" ]</automated>
  </verify>
  <acceptance_criteria>
    - HEAD commit subject matches `docs(36): re-archive v1.5 phase dirs to .planning/milestones/v1.5-phases/ (HOUSE-10)`
    - HEAD commit shows rename detection on the six top-level dirs (verifiable via `git show --stat HEAD` or `git log --diff-filter=R HEAD~1..HEAD`)
    - HEAD commit is not an amend
    - `git status --porcelain` shows zero entries for paths under `.planning/phases/3[0-5]-*` or `.planning/milestones/v1.5-phases/`
    - Working tree is clean for the v1.5 archive set
  </acceptance_criteria>
  <done>Commit #7 lands cleanly with the six dir renames; rename history is preserved; working tree is clean for the archived set.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | `.planning/` docs only. `git mv` is a working-tree + index operation; no code, no I/O outside git. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36-07-01 | Tampering | Archive contents during move | accept | `git mv` is byte-preserving — files are identical at source and destination. Task 1 verify confirms the prior plans' edits (status: verified / passed depending on artifact type, requirements-completed) traveled with the move. |
| T-36-07-02 | Information Disclosure | Rename history | accept | The new archive paths are public planning artifacts; rename history is the audit trail, not a leak. |
| T-36-07-03 | Repudiation | Move vs delete+add | mitigate | Verified the operation uses `git mv` (not `mv` + `git add`), which preserves rename detection in `git log --follow` — future operators can trace any file's history through the archive landing. |
</threat_model>

<verification>
- All six v1.5 dirs land at `.planning/milestones/v1.5-phases/` (Task 1 verify)
- Source paths under `.planning/phases/3[0-5]-*` are empty (Task 1 verify)
- Backfilled artifacts (31 VERIFICATION status: passed, 33/35 VALIDATION status: verified, 32/33/34/35 requirements-completed) all traveled with the move (Task 1 verify)
- Working tree is clean for the affected paths after commit #7 (Task 2 verify)
- Single commit lands cleanly with rename tracking
</verification>

<success_criteria>
- HOUSE-10 closed: six v1.5 dirs re-archived to `.planning/milestones/v1.5-phases/`; rename history preserved; working tree clean
- Phase 36 success criterion #3 fully closed (HOUSE-09 and HOUSE-10 both done)
- Phase 28's restored SUMMARYs from 36-05 stay at `.planning/phases/28-phone-install-banner/` — v1.4 archive backfill remains deferred
</success_criteria>

<output>
After completion, create `.planning/phases/36-housekeeping-bookkeeping-reset/36-07-SUMMARY.md`.
</output>
