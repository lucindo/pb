---
phase: 45-ring-progress-cue-toggle
plan: 1
type: execute
wave: 1
depends_on: []
files_modified:
  - src/featureFlags.ts
  - src/featureFlags.test.ts
autonomous: true
# Phase 45 has Requirements: TBD in ROADMAP. The must_haves below fully cover
# the ROADMAP "Done means" criteria for the flag layer (parse + default).
requirements: []

must_haves:
  truths:
    - "`readFeatureFlags('')` returns `ringCue: 'outer-inner'` (production default unchanged)."
    - "`readFeatureFlags('?ringCue=progress-arc')` returns `ringCue: 'progress-arc'`."
    - "Aliases `progress` / `arc` / `south` (case-insensitive, whitespace-trimmed) all resolve to `'progress-arc'`."
    - "Aliases `outer-inner` / `production` / `rings` / `default` (case-insensitive, whitespace-trimmed) all resolve to `'outer-inner'`."
    - "Invalid `ringCue` values fall back to `'outer-inner'` (default-on-junk, same shape as `breathingShape` / `orbIdle`)."
  artifacts:
    - path: "src/featureFlags.ts"
      provides: "RingCueStyle type + RING_CUE_FLAG spec + ringCue field on FeatureFlags + readFeatureFlags wiring"
      contains: "RingCueStyle"
    - path: "src/featureFlags.ts"
      provides: "RING_CUE_FLAG flag constant"
      contains: "RING_CUE_FLAG"
    - path: "src/featureFlags.test.ts"
      provides: "Coverage matching the breathingShape / orbIdle test shape for the new ringCue flag"
      contains: "ringCue"
  key_links:
    - from: "src/featureFlags.ts"
      to: "FeatureFlags consumers (useFeatureFlags, useAppViewModel)"
      via: "type extension — ringCue: RingCueStyle"
      pattern: "ringCue: RingCueStyle"
    - from: "src/featureFlags.ts::readFeatureFlags"
      to: "RING_CUE_FLAG"
      via: "readQueryFeatureFlag(search, RING_CUE_FLAG)"
      pattern: "readQueryFeatureFlag\\(search, RING_CUE_FLAG\\)"
---

<objective>
Add the `RingCueStyle` type, the `RING_CUE_FLAG` query-string spec, and a fourth `ringCue` field on `FeatureFlags`. This is the pure feature-flag layer for Phase 45 — no rendering changes, no plumbing, no consumer wiring. The flag plumbs automatically through `useFeatureFlags` → `useAppViewModel` once the type is extended.

Purpose: Mirror the existing `BREATHING_SHAPE_FLAG` / `ORB_IDLE_FLAG` pattern so the ring-cue toggle is read from `?ringCue=` per-tab, same as the other two dev-only visual flags.

Output: `src/featureFlags.ts` gains 4 sibling lines (type, flag constant, FeatureFlags field, readFeatureFlags wiring); `src/featureFlags.test.ts` gains a `ringCue`-shaped block mirroring the existing `breathingShape` coverage.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/45-ring-progress-cue-toggle/45-PATTERNS.md
@.planning/spikes/011-ring-progress-cue/README.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from src/featureFlags.ts. -->
<!-- Executor should read the whole file (90 lines) and mirror BREATHING_SHAPE_FLAG 1:1. -->

From src/featureFlags.ts (current state, the analog):

```typescript
export interface QueryFeatureFlagSpec<T> {
  queryParam: string
  defaultValue: T
  parse(this: void, rawValue: string): T | null
}

export type BreathingShapeVariant = 'orb-halo' | 'minimal-rings'
export type OrbIdleBehavior = 'still' | 'ambient'

export interface FeatureFlags {
  switcherIcon: boolean
  breathingShape: BreathingShapeVariant
  orbIdle: OrbIdleBehavior
}

// The exact template to mirror for RING_CUE_FLAG:
const BREATHING_SHAPE_FLAG = {
  queryParam: 'breathingShape',
  defaultValue: 'orb-halo' as BreathingShapeVariant,
  parse(rawValue: string): BreathingShapeVariant | null {
    const v = rawValue.trim().toLowerCase()
    if (v === 'orb-halo' || v === 'orb' || v === 'halo') return 'orb-halo'
    if (v === 'minimal-rings' || v === 'minimal' || v === 'rings') return 'minimal-rings'
    return null
  },
} satisfies QueryFeatureFlagSpec<BreathingShapeVariant>

export function readFeatureFlags(search: string): FeatureFlags {
  return {
    switcherIcon: readQueryFeatureFlag(search, SWITCHER_ICON_FLAG),
    breathingShape: readQueryFeatureFlag(search, BREATHING_SHAPE_FLAG),
    orbIdle: readQueryFeatureFlag(search, ORB_IDLE_FLAG),
  }
}
```

Cross-flag alias note (ROADMAP line 240): `rings` already appears as an alias of `minimal-rings` on `breathingShape` line 67. It also appears in the new `outer-inner` alias list on `ringCue`. This is intentional — the two flags live in independent query params (`?breathingShape=` vs `?ringCue=`) and never share a parser. No deduplication needed.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add RingCueStyle + RING_CUE_FLAG + readFeatureFlags wiring</name>
  <files>src/featureFlags.ts</files>
  <read_first>
    - src/featureFlags.ts (whole file, ~90 lines — the executor mirrors BREATHING_SHAPE_FLAG 1:1)
    - .planning/phases/45-ring-progress-cue-toggle/45-PATTERNS.md §1 (the exact alias mapping + line-number anchors in the source file)
    - .planning/ROADMAP.md lines 222–267 (Phase 45 Details — locked alias list)
  </read_first>
  <behavior>
    - Type addition: `export type RingCueStyle = 'outer-inner' | 'progress-arc'` (sibling of `BreathingShapeVariant` / `OrbIdleBehavior`).
    - Interface extension: `FeatureFlags` gains a fourth field `ringCue: RingCueStyle`.
    - Flag constant `RING_CUE_FLAG` with `queryParam: 'ringCue'`, `defaultValue: 'outer-inner'`, `parse` accepting (case-insensitive, trim-whitespace):
      - `outer-inner` / `production` / `rings` / `default` → `'outer-inner'`
      - `progress-arc` / `progress` / `arc` / `south` → `'progress-arc'`
      - anything else → `null` (caller falls back to default via `readQueryFeatureFlag`)
    - `readFeatureFlags` wiring: a fourth line `ringCue: readQueryFeatureFlag(search, RING_CUE_FLAG)`.
    - No other changes — `SWITCHER_ICON_FLAG`, `BREATHING_SHAPE_FLAG`, `ORB_IDLE_FLAG`, helper functions stay byte-identical.
  </behavior>
  <action>
    Mirror `BREATHING_SHAPE_FLAG` (current lines 62–71) verbatim in structure for the new `RING_CUE_FLAG`. Place the new type next to `BreathingShapeVariant` / `OrbIdleBehavior` (lines 7–8). Place `RING_CUE_FLAG` between `ORB_IDLE_FLAG` and `readFeatureFlags` (after current line 82). Add the new field as the **last** entry on the `FeatureFlags` interface (after `orbIdle`) and as the **last** field in the `readFeatureFlags` return object (after `orbIdle`). Use `satisfies QueryFeatureFlagSpec<RingCueStyle>` exactly as the analogs use `satisfies QueryFeatureFlagSpec<...>`. Trim + lowercase inside `parse` (`const v = rawValue.trim().toLowerCase()`) before alias comparisons — matches the analog idiom.
  </action>
  <verify>
    <automated>npm test -- --run src/featureFlags.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "export type RingCueStyle = 'outer-inner' | 'progress-arc'" src/featureFlags.ts` exits 0
    - `grep -q "ringCue: RingCueStyle" src/featureFlags.ts` exits 0 (the new interface field)
    - `grep -q "RING_CUE_FLAG" src/featureFlags.ts` matches at least 3 lines (declaration + readFeatureFlags wire-in + satisfies)
    - `grep -q "queryParam: 'ringCue'" src/featureFlags.ts` exits 0
    - `grep -q "ringCue: readQueryFeatureFlag(search, RING_CUE_FLAG)" src/featureFlags.ts` exits 0
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>RingCueStyle is exported, FeatureFlags has 4 fields, RING_CUE_FLAG mirrors BREATHING_SHAPE_FLAG structure 1:1 with the spec'd aliases, readFeatureFlags returns the 4-field object. No other code in featureFlags.ts is altered.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend featureFlags.test.ts with ringCue coverage</name>
  <files>src/featureFlags.test.ts</files>
  <read_first>
    - src/featureFlags.test.ts (whole file, ~117 lines — the executor extends the existing patterns)
    - src/featureFlags.ts (post-Task-1 state — to know the exact type/flag names to import)
    - .planning/phases/45-ring-progress-cue-toggle/45-PATTERNS.md §2 (the exact analog blocks: orbIdle = lines 85–101, breathingShape = lines 60–83)
  </read_first>
  <behavior>
    - Update the default-shape assertion (current lines 36–42 — the `it('returns defaults for empty search', ...)` test) to include `ringCue: 'outer-inner'` in the expected object alongside the three existing defaults. Do not remove/rename the existing default checks.
    - Add a new block of `it(...)` cases mirroring the `breathingShape` block (lines 60–83) and the `orbIdle` block (lines 85–101) for `ringCue`:
      - default: `readFeatureFlags('').ringCue === 'outer-inner'`
      - parses `'progress-arc'` and its aliases `'progress'` / `'arc'` / `'south'`
      - parses `'outer-inner'` and its aliases `'production'` / `'rings'` / `'default'`
      - case-insensitive + whitespace-trim: `'?ringCue=PROGRESS-ARC'`, `'?ringCue=%20Arc%20'`
      - invalid → falls back to `'outer-inner'` (e.g. `'?ringCue=junk'`)
  </behavior>
  <action>
    Mirror the structure of the `breathingShape` `it(...)` block (current lines 60–83 of `src/featureFlags.test.ts`) for `ringCue`. Place the new block immediately after the `orbIdle` block (after current line 101) and before the `describe('readQueryFeatureFlag', ...)` block. Update the default-shape `expect(...).toEqual(...)` (current lines 36–42) to include `ringCue: 'outer-inner'`. Use `readFeatureFlags(...)` (already imported on line 5) — no new imports required.
  </action>
  <verify>
    <automated>npm test -- --run src/featureFlags.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "ringCue" src/featureFlags.test.ts` returns >= 8 (default + parse + aliases + case-insensitive + invalid + the updated default-shape object — at least one occurrence per assertion)
    - `npm test -- --run src/featureFlags.test.ts` exits 0
    - Test output shows >= 5 new `ringCue`-named `it(...)` cases pass (default, progress-arc aliases, outer-inner aliases, case-insensitive, invalid fallback)
    - The existing `breathingShape` / `orbIdle` / `switcherIcon` / `parseQueryBoolean` test counts are unchanged (no removed/renamed cases)
  </acceptance_criteria>
  <done>featureFlags.test.ts has a ringCue block parallel to the breathingShape block, the default-shape assertion includes the fourth field, and all tests in the file pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| URL → app | The browser URL bar / link is the untrusted input source. `?ringCue=<value>` enters the parser via `new URLSearchParams(search).get(...)` inside `readQueryFeatureFlag` (existing helper). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-45-01 | Tampering | RING_CUE_FLAG.parse() | accept | Input is a single dev-only enum. `parse` returns `null` for any non-alias input; `readQueryFeatureFlag` then falls back to `defaultValue` (`'outer-inner'`). No XSS surface — the parsed value flows into a TypeScript-typed string union that drives a render branch, never DOM-injected as raw text or HTML. Same shape as the existing `BREATHING_SHAPE_FLAG` and `ORB_IDLE_FLAG`, both audited under Phase 41 J5/J6. |
| T-45-02 | Information Disclosure | dev-only query flag | accept | The flag is not user-discoverable in the UI (no picker, no documentation in user-facing copy). It is a developer affordance per the ROADMAP "Out of scope" line ("Surfacing `ringCue` in user-facing App Settings — dev toggle only"). No PII, no auth state, no session keys — only a render-path selector. |

**Notes:** No new network calls, no new storage, no new env vars, no new dependencies. The only attack surface added is a fourth case in a URL-parameter parser that already handles three peers under the same pattern; the new case adds no novel input shape. **Package legitimacy gate:** N/A — no `npm install` in this plan.
</threat_model>

<verification>
- `npm test -- --run src/featureFlags.test.ts` exits 0 (full file green, including the new ringCue cases)
- `npm test` exits 0 (whole suite green — no regression in downstream consumers that import `FeatureFlags`)
- `npx tsc --noEmit` exits 0 (FeatureFlags interface change does not break any consumer)
- `npm run lint` (if part of the green gate per Phase 40 carry-forward note) shows no new errors in `src/featureFlags.ts` or `src/featureFlags.test.ts` vs pre-Plan baseline
</verification>

<success_criteria>
- `readFeatureFlags('?ringCue=progress-arc')` returns `{ ..., ringCue: 'progress-arc' }`.
- `readFeatureFlags('')` returns `{ ..., ringCue: 'outer-inner' }`.
- All aliases parse case-insensitively with whitespace trimming.
- Invalid `?ringCue=` values fall back to `'outer-inner'`.
- Type extension flows automatically: `vm.featureFlags.ringCue` is typed as `RingCueStyle` everywhere downstream (no edit to `useFeatureFlags.ts` or `appViewModel.ts` required).
- No regression in `breathingShape` / `orbIdle` / `switcherIcon` / `parseQueryBoolean` behavior.
</success_criteria>

<output>
Create `.planning/phases/45-ring-progress-cue-toggle/45-01-SUMMARY.md` when done.
</output>
