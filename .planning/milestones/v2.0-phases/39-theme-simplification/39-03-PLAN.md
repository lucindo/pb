---
phase: 39-theme-simplification
plan: 03
type: execute
wave: 2
depends_on:
  - 39-01
files_modified:
  - src/content/strings.ts
  - src/content/strings.test.ts
  - src/components/ThemePicker.tsx
  - src/components/ThemePicker.test.tsx
  - src/hooks/useTheme.test.ts
  - src/hooks/useThemeChoice.test.ts
  - src/hooks/useFavicon.test.ts
  - src/app/App.session.test.tsx
autonomous: true
requirements:
  - THM-04
requirements_addressed:
  - THM-04
must_haves:
  truths:
    - "src/content/strings.ts UiStrings interface `themes` block contains exactly 3 readonly fields: `light`, `dark`, `system` — no `moss`, `slate`, `dusk` readonly fields (D-07)."
    - "src/content/strings.ts EN catalog `themes` block contains exactly 3 entries: `light: 'Light'`, `dark: 'Dark'`, `system: 'System'`."
    - "src/content/strings.ts PT-BR catalog `themes` block contains exactly 3 entries: `light: 'Claro'`, `dark: 'Escuro'`, `system: 'Sistema'` — no `Musgo`, `Ardósia`, `Crepúsculo` (D-07)."
    - "src/content/strings.test.ts contains no `moss`, `slate`, `dusk`, `Moss`, `Slate`, `Dusk`, `Musgo`, `Ardósia`, `Crepúsculo` literal references; LOCKED_COPY byte-equality guard (Phase 19) is intact (palette display strings are not in LOCKED_COPY)."
    - "src/components/ThemePicker.tsx renders 3 options at runtime (auto-shrink via `THEME_OPTIONS.map(...)` consumer); the L8 comment is updated from enumerating 6 themes to enumerating 3 themes per Tiger Style WHY-only (planner discretion if no such comment exists)."
    - "src/components/ThemePicker.test.tsx asserts the picker renders exactly 3 radio buttons with labels in order `['Light', 'Dark', 'System']`; the moss/slate/dusk-specific click/aria-checked test cases are ROTATED to surviving themes (per PATTERNS §'rotation strategy'), not deleted wholesale — the behavioral coverage of the click/select/disabled invariants is preserved against surviving theme IDs (THM-04 render half). The L87 it() description string updates from 'all 6 buttons' to 'all 3 buttons' to match the post-shrink option count."
    - "src/hooks/useTheme.test.ts has all `seedPrefs('moss'|'dusk')` fixture seeds rotated to surviving themes (`'dark'` or `'light'`); cross-tab change assertions retarget to `'dark'` ↔ `'light'`; no deprecated theme literal remains in the file (D-12 surgical strip / Phase 38 rotation pattern)."
    - "src/hooks/useThemeChoice.test.ts has all 16 deprecated-theme hits rotated to surviving themes — both `seedPrefs(..., theme: 'moss')` calls AND `setTheme('dusk'|'slate')` calls AND the 3 it() description strings at L37/L48/L63 that embed the rotated setTheme target name; behavioral coverage preserved (D-12)."
    - "src/hooks/useFavicon.test.ts has ALL 8 deprecated-theme hits rotated to surviving themes — including the L80 it() description string, the L81 `seedPrefs('moss')` call, the L84 `FAVICON_COLORS.moss` property access, the L134 cross-tab envelope `theme: 'moss'`, the L150 `FAVICON_COLORS.moss` assertion, the L181 `seedPrefs('dusk')`, the L187 CustomEvent `value: 'dusk'`, and the L191 `FAVICON_COLORS.dusk` assertion. `FAVICON_COLORS.moss` → `FAVICON_COLORS.dark` and `FAVICON_COLORS.dusk` → `FAVICON_COLORS.light` property-access rewrites preserve the surviving-named-theme intent (D-12)."
    - "src/app/App.session.test.tsx — if a literal `'moss'`/`'slate'`/`'dusk'` reference exists (per CONTEXT — grep verifies at plan time), it is rotated to a surviving theme; if grep returns zero matches the file is zero-edit (D-12)."
    - "After this plan's commits land on top of Plan 01: `pnpm tsc --noEmit` exits 0 (no UiStrings consumer references the deleted fields AND no test file references `FAVICON_COLORS.moss|slate|dusk` which were deleted by Plan 02); `pnpm test src/components/ThemePicker.test.tsx --run`, `pnpm test src/content/strings.test.ts --run`, `pnpm test src/hooks --run`, `pnpm test src/app/App.session.test.tsx --run` all exit 0."
    - "D-07 satisfied: clean cut — themes.moss/slate/dusk deleted from UiStrings type + EN + PT-BR catalogs; no orphan strings, no review-marker debt."
    - "D-11 satisfied: delete-with-component test policy (Phase 37 D-06 / Phase 38 D-09 precedent) — `ThemePicker.test.tsx`, `useTheme.test.ts`, `useThemeChoice.test.ts`, `useFavicon.test.ts` are modified in the same plan/commits as their source-file collapse counterparts; the policy spans the phase: `prefs.test.ts` co-located with `settings.ts` in Plan 01; `faviconPalette.test.ts` + `favicon.sync.test.ts` + `theme.contrast.test.ts` co-located with their CSS/source collapse in Plan 02; this plan covers the remaining 4 hook/component tests."
    - "D-12 satisfied: surgical strip of cross-cutting tests rotates deprecated-theme fixtures to surviving themes (mirrors Phase 38 D-10 / Phase 37 D-07 precedent)."
    - "THM-04 closed end-to-end: type-level (Plan 01 narrowed THEME_OPTIONS to 3) + render-level (ThemePicker.tsx auto-shrinks; ThemePicker.test.tsx asserts 3 options) + display-strings-level (strings.ts catalogs contain 3 entries)."
  artifacts:
    - path: src/content/strings.ts
      provides: "UiStrings interface with `themes: { light, dark, system }` (3 readonly fields); EN catalog with `themes: { light: 'Light', dark: 'Dark', system: 'System' }`; PT-BR catalog with `themes: { light: 'Claro', dark: 'Escuro', system: 'Sistema' }`."
      forbids: "moss|slate|dusk|Moss|Slate|Dusk|Musgo|Ardósia|Crepúsculo"
    - path: src/components/ThemePicker.test.tsx
      provides: "Test suite asserting picker renders 3 radio buttons with labels ['Light', 'Dark', 'System'] in order; behavioral cases (click writes / disabled blocks / aria-checked tracks state) rotated to surviving theme IDs; all `all N buttons|options` description strings refer to N=3."
      forbids: "moss|slate|dusk|Moss|Slate|Dusk"
    - path: src/hooks/useTheme.test.ts
      provides: "Hook test with all `seedPrefs('moss'|'dusk')` rotated to `'dark'`/`'light'`; cross-tab change tests use surviving theme IDs."
      forbids: "moss|slate|dusk"
    - path: src/hooks/useThemeChoice.test.ts
      provides: "Hook test with all `setTheme('dusk'|'slate')` and `seedPrefs(..., theme: 'moss')` rotated to surviving themes; it() description strings at L37/L48/L63 rotated to match the chosen setTheme target; behavioral coverage preserved."
      forbids: "moss|slate|dusk"
    - path: src/hooks/useFavicon.test.ts
      provides: "Hook test with all 8 deprecated-theme hits rotated — `seedPrefs('moss'|'dusk')`, `theme: 'moss'` envelope literal, `value: 'dusk'` CustomEvent payload, AND every `FAVICON_COLORS.moss|.dusk` property-access rewritten to `FAVICON_COLORS.dark|.light` (Plan 02 deletes these keys so the rewrites are tsc-mandatory at this plan's commit boundary); it() description at L80 also rotated."
      forbids: "moss|slate|dusk|FAVICON_COLORS\\.(moss|slate|dusk)"
  key_links:
    - from: src/content/strings.ts
      to: src/components/SettingsDialog.tsx
      via: "the Pick<UiStrings, 'settings' | 'themes' | 'cue' | 'timbres' | 'install'> type narrowing — the 'themes' key still exists in the Pick union; only the INTERIOR of the themes block shrinks from 6 to 3 fields"
      pattern: "Pick<UiStrings,\\s*'settings'\\s*\\|\\s*'themes'"
    - from: src/components/ThemePicker.tsx
      to: src/domain/settings.ts
      via: "THEME_OPTIONS.map(id => ...) consumer — after Plan 01 narrowed THEME_OPTIONS to 3 entries, the picker renders 3 buttons automatically; no JSX edit needed"
      pattern: "THEME_OPTIONS\\.map"
    - from: src/hooks/useFavicon.test.ts
      to: src/styles/faviconPalette.ts
      via: "FAVICON_COLORS.{light|dark|moss|slate|dusk} property access — Plan 02 deletes the moss/slate/dusk keys; Plan 03's rotations rewrite every `.moss` → `.dark` and every `.dusk` → `.light` property access so tsc remains green at this plan's commit boundary"
      pattern: "FAVICON_COLORS\\."
---

<objective>
Strip the `themes.moss`, `themes.slate`, `themes.dusk` fields from `src/content/strings.ts` — the `UiStrings.themes` type block AND both the EN catalog (`Moss`, `Slate`, `Dusk`) AND the PT-BR catalog (`Musgo`, `Ardósia`, `Crepúsculo`). Update `src/components/ThemePicker.test.tsx` to assert the picker renders 3 options with labels `['Light', 'Dark', 'System']`, rotate moss/slate/dusk-specific behavioral cases to surviving theme IDs (per PATTERNS §"rotation strategy" — preserves coverage of click/aria-checked/disabled invariants without losing test cases), AND update the L87 it() description from `'all 6 buttons'` to `'all 3 buttons'`. Apply surgical-strip rotations to `src/hooks/useTheme.test.ts`, `src/hooks/useThemeChoice.test.ts`, `src/hooks/useFavicon.test.ts`, and `src/app/App.session.test.tsx` per CONTEXT D-12. Update `src/content/strings.test.ts` if any iteration over themes-keys needs updating (likely zero-edit per PATTERNS). Optional comment-debt update on `src/components/ThemePicker.tsx` L8 (enumerated themes in the leading comment).

Purpose: collapse the i18n surface and align all cross-cutting tests to the surviving 3-option theme set. After this plan, no production code, no test code, and no display string references any of the 3 deprecated palette names. Closes THM-04 (render-level half — Plan 01 closed the type-level THEME_OPTIONS half; this plan closes the display-strings + ThemePicker render-verification half).

Per CONTEXT D-07: clean cut — delete the 3 keys from UiStrings type + EN + PT-BR catalogs. No `[review-needed]` markers (Phase 26 I18N-07 precedent), no orphan strings (Phase 38 D-08 precedent).

Per CONTEXT D-12 + PATTERNS §"Surgical strip with rotation": for tests that exercise behavior (`setTheme → savePrefs → loadPrefs → assert`), rotate the deprecated theme ID to a surviving one — preserves coverage without losing test cases. This differs from Phase 38 where the subject (variant axis) was deleted wholesale — Phase 39 keeps the subject (theme axis), only deletes specific IDs.

**CRITICAL — Plan 02 + Plan 03 compile-boundary coupling:** Plan 02 deletes the `moss`, `slate`, `dusk` keys from `FAVICON_COLORS` in `src/styles/faviconPalette.ts`. After Plan 02 lands, ANY `FAVICON_COLORS.moss|.slate|.dusk` property access becomes a TS-2339 compile error. `src/hooks/useFavicon.test.ts` currently contains 3 such accesses (L84, L150, L191). Plan 03 MUST rewrite those property accesses in the same wave (and ideally same commit chain) so tsc stays green. This plan's Edit C explicitly enumerates each of the 8 deprecated-theme hits in useFavicon.test.ts with concrete `.moss → .dark` and `.dusk → .light` rewrites.

Output: 8 files edited. strings.ts loses 3 type fields + 6 catalog entries (3 EN + 3 PT-BR). strings.test.ts is likely zero-edit. ThemePicker.tsx is zero-edit or comment-debt-only. ThemePicker.test.tsx has ~6 cases edited (option-list assertion + L87 description string + 4 rotated cases). useTheme.test.ts has ~5 lines edited (seedPrefs / assertions / cross-tab event). useThemeChoice.test.ts has 16 lines edited (13 fixture/setter/assertion lines + 3 it() description rotations at L37/L48/L63). useFavicon.test.ts has 8 lines edited (per the explicit enumeration in Edit C). App.session.test.tsx is zero-edit (no current moss/slate/dusk grep hit per PATTERNS) — but verified at plan time.
</objective>

<execution_context>
@$HOME/.claude-personal-personal/get-shit-done/workflows/execute-plan.md
@$HOME/.claude-personal-personal/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/39-theme-simplification/39-CONTEXT.md
@.planning/phases/39-theme-simplification/39-PATTERNS.md
@.planning/phases/39-theme-simplification/39-01-SUMMARY.md
@src/content/strings.ts
@src/content/strings.test.ts
@src/components/ThemePicker.tsx
@src/components/ThemePicker.test.tsx
@src/hooks/useTheme.test.ts
@src/hooks/useThemeChoice.test.ts
@src/hooks/useFavicon.test.ts
@src/app/App.session.test.tsx

<interfaces>
<!-- Concrete excerpts from each file the executor will touch. Sourced from 39-PATTERNS.md and verified at plan-revision time via grep. -->

From src/content/strings.ts:

UiStrings interface (~L35-42, the `themes` block inside the interface):
```typescript
// Before:
readonly themes: {
  readonly light: string
  readonly dark: string
  readonly system: string
  readonly moss: string     // DELETE
  readonly slate: string    // DELETE
  readonly dusk: string     // DELETE
}

// After:
readonly themes: {
  readonly light: string
  readonly dark: string
  readonly system: string
}
```

EN catalog (~L193-200, inside the `en` const):
```typescript
// Before:
themes: {
  light: 'Light',
  dark: 'Dark',
  system: 'System',
  moss: 'Moss',     // DELETE
  slate: 'Slate',   // DELETE
  dusk: 'Dusk',     // DELETE
},

// After: 3 lines only.
```

PT-BR catalog (~L349-356):
```typescript
// Before:
themes: {
  light: 'Claro',
  dark: 'Escuro',
  system: 'Sistema',
  moss: 'Musgo',         // DELETE
  slate: 'Ardósia',      // DELETE
  dusk: 'Crepúsculo',    // DELETE
},

// After: 3 lines only.
```

From src/content/strings.test.ts (per PATTERNS — likely zero-edit; verify via grep):
- grep at plan time: `grep -nE "moss|slate|dusk|Moss|Slate|Dusk|Musgo|Ardósia|Crepúsculo" src/content/strings.test.ts`
- If zero hits: no edit. The type narrowing from the strings.ts edit is the only change strings.test.ts will see, and the existing iterations over `themes` keys auto-shrink.
- If any hits: rotate or delete per Tiger Style.

From src/components/ThemePicker.tsx:
- L8 leading comment may say something like `"English-locked labels (Light/Dark/System/Moss/Slate/Dusk verbatim)"` — update to `"English-locked labels (Light/Dark/System verbatim)"`. Verify the exact comment text via Read first.
- The `THEME_OPTIONS.map(id => ...)` render loop (L~32) is unchanged — auto-shrinks.

From src/components/ThemePicker.test.tsx (concrete edits — verified hits via plan-revision grep at L42/L46/L49/L50/L51/L52/L62/L63/L69/L87/L100/L101/L112/L114/L115):

1. L37-43 (the "renders all 6 options" case) — update label list + count:
```typescript
// Before:
it('renders all 6 options as radio buttons with correct labels in order', () => {
  ...
  expect(radios).toHaveLength(6)
  const labels = Array.from(radios).map((b) => b.textContent)
  expect(labels).toEqual(['Light', 'Dark', 'System', 'Moss', 'Slate', 'Dusk'])
})

// After:
it('renders all 3 options as radio buttons with correct labels in order', () => {
  ...
  expect(radios).toHaveLength(3)
  const labels = Array.from(radios).map((b) => b.textContent)
  expect(labels).toEqual(['Light', 'Dark', 'System'])
})
```

2. L45-56 — rotate `seedTheme('moss')` test to a surviving theme (e.g. `seedTheme('dark')`; assert Dark is aria-checked) per PATTERNS §"rotation strategy". Also rotates the L49 inline comment ("Find the Moss button (index 3 in order...)") to reference the surviving target.

3. L58-70 — rotate `slateButton` click test to a surviving theme (e.g. `seedTheme('light')` → click `System` → assert stored theme === `'system'`).

4. **L87 it() description string** — rotate `'all 6 buttons'` to `'all 3 buttons'`:
```typescript
// Before:
it('when disabled=true, all 6 buttons have the disabled attribute and radiogroup has aria-disabled=true', ...)

// After:
it('when disabled=true, all 3 buttons have the disabled attribute and radiogroup has aria-disabled=true', ...)
```
This is NOT covered by the L37-43 / L45-56 / L58-70 / L96-109 / L111-117 rotation blocks above — it is a standalone description-string edit that must be enumerated explicitly. After Plan 01 shrinks THEME_OPTIONS to 3, this disabled-attribute sweep test now iterates 3 buttons.

5. L96-109 — rotate `duskButton` disabled-click test to a surviving theme (e.g. click `'System'` or `'Dark'`).

6. L111-117 — rotate `seedTheme('dusk')` aria-checked-while-disabled test to `seedTheme('system')` and `systemButton`.

The behavioral semantics are preserved: click writes / disabled blocks / aria-checked tracks state. Only the choice of theme ID rotates.

From src/hooks/useTheme.test.ts (per PATTERNS §"Plan 3 — `src/hooks/useTheme.test.ts`"):
- L126: `prefs: { theme: 'moss', timbre: 'bowl', locale: 'en' },` → rotate `'moss'` → `'dark'` (or `'light'`).
- L142-143: `expect(result.current.theme).toBe('moss')` + `expect(document.documentElement.dataset.theme).toBe('moss')` → assert the rotated theme.
- L172: `seedPrefs('dusk')` → `seedPrefs('dark')` (or another surviving non-system theme).
- L178: `new CustomEvent('hrv:prefs-changed', { detail: { key: 'theme', value: 'dusk' } })` → `value: 'dark'` or another surviving theme.
- L182-183: assertions to match the rotated value.

CONSTRAINT: rotate to a value that DIFFERS from the seeded value (so the cross-tab change has a non-trivial delta). Common pattern: seed `'dark'`, push `'light'`. Avoid `'system'` for the dataset-attribute assertion because useTheme's apply effect does NOT write a `data-theme` attribute for system (only for named themes).

From src/hooks/useThemeChoice.test.ts (16 hits verified via plan-revision grep at L32/L34/L37/L38/L42/L45/L48/L49/L53/L60/L63/L64/L68/L75/L81/L103):

ALL three categories of hits MUST be rotated together — fixtures, setters, AND it() description strings:

- **L32, L38, L49, L64, L81, L103 (6 fixture seeds):** `seedPrefs(..., theme: 'moss')` → `theme: 'dark'`.
- **L34 (assertion):** `expect(result.current.theme).toBe('moss')` → `'dark'`.
- **L42, L53 (setter calls):** `result.current.setTheme('dusk')` → `setTheme('system')` (or `'light'`).
- **L45, L60 (assertions):** `expect(...).toBe('dusk')` → assert the rotated setTheme target.
- **L68 (setter):** `setTheme('slate')` → `setTheme('system')`.
- **L75 (assertion):** `expect(raw.prefs.theme).toBe('slate')` → `toBe('system')`.

- **L37 (it() description):** `it('setTheme("dusk") updates local state optimistically', ...)` — rotate `"dusk"` in the description to match the chosen setTheme target (e.g. if L42 becomes `setTheme('system')`, L37 becomes `it('setTheme("system") updates local state optimistically', ...)`).
- **L48 (it() description):** `it('setTheme("dusk") writes the new theme to disk via savePrefs', ...)` — same rotation; match L53's setTheme target.
- **L63 (it() description):** `it('setTheme("slate") preserves other prefs fields — envelope merge contract', ...)` — rotate to match L68's setTheme target (e.g. `it('setTheme("system") preserves other prefs fields...', ...)`).

ROTATION STRATEGY: pick two distinct surviving themes per test case so the before/after delta is non-trivial. Recommended pairing: seed `'dark'` (replaces every `'moss'`), set `'system'` (replaces every `'dusk'` and every `'slate'`) — this is internally consistent across all 16 hits and yields a clean before/after delta.

From src/hooks/useFavicon.test.ts (8 hits verified via plan-revision grep at L80/L81/L84/L134/L150/L181/L187/L191):

All 8 hits MUST be enumerated and rotated. Hits split into two semantic groups:

**Group A — `moss`-track hits (rotate to surviving theme `dark`):**
- **L80 (it() description):** `it('seeds favicon from loadPrefs().theme at mount (named theme: moss)', ...)` → `it('seeds favicon from loadPrefs().theme at mount (named theme: dark)', ...)`.
- **L81 (seedPrefs call):** `seedPrefs('moss')` → `seedPrefs('dark')`.
- **L84 (FAVICON_COLORS property access):** `expect(href).toContain(FAVICON_COLORS.moss.replace('#', '%23').slice(1))` → `expect(href).toContain(FAVICON_COLORS.dark.replace('#', '%23').slice(1))`. **TS-mandatory after Plan 02 deletes `.moss` from the FAVICON_COLORS record.**

**Group B — cross-tab storage event test (~L126-151) — `moss`-track:**
- **L134 (envelope theme literal):** `prefs: { theme: 'moss', timbre: 'bowl', locale: 'en' }` → `prefs: { theme: 'dark', timbre: 'bowl', locale: 'en' }`.
- **L150 (FAVICON_COLORS property access):** `expect(getFaviconHref()).toContain(FAVICON_COLORS.moss.replace('#', '%23').slice(1))` → `expect(getFaviconHref()).toContain(FAVICON_COLORS.dark.replace('#', '%23').slice(1))`. **TS-mandatory.**

**Group C — same-tab CustomEvent test (~L175-192) — `dusk`-track (rotate to surviving theme `light`):**
- **L181 (seedPrefs call):** `seedPrefs('dusk')` → `seedPrefs('light')`. The seed-then-change pattern needs the post-event value to differ from the seeded value — see L187.
- **L187 (CustomEvent detail.value):** `new CustomEvent('hrv:prefs-changed', { detail: { key: 'theme', value: 'dusk' } })` → choose a different surviving theme. Recommended: `value: 'dark'` so the seeded `'light'` (L181) and pushed `'dark'` give a non-trivial delta. The L191 property access must then track `FAVICON_COLORS.dark`.
- **L191 (FAVICON_COLORS property access):** `expect(getFaviconHref()).toContain(FAVICON_COLORS.dusk.replace('#', '%23').slice(1))` → `expect(getFaviconHref()).toContain(FAVICON_COLORS.dark.replace('#', '%23').slice(1))` (matching the L187 pushed value). **TS-mandatory.**

ROTATION RATIONALE for Group C: the test asserts "favicon updates when a same-tab CustomEvent fires with a new theme value". The original test seeded `'dark'` and pushed `'dusk'`. After rotation, seed `'light'` (a surviving named theme distinct from the L181-existing pattern) and push `'dark'` — preserves the named-theme-A → named-theme-B transition the test is verifying. The alternative (seed `'dark'` push `'light'`) is also acceptable; planner discretion at execution time, but ensure L181/L187/L191 stay internally consistent (the L191 assertion must reference whichever surviving theme L187 pushed).

From src/app/App.session.test.tsx:
- Per CONTEXT §canonical_refs: "current grep shows none beyond the spurious 'scale(0.79)' substring matches". PATTERNS classifies this as likely zero-edit ("CONTEXT notes grep finds at least one moss reference; verify at plan time").
- Execute: `grep -n "moss\|slate\|dusk" src/app/App.session.test.tsx`. If zero hits: no edit. If any hits: rotate per the same strategy.

NOTE: the `LOCKED_COPY` byte-equality guard (Phase 19 I18N) is UNAFFECTED by this plan. Palette display strings (Moss/Slate/Dusk/Musgo/Ardósia/Crepúsculo) are NOT in LOCKED_COPY — only Forrest/medical claim-safe copy is. Phase 26's `content.no-review-markers.test.ts` is also UNAFFECTED — no review markers are introduced; the strings disappear entirely.
</interfaces>
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (no new attack surface) | Pure i18n catalog deletion + test fixture rotations — no new code paths, no new input handling |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-39-06 | I (info — accidental LOCKED_COPY drift) | src/content/strings.ts | mitigate | Palette display strings (Moss/Slate/Dusk/Musgo/Ardósia/Crepúsculo) are NOT in LOCKED_COPY (only Forrest/medical claim-safe copy is, per Phase 19 I18N + Phase 26 I18N-07 establishment); the frozen-EN byte-equality guard is untouched; verify post-edit `pnpm test` includes the LOCKED_COPY guard suite passing |
| T-39-07 | I (info — review-marker debt) | src/content/strings.ts | mitigate | Clean-cut deletion (per D-07 + Phase 38 D-08 precedent) — no `[review-needed]` markers are inserted; the Phase 26 `content.no-review-markers.test.ts` drift-guard continues to pass |
| T-39-08 | I (info — behavioral coverage loss in hook tests) | src/hooks/useTheme.test.ts, useThemeChoice.test.ts, useFavicon.test.ts | mitigate | Per PATTERNS §"rotation strategy": test cases are ROTATED to surviving theme IDs, not deleted; behavioral semantics (click writes, cross-tab listener, aria-checked tracking) preserved against the surviving 3-option set; cross-tab change tests retain a non-trivial before/after delta by rotating to two distinct surviving themes |
| T-39-09 | T (tampering — tsc-broken commit boundary) | src/hooks/useFavicon.test.ts | mitigate | Plan 02 deletes `FAVICON_COLORS.moss|.slate|.dusk`; Plan 03's Edit C enumerates every property-access in useFavicon.test.ts (L84, L150, L191) and rewrites them to surviving keys (`.dark` / `.light`) — same wave, ideally same commit chain; final acceptance gate runs `npx tsc --noEmit` to confirm zero TS-2339 errors |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Delete themes.moss/slate/dusk from UiStrings type and both EN + PT-BR catalogs in src/content/strings.ts</name>
  <files>
    - src/content/strings.ts
    - src/content/strings.test.ts
  </files>
  <read_first>
    - src/content/strings.ts (the UiStrings.themes block at ~L35-42, EN themes catalog at ~L193-200, PT-BR themes catalog at ~L349-356 — verify exact lines)
    - src/content/strings.test.ts (entire file — grep for moss/slate/dusk to confirm zero or near-zero hits per PATTERNS)
    - .planning/phases/39-theme-simplification/39-PATTERNS.md §"Plan 3 — `src/content/strings.ts`" and §"strings.test.ts — likely zero-edit"
    - .planning/phases/39-theme-simplification/39-CONTEXT.md §decisions D-07 (clean-cut)
    - .planning/phases/38-variant-removal/38-02-SUMMARY.md (Phase 38 D-08 i18n clean-cut precedent)
  </read_first>
  <action>
    Surgically edit `src/content/strings.ts` in three places + verify `src/content/strings.test.ts`.

    Edit A — `src/content/strings.ts` UiStrings type:
    1. Locate the `readonly themes: { ... }` block inside `UiStrings` (~L35-42 per PATTERNS).
    2. Delete the 3 lines `readonly moss: string`, `readonly slate: string`, `readonly dusk: string`.
    3. Preserve `readonly light: string`, `readonly dark: string`, `readonly system: string` in their existing order.
    4. Preserve the surrounding `readonly themes: { ... }` brace structure and trailing comma/semicolon.

    Edit B — `src/content/strings.ts` EN catalog:
    1. Locate the `themes: { ... }` block inside the `en` const (~L193-200).
    2. Delete the 3 lines `moss: 'Moss',`, `slate: 'Slate',`, `dusk: 'Dusk',`.
    3. Preserve `light: 'Light',`, `dark: 'Dark',`, `system: 'System',`.

    Edit C — `src/content/strings.ts` PT-BR catalog:
    1. Locate the `themes: { ... }` block inside the `pt-BR` (or `ptBR`) catalog (~L349-356).
    2. Delete the 3 lines `moss: 'Musgo',`, `slate: 'Ardósia',`, `dusk: 'Crepúsculo',`.
    3. Preserve `light: 'Claro',`, `dark: 'Escuro',`, `system: 'Sistema',`.

    Edit D — `src/content/strings.test.ts` verification:
    1. `grep -nE "moss|slate|dusk|Moss|Slate|Dusk|Musgo|Ardósia|Crepúsculo" src/content/strings.test.ts` — if zero hits, no edit.
    2. If any hit exists: rotate to a surviving value or delete the assertion if it specifically iterates the deprecated set.
    3. Verify any `Object.keys(themes)` length assertion (if any) is updated from 6 to 3.

    Tsc + test sanity:
    - `pnpm tsc --noEmit` exits 0 — the type system is coherent (3-field themes block) and all consumers (ThemePicker.tsx + useTheme.tsx + tests) only reference `light`/`dark`/`system`.
    - `pnpm test src/content/strings.test.ts --run` exits 0.
    - LOCKED_COPY suite (if it runs as part of strings.test.ts or as a separate test) passes — palette display strings are NOT in LOCKED_COPY per Phase 19.

    Commit: `feat(39): delete moss/slate/dusk from UiStrings + EN/PT-BR catalogs (THM-04 strings)`.
  </action>
  <verify>
    <automated>pnpm test src/content/strings.test.ts --run && pnpm tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE "(moss|slate|dusk):" src/content/strings.ts` returns 0 matches.
    - `grep -nE "'(Moss|Slate|Dusk|Musgo|Ardósia|Crepúsculo)'" src/content/strings.ts` returns 0 matches.
    - `grep -n "readonly themes" src/content/strings.ts` returns 1 line (the type declaration); within ~10 lines below it, exactly 3 `readonly light: string` / `readonly dark: string` / `readonly system: string` lines exist (and zero deprecated-theme readonly fields).
    - `grep -nE "themes:\\s*\\{" src/content/strings.ts` returns at least 2 matches (the EN and PT-BR catalog blocks).
    - `grep -n "system: 'System'" src/content/strings.ts` returns 1 match (EN); `grep -n "system: 'Sistema'" src/content/strings.ts` returns 1 match (PT-BR).
    - `grep -nE "moss|slate|dusk|Moss|Slate|Dusk|Musgo|Ardósia|Crepúsculo" src/content/strings.test.ts` returns 0 matches (or the planner-verified rotation handles any).
    - `pnpm tsc --noEmit` exits 0.
    - `pnpm test src/content/strings.test.ts --run` exits 0 (LOCKED_COPY guard passing remains intact).
  </acceptance_criteria>
  <done>UiStrings.themes type has 3 readonly fields; EN and PT-BR catalogs have 3 entries each; strings.test.ts is clean (zero deprecated-theme references); tsc green; LOCKED_COPY guard intact.</done>
</task>

<task type="auto">
  <name>Task 2: Update ThemePicker.test.tsx — 3-option assertion + L87 description fix + rotation of moss/slate/dusk behavioral cases (THM-04 render)</name>
  <files>
    - src/components/ThemePicker.tsx
    - src/components/ThemePicker.test.tsx
  </files>
  <read_first>
    - src/components/ThemePicker.tsx (entire file — confirm `THEME_OPTIONS.map(...)` consumer at ~L32; verify the L8 leading comment for the optional comment-debt update)
    - src/components/ThemePicker.test.tsx (entire file — confirm the L37-43 option-list assertion; L45-56 moss-seeded case; L58-70 slate click case; **L87 "all 6 buttons" disabled-attribute sweep description**; L96-109 dusk disabled case; L111-117 dusk-while-disabled case)
    - .planning/phases/39-theme-simplification/39-PATTERNS.md §"Plan 3 — `src/components/ThemePicker.tsx`" and §"Plan 3 — `src/components/ThemePicker.test.tsx`" — concrete rotation strategy
    - .planning/phases/39-theme-simplification/39-CONTEXT.md §decisions D-12 (surgical strip + rotation)
  </read_first>
  <action>
    Two coordinated edits — ThemePicker.tsx (optional comment-debt) and ThemePicker.test.tsx (assertion + description-string + rotations).

    Edit A — `src/components/ThemePicker.tsx`:
    1. If the L8 leading comment enumerates the 6 deprecated themes (e.g. `"English-locked labels (Light/Dark/System/Moss/Slate/Dusk verbatim)"`), update to `"English-locked labels (Light/Dark/System verbatim)"`. Verify the exact text by reading the file first.
    2. If no such enumeration exists, this is a zero-edit file (the `THEME_OPTIONS.map(...)` consumer auto-shrinks via Plan 01).
    3. DO NOT touch the JSX render body or any prop/import.

    Edit B — `src/components/ThemePicker.test.tsx`:

    1. **Option-list assertion** (~L37-43): update from 6 to 3 — both the it() description and the `toHaveLength` assertion and the `toEqual` labels list. The labels must be exactly `['Light', 'Dark', 'System']` in this order.

    2. **moss-seeded test (~L45-56)**: rotate `seedTheme('moss')` to `seedTheme('dark')` (or `'light'` — planner discretion; pick the one that yields the cleanest test narrative). Update the corresponding `findByRole`/`getByRole` query for the seeded theme's button (now the Dark button is aria-checked). Update the L49 inline comment ("Find the Moss button (index 3 in order: light, dark, system, moss, slate, dusk)") to reference the surviving target and the 3-option ordering. Update the assertion to match.

    3. **slate click test (~L58-70)**: rotate the `slateButton` reference. Pick a click-target that exists in the surviving 3-option set — e.g. seed `'light'`, click `System`, assert stored theme === `'system'`. Preserve the click-writes-to-disk semantics.

    4. **L87 it() description string — "all 6 buttons" → "all 3 buttons"** (REQUIRED, not optional):
       - Before: `it('when disabled=true, all 6 buttons have the disabled attribute and radiogroup has aria-disabled=true', () => {`
       - After:  `it('when disabled=true, all 3 buttons have the disabled attribute and radiogroup has aria-disabled=true', () => {`
       - The test body itself does NOT enumerate buttons; it iterates `screen.getAllByRole('radio')` and calls `expect(button).toBeDisabled()` for each. After Plan 01 narrows THEME_OPTIONS to 3, the iteration covers 3 buttons — the description string MUST track that count or it becomes misleading documentation.

    5. **dusk disabled-click test (~L96-109)**: rotate `duskButton` to `systemButton` or `darkButton`. Preserve the disabled-blocks-click semantics.

    6. **dusk aria-checked-while-disabled test (~L111-117)**: rotate `seedTheme('dusk')` to `seedTheme('system')` and `duskButton` to `systemButton`. Preserve the aria-checked-survives-disable semantics.

    7. ROTATION RULE: pick rotations that preserve the test's behavioral assertion. e.g. if a test asserts "clicking writes to disk", the rotated theme must be a click target that's enabled. If a test asserts "aria-checked survives disable", the rotated theme must be the seeded one.

    8. DO NOT delete any it() block. Tiger Style: surviving coverage of click/disabled/aria-checked invariants matters; the choice of theme is incidental.

    9. Tsc + test sanity:
       - `pnpm tsc --noEmit` exits 0.
       - `pnpm test src/components/ThemePicker.test.tsx --run` exits 0 with all cases passing (the count may shift if any case literally cannot rotate — verify each rotation preserves the original assertion's semantics).

    Commit: `feat(39): align ThemePicker tests to 3-option theme set (THM-04 render)`.
  </action>
  <verify>
    <automated>pnpm test src/components/ThemePicker.test.tsx --run</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE "moss|slate|dusk|Moss|Slate|Dusk" src/components/ThemePicker.test.tsx` returns 0 matches.
    - `grep -nE "moss|slate|dusk|Moss|Slate|Dusk" src/components/ThemePicker.tsx` returns 0 matches.
    - `grep -n "toHaveLength(3)" src/components/ThemePicker.test.tsx` returns at least 1 match (the option-list count assertion updated).
    - `grep -nE "toEqual\\(\\[['\"](Light)['\"],\\s*['\"](Dark)['\"],\\s*['\"](System)['\"]\\]\\)" src/components/ThemePicker.test.tsx` returns 1 match (the option-list labels assertion).
    - **`grep -nE "all [0-9]+ (buttons|options)" src/components/ThemePicker.test.tsx` returns only matches with `3` — zero matches with `6`, `5`, or `4`.** (Confirms the L87 description string update and any sibling count phrases.)
    - **`grep -n "all 6" src/components/ThemePicker.test.tsx` returns 0 matches** (belt-and-suspenders against the L87 regression).
    - `pnpm tsc --noEmit` exits 0.
    - `pnpm test src/components/ThemePicker.test.tsx --run` exits 0 with all cases passing.
    - The number of `it(...)` blocks in ThemePicker.test.tsx after the rotation should be EQUAL to (or 1-off from, in edge cases) the original count — NO case wholesale deleted (per the rotation rule).
  </acceptance_criteria>
  <done>ThemePicker.test.tsx asserts the picker renders 3 options ['Light', 'Dark', 'System'] in order; the L87 "all 6 buttons" description is updated to "all 3 buttons"; all moss/slate/dusk-specific behavioral cases are rotated to surviving theme IDs; ThemePicker.tsx is zero-edit OR has only a comment-debt update; tsc + ThemePicker test suite green.</done>
</task>

<task type="auto">
  <name>Task 3: Rotate moss/slate/dusk fixture seeds + setter calls + it() descriptions + FAVICON_COLORS property accesses across useTheme.test.ts, useThemeChoice.test.ts, useFavicon.test.ts, and App.session.test.tsx (D-12)</name>
  <files>
    - src/hooks/useTheme.test.ts
    - src/hooks/useThemeChoice.test.ts
    - src/hooks/useFavicon.test.ts
    - src/app/App.session.test.tsx
  </files>
  <read_first>
    - src/hooks/useTheme.test.ts (PATTERNS lists hits at L126, L142-143, L172, L178, L182-183 — verify and confirm)
    - src/hooks/useThemeChoice.test.ts (16 hits verified at plan-revision time via `grep -nE "moss|slate|dusk" src/hooks/useThemeChoice.test.ts`: L32, L34, L37, L38, L42, L45, L48, L49, L53, L60, L63, L64, L68, L75, L81, L103 — three of these (L37/L48/L63) are it() description strings, not fixture/setter calls)
    - src/hooks/useFavicon.test.ts (8 hits verified at plan-revision time via `grep -nE "moss|slate|dusk" src/hooks/useFavicon.test.ts`: L80, L81, L84, L134, L150, L181, L187, L191 — three of these (L84, L150, L191) are `FAVICON_COLORS.moss|.dusk` property accesses that become TS-2339 errors after Plan 02)
    - src/app/App.session.test.tsx (verify zero/near-zero hits — PATTERNS classifies as likely zero-edit)
    - .planning/phases/39-theme-simplification/39-PATTERNS.md §"Plan 3 — `src/hooks/useTheme.test.ts`", §"Plan 3 — `src/hooks/useThemeChoice.test.ts`", §"Plan 3 — `src/hooks/useFavicon.test.ts`"
    - .planning/phases/39-theme-simplification/39-CONTEXT.md §decisions D-12 (surgical strip with rotation)
    - .planning/phases/39-theme-simplification/39-02-PLAN.md (Plan 02 deletes `FAVICON_COLORS.moss|.slate|.dusk` — context for the property-access rewrites in this task's Edit C)
  </read_first>
  <action>
    Surgical strip with rotation across 4 test files. Per CONTEXT D-12 + PATTERNS §"rotation strategy": replace deprecated theme literals AND deprecated property-access expressions with surviving alternatives, preserving each test's behavioral assertion semantics. Tiger Style: rotate, don't delete. The surviving themes set is `'light' | 'dark' | 'system'`. The surviving FAVICON_COLORS keys (after Plan 02) are `light` and `dark`.

    Edit A — `src/hooks/useTheme.test.ts`:
    1. L126: change `theme: 'moss'` to `theme: 'dark'` (or `'light'` — pick one consistent with the surrounding test narrative).
    2. L142-143: change both `expect(...).toBe('moss')` to `toBe('dark')` (to match the seed in L126).
    3. L172: change `seedPrefs('dusk')` to `seedPrefs('dark')`. Verify the test's narrative — the cross-tab change test needs a different post-event value than the seeded value.
    4. L178: change `value: 'dusk'` to `value: 'light'` (so the cross-tab event has a non-trivial delta from the L172 seed of `'dark'`).
    5. L182-183: assertions update to `toBe('light')` (matching the L178 event payload).
    6. CONSTRAINT: avoid `'system'` for any dataset-attribute assertion (`document.documentElement.dataset.theme`) — useTheme's apply effect does NOT write a `data-theme` for system. Use named themes (`'dark'`/`'light'`) for dataset assertions.

    Edit B — `src/hooks/useThemeChoice.test.ts` (16 hits — enumerated below with concrete rotation targets):

    Recommended internally-consistent rotation: every `'moss'` → `'dark'`; every `'dusk'` and `'slate'` → `'system'`. Apply ALL 16 hits in one pass:

    **Fixture seeds (6 lines — `seedPrefs(..., theme: 'moss')` → `theme: 'dark'`):**
    1. L32: `seedPrefs({ ...DEFAULT_FULL_PREFS, theme: 'moss' })` → `theme: 'dark'`.
    2. L38: same rewrite.
    3. L49: same rewrite.
    4. L64: `seedPrefs({ theme: 'moss', timbre: 'bell', cue: 'labels', locale: 'pt-BR' })` → `theme: 'dark'`.
    5. L81: `seedPrefs({ ...DEFAULT_FULL_PREFS, theme: 'moss' })` → `theme: 'dark'`.
    6. L103: same rewrite.

    **Assertion at L34:**
    7. L34: `expect(result.current.theme).toBe('moss')` → `toBe('dark')` (matches L32 seed).

    **Setter calls + paired assertions (L42/L45, L53/L60, L68/L75):**
    8. L42: `result.current.setTheme('dusk')` → `setTheme('system')`.
    9. L45: `expect(result.current.theme).toBe('dusk')` → `toBe('system')` (matches L42).
    10. L53: `result.current.setTheme('dusk')` → `setTheme('system')`.
    11. L60: `expect(raw.prefs.theme).toBe('dusk')` → `toBe('system')` (matches L53).
    12. L68: `result.current.setTheme('slate')` → `setTheme('system')`.
    13. L75: `expect(raw.prefs.theme).toBe('slate')` → `toBe('system')` (matches L68).

    **it() description string rotations (3 lines — must match the rotated setter target):**
    14. L37: `it('setTheme("dusk") updates local state optimistically', ...)` → `it('setTheme("system") updates local state optimistically', ...)`. (Matches the rotated L42 setter.)
    15. L48: `it('setTheme("dusk") writes the new theme to disk via savePrefs', ...)` → `it('setTheme("system") writes the new theme to disk via savePrefs', ...)`. (Matches the rotated L53 setter.)
    16. L63: `it('setTheme("slate") preserves other prefs fields — envelope merge contract', ...)` → `it('setTheme("system") preserves other prefs fields — envelope merge contract', ...)`. (Matches the rotated L68 setter.)

    SELF-CHECK: after applying all 16 edits, `grep -nE "moss|slate|dusk" src/hooks/useThemeChoice.test.ts` MUST return 0 matches. If non-zero, re-read the file and locate the missed hit.

    Edit C — `src/hooks/useFavicon.test.ts` (8 hits — enumerated below; 3 of which are TS-mandatory property-access rewrites tied to Plan 02):

    Recommended internally-consistent rotation: `moss`-track → `dark` (it() description, seedPrefs call, envelope literal, FAVICON_COLORS property access); `dusk`-track → seeded as `light`, pushed as `dark`, FAVICON_COLORS asserted on `.dark` (preserves named-theme-A → named-theme-B transition the cross-tab test verifies).

    **Group A — mount-time seed test (~L78-85) — moss-track:**
    1. **L80 (it() description):** `it('seeds favicon from loadPrefs().theme at mount (named theme: moss)', ...)` → `it('seeds favicon from loadPrefs().theme at mount (named theme: dark)', ...)`.
    2. **L81 (seedPrefs call):** `seedPrefs('moss')` → `seedPrefs('dark')`.
    3. **L84 (FAVICON_COLORS property access — TS-mandatory after Plan 02):** `expect(href).toContain(FAVICON_COLORS.moss.replace('#', '%23').slice(1))` → `expect(href).toContain(FAVICON_COLORS.dark.replace('#', '%23').slice(1))`.

    **Group B — cross-tab storage event test (~L126-151) — moss-track:**
    4. **L134 (envelope theme literal):** `prefs: { theme: 'moss', timbre: 'bowl', locale: 'en' }` → `prefs: { theme: 'dark', timbre: 'bowl', locale: 'en' }`.
    5. **L150 (FAVICON_COLORS property access — TS-mandatory after Plan 02):** `expect(getFaviconHref()).toContain(FAVICON_COLORS.moss.replace('#', '%23').slice(1))` → `.toContain(FAVICON_COLORS.dark.replace('#', '%23').slice(1))`.

       NOTE: this test seeds `'dark'` at L127 (`seedPrefs('dark')`) and uses the cross-tab storage event to push a NEW envelope. After rotation the L134 envelope and L150 assertion both target `'dark'` — meaning the cross-tab push is a no-op delta. That degrades the test's coverage of "cross-tab event triggers a re-read". To preserve a non-trivial delta, rotate L134 to `'light'` AND L150 to `FAVICON_COLORS.light` instead (so seeded `'dark'` → pushed `'light'` is a real transition). Planner discretion at execute time, but Tiger Style strongly prefers the non-trivial-delta path: **rewrite L134 to `theme: 'light'` AND L150 to `FAVICON_COLORS.light`** for a clean before/after.

    **Group C — same-tab CustomEvent test (~L175-192) — dusk-track:**
    6. **L181 (seedPrefs call):** `seedPrefs('dusk')` → `seedPrefs('light')`.
    7. **L187 (CustomEvent detail.value):** `new CustomEvent('hrv:prefs-changed', { detail: { key: 'theme', value: 'dusk' } })` → `value: 'dark'` (pairs with L181's `'light'` seed to give a non-trivial named-theme transition).
    8. **L191 (FAVICON_COLORS property access — TS-mandatory after Plan 02):** `expect(getFaviconHref()).toContain(FAVICON_COLORS.dusk.replace('#', '%23').slice(1))` → `.toContain(FAVICON_COLORS.dark.replace('#', '%23').slice(1))` (matches the L187 pushed `'dark'`).

    SELF-CHECK: after applying all 8 edits, `grep -nE "moss|slate|dusk" src/hooks/useFavicon.test.ts` MUST return 0 matches. AND `grep -nE "FAVICON_COLORS\\.(moss|slate|dusk)" src/hooks/useFavicon.test.ts` MUST return 0 matches. Final `npx tsc --noEmit` MUST exit 0 (confirms no TS-2339 on FAVICON_COLORS deleted keys).

    Edit D — `src/app/App.session.test.tsx`:
    1. Execute `grep -nE "moss|slate|dusk" src/app/App.session.test.tsx`. PATTERNS expects zero hits (or only spurious matches like `'scale(0.79)'`).
    2. If zero hits: NO EDIT.
    3. If any hits: rotate per the same strategy. Pick rotations that preserve the test's behavioral assertion.

    Tsc + test sanity:
    - `pnpm tsc --noEmit` exits 0.
    - `pnpm test src/hooks/useTheme.test.ts --run` exits 0.
    - `pnpm test src/hooks/useThemeChoice.test.ts --run` exits 0.
    - `pnpm test src/hooks/useFavicon.test.ts --run` exits 0.
    - `pnpm test src/app/App.session.test.tsx --run` exits 0.

    Commit: `test(39): rotate deprecated-theme fixtures + property accesses in hook + app tests (D-12)`.
  </action>
  <verify>
    <automated>pnpm test src/hooks --run && pnpm test src/app/App.session.test.tsx --run</automated>
  </verify>
  <acceptance_criteria>
    - **`grep -cnE "moss|slate|dusk" src/hooks/useFavicon.test.ts` returns 0.** (Belt-and-suspenders against any missed hit among L80/L81/L84/L134/L150/L181/L187/L191.)
    - **`grep -nE "FAVICON_COLORS\\.(moss|slate|dusk)" src/hooks/useFavicon.test.ts` returns 0 matches.** (Confirms the 3 property-access rewrites at L84/L150/L191.)
    - **`grep -nE "moss|slate|dusk" src/hooks/useThemeChoice.test.ts` returns 0 matches.** (Confirms all 16 hits — fixtures + setters + descriptions — were rotated.)
    - **`npx tsc --noEmit` exits 0** (confirms no TS-2339 errors on the deleted FAVICON_COLORS keys at this plan's commit boundary).
    - `grep -nE "moss|slate|dusk" src/hooks/useTheme.test.ts` returns 0 matches.
    - `grep -nE "moss|slate|dusk" src/app/App.session.test.tsx | grep -v "scale(" | grep -v "scale-"` returns 0 matches (filter out the spurious CSS `scale(0.79)` matches PATTERNS noted).
    - `pnpm test src/hooks --run` exits 0.
    - `pnpm test src/app/App.session.test.tsx --run` exits 0.
    - The number of `it(...)` blocks in each modified test file is UNCHANGED from before the rotation (rotation preserves cases; deletion of an entire case is forbidden here unless the case's narrative is solely about a deprecated theme's specific characteristics — none of the L32-103 cases per PATTERNS are such cases).
  </acceptance_criteria>
  <done>All deprecated-theme literals AND all `FAVICON_COLORS.moss|.slate|.dusk` property accesses in the 4 cross-cutting test files are rotated to surviving alternatives; behavioral coverage is preserved; tsc and the hook + App.session test suites are green.</done>
</task>

</tasks>

<verification>
After all three tasks complete:
- `grep -rE "moss|slate|dusk|Moss|Slate|Dusk|Musgo|Ardósia|Crepúsculo" src/content/ src/components/ src/hooks/ src/app/ | grep -v ".test.ts:.*scale(" | wc -l` returns 0 (or a small number ≤ 0 after filtering legitimate non-theme matches; the drift-guard from Plan 05 makes this a CI invariant).
- `pnpm tsc --noEmit` exits 0.
- `pnpm test src/content/ src/components/ThemePicker.test.tsx src/hooks/ src/app/App.session.test.tsx --run` exits 0 across all named suites.
- THM-04 closed end-to-end (THEME_OPTIONS shrunk in Plan 01 + display strings shrunk + ThemePicker render asserted at 3 options).
- LOCKED_COPY byte-equality guard (Phase 19) intact — palette display strings were never in LOCKED_COPY.

Three atomic commits scoped `(39)`: `feat(39): delete moss/slate/dusk from UiStrings + EN/PT-BR catalogs (THM-04 strings)`, `feat(39): align ThemePicker tests to 3-option theme set (THM-04 render)`, `test(39): rotate deprecated-theme fixtures + property accesses in hook + app tests (D-12)`.
</verification>

<success_criteria>
- UiStrings.themes type has 3 fields (light, dark, system); EN + PT-BR catalogs aligned.
- ThemePicker.test.tsx asserts 3 options with labels ['Light', 'Dark', 'System']; the L87 "all 6 buttons" description is updated to "all 3 buttons"; all moss/slate/dusk-specific test cases rotated to surviving theme IDs.
- useTheme.test.ts / useThemeChoice.test.ts / useFavicon.test.ts / App.session.test.tsx all rotated to surviving themes; no deprecated-theme literal remains; no `FAVICON_COLORS.moss|.slate|.dusk` property access remains.
- THM-04 closed end-to-end.
- LOCKED_COPY guard intact; Phase 26 review-marker drift-guard unaffected.
- Tiger Style: 3 atomic commits this plan.
</success_criteria>

<output>
After completion, create `.planning/phases/39-theme-simplification/39-03-SUMMARY.md` summarising:
- The before/after of strings.ts UiStrings type + both catalogs (with exact 3-field shape recorded).
- ThemePicker.test.tsx assertion changes (6 → 3 options), the L87 "all 6 buttons" → "all 3 buttons" description fix, and the 4 rotation cases with their seed → set theme choices documented.
- useTheme.test.ts / useThemeChoice.test.ts (all 16 hits including L37/L48/L63 it() descriptions) / useFavicon.test.ts (all 8 hits including L84/L150/L191 FAVICON_COLORS property-access rewrites) rotation summary (line-by-line if useful).
- App.session.test.tsx zero-edit confirmation (or rotation summary if grep found hits).
- D-07 + D-12 satisfied; LOCKED_COPY intact; Phase 26 review-marker guard intact.
- THM-04 closed end-to-end (type + display strings + render).
- Pointer forward to Plan 04 (index.html FOUC script).
- Tiger Style: 3 atomic commits this plan.
</output>
