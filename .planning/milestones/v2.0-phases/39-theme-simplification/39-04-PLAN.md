---
phase: 39-theme-simplification
plan: 04
type: execute
wave: 2
depends_on:
  - 39-01
files_modified:
  - index.html
autonomous: true
requirements:
  - THM-07
requirements_addressed:
  - THM-07
must_haves:
  truths:
    - "index.html L18 inline FOUC script no longer references `'moss'`, `'slate'`, `'dusk'` anywhere — neither in the allowlist nor in the color map `c`."
    - "The allowlist array shrinks from `['light','dark','moss','slate','dusk']` to `['light','dark']` (3 tokens deleted; 2 tokens preserved) per D-08."
    - "The color map `c` shrinks from `{'light':'#5e81ac','dark':'#81a1c1','moss':'#35a77c','slate':'#3760bf','dusk':'#f6c177'}` to `{'light':'#5e81ac','dark':'#81a1c1'}` (3 hex entries deleted; 2 entries preserved byte-unchanged) per D-08."
    - "The IIFE wrapper, single-line shape, matchMedia fallback branch, catch fallback, and pre-paint write timing are byte-preserved outside the 2 surgical excision points (D-08 — surgical edit, preserve minified one-liner shape)."
    - "Light + dark surviving hex values (`#5e81ac`, `#81a1c1`) are UNCHANGED — Phase 41 will retune them; Phase 39 only deletes the 3 deprecated entries."
    - "Returning-user THM-05 invariant in the pre-paint context: a persisted `theme: 'moss'` is read as `t === 'moss'`; the shrunk allowlist `['light','dark'].indexOf('moss') < 0` evaluates TRUE, so the fallback branch runs and resolves to the OS-active palette (light or dark). No FOUC, no flicker, no broken favicon. The persisted `'moss'` is then coerced to `'system'` once React mounts (Plan 01's THM-05 mechanism)."
    - "After this plan's commit lands on top of Plan 02's theme.css deletions: `pnpm test src/styles/favicon.sync.test.ts --run` exits 0 — the cross-file drift-guard now reads matching 2-palette mappings on BOTH sides (theme.css [data-theme='dark']:root + light @theme baseline, AND index.html allowlist + color map) per THM-07."
    - "`grep -nE \"moss|slate|dusk\" index.html` returns 0 matches (CONTEXT canonical_refs audit invariant)."
    - "D-08 satisfied: exactly 2 surgical touch-points within L18 — allowlist (3 tokens removed) + color map (3 hex entries removed); no third touch-point because matchMedia + catch fallback branches already write only `'light'` or `'dark'`."
    - "THM-07 closed end-to-end: favicon.sync.test.ts (Plan 02) guards the 2-palette mapping AND index.html (this plan) writes only the 2-palette pre-paint allowlist/colors."
  artifacts:
    - path: index.html
      provides: "Inline FOUC script (L18) with 2-token allowlist and 2-entry color map; light + dark surviving hex values byte-unchanged; IIFE wrapper, fallback branches, and single-line shape preserved."
      forbids: "(moss|slate|dusk)"
  key_links:
    - from: index.html
      to: src/styles/theme.css
      via: "favicon.sync.test.ts drift-guard parses both files for `[data-theme='X']:root` blocks (theme.css) and the FOUC color map entries (index.html); the THM-07 invariant requires matching keys + matching hex values"
      pattern: "\\[data-theme=|var c=\\{"
    - from: index.html
      to: src/storage/prefs.ts
      via: "the FOUC script reads localStorage to determine the pre-paint theme; the shrunk allowlist combined with coerceTheme (Plan 01) handles a deprecated persisted value: pre-paint falls back to OS-active; post-mount coerceTheme re-persists as 'system'"
      pattern: "localStorage|prefers-color-scheme"
---

<objective>
Surgically excise the 3 deprecated palette tokens from the inline FOUC script at `index.html:18`. Two touch-points within the single-line IIFE: (1) the allowlist array — delete `'moss'`, `'slate'`, `'dusk'` from `['light','dark','moss','slate','dusk']`; (2) the color map `c` — delete the 3 hex entries `'moss':'#35a77c'`, `'slate':'#3760bf'`, `'dusk':'#f6c177'`. Preserve the IIFE wrapper, the matchMedia fallback branch, the catch fallback, the `data-theme` write timing, and every byte outside the 2 excision points. Closes THM-07 (the second half — Plan 02 closed the favicon.sync.test.ts CSS-side guard; this plan closes the index.html pre-paint side so the cross-file drift-guard regression passes end-to-end).

Purpose: the FOUC pre-paint contract is load-bearing — the IIFE reads `localStorage`, resolves system→OS, writes `data-theme` + favicon BEFORE first paint. Phase 39 must preserve this timing-critical contract while eliminating dead allowlist tokens and dead hex map entries. After Plan 02 deleted the matching theme.css `[data-theme='X']:root` blocks, the index.html FOUC color map entries are dead weight (no theme.css block matches them anymore) and the allowlist references admit a value (`'moss'`) that would only fall through to the matchMedia fallback. Tiger Style: clean cut.

Per CONTEXT D-08 ("Surgical edit, preserve minified one-liner shape"). The IIFE stays a single-line minified one-liner — Phase 41 (Mono Zen) will revisit this script for the surviving 2 hex value retune. Phase 39 only deletes the 3 deprecated ones.

Per CONTEXT D-08's explicit "no third touch-point" clause: the matchMedia branch (`t=(window.matchMedia&&window.matchMedia('(prefers-color-scheme: dark)').matches)?'dark':'light';`) and the catch fallback (`document.documentElement.setAttribute('data-theme','light');setFav(favUri('#5e81ac'));`) already write only `'light'` or `'dark'` — they contain no moss/slate/dusk references and require no edit.

Output: 1 file, 1 line, 2 surgical excisions.
</objective>

<execution_context>
@$HOME/.claude-personal-personal/get-shit-done/workflows/execute-plan.md
@$HOME/.claude-personal-personal/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/39-theme-simplification/39-CONTEXT.md
@.planning/phases/39-theme-simplification/39-PATTERNS.md
@.planning/phases/39-theme-simplification/39-01-SUMMARY.md
@.planning/phases/39-theme-simplification/39-02-SUMMARY.md
@index.html

<interfaces>
<!-- Concrete excerpts from index.html L18. Sourced from 39-PATTERNS.md §"Plan 4 — `index.html:18` — surgical edit". -->

The FOUC IIFE at L18 contains a single-line minified script. Two relevant fragments (the rest of the IIFE is byte-preserved):

Before — Fragment 1 (allowlist):
`if(t==='system'||!t||['light','dark','moss','slate','dusk'].indexOf(t)<0)`

After:
`if(t==='system'||!t||['light','dark'].indexOf(t)<0)`

Before — Fragment 2 (color map):
`var c={'light':'#5e81ac','dark':'#81a1c1','moss':'#35a77c','slate':'#3760bf','dusk':'#f6c177'};`

After:
`var c={'light':'#5e81ac','dark':'#81a1c1'};`

Touch-point count: exactly 2 (per CONTEXT D-08). No third edit:
- The matchMedia fallback at the same line (`t=(window.matchMedia&&window.matchMedia('(prefers-color-scheme: dark)').matches)?'dark':'light';`) writes only `'dark'`/`'light'` — no moss/slate/dusk references.
- The catch fallback (`document.documentElement.setAttribute('data-theme','light');setFav(favUri('#5e81ac'));`) writes only `'light'` — no moss/slate/dusk references.

Returning-user behavior (THM-05 in pre-paint context):
- A returning user with persisted `localStorage.prefs.theme === 'moss'` reads `t === 'moss'`.
- The surgically-shrunk allowlist evaluates `['light','dark'].indexOf('moss') < 0` to TRUE.
- The IIFE's existing fallback path runs (matchMedia → resolve OS-active to `'light'` or `'dark'`).
- `data-theme` is written to `'light'` or `'dark'`, `favicon` is set to the corresponding hex via the surviving color map.
- No FOUC, no flicker. The persisted `'moss'` value reads via coercePrefs as `'system'` once React mounts (Plan 01's THM-05 mechanism); after the next savePrefs the disk value re-persists as `'system'`.

IIFE structural preservation rules:
1. Single-line minified shape preserved (no inserted newlines/whitespace beyond the deletions).
2. The IIFE wrapper `(function(){ ... })()` is byte-preserved.
3. The order of statements within the IIFE is preserved.
4. All semicolons and var/const declarations outside the 2 excision points are byte-preserved.
5. The `data-theme` write call timing (synchronous, before first paint) is preserved.

CRITICAL ORDERING NOTE: this plan should land AFTER Plan 02 (so the favicon.sync.test.ts cross-file invariant is satisfied across both files at the same commit boundary). However, this plan is independent of Plan 02 file-wise — they touch different files. The wave-2 ordering: Plans 02, 03, 04 are independent of each other file-wise; favicon.sync.test.ts requires BOTH Plan 02 AND Plan 04 to be fully green at the cumulative wave-2 commit boundary.
</interfaces>
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| localStorage → pre-paint IIFE | A returning user's persisted `theme` value crosses into the IIFE; the allowlist check is the trust boundary — invalid/deprecated values fall through to matchMedia resolution |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-39-09 | I (info — pure literal deletion) | index.html:18 FOUC script | accept | No new attack surface — pure deletion of 3 hex entries + 3 allowlist tokens; no new code path, no new input handling. The fallback behavior for deprecated values was ALREADY in place (matchMedia branch); this plan removes the dead allowlist entries that would otherwise have admitted the deprecated values to a never-matched code path. |
| T-39-10 | T (tampering — FOUC contract breakage) | index.html:18 FOUC script structure | mitigate | The 2 surgical excisions preserve the IIFE wrapper, matchMedia branch, catch fallback, single-line shape, and `data-theme` write timing; manual diff review confirms zero structural change; the favicon.sync.test.ts drift-guard (Plan 02 + this plan) verifies cross-file invariants |
| T-39-11 | I (info — accidental change to surviving hex values) | The 2 surviving entries `'light':'#5e81ac'`, `'dark':'#81a1c1'` | mitigate | The surviving hex values are byte-preserved — Phase 41 (Mono Zen) will intentionally retune; Phase 39 must leave them alone; verification grep confirms |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Surgically excise moss/slate/dusk from index.html L18 inline FOUC script (THM-07)</name>
  <files>
    - index.html
  </files>
  <read_first>
    - index.html (entire file — the FOUC IIFE is at L18 per CONTEXT canonical_refs; verify the exact line by reading)
    - .planning/phases/39-theme-simplification/39-PATTERNS.md §"Plan 4 — `index.html:18` — surgical edit (preserve minified IIFE shape)" — concrete before/after fragments
    - .planning/phases/39-theme-simplification/39-CONTEXT.md §decisions D-08 (surgical edit, no third touch-point)
    - .planning/phases/39-theme-simplification/39-02-SUMMARY.md (confirm Plan 02 deleted the theme.css [data-theme='moss'|'slate'|'dusk']:root blocks — so this plan's index.html allowlist + color map collapse is coherent with the CSS surface)
  </read_first>
  <action>
    Surgically excise exactly 2 fragments from `index.html` L18.

    1. Read `index.html`. Confirm the inline FOUC IIFE is at L18 (the value from CONTEXT canonical_refs as of 2026-05-21). If line number has shifted: locate by grep `grep -n "indexOf(t)" index.html` (the allowlist fragment is the load-bearing identifier).

    2. Edit Fragment 1 (allowlist):
       - Locate the substring `['light','dark','moss','slate','dusk'].indexOf(t)<0` within L18.
       - Replace with `['light','dark'].indexOf(t)<0`.
       - Preserve all surrounding bytes including the leading `if(t==='system'||!t||` and the trailing `)`.

    3. Edit Fragment 2 (color map):
       - Locate the substring `var c={'light':'#5e81ac','dark':'#81a1c1','moss':'#35a77c','slate':'#3760bf','dusk':'#f6c177'};` within L18.
       - Replace with `var c={'light':'#5e81ac','dark':'#81a1c1'};`.
       - Preserve all surrounding bytes including the leading `var c={` decision (it stays) and the trailing `;`.

    4. DO NOT change anything else in L18:
       - The matchMedia branch stays byte-identical.
       - The catch fallback stays byte-identical.
       - The IIFE wrapper `(function(){ ... })()` stays byte-identical.
       - All semicolons, var/const declarations, function calls, parentheses outside the 2 excision points stay byte-identical.

    5. DO NOT change anything else in the file:
       - The `<html lang="en">` declaration stays.
       - All `<head>` / `<meta>` / `<title>` / `<link>` elements stay byte-identical.
       - The `<body>` and any other inline scripts stay byte-identical.

    6. Build sanity: `pnpm build` exits 0 — Vite parses the modified HTML cleanly. The IIFE is syntactically valid (an inadvertent unbalanced brace/bracket would fail the build).

    7. Cross-file sanity: after this commit, `pnpm test src/styles/favicon.sync.test.ts --run` exits 0 (cross-file drift-guard sees matching 2-palette mappings on both sides — theme.css and index.html). This is the FIRST time in the Phase 39 plan sequence where the favicon.sync test is green end-to-end.

    8. Commit: `feat(39): excise moss/slate/dusk from index.html FOUC script (THM-07)`.
  </action>
  <verify>
    <automated>grep -c "moss\|slate\|dusk" index.html</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE "moss|slate|dusk" index.html` returns 0 matches.
    - `grep -n "var c={'light':'#5e81ac','dark':'#81a1c1'};" index.html` returns 1 match (the shrunk color map, exact byte sequence).
    - `grep -n "\\['light','dark'\\].indexOf(t)<0" index.html` returns 1 match (the shrunk allowlist).
    - `grep -n "'#5e81ac'" index.html` returns at least 1 match (the surviving light hex — preserved byte-for-byte).
    - `grep -n "'#81a1c1'" index.html` returns at least 1 match (the surviving dark hex — preserved byte-for-byte).
    - `grep -nE "#35a77c|#3760bf|#f6c177" index.html` returns 0 matches (the 3 deprecated accent-strong hex values are gone).
    - `git diff --stat index.html` reports a small diff (≤ ~2 line changes — the deletions land within L18, so the diff shows 1 line changed; if the editor preserved the line as a single line, the diff shows 1 removed + 1 added).
    - The single-line shape of L18 is preserved (no inserted newlines): `awk 'NR==18 {print length($0)}' index.html` returns a number ~50-80 chars shorter than before the deletion, but L18 is still a single line and L19 still has the same content it had before.
    - `pnpm build` exits 0.
    - `pnpm test src/styles/favicon.sync.test.ts --run` exits 0 (THM-07 cross-file drift-guard satisfied).
  </acceptance_criteria>
  <done>index.html L18 FOUC script has the 2 surgical excisions applied; no moss/slate/dusk references remain in index.html; light + dark hex values byte-preserved; IIFE wrapper + fallback branches + single-line shape preserved; build green; favicon.sync.test.ts green; THM-07 closed end-to-end.</done>
</task>

</tasks>

<verification>
After this single task:
- `grep -nE "moss|slate|dusk" index.html` returns 0 matches.
- `grep -n "'#5e81ac'" index.html` and `grep -n "'#81a1c1'" index.html` each return ≥1 match (surviving hex values preserved).
- `pnpm build` exits 0 — Vite parses the file cleanly.
- `pnpm test src/styles/favicon.sync.test.ts --run` exits 0 (THM-07 cross-file invariant now closed at both ends of the drift-guard).
- `pnpm tsc --noEmit` exits 0 (index.html is not TS-typed; this verification is for completeness).

One atomic commit scoped `(39)`: `feat(39): excise moss/slate/dusk from index.html FOUC script (THM-07)`.
</verification>

<success_criteria>
- index.html L18 FOUC IIFE has 2-token allowlist (`['light','dark']`) and 2-entry color map (`{'light':'#5e81ac','dark':'#81a1c1'}`).
- IIFE wrapper, matchMedia branch, catch fallback, `data-theme` write timing all byte-preserved.
- Light + dark surviving hex values byte-unchanged.
- THM-07 closed end-to-end (favicon.sync.test.ts green; index.html aligned to 2-palette).
- Returning-user pre-paint behavior for a deprecated persisted value is correct (fallback to matchMedia → OS-active; no FOUC).
- Tiger Style: 1 atomic commit this plan.
</success_criteria>

<output>
After completion, create `.planning/phases/39-theme-simplification/39-04-SUMMARY.md` summarising:
- The exact before/after byte-sequence of the 2 surgical excisions (allowlist + color map).
- Confirmation of preservation: IIFE wrapper, matchMedia branch, catch fallback, `data-theme` write timing, single-line shape, surviving hex values.
- D-08 satisfied (2 touch-points, no third).
- THM-07 closed end-to-end (the cross-file drift-guard from Plan 02 + the index.html alignment here).
- Returning-user behavior verification (a persisted `theme: 'moss'` resolves to OS-active in pre-paint via matchMedia fallback; post-mount coerceTheme re-persists as `'system'`).
- Pointer forward to Plan 05 (drift-guard test) — the final wave 3 plan.
- Tiger Style: 1 atomic commit this plan.
</output>
</content>
</invoke>