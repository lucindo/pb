---
phase: 39-theme-simplification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/domain/settings.ts
  - src/storage/prefs.test.ts
autonomous: true
requirements:
  - THM-01
  - THM-02
  - THM-03
  - THM-04
  - THM-05
requirements_addressed:
  - THM-01
  - THM-02
  - THM-03
  - THM-04
  - THM-05
must_haves:
  truths:
    - "src/domain/settings.ts exports `ThemeId = 'light' | 'dark' | 'system'` exactly — no `moss | slate | dusk` literal types remain (THM-01..03)."
    - "src/domain/settings.ts exports `THEME_OPTIONS = ['light', 'dark', 'system'] as const satisfies readonly ThemeId[]` — 3 entries only (THM-04 type-level half)."
    - "`isValidTheme` body is unchanged (one-line `typeof === 'string' && THEME_OPTIONS.includes(v)`); `DEFAULT_THEME` stays `'system'` (per D-01 — no body edits, return-union narrows via THEME_OPTIONS shrink)."
    - "`coerceTheme(raw)` in src/storage/prefs.ts now returns `'system'` for any of `'moss' | 'slate' | 'dusk'` (because `isValidTheme(deprecated)` returns false against the narrowed THEME_OPTIONS) — THM-05 read-side mechanism."
    - "src/storage/prefs.test.ts contains a new it() block named per D-02 that seeds `localStorage[STATE_KEY]` with each of `theme: 'moss'`, `'slate'`, `'dusk'` (3 iterations or 3 cases) and asserts `loadPrefs().theme === 'system'` (THM-05 read-coerce half)."
    - "src/storage/prefs.test.ts contains a second new it() block (round-trip) that seeds `theme: 'moss'`, calls `loadPrefs()`, then `savePrefs(loaded)`, then re-reads the raw `localStorage[STATE_KEY]` JSON and asserts `parsed.prefs.theme === 'system'` (THM-05 re-persist half)."
    - "No `STATE_VERSION` bump (per D-01 — Phase 8 D-01 envelope tolerance + per-field coercer carries this, same stance as Phase 38)."
    - "`pnpm tsc --noEmit` exits 0 INSIDE this plan's commit boundary IS NOT REQUIRED — downstream consumers (faviconPalette.ts `Record<Exclude<ThemeId,'system'>, string>` literal, theme.contrast.test.ts `THEME_05_FLOORS` literal, strings.ts `themes` block, index.html and CSS surfaces) will fail compile until Plans 02/03/04 land. tsc green is restored across the wave 2 commits."
    - "`pnpm test src/storage/prefs.test.ts --run` exits 0 — the new THM-05 cases pass against the current source (coerceTheme already routes invalid → DEFAULT_THEME)."
    - "D-01 satisfied: ThemeId union narrowed to 3 members; THEME_OPTIONS shrunk to 3 entries; coerceTheme body unchanged; no STATE_VERSION bump."
    - "D-02 satisfied: two-level THM-05 regression test (read-coerce + round-trip) added up-front, matching the Phase 38 VAR-05 forward-compat pattern (commit `4bd5e78` analog)."
  artifacts:
    - path: src/domain/settings.ts
      provides: "ThemeId union narrowed to 'light' | 'dark' | 'system'; THEME_OPTIONS list shrunk to 3 entries; DEFAULT_THEME = 'system' unchanged; isValidTheme body unchanged."
      forbids: "['\"]moss['\"]|['\"]slate['\"]|['\"]dusk['\"]"
    - path: src/storage/prefs.test.ts
      provides: "Two new it() cases locking THM-05: (1) read-coerce of deprecated values to 'system' across moss/slate/dusk; (2) read-coerce → savePrefs → re-read raw JSON asserts persisted theme === 'system'."
      contains: "THM-05"
      min_lines: 110
  key_links:
    - from: src/domain/settings.ts
      to: src/storage/prefs.ts
      via: "isValidTheme(raw) consumer inside coerceTheme — once THEME_OPTIONS shrinks, isValidTheme('moss'|'slate'|'dusk') returns false and coerceTheme routes to DEFAULT_THEME ('system')"
      pattern: "isValidTheme\\(raw\\)\\s*\\?\\s*raw\\s*:\\s*DEFAULT_THEME"
    - from: src/storage/prefs.test.ts
      to: src/storage/prefs.ts
      via: "loadPrefs() reads STATE_KEY, runs coercePrefs → coerceTheme, returns coerced value; savePrefs(prefs) writes the coerced value back to STATE_KEY — the THM-05 round-trip case exercises this whole loop"
      pattern: "loadPrefs\\(\\)|savePrefs\\(loaded\\)"
---

<objective>
Narrow the `ThemeId` union in `src/domain/settings.ts` from `'light' | 'dark' | 'system' | 'moss' | 'slate' | 'dusk'` to `'light' | 'dark' | 'system'`; shrink `THEME_OPTIONS` from 6 to 3 entries; leave `isValidTheme` and `DEFAULT_THEME` bodies untouched (the union narrowing is what flips `isValidTheme('moss'|'slate'|'dusk')` to `false`, and `coerceTheme` already routes invalid input to `DEFAULT_THEME`). In the same plan, add the THM-05 two-level regression test to `src/storage/prefs.test.ts`: (a) read-coerce — seed `localStorage` with each of `moss`/`slate`/`dusk` and assert `loadPrefs().theme === 'system'`; (b) round-trip — seed `moss`, `loadPrefs()` then `savePrefs(loaded)`, re-read the raw JSON, assert `parsed.prefs.theme === 'system'` (locks the re-persist contract). Closes THM-01..04 (type-level deletion) and THM-05 (read + re-persist contract).

Purpose: Plan 01 is the keystone — every other Phase 39 plan depends on this union shrinking. After this commit, the `Record<Exclude<ThemeId, 'system'>, X>` shape in `faviconPalette.ts` and `theme.contrast.test.ts` will TypeScript-fail compile against `moss`/`slate`/`dusk` literal entries, forcing those table literals to collapse in Plan 02. The `UiStrings.themes` block in `strings.ts` becomes type-incoherent (3 extra fields), forcing the i18n clean-cut in Plan 03. The drift-guard test in Plan 05 only fires AFTER all four surface deletions land in Plans 02/03/04.

Note: this plan intentionally LEAVES `tsc --noEmit` broken at its commit boundary because downstream literals (faviconPalette / theme.contrast.test / strings.ts / theme.css / index.html) still contain `moss`/`slate`/`dusk` keys/blocks. Wave 2 plans (02/03/04) restore tsc/test green. This matches Phase 38 Plan 01's pattern (BLOCKER fix was documented mid-plan).

Per CONTEXT D-02 and the Phase 38 VAR-05 analog (commit `4bd5e78`): the THM-05 regression test is captured up-front to avoid a retroactive validation cycle.

No `STATE_VERSION` bump (CONTEXT D-01 — Phase 8 D-01 envelope tolerance preserves the deprecated `moss`/`slate`/`dusk` value on disk until the next `savePrefs` overwrites it with `'system'`). Same stance as Phase 38.

Output: 2 surgically edited files (settings.ts ~9-line union/options edit; prefs.test.ts ~40 added lines for the 2 new it() blocks).
</objective>

<execution_context>
@$HOME/.claude-personal-personal/get-shit-done/workflows/execute-plan.md
@$HOME/.claude-personal-personal/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/39-theme-simplification/39-CONTEXT.md
@.planning/phases/39-theme-simplification/39-PATTERNS.md
@.planning/phases/38-variant-removal/38-CONTEXT.md
@.planning/phases/38-variant-removal/38-02-SUMMARY.md
@src/domain/settings.ts
@src/storage/prefs.ts
@src/storage/prefs.test.ts

<interfaces>
<!-- Key contracts the executor must respect. Sourced from src/ and the Phase 38 analog. -->

From src/domain/settings.ts (current, L97-105 — the surgical edit target):
- `export type ThemeId = 'light' | 'dark' | 'system' | 'moss' | 'slate' | 'dusk'`
- `export const THEME_OPTIONS = ['light', 'dark', 'system', 'moss', 'slate', 'dusk'] as const satisfies readonly ThemeId[]`
- `export function isValidTheme(v: unknown): v is ThemeId { return typeof v === 'string' && (THEME_OPTIONS as readonly string[]).includes(v) }`
- `export const DEFAULT_THEME: ThemeId = 'system'`

After this plan:
- `export type ThemeId = 'light' | 'dark' | 'system'`
- `export const THEME_OPTIONS = ['light', 'dark', 'system'] as const satisfies readonly ThemeId[]`
- `isValidTheme` body unchanged
- `DEFAULT_THEME: ThemeId = 'system'` unchanged

From src/storage/prefs.ts (relevant — no body edits):
- `coerceTheme(raw: unknown): ThemeId { return isValidTheme(raw) ? raw : DEFAULT_THEME }`
- `coerceTheme` is called inside `coercePrefs` which is called inside `loadPrefs`.

From src/storage/prefs.test.ts current state (imports L1-24):
- Already imports `STATE_KEY` (from './storage'), `loadPrefs`, `savePrefs`, `DEFAULT_PREFS`, `coercePrefs`, `coerceTheme`, etc.
- `beforeEach(() => window.localStorage.clear())` at L26.
- No edit needed to imports — `STATE_KEY`, `loadPrefs`, `savePrefs` are already in scope.

From .planning/phases/38-variant-removal/38-02-SUMMARY.md (the Phase 38 VAR-05 analog block — the pattern to mirror):
- Phase 38 added a similar forward-compat round-trip test to prefs.test.ts at end-of-phase (commit `4bd5e78`).
- Phase 39 captures the equivalent up-front per CONTEXT §specifics.

Phase 39 THM-05 test structure (two it() cases — copy structurally from PATTERNS §"Phase 39 D-02 test"):

```typescript
it('coerces deprecated persisted theme values to "system" on read — THM-05 (CONTEXT D-02)', () => {
  for (const deprecated of ['moss', 'slate', 'dusk']) {
    window.localStorage.setItem(STATE_KEY, JSON.stringify({
      version: 1,
      prefs: { theme: deprecated, timbre: 'bowl', cue: 'arrow', locale: 'en' },
    }))
    expect(loadPrefs().theme).toBe('system')
  }
})

it('re-persists deprecated theme as "system" on the next savePrefs call — THM-05 round-trip (CONTEXT D-02)', () => {
  window.localStorage.setItem(STATE_KEY, JSON.stringify({
    version: 1,
    prefs: { theme: 'moss', timbre: 'bowl', cue: 'arrow', locale: 'en' },
  }))
  const loaded = loadPrefs()
  expect(loaded.theme).toBe('system')
  savePrefs(loaded)
  // eslint-disable-next-line @typescript-eslint/no-non-null-assertion
  const raw = JSON.parse(window.localStorage.getItem(STATE_KEY)!) as { prefs: { theme: string } }
  expect(raw.prefs.theme).toBe('system')
})
```

Reason executor must NOT change the test pattern beyond renames: this case mirrors the Phase 38 VAR-05 commit `4bd5e78` shape almost verbatim. Any deviation must be flagged and approved.

CORRECTION TO BOUNDARY (analogous to Phase 38 Plan 01): the prefs.test.ts file may also iterate `THEME_OPTIONS` in an existing case (e.g. "every THEME_OPTIONS value round-trips through coercePrefs"). That existing loop is loop-invariant — it iterates whatever THEME_OPTIONS contains, so it auto-shrinks from 6 → 3. NO surgical strip is needed there. Verify with `grep -n "THEME_OPTIONS" src/storage/prefs.test.ts` during the read step.

The prefs.test.ts file currently has NO literal `moss`/`slate`/`dusk` strings (verified by 39-PATTERNS.md). The new THM-05 cases will be the only such references in the file — and they live inside the test, not in production code, so the drift-guard from Plan 05 ignores them via the `.test.ts` filename filter.
</interfaces>
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Persisted localStorage envelope → coercePrefs | A returning user's `theme: 'moss'/'slate'/'dusk'` value crosses into the in-memory pref object; coerceTheme is the trust boundary that maps invalid → DEFAULT_THEME |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-39-01 | T (tampering — persisted theme value) | src/storage/prefs.ts coerceTheme path | mitigate | The narrowed THEME_OPTIONS makes `isValidTheme('moss')` return false; `coerceTheme` routes to DEFAULT_THEME ('system'); THM-05 read-coerce + round-trip test in prefs.test.ts locks the behavior on read AND on re-persist |
| T-39-02 | I (info — silent acceptance of invalid persisted value) | coerceTheme | accept | No new attack surface — `coerceTheme` already exists in production and the body is unchanged; the only change is the union narrowing in settings.ts, which makes more values route through the existing DEFAULT_THEME fallback. No code path added. |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Narrow ThemeId union and shrink THEME_OPTIONS in src/domain/settings.ts</name>
  <files>
    - src/domain/settings.ts
  </files>
  <read_first>
    - src/domain/settings.ts (entire file — verify line numbers; the 9-line `ThemeId` / `THEME_OPTIONS` / `isValidTheme` / `DEFAULT_THEME` block is at ~L97-105 per CONTEXT canonical_refs)
    - .planning/phases/39-theme-simplification/39-PATTERNS.md §"Plan 1 — `src/domain/settings.ts` — surgical edit (union narrowing)" — before/after excerpts
    - .planning/phases/39-theme-simplification/39-CONTEXT.md §decisions D-01 — locked stance: keep field, shrink union, no STATE_VERSION bump
    - .planning/phases/38-variant-removal/38-02-SUMMARY.md — Phase 38 settings.ts analog precedent (VisualVariantId block deletion)
  </read_first>
  <action>
    Surgically edit `src/domain/settings.ts` at the `ThemeId` / `THEME_OPTIONS` block (~L97-105 per CONTEXT canonical_refs — verify exact lines from the Read above).

    1. Locate the `export type ThemeId = ...` line. Replace the union members from `'light' | 'dark' | 'system' | 'moss' | 'slate' | 'dusk'` to `'light' | 'dark' | 'system'`. Drop exactly the three deprecated literals (moss, slate, dusk). Preserve all whitespace, leading export keyword, and trailing newlines.

    2. Locate the `export const THEME_OPTIONS = [...] as const satisfies readonly ThemeId[]` line. Replace the array literal from `['light', 'dark', 'system', 'moss', 'slate', 'dusk']` to `['light', 'dark', 'system']`. Preserve the `as const satisfies readonly ThemeId[]` suffix and export keyword.

    3. DO NOT touch the `isValidTheme(v: unknown): v is ThemeId { ... }` body — per D-01, the body is unchanged; the union narrowing is what flips moss/slate/dusk to invalid input.

    4. DO NOT touch `DEFAULT_THEME: ThemeId = 'system'` — value and type unchanged.

    5. Comment-debt sweep on settings.ts: if any nearby comment enumerates the 5 deprecated themes by name (e.g. "the 5 palette set"), update it to reflect the 3-option shape. Tiger Style WHY-only: a stale enumeration that no longer matches the union is a WHY-claim that no longer holds — update or delete per planner judgment.

    6. DO NOT touch any other identifier or block in settings.ts (the other 4 unions — TimbreId, CueId, LocaleId, plus their `_OPTIONS` arrays — are untouched).

    7. DO NOT run `pnpm tsc --noEmit` as a gate at this commit — it WILL fail until Plan 02's faviconPalette + theme.contrast.test literal collapse lands and Plan 03's strings.ts UiStrings strip lands. This is intentional and matches Phase 38 Plan 01's "tsc green deferred to the wave 2 cumulative state" pattern.

    8. Commit with the conventional message scoped `(39)`: `feat(39): narrow ThemeId union to 3 options (THM-01..04)`.
  </action>
  <verify>
    <automated>grep -nE "^export (type ThemeId|const THEME_OPTIONS)" src/domain/settings.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "export type ThemeId" src/domain/settings.ts` returns exactly 1 line and that line reads `export type ThemeId = 'light' | 'dark' | 'system'` (no `moss | slate | dusk`).
    - `grep -n "export const THEME_OPTIONS" src/domain/settings.ts` returns exactly 1 line and that line contains `['light', 'dark', 'system']` followed by `as const satisfies readonly ThemeId[]`.
    - `grep -cE "'(moss|slate|dusk)'" src/domain/settings.ts` returns 0.
    - `grep -n "export const DEFAULT_THEME" src/domain/settings.ts` returns 1 line with value `'system'` (unchanged).
    - `grep -n "export function isValidTheme" src/domain/settings.ts` returns 1 line; the function body still uses `THEME_OPTIONS` and `typeof === 'string'` (no behavior change).
    - `git diff --stat src/domain/settings.ts` reports a small diff (≤ ~15 line changes including comment-debt updates; if substantially larger, the executor over-edited).
    - `pnpm tsc --noEmit` is NOT required to pass at this commit boundary (downstream literals fail until wave 2). This is documented behavior, not a regression.
  </acceptance_criteria>
  <done>ThemeId union is `'light' | 'dark' | 'system'`, THEME_OPTIONS contains 3 entries in that order, isValidTheme and DEFAULT_THEME are byte-identical to before in body/value, and the commit is in place with no other edits in settings.ts.</done>
</task>

<task type="auto">
  <name>Task 2: Add the two-level THM-05 regression test to src/storage/prefs.test.ts</name>
  <files>
    - src/storage/prefs.test.ts
  </files>
  <read_first>
    - src/storage/prefs.test.ts (entire file — confirm import block at L1-24 already has `STATE_KEY`, `loadPrefs`, `savePrefs`, and that `beforeEach` clears localStorage)
    - src/storage/prefs.ts (the surrounding `coercePrefs` / `coerceTheme` / `loadPrefs` / `savePrefs` definitions — confirm coerceTheme body is `return isValidTheme(raw) ? raw : DEFAULT_THEME`)
    - .planning/phases/39-theme-simplification/39-PATTERNS.md §"Plan 1 — `src/storage/prefs.test.ts` — surgical edit + ADD THM-05 round-trip (D-02)" — verbatim two-block test pattern
    - .planning/phases/39-theme-simplification/39-CONTEXT.md §decisions D-02 — two-level regression locked
    - .planning/phases/38-variant-removal/38-02-SUMMARY.md (Phase 38 VAR-05 analog — the up-front lock approach)
  </read_first>
  <action>
    Surgically edit `src/storage/prefs.test.ts` to ADD two new it() cases that lock THM-05. Do NOT modify any other existing test in this file (existing iterations over `THEME_OPTIONS` are loop-invariant and auto-shrink to 3 entries — no edit needed there).

    1. Read the file. Confirm:
       - `STATE_KEY` is imported at L14 from `./storage` (no new import needed).
       - `loadPrefs`, `savePrefs` are imported at L8-10 from `./prefs` (no new import needed).
       - `beforeEach(() => window.localStorage.clear())` is at L26 (so each new it() case starts with a clean localStorage).
       - No literal `moss`/`slate`/`dusk` strings exist in the file today (the new cases are the only references; .test.ts filename excludes the file from the Plan 05 drift-guard).

    2. Locate a sensible insertion point — at the end of the existing `describe('coerceTheme', ...)` block OR as a new top-level `describe('THM-05 forward-compat (CONTEXT D-02)', ...)` block at the END of the file (planner's discretion; the file's existing structure should guide the choice). The Phase 38 VAR-05 case landed at the end of a relevant describe — mirror that placement style.

    3. ADD the first it() case — read-coerce loop:
       - Name: `'coerces deprecated persisted theme values to "system" on read — THM-05 (CONTEXT D-02)'`.
       - Iterate over `['moss', 'slate', 'dusk']` (use `for (const deprecated of [...])`).
       - For each iteration: `window.localStorage.setItem(STATE_KEY, JSON.stringify({ version: 1, prefs: { theme: deprecated, timbre: 'bowl', cue: 'arrow', locale: 'en' } }))`.
       - Then assert `expect(loadPrefs().theme).toBe('system')`.
       - The seeded `timbre`/`cue`/`locale` values must be valid current values (per src/domain/settings.ts current options — `'bowl'`, `'arrow'`, `'en'`). Verify by reading the surrounding existing tests.

    4. ADD the second it() case — round-trip:
       - Name: `'re-persists deprecated theme as "system" on the next savePrefs call — THM-05 round-trip (CONTEXT D-02)'`.
       - Seed `localStorage[STATE_KEY]` with `{ version: 1, prefs: { theme: 'moss', timbre: 'bowl', cue: 'arrow', locale: 'en' } }`.
       - `const loaded = loadPrefs()`.
       - `expect(loaded.theme).toBe('system')`.
       - `savePrefs(loaded)`.
       - `const raw = JSON.parse(window.localStorage.getItem(STATE_KEY)!) as { prefs: { theme: string } }` (use `eslint-disable-next-line @typescript-eslint/no-non-null-assertion` if the project lints non-null assertions strictly).
       - `expect(raw.prefs.theme).toBe('system')`.

    5. DO NOT modify any other test case. DO NOT add or remove imports beyond what is already present.

    6. Run `pnpm test src/storage/prefs.test.ts --run`. The 2 new cases MUST pass against the current Plan 01 state — `coerceTheme` is unchanged in body; the union narrowing from Task 1 makes `isValidTheme('moss'|'slate'|'dusk')` return false; the routing to DEFAULT_THEME ('system') is exercised. If a case fails, do NOT loosen the assertion — debug the seed shape or imports.

    7. Commit with: `test(39): lock THM-05 read-coerce + round-trip (CONTEXT D-02)`.

    8. End-of-task verification (Tiger Style): `pnpm test src/storage/prefs.test.ts --run` exits 0. Other suites may still be red (faviconPalette test, contrast test, strings test, etc.) — those land in Plans 02/03.
  </action>
  <verify>
    <automated>pnpm test src/storage/prefs.test.ts --run</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "THM-05" src/storage/prefs.test.ts` returns at least 2 lines (the two new it() case names).
    - `grep -nE "for \(const deprecated of \[['\"]moss['\"], ['\"]slate['\"], ['\"]dusk['\"]\]\)" src/storage/prefs.test.ts` returns at least 1 line (the read-coerce loop).
    - `grep -n "savePrefs(loaded)" src/storage/prefs.test.ts` returns at least 1 line (the round-trip half).
    - `grep -n "raw.prefs.theme" src/storage/prefs.test.ts` returns at least 1 line (the persisted-JSON re-read).
    - `pnpm test src/storage/prefs.test.ts --run` exits 0 with all cases passing (including the 2 new THM-05 cases).
    - `git diff --stat src/storage/prefs.test.ts` reports a small additive diff (≤ ~45 added lines for the 2 new it() blocks; if substantially larger, the executor added scaffolding beyond the spec).
    - The file's existing `THEME_OPTIONS` iteration loops (if any) are UNCHANGED — `git diff src/storage/prefs.test.ts | grep -E "^[+-].*THEME_OPTIONS"` returns 0 (the loops auto-shrink to 3 entries without code edits).
  </acceptance_criteria>
  <done>Two new it() cases exist in prefs.test.ts implementing the THM-05 read-coerce loop and the THM-05 round-trip; both pass against the post-Task-1 state of settings.ts; no other test case is modified; the persisted-disk JSON re-read confirms the re-persisted theme is `'system'`.</done>
</task>

</tasks>

<verification>
After both tasks complete:
- `grep -E "'(moss|slate|dusk)'" src/domain/settings.ts` returns 0 matches.
- `grep -n "export type ThemeId" src/domain/settings.ts` reads exactly `export type ThemeId = 'light' | 'dark' | 'system'`.
- `grep -c "THM-05" src/storage/prefs.test.ts` returns ≥ 2.
- `pnpm test src/storage/prefs.test.ts --run` exits 0.
- `pnpm tsc --noEmit` is EXPECTED to be red (downstream literals in faviconPalette / theme.contrast.test / strings.ts still contain moss/slate/dusk keys) — wave 2 plans (02/03/04) restore it.
- Two atomic commits scoped `(39)`: `feat(39): narrow ThemeId union to 3 options (THM-01..04)` and `test(39): lock THM-05 read-coerce + round-trip (CONTEXT D-02)`.
</verification>

<success_criteria>
- ThemeId is `'light' | 'dark' | 'system'` and THEME_OPTIONS has 3 entries.
- coerceTheme body unchanged; DEFAULT_THEME value unchanged.
- THM-05 two-level regression test added up-front; both halves pass.
- No STATE_VERSION bump anywhere.
- THM-01, THM-02, THM-03 closed at the type-level; THM-04 closed at the option-list level (the ThemePicker render auto-shrinks via Plan 03); THM-05 closed end-to-end (read-coerce + re-persist).
</success_criteria>

<output>
After completion, create `.planning/phases/39-theme-simplification/39-01-SUMMARY.md` summarising:
- The exact before/after of settings.ts (ThemeId union shrunk, THEME_OPTIONS shrunk; isValidTheme/DEFAULT_THEME unchanged in body/value).
- The two new it() cases added to prefs.test.ts with their exact names; the seed shapes used; the assertion shapes.
- Confirmation that the new cases pass; explicit acknowledgment that `tsc --noEmit` is expected red until Plans 02/03/04 land (mirroring Phase 38 Plan 01's deferred-green pattern).
- D-01 + D-02 satisfied; no STATE_VERSION bump; Phase 8 D-01 envelope tolerance carries the on-disk deprecated value until the next savePrefs call.
- THM-01..05 status: closed at the source level; THM-06/07/08 still pending (Plan 02); THM-04's render-level half pending (Plan 03's ThemePicker.test.tsx confirms 3 options).
- Pointer forward to Plan 02 (CSS + faviconPalette + contrast test + favicon.sync test) — the type literals there will TS-fail until they collapse to 2 entries.
- Tiger Style: 2 atomic commits this plan.
</output>
</content>
</invoke>