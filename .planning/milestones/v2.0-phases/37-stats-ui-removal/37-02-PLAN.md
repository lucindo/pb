---
phase: 37-stats-ui-removal
plan: 02
type: execute
wave: 2
depends_on:
  - 37-01
files_modified:
  - src/storage/practices.ts
  - src/storage/practices.test.ts
  - src/storage/format.ts
  - src/storage/format.test.ts
autonomous: true
requirements:
  - STATS-03
  - STATS-04
must_haves:
  truths:
    - "src/storage/practices.ts no longer exports resetPracticeStats."
    - "src/storage/format.ts no longer exports formatLastSession; orphaned formatters (formatLastSessionDate, formatLastSessionDuration) are also deleted after grep-verifying no remaining consumers."
    - "src/storage/practices.test.ts contains a STATS-04 regression block with one it case per practice (resonant / stretch / naviKriya); each case calls recordXSession then loadEnvelope/loadPractices and asserts totalSessions+1, totalElapsedSeconds+N, lastSessionAtMs set to deps.now()."
    - "recordResonantSession, recordStretchSession, recordNaviKriyaSession signatures and persistence behavior are unchanged (D-05 invariant)."
    - "STATE_VERSION is unchanged (still v3 per src/storage/storage.ts; D-05 lock)."
    - "D-02: resetPracticeStats is deleted from src/storage/practices.ts plus its tests — no UI consumers remain after ResetStatsDialog deletion."
    - "D-03: formatLastSession is deleted from src/storage/format.ts plus its tests; orphan formatters (formatLastSessionDate, formatLastSessionDuration) are removed after grep-verifying zero remaining consumers."
    - "D-08: STATS-04 regression test lives in src/storage/practices.test.ts (one it case per practice — resonant / stretch / naviKriya — calling recordXSession then loadEnvelope and asserting totalSessions+1, totalElapsedSeconds+N, lastSessionAtMs set); no React render, no integration smoke."
  artifacts:
    - path: src/storage/practices.ts
      provides: "Per-practice settings + stats persistence with the unused resetPracticeStats removed."
      forbids: "export function resetPracticeStats"
    - path: src/storage/format.ts
      provides: "Pure display formatters with formatLastSession removed and any orphaned helpers (formatLastSessionDate, formatLastSessionDuration) deleted after grep verification."
      forbids: "formatLastSession\\b"
    - path: src/storage/practices.test.ts
      provides: "STATS-04 record-and-persist regression block — one it per practice; asserts the envelope is incremented losslessly across the v3 practices.{resonant,stretch,naviKriya}.stats slices."
      contains: "STATS-04 record-and-persist regression"
  key_links:
    - from: src/storage/practices.test.ts (STATS-04 block)
      to: src/storage/practices.ts (recordResonantSession, recordStretchSession, recordNaviKriyaSession)
      via: "deps-injection { now: () => 1_700_000_000_000 } per existing recordResonantSession (Pitfall 3) analog"
      pattern: "recordResonantSession\\(\\d|recordStretchSession\\(\\d|recordNaviKriyaSession\\(\\d"
---

<objective>
Remove dead-code data-layer surfaces left behind by Plan 01 (per CONTEXT D-02, D-03) and add the STATS-04 regression test that locks the invariant in CONTEXT D-05 (per-practice recordXSession persistence is unchanged). Implements REQUIREMENTS STATS-04 and finishes STATS-03 (the data-layer half of the Reset stats removal — UI was excised in Plan 01).

Purpose: keep src/storage/ clean of unconsumed exports (CONTEXT D-02 / D-03 rationale: re-introduction will design fresh reset semantics from scratch; preserved scaffolding would be misleading), and lock the STATS-04 invariant with a unit-level regression test (CONTEXT D-08: no React render, no integration smoke — just envelope round-trip per practice).

Output: 2 functions removed from storage/, their tests removed, orphan formatters in format.ts verified-and-removed, one new top-level describe block (STATS-04) in src/storage/practices.test.ts.
</objective>

<execution_context>
@$HOME/.claude-personal-personal/get-shit-done/workflows/execute-plan.md
@$HOME/.claude-personal-personal/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/37-stats-ui-removal/37-CONTEXT.md
@.planning/phases/37-stats-ui-removal/37-PATTERNS.md
@.planning/phases/37-stats-ui-removal/37-01-SUMMARY.md

<interfaces>
<!-- Existing signatures and analogs the executor will mirror. -->

From src/storage/practices.ts (to delete):
- L333-349: `// Pitfall 4: practice-scoped reset.\n// (3 comment lines)\nexport function resetPracticeStats(practice: PracticeId, deps: StorageDeps = {}): void { ... }`

From src/storage/practices.test.ts (to delete):
- L17: import `resetPracticeStats,` inside the multi-import block from './practices'
- L320-348: `describe('resetPracticeStats (Pitfall 4)', ...)` — 2 it cases + a `seedBothPractices` helper. Delete the entire describe and the helper.
- L515-532: `describe("resetPracticeStats('stretch') (Phase 34)", ...)` — 1 it case.

From src/storage/format.ts (to delete):
- L67-71: `// D-08: "Last: May 7 · 10 min" — null when stats has no last-session data.\nexport function formatLastSession(stats: PersistedStats, now: () => number = Date.now): string | null { ... }`
- After confirming via grep that the only remaining consumer of formatLastSessionDate (L55-60) and formatLastSessionDuration (L63-65) is the just-deleted formatLastSession (because StatsFooter.tsx — the only other consumer per current code — was deleted in Plan 01), also delete those two functions.
- Verify formatTotalMinutes (L40-45) and formatSessionCount (L48-50) BEFORE deleting — these may have other consumers besides StatsFooter (run grep). Per PATTERNS §"Delete src/storage/format.ts:formatLastSession", these likely have no other consumer either, so verify and delete if orphaned.

From src/storage/format.test.ts (to delete):
- L4-8: any imports for the deleted functions
- L11-43: `describe('formatTotalMinutes (D-06)', ...)` — delete if formatTotalMinutes is deleted
- L45-52: `describe('formatSessionCount (D-06 singular/plural)', ...)` — delete if formatSessionCount is deleted
- L54-78: `describe('formatLastSessionDate (D-05)', ...)` — delete (formatLastSessionDate is being deleted)
- L80-87: `describe('formatLastSessionDuration (D-07)', ...)` — delete
- L89-115: `describe('formatLastSession (composition + null guard)', ...)` — delete

From src/storage/practices.test.ts (existing pattern to copy for the new STATS-04 block — at L240-265):
- `describe('recordResonantSession (Pitfall 3 / T-30-08)', () => { it('increments only practices.resonant.stats and leaves naviKriya untouched', () => { const next = recordResonantSession(40_000, false, { now: () => 1_700_000_000_000 }); ... }) })`
- The `loadPractices()` selector returns `{ resonant: { settings, stats }, stretch: { settings, stats }, naviKriya: { settings, stats } }`.
- ZERO_STATS, statsOf helper (L34-42), DEFAULT_SETTINGS / DEFAULT_STRETCH_SETTINGS / DEFAULT_NK_SETTINGS imports already present at L19-23.
- COUNT_THRESHOLD: any elapsedMs ≥ ~10s (the actual threshold is in stats.ts COUNT_THRESHOLD_MS); 40_000ms is safely above it (mirrors existing analog).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete resetPracticeStats from storage layer and its tests</name>
  <files>src/storage/practices.ts, src/storage/practices.test.ts</files>
  <read_first>
    - src/storage/practices.ts (entire file — to confirm L333-349 boundaries and that no other function in the file calls resetPracticeStats internally)
    - src/storage/practices.test.ts (entire file — to confirm the three sites and snapshot the import block at L17)
    - src/storage/index.ts (the barrel — verify `export * from './practices'` line is the only re-export hook; deleting resetPracticeStats from practices.ts automatically removes it from the barrel)
    - .planning/phases/37-stats-ui-removal/37-PATTERNS.md (§"Delete src/storage/practices.ts:resetPracticeStats")
    - .planning/phases/37-stats-ui-removal/37-CONTEXT.md (§decisions D-02)
  </read_first>
  <action>
    Edit src/storage/practices.ts — delete the entire function block at L333-349 per CONTEXT D-02 (no UI consumers remain after ResetStatsDialog deletion; re-introduction will design fresh reset semantics from scratch). This includes the preceding three-line `// Pitfall 4: practice-scoped reset.` WHY-comment (lines that introduce the function). Do NOT touch `coercePractices`, `readEnvelope`/`writeEnvelope` helpers, or any of the recordXSession functions — those remain in use.

    Edit src/storage/practices.test.ts — execute three deletions:
    1. Remove `resetPracticeStats,` from the import block at L17 (keep all other imports).
    2. Delete `describe('resetPracticeStats (Pitfall 4)', ...)` at L320-348 wholesale, including the `seedBothPractices` helper function inside it.
    3. Delete `describe("resetPracticeStats('stretch') (Phase 34)", ...)` at L515-532 wholesale.

    Verify via grep that no other test file in src/ imports resetPracticeStats — if any consumer surfaces (unlikely after Plan 01), delete the consumer reference.

    Run `npx tsc --noEmit && npx vitest run src/storage/practices.test.ts` — both must exit 0. tsc surfaces any missed consumer; vitest confirms the existing per-practice round-trip and recordXSession tests still pass.

    Commit message: `chore(37): delete resetPracticeStats from storage layer + tests (STATS-03 D-02)`.
  </action>
  <verify>
    <automated>grep -rE "resetPracticeStats" src/; test $? -eq 1 && npx tsc --noEmit && npx vitest run src/storage/practices.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rE "resetPracticeStats" src/ --include="*.ts" --include="*.tsx"` returns no matches (exit code 1).
    - `grep -n "Pitfall 4: practice-scoped reset" src/storage/practices.ts` returns no matches.
    - `npx tsc --noEmit` exits 0.
    - `npx vitest run src/storage/practices.test.ts` exits 0.
    - The remaining describe blocks in practices.test.ts (coerceActivePractice, coerceNaviKriyaSettings, coercePractices, loadPractices defaults, per-practice round-trips, recordResonantSession, recordNaviKriyaSession, recordStretchSession, v1→v2→v3 migration, coerceStretchSettings, saveStretchSettings round-trip) are byte-identical apart from the removed import — verifiable via `git diff src/storage/practices.test.ts` showing only the import line + the two describe deletions.
    - `git show main:src/storage/storage.ts | grep STATE_VERSION` and `grep STATE_VERSION src/storage/storage.ts` show the same value (D-05 lock — Plan 02 does not bump storage version).
  </acceptance_criteria>
  <done>resetPracticeStats no longer exists in the codebase, all referencing tests are deleted, and the rest of the per-practice persistence surface (recordXSession × 3, loadPractices, saveXSettings × 3) is untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Verify and delete orphaned format.ts helpers + their tests</name>
  <files>src/storage/format.ts, src/storage/format.test.ts</files>
  <read_first>
    - src/storage/format.ts (entire file — confirm L40-45, L48-50, L55-60, L63-65, L67-71)
    - src/storage/format.test.ts (entire file — confirm the describe boundaries L11-43, L45-52, L54-78, L80-87, L89-115)
    - .planning/phases/37-stats-ui-removal/37-PATTERNS.md (§"Delete src/storage/format.ts:formatLastSession — verify formatLastSessionDate orphan status")
    - .planning/phases/37-stats-ui-removal/37-CONTEXT.md (§decisions D-03; §canonical_refs line 89 caveat)
  </read_first>
  <action>
    Resolves PATTERNS caveat 2: confirm orphan status of helper formatters BEFORE deletion.

    Step 1 — Verify orphan status via grep. Run each command and record the result before editing:
    - `grep -rnE "formatLastSession\\b" src/ --include="*.ts" --include="*.tsx"` — expected: only format.ts (self) + format.test.ts (self) after Plan 01 removed StatsFooter and Task 1 here did not touch this function. If any other consumer is listed, STOP and add it to the deletion list before proceeding.
    - `grep -rnE "formatLastSessionDate\\b" src/ --include="*.ts" --include="*.tsx"` — expected: only format.ts (self definition + self use inside formatLastSession) + format.test.ts (self). Plan 01 deleted the StatsFooter.tsx import at its line 24.
    - `grep -rnE "formatLastSessionDuration\\b" src/ --include="*.ts" --include="*.tsx"` — expected: only format.ts (self) + format.test.ts (self).
    - `grep -rnE "formatTotalMinutes\\b" src/ --include="*.ts" --include="*.tsx"` — expected: only format.ts + format.test.ts after Plan 01. If any other consumer is listed (e.g. a stats-display view we missed), STOP and surface it as a Plan 01 gap — do not delete the function.
    - `grep -rnE "formatSessionCount\\b" src/ --include="*.ts" --include="*.tsx"` — expected: only format.ts + format.test.ts after Plan 01.

    Step 2 — Delete confirmed-orphan exports. For each function that grep confirmed orphan (consumer set = {self, self.test.ts}):
    1. Delete `formatLastSession` at L67-71 (CONTEXT D-03 lock — this is the primary delete-target).
    2. Delete `formatLastSessionDate` at L55-60 if orphaned.
    3. Delete `formatLastSessionDuration` at L63-65 if orphaned.
    4. Delete `formatTotalMinutes` at L40-45 if orphaned.
    5. Delete `formatSessionCount` at L48-50 if orphaned.
    6. If all five functions are deleted, the cached `Intl.DateTimeFormat` instances at L8-16 and the `dateFormatterFor` helper at L22-27 are dead — delete them too along with their WHY-comments. If only some are deleted, prune the unused helpers correspondingly.
    7. The `import type { PersistedStats } from './stats'` at L6 is only used by `formatLastSession`'s signature — if formatLastSession is deleted, delete that import.

    Step 3 — Delete the corresponding test describes in src/storage/format.test.ts:
    1. Remove the imports at L4-8 for the deleted function names.
    2. Delete each `describe` block whose subject function was deleted in Step 2:
       - `describe('formatTotalMinutes (D-06)', ...)` at L11-43
       - `describe('formatSessionCount (D-06 singular/plural)', ...)` at L45-52
       - `describe('formatLastSessionDate (D-05)', ...)` at L54-78
       - `describe('formatLastSessionDuration (D-07)', ...)` at L80-87
       - `describe('formatLastSession (composition + null guard)', ...)` at L89-115

    Step 4 — If all functions and all describes are deleted, format.test.ts may be reduced to an effectively-empty file. In that case, delete the file outright via `git rm src/storage/format.test.ts`. If only some describes are deleted, leave the file but ensure imports are pruned to match.

    Step 5 — If format.ts itself becomes effectively empty (no exports), delete it outright via `git rm src/storage/format.ts`. Then remove the `export * from './format'` line from src/storage/index.ts (the barrel at line 7). If the file retains any exports, leave it and its barrel re-export intact.

    Commit message: `chore(37): delete formatLastSession + orphan format.ts helpers (STATS-03 D-03)`.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx vitest run src/storage/</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rE "formatLastSession\\b" src/ --include="*.ts" --include="*.tsx"` returns no matches.
    - The deletion-or-keep decision for `formatTotalMinutes`, `formatSessionCount`, `formatLastSessionDate`, `formatLastSessionDuration` is justified by the grep results recorded in the SUMMARY (if kept, the executor states the surviving consumer; if deleted, the executor states the grep-confirmed orphan status).
    - If src/storage/format.ts was deleted, `test ! -f src/storage/format.ts` AND `grep -n "from './format'" src/storage/index.ts` returns no match. If retained, `grep -n "from './format'" src/storage/index.ts` still returns its match.
    - If src/storage/format.test.ts was deleted, `test ! -f src/storage/format.test.ts`. If retained, only describes for surviving exports are present.
    - `npx tsc --noEmit` exits 0.
    - `npx vitest run src/storage/` exits 0.
    - `grep -rnE "format\\.(ts|test)" src/storage/index.ts` reflects the post-deletion state (line removed if format.ts was deleted; line present otherwise).
  </acceptance_criteria>
  <done>format.ts and its tests reflect the actual post-Plan-01 consumer set — every export is either grep-confirmed-in-use or deleted; no unused-export lint noise; the storage barrel is consistent.</done>
</task>

<task type="auto">
  <name>Task 3: Add STATS-04 record-and-persist regression block</name>
  <files>src/storage/practices.test.ts</files>
  <read_first>
    - src/storage/practices.test.ts (entire file — specifically the existing analog at L240-265 `describe('recordResonantSession (Pitfall 3 / T-30-08)', ...)` and the imports at L1-23)
    - src/storage/practices.ts (recordResonantSession, recordStretchSession, recordNaviKriyaSession signatures — confirm the deps shape `{ now?: () => number }` is unchanged)
    - src/storage/stats.ts (PersistedStats shape, ZERO_STATS, COUNT_THRESHOLD_MS — to choose an elapsedMs that is safely above the threshold; the analog uses 40_000)
    - .planning/phases/37-stats-ui-removal/37-PATTERNS.md (§"src/storage/practices.test.ts — add STATS-04 regression block")
    - .planning/phases/37-stats-ui-removal/37-CONTEXT.md (§decisions D-05, D-08)
  </read_first>
  <action>
    Append a new top-level describe block at the END of src/storage/practices.test.ts named `describe('STATS-04 record-and-persist regression (CONTEXT D-05 / D-08)', () => { ... })` per CONTEXT D-08. Place AFTER all existing describes (after the `resetPracticeStats('stretch')` block has been deleted in Task 1 of this plan, the new tail is `describe('recordStretchSession (Phase 34 T-34-02)', ...)` ending at the prior L513) so the regression block reads as a phase-level invariant lock, not duplicate coverage.

    Inside the new describe, add three `it` cases — one per practice. Each case follows the existing recordResonantSession analog at L240-248 exactly:

    Case 1 — `it('recordResonantSession increments resonant slice losslessly across loadEnvelope', ...)`:
    - Call `recordResonantSession(40_000, false, { now: () => 1_700_000_000_000 })` and capture the return value.
    - Re-read via `loadPractices()` (the existing analog uses loadPractices; do NOT switch to loadEnvelope — keep parity with the analog and with WR-07).
    - Assert: `map.resonant.stats.totalSessions === 1`, `map.resonant.stats.totalElapsedSeconds === 40`, `map.resonant.stats.lastSessionAtMs === 1_700_000_000_000`, `map.resonant.stats.lastSessionDurationSeconds === 40`.
    - Assert the other slices are untouched: `map.stretch.stats.toEqual(ZERO_STATS)` and `map.naviKriya.stats.toEqual(ZERO_STATS)`.

    Case 2 — `it('recordStretchSession increments stretch slice losslessly across loadEnvelope', ...)`:
    - Call `recordStretchSession(40_000, false, { now: () => 1_700_000_000_000 })`.
    - Re-read via `loadPractices()`.
    - Assert the stretch slice has `totalSessions === 1`, `totalElapsedSeconds === 40`, `lastSessionAtMs === 1_700_000_000_000`, `lastSessionDurationSeconds === 40`.
    - Assert `map.resonant.stats.toEqual(ZERO_STATS)` and `map.naviKriya.stats.toEqual(ZERO_STATS)`.

    Case 3 — `it('recordNaviKriyaSession increments naviKriya slice losslessly across loadEnvelope', ...)`:
    - Call `recordNaviKriyaSession(60_000, 3, true, { now: () => 1_700_000_000_000 })`.
    - Re-read via `loadPractices()`.
    - Assert `map.naviKriya.stats.totalSessions === 1`, `totalElapsedSeconds === 60`, `lastSessionAtMs === 1_700_000_000_000`, `lastSessionDurationSeconds === 60`, `roundsCompleted === 3`.
    - Assert `map.resonant.stats.toEqual(ZERO_STATS)` and `map.stretch.stats.toEqual(ZERO_STATS)`.

    Use only existing imports (`recordResonantSession`, `recordStretchSession`, `recordNaviKriyaSession`, `loadPractices`, `ZERO_STATS` — all already imported at L4-23). Use the existing `beforeEach`/`afterEach` localStorage.clear() seam (L25-32) — do not add new fixtures.

    No React render, no integration smoke (CONTEXT D-08 explicit). No bump to STATE_VERSION (D-05 lock — verified by reading src/storage/storage.ts unchanged).

    Commit message: `test(37): STATS-04 record-and-persist regression locks D-05 invariant`.
  </action>
  <verify>
    <automated>npx vitest run src/storage/practices.test.ts -t "STATS-04"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "STATS-04 record-and-persist regression" src/storage/practices.test.ts` returns exactly one match (the new describe).
    - `npx vitest run src/storage/practices.test.ts -t "STATS-04"` exits 0 and reports 3 passing it cases.
    - `npx vitest run src/storage/practices.test.ts` exits 0 across the entire file (no pre-existing test was disturbed).
    - The new block uses `loadPractices()`, `recordResonantSession`, `recordStretchSession`, `recordNaviKriyaSession`, `ZERO_STATS` — all pre-existing imports; no new import lines were added.
    - `git show main:src/storage/storage.ts | grep "STATE_VERSION"` and the local file show the same numeric version (D-05 lock holds across Plans 01 + 02).
    - The new block is the LAST describe in the file (verify by checking the line count and that no describe appears after it).
  </acceptance_criteria>
  <done>STATS-04 invariant is locked by an automated regression: three practices, one it case each, asserting the recordXSession→loadPractices round-trip is lossless across the v3 practices.{resonant,stretch,naviKriya}.stats envelope without a STATE_VERSION bump.</done>
</task>

</tasks>

<verification>
After all three tasks complete:
- `npx tsc --noEmit` — exit 0.
- `npx vitest run` — exit 0 across the whole suite.
- `grep -rE "resetPracticeStats|formatLastSession\\b" src/` — no matches.
- `grep -n "STATS-04" src/storage/practices.test.ts` — one match (the new describe).
- `git diff --stat` — only the listed files modified; storage/storage.ts is untouched (D-05 lock).
</verification>

<success_criteria>
- resetPracticeStats is gone from src/storage/practices.ts and from every test file.
- formatLastSession is gone from src/storage/format.ts and from every test file.
- Orphaned helpers (formatLastSessionDate, formatLastSessionDuration, possibly formatTotalMinutes / formatSessionCount) are either grep-confirmed-orphan-and-deleted or grep-confirmed-still-in-use-and-kept; the decision is documented in the SUMMARY.
- STATS-04 regression block exists in src/storage/practices.test.ts with three it cases (one per practice).
- STATE_VERSION is unchanged.
- `npx tsc --noEmit && npx vitest run` exits 0.
</success_criteria>

<output>
After completion, create `.planning/phases/37-stats-ui-removal/37-02-SUMMARY.md` summarising:
- Functions deleted from src/storage/.
- Grep results for the orphan-verification step (record the actual grep output for formatTotalMinutes / formatSessionCount / formatLastSessionDate / formatLastSessionDuration) so the deletion decision is auditable.
- STATS-04 regression block — three it cases, the assertions, the deps fixture `{ now: () => 1_700_000_000_000 }`.
- Confirmation that STATE_VERSION in src/storage/storage.ts is unchanged (D-05 invariant lock).
- Closes STATS-03 (data-layer half — Plan 01 closed the UI half) and STATS-04.
- Pre-handoff for Plan 03: the drift-guard test runs LAST so it asserts the clean post-Plan-01/02 state; confirm by listing any forbidden-token grep hits remaining in src/components/ + src/app/ (excluding test files) — should be zero.
</output>
