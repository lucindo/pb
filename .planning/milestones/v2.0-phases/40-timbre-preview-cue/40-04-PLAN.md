---
phase: 40-timbre-preview-cue
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/40-timbre-preview-cue/40-HUMAN-UAT.md
autonomous: true
requirements: [PREV-01, PREV-03, PREV-05]
requirements_addressed: [PREV-01, PREV-03, PREV-05]
tags: [docs, human-uat, ios-safari, empirical-confirmation]

must_haves:
  truths:
    - "40-HUMAN-UAT.md exists with frontmatter status: pending"
    - "The document carries 4 empirical items (CONTEXT D-13) — cue correctness, mute irrelevance, rapid-tap overlap feel, iOS Safari cold-start"
    - "Each item has an explicit expected/result block so the operator can mark resolved per item"
    - "The iOS Safari cold-start item is flagged as HIGH-SIGNAL (closest to the v1.x carry-forward iOS audio recovery bug surface)"
  artifacts:
    - path: ".planning/phases/40-timbre-preview-cue/40-HUMAN-UAT.md"
      provides: "Operator-runnable empirical verification of PREV-01, PREV-03 (empirical confirmation of D-11 structural lock), PREV-05 (real-device latency), and D-08 (rapid-tap overlap feel)"
      contains: "iOS Safari"
  key_links:
    - from: ".planning/phases/40-timbre-preview-cue/40-HUMAN-UAT.md"
      to: "the operator (manual test runs on real hardware)"
      via: "doc-only — no executable code path"
      pattern: "## Tests"
---

<objective>
Create `.planning/phases/40-timbre-preview-cue/40-HUMAN-UAT.md` carrying the 4 empirical test items specified in CONTEXT D-13. The doc is the operator-runnable confirmation that the unit-test-locked invariants hold on real hardware — most importantly the iOS Safari cold-start path, which jsdom + FakeAudioContext cannot exercise.

Purpose: PREV-05 (≤100 ms latency) is locked structurally by the synchronous-call-path contract (D-12), but real-device latency confirmation lives here. PREV-03 ("preview plays when muted") is locked structurally by the import-graph drift-guard (D-11 / Plan 02), but the empirical "I muted and the preview still played" check lives here. PREV-01 audible correctness (right cue at A4) is verified by ear here — the unit tests can confirm the call shape but not the audible output.

Output: One markdown file under `.planning/phases/40-timbre-preview-cue/`. Doc-only — no source changes, no test additions.
</objective>

<execution_context>
@$HOME/.claude-personal/get-shit-done/workflows/execute-plan.md
@$HOME/.claude-personal/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/40-timbre-preview-cue/40-CONTEXT.md
@.planning/phases/40-timbre-preview-cue/40-PATTERNS.md

<!-- READ-ONLY analog file for the HUMAN-UAT.md shape. -->
@.planning/phases/37-stats-ui-removal/37-HUMAN-UAT.md

<interfaces>
<!-- Frontmatter + structure pattern. Extracted from the most recent HUMAN-UAT analog. -->

From .planning/phases/37-stats-ui-removal/37-HUMAN-UAT.md (lines 1-13):
  ---
  status: pending | resolved
  phase: <phase-slug>
  source: [<source-artifacts>]
  started: <ISO timestamp>
  updated: <ISO timestamp>
  ---

  ## Current Test
  [pending | resolved]

  ## Tests

  ### N. <test-id> — <test-name>
  expected: ...
  result: ...

From CONTEXT D-13 (the four required items):
  1. Cue correctness (PREV-01) — tap each of 4 timbres, confirm correct inhale cue at A4 (fundamentalHzIn = 440)
  2. Mute irrelevance (PREV-03 empirical) — mute via MuteToggle, then tap timbres → cues still audible
  3. Rapid-tap overlap feel (D-08 empirical) — tap rapidly across 3-4 timbres → polyphonic overlap is OK, no glitches
  4. iOS Safari cold-start (HIGH-SIGNAL) — fresh app, open Settings before any session, tap a timbre → cue plays (cold AudioContext + resume + first oscillator schedule on the platform that historically breaks audio invariants)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create 40-HUMAN-UAT.md with 4 empirical items (D-13)</name>
  <files>.planning/phases/40-timbre-preview-cue/40-HUMAN-UAT.md</files>
  <read_first>
    - .planning/phases/37-stats-ui-removal/37-HUMAN-UAT.md (full file — the structural twin for frontmatter + section headings + expected/result format)
    - .planning/phases/40-timbre-preview-cue/40-CONTEXT.md (D-13, D-08, D-01, D-02)
    - .planning/phases/40-timbre-preview-cue/40-PATTERNS.md (`40-HUMAN-UAT.md` section — frontmatter + 4-item adaptation per CONTEXT D-13)
  </read_first>
  <behavior>
    - The file contains YAML frontmatter with `status: pending`, `phase: 40-timbre-preview-cue`, `source: [40-CONTEXT.md, 40-VERIFICATION.md]` (or planner-chosen sources), `started: <ISO timestamp at creation time>`, `updated: <same ISO timestamp>`.
    - Below the frontmatter: a `## Current Test` block reading `[pending]`.
    - A `## Tests` heading followed by four `### N. <title>` items, each with explicit `expected:` and `result:` lines (initially blank or `pending`) so the operator fills them in on real hardware.
    - Item 4 (iOS Safari cold-start) is explicitly flagged as HIGH-SIGNAL in its body — this is the load-bearing manual check.
  </behavior>
  <action>
    Create `.planning/phases/40-timbre-preview-cue/40-HUMAN-UAT.md`. Mirror the frontmatter + structure of `.planning/phases/37-stats-ui-removal/37-HUMAN-UAT.md` verbatim.

    Frontmatter (YAML):
    - `status: pending` (operator flips to `resolved` after running each item on real hardware)
    - `phase: 40-timbre-preview-cue`
    - `source: [40-CONTEXT.md, 40-VERIFICATION.md]` (the artifacts this UAT confirms)
    - `started: <ISO-8601 timestamp at file-creation time>`
    - `updated: <same ISO-8601 timestamp>`

    Body (Markdown):
    - `## Current Test` block reading `[pending]`.
    - `## Tests` heading.
    - Four `### N. <title>` sub-headings, in order:

      ### 1. PREV-01 — Cue correctness across all four timbres
      Context: Each TIMBRE_PRESETS[timbre].fundamentalHzIn = 440 Hz (A4) per Phase 18 TIMBRE-05 / D-21.
      expected: Tap each of the four timbre options (Bowl, Bell, Sine, Flute) once in App Settings. Each tap plays a single inhale cue at A4, distinguishable by timbre (bowl is rich-harmonic, bell is sharper attack, sine is the pure fundamental, flute is the soft-attack airy envelope per Phase 35 AUDIO-01). No looping, no exhale cue, no double-trigger.
      result: <operator fills in: pass/fail per timbre + any notes>

      ### 2. PREV-03 (empirical confirmation of D-11 structural lock) — Mute irrelevance
      Context: PREV-03 is locked structurally by Plan 02's drift-guard (previewContext.ts imports no audioEngine). This item is the audible cross-check that the structural lock corresponds to real-world behavior.
      expected: Start a session briefly, mute via MuteToggle (in-session toggle), end the session. Open Settings (or trigger the timbre picker surface). Tap each of the four timbres. Cues remain audible regardless of the MuteToggle state from the prior session.
      result: <operator fills in: pass/fail + notes>

      ### 3. D-08 — Rapid-tap overlap feel
      Context: CONTEXT D-08 — brief polyphonic overlap during rapid auditioning is by design (matches in-session cueSynth queueing behavior at boundaries).
      expected: Tap rapidly across 3-4 different timbres in quick succession (e.g., bowl → bell → sine → flute within ~1 second). Cues overlap briefly — this is expected and feels expressive, not buggy. No silence gaps, no audible glitches/crackles, no app crashes, no console errors.
      result: <operator fills in: pass/fail + qualitative note on overlap feel>

      ### 4. PREV-05 + D-01 + D-02 — iOS Safari standalone-PWA cold-start (HIGH-SIGNAL)
      Context: This item exercises the cold AudioContext creation + resume + first oscillator schedule on the platform that historically breaks audio invariants (Phase 27 PWA-03, v1.x carry-forward iOS audio recovery bug). It is the highest-signal manual test in Phase 40 — explicitly flagged as such per CONTEXT specifics line 4.
      expected: On iOS Safari (preferably the installed standalone PWA, but Safari browser also works), fully close the app/tab. Re-open the app. WITHOUT starting any session, navigate to App Settings (Phase 43 surface) or the existing SettingsDialog (current v1.5 surface). Tap a timbre — the cue plays. Latency from tap to first audio sample feels imperceptible (well under 100 ms per PREV-05 — operator subjective judgment). Repeat for a second timbre — subsequent tap is even faster (singleton reuse per D-01).
      result: <operator fills in: pass/fail + device + iOS version + subjective latency note>

    Hard invariants:
    - Doc-only file. No code, no script, no executable assertion.
    - Initial status is `pending`, NOT `resolved` — the operator flips status after running.
    - Use the same heading levels (`## Current Test`, `## Tests`, `### N.`) as the 37 analog.
    - Item 4 explicitly labels itself HIGH-SIGNAL in its body so operators don't deprioritize it.
    - No NEVER place fenced code blocks inside this action.
  </action>
  <verify>
    <automated>test -f .planning/phases/40-timbre-preview-cue/40-HUMAN-UAT.md && head -10 .planning/phases/40-timbre-preview-cue/40-HUMAN-UAT.md && grep -c '^### ' .planning/phases/40-timbre-preview-cue/40-HUMAN-UAT.md</automated>
  </verify>
  <acceptance_criteria>
    - Source: `.planning/phases/40-timbre-preview-cue/40-HUMAN-UAT.md` exists.
    - Source: YAML frontmatter starts at line 1 and contains `status: pending`, `phase: 40-timbre-preview-cue`, `source:`, `started:`, `updated:` (5 keys minimum).
    - Source: file contains `## Current Test` heading followed by `[pending]`.
    - Source: file contains `## Tests` heading.
    - Source: file contains exactly four `### ` sub-headings (count via `grep -c '^### ' 40-HUMAN-UAT.md` returning `4`).
    - Source: file contains the substring `PREV-01` in item 1.
    - Source: file contains the substring `PREV-03` in item 2.
    - Source: file contains the substring `D-08` in item 3.
    - Source: file contains both `PREV-05` and `iOS Safari` AND `HIGH-SIGNAL` in item 4.
    - Source: each of the four items contains the substrings `expected:` and `result:`.
    - CLI: file is valid markdown (no syntax errors that would break the planning tooling) — confirmed by the file rendering correctly in any markdown viewer.
  </acceptance_criteria>
  <done>
    `.planning/phases/40-timbre-preview-cue/40-HUMAN-UAT.md` exists with `status: pending` frontmatter and the four D-13 items in the required structure. The operator can run each item on real hardware and flip the status to `resolved` after empirical confirmation.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → real hardware | The UAT items run on real devices (iOS Safari, commodity desktop). No code surface; trust boundary is the operator's testing diligence. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-40-11 | (n/a — doc artifact) | 40-HUMAN-UAT.md | accept | The artifact is a documentation file with no executable surface, no input handling, no network or storage interaction. The only "threat" is operator skipping the iOS Safari item — mitigated by the explicit HIGH-SIGNAL label and CONTEXT specifics line 4 calling it out. |

Severity ceiling: no executable surface → no STRIDE threats apply. Doc-only artifact. Gate passes trivially.
</threat_model>

<verification>
- `.planning/phases/40-timbre-preview-cue/40-HUMAN-UAT.md` exists.
- Frontmatter status is `pending` (not `resolved`) at creation.
- Four `### N.` test items present, covering PREV-01, PREV-03 empirical, D-08, and PREV-05 + iOS Safari cold-start.
- Item 4 explicitly carries the HIGH-SIGNAL label per CONTEXT specifics.
- Each item has expected/result blocks ready for the operator to fill in.
</verification>

<success_criteria>
- HUMAN-UAT.md created with the 4 D-13 items.
- Operator-runnable per-item testing surface established.
- PREV-01 audible correctness, PREV-03 empirical confirmation, PREV-05 real-device latency, and D-08 rapid-tap feel all have a manual-test home.
- Atomic commit `docs(40): add HUMAN-UAT.md` per Tiger Style PATTERNS precedent.
- Per-commit green gate: `tsc && lint && build && test` exit 0 (trivially — no source changes).
</success_criteria>

<output>
After completion, create `.planning/phases/40-timbre-preview-cue/40-04-SUMMARY.md` per the SUMMARY template — record: file added, frontmatter status (`pending`), the 4 item titles, the HIGH-SIGNAL item flagged, and the green-gate evidence (trivial — doc-only change).
</output>
