---
phase: 44-final-polish
plan: 03
type: execute
wave: 3
depends_on: ["44-01", "44-02"]
files_modified:
  - src/**/*.{ts,tsx}  # comments only — zero code behavior change
autonomous: true
requirements:
  - POLISH-04
user_setup: []

must_haves:
  truths:
    - "No narration-of-WHAT comments survive in `src/` (`// loop through the array`, `// uses useState to hold the count`, `// Phase 15 — render the dialog`-style)"
    - "All KEEP-able WHY-comments survive verbatim — constraints, invariants, surprising behavior, workarounds (analog example: `previewContext.ts:3-15` D-01/D-02 latency block; `content.no-removed-keys.test.ts:1-21` drift-guard self-description)"
    - "Sibling-pattern broad sweep applied (Item I lesson `80da948`): every deleted-component reference, every stale phase-narration marker, every 'scheduled for removal' / 'legacy modal' / 'deprecated' marker is dispositioned"
    - "Zero code behavior change — tests pass byte-equivalent to pre-sweep count from 44-02"
    - "Audit grep guards return zero (or all kept-with-rationale): deleted-component refs / stale phase-narration / scheduled-for-removal / TODO / FIXME / XXX / HACK markers"
  artifacts:
    - path: "src/**/*.{ts,tsx}"
      provides: "Narration-of-WHAT comments removed; WHY-comments preserved"
    - path: ".planning/phases/44-final-polish/44-03-SUMMARY.md"
      provides: "Per-category sweep counts + grep-guard evidence + per-cluster commit SHA"
  key_links:
    - from: "every kept WHY-comment"
      to: "the load-bearing invariant it encodes"
      via: "Tiger Style rule (constraints / invariants / surprising behavior / workarounds)"
      pattern: "//.*[Dd]on'?t |//.*[Mm]ust |//.*[Ww]hy |//.*[Bb]ecause |//.*[Ii]nvariant"
    - from: "every dropped narration-of-WHAT comment"
      to: "the code it described (which now stands on its own with tight naming)"
      via: "delete-when-restating discipline"
      pattern: "deleted: //.*Phase \\d+|deleted: //.*scheduled for removal"
---

<objective>
Sweep `src/**/*.{ts,tsx}` for narration-of-WHAT comments (Tiger Style "WHY-only" rule) and remove them; preserve every WHY-comment (constraints, invariants, surprising behavior, workarounds). The sweep is BROAD (per Item I lesson — Item H "applied the rule narrowly, not broadly", Item I caught what H missed). Zero code behavior change; tests pass unchanged.

Purpose: Close POLISH-04 (Tiger Style WHY-only comment audit). The mechanic mirrors Item I's commit `80da948` — comments only, zero code behavior change, but applied to the FULL src/ tree (Item I was 5 files; POLISH-04 is the broad pass — PATTERNS.md reports 98 grep-matches for `^\s*// Phase \d+` as a candidate surface).

Output: One (or several, per category) `docs(44): Tiger Style WHY-only comment sweep` commits + 44-03-SUMMARY.md with per-category counts + grep-guard evidence.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/44-final-polish/44-CONTEXT.md
@.planning/phases/44-final-polish/44-PATTERNS.md
@.planning/REFACTOR-LOOP-STATE.md
@.planning/phases/44-final-polish/44-01-SUMMARY.md
</context>

<propose_step_checklist>

## MANDATORY (per CONTEXT D-13 + [[propose-step-checklist]])

Before executing any task in this plan, the executor MUST first produce a PROPOSE block leading with:

1. **Downstream Constraints** — What downstream tasks (other Phase 44 clusters, future v2.x work, milestone close, drift-guard tests) might this change constrain? List 2-5 concrete items. **Especially for this plan: COMMENT-ONLY sweep — zero code behavior change must be a Downstream Constraint.**

2. **Applicable Memory Rules** — Which rules from `~/.claude-personal/projects/-Users-lucindo-Code-hrv/memory/MEMORY.md` apply to this work? At minimum surface (cite by `[[rule-name]]`):
   - `[[propose-step-checklist]]` — meta-rule, always applies
   - `[[no-design-locking]]` — applies if any test edit or token assertion
   - `[[use-lsp-for-renames]]` — applies if any symbol/property/CSS-class rename
   - `[[design-logic-separation]]` — applies if refactor touches any boundary (this plan is COMMENT-ONLY)
   - `[[ack-dont-fix-inline]]` — applies during operator feedback dumps
   - `[[spike-locked-values-are-not-decisions]]` — applies if any spike-010-locked visual would be touched

3. **THEN** — Goal / Scope / Risk / Verification / Commit message (the standard propose-block content).

No exceptions for "small" cleanup tasks. The Section A + Section B preface is the gate, not the content.

This requirement is operator-locked at CONTEXT D-13 verbatim: "Mandatory propose-step checklist applies to every Phase 44 plan per [[propose-step-checklist]]: Downstream Constraints + Applicable Memory Rules sections BEFORE Goal/Scope/Risk. No exceptions for 'small' cleanup plans."

</propose_step_checklist>

<tasks>

<task type="auto">
  <name>Task 1: Audit — surface every narration-of-WHAT comment candidate; record KEEP rationale for ambiguous WHY-comments</name>
  <files>
    - (read-only audit) src/**/*.ts, src/**/*.tsx
    - .planning/phases/44-final-polish/44-03-AUDIT-NOTES.md (new — proposal record; absorbed into SUMMARY at Task 2)
  </files>
  <read_first>
    - .planning/phases/44-final-polish/44-CONTEXT.md (POLISH-04 in-scope description; D-02 per-cluster commit)
    - .planning/phases/44-final-polish/44-PATTERNS.md ("Tiger Style WHY-only contract (POLISH-04 ROADMAP wording)" block; "Concrete keep-vs-drop examples from current codebase" block — `previewContext.ts:3-15` KEEP; `content.no-removed-keys.test.ts:1-21` KEEP; Item I `80da948` DROP examples; "Audit mechanic (per Item I — 4 verification grep guards)" block — the 3 grep guards)
    - .planning/REFACTOR-LOOP-STATE.md (Item I commit `80da948` — 5 files, 9 stale comment references; the meta-pattern documented: "the rule fires on the family I'm thinking about, not on every applicable instance")
    - src/audio/previewContext.ts (KEEP analog — D-01/D-02/D-03 latency block lines 3-15)
    - src/content/content.no-removed-keys.test.ts (KEEP analog — drift-guard self-description lines 1-21)
    - src/app/pages/AppSettingsPage.tsx (Item I touched — confirm "Replaces the legacy SettingsDialog modal route" no longer present; sweep siblings)
    - src/components/SettingsAnchor.tsx (Item I touched — confirm "Phase 15 SettingsDialog Shell — D-07, D-08..." no longer present)
    - src/components/SettingsPanelBody.tsx (Item I touched — confirm "legacy modal path" no longer present)
  </read_first>
  <action>
    Produce `.planning/phases/44-final-polish/44-03-AUDIT-NOTES.md`. Run each PATTERNS.md "Audit mechanic" grep against `src/` and record the candidate set in 5 sections (Section 1 deleted-component refs / Section 2 stale phase-narration markers / Section 3 scheduled-for-removal+TODO sweep / Section 4 ambiguous-WHY KEEP-with-rationale / Section 5 Tiger Style interpretation).

    Section 1 — DELETED-COMPONENT REFS (extending Item I's 3 verification grep guards): run the three greps from PATTERNS.md "Audit mechanic" block verbatim. All three should ideally return zero (per Item I post-state). Any hit = sibling pattern Item I missed → DROP candidate (with rationale: "extends Item I's `80da948` sweep"). NOTE the third grep overlaps with cluster 44-05 (POLISH-07 readability) — cluster 44-03 owns CODE-comment hits; 44-05 owns prose/docstring hits. If unclear, defer to 44-05 (per PATTERNS.md "Overlap with cluster 44-03" block: "If 44-03's grep zeroes the POLISH-07 grep, fold and save a commit").

    Section 2 — STALE PHASE-NARRATION MARKERS (the 98-hit surface from PATTERNS.md): `grep -rEn '^\s*//\s*Phase \d+' src`. For EACH match, classify as KEEP (encodes load-bearing WHY — `previewContext.ts` D-01/D-02/D-03 pattern is the gold standard) or DROP (narration-of-WHAT — the `App.*.test.tsx` "// Phase 18 Plan 03: <restates what code does>" pattern is the gold drop standard). Record the classification per-hit with one-line rationale.

    Section 3 — SCHEDULED-FOR-REMOVAL / LEGACY / DEPRECATED / TODO / FIXME / XXX / HACK SWEEP: `grep -rn 'scheduled for removal\|legacy modal\|deprecated\|TODO\|FIXME\|XXX\|HACK' src`. Per PATTERNS.md: "Expected: zero (or all dispositioned in 44-INFO-FINDINGS.md)". Any TODO/FIXME found is cross-referenced against 44-INFO-FINDINGS.md from cluster 44-01; if not already listed there, surface as a new Info-class finding (operator decides: defer-to-44-INFO-FINDINGS / drop comment / convert to issue tracking).

    Section 4 — AMBIGUOUS WHY CANDIDATES (KEEP-WITH-RATIONALE): for each KEEP decision in Section 2 that is non-obvious, record the load-bearing WHY in one sentence. This is the audit trail for the next reader.

    Section 5 — TIGER STYLE INTERPRETATION: document the planner's per-section judgment. Bias: per CONTEXT D-05 "when in doubt, prefer fix-or-defer over obsolete to keep the audit trail honest" — analogously, prefer KEEP if a comment plausibly encodes a WHY.
  </action>
  <verify>
    <automated>test -f .planning/phases/44-final-polish/44-03-AUDIT-NOTES.md &amp;&amp; grep -cE '^## Section [0-9]' .planning/phases/44-final-polish/44-03-AUDIT-NOTES.md ; grep -cE '^- DROP:' .planning/phases/44-final-polish/44-03-AUDIT-NOTES.md ; grep -cE '^- KEEP:' .planning/phases/44-final-polish/44-03-AUDIT-NOTES.md ; grep -c '44-05\|POLISH-07' .planning/phases/44-final-polish/44-03-AUDIT-NOTES.md</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/44-final-polish/44-03-AUDIT-NOTES.md` exists
    - Contains 5 sections (`## Section 1` through `## Section 5` headers)
    - Section 1 grep guards' candidate counts recorded (even if zero)
    - Section 2 every `^\s*// Phase \d+` hit classified as KEEP or DROP with one-line rationale
    - Section 3 every TODO/FIXME hit cross-referenced against `44-INFO-FINDINGS.md` from cluster 44-01
    - Section 4 has at least 1 entry OR explicitly states "no ambiguous KEEPs in Section 2"
    - Cross-cluster overlap with 44-05 is noted (the third Section 1 grep hits are deferred to 44-05 unless they're inside code comments)
  </acceptance_criteria>
  <done>Audit notes complete; every grep-surface candidate has a KEEP or DROP disposition; cross-cluster overlap documented.</done>
</task>

<task type="auto">
  <name>Task 2: Apply DROP edits (comments only) + run grep guards + commit per-cluster</name>
  <files>
    - src/**/*.{ts,tsx} (exact files determined by Task 1 audit notes — comments only)
    - .planning/phases/44-final-polish/44-03-SUMMARY.md (new)
    - (delete after commit) .planning/phases/44-final-polish/44-03-AUDIT-NOTES.md
  </files>
  <read_first>
    - .planning/phases/44-final-polish/44-CONTEXT.md (D-02 per-cluster commit; D-11 per-commit green-gate)
    - .planning/phases/44-final-polish/44-PATTERNS.md ("Per-cluster commit (D-02)" block at end of cluster 44-03 — exact commit message shape)
    - .planning/phases/44-final-polish/44-03-AUDIT-NOTES.md (from Task 1)
    - .planning/REFACTOR-LOOP-STATE.md (Item I commit `80da948` — exact mechanic; comments-only; 4 verification grep guards)
  </read_first>
  <action>
    For EACH DROP entry in Section 1 + Section 2 + Section 3 of `44-03-AUDIT-NOTES.md`: use the Edit tool to remove the comment. NEVER edit code (per [[design-logic-separation]] — Phase 44-03 is a comment-only sweep; code edits belong in 44-01 mega-commit or 44-04 refactor pass). NEVER touch any test file's drift-guard self-description (Section 4 of cluster 44-02's audit notes; PATTERNS.md "Drift-guard preservation invariant" block).

    For EACH KEEP entry in Section 4: leave verbatim. Optionally TIGHTEN the comment if a more concise WHY can be expressed — but this is OPTIONAL; the default action for KEEP is no edit.

    Run the per-commit green-gate after edits: `npx tsc --noEmit -p tsconfig.app.json && npm run lint && npm test -- --run && npm run build`. All four must exit 0. Test count MUST equal post-44-02 count (zero behavior change — Item I lesson).

    Run the closing audit greps from PATTERNS.md "Audit mechanic" verbatim — record the post-sweep counts in 44-03-SUMMARY.md:
    - `grep -rn 'LearnDialog\b\|SettingsDialog\b\|SettingsPanel\b' src | grep -v SettingsPanelBody`
    - `grep -rn 'BooleanToggle\|StatusPanel\b\|primitives/Card\b' src`
    - `grep -rEn '^\s*//\s*Phase \d+' src | wc -l`
    - `grep -rn 'scheduled for removal\|legacy modal\|deprecated\|TODO\|FIXME\|XXX\|HACK' src`

    Expected post-state: greps 1, 2, and 4 return zero (or all hits documented as KEEP-with-rationale in SUMMARY); grep 3 returns ≤ N where N is the historically-justified phase-numbered comments count from Section 4 of audit notes.

    Commit using PATTERNS.md "Per-cluster commit (D-02)" exact shape, scope prefix `docs(44):`. Then write 44-03-SUMMARY.md with per-category counts (Section 1 / Section 2 / Section 3 DROPs landed), kept-with-rationale list (Section 4 entries), grep-guard post-state evidence, per-cluster commit SHA, and confirmation that test pass count is unchanged from post-44-02. Delete `44-03-AUDIT-NOTES.md`.

    If TODOs were surfaced in Section 3 that don't already appear in `44-INFO-FINDINGS.md`, ADD them as new rows there with Disposition=`defer-with-reason` and a 1-commit append `docs(44): append 44-03 TODO discoveries to 44-INFO-FINDINGS.md`.
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.app.json &amp;&amp; npm run lint &amp;&amp; npm test -- --run &amp;&amp; npm run build &amp;&amp; echo '---GREP GUARD 1 (deleted-component refs)---' &amp;&amp; grep -rn 'LearnDialog\b\|SettingsDialog\b\|SettingsPanel\b' src | grep -v SettingsPanelBody | wc -l &amp;&amp; echo '---GREP GUARD 2 (deleted primitives)---' &amp;&amp; grep -rn 'BooleanToggle\|StatusPanel\b\|primitives/Card\b' src | wc -l &amp;&amp; echo '---GREP GUARD 4 (scheduled-for-removal)---' &amp;&amp; grep -rn 'scheduled for removal\|legacy modal\|deprecated\|TODO\|FIXME\|XXX\|HACK' src | wc -l &amp;&amp; git log --oneline -3</automated>
  </verify>
  <acceptance_criteria>
    - All 4 green-gate commands exit 0
    - Grep guard 1 (deleted-component refs, excluding SettingsPanelBody) returns 0 OR all hits documented as KEEP-with-rationale in 44-03-SUMMARY.md
    - Grep guard 2 (deleted primitives) returns 0
    - Grep guard 4 (scheduled-for-removal markers) returns 0 OR all hits cross-referenced into 44-INFO-FINDINGS.md as defer-with-reason
    - Test pass count is byte-equivalent to post-44-02 count (zero behavior change — comments-only sweep)
    - `git log --oneline -3` shows a `docs(44):` prefixed commit
    - 44-03-SUMMARY.md exists with: per-category DROP counts (Section 1/2/3), KEEP-with-rationale list, grep-guard post-state evidence, per-cluster commit SHA, test-pass-count parity confirmation
    - `git show HEAD` diff shows ONLY comment edits (no code changes) — visual inspection
    - `.planning/phases/44-final-polish/44-03-AUDIT-NOTES.md` is deleted (absorbed into SUMMARY)
  </acceptance_criteria>
  <done>POLISH-04 closed: WHY-only comment sweep landed; grep guards green; zero code behavior change.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| comment edits → source files | Edits are restricted to comment lines and docstrings; code lines untouched. Per-commit green-gate (tsc + test) catches accidental code edits. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-44-03-01 | Tampering | accidental code edit during comment-only sweep | mitigate | Per-commit green-gate; test pass count must equal post-44-02 count (zero behavior change invariant from Item I) |
| T-44-03-02 | Information Disclosure | a dropped comment removes a load-bearing WHY a future reader needs | mitigate | Task 1 Section 4 ambiguous-WHY KEEP-with-rationale — defaults to KEEP when uncertain; CONTEXT D-05 bias verbatim |
| T-44-03-03 | Repudiation | sweep deletes comments without recording the deletion | mitigate | 44-03-SUMMARY.md per-category DROP counts + the audit notes' per-hit rationale (preserved in SUMMARY before deleting notes file) |
| T-44-03-SC | Tampering | no package installs in this plan | accept | Comment-only sweep modifies no dependencies |

</threat_model>

<verification>

- Grep guards 1, 2, 4 return zero (or kept-with-rationale)
- Grep guard 3 (`// Phase \d+`) returns ≤ N per KEEP-with-rationale list
- Test pass count unchanged from post-44-02
- `docs(44):` commit landed
- 44-03-SUMMARY.md populated

</verification>

<success_criteria>

POLISH-04: ✅ when the closing grep guards return zero (or kept-with-rationale), test pass count is unchanged, and 44-03-SUMMARY.md records the sweep evidence.

</success_criteria>

<output>
Create `.planning/phases/44-final-polish/44-03-SUMMARY.md` when done. Include:
- Per-category DROP counts (Section 1 / Section 2 / Section 3)
- KEEP-with-rationale list (Section 4 entries with one-sentence WHY each)
- Grep-guard post-state evidence (4 grep outputs with counts)
- Per-cluster commit SHA + diff-stat (comments-only)
- Test-pass-count parity confirmation (post-44-03 == post-44-02)
- Cross-cluster overlap note (any items deferred to 44-05)
</output>
