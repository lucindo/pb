---
phase: 44-final-polish
plan: 05
type: execute
wave: 5
depends_on: ["44-01", "44-03", "44-04"]
files_modified:
  - src/**/*  # exact files determined by grep sweep; primarily prose/docstring edits
autonomous: true
requirements:
  - POLISH-07
user_setup: []

must_haves:
  truths:
    - "Repo-wide grep for Square/Diamond/Moss/Slate/Dusk/Chime returns zero hits in `src/` (excluding `.test.` files AND drift-guard tests that intentionally lock the removed names) — POLISH-07 ROADMAP wording verbatim"
    - "Any surviving readability hits are either (a) deleted, (b) rewritten to a structural description, or (c) kept-with-rationale (load-bearing WHY documenting the removal — rare, per D-05 bias)"
    - "Cluster 44-03 (comment audit) overlap is reconciled: if 44-03's sweep zeroed the POLISH-07 grep, 44-05 collapses to a one-line SUMMARY note instead of a separate commit (PATTERNS.md 'Overlap with cluster 44-03' policy)"
    - "Zero code behavior change — tests pass byte-equivalent to post-44-04 count"
    - "Per-commit green-gate (tsc + lint + test + build) passes"
  artifacts:
    - path: ".planning/phases/44-final-polish/44-05-SUMMARY.md"
      provides: "Grep-guard evidence + per-hit disposition (delete/rewrite/keep-with-rationale) + overlap-with-44-03 note + per-cluster commit SHA"
    - path: "src/** (prose/docstring edits OR none)"
      provides: "Readability remainder — no leftover Square/Diamond/Moss/Slate/Dusk/Chime refs (POLISH-07 close criterion)"
  key_links:
    - from: "44-05 grep sweep"
      to: "44-03 grep sweep"
      via: "shared deleted-name vocabulary — overlap reconciled per PATTERNS.md policy"
      pattern: "POLISH-07 grep zero-state OR fold-into-44-03"
    - from: "44-05 grep sweep"
      to: "drift-guard tests (content.no-removed-themes / content.no-removed-variants)"
      via: "drift-guards intentionally contain the names — `grep -v` excludes them"
      pattern: "grep -v.*content\\.no-removed"
---

<objective>
Run the POLISH-07 readability sweep — grep `src/` for any leftover Square/Diamond/Moss/Slate/Dusk/Chime references (the v2.0-removed shape variants + themes + audio timbres) outside the drift-guard tests that intentionally lock their absence. Dispose each hit: delete (no longer aids reader understanding), rewrite (structural description supersedes name reference), or keep-with-rationale (rare — load-bearing WHY).

Per PATTERNS.md "Overlap with cluster 44-03" policy: if cluster 44-03 (Tiger Style comment sweep) already zeroed the POLISH-07 grep, fold 44-05 into a one-line SUMMARY note ("POLISH-07 readability remainder closed by the 44-03 comment sweep; no separate plan needed") and save the commit.

Purpose: Close POLISH-07 remainder (broader readability beyond Phase 41 J18's component/i18n cleanup). The drift-guard tests (`content.no-removed-themes.test.ts`, `content.no-removed-variants.test.ts`) lock the deletion at the source-text level; this plan sweeps for prose/docstring/comment remainders the drift-guards don't catch.

Output: 44-05-SUMMARY.md with grep-guard evidence + per-hit disposition + overlap reconciliation. Per-cluster commit ONLY if hits surfaced and edits landed.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/44-final-polish/44-CONTEXT.md
@.planning/phases/44-final-polish/44-PATTERNS.md
@.planning/phases/44-final-polish/44-03-SUMMARY.md
@.planning/phases/44-final-polish/44-04-SUMMARY.md
</context>

<propose_step_checklist>

## MANDATORY (per CONTEXT D-13 + [[propose-step-checklist]])

Before executing any task in this plan, the executor MUST first produce a PROPOSE block leading with:

1. **Downstream Constraints** — What downstream tasks (other Phase 44 clusters, future v2.x work, milestone close, drift-guard tests) might this change constrain? List 2-5 concrete items. **Especially for this plan: drift-guards `content.no-removed-themes.test.ts` and `content.no-removed-variants.test.ts` MUST contain the removed names verbatim — sweep MUST NOT touch them.**

2. **Applicable Memory Rules** — Which rules from `~/.claude-personal/projects/-Users-lucindo-Code-hrv/memory/MEMORY.md` apply to this work? At minimum surface (cite by `[[rule-name]]`):
   - `[[propose-step-checklist]]` — meta-rule, always applies
   - `[[no-design-locking]]` — applies if any test edit or token assertion
   - `[[use-lsp-for-renames]]` — applies if any symbol/property/CSS-class rename (this is prose, but rename-discipline still applies)
   - `[[design-logic-separation]]` — applies if refactor touches any boundary (this plan is PROSE-ONLY)
   - `[[ack-dont-fix-inline]]` — applies during operator feedback dumps
   - `[[spike-locked-values-are-not-decisions]]` — applies if any spike-010-locked visual would be touched

3. **THEN** — Goal / Scope / Risk / Verification / Commit message (the standard propose-block content).

No exceptions for "small" cleanup tasks. The Section A + Section B preface is the gate, not the content.

This requirement is operator-locked at CONTEXT D-13 verbatim: "Mandatory propose-step checklist applies to every Phase 44 plan per [[propose-step-checklist]]: Downstream Constraints + Applicable Memory Rules sections BEFORE Goal/Scope/Risk. No exceptions for 'small' cleanup plans."

</propose_step_checklist>

<tasks>

<task type="auto">
  <name>Task 1: Run POLISH-07 grep sweep + per-hit disposition + (conditional) edits + (conditional) commit</name>
  <files>
    - src/** (exact files determined by grep — likely 0 to N prose/docstring edits)
    - .planning/phases/44-final-polish/44-05-SUMMARY.md (new — always created, even if no hits)
  </files>
  <read_first>
    - .planning/phases/44-final-polish/44-CONTEXT.md (POLISH-07 remainder description; D-05 obsolete-by-redesign threshold; D-02 per-cluster commit; D-11 per-commit green-gate)
    - .planning/phases/44-final-polish/44-PATTERNS.md ("POLISH-07 ROADMAP success criterion" block — verbatim wording; "Audit mechanic (live grep)" block — the exact grep command; "Overlap with cluster 44-03 (comment audit)" block — fold-into-44-03 policy when delta is trivial; "Per-cluster commit (D-02)" block — exact commit message shape)
    - .planning/phases/44-final-polish/44-03-SUMMARY.md (from cluster 44-03; check if 44-03 already zeroed the POLISH-07 grep)
    - src/content/content.no-removed-variants.test.ts (drift-guard intentionally containing Square/Diamond — preserve verbatim)
    - src/content/content.no-removed-themes.test.ts (drift-guard intentionally containing Moss/Slate/Dusk — preserve verbatim)
  </read_first>
  <action>
    Step 1 — Run the PATTERNS.md "Audit mechanic" grep verbatim:
    ```
    grep -rin 'square\|diamond\|moss\|slate\|dusk\|chime' src/ | grep -v '\.test\.' | grep -v -E 'content\.no-removed-(themes|variants)\.test\.ts'
    ```
    Note the `grep -v '\.test\.'` excludes ALL test files; the second `grep -v -E ...` is belt-and-suspenders for the drift-guards (which the first grep already excluded). If you want to ALSO sweep test files for prose-style remainders, run a second grep without `-v '\.test\.'` and filter manually.

    Step 2 — Cross-reference against 44-03's SUMMARY (the cluster 44-03 grep guards included one of the same `Square\|Diamond\|Moss\|...` searches under Section 1, scoped to CODE comments). Tally:
    - If 44-03 closed N hits and the current grep returns 0 → POLISH-07 is fully closed by 44-03; SKIP to Step 5 (fold-into-44-03 note).
    - If current grep returns >0 hits → these are prose/docstring/non-code hits 44-03 didn't pick up; proceed to Step 3.

    Step 3 — For each surviving hit, classify per the PATTERNS.md "If the sweep surfaces hits, dispose them" block:
    - **delete** — the prose / comment / doc no longer aids reader understanding (most common — the name was descriptive in v1.x but the redesign obviated the concept)
    - **rewrite** — replace the name reference with a structural description (e.g. "Square/Diamond shape variants" → "the v1.x shape vocabulary, simplified to Orb-only in Phase 38")
    - **keep-with-rationale** — encodes a load-bearing WHY (rare — per D-05 bias). Document the WHY in 44-05-SUMMARY.md.

    Step 4 — Apply the edits via the Edit tool (NOT sed/perl per [[use-lsp-for-renames]] — though these are prose edits, the rename-discipline rule still applies for principled per-file edits). Comments/prose only — NO code edits (those belong in 44-01 mega-commit or 44-04 refactor pass).

    Step 5 — Run the per-commit green-gate: `npx tsc --noEmit -p tsconfig.app.json && npm run lint && npm test -- --run && npm run build`. Test pass count MUST equal post-44-04 count (zero behavior change). Run the closing grep again to confirm zero hits (or all kept-with-rationale).

    Step 6 — Commit per PATTERNS.md "Per-cluster commit (D-02)" exact shape, scope prefix `docs(44):` (or `refactor(44):` if code touched — which it should NOT be for POLISH-07). Commit message names the deleted/rewritten/kept counts and includes the verification grep + green-gate evidence:
    ```
    docs(44): readability sweep — POLISH-07 remainder (no leftover Square/Diamond/Moss/Slate/Dusk/Chime refs)
    ```
    
    If hit count was 0 from Step 1 (fold case), DO NOT create an empty commit. Instead write only 44-05-SUMMARY.md with the fold note and skip the commit entirely.

    Step 7 — Write 44-05-SUMMARY.md regardless of edit count. Include: grep-guard pre-sweep count, per-hit disposition table (if any), grep-guard post-sweep count (must be 0 or all-kept-with-rationale), overlap-with-44-03 note (which case applied), per-cluster commit SHA (or "fold — no commit"), green-gate evidence, test pass count parity confirmation.
  </action>
  <verify>
    <automated>echo '---POLISH-07 GREP (must be 0 or all kept-with-rationale)---' &amp;&amp; grep -rin 'square\|diamond\|moss\|slate\|dusk\|chime' src/ | grep -v '\.test\.' | grep -v -E 'content\.no-removed-(themes|variants)\.test\.ts' | wc -l ; echo '---DRIFT-GUARDS STILL CONTAIN THE NAMES (sanity — should be >0)---' ; grep -c 'square\|diamond' src/content/content.no-removed-variants.test.ts ; grep -c 'moss\|slate\|dusk' src/content/content.no-removed-themes.test.ts ; echo '---GREEN GATE---' &amp;&amp; npx tsc --noEmit -p tsconfig.app.json &amp;&amp; npm run lint &amp;&amp; npm test -- --run 2>&amp;1 | tail -3 &amp;&amp; npm run build 2>&amp;1 | tail -3 ; echo '---SUMMARY EXISTS---' ; test -f .planning/phases/44-final-polish/44-05-SUMMARY.md &amp;&amp; head -20 .planning/phases/44-final-polish/44-05-SUMMARY.md ; echo '---LATEST COMMITS---' ; git log --oneline -3</automated>
  </verify>
  <acceptance_criteria>
    - PATTERNS.md "Audit mechanic" grep returns 0 hits OR every hit is documented as kept-with-rationale in 44-05-SUMMARY.md
    - Drift-guards STILL contain the names (sanity check — `content.no-removed-variants.test.ts` has Square/Diamond; `content.no-removed-themes.test.ts` has Moss/Slate/Dusk) — verified via the verify-block grep returning >0 on each
    - All 4 green-gate commands exit 0
    - Test pass count equals post-44-04 count (zero behavior change)
    - 44-05-SUMMARY.md exists and includes: pre-sweep grep count, per-hit disposition (if any), post-sweep grep count, overlap-with-44-03 reconciliation note (fold or separate), per-cluster commit SHA (or explicit "fold — no commit"), test-pass-count parity confirmation
    - If commit landed, `git log --oneline -3` shows a `docs(44):` (or `refactor(44):` if code touched) prefixed commit
    - If fold case (Step 1 returned 0), NO empty commit was created — 44-05-SUMMARY.md documents the fold and that's the only artifact
  </acceptance_criteria>
  <done>POLISH-07 remainder closed: zero leftover Square/Diamond/Moss/Slate/Dusk/Chime references in src/ (excluding drift-guards) OR fold-into-44-03 documented.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| prose edits → source files | Edits are restricted to comments/docstrings/prose; code lines untouched. Per-commit green-gate (tsc + test) catches accidental code edits. |
| drift-guard tests intentionally contain the names → preservation invariant | Phase 38 D-06 / Phase 39 D-06 exit-ramp policy — drift-guards are STRUCTURAL LOCKS; sweep must NOT touch them. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-44-05-01 | Tampering | accidental drift-guard modification silently unlocks Square/Diamond/Moss/Slate/Dusk re-introduction | mitigate | Grep filter `grep -v -E 'content\.no-removed-(themes|variants)\.test\.ts'` excludes drift-guards from candidate list; explicit sanity check in verify block confirms drift-guards still contain the names |
| T-44-05-02 | Tampering | accidental code edit during prose-only sweep | mitigate | Per-commit green-gate; test pass count equals post-44-04 count |
| T-44-05-03 | Information Disclosure | a deleted prose comment removes a load-bearing WHY explaining the removal | mitigate | Per-hit disposition includes keep-with-rationale option; D-05 bias toward keep-with-rationale when uncertain |
| T-44-05-SC | Tampering | no package installs in this plan | accept | Prose/comment-only sweep modifies no dependencies |

</threat_model>

<verification>

- POLISH-07 grep returns 0 (or all kept-with-rationale)
- Drift-guards `content.no-removed-variants.test.ts` and `content.no-removed-themes.test.ts` STILL contain the names
- Per-commit green-gate green
- Test pass count unchanged
- 44-05-SUMMARY.md created (either with edits or with fold note)

</verification>

<success_criteria>

POLISH-07: ✅ when the readability grep returns 0 hits (or all kept-with-rationale), drift-guards preserved verbatim, and 44-05-SUMMARY.md records the sweep evidence (either edits landed or fold-into-44-03 documented).

</success_criteria>

<output>
Create `.planning/phases/44-final-polish/44-05-SUMMARY.md` when done. Include:
- Pre-sweep grep count + the exact grep command
- Per-hit disposition table (if any hits surfaced)
- Post-sweep grep count (must be 0 OR all kept-with-rationale)
- Overlap-with-44-03 reconciliation note (which case applied: separate commit OR fold-into-44-03)
- Per-cluster commit SHA (or "fold — no commit" if 44-03 already closed it)
- Green-gate evidence + test-pass-count parity confirmation
</output>
</content>
