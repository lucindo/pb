---
phase: 22-bpm-stretch-session
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/content/strings.ts
  - src/content/strings.test.ts
autonomous: true
requirements: [STRETCH-01, STRETCH-03, STRETCH-06]

must_haves:
  truths:
    - "Every new stretch UI string exists in both UI_STRINGS.en and UI_STRINGS['pt-BR'] with no missing key"
    - "The settingsForm slice carries mode-picker, stretch-field, gate-hint, and computed-total label keys"
    - "The readout slice carries live-BPM-unit and three stage-label keys"
    - "New PT-BR entries carry the // TODO: native-speaker review marker per the I18N-07 convention"
  artifacts:
    - path: "src/content/strings.ts"
      provides: "Stretch label keys in the settingsForm and readout slices, EN + PT-BR"
      contains: "sessionModeLabel"
  key_links:
    - from: "src/content/strings.ts"
      to: "UiStrings interface"
      via: "new keys added to the settingsForm and readout slice type definitions"
      pattern: "sessionModeLabel"
---

<objective>
Add the EN + PT-BR copy for every Phase 22 stretch UI surface to the typed
`UI_STRINGS` catalog, exactly per the approved 22-UI-SPEC.md Copywriting Contract.

Purpose: The settings UI (Plan 04) and in-session readout (Plan 04) consume these
keys. Doing the catalog as its own plan keeps the string surface isolated and
parallelizable, and guarantees EN/PT-BR parity is verified before the UI wires them.
Output: Extended `UiStrings.settingsForm` and `UiStrings.readout` interface slices,
and matching entries in both locale blocks of `UI_STRINGS`.
</objective>

<execution_context>
@$HOME/.claude-personal-personal/get-shit-done/workflows/execute-plan.md
@$HOME/.claude-personal-personal/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/22-bpm-stretch-session/22-CONTEXT.md
@.planning/phases/22-bpm-stretch-session/22-UI-SPEC.md
@.planning/phases/22-bpm-stretch-session/22-PATTERNS.md

<interfaces>
<!-- Existing UiStrings slices to extend. The settingsForm and readout slices are
     NOT part of the frozen-EN LOCKED_COPY guard — additions are permitted. -->

Existing — src/content/strings.ts UiStrings interface:
```
readonly settingsForm: {
  readonly ariaLabel: string
  readonly bpmLabel: string
  readonly ratioLabel: string
  readonly durationLabel: string
  readonly openEndedLabel: string
  readonly bpmUnit: string
  readonly minutesUnit: string
  readonly stepper: { fieldAriaLabel; decreaseLabel; increaseLabel }
}
readonly readout: {
  readonly elapsed: string
  readonly remaining: string
  readonly readoutAriaLabel: string
  readonly announcementAriaLabel: string
  readonly sessionComplete: string
}
```
UI_STRINGS has two locale blocks: `en` and `pt-BR`. Every interface key must appear
in both. PT-BR additions carry `// TODO: native-speaker review`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add stretch label keys to UiStrings settingsForm and readout slices</name>
  <files>src/content/strings.ts, src/content/strings.test.ts</files>
  <read_first>
    - src/content/strings.ts (UiStrings interface settingsForm + readout slice definitions; both en and pt-BR UI_STRINGS blocks; existing // TODO: native-speaker review markers)
    - src/content/strings.test.ts (existing EN/PT-BR parity test structure, if present)
    - .planning/phases/22-bpm-stretch-session/22-UI-SPEC.md (Copywriting Contract table — the authoritative EN/PT-BR values)
    - .planning/phases/22-bpm-stretch-session/22-PATTERNS.md (strings.ts section — exact key names)
  </read_first>
  <action>
    Extend the `UiStrings` interface in `src/content/strings.ts`. In the `settingsForm`
    slice add these `readonly string` keys, and in the `readout` slice add the readout
    keys. Then add identical key sets to BOTH the `en` and `pt-BR` blocks of `UI_STRINGS`
    with the exact values from the 22-UI-SPEC.md Copywriting Contract:

    settingsForm slice (EN → PT-BR):
    - `sessionModeLabel`: `"Session mode"` → `"Modo de sessão"`
    - `modeStandard`: `"Standard"` → `"Padrão"`
    - `modeStretch`: `"Stretch"` → `"Alongamento"`
    - `stretchGateHint`: `"Needs a 15+ min session"` → `"Necessita sessão de 15+ min"`
    - `initialBpmLabel`: `"Start BPM"` → `"BPM inicial"`
    - `targetBpmLabel`: `"Target BPM"` → `"BPM alvo"`
    - `holdInitialLabel`: `"Warm-up"` → `"Aquecimento"`
    - `holdTargetLabel`: `"Cool-down"` → `"Resfriamento"`
    - `rampDurationLabel`: `"Ramp"` → `"Rampa"`
    - `holdNoneLabel`: `"None"` → `"Nenhum"` (hold stepper value when 0)
    - `holdOpenEndedLabel`: `"Open-ended"` → `"Aberto"` (holdTarget stepper 'open-ended' value)
    - `totalLabel`: `"Total:"` → `"Total:"`
    - `totalOpenEndedLabel`: `"Total: Open-ended"` → `"Total: Aberto"`

    readout slice (EN → PT-BR):
    - `currentBpmLabel`: `"BPM"` → `"BPM"`
    - `stageHoldInitial`: `"Warm-up"` → `"Aquecimento"`
    - `stageRamp`: `"Ramp"` → `"Rampa"`
    - `stageHoldTarget`: `"Cool-down"` → `"Resfriamento"`

    Every new key in the `pt-BR` block carries a `// TODO: native-speaker review`
    comment on its line, matching the existing I18N-07 convention used by other PT-BR
    entries in the file. These keys land in the non-frozen `settingsForm`/`readout`
    slices — they are NOT part of `LOCKED_COPY`, so no frozen-EN guard interaction.

    If `src/content/strings.test.ts` has an EN/PT-BR key-parity test, it will exercise
    the new keys automatically. If a parity test does not exist, add one assertion that
    iterates the `settingsForm` and `readout` keys of `UI_STRINGS.en` and asserts each
    key is also present (and a string) in `UI_STRINGS['pt-BR']`.
  </action>
  <verify>
    <automated>npx vitest run src/content/strings.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` exits 0 — both locale blocks satisfy the extended `UiStrings` interface (a missing key in either block is a compile error)
    - `npx vitest run src/content/strings.test.ts` exits 0
    - `grep -c "sessionModeLabel" src/content/strings.ts` returns 3 (one interface declaration + en + pt-BR)
    - `grep -c "stageHoldInitial" src/content/strings.ts` returns 3
    - Every new PT-BR line carries `// TODO: native-speaker review` — verify with `grep -c "native-speaker review" src/content/strings.ts` increased by the count of new PT-BR keys (17)
  </acceptance_criteria>
  <done>All 17 stretch label keys exist in the UiStrings interface and both UI_STRINGS locale blocks; PT-BR entries flagged for native review; tsc and strings tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | Static string catalog; no runtime input, no storage, no network |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-07 | Tampering | UI_STRINGS catalog | accept | Compile-time constant; the `UiStrings` interface enforces shape at build time. No runtime mutation path. No threat surface. |

No trust boundaries crossed. No high-severity threats. This plan adds static
compile-time string constants only.
</threat_model>

<verification>
- `npx tsc --noEmit` — exits 0 (interface parity enforced at compile time)
- `npx vitest run src/content/` — green
- `npm run lint` — exits 0
</verification>

<success_criteria>
- 13 new settingsForm keys + 4 new readout keys exist in the UiStrings interface and both locale blocks
- EN/PT-BR parity holds (compile-enforced)
- PT-BR entries carry the native-speaker-review marker
</success_criteria>

<output>
After completion, create `.planning/phases/22-bpm-stretch-session/22-03-SUMMARY.md`
</output>
