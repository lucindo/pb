---
phase: 22-bpm-stretch-session
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/domain/settings.ts
  - src/domain/settings.test.ts
  - src/domain/sessionMath.ts
  - src/domain/stretchRamp.ts
  - src/domain/stretchRamp.test.ts
autonomous: true
requirements: [STRETCH-02, STRETCH-03, STRETCH-04, STRETCH-05, STRETCH-06]

must_haves:
  truths:
    - "buildStretchSegments produces a segment table whose every ramp step is strictly < 0.5 BPM"
    - "getStretchFrame returns a monotonically increasing absolute cycleIndex across all segments"
    - "getStretchFrame completes a finite stretch session at the correct cycle-rounded boundary using the last segment's cycleMs"
    - "An open-ended stretch session (holdTargetSeconds 'open-ended') never reports isComplete"
    - "computeStretchTotalMs returns null for open-ended; isStretchGateClear returns true at exactly 15:00 and false at 14:59"
    - "isValidMode/isValidHoldSeconds/isValidHoldTarget/isValidRampDuration correctly accept valid values and reject invalid ones"
    - "D-01: validateSettings rejects a stretch settings object whose targetBpm is not strictly below initialBpm (down-only ramp)"
    - "D-02: rampDurationMinutes is its own validated picker field (RAMP_DURATION_OPTIONS), not derived by subtracting holds from a total"
    - "D-03: an open-ended stretch session runs holdInitial + ramp finitely then holds at targetBpm forever; getStretchFrame never reports isComplete for it"
    - "D-04: buildStretchSegments produces a linear ramp where each individual step is strictly < 0.5 BPM (uniform BPM change, no easing)"
    - "D-07: HOLD_SECONDS_OPTIONS is 0/15/30/45/60 and RAMP_DURATION_OPTIONS is 5..60 in 5-min steps, with 'open-ended' carried on the holdTarget option set"
    - "D-09: isStretchGateClear evaluates the computed stretch total (holdInitial + ramp + holdTarget), not a standalone duration field"
    - "D-10: STRETCH_MIN_TOTAL_MS is 15 minutes and isStretchGateClear uses it as the threshold"
    - "D-11: HOLD_TARGET_OPTIONS carries an 'open-ended' value past 60s; an open-ended total returns null from computeStretchTotalMs and always clears the gate"
  artifacts:
    - path: "src/domain/stretchRamp.ts"
      provides: "StretchSegment + StretchStage + StretchSessionFrame types, buildStretchSegments, getStretchFrame, computeStretchTotalMs, isStretchGateClear, STRETCH_MIN_TOTAL_MS"
      min_lines: 120
    - path: "src/domain/settings.ts"
      provides: "SessionMode, stretch field types/options/predicates, extended SessionSettings, DEFAULT_STRETCH_SETTINGS, extended validateSettings"
      contains: "SessionMode"
    - path: "src/domain/stretchRamp.test.ts"
      provides: "Coverage for STRETCH-04/05/06 ramp math, step invariant, gate predicate"
      contains: "buildStretchSegments"
  key_links:
    - from: "src/domain/stretchRamp.ts"
      to: "src/domain/settings.ts"
      via: "imports SessionSettings, SessionMode, RatioLabel, BPM_OPTIONS"
      pattern: "from './settings'"
    - from: "src/domain/sessionMath.ts"
      to: "SessionFrame"
      via: "optional stretch-only fields appended to the SessionFrame interface"
      pattern: "cycleStartMs"
---

<objective>
Build the pure domain foundation for BPM stretch sessions: the stretch settings
schema (mode picker + stretch fields + predicates), the ramp engine
(`stretchRamp.ts` — segment table builder, frame lookup, computed-total and gate
predicates), and the optional stretch-only fields on `SessionFrame`.

Purpose: This is the load-bearing core. Every downstream plan (controller wiring,
storage, UI, audio) consumes the types and functions defined here. Building it
first as a fully-tested pure domain layer means downstream plans receive concrete
contracts with no codebase exploration.
Output: Extended `settings.ts`, new `stretchRamp.ts`, extended `SessionFrame`
interface in `sessionMath.ts`, and full unit test coverage for STRETCH-04/05/06
plus the new predicates.
</objective>

<execution_context>
@$HOME/.claude-personal-personal/get-shit-done/workflows/execute-plan.md
@$HOME/.claude-personal-personal/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-bpm-stretch-session/22-CONTEXT.md
@.planning/phases/22-bpm-stretch-session/22-RESEARCH.md
@.planning/phases/22-bpm-stretch-session/22-PATTERNS.md

<interfaces>
<!-- Concrete contracts the executor builds. Use these verbatim; no exploration needed. -->

Existing — src/domain/settings.ts (extend in place):
```
export type RatioLabel = '50:50' | '40:60' | '30:70' | '20:80'
export type DurationOption = number | 'open-ended'
export interface SessionSettings { bpm: number; ratio: RatioLabel; durationMinutes: DurationOption }
export const BPM_OPTIONS = [1, 1.5, 2, 2.5, 3, 3.5, 4, 4.5, 5, 5.5, 6, 6.5, 7] as const
export const DEFAULT_SETTINGS: SessionSettings = { bpm: 5.5, ratio: '40:60', durationMinutes: 10 }
export function isValidBpm(v: unknown): v is number  // BPM_OPTIONS membership
export function isValidTheme(v: unknown): v is ThemeId  // pattern for string-enum predicates
export function validateSettings(settings: SessionSettings): SessionSettings  // throws RangeError per field
```

Existing — src/domain/sessionMath.ts (extend SessionFrame interface only):
```
export interface SessionFrame {
  phase: BreathPhase; phaseLabel: 'In' | 'Out'; elapsedMs: number
  remainingMs: number | null; phaseProgress: number; cycleIndex: number; isComplete: boolean
}
export function getSessionFrame(plan: BreathingPlan, elapsedMs: number): SessionFrame  // DO NOT MODIFY behavior
```

Existing — src/domain/breathingPlan.ts (reference for ratio math, do not modify):
```
RATIO_PARTS: '50:50'={50,50} '40:60'={40,60} '30:70'={30,70} '20:80'={20,80}
cycleMs = 60000 / bpm; inhaleMs = cycleMs * ratio.inhale/100; exhaleMs = cycleMs * ratio.exhale/100
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend settings.ts with stretch schema, options, predicates, and validation</name>
  <files>src/domain/settings.ts, src/domain/settings.test.ts</files>
  <read_first>
    - src/domain/settings.ts (current schema, BPM_OPTIONS, isValidBpm/isValidTheme/isValidDuration predicate patterns, validateSettings throw pattern)
    - src/domain/settings.test.ts (existing test structure for predicates and validateSettings)
    - .planning/phases/22-bpm-stretch-session/22-PATTERNS.md (settings.ts section — exact list of new additions)
    - .planning/phases/22-bpm-stretch-session/22-CONTEXT.md (D-01, D-07, D-10, D-11 — picker ranges and constraints)
  </read_first>
  <behavior>
    - isValidMode accepts 'standard' and 'stretch'; rejects 'foo', 42, null, undefined
    - isValidHoldSeconds accepts 0, 15, 30, 45, 60; rejects 10, 'open-ended', -1, NaN
    - isValidHoldTarget accepts 0, 15, 30, 45, 60, 'open-ended'; rejects 10, 'foo', null
    - isValidRampDuration accepts 5, 10, ..., 60 (5-min steps); rejects 7, 'open-ended', 0, 65
    - validateSettings throws RangeError for an invalid mode; throws for invalid stretch fields ONLY when mode is 'stretch'; does NOT throw for stretch fields when mode is 'standard'
    - validateSettings on a standard-mode settings object with valid bpm/ratio/duration still returns successfully (Standard path unchanged)
  </behavior>
  <action>
    Extend `src/domain/settings.ts` following the established options-array + type-guard
    predicate pattern (copy the shape of `isValidTheme` for string enums and `isValidDuration`
    for the 'open-ended' sentinel). Add:

    - `export type SessionMode = 'standard' | 'stretch'`
    - `export const MODE_OPTIONS = ['standard', 'stretch'] as const satisfies readonly SessionMode[]`
    - `export type HoldSecondsOption = 0 | 15 | 30 | 45 | 60`
    - `export const HOLD_SECONDS_OPTIONS = [0, 15, 30, 45, 60] as const satisfies readonly HoldSecondsOption[]`
    - `export type HoldTargetOption = HoldSecondsOption | 'open-ended'`
    - `export const HOLD_TARGET_OPTIONS = [0, 15, 30, 45, 60, 'open-ended'] as const satisfies readonly HoldTargetOption[]`
    - `export const RAMP_DURATION_OPTIONS = [5, 10, 15, 20, 25, 30, 35, 40, 45, 50, 55, 60] as const satisfies readonly number[]`
    - `export const STRETCH_INITIAL_BPM_OPTIONS` = `BPM_OPTIONS` filtered to values `>= 1.5` (per 22-UI-SPEC.md: minimum initialBpm is 1.5 so targetBpm always has at least one valid option). Export it as a `readonly number[]`.
    - Predicates: `isValidMode`, `isValidHoldSeconds`, `isValidHoldTarget`, `isValidRampDuration`. `isValidMode` mirrors `isValidTheme`. `isValidHoldSeconds`/`isValidRampDuration` mirror the numeric branch of `isValidDuration`. `isValidHoldTarget` mirrors `isValidDuration`'s `'open-ended'` early-return then numeric membership.
    - Extend `SessionSettings` with required fields: `mode: SessionMode`, `initialBpm: number`, `targetBpm: number`, `holdInitialSeconds: HoldSecondsOption`, `holdTargetSeconds: HoldTargetOption`, `rampDurationMinutes: number`. Make them required (not optional) so the type is total; `coerceSettings` (Plan 02) fills them from defaults for old envelopes.
    - Extend `DEFAULT_SETTINGS`: add `mode: 'standard'`, `initialBpm: 5.5`, `targetBpm: 4.5`, `holdInitialSeconds: 0`, `holdTargetSeconds: 0`, `rampDurationMinutes: 20`. Per RESEARCH Pitfall 5: these defaults yield a 20-minute computed total so Stretch is reachable on first visit.
    - Add `export const DEFAULT_STRETCH_SETTINGS` exposing just the stretch field defaults (`initialBpm: 5.5, targetBpm: 4.5, holdInitialSeconds: 0, holdTargetSeconds: 0, rampDurationMinutes: 20`) — Plan 02 storage coercer references it per-field.
    - Extend `validateSettings`: after the existing bpm/ratio/duration checks, add `if (!isValidMode(settings.mode)) throw new RangeError(...)`. Then, gated on `settings.mode === 'stretch'`, validate `initialBpm` (isValidBpm), `targetBpm` (isValidBpm AND `< initialBpm` per D-01), `holdInitialSeconds` (isValidHoldSeconds), `holdTargetSeconds` (isValidHoldTarget), `rampDurationMinutes` (isValidRampDuration) — each throwing `RangeError` with a message matching the existing `Unsupported X: ${String(value)}` format. Standard-mode settings skip the stretch-field validation entirely so existing Standard sessions are unaffected.

    Add test cases to `src/domain/settings.test.ts` covering each new predicate (valid + invalid inputs per the behavior block) and the validateSettings stretch-mode throw paths plus the standard-mode no-throw path.
  </action>
  <verify>
    <automated>npx vitest run src/domain/settings.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/domain/settings.test.ts` exits 0 with new stretch predicate + validateSettings cases passing
    - `grep -c "export" src/domain/settings.ts` shows the new exports (SessionMode, MODE_OPTIONS, HOLD_SECONDS_OPTIONS, HOLD_TARGET_OPTIONS, RAMP_DURATION_OPTIONS, STRETCH_INITIAL_BPM_OPTIONS, DEFAULT_STRETCH_SETTINGS, isValidMode, isValidHoldSeconds, isValidHoldTarget, isValidRampDuration) all present
    - `npx tsc --noEmit` exits 0 — SessionSettings is total (all 6 new fields required)
    - DEFAULT_SETTINGS.mode === 'standard' and the stretch defaults yield holdInitial 0 + ramp 20min + holdTarget 0 = 20 minutes
  </acceptance_criteria>
  <done>Stretch settings schema, options arrays, predicates, defaults, and validation extensions are in place; settings.test.ts is green; tsc passes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create stretchRamp.ts ramp engine and extend SessionFrame</name>
  <files>src/domain/stretchRamp.ts, src/domain/stretchRamp.test.ts, src/domain/sessionMath.ts</files>
  <read_first>
    - src/domain/sessionMath.ts (SessionFrame interface, getSessionFrame cycleIndex/cycleElapsedMs/completionMs math — the pattern getStretchFrame mirrors)
    - src/domain/breathingPlan.ts (RATIO_PARTS, cycleMs/inhaleMs/exhaleMs formulas)
    - src/domain/sessionMath.test.ts (test file structure to mirror for stretchRamp.test.ts)
    - .planning/phases/22-bpm-stretch-session/22-RESEARCH.md (Pattern 1, Pattern 2, Pitfalls 1/3/6 — segment table, absolute cycleIndex, isComplete rounding, open-ended)
    - .planning/phases/22-bpm-stretch-session/22-PATTERNS.md (stretchRamp.ts section — file structure)
  </read_first>
  <behavior>
    - buildStretchSegments(settings, ratio) for initialBpm 6, targetBpm 4, rampDurationMinutes 20, holds 0: produces a ramp with `numSteps = Math.ceil((6-4)/0.4999)` segments; every ramp segment's BPM differs from its neighbor by < 0.5; segment BPMs decrease monotonically from 6 toward 4
    - With holdInitialSeconds 15: first segment has stage 'hold-initial', bpm === initialBpm, duration 15000ms
    - With holdTargetSeconds 30: last segment has stage 'hold-target', bpm === targetBpm, finite endMs
    - With holdTargetSeconds 'open-ended': last segment stage 'hold-target', endMs Infinity sentinel
    - getStretchFrame: cycleIndex is strictly non-decreasing as elapsedMs increases across segment boundaries (never resets to 0 at a new segment)
    - getStretchFrame: for a finite total, isComplete is false just before the cycle-rounded completion and true at/after it; completionMs uses the LAST segment's cycleMs for the ceil rounding
    - getStretchFrame: for open-ended (totalMs null), isComplete is always false and remainingMs is null at every elapsedMs
    - getStretchFrame frame carries currentBpm, stage, cycleStartMs, currentCycleMs, currentInhaleMs, currentExhaleMs matching the active segment
    - computeStretchTotalMs returns holdInitialMs + rampMs + holdTargetMs for finite holds; returns null when holdTargetSeconds is 'open-ended'
    - isStretchGateClear returns true when computeStretchTotalMs is null (open-ended); true at exactly 15*60000; false at 15*60000 - 1
  </behavior>
  <action>
    Create `src/domain/stretchRamp.ts` as a pure-domain module (no React, no I/O) mirroring
    the `sessionMath.ts`/`breathingPlan.ts` pure-function style. Define:

    - `export type StretchStage = 'hold-initial' | 'ramp' | 'hold-target'`
    - `export interface StretchSegment { startMs: number; endMs: number; bpm: number; cycleMs: number; inhaleMs: number; exhaleMs: number; stage: StretchStage; cycleBaseIndex: number }` — `cycleBaseIndex` is the absolute count of cycles completed in ALL prior segments (Pitfall 1 — used to keep cycleIndex monotonic).
    - `export interface StretchSessionFrame extends SessionFrame { currentBpm: number; stage: StretchStage; cycleStartMs: number; currentCycleMs: number; currentInhaleMs: number; currentExhaleMs: number }`
    - `export const STRETCH_MIN_TOTAL_MS = 15 * 60_000` (D-10)
    - `buildStretchSegments(settings, ratio: RatioLabel): StretchSegment[]` — pure. Step 1: optional hold-initial segment at `initialBpm` for `holdInitialSeconds * 1000` (omit if 0). Step 2: ramp — compute `numSteps = Math.ceil((initialBpm - targetBpm) / 0.4999)` (guarantees each step strictly < 0.5 BPM per STRETCH-04, D-04 linear), `stepDurationMs = rampDurationMinutes * 60000 / numSteps`, segment `i` BPM = `initialBpm - i * (initialBpm - targetBpm) / numSteps`. Step 3: hold-target segment at `targetBpm` for `holdTargetSeconds * 1000`, or `endMs = Infinity` when `holdTargetSeconds === 'open-ended'` (D-03/D-11). For every segment compute `cycleMs = 60000/bpm`, `inhaleMs`/`exhaleMs` from RATIO_PARTS, contiguous `startMs`/`endMs`, and `cycleBaseIndex` (running sum of `Math.floor(segmentDurationMs / cycleMs)` for prior segments; the open-ended last segment contributes nothing to any later base).
    - `getStretchFrame(segments: StretchSegment[], totalMs: number | null, elapsedMs: number): StretchSessionFrame` — pure. Clamp elapsed `>= 0`. Find the segment containing `elapsedMs` (linear walk or binary search; the open-ended last segment matches any elapsed past its `startMs`). Within the segment compute `cycleInSegment = Math.floor(elapsedInSegment / segment.cycleMs)`, `absoluteCycleIndex = segment.cycleBaseIndex + cycleInSegment`, `cycleStartMs = segment.startMs + cycleInSegment * segment.cycleMs`, phase from `cycleElapsedMs % cycleMs` vs `inhaleMs` (same In/Out logic as getSessionFrame). `remainingMs = totalMs === null ? null : Math.max(0, totalMs - elapsedMs)`. `completionMs`: when `totalMs === null` → null; else round up using the LAST segment's `cycleMs` (Pitfall 3): `lastSeg.startMs + Math.ceil((totalMs - lastSeg.startMs) / lastSeg.cycleMs) * lastSeg.cycleMs`. `isComplete = completionMs !== null && elapsedMs >= completionMs`. Populate all `StretchSessionFrame` fields including `currentBpm`, `stage`, `cycleStartMs`, `currentCycleMs`/`currentInhaleMs`/`currentExhaleMs` from the active segment.
    - `computeStretchTotalMs(settings): number | null` — `holdInitialSeconds*1000 + rampDurationMinutes*60000 + (holdTargetSeconds === 'open-ended' ? null-path : holdTargetSeconds*1000)`; return `null` when holdTargetSeconds is `'open-ended'`.
    - `isStretchGateClear(settings): boolean` — `const total = computeStretchTotalMs(settings); return total === null || total >= STRETCH_MIN_TOTAL_MS`.

    Then extend `src/domain/sessionMath.ts`: append optional stretch-only fields to the
    `SessionFrame` interface — `cycleStartMs?: number`, `currentCycleMs?: number`,
    `currentInhaleMs?: number`, `currentExhaleMs?: number`, `currentBpm?: number`,
    `stage?: StretchStage` (import `StretchStage` type from `./stretchRamp`). These are
    `undefined` for standard sessions so `getSessionFrame` stays byte-unchanged in behavior.
    Do NOT modify the `getSessionFrame` function body.

    Create `src/domain/stretchRamp.test.ts` mirroring `sessionMath.test.ts` structure with
    three describe blocks: buildStretchSegments (segment count, step invariant < 0.5,
    stage tags, monotonic BPM, open-ended endMs), getStretchFrame (monotonic absolute
    cycleIndex across segment boundary, isComplete timing with last-segment rounding,
    open-ended never completes), and computeStretchTotalMs/isStretchGateClear (15-min
    boundary exact + off-by-one, open-ended always clears).
  </action>
  <verify>
    <automated>npx vitest run src/domain/stretchRamp.test.ts src/domain/sessionMath.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/domain/stretchRamp.test.ts` exits 0 with buildStretchSegments / getStretchFrame / gate-predicate describe blocks all passing
    - `npx vitest run src/domain/sessionMath.test.ts` still exits 0 — getSessionFrame behavior unchanged for standard sessions
    - A test asserts every ramp segment's `Math.abs(bpm[i] - bpm[i-1]) < 0.5` (STRETCH-04 invariant)
    - A test asserts getStretchFrame cycleIndex never decreases across an elapsedMs sweep that crosses a segment boundary
    - A test asserts open-ended (`totalMs` null) getStretchFrame returns `isComplete: false` and `remainingMs: null`
    - A test asserts `isStretchGateClear` is true at exactly `15*60000` and false at `15*60000 - 1`
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>stretchRamp.ts ramp engine exists with full STRETCH-04/05/06 coverage; SessionFrame carries optional stretch fields; getSessionFrame behavior is unchanged; tsc passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| settings object → domain functions | SessionSettings may arrive from coerced localStorage (Plan 02) carrying drifted/malformed stretch field values |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-01 | Tampering | buildStretchSegments / getStretchFrame inputs | mitigate | `validateSettings` throws RangeError on out-of-range stretch fields before `createBreathingPlan`/segment build runs; `isValid*` predicates back the non-throwing storage coerce path (Plan 02). Discrete option-array membership checks reject arbitrary values. |
| T-22-02 | Denial of Service | buildStretchSegments numSteps computation | mitigate | `numSteps = Math.ceil(bpmSpan / 0.4999)`; bpmSpan is bounded by BPM_OPTIONS (max span 7 - 1 = 6 → max ~13 ramp segments). No unbounded loop; segment table size is small and deterministic. Open-ended hold uses an `Infinity` sentinel on a single segment, not an unbounded array. |
| T-22-03 | Information Disclosure | pure domain module | accept | No PII, no network, no storage access in this module. Pure functions over numeric settings only. No threat surface. |

No high-severity threats. ASVS L1 input validation (V5) satisfied by discrete option-array membership predicates.
</threat_model>

<verification>
- `npx vitest run src/domain/` — all domain tests green (settings, stretchRamp, sessionMath, breathingPlan, sessionController unchanged)
- `npx tsc --noEmit` — exits 0
- `npm run lint` — exits 0
</verification>

<success_criteria>
- SessionSettings carries all 6 stretch fields as required typed members
- stretchRamp.ts exports buildStretchSegments, getStretchFrame, computeStretchTotalMs, isStretchGateClear, STRETCH_MIN_TOTAL_MS, StretchSegment, StretchStage, StretchSessionFrame
- Every ramp step is provably < 0.5 BPM (STRETCH-04), enforced and tested
- Open-ended sessions never complete; finite sessions complete at the correct last-segment-rounded boundary
- The 15-minute gate predicate is correct at the boundary
- getSessionFrame behavior for standard sessions is unchanged
</success_criteria>

<output>
After completion, create `.planning/phases/22-bpm-stretch-session/22-01-SUMMARY.md`
</output>
