---
phase: 05-mobile-hands-off-resilience
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - vitest.setup.ts
autonomous: true
requirements:
  - MOBL-02
tags:
  - test-infra
  - polyfill
  - wake-lock
  - vitest

must_haves:
  truths:
    - "vitest.setup.ts has a conditional `navigator.wakeLock` polyfill that installs a fake `WakeLock`/`WakeLockSentinel` when the host environment lacks one (D-13)."
    - "After the polyfill is added but BEFORE any hook code, the full existing test suite (`npm run test:run`) passes with zero regressions (RESEARCH A4 carry-forward smoke)."
    - "The fake `WakeLockSentinel` extends `EventTarget` so `addEventListener('release', ...)` and `dispatchEvent(new Event('release'))` work natively (RESEARCH A1)."
  artifacts:
    - path: "vitest.setup.ts"
      provides: "navigator.wakeLock fake (FakeWakeLockSentinel + request: vi.fn) installed via Object.defineProperty(navigator, 'wakeLock', { writable: true, configurable: true, value: ... })."
      contains: "FakeWakeLockSentinel"
  key_links:
    - from: "vitest.setup.ts"
      to: "navigator.wakeLock"
      via: "Object.defineProperty(navigator, 'wakeLock', { writable: true, configurable: true, value: ... })"
      pattern: "Object\\.defineProperty\\(navigator, 'wakeLock'"
    - from: "FakeWakeLockSentinel"
      to: "EventTarget"
      via: "class FakeWakeLockSentinel extends EventTarget"
      pattern: "class FakeWakeLockSentinel extends EventTarget"
---

<objective>
Extend `vitest.setup.ts` with a `navigator.wakeLock` polyfill (D-13) following the existing project polyfill convention (`AudioContext` block at lines 126-200; `Storage` block at lines 29-81). Run the full existing suite to confirm 0 regressions before any hook or App-integration code is added.

Purpose: The polyfill is a strict prerequisite for the Phase 5 hook unit tests (Plan 02) and the App integration tests (Plan 03). Adding it in isolation, BEFORE any consumer code, mitigates RESEARCH A4 (carry-forward risk on Phase 1–4 tests) and A3 (jsdom 29.1.1 + Node 25.9.0 acceptance of `Object.defineProperty(navigator, 'wakeLock', ...)`) in the cheapest possible way: if the smoke fails, we know the polyfill itself is the cause, not any new application code.

Output: One commit modifying `vitest.setup.ts` only. Full test suite green pre-implementation.
</objective>

<execution_context>
@$HOME/.claude-personal-personal/get-shit-done/workflows/execute-plan.md
@$HOME/.claude-personal-personal/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-mobile-hands-off-resilience/05-CONTEXT.md
@.planning/phases/05-mobile-hands-off-resilience/05-RESEARCH.md
@.planning/phases/05-mobile-hands-off-resilience/05-PATTERNS.md
@.planning/phases/05-mobile-hands-off-resilience/05-VALIDATION.md
@vitest.setup.ts

<interfaces>
<!-- Existing polyfill conventions in vitest.setup.ts. The new block MUST follow these shapes verbatim. -->

Storage polyfill install line (vitest.setup.ts:71-80):
```typescript
Object.defineProperty(window, 'localStorage', {
  writable: true,
  configurable: true,
  value: makeFakeStorage(),
})
```

AudioContext polyfill install line (vitest.setup.ts:195-199):
```typescript
Object.defineProperty(window, 'AudioContext', {
  writable: true,
  configurable: true, // allow vi.stubGlobal('AudioContext', ...) per-test overrides
  value: FakeAudioContext,
})
```

Conditional install gate convention (used by both Storage at line 29 and AudioContext at line 129):
```typescript
if (typeof window !== 'undefined' && !window.AudioContext) { ... }
```
The Wake Lock equivalent uses `navigator` as the target:
```typescript
if (typeof navigator !== 'undefined' && !('wakeLock' in navigator)) { ... }
```

TypeScript types (already bundled in TS 6.0.2 lib.dom.d.ts — verified RESEARCH Standard Stack):
- `interface WakeLockSentinel extends EventTarget { type: WakeLockType; released: boolean; onrelease: ... | null; release(): Promise<void>; addEventListener('release', ...) }`
- `interface WakeLock { request(type?: WakeLockType): Promise<WakeLockSentinel>; }`
- `type WakeLockType = "screen";`
- `Navigator.wakeLock: WakeLock` (non-optional; runtime feature-detect still required — RESEARCH Pitfall 4)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add navigator.wakeLock polyfill to vitest.setup.ts</name>
  <files>vitest.setup.ts</files>
  <read_first>
    - vitest.setup.ts (read lines 1-200; the new block MUST be appended AFTER the existing AudioContext block, NOT inserted into the middle)
    - .planning/phases/05-mobile-hands-off-resilience/05-PATTERNS.md (the `vitest.setup.ts` section under "Pattern Assignments" — verbatim shape requirements)
    - .planning/phases/05-mobile-hands-off-resilience/05-RESEARCH.md (Pattern 3 polyfill-and-spy + Code Examples "Vitest polyfill" block at ~lines 620-647)
    - .planning/phases/05-mobile-hands-off-resilience/05-CONTEXT.md (D-13: `vitest.setup.ts` is extended with a `navigator.wakeLock` polyfill following the existing `Object.defineProperty(navigator, ...)` pattern)
  </read_first>
  <action>
APPEND a new block to `vitest.setup.ts` AFTER the existing AudioContext block (which currently ends at line 200). The new block must be the LAST polyfill block in the file. Do NOT modify any existing polyfill (Storage / HTMLDialogElement / matchMedia / AudioContext).

The new block MUST contain, in this order:

1. A leading comment (3-4 lines) explaining: source = D-13 + RESEARCH Pattern 3 + lib.dom.d.ts bundled types; jsdom 29.1.1 does not implement Wake Lock; per-test failure paths use `vi.spyOn(navigator.wakeLock, 'request').mockRejectedValueOnce(...)` and per-test API-absent paths use `Object.defineProperty(navigator, 'wakeLock', { value: undefined, configurable: true, writable: true })`.

2. A conditional install gate matching the project convention verbatim:
```typescript
if (typeof navigator !== 'undefined' && !('wakeLock' in navigator)) {
```

3. A `class FakeWakeLockSentinel extends EventTarget` declaration with EXACTLY these fields:
   - `type: WakeLockType = 'screen'`
   - `released = false`
   - `onrelease: ((this: WakeLockSentinel, ev: Event) => any) | null = null`
   - `async release(): Promise<void>` — body: if `this.released` return early; set `this.released = true`; create `const event = new Event('release')`; if `this.onrelease` is non-null, call it with `this` cast (matches lib.dom.d.ts contract); then call `this.dispatchEvent(event)` so `addEventListener('release', ...)` listeners fire.

4. The install line:
```typescript
Object.defineProperty(navigator, 'wakeLock', {
  writable: true,
  configurable: true,  // allow per-test vi.stubGlobal / Object.defineProperty override (D-09 failure-path tests)
  value: {
    request: vi.fn(async (_type?: WakeLockType) => new FakeWakeLockSentinel()),
  },
})
```

The exact code shape is given in 05-RESEARCH.md "Vitest polyfill" Code Examples (~lines 620-647) and 05-PATTERNS.md "Specific structural elements to mirror for Wake Lock". Use `Object.defineProperty(navigator, ...)` — do NOT use `Object.defineProperty(window.navigator, ...)` unless the install line throws (RESEARCH A3 fallback; only switch if needed).

CRITICAL — the `request: vi.fn(...)` wrapper is load-bearing: it lets tests use `vi.spyOn(navigator.wakeLock, 'request')` AND read call history directly off `navigator.wakeLock.request.mock` without extra setup (PATTERNS line ~437-438).

CRITICAL — `class FakeWakeLockSentinel extends EventTarget` is load-bearing: tests need REAL `EventTarget` semantics because the visibility re-acquire test fires an actual `'release'` event via `dispatchEvent`. Do NOT use `vi.fn()` for `addEventListener`/`dispatchEvent` (PATTERNS line ~436).

Do NOT add an unconditional install (the gate `!('wakeLock' in navigator)` is required so a future jsdom that ships Wake Lock natively becomes a no-op — RESEARCH A5).

Do NOT touch any other line in `vitest.setup.ts`. Do NOT remove the conditional from the existing Storage / AudioContext blocks.

Do NOT add any types-augmentation file or any `@types/dom-screen-wake-lock` package — TypeScript 6.0.2 already bundles `WakeLockSentinel`/`WakeLock`/`WakeLockType`/`Navigator.wakeLock` in `lib.dom.d.ts` (RESEARCH Standard Stack — verified).
  </action>
  <verify>
    <automated>npm run test:run</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "FakeWakeLockSentinel" vitest.setup.ts` returns a number >= 2 (declaration + usage in `request: vi.fn(... new FakeWakeLockSentinel())`).
    - `grep "class FakeWakeLockSentinel extends EventTarget" vitest.setup.ts` finds exactly 1 line (real EventTarget inheritance, not a stub).
    - `grep "Object.defineProperty(navigator, 'wakeLock'" vitest.setup.ts` finds exactly 1 line.
    - `grep "if (typeof navigator !== 'undefined' && !('wakeLock' in navigator))" vitest.setup.ts` finds exactly 1 line.
    - `grep "request: vi.fn(async (_type" vitest.setup.ts` finds exactly 1 line (the request mock with `WakeLockType` parameter).
    - `grep -c "this.dispatchEvent(event)" vitest.setup.ts` returns a number >= 1 (sentinel `release()` body fires the event).
    - `grep "writable: true" vitest.setup.ts | wc -l` increases by exactly 1 vs prior to this change (one new defineProperty install line).
    - `grep "configurable: true" vitest.setup.ts | wc -l` increases by exactly 1 vs prior to this change.
    - `npm run test:run` exits with code 0 — full suite passes with zero regressions on Phase 1–4 tests (A4 smoke).
    - `grep -c "Object.defineProperty(window, 'localStorage'" vitest.setup.ts` is still 1 (existing localStorage install untouched).
    - `grep -c "Object.defineProperty(window, 'AudioContext'" vitest.setup.ts` is still 1 (existing AudioContext install untouched).
    - The new polyfill block is positioned AFTER the existing AudioContext block: `awk '/Object.defineProperty\(window, .AudioContext./{a=NR} /Object.defineProperty\(navigator, .wakeLock./{b=NR} END{exit !(a>0 && b>a)}' vitest.setup.ts` exits 0.
  </acceptance_criteria>
  <done>
    The polyfill is installed conditionally; the full existing test suite passes; no other file is modified.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test environment ↔ jsdom navigator | Polyfill modifies a host-supplied global. No untrusted input crosses this boundary; the value is a static fake under our control. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Tampering | `Object.defineProperty(navigator, 'wakeLock', ...)` install in `vitest.setup.ts` | accept | Test-only file (NOT shipped to production bundle — Vite's test config injects `vitest.setup.ts` only in test runs). The polyfill replaces a missing API in jsdom; in real browsers and the prod bundle the conditional `!('wakeLock' in navigator)` makes it a no-op. No attacker reachability. |
| T-05-02 | Information Disclosure | Fake sentinel `release()` body | accept | No PII, no auth, no network. The fake dispatches a synthetic `'release'` event purely for test assertions. No data leaves the test process. |
| T-05-03 | Elevation of Privilege | Polyfill granting capabilities the host doesn't have | accept | The fake implements ONLY the W3C-spec surface (`request`, `release`, `'release'` event). It cannot keep a real screen awake; it is a behavior stub. Production code paths use the real `navigator.wakeLock`; the polyfill is gated behind `!('wakeLock' in navigator)`. |

**Mitigated risks (non-STRIDE notes):**
- D-09 silent-fallback rejection path: the polyfill itself does not log; per-test rejection mocks via `vi.spyOn(navigator.wakeLock, 'request').mockRejectedValueOnce(...)` are surfaced through the test assertion API only — never to a user-visible surface (PATTERNS Pattern 3 + Plan 02 hook tests). Information leak via console (T-05-02 adjacent) is contained because D-09 explicitly bans console.warn/error and only allows optional `console.debug` (CONTEXT D-09).

**Phase 5 surface summary:** Test-infra-only change. Zero attack surface added (no network, no storage, no auth, no PII, no rendered UI).
</threat_model>

<verification>
- `npm run test:run` exits 0 with the full pre-Phase-5 test surface still passing.
- Manual file inspection: the new block is the LAST polyfill block in `vitest.setup.ts` and follows the same shape as the AudioContext block (conditional gate → fake class declarations → `Object.defineProperty(...)` install line).
- No files outside `vitest.setup.ts` are modified.
</verification>

<success_criteria>
- `vitest.setup.ts` contains a conditional `navigator.wakeLock` polyfill with a `FakeWakeLockSentinel extends EventTarget` and a `vi.fn`-wrapped `request` (verifiable via grep, see acceptance criteria).
- `npm run test:run` passes (Phase 1–4 carry-forward smoke clean).
- Plan 02 (hook + unit tests) and Plan 03 (App integration) can now build on a stable test seam without bisecting "is it the polyfill or the new code that broke things".
</success_criteria>

<output>
After completion, create `.planning/phases/05-mobile-hands-off-resilience/05-01-SUMMARY.md` documenting: the new polyfill block (with line range), the smoke result (`npm run test:run` exit code + pass count), and any A3/A4 surprises (jsdom defineProperty behavior, unexpected test failures with the polyfill in place).
</output>
