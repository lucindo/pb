---
phase: 05.1
slug: hands-off-resilience-polish
status: draft
nyquist_compliant: true
wave_0_complete: false
created: 2026-05-10
---

# Phase 05.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | vitest |
| **Config file** | vite.config.ts (test block) |
| **Quick run command** | `npm test` |
| **Full suite command** | `npm test && npx tsc --noEmit` |
| **Estimated runtime** | ~5 seconds |

---

## Sampling Rate

- **After every task commit:** Run `npm test`
- **After every plan wave:** Run `npm test && npx tsc --noEmit`
- **Before `/gsd-verify-work`:** Full suite must be green + real-Safari UAT must pass (D-43)
- **Max feedback latency:** 5 seconds (jsdom); real-Safari UAT is blocking but manual

---

## Per-Task Verification Map

> Populated from `05.1-06-PLAN.md` task verify blocks. Each row maps a Plan 06 task
> to its automated command (or device-UAT note for Task 8). Status starts ⬜ pending;
> execute-phase flips to ✅ / ❌ / ⚠️ after each task runs.

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| 05.1-06-01 | 06 | 1 | AUDI-01, AUDI-02 | T-05.1-03 (carry-forward, ACCEPT), T-05.1-07 (test-only hooks, ACCEPT) | FakeAudioContext extended with `'interrupted'` state + `_simulateInterrupted` / `_simulateResumeReject` / `dispatchStateChange` test hooks; install gate at `vitest.setup.ts:127` keeps the polyfill jsdom-only — no production exposure (T-05.1-07 ACCEPT). | structural + smoke | `npx tsc --noEmit -p . && node -e "..." && npx vitest run vitest.setup 2>/dev/null; npx vitest run src/audio src/hooks` | ❌ W0 (this task creates the Wave-0 fixtures) | ⬜ pending |
| 05.1-06-02 | 06 | 1 | AUDI-01, AUDI-02 | T-05.1-05 (mitigate — enum-shaped onStateChange), T-05.1-01 (carry-forward, ACCEPT) | Engine forwards every AC `statechange` transition through `opts.onStateChange?.(state)` (D-36); `resume()` narrows silent-absorb to surface `InvalidStateError` as a state-change event (D-38). Callback receives ONLY the enum state value — no error message, stack, or browser code leaks (T-05.1-05). | structural + unit | `npx tsc --noEmit -p . && node -e "..." && npx vitest run src/audio` | ✅ (after Task 1) | ⬜ pending |
| 05.1-06-03 | 06 | 1 | AUDI-01, AUDI-02 | T-05.1-04 (DoS on visibility spam, ACCEPT), T-05.1-09 (reconstruction inside gesture, ACCEPT) | Hook exposes `audioStatus: 'ok' \| 'needs-resume' \| 'unavailable'` and a `resume()` method (D-34). Reconstruction path (D-33) closes old AC + builds new one + replays `setMuted(currentMuted)` (D-35b) + signals re-anchor (D-35) — all inside the gesture context, no setTimeout/Promise.then break (Pitfall 2). Lead-in is NOT replayed (D-35a). | structural + grep gate | `npx tsc --noEmit -p . && node -e "..."` (token-presence + lead-in-replay regex gate; vitest runs as part of Task 5) | ✅ (after Task 1) | ⬜ pending |
| 05.1-06-04 | 06 | 1 | AUDI-02, GUID-01 | T-05.1-06 (aria-live cross-origin, ACCEPT) | MuteToggle gains optional `needsResume?: boolean` prop with priority `audioAvailable=false > needsResume=true > muted` (D-32). When `needsResume`, renders ResumeIcon, accessible name `'Resume audio'`, `aria-pressed` omitted. Phase 2 D-15 hit-area (`size-11 min-h-11 min-w-11`) + focus-ring + reduced-motion classes preserved byte-identical. | unit (component) + structural | `npx tsc --noEmit -p . && node -e "..." && npx vitest run src/components/MuteToggle` | ✅ (after Task 1) | ⬜ pending |
| 05.1-06-05 | 06 | 1 | AUDI-01, AUDI-02 | T-05.1-05 (mitigate), T-05.1-07 (test-only hooks, ACCEPT) | Four-state D-41 matrix (running / suspended / interrupted / closed) — the closed-transition case is DISCRIMINATING (asserts `audioStatus === 'unavailable'` BEFORE stop() resets to `'ok'`, so the test FAILS if the close→unavailable branch is removed). Three reconstruction tests verify close-then-construct, mute replay (D-35b), and that plain resume does NOT re-anchor (D-06 preserved). | unit (hook) | `npx vitest run src/hooks/useAudioCues.test.tsx` | ✅ (after Task 1) | ⬜ pending |
| 05.1-06-06 | 06 | 1 | AUDI-01, AUDI-02, GUID-01 | T-05.1-08 (synthetic gesture spoofing, ACCEPT), T-05.1-09 (reconstruction in click handler, ACCEPT) | App.tsx click handler `onMuteOrResumeClick` awaits `audio.resume()` FIRST when `audioStatus === 'needs-resume'` (gesture-attached chain — D-31/D-33). `onAudioReanchorRequired` callback writes new AC `currentTime` to `audioAnchorRef.current` (D-35). Visually-hidden `<div role="status" aria-live="polite">` announces `'Audio paused, tap to resume'` on transition (D-32b, GUID-01). SessionControls forwards `needsResume` through to MuteToggle. | structural + unit (App + SessionControls) | `npx tsc --noEmit -p . && node -e "..." && grep -E 'needsResume' src/components/SessionControls.tsx && npx vitest run src/app` | ✅ (after Task 1) | ⬜ pending |
| 05.1-06-07 | 06 | 1 | AUDI-01, AUDI-02, GUID-01 | T-05.1-05, T-05.1-09 (both ACCEPT/mitigate carry-forward) | D-42 integration tests on the App tree: morphed MuteToggle renders when `audioStatus === 'needs-resume'`; mute click in resume mode dispatches `resume()` BEFORE `setMuted`; reconstruction preserves muted state (D-35b); `audioAnchorRef` re-anchors on reconstruction (D-35) and is UNCHANGED on plain resume (D-06); aria-live region announces the transition. | integration (App + hook + components) | `npx vitest run src/app/App.audio.test.tsx` | ✅ (after Task 1) | ⬜ pending |
| 05.1-06-08 | 06 | 1 | AUDI-01, AUDI-02, GUID-01 | All Plan 06 threats (T-05.1-04..09) — device confirms mitigations actually work | Real-iPhone iOS Safari BLOCKING gate (D-43). Three scenarios captured with Web Inspector evidence in `05.1-UAT.md`: (1) lock 30s + tap morphed affordance → audio resumes within ≤1 phase boundary; (2) lock 30s + tap mute toggle → audio resumes (D-31 dual recovery); (3) muted-then-locked session returns muted (D-35b on device). Per `.continue-here.md` anti-pattern mitigation: NO jsdom-green-alone sign-off. | manual device UAT (real iPhone, real iOS Safari, Web Inspector) | manual real-iPhone UAT — no automated command; results captured in `.planning/phases/05.1-hands-off-resilience-polish/05.1-UAT.md` | ✅ (after Task 1) | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [ ] Extend `src/audio/FakeAudioContext.ts` with `'interrupted'` state, `_simulateInterrupted()`, `_simulateResumeReject(errorName)`, `dispatchStateChange()` (per RESEARCH.md D-40 hooks)
- [ ] Test fixtures for reconstruction scenarios reuse the SpyableAC pattern from `05.1-01-PLAN.md` test file (lines 308-412), extended with `static lastInstance` so prototype-spy tests can reach the live AC (used by D-41 (d) discriminating assertion)

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Audio resumes after iOS Safari lock/unlock ≥30s | AUDI-01, AUDI-02 | jsdom + FakeAudioContext cannot reproduce iOS `'interrupted'` state, the AC close+reconstruct cycle, or the gesture-attached resume requirement. Real iPhone Safari is the only environment that exercises the full path. | (1) Open session on real iPhone iOS Safari. (2) Lock screen mid-session. (3) Wait ≥30s. (4) Unlock. (5) Tap resume affordance if shown. (6) Verify audio cues play within 1 inhale/exhale cycle. (7) Repeat for ≥3 trials. |
| Resume affordance reachable & labeled | AUDI-02, GUID-01 | Touch-target sizing and aria-live announcement timing must be confirmed on device. | Verify affordance ≥44×44pt, contrast ≥3:1, aria-live region announces state change within 1s. |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies (after planner fills the per-task map)
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references (FakeAudioContext extensions)
- [ ] No watch-mode flags
- [ ] Feedback latency < 5s
- [ ] Device UAT scenario documented (D-43 BLOCKING gate)
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
