---
phase: 16.3
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - .planning/phases/16.3-thorough-theme-revision-inserted-2026-05-13/16.3-01-SUMMARY.md
autonomous: true
requirements: []
honored_constraints: [THEME-05, THEME-UI-01]
tags: [theme, palette-redesign, preflight, baseline, mapping-rubric, dry-run]
notes: |
  Plan count = 7 (1 preflight + 5 per-palette + 1 close). Operator may compress
  later by merging close into Plan 06 if all derived rgb/alpha tokens land
  cleanly per-palette (see 16.3-RESEARCH.md §"Assumptions Log" A4/A5). Default
  ships 7 plans for cleanest bisect surface.

must_haves:
  truths:
    - "Pre-phase contrast baseline captured for all 5 palettes (THEME-05 and D-01 ratios) — recorded in 16.3-01-SUMMARY.md as the 'before' column for delta-reporting in the phase SUMMARY (Plan 07)"
    - "Per-palette pre-phase md5 sentinel captured (one md5 per selector block) — enables 'byte-identical to pre-phase' assertion in Plan 07 if any palette ends up declining redesign"
    - "Mapping rubric (RESEARCH §Pattern 2) end-to-end validated on a single worked example (operator's choice — default Catppuccin Frappé → Dark) via pre-edit contrast calculator, WITHOUT writing src/styles/theme.css"
    - "Token surface inventory confirmed: grep counts return 30 orb tokens + 40 chrome tokens (5 palettes × 6 orb + 5 × 8 chrome) — locks the 'NAMES are locked, only VALUES change' contract"
    - "Frozen guards confirmed green at Wave 0 entry: theme.contrast.test.ts exits 0, theme.no-hardcoded-classes.test.ts exits 0, full chain (tsc + lint + build + test) exits 0"
    - "No edits to src/styles/theme.css in this plan — preflight is read-only against the codebase; only the summary doc is created"
  artifacts:
    - path: ".planning/phases/16.3-thorough-theme-revision-inserted-2026-05-13/16.3-01-SUMMARY.md"
      provides: "Pre-phase baseline (5 palette contrast ratios + 5 selector-block md5s) + mapping rubric dry-run worked example + token sentinels verified + guards green confirmation"
      contains: "Pre-Phase Baseline"
  key_links:
    - from: ".planning/phases/16.3-thorough-theme-revision-inserted-2026-05-13/16.3-01-SUMMARY.md baseline section"
      to: "Per-palette plans 02–06 'Δ vs pre-phase' column in each per-palette SUMMARY"
      via: "Baseline ratio table consumed by each per-palette plan for delta computation"
      pattern: "Δ vs pre-phase"
    - from: ".planning/phases/16.3-thorough-theme-revision-inserted-2026-05-13/16.3-01-SUMMARY.md rubric worked example"
      to: "Plans 02–06 mapping rubric application step"
      via: "Worked example demonstrates the rubric end-to-end so per-palette plans can apply with confidence"
      pattern: "mapping rubric"
---

<objective>
Wave 0 / Plan 01: PREFLIGHT only. No edits to `src/styles/theme.css`. Capture pre-phase baseline (contrast ratios + per-block md5s) for all 5 palettes; confirm frozen guards are green at phase entry; dry-run the mapping rubric on one worked example (operator default: Catppuccin Frappé → Dark) using the pre-edit contrast calculator from RESEARCH §"Pattern 3"; record everything in `16.3-01-SUMMARY.md`.

Purpose: The mapping rubric (RESEARCH §Pattern 2) is the load-bearing new artifact of Phase 16.3 — it routes open-source design-system role slots onto OUR 17-token surface. Before applying it to 5 live palette commits (Plans 02–06), validate it end-to-end on one worked example. Catches polarity inversions (RESEARCH Pitfall 1), D-01 accent-strong/on-accent failures (Pitfall 3), and derived rgb()/alpha drift (Pitfall 2) BEFORE they land in theme.css. Also captures the pre-phase "before" column for the eventual phase SUMMARY's delta table.

Output: Pre-phase ratios + md5s captured (no theme.css edits) + rubric dry-run table for ONE worked palette (Catppuccin Frappé → Dark by default; operator may name a different example) + 16.3-01-SUMMARY.md. 1 docs commit at end.
</objective>

<execution_context>
@$HOME/.claude-personal-personal/get-shit-done/workflows/execute-plan.md
@$HOME/.claude-personal-personal/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/16.3-thorough-theme-revision-inserted-2026-05-13/16.3-RESEARCH.md
@.planning/phases/16.3-thorough-theme-revision-inserted-2026-05-13/16.3-PATTERNS.md
@.planning/phases/16.3-thorough-theme-revision-inserted-2026-05-13/16.3-VALIDATION.md
@.planning/phases/16.2-palette-aesthetic-refresh-inserted-2026-05-13/16.2-SUMMARY.md
@.planning/phases/16.2-palette-aesthetic-refresh-inserted-2026-05-13/16.2-01-SUMMARY.md
@src/styles/theme.css
@src/styles/theme.contrast.test.ts

<interfaces>
The 17-token surface this phase will eventually edit (CANONICAL ORDER per RESEARCH §Pattern 2 + PATTERNS §Pattern 3):

  --color-breathing-bg            (chrome 1/8)
  --color-breathing-bg-soft       (chrome 2/8)
  --color-breathing-bg-edge       (chrome 3/8)
  --color-breathing-surface       (chrome 4/8)
  --color-breathing-accent        (chrome 5/8)
  --color-breathing-accent-strong (chrome 6/8)  ← D-01 contrast pair (vs on-accent)
  --color-breathing-muted         (chrome 7/8)
  --color-breathing-on-accent     (chrome 8/8)  ← D-01 contrast pair (vs accent-strong)
  --color-orb-in-from             (orb 1/6)     ← THEME-05 contrast pair (vs out midpoint)
  --color-orb-in-to               (orb 2/6)
  --color-orb-out-from            (orb 3/6)     ← THEME-05 contrast pair (vs in midpoint)
  --color-orb-out-to              (orb 4/6)
  --color-orb-in-text             (orb 5/6)     ← legibility check
  --color-orb-out-text            (orb 6/6)     ← legibility check
  --color-ring-outer              (derived rgb/alpha — from orb-in-from family)
  --color-ring-inner              (derived rgb/alpha — from orb-out-from family)
  --color-modal-backdrop          (derived rgb/alpha — from deepest neutral)

Per-palette selector blocks in src/styles/theme.css (block boundaries per PATTERNS Pattern 3):

  Plan 16.3-02 — Light — @theme { ... }                       (block ~lines 1–38)
  Plan 16.3-03 — Dark  — [data-theme='dark']:root { ... }     (block ~lines 40–62)
  Plan 16.3-04 — Moss  — [data-theme='moss']:root { ... }     (block ~lines 64–90)
  Plan 16.3-05 — Slate — [data-theme='slate']:root { ... }    (block ~lines 92–118)
  Plan 16.3-06 — Dusk  — [data-theme='dusk']:root { ... }     (block ~lines 120–155)

Note: Exact line numbers may drift slightly from PATTERNS Pattern 3 (file evolved during 16.2). The SELECTOR LINE is the authoritative boundary; use `awk '/@theme {/,/^}/' src/styles/theme.css` (Light) or `awk "/\\[data-theme='dark'\\]:root {/,/^}/" src/styles/theme.css` (Dark etc.) to extract the block reliably.

Pre-phase post-16.2 contrast baseline (from 16.2-SUMMARY.md — reference figures, MUST be re-verified by Task 1's measurement step):

| Palette | THEME-05 (orb midpoint) | D-01 (accent-strong vs on-accent) |
|---------|-------------------------|-----------------------------------|
| Light   | 1.59                    | 1.81                              |
| Dark    | 4.85                    | (computed from test output)       |
| Moss    | 2.36                    | (computed from test output)       |
| Slate   | 3.68                    | (computed from test output)       |
| Dusk    | 1.85                    | (computed from test output)       |

(Light THEME-05 1.59 + Light D-01 1.81 are anchor figures from 16.2-01-SUMMARY.md. The other 4 D-01 ratios were not explicitly numerically captured in 16.2 — extract via `--reporter=verbose` or by temporary `console.log` injection during this preflight.)

The pre-edit contrast calculator (RESEARCH §"Code Examples" lines 420–480) — mirrors theme.contrast.test.ts:20–90 WCAG math exactly. May be saved as a scratch file at `.planning/phases/16.3-thorough-theme-revision-inserted-2026-05-13/scripts/contrast-calc.mjs` OR run inline via `node -e '...'`. Either approach works; the math MUST match the guard test verbatim so the guard never disagrees with the pre-edit calc.

The catalog of MIT-licensed hex-publishing source design systems (RESEARCH §"Standard Stack > Supporting"):
  - Catppuccin (4 flavors: Latte/Frappé/Macchiato/Mocha) — `catppuccin.com/palette` + `github.com/catppuccin/catppuccin`
  - Nord (Polar Night / Snow Storm / Frost / Aurora) — `nordtheme.com/docs/colors-and-palettes`
  - Rosé Pine (Main / Moon / Dawn) — `raw.githubusercontent.com/rose-pine/rose-pine-palette/main/dist/css/rose-pine.css`
  - Tokyo Night (Storm / Night) — `github.com/enkia/tokyo-night-vscode-theme`
  - Solarized (Dark / Light) — `ethanschoonover.com/solarized/`
  - Gruvbox (Dark / Light, soft/medium/hard) — `github.com/morhetz/gruvbox/blob/master/colors/gruvbox.vim`

NO theme.css edits in this plan. The Worked Example writes its candidate mapping into the SUMMARY only, not into theme.css. The first theme.css edit happens in Plan 02 (Light).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Capture pre-phase baseline (contrast ratios + per-block md5s + guards green)</name>
  <files>
    .planning/phases/16.3-thorough-theme-revision-inserted-2026-05-13/16.3-01-SUMMARY.md
  </files>
  <read_first>
    - src/styles/theme.css (full file — 273 lines as of 2026-05-13 close of 16.2; canonical 5-palette token surface)
    - src/styles/theme.contrast.test.ts (full file — to confirm the 10 it() assertion shape: 5 palettes × 2 it() each = 10 pass on baseline)
    - .planning/phases/16.3-thorough-theme-revision-inserted-2026-05-13/16.3-RESEARCH.md §"Pattern 3: Pre-edit contrast calculator" (the canonical WCAG math the calculator must mirror)
    - .planning/phases/16.3-thorough-theme-revision-inserted-2026-05-13/16.3-RESEARCH.md §"Validation Architecture > Phase Requirements → Test Map" (the gate commands)
    - .planning/phases/16.3-thorough-theme-revision-inserted-2026-05-13/16.3-PATTERNS.md §"Pattern 8: Pre-phase baseline snapshot (Wave 0 / Plan 01)" (the per-block md5 extraction recipe)
    - .planning/phases/16.2-palette-aesthetic-refresh-inserted-2026-05-13/16.2-SUMMARY.md (anchor contrast ratios: Light 1.59 / Dark 4.85 / Moss 2.36 / Slate 3.68 / Dusk 1.85 — these are the EXPECTED baseline)
  </read_first>
  <action>
    Pure read-only against the codebase. NO src/styles/theme.css edit. Only artifact written: `16.3-01-SUMMARY.md`. This task captures everything subsequent plans need: pre-phase baseline + sentinels + a confirmation that the frozen guards are green at Wave 0 entry.

    Step 1 — Verify frozen guards green (the gate inputs):
      a. `npm test -- src/styles/theme.contrast.test.ts src/styles/theme.no-hardcoded-classes.test.ts --run` exits 0. Capture stdout (the 10 it() pass for theme.contrast + 10 it() pass for theme.no-hardcoded-classes).
      b. `npx tsc --noEmit` exits 0.
      c. `npm run lint` exits 0.
      d. `npm run build` exits 0.
      e. `npm test -- --run` exits 0 (full suite). Capture pass count for the phase SUMMARY.

    Step 2 — Capture pre-phase contrast ratios (the baseline). The guard test does NOT print ratios on pass — print them via the verbose reporter OR by temporary console.log injection OR (recommended) by running the pre-edit calculator from RESEARCH §"Code Examples" on each palette's current hex values:
      - Extract each palette's 17-token block from src/styles/theme.css (use `awk` per PATTERNS Pattern 3, or read the file once and parse 5 blocks).
      - For each palette, compute via the calculator math (mirror theme.contrast.test.ts:20–90 verbatim):
          inMid    = midpoint(hexToRgb(orb-in-from), hexToRgb(orb-in-to))
          outMid   = midpoint(hexToRgb(orb-out-from), hexToRgb(orb-out-to))
          theme05  = contrastRatio(inMid, outMid)                      ← must be ≥ 1.5
          d01      = contrastRatio(hexToRgb(accent-strong), hexToRgb(on-accent))  ← must be ≥ 1.5
      - Build a 5-row baseline table (Light/Dark/Moss/Slate/Dusk) with columns: THEME-05 ratio, D-01 ratio, In midpoint luminance, Out midpoint luminance.

    Step 3 — Capture per-block md5 sentinels (one md5 per selector block). Per PATTERNS Pattern 8:
      - Light:  `awk '/^@theme \\{/,/^\\}$/' src/styles/theme.css | md5 -q`
      - Dark:   `awk "/^\\[data-theme='dark'\\]:root \\{/,/^\\}$/" src/styles/theme.css | md5 -q`
      - Moss:   `awk "/^\\[data-theme='moss'\\]:root \\{/,/^\\}$/" src/styles/theme.css | md5 -q`
      - Slate:  `awk "/^\\[data-theme='slate'\\]:root \\{/,/^\\}$/" src/styles/theme.css | md5 -q`
      - Dusk:   `awk "/^\\[data-theme='dusk'\\]:root \\{/,/^\\}$/" src/styles/theme.css | md5 -q`
      (Note: `md5 -q` is macOS; on Linux substitute `md5sum | awk '{print $1}'`.)

    Step 4 — Verify token-count sentinels (CRITICAL — locks the "NAMES are locked, only VALUES change" contract from RESEARCH "Anti-Patterns to Avoid"):
      - `grep -cE "^  --color-orb-" src/styles/theme.css` returns **30** (5 palettes × 6 orb tokens)
      - `grep -cE "^  --color-breathing-" src/styles/theme.css` returns **40** (5 palettes × 8 chrome tokens)
      - Total themable: 30 + 40 = 70. Total counts including derived rgb/alpha and layout tokens captured separately for awareness.

    Step 5 — Write `.planning/phases/16.3-thorough-theme-revision-inserted-2026-05-13/16.3-01-SUMMARY.md` containing:
      - Frontmatter: phase 16.3, plan 01, type execute, wave 0, completed `<ISO date>`
      - **Pre-Phase Baseline — Contrast Ratios** (5-row table from Step 2)
      - **Pre-Phase Baseline — Per-Block md5** (5-row table from Step 3 — these are the "byte-identical pre-phase" reference values used by Plans 02–06 acceptance criteria)
      - **Sentinels Verified at Wave 0 Entry** (5-row table: orb count = 30, chrome count = 40, full chain exits 0, theme.contrast exits 0, theme.no-hardcoded-classes exits 0)
      - **Frozen-Guard Confirmation** (one-line confirmations: theme.contrast.test.ts:167 lines unchanged; theme.no-hardcoded-classes.test.ts:63 lines unchanged; theme.alpha-probe.test.ts unchanged; src/domain/settings.ts THEME_OPTIONS unchanged; src/components/BreathingShape.tsx orb-text consumers unchanged; src/index.css token-aware references unchanged)
      - **Catalog of MIT-licensed hex-publishing source design systems** (5-row table from RESEARCH §"Standard Stack > Supporting" — for operator reference at execution of Plans 02–06)
      - (Worked Example deferred to Task 2 — appended to this same SUMMARY)
  </action>
  <verify>
    <automated>npm test -- src/styles/theme.contrast.test.ts src/styles/theme.no-hardcoded-classes.test.ts --run && npx tsc --noEmit && npm run lint && npm run build && npm test -- --run && [ "$(grep -cE '^  --color-orb-' src/styles/theme.css)" -eq 30 ] && [ "$(grep -cE '^  --color-breathing-' src/styles/theme.css)" -eq 40 ] && [ -z "$(git diff --name-only src/styles/theme.css)" ] && [ -f .planning/phases/16.3-thorough-theme-revision-inserted-2026-05-13/16.3-01-SUMMARY.md ]</automated>
  </verify>
  <acceptance_criteria>
    - `src/styles/theme.css` is byte-identical to its pre-task state — `git diff --name-only src/styles/theme.css` returns empty.
    - `grep -cE "^  --color-orb-" src/styles/theme.css` returns 30 (orb token sentinel).
    - `grep -cE "^  --color-breathing-" src/styles/theme.css` returns 40 (chrome token sentinel).
    - `npm test -- src/styles/theme.contrast.test.ts --run` exits 0 (10 it() pass — 5 THEME-05 + 5 D-01).
    - `npm test -- src/styles/theme.no-hardcoded-classes.test.ts --run` exits 0.
    - `npx tsc --noEmit && npm run lint && npm run build && npm test -- --run` exits 0.
    - `.planning/phases/16.3-thorough-theme-revision-inserted-2026-05-13/16.3-01-SUMMARY.md` exists and contains a "Pre-Phase Baseline — Contrast Ratios" 5-row table covering all 5 palettes (Light/Dark/Moss/Slate/Dusk) with both THEME-05 and D-01 columns populated with numeric values.
    - The same SUMMARY contains a "Pre-Phase Baseline — Per-Block md5" 5-row table with one md5 per palette block.
    - The same SUMMARY contains a "Sentinels Verified at Wave 0 Entry" block showing orb count = 30, chrome count = 40, and the green status of the 3 frozen-guard test files.
    - The same SUMMARY contains a "Catalog of MIT-licensed hex-publishing source design systems" reference table (Catppuccin / Nord / Rosé Pine / Tokyo Night / Solarized / Gruvbox).
  </acceptance_criteria>
  <done>
    Pre-phase baseline captured. All 5 palettes' THEME-05 + D-01 ratios + per-block md5s recorded. Frozen guards confirmed green. Token-name-locked contract verified via sentinels (30 orb + 40 chrome). 16.3-01-SUMMARY.md written. No theme.css edits in this task — all changes are SUMMARY-only.
  </done>
</task>

<task type="auto">
  <name>Task 2: Mapping rubric dry-run worked example (operator default: Catppuccin Frappé → Dark)</name>
  <files>
    .planning/phases/16.3-thorough-theme-revision-inserted-2026-05-13/16.3-01-SUMMARY.md
  </files>
  <read_first>
    - .planning/phases/16.3-thorough-theme-revision-inserted-2026-05-13/16.3-RESEARCH.md §"Pattern 2: Mapping rubric" (full table — the rubric this task validates end-to-end)
    - .planning/phases/16.3-thorough-theme-revision-inserted-2026-05-13/16.3-RESEARCH.md §"Code Examples > Example mapping table (Catppuccin Frappé → Dark)" (the indicative example to verify numerically)
    - .planning/phases/16.3-thorough-theme-revision-inserted-2026-05-13/16.3-RESEARCH.md §"Common Pitfalls > Pitfall 1 (polarity)" + §"Pitfall 2 (derived rgb/alpha drift)" + §"Pitfall 3 (D-01 regression)" (the three failure modes this dry-run must surface BEFORE per-palette plans run)
    - .planning/phases/16.3-thorough-theme-revision-inserted-2026-05-13/16.3-PATTERNS.md §"Pattern 9: Per-palette SUMMARY hex table" (the per-token table shape this Worked Example demonstrates)
    - src/styles/theme.css [data-theme='dark']:root block (the block this dry-run shadows — but the dry-run does NOT write into it)
  </read_first>
  <action>
    Goal: Validate the mapping rubric (RESEARCH §Pattern 2) end-to-end on ONE worked example BEFORE per-palette plans 02–06 commit live edits. This task appends a Worked Example section to `16.3-01-SUMMARY.md`. NO src/styles/theme.css edit. NO commit beyond the docs commit at end of plan.

    Step 1 — Worked example source selection: Default to **Catppuccin Frappé** (RESEARCH §"Code Examples" provides the partial mapping; A2 in Assumptions Log flags the numbers as indicative and instructs verification here). If a `16.3-CONTEXT.md` exists with a different operator-specified worked-example source, use that instead.

    Step 2 — Fetch the canonical Catppuccin Frappé hex table. Use `gh api repos/catppuccin/catppuccin/contents/docs/palette.json` OR `WebFetch https://catppuccin.com/palette` (Frappé section). Per RESEARCH Pitfall 8: if WebFetch returns "displays visually" or swatch-only descriptions, switch to `gh api` against `catppuccin/catppuccin` raw repo contents. Expected hex slots needed (16 total): base, mantle, crust, surface0/1/2, text, subtext0/1, overlay0/1, blue, lavender, mauve, peach, yellow, green.

    Step 3 — Apply the mapping rubric (RESEARCH §Pattern 2 table) to produce a candidate 17-token Dark-palette mapping. Build the Per-Token Mapping table (PATTERNS Pattern 9 shape):

      | OUR slot | Source role (Frappé) | Source hex | Rationale |
      |----------|----------------------|------------|-----------|
      | --color-breathing-bg             | base            | <hex> | widest-coverage Dark surface |
      | --color-breathing-bg-soft        | mantle          | <hex> | one tier lighter than bg |
      | --color-breathing-bg-edge        | crust           | <hex> | deepest available |
      | --color-breathing-surface        | surface0        | <hex> | card/dialog inner surface |
      | --color-breathing-accent         | blue            | <hex> | secondary semantic-blue |
      | --color-breathing-accent-strong  | mauve           | <hex> | primary action bg |
      | --color-breathing-muted          | overlay0/1      | <hex> | mid-luminance text |
      | --color-breathing-on-accent      | base            | <hex> | dark text on light mauve |
      | --color-orb-in-from              | lavender        | <hex> | light-pole, blue-family |
      | --color-orb-in-to                | blue            | <hex> | light-pole, blue-family |
      | --color-orb-out-from             | surface1        | <hex> | dark-pole, neutral |
      | --color-orb-out-to               | surface0        | <hex> | dark-pole, neutral |
      | --color-orb-in-text              | crust           | <hex> | darkest text on light In gradient |
      | --color-orb-out-text             | text            | <hex> | lightest text on dark Out gradient |
      | --color-ring-outer               | lavender (channels)  | rgb(R G B / 0.5)   | recomputed from orb-in-from family |
      | --color-ring-inner               | surface1 (channels)  | rgb(R G B / 0.40)  | recomputed from orb-out-from family |
      | --color-modal-backdrop           | crust (channels)     | rgb(R G B / 0.50)  | recomputed from deepest neutral |

    Step 4 — Run the pre-edit contrast calculator (RESEARCH §"Code Examples") against the candidate. Either save it as `.planning/phases/16.3-thorough-theme-revision-inserted-2026-05-13/scripts/contrast-calc.mjs` and run with `node`, OR run inline via `node -e '...'`. The math MUST mirror `theme.contrast.test.ts:20–90` exactly (sRGB→linear with 0.03928 threshold, 0.2126/0.7152/0.0722 channel weights, (max+0.05)/(min+0.05) ratio).

    Verify three invariants:
      a. **THEME-05 polarity:** inMidLum > outMidLum (In is the LIGHT pole). If FAIL, flip In/Out role assignments per RESEARCH Pitfall 1.
      b. **THEME-05 floor:** contrastRatio(inMid, outMid) ≥ 1.5. If FAIL, iterate: lighten In further OR deepen Out further; document the iteration in the SUMMARY.
      c. **D-01 floor:** contrastRatio(accent-strong, on-accent) ≥ 1.5 — per RESEARCH Pitfall 3. If FAIL, swap on-accent to a deeper tone (Dark palette → use source's `base` or `crust` directly).

    Record the three computed numbers in the SUMMARY (the indicative figures from RESEARCH A2 are ~0.49 in-mid lum / ~0.07 out-mid lum / ~4.5 THEME-05 ratio / ~5.1 D-01 ratio — verify or refute).

    Step 5 — DRY-RUN OUTCOME (NOT a theme.css edit). Append to `16.3-01-SUMMARY.md`:
      - **Worked Example: Catppuccin Frappé → Dark (DRY-RUN; no theme.css edit)** section header
      - The 17-row Per-Token Mapping table from Step 3 with `Source hex` and `Rationale` columns populated
      - The computed THEME-05 ratio + D-01 ratio + polarity check verdict from Step 4
      - **Rubric validation verdict:** "rubric end-to-end validated — Frappé → Dark candidate passes both gates" (if all three invariants pass) OR a documented iteration log if any failed and was corrected
      - **Implications for Plans 02–06:** one-paragraph note saying "the rubric is ready to apply to live edits in Plans 02–06; per-palette plans should expect to iterate hex picks WITHIN a source flavor (e.g. Latte/Frappé/Macchiato/Mocha for Catppuccin) before all three invariants pass; the pre-edit calculator should be re-run inline before any theme.css write"
      - **No theme.css mutation occurred in this task** — explicit assertion

    Step 6 — Commit the SUMMARY as a docs commit:
      `docs(16.3-01): Wave 0 preflight — pre-phase baseline + mapping rubric dry-run (Frappé → Dark)`
  </action>
  <verify>
    <automated>[ -z "$(git diff --name-only src/styles/theme.css)" ] && grep -q 'Worked Example' .planning/phases/16.3-thorough-theme-revision-inserted-2026-05-13/16.3-01-SUMMARY.md && grep -q 'Frappé\|Frappe' .planning/phases/16.3-thorough-theme-revision-inserted-2026-05-13/16.3-01-SUMMARY.md && grep -q 'rubric' .planning/phases/16.3-thorough-theme-revision-inserted-2026-05-13/16.3-01-SUMMARY.md && grep -q 'polarity' .planning/phases/16.3-thorough-theme-revision-inserted-2026-05-13/16.3-01-SUMMARY.md && grep -q 'DRY-RUN\|dry-run' .planning/phases/16.3-thorough-theme-revision-inserted-2026-05-13/16.3-01-SUMMARY.md && npm test -- src/styles/theme.contrast.test.ts src/styles/theme.no-hardcoded-classes.test.ts --run && npx tsc --noEmit && npm run lint && npm run build && npm test -- --run</automated>
  </verify>
  <acceptance_criteria>
    - `src/styles/theme.css` is byte-identical to its state at the end of Task 1 (this task is dry-run only) — `git diff --name-only src/styles/theme.css` returns empty.
    - `16.3-01-SUMMARY.md` contains a "Worked Example" section referencing Catppuccin Frappé (or operator-named alternative source).
    - The Worked Example contains a 17-row Per-Token Mapping table for the Dark palette.
    - The Worked Example records the THEME-05 ratio (in-mid vs out-mid) numerically AND the D-01 ratio (accent-strong vs on-accent) numerically.
    - The Worked Example records the polarity check verdict (inMidLum > outMidLum).
    - The Worked Example states that no theme.css mutation occurred.
    - All frozen guards still exit 0: `npm test -- src/styles/theme.contrast.test.ts src/styles/theme.no-hardcoded-classes.test.ts --run` exits 0.
    - Full chain `npx tsc --noEmit && npm run lint && npm run build && npm test -- --run` exits 0.
    - The docs commit for this plan has message starting `docs(16.3-01):` and touches only `.planning/phases/16.3-thorough-theme-revision-inserted-2026-05-13/16.3-01-SUMMARY.md` (and optionally `scripts/contrast-calc.mjs` if the calculator was saved as a scratch file).
  </acceptance_criteria>
  <done>
    Mapping rubric validated end-to-end on Catppuccin Frappé → Dark via the pre-edit contrast calculator. All three invariants checked (polarity, THEME-05 floor, D-01 floor). Per-Token Mapping table written into SUMMARY. Plans 02–06 can now apply the rubric with confidence — failure modes (polarity inversion, derived rgb/alpha drift, D-01 regression) are documented up front.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| no new boundaries | Plan is read-only against the codebase + writes one new SUMMARY markdown file. No CSS edits, no runtime surface change, no new dependencies. Web-fetch of source design-system hex tables is INFORMATION INGRESS to the planning corpus, NOT runtime ingress — hex values copied are facts (numeric triplets), not code. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16.3-01-01 | Tampering | Accidental theme.css edit during preflight | mitigate | Task verify chain asserts `git diff --name-only src/styles/theme.css` is empty. Worked Example is a DRY-RUN; the calculator runs on extracted hex values in a scratch context (node -e or scripts/contrast-calc.mjs), NEVER via an Edit/Write on theme.css. |
| T-16.3-01-02 | Information Disclosure | Web-fetched palette source — license attribution missing | accept | RESEARCH Pitfall 6 + A1: copying hex values from MIT-licensed sources is not a copyright trigger (hex values are facts). Optional per-palette block-comment attribution is a courtesy, not a legal requirement. Solo-developer workflow — operator's discretion. |
| T-16.3-01-03 | Spoofing (a11y) | Stale baseline ratios fail to detect later regression | mitigate | Task 1 captures both THEME-05 and D-01 numerically for all 5 palettes; per-palette plans 02–06 reference these as "Δ vs pre-phase" in their respective SUMMARYs (PATTERNS Pattern 9). Any plan that regresses a non-target palette's ratio is caught by the contrast guard (theme.contrast.test.ts) AND by the SUMMARY delta check. |
| T-16.3-01-04 | Tampering | Polarity inversion in rubric goes undetected until per-palette plan executes | mitigate | Task 2 explicitly runs the polarity check (inMidLum > outMidLum) on the Worked Example. Rubric is validated end-to-end BEFORE Plans 02–06 commit live edits — RESEARCH Pitfall 1 surfaces here, not in production. |
| T-16.3-01-05 | Repudiation | Per-block md5 baseline missing — Plan 07 cannot assert "byte-identical to pre-phase" for any palette intentionally left untouched | mitigate | Task 1 captures one md5 per selector block (5 md5s total) — recorded in 16.3-01-SUMMARY.md. Plan 07 cross-checks. |
</threat_model>

<verification>
After Task 1 + Task 2 complete:

```bash
# Frozen-guard re-confirmation (the actual gate inputs for the rest of the phase):
npm test -- src/styles/theme.contrast.test.ts src/styles/theme.no-hardcoded-classes.test.ts --run
# Expected: 10 it() pass (5 THEME-05 + 5 D-01) + 10 it() pass (10 banned patterns).

# Full chain still green:
npx tsc --noEmit && npm run lint && npm run build && npm test -- --run
# Expected: all exit 0.

# Sentinel re-confirmation:
[ "$(grep -cE '^  --color-orb-' src/styles/theme.css)" -eq 30 ] && echo "orb tokens OK"
[ "$(grep -cE '^  --color-breathing-' src/styles/theme.css)" -eq 40 ] && echo "chrome tokens OK"

# theme.css untouched (this whole plan is preflight + dry-run + docs):
[ -z "$(git diff --name-only src/styles/theme.css)" ] && echo "theme.css byte-identical to pre-plan state"

# SUMMARY artifact exists and references the rubric:
test -f .planning/phases/16.3-thorough-theme-revision-inserted-2026-05-13/16.3-01-SUMMARY.md
grep -q 'rubric' .planning/phases/16.3-thorough-theme-revision-inserted-2026-05-13/16.3-01-SUMMARY.md
grep -q 'polarity' .planning/phases/16.3-thorough-theme-revision-inserted-2026-05-13/16.3-01-SUMMARY.md
grep -q 'Baseline' .planning/phases/16.3-thorough-theme-revision-inserted-2026-05-13/16.3-01-SUMMARY.md
```
</verification>

<success_criteria>
1. Pre-phase baseline contrast ratios (THEME-05 + D-01) captured numerically for all 5 palettes in `16.3-01-SUMMARY.md`.
2. Per-block md5 sentinels captured for all 5 palette selector blocks.
3. Token-count sentinels verified: 30 orb declarations, 40 chrome declarations (locks "NAMES are locked, only VALUES change" contract).
4. Frozen guards confirmed green at Wave 0 entry: `theme.contrast.test.ts`, `theme.no-hardcoded-classes.test.ts`, full chain `tsc + lint + build + test` all exit 0.
5. Mapping rubric validated end-to-end on one worked example (Catppuccin Frappé → Dark by default). Three invariants (polarity, THEME-05 floor, D-01 floor) numerically checked.
6. `src/styles/theme.css` is byte-identical to its pre-plan state (no edits in Wave 0 — this is the contract Wave 0 must hold).
7. `16.3-01-SUMMARY.md` written and committed as a single docs commit. Frontmatter + Pre-Phase Baseline tables + Sentinels Verified block + Frozen-Guard Confirmation + Worked Example section + Catalog of source design systems.
</success_criteria>

<output>
After this plan completes, `.planning/phases/16.3-thorough-theme-revision-inserted-2026-05-13/16.3-01-SUMMARY.md` is the canonical pre-phase baseline artifact. Plans 02–06 reference it for their "Δ vs pre-phase" delta column. Plan 07 cross-references it to assert any unintentionally-untouched palette is byte-identical via md5.
</output>
