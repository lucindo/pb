---
phase: 16.3
plan: 06
type: execute
wave: 5
depends_on: [16.3-05]
files_modified:
  - src/styles/theme.css
autonomous: false
requirements: []
honored_constraints: [THEME-05, THEME-UI-01]
tags: [theme, palette-redesign, css-only, interactive, dusk-palette]

must_haves:
  truths:
    - "Operator names an open-source design system + flavor for Dusk at execution time (Dusk is a Dark-flavor palette with deep-violet background + amber accent — Catppuccin Mocha 'mauve'/'peach', Rosé Pine Main 'iris'/'gold', Tokyo Night Storm 'magenta'/'orange', Nord Polar Night + Aurora 'frost'/'orange' all natural fits)"
    - "Agent fetches source via gh api / raw.githubusercontent.com (RESEARCH Pitfall 8)"
    - "Agent applies RESEARCH §Pattern 2 mapping rubric to [data-theme='dusk']:root block — Dusk is a DARK-flavor palette so use a Dark source flavor"
    - "Dusk-specific: the 16.2 F6+F7 retune was coupled (both In and Out tuned together because both midpoints feed THEME-05). 16.3 redesign of Dusk MUST also tune In and Out together (they remain coupled) and verify polarity (RESEARCH Pitfall 1 — Dusk's deep-purple chrome + amber accent makes polarity inversion easy to slip)"
    - "Pre-edit contrast calculator validates THEME-05 + D-01 + polarity BEFORE write"
    - "ONLY [data-theme='dusk']:root edited. Light (post-Plan-02), Dark (post-Plan-03), Moss (post-Plan-04), Slate (post-Plan-05) all byte-identical"
    - "Per-palette UAT (1-row verdict for Dusk; legibility check is acute because Dusk orb-out-text was the only marginal text token in 16.2 close — 3.83:1 on amber-900 vs new Out midpoint passed only because consumers are large-display)"
    - "Atomic B1 commit; full diff = src/styles/theme.css"
    - "Per-commit green-gate: tsc + lint + build + test exits 0"
  artifacts:
    - path: "src/styles/theme.css"
      provides: "[data-theme='dusk']:root block redesigned from a named source; 17 tokens (coupled In+Out tune)"
      contains: "[data-theme='dusk']:root"
  key_links:
    - from: "src/styles/theme.css [data-theme='dusk']:root block"
      to: "THEME-05 + D-01 contrast guards 'theme=dusk' iterations"
      via: "in/out midpoint + accent-strong/on-accent contrast ≥ 1.5"
      pattern: "data-theme='dusk'"
---

<objective>
Wave 5 / Plan 06: Redesign Dusk palette `[data-theme='dusk']:root` block from an operator-named open-source design system. Same interactive 8-step workflow as Plans 02–05, adapted for Dusk's Dark-flavor source + coupled In+Out tune.

Dusk has the narrowest pre-phase THEME-05 margin (1.85 — closer to the 1.5 floor than any other palette). The 16.2 F6+F7 retune was COUPLED (In and Out tuned together because they both feed the contrast iteration). The 16.3 redesign preserves this coupling: changing Dusk In or Dusk Out alone risks tipping below 1.5. Operator must select a Dark source that has BOTH a light-pole lavender/violet family AND a contrasting amber/gold accent family. Final palette of the per-palette wave; after this Plan 07 (phase close) aggregates the 5/5 verdict + SUMMARY.

Cadence: B1. Output: 1 commit, src/styles/theme.css only.
</objective>

<execution_context>
@$HOME/.claude-personal-personal/get-shit-done/workflows/execute-plan.md
@$HOME/.claude-personal-personal/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/16.3-thorough-theme-revision-inserted-2026-05-13/16.3-RESEARCH.md
@.planning/phases/16.3-thorough-theme-revision-inserted-2026-05-13/16.3-PATTERNS.md
@.planning/phases/16.3-thorough-theme-revision-inserted-2026-05-13/16.3-01-SUMMARY.md
@.planning/phases/16.3-thorough-theme-revision-inserted-2026-05-13/16.3-05-PLAN.md
@src/styles/theme.css
@src/styles/theme.contrast.test.ts

<interfaces>
Dusk palette 17-token surface (selector `[data-theme='dusk']:root` — block boundaries ~ lines 120–155):

Current Dusk state (pre-plan, post-16.2):

  --color-breathing-bg:            #2e1065;   (violet-950)
  --color-breathing-bg-soft:       #4c1d95;   (violet-900)
  --color-breathing-bg-edge:       #1e0550;   (custom deeper violet)
  --color-breathing-surface:       #3b0f8c;   (custom violet-mid)
  --color-breathing-accent:        #fbbf24;   (amber-400)
  --color-breathing-accent-strong: #f59e0b;   (amber-500)
  --color-breathing-muted:         #a78bfa;   (violet-400)
  --color-breathing-on-accent:     #1e0550;   (custom deeper violet)
  --color-orb-in-from:             #ddd6fe;   (violet-200 — set in 16.2 F6)
  --color-orb-in-to:               #ede9fe;   (violet-100 — set in 16.2 F6)
  --color-orb-out-from:             #b45309;  (amber-700 — set in 16.2 F7)
  --color-orb-out-to:              #fde68a;   (amber-200 — set in 16.2 F7)
  --color-orb-in-text:             #4c1d95;   (violet-900)
  --color-orb-out-text:            #78350f;   (amber-900 — 3.83:1 vs amber Out midpoint per 16.2 close; passes large-text WCAG)
  --color-ring-outer:              rgb(237 233 254 / 0.5);   (violet-100 channels)
  --color-ring-inner:              rgb(251 191 36 / 0.45);   (amber-400 channels — already harmonized with orb-out family by 16.2, not a carry-forward)
  --color-modal-backdrop:          rgb(0 0 0 / 0.50);

Pre-phase contrast baseline (from 16.3-01-SUMMARY.md):
  Dusk THEME-05: 1.85 (NARROWEST margin of all 5 palettes — closest to 1.5 floor)
  Dusk D-01:     (captured in 16.3-01 — amber-500 #f59e0b vs violet-deep is ~3+; verify)

Dusk-suitable Dark-flavor sources (the personality is deep-violet bg + warm amber/gold accent):

| Source | Flavor | Why Dusk-suitable | Canonical fetch |
|--------|--------|-------------------|-----------------|
| Catppuccin Mocha | base #1e1e2e; mauve #cba6f7; peach #fab387; yellow #f9e2af | mauve-rich Dark with peach accent | gh api repos/catppuccin/catppuccin |
| Catppuccin Macchiato | base #24273a; mauve #c6a0f6; peach #f5a97f | between Frappé/Mocha; mauve+peach combo | gh api repos/catppuccin/catppuccin |
| Rosé Pine Main | base #191724; iris #c4a7e7; gold #f6c177; rose #ebbcba | iris+gold reads as deep-violet+amber | raw rose-pine-palette CSS |
| Rosé Pine Moon | base #232136; iris #c4a7e7; gold #f6c177 | similar to Main, slightly lifted bg | raw rose-pine-palette CSS |
| Tokyo Night Storm | base #1a1b26; magenta #bb9af7; orange #ff9e64 | magenta-mauve + orange accent | github.com/enkia/tokyo-night-vscode-theme |
| Nord Polar Night + Aurora | nord0 #2e3440; nord15 #b48ead (purple); nord12 #d08770 (orange) | Polar Night bg + Aurora purple/orange | nordtheme.com/docs |

Dusk-specific rubric notes:
  - Chrome: bg in source's darkest deep-violet/dark tier (Mocha base #1e1e2e, Main base #191724, etc.). bg-soft = one tier lighter (typically still dark — e.g. Mocha surface0 #313244). bg-edge = deepest available.
  - Accent = source's warm amber/gold/peach/yellow accent (Mocha peach, Rosé Pine gold, Tokyo Night orange).
  - Accent-strong = source's deeper warm accent OR source's mauve/iris/magenta (D-01 contrast). Verify D-01 ≥ 1.5 with on-accent.
  - On-accent = source's darkest tone (typically base/crust) since accent-strong is light.
  - **Coupled In+Out tune (16.2 F6+F7 lesson):**
    - Orb In = source's LIGHTEST lavender/mauve/iris pair (the LIGHT pole, even in a Dark palette).
    - Orb Out = source's MID-luminance warm family (peach/gold/orange) at LOWER luminance than the In pair.
    - **In midpoint MUST be lighter than Out midpoint.** RESEARCH Pitfall 1 is acute here: source's `mauve` (lum ~0.45) vs `peach` (lum ~0.55) — naive routing would invert polarity.
    - The route that works (per 16.2 F6+F7 ratio 1.85): In = light violet-200/violet-100 (lum ~0.77), Out = mid amber-700/amber-200 (lum ~0.39), ratio 1.85.
    - A source-curated re-mapping might be: In = Mocha lavender + mauve (light pole in Mocha's blue family), Out = Mocha peach + yellow (mid-warm pole). VERIFY POLARITY before write.
  - Orb-in-text on the light violet In gradient = source's deepest violet/mauve text (dark text on light In).
  - Orb-out-text on the mid amber Out gradient = source's deepest warm text OR a deep neutral text. ACUTE legibility check — 16.2 close noted amber-900 #78350f ONLY clears 3.83:1 against the amber-700/amber-200 Out midpoint (passes large-text 3.0, fails body-text 4.5). New mapping should aim to clear 4.5+ if possible.
  - Derived rgb/alpha:
    - ring-outer = rgb(<new orb-in-from channels>) / 0.5..0.7
    - ring-inner = rgb(<new orb-out-from channels>) / 0.35..0.45 (current 16.2 Dusk ring-inner uses amber-400 channels — already harmonized with the orb-out amber family, so this is NOT a carry-forward fix unlike Moss/Slate; but rubric still applies)
    - modal-backdrop = rgb(<deepest tone channels>) / 0.30..0.50

Polarity inversion warning is most acute for Dusk (RESEARCH Pitfall 1) — Dark-flavor source + warm-vs-cool routing requires explicit polarity check.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Dusk palette redesign — prompt operator → fetch → apply rubric (coupled In+Out tune) → edit [data-theme='dusk']:root → guards → UAT → commit</name>
  <files>src/styles/theme.css</files>
  <read_first>
    - src/styles/theme.css (full file — edit ONLY [data-theme='dusk']:root block ~lines 120–155)
    - src/styles/theme.contrast.test.ts ('theme=dusk' iteration — the narrowest pre-phase margin)
    - .planning/phases/16.3-thorough-theme-revision-inserted-2026-05-13/16.3-RESEARCH.md §"Pattern 2"
    - .planning/phases/16.3-thorough-theme-revision-inserted-2026-05-13/16.3-RESEARCH.md §"Pitfall 1 (polarity — acute for Dusk's mauve-vs-peach routing)"
    - .planning/phases/16.3-thorough-theme-revision-inserted-2026-05-13/16.3-PATTERNS.md §"Pattern 7"
    - .planning/phases/16.3-thorough-theme-revision-inserted-2026-05-13/16.3-01-SUMMARY.md (Dusk pre-phase baseline — narrowest 1.85)
    - .planning/phases/16.2-palette-aesthetic-refresh-inserted-2026-05-13/16.2-01-SUMMARY.md (16.2 F6+F7 coupled retune lesson — In and Out must be tuned together)
    - The previous palette plan's commit body (Plan 05 Slate — for cross-palette ratio drift sanity)
  </read_first>
  <action>
    Same interactive 8-step shape as Plans 02–05. `autonomous: false`. KEY DIFFERENCE: explicit coupled In+Out tune (RESEARCH Pitfall 1 + 16.2 F6+F7 lesson).

    Step 1 — PROMPT THE OPERATOR for the Dusk source:
      ```
      Phase 16.3 Plan 06 — Dusk palette redesign (FINAL palette).

      Dusk has the narrowest pre-phase THEME-05 margin (1.85 — closest to 1.5 floor).
      Coupled In+Out tune required (RESEARCH Pitfall 1 + 16.2 F6+F7 lesson).

      Dusk-suitable Dark sources (MIT-licensed, hex-publishing):
        • Catppuccin Mocha       (base #1e1e2e; mauve+peach combo)
        • Catppuccin Macchiato   (base #24273a; mauve+peach)
        • Rosé Pine Main         (base #191724; iris+gold)
        • Rosé Pine Moon         (base #232136; iris+gold)
        • Tokyo Night Storm      (base #1a1b26; magenta+orange)
        • Nord Polar Night + Aurora (nord0 #2e3440 + nord15 purple + nord12 orange)

      Please respond with source + flavor (e.g. "Catppuccin Mocha" or "Rosé Pine Main").
      ```

      Wait for `<chosen-source>`.

    Step 2 — FETCH canonical hex table per RESEARCH Pitfall 8.

    Step 3 — APPLY rubric (RESEARCH §Pattern 2) — Dusk-specific routing with COUPLED In+Out:
      - Chrome: bg/bg-soft/bg-edge from source's darkest tiers (deep-violet/deep-neutral). Surface = source's surface0 or one-tier-lifted. Accent = source's warm peach/gold/orange. Accent-strong = source's deeper warm. Muted = source's overlay/subtext. On-accent = source's base/crust (dark on light warm).
      - **Orb In + Orb Out coupled tune:**
        - Orb In = source's LIGHTEST cool family pair (mauve/lavender/iris pastel) — light pole. Aim for inMidLum ~0.65–0.80.
        - Orb Out = source's MID-luminance warm family pair (peach/gold/orange) — dark pole RELATIVE TO In. Aim for outMidLum ~0.35–0.50.
        - **Polarity check is acute here. Run pre-edit calculator BEFORE writing — if inMidLum < outMidLum, re-route (the typical fix: use source's lightest violet/lavender + a tier-lighter blend for In, and source's mid-amber-tier pair for Out, not the brightest amber).**
        - Target ratio: aim for ≥ 1.85 (matching or improving on 16.2 close) — but ≥ 1.5 is the hard gate.
      - Orb-in-text = source's deepest violet/mauve text. Verify ≥ WCAG AA on new In midpoint.
      - Orb-out-text = source's deepest warm text OR a deep neutral. ACUTE legibility — aim for ≥ 4.5 if possible (16.2 only cleared 3.83 large-text).
      - Derived: ring-outer + ring-inner + modal-backdrop recomputed from new families.

    Step 4 — RUN pre-edit contrast calculator. Verify THREE invariants explicitly. Document the iteration count if the operator's source needs >1 hex pick attempt to clear all three (Dusk's narrow margin makes this likely).

    Step 5 — EDIT `src/styles/theme.css` `[data-theme='dusk']:root` block ONLY. UPDATE comment block above to reference source + note the coupled In+Out tune.

    DO NOT TOUCH: @theme (post-Plan-02), [data-theme='dark']:root (post-Plan-03), [data-theme='moss']:root (post-Plan-04), [data-theme='slate']:root (post-Plan-05), .orb rules, @media block, .tsx files, test files.

    Step 6 — RUN guards.

    Step 7 — PER-PALETTE UAT for Dusk (with explicit legibility focus):
      ```
      Phase 16.3 Plan 06 — Dusk palette UAT (final palette before phase close).

      1. Open http://localhost:5173/hrv/ in Chrome.
      2. SettingsDialog → Dusk → close.
      3. Orb idle: In gradient (lavender/mauve) feels integrated with deep-violet chrome — NOT punching through as near-white?
      4. Start session → 3-2-1 lead-in digit legibility on the new (deeper-than-16.2) In gradient?
      5. ≥2 In→Out→In transitions:
         - Out gradient (warm peach/gold/orange) reads as "exhale warmth" against the violet chrome?
         - **Active phase label LEGIBILITY on the Out gradient — critical check; 16.2 close noted Dusk orb-out-text only clears 3.83 large-text. New mapping should improve.**
         - In and Out perceptually distinct?
      6. Stop. SettingsDialog renders correctly in Dusk?

      | 5 | Dusk | dusk | <chosen-source> | bg <hex>, surface <hex>, accent <hex> | <in family> | <out family> | approved/rejected | pass/fail | pass/fail | source citation |

      Reply `approved (Dusk)` + row, OR `rejected: <observation>`.
      Loop bound: ≥3 distinct-source-system rejections → `phase-blocked`.
      ```

    Step 8 — COMMIT:
      `feat(16.3-06): redesign Dusk palette from <chosen-source> (<flavor> flavor); coupled In+Out tune`

      Body: source citation + ratios (THEME-05 + D-01 + Δ vs pre-phase 1.85) + explicit polarity verification (inMidLum X > outMidLum Y) + orb-out-text legibility ratio.
  </action>
  <verify>
    <automated>npm test -- src/styles/theme.contrast.test.ts src/styles/theme.no-hardcoded-classes.test.ts --run && npx tsc --noEmit && npm run lint && npm run build && npm test -- --run && [ "$(grep -cE '^  --color-orb-' src/styles/theme.css)" -eq 30 ] && [ "$(grep -cE '^  --color-breathing-' src/styles/theme.css)" -eq 40 ] && [ "$(git diff --name-only HEAD~1 HEAD)" = "src/styles/theme.css" ]</automated>
  </verify>
  <acceptance_criteria>
    - `[data-theme='dusk']:root` block redesigned from a named open-source design system (operator-chosen Dark flavor).
    - At least one of `--color-breathing-bg`, `--color-breathing-accent`, `--color-orb-in-from`, `--color-orb-out-from` differs from pre-plan value.
    - Both orb-in (from+to) AND orb-out (from+to) hex values are updated (coupled In+Out tune required for Dusk).
    - All 8 chrome + 6 orb tokens are hex literals; 3 derived use `rgb(R G B / alpha)`.
    - Light (post-Plan-02) / Dark (post-Plan-03) / Moss (post-Plan-04) / Slate (post-Plan-05) blocks byte-identical to their respective baselines.
    - `grep -cE "^  --color-orb-" src/styles/theme.css` returns 30; `grep -cE "^  --color-breathing-" src/styles/theme.css` returns 40.
    - `git diff --name-only HEAD~1 HEAD` returns exactly `src/styles/theme.css`.
    - `npm test -- src/styles/theme.contrast.test.ts --run` exits 0.
    - `npx tsc --noEmit && npm run lint && npm run build && npm test -- --run` exits 0.
    - Polarity invariant (Dusk inMidLum > outMidLum) explicitly verified and documented in commit body.
    - THEME-05 ≥ 1.5 (target ≥ 1.85 if achievable); D-01 ≥ 1.5.
    - D-01 regression check: if post-redesign D-01 < pre-phase D-01 captured in Plan 01 baseline, document the regression rationale + Δ in the commit body. ≥1.5 floor must hold but any reduction must be intentional and noted (Dusk's amber-vs-violet pair is the highest-risk D-01 vector).
    - Per-palette UAT: operator returned `approved (Dusk)` with completed verdict row before commit.
    - Commit subject pattern `feat(16.3-06): redesign Dusk palette from <source> (<flavor>); coupled In+Out tune`.
  </acceptance_criteria>
  <done>
    Dusk palette redesigned from named Dark-flavor source with coupled In+Out tune (RESEARCH Pitfall 1 + 16.2 F6+F7 lesson observed). Polarity invariant verified. All other 4 blocks byte-identical. Per-palette UAT approved. Atomic B1 commit closes the per-palette wave; Plan 07 (phase close) follows.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| no new boundaries | Same as Plans 02–05 — pure CSS value substitution. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16.3-06-01 | Spoofing (a11y) | Dusk THEME-05 floor breached (narrowest pre-phase margin 1.85 → easy to tip below 1.5) | mitigate | Pre-edit calculator validates ratio BEFORE write. Acceptance criterion + commit body record post-redesign ratio. Iterate hex picks if first attempt drops below 1.5. |
| T-16.3-06-02 | Tampering | Polarity inversion (acute for Dusk — source's mauve and peach are close in luminance) | mitigate | Step 3 explicitly routes In to source's LIGHTEST cool pair AND Out to source's MID-luminance warm pair. Pre-edit calculator polarity check is the auto-gate. RESEARCH Pitfall 1 explicit on Dusk. |
| T-16.3-06-03 | Spoofing (a11y) | Orb-out-text legibility marginal (16.2 close: 3.83 large-text only) | mitigate | UAT Step 7 specifically calls out legibility on the Out gradient. Rubric Step 3 aims for ≥ 4.5 if achievable. Commit body records the post-redesign orb-out-text vs Out-mid ratio. |
| T-16.3-06-04 | Tampering | Decoupled In or Out tune breaks the F6+F7 coupling (16.2 lesson — adjusting one alone tips ratio below 1.5) | mitigate | Acceptance criterion requires BOTH orb-in (from+to) AND orb-out (from+to) hex values to update. Commit subject says "coupled In+Out tune". |
| T-16.3-06-05 | Tampering | Cross-palette regression | mitigate | Md5 cross-check + scope guard. |
| T-16.3-06-06 | Denial of Service | UAT rejection loop spins | mitigate | W3 fix: 3-rejection bound. |
</threat_model>

<verification>
After Task 1:

```bash
# Standard chain (same as Plans 02–05):
npm test -- src/styles/theme.contrast.test.ts --run
npm test -- src/styles/theme.no-hardcoded-classes.test.ts --run
[ "$(grep -cE '^  --color-orb-' src/styles/theme.css)" -eq 30 ]
[ "$(grep -cE '^  --color-breathing-' src/styles/theme.css)" -eq 40 ]
[ "$(git diff --name-only HEAD~1 HEAD)" = "src/styles/theme.css" ]
npx tsc --noEmit && npm run lint && npm run build && npm test -- --run

# Coupled In+Out verification — both pairs should differ from pre-plan:
awk "/^\[data-theme='dusk'\]:root \{/,/^\}$/" src/styles/theme.css | grep -E "^  --color-orb-(in|out)-(from|to)"

# All 5 palettes complete — phase-level diff for the 5 per-palette commits:
git diff --name-only HEAD~5 HEAD
# Expected: src/styles/theme.css (single file — all 5 per-palette plans honor scope guard)
```
</verification>

<success_criteria>
1. [data-theme='dusk']:root block redesigned from an operator-named Dark-flavor open-source design system.
2. THEME-05 ≥ 1.5 (target ≥ 1.85); D-01 ≥ 1.5; polarity invariant holds.
3. Coupled In+Out tune: BOTH orb-in (from+to) AND orb-out (from+to) hex values updated.
4. Other 4 palette blocks byte-identical to their respective baselines.
5. Token-name contract: 30 orb + 40 chrome.
6. theme.no-hardcoded-classes.test.ts exits 0; full chain exits 0.
7. Per-palette UAT for Dusk approved BEFORE commit.
8. Atomic B1 commit; subject `feat(16.3-06): redesign Dusk palette from <source> (<flavor>); coupled In+Out tune`.
9. Phase-level diff `git diff --name-only HEAD~5 HEAD` returns exactly `src/styles/theme.css` (5 atomic per-palette commits over Plans 02–06).
</success_criteria>

<output>
After Task 1, all 5 palettes are redesigned. No per-palette SUMMARY file is written — the atomic B1 commit's body carries the per-palette mapping/ratios/UAT row, and Plan 07 (phase close) aggregates all 5 per-palette UAT verdicts into the single phase SUMMARY + updates STATE/ROADMAP.
</output>
