---
phase: 19-language-switching
plan: 04
type: execute
wave: 2
depends_on: ["19-01"]
files_modified:
  - src/hooks/useLocale.ts
  - src/hooks/useLocale.test.ts
autonomous: true
requirements:
  - I18N-01
  - I18N-03
must_haves:
  truths:
    - "useLocale returns { locale: LocaleId, uiStrings: UiStrings } seeded from loadPrefs().locale"
    - "On locale change, document.documentElement.lang is updated to the new LocaleId"
    - "Cross-tab 'storage' events with key === STATE_KEY re-read loadPrefs().locale and update state"
    - "Same-tab 'hrv:prefs-changed' events with detail.key === 'locale' (or undefined) re-read and update state"
    - "useLocale has exactly 3 useEffect blocks (apply lang, cross-tab storage, same-tab prefs-changed)"
  artifacts:
    - path: "src/hooks/useLocale.ts"
      provides: "App-side orchestrator hook returning resolved uiStrings"
      contains: "export function useLocale"
    - path: "src/hooks/useLocale.test.ts"
      provides: "seed + 3 effect tests"
      contains: "documentElement.lang"
  key_links:
    - from: "src/hooks/useLocale.ts"
      to: "src/content/strings.ts"
      via: "UI_STRINGS lookup on locale change"
      pattern: "UI_STRINGS\\[locale\\]"
    - from: "src/hooks/useLocale.ts"
      to: "src/storage/prefs.ts"
      via: "loadPrefs seed + listener re-read"
      pattern: "loadPrefs\\(\\).locale"
    - from: "src/hooks/useLocale.ts"
      to: "window"
      via: "storage + hrv:prefs-changed listeners"
      pattern: "addEventListener.*storage|hrv:prefs-changed"
---

<objective>
Create `src/hooks/useLocale.ts` — App-side orchestrator hook for the locale dimension. Mirrors `useVisualVariant.ts` structurally (2 listener effects) plus a 3rd `useEffect([locale])` that writes `document.documentElement.lang` (analogous to how `useTheme.ts` writes `documentElement.dataset.theme` per D-07 minus the system-mode branch). Returns `{ locale, uiStrings: UI_STRINGS[locale] }` per D-06. Plus `src/hooks/useLocale.test.ts` covering seed, both listener effects, and the `documentElement.lang` write.

Purpose: App.tsx invokes `useLocale()` once (Plan 07) and drills `uiStrings.*` slices to ~15 children. The 3-effect listener pattern keeps the picker write-path and the App-state read-path decoupled (D-05).

Output: `useLocale.ts` (~60 lines) + `useLocale.test.ts`. Tests pass.
</objective>

<execution_context>
@$HOME/.claude-personal-personal/get-shit-done/workflows/execute-plan.md
@$HOME/.claude-personal-personal/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/19-language-switching/19-CONTEXT.md
@.planning/phases/19-language-switching/19-PATTERNS.md
@.planning/phases/19-language-switching/19-RESEARCH.md
@.planning/phases/19-language-switching/19-01-SUMMARY.md

<interfaces>
From src/hooks/useVisualVariant.ts (structural mirror — 2 effects, no system-mode branch):
```typescript
import { useEffect, useState } from 'react'
import { loadPrefs } from '../storage/prefs'
import { STATE_KEY } from '../storage'
import type { VisualVariantId } from '../domain/settings'

export function useVisualVariant(): { variant: VisualVariantId; setVariant: (next: VisualVariantId) => void } {
  const [variant, setVariant] = useState<VisualVariantId>(() => loadPrefs().variant)
  // Effect: cross-tab 'storage' listener (empty deps)
  // Effect: same-tab 'hrv:prefs-changed' listener (empty deps, filtered on detail.key === 'variant')
  return { variant, setVariant }
}
```

From src/hooks/useTheme.ts (apply-effect mirror — line 31–34 dataset.theme write):
```typescript
useEffect(() => {
  if (theme === 'system') return
  document.documentElement.dataset.theme = theme
}, [theme])
```

Phase 19 useLocale signature (D-06):
```typescript
export function useLocale(): { locale: LocaleId; uiStrings: UiStrings }
```

UI_STRINGS from Plan 01:
```typescript
import { UI_STRINGS, type UiStrings } from '../content/strings'
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create src/hooks/useLocale.ts (orchestrator with 3 effects)</name>
  <files>src/hooks/useLocale.ts</files>
  <read_first>
    - src/hooks/useVisualVariant.ts (entire 50 lines — structural mirror for cross-tab + same-tab listeners)
    - src/hooks/useTheme.ts (lines 30–34 — apply-effect pattern for documentElement write; lines 60–89 — listener pattern)
    - src/content/strings.ts (Plan 01 output — `UI_STRINGS` + `UiStrings` exports)
    - .planning/phases/19-language-switching/19-PATTERNS.md (lines 234–306 — useLocale.ts pattern with 3 effects)
    - .planning/phases/19-language-switching/19-CONTEXT.md (D-05/D-06/D-07/D-11a — hook split, return shape, lang write, no FOUC script)
  </read_first>
  <behavior>
    - `useLocale()` returns `{ locale: LocaleId, uiStrings: UiStrings }`.
    - Initial state is seeded via `useState<LocaleId>(() => loadPrefs().locale)` (lazy seed; Phase 14 D-17 coerce guarantees a valid LocaleId).
    - Effect 1 (apply lang, dep `[locale]`): writes `document.documentElement.lang = locale`. No system-mode branch (D-07).
    - Effect 2 (cross-tab storage, empty deps): listens for `'storage'` events; when `e.key === STATE_KEY`, calls `setLocale(loadPrefs().locale)`.
    - Effect 3 (same-tab prefs-changed, empty deps): listens for `'hrv:prefs-changed'` CustomEvents; when `detail.key === 'locale'` OR `detail.key === undefined`, calls `setLocale(loadPrefs().locale)`.
    - Return value: `{ locale, uiStrings: UI_STRINGS[locale] }`.
    - Empty deps `[]` on Effects 2 & 3 are correct per D-25 (`setLocale` from useState is stable; `loadPrefs`, `STATE_KEY`, `UI_STRINGS` are module-level).
    - `useLocale` does NOT call `savePrefs` (D-05 — picker-side `useLocaleChoice` owns that path).
  </behavior>
  <action>
    Create `src/hooks/useLocale.ts` (NEW). Structure based on useVisualVariant.ts shell + useTheme.ts apply-effect pattern.

    Header comment block (style: useVisualVariant.ts:1–8) citing Phase 19 I18N-01..07; explain the 3-effect structure (apply lang, cross-tab storage, same-tab prefs-changed); note that the gated mql effect from useTheme.ts has NO analog here (locale has no system-mode counterpart per D-07); note that `useLocale` does NOT call `savePrefs` (D-05 separation).

    Imports:
    - `import { useEffect, useState } from 'react'`
    - `import { loadPrefs } from '../storage/prefs'`
    - `import { STATE_KEY } from '../storage'`
    - `import type { LocaleId } from '../domain/settings'`
    - `import { UI_STRINGS, type UiStrings } from '../content/strings'`

    Function signature: `export function useLocale(): { locale: LocaleId; uiStrings: UiStrings }`.

    Body:
    1. `const [locale, setLocale] = useState<LocaleId>(() => loadPrefs().locale)` — lazy seed.
    2. Effect 1 (apply): `useEffect(() => { document.documentElement.lang = locale }, [locale])`. Comment: "Apply effect — write documentElement.lang on every locale change (D-07; no FOUC script in index.html)".
    3. Effect 2 (cross-tab): verbatim from useVisualVariant.ts:20–30 but with `loadPrefs().variant → loadPrefs().locale` and `setVariant → setLocale`. Empty deps `[]`. Comment: "Cross-tab 'storage' listener — A-04 mirror".
    4. Effect 3 (same-tab): verbatim from useVisualVariant.ts:34–46 but with `detail.key === 'variant' → detail.key === 'locale'` and `setVariant(loadPrefs().variant) → setLocale(loadPrefs().locale)`. Keep `|| detail.key === undefined` branch for forward-compat (per useTheme.ts:81 comment). Empty deps `[]`. Comment: "Same-tab 'hrv:prefs-changed' listener — D-21 contract reuse".
    5. Return: `return { locale, uiStrings: UI_STRINGS[locale] }`.

    NO additional effects, NO `savePrefs` calls, NO `eslint-disable` lines (D-24).
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -10 && grep -c "useEffect" src/hooks/useLocale.ts && grep -c "savePrefs" src/hooks/useLocale.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/hooks/useLocale.ts` exists.
    - `npx tsc --noEmit` exits 0.
    - `grep -c "useEffect" src/hooks/useLocale.ts` returns 3 (apply + storage + prefs-changed; no mql).
    - `grep -c "savePrefs" src/hooks/useLocale.ts` returns 0 (D-05 — picker owns writes).
    - `grep -c "documentElement.lang" src/hooks/useLocale.ts` returns 1 (apply effect).
    - `grep -c "detail.key === 'locale'" src/hooks/useLocale.ts` returns 1.
    - `grep -c "detail.key === undefined" src/hooks/useLocale.ts` returns 1 (forward-compat branch).
    - `grep -c "UI_STRINGS\\[locale\\]" src/hooks/useLocale.ts` returns 1 (return value).
    - `grep -c "eslint-disable" src/hooks/useLocale.ts` returns 0.
  </acceptance_criteria>
  <done>
    `useLocale.ts` exists with 3 effects, returns `{ locale, uiStrings }`, no savePrefs call, no eslint-disable lines.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create src/hooks/useLocale.test.ts (seed + 3 effect tests)</name>
  <files>src/hooks/useLocale.test.ts</files>
  <read_first>
    - src/hooks/useTheme.test.ts (lines 1–50 — beforeEach pattern; lines 118–200 — cross-tab + same-tab listener tests; pattern target)
    - src/hooks/useLocale.ts (the file just created in Task 1)
    - src/hooks/useVisualVariant.test.ts (closest 2-effect analog)
    - .planning/phases/19-language-switching/19-PATTERNS.md (lines 310–360 — useLocale.test.ts pattern)
  </read_first>
  <behavior>
    - Test 1 (seed): seed localStorage with `locale: 'en'`, renderHook, assert `result.current.locale === 'en'` and `result.current.uiStrings.controls.startSession.length > 0`.
    - Test 2 (apply effect): seed `locale: 'en'`, renderHook, assert `document.documentElement.lang === 'en'` after mount.
    - Test 3 (apply effect on change): seed `locale: 'en'`, renderHook, then write `locale: 'pt-BR'` and dispatch a `storage` event with `key: STATE_KEY`, await act, assert `document.documentElement.lang === 'pt-BR'`.
    - Test 4 (cross-tab storage listener): renderHook with seeded `'en'`, write `'pt-BR'` to localStorage + dispatch `StorageEvent` with `key: STATE_KEY`, assert `result.current.locale === 'pt-BR'`.
    - Test 5 (cross-tab storage ignored for non-STATE_KEY): renderHook with seeded `'en'`, dispatch `StorageEvent` with `key: 'other-key'`, assert `result.current.locale === 'en'` (unchanged).
    - Test 6 (same-tab prefs-changed filtered on detail.key === 'locale'): renderHook with seeded `'en'`, write `'pt-BR'` to localStorage, dispatch `new CustomEvent('hrv:prefs-changed', { detail: { key: 'locale', value: 'pt-BR' } })`, assert `result.current.locale === 'pt-BR'`.
    - Test 7 (same-tab prefs-changed ignored for non-locale key): renderHook with seeded `'en'`, write `'pt-BR'` to localStorage, dispatch `new CustomEvent('hrv:prefs-changed', { detail: { key: 'theme', value: 'dark' } })`, assert `result.current.locale === 'en'` (unchanged — locale filter rejects non-locale keys).
    - Test 8 (same-tab prefs-changed forward-compat: detail.key === undefined re-reads): renderHook with seeded `'en'`, write `'pt-BR'` to localStorage, dispatch `new CustomEvent('hrv:prefs-changed', { detail: {} })`, assert `result.current.locale === 'pt-BR'` (undefined key branch re-reads).
    - All tests pass under `npx vitest run src/hooks/useLocale.test.ts`.
  </behavior>
  <action>
    Create `src/hooks/useLocale.test.ts` (NEW). Imports: `describe`, `expect`, `it`, `beforeEach`, `afterEach`, `vi` from `'vitest'`; `renderHook`, `act` from `'@testing-library/react'`; `useLocale` from `'./useLocale'`; `STATE_KEY` from `'../storage'`; `type { UserPrefs }` from `'../storage/prefs'`.

    Helper at top of file: `function seedPrefs(prefs: UserPrefs): void` writing `localStorage.setItem(STATE_KEY, JSON.stringify({ version: 1, prefs }))` (mirror useTimbreChoice.test.ts:10–12).

    Constant: `const DEFAULT_FULL_PREFS: UserPrefs = { theme: 'system', timbre: 'bowl', variant: 'orb', locale: 'en' }`.

    `beforeEach`: `window.localStorage.clear(); document.documentElement.lang = ''` (test isolation — reset both stores).

    `afterEach`: `window.localStorage.clear(); document.documentElement.lang = ''; vi.restoreAllMocks()`.

    Eight `it` blocks per the behavior list above. Use `act` (sync arrow returning void) for state-change side effects. For the storage event test:
    ```
    const newEnvelope = JSON.stringify({ version: 1, prefs: { ...DEFAULT_FULL_PREFS, locale: 'pt-BR' } })
    act(() => {
      window.localStorage.setItem(STATE_KEY, newEnvelope)
      window.dispatchEvent(new StorageEvent('storage', { key: STATE_KEY, newValue: newEnvelope, oldValue: null }))
    })
    ```

    For the apply-effect mount check (Test 2), assert `document.documentElement.lang === 'en'` immediately after `renderHook`.

    For Test 3 (apply effect on change), pair the storage-event dispatch with an assertion on `document.documentElement.lang === 'pt-BR'` AFTER the act block completes — this verifies Effect 1 fires on state change.

    Use `// eslint-disable-next-line @typescript-eslint/no-non-null-assertion` only where strictly required (e.g. typed `localStorage.getItem(STATE_KEY)!` reads), with `// Reason:` comment per Phase 7 D-04.
  </action>
  <verify>
    <automated>npx vitest run src/hooks/useLocale.test.ts 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/hooks/useLocale.test.ts` exits 0.
    - Output reports at least 8 passing tests.
    - `grep -c "it(" src/hooks/useLocale.test.ts` returns >= 8.
    - `grep -c "document.documentElement.lang" src/hooks/useLocale.test.ts` returns >= 4 (resetters in beforeEach/afterEach + 2 apply-effect assertions = at least 4 occurrences).
    - `grep -c "StorageEvent" src/hooks/useLocale.test.ts` returns >= 1.
    - `grep -c "hrv:prefs-changed" src/hooks/useLocale.test.ts` returns >= 3 (three CustomEvent tests).
    - `grep -c "detail.key === undefined\\|detail: {}" src/hooks/useLocale.test.ts` returns >= 1 (forward-compat test).
  </acceptance_criteria>
  <done>
    `useLocale.test.ts` covers seed, apply effect on mount, apply effect on change, cross-tab key match, cross-tab key mismatch, same-tab key match, same-tab key mismatch, same-tab undefined-key forward-compat. 8 tests pass.
  </done>
</task>

<task type="auto">
  <name>Task 3: Per-commit green-gate (D-16)</name>
  <files>(no source edit — verification only)</files>
  <read_first>
    - .planning/phases/19-language-switching/19-CONTEXT.md (D-16)
  </read_first>
  <action>
    Run `npx tsc --noEmit && npm run lint && npm run build && npm test -- --run`. After exit 0, commit `feat(19-04): useLocale orchestrator hook + tests` containing only `src/hooks/useLocale.ts` and `src/hooks/useLocale.test.ts`.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run lint && npm run build && npm test -- --run 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - All 4 green-gate steps exit 0.
    - Commit `feat(19-04): useLocale orchestrator hook + tests` created.
    - `git diff HEAD~1 --stat` shows only 2 files: `src/hooks/useLocale.ts`, `src/hooks/useLocale.test.ts`.
  </acceptance_criteria>
  <done>
    Green-gate passes; commit landed; no collateral file changes.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| storage event listener → loadPrefs read | Cross-tab StorageEvent triggers a re-read of `loadPrefs().locale`. Trust input shape: `e.key === STATE_KEY` filter rejects unrelated storage writes; `loadPrefs()` coercer (Phase 14) defends against bad values. |
| hrv:prefs-changed listener → loadPrefs read | Same-tab CustomEvent. Trust input shape: `instanceof CustomEvent` + `detail.key` filter. |
| useEffect → document.documentElement.lang write | App-DOM write. Only writes a LocaleId (enum-narrowed); no user-controlled string. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-10 | Spoofing | CustomEvent 'hrv:prefs-changed' | mitigate | `instanceof CustomEvent` runtime check + `detail.key === 'locale' || undefined` filter; loadPrefs coercer narrows the actual value to `LocaleId`. Same-origin window event only. |
| T-19-11 | Tampering | document.documentElement.lang | mitigate | Value is `setState<LocaleId>` — TypeScript enum-narrowed to `'en' \| 'pt-BR'`. No user-controlled string reaches DOM. |
| T-19-12 | Information Disclosure | useLocale effect on `[locale]` running cross-renders | accept | Per D-11a — cross-tab locale change mid-session updates BreathingShape strings prop, which is desirable behavior. Timing is unaffected. |
</threat_model>

<verification>
- `npx tsc --noEmit && npm run lint && npm run build && npm test -- --run` exits 0.
- `useLocale` has exactly 3 effects; no `savePrefs` call.
- 8 tests pass covering seed, apply effect, both listener filters (positive + negative), and forward-compat undefined-key branch.
- No edits to `src/domain/settings.ts`, `src/storage/prefs.ts`, or `src/content/strings.ts` (D-18 + Plan 01 invariant).
</verification>

<success_criteria>
1. `useLocale` returns `{ locale, uiStrings: UI_STRINGS[locale] }` per D-06.
2. `document.documentElement.lang` is written via `useEffect([locale])` only — no FOUC inline script in index.html (D-07).
3. Cross-tab + same-tab listeners mirror useVisualVariant pattern verbatim modulo the `locale` filter.
4. No `savePrefs` call (D-05 separation from useLocaleChoice).
5. 8 tests pass including forward-compat `detail.key === undefined` branch.
6. Green-gate exits 0 after the commit.
</success_criteria>

<output>
After completion, create `.planning/phases/19-language-switching/19-04-SUMMARY.md` capturing: 2 files created, total test count (8), effect-count verification (3 effects, no mql), confirmation that no savePrefs call exists.
</output>
