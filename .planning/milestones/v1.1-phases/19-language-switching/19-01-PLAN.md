---
phase: 19-language-switching
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/content/strings.ts
  - src/content/strings.test.ts
autonomous: true
requirements:
  - I18N-04
  - I18N-07
must_haves:
  truths:
    - "UI_STRINGS exposes a nested UiStrings catalog for both 'en' and 'pt-BR' locales"
    - "Every PT-BR translatable entry is preceded by a // TODO: native-speaker review marker"
    - "Template-interpolated entries are typed as functions in UiStrings (D-15)"
    - "LOCALE_DISPLAY_NAMES exports native endonyms for the LanguagePicker (D-14)"
  artifacts:
    - path: "src/content/strings.ts"
      provides: "UiStrings interface + UI_STRINGS catalog + LOCALE_DISPLAY_NAMES"
      contains: "export interface UiStrings"
    - path: "src/content/strings.test.ts"
      provides: "exhaustiveness + non-empty assertions per locale"
      contains: "for (const locale of LOCALE_OPTIONS)"
  key_links:
    - from: "src/content/strings.ts"
      to: "src/domain/settings.ts"
      via: "type LocaleId import"
      pattern: "import type \\{ LocaleId \\} from '\\.\\./domain/settings'"
    - from: "src/content/strings.test.ts"
      to: "src/content/strings.ts"
      via: "UI_STRINGS exhaustiveness loop"
      pattern: "LOCALE_OPTIONS"
---

<objective>
Create the typed `UiStrings` interface, the `UI_STRINGS: Readonly<Record<LocaleId, UiStrings>>` catalog with EN + PT-BR entries, and the `LOCALE_DISPLAY_NAMES` constant. Add `strings.test.ts` that asserts every locale has every slice (exhaustiveness) and that every slice's leaf strings are non-empty.

Purpose: The string catalog is the type-level floor for every downstream component edit (Waves 3–5). Wave 0 ships the contract so a missing slice or signature mismatch surfaces here instead of as a runtime miss inside a half-migrated component.

Output: `src/content/strings.ts` exporting `UiStrings`, `UI_STRINGS`, `LOCALE_DISPLAY_NAMES`. `src/content/strings.test.ts` exercising exhaustiveness + non-empty + function-typed entry shape.
</objective>

<execution_context>
@$HOME/.claude-personal-personal/get-shit-done/workflows/execute-plan.md
@$HOME/.claude-personal-personal/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-language-switching/19-CONTEXT.md
@.planning/phases/19-language-switching/19-PATTERNS.md
@.planning/phases/19-language-switching/19-RESEARCH.md

<interfaces>
<!-- LocaleId / LOCALE_OPTIONS / DEFAULT_LOCALE are LOCKED in src/domain/settings.ts (Phase 14 D-09 — NOT edited by Phase 19). -->

From src/domain/settings.ts (lines 83–91):
```typescript
export type LocaleId = 'en' | 'pt-BR'
export const LOCALE_OPTIONS = ['en', 'pt-BR'] as const satisfies readonly LocaleId[]
export function isValidLocale(v: unknown): v is LocaleId
export const DEFAULT_LOCALE: LocaleId = 'en'
```

From src/content/learnContent.ts (lines 8–32) — pattern target for `as const satisfies`:
```typescript
export interface LearnContent {
  readonly explainer: { readonly hrv: ExplainerSection; readonly timing: ExplainerSection; readonly forrest: ExplainerSection }
  readonly links: { readonly youtubeChannel: LearnLink; ... }
}
export const LEARN_CONTENT: LearnContent = { ... } as const satisfies LearnContent
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create UiStrings interface + UI_STRINGS catalog + LOCALE_DISPLAY_NAMES in src/content/strings.ts</name>
  <files>src/content/strings.ts</files>
  <read_first>
    - src/content/learnContent.ts (pattern target: header comment style, `as const satisfies` typing at line 85)
    - src/domain/settings.ts (lines 83–91 — LocaleId, LOCALE_OPTIONS, DEFAULT_LOCALE — DO NOT EDIT)
    - .planning/phases/19-language-switching/19-PATTERNS.md (lines 47–101 — strings.ts module pattern + template-fn entry shape)
    - .planning/phases/19-language-switching/19-CONTEXT.md (D-10 nested UiStrings + D-12 option-name translation + D-14 LOCALE_DISPLAY_NAMES + D-15 template-fn entries)
  </read_first>
  <behavior>
    - Compiles under `tsc --strict` with `noUncheckedIndexedAccess`.
    - `UI_STRINGS.en` and `UI_STRINGS['pt-BR']` both satisfy the `UiStrings` interface — TypeScript flags any missing slice or signature mismatch.
    - Function-typed entries (D-15) carry parity signatures across locales: `(label: string) => string`.
    - PT-BR entries each carry `// TODO: native-speaker review` (I18N-07) on the same line or the line above.
    - `LOCALE_DISPLAY_NAMES.en === 'English'` and `LOCALE_DISPLAY_NAMES['pt-BR'] === 'Português (Brasil)'` (D-14 — same labels in both UIs).
  </behavior>
  <action>
    Create `src/content/strings.ts` (NEW file). Imports: `type { LocaleId }` from `'../domain/settings'`. Header comment block follows the `learnContent.ts:1–6` style, citing `Phase 19 I18N-01..I18N-07`, D-01 strings catalog separation, D-10 nested-interface decision, D-12 option-name translation, D-14 native-endonym `LOCALE_DISPLAY_NAMES`, D-15 template-fn entries.

    Export `interface UiStrings` with these readonly sub-objects (every leaf is `readonly string` unless marked function):
    - `controls`: `startSession`, `endSession` (per `SessionControls.tsx:55,72`).
    - `endSessionDialog`: `title`, `confirm`, `cancel` (matches `EndSessionDialog.tsx:61,76,83`: "End this session?" / "End" / "Keep going").
    - `resetStatsDialog`: `title`, `confirm`, `cancel` (matches `ResetStatsDialog.tsx:61,71,78`: "Reset practice stats?" / "Reset" / "Keep").
    - `settings`: `title`, `close`, `themeLabel`, `variantLabel`, `timbreLabel`, `languageLabel` (D-19 — drives `SettingsDialog.tsx:80,92` and the four picker section labels).
    - `themes`: object with keys `light`, `dark`, `system`, `moss`, `slate`, `dusk` — strings (D-12 option-name translation; mirrors `THEME_OPTIONS`).
    - `variants`: object with keys `orb`, `square`, `diamond` — strings (D-12).
    - `timbres`: object with keys `bowl`, `bell`, `sine`, `chime` — strings (D-12).
    - `settingsForm`: `ariaLabel`, `bpmLabel`, `ratioLabel`, `durationLabel`, `openEndedLabel`, `bpmUnit`, `minutesUnit`, `stepper` (nested — see below).
    - `settingsForm.stepper`: function types `fieldAriaLabel(label: string) => string`, `decreaseLabel(label: string) => string`, `increaseLabel(label: string) => string` (D-15).
    - `mute`: `mute`, `unmute`, `resume`, `unavailable` (matches `MuteToggle.tsx:32–38` four-state label derivation).
    - `readout`: `elapsed`, `remaining`, `readoutAriaLabel`, `announcementAriaLabel`, `sessionComplete` (matches `SessionReadout.tsx:22,28,33,54,63,68,73`).
    - `anchors`: `settings`, `settingsDisabled`, `learn`, `learnDisabled` (matches `SettingsAnchor.tsx:21,44` and `LearnAnchor.tsx:22,45`).
    - `stats`: function types `sessionsCount(n: number) => string`, `totalMinutes(seconds: number) => string`, `lastSessionPrefix(date: string, duration: string) => string`, plus strings `totalSuffix`, `reset` (matches `StatsFooter.tsx:32,37–50` + `format.ts:38,59` patterns).
    - `breathing`: `inhale`, `exhale`, `breathingShapeAriaLabel`, function `leadInAriaLabel(digit: number) => string`.
    - `learn`: `title`, `close`, `resourcesHeading`, `videosHeading` (matches `LearnDialog.tsx:77,104,143,182`).

    Export `const UI_STRINGS: Readonly<Record<LocaleId, UiStrings>>` with two entries:
    - `en`: every leaf populated with the EN literal CURRENTLY shipped in source (extract from the files cited above; e.g. `controls.startSession = 'Start session'`, `controls.endSession = 'End session'`, `settings.title = 'Settings'`, `settings.close = 'Close'`, `endSessionDialog.title = 'End this session?'`, `endSessionDialog.confirm = 'End'`, `endSessionDialog.cancel = 'Keep going'`, `resetStatsDialog.title = 'Reset practice stats?'`, `resetStatsDialog.confirm = 'Reset'`, `resetStatsDialog.cancel = 'Keep'`, `themes.light = 'Light'`, `themes.dark = 'Dark'`, `themes.system = 'System'`, `themes.moss = 'Moss'`, `themes.slate = 'Slate'`, `themes.dusk = 'Dusk'`, `variants.orb = 'Orb'`, `variants.square = 'Square'`, `variants.diamond = 'Diamond'`, `timbres.bowl = 'Bowl'`, `timbres.bell = 'Bell'`, `timbres.sine = 'Sine'`, `timbres.chime = 'Chime'`, `settingsForm.ariaLabel = 'Session settings'`, `settingsForm.bpmLabel = 'BPM'`, `settingsForm.ratioLabel = 'Ratio'`, `settingsForm.durationLabel = 'Duration'`, `settingsForm.openEndedLabel = 'Open-ended'`, `settingsForm.bpmUnit = 'BPM'`, `settingsForm.minutesUnit = 'min'`, `settingsForm.stepper.fieldAriaLabel = (l) => l`, `settingsForm.stepper.decreaseLabel = (l) => `Decrease ${l}``, `settingsForm.stepper.increaseLabel = (l) => `Increase ${l}``, `mute.mute = 'Mute audio cues'`, `mute.unmute = 'Unmute audio cues'`, `mute.resume = 'Resume audio'`, `mute.unavailable = 'Audio unavailable in this browser'`, `readout.elapsed = 'Elapsed'`, `readout.remaining = 'Remaining'`, `readout.readoutAriaLabel = 'Session readout'`, `readout.announcementAriaLabel = 'Session announcement'`, `readout.sessionComplete = 'Session complete'`, `anchors.settings = 'Settings'`, `anchors.settingsDisabled = 'Settings (unavailable during session)'`, `anchors.learn = 'Learn'`, `anchors.learnDisabled = 'Learn (unavailable during session)'`, `stats.sessionsCount = (n) => n === 1 ? '1 session' : `${String(n)} sessions``, `stats.totalMinutes = (seconds) => ...`, `stats.lastSessionPrefix = (date, duration) => `Last: ${date} · ${duration}``, `stats.totalSuffix = 'total'`, `stats.reset = 'Reset'`, `breathing.inhale = 'In'`, `breathing.exhale = 'Out'`, `breathing.breathingShapeAriaLabel = 'Breathing shape'`, `breathing.leadInAriaLabel = (d) => `Lead-in ${String(d)}``, `learn.title = 'About this practice'`, `learn.close = 'Close'`, `learn.resourcesHeading = 'Forrest Knutson Resources'`, `learn.videosHeading = 'Selected HRV Breathing Videos'`).
    - `'pt-BR'`: every leaf machine-translated per D-13 with `// TODO: native-speaker review` marker. Use the candidates from 19-CONTEXT.md `<specifics>` where provided (e.g. `Idioma`, `Tema`, `Tigela`, `Sino`, `Senoidal`, `Carrilhão`, `Esfera`, `Quadrado`, `Losango`, `Claro`, `Escuro`, `Sistema`, `Musgo`, `Ardósia`, `Crepúsculo`, `Iniciar sessão`, `Encerrar sessão`, `Encerrar esta sessão?`, `Encerrar`, `Continuar`, `Reiniciar estatísticas?`, `Reiniciar`, `Manter`, `Configurações`, `Fechar`, `Tema`, `Variante`, `Timbre`, `Idioma`, `BPM` (loanword), `Proporção`, `Duração`, `Aberta`, `min` (loanword), `Iniciar`, `Diminuir ${l}`, `Aumentar ${l}`, `Silenciar pistas de áudio`, `Reativar pistas de áudio`, `Retomar áudio`, `Áudio indisponível neste navegador`, `Decorrido`, `Restante`, `Leitura da sessão`, `Anúncio da sessão`, `Sessão concluída`, `Configurações`, `Configurações (indisponíveis durante a sessão)`, `Aprender`, `Aprender (indisponível durante a sessão)`, `${String(n)} sessões` / `1 sessão`, `Último: ${date} · ${duration}`, `total`, `Reiniciar`, `Entra`, `Sai`, `Forma da respiração`, `Contagem regressiva ${String(d)}`, `Sobre esta prática`, `Fechar`, `Recursos de Forrest Knutson`, `Vídeos selecionados de respiração HRV`).
    - Inline `// TODO: native-speaker review` comment on each PT-BR translatable string entry per D-13 + I18N-07. Place comment on the line above the entry OR at end-of-line; choose for legibility.
    - For PT-BR `stats.sessionsCount`, mirror EN structure: `(n) => n === 1 ? '1 sessão' : `${String(n)} sessões``.

    Export `const LOCALE_DISPLAY_NAMES: Readonly<Record<LocaleId, string>>` with values `en: 'English'`, `'pt-BR': 'Português (Brasil)'` (D-14 — native endonyms; same in BOTH UI locales; NO `// TODO: native-speaker review` marker because endonyms are not translated).

    Apply `as const satisfies Readonly<Record<LocaleId, UiStrings>>` to UI_STRINGS for exhaustiveness narrowing per D-10. Apply `as const satisfies Readonly<Record<LocaleId, string>>` to LOCALE_DISPLAY_NAMES.

    NO `eslint-disable` lines (D-24 — strict TS + strictTypeChecked baseline).
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -40 && grep -c "TODO: native-speaker review" src/content/strings.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/content/strings.ts` exists and exports `UiStrings`, `UI_STRINGS`, `LOCALE_DISPLAY_NAMES`.
    - `npx tsc --noEmit` exits 0.
    - `grep -c "TODO: native-speaker review" src/content/strings.ts` returns a count > 0 (every PT-BR translatable entry flagged).
    - `grep -c "as const satisfies" src/content/strings.ts` returns >= 2 (one for UI_STRINGS, one for LOCALE_DISPLAY_NAMES).
    - `grep -E "LOCALE_DISPLAY_NAMES.*English|'English'" src/content/strings.ts` matches (D-14 endonym present).
    - `grep -E "Português \(Brasil\)" src/content/strings.ts` matches (D-14 endonym present).
    - File contains no `text-slate-*`, `bg-teal-*`, `text-white`, `bg-white` Tailwind classes (D-22 — not applicable but verified zero-cost).
  </acceptance_criteria>
  <done>
    `src/content/strings.ts` compiles under strict TS, exports the three named symbols, contains both locales fully populated, every PT-BR translatable string carries a TODO marker, LOCALE_DISPLAY_NAMES carries native endonyms.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create src/content/strings.test.ts with exhaustiveness + non-empty assertions</name>
  <files>src/content/strings.test.ts</files>
  <read_first>
    - src/content/learnContent.test.ts (lines 1–24 — `describe` + `for (const ...)` patterns; pattern target for shape-contract tests)
    - src/content/strings.ts (the file just created in Task 1 — interface + catalog under test)
    - src/domain/settings.ts (line 85 — `LOCALE_OPTIONS` array used for iteration)
    - .planning/phases/19-language-switching/19-PATTERNS.md (lines 105–139 — strings.test.ts exhaustiveness pattern)
  </read_first>
  <behavior>
    - Test 1: every `LocaleId` from `LOCALE_OPTIONS` has a defined `UI_STRINGS[locale]` entry.
    - Test 2: every locale's `controls.startSession`, `controls.endSession`, `settings.title`, `settings.close`, and a sample of leaf strings are non-empty.
    - Test 3: every locale's `themes`, `variants`, `timbres` sub-objects contain entries for every option key (`light`/`dark`/`system`/`moss`/`slate`/`dusk`, `orb`/`square`/`diamond`, `bowl`/`bell`/`sine`/`chime`).
    - Test 4: `LOCALE_DISPLAY_NAMES.en === 'English'` and `LOCALE_DISPLAY_NAMES['pt-BR'] === 'Português (Brasil)'` (D-14 byte-equality).
    - Test 5: function-typed entries — `settingsForm.stepper.decreaseLabel('BPM')` returns a non-empty string for both locales.
    - All tests pass under `npx vitest run src/content/strings.test.ts`.
  </behavior>
  <action>
    Create `src/content/strings.test.ts` (NEW). Imports: `describe`, `expect`, `it` from `'vitest'`; `LOCALE_OPTIONS`, `THEME_OPTIONS`, `VARIANT_OPTIONS`, `TIMBRE_OPTIONS` from `'../domain/settings'`; `UI_STRINGS`, `LOCALE_DISPLAY_NAMES` from `'./strings'`.

    Three `describe` blocks:

    `describe('UI_STRINGS exhaustiveness')`:
    - `it('every LocaleId has a UI_STRINGS entry')` — `for (const locale of LOCALE_OPTIONS) { expect(UI_STRINGS[locale]).toBeDefined() }` (mirror of learnContent.test.ts:6 pattern).
    - `it('every locale has non-empty controls.startSession and controls.endSession')` — loop locales, assert both `.length > 0`.
    - `it('every locale has non-empty settings.title, settings.close, settings.{themeLabel,variantLabel,timbreLabel,languageLabel}')` — loop + per-key assertion.
    - `it('every locale has themes entries for every THEME_OPTIONS id')` — nested loop over `LOCALE_OPTIONS` and `THEME_OPTIONS`; assert `UI_STRINGS[locale].themes[id].length > 0`.
    - `it('every locale has variants entries for every VARIANT_OPTIONS id')` — same pattern for variants.
    - `it('every locale has timbres entries for every TIMBRE_OPTIONS id')` — same pattern for timbres.

    `describe('UI_STRINGS template-fn entries (D-15)')`:
    - `it('settingsForm.stepper.decreaseLabel returns non-empty string with interpolation')` — loop locales, assert `UI_STRINGS[locale].settingsForm.stepper.decreaseLabel('BPM').length > 0` and `.includes('BPM')`.
    - `it('settingsForm.stepper.increaseLabel returns non-empty string with interpolation')` — same pattern.
    - `it('stats.sessionsCount(1) and stats.sessionsCount(2) both return non-empty')` — assert distinct outputs for singular vs plural per locale.

    `describe('LOCALE_DISPLAY_NAMES (D-14 native endonyms)')`:
    - `it('en label is "English"')` — `expect(LOCALE_DISPLAY_NAMES.en).toBe('English')`.
    - `it('pt-BR label is "Português (Brasil)"')` — `expect(LOCALE_DISPLAY_NAMES['pt-BR']).toBe('Português (Brasil)')`.

    No `vi`, no `beforeEach`, no `localStorage` — pure data assertion file; no test isolation needed.
  </action>
  <verify>
    <automated>npx vitest run src/content/strings.test.ts 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/content/strings.test.ts` exits 0.
    - Output shows at least 11 passing tests (8 exhaustiveness + 3 template-fn + 2 endonym = at minimum 13).
    - `grep -c "for (const locale of LOCALE_OPTIONS)" src/content/strings.test.ts` returns >= 4 (loops per slice).
    - `grep -E "'English'|'Português \\(Brasil\\)'" src/content/strings.test.ts` matches twice (D-14 byte-equality assertions present).
  </acceptance_criteria>
  <done>
    `src/content/strings.test.ts` exists and passes. Exhaustiveness covers all four enums (locale × {theme, variant, timbre} options). Template-fn entries exercise interpolation. LOCALE_DISPLAY_NAMES byte-equality covered.
  </done>
</task>

<task type="auto">
  <name>Task 3: Per-commit green-gate (D-16)</name>
  <files>(no source edit — verification only)</files>
  <read_first>
    - .planning/phases/19-language-switching/19-CONTEXT.md (D-16 per-commit green-gate invariant)
    - .planning/PROJECT.md (Per-commit green-gate Key Decision)
  </read_first>
  <action>
    Run the per-commit green-gate sequence verbatim. If any step fails, fix in this plan before committing. The sequence is `npx tsc --noEmit && npm run lint && npm run build && npm test -- --run`. After it exits 0, create a single commit `feat(19-01): strings catalog with EN+PT-BR + exhaustiveness tests` containing only `src/content/strings.ts` and `src/content/strings.test.ts`. Do NOT touch any other file in this commit.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run lint && npm run build && npm test -- --run 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` exits 0.
    - `npm run lint` exits 0 (NO new `// eslint-disable` lines per D-24).
    - `npm run build` exits 0.
    - `npm test -- --run` exits 0 (full suite green including the new strings.test.ts).
    - Single commit created with subject `feat(19-01): strings catalog with EN+PT-BR + exhaustiveness tests`.
    - `git diff HEAD~1 --stat` shows only `src/content/strings.ts` and `src/content/strings.test.ts`.
  </acceptance_criteria>
  <done>
    Green-gate passes; commit landed; no collateral file changes.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none active in this plan) | Plan 01 ships pure static data + a unit test. No untrusted input, no DOM write, no storage, no network. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-01 | Tampering | src/content/strings.ts (PT-BR entries machine-translated) | accept | Per D-13 + I18N-07, machine-translated PT-BR ships with `// TODO: native-speaker review` markers; native review is v1.x carry-forward already tracked in REQUIREMENTS.md. No security impact — strings are static UI labels. |
| T-19-02 | Information Disclosure | src/content/strings.ts | accept | No PII, no secrets, no user-controlled content — pure static catalog. |
</threat_model>

<verification>
- `npx tsc --noEmit && npm run lint && npm run build && npm test -- --run` exits 0 (D-16 per-commit green-gate).
- `grep -c "TODO: native-speaker review" src/content/strings.ts` > 0 (I18N-07 markers present).
- No edits to `src/domain/settings.ts` or `src/storage/prefs.ts` (D-18 file-split invariant).
</verification>

<success_criteria>
1. `src/content/strings.ts` exports `UiStrings`, `UI_STRINGS`, `LOCALE_DISPLAY_NAMES` with both `en` and `pt-BR` entries.
2. `UiStrings` nested interface (D-10) covers every slice the downstream component edits will consume.
3. Template-fn entries typed per D-15.
4. PT-BR entries flagged with `// TODO: native-speaker review` per I18N-07.
5. `LOCALE_DISPLAY_NAMES` provides native endonyms per D-14.
6. Green-gate exits 0 after the commit.
</success_criteria>

<output>
After completion, create `.planning/phases/19-language-switching/19-01-SUMMARY.md` capturing: files created, total PT-BR entry count, TODO marker count, any deviations from CONTEXT.md `<specifics>` PT-BR candidate list.
</output>
