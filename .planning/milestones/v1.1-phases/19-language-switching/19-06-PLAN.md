---
phase: 19-language-switching
plan: 06
type: execute
wave: 3
depends_on: ["19-01", "19-03", "19-05", "19-07"]
files_modified:
  - src/components/SettingsDialog.tsx
  - src/components/SettingsDialog.test.tsx
  - src/components/EndSessionDialog.tsx
  - src/components/EndSessionDialog.test.tsx
  - src/components/ResetStatsDialog.tsx
  - src/components/ResetStatsDialog.test.tsx
  - src/components/SettingsAnchor.tsx
  - src/components/SettingsAnchor.test.tsx
  - src/components/LearnAnchor.tsx
  - src/components/LearnAnchor.test.tsx
  - src/components/MuteToggle.tsx
  - src/components/MuteToggle.test.tsx
  - src/components/ThemePicker.tsx
  - src/components/ThemePicker.test.tsx
  - src/components/VariantPicker.tsx
  - src/components/VariantPicker.test.tsx
  - src/components/TimbrePicker.tsx
  - src/components/TimbrePicker.test.tsx
  - src/components/LanguagePicker.tsx
  - src/components/LanguagePicker.test.tsx
  - src/app/App.tsx
autonomous: true
requirements:
  - I18N-04
must_haves:
  truths:
    - "SettingsDialog accepts strings: UiStrings['settings'] and drills sectionLabel + strings slices to the 4 picker children"
    - "EndSessionDialog and ResetStatsDialog accept strings slices and render translated title + confirm + cancel labels"
    - "SettingsAnchor and LearnAnchor accept strings slices and render translated aria-label + visible label per disabled state"
    - "MuteToggle accepts strings slice and uses translated labels for the 4 states (mute/unmute/resume/unavailable)"
    - "ThemePicker, VariantPicker, TimbrePicker accept strings slice + sectionLabel; option labels come from strings[id] not id-capitalization"
    - "Component test fixtures use EN_STRINGS_FIXTURE = UI_STRINGS.en to avoid duplicating literals"
    - "THEME-UI-01 token-binding guard remains green (no new hardcoded utility colors)"
  artifacts:
    - path: "src/components/SettingsDialog.tsx"
      provides: "translatable title/close + slice drill to 4 pickers"
      contains: "strings.title"
    - path: "src/components/ThemePicker.tsx"
      provides: "translated option labels via strings[id]"
      contains: "strings\\[id\\]"
  key_links:
    - from: "src/components/SettingsDialog.tsx"
      to: "src/components/ThemePicker.tsx"
      via: "strings={strings.themes} sectionLabel={strings.themeLabel} prop drill"
      pattern: "strings=\\{strings.themes\\}"
    - from: "src/components/ThemePicker.tsx"
      to: "src/content/strings.ts"
      via: "UiStrings['themes'] type"
      pattern: "UiStrings\\['themes'\\]"
---

<objective>
Edit 9 components (3 dialogs + 2 anchors + 1 toggle + 3 pickers) to accept a typed `strings` prop and replace their inline English literals with `strings.*` lookups. Update the corresponding test files to use the `EN_STRINGS_FIXTURE = UI_STRINGS.en` pattern. The picker components (Theme/Variant/Timbre) also accept `sectionLabel: string` so SettingsDialog can drill `strings.themeLabel` / `strings.variantLabel` / `strings.timbreLabel` per D-12 (option-name translation) + D-19 (SettingsDialog minimal restructure exception).

Purpose: Plan 06 covers the "chrome" component family — dialogs, anchors, toggle, pickers — that share `strings` prop ownership but DO NOT touch session-state machinery. Plan 06b handles the form/footer/controls/breathing family. App.tsx wiring (Plan 07) drills the slices.

Output: 9 component files + 9 test files + `src/app/App.tsx` (EN-fixture stop-gap drill) = 19 files. Green-gate exits 0.

**Why App.tsx is in this plan (and not deferred to Plan 08):** Plan 06 adds REQUIRED `strings` / `sectionLabel` props to 9 consumer components. App.tsx call sites at lines 635, 636, 693, 698, 707 (SettingsAnchor, LearnAnchor, EndSessionDialog, ResetStatsDialog, SettingsDialog) invoke these components without the new required props → TS compile fails → D-16 per-commit green-gate cannot pass. The least-invasive fix is a one-shot App.tsx drill of `UI_STRINGS.en.<slice>` EN fixtures at every modified call site in THIS plan's commit. **Wave order:** Plan 06 ships in wave 3 — AFTER Plan 07 (wave 2) which already added the `import { UI_STRINGS }` line and 5 prop drills for its own session-UI consumers. Plan 06 therefore does NOT add the import (it's already there); Plan 06 adds 5 additional `strings={UI_STRINGS.en.<slice>}` prop drills for the dialog/anchor consumers. Plan 08 removes the stop-gap (swaps `UI_STRINGS.en.<slice>` → `uiStrings.<slice>` from `useLocale()` hook destructure). After Plan 06: `grep -c 'UI_STRINGS.en' src/app/App.tsx` returns >= 10 (Plan 07's 5 + Plan 06's 5). After Plan 08: returns 0.
</objective>

<execution_context>
@$HOME/.claude-personal-personal/get-shit-done/workflows/execute-plan.md
@$HOME/.claude-personal-personal/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/19-language-switching/19-CONTEXT.md
@.planning/phases/19-language-switching/19-PATTERNS.md
@.planning/phases/19-language-switching/19-01-SUMMARY.md

<interfaces>
From src/content/strings.ts (Plan 01 output):
```typescript
export interface UiStrings {
  controls: { startSession: string; endSession: string }
  endSessionDialog: { title: string; confirm: string; cancel: string }
  resetStatsDialog: { title: string; confirm: string; cancel: string }
  settings: { title: string; close: string; themeLabel: string; variantLabel: string; timbreLabel: string; languageLabel: string }
  themes: { light: string; dark: string; system: string; moss: string; slate: string; dusk: string }
  variants: { orb: string; square: string; diamond: string }
  timbres: { bowl: string; bell: string; sine: string; chime: string }
  mute: { mute: string; unmute: string; resume: string; unavailable: string }
  anchors: { settings: string; settingsDisabled: string; learn: string; learnDisabled: string }
  // (other slices owned by Plan 06b: settingsForm, readout, stats, breathing, learn)
}
export const UI_STRINGS: Readonly<Record<LocaleId, UiStrings>>
```

Current EN literals (replaced in this plan):
- SettingsDialog.tsx:80 `Settings` → `strings.title`
- SettingsDialog.tsx:92 `Close` → `strings.close`
- EndSessionDialog.tsx (title `End this session?` / confirm `End` / cancel `Keep going`)
- ResetStatsDialog.tsx (title `Reset practice stats?` / confirm `Reset` / cancel `Keep`)
- SettingsAnchor.tsx:21 aria-label `Settings (unavailable during session)` / `Settings`; line 44 visible label `Settings`
- LearnAnchor.tsx:22 aria-label `Learn (unavailable during session)` / `Learn`; line 45 visible label `Learn`
- MuteToggle.tsx:32–38 label derivation 4-state literals
- ThemePicker.tsx:22 section label `Theme`; line 31 `id.charAt(0).toUpperCase() + id.slice(1)` → `strings[id]`
- VariantPicker.tsx:24 section label `Variant`; line 33 same id-capitalization → strings[id]
- TimbrePicker.tsx:31 section label `Timbre`; line 40 same id-capitalization → strings[id]
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: SettingsDialog accepts strings prop + drills slices to 4 pickers</name>
  <files>src/components/SettingsDialog.tsx, src/components/SettingsDialog.test.tsx</files>
  <read_first>
    - src/components/SettingsDialog.tsx (entire 97 lines — minimal restructure per D-19; only title/Close literals + child prop drills change)
    - src/components/SettingsDialog.test.tsx (existing test file — used to confirm restructure stays minimal)
    - src/content/strings.ts (Plan 01 output — `UiStrings['settings']` shape)
    - .planning/phases/19-language-switching/19-PATTERNS.md (lines 817–862 — SettingsDialog mutation pattern)
  </read_first>
  <behavior>
    - `SettingsDialogProps` extends with `strings: UiStrings['settings']`.
    - Line 80 `<h2>Settings</h2>` becomes `<h2 ...>{strings.title}</h2>`.
    - Line 92 `<button>Close</button>` becomes `<button ...>{strings.close}</button>`.
    - Picker child invocations gain props:
      - `<ThemePicker disabled={inSessionView} strings={strings.themes} sectionLabel={strings.themeLabel} />`
      - `<VariantPicker disabled={inSessionView} strings={strings.variants} sectionLabel={strings.variantLabel} />`
      - `<TimbrePicker disabled={inSessionView} strings={strings.timbres} sectionLabel={strings.timbreLabel} />`
      - `<LanguagePicker disabled={inSessionView} sectionLabel={strings.languageLabel} />`
    - No other restructuring per D-19 (existing dialog primitives unchanged: showModal, cancel handler, backdrop-click — all unchanged).
    - All existing tests in `SettingsDialog.test.tsx` continue to pass after the test render helper is updated to pass `strings={EN_STRINGS_FIXTURE.settings}` (and the existing tests assert against the same literal text via the new strings).
  </behavior>
  <action>
    Edit `src/components/SettingsDialog.tsx`:
    1. Add import: `import type { UiStrings } from '../content/strings'`.
    2. Extend `SettingsDialogProps` (line 27) with `strings: UiStrings['settings']`.
    3. Destructure `strings` in the component signature.
    4. Replace `Settings` literal at line 80 with `{strings.title}`.
    5. Replace `Close` literal at line 92 with `{strings.close}`.
    6. Update each of the 4 picker invocations (lines 82–85) per the behavior list above.
    7. Note: `LanguagePicker` accepts `sectionLabel: string` per the Plan 05 D-20 widening — Plan 05 NOTE deferred the `sectionLabel` plumb to "Plan 07 App-side wiring", but this picker child invocation IS the wiring. So this plan widens the LanguagePicker prop contract to `{ disabled, sectionLabel }`. The LanguagePicker.tsx body must be updated to consume `sectionLabel` and render it instead of the hardcoded `Language` literal — see Task 7 in this plan.

    Edit `src/components/SettingsDialog.test.tsx`:
    - Add `import { UI_STRINGS } from '../content/strings'` and `const EN_STRINGS_FIXTURE = UI_STRINGS.en`.
    - Update the render helper to pass `strings={EN_STRINGS_FIXTURE.settings}`.
    - Existing assertions about `Settings` title text + `Close` button continue to work because EN strings match shipped literals.
  </action>
  <verify>
    <automated>npx vitest run src/components/SettingsDialog.test.tsx 2>&1 | tail -10 && npx tsc --noEmit 2>&1 | head -10</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/SettingsDialog.tsx` exports a `SettingsDialogProps` interface containing `strings: UiStrings['settings']`.
    - `grep -c "strings.title" src/components/SettingsDialog.tsx` returns 1.
    - `grep -c "strings.close" src/components/SettingsDialog.tsx` returns 1.
    - `grep -c "sectionLabel=" src/components/SettingsDialog.tsx` returns 4 (drilled to all four pickers).
    - `grep -c "strings={strings.themes\\|strings={strings.variants\\|strings={strings.timbres" src/components/SettingsDialog.tsx` returns 3 (one per non-language picker).
    - Existing dialog primitives unchanged: `grep -c "showModal\\|handleCancel\\|handleBackdropClick" src/components/SettingsDialog.tsx` returns >= 3.
    - `npx vitest run src/components/SettingsDialog.test.tsx` exits 0.
  </acceptance_criteria>
  <done>
    SettingsDialog accepts strings prop, drills slices to 4 picker children, minimal restructure preserved per D-19. Tests pass.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: EndSessionDialog + ResetStatsDialog accept strings prop</name>
  <files>src/components/EndSessionDialog.tsx, src/components/EndSessionDialog.test.tsx, src/components/ResetStatsDialog.tsx, src/components/ResetStatsDialog.test.tsx</files>
  <read_first>
    - src/components/EndSessionDialog.tsx (lines 52–83 — title + confirm + cancel literals)
    - src/components/ResetStatsDialog.tsx (lines 52–78 — title + confirm + cancel literals)
    - src/components/EndSessionDialog.test.tsx (existing fixtures)
    - src/components/ResetStatsDialog.test.tsx (existing fixtures)
    - src/content/strings.ts (Plan 01 — `UiStrings['endSessionDialog']` and `['resetStatsDialog']` shapes)
    - .planning/phases/19-language-switching/19-PATTERNS.md (lines 1044–1062 — dialog mutation pattern)
  </read_first>
  <behavior>
    - EndSessionDialog: `EndSessionDialogProps` gains `strings: UiStrings['endSessionDialog']`. Title literal `End this session?` becomes `{strings.title}`. Confirm button `End` → `{strings.confirm}`. Cancel button `Keep going` → `{strings.cancel}`.
    - ResetStatsDialog: `ResetStatsDialogProps` gains `strings: UiStrings['resetStatsDialog']`. Title `Reset practice stats?` → `{strings.title}`. Confirm `Reset` → `{strings.confirm}`. Cancel `Keep` → `{strings.cancel}`.
    - Imperative dialog primitives (showModal, cancel handler, focus-on-Keep for ResetStats per D-13) NOT changed.
    - Both test files update fixtures via `EN_STRINGS_FIXTURE` pattern; existing assertions continue to match.
  </behavior>
  <action>
    For BOTH `EndSessionDialog.tsx` and `ResetStatsDialog.tsx`:
    1. Add `import type { UiStrings } from '../content/strings'`.
    2. Extend the Props interface with `strings: UiStrings['endSessionDialog']` (or `['resetStatsDialog']`).
    3. Destructure `strings` in the component signature.
    4. Replace title, confirm, cancel literals with `{strings.title}`, `{strings.confirm}`, `{strings.cancel}`.

    For BOTH test files:
    - Add `EN_STRINGS_FIXTURE = UI_STRINGS.en` import.
    - Update render helper to pass `strings={EN_STRINGS_FIXTURE.endSessionDialog}` (or `.resetStatsDialog`).
    - Existing assertions stay (EN strings match shipped literals).
  </action>
  <verify>
    <automated>npx vitest run src/components/EndSessionDialog.test.tsx src/components/ResetStatsDialog.test.tsx 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "strings.title\\|strings.confirm\\|strings.cancel" src/components/EndSessionDialog.tsx` returns 3.
    - `grep -c "strings.title\\|strings.confirm\\|strings.cancel" src/components/ResetStatsDialog.tsx` returns 3.
    - `grep -c "EN_STRINGS_FIXTURE" src/components/EndSessionDialog.test.tsx` returns >= 1.
    - `grep -c "EN_STRINGS_FIXTURE" src/components/ResetStatsDialog.test.tsx` returns >= 1.
    - `npx vitest run src/components/EndSessionDialog.test.tsx src/components/ResetStatsDialog.test.tsx` exits 0.
  </acceptance_criteria>
  <done>
    Both dialogs accept strings prop; existing test assertions migrate to EN fixture.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: SettingsAnchor + LearnAnchor accept strings prop</name>
  <files>src/components/SettingsAnchor.tsx, src/components/SettingsAnchor.test.tsx, src/components/LearnAnchor.tsx, src/components/LearnAnchor.test.tsx</files>
  <read_first>
    - src/components/SettingsAnchor.tsx (entire 47 lines — lines 21, 44 are the literals)
    - src/components/LearnAnchor.tsx (entire 48 lines — lines 22, 45 are the literals)
    - src/components/SettingsAnchor.test.tsx (existing fixtures)
    - src/components/LearnAnchor.test.tsx (existing fixtures)
    - .planning/phases/19-language-switching/19-PATTERNS.md (lines 1009–1041 — anchor mutation pattern)
  </read_first>
  <behavior>
    - Both anchors accept `strings: UiStrings['anchors']`.
    - SettingsAnchor: `aria-label={disabled ? strings.settingsDisabled : strings.settings}` and visible `<span>{strings.settings}</span>`.
    - LearnAnchor: `aria-label={disabled ? strings.learnDisabled : strings.learn}` and visible `<span>{strings.learn}</span>`.
    - 44×44 hit-area + focus-visible + token-bound colors unchanged.
  </behavior>
  <action>
    For both anchor components:
    1. Add `import type { UiStrings } from '../content/strings'`.
    2. Extend Props with `strings: UiStrings['anchors']`.
    3. Replace aria-label literal at line 21 (SettingsAnchor) / 22 (LearnAnchor) with the ternary using strings.
    4. Replace inline `<span>Settings</span>` / `<span>Learn</span>` at line 44 / 45 with `{strings.settings}` / `{strings.learn}`.

    For both test files:
    - Add `EN_STRINGS_FIXTURE = UI_STRINGS.en` import.
    - Update fixtures to pass `strings={EN_STRINGS_FIXTURE.anchors}`.
  </action>
  <verify>
    <automated>npx vitest run src/components/SettingsAnchor.test.tsx src/components/LearnAnchor.test.tsx 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "strings.settings\\|strings.settingsDisabled" src/components/SettingsAnchor.tsx` returns 2.
    - `grep -c "strings.learn\\|strings.learnDisabled" src/components/LearnAnchor.tsx` returns 2.
    - `npx vitest run src/components/SettingsAnchor.test.tsx src/components/LearnAnchor.test.tsx` exits 0.
    - 44×44 hit-area + focus-visible classes unchanged (`grep -c "min-h-\\[44px\\]" src/components/SettingsAnchor.tsx` >= 1; same for LearnAnchor).
  </acceptance_criteria>
  <done>
    Both anchors accept strings prop; aria-labels + visible labels translated; existing a11y class invariants preserved.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: MuteToggle accepts strings prop for 4-state label</name>
  <files>src/components/MuteToggle.tsx, src/components/MuteToggle.test.tsx</files>
  <read_first>
    - src/components/MuteToggle.tsx (lines 32–38 — 4-state label derivation; lines 45,46 — aria-label + title)
    - src/components/MuteToggle.test.tsx (existing fixtures)
    - .planning/phases/19-language-switching/19-PATTERNS.md (lines 946–975 — MuteToggle mutation pattern)
  </read_first>
  <behavior>
    - `MuteToggleProps` gains `strings: UiStrings['mute']` (4-key shape: mute, unmute, resume, unavailable).
    - Label derivation at lines 32–38 uses `strings.unavailable`, `strings.resume`, `strings.unmute`, `strings.mute` in the same priority order.
    - aria-label and title bindings at lines 45–46 continue to reference the local `label` variable (no signature change).
    - Test fixtures pass `strings={EN_STRINGS_FIXTURE.mute}`.
  </behavior>
  <action>
    Edit `src/components/MuteToggle.tsx`:
    1. Add `import type { UiStrings } from '../content/strings'`.
    2. Extend `MuteToggleProps` (lines 10–24) with `strings: UiStrings['mute']`.
    3. Destructure `strings` in component signature.
    4. Replace the 4 inline string literals at lines 32–38 with `strings.unavailable`, `strings.resume`, `strings.unmute`, `strings.mute` (priority order preserved).

    Edit `src/components/MuteToggle.test.tsx`:
    - Add `EN_STRINGS_FIXTURE` import.
    - Update fixtures to pass `strings={EN_STRINGS_FIXTURE.mute}`.
    - Existing assertions that check `getByLabelText('Mute audio cues')` etc. continue to match because EN literals are preserved.
  </action>
  <verify>
    <automated>npx vitest run src/components/MuteToggle.test.tsx 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "strings.mute\\|strings.unmute\\|strings.resume\\|strings.unavailable" src/components/MuteToggle.tsx` returns 4.
    - `grep -c "Audio unavailable in this browser\\|Resume audio\\|Unmute audio cues\\|Mute audio cues" src/components/MuteToggle.tsx` returns 0 (literals removed).
    - `npx vitest run src/components/MuteToggle.test.tsx` exits 0.
    - Icon JSX (SpeakerIcon, SpeakerSlashIcon, ResumeIcon) at lines 51–129 unchanged.
  </acceptance_criteria>
  <done>
    MuteToggle accepts strings prop; 4-state label derivation uses translated strings; icon JSX preserved.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 5: ThemePicker accepts strings + sectionLabel; option label via strings[id]</name>
  <files>src/components/ThemePicker.tsx, src/components/ThemePicker.test.tsx</files>
  <read_first>
    - src/components/ThemePicker.tsx (entire 53 lines — line 22 section label, line 31 id-capitalization)
    - src/components/ThemePicker.test.tsx (entire 116 lines — radiogroup posture tests assert label text `Light`, `Dark`, etc.)
    - src/content/strings.ts (Plan 01 — `UiStrings['themes']` with 6 keys)
    - .planning/phases/19-language-switching/19-PATTERNS.md (lines 1186–1216 — picker translation wedge)
  </read_first>
  <behavior>
    - `ThemePickerProps` gains `strings: UiStrings['themes']` and `sectionLabel: string`.
    - Section label literal `Theme` at line 22 replaced with `{sectionLabel}`.
    - Option label at line 31 `const label = id.charAt(0).toUpperCase() + id.slice(1)` replaced with `const label = strings[id]` (type-safe — TypeScript enforces every theme key exists in `UiStrings['themes']`).
    - All a11y posture (role=radiogroup, role=radio, aria-checked, aria-disabled, 44×44 hit area, focus-visible) unchanged.
    - All existing tests in ThemePicker.test.tsx continue to pass after the render helper passes `strings={EN_STRINGS_FIXTURE.themes}` and `sectionLabel={EN_STRINGS_FIXTURE.settings.themeLabel}` — because the EN strings produce the same `Light/Dark/System/Moss/Slate/Dusk` labels.
  </behavior>
  <action>
    Edit `src/components/ThemePicker.tsx`:
    1. Add `import type { UiStrings } from '../content/strings'`.
    2. Extend `ThemePickerProps` with `strings: UiStrings['themes']; sectionLabel: string`.
    3. Replace `Theme` literal at line 22 with `{sectionLabel}`.
    4. Replace the `const label = id.charAt(0).toUpperCase() + id.slice(1)` line at line 31 with `const label = strings[id]`.

    Edit `src/components/ThemePicker.test.tsx`:
    - Add `EN_STRINGS_FIXTURE = UI_STRINGS.en` import.
    - Update every `render(<ThemePicker disabled={...} />)` call to `render(<ThemePicker disabled={...} strings={EN_STRINGS_FIXTURE.themes} sectionLabel={EN_STRINGS_FIXTURE.settings.themeLabel} />)`. (5 invocations across 7 tests — find them via grep.)
    - Existing assertions (`expect(screen.getByText('Theme')).toBeInTheDocument()`, `expect(labels).toEqual(['Light', 'Dark', 'System', 'Moss', 'Slate', 'Dusk'])`) continue to work because EN strings produce these same labels.
  </action>
  <verify>
    <automated>npx vitest run src/components/ThemePicker.test.tsx 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "strings\\[id\\]" src/components/ThemePicker.tsx` returns 1 (option label lookup).
    - `grep -c "sectionLabel" src/components/ThemePicker.tsx` returns >= 2 (prop type + JSX consumer).
    - `grep -c "charAt(0).toUpperCase" src/components/ThemePicker.tsx` returns 0 (capitalization fallback removed).
    - `npx vitest run src/components/ThemePicker.test.tsx` exits 0.
    - Existing aria-checked / disabled / token-binding assertions still pass.
  </acceptance_criteria>
  <done>
    ThemePicker accepts strings + sectionLabel props; option labels via strings[id]; tests migrate to EN fixture; THEME-UI-01 invariant preserved.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 6: VariantPicker + TimbrePicker accept strings + sectionLabel</name>
  <files>src/components/VariantPicker.tsx, src/components/VariantPicker.test.tsx, src/components/TimbrePicker.tsx, src/components/TimbrePicker.test.tsx</files>
  <read_first>
    - src/components/VariantPicker.tsx (entire 71 lines — line 24 section label `Variant`; line 33 id-capitalization)
    - src/components/TimbrePicker.tsx (entire 62 lines — line 31 section label `Timbre`; line 40 id-capitalization)
    - src/components/VariantPicker.test.tsx + src/components/TimbrePicker.test.tsx (existing fixtures)
    - src/content/strings.ts (Plan 01 — `UiStrings['variants']` 3 keys, `UiStrings['timbres']` 4 keys)
  </read_first>
  <behavior>
    - Same shape as Task 5 applied to VariantPicker and TimbrePicker.
    - VariantPicker: section label `Variant` → `{sectionLabel}`; option label via `strings[id]` where `strings: UiStrings['variants']`.
    - TimbrePicker: section label `Timbre` → `{sectionLabel}`; option label via `strings[id]` where `strings: UiStrings['timbres']`.
    - Test fixtures updated to pass `strings={EN_STRINGS_FIXTURE.variants}` + `sectionLabel={EN_STRINGS_FIXTURE.settings.variantLabel}` (and analogous for timbre).
    - All a11y posture and inline-shape-swatch JSX (VariantPicker) unchanged.
  </behavior>
  <action>
    Apply the Task 5 pattern to both VariantPicker and TimbrePicker:
    1. Each component file: import `UiStrings` type, extend Props with `strings` + `sectionLabel`, replace section-label literal with `{sectionLabel}`, replace id-capitalization with `strings[id]`.
    2. Each test file: import `EN_STRINGS_FIXTURE`, update render calls to pass the new props.

    For VariantPicker, preserve any inline-shape-swatch JSX (polygon SVG for Diamond, CSS-only for Orb + Square) — string changes are limited to the option label TEXT under the swatch.
    For TimbrePicker, all 4 timbre option labels come from `strings[id]` (matches Phase 18 D-19 token-binding invariant).
  </action>
  <verify>
    <automated>npx vitest run src/components/VariantPicker.test.tsx src/components/TimbrePicker.test.tsx 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "strings\\[id\\]" src/components/VariantPicker.tsx` returns 1.
    - `grep -c "strings\\[id\\]" src/components/TimbrePicker.tsx` returns 1.
    - `grep -c "sectionLabel" src/components/VariantPicker.tsx` returns >= 2.
    - `grep -c "sectionLabel" src/components/TimbrePicker.tsx` returns >= 2.
    - `grep -c "charAt(0).toUpperCase" src/components/VariantPicker.tsx src/components/TimbrePicker.tsx` returns 0.
    - `npx vitest run src/components/VariantPicker.test.tsx src/components/TimbrePicker.test.tsx` exits 0.
  </acceptance_criteria>
  <done>
    VariantPicker + TimbrePicker accept strings + sectionLabel; option labels via strings[id]; shape-swatch JSX preserved in VariantPicker.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 7: LanguagePicker accepts sectionLabel prop (widens Plan 05 contract)</name>
  <files>src/components/LanguagePicker.tsx, src/components/LanguagePicker.test.tsx</files>
  <read_first>
    - src/components/LanguagePicker.tsx (the file from Plan 05 — section label currently hardcoded as `Language`)
    - src/components/LanguagePicker.test.tsx (Plan 05 tests — 9 cases)
    - .planning/phases/19-language-switching/19-CONTEXT.md (D-20 prop contract widening note: section label translates via SettingsDialog drill)
  </read_first>
  <behavior>
    - `LanguagePickerProps` gains `sectionLabel: string` (alongside existing `disabled: boolean`).
    - Section label literal `Language` at line of the `<p id="language-picker-label">` replaced with `{sectionLabel}`.
    - Native endonyms (`English`, `Português (Brasil)`) STAY hardcoded via `LOCALE_DISPLAY_NAMES[id]` per D-14 — they do NOT flow through `sectionLabel` or any strings prop.
    - All 9 existing tests in `LanguagePicker.test.tsx` continue to pass after the render helper passes `sectionLabel={EN_STRINGS_FIXTURE.settings.languageLabel}` — because EN `languageLabel === 'Language'` produces the same rendered text.
  </behavior>
  <action>
    Edit `src/components/LanguagePicker.tsx`:
    1. Extend `LanguagePickerProps` with `sectionLabel: string`.
    2. Destructure `sectionLabel`.
    3. Replace the hardcoded `Language` text inside `<p id="language-picker-label">...</p>` with `{sectionLabel}`.

    Edit `src/components/LanguagePicker.test.tsx`:
    - Add `EN_STRINGS_FIXTURE = UI_STRINGS.en` import.
    - Update every `render(<LanguagePicker disabled={...} />)` call to `render(<LanguagePicker disabled={...} sectionLabel={EN_STRINGS_FIXTURE.settings.languageLabel} />)`. (Find all calls via grep; there are typically 9 in the Plan 05 file.)
    - Existing assertions about `getByText('Language')` continue to pass because EN `languageLabel === 'Language'`.
  </action>
  <verify>
    <automated>npx vitest run src/components/LanguagePicker.test.tsx 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "sectionLabel" src/components/LanguagePicker.tsx` returns >= 2.
    - `grep -c ">Language<\\|{'Language'}" src/components/LanguagePicker.tsx` returns 0 (section label hardcode removed).
    - `grep -c "LOCALE_DISPLAY_NAMES" src/components/LanguagePicker.tsx` returns >= 2 (option labels still come from constants, NOT strings prop — D-14 invariant preserved).
    - `npx vitest run src/components/LanguagePicker.test.tsx` exits 0 (all 9 Plan 05 tests still pass with the new prop).
  </acceptance_criteria>
  <done>
    LanguagePicker section label translates via `sectionLabel` prop; native endonyms remain hardcoded per D-14; 9 Plan 05 tests still pass.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 8: App.tsx EN-fixture drill — add UI_STRINGS imports + drill strings/sectionLabel to all 5 modified consumers (stop-gap; Plan 08 removes)</name>
  <files>src/app/App.tsx</files>
  <read_first>
    - src/app/App.tsx (lines 1–50 import block, lines 620–710 JSX call sites; consumers modified by this plan: SettingsAnchor (~635), LearnAnchor (~636), EndSessionDialog (~693), ResetStatsDialog (~698), SettingsDialog (~707))
    - src/content/strings.ts (Plan 01 — `UI_STRINGS.en` shape)
    - .planning/phases/19-language-switching/19-CONTEXT.md (D-09 prop-drill pattern + D-16 per-commit green-gate)
  </read_first>
  <behavior>
    - App.tsx ALREADY imports `UI_STRINGS` from Plan 07 (wave 2 shipped earlier); this task does NOT re-add the import.
    - Every consumer modified by THIS plan that now requires `strings` or `sectionLabel` props receives `strings={UI_STRINGS.en.<slice>}` at its App.tsx call site:
      - SettingsAnchor (line ~635): `<SettingsAnchor disabled={inSessionView} onClick={onSettingsClick} strings={UI_STRINGS.en.anchors} />`
      - LearnAnchor (line ~636): `<LearnAnchor disabled={inSessionView} onClick={onLearnClick} strings={UI_STRINGS.en.anchors} />`
      - EndSessionDialog (line ~693): `<EndSessionDialog open={endDialogOpen} onConfirm={confirmEnd} onCancel={cancelEnd} strings={UI_STRINGS.en.endSessionDialog} />`
      - ResetStatsDialog (line ~698): `<ResetStatsDialog open={resetDialogOpen} onConfirm={confirmReset} onCancel={cancelReset} strings={UI_STRINGS.en.resetStatsDialog} />`
      - SettingsDialog (line ~707): `<SettingsDialog open={settingsDialogOpen} onClose={onSettingsClose} inSessionView={inSessionView} strings={UI_STRINGS.en.settings} />`
    - SettingsDialog internally drills `strings.themes` / `strings.variants` / `strings.timbres` to ThemePicker / VariantPicker / TimbrePicker, and `sectionLabel` from its `strings.themeLabel` / `strings.variantLabel` / `strings.timbreLabel` slices (per Task 1) — so the picker call sites inside SettingsDialog do NOT need App.tsx-level intervention.
    - MuteToggle (already inside SessionControls — App.tsx does not invoke MuteToggle directly per the current App.tsx structure; verify by reading App.tsx lines 658–667 SessionControls block. If SessionControls owns MuteToggle invocation, MuteToggle's strings prop is drilled inside SessionControls; if App.tsx invokes MuteToggle directly, add `strings={UI_STRINGS.en.mute}` to that call site too. Per the current src/app/App.tsx:658–667 SessionControls invocation, MuteToggle props are drilled THROUGH SessionControls (`muted`, `audioAvailable`, `needsResume`, `onMuteToggle` are passed to SessionControls) — Plan 07 handles SessionControls drill including MuteToggle. So Plan 06 does NOT need to add a separate MuteToggle drill in App.tsx; Plan 07's MuteToggle stop-gap is owned by Plan 07.
    - LanguagePicker is invoked INSIDE SettingsDialog (not App.tsx) — SettingsDialog Task 1 already drills `sectionLabel={strings.languageLabel}`; no App.tsx-level drill needed for LanguagePicker.
    - This stop-gap is intentionally minimal: 1 import + 5 prop additions (or fewer if some consumers are already drilled by intermediate components).
  </behavior>
  <action>
    Edit `src/app/App.tsx`:
    1. Verify `import { UI_STRINGS } from '../content/strings'` is already present (Plan 07 added it in wave 2). If missing for any reason, add it now with the same `// Phase 19 stop-gap: EN fixture until Plan 08 wires useLocale().` trailing comment.
    2. For each consumer call site listed in `<behavior>`, add the `strings={UI_STRINGS.en.<slice>}` prop to the JSX invocation. Find call sites via `grep -n "<SettingsAnchor\|<LearnAnchor\|<EndSessionDialog\|<ResetStatsDialog\|<SettingsDialog" src/app/App.tsx`.
    3. Verify NO other props on those JSX call sites are modified (D-19 minimal-diff invariant — only the `strings={...}` addition).
    4. Do NOT add `useLocale()` invocation, do NOT add `LOCKED_COPY` import, do NOT touch the medical-advice literal at line 686, do NOT touch the LearnDialog invocation at line 705 (Plan 03's LearnDialog stop-gap reads `LOCKED_COPY.en.*` directly inside LearnDialog; no LearnDialog-level App.tsx drill needed). All of those belong to Plan 08.
    5. The import comment was added by Plan 07; do not re-add. The 5 NEW prop drills in this plan inherit the same `// Phase 19 stop-gap: ...` semantic — no extra annotation needed on individual prop lines.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20 && grep -c "UI_STRINGS.en" src/app/App.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "import { UI_STRINGS }" src/app/App.tsx` returns 1 (Plan 07 added it in wave 2).
    - `grep -c "UI_STRINGS.en" src/app/App.tsx` returns >= 10 (Plan 07's 5 + Plan 06's 5: SettingsAnchor + LearnAnchor + EndSessionDialog + ResetStatsDialog + SettingsDialog).
    - `npx tsc --noEmit` exits 0 (no missing required-prop errors).
    - `npx vitest run src/app` exits 0 (all App-level integration tests still pass with the EN fixtures).
    - SettingsAnchor, LearnAnchor, EndSessionDialog, ResetStatsDialog, SettingsDialog call sites carry the new `strings={UI_STRINGS.en.<slice>}` prop.
    - NO `useLocale()` invocation in App.tsx yet (Plan 08 owns that): `grep -c "useLocale" src/app/App.tsx` returns 0.
    - The medical-advice literal at App.tsx:686 is UNCHANGED (still `'Guided breathing practice — not medical advice.'` — Plan 08 migrates it).
    - The LearnDialog invocation at App.tsx:705 is UNCHANGED (Plan 03's LearnDialog stop-gap covers it; Plan 08 widens LearnDialogProps).
  </acceptance_criteria>
  <done>
    App.tsx imports UI_STRINGS, drills EN-fixture `strings` slices to 5 consumer call sites, passes TS compile + App-level tests, leaves Plan 08's responsibilities intact.
  </done>
</task>

<task type="auto">
  <name>Task 9: Per-commit green-gate (D-16)</name>
  <files>(no source edit — verification only)</files>
  <read_first>
    - .planning/phases/19-language-switching/19-CONTEXT.md (D-16, D-22)
  </read_first>
  <action>
    Run `npx tsc --noEmit && npm run lint && npm run build && npm test -- --run`. Also explicitly run `npx vitest run src/styles/theme.no-hardcoded-classes.test.ts` to verify the THEME-UI-01 guard remains green. After all green, commit `feat(19-06): dialogs + anchors + pickers accept strings slices + App.tsx EN-fixture stop-gap` containing the 21 files in `files_modified` (20 component sources/tests + `src/app/App.tsx`).
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run lint && npm run build && npm test -- --run 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - All 4 green-gate steps exit 0.
    - `npx vitest run src/styles/theme.no-hardcoded-classes.test.ts` exits 0 (D-22).
    - Commit `feat(19-06): dialogs + anchors + pickers accept strings slices + App.tsx EN-fixture stop-gap` created.
    - `git diff HEAD~1 --stat` shows exactly the 21 files in `files_modified` (20 component sources/tests + `src/app/App.tsx`).
    - App.tsx IS in this commit (Task 9 EN-fixture stop-gap so TS compile passes).
    - `grep -c "UI_STRINGS.en" src/app/App.tsx` returns >= 10 (Plan 07's 5 + Plan 06's 5 stop-gap fixtures combined). Plan 08 will drive this count to 0.
  </acceptance_criteria>
  <done>
    Green-gate passes; commit landed; THEME-UI-01 still green.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| App.tsx → component prop drill | Typed `strings: UiStrings['<slice>']` enforces shape at compile time. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-16 | Tampering | strings prop drilling | mitigate | TypeScript surface — every consumer typed against `UiStrings[<slice>]`; missing slices fail compile. |
| T-19-17 | Information Disclosure | aria-labels translated via strings prop | accept | aria-labels are user-facing UI; translation is the requirement (I18N-04). |
</threat_model>

<verification>
- `npx tsc --noEmit && npm run lint && npm run build && npm test -- --run` exits 0.
- `npx vitest run src/styles/theme.no-hardcoded-classes.test.ts` exits 0 (D-22).
- 21 files modified: 20 component sources/tests + `src/app/App.tsx` (EN-fixture stop-gap).
- `grep -c "UI_STRINGS.en" src/app/App.tsx` returns >= 10 (Plan 07 wave 2 + Plan 06 wave 3 fixtures combined); Plan 08 drives this to 0 when it wires `useLocale()`.
- App.tsx does NOT yet invoke `useLocale()` — that's Plan 08's responsibility.
- App.tsx:686 medical-advice literal is UNCHANGED in this plan (Plan 08 migrates it).
- App.tsx:705 LearnDialog invocation is UNCHANGED in this plan (Plan 03's LearnDialog stop-gap covers it; Plan 08 widens LearnDialogProps).
</verification>

<success_criteria>
1. 9 component files updated to accept strings prop (3 dialogs + 2 anchors + 1 toggle + 3 pickers + LanguagePicker section-label widening).
2. 9 test files updated to use `EN_STRINGS_FIXTURE = UI_STRINGS.en` pattern.
3. Picker option labels come from `strings[id]` (D-12 — option-name translation).
4. Picker section labels come from `sectionLabel` prop drilled by SettingsDialog.
5. LanguagePicker native endonyms remain hardcoded via LOCALE_DISPLAY_NAMES per D-14.
6. THEME-UI-01 token-binding guard remains green.
7. App.tsx EN-fixture stop-gap drills `UI_STRINGS.en.<slice>` to 5 modified consumer call sites; Plan 08 removes the stop-gap.
8. Green-gate exits 0 after the commit (no separate stop-gap commit needed; the App.tsx drill IS in this commit).
</success_criteria>

<output>
After completion, create `.planning/phases/19-language-switching/19-06-SUMMARY.md` capturing: 21 files touched (9 component sources + 9 component tests + LanguagePicker source/test (Task 7) + App.tsx EN-fixture stop-gap (Task 9)), per-component strings-slice mapping table, confirmation of THEME-UI-01 green, the exact App.tsx stop-gap delta (1 import + 5 prop additions), final `grep -c "UI_STRINGS.en" src/app/App.tsx` count (>= 10 after Plan 06; Plan 08 inverts to 0).
</output>
