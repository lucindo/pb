---
phase: 19-language-switching
plan: 08
type: execute
wave: 4
depends_on: ["19-01", "19-02", "19-03", "19-04", "19-05", "19-06", "19-07"]
files_modified:
  - src/app/App.tsx
  - src/components/LearnDialog.tsx
  - src/components/LearnDialog.test.tsx
autonomous: true
requirements:
  - I18N-01
  - I18N-02
  - I18N-06
must_haves:
  truths:
    - "App.tsx invokes const { locale, uiStrings } = useLocale() once per render"
    - "App.tsx resolves const learnContent = LEARN_CONTENT[locale] and const lockedCopy = LOCKED_COPY[locale] once per render"
    - "App.tsx drills uiStrings.* slices to every modified child component (~15 consumers)"
    - "App.tsx:686 inline medical-advice literal is replaced with {lockedCopy.medicalAdviceLine}"
    - "LearnDialog accepts learnContent + lockedCopy + strings props; renders lockedCopy.inspiredByForrest as a separate paragraph after forrest.body"
    - "LearnDialog stop-gap removed: `LEARN_CONTENT.en` / `LOCKED_COPY.en` direct accesses become prop-driven `learnContent` / `lockedCopy`"
    - "LearnDialog.test.tsx renderHelper accepts a locale parameter and passes `LEARN_CONTENT[locale]` / `LOCKED_COPY[locale]` / `UI_STRINGS[locale].learn` to the rendered dialog"
    - "App.tsx EN-fixture stop-gap removed: `import { UI_STRINGS }` line deleted; every `strings={UI_STRINGS.en.<slice>}` becomes `strings={uiStrings.<slice>}`"
    - "After Plan 08: `grep -c \"UI_STRINGS.en\" src/app/App.tsx` returns 0; `grep -c \"uiStrings\\.\" src/app/App.tsx` returns >= 10"
  artifacts:
    - path: "src/app/App.tsx"
      provides: "useLocale orchestrator invocation + slice drill to 15 children + medical-advice migration"
      contains: "useLocale\\|lockedCopy.medicalAdviceLine"
    - path: "src/components/LearnDialog.tsx"
      provides: "learnContent + lockedCopy + strings prop consumption; locked-phrase paragraph composition"
      contains: "lockedCopy.inspiredByForrest"
  key_links:
    - from: "src/app/App.tsx"
      to: "src/hooks/useLocale.ts"
      via: "useLocale invocation"
      pattern: "useLocale\\(\\)"
    - from: "src/app/App.tsx"
      to: "src/components/LearnDialog.tsx"
      via: "learnContent + lockedCopy + strings prop drill"
      pattern: "learnContent=\\{learnContent\\}"
    - from: "src/components/LearnDialog.tsx"
      to: "src/content/lockedCopy.ts"
      via: "lockedCopy.inspiredByForrest + lockedCopy.affiliationLine consumption"
      pattern: "lockedCopy.inspiredByForrest\\|lockedCopy.affiliationLine"
---

<objective>
Wire `useLocale()` into `src/app/App.tsx`; resolve `learnContent` + `lockedCopy` per render; drill `uiStrings.*` slices to every modified child component (15+ consumers across Plans 05–07); migrate the inline medical-advice literal at App.tsx:686 to `{lockedCopy.medicalAdviceLine}`. Update `LearnDialog.tsx` to accept `learnContent` + `lockedCopy` + `strings` props (replacing the current direct `LEARN_CONTENT` import) and render `lockedCopy.inspiredByForrest` as a separate paragraph after the forrest section body; replace the affiliation literal at `LearnDialog.tsx:171` with `{lockedCopy.affiliationLine}`. Update `LearnDialog.test.tsx` render helper to pass the new props; existing medical-advice modal-absence guard (line 48) stays green; existing locked-phrase presence guard (line 43-46) stays green because the phrase still appears in the rendered modal — just composed from a separate prop. Add a new PT-BR rendering smoke test.

Purpose: Plan 08 is the integration plan. It ties every Wave 1–3 contribution into the running App.tsx render tree and migrates the last 2 inline locked-copy literals (medical-advice + affiliation) to `LOCKED_COPY` consumption. After this plan, language switching is end-to-end functional in idle state.

Output: 3 files modified; full app supports EN + PT-BR rendering via SettingsDialog. App-level locale-switch test deferred to Plan 09 (separate concern from this plan's integration scope).

**Plan 08's stop-gap-removal responsibility (UPDATED post-revision):**
After Plans 03/06/07, three stop-gaps exist:
- **LearnDialog stop-gap (from Plan 03):** `LearnDialog.tsx` uses `LEARN_CONTENT.en` + `LOCKED_COPY.en` direct access; `LearnDialog.test.tsx` uses `LEARN_CONTENT.en.links.keyVideos` direct access.
- **App.tsx Plan 06 stop-gap:** `import { UI_STRINGS } from '../content/strings'` + 5 `strings={UI_STRINGS.en.<slice>}` prop drills (SettingsAnchor, LearnAnchor, EndSessionDialog, ResetStatsDialog, SettingsDialog).
- **App.tsx Plan 07 stop-gap:** 5 additional `strings={UI_STRINGS.en.<slice>}` prop drills (BreathingShape, SessionReadout, SettingsForm, SessionControls, StatsFooter).

Plan 08 REMOVES all three stop-gaps:
- LearnDialog widens props to accept `learnContent` / `lockedCopy` / `strings`; the body switches from `LEARN_CONTENT.en` / `LOCKED_COPY.en` direct access to the prop-driven `learnContent` / `lockedCopy`; LearnDialog.test.tsx renderHelper passes `LEARN_CONTENT[locale]` / `LOCKED_COPY[locale]` / `UI_STRINGS[locale].learn` (locale=`'en'` default; new PT-BR smoke test passes `'pt-BR'`).
- App.tsx removes the `import { UI_STRINGS } from '../content/strings'` line, adds `import { useLocale } from '../hooks/useLocale'` + `import { LOCKED_COPY } from '../content/lockedCopy'` + (re-add or keep) `import { LEARN_CONTENT } from '../content/learnContent'`. Invokes `const { locale, uiStrings } = useLocale()` once per render. Resolves `const learnContent = LEARN_CONTENT[locale]; const lockedCopy = LOCKED_COPY[locale]`. Replaces every `strings={UI_STRINGS.en.<slice>}` with `strings={uiStrings.<slice>}`. Drills the 3 new LearnDialog props at the call site. Migrates the medical-advice literal at line 686.

Acceptance after Plan 08: `grep -c "UI_STRINGS.en" src/app/App.tsx` returns 0; `grep -c "uiStrings\." src/app/App.tsx` returns >= 10 (matches the stop-gap count from Plans 06+07); `grep -c "LEARN_CONTENT.en\|LOCKED_COPY.en" src/components/LearnDialog.tsx` returns 0 (replaced by prop-driven access).
</objective>

<execution_context>
@$HOME/.claude-personal-personal/get-shit-done/workflows/execute-plan.md
@$HOME/.claude-personal-personal/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/19-language-switching/19-CONTEXT.md
@.planning/phases/19-language-switching/19-PATTERNS.md
@.planning/phases/19-language-switching/19-01-SUMMARY.md
@.planning/phases/19-language-switching/19-03-SUMMARY.md
@.planning/phases/19-language-switching/19-04-SUMMARY.md
@.planning/phases/19-language-switching/19-06-SUMMARY.md
@.planning/phases/19-language-switching/19-07-SUMMARY.md

<interfaces>
From src/hooks/useLocale.ts (Plan 04):
```typescript
export function useLocale(): { locale: LocaleId; uiStrings: UiStrings }
```

From src/content/learnContent.ts (Plan 03):
```typescript
export const LEARN_CONTENT: Readonly<Record<LocaleId, LearnContent>>
```

From src/content/lockedCopy.ts (Plan 03):
```typescript
export interface LockedCopy { ... }
export const LOCKED_COPY: Readonly<Record<LocaleId, LockedCopy>>
```

From Plans 05–07, all consuming components accept `strings` (and `sectionLabel` for pickers, plus `learnContent` + `lockedCopy` for LearnDialog). The slice mapping is in 19-CONTEXT.md `<domain>` ##7.

App.tsx current state (post-Plans 06/07 if a stop-gap was applied — see 19-06-SUMMARY + 19-07-SUMMARY):
- May contain an `import { UI_STRINGS } from '../content/strings'` line + EN-fixture slice passes as temporary props.
- This plan REMOVES the stop-gap and replaces with the real `useLocale()` wiring.
- App.tsx:686 inline literal `Guided breathing practice — not medical advice.` (em-dash U+2014) — to be replaced.
- App.tsx:705 `<LearnDialog open={learnDialogOpen} onClose={onLearnClose} />` — to be widened with 3 new props.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: LearnDialog accepts learnContent + lockedCopy + strings props; locked-phrase composition</name>
  <files>src/components/LearnDialog.tsx, src/components/LearnDialog.test.tsx</files>
  <read_first>
    - src/components/LearnDialog.tsx (entire 187 lines — pay attention to line 3 import, line 14–17 props, line 67 destructure, line 77 title, lines 90–94 forrest section, line 104 + 143 section headings, line 171 affiliation literal, line 182 Close button)
    - src/components/LearnDialog.test.tsx (existing fixtures + line 43–46 locked-phrase presence guard + line 48 medical-advice absence guard — both MUST stay green)
    - src/content/learnContent.ts (Plan 03 output — `LEARN_CONTENT` is now `Record<LocaleId, LearnContent>`)
    - src/content/lockedCopy.ts (Plan 03 output)
    - src/content/strings.ts (Plan 01 — `UiStrings['learn']` slice)
    - .planning/phases/19-language-switching/19-PATTERNS.md (lines 677–755 — LearnDialog mutation pattern + JSX shape for extracted Forrest phrase)
    - .planning/phases/19-language-switching/19-CONTEXT.md (D-04 composition rule + D-11 prop drill choice + Claude's Discretion §"Planner picks JSX shape for the extracted Forrest phrase")
  </read_first>
  <behavior>
    - `LearnDialogProps` gains `learnContent: LearnContent`, `lockedCopy: LockedCopy`, `strings: UiStrings['learn']`.
    - The current `import { LEARN_CONTENT } from '../content/learnContent'` at line 3 is REMOVED (LearnDialog is locale-agnostic; receives resolved catalog).
    - Add type imports: `import type { LearnContent } from '../content/learnContent'`, `import type { LockedCopy } from '../content/lockedCopy'`, `import type { UiStrings } from '../content/strings'`.
    - Line 67 destructure: `const { explainer, links } = LEARN_CONTENT` → `const { explainer, links } = learnContent`.
    - Line 77 `About this practice` literal → `{strings.title}`.
    - Line 90–94 forrest section: after the existing `{explainer.forrest.body.split('\n\n').map(...)}` block, add a NEW `<p>` element (italicized closing paragraph per Claude's Discretion JSX-shape pick — preferred for aesthetic and accessibility per PATTERNS.md option A): `<p className="text-base leading-6 italic text-[var(--color-breathing-muted)] [&:not(:first-of-type)]:mt-2">{lockedCopy.inspiredByForrest}</p>`. Place INSIDE the same parent `<div>` as the forrest title + body paragraphs.
    - Line 104 `Forrest Knutson Resources` literal → `{strings.resourcesHeading}`.
    - Line 143 `Selected HRV Breathing Videos` literal → `{strings.videosHeading}`.
    - Line 171 affiliation `Independent project. Not affiliated with Forrest Knutson.` → `{lockedCopy.affiliationLine}`.
    - Line 182 `Close` button → `{strings.close}`.
    - Imperative dialog primitives (showModal, focus on closeButtonRef, cancel handler, backdrop-click) UNCHANGED.
    - LearnDialog.test.tsx:43–46 existing test `renders the locked phrase "inspired by Forrest's teachings"` STILL PASSES — the phrase is now rendered from `lockedCopy.inspiredByForrest` as a separate paragraph but the rendered text in the modal is byte-identical.
    - LearnDialog.test.tsx:48 medical-advice modal-absence guard STILL PASSES — App.tsx still owns the medical-advice literal; LearnDialog does NOT render `lockedCopy.medicalAdviceLine`.
    - New PT-BR rendering smoke test added: renders dialog with `locale='pt-BR'` fixtures and asserts the PT-BR forrest title appears + PT-BR `lockedCopy.inspiredByForrest` value appears; English baseline `About this practice` is absent.
  </behavior>
  <action>
    Edit `src/components/LearnDialog.tsx`:
    1. Remove the value imports added by Plan 03: `import { LEARN_CONTENT } from '../content/learnContent'` (line 3) AND `import { LOCKED_COPY } from '../content/lockedCopy'`. Both are replaced with type imports + prop-driven access (Plan 03 used value imports as a stop-gap; Plan 08 closes it).
    2. Add `import type { LearnContent } from '../content/learnContent'`, `import type { LockedCopy } from '../content/lockedCopy'`, `import type { UiStrings } from '../content/strings'`.
    3. Extend `LearnDialogProps` (lines 14–17) with `learnContent: LearnContent`, `lockedCopy: LockedCopy`, `strings: UiStrings['learn']`.
    4. Destructure the 3 new props in the function signature (line 19).
    5. Replace line 67 `const { explainer, links } = LEARN_CONTENT.en` (Plan 03 stop-gap form) with `const { explainer, links } = learnContent` (prop-driven, locale-agnostic).
    6. Replace line 77 inner text `About this practice` with `{strings.title}`.
    7. Plan 03 ALREADY added the locked-phrase paragraph using the stop-gap form `<p className="...">{LOCKED_COPY.en.inspiredByForrest}</p>`. Plan 08 SWAPS the data source: replace `{LOCKED_COPY.en.inspiredByForrest}` with `{lockedCopy.inspiredByForrest}` (prop-driven). Do NOT add a duplicate paragraph; do NOT change the className. The italicized closing paragraph JSX shape (D-04 + Claude's Discretion option A) is preserved verbatim from Plan 03.
    8. Replace line 104 `Forrest Knutson Resources` with `{strings.resourcesHeading}`.
    9. Replace line 143 `Selected HRV Breathing Videos` with `{strings.videosHeading}`.
    10. Plan 03 ALREADY migrated line 171 from the inline literal to `{LOCKED_COPY.en.affiliationLine}`. Plan 08 SWAPS the data source: replace `{LOCKED_COPY.en.affiliationLine}` with `{lockedCopy.affiliationLine}` (prop-driven).
    11. Replace line 182 `Close` button text with `{strings.close}`.

    Edit `src/components/LearnDialog.test.tsx`:
    1. Add imports: `import { LEARN_CONTENT } from '../content/learnContent'`, `import { LOCKED_COPY } from '../content/lockedCopy'`, `import { UI_STRINGS } from '../content/strings'`, `import type { LocaleId } from '../domain/settings'`.
    2. Update the existing `renderDialog` helper to accept an optional `locale: LocaleId` (default `'en'`) and pass `learnContent={LEARN_CONTENT[locale]}`, `lockedCopy={LOCKED_COPY[locale]}`, `strings={UI_STRINGS[locale].learn}` to the rendered `<LearnDialog .../>`.
    2a. Plan 03 stop-gap line 147 currently reads `const keyVideos = LEARN_CONTENT.en.links.keyVideos`. Update to `const keyVideos = LEARN_CONTENT[locale].links.keyVideos` where `locale` is derived from a default within the test (e.g. `const TEST_LOCALE: LocaleId = 'en'`) OR simply hardcode `LEARN_CONTENT.en.links.keyVideos` if the test is EN-specific (line 145–155 test asserts EN keyVideos security attrs). Planner picks: keep `LEARN_CONTENT.en.links.keyVideos` for this EN-only security-attrs test (it's a constant-data assertion; locale-agnostic; the `.en` qualifier remains because the test specifically asserts EN URLs).
    3. The existing line-43-46 test `renders the locked phrase "inspired by Forrest's teachings" (D-11, D-19e)` STAYS — assertion `expect(screen.getByText(/inspired by Forrest's teachings/)).toBeInTheDocument()` continues to pass (phrase now comes from `lockedCopy.inspiredByForrest` paragraph).
    4. The existing line-48 medical-advice modal-absence guard STAYS — assertion `expect(screen.queryByText(/not medical advice/i)).not.toBeInTheDocument()` continues to pass (medical-advice lives in App.tsx, not LearnDialog).
    5. Add new `it('renders PT-BR forrest title + lockedCopy.inspiredByForrest when locale="pt-BR"', () => { renderDialog({ open: true, locale: 'pt-BR' }); expect(screen.getByText(UI_STRINGS['pt-BR'].learn.title)).toBeInTheDocument(); expect(screen.getByText(LOCKED_COPY['pt-BR'].inspiredByForrest)).toBeInTheDocument(); expect(screen.queryByText('About this practice')).not.toBeInTheDocument() })`.
  </action>
  <verify>
    <automated>npx vitest run src/components/LearnDialog.test.tsx 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "import { LEARN_CONTENT }" src/components/LearnDialog.tsx` returns 0 (Plan 03 stop-gap value import removed).
    - `grep -c "import { LOCKED_COPY }" src/components/LearnDialog.tsx` returns 0 (Plan 03 stop-gap value import removed; locked copy now flows through props).
    - `grep -c "LEARN_CONTENT.en\|LOCKED_COPY.en" src/components/LearnDialog.tsx` returns 0 (Plan 03 `.en` direct access stop-gaps replaced by prop-driven `learnContent` / `lockedCopy`).
    - `grep -c "import type { LearnContent }\\|import type { LockedCopy }\\|import type { UiStrings }" src/components/LearnDialog.tsx` returns 3.
    - `grep -c "lockedCopy.inspiredByForrest" src/components/LearnDialog.tsx` returns 1 (new paragraph).
    - `grep -c "lockedCopy.affiliationLine" src/components/LearnDialog.tsx` returns 1.
    - `grep -c "strings.title\\|strings.close\\|strings.resourcesHeading\\|strings.videosHeading" src/components/LearnDialog.tsx` returns 4.
    - `grep -c "Independent project. Not affiliated" src/components/LearnDialog.tsx` returns 0 (affiliation literal removed).
    - `grep -c "About this practice" src/components/LearnDialog.tsx` returns 0.
    - `npx vitest run src/components/LearnDialog.test.tsx` exits 0.
    - Existing tests at lines 43–46 (locked phrase presence) and 48 (medical-advice absence) still pass — `grep -c "inspired by Forrest's teachings\\|not medical advice" src/components/LearnDialog.test.tsx` returns >= 2.
    - New PT-BR smoke test present (`grep -c "locale: 'pt-BR'\\|locale='pt-BR'" src/components/LearnDialog.test.tsx` returns >= 1).
  </acceptance_criteria>
  <done>
    LearnDialog accepts new props; locked-phrase composed as separate paragraph; medical-advice still absent from modal; affiliation comes from lockedCopy; PT-BR smoke test passes.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: App.tsx invokes useLocale + resolves catalogs + drills slices + migrates medical-advice literal</name>
  <files>src/app/App.tsx</files>
  <read_first>
    - src/app/App.tsx (entire file — 710 lines; pay attention to: imports near top, hook invocations near line 141–142, line 686 medical-advice literal, line 705 LearnDialog invocation, line 707 SettingsDialog invocation, and EVERY child component invocation that was modified in Plans 05–07)
    - .planning/phases/19-language-switching/19-PATTERNS.md (lines 1219–1257 — App.tsx mutation pattern)
    - .planning/phases/19-language-switching/19-CONTEXT.md (D-06 hook return shape + D-11 LearnDialog prop shape + D-04 medical-advice migration)
    - 19-06-SUMMARY.md + 19-07-SUMMARY.md (notes on whether a temporary stop-gap was applied; this task removes any stop-gap)
  </read_first>
  <behavior>
    - Imports added near top of `src/app/App.tsx`:
      - `import { useLocale } from '../hooks/useLocale'`
      - `import { LEARN_CONTENT } from '../content/learnContent'` (already imported? verify; if removed elsewhere, re-add)
      - `import { LOCKED_COPY } from '../content/lockedCopy'`
    - Hook invocation added near line 142 (after `useTheme()` and `useVisualVariant()`):
      - `const { locale, uiStrings } = useLocale()`
    - Per-render catalog resolution (immediately after `useLocale()`):
      - `const learnContent = LEARN_CONTENT[locale]`
      - `const lockedCopy = LOCKED_COPY[locale]`
    - Line 686 medical-advice literal `Guided breathing practice — not medical advice.` → `{lockedCopy.medicalAdviceLine}`.
    - Line 705 LearnDialog invocation gains `learnContent={learnContent} lockedCopy={lockedCopy} strings={uiStrings.learn}`.
    - Line 707 SettingsDialog invocation gains `strings={uiStrings.settings}`.
    - Every other modified child invocation (SessionControls, SettingsForm, MuteToggle, SessionReadout, StatsFooter, SettingsAnchor, LearnAnchor, EndSessionDialog, ResetStatsDialog, BreathingShape) gains its `strings={uiStrings.<slice>}` prop. Mapping per CONTEXT.md `<domain>` ##7:
      - `<SessionControls strings={uiStrings.controls} ... />`
      - `<SettingsForm strings={uiStrings.settingsForm} ... />`
      - `<MuteToggle strings={uiStrings.mute} ... />`
      - `<SessionReadout strings={uiStrings.readout} ... />`
      - `<StatsFooter strings={uiStrings.stats} ... />`
      - `<SettingsAnchor strings={uiStrings.anchors} ... />`
      - `<LearnAnchor strings={uiStrings.anchors} ... />`
      - `<EndSessionDialog strings={uiStrings.endSessionDialog} ... />`
      - `<ResetStatsDialog strings={uiStrings.resetStatsDialog} ... />`
      - `<BreathingShape strings={uiStrings.breathing} ... />`
    - The `message='Session complete'` prop passed to SessionReadout (if it's a hardcoded inline literal at the call site) becomes `message={uiStrings.readout.sessionComplete}` (per Plan 07 Task 4 deferral note).
    - Any temporary EN-fixture stop-gap (`import { UI_STRINGS } from '../content/strings'`) added by Plans 06/07 is REMOVED in this task — replaced by the real `useLocale()` wiring. Verify by `grep -c "UI_STRINGS\\.en" src/app/App.tsx` returns 0 after this plan lands.
    - No other refactor: existing hooks, state, effects, refs, callbacks, JSX structure remain unchanged except for prop additions and the medical-advice + LearnDialog/SettingsDialog/SessionReadout literal swaps.
  </behavior>
  <action>
    Edit `src/app/App.tsx`:
    1. Add the 3 new imports near the top of the import block (alongside existing `useTheme` / `useVisualVariant` imports).
    2. Remove any temporary `import { UI_STRINGS } from '../content/strings'` line if present.
    3. Add the `useLocale()` invocation + catalog resolution near line 142 — placement: immediately after `useVisualVariant()` so all 3 prefs hooks sit contiguously.
    4. For EACH modified child component invocation, add the `strings={uiStrings.<slice>}` prop per the mapping above. Find invocations via `grep -n "<SessionControls\\|<SettingsForm\\|<MuteToggle\\|<SessionReadout\\|<StatsFooter\\|<SettingsAnchor\\|<LearnAnchor\\|<EndSessionDialog\\|<ResetStatsDialog\\|<BreathingShape\\|<LearnDialog\\|<SettingsDialog" src/app/App.tsx`.
    5. Replace line 686 medical-advice literal with `{lockedCopy.medicalAdviceLine}`. Verify the em-dash in the source file equals U+2014.
    6. Update the LearnDialog invocation (line 705) to pass `learnContent={learnContent} lockedCopy={lockedCopy} strings={uiStrings.learn}` alongside the existing `open` + `onClose` props.
    7. Update the SettingsDialog invocation (line 707) to pass `strings={uiStrings.settings}`.
    8. If the SessionReadout call site passes `message='Session complete'`, replace with `message={uiStrings.readout.sessionComplete}`.
    9. Verify that the existing cross-tab `'storage'` listener at App.tsx:119–129 still works as before — useLocale registers its own listener but the App-level listener (for stats) is unaffected (per CONTEXT.md `<code_context>` Integration Points). Do NOT merge or remove the App-level storage listener.

    NO changes to the `useSessionEngine`, `useAudioCues`, `useWakeLock`, session ref pattern, `inSessionView` derivation, dialog open-state machinery, or rAF effects. Diff scope: imports + 3 new lines (hook + 2 catalog consts) + ~15 prop drilling additions + 1 medical-advice literal replacement.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20 && npx vitest run src/app 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "import { useLocale }" src/app/App.tsx` returns 1.
    - `grep -c "import { LOCKED_COPY }" src/app/App.tsx` returns 1.
    - `grep -c "useLocale()" src/app/App.tsx` returns 1.
    - `grep -c "const learnContent = LEARN_CONTENT\\[locale\\]" src/app/App.tsx` returns 1.
    - `grep -c "const lockedCopy = LOCKED_COPY\\[locale\\]" src/app/App.tsx` returns 1.
    - `grep -c "{lockedCopy.medicalAdviceLine}" src/app/App.tsx` returns 1.
    - `grep -c "Guided breathing practice — not medical advice" src/app/App.tsx` returns 0 (literal removed).
    - `grep -c "strings=" src/app/App.tsx` returns >= 12 (12+ prop drills for the modified consumers).
    - `grep -c "UI_STRINGS.en" src/app/App.tsx` returns 0 (Plan 06 + Plan 07 EN-fixture stop-gaps removed).
    - `grep -c "import { UI_STRINGS }" src/app/App.tsx` returns 0 (Plan 07's wave-2 stop-gap import line deleted; the import added in Plan 07 wave 2 is what Plan 08 removes).
    - `grep -c "uiStrings\." src/app/App.tsx` returns >= 10 (matches the Plan 06+07 stop-gap count — one `uiStrings.<slice>` per consumer call site).
    - `grep -c "learnContent={learnContent}\\|lockedCopy={lockedCopy}" src/app/App.tsx` returns 2 (drilled to LearnDialog).
    - `npx tsc --noEmit` exits 0.
    - `npx vitest run src/app` exits 0 (all App-level tests pass with the new wiring).
  </acceptance_criteria>
  <done>
    App.tsx invokes useLocale, resolves catalogs, drills slices to 15 consumers, migrates medical-advice literal; no stop-gap remains; all App-level tests pass.
  </done>
</task>

<task type="auto">
  <name>Task 3: Per-commit green-gate (D-16)</name>
  <files>(no source edit — verification only)</files>
  <read_first>
    - .planning/phases/19-language-switching/19-CONTEXT.md (D-16, D-22)
  </read_first>
  <action>
    Run `npx tsc --noEmit && npm run lint && npm run build && npm test -- --run`. After exit 0, commit `feat(19-08): App.tsx wires useLocale + medical-advice migration + LearnDialog locked-copy composition` containing the 3 files in `files_modified`. This is the integration commit — full app supports EN + PT-BR rendering after this commit lands.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run lint && npm run build && npm test -- --run 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - All 4 green-gate steps exit 0.
    - `npx vitest run src/styles/theme.no-hardcoded-classes.test.ts` exits 0 (D-22).
    - Commit `feat(19-08): App.tsx wires useLocale + medical-advice migration + LearnDialog locked-copy composition` created.
    - `git diff HEAD~1 --stat` shows exactly 3 files: `src/app/App.tsx`, `src/components/LearnDialog.tsx`, `src/components/LearnDialog.test.tsx`.
    - Total test count after this commit >= 657 (Phase 18 baseline 644 + Phase 19 additions from Plans 01–07 should add ~13+ tests, plus Plan 08 adds at least 1 PT-BR LearnDialog smoke).
  </acceptance_criteria>
  <done>
    Green-gate passes; commit landed; THEME-UI-01 still green; full app supports EN + PT-BR rendering.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| App.tsx → render tree | All locale-derived data flows through useLocale return value + Plan 03 catalogs; no untrusted input. |
| App.tsx:686 → DOM text node | `lockedCopy.medicalAdviceLine` is byte-locked at the LOCKED_COPY source per D-02 (Plan 03 frozen-EN guard). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-20 | Tampering | App.tsx medical-advice migration | mitigate | LOCKED_COPY.en.medicalAdviceLine asserted byte-equal in Plan 03 lockedCopy.test.ts; future text drift breaks that test. |
| T-19-21 | Information Disclosure | LearnDialog locked-phrase paragraph | accept | The phrase IS visible by design (Forrest claim-safe positioning per Phase 6 D-12); D-04 composition rule splits the phrase from learnContent.body but renders it back at JSX time. |
</threat_model>

<verification>
- `npx tsc --noEmit && npm run lint && npm run build && npm test -- --run` exits 0.
- `npx vitest run src/styles/theme.no-hardcoded-classes.test.ts` exits 0 (D-22).
- App.tsx contains ZERO inline locked-copy literals (`grep -c "Guided breathing practice\\|Independent project. Not affiliated\\|inspired by Forrest's teachings" src/app/App.tsx` returns 0).
- LearnDialog.tsx renders locked phrase + affiliation from LOCKED_COPY props (no inline literals).
- LearnDialog.test.tsx existing locked-phrase + medical-advice-absence guards still pass.
- New PT-BR LearnDialog smoke test passes.
</verification>

<success_criteria>
1. App.tsx invokes useLocale + resolves catalogs + drills slices to all 15 modified consumers.
2. Medical-advice literal migrated to lockedCopy.medicalAdviceLine.
3. LearnDialog accepts learnContent + lockedCopy + strings props; renders inspiredByForrest as separate italic paragraph (Plan 03's italicized JSX shape preserved; data source swapped from `LOCKED_COPY.en` to `lockedCopy` prop).
4. LearnDialog affiliation comes from lockedCopy.affiliationLine (data source swapped from `LOCKED_COPY.en` to `lockedCopy` prop).
5. Existing LearnDialog test guards (locked-phrase presence, medical-advice modal-absence, affiliation byte-equality, keyVideos security attrs) still pass.
6. PT-BR LearnDialog smoke test passes.
7. Plan 03 LearnDialog stop-gap removed: `grep -c "LEARN_CONTENT.en\|LOCKED_COPY.en" src/components/LearnDialog.tsx` returns 0.
8. Plan 06 + Plan 07 App.tsx EN-fixture stop-gaps removed: `grep -c "UI_STRINGS.en" src/app/App.tsx` returns 0; `grep -c "import { UI_STRINGS }" src/app/App.tsx` returns 0.
9. Green-gate exits 0 after the commit.
</success_criteria>

<output>
After completion, create `.planning/phases/19-language-switching/19-08-SUMMARY.md` capturing: 3 files modified, count of prop-drill additions in App.tsx, confirmation that medical-advice literal moved to LOCKED_COPY, confirmation that LearnDialog locked-phrase composition is JSX option A (italicized closing paragraph), test count delta, full app E2E EN + PT-BR rendering confirmation.
</output>
