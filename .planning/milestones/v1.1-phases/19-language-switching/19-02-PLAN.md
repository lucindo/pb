---
phase: 19-language-switching
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/hooks/useLocaleChoice.ts
  - src/hooks/useLocaleChoice.test.ts
autonomous: true
requirements:
  - I18N-01
must_haves:
  truths:
    - "useLocaleChoice returns { locale, setLocale } with stable setLocale identity across re-renders"
    - "setLocale writes new locale to disk via savePrefs while preserving theme/variant/timbre fields"
    - "setLocale dispatches hrv:prefs-changed with detail.key === 'locale' and detail.value === <new locale>"
    - "Initial state is seeded from loadPrefs().locale"
  artifacts:
    - path: "src/hooks/useLocaleChoice.ts"
      provides: "picker-side hook for LanguagePicker"
      contains: "export function useLocaleChoice"
    - path: "src/hooks/useLocaleChoice.test.ts"
      provides: "6 hook tests mirroring useTimbreChoice.test.ts"
      contains: "describe('useLocaleChoice'"
  key_links:
    - from: "src/hooks/useLocaleChoice.ts"
      to: "src/storage/prefs.ts"
      via: "loadPrefs + savePrefs imports"
      pattern: "loadPrefs, savePrefs"
    - from: "src/hooks/useLocaleChoice.ts"
      to: "window"
      via: "CustomEvent dispatch on 'hrv:prefs-changed'"
      pattern: "dispatchEvent.*hrv:prefs-changed"
---

<objective>
Create `src/hooks/useLocaleChoice.ts` as a verbatim clone of `src/hooks/useTimbreChoice.ts` with `timbre → locale` rename per D-08, plus `src/hooks/useLocaleChoice.test.ts` mirroring the 6-test pattern from `useTimbreChoice.test.ts`.

Purpose: Picker-side companion hook for `LanguagePicker.tsx` (Wave 3). Owns the `savePrefs` write path + `'hrv:prefs-changed'` CustomEvent dispatch with `detail.key === 'locale'`. Optimistic-UI local state mirror so the picker reflects selection instantly.

Output: Both hook files + test file passing under `npx vitest run src/hooks/useLocaleChoice.test.ts`.
</objective>

<execution_context>
@$HOME/.claude-personal-personal/get-shit-done/workflows/execute-plan.md
@$HOME/.claude-personal-personal/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/19-language-switching/19-CONTEXT.md
@.planning/phases/19-language-switching/19-PATTERNS.md

<interfaces>
<!-- LocaleId locked in domain/settings.ts. loadPrefs/savePrefs locked in storage/prefs.ts (Phase 14 D-09). -->

From src/domain/settings.ts:
```typescript
export type LocaleId = 'en' | 'pt-BR'
```

From src/hooks/useTimbreChoice.ts (verbatim clone target — read in full, lines 24–49):
```typescript
import { useCallback, useState } from 'react'
import { loadPrefs, savePrefs } from '../storage/prefs'
import type { TimbreId } from '../domain/settings'

export function useTimbreChoice(): { timbre: TimbreId; setTimbre: (next: TimbreId) => void } {
  const [timbre, setTimbreState] = useState<TimbreId>(() => loadPrefs().timbre)
  const setTimbre = useCallback((next: TimbreId): void => {
    const current = loadPrefs()
    savePrefs({ ...current, timbre: next })
    setTimbreState(next)
    window.dispatchEvent(
      new CustomEvent('hrv:prefs-changed', { detail: { key: 'timbre', value: next } }),
    )
  }, [])
  return { timbre, setTimbre }
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create src/hooks/useLocaleChoice.ts as verbatim clone of useTimbreChoice.ts</name>
  <files>src/hooks/useLocaleChoice.ts</files>
  <read_first>
    - src/hooks/useTimbreChoice.ts (verbatim clone source — entire file)
    - src/domain/settings.ts (line 83 — LocaleId type, line 91 — DEFAULT_LOCALE)
    - .planning/phases/19-language-switching/19-PATTERNS.md (lines 364–409 — useLocaleChoice clone pattern)
    - .planning/phases/19-language-switching/19-CONTEXT.md (D-08 verbatim clone decision + D-21 CustomEvent contract)
  </read_first>
  <behavior>
    - `useLocaleChoice` returns `{ locale: LocaleId, setLocale: (next: LocaleId) => void }`.
    - Initial state is seeded from `loadPrefs().locale` via `useState(() => loadPrefs().locale)` (lazy seed — avoids reading on every render).
    - `setLocale(next)` performs four ordered side effects: (1) fresh `loadPrefs()` read, (2) `savePrefs({ ...current, locale: next })` write, (3) `setLocaleState(next)` local optimistic update, (4) `window.dispatchEvent(new CustomEvent('hrv:prefs-changed', { detail: { key: 'locale', value: next } }))`.
    - `setLocale` identity is stable across re-renders (`useCallback` with empty deps).
    - File compiles under strict TS with `react-hooks/exhaustive-deps: error` — empty deps are correct because `setLocaleState` is stable from `useState`, `loadPrefs`/`savePrefs` are module-level, and `window` is global.
  </behavior>
  <action>
    Create `src/hooks/useLocaleChoice.ts` by cloning `src/hooks/useTimbreChoice.ts` verbatim and applying these renames:
    - `TimbreId` → `LocaleId` (type import from `'../domain/settings'`).
    - `useTimbreChoice` → `useLocaleChoice` (export function name).
    - `timbre` → `locale` (all 3 occurrences in destructure, return shape, and parameter names).
    - `setTimbreState` → `setLocaleState`.
    - `setTimbre` → `setLocale` (useCallback const name).
    - `loadPrefs().timbre` → `loadPrefs().locale` (seed).
    - `savePrefs({ ...current, timbre: next })` → `savePrefs({ ...current, locale: next })`.
    - `{ detail: { key: 'timbre', value: next } }` → `{ detail: { key: 'locale', value: next } }` (D-21 contract).

    Preserve the entire comment block at lines 1–22 of useTimbreChoice.ts (header comments explain the Pitfall 4 same-tab sync rationale, the `{ key, value }` shape, and the optimistic mirror). Update phase references from "Phase 18" to "Phase 19" and from "D-08" to "D-08 + D-21" (Phase 19 decision identifiers). Update `D-18 contract reuse` reference to `D-21 contract reuse`. Replace `timbre → theme/variant/locale` forward-compat note with `locale → theme/variant/timbre` (now Phase 19 is the consumer; the prior dispatchers are theme/variant/timbre).

    NO additional logic, NO additional effects, NO extra parameters. The behavioral fingerprint must be identical to `useTimbreChoice` modulo the rename per D-08.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -10 && diff <(grep -v -E "^(//|import|export function|  const|  return)" src/hooks/useTimbreChoice.ts | grep -v "^$") <(grep -v -E "^(//|import|export function|  const|  return)" src/hooks/useLocaleChoice.ts | grep -v "^$") && echo "STRUCTURE_MATCHES"</automated>
  </verify>
  <acceptance_criteria>
    - `src/hooks/useLocaleChoice.ts` exists.
    - `npx tsc --noEmit` exits 0.
    - File exports `useLocaleChoice` with signature `() => { locale: LocaleId; setLocale: (next: LocaleId) => void }`.
    - `grep -c "loadPrefs().locale" src/hooks/useLocaleChoice.ts` returns 1 (seed).
    - `grep -c "savePrefs(.*locale: next" src/hooks/useLocaleChoice.ts` returns 1 (write).
    - `grep -c "detail: { key: 'locale', value: next }" src/hooks/useLocaleChoice.ts` returns 1 (dispatch).
    - `grep -c "useCallback" src/hooks/useLocaleChoice.ts` returns 1 (stable identity).
    - `grep -c "TimbreId" src/hooks/useLocaleChoice.ts` returns 0 (no rename leaks; the forward-compat comment naming theme/variant/timbre is intentional and stays — D-08 + D-21).
    - `grep -c "eslint-disable" src/hooks/useLocaleChoice.ts` returns 0 (D-24 — no new disables).
  </acceptance_criteria>
  <done>
    `src/hooks/useLocaleChoice.ts` is a behaviorally verbatim clone of `useTimbreChoice.ts` with the locale rename applied. No timbre-string residue. Compiles cleanly.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create src/hooks/useLocaleChoice.test.ts mirroring useTimbreChoice.test.ts (6 tests)</name>
  <files>src/hooks/useLocaleChoice.test.ts</files>
  <read_first>
    - src/hooks/useTimbreChoice.test.ts (verbatim clone source — entire 112-line file)
    - src/hooks/useLocaleChoice.ts (the file just created in Task 1)
    - .planning/phases/19-language-switching/19-PATTERNS.md (lines 413–438 — useLocaleChoice.test.ts pattern + helper)
  </read_first>
  <behavior>
    - 6 tests pass under `npx vitest run src/hooks/useLocaleChoice.test.ts`:
      1. initial state matches `loadPrefs().locale` when localStorage is pre-seeded with `pt-BR`.
      2. `setLocale('pt-BR')` updates local state optimistically.
      3. `setLocale('pt-BR')` writes the new locale to disk via savePrefs.
      4. `setLocale('pt-BR')` preserves other prefs fields — envelope merge contract (seed `theme: 'dark', timbre: 'bell', variant: 'square', locale: 'en'`, set locale to `'pt-BR'`, assert theme/timbre/variant unchanged).
      5. `setLocale('pt-BR')` dispatches `hrv:prefs-changed` CustomEvent with `detail.key === 'locale'` and `detail.value === 'pt-BR'`.
      6. `setLocale` identity is stable across re-renders.
    - `localStorage.clear()` in `beforeEach` + `afterEach` (test isolation).
  </behavior>
  <action>
    Create `src/hooks/useLocaleChoice.test.ts` by cloning `src/hooks/useTimbreChoice.test.ts` and applying these substitutions:
    - `useTimbreChoice` → `useLocaleChoice` (import + describe label + renderHook arg).
    - `timbre` (state field) → `locale`.
    - `setTimbre` → `setLocale`.
    - `TimbreId`-typed test values: change all timbre-id strings (`'bell'`, `'bowl'`, `'sine'`, `'chime'`) to locale ids:
      - Test 1 seed: `{ ...DEFAULT_FULL_PREFS, locale: 'pt-BR' }` and assert `result.current.locale === 'pt-BR'` (was: seed timbre `'bell'`).
      - Test 2: seed `locale: 'en'`, call `setLocale('pt-BR')`, assert state `'pt-BR'` (was: bowl → bell).
      - Test 3: seed `locale: 'en'`, call `setLocale('pt-BR')`, assert disk `'pt-BR'` (was: bowl → sine).
      - Test 4 envelope-merge: seed `{ theme: 'dark', timbre: 'bell', variant: 'square', locale: 'en' }`, call `setLocale('pt-BR')`, assert disk `locale: 'pt-BR'`, `theme: 'dark'`, `timbre: 'bell'`, `variant: 'square'` (mirror the envelope-merge test verbatim but switch which field changes vs. which fields are preserved).
      - Test 5 dispatch: call `setLocale('pt-BR')`, assert `event.detail.key === 'locale'` and `event.detail.value === 'pt-BR'` (was: `'timbre'` / `'bell'`).
      - Test 6 stable identity: rerender, assert `result.current.setLocale === initialSetter` (was: setTimbre).
    - Helper `seedPrefs(prefs: UserPrefs)` stays identical (line 10–12 in useTimbreChoice.test.ts).
    - `DEFAULT_FULL_PREFS` constant stays identical (theme: 'system', timbre: 'bowl', variant: 'orb', locale: 'en') — the rename does not affect its shape.
    - Preserve every `// eslint-disable-next-line ...` annotation with its `// Reason:` justification verbatim from the source (3 annotations in useTimbreChoice.test.ts at lines 56–58, 71–73, 95–96).
    - Preserve the `beforeEach`/`afterEach` blocks for localStorage isolation (lines 21–28).
  </action>
  <verify>
    <automated>npx vitest run src/hooks/useLocaleChoice.test.ts 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/hooks/useLocaleChoice.test.ts` exits 0.
    - Output reports 6 passing tests.
    - `grep -c "it(" src/hooks/useLocaleChoice.test.ts` returns 6.
    - `grep -c "'pt-BR'" src/hooks/useLocaleChoice.test.ts` returns >= 5 (used as the target locale across the 6 tests).
    - `grep -c "detail.key.*'locale'" src/hooks/useLocaleChoice.test.ts` returns 1 (CustomEvent contract assertion).
    - `grep -c "timbre\\|TimbreId" src/hooks/useLocaleChoice.test.ts` returns 0 (no rename leaks) — except where required as a preserved field in the envelope-merge seed.
    - Envelope-merge test asserts `raw.prefs.theme === 'dark'`, `raw.prefs.timbre === 'bell'`, `raw.prefs.variant === 'square'`, `raw.prefs.locale === 'pt-BR'` after setLocale.
  </acceptance_criteria>
  <done>
    `src/hooks/useLocaleChoice.test.ts` exists and 6 tests pass; envelope-merge test preserves all non-locale fields; CustomEvent contract validated with `key === 'locale'`.
  </done>
</task>

<task type="auto">
  <name>Task 3: Per-commit green-gate (D-16)</name>
  <files>(no source edit — verification only)</files>
  <read_first>
    - .planning/phases/19-language-switching/19-CONTEXT.md (D-16 per-commit green-gate)
  </read_first>
  <action>
    Run `npx tsc --noEmit && npm run lint && npm run build && npm test -- --run`. If any step fails, fix in this plan. After it exits 0, commit `feat(19-02): useLocaleChoice picker hook + tests` containing only `src/hooks/useLocaleChoice.ts` and `src/hooks/useLocaleChoice.test.ts`.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run lint && npm run build && npm test -- --run 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - All 4 green-gate steps exit 0.
    - Commit `feat(19-02): useLocaleChoice picker hook + tests` created.
    - `git diff HEAD~1 --stat` shows only `src/hooks/useLocaleChoice.ts` and `src/hooks/useLocaleChoice.test.ts`.
  </acceptance_criteria>
  <done>
    Green-gate passes; commit landed; no collateral file changes.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| component → localStorage (write) | `setLocale` writes user choice via `savePrefs`. Trust input shape: `next: LocaleId` is enum-narrowed at TypeScript compile time. |
| component → window CustomEvent (dispatch) | Dispatch payload `{ key: 'locale', value: next }`; consumed by `useLocale` (Plan 04) and other prefs hooks. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-03 | Tampering | savePrefs envelope merge in setLocale | mitigate | `{ ...current, locale: next }` spread preserves theme/timbre/variant per Phase 14 D-17 per-field isolation; Test 4 asserts the envelope-merge contract. |
| T-19-04 | Spoofing | hrv:prefs-changed CustomEvent | accept | Same-origin window event only; not cross-origin. Consumer (useLocale Plan 04) does an instanceof check and key filter before re-reading prefs. |
| T-19-05 | Repudiation | locale write path | accept | No audit log required for v1.1 — local-only single-user app per PROJECT.md. |
</threat_model>

<verification>
- `npx tsc --noEmit && npm run lint && npm run build && npm test -- --run` exits 0.
- `npx vitest run src/hooks/useLocaleChoice.test.ts` shows 6 passing tests.
- Behavioral parity with `useTimbreChoice` validated by test structure (Tests 1–6 mirror useTimbreChoice.test.ts:30–111).
- No edits to `src/domain/settings.ts` or `src/storage/prefs.ts` (D-18).
</verification>

<success_criteria>
1. `useLocaleChoice` is a behaviorally verbatim clone of `useTimbreChoice` (D-08).
2. 6 tests mirror the timbre suite with locale-id substitutions.
3. CustomEvent `detail.key === 'locale'` enforced and tested (D-21).
4. Envelope-merge preserves theme/timbre/variant fields (Phase 14 D-17 contract).
5. Green-gate exits 0 after the commit.
</success_criteria>

<output>
After completion, create `.planning/phases/19-language-switching/19-02-SUMMARY.md` capturing: file paths, test count (6), confirmation of verbatim clone, any deviations from useTimbreChoice.ts (should be only the locale rename + phase reference updates).
</output>
