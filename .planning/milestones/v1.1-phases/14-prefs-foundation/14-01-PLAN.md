---
phase: 14-prefs-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/domain/settings.ts
  - src/domain/settings.test.ts
  - src/storage/storage.ts
  - src/storage/prefs.ts
  - src/storage/prefs.test.ts
  - src/storage/index.ts
autonomous: true
requirements:
  - INFRA-01
  - INFRA-02
  - INFRA-03

must_haves:
  truths:
    - "An Envelope with a `prefs` sub-tree round-trips through readEnvelope / writeEnvelope without losing other sub-keys (SC1, D-16 spread-then-override preserved)."
    - "Predicates isValidTheme / isValidTimbre / isValidVariant / isValidLocale live in src/domain/settings.ts and reject any non-OPTIONS value while accepting every OPTIONS member (SC2, REQ INFRA-02)."
    - "Coercers coerceTheme / coerceTimbre / coerceVariant / coerceLocale (and the coercePrefs aggregator) live in src/storage/prefs.ts and return their DEFAULT_* constants when the input fails the predicate, without throwing (SC3, REQ INFRA-03)."
    - "Every new predicate and coercer has Vitest coverage and `tsc --noEmit && npm run lint && npm run build && npm test` exits 0 at each of the three commit boundaries (SC4, D-14 per-commit green-gate)."
    - "Writing prefs preserves the settings / mute / stats sub-trees of the envelope (D-01 envelope-merge invariant)."
    - "coercePrefs does not propagate `__proto__` keys to either the returned object or `Object.prototype` (D-12 prototype-pollution mitigation)."
    - "Each new enum is exported as both a union type and a frozen `as const satisfies readonly T[]` OPTIONS array (no `const enum`) so picker UIs in later phases iterate the array directly (D-02 OPTIONS-array + derived-type pattern)."
    - "DEFAULT_THEME is the literal `'system'` so first-load users get OS-aware palette resolution without opening SettingsDialog (D-03 default-theme choice)."
    - "DEFAULT_TIMBRE is the literal `'bowl'` to preserve current-behavior zero-regression for users who never open SettingsDialog (D-04 / TIMBRE-02)."
    - "DEFAULT_VARIANT is the literal `'orb'` to preserve current-behavior zero-regression for users who never open SettingsDialog (D-05 / VARIANT-02)."
    - "DEFAULT_LOCALE is the literal `'en'` (D-06 / I18N-01 default)."
    - "DEFAULT_PREFS is composed in src/storage/prefs.ts from the four per-dim DEFAULT_* imports — mirrors the existing DEFAULT_SETTINGS aggregate (D-07 aggregator location)."
    - "LocaleId uses the BCP-47 hyphen literal `'pt-BR'` (not POSIX `'pt_BR'`) so it round-trips through Intl.* / navigator.language / `<html lang>` without translation; grep for the underscore form in src/domain/settings.ts returns 0 (D-08 BCP-47 identifier)."
    - "Phase 14 ships in exactly two source files — src/domain/settings.ts (EXTENDED, domain tier) and src/storage/prefs.ts (NEW, storage tier) — plus a one-line Envelope addition and a one-line barrel re-export; no per-dimension files (D-09 two-file split)."
    - "src/storage/prefs.ts exports the aggregator + per-field + load/save trio (`coercePrefs`, `coerceTheme`/`coerceTimbre`/`coerceVariant`/`coerceLocale`, `loadPrefs`, `savePrefs`) mirroring the existing `coerceSettings`/`loadSettings`/`saveSettings` shape (D-10 API surface)."
    - "The phase ships as a single plan / single wave / three atomic commits (domain layer → storage layer → docs traceability), each independently passing `tsc && lint && build && test` (D-13 commit packaging)."
    - "No new npm dependencies are introduced — package.json is untouched, honoring the v1.1 'Zero net-new runtime deps' invariant (D-15)."
    - "A single drifted prefs sub-key (e.g. `theme: 'unknown-name'`) falls back to DEFAULT_THEME without discarding timbre / variant / locale — per-field coerce-and-fallback (D-17 / Phase 4 D-15 carry-forward)."
  artifacts:
    - path: "src/domain/settings.ts"
      provides: "ThemeId / TimbreId / VisualVariantId / LocaleId types, THEME_OPTIONS / TIMBRE_OPTIONS / VARIANT_OPTIONS / LOCALE_OPTIONS arrays, isValidTheme / isValidTimbre / isValidVariant / isValidLocale predicates, DEFAULT_THEME / DEFAULT_TIMBRE / DEFAULT_VARIANT / DEFAULT_LOCALE constants"
      exports: ["ThemeId", "TimbreId", "VisualVariantId", "LocaleId", "THEME_OPTIONS", "TIMBRE_OPTIONS", "VARIANT_OPTIONS", "LOCALE_OPTIONS", "isValidTheme", "isValidTimbre", "isValidVariant", "isValidLocale", "DEFAULT_THEME", "DEFAULT_TIMBRE", "DEFAULT_VARIANT", "DEFAULT_LOCALE"]
    - path: "src/domain/settings.test.ts"
      provides: "Three-block predicate tests (accept valid / reject malformed / reject wrong type) for each of the four new predicates"
      contains: "isValidTheme"
    - path: "src/storage/storage.ts"
      provides: "Envelope interface with `prefs?: unknown` added (D-11)"
      contains: "prefs?: unknown"
    - path: "src/storage/prefs.ts"
      provides: "UserPrefs interface, DEFAULT_PREFS aggregate, coerceTheme / coerceTimbre / coerceVariant / coerceLocale per-field coercers, coercePrefs aggregator, loadPrefs / savePrefs envelope round-trip"
      exports: ["UserPrefs", "DEFAULT_PREFS", "coerceTheme", "coerceTimbre", "coerceVariant", "coerceLocale", "coercePrefs", "loadPrefs", "savePrefs"]
    - path: "src/storage/prefs.test.ts"
      provides: "coercePrefs block (5 cases + 4 per-field fallback + prototype-pollution mitigation), per-field coercer block, loadPrefs/savePrefs round-trip block (5 cases including envelope-merge, setItem-throws, corrupt JSON)"
      contains: "coercePrefs"
    - path: "src/storage/index.ts"
      provides: "Public re-export of prefs API: `export * from './prefs'`"
      contains: "export * from './prefs'"
  key_links:
    - from: "src/storage/prefs.ts"
      to: "src/domain/settings.ts"
      via: "named imports — predicates, DEFAULT_* constants, type aliases"
      pattern: "from '../domain/settings'"
    - from: "src/storage/prefs.ts"
      to: "src/storage/storage.ts"
      via: "readEnvelope / writeEnvelope / StorageDeps import for loadPrefs and savePrefs"
      pattern: "from './storage'"
    - from: "src/storage/index.ts"
      to: "src/storage/prefs.ts"
      via: "barrel re-export"
      pattern: "export \\* from './prefs'"
    - from: "src/storage/prefs.test.ts"
      to: "src/storage/prefs.ts"
      via: "Vitest imports under test"
      pattern: "from './prefs'"
---

<objective>
Phase 14 lands the storage + domain scaffolding for the four v1.1 customization dimensions (theme, audio timbre, visual variant, locale) so downstream feature phases 15-19 build against a stable, typed foundation. The full v1.1 enum union is locked here per D-01 — predicates are FINAL; downstream phases (16/17/18/19) only add UI/CSS/audio wiring and do NOT re-edit predicates or OPTIONS arrays.

Purpose: Closes INFRA-01 (Envelope `prefs?: unknown`), INFRA-02 (`isValid*` predicates co-located with `isValidBpm`/`isValidRatio`/`isValidDuration`), INFRA-03 (non-throwing `coerce*` fallbacks in `src/storage/`). Per D-13 this ships as ONE plan / ONE wave / THREE atomic commits (domain → storage → docs), each independently green under `tsc && lint && build && test`.

Output:
- `src/domain/settings.ts` extended with 4 type unions + 4 OPTIONS arrays + 4 predicates + 4 per-dim DEFAULT_* constants
- `src/storage/storage.ts` extended with `Envelope.prefs?: unknown` (one-line TS surface widening — runtime spread at line 96 already carries the sub-tree)
- `src/storage/prefs.ts` NEW with `UserPrefs`, `DEFAULT_PREFS`, four per-field coercers, `coercePrefs` aggregator, `loadPrefs`, `savePrefs`
- `src/storage/index.ts` extended with `export * from './prefs'`
- `src/domain/settings.test.ts` extended with three-block tests for each new predicate
- `src/storage/prefs.test.ts` NEW mirroring `src/storage/settings.test.ts` structure
- REQUIREMENTS.md traceability flipped INFRA-01/02/03 → Done
</objective>

<execution_context>
@$HOME/.claude-personal-personal/get-shit-done/workflows/execute-plan.md
@$HOME/.claude-personal-personal/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/14-prefs-foundation/14-CONTEXT.md
@.planning/phases/14-prefs-foundation/14-PATTERNS.md
@.planning/research/ARCHITECTURE.md

<interfaces>
<!-- Key contracts the executor MUST use directly. Extracted from src/domain/settings.ts, src/storage/storage.ts, src/storage/settings.ts. No codebase exploration needed beyond `read_first` files. -->

From src/domain/settings.ts (existing pattern to mirror — BPM/RATIO/DURATION triplet):
- `export type RatioLabel = '50:50' | '40:60' | '30:70' | '20:80'`
- `export const RATIO_OPTIONS = [...] as const satisfies readonly RatioLabel[]`
- `export function isValidRatio(v: unknown): v is RatioLabel { return typeof v === 'string' && (RATIO_OPTIONS as readonly string[]).includes(v) }`
- `export const DEFAULT_SETTINGS: SessionSettings = { bpm: 5.5, ratio: '40:60', durationMinutes: 10 }`

From src/storage/storage.ts:
- `export interface Envelope { version: number; settings?: unknown; mute?: unknown; stats?: unknown }`
- `export interface StorageDeps { now?: () => number; storage?: Storage }`
- `export function readEnvelope(deps: StorageDeps = {}): Envelope` — spread-then-override at line 96 already preserves `prefs` (proven by storage.test.ts:79-99)
- `export function writeEnvelope(env: Envelope, deps: StorageDeps = {}): void` — refuse-downgrade guard + STATE_VERSION stamp

From src/storage/settings.ts (template for prefs.ts):
- `coerceSettings(raw: unknown): SessionSettings` — uses `(raw !== null && typeof raw === 'object' && !Array.isArray(raw)) ? raw as Record<string, unknown> : {}` guard + per-field ternary fallback
- `loadSettings(deps: StorageDeps = {}): SessionSettings { return coerceSettings(readEnvelope(deps).settings) }`
- `saveSettings(settings: SessionSettings, deps: StorageDeps = {}): void { const env = readEnvelope(deps); writeEnvelope({ ...env, settings }, deps) }`

D-01 enum locks (from 14-CONTEXT.md):
- `ThemeId = 'light' | 'dark' | 'system' | 'moss' | 'slate' | 'dusk'`
- `TimbreId = 'bowl' | 'bell' | 'sine' | 'chime'`
- `VisualVariantId = 'orb' | 'square' | 'ring'`
- `LocaleId = 'en' | 'pt-BR'` (BCP-47 hyphen per D-08)

D-03/04/05/06/07 default values:
- `DEFAULT_THEME = 'system'`, `DEFAULT_TIMBRE = 'bowl'`, `DEFAULT_VARIANT = 'orb'`, `DEFAULT_LOCALE = 'en'`
- `DEFAULT_PREFS: UserPrefs` composed in `src/storage/prefs.ts` from these four imports
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend src/domain/settings.ts with four prefs enum surfaces + predicates + per-dim defaults, and mirror tests in src/domain/settings.test.ts</name>
  <files>src/domain/settings.ts, src/domain/settings.test.ts</files>
  <read_first>
    - src/domain/settings.ts (lines 1-84 — full existing file; you are extending it in place)
    - src/domain/settings.test.ts (lines 1-52 — full existing file; mirror its three-block predicate test pattern)
    - .planning/phases/14-prefs-foundation/14-CONTEXT.md (D-01 enum locks, D-02 OPTIONS-array pattern, D-03/04/05/06 default values, D-08 BCP-47 LocaleId)
    - .planning/phases/14-prefs-foundation/14-PATTERNS.md (sections "src/domain/settings.ts (EXTENDED)" lines 22-69 and "src/domain/settings.test.ts (EXTENDED)" lines 202-236)
  </read_first>
  <behavior>
    Predicate behavior (per D-12 test posture + D-01 enum locks):
    - `isValidTheme('light' | 'dark' | 'system' | 'moss' | 'slate' | 'dusk')` returns true; any other string, number, null, undefined, array returns false.
    - `isValidTimbre('bowl' | 'bell' | 'sine' | 'chime')` returns true; other inputs return false.
    - `isValidVariant('orb' | 'square' | 'ring')` returns true; other inputs return false.
    - `isValidLocale('en' | 'pt-BR')` returns true; `'pt_BR'` (underscore — D-08 inconsistency note), other strings, non-strings return false.
    - All four predicates use the string-only `.includes()` pattern from `isValidRatio` (NOT the numeric `Number.isFinite`+`.includes()` pattern from `isValidBpm` — per PATTERNS.md note at line 40-42).

    Test behavior (mirror src/domain/settings.test.ts:23-52 RATIO three-block pattern, replicate four times):
    - Block 1 — accept valid: one `expect(isValidX(<canonical valid value>)).toBe(true)` per predicate (e.g. `isValidTheme('system')`, `isValidTimbre('bowl')`, `isValidVariant('orb')`, `isValidLocale('en')`).
    - Block 2 — reject malformed strings: e.g. `isValidTheme('Light')` (case-sensitive), `isValidLocale('pt_BR')` (underscore reject — D-08 anchors the hyphen-only contract), empty string `''`.
    - Block 3 — reject wrong type: `null`, `undefined`, number `0`, array `['light']`. (NaN/Infinity rejection is bpm-specific; do not add for string predicates.)
  </behavior>
  <action>
    Edit src/domain/settings.ts to add — in order, AFTER the existing DEFAULT_SETTINGS const (after line 48) and BEFORE the `isValidBpm` predicate block (line 50) — four parallel type/OPTIONS/predicate/DEFAULT_* triplet blocks for `ThemeId`, `TimbreId`, `VisualVariantId`, `LocaleId`. Each block consists of: (1) `export type X = ...union literal...`, (2) `export const X_OPTIONS = [...] as const satisfies readonly X[]`, (3) `export function isValidX(v: unknown): v is X { return typeof v === 'string' && (X_OPTIONS as readonly string[]).includes(v) }`, (4) `export const DEFAULT_X: X = '<value from D-03/04/05/06>'`. Mirror the structural location and style of the existing RATIO_OPTIONS block (lines 26 + 54-56). Do NOT add a `validatePrefs` throwing function (D-12 + PATTERNS.md "What is NOT cloned"). Identifier order in each OPTIONS array MUST match D-01: ThemeId `['light','dark','system','moss','slate','dusk']`, TimbreId `['bowl','bell','sine','chime']`, VisualVariantId `['orb','square','ring']`, LocaleId `['en','pt-BR']`. Default constants: `DEFAULT_THEME = 'system'` (D-03), `DEFAULT_TIMBRE = 'bowl'` (D-04), `DEFAULT_VARIANT = 'orb'` (D-05), `DEFAULT_LOCALE = 'en'` (D-06). Use `as const satisfies readonly X[]` exactly as on line 26/42; do NOT use `const enum` (D-02 rejects const enums).

    Edit src/domain/settings.test.ts to extend the import line (line 3) to also import `isValidTheme, isValidTimbre, isValidVariant, isValidLocale`. After the existing `isValidDuration` describe block (after line 52), append four parallel describe blocks — one per new predicate — each containing the three `it()` blocks described in `<behavior>` above. Label each describe block `'isValidTheme (INFRA-02 D-01)'`, `'isValidTimbre (INFRA-02 D-01)'`, etc. (parallel to the existing `'isValidRatio (HYGIENE-02 D-08)'` label style).

    Commit Boundary 1 (Commit 1 — domain layer per D-13): After this task completes, the executor MUST run `tsc --noEmit && npm run lint && npm run build && npm test` and confirm exit 0, then commit with message: `feat(14): extend domain/settings with prefs enum surfaces` (or equivalent; D-13 calls this Commit 1). Per D-14 roll-back, not patch-forward, on red.
  </action>
  <verify>
    <automated>cd /Users/lucindo/Code/hrv &amp;&amp; npx tsc --noEmit &amp;&amp; npm run lint &amp;&amp; npm test -- src/domain/settings.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'export function isValidTheme' src/domain/settings.ts` returns 1.
    - `grep -c 'export function isValidTimbre' src/domain/settings.ts` returns 1.
    - `grep -c 'export function isValidVariant' src/domain/settings.ts` returns 1.
    - `grep -c 'export function isValidLocale' src/domain/settings.ts` returns 1.
    - `grep -c "export const DEFAULT_THEME" src/domain/settings.ts` returns 1, with value `'system'` on the same line.
    - `grep -c "export const DEFAULT_TIMBRE" src/domain/settings.ts` returns 1, with value `'bowl'` on the same line.
    - `grep -c "export const DEFAULT_VARIANT" src/domain/settings.ts` returns 1, with value `'orb'` on the same line.
    - `grep -c "export const DEFAULT_LOCALE" src/domain/settings.ts` returns 1, with value `'en'` on the same line.
    - `grep -c "'pt-BR'" src/domain/settings.ts` returns 2 or more (LocaleId union + LOCALE_OPTIONS entry; D-08 hyphen form).
    - `grep -c "'pt_BR'" src/domain/settings.ts` returns 0 (no underscore form leaks in — D-08).
    - `grep -c "as const satisfies readonly ThemeId\\[\\]" src/domain/settings.ts` returns 1.
    - `grep -c "as const satisfies readonly TimbreId\\[\\]" src/domain/settings.ts` returns 1.
    - `grep -c "as const satisfies readonly VisualVariantId\\[\\]" src/domain/settings.ts` returns 1.
    - `grep -c "as const satisfies readonly LocaleId\\[\\]" src/domain/settings.ts` returns 1.
    - `grep -c "validatePrefs" src/domain/settings.ts` returns 0 (no throwing validator per D-12 / PATTERNS).
    - `grep -c "const enum" src/domain/settings.ts` returns 0 (D-02).
    - `grep -c "isValidTheme" src/domain/settings.test.ts` returns 4 or more (1 import + 3 it() expectations minimum).
    - `npx tsc --noEmit` exits 0.
    - `npm run lint` exits 0.
    - `npm test -- src/domain/settings.test.ts` exits 0 and reports new passing cases for each of the four predicates.
    - Full green-gate before Commit 1: `npx tsc --noEmit && npm run lint && npm run build && npm test` exits 0.
  </acceptance_criteria>
  <done>
    All four prefs enum triplets (type + OPTIONS + predicate + DEFAULT_*) exist in `src/domain/settings.ts` mirroring the RATIO pattern; `src/domain/settings.test.ts` has three-block coverage per new predicate; per-commit green-gate exits 0; Commit 1 (domain layer) lands.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add Envelope.prefs to src/storage/storage.ts, create src/storage/prefs.ts (UserPrefs + DEFAULT_PREFS + 4 per-field coercers + coercePrefs + loadPrefs + savePrefs), re-export from src/storage/index.ts, and add src/storage/prefs.test.ts</name>
  <files>src/storage/storage.ts, src/storage/prefs.ts, src/storage/prefs.test.ts, src/storage/index.ts</files>
  <read_first>
    - src/storage/storage.ts (lines 43-55 — Envelope interface you are widening; lines 59-103 — readEnvelope you depend on but do NOT modify; lines 105-151 — writeEnvelope you depend on but do NOT modify)
    - src/storage/settings.ts (lines 1-50 — literal template for src/storage/prefs.ts; `coerceSettings` lines 18-27, `loadSettings`/`saveSettings` lines 33-40)
    - src/storage/settings.test.ts (lines 1-151 — literal template for src/storage/prefs.test.ts structure; coerceSettings describe block lines 23-80 including prototype-pollution test lines 71-79; loadSettings/saveSettings round-trip block lines 93-128)
    - src/storage/storage.test.ts (lines 75-99 — existing `prefs: { theme: 'dark' }` forward-compat probe; you are NOT editing this file but the probe is load-bearing for SC1)
    - src/storage/index.ts (lines 1-7 — barrel; you are appending one line)
    - src/domain/settings.ts (the file you just extended in Task 1 — you import all four predicates, four DEFAULT_* constants, and four type aliases from here)
    - .planning/phases/14-prefs-foundation/14-CONTEXT.md (D-07 DEFAULT_PREFS aggregator location, D-09 file split, D-10 API surface, D-11 Envelope.prefs is `unknown` not UserPrefs, D-12 test posture including prototype-pollution + envelope-merge + setItem-throws + corrupt-JSON, D-16/D-17 carry-forward invariants)
    - .planning/phases/14-prefs-foundation/14-PATTERNS.md (sections "src/storage/prefs.ts (NEW)" lines 71-145, "src/storage/storage.ts (EXTENDED)" lines 147-173, "src/storage/index.ts (EXTENDED)" lines 175-200, "src/storage/prefs.test.ts (NEW)" lines 239-333; "Shared Patterns" lines 337-385)
  </read_first>
  <behavior>
    Envelope behavior:
    - The Envelope interface gains an optional `prefs?: unknown` field. No runtime change in `readEnvelope`/`writeEnvelope`; the spread at storage.ts:96 already carries the sub-tree (D-16 / proven by storage.test.ts:79-99).

    coercePrefs behavior (mirrors coerceSettings):
    - `coercePrefs(null) | coercePrefs(undefined) | coercePrefs(42) | coercePrefs('str') | coercePrefs([1,2,3])` → `DEFAULT_PREFS`.
    - `coercePrefs({})` → `DEFAULT_PREFS`.
    - `coercePrefs({ theme: 'dark', timbre: 'bell', variant: 'square', locale: 'pt-BR' })` → preserved verbatim.
    - Per-field fallback (one case per dim): one invalid dim does NOT discard the other three. E.g. `coercePrefs({ theme: 'neon', timbre: 'bowl', variant: 'orb', locale: 'en' })` → `{ theme: DEFAULT_THEME, timbre: 'bowl', variant: 'orb', locale: 'en' }`. (D-17 carry-forward.)
    - Prototype-pollution mitigation: `coercePrefs(JSON.parse('{"theme":"system","timbre":"bowl","variant":"orb","locale":"en","__proto__":{"polluted":true}}'))` → returned object has no `polluted` own-property AND `Object.prototype.polluted` is undefined. (D-12 + threat T-14-01 mitigation.)

    Per-field coercer behavior (one block per dim):
    - `coerceTheme(v)` returns `v` if `isValidTheme(v)`, else `DEFAULT_THEME`. Smoke loop across THEME_OPTIONS asserts each member round-trips. Invalid value (`'neon'`, `null`, `0`) returns `DEFAULT_THEME`.
    - Same shape for `coerceTimbre`, `coerceVariant`, `coerceLocale`.

    loadPrefs/savePrefs round-trip behavior (mirrors loadSettings/saveSettings tests):
    - `loadPrefs()` with nothing stored → `DEFAULT_PREFS`.
    - `savePrefs(next)` then `loadPrefs()` round-trips a valid `UserPrefs` object.
    - Envelope merge: pre-seed envelope with `settings + mute + stats`; call `savePrefs({...})`; assert the read-back raw JSON still contains the original settings/mute/stats sub-trees (parallel to settings.test.ts:104-115).
    - `savePrefs` does NOT throw when `Storage.prototype.setItem` is mocked to throw `'quota'` (D-16 silent-fallback carry-forward).
    - `loadPrefs` returns `DEFAULT_PREFS` when stored JSON is `'{not-json'` (D-17 corrupt-fallback carry-forward).
  </behavior>
  <action>
    Edit src/storage/storage.ts to add a single new field `prefs?: unknown` inside the `Envelope` interface (between line 54 `stats?: unknown` and line 55 `}`). Per D-11 the static type is `unknown`, NOT `UserPrefs` — this avoids creating a `storage → domain → prefs.ts → storage` typed circular import. Add a brief inline comment above or beside the field: "Phase 14 D-11: static type acknowledges the runtime forward-compat already proven by the prefs probe at storage.test.ts:79-99. `unknown` mirrors settings/mute/stats; coercer narrows at the boundary." Do NOT add an index signature; do NOT modify `readEnvelope` or `writeEnvelope` (the runtime is unchanged per D-16).

    Create src/storage/prefs.ts NEW. Header comment mirrors src/storage/settings.ts lines 1-6 with Phase 14 D-10/D-17 attribution and the "non-throwing, per-field fallback" callout. Imports from `'../domain/settings'`: types `ThemeId`, `TimbreId`, `VisualVariantId`, `LocaleId` (use `type` import where appropriate); predicates `isValidTheme`, `isValidTimbre`, `isValidVariant`, `isValidLocale`; constants `DEFAULT_THEME`, `DEFAULT_TIMBRE`, `DEFAULT_VARIANT`, `DEFAULT_LOCALE`. Imports from `'./storage'`: `readEnvelope`, `writeEnvelope`, `type StorageDeps`.

    Exports in src/storage/prefs.ts (D-09 / D-10 API surface, per PATTERNS.md):
    1. `export interface UserPrefs { theme: ThemeId; timbre: TimbreId; variant: VisualVariantId; locale: LocaleId }` — exactly four fields in this order; do NOT mark any field optional.
    2. `export const DEFAULT_PREFS: UserPrefs = { theme: DEFAULT_THEME, timbre: DEFAULT_TIMBRE, variant: DEFAULT_VARIANT, locale: DEFAULT_LOCALE }` (D-07 aggregator composed from the four per-dim DEFAULT_* imports).
    3. Four per-field coercers in declaration order theme → timbre → variant → locale, each using the ternary pattern `return isValid<X>(raw) ? raw : DEFAULT_<X>` (PATTERNS.md "Per-field coercers" lines 122-128).
    4. `export function coercePrefs(raw: unknown): UserPrefs` — uses the literal `coerceSettings` guard from src/storage/settings.ts:18-27: `const r = (raw !== null && typeof raw === 'object' && !Array.isArray(raw)) ? raw as Record<string, unknown> : {}` then per-field ternary `theme: coerceTheme(r.theme)`, etc. If the `as Record<string, unknown>` cast triggers an eslint warning, pair the disable with `// Reason: ...` per D-04 / 14-CONTEXT code-context.
    5. `export function loadPrefs(deps: StorageDeps = {}): UserPrefs { return coercePrefs(readEnvelope(deps).prefs) }` — exact mirror of `loadSettings` (settings.ts:33-35).
    6. `export function savePrefs(prefs: UserPrefs, deps: StorageDeps = {}): void { const env = readEnvelope(deps); writeEnvelope({ ...env, prefs }, deps) }` — exact mirror of `saveSettings` (settings.ts:37-40).

    Edit src/storage/index.ts: append `export * from './prefs'` as a new final line after the existing four `export *` lines. No other changes.

    Create src/storage/prefs.test.ts NEW mirroring src/storage/settings.test.ts:1-151 structure verbatim, substituting prefs symbols. Imports: `afterEach, beforeEach, describe, expect, it, vi` from `'vitest'`; `coercePrefs, coerceTheme, coerceTimbre, coerceVariant, coerceLocale, loadPrefs, savePrefs, DEFAULT_PREFS, type UserPrefs` from `'./prefs'`; `STATE_KEY` from `'./storage'`; `DEFAULT_THEME, DEFAULT_TIMBRE, DEFAULT_VARIANT, DEFAULT_LOCALE, THEME_OPTIONS, TIMBRE_OPTIONS, VARIANT_OPTIONS, LOCALE_OPTIONS` from `'../domain/settings'`. Same `beforeEach`/`afterEach` localStorage clear + `vi.restoreAllMocks()`.

    Test blocks in src/storage/prefs.test.ts (mirror settings.test.ts structure):
    - `describe('coercePrefs (D-10 / D-17)', ...)`: (a) returns DEFAULT_PREFS for null/undefined/non-object/array/empty-object (one `it` per ~5 cases mirroring settings.test.ts:24-33); (b) preserves all valid fields verbatim with `{ theme: 'dark', timbre: 'bell', variant: 'square', locale: 'pt-BR' }`; (c) FOUR per-field fallback `it`s — one per dim, asserting the OTHER three dims survive (mirror settings.test.ts:46-58 three-case pattern); (d) prototype-pollution mitigation `it` — JSON string `'{"theme":"system","timbre":"bowl","variant":"orb","locale":"en","__proto__":{"polluted":true}}'` with assertions `out.polluted === undefined` and `(Object.prototype as Record<string, unknown>).polluted === undefined` (mirror settings.test.ts:71-79 verbatim, substituting the JSON literal).
    - `describe('coerceTheme / coerceTimbre / coerceVariant / coerceLocale (D-10 per-field)', ...)`: four small `it` blocks, one per coercer — each loops over its OPTIONS array asserting `coerce<X>(opt) === opt`, plus one invalid-value assertion `coerce<X>('bogus') === DEFAULT_<X>` and one wrong-type assertion (e.g. `coerceTheme(null) === DEFAULT_THEME`).
    - `describe('loadPrefs / savePrefs round-trip', ...)`: mirror settings.test.ts:93-128 cases verbatim — (i) returns DEFAULT_PREFS when nothing stored; (ii) round-trips a valid UserPrefs; (iii) envelope-merge — seed `{ version: 1, settings: { bpm: 4, ratio: '40:60', durationMinutes: 5 }, mute: true, stats: { totalSessions: 3, totalElapsedSeconds: 120, lastSessionAtMs: 1000, lastSessionDurationSeconds: 60 } }`, call `savePrefs({ theme: 'dark', timbre: 'bell', variant: 'square', locale: 'pt-BR' })`, then assert the read-back raw JSON `toMatchObject({ settings: { bpm: 4 }, mute: true, stats: { totalSessions: 3 } })`; (iv) `savePrefs` does NOT throw when `Storage.prototype.setItem` is `vi.spyOn`-mocked to throw `'quota'` (D-16); (v) `loadPrefs()` returns `DEFAULT_PREFS` when `localStorage.setItem(STATE_KEY, '{not-json')` (D-17). For the envelope-merge `it`, re-use the `// Reason: STATE_KEY is always present after savePrefs; non-null asserted by storage contract.` comment pattern + `// eslint-disable-next-line @typescript-eslint/no-non-null-assertion` before the `!` on `getItem(STATE_KEY)!` (PATTERNS.md "eslint-disable `// Reason:` annotation policy" + settings.test.ts:111-113).

    Do NOT add new cases to `src/storage/storage.test.ts` — the existing forward-compat `prefs: { theme: 'dark' }` probe at storage.test.ts:79-99 already covers STORAGE-01 / SC1 (D-12).

    Stretch goal (zero-touch otherwise): the cast `as unknown as Record<string, unknown>` at storage.test.ts:96 becomes unnecessary once `Envelope.prefs?: unknown` exists. PATTERNS.md flags this as a stretch goal; if the executor cleans it, the assertion shape and test outcome MUST stay identical and `npm test` MUST still exit 0. If unsure, leave the cast in place — it remains type-correct.

    Commit Boundary 2 (Commit 2 — storage layer per D-13): After this task completes, run `tsc --noEmit && npm run lint && npm run build && npm test` and confirm exit 0, then commit: `feat(14): add prefs storage layer (UserPrefs, coercePrefs, loadPrefs/savePrefs)`. Per D-14 roll-back, not patch-forward, on red.
  </action>
  <verify>
    <automated>cd /Users/lucindo/Code/hrv &amp;&amp; npx tsc --noEmit &amp;&amp; npm run lint &amp;&amp; npm run build &amp;&amp; npm test</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "prefs?: unknown" src/storage/storage.ts` returns 1 (Envelope field added — D-11).
    - `grep -c "\\[k: string\\]: unknown" src/storage/storage.ts` returns 0 (no index signature added — D-11 / RESEARCH RQ-4 Option b).
    - `wc -l src/storage/prefs.ts` returns at least 40 (mirrors settings.ts at 50 lines).
    - `grep -c "export interface UserPrefs" src/storage/prefs.ts` returns 1.
    - `grep -c "export const DEFAULT_PREFS" src/storage/prefs.ts` returns 1.
    - `grep -c "export function coerceTheme" src/storage/prefs.ts` returns 1.
    - `grep -c "export function coerceTimbre" src/storage/prefs.ts` returns 1.
    - `grep -c "export function coerceVariant" src/storage/prefs.ts` returns 1.
    - `grep -c "export function coerceLocale" src/storage/prefs.ts` returns 1.
    - `grep -c "export function coercePrefs" src/storage/prefs.ts` returns 1.
    - `grep -c "export function loadPrefs" src/storage/prefs.ts` returns 1.
    - `grep -c "export function savePrefs" src/storage/prefs.ts` returns 1.
    - `grep -c "from '../domain/settings'" src/storage/prefs.ts` returns 1.
    - `grep -c "from './storage'" src/storage/prefs.ts` returns 1.
    - `grep -c "writeEnvelope({ \\.\\.\\.env, prefs }" src/storage/prefs.ts` returns 1 (envelope-merge invariant — D-01).
    - `grep -c "export \\* from './prefs'" src/storage/index.ts` returns 1.
    - `grep -c "__proto__" src/storage/prefs.test.ts` returns at least 1 (prototype-pollution test exists — D-12 / threat T-14-01).
    - `grep -c "Object.prototype" src/storage/prefs.test.ts` returns at least 1 (prototype-pollution second assertion).
    - `grep -c "toMatchObject" src/storage/prefs.test.ts` returns at least 1 (envelope-merge test).
    - `grep -c "vi.spyOn(Storage.prototype" src/storage/prefs.test.ts` returns at least 1 (setItem-throws test — D-16).
    - `grep -c "'{not-json'" src/storage/prefs.test.ts` returns at least 1 (corrupt JSON test — D-17).
    - `grep -c "validatePrefs" src/storage/prefs.ts` returns 0 (no throwing validator — D-12).
    - `npx tsc --noEmit` exits 0.
    - `npm run lint` exits 0.
    - `npm run build` exits 0.
    - `npm test` exits 0 — full suite passes including new `prefs.test.ts` cases AND existing `storage.test.ts:79-99` forward-compat probe (which now type-cleanly references `env.prefs` if the executor cleans the storage.test.ts:96 cast).
    - Full green-gate before Commit 2: `npx tsc --noEmit && npm run lint && npm run build && npm test` exits 0.
  </acceptance_criteria>
  <done>
    `src/storage/storage.ts` Envelope has `prefs?: unknown`; `src/storage/prefs.ts` exports `UserPrefs`, `DEFAULT_PREFS`, four per-field coercers, `coercePrefs`, `loadPrefs`, `savePrefs` mirroring the `settings.ts` API; `src/storage/index.ts` re-exports prefs; `src/storage/prefs.test.ts` mirrors `settings.test.ts` coverage including prototype-pollution / envelope-merge / setItem-throws / corrupt-JSON cases; per-commit green-gate exits 0; Commit 2 (storage layer) lands.
  </done>
</task>

<task type="auto">
  <name>Task 3: Update REQUIREMENTS.md traceability to flip INFRA-01/02/03 from Pending → Done</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (lines 18-23 — INFRA-01/02/03 requirement checkboxes; lines 100-103 — traceability table rows for INFRA-01 / INFRA-02 / INFRA-03)
    - .planning/phases/14-prefs-foundation/14-CONTEXT.md (D-13 calls out Commit 3 as docs-only traceability flip)
  </read_first>
  <action>
    Edit .planning/REQUIREMENTS.md:
    1. Lines 19-21 (INFRA-01 / INFRA-02 / INFRA-03 requirement checkboxes): flip each `[ ]` to `[x]`. Keep the requirement text unchanged.
    2. Traceability table (around lines 100-102): change the `Pending` status to `Done` for the INFRA-01, INFRA-02, and INFRA-03 rows. Format match the existing `Pending` text style (single word, no decoration).
    3. Update the "Last updated" timestamp footer (line 136) to today's date (2026-05-12) and a short note `Last updated: 2026-05-12 — Phase 14 INFRA-01/02/03 traceability flipped to Done`.

    Do NOT modify any other rows or out-of-scope items. Do NOT alter the "Coverage" totals on lines 129-132 — they already correctly state 29 total mapped, and the count is unchanged by a status flip.

    Commit Boundary 3 (Commit 3 — docs per D-13): After this task completes, run the green-gate one more time (no code changed but the per-commit invariant per D-14 still requires verification), then commit: `docs(14): mark INFRA-01/02/03 done in REQUIREMENTS traceability`. This is the final commit of Phase 14.
  </action>
  <verify>
    <automated>cd /Users/lucindo/Code/hrv &amp;&amp; grep -c "INFRA-0[123] | Phase 14 | Done" .planning/REQUIREMENTS.md &amp;&amp; npx tsc --noEmit &amp;&amp; npm run lint &amp;&amp; npm run build &amp;&amp; npm test</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "\\[x\\] \\*\\*INFRA-01\\*\\*" .planning/REQUIREMENTS.md` returns 1.
    - `grep -c "\\[x\\] \\*\\*INFRA-02\\*\\*" .planning/REQUIREMENTS.md` returns 1.
    - `grep -c "\\[x\\] \\*\\*INFRA-03\\*\\*" .planning/REQUIREMENTS.md` returns 1.
    - `grep -c "\\[ \\] \\*\\*INFRA-04\\*\\*" .planning/REQUIREMENTS.md` returns 1 (INFRA-04 stays Pending — Phase 15 scope).
    - `grep -cE "INFRA-0[123] \\| Phase 14 \\| Done" .planning/REQUIREMENTS.md` returns 3.
    - `grep -cE "INFRA-04 \\| Phase 15 \\| Pending" .planning/REQUIREMENTS.md` returns 1 (other INFRA row stays Pending).
    - `grep -c "Last updated: 2026-05-12 — Phase 14" .planning/REQUIREMENTS.md` returns 1.
    - Full green-gate before Commit 3: `npx tsc --noEmit && npm run lint && npm run build && npm test` exits 0 (no source changed but D-14 invariant verified).
  </acceptance_criteria>
  <done>
    REQUIREMENTS.md reflects INFRA-01/02/03 done; INFRA-04 untouched; Last-updated stamp current; per-commit green-gate exits 0; Commit 3 (docs) lands; Phase 14 complete.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| localStorage → app | Untrusted JSON crosses this boundary on every page load. `readEnvelope` parses it via `JSON.parse`; an attacker (or a stale-versioned tab from a future build) can write arbitrary keys/values. Phase 14 widens the static surface (`prefs?: unknown`) but introduces NO new I/O — the boundary is unchanged. |
| Cross-tab storage event | Another tab running the same origin can write the envelope concurrently. Phase 8's STORAGE-02 refuse-downgrade + STORAGE-03 storage listener already mitigate. Phase 14 inherits both invariants without modification. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-01 | Tampering | `coercePrefs(raw)` in `src/storage/prefs.ts` | mitigate | Prototype-pollution: a stored `__proto__` key in the JSON envelope MUST NOT leak to the returned object or `Object.prototype`. The guard pattern is the literal clone of `coerceSettings` (storage/settings.ts:18-21) — `(raw !== null && typeof raw === 'object' && !Array.isArray(raw)) ? raw as Record<string,unknown> : {}` then per-field reads only on the four known keys (`r.theme`, `r.timbre`, `r.variant`, `r.locale`). We never spread `raw` into an object used as a prototype. D-12 mandates the test case mirroring settings.test.ts:71-79: `JSON.parse('{"theme":"system",..."__proto__":{"polluted":true}}')` → `out.polluted === undefined` AND `Object.prototype.polluted === undefined`. Implemented in Task 2 with explicit acceptance criterion. |
| T-14-02 | Tampering | `isValidTheme / isValidTimbre / isValidVariant / isValidLocale` in `src/domain/settings.ts` | mitigate | Untrusted-string predicate: attacker stores `prefs.theme = 'javascript:alert(1)'` or `prefs.theme = '<script>'`. Predicates use `(X_OPTIONS as readonly string[]).includes(v)` — strict equality match against a closed allowlist. The downstream consumer (Phase 16 will set `document.documentElement.dataset.theme`) receives ONLY values that passed the allowlist. Phase 14 ships the predicate; Phase 16 is responsible for not constructing dangerous sinks (e.g., NOT injecting the theme string into innerHTML). Test coverage in Task 1: each predicate rejects malformed strings + wrong types. |
| T-14-03 | Denial of Service | `JSON.parse` in `readEnvelope` (storage.ts:64) | accept | Storage quota / corrupt JSON / Safari ITP throws are already silently absorbed by Phase 4 D-16 (outer try/catch returns `EMPTY_ENVELOPE`) and Phase 4 D-17 (`coerce*` returns DEFAULT_*). Phase 14 inherits both invariants verbatim. We add no new I/O surface; the existing `readEnvelope`/`writeEnvelope` are NOT modified. Test coverage in Task 2: `savePrefs` does NOT throw under `setItem` quota mock (D-16 mirror); `loadPrefs` returns `DEFAULT_PREFS` under `'{not-json'` (D-17 mirror). |
| T-14-04 | Information Disclosure | `prefs` envelope sub-tree contains only customization preferences (theme/timbre/variant/locale) | accept | No PII, no credentials. The sub-tree is enum-only by D-01. localStorage is same-origin scoped; cross-origin scripts cannot read it without an existing XSS bug elsewhere in the app (orthogonal to Phase 14 scope). |
| T-14-05 | Tampering | Cross-tab storage race — another tab writes the envelope with newer prefs while this tab is mid-write | accept (carry-forward) | Phase 8 STORAGE-02 refuse-downgrade guard at storage.ts:129-142 already covers this: if disk version > STATE_VERSION, `writeEnvelope` returns silently. Phase 14 adds NO new write paths; `savePrefs` uses the same `writeEnvelope({ ...env, prefs }, deps)` envelope-merge as `saveSettings`. The cross-tab `storage` listener (Phase 8 STORAGE-03) keys on `STATE_KEY` — no filter update needed for the new sub-tree. Documented in 14-CONTEXT.md D-16. |
| T-14-06 | Spoofing | Forging a future-schema envelope (e.g., `version: 2` with malicious sub-key shapes) to break this tab | accept | Phase 8 STORAGE-01 spread-then-override read at storage.ts:91-96 returns `{ ...p, version: onDiskVersion }`. Unknown sub-keys round-trip; known sub-keys are narrowed by `coerce*`. A spoofed `version: 99999` with `prefs: <malicious shape>` would (a) trigger STORAGE-02 refuse-downgrade on subsequent writes (preserves attacker's data — same trust posture as Phase 8) and (b) trigger `coercePrefs` to per-field-fallback any non-OPTIONS values. Combined effect: same security posture as `coerceSettings` against a `version: 2` envelope with a bogus `settings` sub-tree. No new mitigation required at Phase 14. |

**ASVS L1 coverage:** All `mitigate`-disposition threats have explicit test cases in Tasks 1 and 2 (allowlist coverage, prototype-pollution coverage). No `high`-severity threats unmitigated. `accept`-disposition threats are documented carry-forwards from Phases 4 (D-16/D-17 silent-fallback) and 8 (STORAGE-01/02/03), or scope-bounded (no PII in the `prefs` sub-tree). Block-on-high gate: PASS.
</threat_model>

<verification>
**Per-commit green-gate (D-14, three boundaries):**
- After Task 1 (Commit 1 — domain layer): `npx tsc --noEmit && npm run lint && npm run build && npm test` exits 0; new predicate test cases pass.
- After Task 2 (Commit 2 — storage layer): same command exits 0; `src/storage/prefs.test.ts` passes; existing `src/storage/storage.test.ts:79-99` forward-compat probe continues to pass.
- After Task 3 (Commit 3 — docs): same command exits 0 (no source change; invariant verified).

**ROADMAP §14 Success Criteria coverage:**
- SC1 (Envelope `prefs` round-trip + D-01 preserved): covered by existing storage.test.ts:79-99 probe (no edit needed per D-12) + Task 2 `savePrefs` envelope-merge test.
- SC2 (predicates in src/domain/settings.ts alongside isValidBpm/isValidRatio/isValidDuration): covered by Task 1 grep acceptance criteria + new describe blocks in src/domain/settings.test.ts.
- SC3 (coercers fall back to DEFAULT_*, non-throwing, in src/storage/): covered by Task 2 per-field coercer tests + coercePrefs per-field fallback tests.
- SC4 (Vitest coverage + green-gate): covered by every task's `<verify><automated>` block + per-commit green-gate.

**Source-coverage audit:**
- GOAL (ROADMAP §14 goal — envelope accepts prefs, domain enforces valid values): covered by Task 1 (domain) + Task 2 (storage).
- REQ INFRA-01 (Envelope `prefs?` field + D-01 spread-then-override): covered by Task 2 storage.ts edit + test reuse of storage.test.ts:79-99.
- REQ INFRA-02 (four `isValid*` predicates in src/domain/settings.ts): covered by Task 1.
- REQ INFRA-03 (four `coerce*` functions in src/storage/, falling back to DEFAULT_*): covered by Task 2.
- RESEARCH (.planning/research/ARCHITECTURE.md §Prefs Data Model, §Envelope Schema Strategy, §Phase B): Phase 14 expands the placeholder `ThemeId = 'default' | 'dark'` to the full v1.1 lock per D-01 (architecture surrounding it carries forward verbatim); all Phase B build-order items 1-3 + 5 are covered by Tasks 1-2 (item 4 "Wire `loadPrefs()` at App mount" is explicitly Phase 15 scope per 14-CONTEXT.md `<deferred>`).
- CONTEXT D-01 through D-17: every locked decision is implemented (D-01 enum locks → Task 1; D-02 OPTIONS pattern → Task 1; D-03/04/05/06/07 defaults → Tasks 1+2; D-08 BCP-47 locale → Task 1; D-09 file split → Tasks 1+2; D-10 API shape → Task 2; D-11 Envelope.prefs = unknown → Task 2; D-12 test posture → Tasks 1+2; D-13 single-plan/three-commits → task ordering; D-14 green-gate → all three task verify blocks; D-15 zero new deps → grep no `package.json` edits; D-16/17 carry-forward → Task 2 setItem-throws / corrupt-JSON tests).

**No source items unplanned. No items deferred without 14-CONTEXT.md `<deferred>` justification.**
</verification>

<success_criteria>
Phase 14 is COMPLETE when:
- [ ] `src/domain/settings.ts` contains four prefs enum triplets (type + OPTIONS + isValid* + DEFAULT_*) mirroring the RATIO pattern.
- [ ] `src/storage/storage.ts` Envelope interface contains `prefs?: unknown`.
- [ ] `src/storage/prefs.ts` exists and exports `UserPrefs`, `DEFAULT_PREFS`, `coerceTheme`, `coerceTimbre`, `coerceVariant`, `coerceLocale`, `coercePrefs`, `loadPrefs`, `savePrefs`.
- [ ] `src/storage/index.ts` re-exports `'./prefs'`.
- [ ] `src/domain/settings.test.ts` has three-block coverage for each new predicate; `src/storage/prefs.test.ts` mirrors `src/storage/settings.test.ts` structure including prototype-pollution / envelope-merge / setItem-throws / corrupt-JSON cases.
- [ ] `.planning/REQUIREMENTS.md` shows INFRA-01/02/03 as `Done` in the traceability table and `[x]` on the requirement checkboxes.
- [ ] Three atomic git commits exist with the per-commit green-gate exit-0 verified at each boundary (D-13).
- [ ] No new npm dependencies introduced (D-15 — verify `package.json` unchanged).
- [ ] No SettingsDialog / no React state / no App.tsx edits (Phase 14 ships a pure library surface; Phase 15 is the consumer).
</success_criteria>

<output>
After completion, create `.planning/phases/14-prefs-foundation/14-01-SUMMARY.md` capturing:
- Three atomic commits' SHAs + messages
- Files modified (count: 4 source + 2 test = 6 files)
- Number of new test cases added (predicate count: 4 × 3 blocks ≈ 12 domain it()s; prefs.test.ts: ~15 it()s)
- Per-commit green-gate verification (3 × exit-0 records)
- REQUIREMENTS.md traceability deltas (INFRA-01/02/03 → Done; INFRA-04 untouched)
- Carry-forward note: Phase 15 consumes this API via `import { loadPrefs, savePrefs, UserPrefs, DEFAULT_PREFS } from '../storage'`.
- Phase 19 reconciliation flag: `LocaleId = 'pt-BR'` (hyphen) locked here; REQ I18N-05 example uses `'pt_BR'` (underscore) — Phase 19 plan MUST reconcile (per 14-CONTEXT.md D-08 / `<deferred>`).
</output>
