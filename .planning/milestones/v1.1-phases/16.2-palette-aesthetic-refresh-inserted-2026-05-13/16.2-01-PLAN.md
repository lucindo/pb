---
phase: 16.2
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/styles/theme.css
autonomous: true
requirements: []
honored_constraints: [THEME-05]
tags: [theme, palette-retune, aesthetic, css-only, contrast-guard]

must_haves:
  truths:
    - "Light Out gradient is desaturated from #f97316 / #fed7aa to a softer warm-family pair that reads as ambient light against the cool near-white Light chrome (F1)"
    - "Moss Out gradient swings out of the blue family (#3b82f6 / #bfdbfe) and into a green-adjacent family (emerald / teal / lime) that visually integrates with the moss greens (F4)"
    - "Slate Out gradient softens the vivid indigo (#6366f1 / #a5b4fc) toward a desaturated cool-grey or muted-indigo pair that integrates with the slate-grey chrome (F5)"
    - "Dusk In gradient drops the near-white (#ede9fe / #faf5ff) to a deeper lavender / violet-tinted pair that respects the deep purple Dusk chrome instead of punching through it (F6)"
    - "Dusk Out gradient softens (#d97706 / #fcd34d) toward a less saturated amber/gold pair (F7)"
    - "Dark palette orb-in/out tokens remain unchanged (approved in 16.1-06 UAT — no finding raised)"
    - "Moss In tokens remain unchanged (approved in 16.1-06 UAT — no finding raised)"
    - "All 5 palettes pass THEME-05 ≥ 1.5 contrast guard after retune (theme.contrast.test.ts exits 0 after EACH per-palette edit — per-palette commit boundary enables bisecting any regression to the exact palette that introduced it)"
    - "theme.no-hardcoded-classes.test.ts guard remains GREEN (no .tsx files touched)"
    - "Per-palette orb-in-text / orb-out-text legibility tokens are re-checked and shifted if the new from/to luminance crosses the legibility threshold against the new midpoint"
  artifacts:
    - path: "src/styles/theme.css"
      provides: "Retuned --color-orb-{in,out}-{from,to,text} tokens for Light + Moss + Slate + Dusk palettes; Dark unchanged"
      contains: "[data-theme='dusk']:root"
      # Sanity: still 30 orb token declarations total (5 palettes × 6 tokens).
  key_links:
    - from: "src/styles/theme.css :root @theme block (Light)"
      to: "THEME-05 contrast guard via theme.contrast.test.ts iteration 'theme=light'"
      via: "midpoint(orb-in-from, orb-in-to) vs midpoint(orb-out-from, orb-out-to) ratio >= 1.5"
      pattern: "--color-orb-out-from"
    - from: "src/styles/theme.css [data-theme='moss']:root block"
      to: "THEME-05 contrast guard via theme.contrast.test.ts iteration 'theme=moss'"
      via: "midpoint(orb-in-from, orb-in-to) vs midpoint(orb-out-from, orb-out-to) ratio >= 1.5"
      pattern: "data-theme='moss'"
    - from: "src/styles/theme.css [data-theme='slate']:root block"
      to: "THEME-05 contrast guard via theme.contrast.test.ts iteration 'theme=slate'"
      via: "midpoint(orb-in-from, orb-in-to) vs midpoint(orb-out-from, orb-out-to) ratio >= 1.5"
      pattern: "data-theme='slate'"
    - from: "src/styles/theme.css [data-theme='dusk']:root block"
      to: "THEME-05 contrast guard via theme.contrast.test.ts iteration 'theme=dusk'"
      via: "midpoint(orb-in-from, orb-in-to) vs midpoint(orb-out-from, orb-out-to) ratio >= 1.5"
      pattern: "data-theme='dusk'"
---

<objective>
Wave 1 / Plan 01: Re-tune the orb In/Out gradient hex values for the 4 palettes that surfaced findings during Phase 16.1 plan 06 per-palette UAT (Light F1, Moss F4, Slate F5, Dusk F6 + F7). Pure CSS retune. Closes the 5 aesthetic findings explicitly deferred from Phase 16.1.

Purpose: Each palette's breathing-shape orb gradient must feel visually integrated with its chrome (bg, surface, accent). Phase 16 shipped the orb gradients with WCAG-floor-driven hex picks; perceptual UAT in 16.1-06 surfaced that several pairs are mathematically distinct (clear ≥ 1.5 ratio) but aesthetically dissonant with the palette's overall personality (e.g. Moss greens with a vivid blue Out gradient, Slate chrome with a vivid indigo Out gradient, Dusk's deep purple bg with near-white In gradient that punches through). This plan closes that loop — pure hex retune in theme.css, no `.tsx` touch.

Cadence rule (per plan-checker B1): each palette is its own task with its own contrast-guard verify and its own commit, so any regression bisects to the exact palette that introduced it (Tasks 1a / 1b / 1c each handle one palette; Task 1d handles Dusk's coupled In+Out together since F6 and F7 share the same contrast iteration and cannot be tuned independently). Tasks 1a–1d all sit in Wave 1; depends_on remains [].

Output: 1 modified file (`src/styles/theme.css`), 4 palette blocks edited (Light @theme, Moss / Slate / Dusk [data-theme]:root). Approximately 8–12 hex-value substitutions plus possible orb-{in,out}-text legibility follow-ups if from/to luminance shifts. Dark palette block is NOT touched (approved in 16.1-06). 4 commits total (one per task).
</objective>

<execution_context>
@$HOME/.claude-personal-personal/get-shit-done/workflows/execute-plan.md
@$HOME/.claude-personal-personal/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/16.1-ui-token-migration/16.1-CONTEXT.md
@.planning/phases/16.1-ui-token-migration/16.1-SUMMARY.md
@.planning/phases/16.1-ui-token-migration/16.1-06-PLAN.md
@.planning/phases/16.1-ui-token-migration/16.1-06-SUMMARY.md
@src/styles/theme.css
@src/styles/theme.contrast.test.ts
@src/styles/theme.no-hardcoded-classes.test.ts

<interfaces>
The token surface this plan edits — exact line ranges from `src/styles/theme.css`:

Light palette (@theme block, lines 1–36): orb tokens at lines 16–21
  --color-orb-in-from: #e0f2fe    (sky-100 — KEEP, no F-finding on Light In)
  --color-orb-in-to:   #f0f9ff    (sky-50  — KEEP, no F-finding on Light In)
  --color-orb-out-from: #f97316   (orange-500 — F1: too saturated)
  --color-orb-out-to:   #fed7aa   (orange-200 — F1: too saturated)
  --color-orb-in-text:  #0c4a6e   (sky-900 — re-verify legibility against unchanged In midpoint)
  --color-orb-out-text: #7c2d12   (orange-900 — re-verify legibility against new Out midpoint)

Dark palette ([data-theme='dark']:root, lines 42–60): orb tokens at lines 51–56
  *** DO NOT TOUCH *** — approved in 16.1-06 UAT, no finding raised. Re-running the contrast
  guard after edits to other palettes will re-verify Dark stayed within the floor unchanged.

Moss palette ([data-theme='moss']:root, lines 68–86): orb tokens at lines 77–82
  --color-orb-in-from: #99f6e4    (teal-200 — KEEP, no F-finding on Moss In)
  --color-orb-in-to:   #ecfdf5    (emerald-50 — KEEP, no F-finding on Moss In)
  --color-orb-out-from: #3b82f6   (blue-500 — F4: vivid blue clashes with moss greens)
  --color-orb-out-to:   #bfdbfe   (blue-200 — F4: vivid blue clashes with moss greens)
  --color-orb-in-text:  #134e4a   (teal-900 — re-verify legibility against unchanged In midpoint)
  --color-orb-out-text: #1e3a8a   (blue-900 — re-verify legibility against new Out midpoint; almost certainly must shift family with the new from/to)

Slate palette ([data-theme='slate']:root, lines 92–110): orb tokens at lines 101–106
  --color-orb-in-from: #eff6ff    (blue-50 — KEEP, no F-finding on Slate In)
  --color-orb-in-to:   #dbeafe    (blue-100 — KEEP, no F-finding on Slate In)
  --color-orb-out-from: #6366f1   (indigo-500 — F5: vivid indigo too strong vs slate-grey chrome)
  --color-orb-out-to:   #a5b4fc   (indigo-300 — F5: vivid indigo too strong vs slate-grey chrome)
  --color-orb-in-text:  #1e3a8a   (blue-900 — re-verify against unchanged In midpoint)
  --color-orb-out-text: #1e1b4b   (indigo-950 — re-verify against new Out midpoint)

Dusk palette ([data-theme='dusk']:root, lines 116–134): orb tokens at lines 125–130
  --color-orb-in-from: #ede9fe    (violet-100 — F6: near-white too bright vs deep purple chrome)
  --color-orb-in-to:   #faf5ff    (purple-50 — F6: near-white too bright vs deep purple chrome)
  --color-orb-out-from: #d97706   (amber-600 — F7: room to soften)
  --color-orb-out-to:   #fcd34d   (amber-300 — F7: room to soften)
  --color-orb-in-text:  #4c1d95   (violet-900 — re-verify against new (deeper) In midpoint)
  --color-orb-out-text: #78350f   (amber-900 — re-verify against new (softer) Out midpoint)

THEME-05 contrast guard (src/styles/theme.contrast.test.ts):
- The test runs `describe.each(CONCRETE_THEMES)` over 5 themes (light/dark/moss/slate/dusk).
- For each: parses `--color-orb-in-from` + `--color-orb-in-to` -> midpoint; same for out;
  asserts `contrastRatio(inMid, outMid) >= 1.5`.
- WCAG canonical luminance formula (sRGB linear, 0.2126 R + 0.7152 G + 0.0722 B).
- Also asserts `contrastRatio(accent-strong, on-accent) >= 1.5` — unchanged here, since this
  plan does NOT touch accent/accent-strong/on-accent tokens.

theme.no-hardcoded-classes.test.ts: scans src/**/*.tsx for 10 banned patterns. This plan
edits NO .tsx files, so the guard stays GREEN by construction.

Suggested candidate hex families (executor's discretion within these families — pick exact
hexes that visually integrate AND pass the contrast guard; iterate as needed):

F1 Light Out — desaturate the orange:
  Candidate A: amber-400 / amber-200 family (`#fbbf24` / `#fde68a`) — softer warm-gold
  Candidate B: peach / salmon family (`#fb923c` / `#fed7aa` -> lighter `#fdba74` / `#fef3c7`)
  Constraint: must keep Out-midpoint luminance ~0.40 or below to preserve ratio vs sky In ~0.90.

F4 Moss Out — pivot from blue to green-family:
  Candidate A: emerald-700 / emerald-300 (`#047857` / `#6ee7b7`) — deeper green-on-mid-green
  Candidate B: teal-600 / teal-200 (`#0d9488` / `#99f6e4`) — but overlaps Moss In (#99f6e4); shift In midpoint or pick differently
  Candidate C: forest / pine pair (`#166534` / `#86efac`) — deeper saturation, more contrast
  Constraint: Moss In midpoint is teal-200/emerald-50 (~luminance 0.90); Out midpoint must
  drop to ~0.40 or below for ratio >= 1.5. Pure-green dark-to-mid pair preferred over blue.
  CAUTION: avoid landing too close to Moss accent (#0f766e, teal-700) or accent-strong
  (#115e59, teal-900) — those are the chrome anchors; orb-out wants to harmonize with them,
  not collide as a duplicate. Slight hue separation (emerald vs teal, or moss vs jungle) is
  the goal.

F5 Slate Out — desaturate indigo toward slate-grey or muted-violet:
  Candidate A: slate-600 / slate-400 (`#475569` / `#94a3b8`) — pure neutral; aligns with chrome
  Candidate B: muted-violet pair (`#7c3aed` / `#c4b5fd`) — keeps Slate's accent indigo family
  but lower saturation
  Candidate C: stone-700 / stone-300 (`#44403c` / `#d6d3d1`) — warmer neutral
  Constraint: Slate In midpoint is blue-50/blue-100 (luminance ~0.94); Out midpoint must drop
  to ~0.45 or below. Avoid landing too close to accent (`#4f46e5`) or accent-strong (`#3730a3`).

F6 Dusk In — drop near-white to deeper lavender / violet-tinted:
  Candidate A: violet-300 / violet-200 (`#c4b5fd` / `#ddd6fe`) — light lavender, still light
  Candidate B: purple-300 / fuchsia-200 (`#d8b4fe` / `#f5d0fe`) — warmer purple
  Candidate C: lavender-tinted off-white (`#e9d5ff` / `#f3e8ff`) — slight tint, still close to original
  Constraint: Dusk Out midpoint is currently amber (luminance ~0.55 with current values; will
  drop further on F7 softening). In midpoint must stay HIGHER than Out midpoint for ratio
  >= 1.5. Don't go too dark — keep In as the "light/airy" pole. Iterate against contrast
  guard.

F7 Dusk Out — soften amber:
  Candidate A: amber-500 / amber-200 (`#f59e0b` / `#fde68a`) — slightly less saturated
  Candidate B: gold-400 / yellow-200 (`#facc15` / `#fef08a`) — yellower, gentler
  Candidate C: peach pair (`#fb923c` / `#fed7aa`) — overlap with Light F1 candidate;
  intentional family-share is fine if both palettes want a peach Out (they're independent
  selectors).
  Constraint: keep Out midpoint distinct enough from new (deeper) Dusk In midpoint for
  ratio >= 1.5. F6 + F7 are coupled — they must be tuned jointly and the contrast guard
  re-run after each tweak.

ORB-TEXT LEGIBILITY (orb-in-text / orb-out-text):
- These tokens are consumed via inline `style.color = var(--color-orb-{in,out}-text)` on
  the active phase label (BreathingShape:135) and the lead-in countdown digit
  (BreathingShape:216) — set in plans 16.1-05 and 16.1-06 respectively.
- They render OVER the orb's In or Out gradient at the visible orb scale.
- If the from/to luminance of a new gradient shifts significantly, the corresponding
  text token MAY need to shift too (e.g. if F6 drops Dusk In from near-white to mid-violet,
  the existing #4c1d95 (violet-900) text may go from "dark-on-light works" to "dark-on-mid
  marginal").
- Executor must verify legibility for each retuned palette and shift the text token if the
  new from/to midpoint is too close in luminance to the existing text color (rule of thumb:
  text vs gradient midpoint should clear WCAG AA 4.5:1 for body text, AAA 7:1 ideal).
- This is NOT automated by theme.contrast.test.ts (which only asserts in-mid vs out-mid).
  Verify visually during retune (`npm run dev` + start a session + watch lead-in digit
  AND active phase label in each retuned palette) OR add a 7th contrast iteration to
  theme.contrast.test.ts asserting orb-in-text vs in-midpoint and orb-out-text vs out-midpoint
  (planner discretion — adding the iteration is the cleaner long-term safety net but is
  beyond the strict F-list; if added, must NOT regress for Dark or Moss In which aren't
  being retuned).

Phase 16 reference math (from theme.css comments):
- Light: ratio ~1.78 (sky-blue vs orange, hue-separated)
- Dark: ratio ~4.85 (widest margin — pale-blue vs deep-teal)
- Moss: ratio ~1.94 (teal-pastel vs vivid-blue — F4 will likely shift this; floor is 1.5)
- Slate: ratio ~2.56 (blue-50 vs indigo-300 — F5 will likely shift this; floor is 1.5)
- Dusk: ratio ~1.87 (violet-light vs amber — F6 + F7 will shift this; floor is 1.5)

After retune, each ratio MUST remain >= 1.5. The test prints no ratio number on pass —
to inspect actual ratio for a candidate, run the test then temporarily console.log the
ratio in test (or just iterate hex picks until the assertion passes).

Per-task ORDER & COMMIT CADENCE (B1 fix — per-palette bisect boundary):
  Task 1a → Light F1 → commit `feat(16.2-01): retune Light orb-out gradient (closes F1; THEME-05 holds)`
  Task 1b → Moss  F4 → commit `feat(16.2-01): retune Moss orb-out gradient (closes F4; THEME-05 holds)`
  Task 1c → Slate F5 → commit `feat(16.2-01): retune Slate orb-out gradient (closes F5; THEME-05 holds)`
  Task 1d → Dusk  F6+F7 (coupled) → commit `feat(16.2-01): retune Dusk orb in/out gradients (closes F6/F7; coupled tune; THEME-05 holds)`
Each task runs the contrast guard independently. If Task 1b drops Moss below 1.5 AND Slate would also fail later, Task 1b fails fast on Moss alone — Slate doesn't get touched until Moss is green. Bisection becomes trivial.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1a: Retune Light Out gradient (closes F1)</name>
  <files>src/styles/theme.css</files>
  <read_first>
    - src/styles/theme.css (full file — 252 lines; edits concentrated in the @theme block lines 1–36)
    - src/styles/theme.contrast.test.ts (full file — to understand the WCAG luminance math the guard applies; specifically the midpoint() + contrastRatio() helpers and the 1.5 floor)
    - .planning/phases/16.1-ui-token-migration/16.1-06-SUMMARY.md §"Phase 16 palette retune (deferred to Phase 16.2)" (the 5-finding table with F1's exact current hex values)
    - .planning/ROADMAP.md Phase 16.2 section (the Findings to address block — authoritative finding descriptions)
  </read_first>
  <action>
    Edit ONLY the Light `@theme` block in `src/styles/theme.css` (lines 16–21). Goal: replace the Out gradient's `from`/`to` hex pair with a desaturated/family-shifted pair that visually integrates with the cool near-white Light chrome AND preserves THEME-05 ≥ 1.5 contrast against the unchanged sky-100/50 In midpoint.

    F1 — Light @theme block (lines 16–21): replace `--color-orb-out-from: #f97316` and `--color-orb-out-to: #fed7aa` with a softer warm pair (suggested family: amber-400/amber-200 `#fbbf24` / `#fde68a`, or amber-500/amber-200 `#f59e0b` / `#fde68a`, or a peach pair `#fdba74` / `#fef3c7`). After change, also evaluate whether `--color-orb-out-text: #7c2d12` (orange-900) still reads cleanly against the new Out midpoint — if the new midpoint shifts the warm family from orange toward amber/gold, the text may want to shift to amber-950 (`#451a03`) or stay deep brown. Apply judgment; keep current value if still legible.

    Workflow:
    1. Edit the 2 (or 2–3, if text token shifts) hex values in the Light @theme block.
    2. Run `npx vitest run src/styles/theme.contrast.test.ts` — must exit 0 (Light passes the ≥ 1.5 floor; the 4 unchanged palettes also pass, by construction since they weren't touched).
    3. (Recommended) Run `npm run dev`, swap to Light, start a session, watch the orb In→Out transition. Adjust hex if visual integration is not yet achieved.
    4. If text token shifted, re-verify legibility visually.
    5. Run the per-commit green-gate chain:
       - `npx tsc --noEmit` exits 0
       - `npm run lint` exits 0
       - `npm run build` exits 0
       - `npm test -- --run` exits 0 (theme.contrast.test.ts AND theme.no-hardcoded-classes.test.ts both green; full suite green)

    Sanity grep — after edits, the count of orb token declarations must still be 30 (5 palettes × 6 tokens):
       `grep -cE "^  --color-orb-" src/styles/theme.css` returns 30

    Scope guard — the FULL diff must be exactly `src/styles/theme.css` (no other files touched anywhere in repo):
       `[ "$(git diff --name-only)" = "src/styles/theme.css" ]`

    Commit as `feat(16.2-01): retune Light orb-out gradient (closes F1; THEME-05 holds)`.

    Per CONTEXT-style rule: do not touch any palette other than Light in this task; do not touch any `--color-breathing-*` chrome token; do not touch any `.tsx` file.
  </action>
  <verify>
    <automated>npx vitest run src/styles/theme.contrast.test.ts src/styles/theme.no-hardcoded-classes.test.ts && npx tsc --noEmit && npm run lint && npm run build && [ "$(grep -cE '^  --color-orb-' src/styles/theme.css)" -eq 30 ] && [ "$(git diff --name-only)" = "src/styles/theme.css" ]</automated>
  </verify>
  <acceptance_criteria>
    - The Light `@theme` block of `src/styles/theme.css` defines `--color-orb-out-from` to a non-`#f97316` value AND `--color-orb-out-to` to a non-`#fed7aa` value (F1 closed).
    - The Dark `[data-theme='dark']:root` block lines 51–56 are byte-identical to the pre-task state.
    - All other palette blocks (Moss / Slate / Dusk) are byte-identical to the pre-task state (this is a Light-only task).
    - `grep -cE "^  --color-orb-" src/styles/theme.css` returns 30 (sanity — no tokens dropped or added).
    - `[ "$(git diff --name-only)" = "src/styles/theme.css" ]` (FULL repo diff is exactly that one file — no `.tsx`, no test, no config files modified).
    - `npx vitest run src/styles/theme.contrast.test.ts` exits 0 (all 5 palettes — including Dark which wasn't touched — pass the ≥ 1.5 luminance floor).
    - `npx vitest run src/styles/theme.no-hardcoded-classes.test.ts` exits 0.
    - `npx tsc --noEmit && npm run lint && npm run build && npm test -- --run` exits 0.
  </acceptance_criteria>
  <done>
    Light Out gradient is retuned to a family that visually integrates with the cool near-white Light chrome. THEME-05 ≥ 1.5 luminance contrast holds across all 5 palettes (the test asserts mathematically; visual integration is confirmed in plan 02 UAT). Commit boundary: F1 isolated — any later regression bisects directly to this commit.
  </done>
</task>

<task type="auto">
  <name>Task 1b: Retune Moss Out gradient (closes F4)</name>
  <files>src/styles/theme.css</files>
  <read_first>
    - src/styles/theme.css (post-Task-1a state — edits concentrated in [data-theme='moss']:root lines 68–86)
    - src/styles/theme.contrast.test.ts (the iteration 'theme=moss' is the gate this task must pass)
    - .planning/phases/16.1-ui-token-migration/16.1-06-SUMMARY.md (F4 finding description: vivid blue clashing with moss greens)
    - .planning/ROADMAP.md Phase 16.2 (Findings to address — F4 hue-family pivot directive)
  </read_first>
  <action>
    Edit ONLY the `[data-theme='moss']:root` block in `src/styles/theme.css` (orb tokens at lines 77–82). Goal: pivot the Out gradient out of the blue family and into a green-adjacent family that visually integrates with the moss greens AND preserves THEME-05 ≥ 1.5 contrast against the unchanged teal-200/emerald-50 In midpoint.

    F4 — [data-theme='moss']:root block (lines 77–82): replace `--color-orb-out-from: #3b82f6` and `--color-orb-out-to: #bfdbfe` with a green-family pair (suggested: emerald-700/emerald-300 `#047857` / `#6ee7b7`, or a deeper jungle/forest pair `#166534` / `#86efac`). CAUTION: avoid landing too close to Moss accent (#0f766e teal-700) or accent-strong (#115e59 teal-900) — the orb-out is meant to harmonize with the chrome's teal anchors via slight hue separation (emerald vs teal). After change, the `--color-orb-out-text: #1e3a8a` (blue-900) is almost certainly wrong for a green Out gradient; shift to emerald-950 (`#022c22`) or teal-900 (`#134e4a`) — pick whichever reads as deeper than the new Out midpoint.

    Do NOT touch Moss In tokens (lines 77–78 `--color-orb-in-from: #99f6e4` / `--color-orb-in-to: #ecfdf5`) — Moss In was approved in 16.1-06 UAT and is byte-identical from the pre-phase state.

    Workflow:
    1. Edit the Out from/to (lines 79–80) and likely the orb-out-text (line 82).
    2. Run `npx vitest run src/styles/theme.contrast.test.ts` — must exit 0 (Moss passes the ≥ 1.5 floor; other palettes including Light's Task 1a retune don't regress).
    3. (Recommended) Run `npm run dev`, swap to Moss, start a session, watch the orb In→Out transition. Adjust hex if visual integration is not yet achieved (specifically: does the Out feel like a sibling green to the chrome, or like a foreign green poking through?).
    4. If text token shifted, re-verify legibility visually (lead-in digit + active phase label both render the orb-text colors).
    5. Run the per-commit green-gate chain (same as Task 1a).

    Sanity grep — orb token count remains 30:
       `grep -cE "^  --color-orb-" src/styles/theme.css` returns 30

    Scope guard — the FULL diff must be exactly `src/styles/theme.css`:
       `[ "$(git diff --name-only)" = "src/styles/theme.css" ]`

    Commit as `feat(16.2-01): retune Moss orb-out gradient (closes F4; THEME-05 holds)`.

    Per scope rule: do not touch any palette other than Moss in this task; do not touch Moss In tokens; do not touch any `.tsx` file.
  </action>
  <verify>
    <automated>npx vitest run src/styles/theme.contrast.test.ts src/styles/theme.no-hardcoded-classes.test.ts && npx tsc --noEmit && npm run lint && npm run build && [ "$(grep -cE '^  --color-orb-' src/styles/theme.css)" -eq 30 ] && [ "$(git diff --name-only)" = "src/styles/theme.css" ]</automated>
  </verify>
  <acceptance_criteria>
    - `[data-theme='moss']:root` block of `src/styles/theme.css` defines `--color-orb-out-from` to a non-`#3b82f6` value AND `--color-orb-out-to` to a non-`#bfdbfe` value (F4 closed).
    - Moss In tokens at lines 77–78 (`--color-orb-in-from: #99f6e4` / `--color-orb-in-to: #ecfdf5`) are byte-identical to the pre-phase state (F4 is Out-only).
    - The Dark `[data-theme='dark']:root` block lines 51–56 remain byte-identical to the pre-phase state.
    - Slate + Dusk palette blocks are byte-identical to their post-Task-1a state (this is a Moss-only task).
    - Light @theme block contains the Task 1a edits AND is not regressed by this task.
    - `grep -cE "^  --color-orb-" src/styles/theme.css` returns 30 (sanity).
    - `[ "$(git diff --name-only)" = "src/styles/theme.css" ]` (FULL repo diff is exactly that one file).
    - `npx vitest run src/styles/theme.contrast.test.ts` exits 0 (Moss passes; no other palette regresses).
    - `npx vitest run src/styles/theme.no-hardcoded-classes.test.ts` exits 0.
    - `npx tsc --noEmit && npm run lint && npm run build && npm test -- --run` exits 0.
  </acceptance_criteria>
  <done>
    Moss Out gradient is retuned out of the blue family into a green-adjacent family that harmonizes with the moss chrome anchors via slight hue separation. THEME-05 ≥ 1.5 contrast holds. Commit boundary: F4 isolated — any later regression bisects directly to this commit.
  </done>
</task>

<task type="auto">
  <name>Task 1c: Retune Slate Out gradient (closes F5)</name>
  <files>src/styles/theme.css</files>
  <read_first>
    - src/styles/theme.css (post-Task-1b state — edits concentrated in [data-theme='slate']:root lines 92–110)
    - src/styles/theme.contrast.test.ts (the iteration 'theme=slate' is the gate this task must pass)
    - .planning/phases/16.1-ui-token-migration/16.1-06-SUMMARY.md (F5 finding description: vivid indigo too strong vs slate-grey chrome)
    - .planning/ROADMAP.md Phase 16.2 (Findings to address — F5 desaturation directive)
  </read_first>
  <action>
    Edit ONLY the `[data-theme='slate']:root` block in `src/styles/theme.css` (orb tokens at lines 101–106). Goal: desaturate the Out gradient from vivid indigo toward a cool-neutral (slate-grey) or muted-violet pair that reads as ambient chrome, not as a competing accent.

    F5 — [data-theme='slate']:root block (lines 101–106): replace `--color-orb-out-from: #6366f1` and `--color-orb-out-to: #a5b4fc` with a desaturated cool-neutral pair (suggested: slate-600/slate-400 `#475569` / `#94a3b8`, or a muted-violet pair `#7c3aed` / `#c4b5fd` if some indigo identity is desired). CAUTION: avoid landing too close to Slate accent (#4f46e5 indigo-600) or accent-strong (#3730a3 indigo-700) — the orb-out wants to read as ambient chrome, not as a competing accent. After change, evaluate `--color-orb-out-text: #1e1b4b` (indigo-950) — if Out family shifted to neutral slate, the text wants to shift to slate-900 (`#0f172a`); if Out family kept muted-violet, indigo-950 stays.

    Do NOT touch Slate In tokens (lines 101–102 `--color-orb-in-from: #eff6ff` / `--color-orb-in-to: #dbeafe`) — Slate In was approved in 16.1-06 UAT.

    Workflow:
    1. Edit the Out from/to (lines 103–104) and possibly the orb-out-text (line 106).
    2. Run `npx vitest run src/styles/theme.contrast.test.ts` — must exit 0 (Slate passes the ≥ 1.5 floor; Light + Moss edits don't regress).
    3. (Recommended) Run `npm run dev`, swap to Slate, start a session, watch the orb In→Out transition. Adjust hex if visual integration is not yet achieved (specifically: does the Out fade into the chrome bg #f1f5f9 too much, or does it still feel like a clear "exhale" pole?).
    4. If text token shifted, re-verify legibility visually.
    5. Run the per-commit green-gate chain (same as Tasks 1a/1b).

    Sanity grep — orb token count remains 30.

    Scope guard — the FULL diff must be exactly `src/styles/theme.css`:
       `[ "$(git diff --name-only)" = "src/styles/theme.css" ]`

    Commit as `feat(16.2-01): retune Slate orb-out gradient (closes F5; THEME-05 holds)`.

    Per scope rule: do not touch any palette other than Slate in this task; do not touch Slate In tokens; do not touch any `.tsx` file.
  </action>
  <verify>
    <automated>npx vitest run src/styles/theme.contrast.test.ts src/styles/theme.no-hardcoded-classes.test.ts && npx tsc --noEmit && npm run lint && npm run build && [ "$(grep -cE '^  --color-orb-' src/styles/theme.css)" -eq 30 ] && [ "$(git diff --name-only)" = "src/styles/theme.css" ]</automated>
  </verify>
  <acceptance_criteria>
    - `[data-theme='slate']:root` block of `src/styles/theme.css` defines `--color-orb-out-from` to a non-`#6366f1` value AND `--color-orb-out-to` to a non-`#a5b4fc` value (F5 closed).
    - Slate In tokens at lines 101–102 are byte-identical to the pre-phase state (F5 is Out-only).
    - The Dark `[data-theme='dark']:root` block lines 51–56 remain byte-identical to the pre-phase state.
    - Dusk palette block is byte-identical to its post-Task-1b state (this is a Slate-only task).
    - Light + Moss blocks contain the Task 1a + 1b edits respectively AND are not regressed by this task.
    - `grep -cE "^  --color-orb-" src/styles/theme.css` returns 30 (sanity).
    - `[ "$(git diff --name-only)" = "src/styles/theme.css" ]` (FULL repo diff is exactly that one file).
    - `npx vitest run src/styles/theme.contrast.test.ts` exits 0 (Slate passes; no other palette regresses).
    - `npx vitest run src/styles/theme.no-hardcoded-classes.test.ts` exits 0.
    - `npx tsc --noEmit && npm run lint && npm run build && npm test -- --run` exits 0.
  </acceptance_criteria>
  <done>
    Slate Out gradient is retuned to a desaturated cool-neutral or muted-violet pair that integrates with the slate-grey chrome without competing with the indigo accent. THEME-05 ≥ 1.5 contrast holds. Commit boundary: F5 isolated — any later regression bisects directly to this commit.
  </done>
</task>

<task type="auto">
  <name>Task 1d: Retune Dusk In + Dusk Out gradients (closes F6, F7; coupled tune)</name>
  <files>src/styles/theme.css</files>
  <read_first>
    - src/styles/theme.css (post-Task-1c state — edits concentrated in [data-theme='dusk']:root lines 116–134)
    - src/styles/theme.contrast.test.ts (the iteration 'theme=dusk' is the gate this task must pass)
    - .planning/phases/16.1-ui-token-migration/16.1-06-SUMMARY.md (F6 + F7 finding descriptions)
    - .planning/ROADMAP.md Phase 16.2 (Findings to address — F6 deep purple chrome context + F7 amber softening note)
  </read_first>
  <action>
    Edit ONLY the `[data-theme='dusk']:root` block in `src/styles/theme.css` (lines 116–134). F6 and F7 are coupled — both In and Out are being retuned together because Dusk's contrast guard checks the In-midpoint vs Out-midpoint pair, and shifting one without the other risks dropping below the ≥ 1.5 floor. Per B1 cadence, this coupled-pair tune is its own task (Task 1d) with its own commit; the Light/Moss/Slate Out-only retunes already committed separately in Tasks 1a/1b/1c.

    F6 — Dusk In: replace `--color-orb-in-from: #ede9fe` (violet-100) and `--color-orb-in-to: #faf5ff` (purple-50) with a deeper lavender / violet-tinted pair that respects the deep purple Dusk chrome (`--color-breathing-bg: #2e1065` violet-950; `--color-breathing-surface: #3b0f8c` violet-900) instead of punching through it as a near-white. Suggested candidates: violet-300/violet-200 (`#c4b5fd` / `#ddd6fe`), or violet-200/violet-100 (`#ddd6fe` / `#ede9fe` — slight deepening), or a lavender-tinted pair (`#d8b4fe` / `#e9d5ff`). Goal: still legibly LIGHTER than the Out gradient (so the In phase reads as "expansion / light"), but not stark white-on-purple.

    F7 — Dusk Out: replace `--color-orb-out-from: #d97706` (amber-600) and `--color-orb-out-to: #fcd34d` (amber-300) with a softer amber/gold pair. Suggested candidates: amber-500/amber-200 (`#f59e0b` / `#fde68a`), or gold-400/yellow-200 (`#facc15` / `#fef08a`). Goal: less saturated, gentler "contraction / warmth" pole. May overlap with Light F1 amber candidate — intentional family-share between palettes is fine; selectors are independent.

    Joint coupling check — after editing both In and Out:
    1. The new Dusk In midpoint MUST be LIGHTER (higher luminance) than the new Dusk Out midpoint — In is the "light" phase pole.
    2. Their contrast ratio MUST clear ≥ 1.5 (theme.contrast.test.ts `theme=dusk` iteration).
    3. Iterate: if contrast fails, either lighten In further (toward original ede9fe) or deepen Out further (toward amber-700/amber-500), depending on which aesthetic shift is hurting the ratio more.

    Text legibility re-check:
    - `--color-orb-in-text: #4c1d95` (violet-900) — render-time consumed on lead-in digit (BreathingShape:216) and active In phase label (BreathingShape:135). Against the original near-white In gradient (#ede9fe / #faf5ff, luminance ~0.93) violet-900 (luminance ~0.07) is high-contrast. If F6 drops the In midpoint to violet-300 (luminance ~0.65), violet-900 still clears 4.5:1 — keep. If F6 drops further to violet-400/300 (luminance ~0.55), reconsider — possibly shift to violet-950 (`#2e1065`) or near-black for safety. Verify visually in `npm run dev` Dusk palette session.
    - `--color-orb-out-text: #78350f` (amber-900) — same path, against the Out gradient. If F7 softens to amber-200/amber-100, dark brown stays safe. If F7 shifts hue toward yellow, consider yellow-900 (`#713f12`) — but amber-900 is hue-compatible with yellow-amber pairs. Verify visually.

    Workflow:
    1. Edit Dusk In `from` + `to` (lines 125–126).
    2. Edit Dusk Out `from` + `to` (lines 127–128).
    3. (If needed) Adjust `--color-orb-in-text` (line 129) and/or `--color-orb-out-text` (line 130) based on the new midpoints.
    4. Run `npx vitest run src/styles/theme.contrast.test.ts` — must exit 0 (Dusk passes; Light + Moss + Slate retunes from Tasks 1a–1c don't regress).
    5. Run `npm run dev`, swap to Dusk, start a session, observe the In→Out transition and the lead-in digit. Adjust if visual integration is not yet achieved.
    6. Final chain: `npx tsc --noEmit && npm run lint && npm run build && npm test -- --run` exits 0.

    Sanity grep — orb token count remains 30.

    Scope guard — the FULL diff must be exactly `src/styles/theme.css`:
       `[ "$(git diff --name-only)" = "src/styles/theme.css" ]`

    Commit as `feat(16.2-01): retune Dusk orb in/out gradients (closes F6/F7; coupled tune; THEME-05 holds)`.

    Per scope rule: do not touch any palette other than Dusk in this task; do not touch any `.tsx` file.
  </action>
  <verify>
    <automated>npx vitest run src/styles/theme.contrast.test.ts src/styles/theme.no-hardcoded-classes.test.ts && npx tsc --noEmit && npm run lint && npm run build && [ "$(grep -cE '^  --color-orb-' src/styles/theme.css)" -eq 30 ] && [ "$(git diff --name-only)" = "src/styles/theme.css" ]</automated>
  </verify>
  <acceptance_criteria>
    - `[data-theme='dusk']:root` block of `src/styles/theme.css` defines `--color-orb-in-from` to a non-`#ede9fe` value AND `--color-orb-in-to` to a non-`#faf5ff` value (F6 closed).
    - `[data-theme='dusk']:root` block of `src/styles/theme.css` defines `--color-orb-out-from` to a non-`#d97706` value AND `--color-orb-out-to` to a non-`#fcd34d` value (F7 closed).
    - The Dark `[data-theme='dark']:root` block lines 51–56 remain byte-identical to the pre-phase state.
    - The Light @theme block, Moss [data-theme='moss']:root block, and Slate [data-theme='slate']:root block contain the Tasks 1a/1b/1c edits AND are not regressed by Task 1d (only Dusk edited here).
    - `grep -cE "^  --color-orb-" src/styles/theme.css` returns 30 (sanity).
    - `[ "$(git diff --name-only)" = "src/styles/theme.css" ]` (FULL repo diff for THIS task is exactly that one file).
    - `npx vitest run src/styles/theme.contrast.test.ts` exits 0 (Dusk passes the ≥ 1.5 floor AFTER coupled In+Out shift; no other palette regresses).
    - `npx vitest run src/styles/theme.no-hardcoded-classes.test.ts` exits 0.
    - `npx tsc --noEmit && npm run lint && npm run build && npm test -- --run` exits 0.
  </acceptance_criteria>
  <done>
    Dusk In and Dusk Out gradients are jointly retuned. The deep-purple Dusk chrome is no longer punctured by a near-white In gradient (F6 closed); the amber Out gradient is softened (F7 closed). Coupled In+Out edit preserves THEME-05 ≥ 1.5 floor for Dusk. Other 4 palettes unchanged by this task. Plan 01 complete (4 commits: F1, F4, F5, F6+F7) heading into plan 02 UAT.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| no new boundaries | Pure CSS hex-value substitution in a stylesheet that ships at build time. No new runtime computation, no new event handlers, no new auth/data surface, no new dependencies. The retune affects only what the browser renders for the orb's two `linear-gradient` background layers (and the inline `style.color` consumers of orb-text). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16.2-01-01 | Spoofing (a11y) | Orb In/Out gradient contrast for reduced-motion users | mitigate | THEME-05 ≥ 1.5 luminance contrast guard (`theme.contrast.test.ts`) MUST exit 0 after EVERY per-palette hex edit. Per B1 cadence, the test runs in each of Tasks 1a / 1b / 1c / 1d's `<verify>` block — independently. Any retune that drops a palette below the ≥ 1.5 floor MUST be reverted or re-tuned BEFORE the task's commit. Per-task commit boundary means the offending palette's edit is the only thing to roll back, never multiple palette retunes at once. |
| T-16.2-01-02 | Spoofing (a11y) | Orb-in-text / orb-out-text legibility on retuned gradients | mitigate | Task actions instruct executor to evaluate orb-text token vs new from/to midpoint and shift the text hex if luminance gap drops below WCAG AA (4.5:1 for body-text). Visual verification during `npm run dev` smoke. Long-term: plan 02 UAT confirms perceptual legibility per palette (lead-in digit + active phase label). |
| T-16.2-01-03 | Tampering | Accidental .tsx modification breaking THEME-UI-01 grep guard | mitigate | `[ "$(git diff --name-only)" = "src/styles/theme.css" ]` (FULL repo diff, not the `src/`-only subset) MUST hold after each of Tasks 1a/1b/1c/1d's commit. This catches any stray edit anywhere in the repo (tests, root config, planning docs included). `theme.no-hardcoded-classes.test.ts` runs in the full suite as a second gate — any .tsx change introducing a banned class fails the build before merge. |
| T-16.2-01-04 | Tampering | Accidental Dark palette modification regressing approved chrome | accept | Dark palette has 6 untouched lines (51–56). No automated guard enforces "do not edit Dark" beyond the contrast test (which would still pass an aesthetic-but-mathematically-valid Dark retune). Mitigation is procedural — explicit `<read_first>` and `<action>` callouts to NOT touch Dark, plus acceptance criteria in every Task 1x asserting Dark lines are byte-identical to the pre-phase state. If a stray Dark edit slips through, plan 02 UAT catches it (Dark is included in the 5/5 palette walk-through). |
| T-16.2-01-05 | Tampering | Multi-palette retune in single commit hides which palette regressed | mitigate | B1-driven cadence: each task is one palette (Light / Moss / Slate alone; Dusk In+Out coupled). One commit per task. `git bisect` against any future contrast or visual regression bisects to the single offending palette's commit. |
</threat_model>

<verification>
After Task 1a completes:

```bash
# Contrast guard: Light retuned; Dark/Moss/Slate/Dusk unchanged from pre-phase state.
npx vitest run src/styles/theme.contrast.test.ts

# Hardcoded-classes guard: stays GREEN (no .tsx touched).
npx vitest run src/styles/theme.no-hardcoded-classes.test.ts

# Token-count sanity: still 30 orb declarations (5 palettes × 6 tokens).
grep -cE "^  --color-orb-" src/styles/theme.css

# Scope guard: ENTIRE repo diff is exactly src/styles/theme.css.
[ "$(git diff --name-only)" = "src/styles/theme.css" ]

# Per-commit green-gate:
npx tsc --noEmit && npm run lint && npm run build && npm test -- --run
```

After Tasks 1b, 1c, 1d complete in sequence: same chain at each task boundary, with the cumulative diff growing within the same single file. After all 4 tasks of plan 01 complete:

```bash
# Cumulative diff across the 4 plan-01 commits — still only src/styles/theme.css.
git diff --name-only HEAD~4 HEAD
# Expected: src/styles/theme.css

# Phase-level sentinel before plan 02 UAT runs — all 5 palettes pass THEME-05:
npx vitest run src/styles/theme.contrast.test.ts -- --reporter=verbose
# Should show 5 'reduced-motion crossfade' + 5 'accent-strong vs on-accent' = 10 it() pass.

# Production .tsx surface untouched across the whole phase:
git log --name-only --pretty=format: c31b125..HEAD | grep '^src/.*\.tsx$' | wc -l
# Expected: 0 (no .tsx modifications since Phase 16.1 close).
```
</verification>

<success_criteria>
1. F1 closed: Light @theme orb-out hex values shifted from `#f97316` / `#fed7aa` to a softer warm-family pair (Task 1a).
2. F4 closed: Moss [data-theme='moss']:root orb-out hex values shifted from `#3b82f6` / `#bfdbfe` to a green-family pair (out of the blue family) (Task 1b).
3. F5 closed: Slate [data-theme='slate']:root orb-out hex values shifted from `#6366f1` / `#a5b4fc` to a desaturated cool-neutral or muted-violet pair (Task 1c).
4. F6 closed: Dusk [data-theme='dusk']:root orb-in hex values shifted from `#ede9fe` / `#faf5ff` to a deeper lavender/violet pair (Task 1d).
5. F7 closed: Dusk [data-theme='dusk']:root orb-out hex values shifted from `#d97706` / `#fcd34d` to a softer amber/gold pair (Task 1d, coupled with F6).
6. Dark `[data-theme='dark']:root` orb tokens lines 51–56 are byte-identical to the pre-plan state (no retune of approved palette).
7. Moss In tokens at lines 77–78 are byte-identical (Moss In was not a finding).
8. Slate In tokens at lines 101–102 are byte-identical (Slate In was not a finding).
9. Orb token count remains 30 (`grep -cE "^  --color-orb-" src/styles/theme.css` returns 30).
10. After every plan-01 task (Tasks 1a/1b/1c/1d), `git diff --name-only` for that task's commit equals exactly `src/styles/theme.css` (FULL repo diff, no `src/`-only subset). Cumulative diff `git diff --name-only HEAD~4 HEAD` likewise equals exactly `src/styles/theme.css` (success criterion #3 from ROADMAP — no `.tsx` files modified).
11. `theme.contrast.test.ts` exits 0 after EACH per-palette commit (success criterion #2 from ROADMAP — THEME-05 ≥ 1.5 floor holds across all 5 palettes at every commit boundary; per-palette cadence enables bisection on regression).
12. `theme.no-hardcoded-classes.test.ts` exits 0 after each commit (success criterion #4 from ROADMAP — guard remains green).
13. `npx tsc --noEmit && npm run lint && npm run build && npm test -- --run` exits 0 after each commit (success criterion #4 from ROADMAP — per-commit green-gate).
14. Plan 01 ships 4 commits (one per finding-cluster: F1 alone, F4 alone, F5 alone, F6+F7 coupled). Plan 02 UAT (Wave 2) gates final approval — automated gates here are necessary but not sufficient; visual integration is verified in plan 02.
</success_criteria>

<output>
After all 4 tasks complete, create `.planning/phases/16.2-palette-aesthetic-refresh-inserted-2026-05-13/16.2-01-SUMMARY.md` documenting:
- Per-finding (F1, F4, F5, F6, F7): before-hex → after-hex pair AND a one-line rationale for the family choice (why amber over peach for F1, why emerald over teal for F4, etc.).
- Per-palette ratio (Light, Dark, Moss, Slate, Dusk) post-retune — the actual WCAG luminance contrast ratio measured (extract from a temporary `console.log` in the test, or compute by hand from the new hex values via the helpers in `theme.contrast.test.ts`).
- Whether `--color-orb-in-text` and/or `--color-orb-out-text` shifted on any palette (and why).
- Sentinels verified: orb token count = 30; `git diff --name-only HEAD~4 HEAD` = `src/styles/theme.css`; full chain green at every commit boundary.
- Commits: 4 (one per task — F1 / F4 / F5 / F6+F7) and the rationale that the per-palette cadence is a deliberate bisect boundary (B1).
- Heading into plan 02: all 5 palettes mathematically pass THEME-05; manual per-palette UAT is the perception-side gate that decides phase close.
</output>
