---
phase: 15-settingsdialog-shell
plan: 03
type: execute
wave: 2
depends_on:
  - 15-01
  - 15-02
files_modified:
  - src/components/SettingsDialog.tsx
  - src/components/SettingsDialog.test.tsx
autonomous: true
requirements:
  - INFRA-04

must_haves:
  truths:
    - "A `SettingsDialog` component exists at `src/components/SettingsDialog.tsx` exporting `SettingsDialog` and `SettingsDialogProps` (D-11)."
    - "Props interface = exactly `{ open: boolean; onClose(this: void): void; inSessionView: boolean }` (D-05 / D-11 single onClose + inSessionView for picker disable threading)."
    - "Dialog uses the project's established native `<dialog>` imperative pattern: `useEffect([open])` calls `dialog.showModal()` / `dialog.close()` via `dialogRef.current` (D-15 zero new deps; ResetStatsDialog template)."
    - "Dialog has NO explicit focus call on open (D-13 — SettingsDialog has no destructive default; rely on native focus-return; differs from ResetStatsDialog which focuses Keep)."
    - "Esc handler: `useEffect([onClose])` adds a `cancel` event listener that calls `event.preventDefault()` then `onClose()` — Pitfall-5 double-fire mitigation (Landmine 1)."
    - "Backdrop click handler: `if (event.target === dialogRef.current) onClose()` — mirror of ResetStatsDialog.tsx lines 43-47 (Landmine 2)."
    - "Dialog element carries `aria-labelledby=\"settings-dialog-title\"` and the inner `<h2 id=\"settings-dialog-title\">Settings</h2>` carries the matching id (D-18 locked title `Settings`; Landmine 9)."
    - "Content layout: single column in order Theme → Variant → Timbre → Language followed by a single Close button (D-10 / D-11 / D-18)."
    - "Each picker receives `disabled={inSessionView}` from the dialog (D-02 contract — only the disabled prop is threaded; pickers self-read storage) — Landmine 7."
    - "Close button text is the literal `Close` (D-18); Close button styling matches LearnDialog's Close button class string (RESEARCH.md §2)."
    - "Dialog max-width class is `max-w-md` (Claude's discretion per RESEARCH §user_constraints — Theme/Variant/Timbre/Language stack vertically and four single-line stubs fit comfortably; ResetStatsDialog uses `max-w-sm` but SettingsDialog hosts four sections)."
    - "The component never listens for the `close` event (Landmine 2 — only `cancel` is observed; close-prop transition is handled by the imperative useEffect)."
    - "Vitest coverage: closed-state, open-state title + Close button + dialog.open=true, Close click → onClose, Esc cancel → onClose, backdrop click → onClose, inner-panel click → onClose NOT called, open→close transition (dialog.open becomes false), inSessionView=true → all four picker labels render with their D-18 stub text."
    - "`npx vitest run src/components/SettingsDialog.test.tsx` exits 0; full green-gate exits 0; test count ≥ 438 baseline + Plan 01 + Plan 02 + new SettingsDialog cases."
    - "No new npm dependencies (D-15); no edits to `src/domain/settings.ts` or `src/storage/prefs.ts` (D-16)."
  artifacts:
    - path: "src/components/SettingsDialog.tsx"
      provides: "Native <dialog> shell hosting the four picker stubs in Theme→Variant→Timbre→Language order; Close button; cancel/backdrop close paths; aria-labelledby='settings-dialog-title'; imperative showModal/close pattern"
      exports: ["SettingsDialog", "SettingsDialogProps"]
      contains: "aria-labelledby=\"settings-dialog-title\""
    - path: "src/components/SettingsDialog.test.tsx"
      provides: "Vitest coverage mirroring ResetStatsDialog.test.tsx structure: closed-state, open-state, transition, plus a SettingsDialog-specific inSessionView→all-pickers-render test"
      contains: "describe('SettingsDialog"
  key_links:
    - from: "src/components/SettingsDialog.tsx"
      to: "src/components/ThemePicker.tsx"
      via: "named import — child component rendered in dialog body"
      pattern: "from './ThemePicker'"
    - from: "src/components/SettingsDialog.tsx"
      to: "src/components/VariantPicker.tsx"
      via: "named import — child component"
      pattern: "from './VariantPicker'"
    - from: "src/components/SettingsDialog.tsx"
      to: "src/components/TimbrePicker.tsx"
      via: "named import — child component"
      pattern: "from './TimbrePicker'"
    - from: "src/components/SettingsDialog.tsx"
      to: "src/components/LanguagePicker.tsx"
      via: "named import — child component"
      pattern: "from './LanguagePicker'"
    - from: "src/components/SettingsDialog.tsx"
      to: "HTMLDialogElement"
      via: "useRef<HTMLDialogElement>(null) + imperative showModal/close"
      pattern: "useRef<HTMLDialogElement>"
---

<objective>
Plan 03 ships `SettingsDialog` — the native `<dialog>` shell that hosts the four customization picker stubs (from Plan 02) in the Theme → Variant → Timbre → Language order (D-10) with a single Close button (D-11). Mirrors `ResetStatsDialog.tsx` structurally — imperative showModal/close via ref, `cancel` event with `preventDefault()` (Pitfall-5 mitigation), backdrop-click via `event.target === dialogRef.current` (Landmine 2), `aria-labelledby` link to the `<h2>` title (Landmine 9). Differs from ResetStatsDialog in three ways: (1) single `onClose` prop instead of `onConfirm`/`onCancel` (no destructive default), (2) NO explicit focus call on open (D-13; rely on native focus return; differs from ResetStatsDialog which focuses Keep), (3) wider `max-w-md` to host four picker sections (Claude's discretion per RESEARCH §user_constraints).

Purpose: Closes the SC2 dialog-open/close-trap portion AND the SC4 native-`<dialog>`/locked-copy portion of INFRA-04 — "Activating the gear control opens the SettingsDialog; focus is trapped inside the dialog (native `<dialog>` behavior); closing with Escape or a close button returns focus to the trigger." (ROADMAP.md SC2) and "The dialog pattern follows the native `<dialog>` + locked-copy conventions of ResetStatsDialog / LearnDialog; no new library dependency is introduced." (SC4). Also closes SC3's `disabled={inSessionView}` threading to all four pickers (Landmine 7).

Output:
- `src/components/SettingsDialog.tsx` NEW (mirrors ResetStatsDialog.tsx structure + LearnDialog Close button styling)
- `src/components/SettingsDialog.test.tsx` NEW (mirrors ResetStatsDialog.test.tsx + adds inSessionView picker-thread test)

Dependencies: Plan 01 (SettingsAnchor) is NOT required at this layer — SettingsDialog is independent. Plan 02 (four pickers) IS required since SettingsDialog imports all four.

Wave: 2 (after Plan 01 + Plan 02 in Wave 1). No edits to App.tsx (Plan 04 owns).
</objective>

<execution_context>
@$HOME/.claude-personal-personal/get-shit-done/workflows/execute-plan.md
@$HOME/.claude-personal-personal/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/15-settingsdialog-shell/15-CONTEXT.md
@.planning/phases/15-settingsdialog-shell/15-RESEARCH.md
@.planning/phases/15-settingsdialog-shell/15-PATTERNS.md
@.planning/phases/15-settingsdialog-shell/15-VALIDATION.md
@src/components/ResetStatsDialog.tsx
@src/components/ResetStatsDialog.test.tsx
@src/components/LearnDialog.tsx
@vitest.setup.ts
</context>

<interfaces>
<!-- Key types/exports consumed by SettingsDialog. Plan 02 ships these. -->

From src/components/ThemePicker.tsx (Plan 02):
- `export interface ThemePickerProps { disabled: boolean }`
- `export function ThemePicker({ disabled }: ThemePickerProps): JSX.Element`

From src/components/VariantPicker.tsx (Plan 02): same shape, `VariantPicker` / `VariantPickerProps`.
From src/components/TimbrePicker.tsx (Plan 02): same shape, `TimbrePicker` / `TimbrePickerProps`.
From src/components/LanguagePicker.tsx (Plan 02): same shape, `LanguagePicker` / `LanguagePickerProps`.

JSDOM polyfill provided by vitest.setup.ts lines 87-114:
- `HTMLDialogElement.prototype.showModal = function () { this.open = true }`
- `HTMLDialogElement.prototype.close = function (returnValue?) { this.open = false; ...dispatchEvent(new Event('close')) }`
- Tests fire the cancel event manually: `fireEvent(dialog, new Event('cancel', { cancelable: true }))` (Landmine 1).

Project-wide locked strings consumed here (D-18):
- Dialog title text: `Settings`
- Close button label: `Close`
- Picker section labels (D-10 order Theme→Variant→Timbre→Language) — already rendered by Plan 02 pickers.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create SettingsDialog component (native <dialog> shell, ResetStatsDialog template)</name>
  <files>src/components/SettingsDialog.tsx</files>
  <read_first>
    - src/components/ResetStatsDialog.tsx (lines 1-84 — literal template; lines 15-24 open/close effect, 28-39 cancel handler, 43-47 backdrop handler, 50-54 dialog element with aria-labelledby + className)
    - src/components/LearnDialog.tsx (Close button pattern — read RESEARCH §2 "Close button" subsection for the exact class string; LearnDialog.tsx lines 176-183)
    - src/components/ThemePicker.tsx, VariantPicker.tsx, TimbrePicker.tsx, LanguagePicker.tsx (Plan 02 outputs — confirm export names and prop shapes)
    - vitest.setup.ts (lines 87-114 — HTMLDialogElement polyfill behaviour notes)
    - .planning/phases/15-settingsdialog-shell/15-PATTERNS.md (§"src/components/SettingsDialog.tsx" — line-numbered diff against ResetStatsDialog template, full inner-content JSX block)
    - .planning/phases/15-settingsdialog-shell/15-RESEARCH.md (§2 "SettingsDialog Shell — Full Template" + §6 Landmines 1, 2, 3, 7, 9)
    - .planning/phases/15-settingsdialog-shell/15-CONTEXT.md (D-05, D-10, D-11, D-12, D-13, D-15, D-18)
  </read_first>
  <behavior>
    Production component:
    - Default export NOT used; named `export function SettingsDialog`.
    - Props: `export interface SettingsDialogProps { open: boolean; onClose(this: void): void; inSessionView: boolean }`.
    - Refs: `const dialogRef = useRef<HTMLDialogElement>(null)`. NO `closeButtonRef` (D-13 — no explicit focus).
    - Effect 1: `useEffect(() => { ...showModal()/close() guarded by !dialog.open / dialog.open ... }, [open])` — mirror ResetStatsDialog.tsx:15-24 but DROP the `cancelButtonRef.current?.focus()` line per D-13.
    - Effect 2: `useEffect(() => { addEventListener('cancel', handleCancel) → preventDefault() + onClose() ... cleanup ... }, [onClose])` — mirror ResetStatsDialog.tsx:28-39 verbatim with `onCancel` → `onClose`.
    - Handler: `const handleBackdropClick: MouseEventHandler<HTMLDialogElement> = (event) => { if (event.target === dialogRef.current) onClose() }` — mirror ResetStatsDialog.tsx:43-47.
    - JSX `<dialog>` element: `ref={dialogRef}`, `aria-labelledby="settings-dialog-title"`, `onClick={handleBackdropClick}`, className matches ResetStatsDialog pattern with `max-w-md` instead of `max-w-sm`: `"modal-fade m-auto max-w-md rounded-3xl border border-teal-100 bg-white p-0 shadow-[var(--shadow-breathing-card)] backdrop:bg-[var(--color-modal-backdrop)]"`.
    - Inner content: a `<div className="grid gap-5 p-6 sm:p-7">` containing in order: (1) `<h2 id="settings-dialog-title" className="text-2xl font-semibold tracking-tight text-slate-950">Settings</h2>` (D-18 locked title), (2) `<ThemePicker disabled={inSessionView} />`, (3) `<VariantPicker disabled={inSessionView} />`, (4) `<TimbrePicker disabled={inSessionView} />`, (5) `<LanguagePicker disabled={inSessionView} />`, (6) `<div className="flex justify-center"><button type="button" onClick={onClose} className="...LearnDialog Close button class string...">Close</button></div>`.
    - The component MUST NOT listen for the `close` event (Landmine 2 — close fires from imperative `close()` call; we only react to `cancel`).
  </behavior>
  <action>
    Create `src/components/SettingsDialog.tsx` mirroring `src/components/ResetStatsDialog.tsx` with the structural deltas in `<behavior>`. Imports:
    - `import { useEffect, useRef, type MouseEventHandler } from 'react'`
    - `import { ThemePicker } from './ThemePicker'`
    - `import { VariantPicker } from './VariantPicker'`
    - `import { TimbrePicker } from './TimbrePicker'`
    - `import { LanguagePicker } from './LanguagePicker'`

    Picker import order in the import list: Theme → Variant → Timbre → Language (matches D-10 render order; matches Phase 14 four-dim ordering for consistency).

    For the Close button, copy the verbatim Tailwind class string from `LearnDialog.tsx` lines 176-183 (RESEARCH.md §2 "Close button" subsection). Do NOT include a destructive-button variant (D-13 — no Keep/Reset semantics).

    Top-of-file comment block: cite Phase 15 D-05, D-10, D-11, D-12, D-13, D-15, D-18. Note the three deltas from ResetStatsDialog: (a) single `onClose` prop, (b) `inSessionView` threaded to four pickers, (c) NO `cancelButtonRef.focus()` (D-13).

    Pitfall-5 (Landmine 1) annotation: keep ResetStatsDialog's comment about `event.preventDefault()` purpose; rewording optional but the `preventDefault()` call MUST be present.

    Do NOT add `useState`, `useCallback`, or any state hooks — SettingsDialog is purely controlled by the `open` prop. Do NOT pre-mount the pickers when `open === false`: render them inside the `<dialog>` body unconditionally (React reconciles inside the closed dialog; same posture as ResetStatsDialog rendering buttons unconditionally). This means picker `loadPrefs()` may execute on the render where `open === false` — acceptable per Landmine 5 (two synchronous ops on a small object).
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx eslint src/components/SettingsDialog.tsx</automated>
  </verify>
  <acceptance_criteria>
    - src/components/SettingsDialog.tsx exists.
    - `grep -c "export function SettingsDialog(" src/components/SettingsDialog.tsx` returns 1.
    - `grep -c "export interface SettingsDialogProps" src/components/SettingsDialog.tsx` returns 1.
    - `grep -E "open: boolean" src/components/SettingsDialog.tsx` returns at least 1 match (props interface field).
    - `grep -E "onClose\\(this: void\\): void" src/components/SettingsDialog.tsx` returns at least 1 match (props field).
    - `grep -E "inSessionView: boolean" src/components/SettingsDialog.tsx` returns at least 1 match (props field).
    - `grep -c "useRef<HTMLDialogElement>(null)" src/components/SettingsDialog.tsx` returns 1.
    - `grep -c "dialog.showModal()" src/components/SettingsDialog.tsx` returns 1 (imperative open).
    - `grep -c "dialog.close()" src/components/SettingsDialog.tsx` returns 1 (imperative close).
    - `grep -c "addEventListener('cancel'" src/components/SettingsDialog.tsx` returns 1 (Pitfall-5 wiring).
    - `grep -c "event.preventDefault()" src/components/SettingsDialog.tsx` returns 1 (Landmine 1 mitigation).
    - `grep -c "addEventListener('close'" src/components/SettingsDialog.tsx` returns 0 (Landmine 2 — never listen for close).
    - `grep -c "event.target === dialogRef.current" src/components/SettingsDialog.tsx` returns 1 (backdrop click pattern).
    - `grep -c 'aria-labelledby="settings-dialog-title"' src/components/SettingsDialog.tsx` returns 1 (Landmine 9 — accessible name link).
    - `grep -c 'id="settings-dialog-title"' src/components/SettingsDialog.tsx` returns 1 (h2 id matches aria-labelledby).
    - `grep -c "max-w-md" src/components/SettingsDialog.tsx` returns 1 (Claude's discretion — wider than ResetStatsDialog's max-w-sm).
    - `grep -c "max-w-sm" src/components/SettingsDialog.tsx` returns 0 (not the Reset dialog width).
    - `grep -c '>Settings<' src/components/SettingsDialog.tsx` returns 1 (D-18 locked title text inside h2).
    - `grep -c '>Close<' src/components/SettingsDialog.tsx` returns 1 (D-18 locked Close button text).
    - `grep -c "<ThemePicker disabled={inSessionView}" src/components/SettingsDialog.tsx` returns 1.
    - `grep -c "<VariantPicker disabled={inSessionView}" src/components/SettingsDialog.tsx` returns 1.
    - `grep -c "<TimbrePicker disabled={inSessionView}" src/components/SettingsDialog.tsx` returns 1.
    - `grep -c "<LanguagePicker disabled={inSessionView}" src/components/SettingsDialog.tsx` returns 1.
    - Picker order in source — `awk '/<ThemePicker|<VariantPicker|<TimbrePicker|<LanguagePicker/' src/components/SettingsDialog.tsx` lists ThemePicker first, then VariantPicker, TimbrePicker, LanguagePicker (D-10 order).
    - `grep -c "cancelButtonRef" src/components/SettingsDialog.tsx` returns 0 (D-13 — no explicit focus on open).
    - `grep -c "useState" src/components/SettingsDialog.tsx` returns 0 (controlled by parent props only — D-05).
    - `grep -Ec "from ['\"](headlessui|@radix-ui|react-aria|react-modal)" src/components/SettingsDialog.tsx` returns 0 (D-15 zero new deps — native dialog only).
    - `npx tsc --noEmit` exits 0.
    - `npx eslint src/components/SettingsDialog.tsx` exits 0.
  </acceptance_criteria>
  <done>
    SettingsDialog.tsx exists, uses native `<dialog>` imperative pattern, threads `inSessionView` to all four pickers, type-checks and lints clean.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create SettingsDialog.test.tsx (closed/open/transition/inSessionView coverage; mirror ResetStatsDialog.test.tsx)</name>
  <files>src/components/SettingsDialog.test.tsx</files>
  <read_first>
    - src/components/ResetStatsDialog.test.tsx (lines 1-94 — literal template; helper at 8-17, closed-state describe at 19-26, open-state describe at 28-84, transition describe at 86-94)
    - src/components/SettingsDialog.tsx (from Task 1 — under test)
    - .planning/phases/15-settingsdialog-shell/15-PATTERNS.md (§"src/components/SettingsDialog.test.tsx" — full scaffold including the inSessionView-threading describe block)
    - .planning/phases/15-settingsdialog-shell/15-RESEARCH.md (Landmines 1, 8, 9; §1 "Testing Pattern for the cancel Event"; §"SC2 focus-return note")
    - vitest.setup.ts (lines 87-114 — polyfill behaviour: `dialog.open` boolean reliable; `cancel` event must be fired manually; `close` event fires from `close()` polyfill)
  </read_first>
  <behavior>
    Four describe blocks:

    1. `describe('SettingsDialog — closed state')` — 1 case:
       - `it('does not show the modal when open=false')` — `container.querySelector('dialog')` exists and `dialog.open === false`. Mirror ResetStatsDialog.test.tsx:19-26.

    2. `describe('SettingsDialog — open state (D-18 locked copy)')` — 6 cases:
       - `it('opens the native dialog when open=true')` — `screen.getByRole('dialog', { name: 'Settings' })` and `dialog.open === true`. NO `toHaveFocus` assertion on any button (D-13 — Landmine 1 note: SC2 focus-return is real-browser-only per VALIDATION.md Manual-Only table).
       - `it('renders Close button and Settings title with locked copy (D-18)')` — `getByRole('button', { name: 'Close' })` visible; `getByText('Settings')` visible.
       - `it('clicking Close invokes onClose exactly once')` — `userEvent.click(Close)` → `expect(onClose).toHaveBeenCalledTimes(1)`.
       - `it('Esc (dialog cancel event) invokes onClose via preventDefault path')` — `fireEvent(dialog, new Event('cancel', { cancelable: true }))` → `onClose` called once. Use the `// Reason: ... // eslint-disable-next-line @typescript-eslint/no-non-null-assertion` pattern from RESEARCH §6 Landmine 8.
       - `it('clicking the backdrop invokes onClose')` — `fireEvent.click(dialog, { target: dialog })` → `onClose` called once.
       - `it('clicking inside the inner panel does NOT invoke onClose')` — `userEvent.click(getByText('Settings'))` → `expect(onClose).not.toHaveBeenCalled()`.

    3. `describe('SettingsDialog — open→close transition')` — 1 case:
       - `it('closes the dialog when open transitions from true to false')` — rerender with `open={false}`; `dialog.open === false`. Mirror ResetStatsDialog.test.tsx:86-94.

    4. `describe('SettingsDialog — inSessionView picker disable threading')` — 1 case:
       - `it('renders all four picker stub texts when open=true with inSessionView=true (Landmine 7)')` — `renderDialog({ open: true, inSessionView: true })` → assert `getByText('Theme: system')`, `getByText('Variant: orb')`, `getByText('Timbre: bowl')`, `getByText('Language: en')` are all in the document (pickers each receive `disabled={true}` but the text content is unchanged — assertion confirms the picker children rendered with the threaded prop).

    Total: 9 test cases across 4 describe blocks.
  </behavior>
  <action>
    Create `src/components/SettingsDialog.test.tsx` mirroring `src/components/ResetStatsDialog.test.tsx` structure (helper + 3 describe blocks) plus the new inSessionView-threading describe block.

    Imports (lines 1-6 from ResetStatsDialog.test.tsx verbatim shape):
    - `import '@testing-library/jest-dom/vitest'`
    - `import { fireEvent, render, screen } from '@testing-library/react'`
    - `import userEvent from '@testing-library/user-event'`
    - `import { describe, expect, it, vi } from 'vitest'`
    - `import { SettingsDialog } from './SettingsDialog'`

    Helper `renderDialog` (mirror ResetStatsDialog.test.tsx:8-17 with the SettingsDialog prop shape):
    - Signature: `function renderDialog(props: Partial<{ open: boolean; onClose: () => void; inSessionView: boolean }> = {})`.
    - Defaults: `open=false`, `onClose=vi.fn()`, `inSessionView=false`.
    - Returns `{ ...utils, onClose }`.

    Test case content per `<behavior>`. The `Esc` and `backdrop` cases require the `container.querySelector('dialog')!` non-null-assertion pattern with the project's exact `// Reason: ... // eslint-disable-next-line @typescript-eslint/no-non-null-assertion` comment block from ResetStatsDialog.test.tsx:61-63 (Landmine 8).

    Include a top-of-file comment block citing the project's JSDOM limitations: native focus-trap and focus-return are NOT testable in JSDOM (Manual-Only verifications per `.planning/phases/15-settingsdialog-shell/15-VALIDATION.md`); tests therefore assert `dialog.open` boolean, `onClose` invocations, and child text rendering only.

    Do NOT use `toHaveFocus()` anywhere (D-13 + JSDOM limitation). Do NOT use `userEvent.keyboard('{Escape}')` (Landmine 1 — `cancel` event must be fired manually with `fireEvent`).
  </action>
  <verify>
    <automated>npx vitest run src/components/SettingsDialog.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - src/components/SettingsDialog.test.tsx exists.
    - `grep -c "describe('SettingsDialog" src/components/SettingsDialog.test.tsx` returns 4 (closed-state, open-state, transition, inSessionView).
    - `grep -c "it(" src/components/SettingsDialog.test.tsx` returns 9 (1+6+1+1 cases).
    - `grep -c "import { SettingsDialog } from './SettingsDialog'" src/components/SettingsDialog.test.tsx` returns 1.
    - `grep -c "name: 'Settings'" src/components/SettingsDialog.test.tsx` returns at least 1 (getByRole dialog name).
    - `grep -c "name: 'Close'" src/components/SettingsDialog.test.tsx` returns at least 1 (Close button role query).
    - `grep -c "new Event('cancel', { cancelable: true })" src/components/SettingsDialog.test.tsx` returns 1 (Landmine 1 — manual cancel fire).
    - `grep -c "userEvent.keyboard" src/components/SettingsDialog.test.tsx` returns 0 (Landmine 1 — never use keyboard helper for Esc).
    - `grep -c "toHaveFocus" src/components/SettingsDialog.test.tsx` returns 0 (D-13 + JSDOM limitation).
    - `grep -c "Theme: system" src/components/SettingsDialog.test.tsx` returns at least 1 (Landmine 7 picker-thread test).
    - `grep -c "Variant: orb" src/components/SettingsDialog.test.tsx` returns at least 1.
    - `grep -c "Timbre: bowl" src/components/SettingsDialog.test.tsx` returns at least 1.
    - `grep -c "Language: en" src/components/SettingsDialog.test.tsx` returns at least 1.
    - `grep -c "no-non-null-assertion" src/components/SettingsDialog.test.tsx` returns at least 2 (Landmine 8 — Esc + backdrop cases).
    - `npx vitest run src/components/SettingsDialog.test.tsx` exits 0 with all 9 tests passing.
    - `npx tsc --noEmit && npx eslint src/components/SettingsDialog.test.tsx` exits 0.
    - Full green-gate (`npx tsc --noEmit && npm run lint && npm run build && npm run test:run`) exits 0 (D-14); test count ≥ 438 baseline + Plan 01 (7) + Plan 02 (12) + Plan 03 (9) = 466+.
    - `git diff package.json package-lock.json` returns empty (D-15).
  </acceptance_criteria>
  <done>
    SettingsDialog.test.tsx exists with 9 passing cases across 4 describe blocks; full green-gate passes.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User keyboard/pointer → React event handlers | Esc → native `cancel` event; backdrop click → `event.target === dialogRef.current` |
| Native `<dialog>` API → React state | `dialog.open` mutation via imperative `showModal()`/`close()`; reflective via `cancel` event listener |
| Parent (App.tsx) props → Dialog children | `inSessionView` threaded as `disabled` to four pickers |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-09 | T (Tampering) | `cancel` event double-fire (Pitfall-5) | mitigate | `event.preventDefault()` inside the cancel handler (Landmine 1; ResetStatsDialog/LearnDialog/EndSessionDialog parity). Verified by acceptance_criteria grep on `event.preventDefault()` in SettingsDialog.tsx. |
| T-15-10 | T (Tampering) | Backdrop click vs inner-panel click | mitigate | `event.target === dialogRef.current` guard (Landmine 2). Inner-panel clicks (h2, picker children, Close button) have `event.target !== dialog`, so the early-return holds. Test case 6 in describe('open state') verifies. |
| T-15-11 | I (Information Disclosure) | aria-labelledby exposes title | accept | Title text is the literal locked string `Settings` (D-18). No PII. Screen readers announce "Settings dialog" on open — intended accessibility behaviour. |
| T-15-12 | D (DoS) | Imperative showModal called on re-render | mitigate | Guard `if (open && !dialog.open) showModal()` prevents repeated calls (ResetStatsDialog.tsx:18 pattern). JSDOM polyfill is idempotent on `open = true` anyway; native browsers throw `InvalidStateError` on double-showModal without the guard, so this is also a runtime-correctness measure. |
| T-15-13 | E (Elevation of Privilege) | inSessionView prop = false bypasses picker disable | mitigate | Defence-in-depth: SettingsAnchor (Plan 01) refuses to invoke onClick when disabled (App-level open-guard in Plan 04 short-circuits); Settings dialog never opens during session. If somehow `open=true` AND `inSessionView=true` (race window — D-12 auto-close handles), pickers receive `disabled={true}` (Landmine 7 threading). Combined the three layers prevent in-session picker interaction. |
| T-15-14 | S (Spoofing) | Native `<dialog>` top-layer + inert | accept | Native browsers handle inert background + top-layer rendering via `showModal()`; no clickjacking surface. JSDOM does not simulate these — manual smoke per VALIDATION.md covers. |
</threat_model>

<verification>
Plan 03 verification:
- SettingsDialog component renders, type-checks, lints.
- 9 unit tests cover closed/open/transition/inSessionView-threading.
- All four picker components from Plan 02 are imported and rendered in D-10 order.
- D-18 locked strings (`Settings` title, `Close` button) present and asserted.
- D-13 native-focus-return contract honoured — no explicit `.focus()` call on open.
- Pitfall-5 + Landmines 1, 2, 7, 8, 9 mitigated per acceptance_criteria grep gates.
- Manual smoke verification (focus-trap, focus-return after close) is deferred to phase-level VERIFICATION.md per VALIDATION.md Manual-Only table.
</verification>

<success_criteria>
- [ ] `src/components/SettingsDialog.tsx` exists with `SettingsDialog` + `SettingsDialogProps` exports.
- [ ] Imperative `<dialog>` pattern present (`useRef<HTMLDialogElement>`, `useEffect([open])` showModal/close, `useEffect([onClose])` cancel listener, `handleBackdropClick`).
- [ ] All four pickers from Plan 02 rendered in D-10 order, each receiving `disabled={inSessionView}`.
- [ ] D-18 locked strings present verbatim (`Settings` title, `Close` button).
- [ ] `src/components/SettingsDialog.test.tsx` exists with 9 passing cases across 4 describe blocks.
- [ ] `npx tsc --noEmit && npm run lint && npm run build && npm run test:run` exits 0 (D-14).
- [ ] Test count ≥ 438 baseline + 28 new (Plan 01: 7, Plan 02: 12, Plan 03: 9).
- [ ] No new npm dependencies; no edits to `src/domain/settings.ts` or `src/storage/prefs.ts` (D-15 + D-16).
</success_criteria>

<output>
After completion, create `.planning/phases/15-settingsdialog-shell/15-03-SUMMARY.md` per `@$HOME/.claude-personal-personal/get-shit-done/templates/summary.md`.
</output>
