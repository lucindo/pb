---
phase: 15-settingsdialog-shell
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/ThemePicker.tsx
  - src/components/ThemePicker.test.tsx
  - src/components/VariantPicker.tsx
  - src/components/VariantPicker.test.tsx
  - src/components/TimbrePicker.tsx
  - src/components/TimbrePicker.test.tsx
  - src/components/LanguagePicker.tsx
  - src/components/LanguagePicker.test.tsx
autonomous: true
requirements:
  - INFRA-04

must_haves:
  truths:
    - "Four picker components exist (ThemePicker, VariantPicker, TimbrePicker, LanguagePicker) each in its own file under src/components/ (D-01 file-per-dim split; D-03 flat folder)."
    - "Each picker accepts EXACTLY `{ disabled: boolean }` as its prop interface — no `value`, no `onChange`, no other props (D-02 picker contract; Phase 16-19 will add onChange inside the picker file, not at the call site)."
    - "Each picker self-reads `loadPrefs()` from `../storage/prefs` with no arguments (D-02 self-read pattern; D-06 zero savePrefs calls — read-only stub)."
    - "Each picker renders the section label + `Label: value` stub text using the literal Phase 14 default values when storage is empty (D-04 / D-18 locked-copy stub format): ThemePicker → `Theme: system`, VariantPicker → `Variant: orb`, TimbrePicker → `Timbre: bowl`, LanguagePicker → `Language: en`."
    - "Each picker file imports `loadPrefs` ONLY from `../storage/prefs` (no `savePrefs` import — D-06 zero writes at Phase 15)."
    - "Each picker does NOT import or edit `src/domain/settings.ts` or `src/storage/prefs.ts` (D-16 Phase 14 file-split preserved)."
    - "Each picker test file asserts the default-value render path (`Theme: system` etc.) when localStorage is empty — relies on vitest.setup.ts polyfill providing a clean store + Phase 14 coercePrefs returning DEFAULT_PREFS."
    - "Each picker test file asserts the disabled-styling render path (text stays present; disabled changes a class, not content)."
    - "Disabled visual cue: when `disabled={true}`, the value paragraph uses class `text-[var(--color-breathing-muted)]` (LearnAnchor disabled-palette parity)."
    - "Zero new npm dependencies (D-15)."
    - "`npx vitest run src/components/ThemePicker.test.tsx src/components/VariantPicker.test.tsx src/components/TimbrePicker.test.tsx src/components/LanguagePicker.test.tsx` exits 0; full green-gate (`npx tsc --noEmit && npm run lint && npm run build && npm run test:run`) exits 0."
  artifacts:
    - path: "src/components/ThemePicker.tsx"
      provides: "Read-only stub: section label `Theme` + `Theme: {prefs.theme}` value text; self-reads loadPrefs() — Phase 16 fills this file body to add the picker UI without touching SettingsDialog.tsx"
      exports: ["ThemePicker", "ThemePickerProps"]
      contains: "Theme: {prefs.theme}"
    - path: "src/components/ThemePicker.test.tsx"
      provides: "Vitest coverage: default render asserts `Theme: system`; disabled render still shows the value text"
      contains: "Theme: system"
    - path: "src/components/VariantPicker.tsx"
      provides: "Read-only stub for visual variant dimension; Phase 17 fills body"
      exports: ["VariantPicker", "VariantPickerProps"]
      contains: "Variant: {prefs.variant}"
    - path: "src/components/VariantPicker.test.tsx"
      provides: "Vitest coverage: default render asserts `Variant: orb`"
      contains: "Variant: orb"
    - path: "src/components/TimbrePicker.tsx"
      provides: "Read-only stub for audio timbre dimension; Phase 18 fills body"
      exports: ["TimbrePicker", "TimbrePickerProps"]
      contains: "Timbre: {prefs.timbre}"
    - path: "src/components/TimbrePicker.test.tsx"
      provides: "Vitest coverage: default render asserts `Timbre: bowl`"
      contains: "Timbre: bowl"
    - path: "src/components/LanguagePicker.tsx"
      provides: "Read-only stub for locale dimension; Phase 19 fills body"
      exports: ["LanguagePicker", "LanguagePickerProps"]
      contains: "Language: {prefs.locale}"
    - path: "src/components/LanguagePicker.test.tsx"
      provides: "Vitest coverage: default render asserts `Language: en`"
      contains: "Language: en"
  key_links:
    - from: "src/components/ThemePicker.tsx"
      to: "src/storage/prefs.ts"
      via: "named import — loadPrefs() consumer"
      pattern: "from '../storage/prefs'"
    - from: "src/components/VariantPicker.tsx"
      to: "src/storage/prefs.ts"
      via: "named import — loadPrefs() consumer"
      pattern: "from '../storage/prefs'"
    - from: "src/components/TimbrePicker.tsx"
      to: "src/storage/prefs.ts"
      via: "named import — loadPrefs() consumer"
      pattern: "from '../storage/prefs'"
    - from: "src/components/LanguagePicker.tsx"
      to: "src/storage/prefs.ts"
      via: "named import — loadPrefs() consumer"
      pattern: "from '../storage/prefs'"
---

<objective>
Plan 02 ships the four customization picker stubs — `ThemePicker`, `VariantPicker`, `TimbrePicker`, `LanguagePicker` — each in its own file under `src/components/`. Each stub accepts exactly `{ disabled: boolean }`, self-reads `loadPrefs()` from Phase 14 storage, and renders a section label plus a `Label: value` text snapshot of the currently stored value (D-04 read-only stub; D-18 locked copy).

Why four files and not one shared shell: D-01 ensures Phase 16-19 each fills its own picker file body WITHOUT re-editing `SettingsDialog.tsx`. The dialog imports four stable component names; the bodies turn into real radio-groups / selects feature phase by feature phase with zero cross-phase merge surface.

Purpose: Closes the SC3 picker-render portion of INFRA-04 — "All four picker placeholders (theme, timbre, variant, language) render inside the dialog with stub labels and are visually disabled while `inSessionView` is true." (ROADMAP.md). The disabled-styling assertion is per-picker here; the SettingsDialog wires `disabled={inSessionView}` to all four pickers in Plan 03; App.tsx mounts the dialog in Plan 04.

Output:
- `src/components/ThemePicker.tsx` + `.test.tsx` NEW
- `src/components/VariantPicker.tsx` + `.test.tsx` NEW
- `src/components/TimbrePicker.tsx` + `.test.tsx` NEW
- `src/components/LanguagePicker.tsx` + `.test.tsx` NEW

No edits to `src/domain/settings.ts` or `src/storage/prefs.ts` (D-16). No edits to App.tsx (Plan 04 owns). No new npm dependencies (D-15).

Wave: 1 (parallel with Plan 01). No dependencies on other Phase 15 plans. The four picker files are independent of each other (no shared file), so the four tasks within this plan can execute sequentially or be batched as a single sweep.
</objective>

<execution_context>
@$HOME/.claude-personal-personal/get-shit-done/workflows/execute-plan.md
@$HOME/.claude-personal-personal/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/15-settingsdialog-shell/15-CONTEXT.md
@.planning/phases/15-settingsdialog-shell/15-RESEARCH.md
@.planning/phases/15-settingsdialog-shell/15-PATTERNS.md
@.planning/phases/15-settingsdialog-shell/15-VALIDATION.md
@src/storage/prefs.ts
@src/domain/settings.ts
</context>

<interfaces>
<!-- Key types/exports consumed by every picker stub. Extracted from Phase 14 baseline. -->
<!-- Source: src/storage/prefs.ts lines 24-30, 31-36, 68-70 -->

From src/storage/prefs.ts:
- `export interface UserPrefs { theme: ThemeId; timbre: TimbreId; variant: VisualVariantId; locale: LocaleId }`
- `export const DEFAULT_PREFS: UserPrefs = { theme: 'system', timbre: 'bowl', variant: 'orb', locale: 'en' }` (composed from `DEFAULT_THEME` / `DEFAULT_TIMBRE` / `DEFAULT_VARIANT` / `DEFAULT_LOCALE` imports)
- `export function loadPrefs(deps: StorageDeps = {}): UserPrefs` — returns coerced prefs (DEFAULT_PREFS when storage is empty)

From src/domain/settings.ts:
- `export type ThemeId = 'light' | 'dark' | 'system' | 'moss' | 'slate' | 'dusk'`
- `export type TimbreId = 'bowl' | 'bell' | 'sine' | 'chime'`
- `export type VisualVariantId = 'orb' | 'square' | 'ring'`
- `export type LocaleId = 'en' | 'pt-BR'`

Phase 14 default-value baseline (verified at HEAD): empty localStorage → `loadPrefs()` returns `{ theme: 'system', timbre: 'bowl', variant: 'orb', locale: 'en' }`. Tests assert exactly these literal strings per D-18 (no display mapping in Phase 15).
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create ThemePicker stub + test (Theme: system)</name>
  <files>src/components/ThemePicker.tsx, src/components/ThemePicker.test.tsx</files>
  <read_first>
    - src/storage/prefs.ts (lines 1-76 — full file; loadPrefs at line 68; UserPrefs at line 24)
    - src/domain/settings.ts (DEFAULT_THEME on line 61 — value `'system'`)
    - .planning/phases/15-settingsdialog-shell/15-PATTERNS.md (§"src/components/ThemePicker.tsx" — full template)
    - .planning/phases/15-settingsdialog-shell/15-RESEARCH.md (§2 "Picker Stub Template"; §"Wave 0 Gaps" testing notes)
    - .planning/phases/15-settingsdialog-shell/15-CONTEXT.md (D-01, D-02, D-04, D-06, D-16, D-18)
  </read_first>
  <behavior>
    Production component:
    - `export interface ThemePickerProps { disabled: boolean }` — only this prop.
    - `export function ThemePicker({ disabled }: ThemePickerProps)` reads `loadPrefs()` synchronously and returns a `<div>` containing the section label `Theme` (in a `<p>` with `text-sm font-semibold text-slate-900`) and a value paragraph `Theme: {prefs.theme}` (in a `<p>` with text-color class toggled by `disabled`).
    - When `disabled === true`, the value paragraph uses `text-[var(--color-breathing-muted)]`; when `false`, uses `text-slate-700`.
    Tests:
    - Test 1 (default render): `render(<ThemePicker disabled={false} />)` → `expect(screen.getByText('Theme: system')).toBeInTheDocument()` (relies on coercePrefs DEFAULT_THEME fallback with empty localStorage).
    - Test 2 (enabled label): `render(<ThemePicker disabled={false} />)` → `expect(screen.getByText('Theme')).toBeInTheDocument()`.
    - Test 3 (disabled still renders): `render(<ThemePicker disabled={true} />)` → `expect(screen.getByText('Theme: system')).toBeInTheDocument()` (text unchanged; only styling differs).
  </behavior>
  <action>
    Create `src/components/ThemePicker.tsx`:
    - Import `loadPrefs` from `'../storage/prefs'`. Do NOT import `savePrefs` (D-06 zero writes).
    - Do NOT import from `'../domain/settings'` (D-16; the picker stays a pure consumer of the storage API).
    - Export the interface and function per `<behavior>` above. Use the literal display format `Theme: ${prefs.theme}` via JSX interpolation `Theme: {prefs.theme}` (D-18 verbatim — `Theme` capitalized, single space after colon, no display-mapping function).
    - Top-of-file comment block citing Phase 15 D-01 / D-02 / D-04 / D-06 / D-18 and noting that Phase 16 (THEME-01..05) will fill the body of THIS file to wire onChange + savePrefs.

    Create `src/components/ThemePicker.test.tsx`:
    - Imports: `@testing-library/jest-dom/vitest`, `render`, `screen` from `@testing-library/react`, `describe`, `expect`, `it` from `vitest`, `ThemePicker` from `'./ThemePicker'`.
    - Single describe block `describe('ThemePicker — read-only stub (Phase 15 D-04)', ...)` with the three it() cases from `<behavior>`.
    - No need to seed localStorage — vitest.setup.ts provides a clean store per test; `loadPrefs()` returns DEFAULT_PREFS via coercePrefs when the envelope is empty.
  </action>
  <verify>
    <automated>npx vitest run src/components/ThemePicker.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - src/components/ThemePicker.tsx exists.
    - `grep -c "export function ThemePicker(" src/components/ThemePicker.tsx` returns 1.
    - `grep -c "export interface ThemePickerProps" src/components/ThemePicker.tsx` returns 1.
    - `grep -c "from '../storage/prefs'" src/components/ThemePicker.tsx` returns 1.
    - `grep -c "loadPrefs" src/components/ThemePicker.tsx` returns at least 1 (consumer of loadPrefs).
    - `grep -c "savePrefs" src/components/ThemePicker.tsx` returns 0 (D-06 zero writes).
    - `grep -c "from '../domain/settings'" src/components/ThemePicker.tsx` returns 0 (D-16 — pickers consume storage only).
    - `grep -c "Theme: {prefs.theme}" src/components/ThemePicker.tsx` returns 1 (D-18 stub format).
    - `grep -c "text-\[var(--color-breathing-muted)\]" src/components/ThemePicker.tsx` returns at least 1 (disabled palette).
    - src/components/ThemePicker.test.tsx exists.
    - `grep -c "describe('ThemePicker" src/components/ThemePicker.test.tsx` returns 1.
    - `grep -c "Theme: system" src/components/ThemePicker.test.tsx` returns at least 2 (default-render + disabled-render assertions).
    - `npx vitest run src/components/ThemePicker.test.tsx` exits 0 with all 3 tests passing.
    - `npx tsc --noEmit && npx eslint src/components/ThemePicker.tsx src/components/ThemePicker.test.tsx` exits 0.
  </acceptance_criteria>
  <done>
    ThemePicker.tsx + .test.tsx exist; tests pass; type-check and lint clean.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create VariantPicker stub + test (Variant: orb)</name>
  <files>src/components/VariantPicker.tsx, src/components/VariantPicker.test.tsx</files>
  <read_first>
    - src/components/ThemePicker.tsx (from Task 1 — template for this file; substitute theme → variant)
    - src/components/ThemePicker.test.tsx (from Task 1 — template for this test file)
    - src/storage/prefs.ts (lines 24-30 — UserPrefs.variant field)
    - src/domain/settings.ts (DEFAULT_VARIANT on line 81 — value `'orb'`)
    - .planning/phases/15-settingsdialog-shell/15-PATTERNS.md (§"src/components/VariantPicker.tsx")
  </read_first>
  <behavior>
    Identical to ThemePicker with these substitutions:
    - `ThemePicker` → `VariantPicker`, `ThemePickerProps` → `VariantPickerProps`.
    - Section label text `Theme` → `Variant` (D-18).
    - Display format `Theme: {prefs.theme}` → `Variant: {prefs.variant}` (D-18).
    - Default-value tests: `'Theme: system'` → `'Variant: orb'`, `'Theme'` label → `'Variant'`.
  </behavior>
  <action>
    Create `src/components/VariantPicker.tsx` and `src/components/VariantPicker.test.tsx` by mirroring `ThemePicker.tsx` / `ThemePicker.test.tsx` from Task 1 with the four substitutions in `<behavior>`. Keep import path identical (`from '../storage/prefs'`). Keep the top-of-file comment style identical, citing Phase 17 (VARIANT-01..07) as the body-filling phase. No new types — `prefs.variant: VisualVariantId` is already supplied by `loadPrefs()`.
  </action>
  <verify>
    <automated>npx vitest run src/components/VariantPicker.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - src/components/VariantPicker.tsx exists.
    - `grep -c "export function VariantPicker(" src/components/VariantPicker.tsx` returns 1.
    - `grep -c "export interface VariantPickerProps" src/components/VariantPicker.tsx` returns 1.
    - `grep -c "Variant: {prefs.variant}" src/components/VariantPicker.tsx` returns 1 (D-18 stub format).
    - `grep -c "savePrefs" src/components/VariantPicker.tsx` returns 0 (D-06).
    - `grep -c "from '../domain/settings'" src/components/VariantPicker.tsx` returns 0 (D-16).
    - src/components/VariantPicker.test.tsx exists.
    - `grep -c "Variant: orb" src/components/VariantPicker.test.tsx` returns at least 2.
    - `npx vitest run src/components/VariantPicker.test.tsx` exits 0 with all 3 tests passing.
    - `npx tsc --noEmit && npx eslint src/components/VariantPicker.tsx src/components/VariantPicker.test.tsx` exits 0.
  </acceptance_criteria>
  <done>
    VariantPicker.tsx + .test.tsx exist; tests pass; type-check and lint clean.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Create TimbrePicker stub + test (Timbre: bowl)</name>
  <files>src/components/TimbrePicker.tsx, src/components/TimbrePicker.test.tsx</files>
  <read_first>
    - src/components/ThemePicker.tsx (from Task 1 — template)
    - src/components/ThemePicker.test.tsx (from Task 1 — test template)
    - src/storage/prefs.ts (lines 24-30 — UserPrefs.timbre field)
    - src/domain/settings.ts (DEFAULT_TIMBRE on line 71 — value `'bowl'`)
    - .planning/phases/15-settingsdialog-shell/15-PATTERNS.md (§"src/components/TimbrePicker.tsx")
  </read_first>
  <behavior>
    Identical to ThemePicker with these substitutions:
    - `ThemePicker` → `TimbrePicker`, `ThemePickerProps` → `TimbrePickerProps`.
    - Section label `Theme` → `Timbre`.
    - Display `Theme: {prefs.theme}` → `Timbre: {prefs.timbre}`.
    - Default-value tests: `'Timbre: bowl'`, label `'Timbre'`.
  </behavior>
  <action>
    Mirror `ThemePicker.tsx` / `ThemePicker.test.tsx` from Task 1 with the four substitutions in `<behavior>`. Phase 18 (TIMBRE-01..05) will fill the body of this file.
  </action>
  <verify>
    <automated>npx vitest run src/components/TimbrePicker.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - src/components/TimbrePicker.tsx exists.
    - `grep -c "export function TimbrePicker(" src/components/TimbrePicker.tsx` returns 1.
    - `grep -c "export interface TimbrePickerProps" src/components/TimbrePicker.tsx` returns 1.
    - `grep -c "Timbre: {prefs.timbre}" src/components/TimbrePicker.tsx` returns 1.
    - `grep -c "savePrefs" src/components/TimbrePicker.tsx` returns 0 (D-06).
    - src/components/TimbrePicker.test.tsx exists.
    - `grep -c "Timbre: bowl" src/components/TimbrePicker.test.tsx` returns at least 2.
    - `npx vitest run src/components/TimbrePicker.test.tsx` exits 0 with all 3 tests passing.
    - `npx tsc --noEmit && npx eslint src/components/TimbrePicker.tsx src/components/TimbrePicker.test.tsx` exits 0.
  </acceptance_criteria>
  <done>
    TimbrePicker.tsx + .test.tsx exist; tests pass; type-check and lint clean.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Create LanguagePicker stub + test (Language: en)</name>
  <files>src/components/LanguagePicker.tsx, src/components/LanguagePicker.test.tsx</files>
  <read_first>
    - src/components/ThemePicker.tsx (from Task 1 — template)
    - src/components/ThemePicker.test.tsx (from Task 1 — test template)
    - src/storage/prefs.ts (lines 24-30 — UserPrefs.locale field)
    - src/domain/settings.ts (DEFAULT_LOCALE on line 91 — value `'en'`; LocaleId on line 83 uses BCP-47 hyphen `'pt-BR'`)
    - .planning/phases/15-settingsdialog-shell/15-PATTERNS.md (§"LanguagePicker")
    - .planning/phases/15-settingsdialog-shell/15-CONTEXT.md (Deferred — pt_BR vs pt-BR reconciliation deferred to Phase 19; Phase 15 ships raw `prefs.locale` identifier verbatim, no display mapping)
  </read_first>
  <behavior>
    Identical to ThemePicker with these substitutions:
    - `ThemePicker` → `LanguagePicker`, `ThemePickerProps` → `LanguagePickerProps`.
    - Section label `Theme` → `Language` (D-18 — label is `Language`, NOT `Locale`).
    - Display `Theme: {prefs.theme}` → `Language: {prefs.locale}` (label says "Language" while the field interpolated is `prefs.locale` — this is intentional per D-18).
    - Default-value tests: `'Language: en'`, label `'Language'`.
  </behavior>
  <action>
    Mirror `ThemePicker.tsx` / `ThemePicker.test.tsx` from Task 1 with the four substitutions in `<behavior>`. Phase 19 (I18N-01..07) will fill the body of this file. Note in the top-of-file comment: section label `Language` (D-18) intentionally maps to the underlying `locale` storage field — display-name translation (e.g. `English` / `Português (Brasil)`) is a Phase 19 concern.
  </action>
  <verify>
    <automated>npx vitest run src/components/LanguagePicker.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - src/components/LanguagePicker.tsx exists.
    - `grep -c "export function LanguagePicker(" src/components/LanguagePicker.tsx` returns 1.
    - `grep -c "export interface LanguagePickerProps" src/components/LanguagePicker.tsx` returns 1.
    - `grep -c "Language: {prefs.locale}" src/components/LanguagePicker.tsx` returns 1 (D-18: label is `Language`, field is `prefs.locale`).
    - `grep -c "savePrefs" src/components/LanguagePicker.tsx` returns 0 (D-06).
    - src/components/LanguagePicker.test.tsx exists.
    - `grep -c "Language: en" src/components/LanguagePicker.test.tsx` returns at least 2.
    - `npx vitest run src/components/LanguagePicker.test.tsx` exits 0 with all 3 tests passing.
    - Full green-gate (`npx tsc --noEmit && npm run lint && npm run build && npm run test:run`) exits 0 (D-14); test count ≥ 438 baseline + 12 new picker cases (3 cases × 4 pickers) [plus any Plan 01 cases if Plan 01 has already merged].
    - `git diff package.json package-lock.json` returns empty (D-15 zero new deps).
  </acceptance_criteria>
  <done>
    All four picker stub files (+ tests) exist; 12 picker test cases pass; full green-gate green.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Storage layer (localStorage) → React render | `loadPrefs()` → `readEnvelope()` → `JSON.parse(localStorage.getItem(...))` |
| React render → DOM text | `{prefs.theme}` / `{prefs.variant}` / `{prefs.timbre}` / `{prefs.locale}` interpolation into text node |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-05 | T (Tampering) | Storage payload (`localStorage.envelope.prefs`) | mitigate | Phase 14 D-12 / D-17 prototype-pollution + per-field coerce-and-fallback already handles drifted values. `prefs.theme` / `prefs.variant` / `prefs.timbre` / `prefs.locale` are typed unions narrowed to OPTIONS members. A tampered value is coerced to DEFAULT_* before reaching the picker render — text content stays within the bounded set (`light` / `dark` / `system` / `moss` / `slate` / `dusk` for theme, etc.). |
| T-15-06 | I (Information Disclosure) | React text interpolation of stored value | accept | `{prefs.theme}` etc. render into the DOM as text nodes (React auto-escapes). No HTML injection surface. Stored values are non-PII identifiers (`system`, `bowl`, `orb`, `en`). |
| T-15-07 | E (Elevation of Privilege) | Picker imports from `'../domain/settings'` (D-16 violation) | mitigate | Pickers MUST NOT import from `'../domain/settings'` — they consume Phase 14 storage API only (`loadPrefs` from `'../storage/prefs'`). Grep gate in acceptance_criteria enforces this contract per Task. |
| T-15-08 | D (DoS) | `loadPrefs()` called on every picker render | accept | Landmine 5: `loadPrefs()` is two synchronous ops (`localStorage.getItem` + `JSON.parse`) on a small 4-field object. Pickers only mount when dialog is open and re-renders are rare. `useMemo` is explicitly NOT added at Phase 15 (D-16 file-split + D-06 zero-write scope). |
</threat_model>

<verification>
Phase 15 Plan 02 verification:
- Four picker stub files exist, each with its `.test.tsx` neighbour.
- 12 unit tests (3 per picker) pass against vitest.setup.ts clean-localStorage baseline.
- Full green-gate green at plan close.
- INFRA-04 SC3 picker-render portion structurally satisfied at the component layer; the SettingsDialog (Plan 03) wires `disabled={inSessionView}` to all four pickers, and Plan 04 mounts the dialog in App.tsx.
</verification>

<success_criteria>
- [ ] Four picker `.tsx` files exist (`ThemePicker`, `VariantPicker`, `TimbrePicker`, `LanguagePicker`) under `src/components/`.
- [ ] Four picker `.test.tsx` files exist, each asserting the D-18 default-value text (`Theme: system` / `Variant: orb` / `Timbre: bowl` / `Language: en`).
- [ ] Every picker imports `loadPrefs` from `'../storage/prefs'`; NONE imports `savePrefs`; NONE imports from `'../domain/settings'` (D-06 + D-16).
- [ ] `npx tsc --noEmit && npm run lint && npm run build && npm run test:run` exits 0 (D-14 per-commit green-gate).
- [ ] Test count ≥ 438 baseline + 12 new picker cases.
- [ ] `git diff package.json package-lock.json` returns empty (D-15).
</success_criteria>

<output>
After completion, create `.planning/phases/15-settingsdialog-shell/15-02-SUMMARY.md` per `@$HOME/.claude-personal-personal/get-shit-done/templates/summary.md`.
</output>
