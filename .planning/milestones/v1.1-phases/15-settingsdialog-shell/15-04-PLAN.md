---
phase: 15-settingsdialog-shell
plan: 04
type: execute
wave: 3
depends_on:
  - 15-01
  - 15-02
  - 15-03
files_modified:
  - src/app/App.tsx
  - .planning/REQUIREMENTS.md
  - .planning/STATE.md
  - .planning/ROADMAP.md
  - .planning/phases/15-settingsdialog-shell/15-SUMMARY.md
autonomous: true
requirements:
  - INFRA-04

must_haves:
  truths:
    - "`App.tsx` imports `SettingsAnchor` from `'../components/SettingsAnchor'` and `SettingsDialog` from `'../components/SettingsDialog'` (Plan 01 + Plan 03 outputs)."
    - "App.tsx declares exactly one new piece of React state for Phase 15: `const [settingsDialogOpen, setSettingsDialogOpen] = useState<boolean>(false)` (D-05 scope)."
    - "App.tsx adds `onSettingsClick` useCallback with `if (inSessionView) return; setSettingsDialogOpen(true)` body (mirror of `onLearnClick` at App.tsx:414-417, D-08 open-guard)."
    - "App.tsx adds `onSettingsClose` useCallback with `setSettingsDialogOpen(false)` body (mirror of `onLearnClose` at App.tsx:419-421)."
    - "The existing WR-09 useEffect (currently App.tsx:264-271) is EXTENDED — NOT duplicated — to also call `setSettingsDialogOpen(false)` when `inSessionView` flips true (D-12 / D-17; Landmine 6 — each setter gets its own `// Reason:` annotation + eslint-disable-next-line)."
    - "App.tsx renders `<SettingsAnchor disabled={inSessionView} onClick={onSettingsClick} />` inside the existing `<div className=\"relative w-full\">` container (App.tsx:579) — adjacent to the existing `<LearnAnchor>` render at line 586 (D-07 symmetric left-0 / right-0 pair sharing one `position: relative` parent)."
    - "App.tsx renders `<SettingsDialog open={settingsDialogOpen} onClose={onSettingsClose} inSessionView={inSessionView} />` after the existing `<LearnDialog>` render at App.tsx:653 and before the closing `</main>` at line 654."
    - "REQUIREMENTS.md INFRA-04 traceability row flips from `Pending` to `Done` (single-row table edit)."
    - "STATE.md updates `Current Position`, `last_updated`, `last_activity`, `progress.completed_phases` (2 → 3), `progress.total_plans` (2 → 6), `progress.completed_plans` (2 → 6), and `progress.percent` (29% → 43%); Phase 15 row in the per-phase table flips to `Complete` with completion date 2026-05-12."
    - "ROADMAP.md Phase 15 entry flips from `Not started` (or whatever the current orchestrator-set status is) to `Complete`; Phase 15 list item in the v1.1 section adds `(completed 2026-05-12)` parallel to Phase 13/14 patterns; Progress table row flips to `4/4` plans complete with completion date."
    - "Phase close artifact `15-SUMMARY.md` exists per `@$HOME/.claude-personal-personal/get-shit-done/templates/summary.md`, summarizing the four plans, what shipped, and any carry-forward items."
    - "No edits to `src/domain/settings.ts` or `src/storage/prefs.ts` (D-16)."
    - "No new npm dependencies (D-15)."
    - "Full green-gate (`npx tsc --noEmit && npm run lint && npm run build && npm run test:run`) exits 0 with test count ≥ 438 baseline + Phase 15 new tests (Plan 01: 7, Plan 02: 12, Plan 03: 9, Plan 04: this plan may add 0 if App-level dialog wiring is covered structurally; planner accepts 0 new tests here since lower-layer coverage is exhaustive)."
    - "SC1 satisfied end-to-end: a real user opens the app in idle view → sees a gear button at the top-left of the breathing column → presses Enter → SettingsDialog opens (D-07 + D-08 wired through App.tsx)."
    - "SC2 satisfied end-to-end via native browser behaviour (verified by Manual smoke per VALIDATION.md): focus trap inside dialog, focus returns to SettingsAnchor on close."
    - "SC3 satisfied end-to-end: with a session active (`inSessionView=true`), the gear is `aria-disabled`, and if the dialog WAS already open at session start the WR-09 effect closes it; if forced open via dev-tools the pickers all carry the disabled palette."
    - "SC4 satisfied end-to-end: zero new deps (`git diff package.json package-lock.json` returns empty); native `<dialog>` pattern shipped; D-18 locked strings present in every visible surface."
    - "SC5 satisfied end-to-end: per-commit green-gate exits 0 at every Phase 15 commit (D-14)."
  artifacts:
    - path: "src/app/App.tsx"
      provides: "Wiring for Phase 15: SettingsAnchor mount, SettingsDialog mount, settingsDialogOpen state, onSettingsClick/onSettingsClose callbacks, WR-09 auto-close extension"
      contains: "SettingsDialog"
    - path: ".planning/REQUIREMENTS.md"
      provides: "INFRA-04 traceability flipped to Done"
      contains: "| INFRA-04 | Phase 15 | Done |"
    - path: ".planning/STATE.md"
      provides: "Project state advanced to Phase 15 complete"
      contains: "Phase 15"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 15 marked Complete with completion date"
      contains: "Phase 15"
    - path: ".planning/phases/15-settingsdialog-shell/15-SUMMARY.md"
      provides: "Phase-close summary aggregating all four plan outcomes"
      contains: "Phase 15"
  key_links:
    - from: "src/app/App.tsx"
      to: "src/components/SettingsAnchor.tsx"
      via: "named import — gear trigger button rendered in breathing-column container"
      pattern: "from '../components/SettingsAnchor'"
    - from: "src/app/App.tsx"
      to: "src/components/SettingsDialog.tsx"
      via: "named import — dialog rendered at dialog mount block"
      pattern: "from '../components/SettingsDialog'"
    - from: "App.tsx render <SettingsAnchor>"
      to: "App.tsx state onSettingsClick"
      via: "onClick prop wiring"
      pattern: "onClick={onSettingsClick}"
    - from: "App.tsx render <SettingsAnchor>"
      to: "App.tsx state inSessionView"
      via: "disabled prop wiring (D-08 disable-not-hide)"
      pattern: "disabled={inSessionView}"
    - from: "App.tsx render <SettingsDialog>"
      to: "App.tsx state settingsDialogOpen"
      via: "open prop wiring"
      pattern: "open={settingsDialogOpen}"
    - from: "App.tsx render <SettingsDialog>"
      to: "App.tsx state inSessionView"
      via: "inSessionView prop wiring (Landmine 7 — picker disable threading)"
      pattern: "inSessionView={inSessionView}"
    - from: "WR-09 useEffect (App.tsx:264-271)"
      to: "setSettingsDialogOpen"
      via: "auto-close when inSessionView flips true (D-12 / D-17)"
      pattern: "setSettingsDialogOpen\\(false\\)"
---

<objective>
Plan 04 wires Plans 01 + 02 + 03 into the running app: imports the two new components into `App.tsx`, adds one new piece of React state (`settingsDialogOpen`, D-05), two callbacks (`onSettingsClick` + `onSettingsClose`), extends the existing WR-09 auto-close effect, renders the gear at the breathing-column container, and renders the dialog at the dialog mount block. This is the smallest change that completes INFRA-04 end-to-end.

Then closes the phase: flips REQUIREMENTS.md INFRA-04 to Done, advances STATE.md, marks Phase 15 Complete in ROADMAP.md, and writes 15-SUMMARY.md.

Purpose: Closes the App-level wiring for all five Success Criteria of Phase 15 (SC1 visible gear + D-08 disabled-in-session; SC2 dialog opens via gear, native focus return; SC3 inSessionView threads to dialog → pickers; SC4 native `<dialog>` + zero new deps; SC5 per-commit green-gate). Plans 01-03 ship the components; Plan 04 makes them user-reachable.

Output:
- `src/app/App.tsx` MODIFIED (imports, state, callbacks, effect extension, JSX renders)
- `.planning/REQUIREMENTS.md` MODIFIED (INFRA-04 → Done)
- `.planning/STATE.md` MODIFIED (Phase 15 complete)
- `.planning/ROADMAP.md` MODIFIED (Phase 15 marked Complete; plan count 4)
- `.planning/phases/15-settingsdialog-shell/15-SUMMARY.md` NEW

Wave: 3 (depends on Plans 01, 02, 03 — needs SettingsAnchor + SettingsDialog imports + all four pickers to exist before App.tsx can import).
</objective>

<execution_context>
@$HOME/.claude-personal-personal/get-shit-done/workflows/execute-plan.md
@$HOME/.claude-personal-personal/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/15-settingsdialog-shell/15-CONTEXT.md
@.planning/phases/15-settingsdialog-shell/15-RESEARCH.md
@.planning/phases/15-settingsdialog-shell/15-PATTERNS.md
@.planning/phases/15-settingsdialog-shell/15-VALIDATION.md
@src/app/App.tsx
</context>

<interfaces>
<!-- App.tsx integration anchors verified at HEAD 70968fc (RESEARCH.md §4). -->

Existing App.tsx state derivation (line 142):
- `const inSessionView = appPhase !== 'idle'` — already declared; Phase 15 reuses verbatim. NOT a new state.

Existing WR-09 auto-close effect (App.tsx:264-271, verbatim shape from RESEARCH §"App.tsx Auto-Close useEffect Template"):
```text
useEffect(() => {
  if (inSessionView) {
    // eslint-disable-next-line react-hooks/set-state-in-effect
    setLearnDialogOpen(false)
    setResetDialogOpen(false)
  }
}, [inSessionView])
```
Phase 15 extends this same effect — does NOT create a second `useEffect`. Each set-state call gets its own `// Reason: ...` + `// eslint-disable-next-line react-hooks/set-state-in-effect` per Landmine 6 + Phase 7 D-04 annotation policy.

Existing Learn callbacks (App.tsx:414-421):
- `const onLearnClick = useCallback(() => { if (inSessionView) return; setLearnDialogOpen(true) }, [inSessionView])`
- `const onLearnClose = useCallback(() => { setLearnDialogOpen(false) }, [])`

Phase 15's `onSettingsClick` / `onSettingsClose` are literal mirrors with the setter swap.

Existing LearnAnchor render site (App.tsx:579-587, the `<div className="relative w-full">` block):
- This div already provides `position: relative` for LearnAnchor's `absolute right-0 top-0`. SettingsAnchor uses `absolute left-0 top-0` and mounts in the SAME parent — no new wrapper div needed (D-07).

Existing dialog mount block (App.tsx:641-653, contains EndSessionDialog, ResetStatsDialog, LearnDialog renders before `</main>` at line 654):
- SettingsDialog mounts as the fourth dialog in this block.

Plan 01 / Plan 03 exports consumed here:
- `import { SettingsAnchor } from '../components/SettingsAnchor'` — type: `({ disabled: boolean; onClick: () => void }) => JSX.Element`.
- `import { SettingsDialog } from '../components/SettingsDialog'` — type: `({ open: boolean; onClose: () => void; inSessionView: boolean }) => JSX.Element`.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wire SettingsAnchor + SettingsDialog into App.tsx (imports, state, callbacks, effect extension, JSX renders)</name>
  <files>src/app/App.tsx</files>
  <read_first>
    - src/app/App.tsx — full file, but focus on these line-ranges per RESEARCH §4 (verified at HEAD 70968fc): 1-15 (imports block), 50-65 (existing dialog-open useState declarations), 137-155 (inSessionView derivation), 247-272 (WR-01 + WR-09 auto-close effects), 385-425 (Reset + Learn callback blocks), 575-590 (breathing-column container with LearnAnchor render), 640-656 (dialog mount block)
    - src/components/SettingsAnchor.tsx (from Plan 01 — props shape)
    - src/components/SettingsDialog.tsx (from Plan 03 — props shape)
    - .planning/phases/15-settingsdialog-shell/15-RESEARCH.md (§4 "App.tsx Integration Anchors" — exact line numbers; §6 Landmine 6 — three-setter ESLint annotation policy; Landmine 7 — inSessionView threading)
    - .planning/phases/15-settingsdialog-shell/15-PATTERNS.md (§"src/app/App.tsx" — 6 numbered edit sites with full source-of-truth quotes)
    - .planning/phases/15-settingsdialog-shell/15-CONTEXT.md (D-05, D-07, D-08, D-12, D-17 — App-level integration decisions)
  </read_first>
  <behavior>
    Six discrete edits to App.tsx, all in the same task (atomic commit). Each edit must preserve byte-identical surrounding code:

    1. Imports — add after the existing `import { LearnAnchor } from '../components/LearnAnchor'` line (~line 10) and before the next non-component import. Two new lines: `import { SettingsAnchor } from '../components/SettingsAnchor'` and `import { SettingsDialog } from '../components/SettingsDialog'`. Place them adjacent to LearnAnchor/LearnDialog imports for grouping.

    2. State — add after the existing `const [learnDialogOpen, setLearnDialogOpen] = useState<boolean>(false)` (~line 55). New line: `const [settingsDialogOpen, setSettingsDialogOpen] = useState<boolean>(false)`.

    3. WR-09 effect extension (App.tsx:264-271) — EXTEND the existing effect body. After the `setResetDialogOpen(false)` call, add:
       - `// Reason: subscribe-and-reflect — settingsDialogOpen mirrors external inSessionView; same WR-09 pattern as setLearnDialogOpen/setResetDialogOpen.`
       - `// eslint-disable-next-line react-hooks/set-state-in-effect`
       - `setSettingsDialogOpen(false)`

       The existing `setLearnDialogOpen(false)` and `setResetDialogOpen(false)` lines must each have their own `// eslint-disable-next-line react-hooks/set-state-in-effect` comment after the change (Landmine 6 — the current file uses ONE disable comment that covers only `setLearnDialogOpen`; adding `setSettingsDialogOpen` requires either (a) per-line disable comments for all three setters, or (b) a block disable). Pick option (a) to match the rest of the file's style. Preserve the existing `// Reason: subscribe-and-reflect — dialog visibility mirrors external inSessionView; ...` comment above the `setLearnDialogOpen` line; add a parallel `// Reason:` block above `setResetDialogOpen` if it does not already have one (and one above the new `setSettingsDialogOpen` line per the snippet above).

       Do NOT add a second `useEffect` block (D-17 — auto-close behaviour stays consolidated). The `[inSessionView]` dependency array is unchanged.

    4. Callbacks — add after the existing `onLearnClose` block (~line 421). Two new useCallback declarations mirroring `onLearnClick` / `onLearnClose` verbatim with Learn → Settings substitution:
       - `const onSettingsClick = useCallback(() => { if (inSessionView) return; setSettingsDialogOpen(true) }, [inSessionView])`
       - `const onSettingsClose = useCallback(() => { setSettingsDialogOpen(false) }, [])`

    5. SettingsAnchor render — inside the existing `<div className="relative w-full">` block (App.tsx:579), as a sibling of `<LearnAnchor disabled={inSessionView} onClick={onLearnClick} />` (App.tsx:586). Add `<SettingsAnchor disabled={inSessionView} onClick={onSettingsClick} />` immediately before the LearnAnchor render (D-07 — Settings on the left, Learn on the right; symmetric placement). The order in source matches the left-to-right visual order.

    6. SettingsDialog render — at the dialog mount block (App.tsx:641-653). Add `<SettingsDialog open={settingsDialogOpen} onClose={onSettingsClose} inSessionView={inSessionView} />` after the existing `<LearnDialog open={learnDialogOpen} onClose={onLearnClose} />` render (~line 653) and before `</main>` (line 654). Add a JSX comment above the new render: `{/* Phase 15 INFRA-04: SettingsDialog shell — hosts ThemePicker/VariantPicker/TimbrePicker/LanguagePicker stubs. */}`.
  </behavior>
  <action>
    Apply the six numbered edits from `<behavior>` to `src/app/App.tsx`. Use the Edit tool with byte-precise old/new strings sourced from the file (read each region from `<read_first>` first to capture surrounding context for unique matches). Do NOT add any unrelated changes — Phase 15 D-16 limits App.tsx edits to the SettingsAnchor + SettingsDialog wiring.

    For the WR-09 effect (Edit 3), use a single Edit tool call that replaces the existing effect body with the extended version including all three setters annotated. Do NOT split into multiple edits that could leave the file in an intermediate state where the eslint-disable comment is misaligned.

    After the edits, run `npx tsc --noEmit` to catch any type errors (e.g. missing prop, wrong import path). Then run `npx eslint src/app/App.tsx` to catch any lint regressions (e.g. missing eslint-disable annotation, exhaustive-deps violation on the new useCallback `[inSessionView]` array).

    Critical reminders:
    - Do NOT add prefs-related state (D-05 — `settingsDialogOpen` is the ONLY new state).
    - Do NOT add a `useEffect` that reads `loadPrefs()` at App level (pickers self-read; D-02 + D-06).
    - Do NOT create a second auto-close `useEffect` (D-17 — extend the existing one).
    - Do NOT change `useCallback` dependency arrays of unrelated callbacks.
    - Do NOT touch `src/domain/settings.ts`, `src/storage/prefs.ts`, or `vitest.setup.ts` (D-16).
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx eslint src/app/App.tsx && npm run build && npm run test:run</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "import { SettingsAnchor } from '../components/SettingsAnchor'" src/app/App.tsx` returns 1.
    - `grep -c "import { SettingsDialog } from '../components/SettingsDialog'" src/app/App.tsx` returns 1.
    - `grep -c "settingsDialogOpen, setSettingsDialogOpen" src/app/App.tsx` returns 1 (useState declaration).
    - `grep -c "useState<boolean>(false)" src/app/App.tsx` returns at least the original count + 1 (one new boolean state).
    - `grep -c "onSettingsClick" src/app/App.tsx` returns at least 2 (declaration + JSX prop).
    - `grep -c "onSettingsClose" src/app/App.tsx` returns at least 2 (declaration + JSX prop).
    - `grep -c "setSettingsDialogOpen(false)" src/app/App.tsx` returns at least 2 (WR-09 effect + onSettingsClose callback).
    - `grep -c "setSettingsDialogOpen(true)" src/app/App.tsx` returns 1 (onSettingsClick callback).
    - `grep -c "<SettingsAnchor disabled={inSessionView} onClick={onSettingsClick}" src/app/App.tsx` returns 1.
    - `grep -c "<SettingsDialog open={settingsDialogOpen} onClose={onSettingsClose} inSessionView={inSessionView}" src/app/App.tsx` returns 1.
    - `grep -c "useEffect" src/app/App.tsx` returns the same count as HEAD (no new useEffect added — D-17). Pre-run baseline: `grep -c "useEffect" src/app/App.tsx` on HEAD before this task; post-run must equal that count.
    - SettingsAnchor renders inside the `<div className="relative w-full">` block adjacent to LearnAnchor — `awk '/<div className="relative w-full">/,/<\/div>/' src/app/App.tsx | grep -c "<SettingsAnchor"` returns 1 AND the same awk pipe `grep -c "<LearnAnchor"` returns 1 (both anchors in the same div).
    - In source order, `<SettingsAnchor` appears BEFORE `<LearnAnchor` inside the breathing-column div (D-07 left-right visual order — `awk '/<SettingsAnchor/{print NR; exit}' src/app/App.tsx` < `awk '/<LearnAnchor/{print NR; exit}' src/app/App.tsx`).
    - WR-09 effect contains all three setters — `awk '/^  useEffect\(\(\) => \{$/,/^  }, \[inSessionView\]\)$/' src/app/App.tsx | grep -c "setSettingsDialogOpen"` returns 1.
    - WR-09 effect has per-line ESLint disable annotations — `awk '/setSettingsDialogOpen\(false\)/' src/app/App.tsx | head -1` and preceding 1 line contains `eslint-disable-next-line react-hooks/set-state-in-effect` (Landmine 6 policy).
    - `grep -c "from '../domain/settings'" src/app/App.tsx` returns the same count as HEAD (no new domain-settings import — D-16).
    - `grep -c "loadPrefs\\|savePrefs" src/app/App.tsx` returns 0 (D-05 / D-06 — App.tsx does NOT touch prefs storage).
    - `git diff package.json package-lock.json` returns empty (D-15).
    - `npx tsc --noEmit` exits 0.
    - `npx eslint src/app/App.tsx` exits 0.
    - `npm run build` exits 0.
    - `npm run test:run` exits 0; test count ≥ 438 baseline + 28 new Phase 15 tests (Plan 01: 7, Plan 02: 12, Plan 03: 9).
  </acceptance_criteria>
  <done>
    App.tsx wires SettingsAnchor + SettingsDialog with byte-precise edits at six sites; type-check, lint, build, and full test suite all green.
  </done>
</task>

<task type="auto">
  <name>Task 2: Phase-close documentation — REQUIREMENTS.md INFRA-04 Done, STATE.md advance, ROADMAP.md Phase 15 Complete, 15-SUMMARY.md</name>
  <files>.planning/REQUIREMENTS.md, .planning/STATE.md, .planning/ROADMAP.md, .planning/phases/15-settingsdialog-shell/15-SUMMARY.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (lines 95-128 — Traceability table; line 103 currently `| INFRA-04 | Phase 15 | Pending |`; line 136 last-updated footer)
    - .planning/STATE.md (full file — frontmatter progress block + Current Position section + Performance Metrics By Phase table)
    - .planning/ROADMAP.md (lines 45-55 — v1.1 phase list; lines 80-91 — Phase 15 detail block; lines 161-167 — per-phase Progress table)
    - .planning/phases/14-prefs-foundation/14-01-SUMMARY.md (template for tone/structure of Phase 15 SUMMARY)
    - @$HOME/.claude-personal-personal/get-shit-done/templates/summary.md (canonical summary template)
    - .planning/phases/15-settingsdialog-shell/15-VERIFICATION.md (if /gsd-verify-work has already been run; otherwise reference the per-plan SUMMARYs from Plans 01-03 as inputs)
    - .planning/phases/15-settingsdialog-shell/15-01-SUMMARY.md (from Plan 01)
    - .planning/phases/15-settingsdialog-shell/15-02-SUMMARY.md (from Plan 02)
    - .planning/phases/15-settingsdialog-shell/15-03-SUMMARY.md (from Plan 03)
  </read_first>
  <behavior>
    Four files updated in this task (atomic commit):

    1. `.planning/REQUIREMENTS.md` — single-row table edit. Change `| INFRA-04 | Phase 15 | Pending |` (line ~103) to `| INFRA-04 | Phase 15 | Done |`. Update the footer line `*Last updated: 2026-05-12 — Phase 14 INFRA-01/02/03 traceability flipped to Done*` to `*Last updated: 2026-05-12 — Phase 15 INFRA-04 traceability flipped to Done*`.

    2. `.planning/STATE.md` — frontmatter + body updates:
       - `status: planning` → `status: in_progress` (or keep `planning` if more phases pending; check existing convention — Phase 14 SUMMARY shows `status: planning` was kept at v1.1 progression so retain `planning` for v1.1 continuation).
       - `stopped_at: Phase 15 context gathered (CONTEXT.md + DISCUSSION-LOG.md)` → `stopped_at: Phase 15 verified PASS — INFRA-04 Done, ready for Phase 16`.
       - `last_updated:` to ISO timestamp for 2026-05-12.
       - `last_activity:` mirror the new stopped_at content.
       - `progress.completed_phases: 2` → `3`.
       - `progress.total_plans: 2` → `6` (Phase 13: 1, Phase 14: 1, Phase 15: 4).
       - `progress.completed_plans: 2` → `6`.
       - `progress.percent: 100` (within v1.1 milestone counted to-date) — recompute as `completed_phases / total_phases` × 100 using `total_phases: 7` from existing frontmatter → 3/7 = 43% (round to integer). Update accordingly.
       - Body `Current Position` block: update Phase number/status and `Next command:` to `Phase 16 (Themes) planning: /gsd-plan-phase 16`.
       - `Progress: ██░░░...` bar reflecting 43%.
       - `Performance Metrics > By Phase (v1.1)` table: flip Phase 15 row from `Not started` to `Complete`; record `Plans` count as `4`.
       - Add a `Pending Todos` entry if any carry-forward items emerged from Phase 15 verification (e.g. manual focus-trap smoke per VALIDATION.md Manual-Only table — if /gsd-verify-work has recorded these, link them).

    3. `.planning/ROADMAP.md` — three edits:
       - Line 49 (the v1.1 phase list checkbox): `- [ ] **Phase 15: SettingsDialog Shell** - Native <dialog> ...` → `- [x] **Phase 15: SettingsDialog Shell** - Native <dialog> gear-triggered settings panel with stub pickers; inSessionView disable contract (completed 2026-05-12)`.
       - Lines 80-91 (Phase Details block): update `**Plans**: TBD` → `**Plans**: 4 plans` (or `4 plans (4/4) — COMPLETE` to mirror Phase 14's verbose form). List the four PLAN.md filenames as bullets under Plans: `15-01-PLAN.md — SettingsAnchor`, `15-02-PLAN.md — Picker stubs`, `15-03-PLAN.md — SettingsDialog shell`, `15-04-PLAN.md — App.tsx integration + phase close`.
       - Line 163 (Progress table row): `| 15. SettingsDialog Shell | v1.1 | 0/? | Not started | - |` → `| 15. SettingsDialog Shell | v1.1 | 4/4 | Complete | 2026-05-12 |`.

    4. `.planning/phases/15-settingsdialog-shell/15-SUMMARY.md` — NEW file per canonical summary template (@$HOME/.claude-personal-personal/get-shit-done/templates/summary.md). Required sections:
       - Frontmatter: `phase: 15-settingsdialog-shell`, `completed: 2026-05-12`, `plans_executed: [15-01, 15-02, 15-03, 15-04]`, `requirements_closed: [INFRA-04]`, `next_phase: 16-themes`.
       - **What Shipped** — bullet list summarising the 6 new component files + 6 new test files + App.tsx wiring + REQUIREMENTS/STATE/ROADMAP updates. Cite locked-copy strings actually shipped (D-18). Cite total new tests: 28 (Plan 01: 7, Plan 02: 12, Plan 03: 9, Plan 04: 0).
       - **Decisions Honoured** — quick recap of D-01..D-18 with a one-line confirmation per decision (or cluster them; planner decides whether per-decision or per-area).
       - **Success Criteria Verification** — five-row table mapping SC1..SC5 to the artifact/test/manual-smoke that closes it.
       - **Carry-Forward** — Manual smoke tasks per VALIDATION.md Manual-Only table (focus-trap, focus-return on close, gear-icon visual rendering). Note that real-browser verification is expected at /gsd-verify-work or before /gsd-milestone-close.
       - **Files Changed** — `git diff --name-only origin/main..HEAD` style list (planner can leave a structured bullet list; executor will populate).
       - **Phase Metrics** — total tests added, total LOC added (approximate), commits in this phase.
       - **Operator Next Steps** — `/gsd-verify-work 15` (if not already run) and `/gsd-plan-phase 16` (Themes).
  </behavior>
  <action>
    Execute the four file updates as listed in `<behavior>`. For REQUIREMENTS.md and ROADMAP.md use the Edit tool with byte-precise old/new strings. For STATE.md, recompute frontmatter math (completed_phases, total_plans, completed_plans, percent) BEFORE writing — do not approximate. For 15-SUMMARY.md, use the Write tool with the structure described in `<behavior>`; pull factual content (test counts, file paths, locked strings) from the Plan 01/02/03 SUMMARY files if present, else from the corresponding PLAN.md must_haves.

    Critical: this task assumes Plans 01-03 have already executed and their per-plan SUMMARY files exist. If a per-plan SUMMARY is missing, surface the gap to the operator before writing 15-SUMMARY.md — do NOT fabricate content. (At /gsd-execute-phase time, each plan's task ends by writing its SUMMARY, so this should hold.)
  </action>
  <verify>
    <automated>grep -c "| INFRA-04 | Phase 15 | Done |" .planning/REQUIREMENTS.md && grep -c "Phase 15.*Complete" .planning/ROADMAP.md && test -f .planning/phases/15-settingsdialog-shell/15-SUMMARY.md && npm run test:run</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "| INFRA-04 | Phase 15 | Done |" .planning/REQUIREMENTS.md` returns 1.
    - `grep -c "| INFRA-04 | Phase 15 | Pending |" .planning/REQUIREMENTS.md` returns 0.
    - REQUIREMENTS.md footer last-updated line mentions Phase 15: `grep -c "Phase 15 INFRA-04" .planning/REQUIREMENTS.md` returns at least 1.
    - STATE.md frontmatter `completed_phases: 3` present: `grep -c "completed_phases: 3" .planning/STATE.md` returns 1.
    - STATE.md frontmatter `completed_plans: 6` present: `grep -c "completed_plans: 6" .planning/STATE.md` returns 1.
    - STATE.md frontmatter `total_plans: 6` present: `grep -c "total_plans: 6" .planning/STATE.md` returns 1.
    - STATE.md `Phase 15` row in the By Phase table flipped to `Complete`: `grep -E "15\\. SettingsDialog Shell.*Complete" .planning/STATE.md` returns at least 1 match.
    - STATE.md `Next command:` line mentions Phase 16: `grep -c "Phase 16" .planning/STATE.md` returns at least 1.
    - ROADMAP.md Phase 15 checkbox flipped: `grep -c "- \[x\] \*\*Phase 15: SettingsDialog Shell\*\*" .planning/ROADMAP.md` returns 1.
    - ROADMAP.md Phase 15 Progress table row: `grep -E "15\\. SettingsDialog Shell .* 4/4 .* Complete .* 2026-05-12" .planning/ROADMAP.md` returns 1.
    - ROADMAP.md Phase 15 detail block has plans count: `grep -c "Plans\\*\\*: 4 plans" .planning/ROADMAP.md` returns at least 1 (or the verbose 4/4 form).
    - `.planning/phases/15-settingsdialog-shell/15-SUMMARY.md` exists.
    - 15-SUMMARY.md contains `INFRA-04`, the five SC labels (`SC1`/`SC2`/`SC3`/`SC4`/`SC5`), the four plan IDs (`15-01`, `15-02`, `15-03`, `15-04`), and all four D-18 locked stub strings (`Theme: system`, `Variant: orb`, `Timbre: bowl`, `Language: en`) — `grep -c "INFRA-04\\|SC1\\|SC2\\|SC3\\|SC4\\|SC5\\|15-01\\|15-02\\|15-03\\|15-04\\|Theme: system\\|Variant: orb\\|Timbre: bowl\\|Language: en" .planning/phases/15-settingsdialog-shell/15-SUMMARY.md` returns at least 13 matches.
    - 15-SUMMARY.md mentions all five SC labels at least once each: `for sc in SC1 SC2 SC3 SC4 SC5; do grep -c $sc .planning/phases/15-settingsdialog-shell/15-SUMMARY.md; done` returns five lines each ≥ 1.
    - `npm run test:run` exits 0 (no regression from doc-only changes).
    - `git diff package.json package-lock.json` returns empty (D-15).
    - Full green-gate (`npx tsc --noEmit && npm run lint && npm run build && npm run test:run`) exits 0 (D-14 final commit boundary).
  </acceptance_criteria>
  <done>
    REQUIREMENTS, STATE, ROADMAP updated to reflect Phase 15 completion; 15-SUMMARY.md exists with all required sections; full green-gate green.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| App.tsx state (`settingsDialogOpen`) → SettingsDialog `open` prop | Pure React parent→child prop wiring |
| App.tsx state (`inSessionView` derived from `appPhase`) → SettingsAnchor `disabled` prop AND SettingsDialog `inSessionView` prop | Shared session-state contract: anchor disable + dialog auto-close + picker disable threading all depend on the same boolean |
| User click on `<SettingsAnchor>` → `onSettingsClick` → setSettingsDialogOpen(true) | Standard React event-handler boundary; gated by `inSessionView` early-return |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-15 | T (Tampering) | `onSettingsClick` open-guard vs SettingsAnchor JSX gate | mitigate | Two-layer defence (D-08): SettingsAnchor's `onClick={disabled ? undefined : onClick}` removes the handler at the JSX layer; if somehow invoked (programmatic dispatchEvent), the App-level `if (inSessionView) return` early-return inside `onSettingsClick` short-circuits the state mutation. Both layers required — Plan 01 ships the JSX gate, this plan ships the App-level guard. |
| T-15-16 | T (Tampering) | WR-09 effect race — dialog open at session start | mitigate | D-12 / D-17 auto-close: the extended WR-09 effect calls `setSettingsDialogOpen(false)` when `inSessionView` flips true. Landmine 3 (lead-in timer firing while dialog open) is handled by React's batched state updates + the dialog component's `if (!open && dialog.open)` guard at SettingsDialog.tsx open-effect. |
| T-15-17 | I (Information Disclosure) | New JSX comment `{/* Phase 15 INFRA-04: ... */}` in App.tsx | accept | Comment names a requirement ID and a component name; no PII, no secret, no security-sensitive surface area. Build output strips JSX comments. |
| T-15-18 | E (Elevation of Privilege) | Picker `disabled={inSessionView}` threading via SettingsDialog | mitigate | Plan 03's SettingsDialog renders each picker with `disabled={inSessionView}`. This plan's App.tsx wiring passes the live `inSessionView` boolean. Loss of contract (e.g. accidentally passing `false`) would break SC3; acceptance_criteria grep on the literal JSX line catches the regression. |
| T-15-19 | D (DoS) | `setSettingsDialogOpen` triggers re-render of four picker children on every open transition | accept | React reconciliation handles this; pickers' `loadPrefs()` is two synchronous ops on a 4-field object (Landmine 5). User open-rate is human-scale (one open per intent), not pathological. |
| T-15-20 | S (Spoofing) | Phase-close documentation truthfulness | mitigate | 15-SUMMARY.md is bound to the per-plan SUMMARYs (factual provenance); REQUIREMENTS.md status flip is verified by acceptance_criteria grep; STATE.md frontmatter math is recomputed deterministically. Doc drift is prevented by `/gsd-verify-work` running structural checks against PLAN.md frontmatter. |
</threat_model>

<verification>
Plan 04 end-of-phase verification:
- App.tsx wiring is the smallest possible delta (6 byte-precise edits at documented line anchors).
- All five Phase 15 Success Criteria are closed end-to-end after this plan ships (SC1 visible gear + disabled-in-session, SC2 dialog opens via gear with native focus return, SC3 inSessionView → dialog → pickers, SC4 native `<dialog>` + zero new deps, SC5 per-commit green-gate).
- REQUIREMENTS.md INFRA-04 flipped to Done.
- STATE.md advanced; ROADMAP.md Phase 15 marked Complete.
- 15-SUMMARY.md exists summarising the four plans with provenance to per-plan SUMMARYs.
- Manual smoke verifications (focus-trap, focus-return, gear icon visual) per VALIDATION.md Manual-Only table are flagged as carry-forward for `/gsd-verify-work 15` or `/gsd-milestone-close`.
</verification>

<success_criteria>
- [ ] `src/app/App.tsx` modified with six byte-precise edits (imports, state, WR-09 extension, callbacks, SettingsAnchor render, SettingsDialog render).
- [ ] Phase 15 SC1, SC2 (testable portion), SC3, SC4, SC5 closed end-to-end at the App layer.
- [ ] `.planning/REQUIREMENTS.md` INFRA-04 row flipped from Pending to Done; footer updated.
- [ ] `.planning/STATE.md` frontmatter math recomputed and body advanced to Phase 16 next.
- [ ] `.planning/ROADMAP.md` Phase 15 marked Complete in both the v1.1 phase list and the Progress table; 4-plan count recorded.
- [ ] `.planning/phases/15-settingsdialog-shell/15-SUMMARY.md` exists with all required sections (What Shipped, Decisions Honoured, SC Verification, Carry-Forward, Files Changed, Phase Metrics, Operator Next Steps).
- [ ] Full green-gate (`npx tsc --noEmit && npm run lint && npm run build && npm run test:run`) exits 0 (D-14 final commit boundary); total tests ≥ 438 + 28 Phase 15 cases.
- [ ] No new npm dependencies (D-15); no edits to `src/domain/settings.ts` or `src/storage/prefs.ts` (D-16).
</success_criteria>

<output>
After completion, ensure `.planning/phases/15-settingsdialog-shell/15-SUMMARY.md` is the canonical phase-close summary (created in Task 2). Plan 04 does NOT produce a separate `15-04-SUMMARY.md` — the phase summary aggregates Plans 01-04 and replaces a per-plan-04 summary.
</output>
