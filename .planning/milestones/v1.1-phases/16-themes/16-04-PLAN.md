---
phase: 16
plan: 04
type: execute
wave: 2
depends_on:
  - "16-01"
  - "16-02"
  - "16-03"
files_modified:
  - src/components/ThemePicker.tsx
  - src/components/ThemePicker.test.tsx
  - src/app/App.tsx
  - .planning/REQUIREMENTS.md
  - .planning/STATE.md
  - .planning/ROADMAP.md
autonomous: false
requirements:
  - THEME-01
  - THEME-02
  - THEME-03
  - THEME-04
  - THEME-05
user_setup: []

must_haves:
  truths:
    - "ThemePicker renders a radiogroup with 6 native <button role='radio'> options labelled Light / Dark / System / Moss / Slate / Dusk (D-22 verbatim)"
    - "Clicking an option calls useThemeChoice().setTheme(id) which writes savePrefs + dispatches 'hrv:prefs-changed'; useTheme (App) listens and updates <html data-theme>"
    - "The currently-selected option carries aria-checked='true' + 2px accent border + bg-soft fill + accent-strong text (UI-SPEC §3 selected-state contract)"
    - "When { disabled: true } (Phase 15 D-02 prop interface), all 6 option buttons render the disabled attribute and the radiogroup carries aria-disabled='true'; selected option keeps its highlight"
    - "App.tsx invokes useTheme() exactly once inside the function App() body; the return value is not consumed (hook called for side effect)"
    - "ThemePicker.test.tsx replaces the 3 stub tests with ≥ 7 tests covering: 6 options rendered, section label, aria-checked reflects stored theme, click writes savePrefs, click dispatches 'hrv:prefs-changed', disabled gating disables all buttons + sets aria-disabled, disabled click no-op (no savePrefs write)"
    - "Phase close artifacts: REQUIREMENTS.md THEME-01..05 traceability flipped to Done; STATE.md updated with phase completion + next-up Phase 17; ROADMAP.md Phase 16 marked [x] with plan count + per-plan checkboxes; per-phase 16-SUMMARY.md created summarizing all four plans"
    - "D-19: Phase 15 D-01 picker contract preserved — src/components/SettingsDialog.tsx is NOT edited in Phase 16; only ThemePicker.tsx body is touched, leaving Phase 17/18/19 free to fill their pickers in parallel"
    - "D-17: per-commit green-gate — `npx tsc --noEmit && npm run lint && npm run build && npm test` exits 0 at every commit boundary"
    - "D-18: zero new npm dependencies"
  artifacts:
    - path: "src/components/ThemePicker.tsx"
      provides: "Radiogroup picker over THEME_OPTIONS; consumes useThemeChoice; preserves { disabled: boolean } prop interface (D-20)"
      exports: ["ThemePicker", "ThemePickerProps"]
      contains: "role=\"radiogroup\", role=\"radio\", aria-checked, useThemeChoice"
      min_lines: 40
    - path: "src/components/ThemePicker.test.tsx"
      provides: "Replacement of the 3 Phase 15 stub tests with full picker coverage"
      exports: ["(no exports — vitest test file)"]
      min_lines: 80
    - path: "src/app/App.tsx"
      provides: "useTheme() invocation added to App body"
      contains: "useTheme()"
    - path: ".planning/phases/16-themes/16-SUMMARY.md"
      provides: "Phase-close artifact summarizing plans 01-04 outcomes"
      contains: "Phase 16, THEME-01"
  key_links:
    - from: "src/components/ThemePicker.tsx"
      to: "src/hooks/useThemeChoice.ts"
      via: "import { useThemeChoice } from '../hooks/useThemeChoice'; consume { theme, setTheme }"
      pattern: "useThemeChoice\\(\\)"
    - from: "src/components/ThemePicker.tsx"
      to: "src/domain/settings.ts"
      via: "import { THEME_OPTIONS, type ThemeId } from '../domain/settings'; iterate for options"
      pattern: "THEME_OPTIONS\\.map"
    - from: "src/app/App.tsx"
      to: "src/hooks/useTheme.ts"
      via: "import { useTheme } from '../hooks/useTheme'; invoke as useTheme() inside App body"
      pattern: "useTheme\\(\\)"
    - from: "Click flow"
      to: "<html data-theme> attribute"
      via: "user click → useThemeChoice.setTheme → savePrefs + dispatchEvent → useTheme listener → setTheme state → apply effect writes documentElement.dataset.theme"
      pattern: "useThemeChoice|useTheme"
---

<objective>
Close Phase 16 by:

1. **Replace the ThemePicker stub** (`src/components/ThemePicker.tsx`) with the real picker: 6 native `<button role="radio">` options inside a `<div role="radiogroup">`, consuming `useThemeChoice()` from plan 02. Preserves the Phase 15 D-02 `{ disabled: boolean }` prop interface (D-20).

2. **Rewrite the ThemePicker tests** (`src/components/ThemePicker.test.tsx`) to cover the radiogroup pattern + click side-effects + disabled gating. The Phase 15 stub had 3 tests (`Theme: system` text); those tests are replaced by ≥ 7 new tests.

3. **Wire `useTheme()` into App.tsx** with a single new line of `useTheme()` invocation inside the `function App()` body (and one new import line). NO new useState, NO new JSX, NO new effect — the hook is self-contained and called for side effect (CONTEXT.md item 6).

4. **Phase close artifacts**: update `.planning/REQUIREMENTS.md` to flip THEME-01..05 traceability to Done. Update `.planning/STATE.md` with the phase-complete status + next-up (Phase 17). Update `.planning/ROADMAP.md` Phase 16 entry: mark `[x]`, set plan count to 4, list 16-01..16-04 with brief objectives. Create `.planning/phases/16-themes/16-SUMMARY.md` summarizing all four plans.

Purpose: This is the wire-up plan. THEME-01 (user can choose Light/Dark/System), THEME-03 (3 named palettes selectable), and THEME-04 (theme applies on click + persists) become observable in the browser only after this plan lands. THEME-02 (System tracks OS) requires `useTheme()` to be invoked from App — the gated mql listener cannot fire until the App-side hook is mounted. THEME-05 was already enforced automatically by plan 01's contrast test, but the full picker test surface in this plan additionally verifies that the click path does not bypass the contrast guard (no per-option overrides land).

This plan is **NOT autonomous**: it ends with a `checkpoint:human-verify` task because Phase 16 ships a visible UI surface with new aesthetic decisions across 5 palettes that benefit from human visual review (CONTEXT.md D-13 notes manual UAT is informal/non-blocking, but for a user-customization feature shipping 5 new palettes a short visual sanity check before phase close prevents shipping a contrast-passing-but-aesthetically-awkward Dark or Dusk palette).

Output: 2 edited TS files (ThemePicker.tsx + ThemePicker.test.tsx), 1 edited TS file (App.tsx single line), 3 edited planning docs (REQUIREMENTS, STATE, ROADMAP), 1 new planning doc (16-SUMMARY.md), 1 checkpoint task.
</objective>

<execution_context>
@$HOME/.claude-personal-personal/get-shit-done/workflows/execute-plan.md
@$HOME/.claude-personal-personal/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/16-themes/16-CONTEXT.md
@.planning/phases/16-themes/16-RESEARCH.md
@.planning/phases/16-themes/16-UI-SPEC.md
@.planning/phases/16-themes/16-PATTERNS.md
@.planning/phases/16-themes/16-01-PLAN.md
@.planning/phases/16-themes/16-02-PLAN.md
@.planning/phases/16-themes/16-03-PLAN.md

@src/components/ThemePicker.tsx
@src/components/ThemePicker.test.tsx
@src/components/SettingsDialog.tsx
@src/components/SettingsStepper.tsx
@src/components/ResetStatsDialog.tsx
@src/components/ResetStatsDialog.test.tsx
@src/hooks/useTheme.ts
@src/hooks/useThemeChoice.ts
@src/app/App.tsx
@src/domain/settings.ts

<interfaces>
<!-- Hooks shipped by plan 02 (this plan consumes both). -->

From src/hooks/useTheme.ts (plan 02 output):
  export function useTheme(): { theme: ThemeId; setTheme: (next: ThemeId) => void }
  // App-side orchestrator. Side effects: <html data-theme> apply, gated matchMedia, storage listener, prefs-changed listener.
  // Called once in App.tsx; return value not consumed.

From src/hooks/useThemeChoice.ts (plan 02 output):
  export function useThemeChoice(): { theme: ThemeId; setTheme: (next: ThemeId) => void }
  // Picker-side companion. setTheme(id) writes savePrefs + dispatches 'hrv:prefs-changed' CustomEvent.
  // Called from ThemePicker; theme is mirrored locally for optimistic UI.

From src/domain/settings.ts:
  export type ThemeId = 'light' | 'dark' | 'system' | 'moss' | 'slate' | 'dusk'
  export const THEME_OPTIONS = ['light','dark','system','moss','slate','dusk'] as const

From src/components/ThemePicker.tsx (Phase 15 baseline — to be replaced):
  export interface ThemePickerProps {
    disabled: boolean
  }
  export function ThemePicker({ disabled }: ThemePickerProps)

Phase 15 D-02 prop contract (D-20 preservation): only `disabled` may appear in ThemePickerProps. The setter comes from useThemeChoice() inside the component, NOT from props. SettingsDialog.tsx is NOT touched.

UI-SPEC §"Visual & Interaction Specifications" §3 selected-state contract (locked):
  Not selected:    border border-teal-200    bg-white                                            text-teal-800
  Hover:                                     bg-teal-50                                          text-teal-800
  Selected:        border-2 border-[var(--color-breathing-accent)]    bg-[var(--color-breathing-bg-soft)]    text-[var(--color-breathing-accent-strong)]
  Focus-visible:   focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-breathing-accent focus-visible:ring-offset-2
  Disabled:        disabled:cursor-not-allowed disabled:opacity-45

Layout (UI-SPEC §6):
  Container <div>: no fieldset (avoid double-label).
  Label: <p id="theme-picker-label" className="text-sm font-semibold text-slate-900">Theme</p>
  Radiogroup: <div role="radiogroup" aria-labelledby="theme-picker-label" aria-disabled={disabled} className="mt-2 grid grid-cols-3 gap-2">
  Buttons: native <button type="button" role="radio" aria-checked={selected} disabled={disabled} onClick={() => setTheme(id)}>{label}</button>
  Min-height: min-h-12 (48px) → 44×44 hit-area floor satisfied.
  Padding: px-3 py-2 (small for 6 buttons in 3-col grid).
  Transition: transition motion-reduce:transition-none on every button.

Labels (D-22 verbatim):
  light → 'Light'
  dark → 'Dark'
  system → 'System'
  moss → 'Moss'
  slate → 'Slate'
  dusk → 'Dusk'

Capitalization: first-char uppercase (`id.charAt(0).toUpperCase() + id.slice(1)`) — no LABELS map needed unless TS catches a string-coercion issue.

From src/app/App.tsx integration site (line 136-137):
  const audio = useAudioCues(initialMute, onAudioReanchorRequired) // Phase 3 + Plan 06 D-35
  const wakeLock = useWakeLock() // Phase 5: imperative resource — D-11/D-12 (no React state surface)
  // useTheme() goes near this cluster — recommended placement at top of App body, alongside other side-effectful hooks.

From src/components/ResetStatsDialog.test.tsx (test pattern template):
  - renderDialog helper with destructured props
  - userEvent.setup() then user.click(screen.getByRole(...))
  - vi.fn() spies for onConfirm/onCancel; assert with toHaveBeenCalledTimes(1)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Replace src/components/ThemePicker.tsx body with the radiogroup picker (consumes useThemeChoice)</name>
  <files>src/components/ThemePicker.tsx, src/components/ThemePicker.test.tsx</files>
  <read_first>
    - src/components/ThemePicker.tsx (the current 26-line stub — replace body but preserve the file header comment lines 1-8 EXCEPT the D-04 stub language; update the comment to reflect the Phase 16 real picker)
    - src/components/ThemePicker.test.tsx (the current 27-line stub — replace all 3 stub tests with the ≥ 7 new tests; preserve the imports header line 1)
    - src/components/ResetStatsDialog.tsx lines 63-80 (close-button + destructive-button class patterns)
    - src/components/SettingsStepper.tsx lines 42-50, 55-62 (disabled-button class pattern + 44×44 hit-area pattern)
    - src/components/ResetStatsDialog.test.tsx (full file — literal template for click + spy + userEvent.setup pattern)
    - src/hooks/useThemeChoice.ts (the hook this component consumes — confirm the `useThemeChoice(): { theme, setTheme }` signature; created by plan 02)
    - .planning/phases/16-themes/16-UI-SPEC.md §"Visual & Interaction Specifications" §1-7 (radiogroup over <select>/segmented; text label only; aria-checked + filled background + bordered ring for selected; mirror SettingsStepper for disabled; Tab navigation only — no arrow keys; specific layout `grid grid-cols-3 gap-2 mt-2`; instant theme application — no transition on the data-theme swap, 150ms on button background-color only)
    - .planning/phases/16-themes/16-UI-SPEC.md §"Color" §"Accent reserved for" (accent applied to: selected indicator, focus-visible ring; NOT applied to: non-selected buttons, decorative borders)
    - .planning/phases/16-themes/16-CONTEXT.md D-20 (prop interface `{ disabled: boolean }` ONLY), D-22 (locked option labels: Light, Dark, System, Moss, Slate, Dusk verbatim — no description copy, no localized strings)
    - .planning/phases/16-themes/16-PATTERNS.md §"src/components/ThemePicker.tsx" (composite from UI-SPEC + analogs — final composition for executor)
    - src/storage/storage.ts STATE_KEY export (used by tests for direct-disk-read assertion)
  </read_first>
  <behavior>
    `ThemePicker.tsx` final state:
      - File header comment block (top of file) updated to reflect Phase 16:
        - Reference Phase 15 D-01 (one file per dimension; Phase 16 fills body).
        - Reference D-20 (prop interface `{ disabled: boolean }` ONLY — setter from internal useThemeChoice).
        - Reference UI-SPEC radiogroup + aria-checked semantics.
        - Reference D-22 (English-locked labels; Phase 19 owns translation).
      - Imports: `{ THEME_OPTIONS, type ThemeId } from '../domain/settings'`, `{ useThemeChoice } from '../hooks/useThemeChoice'`. No other imports needed (no react imports — JSX doesn't require it in TS 5+).
      - `ThemePickerProps` interface: ONLY `disabled: boolean` (D-20 invariant). NO new prop.
      - `ThemePicker` function body:
        - Call `useThemeChoice()` → destructure `{ theme, setTheme }`.
        - Return JSX:
          - Outer `<div>` (NOT `<fieldset>`).
          - `<p id="theme-picker-label" className="text-sm font-semibold text-slate-900">Theme</p>` (verbatim from Phase 15 D-18 stub).
          - `<div role="radiogroup" aria-labelledby="theme-picker-label" aria-disabled={disabled} className="mt-2 grid grid-cols-3 gap-2">`.
          - `THEME_OPTIONS.map((id) => ...)` rendering one `<button>` per id.
        - Each `<button>`:
          - `key={id}`.
          - `type="button"` (prevent default form-submit semantics).
          - `role="radio"`.
          - `aria-checked={theme === id}`.
          - `disabled={disabled}`.
          - `onClick={() => { setTheme(id) }}` (arrow with statement body — matches existing codebase style).
          - `className={...}` — composition of: base classes (`min-h-12 rounded-full px-3 py-2 text-sm font-semibold shadow-sm transition motion-reduce:transition-none focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-breathing-accent focus-visible:ring-offset-2 disabled:cursor-not-allowed disabled:opacity-45`); selected-state classes (`border-2 border-[var(--color-breathing-accent)] bg-[var(--color-breathing-bg-soft)] text-[var(--color-breathing-accent-strong)]`) OR not-selected classes (`border border-teal-200 bg-white text-teal-800 hover:bg-teal-50 active:bg-teal-100`).
          - Children: the label string — derived as `id.charAt(0).toUpperCase() + id.slice(1)` (Light/Dark/System/Moss/Slate/Dusk).

    `ThemePicker.test.tsx` final state:
      - Imports: `'@testing-library/jest-dom/vitest'`, `{ render, screen } from '@testing-library/react'`, `userEvent from '@testing-library/user-event'`, `{ afterEach, beforeEach, describe, expect, it, vi } from 'vitest'`, `{ ThemePicker } from './ThemePicker'`, `{ STATE_KEY } from '../storage'`, `type { ThemeId } from '../domain/settings'`.
      - `beforeEach(() => { window.localStorage.clear() })` + `afterEach(() => { window.localStorage.clear(); vi.restoreAllMocks() })`.
      - Helper: `seedTheme(theme: ThemeId)` writes `{ version: 1, prefs: { theme, timbre: 'bowl', variant: 'orb', locale: 'en' } }` to STATE_KEY.
      - Test cases (≥ 7):
        1. Renders the 'Theme' section label.
        2. Renders all 6 options as `getAllByRole('radio')` with labels `Light`, `Dark`, `System`, `Moss`, `Slate`, `Dusk` (exact case + order).
        3. The currently-stored theme has `aria-checked='true'` and all other 5 have `aria-checked='false'`. Seed with `theme: 'moss'`; assert.
        4. Clicking an unselected option calls `savePrefs` — assert via direct-disk-read of STATE_KEY after `await user.click(...)`; expect `JSON.parse(localStorage.getItem(STATE_KEY)).prefs.theme === clickedThemeId`.
        5. Clicking an option dispatches `'hrv:prefs-changed'` CustomEvent. Attach `vi.fn()` listener via `window.addEventListener('hrv:prefs-changed', spy)` BEFORE click; assert `spy` was called once with detail `{ key: 'theme', value: <clickedId> }`. Cleanup listener in afterEach via captured ref or top-of-test attach.
        6. When `disabled=true`, all 6 buttons have the `disabled` attribute (`expect(button).toBeDisabled()` for each); the radiogroup has `aria-disabled='true'`.
        7. When `disabled=true`, clicking an option does NOT call savePrefs — assert disk envelope unchanged after `await user.click(...)`.
      - (Optional 8th test if budget permits) Active option retains its highlight even when disabled (UI-SPEC §4) — assert via class list inspection of the currently-checked option after `disabled=true` render.
  </behavior>
  <action>
    Step 1 — Edit `src/components/ThemePicker.tsx`: replace the entire file body (preserving only the file path comment at the top). Use the exact JSX structure described in <behavior>. Composition:

      - File-top comment block: ~6 lines documenting Phase 16 D-01/D-19/D-20/D-22 contract preservation.
      - Imports (3 lines): THEME_OPTIONS + ThemeId from settings; useThemeChoice from hooks.
      - `export interface ThemePickerProps { disabled: boolean }` (UNCHANGED from stub — D-20).
      - `export function ThemePicker({ disabled }: ThemePickerProps) { ... }`.

    For the className composition: declare a `baseClasses` string constant ONCE inside the map, then a ternary for selected vs not-selected. Keep the className expression on a single multi-line template literal or array.join(' ') — pick whichever the existing codebase style prefers (SettingsStepper uses a single-line string; ResetStatsDialog uses a single-line string). Recommendation: use a single template-literal string per button with conditional class fragments — minimizes diff churn for future palette tweaks.

    Step 2 — Edit `src/components/ThemePicker.test.tsx`: delete the existing 3 stub tests (the entire describe block at lines 11-27). Replace with the new test suite per <behavior> test cases.

    For each test, use the existing codebase patterns:
      - `userEvent.setup()` returns the user instance per test (NOT a module-level setup; matches ResetStatsDialog.test.tsx:46-47).
      - `screen.getAllByRole('radio')` returns all 6 buttons; assert `.toHaveLength(6)` and label content via `Array.from(buttons).map(b => b.textContent)`.
      - For disabled assertion: `screen.getAllByRole('radio').forEach(button => expect(button).toBeDisabled())`.
      - For aria-disabled assertion: `expect(screen.getByRole('radiogroup')).toHaveAttribute('aria-disabled', 'true')`.
      - For CustomEvent spy: declare the spy outside `act`; attach `window.addEventListener` BEFORE the user.click; assert via `spy.mock.calls[0][0]` (narrowed to CustomEvent via `instanceof`).
      - For disk-state assertion: `JSON.parse(window.localStorage.getItem(STATE_KEY)!).prefs.theme` (Phase 14 envelope shape; cast non-null because seedTheme guarantees it's set).

    Step 3 — Run `npx vitest run src/components/ThemePicker.test.tsx` and confirm all 7+ tests pass. If a test fails due to ordering (e.g. THEME_OPTIONS list doesn't match the test's expected label list), confirm the test expects ['Light','Dark','System','Moss','Slate','Dusk'] in that exact source order from src/domain/settings.ts.

    Step 4 — Run `npm run test:run` to confirm the full suite passes (Phase 15 baseline 438 + plan 01 5 + plan 02 ≥ 16 + this plan ≥ 7 = ≥ 466 total tests, all green).

    Do NOT:
      - Add a new prop to `ThemePickerProps` beyond `disabled` (D-20 hard invariant).
      - Use a LABELS Record<ThemeId, string> if `id.charAt(0).toUpperCase() + id.slice(1)` works (D-22 explicitly states "ThemeId values capitalized for first character; no display-mapping function"). HOWEVER, if TS or lint complains about the type narrowing of charAt, fall back to a small `const LABELS: Record<ThemeId, string> = { light: 'Light', ... }` inline — RESEARCH §"Picker UI Shape Recommendation" skeleton shows both forms acceptable; the codebase already uses inline-object Records (e.g. STATS_LABELS in StatsFooter).
      - Render any color swatch, icon, or description copy (UI-SPEC §2 — text label only; UI-SPEC §"Copywriting Contract" — no description copy under options).
      - Apply a CSS transition to the `data-theme` swap (UI-SPEC §7 — theme switching is INSTANT; the 150ms transition is on the button's own background-color/border-color, not on the cascade).
      - Touch `src/components/SettingsDialog.tsx` (D-19 hard invariant).
      - Edit `src/domain/settings.ts` or `src/storage/prefs.ts` (D-21 hard invariants).
      - Implement arrow-key navigation inside the radiogroup (UI-SPEC §5 — defer; consistency with existing pickers wins for v1.1).
  </action>
  <verify>
    <automated>npx vitest run src/components/ThemePicker.test.tsx --reporter=verbose 2>&1 | tee /tmp/p16-04-t1-themePicker.log; grep -E "✓" /tmp/p16-04-t1-themePicker.log | wc -l | awk '{ if ($1 >= 7) print "OK"; else print "FAIL"; }' | grep -q "OK"</automated>
    <automated>grep -q "role=\"radiogroup\"" src/components/ThemePicker.tsx && grep -q "role=\"radio\"" src/components/ThemePicker.tsx && grep -q "aria-checked" src/components/ThemePicker.tsx && grep -q "useThemeChoice" src/components/ThemePicker.tsx</automated>
    <automated>grep -E "THEME_OPTIONS\\.map" src/components/ThemePicker.tsx | grep -q "map"</automated>
    <automated>grep -E "interface ThemePickerProps" src/components/ThemePicker.tsx | head -1 && grep -A 2 "interface ThemePickerProps" src/components/ThemePicker.tsx | grep -c ":" | awk '{ if ($1 <= 2) print "OK"; else print "FAIL: extra props"; }' | grep -q "OK"</automated>
    <automated>! grep -E "fieldset" src/components/ThemePicker.tsx</automated>
    <automated>git diff src/components/SettingsDialog.tsx | wc -l | grep -q "^0$"</automated>
    <automated>git diff src/domain/settings.ts src/storage/prefs.ts | wc -l | grep -q "^0$"</automated>
    <automated>npx tsc --noEmit && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/ThemePicker.tsx` exports `ThemePicker` and `ThemePickerProps`; the interface contains EXACTLY one property: `disabled: boolean` (D-20 invariant).
    - The component renders `role="radiogroup"` on its inner div with `aria-labelledby="theme-picker-label"` and `aria-disabled={disabled}`.
    - The component renders exactly 6 `role="radio"` buttons, one per THEME_OPTIONS entry, in source order (light, dark, system, moss, slate, dusk).
    - Each button has `type="button"`, `aria-checked={theme === id}`, `disabled={disabled}`, and `onClick={() => { setTheme(id) }}`.
    - Each button label is the THEME_OPTIONS value with the first character capitalized: `Light`, `Dark`, `System`, `Moss`, `Slate`, `Dusk` (D-22 verbatim).
    - The component imports `useThemeChoice` from `'../hooks/useThemeChoice'` (NOT from props; D-02 preservation).
    - The component imports `THEME_OPTIONS` and `type ThemeId` from `'../domain/settings'`.
    - The component does NOT use `<fieldset>` (UI-SPEC §6 — avoid double-label against aria-labelledby).
    - The component does NOT consume any new prop, context, or imperative ref.
    - The selected-state class fragment includes `border-2 border-[var(--color-breathing-accent)] bg-[var(--color-breathing-bg-soft)] text-[var(--color-breathing-accent-strong)]` (UI-SPEC §3 contract — verified via `grep "border-\\[var(--color-breathing-accent)\\]" src/components/ThemePicker.tsx`).
    - The not-selected-state class fragment includes `border border-teal-200 bg-white text-teal-800 hover:bg-teal-50 active:bg-teal-100` (UI-SPEC §3 contract).
    - Every button has `min-h-12` (44×44 hit-area floor) and `transition motion-reduce:transition-none` and `focus-visible:ring-2 focus-visible:ring-breathing-accent focus-visible:ring-offset-2 disabled:cursor-not-allowed disabled:opacity-45`.
    - `src/components/ThemePicker.test.tsx` contains ≥ 7 `it(...)` test cases (verified `grep -c "^  it(" src/components/ThemePicker.test.tsx` ≥ 7).
    - The test for option count asserts 6 radios with the exact label list `['Light','Dark','System','Moss','Slate','Dusk']` in order.
    - The click-writes-savePrefs test asserts via direct-disk-read of STATE_KEY (NOT via mocking savePrefs).
    - The click-dispatches-event test asserts the CustomEvent detail shape `{ key: 'theme', value: <id> }` (forward-compat for Phase 17/18/19).
    - The disabled-no-write test confirms that clicks on disabled buttons do not change the disk envelope.
    - `npx vitest run src/components/ThemePicker.test.tsx` exits 0 with all tests passing.
    - `git diff src/components/SettingsDialog.tsx` is empty (D-19 invariant).
    - `git diff src/domain/settings.ts src/storage/prefs.ts` is empty (D-21 invariant).
    - `npx tsc --noEmit && npm run lint && npm run build && npm test` all exit 0.
  </acceptance_criteria>
  <done>ThemePicker.tsx renders the radiogroup with 6 options, consumes useThemeChoice for click handling, preserves { disabled: boolean }-only prop interface; ThemePicker.test.tsx covers ≥ 7 behaviors; SettingsDialog.tsx unchanged; per-commit green-gate green.</done>
</task>

<task type="auto">
  <name>Task 2: Wire useTheme() into src/app/App.tsx (single line + import)</name>
  <files>src/app/App.tsx</files>
  <read_first>
    - src/app/App.tsx (full file — observe the import block at lines 1-37 and the function App() body opening at line 42; the new `useTheme()` invocation goes inside the body, recommended placement near the existing hook-invocation cluster at lines 136-137 where `useAudioCues` + `useWakeLock` are called)
    - src/hooks/useTheme.ts (plan 02 output — confirm the named export `useTheme` and that it takes no arguments)
    - .planning/phases/16-themes/16-CONTEXT.md item 6 (single new line of `useTheme()` invocation; return value `{ theme, setTheme }` NOT consumed by App; hook owns its internal state — App does NOT add useState; Phase 15 D-05 minimal-App-state posture approximately preserved)
    - .planning/phases/16-themes/16-PATTERNS.md §"src/app/App.tsx" (analog: existing hook-invocation pattern at lines 136-137; comment recommendation: "// Phase 16 THEME-01..04: orchestrates <html data-theme> writes, mql, cross-tab + same-tab sync")
  </read_first>
  <behavior>
    Final state of App.tsx:
      - One new import line: `import { useTheme } from '../hooks/useTheme'`.
      - The new import is alphabetized within the existing import block: existing imports use relative paths grouped roughly by source folder. Place the new import in the `'../hooks/...'` cluster (between `useAudioCues` import on line 15 and `useWakeLock` on line 16, OR appended after `useWakeLock` — planner's call; readability priority).
      - One new line inside `function App()` body: `useTheme()` invocation. The return value is NOT destructured (the hook is called for side effect). Annotate with a one-line trailing comment: `// Phase 16 THEME-01..04: orchestrates <html data-theme> writes (S-01/S-04), cross-tab + same-tab sync (A-03/A-04)`.
      - Placement: among the other side-effectful hook invocations, recommended position AFTER `const session = useSessionEngine(initialSettings)` (line 54) and BEFORE the `const [endDialogOpen, ...]` useState declarations (line 56). OR alongside the audio/wakeLock cluster at lines 136-137. Either placement is acceptable; the criterion is "near other side-effectful hook invocations, NOT inside a useEffect or callback".
      - NO new useState. NO new useEffect. NO new useMemo. NO new useRef. NO new JSX.
      - All existing code is preserved byte-for-byte except the two new lines (import + invocation).
  </behavior>
  <action>
    Step 1 — Add import: insert `import { useTheme } from '../hooks/useTheme'` in the import block. Recommended position: immediately after `import { useWakeLock } from '../hooks/useWakeLock'` (currently line 16).

    Step 2 — Add invocation: inside `function App() { ... }` (currently begins line 42), add the single line `useTheme()` with the trailing comment per <behavior>. Recommended position: immediately after the existing `const wakeLock = useWakeLock()` at line 137 (the rightmost edge of the existing side-effectful-hook cluster).

    Step 3 — Run `npx vitest run src/app/App.persistence.test.tsx` to verify the existing App-level tests still pass (the App-level persistence suite is the most likely to surface a regression from a new hook invocation).

    Step 4 — Run `npm run test:run` to confirm the full suite passes.

    Step 5 — Manual smoke (OPTIONAL pre-checkpoint, but useful before declaring this task done): `npm run dev`, open the browser, observe `<html data-theme="...">` is set on initial paint (matches OS preference if no localStorage exists). Open SettingsDialog from the gear icon, click Dark/Light/Moss/Slate/Dusk/System in sequence, verify each click changes the page palette instantly.

    Do NOT:
      - Destructure `useTheme()`'s return value (App doesn't use `theme` or `setTheme`; ThemePicker pulls its own setter from useThemeChoice).
      - Add a new `const` declaration that aliases the hook result.
      - Wrap `useTheme()` in `useMemo` or `useCallback` (it's a hook call, not a function call — direct invocation is correct).
      - Move `useTheme()` inside an effect, callback, or conditional (rules of hooks: must be called at the top level of the function body).
      - Re-order any existing hook calls (the `useSessionEngine` → `useAudioCues` → `useWakeLock` order is established in earlier phases; useTheme is added, not interposed).
      - Add the import to a different import group (keep it adjacent to the other hook imports for discoverability).
  </action>
  <verify>
    <automated>grep -E "^import \\{ useTheme \\} from '\\.\\./hooks/useTheme'" src/app/App.tsx | grep -q "useTheme"</automated>
    <automated>grep -E "^\\s+useTheme\\(\\)" src/app/App.tsx | grep -q "useTheme"</automated>
    <automated>grep -c "useTheme" src/app/App.tsx | awk '{ if ($1 == 2) print "OK"; else print "FAIL: expected 2 occurrences (1 import + 1 call), got " $1; }' | grep -q "OK"</automated>
    <automated>! grep -E "const \\{ theme.*\\} = useTheme\\(\\)" src/app/App.tsx</automated>
    <automated>npx vitest run src/app/App.persistence.test.tsx 2>&1 | tee /tmp/p16-04-t2-app-persistence.log; grep -E "Tests.*passed" /tmp/p16-04-t2-app-persistence.log | head -1</automated>
    <automated>npx tsc --noEmit && npm run lint && npm run build && npm test</automated>
  </verify>
  <acceptance_criteria>
    - `src/app/App.tsx` contains exactly one `import { useTheme } from '../hooks/useTheme'` line (placed near the existing hook imports).
    - `src/app/App.tsx` contains exactly one `useTheme()` invocation inside the App function body (NOT destructured; NOT wrapped in another hook or effect).
    - `grep -c "useTheme" src/app/App.tsx` outputs `2` (exactly: 1 import + 1 invocation).
    - No new `useState`, `useEffect`, `useMemo`, `useRef`, `useCallback` is added to App.tsx (Phase 15 D-05 minimal-state posture; CONTEXT.md item 6).
    - No new JSX is added to App.tsx.
    - The existing hook invocation order is preserved: `useSessionEngine` → `useAudioCues` → `useWakeLock` (verified by `grep -n "useSessionEngine\\|useAudioCues\\|useWakeLock" src/app/App.tsx` showing the original line numbers ± 2 for the new useTheme insertion).
    - `npx vitest run src/app/App.persistence.test.tsx` passes (the App-level persistence suite catches regressions from new hook side effects).
    - `npm run test:run` passes — full suite green (D-17 per-commit green-gate).
    - `npx tsc --noEmit && npm run lint && npm run build` exit 0.
    - The file `src/components/ThemePicker.tsx` is unchanged by this task (Task 1 owns the picker edit; this task owns only the App wire).
  </acceptance_criteria>
  <done>App.tsx invokes useTheme() once for side effects; no new App state introduced; full test suite passes.</done>
</task>

<task type="auto">
  <name>Task 3: Phase close artifacts — update REQUIREMENTS.md, STATE.md, ROADMAP.md, and create 16-SUMMARY.md</name>
  <files>.planning/REQUIREMENTS.md, .planning/STATE.md, .planning/ROADMAP.md, .planning/phases/16-themes/16-SUMMARY.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (full file — find the Traceability table; flip THEME-01 through THEME-05 status from Pending to Done; mirrors the pattern used for INFRA-01..04 after Phase 14/15 close)
    - .planning/STATE.md (full file — current state shows "Phase 16 UI-SPEC approved"; update to "Phase 16 complete"; bump progress percentages; advance Current Position to Phase 17; update Stopped At; update Last Activity to today)
    - .planning/ROADMAP.md (full file — Phase 16 entry currently shows `**Plans:** TBD` and the bullet `- [ ] **Phase 16: Themes** - CSS custom-property token system...`; update to `[x]`, set plan count = 4, list plans 16-01..16-04 with brief objectives)
    - .planning/phases/15-settingsdialog-shell/15-SUMMARY.md (if it exists — pattern template for the phase-close summary doc; if not, look at .planning/milestones/v1.0.1-phases/12-*-SUMMARY.md or any prior phase summary as a template)
    - .planning/phases/16-themes/16-01-PLAN.md, 16-02-PLAN.md, 16-03-PLAN.md (the four plans this summary summarizes — read the <output> sections of each to know what each plan recorded in its individual SUMMARY)
    - .planning/phases/16-themes/16-CONTEXT.md (the Phase 16 contract — for cross-reference in the summary)
  </read_first>
  <behavior>
    `REQUIREMENTS.md` final state:
      - Lines for THEME-01, THEME-02, THEME-03, THEME-04, THEME-05 in the §"Themes (CUST-01)" section: each checkbox `[ ]` flipped to `[x]`.
      - Traceability table (lines ~104-108 currently): THEME-01..05 rows status flipped from `Pending` to `Done`.
      - Coverage block at end: no structural change (29 total requirements, 29 mapped; nothing new added).
      - Optional: add a one-line phase-close marker comment at the top of the THEME-01 entry if the file pattern uses such markers (check Phase 14/15 entries; if INFRA-01..04 have no marker comments, then THEME-* also don't).
      - "Last updated" footer line: bump to today's date with note "Phase 16 THEME-01..05 traceability flipped to Done".

    `STATE.md` final state:
      - Frontmatter:
        - `status: planning` → `status: planning` (still planning v1.1; do NOT prematurely mark milestone done).
        - `stopped_at: Phase 16 UI-SPEC approved` → `stopped_at: Phase 16 themes complete`.
        - `last_updated: <ISO timestamp>` → today's ISO timestamp.
        - `last_activity: <date>` → today's date.
        - `progress.completed_phases: 3` → `4` (Phase 16 now joins 13, 14, 15 as complete; verify the count: 13, 14, 15, 16 = 4 — note that pre-edit the count was 3 but 15 just shipped per ROADMAP entry, so confirm from the running state).
        - `progress.completed_plans: 6` → `10` (6 + plan 16-01 + 16-02 + 16-03 + 16-04 = 10; sanity-check by reading ROADMAP plan counts).
        - `progress.percent: 100` (this is the milestone-internal percent of complete-vs-total plans for the current state, which depends on the formula; update to match the new ratio — recompute from total_phases and completed_phases or total_plans and completed_plans).
      - "Current Position" section: Phase `16` → `17`; Plan `Not started` → `Not started`; Status `Ready to plan` → `Ready to plan`; Last activity `<date>` → today.
      - Progress bar: recompute to match the new completed/total ratio.
      - "Performance Metrics" table:
        - "By Phase (v1.1)" row for Phase 16: `Plans: TBD` → `Plans: 4`; `Status: Not started` → `Status: Complete`.
      - "Session Continuity":
        - `Last session: <timestamp>` → today's ISO.
        - `Stopped at: Phase 16 UI-SPEC approved` → `Phase 16 themes complete`.
        - `Resume file: .planning/phases/16-themes/16-UI-SPEC.md` → `.planning/phases/16-themes/16-SUMMARY.md`.
        - `Next command: Phase 15 (SettingsDialog Shell) planning` → `Phase 17 (Visual Variants) planning`.
      - "Operator Next Steps":
        - `Begin Phase 15 (SettingsDialog Shell) planning: /gsd-plan-phase 15` → `Begin Phase 17 (Visual Variants) planning: /gsd-plan-phase 17`.

    `ROADMAP.md` final state:
      - Top of "v1.1 Customization (planned)" section: Phase 16 line:
        - `- [ ] **Phase 16: Themes** - CSS custom-property token system (data-theme); FOUC-prevention inline script; Light / Dark / System + 3 named palettes` → `- [x] **Phase 16: Themes** - CSS custom-property token system (data-theme); FOUC-prevention inline script; Light / Dark / System + 3 named palettes (completed <today>)`.
      - "Phase 16: Themes" entry detail block:
        - `**Plans**: TBD` → `**Plans**: 4 plans` followed by an unordered list of plan files + objectives:
          - `- [x] 16-01-PLAN.md — theme.css token overrides (Light @theme + Dark/Moss/Slate/Dusk [data-theme] blocks) + THEME-05 contrast guard`
          - `- [x] 16-02-PLAN.md — useTheme orchestrator hook + useThemeChoice picker setter hook + hook tests`
          - `- [x] 16-03-PLAN.md — index.html FOUC inline script + storage.ts SYNC comment`
          - `- [x] 16-04-PLAN.md — ThemePicker radiogroup UI + App.tsx useTheme wire-up + phase close`
      - "Progress" table at end of ROADMAP: Phase 16 row:
        - `Plans Complete: 0/?` → `Plans Complete: 4/4`.
        - `Status: Not started` → `Status: Complete`.
        - `Completed: -` → today's date.

    `16-SUMMARY.md` (NEW FILE) structure:
      - YAML frontmatter: phase, completed (today), plans (4), requirements (THEME-01..05).
      - "## Summary" section: one paragraph describing what Phase 16 delivered (6-theme switching system, FOUC, persistence, automated contrast guard, zero new deps).
      - "## Plans" section: 4 sub-sections, one per plan, each summarizing the plan's deliverable + key outputs from its own `<plan>-SUMMARY.md` (e.g. 16-01 ships theme.css + 5-iteration contrast test; 16-02 ships two hooks + ≥ 16 tests; 16-03 ships FOUC + SYNC comment; 16-04 ships picker + App wire + this file).
      - "## Locked Decisions Honored" section: bullet list of D-01..D-22 from CONTEXT.md with a one-line "honored by plan NN" pointer for each.
      - "## Test Counts" section: pre-phase baseline (438), per-plan additions, final total (>= 466).
      - "## Files Modified" section: list every file touched across plans 01-04 with the plan number that touched it.
      - "## Carry-Forward" section: note any deferred ideas from CONTEXT.md §"Deferred Ideas" that became v1.x carry-forwards (e.g. arrow-key navigation in radiogroup, theme-aware --shadow-breathing-card, <meta theme-color>, schema-drift guard test).
      - "## Next Phase" section: pointer to Phase 17 (Visual Variants) and the unblocked-by-this-phase contract (useThemeChoice + useTheme pattern is the template Phase 17/18/19 will mirror).
  </behavior>
  <action>
    Step 1 — REQUIREMENTS.md edits:
      - For each of THEME-01, THEME-02, THEME-03, THEME-04, THEME-05 in §"Themes (CUST-01)": change the leading `- [ ]` to `- [x]`.
      - In the Traceability table, flip the Status column for THEME-01..05 rows from `Pending` to `Done`.
      - Update the "Last updated" footer line.

    Step 2 — STATE.md edits:
      - Update frontmatter fields per <behavior>.
      - Update "Current Position" Phase: 16 → 17.
      - Update "Performance Metrics" Phase 16 row to Complete with 4 plans.
      - Update "Session Continuity" stopped_at, resume file, next command.
      - Update "Operator Next Steps" to point at Phase 17.
      - Recompute the progress bar to match the new completed/total ratio.

    Step 3 — ROADMAP.md edits:
      - Flip Phase 16 line in the v1.1 list to `[x]` with completion date.
      - Replace `**Plans**: TBD` with `**Plans**: 4 plans` + per-plan bullets.
      - Update Progress table row for Phase 16 to 4/4 Complete with today's date.

    Step 4 — Create `.planning/phases/16-themes/16-SUMMARY.md` per the <behavior> structure. Reference the four individual plan SUMMARY files (16-01-SUMMARY.md, 16-02-SUMMARY.md, 16-03-SUMMARY.md, this file's own 16-04-SUMMARY.md to be produced as the <output> of plan 04) for the specifics — the phase-level summary points at them rather than duplicating their content.

    Step 5 — Verify: run `gsd-sdk query frontmatter.validate .planning/STATE.md --schema state` (or equivalent) if available. Manually re-read STATE.md, ROADMAP.md, REQUIREMENTS.md to confirm:
      - REQUIREMENTS.md has 5 `- [x] **THEME-XX**` lines (one per requirement).
      - STATE.md frontmatter has `status: planning`, `completed_phases: 4` (assuming Phase 15 was 3 prior to this update), `stopped_at: Phase 16 themes complete`.
      - ROADMAP.md Phase 16 row in the Progress table shows `4/4 | Complete | <today>`.

    Step 6 — Do NOT prematurely mark v1.1 milestone as `shipped` in STATE.md. The milestone field stays `v1.1` with `status: planning` because Phase 17/18/19 are still pending. Only when ALL v1.1 phases close does the milestone status flip.

    Step 7 — Do NOT delete or relocate `.planning/phases/16-themes/16-CONTEXT.md`, `16-RESEARCH.md`, `16-PATTERNS.md`, `16-UI-SPEC.md`, `16-DISCUSSION-LOG.md`, `16-VALIDATION.md`. They remain as the phase's source-of-truth artifacts.
  </action>
  <verify>
    <automated>grep -c "^- \\[x\\] \\*\\*THEME-0[1-5]\\*\\*" .planning/REQUIREMENTS.md | grep -q "^5$"</automated>
    <automated>grep -E "^\\| THEME-0[1-5] \\| Phase 16 \\| Done \\|" .planning/REQUIREMENTS.md | wc -l | grep -q "^5$"</automated>
    <automated>grep -E "stopped_at: Phase 16 themes complete" .planning/STATE.md | grep -q "complete"</automated>
    <automated>grep -E "completed_phases: [4-9]" .planning/STATE.md | grep -q "completed_phases"</automated>
    <automated>grep -E "^- \\[x\\] \\*\\*Phase 16: Themes\\*\\*" .planning/ROADMAP.md | grep -q "Phase 16"</automated>
    <automated>grep -E "^\\| 16\\. Themes \\| v1\\.1 \\| 4/4 \\| Complete" .planning/ROADMAP.md | grep -q "Complete"</automated>
    <automated>test -f .planning/phases/16-themes/16-SUMMARY.md && wc -l .planning/phases/16-themes/16-SUMMARY.md | awk '{ if ($1 >= 30) print "OK"; else print "FAIL: too short"; }' | grep -q "OK"</automated>
    <automated>grep -E "THEME-01|THEME-02|THEME-03|THEME-04|THEME-05" .planning/phases/16-themes/16-SUMMARY.md | wc -l | awk '{ if ($1 >= 5) print "OK"; else print "FAIL"; }' | grep -q "OK"</automated>
    <automated>npx tsc --noEmit && npm run lint && npm run build && npm test</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/REQUIREMENTS.md` has 5 lines matching `^- \[x\] \*\*THEME-0[1-5]\*\*` (THEME-01 through THEME-05 all flipped to checked).
    - The Traceability table in REQUIREMENTS.md has 5 rows matching `^\| THEME-0[1-5] \| Phase 16 \| Done \|` (status flipped from Pending to Done).
    - `.planning/STATE.md` frontmatter `stopped_at` contains the literal string `Phase 16 themes complete`.
    - `.planning/STATE.md` frontmatter `completed_phases` is at least 4 (one more than the pre-edit value).
    - `.planning/STATE.md` "Current Position" Phase is `17`.
    - `.planning/STATE.md` "Operator Next Steps" references `/gsd-plan-phase 17`.
    - `.planning/ROADMAP.md` Phase 16 v1.1-list line is `- [x] **Phase 16: Themes** ...` (checked).
    - `.planning/ROADMAP.md` Phase 16 detail block has `**Plans**: 4 plans` and lists `16-01-PLAN.md`, `16-02-PLAN.md`, `16-03-PLAN.md`, `16-04-PLAN.md` as `- [x]` items with brief objectives.
    - `.planning/ROADMAP.md` Progress table row for Phase 16 shows `4/4 | Complete | <date>`.
    - `.planning/phases/16-themes/16-SUMMARY.md` exists, is at least 30 lines, mentions all 5 requirement IDs (THEME-01..05), references all 4 plan files (16-01..16-04), and contains sections "Summary", "Plans", "Locked Decisions Honored", "Test Counts", "Files Modified", "Carry-Forward", "Next Phase".
    - `npx tsc --noEmit && npm run lint && npm run build && npm test` all exit 0 (planning-doc edits should not affect TS compilation or runtime tests, but D-17 per-commit green-gate is enforced regardless).
    - No source files (`src/**`) are modified by this task — it is documentation-only.
  </acceptance_criteria>
  <done>REQUIREMENTS.md THEME-01..05 traceability flipped to Done; STATE.md updated with Phase 16 complete + next-up Phase 17; ROADMAP.md Phase 16 marked [x] with 4 plans listed; 16-SUMMARY.md created. Per-commit green-gate green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Human visual verification — exercise all 6 themes in the browser</name>
  <what-built>
    Phase 16 ships the full theme switching system: 6 themes (Light, Dark, System, Moss, Slate, Dusk) selectable from the SettingsDialog gear → ThemePicker; FOUC-prevention inline script in `index.html` applies the stored theme before React hydrates; `useTheme()` orchestrator hook handles cross-tab + same-tab + OS-dark/light sync; automated THEME-05 contrast test runs in CI.
  </what-built>
  <how-to-verify>
    1. Stop the running dev server if active. Run `npm run build` and observe it exits 0.
    2. Run `npm run preview` (Vite production preview) and open the local URL (typically `http://localhost:4173/`).
    3. **System mode + OS-dark probe**: in the browser DevTools, simulate `prefers-color-scheme: dark` (Chrome: DevTools → ⋮ → More tools → Rendering → Emulate CSS media feature). Hard-refresh. Observe `<html data-theme="dark">` on first paint (no flash of Light). Inspect the `<html>` element to confirm the attribute.
    4. **System mode + OS-light probe**: flip the DevTools emulation to `prefers-color-scheme: light`. The page should re-paint to the Light palette WITHOUT a reload (the mql listener in useTheme should fire). Confirm `<html data-theme="light">`.
    5. Click the gear icon (SettingsAnchor) to open SettingsDialog. Confirm the ThemePicker shows 6 buttons in a 3×2 grid: Light, Dark, System (row 1); Moss, Slate, Dusk (row 2).
    6. Click each named theme in sequence (Dark, Moss, Slate, Dusk, Light, System). After each click:
       - The page palette should change instantly (no transition).
       - The currently-selected button should carry a 2px accent border, soft-bg fill, accent-strong text (UI-SPEC §3).
       - DevTools: `<html data-theme="..."/>` should update to match (named themes write the literal id; 'system' resolves to 'dark' or 'light').
       - DevTools localStorage: `hrv:state:v1` should reflect the new `prefs.theme` value.
    7. **Persistence probe**: pick Dusk, hard-refresh the page. Confirm `<html data-theme="dusk">` on first paint with NO flash of any other palette.
    8. **In-session gating**: from the Idle view, configure a session (set duration to 1 minute for speed) and click Start. Wait for the 3-2-1 lead-in to begin (`inSessionView = true`). Open SettingsDialog (the gear may be hidden during session per Phase 15 D-XX — if so, skip this step; if the dialog is open and reachable, confirm all 6 ThemePicker buttons are `disabled` with the `aria-disabled='true'` group + dimmed cursor, and clicking does nothing). End the session.
    9. **Cross-tab probe (OPTIONAL but recommended)**: open the app in two tabs. In tab A, switch to Moss. In tab B (still showing the previous theme), without focus or reload, the tab B page should update to Moss within ~50ms (the 'storage' event fires in tab B; useTheme re-reads loadPrefs and writes data-theme).
    10. **Contrast sanity (visual cross-check)**: for each of the 5 named themes, visually confirm that during a session the orb-in (teal-ish) vs orb-out (blue-ish) crossfade is perceptually distinguishable. The automated test enforces ≥ 1.5 WCAG ratio; this human pass catches "passes the math but feels muddy" cases.
    11. **Aesthetic sanity**: pick each palette and judge whether it feels intentional vs accidental. Specific risks: Dark may have too-saturated accent colors; Dusk may have orb stops too close in hue; Light may be visually indistinguishable from a default browser white. If any palette feels off, propose specific hex tweaks for plan 01 (theme.css) to address.
  </how-to-verify>
  <resume-signal>
    Type "approved" if all 11 verification steps pass.

    If step 3 or 7 fails (FOUC flash on first paint): re-check that index.html has the FOUC script in <head> WITHOUT type="module"/async/defer and that the script appears before <title>. Halt phase close until resolved.

    If step 4 fails (System mode does not flip live): re-check useTheme.ts has the gated mql effect with dep array `[theme]` and that the listener writes to `documentElement.dataset.theme` (not to React state) when state === 'system'. Halt phase close until resolved.

    If step 6 fails (selected button styling doesn't match UI-SPEC §3): re-check ThemePicker.tsx's selected-state className includes `border-2 border-[var(--color-breathing-accent)] bg-[var(--color-breathing-bg-soft)] text-[var(--color-breathing-accent-strong)]`. Halt and revise.

    If step 11 surfaces an aesthetic concern that the automated contrast test won't catch (e.g. "Dark accent is too saturated"), specify the token and desired hex shift. Re-open plan 01 to revise theme.css; re-run the contrast test; resume from step 6 of this checkpoint.

    Approval means: phase close artifacts (Task 3) can be committed and Phase 16 ships.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User click → component → hook → DOM | Click event flows through React handler → useThemeChoice.setTheme → savePrefs (validates via Phase 14 coerceTheme) → CustomEvent → useTheme listener → setState → apply effect writes attribute. Every hop has type-narrowing or validation. |
| App mount → useTheme | The hook reads localStorage at mount via loadPrefs → coerceTheme (Phase 14 D-17 — non-throwing per-field coercer). Invalid stored values become DEFAULT_THEME ('system'); never propagate as untrusted strings. |
| ThemePicker → SettingsDialog | The picker is rendered inside SettingsDialog via the Phase 15-locked import. NO new prop, NO new event, NO new ref crosses this boundary (D-19 + D-20 invariants). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-04-01 | Tampering | ThemePicker onClick handler receives `id` parameter | mitigate | `id` is constrained to `ThemeId` union literal at compile time via `THEME_OPTIONS.map((id) => ...)` — TS narrows the iterator element to `ThemeId`. No user-supplied string can reach `setTheme`. |
| T-16-04-02 | Tampering | useTheme apply effect writes attribute from state | mitigate | State is typed `ThemeId`; runtime values originate from `loadPrefs` → `coerceTheme` (Phase 14 D-17). The apply effect's branch on `theme === 'system'` is the only conditional; default branch writes `document.documentElement.dataset.theme = theme` where `theme: ThemeId`. No XSS surface. |
| T-16-04-03 | Information disclosure | App.tsx now has theme orchestrator | accept | No new data flows out of App. The hook is called for its side-effect, return value unused. No new logging, telemetry, or storage write originates from this plan's App.tsx edit. |
| T-16-04-04 | Denial of service | Cross-tab storage event fires repeatedly | accept | Same mitigation as plan 02 T-16-02-04 — listener writes one attribute per fire, no compute. |
| T-16-04-05 | Tampering | ThemePicker.tsx renders 6 buttons with onClick handlers | mitigate | Each handler is `() => { setTheme(id) }` where `id` is bound at map-iteration time. No dynamic dispatch, no eval, no innerHTML. React's event delegation system rejects any handler that throws. |
| T-16-04-06 | Repudiation | (theme changes are non-auditable) | accept | Per plan-02 T-16-02-06 — UI preference is not security-relevant. |
| T-16-04-07 | Elevation of privilege | (no privileged actions in this plan) | accept | All operations write to localStorage and the DOM. No new auth/authz surfaces. |
| T-16-04-08 | Tampering | Phase close artifacts (REQUIREMENTS/STATE/ROADMAP/SUMMARY) | mitigate | Docs are author-controlled at commit time and reviewed in PR. Frontmatter validation (Step 5 of Task 3) catches structural errors. No runtime impact. |
</threat_model>

<verification>
After all 4 tasks land:

1. `git diff --stat` shows changes to: `src/components/ThemePicker.tsx`, `src/components/ThemePicker.test.tsx`, `src/app/App.tsx`, `.planning/REQUIREMENTS.md`, `.planning/STATE.md`, `.planning/ROADMAP.md`, and one new file `.planning/phases/16-themes/16-SUMMARY.md`.
2. `git diff src/components/SettingsDialog.tsx` is empty (D-19 invariant).
3. `git diff src/domain/settings.ts src/storage/prefs.ts src/storage/storage.ts src/hooks/useTheme.ts src/hooks/useThemeChoice.ts src/styles/theme.css index.html` is empty for this plan (they were touched in waves 1/plans 01-03; plan 04 only touches the wire-up files).
4. `npx vitest run src/components/ThemePicker.test.tsx` shows ≥ 7 passing tests.
5. `npx vitest run src/app/App.persistence.test.tsx` passes — no regression from the new useTheme() invocation.
6. `npm run test:run` (full suite) shows ≥ 466 passing tests (Phase 15 baseline 438 + plan 01 5 + plan 02 ≥ 16 + plan 04 ≥ 7).
7. `npx tsc --noEmit && npm run lint && npm run build && npm test` all exit 0.
8. Human checkpoint (Task 4) approved.
</verification>

<success_criteria>
- ThemePicker.tsx renders the radiogroup with 6 native role="radio" buttons consuming useThemeChoice; { disabled: boolean } prop interface preserved (D-19/D-20 invariants); SettingsDialog.tsx unchanged.
- ThemePicker.test.tsx has ≥ 7 tests covering radiogroup rendering, aria-checked reflection of stored theme, savePrefs write on click, CustomEvent dispatch with `{ key: 'theme', value: id }` shape, disabled gating, disabled click no-op.
- App.tsx invokes useTheme() exactly once for side effects with one new import line and one new invocation line; no new useState/useEffect/useMemo/useRef.
- REQUIREMENTS.md: THEME-01..05 all flipped to `- [x]` and Traceability table Status flipped to Done.
- STATE.md: stopped_at = "Phase 16 themes complete"; Current Position Phase = 17; Operator Next Steps points at `/gsd-plan-phase 17`.
- ROADMAP.md: Phase 16 line `- [x]` with completion date; **Plans**: 4 plans with per-plan bullets; Progress table Phase 16 row = `4/4 | Complete | <date>`.
- 16-SUMMARY.md exists with sections Summary, Plans, Locked Decisions Honored, Test Counts, Files Modified, Carry-Forward, Next Phase.
- Human checkpoint approved (all 11 verification steps pass; aesthetics judged acceptable).
- Zero new npm dependencies (D-18); per-commit green-gate (D-17) green at every commit boundary.
</success_criteria>

<output>
After completion, create `.planning/phases/16-themes/16-04-SUMMARY.md` (per-plan summary, distinct from the phase-level 16-SUMMARY.md created in Task 3) recording:
  - The final ThemePicker.tsx structure (radiogroup + 6 options + click → useThemeChoice).
  - Confirmation that ThemePickerProps remained `{ disabled: boolean }` (D-20 invariant).
  - The single-line App.tsx edit (import + invocation) and confirmation no new App state was introduced.
  - Phase close artifact summary (which files were updated; which traceability rows flipped).
  - The result of the human checkpoint (approved + any aesthetic notes captured during step 11).
</output>
</content>
</invoke>