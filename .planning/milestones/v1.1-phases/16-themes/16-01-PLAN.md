---
phase: 16
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/styles/theme.css
  - src/styles/theme.contrast.test.ts
autonomous: true
requirements:
  - THEME-03
  - THEME-05
user_setup: []

must_haves:
  truths:
    - "src/styles/theme.css declares 5 concrete palettes (Light as @theme baseline; Dark, Moss, Slate, Dusk as [data-theme='X']:root override blocks)"
    - "Moss block is byte-identical to the prior v1.0.1 @theme palette (D-03 preserves familiar look for opted-in users)"
    - "Light block is fresh cool/neutral palette distinct from Moss"
    - "Every concrete theme (light, dark, moss, slate, dusk) clears the THEME-05 ≥ 1.5 luminance contrast floor on orb-in vs orb-out midpoints"
    - "All 17 themable --color-* tokens are redeclared in every named palette override block (flat override per D-01)"
    - "Reduced-motion @media block at theme.css:160-168 is untouched (theme-independent contract per Phase 2 D-07)"
    - "Test file src/styles/theme.contrast.test.ts injects rewritten theme.css (`@theme {` → `:root {`) into jsdom and iterates 5 themes"
    - "D-13: automated Vitest computed-style contrast test is the primary THEME-05 guard; no manual UAT screenshot pass required"
    - "D-14: contrast metric is WCAG relative-luminance on gradient midpoint colors with floor ≥ 1.5; helper relativeLuminance(r,g,b) is inline in the test file (no shared module)"
    - "D-16: contrast test iteration set = 5 concrete themes ['light','dark','moss','slate','dusk']; 'system' skipped (resolves to light/dark at runtime per S-01)"
    - "D-17: per-commit green-gate enforced — `npx tsc --noEmit && npm run lint && npm run build && npm test` exits 0 at every commit boundary"
    - "D-18: zero new npm dependencies — all color math (sRGB → linear → relative luminance, gradient string parsing) is inline; no `color`, `chroma-js`, `culori`"
  artifacts:
    - path: "src/styles/theme.css"
      provides: "Light @theme baseline + 4 [data-theme='X']:root override blocks (Dark, Moss, Slate, Dusk)"
      contains: "[data-theme='dark']:root, [data-theme='moss']:root, [data-theme='slate']:root, [data-theme='dusk']:root"
    - path: "src/styles/theme.contrast.test.ts"
      provides: "Automated THEME-05 contrast guard"
      exports: ["(no exports — vitest test file)"]
      contains: "describe.each, relativeLuminance, getComputedStyle(documentElement).getPropertyValue"
  key_links:
    - from: "src/styles/theme.css"
      to: "Tailwind v4 @theme directive + CSS [data-theme] cascade"
      via: "@theme block (Light baseline) + four [data-theme='X']:root override blocks (D-04 source order: Dark → Moss → Slate → Dusk)"
      pattern: "\\[data-theme='(dark|moss|slate|dusk)'\\]:root"
    - from: "src/styles/theme.contrast.test.ts"
      to: "src/styles/theme.css"
      via: "readFileSync + replace(/@theme\\s*\\{/g, ':root {') + <style> injection"
      pattern: "readFileSync.*theme\\.css"
    - from: "src/styles/theme.contrast.test.ts"
      to: "src/domain/settings.ts THEME_OPTIONS"
      via: "filter out 'system' (no CSS branch per S-01); iterate remaining 5"
      pattern: "describe\\.each|it\\.each"
---

<objective>
Land the CSS theme token system: convert `src/styles/theme.css`'s `@theme` block into the Light baseline, append four `[data-theme='X']:root { ... }` override blocks (Dark, Moss, Slate, Dusk) per D-01/D-03/D-04, and create the automated THEME-05 luminance-contrast test that iterates all 5 concrete themes and asserts each clears the ≥ 1.5 floor (D-13/D-14/D-15/D-16).

Purpose: THEME-03 (3 named palettes ship via `[data-theme]` cascade) + THEME-05 (every theme preserves reduced-motion orb-in vs orb-out crossfade contrast). This plan ships the cascade surface and its automated guard so downstream plans (FOUC script, hooks, picker) can rely on the data-theme attribute swap producing the correct palette and on regression protection for future palette tweaks.

Output: One edited CSS file containing 5 palettes (~17 tokens × 5 = ~85 hex declarations) + one new Vitest file asserting contrast across the 5 themes. No new dependencies (D-18). No edits to any component, hook, or domain file.
</objective>

<execution_context>
@$HOME/.claude-personal-personal/get-shit-done/workflows/execute-plan.md
@$HOME/.claude-personal-personal/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/16-themes/16-CONTEXT.md
@.planning/phases/16-themes/16-RESEARCH.md
@.planning/phases/16-themes/16-UI-SPEC.md
@.planning/phases/16-themes/16-PATTERNS.md

@src/styles/theme.css
@src/domain/settings.ts
@src/storage/storage.test.ts

<interfaces>
<!-- Token surface to redeclare in every named palette (D-02 — every --color-* is themable; --orb-size / --orb-scale-* / --shadow-breathing-card stay outside scope). -->
<!-- Source: src/styles/theme.css lines 2-49 (current @theme contents). -->

Themable token names (17, all redeclared per palette):
  --color-breathing-bg
  --color-breathing-bg-soft
  --color-breathing-bg-edge
  --color-breathing-surface
  --color-breathing-accent
  --color-breathing-accent-strong
  --color-breathing-muted
  --color-orb-in-from
  --color-orb-in-to
  --color-orb-out-from
  --color-orb-out-to
  --color-orb-in-text
  --color-orb-out-text
  --color-ring-outer
  --color-ring-inner
  --color-modal-backdrop

NOT themable (preserved verbatim in @theme):
  --orb-size, --orb-scale-min, --orb-scale-max, --orb-scale-mid, --shadow-breathing-card

From src/domain/settings.ts:
  export type ThemeId = 'light' | 'dark' | 'system' | 'moss' | 'slate' | 'dusk'
  export const THEME_OPTIONS = ['light', 'dark', 'system', 'moss', 'slate', 'dusk'] as const

Moss palette = byte-identical current @theme (D-03):
  --color-breathing-bg: #f2fbf7
  --color-breathing-bg-soft: #e4f6ef
  --color-breathing-bg-edge: #f8fffc
  --color-breathing-surface: #ffffff
  --color-breathing-accent: #0f766e
  --color-breathing-accent-strong: #115e59
  --color-breathing-muted: #64748b
  --color-orb-in-from: #99f6e4
  --color-orb-in-to: #ecfdf5
  --color-orb-out-from: #bfdbfe
  --color-orb-out-to: #eef2ff
  --color-orb-in-text: #134e4a
  --color-orb-out-text: #1e3a8a
  --color-ring-outer: rgb(153 246 228 / 0.6)
  --color-ring-inner: rgb(59 130 246 / 0.35)
  --color-modal-backdrop: rgb(15 23 42 / 0.30)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Rewrite theme.css — Light @theme baseline + Dark/Moss/Slate/Dusk override blocks</name>
  <files>src/styles/theme.css</files>
  <read_first>
    - src/styles/theme.css (full file — current 168 lines; the @theme block at lines 1-50 is the structural template; lines 52-168 are the orb/ring/dialog/reduced-motion rules that MUST remain unchanged)
    - .planning/phases/16-themes/16-CONTEXT.md (D-01 flat override; D-02 every --color-* themable; D-03 Light fresh / Moss byte-identical current; D-04 source order Dark → Moss → Slate → Dusk)
    - .planning/phases/16-themes/16-UI-SPEC.md (§"Color" Per-Palette Aesthetic Direction table — locks aesthetic intent for Light/Dark/Slate/Dusk; Moss locked byte-identical to v1.0.1)
    - .planning/phases/16-themes/16-RESEARCH.md (§"Common Pitfalls" Pitfall 8 — confirm no non-arbitrary Tailwind utility classes against --color-* tokens; §"Open Questions" #1 — Tailwind starting-point suggestions)
    - src/domain/settings.ts lines 50-62 (ThemeId union + THEME_OPTIONS array — defines the 6 IDs; 5 require CSS branches, 'system' resolves at runtime per S-01)
  </read_first>
  <behavior>
    - Light palette (in @theme): cool/neutral, near-white background; cool slate/cool-gray accent at low saturation (≤ 30%); orb-in cool pastel (sky/mint family); orb-out warm pastel (sand/peach family) with hue separation > 30° and luminance contrast comfortably ≥ 1.5.
    - Dark palette: deep slate-900 / zinc-900 family background (warm-leaning blacks, not pure #000); accent brightened to teal-300/400 family for legibility on dark; orb-in cool-blue at higher luminance, orb-out teal/green at lower luminance — hardest contrast case, MUST pass ≥ 1.5 test.
    - Moss palette: byte-identical to the current v1.0.1 @theme values listed in <interfaces> above — every hex/rgb literal copied verbatim, no rounding, no edits.
    - Slate palette: cool blue-gray neutrals (slate-100 → slate-200 background); indigo-600 / blue-700 accent; orb-in cooler blue-50 family, orb-out neutral slate-100 family — same-cool-family with luminance step ≥ 1.5.
    - Dusk palette: warm violet/plum low-luminance background; amber-400 / golden accent; orb-in violet/plum at higher luminance, orb-out amber/peach at lower luminance — cross-spectrum hue + luminance pair (highest safety margin).
    - Reduced-motion @media block (theme.css:160-168) MUST be unchanged after the edit.
    - .orb, .orb-layer--in, .orb-layer--out, .orb-ring--outer, .orb-ring--inner, dialog.modal-fade rules MUST be unchanged after the edit.
  </behavior>
  <action>
    Step 1 — Replace the contents of the existing `@theme { ... }` block (theme.css:1-50) with the new Light palette. Keep the 17 themable token names from <interfaces>, the four non-themable tokens (--orb-size, --orb-scale-min, --orb-scale-max, --orb-scale-mid, --shadow-breathing-card), and the comment blocks describing token roles. Pick concrete hex/rgb values for the new Light palette per the UI-SPEC §"Color" aesthetic direction (Light = cool/neutral near-white, not the v1.0.1 teal-pastel). Add a single-line comment at the top of the @theme block: `/* Phase 16 D-03: Light palette is the @theme baseline (replaces the v1.0.1 teal-pastel which migrates to [data-theme='moss']:root below). */`.

    Step 2 — Immediately after the closing `}` of the @theme block (around line 50) and BEFORE the existing `.orb` rule (around line 63), append four `[data-theme='X']:root { ... }` blocks in source order Dark → Moss → Slate → Dusk per D-04. Each block:
      - Begins with a one-line comment naming the palette and its aesthetic intent (e.g. `/* Phase 16 D-04: Dark palette — deep slate background, brightened teal accent. */`).
      - Redeclares ALL 17 themable token names from <interfaces> with concrete hex/rgb values.
      - Does NOT redeclare --orb-size, --orb-scale-*, or --shadow-breathing-card (D-02 scope).

    Step 3 — For the Moss block specifically: copy the 17 themable token values from the <interfaces> block VERBATIM (preserve hex casing, rgb spacing, and alpha-channel rgb() form for --color-ring-* and --color-modal-backdrop). This is the only block where exact byte-equality with the prior v1.0.1 palette is mandatory (D-03 commitment to the Moss = familiar-look posture).

    Step 4 — Run `npx vitest run src/styles/theme.contrast.test.ts` (the test from Task 2 must already be in place or will be created in parallel; if Task 2 has not yet committed, defer this check to the post-plan green-gate). Iterate hex values for Dark/Slate/Dusk/Light until every theme clears the ≥ 1.5 floor. If a theme fails, widen the orb-in vs orb-out hue separation (move stops to different points on the color wheel) or increase the luminance step between the In and Out midpoints. Do NOT lower the floor; do NOT remove tokens.

    Step 5 — Verify the reduced-motion @media block at the end of theme.css is unchanged (`grep -A 8 "@media (prefers-reduced-motion: reduce)" src/styles/theme.css` returns the exact pre-edit content).

    Step 6 — Run `npm run build` to confirm Tailwind v4 compiles the modified theme.css without errors (Pitfall 8 — verify no non-arbitrary Tailwind utility classes broke). Run `npm test -- src/styles/theme.contrast.test.ts` and observe all 5 theme variants pass the contrast assertion.
  </action>
  <verify>
    <automated>npx vitest run src/styles/theme.contrast.test.ts --reporter=verbose 2>&1 | tee /tmp/p16-01-t1-contrast.log; grep -E "(✓|×)" /tmp/p16-01-t1-contrast.log | grep -E "(light|dark|moss|slate|dusk)"</automated>
    <automated>grep -c "\\[data-theme='\\(dark\\|moss\\|slate\\|dusk\\)'\\]:root" src/styles/theme.css | grep -q "^4$"</automated>
    <automated>grep -A 1 "data-theme='moss'" src/styles/theme.css | grep -q "Phase 16 D-04\\|Phase 16 D-03" && grep -q "f2fbf7" src/styles/theme.css && grep -q "ecfdf5" src/styles/theme.css && grep -q "rgb(153 246 228 / 0.6)" src/styles/theme.css</automated>
    <automated>npx tsc --noEmit && npm run lint && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "\[data-theme='\(dark\|moss\|slate\|dusk\)'\]:root" src/styles/theme.css` outputs exactly `4` (one match per named palette, in source order Dark → Moss → Slate → Dusk).
    - The Moss override block contains literal byte-identical token values: `--color-breathing-bg: #f2fbf7`, `--color-orb-in-from: #99f6e4`, `--color-orb-in-to: #ecfdf5`, `--color-orb-out-from: #bfdbfe`, `--color-orb-out-to: #eef2ff`, `--color-ring-outer: rgb(153 246 228 / 0.6)`, `--color-ring-inner: rgb(59 130 246 / 0.35)`, `--color-modal-backdrop: rgb(15 23 42 / 0.30)` (verified via `grep -E "(99f6e4|ecfdf5|bfdbfe|eef2ff|rgb\(153 246 228 / 0\.6\))" src/styles/theme.css | wc -l` outputs ≥ 5).
    - The reduced-motion `@media (prefers-reduced-motion: reduce) { ... }` block is structurally unchanged (same `dialog.modal-fade { transition: none !important }` and `.orb-ring--inner { display: none }` rules; verified via `grep -A 8 "prefers-reduced-motion" src/styles/theme.css`).
    - `npx vitest run src/styles/theme.contrast.test.ts` exits 0 with 5 passing tests (one per theme).
    - `npx tsc --noEmit && npm run lint && npm run build` all exit 0 — Tailwind v4 compiles the new override blocks; lint catches no regressions; D-17 per-commit green-gate satisfied.
    - The file `src/domain/settings.ts` is unchanged (`git diff src/domain/settings.ts` empty — D-21 file-split invariant).
    - The file `src/storage/prefs.ts` is unchanged (`git diff src/storage/prefs.ts` empty — D-21).
  </acceptance_criteria>
  <done>theme.css contains one @theme block (Light baseline) + four [data-theme='X']:root override blocks (Dark, Moss, Slate, Dusk) in source order; every concrete theme passes the ≥ 1.5 contrast guard; reduced-motion block untouched; per-commit green-gate green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create src/styles/theme.contrast.test.ts — 5-theme luminance contrast guard</name>
  <files>src/styles/theme.contrast.test.ts</files>
  <read_first>
    - .planning/phases/16-themes/16-RESEARCH.md §"jsdom Cascade Probe" (lines ~849-882 — locked test strategy: readFileSync + rewrite @theme {} → :root {} + <style> injection + direct CSS variable read via getComputedStyle(documentElement).getPropertyValue; do NOT parse linear-gradient strings since jsdom does not substitute var())
    - .planning/phases/16-themes/16-RESEARCH.md §"Code Examples" Example 3 (lines ~571-644 — full template for the test file)
    - .planning/phases/16-themes/16-CONTEXT.md D-13/D-14/D-15/D-16 (metric: WCAG luminance ratio ≥ 1.5 on orb-in vs orb-out midpoints; iterate 5 concrete themes; 'system' excluded; inline relativeLuminance helper — no new deps per D-18)
    - .planning/phases/16-themes/16-PATTERNS.md §"src/styles/theme.contrast.test.ts" (analog: src/storage/storage.test.ts:79-107 envelope probe pattern; beforeAll/afterEach pattern)
    - src/styles/theme.css (the file this test reads via readFileSync; the @theme block + override blocks must be discoverable at the path resolved by `resolve(__dirname, 'theme.css')`)
    - src/domain/settings.ts (THEME_OPTIONS literal for the iteration set; filter 'system' out per D-16)
    - src/hooks/usePrefersReducedMotion.test.ts (vitest + jsdom test conventions used by this codebase — imports, describe/it shape)
  </read_first>
  <behavior>
    - On import, the test file reads `src/styles/theme.css` once via `readFileSync(resolve(__dirname, 'theme.css'), 'utf-8')` inside a `beforeAll` hook.
    - The raw CSS content has `@theme {` rewritten to `:root {` via `.replace(/@theme\s*\{/g, ':root {')` (jsdom 29.1.1 does not recognize Tailwind v4's `@theme` at-rule per RESEARCH Pitfall 1).
    - The rewritten CSS is injected into `document.head` via `<style>` element with `textContent`.
    - `afterEach` resets state by deleting `document.documentElement.dataset.theme` (so consecutive iterations do not leak attribute state).
    - `describe.each` over the array `['light', 'dark', 'moss', 'slate', 'dusk']` (5 concrete themes per D-16) runs one test per theme.
    - For each theme: set `document.documentElement.dataset.theme = id` (or delete the dataset for 'light' since Light is the @theme/`:root` baseline and has no override block; the test uses `delete document.documentElement.dataset.theme` for 'light' and `document.documentElement.dataset.theme = id` for the other four).
    - Read four CSS custom properties via `getComputedStyle(document.documentElement).getPropertyValue('--color-orb-in-from' | '--color-orb-in-to' | '--color-orb-out-from' | '--color-orb-out-to').trim()`.
    - Parse each value with an inline `parseColorToRgb(value: string): [number, number, number]` helper handling hex shorthand `#abc`, full hex `#aabbcc`, and `rgb(r, g, b)` (with or without spaces).
    - Compute midpoint = per-channel average of `from` and `to` (rounded to int).
    - Compute relative luminance via inline `relativeLuminance(r, g, b)` using sRGB → linear conversion (`c <= 0.03928 ? c / 12.92 : Math.pow((c + 0.055) / 1.055, 2.4)`) then WCAG `0.2126*R + 0.7152*G + 0.0722*B`.
    - Compute contrast ratio `(max + 0.05) / (min + 0.05)` between in-midpoint and out-midpoint luminance.
    - Assert `expect(ratio).toBeGreaterThanOrEqual(1.5)` per D-14 floor.
  </behavior>
  <action>
    Create `src/styles/theme.contrast.test.ts` as a new Vitest file co-located with `theme.css`. Implement the structure described in <behavior> using only the imports: `{ describe, it, expect, beforeAll, afterEach }` from `'vitest'`, `{ readFileSync }` from `'node:fs'`, `{ resolve }` from `'node:path'`, and `{ THEME_OPTIONS, type ThemeId }` from `'../domain/settings'` (use the THEME_OPTIONS array filtered to exclude 'system' rather than hardcoding the 5-element list — keeps the iteration set in sync with the enum at compile time).

    Inline helpers (no new deps per D-18):
      - `srgbToLinear(c: number): number` — uses the WCAG 0.03928 threshold (NOT the IEC 0.04045; matches WCAG 2.x canonical formula per RESEARCH Example 1 note).
      - `relativeLuminance(r: number, g: number, b: number): number` — accepts 0-255 channels, internally normalizes to 0-1 via `c / 255`, applies srgbToLinear, returns weighted sum.
      - `parseColorToRgb(value: string): [number, number, number]` — handles `#rgb`, `#rrggbb`, `rgb(r,g,b)`, and `rgb(r, g, b)` shapes per RESEARCH Example 2. Throws with the offending value string on parse failure (helps debug when a future palette token uses an unsupported color form like `oklch()` or `hsl()`).
      - `midpoint(a, b): [number, number, number]` — per-channel `Math.round((a[i] + b[i]) / 2)`.
      - `contrastRatio(rgb1, rgb2): number` — internal use; computes luminance for each, returns WCAG ratio `(bright + 0.05) / (dark + 0.05)`.

    Test fixture (one `beforeAll`, one `afterEach`):
      - `beforeAll`: read theme.css via `readFileSync(resolve(__dirname, 'theme.css'), 'utf-8')`, rewrite via `raw.replace(/@theme\s*\{/g, ':root {')`, create `<style>` element with the rewritten text, append to `document.head`. Store the style element in a `let styleEl: HTMLStyleElement` declared at file scope so it can be removed in `afterAll` for cleanliness.
      - `afterAll`: `styleEl.remove()` (avoids polluting the jsdom document if multiple test files run in the same worker).
      - `afterEach`: `delete document.documentElement.dataset.theme` (reset attribute state between iterations).

    Iteration:
      - Const `CONCRETE_THEMES = THEME_OPTIONS.filter((t): t is Exclude<ThemeId, 'system'> => t !== 'system')` produces the 5-element list.
      - `describe.each(CONCRETE_THEMES)('theme=%s', (themeId) => { it('reduced-motion crossfade midpoint contrast ratio is ≥ 1.5 (THEME-05 / D-14)', () => { ... }) })`.
      - Inside the test body: if `themeId === 'light'`, `delete document.documentElement.dataset.theme` (Light = `:root` baseline; no override block); else `document.documentElement.dataset.theme = themeId`. Read four custom properties, compute midpoints, compute ratio, assert ≥ 1.5.

    Do NOT use:
      - `import './theme.css'` (Vitest does not inject CSS imports into jsdom per RESEARCH Pitfall 3).
      - `getComputedStyle(probeDiv).background` to extract gradient stops (jsdom does not resolve `var()` substitutions per RESEARCH Pitfall 2).
      - Any external color-math dependency (D-18 forbids; inline ~30 LOC of helpers).
      - `@theme inline` rewrites or PostCSS plugin chains (RESEARCH Pitfall 1 — the simple text-substitution `replace` is the empirically verified path).
  </action>
  <verify>
    <automated>npx vitest run src/styles/theme.contrast.test.ts --reporter=verbose 2>&1 | tee /tmp/p16-01-t2-contrast.log; grep -E "(✓|×)" /tmp/p16-01-t2-contrast.log | wc -l | grep -q "^5$"</automated>
    <automated>grep -E "describe\\.each|it\\.each" src/styles/theme.contrast.test.ts | grep -q "CONCRETE_THEMES\\|filter.*system"</automated>
    <automated>grep -q "readFileSync" src/styles/theme.contrast.test.ts && grep -q "replace.*@theme" src/styles/theme.contrast.test.ts && grep -q "appendChild" src/styles/theme.contrast.test.ts</automated>
    <automated>grep -q "toBeGreaterThanOrEqual(1.5)" src/styles/theme.contrast.test.ts</automated>
    <automated>grep -E "import.*'\\.\\./styles/theme\\.css'|import.*'\\./theme\\.css'" src/styles/theme.contrast.test.ts | grep -v "^#" | grep -c "import" | grep -q "^0$"</automated>
    <automated>npx tsc --noEmit && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - File `src/styles/theme.contrast.test.ts` exists and is a syntactically valid TypeScript file (`npx tsc --noEmit` exits 0).
    - The file imports `THEME_OPTIONS` and `type ThemeId` from `'../domain/settings'` and uses `THEME_OPTIONS.filter(...)` to derive the iteration set (NO hardcoded `['light','dark','moss','slate','dusk']` array — schema drift protection).
    - The file does NOT contain any `import` statement matching `'./theme.css'` or `'../styles/theme.css'` (RESEARCH Pitfall 3).
    - The file reads `src/styles/theme.css` via `readFileSync(resolve(__dirname, 'theme.css'), 'utf-8')` inside a `beforeAll` block.
    - The file applies `.replace(/@theme\s*\{/g, ':root {')` to the raw CSS text before injection (RESEARCH Pitfall 1).
    - The file contains an inline `relativeLuminance(r: number, g: number, b: number)` helper using WCAG canonical formula `0.2126*R + 0.7152*G + 0.0722*B` with `0.03928` threshold.
    - The file contains an inline `parseColorToRgb(value: string): [number, number, number]` helper handling `#rgb`, `#rrggbb`, and `rgb(r,g,b)` forms.
    - The test uses `describe.each` (or `it.each`) over the filtered THEME_OPTIONS so test names report per-theme failures clearly (e.g. `theme=dark` not `theme=1`).
    - For `themeId === 'light'` the test deletes `document.documentElement.dataset.theme` (Light = baseline); for the other four themes the test sets `document.documentElement.dataset.theme = themeId`.
    - The contrast assertion is exactly `expect(ratio).toBeGreaterThanOrEqual(1.5)` (the D-14 floor).
    - `npx vitest run src/styles/theme.contrast.test.ts` exits 0 with exactly 5 passing tests (one per concrete theme).
    - `npx tsc --noEmit && npm run lint` exit 0 — no type errors, no lint regressions.
    - The file `src/styles/theme.css` is unchanged by this task (`git diff src/styles/theme.css` empty — Task 1 owns the CSS edit; this task owns only the new test file).
  </acceptance_criteria>
  <done>src/styles/theme.contrast.test.ts exists with 5 passing tests asserting WCAG luminance contrast ≥ 1.5 on orb-in vs orb-out midpoints for each of light/dark/moss/slate/dusk. Per-commit green-gate green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Stylesheet → DOM | CSS values flow from author-defined `theme.css` to the rendered DOM via the cascade; values are NOT user-controlled. |
| Test fixture → jsdom | `readFileSync` reads a build-time, author-controlled file path (`resolve(__dirname, 'theme.css')`); no user input. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-01-01 | Information disclosure | `theme.contrast.test.ts` `readFileSync` | accept | Path is hardcoded relative to `__dirname`. No user input flows into the path. Test runs in dev/CI only — never in production runtime. No PII or secrets in `theme.css` (only color hex literals). |
| T-16-01-02 | Tampering | `theme.css` cascade overrides | mitigate | All hex/rgb values are author-controlled at commit time and reviewed via PR. `@theme` and `[data-theme]` selectors cannot be injected at runtime by an attacker because they are baked into the bundled stylesheet by Vite. THEME-05 automated test catches accidental contrast regressions in future palette tweaks. |
| T-16-01-03 | Denial of service | THEME-05 test runtime | accept | Test reads one ~10 KB file once per worker and iterates 5 themes (each test ~10ms). Total runtime budget ≈ 100ms — within Vitest test budget. No file is written. |
| T-16-01-04 | Spoofing | (CSS cascade) | accept | CSS variables resolved by the browser CSS engine, not by user-controlled code. `data-theme` attribute is written exclusively by `useTheme.ts` (plan 02) and the FOUC script (plan 03), both author-controlled. No untrusted script can set `data-theme` without first executing in our origin. |
</threat_model>

<verification>
After both tasks land:

1. `git diff --stat src/styles/` shows exactly two changed files: `theme.css` (modified) and `theme.contrast.test.ts` (added).
2. `git diff src/domain/ src/storage/ src/components/ src/hooks/ src/app/ index.html` is empty (D-21 file-split invariant — plan 01 only touches `src/styles/`).
3. `npx vitest run src/styles/theme.contrast.test.ts` shows 5/5 tests passing.
4. `npm run test:run` (full suite) shows the Phase 15 baseline (438 tests) PLUS the 5 new theme.contrast tests passing — no regressions (D-17 per-commit green-gate).
5. `npx tsc --noEmit && npm run lint && npm run build` all exit 0.
</verification>

<success_criteria>
- src/styles/theme.css has 1 `@theme` block (Light baseline) + 4 `[data-theme='X']:root` override blocks (Dark, Moss, Slate, Dusk) in source order Dark → Moss → Slate → Dusk per D-04.
- Every concrete theme (light, dark, moss, slate, dusk) clears the THEME-05 ≥ 1.5 luminance contrast floor on orb-in vs orb-out midpoints.
- Moss palette tokens are byte-identical to the prior v1.0.1 @theme palette (D-03 — preserves familiar look for users who pick Moss).
- src/styles/theme.contrast.test.ts is a 5-iteration `describe.each` test using readFileSync + `@theme {` → `:root {` rewrite + `<style>` injection + direct CSS variable reads (no gradient string parsing per RESEARCH Pitfall 2).
- Zero new npm dependencies introduced (D-18).
- `npx tsc --noEmit && npm run lint && npm run build && npm test` all exit 0 — D-17 per-commit green-gate satisfied.
- Files NOT touched by this plan: `src/domain/settings.ts`, `src/storage/prefs.ts`, `src/storage/storage.ts`, `src/components/*`, `src/hooks/*`, `src/app/App.tsx`, `index.html` (D-21).
</success_criteria>

<output>
After completion, create `.planning/phases/16-themes/16-01-SUMMARY.md` recording:
  - Which themable tokens received new hex values (Light, Dark, Slate, Dusk) and which received byte-identical re-declarations (Moss).
  - The actual contrast ratios computed for each of the 5 themes (read from `npx vitest run src/styles/theme.contrast.test.ts --reporter=verbose` test output if available, else recompute from the locked hexes) — establishes baseline for future palette-tweak retrospectives.
  - The path to the locked-hex source of truth: `src/styles/theme.css`.
  - Confirmation that the reduced-motion @media block is unchanged.
</output>
</content>
</invoke>