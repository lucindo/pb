---
phase: 16.1
plan: 06
status: complete
files_modified:
  - src/components/SessionControls.tsx
  - src/components/LearnDialog.tsx
  - src/components/BreathingShape.tsx
commits:
  - ecd9bfe feat(16.1-06) migrate SessionControls primary button (D-01 first consumer)
  - b4db189 fix(16.1-06) UAT remediation — restore body/heading/link 3-tier + dark lead-in digit
---

# Plan 16.1-06 SUMMARY — Primary Action Button + UAT

## What Shipped

### Task 1 — SessionControls Migration

1 file. 2 lines (52 + 69). 8 banned-fragment substitutions:

| Before | After |
|---|---|
| `bg-teal-700` | `bg-[var(--color-breathing-accent-strong)]` |
| `text-white` | `text-[var(--color-breathing-on-accent)]` ← D-01 first consumer |
| `hover:bg-teal-800` | `hover:bg-[var(--color-breathing-accent)]` |
| `active:bg-teal-900` | `active:bg-[var(--color-breathing-accent-strong)]` |
| `shadow-teal-900/20` | PRESERVED (v1.x carry-forward — Out of Scope) |
| `focus-visible:ring-breathing-accent` | PRESERVED (AP-03) |

### Task 2 — Per-Palette UAT

User ran `npm run dev`, swapped through all 5 palettes, captured 7 findings via screenshots.

**16.1-caused regressions (fixed inline in this plan via commit b4db189):**

- **F2 cross-theme** — Links collapsed with headings/body in LearnDialog. Root cause: RESEARCH §3 mapping `text-slate-700 → accent-strong` flattened the 3-tier hierarchy because accent ≈ accent-strong in some palettes (Dark: #2dd4bf vs #5eead4). **Fix:** LearnDialog explainer body p (lines 83/87/92) → `--color-breathing-muted`. New hierarchy: headings=accent-strong, links=accent, body=muted (3 distinct tiers per palette).
- **F3 Dark** — Lead-in countdown digit `2` read pale-teal (`--color-breathing-accent-strong: #5eead4`) on pale-blue orb (`--color-orb-in-from/to`). Active phase label (line 135) already had inline `style.color = --color-orb-{in,out}-text` override; lead-in (line 216) didn't. **Fix:** mirrored pattern — added inline `style.color = var(--color-orb-in-text)` to lead-in digit. Lead-in renders only the In gradient per D-14, so In-text token is correct consumer. className accent-strong remains as no-theme-CSS fallback.

**Phase 16 palette retune (deferred to Phase 16.2 — out of 16.1 scope per CONTEXT.md "Out of scope: New palettes, palette re-tuning"):**

| # | Theme | Issue |
|---|-------|-------|
| F1 | Light | Out gradient `#f97316 → #fed7aa` too saturated vs soft palette |
| F4 | Moss | Out gradient `#3b82f6 → #bfdbfe` too vivid blue vs greens |
| F5 | Slate | Out gradient `#6366f1 → #a5b4fc` too vivid indigo vs slate |
| F6 | Dusk | In gradient `#ede9fe → #faf5ff` too bright vs deep purple |
| F7 | Dusk | Out gradient `#d97706 → #fcd34d` better but softening welcome |

Plan 07 inserts Phase 16.2 (palette retune) into ROADMAP.

### Per-Palette Verdict (post F2+F3 fix)

| Palette | Start/Stop legibility | Verdict |
|---------|----------------------|---------|
| Light | Slate-600 bg + white text — high contrast | approved |
| Dark | Pale-teal bg + dark slate text (Pitfall-2 mitigation) | approved |
| Moss | Deep teal bg + white text | approved |
| Slate | Indigo-700 bg + white text | approved |
| Dusk | Amber bg + deep violet text | approved |

User signal: "approved" (post-remediation).

## Sentinels (verified)

- `grep -cE '\b(text|bg|border)-(slate|teal)-[0-9]|\bbg-white\b' src/components/SessionControls.tsx` → 0
- `grep -o 'var(--color-breathing-on-accent)' src/components/SessionControls.tsx | wc -l` → 2 (D-01 first consumer)
- `grep -o 'var(--color-breathing-accent-strong)' src/components/SessionControls.tsx | wc -l` → 4 (bg + active:bg, 2 sites)
- `grep -c 'shadow-teal-900/20' src/components/SessionControls.tsx` → 2 (carry-forward)
- `grep -c 'focus-visible:ring-breathing-accent' src/components/SessionControls.tsx` → 2 (AP-03)
- LearnDialog body p: 3 occurrences of `--color-breathing-muted` (F2 fix)
- BreathingShape:216 inline `style.color = var(--color-orb-in-text)` (F3 fix)

## Gates

`tsc + eslint + build` exit 0. Suite: SessionControls 8 + App.session 18 + LearnDialog 16 + BreathingShape 11 = all pass. Full chain green (499 passed | 10 skipped — D-04 guard still .skip until plan 07).

## Phase-Level Grep State Heading Into Plan 07

`grep -rEn --include='*.tsx' --exclude='*.test.tsx' '\b(text|bg|border)-(slate|teal)-[0-9]|\b(text|bg)-(white|black)\b' src/` → 2 hits remaining:
- `src/components/EndSessionDialog.tsx:81` — `text-white` on destructive End button (AP-02)
- `src/components/ResetStatsDialog.tsx:76` — `text-white` on destructive Reset button (AP-02)

Plan 07 decides whether to migrate to `text-[var(--color-breathing-on-accent)]` (option (a) — full grep-zero) or carve them out of the D-04 walker (option (b) — rejected per plan 01 Task 3). Option (a) is the planned path.
