---
phase: 16.1
plan: 04
status: complete
files_modified:
  - src/components/SettingsAnchor.tsx
  - src/components/LearnAnchor.tsx
  - src/components/SettingsStepper.tsx
  - src/components/MuteToggle.tsx
commits:
  - e54e8c9 feat(16.1-04) migrate SettingsAnchor + LearnAnchor disabled+enabled branches
  - ce54365 feat(16.1-04) migrate SettingsStepper chrome + MuteToggle button
---

# Plan 16.1-04 SUMMARY — Anchor + Stepper + Mute Toggle Migration

## What Shipped

4 files migrated. ~12 banned-class occurrences cleared. Closes the half-migrated state on both anchors (Phase 16 had only the disabled branch tokenized).

- **SettingsAnchor.tsx + LearnAnchor.tsx**: alpha-base (`bg-white/70` -> `bg-[var(--color-breathing-surface)]/70`, D-02 Path A), disabled branch (`border-slate-200` -> `border-[var(--color-breathing-muted)]`), enabled branch (border-teal-200/text-teal-800/hover:bg-teal-50/active:bg-teal-100 -> --color-breathing-accent / --color-breathing-accent-strong / hover+active --color-breathing-bg-soft).
- **SettingsStepper.tsx**: fieldset (line 36 — border-teal-100 + bg-white/80 -> tokens, D-02 Path A), legend (line 38), decrease+increase buttons (lines 45, 60 — full secondary-button tokenization), value pill (line 53 — bg-teal-50 + text-slate-950 -> --color-breathing-bg-soft + --color-breathing-accent-strong).
- **MuteToggle.tsx**: button (line 49 — full tokenization with `disabled:*` variants preserved verbatim per PATTERNS.md §4 special case).

## D-02 Strategy Realized

**Path A (Tailwind v4 arbitrary-value + alpha-modifier)** applied across all 3 alpha sites in this plan (LearnAnchor:24, SettingsAnchor:23, SettingsStepper:36). Plan 05 inherits Path A for the remaining 2 alpha sites (App.tsx:608, SessionReadout:39+79).

## Sentinels (verified)

- `grep -cE '\b(text|bg|border)-(slate|teal)-[0-9]|\bbg-white\b'` on 4 files → 0
- `grep -c 'shadow-teal-900/5' src/components/SettingsStepper.tsx` → 1 (CONTEXT.md Out of Scope; AP-04 carry-forward)
- `grep -c 'disabled:cursor-not-allowed disabled:opacity-45'` → MuteToggle=1, SettingsStepper=2 (PATTERNS.md MuteToggle special case)
- AP-03 preserved on all focusable buttons

## Gates

`tsc + eslint + build + test` exit 0. Anchor + MuteToggle tests pass without edit (D-06).

## Deviations

Plan recovery (post-stall): Task 1 commit was orphaned on a deleted worktree branch; recovered via `git cherry-pick 326b770` -> commit e54e8c9 on main. Task 2 (SettingsStepper + MuteToggle) executed inline on main.
