---
phase: 16.1
plan: 02
type: execute
wave: 2
depends_on: [16.1-01]
files_modified:
  - src/components/SettingsDialog.tsx
  - src/components/EndSessionDialog.tsx
  - src/components/ResetStatsDialog.tsx
  - src/components/LearnDialog.tsx
autonomous: true
requirements: [THEME-UI-01]
tags: [theme, tailwind-v4, dialog, tsx-migration]

must_haves:
  truths:
    - "SettingsDialog, EndSessionDialog, ResetStatsDialog, LearnDialog dialog shells visibly rebind on Dark/Moss/Slate/Dusk swap"
    - "Close/Keep/Cancel secondary buttons in all 4 dialogs use the canonical tokenized secondary-button class string (PATTERNS.md §4 Group P-DIALOG recipe)"
    - "Destructive red primary buttons on EndSessionDialog:81 and ResetStatsDialog:76 stay untouched (AP-02 sentinel)"
    - "LearnDialog body text (h3 + p × 6 occurrences) uses tokenized text classes"
    - "All existing dialog tests (4 SettingsDialog.test.tsx + sibling test files) continue passing — class composition changed, not semantics (D-06)"
  artifacts:
    - path: "src/components/SettingsDialog.tsx"
      provides: "Tokenized dialog shell + tokenized Close button"
      contains: "var(--color-breathing-surface)"
    - path: "src/components/EndSessionDialog.tsx"
      provides: "Tokenized dialog shell + title + Keep button; red End button preserved"
      contains: "var(--color-breathing-surface)"
    - path: "src/components/ResetStatsDialog.tsx"
      provides: "Tokenized dialog shell + title + Keep button; red Reset button preserved"
      contains: "var(--color-breathing-surface)"
    - path: "src/components/LearnDialog.tsx"
      provides: "Tokenized dialog shell + title + 6 h3 + 4 body p + Close button"
      contains: "var(--color-breathing-surface)"
  key_links:
    - from: "src/components/SettingsDialog.tsx"
      to: "src/styles/theme.css [data-theme]:root chrome tokens"
      via: "className references --color-breathing-surface / --color-breathing-accent-strong / --color-breathing-muted"
      pattern: "bg-\\[var\\(--color-breathing-surface\\)\\]"
    - from: "src/components/EndSessionDialog.tsx:81 (destructive End button)"
      to: "DO NOT MODIFY (AP-02 sentinel)"
      via: "bg-red-700 hover:bg-red-800 active:bg-red-900 text-white"
      pattern: "bg-red-700"
---

<objective>
Wave 1 / Plan 02: Migrate the four-dialog chrome family (Group B from `16.1-PATTERNS.md` §4) — `SettingsDialog`, `EndSessionDialog`, `ResetStatsDialog`, `LearnDialog`. The four dialogs share identical class strings for (a) the `<dialog>` shell border+bg, (b) the title `<h2>`, and (c) the Close/Keep/Cancel secondary button. This plan locks the canonical "dialog shell" and "secondary button" tokenized class strings — Wave 2 plans (03 pickers, 04 anchors/stepper) will copy them verbatim.

Purpose: Closing the "theme propagation gap" on the dialog surface — the single largest visible chrome cluster in the app. Every dialog the user opens (settings gear, end session, reset stats, learn book) currently stays teal-on-white in Dark/Moss/Slate/Dusk; after this plan, all four rebind per palette.

Output: 4 modified TSX files (~22 banned-class occurrences replaced), preserving 2 destructive-red occurrences (EndSession:81 + ResetStats:76 — AP-02). The `theme.no-hardcoded-classes.test.ts` guard (skipped at the suite level by plan 01) will, when run with `--no-skip`, show 4 fewer files violating the regex after this plan ships.
</objective>

<execution_context>
@$HOME/.claude-personal-personal/get-shit-done/workflows/execute-plan.md
@$HOME/.claude-personal-personal/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/16.1-ui-token-migration/16.1-CONTEXT.md
@.planning/phases/16.1-ui-token-migration/16.1-PATTERNS.md
@.planning/phases/16.1-ui-token-migration/16.1-RESEARCH.md
@.planning/phases/16.1-ui-token-migration/16.1-01-SUMMARY.md
@src/components/SettingsDialog.tsx
@src/components/EndSessionDialog.tsx
@src/components/ResetStatsDialog.tsx
@src/components/LearnDialog.tsx
@src/components/ThemePicker.tsx
@src/components/StatsFooter.tsx
@src/components/LearnAnchor.tsx

<interfaces>
PATTERNS.md §4 Group P-DIALOG locked recipes (copy verbatim — these are the canonical strings):

Dialog shell (PATTERNS.md §4 Group P-DIALOG, RESEARCH §"Example 3"):
- BEFORE: `border border-teal-100 bg-white`
- AFTER:  `border border-[var(--color-breathing-muted)] bg-[var(--color-breathing-surface)]`
- All other tokens on the className (modal-fade, m-auto, max-w-*, rounded-3xl, p-0, shadow-[var(--shadow-breathing-card)], backdrop:bg-[var(--color-modal-backdrop)]) STAY VERBATIM.

Title h2 (PATTERNS.md §4 Group P-DIALOG, substitution table):
- BEFORE: `text-2xl font-semibold tracking-tight text-slate-950`
- AFTER:  `text-2xl font-semibold tracking-tight text-[var(--color-breathing-accent-strong)]`

LearnDialog h3 headings (5 occurrences — lines 82, 86, 90, 104, 143):
- BEFORE: `text-base font-semibold text-slate-900`
- AFTER:  `text-base font-semibold text-[var(--color-breathing-accent-strong)]`

LearnDialog body p (3 occurrences — lines 83, 87, 92):
- BEFORE: `text-base leading-6 text-slate-700`
- AFTER:  `text-base leading-6 text-[var(--color-breathing-accent-strong)]`
- Note: PATTERNS.md mapping table maps text-slate-700 to accent-strong (not muted). The Learn body text is the highest-emphasis body copy in the app; keep contrast strong.

Secondary button — Close/Keep/Cancel (PATTERNS.md §4 Group P-DIALOG, RESEARCH §"Example 2", canonical for downstream reuse):
- BEFORE: `border border-teal-200 bg-white px-5 py-2 text-base font-semibold text-teal-800 shadow-sm transition hover:bg-teal-50 active:bg-teal-100 motion-reduce:transition-none focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-breathing-accent focus-visible:ring-offset-2`
- AFTER:  `border border-[var(--color-breathing-accent)] bg-[var(--color-breathing-surface)] px-5 py-2 text-base font-semibold text-[var(--color-breathing-accent-strong)] shadow-sm transition hover:bg-[var(--color-breathing-bg-soft)] active:bg-[var(--color-breathing-bg-soft)] motion-reduce:transition-none focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-breathing-accent focus-visible:ring-offset-2`
- The leading `min-h-12 rounded-full` part is preserved verbatim.

PRESERVE (AP-02 — DO NOT MODIFY):
- EndSessionDialog.tsx:81 — destructive End button — `bg-red-700 hover:bg-red-800 active:bg-red-900 text-white shadow-red-900/20 shadow-lg`. Touching ANY part of this line is a regression.
- ResetStatsDialog.tsx:76 — destructive Reset button — same shape.
- All 4 dialogs' `backdrop:bg-[var(--color-modal-backdrop)]` (already tokenized, Phase 16).
- All 4 dialogs' `shadow-[var(--shadow-breathing-card)]` (already tokenized; Phase 16 D-02 carry-forward).
- All `focus-visible:ring-breathing-accent` instances (AP-03 — Tailwind v4 auto-prefixed utility, already token-aware).

Existing dialog test files (DO NOT edit — D-06 locks class composition only, no semantic change):
- src/components/SettingsDialog.test.tsx
- src/components/LearnDialog.test.tsx
- src/components/EndSessionDialog.test.tsx (if exists — verify with `ls`)
- src/components/ResetStatsDialog.test.tsx
- src/app/App.dialog.test.tsx (asserts focus-visible:ring-breathing-accent on dialog buttons; these strings are NOT changed by this plan — RESEARCH §"Pattern 2")
- src/app/App.settings.test.tsx (asserts dialog open/close behavior, focus traps; orthogonal to className text)

Current banned-class occurrences (from RESEARCH §2 + verified grep 2026-05-13):
- SettingsDialog.tsx: lines 77, 80, 91 (3 occurrences)
- EndSessionDialog.tsx: lines 54, 59, 74 (3 in-scope) + line 81 (PRESERVE)
- ResetStatsDialog.tsx: lines 54, 59, 69 (3 in-scope) + line 76 (PRESERVE)
- LearnDialog.tsx: lines 74, 77, 82, 83, 86, 87, 90, 92, 104, 143, 181 (11 occurrences)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate SettingsDialog + EndSessionDialog + ResetStatsDialog</name>
  <files>src/components/SettingsDialog.tsx, src/components/EndSessionDialog.tsx, src/components/ResetStatsDialog.tsx</files>
  <read_first>
    - src/components/SettingsDialog.tsx (current state — banned classes on lines 77 dialog shell, 80 title, 91 Close button)
    - src/components/EndSessionDialog.tsx (banned classes on lines 54 shell, 59 title, 74 Keep button; line 81 destructive End button is PRESERVED)
    - src/components/ResetStatsDialog.tsx (banned classes on lines 54 shell, 59 title, 69 Keep button; line 76 destructive Reset button is PRESERVED)
    - .planning/phases/16.1-ui-token-migration/16.1-CONTEXT.md (D-01, D-05, D-06 — class-string substitution only, arbitrary-value form, preserve a11y)
    - .planning/phases/16.1-ui-token-migration/16.1-PATTERNS.md §4 Group P-DIALOG (locked recipes — dialog shell + title + secondary button)
    - .planning/phases/16.1-ui-token-migration/16.1-PATTERNS.md §5 (AP-02 sentinel — red destructive preservation)
    - src/components/ThemePicker.tsx:32 (Reference site A — selected-button tokenized recipe; the secondary-button form is structurally identical with different border-width and reversed bg/text role)
  </read_first>
  <action>
    For each of the 3 files, edit ONLY the className strings on the lines below. Per CONTEXT.md D-05 use the arbitrary-value form `*-[var(--color-breathing-*)]`. Per CONTEXT.md D-06 change className text only — no JSX structure, prop, ref, handler, ARIA attribute, or test-touching changes.

    **SettingsDialog.tsx:**

    - Line 77 (dialog `<dialog>` className):
      - Replace `border border-teal-100 bg-white` with `border border-[var(--color-breathing-muted)] bg-[var(--color-breathing-surface)]`.
      - Preserve `modal-fade m-auto max-w-md rounded-3xl ... p-0 shadow-[var(--shadow-breathing-card)] backdrop:bg-[var(--color-modal-backdrop)]` verbatim.

    - Line 80 (`<h2>` title):
      - Replace `text-slate-950` with `text-[var(--color-breathing-accent-strong)]`.
      - Preserve `text-2xl font-semibold tracking-tight` verbatim.

    - Line 91 (Close button):
      - Replace `border border-teal-200 bg-white ... text-teal-800 ... hover:bg-teal-50 active:bg-teal-100` with the canonical secondary-button form from the `<interfaces>` block above.
      - Preserve `min-h-12 rounded-full ... px-5 py-2 text-base font-semibold ... shadow-sm transition motion-reduce:transition-none focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-breathing-accent focus-visible:ring-offset-2` verbatim.

    **EndSessionDialog.tsx:**

    - Line 54 (dialog shell): same shell migration as SettingsDialog:77.
    - Line 59 (title): same title migration as SettingsDialog:80.
    - Line 74 (Keep button): same secondary-button migration as SettingsDialog:91.
    - Line 81 (destructive End button): **DO NOT TOUCH**. AP-02 sentinel — `bg-red-700 hover:bg-red-800 active:bg-red-900 text-white shadow-lg shadow-red-900/20` stays verbatim, including the `text-white`. Plan 07 (phase close) will decide whether to migrate `text-white` to `--color-breathing-on-accent` for danger-pair contrast consistency; for THIS task, do not modify.

    **ResetStatsDialog.tsx:**

    - Line 54 (dialog shell): same shell migration.
    - Line 59 (title): same title migration.
    - Line 69 (Keep button): same secondary-button migration.
    - Line 76 (destructive Reset button): **DO NOT TOUCH**. AP-02 sentinel.

    After all 3 files are edited, run:
    - `npx tsc --noEmit` (must exit 0).
    - `npm run lint` (must exit 0; no new warnings).
    - `npm run build` (must exit 0; Tailwind v4 must accept the arbitrary-value forms).
    - `npm test -- --run` (must exit 0; per-commit green-gate; the existing dialog tests assert behavior + a11y + focus-ring presence, all preserved).

    Commit as `feat(16.1-02): migrate SettingsDialog + EndSessionDialog + ResetStatsDialog chrome to --color-breathing-* tokens`.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run lint && grep -cE '\b(text|bg|border)-(slate|teal)-[0-9]|\b(text|bg)-white\b|\b(text|bg)-black\b' src/components/SettingsDialog.tsx src/components/EndSessionDialog.tsx src/components/ResetStatsDialog.tsx | awk -F: '{sum+=$2} END {print "banned:", sum}' && grep -cE 'bg-red-700' src/components/EndSessionDialog.tsx src/components/ResetStatsDialog.tsx && npm test -- --run src/components/SettingsDialog.test.tsx src/components/EndSessionDialog.test.tsx src/components/ResetStatsDialog.test.tsx src/app/App.dialog.test.tsx src/app/App.settings.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - SettingsDialog.tsx: `grep -cE '\b(text|bg|border)-(slate|teal)-[0-9]|\b(text|bg)-white\b' src/components/SettingsDialog.tsx` returns 0.
    - EndSessionDialog.tsx: `grep -cE '\b(text|bg|border)-(slate|teal)-[0-9]|\bbg-white\b' src/components/EndSessionDialog.tsx` returns 0; AND `grep -c '\btext-white\b' src/components/EndSessionDialog.tsx` returns 1 (the destructive End button — AP-02 preservation).
    - ResetStatsDialog.tsx: `grep -cE '\b(text|bg|border)-(slate|teal)-[0-9]|\bbg-white\b' src/components/ResetStatsDialog.tsx` returns 0; AND `grep -c '\btext-white\b' src/components/ResetStatsDialog.tsx` returns 1 (the destructive Reset button — AP-02).
    - `grep -c 'bg-red-700' src/components/EndSessionDialog.tsx` returns 1 AND `grep -c 'bg-red-700' src/components/ResetStatsDialog.tsx` returns 1 (red destructive preserved on both).
    - `grep -c 'focus-visible:ring-breathing-accent' src/components/SettingsDialog.tsx src/components/EndSessionDialog.tsx src/components/ResetStatsDialog.tsx | awk -F: '{sum+=$2} END {print sum}'` returns >= 3 (AP-03 sentinel — focus-ring utility preserved on every focusable button).
    - `grep -c 'var(--color-breathing-surface)' src/components/SettingsDialog.tsx src/components/EndSessionDialog.tsx src/components/ResetStatsDialog.tsx | awk -F: '{sum+=$2} END {print sum}'` returns >= 6 (2 sites × 3 files: shell bg + Keep button bg).
    - `npm test -- --run src/components/SettingsDialog.test.tsx src/components/EndSessionDialog.test.tsx src/components/ResetStatsDialog.test.tsx src/app/App.dialog.test.tsx src/app/App.settings.test.tsx` exits 0; no regressions.
    - `npx tsc --noEmit && npm run lint && npm run build` exits 0.
  </acceptance_criteria>
  <done>
    Three dialogs (SettingsDialog, EndSessionDialog, ResetStatsDialog) have token-migrated shells, titles, and Keep/Close buttons. Red destructive primaries on End/Reset preserved. The 6 banned-class lines (3 files × ~2 changed lines avg) cleared per file. Per-commit green-gate holds.
  </done>
</task>

<task type="auto">
  <name>Task 2: Migrate LearnDialog (heaviest of the four — 11 occurrences)</name>
  <files>src/components/LearnDialog.tsx</files>
  <read_first>
    - src/components/LearnDialog.tsx (current state — banned classes on lines 74 shell, 77 title, 82/86/90/104/143 h3 headings ×5, 83/87/92 body p ×3, 181 Close button)
    - .planning/phases/16.1-ui-token-migration/16.1-PATTERNS.md §4 Group P-DIALOG (same recipes as Task 1; LearnDialog has the additional h3 and body-p substitutions)
    - .planning/phases/16.1-ui-token-migration/16.1-RESEARCH.md §3 mapping table (text-slate-900 -> accent-strong; text-slate-700 -> accent-strong per PATTERNS.md §4)
    - src/components/LearnDialog.test.tsx (existing tests — D-06 means these continue to pass without edit; verify by reading the test list, do not edit)
  </read_first>
  <action>
    Edit `src/components/LearnDialog.tsx` className text only. Per CONTEXT.md D-05 + D-06.

    - Line 74 (dialog shell): replace `border border-teal-100 bg-white` with `border border-[var(--color-breathing-muted)] bg-[var(--color-breathing-surface)]`. Preserve `modal-fade m-auto max-h-[calc(100dvh-2rem)] w-[calc(100vw-2rem)] max-w-lg overflow-y-auto rounded-3xl ... p-0 shadow-[var(--shadow-breathing-card)] backdrop:bg-[var(--color-modal-backdrop)]` verbatim.

    - Line 77 (h2 title): replace `text-slate-950` with `text-[var(--color-breathing-accent-strong)]`. Preserve `text-2xl font-semibold tracking-tight`.

    - Lines 82, 86, 90, 104, 143 (h3 headings × 5): replace `text-slate-900` with `text-[var(--color-breathing-accent-strong)]`. Preserve `text-base font-semibold`.

    - Lines 83, 87, 92 (body p × 3): replace `text-slate-700` with `text-[var(--color-breathing-accent-strong)]`. Preserve `text-base leading-6` (line 92 also preserves `[&:not(:first-of-type)]:mt-2`).

    - Line 181 (Close button): replace with the canonical secondary-button form from `<interfaces>`. Preserve `min-h-12 rounded-full px-5 py-2 text-base font-semibold shadow-sm transition motion-reduce:transition-none focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-breathing-accent focus-visible:ring-offset-2` verbatim.

    Do NOT touch any other line. LearnDialog has external book/video links inside that already use `text-[var(--color-breathing-accent)]` (the Reference site B pattern from StatsFooter — verify by reading the file before editing); do not adjust them.

    After edits, run:
    - `npx tsc --noEmit` — exits 0.
    - `npm run lint` — exits 0.
    - `npm run build` — exits 0 (Tailwind v4 codegen accepts all arbitrary-value forms).
    - `npm test -- --run src/components/LearnDialog.test.tsx src/components/LearnAnchor.test.tsx` — exits 0.

    Commit as `feat(16.1-02): migrate LearnDialog chrome + body text to --color-breathing-* tokens`.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run lint && grep -cE '\b(text|bg|border)-(slate|teal)-[0-9]|\b(text|bg)-(white|black)\b' src/components/LearnDialog.tsx && grep -c 'focus-visible:ring-breathing-accent' src/components/LearnDialog.tsx && grep -c 'var(--color-breathing-accent-strong)' src/components/LearnDialog.tsx && npm test -- --run src/components/LearnDialog.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE '\b(text|bg|border)-(slate|teal)-[0-9]|\b(text|bg)-(white|black)\b' src/components/LearnDialog.tsx` returns 0 (all 11 banned occurrences eliminated).
    - `grep -c 'var(--color-breathing-accent-strong)' src/components/LearnDialog.tsx` returns >= 9 (1 title + 5 h3 + 3 body p — and possibly more if the Close button text token is also accent-strong = total >= 10).
    - `grep -c 'var(--color-breathing-surface)' src/components/LearnDialog.tsx` returns >= 2 (shell bg + Close button bg).
    - `grep -c 'var(--color-breathing-muted)' src/components/LearnDialog.tsx` returns >= 1 (shell border).
    - `grep -c 'focus-visible:ring-breathing-accent' src/components/LearnDialog.tsx` returns >= 1 (AP-03 sentinel — Close button focus ring preserved).
    - `npm test -- --run src/components/LearnDialog.test.tsx` exits 0; no test edits required (D-06).
    - `npx tsc --noEmit && npm run lint && npm run build` exits 0.
  </acceptance_criteria>
  <done>
    LearnDialog's 11 banned-class lines (shell + title + 5 h3 + 3 body-p + Close button) are fully tokenized. Per-commit green-gate holds. Combined with Task 1, the Group P-DIALOG migration is complete: dialog shell + secondary button class strings are locked as the canonical tokenized form for plan 03 (pickers) and plan 04 (anchors/stepper) to copy verbatim.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| no new boundaries | className text substitution only; no JSX structure, no new event handlers, no new data sources, no new effects. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16.1-02-01 | Tampering | EndSessionDialog/ResetStatsDialog destructive primary button | mitigate | AP-02 sentinel: `grep -c 'bg-red-700' src/components/EndSessionDialog.tsx src/components/ResetStatsDialog.tsx` must return 2 across both files post-task; mechanical preservation of the danger-pair className. |
| T-16.1-02-02 | Spoofing (a11y) | Focus-visible ring removal | mitigate | AP-03 sentinel: `grep -c 'focus-visible:ring-breathing-accent'` returns the pre-migration count on all 4 dialog Close/Keep buttons. App.dialog.test.tsx + App.settings.test.tsx already assert this on every focusable button; tests stay green = a11y preserved. |

This is a pure className substitution plan. No XSS / injection / auth surface introduced.
</threat_model>

<verification>
After both tasks complete, run the full Phase 16.1 per-commit green-gate:

```bash
npx tsc --noEmit && npm run lint && npm run build && npm test -- --run
```

Expected: exit 0.

Plan-level grep sentinels (run as a post-plan smoke check before commit):

```bash
# All 4 dialogs token-migrated, except destructive-red preservations:
grep -cE '\b(text|bg|border)-(slate|teal)-[0-9]|\bbg-white\b' src/components/SettingsDialog.tsx src/components/EndSessionDialog.tsx src/components/ResetStatsDialog.tsx src/components/LearnDialog.tsx
# Expected: all four lines show 0.

# text-white reduced to 2 occurrences (destructive End + Reset, AP-02 preserved):
grep -cE '\btext-white\b' src/components/EndSessionDialog.tsx src/components/ResetStatsDialog.tsx src/components/SettingsDialog.tsx src/components/LearnDialog.tsx | awk -F: '{sum+=$2} END {print sum}'
# Expected: 2 (one in EndSessionDialog:81, one in ResetStatsDialog:76).

# Red destructive preservation (AP-02):
grep -c 'bg-red-700' src/components/EndSessionDialog.tsx src/components/ResetStatsDialog.tsx
# Expected: 1 line per file (= 2 lines total in awk-sum form).

# Focus ring preservation (AP-03):
grep -c 'focus-visible:ring-breathing-accent' src/components/SettingsDialog.tsx src/components/EndSessionDialog.tsx src/components/ResetStatsDialog.tsx src/components/LearnDialog.tsx
# Expected: each file shows >= 1 focus-ring occurrence (Close/Keep + destructive button = >= 2 in 3 files, >= 1 in SettingsDialog).
```

Dialog test files (run individually as a focused smoke check):

```bash
npm test -- --run src/components/SettingsDialog.test.tsx src/components/EndSessionDialog.test.tsx src/components/ResetStatsDialog.test.tsx src/components/LearnDialog.test.tsx src/app/App.dialog.test.tsx src/app/App.settings.test.tsx
# Expected: exit 0; all dialog tests green; D-06 class-composition-only contract verified.
```
</verification>

<success_criteria>
1. `SettingsDialog.tsx`, `EndSessionDialog.tsx`, `ResetStatsDialog.tsx`, `LearnDialog.tsx` together contain ZERO matches for the 8 non-`text-white` banned regex patterns (after Wave 0 D-04 walker passes over them with --no-skip, these 4 files no longer appear in the violator list for slate-N / teal-N / bg-white).
2. The 2 destructive `text-white` occurrences in `EndSessionDialog:81` + `ResetStatsDialog:76` are PRESERVED (deferred to plan 07 for danger-pair contrast review). All OTHER `text-white` references in these 4 files are eliminated (= 0 in SettingsDialog and LearnDialog; = 1 in each of End/Reset).
3. `bg-red-700` count preserved at 1 occurrence per destructive dialog (= 2 total across End+Reset).
4. `focus-visible:ring-breathing-accent` count preserved at pre-migration value on every focusable button across all 4 dialogs.
5. `npx tsc --noEmit && npm run lint && npm run build && npm test -- --run` exits 0.
6. Existing dialog test files (SettingsDialog.test.tsx, LearnDialog.test.tsx, EndSessionDialog/ResetStats sibling tests if present, App.dialog.test.tsx, App.settings.test.tsx) pass unchanged (D-06 verified — class composition changes only, no test edits required).
7. The canonical "dialog shell" and "secondary button" class strings are locked in the 4 dialog files; plan 03 (pickers) and plan 04 (anchors/stepper) will copy these verbatim.
</success_criteria>

<output>
After completion, create `.planning/phases/16.1-ui-token-migration/16.1-02-SUMMARY.md` summarizing:
- 4 files migrated, ~22 banned-class occurrences cleared
- Canonical "secondary button" class string (verbatim) and where it appears (lines per file)
- Canonical "dialog shell" class string (verbatim) and the 4 sites
- 2 destructive-red preservations (EndSession:81, ResetStats:76) — confirm AP-02 sentinels green
- Test counts: pre-plan = post-plan = X (no new tests; no regressions)
- Notes for plan 03 + 04: the secondary-button string is now locked; downstream plans should `grep` it from these files to copy
</output>
