---
phase: 16.1
plan: 04
type: execute
wave: 2
depends_on: [16.1-01]
files_modified:
  - src/components/SettingsAnchor.tsx
  - src/components/LearnAnchor.tsx
  - src/components/SettingsStepper.tsx
  - src/components/MuteToggle.tsx
autonomous: true
requirements: [THEME-UI-01]
tags: [theme, tailwind-v4, anchor, stepper, alpha-modifier, tsx-migration]

must_haves:
  truths:
    - "SettingsAnchor + LearnAnchor disabled-and-enabled branches both rebind per palette (closes the half-migrated state from Phase 16 — disabled branch already tokenized, enabled branch was teal-hardcoded)"
    - "SettingsStepper fieldset + decrease/increase buttons + value-pill output rebind per palette"
    - "MuteToggle border + bg + text rebind per palette while `disabled:*` Tailwind variant prefixes are preserved verbatim (no JS-side conditional needed)"
    - "All 4 files' alpha-modifier sites (LearnAnchor:24 bg-white/70, SettingsAnchor:23 bg-white/70, SettingsStepper:36 bg-white/80) consume the D-02 strategy locked by plan 01"
    - "Per-commit green-gate (`npx tsc --noEmit && npm run lint && npm run build && npm test -- --run`) holds at every commit boundary"
  artifacts:
    - path: "src/components/SettingsAnchor.tsx"
      provides: "Tokenized enabled branch + alpha bg-[var(--color-breathing-surface)]/70 base"
      contains: "var(--color-breathing-accent)"
    - path: "src/components/LearnAnchor.tsx"
      provides: "Tokenized enabled branch + alpha bg base + tokenized disabled border"
      contains: "var(--color-breathing-accent-strong)"
    - path: "src/components/SettingsStepper.tsx"
      provides: "Tokenized fieldset + legend + decrease/increase buttons + value-pill"
      contains: "var(--color-breathing-bg-soft)"
    - path: "src/components/MuteToggle.tsx"
      provides: "Tokenized border + bg + text; disabled:* variant prefixes preserved verbatim"
      contains: "var(--color-breathing-surface)"
  key_links:
    - from: "src/components/LearnAnchor.tsx:24"
      to: "D-02 alpha-modifier strategy (theme.alpha-probe.test.ts JSDoc)"
      via: "bg-[var(--color-breathing-surface)]/70 OR inline style fallback"
      pattern: "var\\(--color-breathing-surface\\)"
    - from: "src/components/SettingsStepper.tsx:36"
      to: "D-02 alpha-modifier strategy"
      via: "bg-[var(--color-breathing-surface)]/80 OR inline style fallback"
      pattern: "var\\(--color-breathing-surface\\)"
    - from: "src/components/MuteToggle.tsx:49"
      to: "secondary-button canonical string (plan 02)"
      via: "same border+bg+text+hover+active token tuple, with disabled:* variants appended"
      pattern: "disabled:opacity-45"
---

<objective>
Wave 1 / Plan 04: Migrate Group P-STATE (anchor + stepper + mute toggle family) — `SettingsAnchor`, `LearnAnchor`, `SettingsStepper`, `MuteToggle`. These four components share the secondary-button shape (which plan 02 locked) but introduce TWO new wrinkles: (a) the conditional state-branch pattern (`disabled ? muted-branch : enabled-branch`) per PATTERNS.md Reference site C `LearnAnchor.tsx:24-27`, and (b) the alpha-modifier sites (`bg-white/70` and `bg-white/80`) gated on the D-02 strategy decision recorded in `theme.alpha-probe.test.ts` JSDoc (plan 01).

Purpose: Close the half-migrated state on the two anchors (disabled branch already tokenized in Phase 16; enabled branch was teal-hardcoded — PATTERNS.md Reference site C), bring the stepper chrome up to the tokenized canonical form, and tokenize the mute-toggle button (which uses Tailwind `disabled:*` variant prefixes instead of a JS conditional — the only secondary button in the migration surface using that pattern).

Output: 4 modified TSX files (~12 banned-class occurrences cleared; 3 alpha-modifier sites consume the D-02 strategy). No test edits (D-06).
</objective>

<execution_context>
@$HOME/.claude-personal-personal/get-shit-done/workflows/execute-plan.md
@$HOME/.claude-personal-personal/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/16.1-ui-token-migration/16.1-CONTEXT.md
@.planning/phases/16.1-ui-token-migration/16.1-PATTERNS.md
@.planning/phases/16.1-ui-token-migration/16.1-RESEARCH.md
@.planning/phases/16.1-ui-token-migration/16.1-01-SUMMARY.md
@src/styles/theme.alpha-probe.test.ts
@src/components/SettingsAnchor.tsx
@src/components/LearnAnchor.tsx
@src/components/SettingsStepper.tsx
@src/components/MuteToggle.tsx
@src/components/StatsFooter.tsx

<interfaces>
PATTERNS.md §4 Group P-STATE locked recipes:

Anchor pattern (Reference site C — `LearnAnchor.tsx:24-27` migration shape, locked in PATTERNS.md §1):

Base segment (line 24 — common to both branches; preserve transition + a11y verbatim):
- BEFORE: `absolute right-0 top-0 inline-flex min-h-[44px] min-w-[44px] items-center justify-center gap-1.5 rounded-full border bg-white/70 px-2.5 py-2 text-sm font-semibold shadow-sm backdrop-blur-sm transition sm:px-4 focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-breathing-accent focus-visible:ring-offset-2 motion-reduce:transition-none`
- AFTER:  same line but `bg-white/70` -> `bg-[var(--color-breathing-surface)]/70` (Path A — D-02 default; switch to inline style if probe surfaces a transparent render at execution time per plan 01 JSDoc).

Disabled branch (line 26 — `LearnAnchor` / line 25 `SettingsAnchor`):
- BEFORE: `cursor-not-allowed border-slate-200 text-[var(--color-breathing-muted)]`
- AFTER:  `cursor-not-allowed border-[var(--color-breathing-muted)] text-[var(--color-breathing-muted)]`

Enabled branch (line 27 — `LearnAnchor` / line 26 `SettingsAnchor`):
- BEFORE: `border-teal-200 text-teal-800 hover:bg-teal-50 active:bg-teal-100`
- AFTER:  `border-[var(--color-breathing-accent)] text-[var(--color-breathing-accent-strong)] hover:bg-[var(--color-breathing-bg-soft)] active:bg-[var(--color-breathing-bg-soft)]`

SettingsStepper.tsx recipes (PATTERNS.md §4 Group P-ALPHA + P-STATE + P-SUB mixed):

- Line 36 (fieldset chrome):
  - BEFORE: `rounded-3xl border border-teal-100 bg-white/80 p-4 shadow-sm shadow-teal-900/5`
  - AFTER:  `rounded-3xl border border-[var(--color-breathing-muted)] bg-[var(--color-breathing-surface)]/80 p-4 shadow-sm shadow-teal-900/5`
  - Note: `shadow-teal-900/5` is a colored shadow — NOT in the banned regex list (alpha is `/5`, not `/`-prefixed). Per CONTEXT.md "Out of scope" and PATTERNS.md Group P-DIALOG anti-pattern reminder ("DO NOT change shadow-[var(--shadow-breathing-card)]"), colored Tailwind palette shadows that ARE NOT in the banned regex list are NOT in 16.1's scope. Preserve `shadow-sm shadow-teal-900/5` verbatim. (The grep guard does NOT match this; sentinel confirms below.)

- Line 38 (`<legend>`):
  - BEFORE: `px-1 text-sm font-semibold uppercase tracking-[0.18em] text-slate-600`
  - AFTER:  `px-1 text-sm font-semibold uppercase tracking-[0.18em] text-[var(--color-breathing-muted)]`

- Lines 45 + 60 (decrease/increase `<button>`):
  - BEFORE: `grid size-12 min-h-11 min-w-11 place-items-center rounded-full border border-teal-200 bg-white text-2xl leading-none text-teal-800 shadow-sm transition hover:bg-teal-50 active:bg-teal-100 motion-reduce:transition-none focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-breathing-accent focus-visible:ring-offset-2 disabled:cursor-not-allowed disabled:opacity-45`
  - AFTER:  `grid size-12 min-h-11 min-w-11 place-items-center rounded-full border border-[var(--color-breathing-accent)] bg-[var(--color-breathing-surface)] text-2xl leading-none text-[var(--color-breathing-accent-strong)] shadow-sm transition hover:bg-[var(--color-breathing-bg-soft)] active:bg-[var(--color-breathing-bg-soft)] motion-reduce:transition-none focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-breathing-accent focus-visible:ring-offset-2 disabled:cursor-not-allowed disabled:opacity-45`

- Line 53 (`<output>` value pill):
  - BEFORE: `min-w-32 rounded-2xl bg-teal-50 px-4 py-3 text-center text-2xl font-semibold text-slate-950`
  - AFTER:  `min-w-32 rounded-2xl bg-[var(--color-breathing-bg-soft)] px-4 py-3 text-center text-2xl font-semibold text-[var(--color-breathing-accent-strong)]`

MuteToggle.tsx (PATTERNS.md §4 Group P-STATE special case — line 49):
- BEFORE: `grid size-11 min-h-11 min-w-11 place-items-center rounded-full border border-teal-200 bg-white text-teal-800 shadow-sm transition hover:bg-teal-50 active:bg-teal-100 motion-reduce:transition-none focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-breathing-accent focus-visible:ring-offset-2 disabled:cursor-not-allowed disabled:opacity-45`
- AFTER:  `grid size-11 min-h-11 min-w-11 place-items-center rounded-full border border-[var(--color-breathing-accent)] bg-[var(--color-breathing-surface)] text-[var(--color-breathing-accent-strong)] shadow-sm transition hover:bg-[var(--color-breathing-bg-soft)] active:bg-[var(--color-breathing-bg-soft)] motion-reduce:transition-none focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-breathing-accent focus-visible:ring-offset-2 disabled:cursor-not-allowed disabled:opacity-45`
- Note: PATTERNS.md confirms `disabled:cursor-not-allowed disabled:opacity-45` are preserved verbatim — Tailwind `disabled:*` variant prefixes do not require a JS conditional; the opacity-45 attenuates the foreground TOKEN color, which composes correctly post-migration (RESEARCH §"Edge Case 3").

Alpha-modifier sites in this plan (3 of the 5 D-02 sites):
- LearnAnchor.tsx:24 — `bg-white/70`
- SettingsAnchor.tsx:23 — `bg-white/70`
- SettingsStepper.tsx:36 — `bg-white/80`
- (Plan 05 handles the other 2: App.tsx:608 + SessionReadout.tsx:39,79.)

D-02 strategy (locked in plan 01 — `src/styles/theme.alpha-probe.test.ts` JSDoc):
- DEFAULT (Path A): `bg-[var(--color-breathing-surface)]/70` / `bg-[var(--color-breathing-surface)]/80`
- FALLBACK (Path B): inline `style={{ backgroundColor: 'rgb(from var(--color-breathing-surface) r g b / 0.7)' }}` — requires Chrome 119+ / Safari 16.4+ / Firefox 113+ (current evergreen).
- Decision rule (from plan 01 acceptance criteria): if Path A produces a `transparent` / `rgba(0,0,0,0)` computed background during the dev-server smoke check at task-time, switch to Path B for ALL 3 sites in this plan AND the 3 sites in plan 05 (consistency — no per-file divergence).

Existing tests (DO NOT edit):
- src/components/LearnAnchor.test.tsx (asserts hit-area floor, focus-ring, aria-disabled propagation; orthogonal to color)
- src/components/SettingsAnchor.test.tsx (same shape)
- src/components/MuteToggle.test.tsx (asserts aria-pressed, aria-describedby resume-hint, disabled state)
- src/app/App.settings.test.tsx (asserts SettingsAnchor + dialog flow; no color assertions)
- SettingsStepper has no dedicated test file (Phase 7-era; spot-checked via App tests).

Current banned-class occurrences (verified grep 2026-05-13):
- SettingsAnchor.tsx: 3 (line 23 alpha, lines 25-26 conditional branches)
- LearnAnchor.tsx: 3 (line 24 alpha, lines 26-27 conditional branches)
- SettingsStepper.tsx: 5 (line 36 fieldset alpha+border, line 38 legend, lines 45+60 stepper buttons, line 53 value pill)
- MuteToggle.tsx: 1 (line 49 — entire button className)
- Total: 12 banned-class occurrences across the 4 files.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate SettingsAnchor + LearnAnchor (alpha-base + state branches)</name>
  <files>src/components/SettingsAnchor.tsx, src/components/LearnAnchor.tsx</files>
  <read_first>
    - src/components/SettingsAnchor.tsx (line 23 alpha base, line 25 disabled branch, line 26 enabled branch — verify current line numbers)
    - src/components/LearnAnchor.tsx (line 24 alpha base, line 26 disabled, line 27 enabled — Reference site C; the migration "shape" is locked in PATTERNS.md §1 Reference site C)
    - .planning/phases/16.1-ui-token-migration/16.1-PATTERNS.md §1 Reference site C (the LITERAL post-migration form to produce — copy verbatim)
    - .planning/phases/16.1-ui-token-migration/16.1-PATTERNS.md §4 Group P-STATE (anchor pattern recipe)
    - src/styles/theme.alpha-probe.test.ts (D-02 strategy decision — read JSDoc to know whether Path A or Path B applies)
    - .planning/phases/16.1-ui-token-migration/16.1-CONTEXT.md D-02 + D-05 + D-06
  </read_first>
  <action>
    For each anchor file, apply the Reference site C migration shape from PATTERNS.md §1 verbatim. Per CONTEXT.md D-05 + D-06.

    **LearnAnchor.tsx** (the canonical Reference site C — implement first; it becomes the literal template for SettingsAnchor):

    - Line 24 (template-literal base segment): replace ONLY the substring `bg-white/70` with `bg-[var(--color-breathing-surface)]/70` (D-02 Path A default). Every other class on line 24 is preserved verbatim — `absolute right-0 top-0 inline-flex min-h-[44px] min-w-[44px] items-center justify-center gap-1.5 rounded-full border ... px-2.5 py-2 text-sm font-semibold shadow-sm backdrop-blur-sm transition sm:px-4 focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-breathing-accent focus-visible:ring-offset-2 motion-reduce:transition-none`.

      D-02 fallback (Path B): if a dev-server smoke check during this task surfaces a transparent computed background, replace the className-side `bg-white/70` with NOTHING and add an inline `style={{ backgroundColor: 'rgb(from var(--color-breathing-surface) r g b / 0.7)' }}` to the `<button>` JSX. Use ternary if inline style needs to vary by branch (it does not for this site — same alpha for both branches). Apply the same fallback to SettingsAnchor and SettingsStepper (Task 2) for consistency — do not mix Path A and Path B across the 3 sites.

    - Line 26 (disabled branch — currently has `border-slate-200 text-[var(--color-breathing-muted)]`): replace `border-slate-200` with `border-[var(--color-breathing-muted)]`. Preserve `cursor-not-allowed text-[var(--color-breathing-muted)]` verbatim.

    - Line 27 (enabled branch — currently `border-teal-200 text-teal-800 hover:bg-teal-50 active:bg-teal-100`): replace entirely with `border-[var(--color-breathing-accent)] text-[var(--color-breathing-accent-strong)] hover:bg-[var(--color-breathing-bg-soft)] active:bg-[var(--color-breathing-bg-soft)]`.

    Result: line 24-28 of `LearnAnchor.tsx` matches the literal post-migration form in PATTERNS.md §1 Reference site C "Migration shape" code block.

    **SettingsAnchor.tsx:**

    - Line 23 (alpha base): same `bg-white/70` -> `bg-[var(--color-breathing-surface)]/70` (Path A) or inline-style fallback (Path B) substitution as LearnAnchor:24.

    - Line 25 (disabled branch): same `border-slate-200` -> `border-[var(--color-breathing-muted)]` substitution.

    - Line 26 (enabled branch): same enabled-branch replacement.

    Note: SettingsAnchor's base segment differs from LearnAnchor's only in `absolute left-0 top-0` vs `absolute right-0 top-0` — the rest is identical. Verify by reading the file before editing.

    After both files edited:
    - `npx tsc --noEmit && npm run lint && npm run build && npm test -- --run src/components/LearnAnchor.test.tsx src/components/SettingsAnchor.test.tsx src/app/App.settings.test.tsx` — exits 0.
    - Spot-check (manual or `grep -nC 2`): the post-migration LearnAnchor lines 24-28 match PATTERNS.md §1 Reference site C "Migration shape" code block literally (modulo whitespace).

    Commit as `feat(16.1-04): migrate SettingsAnchor + LearnAnchor disabled+enabled branches to --color-breathing-* tokens`.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run lint && grep -cE '\b(text|bg|border)-(slate|teal)-[0-9]|\b(text|bg)-(white|black)\b' src/components/SettingsAnchor.tsx src/components/LearnAnchor.tsx | awk -F: '{sum+=$2} END {print "banned:", sum}' && grep -c 'focus-visible:ring-breathing-accent' src/components/SettingsAnchor.tsx src/components/LearnAnchor.tsx && grep -c 'var(--color-breathing-surface)' src/components/LearnAnchor.tsx src/components/SettingsAnchor.tsx && npm test -- --run src/components/LearnAnchor.test.tsx src/components/SettingsAnchor.test.tsx src/app/App.settings.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE '\b(text|bg|border)-(slate|teal)-[0-9]|\b(text|bg)-(white|black)\b' src/components/SettingsAnchor.tsx src/components/LearnAnchor.tsx | awk -F: '{sum+=$2} END {print sum}'` returns 0 (all 6 banned-class lines across both anchors eliminated).
    - `grep -c 'focus-visible:ring-breathing-accent' src/components/SettingsAnchor.tsx src/components/LearnAnchor.tsx | awk -F: '{sum+=$2} END {print sum}'` returns >= 2 (AP-03 sentinel — focus ring preserved on both anchors).
    - For Path A: `grep -c 'bg-\[var(--color-breathing-surface)\]/70' src/components/LearnAnchor.tsx` returns 1; same for SettingsAnchor.tsx. For Path B: `grep -c 'rgb(from var(--color-breathing-surface)' src/components/LearnAnchor.tsx` returns 1 AND the className substring `bg-white/70` is absent (`grep -c 'bg-white/70' src/components/LearnAnchor.tsx` returns 0).
    - `grep -c 'var(--color-breathing-accent-strong)' src/components/LearnAnchor.tsx src/components/SettingsAnchor.tsx | awk -F: '{sum+=$2} END {print sum}'` returns >= 2 (enabled-branch text on both anchors).
    - `npm test -- --run src/components/LearnAnchor.test.tsx src/components/SettingsAnchor.test.tsx src/app/App.settings.test.tsx` exits 0 (hit-area floor, focus-ring, aria-disabled assertions preserved — D-06).
    - `npx tsc --noEmit && npm run lint && npm run build` exits 0.
  </acceptance_criteria>
  <done>
    Both anchor files are tokenized end-to-end: alpha base segment + disabled branch + enabled branch all reference `var(--color-breathing-*)`. The half-migrated state from Phase 16 (disabled branch already tokenized; enabled branch teal-hardcoded — PATTERNS.md Reference site C "current state") is fully resolved. The D-02 strategy (Path A or Path B per plan 01 JSDoc) is consistently applied; if Path B was needed, the inline-style fallback is in place on both anchors and will be applied to SettingsStepper in Task 2 + plan 05 sites for consistency.
  </done>
</task>

<task type="auto">
  <name>Task 2: Migrate SettingsStepper + MuteToggle</name>
  <files>src/components/SettingsStepper.tsx, src/components/MuteToggle.tsx</files>
  <read_first>
    - src/components/SettingsStepper.tsx (lines 36 fieldset, 38 legend, 45 + 60 stepper buttons, 53 value pill — verify line numbers)
    - src/components/MuteToggle.tsx (line 49 button — the only `disabled:*`-variant secondary button in the migration surface)
    - .planning/phases/16.1-ui-token-migration/16.1-PATTERNS.md §4 Group P-STATE "SettingsStepper.tsx special case" + "MuteToggle.tsx special case"
    - .planning/phases/16.1-ui-token-migration/16.1-RESEARCH.md §3 mapping table (`bg-teal-50` -> `bg-[var(--color-breathing-bg-soft)]`; `text-slate-950` -> `text-[var(--color-breathing-accent-strong)]`)
    - .planning/phases/16.1-ui-token-migration/16.1-CONTEXT.md D-05 + D-06
    - .planning/phases/16.1-ui-token-migration/16.1-04-... NOTE: read Task 1's output before this task (LearnAnchor/SettingsAnchor are the immediate precedent for the alpha-modifier strategy choice).
  </read_first>
  <action>
    **SettingsStepper.tsx:**

    - Line 36 (fieldset className):
      - Replace `border border-teal-100` with `border border-[var(--color-breathing-muted)]`.
      - Replace `bg-white/80` with `bg-[var(--color-breathing-surface)]/80` (Path A) OR remove + add inline style `style={{ backgroundColor: 'rgb(from var(--color-breathing-surface) r g b / 0.8)' }}` (Path B; apply only if Task 1 settled on Path B for the anchors).
      - Preserve `rounded-3xl ... p-4 shadow-sm shadow-teal-900/5` verbatim. `shadow-teal-900/5` is NOT in the banned regex list (alpha modifier without preceding digit pattern) and is out of 16.1 scope per CONTEXT.md "Out of scope". See sentinel below.

    - Line 38 (`<legend>`):
      - Replace `text-slate-600` with `text-[var(--color-breathing-muted)]`.
      - Preserve `px-1 text-sm font-semibold uppercase tracking-[0.18em]` verbatim.

    - Line 45 (decrease `<button>`):
      - Replace `border border-teal-200 bg-white ... text-teal-800 ... hover:bg-teal-50 active:bg-teal-100` with `border border-[var(--color-breathing-accent)] bg-[var(--color-breathing-surface)] ... text-[var(--color-breathing-accent-strong)] ... hover:bg-[var(--color-breathing-bg-soft)] active:bg-[var(--color-breathing-bg-soft)]`.
      - Preserve `grid size-12 min-h-11 min-w-11 place-items-center rounded-full ... text-2xl leading-none ... shadow-sm transition motion-reduce:transition-none focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-breathing-accent focus-visible:ring-offset-2 disabled:cursor-not-allowed disabled:opacity-45` verbatim.

    - Line 53 (`<output>` value pill):
      - Replace `bg-teal-50 ... text-slate-950` with `bg-[var(--color-breathing-bg-soft)] ... text-[var(--color-breathing-accent-strong)]`.
      - Preserve `min-w-32 rounded-2xl px-4 py-3 text-center text-2xl font-semibold` verbatim.

    - Line 60 (increase `<button>`): same migration as line 45.

    **MuteToggle.tsx:**

    - Line 49 (button className):
      - Replace `border border-teal-200 bg-white text-teal-800 ... hover:bg-teal-50 active:bg-teal-100` with `border border-[var(--color-breathing-accent)] bg-[var(--color-breathing-surface)] text-[var(--color-breathing-accent-strong)] ... hover:bg-[var(--color-breathing-bg-soft)] active:bg-[var(--color-breathing-bg-soft)]`.
      - Preserve EVERY OTHER segment verbatim, including (critical for a11y + behavior preservation under D-06):
        - `grid size-11 min-h-11 min-w-11 place-items-center rounded-full`
        - `shadow-sm transition motion-reduce:transition-none`
        - `focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-breathing-accent focus-visible:ring-offset-2`
        - `disabled:cursor-not-allowed disabled:opacity-45`  (PATTERNS.md §4 Group P-STATE MuteToggle special case — these variant prefixes layer on top of the now-tokenized base; opacity-45 attenuates the foreground TOKEN color, no JS conditional needed)

    After both files edited:
    - `npx tsc --noEmit && npm run lint && npm run build && npm test -- --run src/components/MuteToggle.test.tsx src/app/App.session.test.tsx src/app/App.settings.test.tsx` — exits 0.
    - Verify per-button-state visual coherence in jsdom assertion form: the test file `MuteToggle.test.tsx` already asserts the button is rendered + has the `disabled` attribute when `audioAvailable` is false; tokenization does not affect these assertions.

    Commit as `feat(16.1-04): migrate SettingsStepper chrome + MuteToggle button to --color-breathing-* tokens`.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run lint && grep -cE '\b(text|bg|border)-(slate|teal)-[0-9]|\b(text|bg)-(white|black)\b' src/components/SettingsStepper.tsx src/components/MuteToggle.tsx | awk -F: '{sum+=$2} END {print "banned:", sum}' && grep -c 'shadow-teal-900/5' src/components/SettingsStepper.tsx && grep -c 'disabled:opacity-45' src/components/MuteToggle.tsx && grep -c 'focus-visible:ring-breathing-accent' src/components/SettingsStepper.tsx src/components/MuteToggle.tsx && npm test -- --run src/components/MuteToggle.test.tsx src/app/App.session.test.tsx src/app/App.settings.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE '\b(text|bg|border)-(slate|teal)-[0-9]|\b(text|bg)-(white|black)\b' src/components/SettingsStepper.tsx src/components/MuteToggle.tsx | awk -F: '{sum+=$2} END {print sum}'` returns 0 (all 6 banned-class lines across both files eliminated).
    - `grep -c 'shadow-teal-900/5' src/components/SettingsStepper.tsx` returns 1 (CONTEXT.md Out-of-Scope sentinel — colored shadow alpha is NOT in regex scope and stays). This is a known carry-forward, not a regression.
    - `grep -c 'disabled:cursor-not-allowed disabled:opacity-45' src/components/MuteToggle.tsx` returns 1 (PATTERNS.md MuteToggle special case — `disabled:*` Tailwind variants preserved verbatim).
    - `grep -c 'disabled:cursor-not-allowed disabled:opacity-45' src/components/SettingsStepper.tsx` returns 2 (one per stepper button — lines 45 and 60).
    - `grep -c 'focus-visible:ring-breathing-accent' src/components/SettingsStepper.tsx src/components/MuteToggle.tsx | awk -F: '{sum+=$2} END {print sum}'` returns >= 3 (AP-03 — 2 stepper buttons + 1 mute toggle).
    - `grep -c 'var(--color-breathing-bg-soft)' src/components/SettingsStepper.tsx` returns >= 1 (value-pill bg + hover/active states on stepper buttons — combined count is more, but minimum sentinel = 1).
    - `npm test -- --run src/components/MuteToggle.test.tsx src/app/App.session.test.tsx src/app/App.settings.test.tsx` exits 0.
    - `npx tsc --noEmit && npm run lint && npm run build` exits 0.
  </acceptance_criteria>
  <done>
    SettingsStepper chrome (fieldset + legend + 2 stepper buttons + value pill) and MuteToggle button are fully tokenized. The `disabled:*` Tailwind variant preservation on MuteToggle:49 and SettingsStepper:45,60 is verified — `opacity-45` attenuates the foreground TOKEN color, no JS conditional is required (PATTERNS.md §4 MuteToggle special case). Combined with Task 1, all 4 files in Group P-STATE are migrated.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| inline style={} fallback path (D-02 Path B, conditional on probe) | Only triggered if Tailwind v4 alpha-modifier parsing fails. Inline style uses a CSS-native `rgb(from var(...) ...)` expression — pure CSS, no JS string concatenation, no untrusted input. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16.1-04-01 | Tampering | inline style={{ backgroundColor: ... }} fallback | mitigate | Path B uses a hardcoded string literal (`rgb(from var(--color-breathing-surface) r g b / 0.7)`); no concatenation from props or state, no XSS surface. CSS-native expression — same security profile as a static className. |
| T-16.1-04-02 | Spoofing (a11y) | MuteToggle disabled:* variant prefix removal | mitigate | `grep -c 'disabled:cursor-not-allowed disabled:opacity-45'` sentinel in acceptance_criteria ensures the prefixes are preserved verbatim; if accidentally stripped, the disabled visual state would silently regress to the enabled appearance (UX bug, not security). |
| T-16.1-04-03 | Repudiation | LearnAnchor / SettingsAnchor focus-ring removal | mitigate | AP-03 sentinel: `grep -c 'focus-visible:ring-breathing-accent'` returns >= 2 across both anchors; App tests assert focus-ring presence; both gates green = a11y preserved. |

Inline style fallback (D-02 Path B) does NOT introduce an XSS surface: the string is a static literal, not a template substitution. No `dangerouslySetInnerHTML`, no string-concatenated CSS, no user-provided color values. Threat model remains minimal as scoped by the orchestrator.
</threat_model>

<verification>
After both tasks complete, run the per-commit green-gate:

```bash
npx tsc --noEmit && npm run lint && npm run build && npm test -- --run
```

Plan-level grep sentinels:

```bash
# All 4 files token-migrated:
grep -cE '\b(text|bg|border)-(slate|teal)-[0-9]|\b(text|bg)-(white|black)\b' \
  src/components/SettingsAnchor.tsx src/components/LearnAnchor.tsx \
  src/components/SettingsStepper.tsx src/components/MuteToggle.tsx
# Expected: 0 on every line.

# AP-03 — focus-ring preservation:
grep -c 'focus-visible:ring-breathing-accent' \
  src/components/SettingsAnchor.tsx src/components/LearnAnchor.tsx \
  src/components/SettingsStepper.tsx src/components/MuteToggle.tsx
# Expected: each line >= 1 (multiple buttons in SettingsStepper).

# Disabled:* variant preservation (PATTERNS.md MuteToggle + SettingsStepper):
grep -c 'disabled:cursor-not-allowed disabled:opacity-45' \
  src/components/MuteToggle.tsx src/components/SettingsStepper.tsx
# Expected: MuteToggle=1, SettingsStepper=2 (one per stepper button).

# Colored-shadow carry-forward (NOT in 16.1 scope):
grep -c 'shadow-teal-900' src/components/SettingsStepper.tsx
# Expected: 1 (`shadow-teal-900/5` on the fieldset — known out-of-scope, preserved).

# D-02 alpha-modifier sites (3 of 5 in this plan):
grep -c 'bg-\[var(--color-breathing-surface)\]/70\|bg-\[var(--color-breathing-surface)\]/80\|rgb(from var(--color-breathing-surface)' \
  src/components/LearnAnchor.tsx src/components/SettingsAnchor.tsx src/components/SettingsStepper.tsx
# Expected: each line >= 1 (Path A or Path B; consistent across the 3 sites).
```
</verification>

<success_criteria>
1. SettingsAnchor.tsx, LearnAnchor.tsx, SettingsStepper.tsx, MuteToggle.tsx together contain ZERO banned-regex matches (after running the Wave 0 D-04 walker with --no-skip).
2. The 3 alpha-modifier sites (LearnAnchor:24, SettingsAnchor:23, SettingsStepper:36) consistently apply EITHER Path A (`bg-[var(--color-breathing-surface)]/70`-style) OR Path B (inline style with `rgb(from ...)`) — no mix per CONTEXT.md D-02.
3. `disabled:cursor-not-allowed disabled:opacity-45` is preserved verbatim on MuteToggle:49 (1×) and SettingsStepper:45+60 (2×) — PATTERNS.md §4 MuteToggle special case.
4. `shadow-teal-900/5` on SettingsStepper:36 is preserved (known carry-forward — CONTEXT.md "Out of scope"; not in banned regex list).
5. `focus-visible:ring-breathing-accent` is preserved on every focusable button across the 4 files (AP-03 sentinel).
6. All existing test files for these components pass without edit (D-06 — class composition only).
7. `npx tsc --noEmit && npm run lint && npm run build && npm test -- --run` exits 0.
</success_criteria>

<output>
After completion, create `.planning/phases/16.1-ui-token-migration/16.1-04-SUMMARY.md` summarizing:
- 4 files migrated, ~12 banned-class lines cleared
- D-02 strategy realized: which Path (A or B) ended up in production, with the dev-server smoke check result (computed background-color observed for `bg-[var(--color-breathing-surface)]/70`) as evidence
- LearnAnchor.tsx lines 24-28 confirmed structurally identical to PATTERNS.md §1 Reference site C "Migration shape" (the canonical post-migration anchor pattern)
- `disabled:*` variant preservation confirmed on 3 sites (MuteToggle, 2× SettingsStepper button)
- `shadow-teal-900/5` carry-forward documented (CONTEXT.md Out of Scope — not regressing)
- Test counts pre/post-plan identical (D-06 compliance)
</output>
