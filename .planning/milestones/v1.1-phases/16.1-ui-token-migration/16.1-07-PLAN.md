---
phase: 16.1
plan: 07
type: execute
wave: 4
depends_on: [16.1-01, 16.1-02, 16.1-03, 16.1-04, 16.1-05, 16.1-06]
files_modified:
  - src/index.css
  - src/components/EndSessionDialog.tsx
  - src/components/ResetStatsDialog.tsx
  - src/styles/theme.no-hardcoded-classes.test.ts
  - .planning/REQUIREMENTS.md
  - .planning/STATE.md
  - .planning/ROADMAP.md
  - .planning/phases/16.1-ui-token-migration/16.1-SUMMARY.md
autonomous: true
requirements: [THEME-UI-01]
tags: [theme, index-css, regression-guard, phase-close, summary]

must_haves:
  truths:
    - "src/index.css :root color and body::selection color rebind per palette (D-03)"
    - "EndSessionDialog:81 + ResetStatsDialog:76 destructive-red buttons no longer reference the banned text-white literal — text foreground is text-[var(--color-breathing-on-accent)]; bg-red-* + shadow-red-*/* remain palette-independent danger semantic"
    - "theme.no-hardcoded-classes.test.ts is no longer .skip'd and exits GREEN (10 banned-pattern iterations all return zero hits against src/**/*.tsx)"
    - "Phase-level grep gate exits zero hits: `grep -rEn --include='*.tsx' --exclude='*.test.tsx' '<banned-regex>' src/` returns 0 lines"
    - "REQUIREMENTS.md THEME-UI-01 status is flipped to Done with bullet checkbox marked"
    - "STATE.md current-position reflects Phase 16.1 complete; current focus advances to Phase 17"
    - "ROADMAP.md Phase 16.1 entry has all plan rows marked [x] and `Plans: N plans` finalized"
    - "Phase SUMMARY.md created enumerating all 7 plans, files modified, key decisions, contrast ratios, test counts, carry-forwards"
  artifacts:
    - path: "src/index.css"
      provides: ":root body color + body::selection color tokenized"
      contains: "var(--color-breathing-accent-strong)"
    - path: "src/components/EndSessionDialog.tsx"
      provides: "Destructive End button text foreground = on-accent token (bg-red-700 + shadow-red-900/20 preserved)"
      contains: "var(--color-breathing-on-accent)"
    - path: "src/components/ResetStatsDialog.tsx"
      provides: "Destructive Reset button text foreground = on-accent token"
      contains: "var(--color-breathing-on-accent)"
    - path: "src/styles/theme.no-hardcoded-classes.test.ts"
      provides: "describe.each (not .skip) — guard flips GREEN"
      contains: "describe.each(BANNED_PATTERNS)"
    - path: ".planning/REQUIREMENTS.md"
      provides: "THEME-UI-01 Done with checkbox"
      contains: "[x] **THEME-UI-01**"
    - path: ".planning/STATE.md"
      provides: "Phase 16.1 complete; current focus = Phase 17"
      contains: "Phase 17"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 16.1 marked [x] with all plan rows complete"
      contains: "Phase 16.1: UI Token Migration"
    - path: ".planning/phases/16.1-ui-token-migration/16.1-SUMMARY.md"
      provides: "End-to-end phase summary"
      contains: "Phase 16.1: UI Token Migration"
  key_links:
    - from: "src/index.css body color"
      to: "src/styles/theme.css --color-breathing-accent-strong"
      via: "var(--color-breathing-accent-strong)"
      pattern: "var\\(--color-breathing-accent-strong\\)"
    - from: "src/index.css body::selection color"
      to: "src/styles/theme.css --color-breathing-on-accent (D-01)"
      via: "var(--color-breathing-on-accent)"
      pattern: "var\\(--color-breathing-on-accent\\)"
    - from: "theme.no-hardcoded-classes.test.ts"
      to: "src/**/*.tsx production files"
      via: "readdirSync + readFileSync + regex.test (10 banned patterns)"
      pattern: "describe.each\\(BANNED_PATTERNS\\)"
---

<objective>
Wave 3 / Plan 07 (phase close): finish the remaining four threads — (a) migrate `src/index.css` lines 6 and 22 per CONTEXT.md D-03, (b) close the two destructive `text-white` lines on EndSessionDialog:81 + ResetStatsDialog:76 (the only remaining banned-class hits after plan 06), (c) flip the `theme.no-hardcoded-classes.test.ts` guard from `.skip` to active so it runs on every commit going forward, (d) update planning documents (`REQUIREMENTS.md` flip THEME-UI-01 to Done, `STATE.md` advance to Phase 17, `ROADMAP.md` mark 16.1 complete) and produce the phase SUMMARY.

Purpose: This is the phase-close plan. It satisfies the goal-backward must-haves: success criterion #2 (grep-zero on `src/**/*.tsx`), success criterion #3 (THEME-05 contrast guard plus new accent-strong vs on-accent guard both green), and success criterion #1 perceptually (already validated in plan 06 UAT). After this plan, the migration guard is the long-term safety net for Phase 17/18/19.

Output: 3 modified `.tsx`/`.css` files + 1 modified test file (guard flip) + 3 planning docs + 1 SUMMARY = 8 files.
</objective>

<execution_context>
@$HOME/.claude-personal-personal/get-shit-done/workflows/execute-plan.md
@$HOME/.claude-personal-personal/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/16.1-ui-token-migration/16.1-CONTEXT.md
@.planning/phases/16.1-ui-token-migration/16.1-PATTERNS.md
@.planning/phases/16.1-ui-token-migration/16.1-RESEARCH.md
@.planning/phases/16.1-ui-token-migration/16.1-VALIDATION.md
@.planning/phases/16.1-ui-token-migration/16.1-01-SUMMARY.md
@.planning/phases/16.1-ui-token-migration/16.1-02-SUMMARY.md
@.planning/phases/16.1-ui-token-migration/16.1-03-SUMMARY.md
@.planning/phases/16.1-ui-token-migration/16.1-04-SUMMARY.md
@.planning/phases/16.1-ui-token-migration/16.1-05-SUMMARY.md
@.planning/phases/16.1-ui-token-migration/16.1-06-SUMMARY.md
@src/index.css
@src/styles/theme.css
@src/styles/theme.no-hardcoded-classes.test.ts
@src/components/EndSessionDialog.tsx
@src/components/ResetStatsDialog.tsx

<interfaces>
D-03 src/index.css migration (PATTERNS.md §4 Group P-INDEX locked recipe):

- Line 5-7 (`:root` color):
  - BEFORE:
    ```css
    :root {
      color: var(--color-slate-900);
      background: var(--color-breathing-bg);
    ```
  - AFTER:
    ```css
    :root {
      color: var(--color-breathing-accent-strong);  /* heading/body text role on light surface */
      background: var(--color-breathing-bg);        /* unchanged — already tokenized */
    ```
  - Rationale (PATTERNS.md §4 Group P-INDEX): `var(--color-slate-900)` is Tailwind v4's built-in slate-900 (not the themable `--color-breathing-*` set), so it does NOT rebind on theme switch. Mapping to `--color-breathing-accent-strong` makes the body text rebind per palette.

- Line 21-24 (`body::selection`):
  - BEFORE:
    ```css
    body::selection {
      color: white;
      background: var(--color-breathing-accent);
    }
    ```
  - AFTER:
    ```css
    body::selection {
      color: var(--color-breathing-on-accent);  /* D-01 — text-on-accent role */
      background: var(--color-breathing-accent);
    }
    ```
  - Rationale (PATTERNS.md §4 Group P-INDEX): D-01's `--color-breathing-on-accent` token is purpose-built for "foreground on accent surface" — selection text-on-accent-background is exactly this role. This is the SECOND consumer of `--color-breathing-on-accent` (after SessionControls in plan 06; selection styling is a polish surface but consistent with the D-01 contract).

Destructive `text-white` migration (closing the last 2 banned-class hits — option (a) from plan 01 Task 3):

- EndSessionDialog.tsx:81 — destructive End button:
  - BEFORE: `... bg-red-700 ... text-white shadow-lg shadow-red-900/20 transition hover:bg-red-800 active:bg-red-900 ...`
  - AFTER:  `... bg-red-700 ... text-[var(--color-breathing-on-accent)] shadow-lg shadow-red-900/20 transition hover:bg-red-800 active:bg-red-900 ...`
  - Replace ONLY `text-white` with `text-[var(--color-breathing-on-accent)]`. Preserve `bg-red-700`, `hover:bg-red-800`, `active:bg-red-900`, `shadow-red-900/20`, AND every other utility class on the line verbatim. The red bg + red-shadow remain palette-independent danger semantic; only the foreground token is theme-aware.

- ResetStatsDialog.tsx:76 — destructive Reset button: same migration as EndSessionDialog:81.

Contrast verification of D-01 token on red destructive (validates option (a) is safe across all 5 palettes):
- Per plan 01 D-01 token values: `--color-breathing-on-accent` = white (Light/Moss/Slate), dark slate-900 (Dark), deep violet (Dusk).
- Red destructive `bg-red-700` = `#b91c1c` (Tailwind v4 default).
- White on `#b91c1c` (Light/Moss/Slate cases): WCAG contrast ratio ≈ 6.4 → far exceeds 1.5 floor.
- Dark slate-900 (`#0f172a`) on `#b91c1c` (Dark case): luminance gap is moderate; ratio ≈ 4.5 → also exceeds 1.5.
- Deep violet (`#1e0550`) on `#b91c1c` (Dusk case): both colors are dark; ratio ≈ 1.6-2.0 → BORDERLINE BUT clears the 1.5 floor (Dusk's amber accent never touches red surface; this is the only place red and Dusk's on-accent interact).
- This contrast review is not formally automated (the `theme.contrast.test.ts` only checks accent-strong vs on-accent — adding red-vs-on-accent would expand THEME-05 scope which is OUT of 16.1 scope per CONTEXT.md). Sentinel: the per-palette UAT in plan 06 ALREADY confirmed legibility on the on-accent foreground for plan 06's primary button consumer; the destructive button consumer of the SAME token uses a different bg (red), so a brief manual spot-check during phase close is sufficient (3rd task below).

Guard test flip:
- `src/styles/theme.no-hardcoded-classes.test.ts` was created in plan 01 with `describe.each.skip` (or equivalent `describe.skip` modifier on the top-level describe). Plan 07 removes the `.skip` so the suite runs on every commit.
- The act of "flipping to green" is a single one-character edit: `describe.each.skip(BANNED_PATTERNS)` -> `describe.each(BANNED_PATTERNS)`.
- After the flip, the test MUST pass — 10/10 banned-pattern iterations return zero hits against the production migration surface.

Planning document updates:
- `.planning/REQUIREMENTS.md`:
  - Flip THEME-UI-01 checkbox from `- [ ]` to `- [x]`.
  - Flip traceability table row from `Pending` to `Done`.
- `.planning/STATE.md`:
  - Update `stopped_at` (set to "Phase 16.1 complete"), `last_updated` (current ISO date), `last_activity` (today's date).
  - Update `progress.completed_phases` (was 4, becomes 5 if Phase 16.1 counts as a phase — confirm by reading STATE.md `progress` block).
  - Update "Current focus" line to Phase 17 (CUST-03 Visual Variants).
  - Add a row to "Performance Metrics > By Phase (v1.1)" for "16.1. UI Token Migration | 7 | Complete".
  - Update "Session Continuity" block to point to `.planning/phases/16.1-ui-token-migration/16.1-SUMMARY.md` as the next resume file.
  - Update "Operator Next Steps" to suggest `/gsd-plan-phase 17`.
- `.planning/ROADMAP.md`:
  - Flip Phase 16.1 checkbox from `- [ ]` to `- [x]`.
  - Update "Plans: TBD" line to "Plans: 7 plans" with all rows marked `[x]`.
  - Update progress table: `16.1. UI Token Migration | v1.1 | 7/7 | Complete | <date>`.

Phase SUMMARY structure (PATTERNS-driven outline for `16.1-SUMMARY.md`):
- Frontmatter: phase: 16.1 / completed / plans: 7 / requirements: [THEME-UI-01]
- Summary section: 1 paragraph end-to-end recap (16-file migration, +1 token, +1 guard test, contrast guard extended, 7 plans across 4 waves, ~58 banned-class lines cleared)
- Plans section: 1 paragraph per plan summarizing what landed
- Locked Decisions Honored: D-01..D-06 with how each was preserved
- Test Counts: pre-phase / post-phase / new tests added (alpha-probe, contrast extension, no-hardcoded-classes guard)
- Files Modified table: per file, which plan changed it, what changed
- Carry-Forward / Out of Scope confirmations: aesthetic palette retune (separate todo), red-* destructive bg (intentional), shadow-teal-900/* (4 sites — SessionControls + SessionReadout + SettingsStepper carry over), shadow-breathing-card (Phase 16 D-02)
- Next Phase: Phase 17 (Visual Variants)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate src/index.css D-03 + close destructive text-white on End/Reset dialogs</name>
  <files>src/index.css, src/components/EndSessionDialog.tsx, src/components/ResetStatsDialog.tsx</files>
  <read_first>
    - src/index.css (full file — 36 lines; the 2 migration sites are lines 6 and 22)
    - src/components/EndSessionDialog.tsx (line 81 destructive End button — only the text-white fragment migrates; red bg + red shadow are preserved verbatim)
    - src/components/ResetStatsDialog.tsx (line 76 destructive Reset button — same shape)
    - .planning/phases/16.1-ui-token-migration/16.1-PATTERNS.md §4 Group P-INDEX (D-03 index.css recipe — locked)
    - .planning/phases/16.1-ui-token-migration/16.1-PATTERNS.md §5 AP-02 (preserve `bg-red-700`/`hover:bg-red-800`/`active:bg-red-900`/`shadow-red-900/20` — only text-white is touched)
    - .planning/phases/16.1-ui-token-migration/16.1-CONTEXT.md D-01 + D-03 + D-06
    - src/styles/theme.css (sanity check: `--color-breathing-on-accent` exists in all 5 palettes from plan 01)
    - src/components/SessionControls.tsx (plan 06 — confirm the same `text-[var(--color-breathing-on-accent)]` substitution to lock the pattern consistency)
  </read_first>
  <action>
    **src/index.css (2 line edits):**

    - Line 6: change `color: var(--color-slate-900);` to `color: var(--color-breathing-accent-strong);` (per PATTERNS.md §4 Group P-INDEX).

    - Line 22: change `color: white;` to `color: var(--color-breathing-on-accent);` (per PATTERNS.md §4 Group P-INDEX — D-01 is the canonical "foreground on accent" token).

    - Do not touch any other line. The `background: var(--color-breathing-bg);` on line 7 (or its line number after the line-6 edit) is already token-aware and stays.

    **src/components/EndSessionDialog.tsx line 81:**

    - Replace ONLY the substring `text-white` with `text-[var(--color-breathing-on-accent)]`. Every other class on the line (`min-h-12 rounded-full bg-red-700 px-5 py-2 text-base font-semibold shadow-lg shadow-red-900/20 transition hover:bg-red-800 active:bg-red-900 motion-reduce:transition-none focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-breathing-accent focus-visible:ring-offset-2`) is PRESERVED verbatim (AP-02 — danger semantic intact).

    **src/components/ResetStatsDialog.tsx line 76:**

    - Same migration as EndSessionDialog:81 — replace only `text-white` with `text-[var(--color-breathing-on-accent)]`.

    After all 3 files edited:
    - `npx tsc --noEmit && npm run lint && npm run build` — exits 0.
    - `npm test -- --run src/components/EndSessionDialog.test.tsx src/components/ResetStatsDialog.test.tsx src/app/App.dialog.test.tsx src/app/App.session.test.tsx` — exits 0 (D-06 — class composition only; existing dialog tests pass).
    - Run the phase-level grep sentinel (the SUCCESS CRITERION #2 grep from CONTEXT.md / 16.1-RESEARCH.md):

      ```bash
      grep -rEn --include='*.tsx' --exclude='*.test.tsx' --exclude='*.stories.tsx' \
        '\b(text|bg|border)-(slate|teal)-[0-9]|\b(text|bg)-(white|black)\b' src/
      ```

      Expected: ZERO lines. (Success criterion #2 of the phase goal: "No production component references a literal `text-slate-[0-9]`, `bg-slate-[0-9]`, `border-slate-[0-9]`, `text-teal-[0-9]`, `bg-teal-[0-9]`, `border-teal-[0-9]`, `text-white`, `bg-white`, `text-black`, or `bg-black` Tailwind class on `src/**/*.tsx`.").

    - Spot-check the destructive button visual: brief manual check (load app, open EndSessionDialog or ResetStatsDialog during a session, observe the End/Reset button text legibility on each palette). Per the `<interfaces>` rationale, all 5 palettes mathematically clear 1.5 contrast on red-700; visually verify for Dark + Dusk specifically (the 2 cases where on-accent is a dark color on a still-dark red).

    Commit as `feat(16.1-07): migrate index.css + destructive text-white to --color-breathing-on-accent (D-01, D-03)`.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run lint && grep -cE '\b(text|bg|border)-(slate|teal)-[0-9]|\b(text|bg)-(white|black)\b' src/index.css src/components/EndSessionDialog.tsx src/components/ResetStatsDialog.tsx | awk -F: '{sum+=$2} END {print "banned:", sum}' && grep -c 'var(--color-slate-900)' src/index.css && grep -c 'var(--color-breathing-on-accent)' src/index.css src/components/EndSessionDialog.tsx src/components/ResetStatsDialog.tsx && grep -c 'bg-red-700' src/components/EndSessionDialog.tsx src/components/ResetStatsDialog.tsx && (! grep -rEn --include='*.tsx' --exclude='*.test.tsx' --exclude='*.stories.tsx' '\b(text|bg|border)-(slate|teal)-[0-9]|\b(text|bg)-(white|black)\b' src/) && npm test -- --run src/components/EndSessionDialog.test.tsx src/components/ResetStatsDialog.test.tsx src/app/App.dialog.test.tsx src/app/App.session.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE '\b(text|bg|border)-(slate|teal)-[0-9]|\b(text|bg)-(white|black)\b' src/index.css src/components/EndSessionDialog.tsx src/components/ResetStatsDialog.tsx | awk -F: '{sum+=$2} END {print sum}'` returns 0. (Note: `index.css` is CSS not TSX, so the regex is a coarse check — `text-slate-900` Tailwind class is NOT present in CSS, but `var(--color-slate-900)` IS; this regex pattern does not match `var(--color-slate-900)` — separate sentinel below for that.)
    - `grep -c 'var(--color-slate-900)' src/index.css` returns 0 (D-03 migration done — the Tailwind v4 built-in slate-900 reference is replaced with the themable accent-strong).
    - `grep -c 'var(--color-breathing-on-accent)' src/index.css` returns 1 (body::selection consumer — the 2nd codebase consumer of D-01 after SessionControls).
    - `grep -c 'var(--color-breathing-accent-strong)' src/index.css` returns 1 (:root body color — D-03 destination).
    - `grep -c 'var(--color-breathing-on-accent)' src/components/EndSessionDialog.tsx src/components/ResetStatsDialog.tsx | awk -F: '{sum+=$2} END {print sum}'` returns 2 (one per file — destructive button text foreground now tokenized).
    - `grep -c 'bg-red-700' src/components/EndSessionDialog.tsx src/components/ResetStatsDialog.tsx | awk -F: '{sum+=$2} END {print sum}'` returns 2 (AP-02 — destructive red bg preserved on both).
    - `grep -c 'shadow-red-900/20' src/components/EndSessionDialog.tsx src/components/ResetStatsDialog.tsx | awk -F: '{sum+=$2} END {print sum}'` returns 2 (AP-02 — destructive red shadow preserved on both; same out-of-scope treatment as shadow-teal-900/N).
    - `grep -c '\btext-white\b' src/components/EndSessionDialog.tsx src/components/ResetStatsDialog.tsx | awk -F: '{sum+=$2} END {print sum}'` returns 0 (all text-white references in production .tsx are eliminated).
    - The PHASE-LEVEL grep sentinel returns ZERO matches: `grep -rEn --include='*.tsx' --exclude='*.test.tsx' --exclude='*.stories.tsx' '\b(text|bg|border)-(slate|teal)-[0-9]|\b(text|bg)-(white|black)\b' src/` exits with code 1 (grep "no match") and prints zero lines. This is the **success criterion #2 acceptance gate** for the entire phase.
    - All dialog/App tests exit 0; full chain (`npx tsc --noEmit && npm run lint && npm run build && npm test -- --run`) exits 0.
  </acceptance_criteria>
  <done>
    `src/index.css` is fully tokenized (no Tailwind built-in slate-900 reference; body text and selection color rebind per palette). The 2 destructive `text-white` lines on EndSessionDialog:81 + ResetStatsDialog:76 are tokenized to the D-01 `--color-breathing-on-accent` token while preserving the red danger semantic (bg + shadow remain palette-independent). The phase-level grep sentinel — the locked success criterion #2 of the ROADMAP phase entry — returns ZERO hits across all production `.tsx`.
  </done>
</task>

<task type="auto">
  <name>Task 2: Flip theme.no-hardcoded-classes.test.ts to GREEN + full chain verification</name>
  <files>src/styles/theme.no-hardcoded-classes.test.ts</files>
  <read_first>
    - src/styles/theme.no-hardcoded-classes.test.ts (current state — `describe.each.skip(BANNED_PATTERNS)` or top-level `describe.skip` wrapper from plan 01 Task 3)
    - .planning/phases/16.1-ui-token-migration/16.1-01-SUMMARY.md (plan 01's documented "RED count" — the number of banned hits the guard would have caught against the pre-Wave-1 codebase; used as a paper-trail comparison)
    - .planning/phases/16.1-ui-token-migration/16.1-CONTEXT.md D-04 (test file lifecycle — RED at creation, GREEN at phase close)
  </read_first>
  <action>
    Edit `src/styles/theme.no-hardcoded-classes.test.ts`:

    - Remove the `.skip` modifier from the `describe.each` (or top-level `describe`) block. If the file was authored with `describe.each.skip(BANNED_PATTERNS)`, change to `describe.each(BANNED_PATTERNS)`. If the wrapping pattern was `describe.skip('banned class hardcoded utilities', ...)`, change to `describe('banned class hardcoded utilities', ...)`.

    - Remove the "TEMPORARILY SKIPPED" JSDoc block from plan 01 Task 3 (or replace it with a "GREEN as of Phase 16.1 close" note for the audit trail).

    Run `npx vitest run src/styles/theme.no-hardcoded-classes.test.ts`:
    - Expected: exits 0; reporter shows 10 PASSED iterations (one per banned pattern, each returning zero hits against the migration surface).

    Run the full chain:
    - `npx tsc --noEmit && npm run lint && npm run build && npm test -- --run` — exits 0.
    - Includes the now-active guard: any future commit re-introducing a banned class will fail CI.

    Commit as `feat(16.1-07): enable theme.no-hardcoded-classes.test.ts guard (flip RED -> GREEN at phase close)`.
  </action>
  <verify>
    <automated>! grep -q 'describe.each.skip\|describe.skip' src/styles/theme.no-hardcoded-classes.test.ts && npx vitest run src/styles/theme.no-hardcoded-classes.test.ts --reporter=verbose 2>&1 | tail -10 && npx tsc --noEmit && npm run lint && npm run build && npm test -- --run</automated>
  </verify>
  <acceptance_criteria>
    - `grep -E 'describe.each.skip|describe.skip' src/styles/theme.no-hardcoded-classes.test.ts` returns no matches (skip modifier removed).
    - `grep -E 'describe.each\(BANNED_PATTERNS' src/styles/theme.no-hardcoded-classes.test.ts` returns 1 match (the test is now active).
    - `npx vitest run src/styles/theme.no-hardcoded-classes.test.ts` exits 0; reporter shows 10 of 10 tests passed.
    - Full chain `npx tsc --noEmit && npm run lint && npm run build && npm test -- --run` exits 0.
    - The total Vitest test count increases by exactly 10 from the pre-plan-01 baseline (5 new from contrast extension + 10 new from this guard - we don't know how many other tests pre-existed; just confirm no test regression).
  </acceptance_criteria>
  <done>
    `theme.no-hardcoded-classes.test.ts` is now an active CI gate. Every future commit (Phase 17/18/19 and beyond) will fail if any of the 10 banned regex patterns reappears in production `.tsx`. This is the long-term safety net the Phase 16.1 contract promised.
  </done>
</task>

<task type="auto">
  <name>Task 3: Update planning documents (REQUIREMENTS, STATE, ROADMAP) + create phase SUMMARY</name>
  <files>.planning/REQUIREMENTS.md, .planning/STATE.md, .planning/ROADMAP.md, .planning/phases/16.1-ui-token-migration/16.1-SUMMARY.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (current state — THEME-UI-01 row added in plan 01; currently `Pending` traceability)
    - .planning/STATE.md (full current state — Phase 16.1 listed as `Not started (INSERTED)` in Performance Metrics; `Current focus` line is Phase 16.1)
    - .planning/ROADMAP.md (Phase 16.1 entry — `Plans: TBD (run /gsd-plan-phase 16.1 to break down)`; progress table row marked `Not started`)
    - .planning/phases/16-themes/16-SUMMARY.md (structural template for the phase SUMMARY — frontmatter shape, sections used, Locked-Decisions-Honored block, Test-Counts block, Files-Modified table, Carry-Forward block, Next-Phase block)
    - All 6 prior plan SUMMARYs (16.1-01..16.1-06) — content sourced from these
  </read_first>
  <action>
    **Update `.planning/REQUIREMENTS.md`:**

    - Flip the THEME-UI-01 bullet from `- [ ] **THEME-UI-01**:` to `- [x] **THEME-UI-01**:`.
    - Flip the traceability table row from `| THEME-UI-01 | Phase 16.1 | Pending |` to `| THEME-UI-01 | Phase 16.1 | Done |`.
    - Update the "Last updated: 2026-05-13 — Phase 16 THEME-01..05 traceability flipped to Done" line to add "; Phase 16.1 THEME-UI-01 flipped to Done" (or the equivalent ISO-formatted current-date append).

    **Update `.planning/STATE.md`:**

    - `gsd_state_version`, `milestone`, `milestone_name` unchanged.
    - `status: planning` -> `status: planning` (next phase being planned; do NOT mark milestone complete — there are still Phases 17, 18, 19 in v1.1).
    - `stopped_at: Phase 16 themes complete` -> `stopped_at: Phase 16.1 UI token migration complete`.
    - `last_updated` -> current ISO datetime.
    - `last_activity` -> current date.
    - `progress.completed_phases` -> increment by 1 (verify the current value first).
    - `progress.total_plans` and `progress.completed_plans` -> add 7 plans to each (the 7 plans 16.1-01..07).
    - `progress.percent` -> recompute based on updated completed/total_phases.
    - `## Current Position` block: `Phase: 16.1` -> `Phase: 17`; `Plan: Not started`; `Status: Ready to plan`; `Last activity: <current-date>`.
    - `## Performance Metrics > By Phase (v1.1)` table: change `| 16.1. UI Token Migration | TBD | Not started (INSERTED) |` to `| 16.1. UI Token Migration | 7 | Complete |`.
    - `## Accumulated Context > Roadmap Evolution`: append a 1-line bullet: "Phase 16.1 complete — 16 production .tsx files + index.css migrated to --color-breathing-* tokens; D-01 --color-breathing-on-accent added; theme.no-hardcoded-classes.test.ts guard active".
    - `## Session Continuity` block: `Last session` -> current datetime; `Stopped at: Phase 16 themes complete` -> `Stopped at: Phase 16.1 UI token migration complete`; `Resume file: .planning/phases/16-themes/16-SUMMARY.md` -> `Resume file: .planning/phases/16.1-ui-token-migration/16.1-SUMMARY.md`; `Next command: Phase 16.1 (UI Token Migration) planning — /gsd-plan-phase 16.1` -> `Next command: Phase 17 (Visual Variants) planning — /gsd-plan-phase 17`.
    - `## Operator Next Steps` block: replace the existing first bullet with: `Begin Phase 17 (Visual Variants) planning: /gsd-plan-phase 17`.

    **Update `.planning/ROADMAP.md`:**

    - Phase 16.1 list-item line: change `- [ ] **Phase 16.1: UI Token Migration (INSERTED 2026-05-13)** - ...` to `- [x] **Phase 16.1: UI Token Migration (INSERTED 2026-05-13)** - ... (completed <date>)`.
    - Phase 16.1 "Phase Details" entry: change `**Plans**: TBD (run \`/gsd-plan-phase 16.1\` to break down)` to:

      ```markdown
      **Plans**: 7 plans
        - [x] 16.1-01-PLAN.md — Wave 0 preflight (REQUIREMENTS + D-01 token + contrast extension + D-04 guard scaffold + D-02 probe)
        - [x] 16.1-02-PLAN.md — Wave 1 dialog chrome (Settings/EndSession/ResetStats/LearnDialog)
        - [x] 16.1-03-PLAN.md — Wave 1 pickers (Theme/Timbre/Variant/Language; closes WR-01)
        - [x] 16.1-04-PLAN.md — Wave 1 anchors + stepper + MuteToggle (Group P-STATE)
        - [x] 16.1-05-PLAN.md — Wave 1 page chrome (App.tsx + BreathingShape + SessionReadout)
        - [x] 16.1-06-PLAN.md — Wave 2 SessionControls primary button (D-01 on-accent consumer) + per-palette UAT
        - [x] 16.1-07-PLAN.md — Wave 3 index.css D-03 + destructive text-white closure + guard flip + phase close
      ```

    - Progress table row: change `| 16.1. UI Token Migration | v1.1 | 0/? | Not started | - |` to `| 16.1. UI Token Migration | v1.1 | 7/7 | Complete | <date> |`.

    **Create `.planning/phases/16.1-ui-token-migration/16.1-SUMMARY.md`:**

    Use the Phase 16 SUMMARY as the structural template. Sections:

    - Frontmatter:
      ```
      ---
      phase: 16.1
      completed: <date>
      plans: 7
      requirements:
        - THEME-UI-01
      ---
      ```

    - `# Phase 16.1: UI Token Migration — Phase Summary`

    - `## Summary` (1 paragraph): end-to-end recap covering ~58 banned-class occurrences cleared across 16 production `.tsx` files + `src/index.css`; 8th chrome token `--color-breathing-on-accent` defined per-palette (D-01); `theme.no-hardcoded-classes.test.ts` regression guard now active; THEME-05 contrast guard extended with 6th iteration (`accent-strong vs on-accent` per palette); D-02 alpha-modifier strategy realized (Path A or B — record final decision); 7 plans across 4 waves; per-commit green-gate held throughout; zero new npm dependencies; per-palette UAT (plan 06) confirmed visual rebind on all 5 palettes.

    - `## Plans` — 7 subsections, one per plan. For each:
      - Plan name + 1-paragraph what-landed summary.
      - Key output files.
      - Commit refs if known (otherwise the SUMMARY file of that plan).

    - `## Locked Decisions Honored` — table mapping D-01..D-06 + plan-01's RED/GREEN test pattern to the specific files/commits that honor each.

    - `## Test Counts` — pre-phase / per-plan delta / post-phase. Specifically:
      - Pre-Phase-16.1 baseline (after Phase 16 close): 492 tests / 39 files.
      - Plan 01 additions: +5 contrast iterations (accent-strong vs on-accent) + 10 RED guard (skipped initially) + alpha-probe (1-3 tests).
      - Plans 02-06: 0 new tests (D-06 — class composition only).
      - Plan 07 additions: 10 guard iterations flip from SKIPPED to PASSED (no count delta from skipped tests, but the active count increases).
      - Post-phase total: 492 + 5 + 10 + alpha-probe = ~507-509 tests / ~41 files.

    - `## Files Modified` table — 19 files total (17 source + 3 planning docs + 1 SUMMARY itself):

      | File | Plan | Change |
      |------|------|--------|
      | `.planning/REQUIREMENTS.md` | 16.1-01 / 07 | THEME-UI-01 added (Pending) / flipped to Done |
      | `src/styles/theme.css` | 16.1-01 | +5 lines `--color-breathing-on-accent` per palette |
      | `src/styles/theme.contrast.test.ts` | 16.1-01 | +1 `it()` block: accent-strong vs on-accent (5 iterations) |
      | `src/styles/theme.no-hardcoded-classes.test.ts` | 16.1-01 / 07 | NEW (RED, skipped) / flipped to GREEN active |
      | `src/styles/theme.alpha-probe.test.ts` | 16.1-01 | NEW: D-02 strategy decision recorded |
      | `src/components/SettingsDialog.tsx` | 16.1-02 | Shell + title + Close button tokenized |
      | `src/components/EndSessionDialog.tsx` | 16.1-02 / 07 | Shell + title + Keep button / destructive text-white -> on-accent |
      | `src/components/ResetStatsDialog.tsx` | 16.1-02 / 07 | same shape |
      | `src/components/LearnDialog.tsx` | 16.1-02 | 11 sites: shell + title + 5 h3 + 3 body p + Close button |
      | `src/components/ThemePicker.tsx` | 16.1-03 | Label + unselectedClasses tokenized (closes WR-01) |
      | `src/components/TimbrePicker.tsx` | 16.1-03 | Label + ternary active |
      | `src/components/VariantPicker.tsx` | 16.1-03 | Label + ternary active |
      | `src/components/LanguagePicker.tsx` | 16.1-03 | Label + ternary active |
      | `src/components/SettingsAnchor.tsx` | 16.1-04 | Alpha base + disabled + enabled branches |
      | `src/components/LearnAnchor.tsx` | 16.1-04 | Alpha base + disabled + enabled branches |
      | `src/components/SettingsStepper.tsx` | 16.1-04 | Fieldset + legend + 2 stepper buttons + value pill |
      | `src/components/MuteToggle.tsx` | 16.1-04 | Border + bg + text with `disabled:*` variant preservation |
      | `src/app/App.tsx` | 16.1-05 | 5 sites: main + eyebrow + h1 + card surface + helper |
      | `src/components/BreathingShape.tsx` | 16.1-05 | Phase label + lead-in digit text |
      | `src/components/SessionReadout.tsx` | 16.1-05 | 7 sites: 2 containers + 2 time chips + 2 eyebrows + complete msg |
      | `src/components/SessionControls.tsx` | 16.1-06 | Primary button × 2 layouts (D-01 first consumer) |
      | `src/index.css` | 16.1-07 | :root color + body::selection color tokenized (D-03) |
      | `.planning/STATE.md` | 16.1-07 | Phase 16.1 complete; current focus -> Phase 17 |
      | `.planning/ROADMAP.md` | 16.1-07 | Phase 16.1 [x]; 7/7 plans listed |

    - `## Carry-Forward` — explicitly enumerate:
      - `shadow-teal-900/20` on SessionControls:52,69 — RESEARCH Assumption A3; banned regex does not cover shadow-* utilities; same out-of-scope treatment as the existing `shadow-breathing-card` Phase 16 D-02 carry-forward and `shadow-teal-900/5` on SessionReadout:29,64 + SettingsStepper:36.
      - `shadow-red-900/20` on EndSessionDialog:81 + ResetStatsDialog:76 — destructive red shadow; danger semantic; intentionally palette-independent.
      - Red destructive `bg-red-700` + `hover:bg-red-800` + `active:bg-red-900` — AP-02; intentionally palette-independent; only foreground (text-white -> on-accent) tokenized in plan 07.
      - Aesthetic palette retune — `.planning/todos/pending/2026-05-13-themes-aesthetic-refresh.md` flags this; out of 16.1 scope per CONTEXT.md.
      - WR-02 `useThemeChoice` cross-tab event subscription — Phase 16 code review warning; AP-08 reminder; out of 16.1 scope.

    - `## Next Phase` — Phase 17 (Visual Variants). The tokenized secondary-button form locked here (plans 02-05) is the canonical pattern Phase 17/18 picker UIs should copy when wiring up real variant/timbre selection.

    After all 4 docs updated, run the full chain one final time:
    - `npx tsc --noEmit && npm run lint && npm run build && npm test -- --run` — exits 0.

    Commit as `docs(16.1-07): phase close — REQUIREMENTS/STATE/ROADMAP updates + SUMMARY`.
  </action>
  <verify>
    <automated>grep -q "\\[x\\] \\*\\*THEME-UI-01\\*\\*" .planning/REQUIREMENTS.md && grep -q "THEME-UI-01 | Phase 16.1 | Done" .planning/REQUIREMENTS.md && grep -q "Phase 16.1 UI token migration complete\\|Phase 16.1 complete" .planning/STATE.md && grep -q "\\[x\\] \\*\\*Phase 16.1: UI Token Migration" .planning/ROADMAP.md && grep -q "7/7 | Complete" .planning/ROADMAP.md && test -f .planning/phases/16.1-ui-token-migration/16.1-SUMMARY.md && grep -q "phase: 16.1" .planning/phases/16.1-ui-token-migration/16.1-SUMMARY.md && npx tsc --noEmit && npm run lint && npm run build && npm test -- --run</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/REQUIREMENTS.md` contains `- [x] **THEME-UI-01**` AND the traceability row `| THEME-UI-01 | Phase 16.1 | Done |`.
    - `.planning/STATE.md` has `Phase: 17` in the Current Position block; `By Phase (v1.1)` table shows `16.1. UI Token Migration | 7 | Complete`; `Session Continuity` block points to `.planning/phases/16.1-ui-token-migration/16.1-SUMMARY.md`.
    - `.planning/ROADMAP.md` has `- [x] **Phase 16.1` and `Plans: 7 plans` with 7 `[x]` plan rows AND a progress-table row `7/7 | Complete`.
    - `.planning/phases/16.1-ui-token-migration/16.1-SUMMARY.md` exists, has the prescribed sections (Summary, Plans, Locked Decisions Honored, Test Counts, Files Modified, Carry-Forward, Next Phase), and frontmatter `phase: 16.1` + `plans: 7` + `requirements: [THEME-UI-01]`.
    - Full chain `npx tsc --noEmit && npm run lint && npm run build && npm test -- --run` exits 0.
    - PHASE-LEVEL grep sentinel still passes (from Task 1): `grep -rEn --include='*.tsx' --exclude='*.test.tsx' --exclude='*.stories.tsx' '<banned-regex>' src/` returns ZERO lines.
    - `theme.no-hardcoded-classes.test.ts` reports 10/10 passing (Task 2 outcome preserved).
    - `theme.contrast.test.ts` reports 10/10 passing (5 reduced-motion crossfade + 5 accent-strong vs on-accent).
  </acceptance_criteria>
  <done>
    Phase 16.1 is fully closed. The 3 planning documents reflect completion. `16.1-SUMMARY.md` is the canonical resume file. Phase 17 is unblocked.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| no new boundaries | className/CSS substitution + planning-doc edits + test config flip. No JS, no event handlers, no auth/data path. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16.1-07-01 | Tampering | theme.no-hardcoded-classes.test.ts active state | mitigate | Sentinel: `! grep -q 'describe.each.skip\|describe.skip' src/styles/theme.no-hardcoded-classes.test.ts` confirms the skip is removed; the test now blocks any future re-introduction of banned classes at CI time. |
| T-16.1-07-02 | Spoofing (a11y / danger semantic) | Destructive End / Reset button visual identity | mitigate | AP-02 sentinel: `grep -c 'bg-red-700'` returns 2 across End+Reset post-task; only foreground (text-white -> on-accent) tokenized; danger red bg + shadow preserved. |
| T-16.1-07-03 | Repudiation | Planning document edits (STATE/REQUIREMENTS/ROADMAP) | accept | These are docs-only changes; git history is the audit trail. No security implication. |

Pure className/CSS substitution + test config flip + planning-doc edits. No injection / XSS / data / auth surface introduced.
</threat_model>

<verification>
After all 3 tasks complete, run the final phase-close verification:

```bash
# Full chain — must exit 0:
npx tsc --noEmit && npm run lint && npm run build && npm test -- --run

# PHASE-LEVEL success criterion #2 grep gate:
grep -rEn --include='*.tsx' --exclude='*.test.tsx' --exclude='*.stories.tsx' \
  '\b(text|bg|border)-(slate|teal)-[0-9]|\b(text|bg)-(white|black)\b' src/
# Expected: zero lines, exit code 1.

# Guard test PASSING (no skip):
npx vitest run src/styles/theme.no-hardcoded-classes.test.ts
# Expected: exit 0; 10/10 passed.

# Contrast guard PASSING (5 reduced-motion + 5 accent-strong vs on-accent):
npx vitest run src/styles/theme.contrast.test.ts
# Expected: exit 0; 10/10 passed.

# Planning docs updated:
grep -E '\[x\] \*\*THEME-UI-01\*\*|THEME-UI-01.*Phase 16.1.*Done' .planning/REQUIREMENTS.md
grep -E '\[x\] \*\*Phase 16\.1' .planning/ROADMAP.md
grep -E '16\.1. UI Token Migration.*7.*Complete' .planning/ROADMAP.md
grep -E 'Phase: 17|Phase 16\.1 UI token migration complete' .planning/STATE.md
test -f .planning/phases/16.1-ui-token-migration/16.1-SUMMARY.md
```

All commands must report green / non-empty matches except the phase-level grep sentinel which must report empty.
</verification>

<success_criteria>
1. `src/index.css` line 6 + 22 migrated to `--color-breathing-accent-strong` + `--color-breathing-on-accent` per D-03.
2. EndSessionDialog:81 + ResetStatsDialog:76 destructive `text-white` -> `text-[var(--color-breathing-on-accent)]` (the only remaining banned-class hits after plan 06).
3. PHASE-LEVEL grep sentinel — the LOCKED success criterion #2 of the ROADMAP — returns ZERO matches on `src/**/*.tsx` (excluding tests).
4. `src/styles/theme.no-hardcoded-classes.test.ts` is active (not `.skip`'d); 10/10 banned-pattern iterations PASS.
5. `src/styles/theme.contrast.test.ts` 10/10 iterations pass (5 original reduced-motion + 5 accent-strong vs on-accent from plan 01).
6. `REQUIREMENTS.md` THEME-UI-01 is `Done` (bullet + traceability table both flipped).
7. `STATE.md` reflects Phase 16.1 complete; current focus = Phase 17; resume file = `16.1-SUMMARY.md`.
8. `ROADMAP.md` Phase 16.1 is `[x]` with 7 plan rows all `[x]`.
9. `.planning/phases/16.1-ui-token-migration/16.1-SUMMARY.md` exists with the prescribed sections.
10. Full chain `npx tsc --noEmit && npm run lint && npm run build && npm test -- --run` exits 0.
11. AP-02 sentinels green: destructive red bg + shadow preserved on End + Reset buttons; the only change is the foreground token.
12. AP-03 sentinel green: every `focus-visible:ring-breathing-accent` in the migration surface is preserved at the pre-migration count.
13. AP-07 sentinel green: `theme.css` non-D-01 token values are byte-identical to pre-phase (only +5 lines added per plan 01).
</success_criteria>

<output>
Phase 16.1 is fully closed when this plan ships. The phase SUMMARY at `.planning/phases/16.1-ui-token-migration/16.1-SUMMARY.md` is the canonical resume file.

Next operator command: `/gsd-plan-phase 17`.
</output>
