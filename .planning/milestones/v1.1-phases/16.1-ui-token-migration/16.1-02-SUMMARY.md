---
phase: 16.1
plan: 02
status: complete
files_modified:
  - src/components/SettingsDialog.tsx
  - src/components/EndSessionDialog.tsx
  - src/components/ResetStatsDialog.tsx
  - src/components/LearnDialog.tsx
commits:
  - 17191d4 feat(16.1-02) migrate SettingsDialog + EndSessionDialog + ResetStatsDialog chrome
  - d167af4 feat(16.1-02) migrate LearnDialog chrome + body text
---

# Plan 16.1-02 SUMMARY — Dialog Chrome Family Migration

## What Shipped

4 dialog files migrated to `--color-breathing-*` tokens. ~22 banned-class occurrences cleared.

- **SettingsDialog.tsx**: shell (line 77), title h2 (line 80), Close button (line 91).
- **EndSessionDialog.tsx**: shell (line 54), title h2 (line 59), Keep button (line 74). Destructive End button (line 81) preserved verbatim (AP-02).
- **ResetStatsDialog.tsx**: shell (line 54), title h2 (line 59), Keep button (line 69). Destructive Reset button (line 76) preserved verbatim (AP-02).
- **LearnDialog.tsx**: shell (line 74), title h2 (line 77), 5 h3 headings (82/86/90/104/143), 3 body p (83/87/92), Close button (line 181).

## Canonical Strings Locked

**Dialog shell** (used 4 sites — copy verbatim for downstream plans):
```
border border-[var(--color-breathing-muted)] bg-[var(--color-breathing-surface)]
```

**Title h2** (used 4 sites):
```
text-2xl font-semibold tracking-tight text-[var(--color-breathing-accent-strong)]
```

**Secondary button — Close/Keep/Cancel** (used 4 sites; downstream pickers/anchors/stepper inherit this):
```
min-h-12 rounded-full border border-[var(--color-breathing-accent)] bg-[var(--color-breathing-surface)] px-5 py-2 text-base font-semibold text-[var(--color-breathing-accent-strong)] shadow-sm transition hover:bg-[var(--color-breathing-bg-soft)] active:bg-[var(--color-breathing-bg-soft)] motion-reduce:transition-none focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-breathing-accent focus-visible:ring-offset-2
```

## Sentinels (verified)

- `grep -cE '\b(text|bg|border)-(slate|teal)-[0-9]|\bbg-white\b'` on all 4 files → 0
- `grep -c 'bg-red-700'` on End + Reset → 1 each (AP-02 preserved)
- `grep -c 'text-white'` on End + Reset → 1 each (AP-02 — deferred to plan 07)
- `grep -c 'focus-visible:ring-breathing-accent'` on all 4 → ≥ 1 per file (AP-03)

## Gates

`tsc + eslint + build + npm test --run` exit 0. Dialog tests (SettingsDialog 4-test, LearnDialog 16-test, App.dialog, App.settings) pass without edit (D-06).

## Deviations

Plan recovery (post-stall): Task 1 commit was orphaned on a deleted worktree branch; recovered via `git cherry-pick c27d45e` -> commit 17191d4 on main.
