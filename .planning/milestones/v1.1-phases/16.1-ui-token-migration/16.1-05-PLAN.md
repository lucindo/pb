---
phase: 16.1
plan: 05
type: execute
wave: 2
depends_on: [16.1-01]
files_modified:
  - src/app/App.tsx
  - src/components/BreathingShape.tsx
  - src/components/SessionReadout.tsx
autonomous: true
requirements: [THEME-UI-01]
tags: [theme, tailwind-v4, page-chrome, alpha-modifier, tsx-migration]

must_haves:
  truths:
    - "App.tsx main page chrome (background-radial gradient consumer + brand eyebrow + h1 + card surface + helper text) rebinds per palette on Dark/Moss/Slate/Dusk swap"
    - "BreathingShape phase label + lead-in countdown digit text rebind per palette (when the inline runtime color override is absent)"
    - "SessionReadout 2× container chrome + 2× time-chip + label + complete-message rebind per palette"
    - "App.tsx:608 card-surface alpha-modifier (bg-white/70 + border-white/80) consumes the D-02 strategy locked by plan 01 and applied in plan 04"
    - "SessionReadout:39 and :79 time-chip alpha-modifier (bg-white/80) consume the same D-02 strategy"
    - "Per-commit green-gate holds; all App + SessionReadout + BreathingShape tests pass without modification (D-06)"
  artifacts:
    - path: "src/app/App.tsx"
      provides: "Tokenized 5 banned-class sites (lines 591, 599, 602, 608, 653)"
      contains: "var(--color-breathing-accent-strong)"
    - path: "src/components/BreathingShape.tsx"
      provides: "Tokenized phase label + lead-in countdown digit (lines 135, 216)"
      contains: "var(--color-breathing-accent-strong)"
    - path: "src/components/SessionReadout.tsx"
      provides: "Tokenized 7 banned-class sites (lines 29, 39, 41, 64, 73, 79, 81)"
      contains: "var(--color-breathing-bg-soft)"
  key_links:
    - from: "src/app/App.tsx:608 (card surface alpha)"
      to: "D-02 alpha-modifier strategy (same path chosen by plan 04 anchors/stepper)"
      via: "bg-[var(--color-breathing-surface)]/70 + border-[var(--color-breathing-surface)]/80 OR inline-style fallback"
      pattern: "var\\(--color-breathing-surface\\)"
    - from: "src/components/SessionReadout.tsx:39 + :79 (time chip alpha)"
      to: "D-02 alpha-modifier strategy"
      via: "bg-[var(--color-breathing-surface)]/80 OR inline style fallback"
      pattern: "var\\(--color-breathing-surface\\)"
    - from: "src/components/BreathingShape.tsx:135,216"
      to: "src/styles/theme.css var(--color-breathing-accent-strong)"
      via: "className text-[var(--color-breathing-accent-strong)] composes with runtime inline style overrides (--color-orb-in-text / --color-orb-out-text) per Pitfall-6 RESEARCH §"Edge Cases" 6"
      pattern: "text-\\[var\\(--color-breathing-accent-strong\\)\\]"
---

<objective>
Wave 1 / Plan 05: Migrate Group E (top-level page + breathing surface + readout chrome) — `src/app/App.tsx`, `src/components/BreathingShape.tsx`, `src/components/SessionReadout.tsx`. These three files form the largest single visual cluster in the app (page header eyebrow + h1 + helper text + card surface + breathing area + 2 readout containers + 2 time chips + complete message). Three of the 5 D-02 alpha-modifier sites live here (App.tsx:608 has both `border-white/80` and `bg-white/70`; SessionReadout.tsx:39 and :79 both have `bg-white/80`).

Purpose: Complete the page-chrome migration so the entire idle/stopped view + active-session view rebinds per palette except for the Start/Stop primary action button (deferred to plan 06 because it consumes the D-01 `--color-breathing-on-accent` token). After this plan, the Wave 1 migration is complete: dialogs (plan 02), pickers (plan 03), anchors/stepper/mute (plan 04), and page+breathing+readout (plan 05) all rebind per `data-theme`.

Output: 3 modified TSX files (~14 banned-class occurrences cleared). 3 alpha-modifier sites consume the D-02 strategy chosen in plan 04. No test edits.
</objective>

<execution_context>
@$HOME/.claude-personal-personal/get-shit-done/workflows/execute-plan.md
@$HOME/.claude-personal-personal/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/16.1-ui-token-migration/16.1-CONTEXT.md
@.planning/phases/16.1-ui-token-migration/16.1-PATTERNS.md
@.planning/phases/16.1-ui-token-migration/16.1-RESEARCH.md
@.planning/phases/16.1-ui-token-migration/16.1-01-SUMMARY.md
@.planning/phases/16.1-ui-token-migration/16.1-04-SUMMARY.md
@src/styles/theme.alpha-probe.test.ts
@src/app/App.tsx
@src/components/BreathingShape.tsx
@src/components/SessionReadout.tsx
@src/components/StatsFooter.tsx

<interfaces>
PATTERNS.md §4 Group P-SUB + P-ALPHA recipes (locked):

App.tsx (RESEARCH §2 + verified grep 2026-05-13):
- Line 591 (main `<main>` className — page bg + text):
  - BEFORE: `min-h-screen bg-[radial-gradient(circle_at_top,_var(--color-breathing-bg-soft),_var(--color-breathing-bg)_48%,_var(--color-breathing-bg-edge))] px-4 py-6 text-slate-900 sm:px-6 sm:py-8`
  - AFTER:  same line but `text-slate-900` -> `text-[var(--color-breathing-accent-strong)]`. The radial-gradient is already token-aware. Per RESEARCH §3 mapping: `text-slate-900` -> `text-[var(--color-breathing-accent-strong)]` (heading-strength body color).

- Line 599 (brand eyebrow `<p>` — "HRV Practice"):
  - BEFORE: `mb-4 text-sm font-semibold uppercase tracking-[0.35em] text-teal-700`
  - AFTER:  `mb-4 text-sm font-semibold uppercase tracking-[0.35em] text-[var(--color-breathing-accent)]`. Per RESEARCH §3 ambiguity note 4: brand eyebrow uppercase = medium-emphasis accent (NOT accent-strong).

- Line 602 (h1 main title):
  - BEFORE: `text-4xl font-semibold tracking-tight text-slate-950 sm:text-5xl`
  - AFTER:  `text-4xl font-semibold tracking-tight text-[var(--color-breathing-accent-strong)] sm:text-5xl`

- Line 608 (card surface `<div>` — TWO banned classes: border-white/80 + bg-white/70):
  - BEFORE template-literal form: `${inSessionView ? 'mt-6' : 'mt-10'} w-full rounded-[2rem] border border-white/80 bg-white/70 p-5 shadow-[var(--shadow-breathing-card)] backdrop-blur sm:p-6`
  - AFTER (Path A — D-02 default): `${inSessionView ? 'mt-6' : 'mt-10'} w-full rounded-[2rem] border border-[var(--color-breathing-surface)]/80 bg-[var(--color-breathing-surface)]/70 p-5 shadow-[var(--shadow-breathing-card)] backdrop-blur sm:p-6`
  - AFTER (Path B — D-02 fallback): drop both alpha classes from the className; add inline style:
    `style={{ backgroundColor: 'rgb(from var(--color-breathing-surface) r g b / 0.7)', borderColor: 'rgb(from var(--color-breathing-surface) r g b / 0.8)' }}`
  - The strategy choice (A vs B) is the SAME as plan 04 anchors/stepper. Read `src/styles/theme.alpha-probe.test.ts` JSDoc and `16.1-04-SUMMARY.md` (where plan 04 documented the realized choice) to know which path applies here.

- Line 653 (helper text `<p>`):
  - BEFORE: `mt-4 text-sm leading-6 text-slate-600`
  - AFTER:  `mt-4 text-sm leading-6 text-[var(--color-breathing-muted)]` (secondary/label text — RESEARCH §3 mapping)

BreathingShape.tsx (RESEARCH §2 — lines 135 + 216):
- Line 135 (phase label `<span>` — visible during In/Out phases):
  - BEFORE: `relative z-10 text-5xl font-semibold tracking-tight text-slate-900 sm:text-6xl`
  - AFTER:  `relative z-10 text-5xl font-semibold tracking-tight text-[var(--color-breathing-accent-strong)] sm:text-6xl`
  - Note: BreathingShape applies a runtime inline `style={{ color: cssVar }}` override (per `BreathingShape.tsx:137-140` per RESEARCH §"Alternatives Considered" + §"Edge Case 6") that consumes `--color-orb-in-text` or `--color-orb-out-text` during a session. Inline style wins over className. So the className `text-[var(--color-breathing-accent-strong)]` ONLY shows when the inline override is absent (pre-session idle state + lead-in countdown digit). The migration here ensures the idle/lead-in fallback color is theme-aware — not a behavioral change in the active-session path.

- Line 216 (lead-in countdown digit `<span>`):
  - BEFORE: `relative z-10 text-7xl font-semibold tracking-tight text-slate-900 sm:text-8xl`
  - AFTER:  `relative z-10 text-7xl font-semibold tracking-tight text-[var(--color-breathing-accent-strong)] sm:text-8xl`

SessionReadout.tsx (RESEARCH §2 — 7 sites on lines 29, 39, 41, 64, 73, 79, 81):
- Line 29 (first readout container — running session):
  - BEFORE: `mb-6 rounded-[1.75rem] border border-teal-100 bg-teal-50/80 p-5 text-center shadow-inner shadow-teal-900/5`
  - AFTER:  `mb-6 rounded-[1.75rem] border border-[var(--color-breathing-muted)] bg-[var(--color-breathing-bg-soft)]/80 p-5 text-center shadow-inner shadow-teal-900/5`
  - Two banned classes on this line: `border-teal-100` and `bg-teal-50/80`. Both migrate. `shadow-teal-900/5` is preserved (CONTEXT.md Out of Scope — same carry-forward as SettingsStepper:36).
  - PATTERNS.md §4 Group P-ALPHA: `bg-teal-50/80` matches the regex `\bbg-teal-[0-9]` (because the regex anchors on the word boundary before `b`, and matches `bg-teal-5` then `0/80` continues — RESEARCH §"Edge Case 9"). It IS banned. Migration target: `bg-[var(--color-breathing-bg-soft)]/80` (Path A) or inline style fallback (Path B — consistent with plan 04 choice).

- Line 39 (time chip during running session):
  - BEFORE: `mt-4 inline-flex items-baseline gap-3 rounded-full bg-white/80 px-5 py-3 text-slate-900`
  - AFTER (Path A): `mt-4 inline-flex items-baseline gap-3 rounded-full bg-[var(--color-breathing-surface)]/80 px-5 py-3 text-[var(--color-breathing-accent-strong)]`
  - AFTER (Path B): drop `bg-white/80` from className + add inline style `style={{ backgroundColor: 'rgb(from var(--color-breathing-surface) r g b / 0.8)' }}`; migrate text-slate-900 to className `text-[var(--color-breathing-accent-strong)]`.

- Line 41 (uppercase eyebrow inside time chip):
  - BEFORE: `text-sm font-semibold uppercase tracking-[0.18em] text-slate-600`
  - AFTER:  `text-sm font-semibold uppercase tracking-[0.18em] text-[var(--color-breathing-muted)]`

- Line 64 (second readout container — session complete; same shape as line 29):
  - same migration as line 29.

- Line 73 (complete-message `<p>`):
  - BEFORE: `text-3xl font-semibold text-teal-900`
  - AFTER:  `text-3xl font-semibold text-[var(--color-breathing-accent-strong)]` (RESEARCH §3 ambiguity note 5 — strongest text emphasis)

- Line 79 (time chip during session-complete view; same shape as line 39):
  - same migration as line 39.

- Line 81 (uppercase eyebrow inside complete-time chip; same shape as line 41):
  - same migration as line 41.

PRESERVE (carry-forwards / out of scope):
- `shadow-teal-900/5` on SessionReadout:29 and :64 — NOT in banned regex (alpha modifier without preceding digit pattern matches differently — same shadow-color preservation as SettingsStepper:36).
- `shadow-inner` on SessionReadout:29 and :64 — neutral utility, no color literal.
- Inline runtime `style={{ color: cssVar }}` on BreathingShape phase label (lines 137-140-ish) — already theme-aware via `--color-orb-in-text`/`--color-orb-out-text` per Phase 16; do not touch.

Existing tests (DO NOT edit):
- src/app/App.audio.test.tsx, App.dialog.test.tsx, App.persistence.test.tsx, App.session.test.tsx, App.settings.test.tsx, App.wakeLock.test.tsx (6 App test files)
- src/components/BreathingShape.test.tsx
- src/components/SessionReadout.test.tsx
- App.dialog.test.tsx specifically asserts `focus-visible:ring-breathing-accent` on dialog buttons — not touched by this plan (App.tsx changes are page-chrome text + card surface, no focus rings affected).

Current banned-class occurrences (verified grep 2026-05-13):
- App.tsx: lines 591, 599, 602, 608 (×2 alpha), 653 — 5 lines with 6 occurrences (line 608 has two banned classes).
- BreathingShape.tsx: lines 135, 216 — 2 occurrences.
- SessionReadout.tsx: lines 29 (×2: border-teal-100 + bg-teal-50/80), 39 (×2: bg-white/80 + text-slate-900), 41, 64 (×2), 73, 79 (×2), 81 — 11 occurrences total (some lines have 2 banned classes).
- Total: ~19 banned-class occurrences across the 3 files.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate App.tsx page chrome (5 banned-class sites including the card-surface alpha-modifier)</name>
  <files>src/app/App.tsx</files>
  <read_first>
    - src/app/App.tsx lines 580-660 (the relevant region — read this range only; the file is 678 lines and the migration surface is concentrated here)
    - .planning/phases/16.1-ui-token-migration/16.1-PATTERNS.md §4 Group P-SUB + P-ALPHA (mapping table + alpha-modifier strategy)
    - .planning/phases/16.1-ui-token-migration/16.1-PATTERNS.md §6 (closest-analog table — App.tsx row maps to Reference B + Reference C for alpha)
    - .planning/phases/16.1-ui-token-migration/16.1-RESEARCH.md §3 mapping table + ambiguity note 4 (brand eyebrow = `text-[var(--color-breathing-accent)]`, not accent-strong)
    - src/styles/theme.alpha-probe.test.ts (D-02 strategy decision JSDoc)
    - .planning/phases/16.1-ui-token-migration/16.1-04-SUMMARY.md (plan 04's realized D-02 path — match it here for consistency)
    - .planning/phases/16.1-ui-token-migration/16.1-CONTEXT.md D-05 + D-06
  </read_first>
  <action>
    Edit ONLY these 5 lines in `src/app/App.tsx`. Per CONTEXT.md D-05 (arbitrary-value form) + D-06 (className text only).

    - Line 591 (main): replace `text-slate-900` with `text-[var(--color-breathing-accent-strong)]`. Preserve the radial-gradient background string verbatim — it is already token-aware.

    - Line 599 (eyebrow `<p>`): replace `text-teal-700` with `text-[var(--color-breathing-accent)]` (medium-emphasis brand accent — NOT accent-strong; see RESEARCH §3 ambiguity note 4).

    - Line 602 (h1): replace `text-slate-950` with `text-[var(--color-breathing-accent-strong)]`.

    - Line 608 (card surface `<div>`): handle BOTH banned classes per the D-02 strategy:
      - Path A: replace `border border-white/80 bg-white/70` with `border border-[var(--color-breathing-surface)]/80 bg-[var(--color-breathing-surface)]/70`. Preserve `${inSessionView ? 'mt-6' : 'mt-10'} w-full rounded-[2rem] ... p-5 shadow-[var(--shadow-breathing-card)] backdrop-blur sm:p-6` verbatim.
      - Path B (only if plan 04 settled on Path B per SUMMARY): drop both `border-white/80` and `bg-white/70` from the className; preserve a generic `border` utility (single class) so the JSX still draws a 1px border via the borderColor inline style; add `style={{ borderColor: 'rgb(from var(--color-breathing-surface) r g b / 0.8)', backgroundColor: 'rgb(from var(--color-breathing-surface) r g b / 0.7)' }}` to the `<div>` JSX (sibling of the className prop).

    - Line 653 (helper text `<p>`): replace `text-slate-600` with `text-[var(--color-breathing-muted)]`.

    Do NOT touch any other line of App.tsx. The file is 678 lines; the rest is JSX structure, state hooks, useEffect logic, and external-component composition — all out of 16.1 scope per D-06.

    After edits:
    - `npx tsc --noEmit && npm run lint && npm run build` — exits 0.
    - `npm test -- --run src/app/` — exits 0 (6 App test files — App.audio, App.dialog, App.persistence, App.session, App.settings, App.wakeLock — all must pass; class composition only, no semantic change).

    Commit as `feat(16.1-05): migrate App.tsx page chrome + card surface to --color-breathing-* tokens`.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run lint && grep -cE '\b(text|bg|border)-(slate|teal)-[0-9]|\b(text|bg)-(white|black)\b' src/app/App.tsx && grep -c 'var(--color-breathing-accent-strong)' src/app/App.tsx && grep -c 'var(--color-breathing-accent)' src/app/App.tsx && grep -c 'var(--color-breathing-muted)' src/app/App.tsx && npm test -- --run src/app/</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE '\b(text|bg|border)-(slate|teal)-[0-9]|\b(text|bg)-(white|black)\b' src/app/App.tsx` returns 0 (5 lines × up-to-2 occurrences = up to 6 banned-class occurrences — all eliminated).
    - `grep -c 'var(--color-breathing-accent-strong)' src/app/App.tsx` returns >= 2 (line 591 + line 602 minimum; line 608 path B may add 0; line 608 path A adds 0 because path-A uses surface not accent-strong).
    - `grep -c 'var(--color-breathing-accent)' src/app/App.tsx | xargs -I{} sh -c 'test {} -ge 1'` (line 599 brand eyebrow, plus the radial-gradient continues to reference accent indirectly — minimum 1).
    - `grep -c 'var(--color-breathing-muted)' src/app/App.tsx` returns >= 1 (line 653 helper text).
    - For Path A: `grep -c 'bg-\[var(--color-breathing-surface)\]/70' src/app/App.tsx` returns 1 AND `grep -c 'border-\[var(--color-breathing-surface)\]/80' src/app/App.tsx` returns 1. For Path B: `grep -c 'rgb(from var(--color-breathing-surface)' src/app/App.tsx` returns 2 (one for backgroundColor, one for borderColor) AND `grep -cE '(border-white/80|bg-white/70)' src/app/App.tsx` returns 0.
    - `npm test -- --run src/app/` exits 0 (all 6 App test files green).
    - `npx tsc --noEmit && npm run lint && npm run build` exits 0.
  </acceptance_criteria>
  <done>
    App.tsx page chrome is fully tokenized: page-text color, brand eyebrow, h1 heading, card surface alpha (consistent with plan 04 anchors/stepper D-02 strategy), helper text — all rebind per palette. The 5 banned-class lines (lines 591, 599, 602, 608, 653) and 6 banned-class occurrences (line 608 had two) are cleared.
  </done>
</task>

<task type="auto">
  <name>Task 2: Migrate BreathingShape + SessionReadout (breathing surface + 2 readouts + 2 time chips)</name>
  <files>src/components/BreathingShape.tsx, src/components/SessionReadout.tsx</files>
  <read_first>
    - src/components/BreathingShape.tsx lines 130-145 + 210-220 (the two migration-target span elements + the inline `style={{ color: cssVar }}` override that wins on the active-session path)
    - src/components/SessionReadout.tsx (full file — 89 lines; 7 banned-class sites concentrated on 7 lines)
    - .planning/phases/16.1-ui-token-migration/16.1-PATTERNS.md §4 Group P-SUB + P-ALPHA (mapping table + dual-class-per-line sites)
    - .planning/phases/16.1-ui-token-migration/16.1-RESEARCH.md §"Edge Cases" 6 (BreathingShape inline runtime style override — informs WHY we tokenize the className: it provides the fallback color when the inline override is absent, which is during idle + lead-in countdown phases)
    - .planning/phases/16.1-ui-token-migration/16.1-RESEARCH.md §"Edge Cases" 9 (text-slate-700 and bg-teal-50/80 regex matching behavior — confirms bg-teal-50/80 IS in scope)
    - .planning/phases/16.1-ui-token-migration/16.1-04-SUMMARY.md (D-02 path realized in plan 04 — apply same here)
    - .planning/phases/16.1-ui-token-migration/16.1-CONTEXT.md D-05 + D-06
  </read_first>
  <action>
    **BreathingShape.tsx** (2 lines):

    - Line 135 (phase label `<span>`): replace `text-slate-900` with `text-[var(--color-breathing-accent-strong)]`. Preserve `relative z-10 text-5xl font-semibold tracking-tight sm:text-6xl` verbatim. Do NOT touch the inline `style={{ color: ... }}` override on the adjacent lines (~137-140 per RESEARCH §"Edge Case 6") — it consumes `--color-orb-in-text`/`--color-orb-out-text` and is already theme-aware.

    - Line 216 (lead-in countdown digit `<span>`): replace `text-slate-900` with `text-[var(--color-breathing-accent-strong)]`. Preserve `relative z-10 text-7xl font-semibold tracking-tight sm:text-8xl` verbatim.

    **SessionReadout.tsx** (7 banned-class lines, ~11 occurrences):

    - Line 29 (first readout container):
      - Replace `border-teal-100` with `border-[var(--color-breathing-muted)]`.
      - Replace `bg-teal-50/80` with `bg-[var(--color-breathing-bg-soft)]/80` (Path A) OR drop className alpha + add inline style `{{ backgroundColor: 'rgb(from var(--color-breathing-bg-soft) r g b / 0.8)' }}` (Path B — consistent with the broader D-02 choice).
      - Preserve `mb-6 rounded-[1.75rem] border ... p-5 text-center shadow-inner shadow-teal-900/5` verbatim (`shadow-teal-900/5` is the same carry-forward as plan 04 SettingsStepper:36).

    - Line 39 (time chip during running session):
      - Replace `bg-white/80` with `bg-[var(--color-breathing-surface)]/80` (Path A) OR drop className + inline style fallback (Path B).
      - Replace `text-slate-900` with `text-[var(--color-breathing-accent-strong)]`.
      - Preserve `mt-4 inline-flex items-baseline gap-3 rounded-full px-5 py-3` verbatim.

    - Line 41 (uppercase eyebrow in time chip):
      - Replace `text-slate-600` with `text-[var(--color-breathing-muted)]`.
      - Preserve `text-sm font-semibold uppercase tracking-[0.18em]` verbatim.

    - Line 64 (second readout container — session complete): same dual-migration as line 29.

    - Line 73 (complete-message `<p>`): replace `text-teal-900` with `text-[var(--color-breathing-accent-strong)]`. Preserve `text-3xl font-semibold` verbatim.

    - Line 79 (time chip during session-complete view): same dual-migration as line 39.

    - Line 81 (uppercase eyebrow in complete-time chip): same migration as line 41.

    After all 3 files (incl. BreathingShape) edited:
    - `npx tsc --noEmit && npm run lint && npm run build` — exits 0.
    - `npm test -- --run src/components/BreathingShape.test.tsx src/components/SessionReadout.test.tsx src/app/App.session.test.tsx` — exits 0.
    - Full chain: `npm test -- --run` — exits 0.

    Commit as `feat(16.1-05): migrate BreathingShape + SessionReadout to --color-breathing-* tokens`.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run lint && grep -cE '\b(text|bg|border)-(slate|teal)-[0-9]|\b(text|bg)-(white|black)\b' src/components/BreathingShape.tsx src/components/SessionReadout.tsx | awk -F: '{sum+=$2} END {print "banned:", sum}' && grep -c 'shadow-teal-900/5' src/components/SessionReadout.tsx && grep -c 'var(--color-breathing-bg-soft)' src/components/SessionReadout.tsx && npm test -- --run src/components/BreathingShape.test.tsx src/components/SessionReadout.test.tsx src/app/App.session.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE '\b(text|bg|border)-(slate|teal)-[0-9]|\b(text|bg)-(white|black)\b' src/components/BreathingShape.tsx src/components/SessionReadout.tsx | awk -F: '{sum+=$2} END {print sum}'` returns 0 (all ~13 banned-class occurrences across both files eliminated).
    - `grep -c 'shadow-teal-900/5' src/components/SessionReadout.tsx` returns 2 (lines 29 + 64 — CONTEXT.md Out of Scope carry-forward, preserved on both readout containers; matches plan 04 SettingsStepper preservation pattern).
    - `grep -c 'var(--color-breathing-bg-soft)' src/components/SessionReadout.tsx` returns >= 2 (lines 29 + 64 — readout container backgrounds; if line 29 + 64 are migrated to bg-soft/80, count is 2; if Path B inline-style fallback, count may differ — check directly).
    - `grep -c 'var(--color-breathing-accent-strong)' src/components/BreathingShape.tsx` returns 2 (lines 135 + 216).
    - `grep -c 'var(--color-breathing-accent-strong)' src/components/SessionReadout.tsx` returns >= 3 (lines 39 + 73 + 79 minimum).
    - `grep -c 'var(--color-breathing-muted)' src/components/SessionReadout.tsx` returns >= 4 (lines 29 + 41 + 64 + 81 — borders + eyebrows).
    - `npm test -- --run src/components/BreathingShape.test.tsx src/components/SessionReadout.test.tsx src/app/App.session.test.tsx` exits 0.
    - Full chain (`npx tsc --noEmit && npm run lint && npm run build && npm test -- --run`) exits 0.
  </acceptance_criteria>
  <done>
    BreathingShape and SessionReadout are fully tokenized. Phase-label + lead-in digit text colors composite correctly with the existing runtime-inline `style.color` override (the inline override wins on the active-session path; the className fallback now rebinds per palette during idle + lead-in countdown). SessionReadout's 2 readout containers + 2 time chips + 2 uppercase eyebrows + complete-message text all rebind per palette. The Wave 1 migration surface is fully landed: dialogs (plan 02), pickers (plan 03), anchors/stepper/mute (plan 04), page+breathing+readout (plan 05). Only the Start/Stop primary action button (plan 06, Wave 2) remains.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| inline style={} fallback path (D-02 Path B) | Same security profile as plan 04 — hardcoded `rgb(from var(...) ...)` literal, no template substitution. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16.1-05-01 | Tampering | App.tsx:608 card surface inline style fallback (Path B only) | mitigate | Hardcoded string literal; no XSS surface. Sentinel: `grep -c 'rgb(from var(--color-breathing-surface)' src/app/App.tsx` returns exactly 2 when Path B applies (backgroundColor + borderColor). |
| T-16.1-05-02 | Spoofing (visual identity) | BreathingShape.tsx:135,216 phase label / lead-in digit | mitigate | Inline `style.color` override (already theme-aware via `--color-orb-in-text`/`--color-orb-out-text`) still wins on active-session path; className tokenization is the idle/lead-in fallback only. Existing `BreathingShape.test.tsx` asserts the runtime color path; tokenizing the className-side fallback does not regress that test. |

This is a pure className substitution plan. No XSS / injection / auth / data surface introduced.
</threat_model>

<verification>
After both tasks complete:

```bash
npx tsc --noEmit && npm run lint && npm run build && npm test -- --run
```

Plan-level grep sentinels:

```bash
# All 3 files token-migrated:
grep -cE '\b(text|bg|border)-(slate|teal)-[0-9]|\b(text|bg)-(white|black)\b' \
  src/app/App.tsx src/components/BreathingShape.tsx src/components/SessionReadout.tsx
# Expected: 0 on every line.

# D-02 alpha-modifier consistency (App + SessionReadout):
grep -c 'bg-\[var(--color-breathing-surface)\]/70\|bg-\[var(--color-breathing-surface)\]/80\|bg-\[var(--color-breathing-bg-soft)\]/80\|rgb(from var(--color-breathing-' \
  src/app/App.tsx src/components/SessionReadout.tsx
# Expected: each file >= 1; total >= 4 (App:608 ×2 if Path A, ×2 if Path B; SessionReadout:29,39,64,79 = 4 alpha sites).

# Carry-forward sentinels:
grep -c 'shadow-teal-900/5' src/components/SessionReadout.tsx
# Expected: 2 (CONTEXT.md Out of Scope).

grep -c 'shadow-[var(--shadow-breathing-card)]' src/app/App.tsx
# Expected: 1 (card surface — Phase 16 D-02 carry-forward, not migrated in 16.1).
```

Phase-level smoke after plan 05 (Wave 1 complete):

```bash
# Combined Wave 1 sentinel — 16 migration-surface .tsx files should have ZERO banned classes EXCEPT
# (a) text-white preserved on EndSessionDialog:81 + ResetStatsDialog:76 (destructive red, deferred to plan 06/07)
# (b) text-white on SessionControls.tsx:52,69 (primary Start/Stop — plan 06)
grep -E '\b(text|bg|border)-(slate|teal)-[0-9]|\bbg-white\b' src/app/App.tsx src/components/SessionControls.tsx src/components/SettingsStepper.tsx src/components/SettingsDialog.tsx src/components/SettingsAnchor.tsx src/components/LearnAnchor.tsx src/components/LearnDialog.tsx src/components/EndSessionDialog.tsx src/components/ResetStatsDialog.tsx src/components/MuteToggle.tsx src/components/SessionReadout.tsx src/components/BreathingShape.tsx src/components/ThemePicker.tsx src/components/TimbrePicker.tsx src/components/VariantPicker.tsx src/components/LanguagePicker.tsx --include='*.tsx'
# Expected after Wave 1 (plans 02-05): hits ONLY in SessionControls.tsx (lines 52, 69 — bg-teal-700 + hover/active red carry).
# Plan 06 will close these. Plan 07 will then decide on the destructive text-white pair.
```
</verification>

<success_criteria>
1. App.tsx, BreathingShape.tsx, SessionReadout.tsx together contain ZERO banned-regex matches (when the Wave 0 D-04 walker runs with --no-skip).
2. The 3 alpha-modifier sites in this plan (App:608 — 2 classes counted as 1 site; SessionReadout:39,79) consistently apply the D-02 strategy chosen in plan 04 (Path A or Path B; no mix).
3. SessionReadout `shadow-teal-900/5` carry-forward preserved on lines 29 + 64 (CONTEXT.md Out of Scope, intentional).
4. BreathingShape runtime `style.color` override (`--color-orb-in-text`/`--color-orb-out-text` consumer) untouched — class composition change only (D-06).
5. App.tsx card surface (`shadow-[var(--shadow-breathing-card)]`) preserved verbatim — Phase 16 D-02 carry-forward.
6. All 6 App test files + BreathingShape test + SessionReadout test pass without modification.
7. `npx tsc --noEmit && npm run lint && npm run build && npm test -- --run` exits 0.
8. After this plan ships, the ONLY remaining banned-class lines in the production migration surface are on SessionControls.tsx:52 and :69 (plan 06's job) and the 2 destructive text-white lines on EndSessionDialog:81 + ResetStatsDialog:76 (plan 07's decision).
</success_criteria>

<output>
After completion, create `.planning/phases/16.1-ui-token-migration/16.1-05-SUMMARY.md` summarizing:
- 3 files migrated, ~14 banned-class occurrences cleared
- D-02 strategy realized — final choice (Path A or B) ratified across all 5 alpha sites (LearnAnchor, SettingsAnchor, SettingsStepper from plan 04; App, SessionReadout here)
- Wave 1 milestone: dialogs + pickers + anchors + page chrome are all theme-bound. Theme-swap visual rebind on Dark/Moss/Slate/Dusk is now near-complete; the only un-bound surfaces are the Start/Stop primary button (plan 06) and the 2 destructive-red text-white pairs (plan 07).
- Test counts pre/post-plan identical (D-06)
- Recommendation for plan 06: this is the right time to perform a manual visual smoke screen (load app at `localhost:5173`, swap palette via ThemePicker, observe full UI rebind except Start/Stop). Plan 06's per-palette UAT is the final acceptance.
</output>
