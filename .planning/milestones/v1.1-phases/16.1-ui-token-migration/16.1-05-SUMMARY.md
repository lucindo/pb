---
phase: 16.1
plan: 05
status: complete
files_modified:
  - src/app/App.tsx
  - src/components/BreathingShape.tsx
  - src/components/SessionReadout.tsx
commits:
  - 219975c feat(16.1-05) migrate App.tsx page chrome + card surface
  - ae47927 feat(16.1-05) migrate BreathingShape + SessionReadout
---

# Plan 16.1-05 SUMMARY — Page + Breathing + Readout Migration

## What Shipped

3 files migrated. ~14 banned-class occurrences cleared. Wave 1 migration surface fully landed.

- **App.tsx**: page text (line 591), brand eyebrow (line 599 — `text-[var(--color-breathing-accent)]` medium-emphasis per RESEARCH §3 note 4), h1 (line 602), card surface alpha (line 608, both border-white/80 + bg-white/70 — D-02 Path A), helper text (line 653).
- **BreathingShape.tsx**: phase label className (line 135) + lead-in countdown digit className (line 216). Runtime inline `style.color` override on line 137-141 (consumes `--color-orb-in-text`/`--color-orb-out-text`) untouched — wins on active-session path; className is idle/lead-in fallback.
- **SessionReadout.tsx**: 2 readout containers (lines 29 + 64, both — border + alpha bg), 2 time chips (lines 39 + 79 — alpha bg + text), 2 chip eyebrows (lines 41 + 81 — muted), complete message (line 73 — accent-strong).

## D-02 Strategy

Path A consistently applied across the 5 alpha sites in plans 04 + 05.

## Sentinels (verified)

- `grep -cE '\b(text|bg|border)-(slate|teal)-[0-9]|\bbg-white\b'` on 3 files → 0
- `grep -c 'shadow-teal-900/5' src/components/SessionReadout.tsx` → 2 (lines 29 + 64 carry-forward; AP-04)
- `grep -c 'shadow-[var(--shadow-breathing-card)]' src/app/App.tsx` → 1 (Phase 16 carry-forward)
- BreathingShape runtime `style.color` override on phase label untouched

## Gates

`tsc + eslint + build + test` exit 0. Full suite 499 passed | 10 skipped (D-04 guard still .skip until plan 07).

## Wave 1+2 Milestone

After this plan, the ONLY remaining banned-class lines in the migration surface are:
- `src/components/SessionControls.tsx` lines 52 + 69 (Start/Stop primary button — plan 06's job; consumes D-01 `--color-breathing-on-accent` token from plan 01).
- `text-white` on EndSessionDialog:81 + ResetStatsDialog:76 (destructive red — plan 07 decision).

## Recommendation for Plan 06

Manual visual smoke at `localhost:5173`: swap palette via ThemePicker, observe full UI rebind except Start/Stop. Plan 06's per-palette UAT is the final acceptance gate.
