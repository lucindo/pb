---
phase: 18-audio-timbres
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/hooks/useTimbreChoice.ts
  - src/hooks/useTimbreChoice.test.ts
autonomous: true
requirements:
  - TIMBRE-04
tags:
  - hooks
  - prefs
  - storage
must_haves:
  truths:
    - "useTimbreChoice hook reads loadPrefs().timbre at mount via lazy useState initializer"
    - "setTimbre(next) writes savePrefs({ ...loadPrefs(), timbre: next }) then dispatches 'hrv:prefs-changed' with detail.key === 'timbre' (D-18)"
    - "setTimbre is stable across re-renders (useCallback empty deps — mirror of useVariantChoice line 32)"
    - "Local React state (setTimbreState) is updated optimistically so the picker reflects the new selection instantly"
    - "Cross-tab and same-tab sync compatible — sibling pickers/hooks can listen for 'hrv:prefs-changed' filtered on detail.key === 'timbre'"
    - "tsc && lint && build && test exit 0 at this commit (D-13)"
  artifacts:
    - path: "src/hooks/useTimbreChoice.ts"
      provides: "Picker-side companion hook for TimbrePicker — returns { timbre, setTimbre }"
      contains: "export function useTimbreChoice"
      contains_also: "savePrefs({ ...current, timbre: next })"
      contains_also: "detail: { key: 'timbre', value: next }"
    - path: "src/hooks/useTimbreChoice.test.ts"
      provides: "Read/setter/dispatch contract tests — verbatim mirror of useVariantChoice.test.ts with variant->timbre substitutions"
      contains: "setTimbre"
      contains_also: "'hrv:prefs-changed'"
  key_links:
    - from: "src/hooks/useTimbreChoice.ts"
      to: "src/storage/prefs.ts"
      via: "import { loadPrefs, savePrefs }"
      pattern: "import.*loadPrefs.*savePrefs.*from.*storage/prefs"
    - from: "src/hooks/useTimbreChoice.ts"
      to: "src/domain/settings.ts"
      via: "import type { TimbreId }"
      pattern: "import type.*TimbreId.*from.*domain/settings"
    - from: "src/hooks/useTimbreChoice.ts"
      to: "window 'hrv:prefs-changed' CustomEvent"
      via: "window.dispatchEvent(new CustomEvent(...))"
      pattern: "dispatchEvent.*CustomEvent.*hrv:prefs-changed"
---

<objective>
Create the picker-side companion hook `src/hooks/useTimbreChoice.ts` — a verbatim mirror of `src/hooks/useVariantChoice.ts` (Phase 17 Plan 04) with `variant → timbre` substitutions. The hook provides `{ timbre, setTimbre }` to `TimbrePicker.tsx` (Plan 05); `setTimbre` writes the merged envelope to storage, updates a local React state mirror for optimistic-UI, and dispatches the existing `'hrv:prefs-changed'` CustomEvent with `detail.key === 'timbre'` (D-18 contract reuse).

Purpose: Land the storage write path for timbre BEFORE the picker UI consumes it (Plan 05). The dispatch path is forward-compat with cross-tab sync infrastructure already in place from Phase 16/17 — even though Phase 18 has no App-side `useAudioTimbre` orchestrator listening (D-08: timbre has no idle-state visual surface), the CustomEvent is dispatched anyway so sibling pickers / future App-side consumers can opt-in without re-engineering.

Output: NEW `src/hooks/useTimbreChoice.ts` (~30 LOC) + NEW `src/hooks/useTimbreChoice.test.ts` (~110 LOC). No edits to `src/storage/prefs.ts` (D-15 file-split invariant — `coerceTimbre` and `loadPrefs/savePrefs` locked at Phase 14). No edits to `src/domain/settings.ts` (D-15 — `TimbreId/TIMBRE_OPTIONS/DEFAULT_TIMBRE` locked Phase 14 D-01/D-04).
</objective>

<execution_context>
@$HOME/.claude-personal-personal/get-shit-done/workflows/execute-plan.md
@$HOME/.claude-personal-personal/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md

@.planning/phases/18-audio-timbres/18-CONTEXT.md
@.planning/phases/18-audio-timbres/18-PATTERNS.md

@src/hooks/useVariantChoice.ts
@src/hooks/useVariantChoice.test.ts
@src/storage/prefs.ts
@src/domain/settings.ts

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from existing source. -->

From `src/hooks/useVariantChoice.ts` (full file is the template to mirror):

  import { useCallback, useState } from 'react'
  import { loadPrefs, savePrefs } from '../storage/prefs'
  import type { VisualVariantId } from '../domain/settings'

  export function useVariantChoice(): { variant: VisualVariantId; setVariant: (next: VisualVariantId) => void } {
    const [variant, setVariantState] = useState<VisualVariantId>(() => loadPrefs().variant)

    const setVariant = useCallback((next: VisualVariantId): void => {
      const current = loadPrefs()
      savePrefs({ ...current, variant: next })
      setVariantState(next)
      window.dispatchEvent(
        new CustomEvent('hrv:prefs-changed', { detail: { key: 'variant', value: next } }),
      )
    }, [])

    return { variant, setVariant }
  }

From `src/storage/prefs.ts` (Phase 14 locked exports):

  export interface UserPrefs { theme: ThemeId; timbre: TimbreId; variant: VisualVariantId; locale: LocaleId }
  export const DEFAULT_PREFS: UserPrefs = { theme: 'system', timbre: 'bowl', variant: 'orb', locale: 'en' }
  export function loadPrefs(deps?: StorageDeps): UserPrefs
  export function savePrefs(prefs: UserPrefs, deps?: StorageDeps): void

From `src/domain/settings.ts` (Phase 14 locked):

  export type TimbreId = 'bowl' | 'bell' | 'sine' | 'chime'
  export const DEFAULT_TIMBRE: TimbreId = 'bowl'

CustomEvent contract (D-18; mirror of Phase 17 D-22):

  new CustomEvent('hrv:prefs-changed', { detail: { key: 'timbre', value: next } })
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create `src/hooks/useTimbreChoice.ts` — verbatim mirror of useVariantChoice</name>
  <read_first>
    src/hooks/useVariantChoice.ts (full file — 47 lines — the template to substitute variant->timbre verbatim),
    src/storage/prefs.ts (loadPrefs/savePrefs signatures — Phase 14 D-10 API surface),
    src/domain/settings.ts (TimbreId / DEFAULT_TIMBRE — Phase 14 D-01/D-04 locked),
    .planning/phases/18-audio-timbres/18-CONTEXT.md (D-09 "no useAudioTimbre orchestrator needed"; D-17 picker prop contract; D-18 CustomEvent contract reuse),
    .planning/phases/18-audio-timbres/18-PATTERNS.md (§"`src/hooks/useTimbreChoice.ts` (NEW)" — full substitution table)
  </read_first>
  <behavior>
    - The hook's mount-time read uses lazy `useState` initializer: `useState<TimbreId>(() => loadPrefs().timbre)`. The initializer is called once per mount only.
    - `setTimbre(next)` performs four ordered effects in this exact order: (1) `const current = loadPrefs()` (fresh read; do NOT use the closure `timbre`), (2) `savePrefs({ ...current, timbre: next })` (spread merge preserves theme/variant/locale per Phase 14 D-17 per-field isolation), (3) `setTimbreState(next)` (optimistic-UI local React state mirror), (4) `window.dispatchEvent(new CustomEvent('hrv:prefs-changed', { detail: { key: 'timbre', value: next } }))` (forward-compat sync surface).
    - `setTimbre` is wrapped in `useCallback` with empty deps `[]` so its identity is stable across re-renders.
    - The hook returns `{ timbre, setTimbre }` — exactly two fields, both narrow types.
    - File compiles under strict mode + strictTypeChecked with no rule disables.
  </behavior>
  <action>
    Create `src/hooks/useTimbreChoice.ts`. Copy `useVariantChoice.ts` (lines 1-47) as the source template. Apply the following exact substitutions: `useVariantChoice` → `useTimbreChoice`; `VisualVariantId` → `TimbreId`; `variant` (every identifier) → `timbre`; `setVariant` → `setTimbre`; `setVariantState` → `setTimbreState`; `loadPrefs().variant` → `loadPrefs().timbre`; `savePrefs({ ...current, variant: next })` → `savePrefs({ ...current, timbre: next })`; the CustomEvent detail `{ key: 'variant', value: next }` → `{ key: 'timbre', value: next }`. Update the leading comment block to reference Phase 18 (replace "Phase 17 Plan 04: Picker-side companion hook to useVisualVariant (A-02)" with "Phase 18: Picker-side companion hook for TimbrePicker.tsx (D-08 — no App-side orchestrator hook needed; the CustomEvent dispatch is kept for forward-compat with the same-tab sibling-picker sync surface)"). Preserve the four numbered inline comments inside `setTimbre` (1. Fresh read / 2. Write merged envelope / 3. Local state mirror / 4. Dispatch event). Do NOT add any new fields to the return shape. Do NOT add a `useEffect` listener — Phase 17 D-22's forward-decl notes the picker is the writer, NOT the listener (the listener role belongs to App-side orchestrator hooks, which Phase 18 explicitly does not introduce per D-09).
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&amp;1 | grep "useTimbreChoice" ; test $? -ne 0 &amp;&amp; echo "tsc clean for useTimbreChoice"</automated>
    <automated>grep -q "export function useTimbreChoice" src/hooks/useTimbreChoice.ts &amp;&amp; echo "Hook exported"</automated>
    <automated>grep -q "detail: { key: 'timbre', value: next }" src/hooks/useTimbreChoice.ts &amp;&amp; echo "D-18 CustomEvent detail shape correct"</automated>
    <automated>grep -q "savePrefs({ ...current, timbre: next })" src/hooks/useTimbreChoice.ts &amp;&amp; echo "Merge-write contract correct"</automated>
    <automated>grep -c "useCallback" src/hooks/useTimbreChoice.ts | awk '$1>=1 {print "useCallback present"}'</automated>
  </verify>
  <done>
    `src/hooks/useTimbreChoice.ts` exists; exports `useTimbreChoice` returning `{ timbre, setTimbre }`; `setTimbre` performs fresh-loadPrefs → savePrefs merge → setTimbreState → dispatchEvent in that order; CustomEvent detail is `{ key: 'timbre', value: next }`; `useCallback([])` empty deps; `npx tsc --noEmit` exit 0; ESLint exit 0.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create `src/hooks/useTimbreChoice.test.ts` — verbatim mirror of useVariantChoice.test.ts</name>
  <read_first>
    src/hooks/useVariantChoice.test.ts (full file — 112 lines — the template; copy structure verbatim with substitutions),
    src/hooks/useTimbreChoice.ts (Task 1 output — the SUT),
    src/storage/prefs.ts (STATE_KEY export + UserPrefs shape),
    src/domain/settings.ts (TIMBRE_OPTIONS / TimbreId — for "every valid timbre round-trips" smoke loop)
  </read_first>
  <behavior>
    - Mount-time read test: `useTimbreChoice` returns the stored timbre when localStorage is seeded with `{ version: 1, prefs: { theme:'system', timbre:'bell', variant:'orb', locale:'en' } }`.
    - Default fallback test: when localStorage is empty, the hook returns `DEFAULT_TIMBRE = 'bowl'` (Phase 14 D-04).
    - setTimbre persists test: calling `setTimbre('sine')` causes a subsequent `loadPrefs()` (or direct localStorage read) to return `{ ...prevPrefs, timbre: 'sine' }`. The other three prefs fields (theme/variant/locale) are preserved untouched (per-field isolation).
    - Optimistic-UI test: after `setTimbre('chime')`, the hook's returned `timbre` is `'chime'` on the next render — no waiting for a storage event.
    - CustomEvent dispatch test: a `window.addEventListener('hrv:prefs-changed', spy)` registered BEFORE `setTimbre('bell')` is called receives one CustomEvent with `event.detail = { key: 'timbre', value: 'bell' }`. After test, remove listener.
    - setTimbre identity stability test: between two renders of the hook with no other state change, the returned `setTimbre` reference is `===` identical.
    - All tests use the `seedPrefs` helper pattern from `useVariantChoice.test.ts` (writes to `STATE_KEY` directly to seed localStorage before render).
    - `beforeEach` clears `window.localStorage` (matching `useVariantChoice.test.ts` posture).
  </behavior>
  <action>
    Create `src/hooks/useTimbreChoice.test.ts`. Copy `useVariantChoice.test.ts` (lines 1-112) verbatim as the source template. Apply the following exact substitutions across the entire file: `useVariantChoice` → `useTimbreChoice`; `variant` (every identifier — both prop names and local vars) → `timbre`; `VisualVariantId` → `TimbreId`; `setVariant` → `setTimbre`; `{ variant, setVariant }` → `{ timbre, setTimbre }`; `'variant'` (in `detail.key` checks) → `'timbre'`; the seeded variant values `'orb'/'square'/'diamond'` map to timbre values `'bowl'/'bell'/'sine'` (or pick `'chime'` for the third position if useVariantChoice uses three distinct values). Update the leading import to import `TIMBRE_OPTIONS, type TimbreId` from `../domain/settings` (replacing `VARIANT_OPTIONS, type VisualVariantId`). The `DEFAULT_FULL_PREFS` helper constant remains the same (`{ theme: 'system', timbre: 'bowl', variant: 'orb', locale: 'en' }`) — only the test-specific timbre values change. Preserve `seedPrefs` helper signature, `beforeEach(() => window.localStorage.clear())`, and the `STATE_KEY` import from `'../storage'`. Do NOT add new tests beyond the verbatim mirror — the goal is parity, not expansion.
  </action>
  <verify>
    <automated>npm test -- src/hooks/useTimbreChoice.test.ts --run 2>&amp;1 | tail -20</automated>
    <automated>grep -q "setTimbre" src/hooks/useTimbreChoice.test.ts &amp;&amp; echo "setTimbre exercised"</automated>
    <automated>grep -q "detail.key === 'timbre'\|detail: { key: 'timbre'" src/hooks/useTimbreChoice.test.ts &amp;&amp; echo "D-18 detail.key === 'timbre' assertion present"</automated>
    <automated>grep -q "STATE_KEY" src/hooks/useTimbreChoice.test.ts &amp;&amp; echo "STATE_KEY seed pattern present"</automated>
    <automated>diff <(grep -c "^  it(" src/hooks/useVariantChoice.test.ts) <(grep -c "^  it(" src/hooks/useTimbreChoice.test.ts) &amp;&amp; echo "Test count parity with useVariantChoice"</automated>
  </verify>
  <done>
    `src/hooks/useTimbreChoice.test.ts` exists and runs green; test count is at parity with `useVariantChoice.test.ts` (same number of `it(` blocks); CustomEvent `detail.key === 'timbre'` assertion present; localStorage seed/clear pattern mirrors the source template; all tests exit 0 via `npm test -- src/hooks/useTimbreChoice.test.ts`.
  </done>
</task>

<task type="auto">
  <name>Task 3: Run global green-gate and commit</name>
  <read_first>
    .planning/phases/18-audio-timbres/18-CONTEXT.md (D-13 per-commit green-gate)
  </read_first>
  <action>
    Run `npx tsc --noEmit && npm run lint && npm run build && npm test --run` — all four MUST exit 0. The test suite count grows from 593 (after Plan 01) to 593 + N (where N matches the it-block count of useVariantChoice.test.ts; expected ~6-8 new tests). If any gate fails, fix in this plan. Stage `src/hooks/useTimbreChoice.ts` + `src/hooks/useTimbreChoice.test.ts` only. Commit with message `feat(18-02): add useTimbreChoice picker-side hook (TIMBRE-04)`.
  </action>
  <verify>
    <automated>npx tsc --noEmit &amp;&amp; npm run lint &amp;&amp; npm run build &amp;&amp; npm test --run 2>&amp;1 | tail -5</automated>
    <automated>git log --oneline -1 | grep -E "feat\(18-02\)" &amp;&amp; echo "Commit landed"</automated>
    <automated>git show --stat HEAD | grep -E "useTimbreChoice\.(ts|test\.ts)" | wc -l | awk '$1==2 {print "Exactly 2 files in commit"}'</automated>
  </verify>
  <done>
    All four gates green; one commit landed with exactly `src/hooks/useTimbreChoice.ts` and `src/hooks/useTimbreChoice.test.ts`; existing tests unaffected; no edits to `prefs.ts`, `settings.ts`, `audioEngine.ts`, `cueSynth.ts`, or `useAudioCues.ts`.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| picker UI → localStorage | `setTimbre(next)` writes a `TimbreId`-typed value through the existing `savePrefs` API (Phase 14 D-10 locked). `next` is constrained to the `TimbreId` union at the TypeScript compile level — TimbrePicker (Plan 05) only iterates `TIMBRE_OPTIONS`, so the value cannot be user-supplied. |
| picker UI → window CustomEvent | `dispatchEvent` fires a same-origin `CustomEvent` on `window`. No cross-origin surface (`detail` is not serialized across origins). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-02-01 | Spoofing | A malicious script could dispatch a forged `'hrv:prefs-changed'` event with `detail.key === 'timbre'` and arbitrary value | accept | Phase 18 has no App-side listener consuming the event (D-08 — timbre has no idle-state visual surface); the only consumer is the picker's own optimistic state mirror, which is local to the writing tab. A forged event in a third-party tab cannot escape that tab's window. No PII, no auth, no privileged action. |
| T-18-02-02 | Tampering | Storage write could overwrite other prefs fields (theme/variant/locale) | mitigate | Spread merge `{ ...current, timbre: next }` preserves all other fields per Phase 14 D-17 per-field isolation. Tested in Task 2 ("setTimbre preserves other prefs fields"). |
| T-18-02-03 | Information Disclosure | localStorage `STATE_KEY` already covers all four prefs dimensions — no NEW disclosure surface introduced | accept | Phase 14 already exposed `Envelope.prefs` storage; Phase 18 only widens the in-use range of one existing field (`timbre`). No PII, no credentials. |
| T-18-02-04 | Repudiation | n/a — local-only client, no audit log expected | accept | Project is local-only by design (PROJECT.md). |
| T-18-02-05 | Denial of Service | A malicious `loadPrefs()` could throw and break the picker | mitigate | Phase 14 D-10 guarantees `loadPrefs` is non-throwing and falls back to `DEFAULT_PREFS` on corrupted storage. The hook calls `loadPrefs()` directly (no try/catch needed). |
| T-18-02-06 | Elevation of Privilege | n/a — no privilege boundaries in local client | accept | |
| T-18-02-07 | Supply chain | New npm dependency could enter via this module | mitigate | D-14 zero-deps invariant; the hook imports only `useCallback`/`useState` from `react` (existing) and project-local `storage/prefs` + `domain/settings`. package.json untouched. |
</threat_model>

<verification>
- `npx tsc --noEmit && npm run lint && npm run build && npm test --run` all exit 0.
- Test count grows by ~6-8 (matching `useVariantChoice.test.ts` it-block count) — current baseline 593 from Plan 01.
- Manual grep verifies: zero edits to `src/storage/prefs.ts`, `src/domain/settings.ts`, `src/audio/cueSynth.ts`, `src/audio/audioEngine.ts`, `src/hooks/useAudioCues.ts`, `src/components/TimbrePicker.tsx`.
- The hook is independently consumable: Plan 05 (TimbrePicker) will import `useTimbreChoice` without further Plan 02 changes.
</verification>

<success_criteria>
- [ ] `src/hooks/useTimbreChoice.ts` exists; exports `useTimbreChoice` with `{ timbre, setTimbre }` return shape.
- [ ] `setTimbre` writes merged envelope to storage AND dispatches `'hrv:prefs-changed'` with `detail: { key: 'timbre', value: next }` (D-18).
- [ ] `useCallback([])` empty deps for stable setter identity.
- [ ] `src/hooks/useTimbreChoice.test.ts` runs green; test count parity with `useVariantChoice.test.ts`.
- [ ] `tsc && lint && build && test` exit 0 (D-13).
- [ ] No edits to `prefs.ts`, `settings.ts`, or any Phase 17/16 hook file (D-15 + D-16 file-split invariants preserved).
- [ ] Single commit on green: `feat(18-02): add useTimbreChoice picker-side hook (TIMBRE-04)`.
</success_criteria>

<output>
After completion, create `.planning/phases/18-audio-timbres/18-02-SUMMARY.md` capturing:
- Files added: `src/hooks/useTimbreChoice.ts`, `src/hooks/useTimbreChoice.test.ts`.
- Test count delta (593 → 593 + N where N is parity with useVariantChoice it-block count).
- Commit hash.
- Forward-decl: Plan 05 (TimbrePicker.tsx) will consume `useTimbreChoice` to wire the radiogroup write path.
</output>
