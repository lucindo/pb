---
phase: 18-audio-timbres
plan: 03
type: execute
wave: 2
depends_on:
  - 18-01
files_modified:
  - src/audio/cueSynth.ts
  - src/audio/cueSynth.test.ts
  - src/audio/audioEngine.ts
  - src/audio/audioEngine.test.ts
autonomous: true
requirements:
  - TIMBRE-01
  - TIMBRE-02
  - TIMBRE-03
  - TIMBRE-05
tags:
  - audio
  - dispatch
  - engine
must_haves:
  truths:
    - "scheduleBowlCue is parameterized to accept a TimbrePreset (replaces loose fundamentalHz + defaultDecayTau args)"
    - "scheduleInCueForTimbre and scheduleOutCueForTimbre dispatch functions exist and look up TIMBRE_PRESETS[timbre] then call the parameterized scheduler"
    - "Existing scheduleInCue / scheduleOutCue public signatures are preserved as Bowl-only thin wrappers (D-01 option (a) — TIMBRE-02 signature-stability proof + smaller diff)"
    - "scheduleTick (lead-in 1200Hz square) is bit-identical to v1.0.1 (D-07 — lead-in tick fixed across all timbres)"
    - "Sustain-floor constants (PERCEPTUAL_SILENCE_TAU_MULT, SUSTAIN_FLOOR_RATIO, PHASE_END_FADE_OUT_TAU, PHASE_END_FADE_OUT_LEAD_SEC, NEAR_SILENCE) remain shared module-level constants (D-12 — shared sustain across all timbres)"
    - "Module-level constants IN_FUNDAMENTAL_HZ / OUT_FUNDAMENTAL_HZ / PARTIALS / IN_DECAY_TIME_CONSTANT / OUT_DECAY_TIME_CONSTANT / FILTER_FREQ_HZ / FILTER_Q / PEAK_GAIN are DELETED from cueSynth.ts — they now live in TIMBRE_PRESETS.bowl (verbatim move; TIMBRE-02 byte-identical Bowl path)"
    - "AudioEngineOptions gains required `timbre: TimbreId` field; createAudioEngine captures it once at construction into a closed-over sessionTimbre const; no setTimbre setter on AudioEngine (D-08 capture-at-Start is the only mutation path)"
    - "scheduleLeadIn forwards sessionTimbre to scheduleInCueForTimbre on the first In cue; scheduleTick call is UNCHANGED (D-07)"
    - "scheduleNextCue forwards sessionTimbre to scheduleInCueForTimbre / scheduleOutCueForTimbre via the in/out ternary"
    - "All 20 existing createAudioEngine() call sites in audioEngine.test.ts are updated to pass { timbre: 'bowl' } (existing tests stay green by passing bowl explicitly — TIMBRE-02 byte-identical proof at the integration layer)"
    - "Existing 3-partial Bowl tests in cueSynth.test.ts stay UNCHANGED and green (TIMBRE-02 byte-identical proof at the cueSynth layer)"
    - "Parameterized it.each(TIMBRE_OPTIONS) blocks cover scheduleInCueForTimbre / scheduleOutCueForTimbre — oscillator count = preset.partials.length, fundamental frequency matches preset.fundamentalHzIn / Out"
    - "tsc && lint && build && test exit 0 at this commit (D-13)"
  artifacts:
    - path: "src/audio/cueSynth.ts"
      provides: "Parameterized scheduleBowlCue(preset, kind, ...) + exported scheduleInCueForTimbre / scheduleOutCueForTimbre dispatch + preserved scheduleInCue / scheduleOutCue Bowl wrappers + unchanged scheduleTick"
      contains: "export function scheduleInCueForTimbre"
      contains_also: "export function scheduleOutCueForTimbre"
      contains_also: "TIMBRE_PRESETS[timbre]"
    - path: "src/audio/audioEngine.ts"
      provides: "AudioEngineOptions.timbre required + createAudioEngine captures it into sessionTimbre closure + scheduleLeadIn / scheduleNextCue forward via dispatch"
      contains: "timbre: TimbreId"
      contains_also: "scheduleInCueForTimbre"
      contains_also: "scheduleOutCueForTimbre"
    - path: "src/audio/cueSynth.test.ts"
      provides: "Existing Bowl tests unchanged + new it.each(TIMBRE_OPTIONS) parameterized blocks for scheduleInCueForTimbre / scheduleOutCueForTimbre"
      contains: "scheduleInCueForTimbre"
      contains_also: "scheduleOutCueForTimbre"
      contains_also: "TIMBRE_OPTIONS"
    - path: "src/audio/audioEngine.test.ts"
      provides: "All createAudioEngine() updated to createAudioEngine({ timbre: 'bowl', ... }) + new tests for timbre propagation through scheduleLeadIn + scheduleNextCue + engine-capture immutability"
      contains: "timbre: 'bowl'"
      contains_also: "timbre: 'bell'"
  key_links:
    - from: "src/audio/cueSynth.ts"
      to: "src/audio/timbres.ts"
      via: "import { TIMBRE_PRESETS, type TimbrePreset }"
      pattern: "import.*TIMBRE_PRESETS.*from './timbres'"
    - from: "src/audio/audioEngine.ts"
      to: "src/audio/cueSynth.ts"
      via: "import { scheduleInCueForTimbre, scheduleOutCueForTimbre, scheduleTick, type CueHandle }"
      pattern: "import.*scheduleInCueForTimbre.*scheduleOutCueForTimbre"
    - from: "src/audio/audioEngine.ts"
      to: "src/domain/settings.ts"
      via: "import type { TimbreId }"
      pattern: "import type.*TimbreId.*from.*domain/settings"
---

<objective>
Plumb the four-timbre dispatch through the audio synthesis layer: parameterize `scheduleBowlCue` to read DSP recipes from `TIMBRE_PRESETS` (Plan 01), expose `scheduleInCueForTimbre` / `scheduleOutCueForTimbre` dispatch functions, preserve the existing `scheduleInCue` / `scheduleOutCue` signatures as Bowl-only wrappers (D-01 option (a) — smaller diff + TIMBRE-02 signature-stability proof), and extend `createAudioEngine` to capture a session timbre once at construction and forward it through `scheduleLeadIn` + `scheduleNextCue`.

**Critical invariant proofs:**
- **TIMBRE-02 byte-identical Bowl path:** the existing 3-partial Bowl tests in `cueSynth.test.ts` (440Hz fundamental, partials at 1214.4 / 2376 Hz, 0.18 peak gain, 1.4s decay) stay UNCHANGED and green. The 20 existing `createAudioEngine()` no-arg call sites in `audioEngine.test.ts` are updated to `createAudioEngine({ timbre: 'bowl', ... })` — bowl explicit-pass guarantees they still exercise the v1.0.1 path verbatim.
- **D-07 lead-in tick fixed:** `scheduleTick` body (lines 205-236) is touched ONLY if the line numbers shift due to module-level constant deletions above. The function logic is bit-identical.
- **D-12 shared sustain-floor:** the `PERCEPTUAL_SILENCE_TAU_MULT`, `SUSTAIN_FLOOR_RATIO`, `PHASE_END_FADE_OUT_TAU`, `PHASE_END_FADE_OUT_LEAD_SEC`, `NEAR_SILENCE` constants remain module-level. Per-timbre threshold auto-derives from `preset.decayTauIn/Out × PERCEPTUAL_SILENCE_TAU_MULT`.
- **D-08 capture-at-construction:** the engine has no `setTimbre` setter; `sessionTimbre` is closed over once.

Purpose: Land the audio plumbing for the four timbres so Plan 04 (useAudioCues `timbreRef` + `start(plan, timbre)`) and Plan 06 (App integration) can wire end-to-end. The Plan 01 data is now actually used.

Output: Edits to four files (~80 LOC of source delta + ~120 LOC of test delta). NEW exports `scheduleInCueForTimbre` and `scheduleOutCueForTimbre`. NEW typed field `AudioEngineOptions.timbre: TimbreId`.
</objective>

<execution_context>
@$HOME/.claude-personal-personal/get-shit-done/workflows/execute-plan.md
@$HOME/.claude-personal-personal/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md

@.planning/phases/18-audio-timbres/18-CONTEXT.md
@.planning/phases/18-audio-timbres/18-PATTERNS.md
@.planning/phases/18-audio-timbres/18-01-SUMMARY.md

@src/audio/cueSynth.ts
@src/audio/audioEngine.ts
@src/audio/cueSynth.test.ts
@src/audio/audioEngine.test.ts
@src/audio/timbres.ts
@src/domain/settings.ts

<interfaces>
<!-- Key types and contracts the executor needs. -->

From `src/audio/timbres.ts` (Plan 01 output):

  export interface TimbrePreset {
    fundamentalHzIn: number; fundamentalHzOut: number;
    partials: ReadonlyArray<{ ratio: number; gain: number }>;
    decayTauIn: number; decayTauOut: number;
    filterFreqHz: number; filterQ: number;
    peakGain: number; oscillatorType: OscillatorType;
  }
  export const TIMBRE_PRESETS: Readonly<Record<TimbreId, TimbrePreset>>

From `src/audio/cueSynth.ts` lines 74-81 (the function to parameterize):

  function scheduleBowlCue(
    audioCtx: AudioContext, when: number, destination: AudioNode,
    fundamentalHz: number,        // ← REPLACE with preset
    defaultDecayTau: number,      // ← REPLACE with preset
    phaseDurationSec?: number,
  ): CueHandle { ... }

New parameterized signature (CONTEXT.md §Phase Boundary item 2 + PATTERNS.md):

  function scheduleBowlCue(
    audioCtx: AudioContext, when: number, destination: AudioNode,
    preset: TimbrePreset,
    kind: 'in' | 'out',           // selects fundamentalHzIn vs Out and decayTauIn vs Out
    phaseDurationSec?: number,
  ): CueHandle

New exported dispatch functions:

  export function scheduleInCueForTimbre(
    audioCtx: AudioContext, when: number, destination: AudioNode,
    timbre: TimbreId, phaseDurationSec?: number,
  ): CueHandle {
    return scheduleBowlCue(audioCtx, when, destination, TIMBRE_PRESETS[timbre], 'in', phaseDurationSec)
  }
  export function scheduleOutCueForTimbre(...): CueHandle { ... 'out' ... }

Preserved Bowl-only thin wrappers (D-01 option (a) — TIMBRE-02 signature stability):

  export function scheduleInCue(audioCtx, when, destination, phaseDurationSec?) {
    return scheduleInCueForTimbre(audioCtx, when, destination, 'bowl', phaseDurationSec)
  }
  export function scheduleOutCue(...) { ... 'bowl' ... }

From `src/audio/audioEngine.ts` lines 59-66 (the options interface to extend):

  export interface AudioEngineOptions {
    onStateChange?: (state: AudioContextState | 'interrupted') => void
    timbre: TimbreId  // NEW — required (D-08); engine captures once
  }

Engine internal capture (CONTEXT.md §Phase Boundary item 4):

  export async function createAudioEngine(opts: AudioEngineOptions): Promise<AudioEngine> {
    const audioCtx = new AudioContext()
    // ... existing WR-06 resume + statechange listener ...
    const sessionTimbre = opts.timbre  // ← D-08 capture-at-construction; immutable for engine lifetime
    // ... existing activeCues / muted / closed setup ...
    // scheduleLeadIn forwards sessionTimbre to scheduleInCueForTimbre (line 170 swap)
    // scheduleNextCue forwards sessionTimbre to scheduleInCueForTimbre / scheduleOutCueForTimbre (lines 183-186 swap)
  }
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Parameterize `scheduleBowlCue` + add `scheduleInCueForTimbre` / `scheduleOutCueForTimbre` in `src/audio/cueSynth.ts`</name>
  <read_first>
    src/audio/cueSynth.ts (full file — 236 lines; specifically lines 11-27 module constants, lines 74-171 scheduleBowlCue body, lines 173-203 scheduleInCue / scheduleOutCue wrappers, lines 205-236 scheduleTick UNCHANGED per D-07),
    src/audio/timbres.ts (Plan 01 output — TIMBRE_PRESETS + TimbrePreset interface to import),
    src/domain/settings.ts (TimbreId type),
    .planning/phases/18-audio-timbres/18-CONTEXT.md (D-01 architecture choice; D-07 scheduleTick unchanged; D-12 sustain-floor shared),
    .planning/phases/18-audio-timbres/18-PATTERNS.md (§"`src/audio/cueSynth.ts` (EDIT — parameterize)" — full edit shape including partials loop swap + filter/envelope swap + new dispatch functions)
  </read_first>
  <behavior>
    - Module-level constants `IN_FUNDAMENTAL_HZ`, `OUT_FUNDAMENTAL_HZ`, `PEAK_GAIN`, `PARTIALS`, `IN_DECAY_TIME_CONSTANT`, `OUT_DECAY_TIME_CONSTANT`, `FILTER_FREQ_HZ`, `FILTER_Q` are DELETED (their values live in `TIMBRE_PRESETS.bowl` per Plan 01).
    - Module-level constants that stay: `STRIKE_RAMP_OFFSET`, `TAIL_MULTIPLIER`, `TAIL_PADDING_SEC`, `CLEANUP_PADDING_SEC`, `PERCEPTUAL_SILENCE_TAU_MULT`, `SUSTAIN_FLOOR_RATIO`, `PHASE_END_FADE_OUT_TAU`, `PHASE_END_FADE_OUT_LEAD_SEC`, `NEAR_SILENCE` (D-12 shared sustain) plus all `TICK_*` constants (D-07 lead-in tick fixed).
    - `scheduleBowlCue` signature changes from `(audioCtx, when, destination, fundamentalHz, defaultDecayTau, phaseDurationSec?)` to `(audioCtx, when, destination, preset: TimbrePreset, kind: 'in' | 'out', phaseDurationSec?)`. Body resolves `fundamentalHz` and `defaultDecayTau` from preset based on kind: `const fundamentalHz = kind === 'in' ? preset.fundamentalHzIn : preset.fundamentalHzOut`; `const defaultDecayTau = kind === 'in' ? preset.decayTauIn : preset.decayTauOut`. All other body references swap: `FILTER_FREQ_HZ → preset.filterFreqHz`, `FILTER_Q → preset.filterQ`, `PEAK_GAIN → preset.peakGain`, `PARTIALS → preset.partials`, `osc.type = 'sine' → osc.type = preset.oscillatorType`.
    - NEW exported `scheduleInCueForTimbre(audioCtx, when, destination, timbre: TimbreId, phaseDurationSec?)` and `scheduleOutCueForTimbre(...)` look up `TIMBRE_PRESETS[timbre]` and call `scheduleBowlCue(..., preset, 'in'|'out', ...)`.
    - EXISTING exported `scheduleInCue(audioCtx, when, destination, phaseDurationSec?)` and `scheduleOutCue(...)` are PRESERVED as Bowl-only thin wrappers (D-01 option (a)): they call `scheduleInCueForTimbre(..., 'bowl', ...)` / `scheduleOutCueForTimbre(..., 'bowl', ...)`. Their public signatures are unchanged so callers that haven't yet migrated (e.g. potential third-party tests not in the repo) keep working — TIMBRE-02 signature stability.
    - `scheduleTick` (currently lines 205-236) is UNCHANGED in body (line numbers may shift due to constants deletion). D-07 hard invariant.
    - File header comment updates to note: "Bowl values now live in src/audio/timbres.ts TIMBRE_PRESETS.bowl (Phase 18 D-02). Per-timbre dispatch via scheduleInCueForTimbre / scheduleOutCueForTimbre."
  </behavior>
  <action>
    Edit `src/audio/cueSynth.ts`. (1) Add import `import { TIMBRE_PRESETS, type TimbrePreset } from './timbres'` and `import type { TimbreId } from '../domain/settings'` at the top. (2) Delete the 8 module-level constants listed in `<behavior>` (lines ~11-24 in current file). KEEP the rest of the module-level constants block (sustain-floor block at lines 52-56 + tick block at lines 60-66). (3) Update the file header comment block (lines 1-9) to reference Phase 18 + TIMBRE_PRESETS. (4) Change `scheduleBowlCue` signature to `(audioCtx, when, destination, preset: TimbrePreset, kind: 'in' | 'out', phaseDurationSec?)`. (5) Inside the body, before any other logic, add `const fundamentalHz = kind === 'in' ? preset.fundamentalHzIn : preset.fundamentalHzOut` and `const defaultDecayTau = kind === 'in' ? preset.decayTauIn : preset.decayTauOut`. (6) Replace `FILTER_FREQ_HZ` with `preset.filterFreqHz`, `FILTER_Q` with `preset.filterQ`, `PEAK_GAIN` with `preset.peakGain`, `PARTIALS` with `preset.partials`, and `osc.type = 'sine'` with `osc.type = preset.oscillatorType` inside the body. The rest of the body (sustain-floor branch, partials loop, filter + envelope chain, AUDIO-04 disconnect listeners, return shape) is UNCHANGED. (7) Add new exported `scheduleInCueForTimbre` and `scheduleOutCueForTimbre` functions immediately after `scheduleBowlCue` — they look up `TIMBRE_PRESETS[timbre]` and forward to `scheduleBowlCue`. (8) Replace the existing `scheduleInCue` and `scheduleOutCue` function BODIES to delegate to `scheduleInCueForTimbre(..., 'bowl', ...)` / `scheduleOutCueForTimbre(..., 'bowl', ...)` respectively — their exported names and parameter lists are unchanged. (9) Leave `scheduleTick` body completely untouched (D-07 hard invariant; only its position in the file may shift). Do NOT delete `scheduleInCue` / `scheduleOutCue` (D-01 option (a) — signature stability). Do NOT change `scheduleTick` body in any way (D-07 + TIMBRE-02 lead-in unchanged).
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&amp;1 | grep "cueSynth\.ts" ; test $? -ne 0 &amp;&amp; echo "tsc clean for cueSynth.ts"</automated>
    <automated>grep -q "export function scheduleInCueForTimbre" src/audio/cueSynth.ts &amp;&amp; echo "scheduleInCueForTimbre exported"</automated>
    <automated>grep -q "export function scheduleOutCueForTimbre" src/audio/cueSynth.ts &amp;&amp; echo "scheduleOutCueForTimbre exported"</automated>
    <automated>grep -q "export function scheduleInCue" src/audio/cueSynth.ts &amp;&amp; echo "scheduleInCue preserved (Bowl wrapper)"</automated>
    <automated>grep -q "export function scheduleOutCue" src/audio/cueSynth.ts &amp;&amp; echo "scheduleOutCue preserved (Bowl wrapper)"</automated>
    <automated>! grep -E "^const (IN_FUNDAMENTAL_HZ|OUT_FUNDAMENTAL_HZ|PEAK_GAIN|PARTIALS|IN_DECAY_TIME_CONSTANT|OUT_DECAY_TIME_CONSTANT|FILTER_FREQ_HZ|FILTER_Q)" src/audio/cueSynth.ts &amp;&amp; echo "8 module constants deleted (moved to TIMBRE_PRESETS.bowl)"</automated>
    <automated>grep -E "^const (PERCEPTUAL_SILENCE_TAU_MULT|SUSTAIN_FLOOR_RATIO|PHASE_END_FADE_OUT_TAU|PHASE_END_FADE_OUT_LEAD_SEC|NEAR_SILENCE)" src/audio/cueSynth.ts | wc -l | awk '$1==5 {print "5 sustain-floor constants preserved (D-12)"}'</automated>
    <automated>grep -E "^(const TICK_|export function scheduleTick)" src/audio/cueSynth.ts | wc -l | awk '$1>=7 {print "Tick constants + function preserved (D-07)"}'</automated>
    <automated>git diff src/audio/cueSynth.ts | grep -E "^-.*440|^-.*220|^-.*3000|^-.*0\.18" | wc -l | awk '$1>=0 {print "Deleted constants confirm verbatim move to TIMBRE_PRESETS.bowl (TIMBRE-02 proof; numbers will reappear in timbres.ts which is uncommitted in this diff)"}'</automated>
  </verify>
  <done>
    `src/audio/cueSynth.ts` compiles green; exports `scheduleInCueForTimbre`, `scheduleOutCueForTimbre`, preserved `scheduleInCue` / `scheduleOutCue` (Bowl wrappers), and unchanged `scheduleTick`; 8 Bowl module constants deleted (their values now live in `TIMBRE_PRESETS.bowl` per Plan 01); sustain-floor constants preserved at module level (D-12); tick constants and function body untouched (D-07).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend `AudioEngineOptions.timbre` + capture-at-construction in `src/audio/audioEngine.ts`</name>
  <read_first>
    src/audio/audioEngine.ts (full file — 262 lines; specifically lines 22-23 imports, lines 59-66 AudioEngineOptions, lines 107-262 createAudioEngine + engine internals + scheduleLeadIn lines 155-173 + scheduleNextCue lines 175-188),
    src/audio/cueSynth.ts (Task 1 output — new exports to import: scheduleInCueForTimbre, scheduleOutCueForTimbre; preserved imports: scheduleTick, type CueHandle),
    src/domain/settings.ts (TimbreId type — Phase 14 D-01 locked),
    .planning/phases/18-audio-timbres/18-CONTEXT.md (D-08 capture-at-construction, no setter; D-07 scheduleTick call site UNCHANGED),
    .planning/phases/18-audio-timbres/18-PATTERNS.md (§"`src/audio/audioEngine.ts` (EDIT)" — import block update + AudioEngineOptions.timbre + sessionTimbre capture + scheduleLeadIn forwarder line 170 + scheduleNextCue forwarder lines 183-186)
  </read_first>
  <behavior>
    - Import line update: replace `import { scheduleInCue, scheduleOutCue, scheduleTick, type CueHandle } from './cueSynth'` with `import { scheduleInCueForTimbre, scheduleOutCueForTimbre, scheduleTick, type CueHandle } from './cueSynth'`. Add new import `import type { TimbreId } from '../domain/settings'`.
    - `AudioEngineOptions` gains a NEW REQUIRED field `timbre: TimbreId`. Add a JSDoc above it: `/** Phase 18 D-08: timbre captured at session start; engine never re-reads prefs. Caller passes the snapshot from useAudioCues.start(plan, timbre). No setter is exposed — capture-at-construction is the only mutation path. */`. The previously-optional `onStateChange` remains optional.
    - `createAudioEngine` signature stays `async (opts: AudioEngineOptions): Promise<AudioEngine>` — but DROP the default `= {}` value on `opts` since `timbre` is now required (TS will enforce caller-side that an options object is passed).
    - Inside `createAudioEngine`, immediately after the existing `let muted = false` declaration (currently line ~142) and BEFORE the `let closed = false` line, add: `const sessionTimbre: TimbreId = opts.timbre`. Mark this with a comment: `// Phase 18 D-08: capture timbre once at construction. Immutable for this engine's lifetime — no setter exposed.`.
    - `scheduleLeadIn` (currently lines 155-173) updates ONLY the first In cue line (currently line 170): change `scheduleInCue(audioCtx, firstInCueTime, audioCtx.destination, firstInPhaseDurationSec)` to `scheduleInCueForTimbre(audioCtx, firstInCueTime, audioCtx.destination, sessionTimbre, firstInPhaseDurationSec)`. The three `scheduleTick` calls (lines 162-164) are UNCHANGED — D-07 hard invariant.
    - `scheduleNextCue` (currently lines 175-188) updates the ternary inside the `cue` const (currently lines 183-186): change `scheduleInCue(audioCtx, clampedAudioTime, audioCtx.destination, phaseDurationSec)` to `scheduleInCueForTimbre(audioCtx, clampedAudioTime, audioCtx.destination, sessionTimbre, phaseDurationSec)` and `scheduleOutCue(...)` to `scheduleOutCueForTimbre(..., sessionTimbre, ...)`.
    - No new method on the `AudioEngine` interface. No `setTimbre` setter (D-08 hard invariant). No re-export of `sessionTimbre` (it stays closed over).
    - All existing engine behavior (mute fade-out, statechange listener, resume, close, prune, activeCues Set) is UNCHANGED.
  </behavior>
  <action>
    Edit `src/audio/audioEngine.ts`. (1) Replace the import on line 23 — old: `import { scheduleInCue, scheduleOutCue, scheduleTick, type CueHandle } from './cueSynth'`; new: `import { scheduleInCueForTimbre, scheduleOutCueForTimbre, scheduleTick, type CueHandle } from './cueSynth'`. (2) Add new import after line 23: `import type { TimbreId } from '../domain/settings'`. (3) Extend `AudioEngineOptions` (lines 59-66) by adding the `timbre: TimbreId` field (with the JSDoc above). Keep `onStateChange` optional. (4) Change `createAudioEngine` signature on line 107 from `(opts: AudioEngineOptions = {})` to `(opts: AudioEngineOptions)` — drop the default-empty-object since `timbre` is required (this is the breaking change the test sweep in Task 4 must cover). (5) Inside the function body, just after `let muted = false` (around line 142) and before `let closed = false`, insert `const sessionTimbre: TimbreId = opts.timbre` with the Phase 18 D-08 comment. (6) In `scheduleLeadIn` (lines 155-173), update only the `scheduleInCue` call at line 170 → `scheduleInCueForTimbre` with `sessionTimbre` inserted as the 4th argument before `firstInPhaseDurationSec`. The three `scheduleTick` calls at lines 162-164 are LEFT UNCHANGED (D-07). (7) In `scheduleNextCue` (lines 175-188), update the in/out ternary at lines 183-186 — both branches: `scheduleInCue` → `scheduleInCueForTimbre` and `scheduleOutCue` → `scheduleOutCueForTimbre`, both with `sessionTimbre` inserted as the 4th argument before `phaseDurationSec`. (8) Do NOT add a `setTimbre` method on the `AudioEngine` interface or on the engine object literal — D-08 forbids it.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&amp;1 | grep -E "audioEngine\.ts" ; test $? -ne 0 &amp;&amp; echo "tsc clean for audioEngine.ts"</automated>
    <automated>grep -q "timbre: TimbreId" src/audio/audioEngine.ts &amp;&amp; echo "AudioEngineOptions.timbre present"</automated>
    <automated>grep -q "const sessionTimbre: TimbreId = opts.timbre" src/audio/audioEngine.ts &amp;&amp; echo "D-08 capture-at-construction present"</automated>
    <automated>grep -q "scheduleInCueForTimbre" src/audio/audioEngine.ts &amp;&amp; echo "scheduleInCueForTimbre wired"</automated>
    <automated>grep -q "scheduleOutCueForTimbre" src/audio/audioEngine.ts &amp;&amp; echo "scheduleOutCueForTimbre wired"</automated>
    <automated>! grep -q "setTimbre" src/audio/audioEngine.ts &amp;&amp; echo "No setTimbre setter (D-08 invariant)"</automated>
    <automated>grep -c "scheduleTick(audioCtx" src/audio/audioEngine.ts | awk '$1==3 {print "3 scheduleTick calls preserved (D-07 lead-in unchanged)"}'</automated>
  </verify>
  <done>
    `src/audio/audioEngine.ts` compiles green; `AudioEngineOptions.timbre: TimbreId` is required; `createAudioEngine` captures `sessionTimbre` once; `scheduleLeadIn` forwards `sessionTimbre` to `scheduleInCueForTimbre` (first In cue only — three `scheduleTick` calls UNCHANGED); `scheduleNextCue` forwards via the in/out ternary; no `setTimbre` setter exists on the engine.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Extend `src/audio/cueSynth.test.ts` — preserve Bowl tests verbatim + add per-timbre it.each blocks</name>
  <read_first>
    src/audio/cueSynth.test.ts (full file — 360 lines; existing Bowl-specific tests at lines 17-100 — KEEP unchanged for TIMBRE-02 byte-identical proof),
    src/audio/cueSynth.ts (Task 1 output — new exports: scheduleInCueForTimbre, scheduleOutCueForTimbre),
    src/audio/timbres.ts (TIMBRE_PRESETS import for parameterized assertions),
    src/domain/settings.ts (TIMBRE_OPTIONS for it.each iteration),
    .planning/phases/18-audio-timbres/18-PATTERNS.md (§"`src/audio/cueSynth.test.ts` (EXTENDED)" — it.each(TIMBRE_OPTIONS) parameterized pattern + import additions)
  </read_first>
  <behavior>
    - All existing tests in `cueSynth.test.ts` (lines 17-end of file) stay UNCHANGED — TIMBRE-02 byte-identical proof at the synthesis layer.
    - NEW imports added: `import { scheduleInCueForTimbre, scheduleOutCueForTimbre } from './cueSynth'`; `import { TIMBRE_OPTIONS } from '../domain/settings'`; `import { TIMBRE_PRESETS } from './timbres'`.
    - NEW `describe('scheduleInCueForTimbre (all timbres)', () => { ... })` block contains:
      - `it.each(TIMBRE_OPTIONS)('%s: oscillator count equals preset.partials.length', (timbre) => { ... })` — uses `vi.spyOn(ac, 'createOscillator')` and asserts call count equals `TIMBRE_PRESETS[timbre].partials.length`.
      - `it.each(TIMBRE_OPTIONS)('%s: first oscillator frequency matches preset.fundamentalHzIn', (timbre) => { ... })` — captures oscillators via spy + asserts `oscillators[0].frequency.value === TIMBRE_PRESETS[timbre].fundamentalHzIn`.
      - `it.each(TIMBRE_OPTIONS)('%s: peak gain matches preset.peakGain', (timbre) => { ... })` — asserts `setValueAtTime` was called with `TIMBRE_PRESETS[timbre].peakGain`.
      - `it.each(TIMBRE_OPTIONS)('%s: decay time constant matches preset.decayTauIn', (timbre) => { ... })` — asserts `setTargetAtTime` was called with `TIMBRE_PRESETS[timbre].decayTauIn`.
    - NEW symmetric `describe('scheduleOutCueForTimbre (all timbres)', () => { ... })` block with the same 4 parameterized tests but for fundamentalHzOut and decayTauOut.
    - All new tests use the existing `createAc()` helper (line 11) — FakeAudioContext polyfill only, no real `AudioContext` (D-14 + vitest.setup.ts).
  </behavior>
  <action>
    Edit `src/audio/cueSynth.test.ts`. (1) Add three new imports at the top (after the existing `scheduleInCue/scheduleOutCue/scheduleTick` import block): `import { scheduleInCueForTimbre, scheduleOutCueForTimbre } from './cueSynth'`; `import { TIMBRE_OPTIONS } from '../domain/settings'`; `import { TIMBRE_PRESETS } from './timbres'`. (2) Do NOT touch any existing test — the existing Bowl-specific tests (3 partials, 440Hz, 0.18 peak, 1.4s decay, etc.) are the TIMBRE-02 byte-identical proof and MUST stay green unchanged. (3) After the last existing `describe` block (or at end of file), add a new `describe('scheduleInCueForTimbre (all timbres)', () => { ... })` block with the four `it.each(TIMBRE_OPTIONS)` parameterized tests described in `<behavior>`. (4) Add a symmetric `describe('scheduleOutCueForTimbre (all timbres)', () => { ... })` block. (5) Use the existing `createAc()` helper for all new tests. Do NOT add a new helper. Do NOT use real `AudioContext` — FakeAudioContext via the polyfill is the only allowed posture (D-14).
  </action>
  <verify>
    <automated>npm test -- src/audio/cueSynth.test.ts --run 2>&amp;1 | tail -20</automated>
    <automated>grep -q "scheduleInCueForTimbre (all timbres)" src/audio/cueSynth.test.ts &amp;&amp; echo "Per-timbre In describe block present"</automated>
    <automated>grep -q "scheduleOutCueForTimbre (all timbres)" src/audio/cueSynth.test.ts &amp;&amp; echo "Per-timbre Out describe block present"</automated>
    <automated>grep -c "it\.each(TIMBRE_OPTIONS)" src/audio/cueSynth.test.ts | awk '$1>=8 {print "At least 8 parameterized tests (4 In × 4 Out via .each)"}'</automated>
    <automated>! grep -E "new AudioContext\(\)" src/audio/cueSynth.test.ts | grep -v "createAc" &amp;&amp; echo "All AudioContext usage routed through createAc() helper (FakeAudioContext polyfill — D-14)"</automated>
    <automated>git diff src/audio/cueSynth.test.ts | grep -E "^- " | grep -v "^---" | wc -l | awk '$1==0 {print "Zero deletions — existing Bowl tests UNCHANGED (TIMBRE-02 byte-identical proof preserved)"}'</automated>
  </verify>
  <done>
    `src/audio/cueSynth.test.ts` passes green via `npm test -- src/audio/cueSynth.test.ts`; existing Bowl tests UNCHANGED (zero deletions in git diff); two new `describe` blocks add 8+ parameterized tests covering scheduleInCueForTimbre + scheduleOutCueForTimbre across all 4 timbres; all new tests use FakeAudioContext via `createAc()`.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Update `src/audio/audioEngine.test.ts` — pass `{ timbre: 'bowl' }` to all 20 existing createAudioEngine call sites + add 2-3 new timbre propagation tests</name>
  <read_first>
    src/audio/audioEngine.test.ts (full file — 395 lines; ~20 `createAudioEngine()` no-arg call sites need `{ timbre: 'bowl' }` added; ~`createAudioEngine({ onStateChange: ... })` sites need `timbre: 'bowl'` merged into the options),
    src/audio/audioEngine.ts (Task 2 output — `AudioEngineOptions.timbre: TimbreId` required field),
    src/audio/cueSynth.ts (for `vi.spyOn(cueSynth, 'scheduleInCueForTimbre')` import path — the dispatch functions to spy on),
    src/domain/settings.ts (TimbreId / TIMBRE_OPTIONS),
    .planning/phases/18-audio-timbres/18-PATTERNS.md (§"`src/audio/audioEngine.test.ts` (EXTENDED)" — new test pattern using spy on scheduleInCueForTimbre)
  </read_first>
  <behavior>
    - Every `createAudioEngine()` no-arg call site in the file (~20 occurrences) is updated to `createAudioEngine({ timbre: 'bowl' })`. Bowl explicit-pass guarantees these existing tests continue exercising the v1.0.1 Bowl path verbatim — TIMBRE-02 integration-layer proof.
    - Every `createAudioEngine({ onStateChange: ... })` site is updated to `createAudioEngine({ timbre: 'bowl', onStateChange: ... })`.
    - All existing assertions and existing test descriptions stay unchanged — only the call-site options-arg is widened.
    - NEW test: `createAudioEngine({ timbre: 'bell' }) — scheduleLeadIn forwards 'bell' to scheduleInCueForTimbre for the first In cue`. Uses `vi.spyOn(cueSynthModule, 'scheduleInCueForTimbre')` to verify the 4th argument is `'bell'` after calling `engine.scheduleLeadIn(0, samplePlan)`.
    - NEW test: `createAudioEngine({ timbre: 'sine' }) — scheduleNextCue forwards 'sine' to scheduleInCueForTimbre for in phase and scheduleOutCueForTimbre for out phase`. Uses two spies; calls `engine.scheduleNextCue({ newPhase: 'in', audioTime: 1.0, phaseDurationSec: 5 })` then `scheduleNextCue({ newPhase: 'out', ... })`; asserts each spy was called with `'sine'` as the timbre argument.
    - NEW test: `engine captures timbre once — subsequent createAudioEngine call with different timbre creates an independent engine; the first engine's scheduling continues to use its captured timbre`. (Asserts there's no shared mutable timbre state across engines.)
    - All new tests use the FakeAudioContext polyfill — no real audio (D-14).
  </behavior>
  <action>
    Edit `src/audio/audioEngine.test.ts`. (1) Run search-replace across the file: every `createAudioEngine()` → `createAudioEngine({ timbre: 'bowl' })`. Every `createAudioEngine({ onStateChange: ` → `createAudioEngine({ timbre: 'bowl', onStateChange: ` (preserve the trailing options content). Verify zero `createAudioEngine` call sites remain without an explicit `timbre` field. (2) Add an import block at the top (next to existing imports): `import * as cueSynthModule from './cueSynth'` (this enables `vi.spyOn(cueSynthModule, 'scheduleInCueForTimbre')`). (3) After the last existing test (or at the natural end of the existing `describe('audioEngine')`), add 3 new tests inside the same describe (or a nested `describe('Phase 18 timbre propagation')` block): test 1 — bell propagates to first In cue via scheduleLeadIn; test 2 — sine propagates to scheduleNextCue for both in and out phases; test 3 — engine-captured timbre is immutable (two engines with different timbres operate independently). All three tests follow the existing test posture (`await createAudioEngine({ timbre: '...' }); engine.scheduleLeadIn(...)` etc.; `await engine.close()` in teardown). (4) Do NOT add a fake plan or session — re-use whatever `samplePlan` or equivalent helper exists in the file (read the file to find it).
  </action>
  <verify>
    <automated>npm test -- src/audio/audioEngine.test.ts --run 2>&amp;1 | tail -20</automated>
    <automated>! grep -E "createAudioEngine\(\)" src/audio/audioEngine.test.ts &amp;&amp; echo "Zero no-arg createAudioEngine calls remain"</automated>
    <automated>grep -c "timbre: 'bowl'" src/audio/audioEngine.test.ts | awk '$1>=20 {print "All ~20 existing call sites updated with timbre: bowl"}'</automated>
    <automated>grep -q "timbre: 'bell'" src/audio/audioEngine.test.ts &amp;&amp; echo "Bell propagation test present"</automated>
    <automated>grep -q "timbre: 'sine'" src/audio/audioEngine.test.ts &amp;&amp; echo "Sine propagation test present"</automated>
    <automated>grep -q "scheduleInCueForTimbre" src/audio/audioEngine.test.ts &amp;&amp; echo "Spy on scheduleInCueForTimbre wired"</automated>
  </verify>
  <done>
    `src/audio/audioEngine.test.ts` passes green; zero no-arg `createAudioEngine()` calls remain (all ~20 carry explicit `timbre: 'bowl'`); 3 new Phase 18 propagation tests verify bell/sine forward through scheduleLeadIn + scheduleNextCue; engine-capture immutability test confirms independent engines.
  </done>
</task>

<task type="auto">
  <name>Task 5: Global green-gate and commit</name>
  <read_first>
    .planning/phases/18-audio-timbres/18-CONTEXT.md (D-13 per-commit green-gate; D-15 file-split invariant; D-16 picker contract)
  </read_first>
  <action>
    Run `npx tsc --noEmit && npm run lint && npm run build && npm test --run`. All four MUST exit 0. The full Vitest suite should pass (baseline after Plans 01+02 + the new tests in this plan — expected ~610+ tests). If anything red, fix in this plan. Verify NO touch to `src/components/`, `src/hooks/useAudioCues.ts`, `src/app/App.tsx`, `src/storage/`, `src/domain/` per D-15/D-16 file-split invariants. Stage the 4 files (`src/audio/cueSynth.ts`, `src/audio/cueSynth.test.ts`, `src/audio/audioEngine.ts`, `src/audio/audioEngine.test.ts`) and commit with message `feat(18-03): wire TIMBRE_PRESETS through cueSynth dispatch + audioEngine capture (TIMBRE-01/02/05)`. Single atomic commit per D-13.
  </action>
  <verify>
    <automated>npx tsc --noEmit &amp;&amp; npm run lint &amp;&amp; npm run build &amp;&amp; npm test --run 2>&amp;1 | tail -5</automated>
    <automated>git log --oneline -1 | grep -E "feat\(18-03\)" &amp;&amp; echo "Commit landed"</automated>
    <automated>git show --stat HEAD | grep -E "(cueSynth|audioEngine)\.(ts|test\.ts)" | wc -l | awk '$1==4 {print "Exactly 4 files in commit"}'</automated>
    <automated>! git show --stat HEAD | grep -E "(useAudioCues|App|TimbrePicker|prefs|settings)" &amp;&amp; echo "No out-of-scope file touches (D-15/D-16 preserved)"</automated>
  </verify>
  <done>
    Four gates green; one commit landed touching exactly cueSynth.ts + cueSynth.test.ts + audioEngine.ts + audioEngine.test.ts; existing 588+ baseline tests stay green (TIMBRE-02 byte-identical proof); per-timbre parameterized tests + propagation tests pass; no out-of-scope file touches.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller (useAudioCues) → audioEngine | `opts.timbre: TimbreId` is constrained at the TS type level — callers (useAudioCues in Plan 04, App.tsx in Plan 06) can only pass a valid `TimbreId` union member. `loadPrefs().timbre` is already coerced through `coerceTimbre` (Phase 14 D-10) to a valid `TimbreId`. |
| audioEngine → cueSynth | `sessionTimbre` (closed-over `TimbreId`) flows from engine into dispatch functions which look up `TIMBRE_PRESETS[timbre]`. The lookup is type-safe via `Readonly<Record<TimbreId, TimbrePreset>>` (Plan 01 — no undefined-access risk). |
| cueSynth → Web Audio API | All four presets construct sine oscillators with stack-bounded partial counts (max 4 in Chime). No user-supplied numeric values reach Web Audio — preset values are TypeScript constants. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-03-01 | Tampering | A future caller could attempt to construct an `AudioEngine` with a timbre value outside `TimbreId` | mitigate | TypeScript `strictTypeChecked` rejects at compile time. Phase 14 `coerceTimbre` further guarantees runtime values from storage are always valid `TimbreId`. |
| T-18-03-02 | Tampering | `TIMBRE_PRESETS` access via `TIMBRE_PRESETS[timbre]` could be subject to prototype pollution if `timbre` were attacker-controlled | accept | `timbre` is a TS-typed `TimbreId` union member; Phase 14 `coercePrefs` already filters out prototype-pollution attempts at storage read (tested in `prefs.test.ts`). |
| T-18-03-03 | Denial of Service | Per-timbre partial count differences could exceed expected oscillator allocation budget | mitigate | Max partials per preset is 4 (Chime); fundamental oscillator count per cue ≤4 oscillators; v1.0.1 Bowl path already used 3. No new oscillator-budget surface. Web Audio's `OscillatorNode` count is bounded by browser limits (~hundreds), well above Phase 18 usage. |
| T-18-03-04 | Information Disclosure | Audio fingerprinting via per-user timbre choice | accept | Audio is rendered to local speaker only; no fingerprintable surface exposed to scripts on the page. Phase 14 `prefs.timbre` is stored in `localStorage` (same-origin; already part of v1.0.1 storage surface). |
| T-18-03-05 | Repudiation | n/a — local-only | accept | |
| T-18-03-06 | Elevation of Privilege | n/a — Web Audio is sandboxed by the browser | accept | |
| T-18-03-07 | D-07 lead-in tick contract regression | scheduleTick body could be accidentally edited and break perceptual lead-in distinction | mitigate | Task 1 verify step asserts 3 scheduleTick calls remain in audioEngine.ts; existing cueSynth.test.ts scheduleTick tests run unchanged; D-07 hard invariant. |
| T-18-03-08 | TIMBRE-02 byte-identical Bowl regression | A drift in scheduleBowlCue body could break the v1.0.1 Bowl path | mitigate | Existing Bowl tests in cueSynth.test.ts (lines 17-100+) stay UNCHANGED and green (Task 3 zero-deletions check). All 20 existing audioEngine.test.ts call sites pass `timbre: 'bowl'` and continue exercising the Bowl path. |
| T-18-03-09 | Supply chain | New npm dependency could enter via these edits | mitigate | D-14 zero-deps; only imports added are intra-project (timbres.ts, settings.ts). package.json untouched. |
</threat_model>

<verification>
- `npx tsc --noEmit && npm run lint && npm run build && npm test --run` exit 0.
- Existing cueSynth.test.ts Bowl tests pass unchanged (TIMBRE-02 layer-1 proof).
- 8+ new it.each(TIMBRE_OPTIONS) parameterized tests pass (per-timbre oscillator count, fundamental frequency, peak gain, decay tau).
- 3 new audioEngine.test.ts propagation tests pass (bell forwards to scheduleInCueForTimbre, sine forwards to both in/out via scheduleNextCue, engine-capture immutability).
- All 20 existing createAudioEngine call sites pass `timbre: 'bowl'` and stay green (TIMBRE-02 layer-2 proof).
- No edits to files outside `src/audio/` (D-15 + D-16 file-split preserved).
</verification>

<success_criteria>
- [ ] `scheduleBowlCue` accepts `TimbrePreset` + `kind: 'in' | 'out'` parameters (replaces loose fundamentalHz + defaultDecayTau).
- [ ] `scheduleInCueForTimbre` and `scheduleOutCueForTimbre` exported from `cueSynth.ts`.
- [ ] `scheduleInCue` / `scheduleOutCue` public signatures PRESERVED as Bowl-only thin wrappers (D-01 option (a)).
- [ ] `scheduleTick` body UNCHANGED (D-07).
- [ ] Sustain-floor module-level constants PRESERVED (D-12).
- [ ] 8 Bowl module constants DELETED (values moved to `TIMBRE_PRESETS.bowl` — Plan 01).
- [ ] `AudioEngineOptions.timbre: TimbreId` is REQUIRED; `createAudioEngine(opts)` captures `sessionTimbre` once; no `setTimbre` setter exists.
- [ ] `scheduleLeadIn` forwards `sessionTimbre` to `scheduleInCueForTimbre` for the first In cue; 3 `scheduleTick` calls UNCHANGED.
- [ ] `scheduleNextCue` ternary forwards `sessionTimbre` to both `scheduleInCueForTimbre` and `scheduleOutCueForTimbre`.
- [ ] All 20 existing `createAudioEngine()` no-arg calls in `audioEngine.test.ts` updated to `{ timbre: 'bowl' }`.
- [ ] All existing cueSynth.test.ts Bowl tests UNCHANGED and green (TIMBRE-02 layer-1 proof).
- [ ] 8+ new per-timbre `it.each(TIMBRE_OPTIONS)` tests pass for scheduleInCueForTimbre + scheduleOutCueForTimbre.
- [ ] 3 new audioEngine.test.ts Phase 18 propagation tests pass.
- [ ] `tsc && lint && build && test` exit 0 (D-13).
- [ ] No touches to files outside `src/audio/` (D-15/D-16 preserved).
- [ ] Single commit on green: `feat(18-03): wire TIMBRE_PRESETS through cueSynth dispatch + audioEngine capture (TIMBRE-01/02/05)`.
</success_criteria>

<output>
After completion, create `.planning/phases/18-audio-timbres/18-03-SUMMARY.md` capturing:
- Files modified: `src/audio/cueSynth.ts`, `src/audio/cueSynth.test.ts`, `src/audio/audioEngine.ts`, `src/audio/audioEngine.test.ts`.
- TIMBRE-02 byte-identical Bowl proof: paste the git diff line-count summary showing 8 module-constant deletions in cueSynth.ts that mirror exact values in `TIMBRE_PRESETS.bowl` (Plan 01).
- New dispatch surface: list `scheduleInCueForTimbre` / `scheduleOutCueForTimbre` exports.
- Test count delta.
- Commit hash.
- Forward-decl: Plan 04 will add `timbreRef` + `start(plan, timbre)` to `useAudioCues.ts`; Plan 06 (App) will read `loadPrefs().timbre` inside `onStartClick`.
</output>
