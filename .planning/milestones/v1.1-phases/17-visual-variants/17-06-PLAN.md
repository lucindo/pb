---
phase: 17-visual-variants
plan: 06
type: execute
wave: 5
depends_on:
  - 17-05
files_modified:
  - src/app/App.tsx
  - src/app/App.session.test.tsx
  - src/components/SettingsDialog.test.tsx
  - .planning/REQUIREMENTS.md
  - .planning/STATE.md
  - .planning/ROADMAP.md
autonomous: false
requirements:
  - VARIANT-01
  - VARIANT-02
  - VARIANT-03
  - VARIANT-04
  - VARIANT-05
  - VARIANT-06
  - VARIANT-07
user_setup: []
tags:
  - app-integration
  - phase-close
  - phase-17

must_haves:
  truths:
    - "App.tsx invokes `useVisualVariant()` once near the existing `useTheme()` invocation and destructures `liveVariant` from its return"
    - "App.tsx declares `sessionVariantRef = useRef<VisualVariantId | null>(null)` near the existing session refs (startGenerationRef + planRef cluster) — D-09 belt-and-suspenders snapshot"
    - "Inside `onStartClick`, the snapshot `sessionVariantRef.current = liveVariant` is written BEFORE `setAppPhase('lead-in')` so the variant is frozen for the entire session (lead-in + breath loop) — D-10"
    - "Inside the cancel-during-lead-in branch of `onStartClick`, `sessionVariantRef.current = null` is cleared alongside `audioAnchorRef.current = null` and `planRef.current = null`"
    - "Inside the leave-running cleanup effect (the one at lines 462-518 area that resets appPhase to 'idle' and nulls audioAnchorRef + planRef), `sessionVariantRef.current = null` is cleared on session end — D-10"
    - "`onAudioReanchorRequired` callback does NOT touch `sessionVariantRef` — audio reconstruction is orthogonal to variant capture (D-11)"
    - "BreathingShape JSX prop site changes from `<BreathingShape frame=... leadInDigit=... />` to `<BreathingShape variant={sessionVariantRef.current ?? liveVariant} frame=... leadInDigit=... />` — uses the captured frozen value during active session, falls back to `liveVariant` at idle (D-09 nullable ref pattern)"
    - "D-12: BreathingShape stays `null` at idle — the dispatcher's `if (frame === null && leadInDigit == null) return null` guard (Plan 05) ensures no live variant preview is rendered before Start; the picker's inline shape swatches are the SOLE preview surface and the App.tsx wire-up does NOT introduce any idle-render path"
    - "VARIANT-03: A new integration test in App.session.test.tsx confirms: (a) starting a session captures the current variant from prefs into the rendered shape, (b) writing a different variant to localStorage during the session does NOT change the rendered shape mid-session, (c) ending the session and starting a new one picks up the new (post-Start) prefs variant"
    - "VARIANT-02 zero-regression: existing App.session.test.tsx scenarios continue to pass — the default Orb behavior for users who never open the picker is unchanged because Phase 14 D-05 `DEFAULT_VARIANT='orb'` flows through `loadPrefs()` → `useVisualVariant().variant` → `liveVariant` → snapshot → BreathingShape"
    - "SettingsDialog.test.tsx existing `inSessionView` disabled-gating coverage continues to pass for the new VariantPicker body (the picker is mounted at SettingsDialog.tsx:83 unchanged from Phase 15)"
    - "REQUIREMENTS.md: VARIANT-01..07 traceability rows flipped from `Pending` to `Done` after phase close"
    - "ROADMAP.md: Phase 17 entry marked complete (Plans: 6/6 Complete; date stamp) with phase-summary checkpoint pointer"
    - "STATE.md: progress counters updated (`completed_phases: 7`, `completed_plans: 28`, `percent: 90`-ish); last_activity reflects Phase 17 close"
    - "D-18: Operator UAT explicitly validates 'zero net-new runtime deps' invariant — `git diff --stat package.json package-lock.json` between pre-Phase-17 and phase-close shows zero added runtime dependencies (Square + Ring shipped as pure CSS + minimal divs + inline SVG; useVisualVariant + useVariantChoice are pure React; VariantPicker swatches are inline `.orb-layer--in` spans + 1 inline `<svg>` — no `react-spring`, `framer-motion`, `react-svg`, or shape-rendering helpers were introduced)"
    - "D-24: Operator UAT validates VARIANT-06 a11y floor at phase close — keyboard tab through SettingsDialog reaches every Variant option button, focus-visible ring is visible on each, Space/Enter activates focused option, and each option button's hit area feels at least 44×44 px (no precision-finger requirement); a11y posture mirrors ThemePicker per D-24 carry-forward"
    - "`tsc && lint && build && test` exits 0 at each commit"
    - "Operator UAT checkpoint at the end of Task 1: operator confirms each of 3 variants renders correctly across 5 palettes, reduced-motion fixed-mid + crossfade works per variant, picker swatches are legible at 24px, cross-tab variant change does not swap mid-session"
  artifacts:
    - path: "src/app/App.tsx"
      provides: "Final integration — useVisualVariant invocation, sessionVariantRef declaration + 3 write sites (snapshot at startSession, clear at cancel-during-lead-in, clear at leave-running cleanup effect), BreathingShape variant prop wired to `sessionVariantRef.current ?? liveVariant`"
      contains: "useVisualVariant"
    - path: "src/app/App.session.test.tsx"
      provides: "Extended test suite covering VARIANT-03 capture-at-start mechanism: snapshot-at-Start, no-mid-session-swap (localStorage simulation), next-session picks up new variant"
      contains: "sessionVariantRef\\|capture-at-start\\|VARIANT-03"
    - path: ".planning/REQUIREMENTS.md"
      provides: "VARIANT-01..07 traceability rows updated from Pending → Done; the per-requirement [ ] → [x] checkboxes flipped in the Visual Variants (CUST-03) section"
      contains: "VARIANT-01"
    - path: ".planning/STATE.md"
      provides: "Updated progress counters + last_activity + position; Phase 17 marked complete"
      contains: "Phase 17"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 17 entry marked complete with date stamp + Plans count update + checkpoint pointer to 17-SUMMARY.md"
      contains: "17. Visual Variants"
    - path: ".planning/phases/17-visual-variants/17-SUMMARY.md"
      provides: "Phase summary document covering all 6 plans + key links + carry-forward updates + per-palette UAT result"
      contains: "Phase 17"
  key_links:
    - from: "src/app/App.tsx (onStartClick handler)"
      to: "src/hooks/useVisualVariant.ts (liveVariant)"
      via: "snapshot at session start, before lead-in begins"
      pattern: "sessionVariantRef\\.current = liveVariant"
    - from: "src/app/App.tsx (BreathingShape mount site)"
      to: "src/components/BreathingShape.tsx (variant prop)"
      via: "JSX prop with nullable-ref fallback to liveVariant"
      pattern: "variant=\\{sessionVariantRef\\.current \\?\\? liveVariant\\}"
    - from: "src/app/App.tsx (leave-running cleanup effect)"
      to: "src/app/App.tsx (sessionVariantRef.current)"
      via: "null write on session end alongside audioAnchorRef + planRef"
      pattern: "sessionVariantRef\\.current = null"
---

<objective>
Close Phase 17 by:

1. **Wiring `useVisualVariant` + `sessionVariantRef` into App.tsx** with the capture-at-session-start mechanism (D-09/D-10/D-11). After this task, the user-facing variant selection path is fully connected: VariantPicker → savePrefs + CustomEvent → useVisualVariant → liveVariant → snapshot at Start → BreathingShape → OrbShape/SquareShape/RingShape.

2. **Extending App.session.test.tsx** with VARIANT-03 capture-at-start coverage (3 new integration tests).

3. **Re-verifying SettingsDialog.test.tsx** continues to pass with the new VariantPicker body (Phase 15 disabled-gating coverage). No new tests are added here — existing tests assert generic picker disabled-state behavior which the new radiogroup body satisfies.

4. **Operator UAT checkpoint** — single human-verify gate where the operator runs the dev server, exercises each variant under each of 5 palettes + reduced-motion + cross-tab scenarios per the Plan 17 VALIDATION.md "Manual-Only Verifications" table. This is the SOLE checkpoint in the phase (UAT for the full picker + capture-at-start mechanism + per-variant reduced-motion + per-palette aesthetic).

5. **Phase close** — flip VARIANT-01..07 to Done in REQUIREMENTS.md; update ROADMAP.md Phase 17 entry to Complete with date stamp; update STATE.md progress counters; create 17-SUMMARY.md.

Per D-19/D-20: SettingsDialog.tsx, settings.ts, prefs.ts unchanged (Phase 14 + Phase 15 invariants).
Per D-21: VariantPicker prop contract is preserved (only `{ disabled }`).
Per D-17 per-commit green-gate: each task ends with `tsc && lint && build && test` exit 0.
</objective>

<execution_context>
@$HOME/.claude-personal-personal/get-shit-done/workflows/execute-plan.md
@$HOME/.claude-personal-personal/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/17-visual-variants/17-CONTEXT.md
@.planning/phases/17-visual-variants/17-PATTERNS.md
@.planning/phases/17-visual-variants/17-UI-SPEC.md
@.planning/phases/17-visual-variants/17-VALIDATION.md
@src/app/App.tsx
@src/app/App.session.test.tsx
@src/components/SettingsDialog.tsx
@src/components/SettingsDialog.test.tsx
@src/components/BreathingShape.tsx
@src/components/VariantPicker.tsx
@src/hooks/useVisualVariant.ts
@src/domain/settings.ts

<interfaces>
<!-- Post-Plan-05 state of App.tsx — additive edit sites: -->

src/app/App.tsx (current line numbers — anchors for Plan 06 edits):
  - Line 16: `import { useTheme } from '../hooks/useTheme'`. ADD next to this: `import { useVisualVariant } from '../hooks/useVisualVariant'`.
  - Line 37: `import type { SessionSettings } from '../domain/settings'`. EXTEND: `import type { SessionSettings, VisualVariantId } from '../domain/settings'`.
  - Line 139: `useTheme()` invocation. ADD on the next line: `const { variant: liveVariant } = useVisualVariant()`.
  - Lines 164–169 cluster (`startGenerationRef = useRef(0)`, `leadInTimeoutsRef = useRef<number[]>([])`, `planRef = useRef<...>(null)`). ADD a new useRef: `const sessionVariantRef = useRef<VisualVariantId | null>(null)` (RESEARCH §"App.tsx Snapshot Mechanics" recommends "near line 164-169").
  - Line 294 area (inside `onStartClick`, inside the `if (appPhase === 'lead-in')` cancel branch): currently nulls `audioAnchorRef.current` and `planRef.current`. ADD `sessionVariantRef.current = null` alongside.
  - Line 313 area (inside `onStartClick`, after the `appPhase !== 'idle'` guard, AT the `const generation = ++startGenerationRef.current` line): ADD `sessionVariantRef.current = liveVariant` IMMEDIATELY BEFORE `setAppPhase('lead-in')` at line 315.
  - Lines 475–477 (leave-running cleanup effect, where `audioAnchorRef.current = null` + `planRef.current = null` + `lastBoundaryKeyRef.current = null` are set): ADD `sessionVariantRef.current = null` alongside.
  - Lines 610–613 (BreathingShape JSX mount site): change from `<BreathingShape frame=... leadInDigit=... />` to `<BreathingShape variant={sessionVariantRef.current ?? liveVariant} frame=... leadInDigit=... />`. **NOTE:** Per Plan 05, BreathingShape's `variant` prop is OPTIONAL with default `'orb'`. Plan 06's edit makes the variant flow EXPLICIT (no longer relying on the default — the wired value is now passed). The optional-prop signature stays as-is in BreathingShape.tsx; tightening to required can be considered post-Phase-17 ergonomic cleanup if desired, but is NOT in scope for this plan.

src/app/App.session.test.tsx (existing patterns to mirror for new VARIANT-03 tests):
  - The existing `startAndAdvancePastLeadIn` helper (~line 50-80 area) drives a session through lead-in to the running state.
  - Existing tests use `screen.getByRole('img', { name: 'Breathing shape: In' })` to grab the shape — preserves this pattern for the new variant tests.
  - The `[aria-hidden="true"].shape-marker--outer / --inner` selectors (post-Plan-01) are available for variant-aware assertions if needed; the simpler approach is to read `data-variant` directly from `[role="img"]`.

src/components/SettingsDialog.test.tsx (no edits needed in Plan 06):
  - Existing tests at SettingsDialog.test.tsx already mount the dialog with `inSessionView={true/false}` and assert that each picker (Theme/Variant/Timbre/Language) honors the disabled state. The new VariantPicker body (Plan 05) preserves the Phase 15 D-02 `{ disabled }` prop contract — so existing dialog-level tests continue to pass. Plan 06 only verifies, does not edit.

REQUIREMENTS.md sections to update at phase close:
  - `### Visual Variants (CUST-03)` block (lines ~46-53) — flip `[ ]` → `[x]` on VARIANT-01..07.
  - Traceability table (lines ~119-125) — flip `| VARIANT-01 | Phase 17 | Pending |` → `| VARIANT-01 | Phase 17 | Done |` (and same for VARIANT-02..07).

ROADMAP.md sections to update:
  - `### Phase 17: Visual Variants` block (lines ~181-192) — update `**Plans**:` count from "TBD" to "6 plans" with the list of plan titles + commit pointers.
  - The `📋 v1.1 Customization (planned)` checkbox at line ~54 — change `[ ] **Phase 17: Visual Variants**` → `[x] **Phase 17: Visual Variants**` with completion date.
  - The Progress table at the bottom — update Phase 17 row from `0/?` `Not started` to `N/N` `Complete` with the date.

STATE.md sections to update:
  - `progress.completed_phases`: increment from 6 to 7.
  - `progress.completed_plans`: increment from 22 to 28 (+ 6 plans from Phase 17).
  - `progress.percent`: increment ~85 → ~90.
  - `last_updated`: bump to phase-close date.
  - `last_activity`: replace with Phase 17 close note.
  - `## Current Position`: bump Phase + Status (Phase 18 / Phase 19 not started; Phase 17 complete).
  - `## Performance Metrics → By Phase (v1.1)` table — add a row for Phase 17 with plans count + Complete status + date.
  - `### Roadmap Evolution`: add a Phase 17 close bullet.
  - `### Pending Todos`: remove the "Phase 17 planning" bullet; add "Phase 18 planning" + "Phase 19 planning" if not already present.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wire useVisualVariant + sessionVariantRef into App.tsx (3 ref-write sites + BreathingShape variant prop); extend App.session.test.tsx with VARIANT-03 capture-at-start coverage</name>
  <files>src/app/App.tsx, src/app/App.session.test.tsx</files>
  <behavior>
    - Test 1 (NEW): "captures variant at session start — Square selected before Start, shape renders with data-variant='square' during the running session". Seed prefs with `variant: 'square'` BEFORE App renders; click Start; advance past lead-in; assert `screen.getByRole('img').getAttribute('data-variant') === 'square'`.
    - Test 2 (NEW): "mid-session localStorage change does NOT swap the rendered variant". Seed `variant: 'orb'`; render App; start session; advance past lead-in; simulate a cross-tab `localStorage.setItem(STATE_KEY, JSON.stringify({...envelope, prefs: {...prefs, variant: 'ring'}}))` + fire `new StorageEvent('storage', { key: STATE_KEY })`; assert the rendered shape STILL has `data-variant='orb'` (sessionVariantRef snapshot wins per D-09/D-10).
    - Test 3 (NEW): "next session picks up post-Start prefs variant". After Test 2's mid-session localStorage write to `'ring'`, end the session via the End button; click Start again; advance past lead-in; assert the rendered shape NOW has `data-variant='ring'` (sessionVariantRef cleared on session end → next Start re-reads liveVariant).
    - Test 4 (NEW): "VARIANT-02 zero-regression — Orb is the rendered variant when prefs.variant is the DEFAULT_VARIANT 'orb' (or absent from localStorage entirely)". With no localStorage seed (fresh load), start a session; advance past lead-in; assert `data-variant='orb'`.
    - Test 5 (NEW): "lead-in countdown is rendered by the captured variant — selecting Square before Start makes the 3-2-1 digit render inside SquareLeadIn". Seed `variant: 'square'`; click Start (lead-in begins); BEFORE advancing past lead-in, assert `screen.getByRole('img', { name: 'Lead-in: 3' }).getAttribute('data-variant') === 'square'`.
    - All EXISTING tests in App.session.test.tsx continue to pass unchanged — VARIANT-02 zero-regression.
  </behavior>
  <read_first>
    - src/app/App.tsx (full file — focus on lines 16, 37, 139, 164-177 cluster, 292-361 onStartClick, 462-518 leave-running cleanup, 610-613 BreathingShape mount)
    - src/app/App.session.test.tsx (full file — `startAndAdvancePastLeadIn` helper + existing test scaffolding patterns)
    - src/hooks/useVisualVariant.ts (Plan 04 — hook contract)
    - src/components/BreathingShape.tsx (Plan 05 — optional `variant?` prop with default `'orb'`)
    - src/domain/settings.ts (VARIANT_OPTIONS, VisualVariantId, DEFAULT_VARIANT)
    - src/storage/prefs.ts (loadPrefs, savePrefs, UserPrefs shape)
    - .planning/phases/17-visual-variants/17-PATTERNS.md §"`src/app/App.tsx` (EDIT — hook invocation + sessionVariantRef + BreathingShape prop)"
    - .planning/phases/17-visual-variants/17-CONTEXT.md §"Implementation Decisions" D-09 (sessionVariantRef snapshot), D-10 (snapshot site inside startSession), D-11 (audio reconstruction does NOT re-snapshot), D-22 (CustomEvent reuse)
  </read_first>
  <action>
    1. Edit `src/app/App.tsx`:

       a. **Import additions** (next to existing imports):
          - Add to existing `import type { SessionSettings } from '../domain/settings'`: extend to `import type { SessionSettings, VisualVariantId } from '../domain/settings'`.
          - Add new line after `import { useTheme } from '../hooks/useTheme'`: `import { useVisualVariant } from '../hooks/useVisualVariant'`.

       b. **Hook invocation** (next to existing `useTheme()` at line 139):
          ```
          useTheme() // Phase 16 THEME-01..04: ...existing comment...
          const { variant: liveVariant } = useVisualVariant() // Phase 17 VARIANT-01..07: live state + cross-tab/same-tab sync (no global attribute write — D-16)
          ```

       c. **sessionVariantRef declaration** (near the existing session-ref cluster at lines 164-169):
          ```
          // Phase 17 D-09: captured-at-Start snapshot. While non-null (during an active session
          // including lead-in), BreathingShape receives this frozen value and ignores liveVariant.
          // Belt-and-suspenders against a cross-tab storage event that the picker-disable cannot block (D-10).
          // D-11: audio reconstruction (Phase 5.1 / Phase 9) does NOT re-snapshot — orthogonal subsystems.
          const sessionVariantRef = useRef<VisualVariantId | null>(null)
          ```

       d. **Snapshot write inside `onStartClick`** — inject BEFORE `setAppPhase('lead-in')` at line 315, immediately AFTER `const generation = ++startGenerationRef.current` at line 313:
          ```
          const generation = ++startGenerationRef.current

          // Phase 17 D-10: capture-at-session-start — set BEFORE lead-in begins so the visual variant
          // is frozen for the entire session (lead-in + breath loop). Reset in the leave-running cleanup
          // effect below. D-11: audio reconstruction does NOT re-snapshot.
          sessionVariantRef.current = liveVariant

          setAppPhase('lead-in')
          ```

       e. **Snapshot clear inside the cancel-during-lead-in branch** — inside the `if (appPhase === 'lead-in')` block at lines 294-307, add `sessionVariantRef.current = null` alongside the existing `audioAnchorRef.current = null` + `planRef.current = null`:
          ```
          if (appPhase === 'lead-in') {
            startGenerationRef.current += 1
            clearLeadInTimeouts()
            setLeadInDigit(null)
            setAppPhase('idle')
            audioAnchorRef.current = null
            planRef.current = null
            sessionVariantRef.current = null  // Phase 17 D-10 tidy clear (redundant with leave-running effect but avoids 1 frame of stale ref)
            void audioStop()
            void wakeLockRelease()
            return
          }
          ```

       f. **Snapshot clear in leave-running cleanup effect** — inside the `if (state.status !== 'running')` block at lines 462-518, add `sessionVariantRef.current = null` alongside `audioAnchorRef.current = null` + `planRef.current = null` + `lastBoundaryKeyRef.current = null` (around line 475-477):
          ```
          audioAnchorRef.current = null
          planRef.current = null
          sessionVariantRef.current = null  // Phase 17 D-10 release the captured variant so next Start re-reads liveVariant
          lastBoundaryKeyRef.current = null
          ```

       g. **BreathingShape JSX mount** — change line 610-613 to thread the `variant` prop:
          ```
          <BreathingShape
            variant={sessionVariantRef.current ?? liveVariant}
            frame={appPhase === 'running' ? session.liveFrame : null}
            leadInDigit={appPhase === 'lead-in' ? leadInDigit : null}
          />
          ```

       h. **D-11 verification — do NOT edit `onAudioReanchorRequired`** (the callback at lines 129-136 that writes `audioAnchorRef.current` on audio reconstruction). It must NOT touch `sessionVariantRef`. RESEARCH §"Reconstruction non-interaction (D-11)" — variant ref stays orthogonal to audio reconstruction.

    2. Edit `src/app/App.session.test.tsx`:

       a. Add helper or use existing infrastructure to seed `localStorage[STATE_KEY]` with a chosen variant before App renders:
          ```typescript
          function seedVariant(variant: VisualVariantId): void {
            const envelope = {
              version: 1,
              prefs: { theme: 'system', timbre: 'bowl', variant, locale: 'en' },
            }
            window.localStorage.setItem(STATE_KEY, JSON.stringify(envelope))
          }
          ```
          (Import `STATE_KEY` from `'../storage'` and `VisualVariantId` from `'../domain/settings'` if not already imported.)

       b. Add a new `describe('VARIANT-03 capture-at-session-start', () => { ... })` block with the 5 new tests per the `<behavior>` list above. Use `startAndAdvancePastLeadIn` for the running-state tests; use the existing fake-timer + click-simulator patterns established in the file.

       c. For Test 2's cross-tab simulation:
          ```typescript
          act(() => {
            window.localStorage.setItem(STATE_KEY, JSON.stringify({ ..., prefs: { ..., variant: 'ring' } }))
            window.dispatchEvent(new StorageEvent('storage', { key: STATE_KEY, newValue: '...' }))
          })
          ```
          The `useVisualVariant` cross-tab listener will fire, but `sessionVariantRef.current` is non-null during the session, so `<BreathingShape variant={sessionVariantRef.current ?? liveVariant} />` continues to receive the captured value.

       d. For Test 3's end-and-restart sequence: click End (via the existing End-session test pattern); seed the new variant; click Start; advance past lead-in; assert the new variant is rendered.

       e. Do NOT delete or modify any existing test in App.session.test.tsx — VARIANT-02 zero-regression.

    Per D-17 per-commit green-gate: App.tsx + App.session.test.tsx land together so the new tests cover the new behavior. `tsc && lint && build && test` must exit 0 at this commit boundary.
  </action>
  <verify>
    <automated>npm test -- --run App.session &amp;&amp; npx tsc --noEmit &amp;&amp; npm run lint &amp;&amp; npm run build &amp;&amp; npm test -- --run</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "import \\{ useVisualVariant \\}" src/app/App.tsx` returns exactly 1.
    - `grep -c "VisualVariantId" src/app/App.tsx` returns at least 2 (type import + useRef type parameter).
    - `grep -c "useVisualVariant()" src/app/App.tsx` returns exactly 1.
    - `grep -c "sessionVariantRef" src/app/App.tsx` returns at least 5 (1 declaration + 1 snapshot write in onStartClick + 1 clear in cancel-during-lead-in + 1 clear in leave-running cleanup + 1 use in BreathingShape JSX prop).
    - `grep -c "sessionVariantRef.current = liveVariant" src/app/App.tsx` returns exactly 1 (the single snapshot site per D-10).
    - `grep -c "sessionVariantRef.current = null" src/app/App.tsx` returns exactly 2 (cancel-during-lead-in clear + leave-running cleanup clear).
    - `grep -c "variant=\\{sessionVariantRef.current \\?\\? liveVariant\\}" src/app/App.tsx` returns exactly 1 (BreathingShape JSX prop).
    - `grep -c "sessionVariantRef" src/app/App.tsx | grep onAudioReanchorRequired` returns 0 (D-11 — reconstruction does NOT touch variant ref); double-check by reading `onAudioReanchorRequired` callback body at lines 129-136 and confirming no sessionVariantRef references.
    - `grep -cE "VARIANT-03|capture-at-session-start|sessionVariantRef" src/app/App.session.test.tsx` returns at least 5 (new describe block + 5 test cases).
    - `npm test -- --run App.session` exits 0 (all existing + 5 new tests pass).
    - `npx tsc --noEmit && npm run lint && npm run build && npm test -- --run` exits 0 at commit boundary.
  </acceptance_criteria>
  <done>
    App.tsx is fully wired: useVisualVariant invocation + sessionVariantRef declaration + snapshot at Start + clear at cancel-during-lead-in + clear at leave-running cleanup + BreathingShape variant prop with nullable-ref fallback. App.session.test.tsx adds 5 VARIANT-03 capture-at-start tests; all existing tests still pass.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Operator UAT — variant rendering, picker UX, per-palette aesthetic, reduced-motion, cross-tab capture-at-Start</name>
  <what-built>
    Plans 01–05 + Plan 06 Task 1 deliver:
    - `.shape-marker--{outer,inner}` rename across CSS + 3 affected files (theme.css + BreathingShape.tsx + BreathingShape.test.tsx + App.session.test.tsx).
    - OrbShape extraction from BreathingShape with `data-variant='orb'` attribute on Body + LeadIn roots.
    - SquareShape (rounded-square 18% geometry) + RingShape (annulus via radial-gradient hollow center at 35% inner stop) — both reusing OrbShape's kinematics + reduced-motion contract.
    - 6 new CSS rules in theme.css covering `[data-variant='square'] .shape-marker--{outer,inner} { border-radius: 18% }` + `[data-variant='ring'] .shape-marker--{outer,inner} { border-width: 1px }` + `[data-variant='ring'] .orb-layer--{in,out}` radial-gradient overrides.
    - useVisualVariant + useVariantChoice hooks mirroring the Phase 16 useTheme/useThemeChoice pattern.
    - BreathingShape dispatcher with required-then-optional `variant?` prop defaulting to `'orb'`; runtime fallback to Orb on unknown values.
    - VariantPicker radiogroup body with inline shape swatches (Option A CSS-only mini renders for Orb + Square; Option B SVG circle stroke for Ring per UI-SPEC Pitfall 8).
    - App.tsx integration: sessionVariantRef snapshot at Start, clear on session end (D-10), BreathingShape variant prop wired to `sessionVariantRef.current ?? liveVariant`.
    - All automated tests green: 5-palette THEME-05 contrast guard, THEME-UI-01 hardcoded-class guard, VARIANT-01..07 unit + integration coverage.
  </what-built>
  <how-to-verify>
    Run `npm run dev` and exercise the following matrix manually (mirrors VALIDATION.md "Manual-Only Verifications" table):

    **Pre-check:**
    1. Open the app in a desktop browser. Navigate to the idle/stopped view.
    2. Click the gear icon → SettingsDialog opens. Confirm the Variant picker shows 3 buttons: Orb (selected by default for fresh state), Square, Ring.
    3. Confirm each option button shows a small inline swatch above the label: Orb = filled circle, Square = filled rounded-square, Ring = circular outline (SVG).

    **Variant rendering smoothness at 6 BPM (VARIANT-01):**
    4. Set BPM to 6 BPM. Select each variant in turn (Orb / Square / Ring). For each: close the dialog, start a 30-second session, observe the breathing shape interpolation.
       - **Orb:** smooth scale 0.58 → 1.0 interpolation; inner reference ring visible at MIN_SCALE boundary on Out; outer ring at MAX_SCALE on In.
       - **Square:** same kinematic envelope as Orb but the body is a rounded-square (18%); markers are rounded-corner outlines matching the body.
       - **Ring:** same kinematic envelope; body is an annulus (hollow center via radial-gradient); markers are concentric thin circular outlines (1px stroke).
    5. **Picker disable during session (VARIANT-03):** While a session is running, open the gear → SettingsDialog → confirm the Variant picker shows the disabled visual (opacity ≈ 0.45, cursor-not-allowed). Confirm clicks do NOT change the active variant.

    **Per-palette contrast on Square + Ring (VARIANT-04 token reuse via D-13):**
    6. For each of the 5 palettes (Light / Dark / Moss / Slate / Dusk):
       a. Switch the theme via ThemePicker.
       b. Test each variant (Orb / Square / Ring).
       c. Confirm: in-text label readable, out-text label readable, boundary markers visible against the body gradient, no clipping or invisible markers.

    **Reduced-motion fallback per variant (VARIANT-04):**
    7. Enable OS-level reduced-motion (macOS: System Settings → Accessibility → Display → Reduce motion).
    8. For each variant (Orb / Square / Ring): start a session; confirm the body sits at MID_SCALE = 0.79 (no scale animation); confirm the `.orb-layer--in/--out` opacity crossfade reads cleanly between In and Out phases; confirm the inner reference marker is hidden (Phase 13 D-03 `.shape-marker--inner { display: none }` rule under reduced-motion); confirm the outer marker is still visible.

    **Lead-in countdown per variant (VARIANT-05):**
    9. For each variant: click Start; observe the 3-2-1 countdown digit renders inside the variant body at the larger `text-7xl/8xl` size; the body sits at MID_SCALE during lead-in regardless of OS reduced-motion preference (Phase 3 D-14 — lead-in is constant by design).

    **Picker hit area + focus-visible (VARIANT-06 + D-24 a11y floor validation):**
    10. Tab through the SettingsDialog with keyboard only. Confirm: focus ring is visible on each variant option button when reached via Tab; Space/Enter activates the focused option; the button hit area feels at least 44×44 px (no precision-finger requirement) — this is the D-24 carry-forward a11y check; if ANY option fails to meet 44×44 or to render focus-visible, flag for remediation BEFORE phase close (cannot ship VARIANT-06 with a regressed a11y floor).

    **Persistence across reload (VARIANT-07):**
    11. Select Square. Reload the page. Open SettingsDialog. Confirm Square is still the selected option. Start a session. Confirm Square is the rendered variant. Repeat for Ring.

    **Cross-tab variant change does not swap mid-session (VARIANT-03 cross-tab):**
    12. Open the app in Tab A. Start a session. Open the app in Tab B. Open SettingsDialog in Tab B. Change the variant to a different value than what's running in Tab A. Switch back to Tab A. Confirm: the shape in Tab A did NOT swap mid-session — it continues rendering with the captured-at-Start variant. End the session in Tab A. Both tabs now reflect Tab B's chosen variant for the next session.

    **D-18 zero-new-deps verification:**
    13. Run `git diff --stat package.json package-lock.json` comparing the pre-Phase-17 baseline (commit before Plan 01 landed) to HEAD. Confirm: zero added entries under `dependencies` or `devDependencies` for any new runtime package; ONLY allowable changes are lockfile churn from existing pinned versions (no new top-level entries). If ANY new package appears (e.g. an accidental `react-svg`, `framer-motion`, shape-rendering helper), flag for removal BEFORE phase close — D-18 invariant blocks Phase 17 if violated.

    **Square border-radius aesthetic (CONTEXT.md SPECIFICS):**
    14. After the initial smoke pass, if 18% Square border-radius feels off, note the perception (too round / not round enough). Operator may request a follow-up tweak to 22% or 25% via a one-line CSS edit before final phase close.

    **Ring inner-radius aesthetic (CONTEXT.md SPECIFICS):**
    15. After smoke pass, if 35% Ring inner-stop feels off, note it (too thick body / too thin body). Operator may request a tweak to 30% or 40%.

    **Swatch legibility at 24px (UI-SPEC Pitfall 8 validation):**
    16. Inspect the SettingsDialog Variant picker. Confirm each swatch is identifiable at its actual render size. If the Ring SVG circle stroke is too thin or too thick at 24px, flag for stroke-width adjustment (current value `strokeWidth="4"`).
  </how-to-verify>
  <resume-signal>
    Type one of:
    - "approved" — all UAT items pass; proceed to Task 3 phase-close.
    - "approved with tweaks: <description>" — describe the small CSS edits needed (e.g. "Square 18 → 22%", "Ring inner 35 → 38%", "Ring SVG strokeWidth 4 → 3"). Claude will apply the tweaks, re-run the full automated suite, and re-present UAT before Task 3.
    - "issues: <description>" — describe the failures. Claude will investigate and propose remediation plans (potentially requires a Plan 07 gap-closure if the issues exceed one-line tweaks).
  </resume-signal>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Phase close — flip VARIANT-01..07 to Done in REQUIREMENTS.md; update ROADMAP.md Phase 17 entry; update STATE.md progress + position; create 17-SUMMARY.md</name>
  <files>.planning/REQUIREMENTS.md, .planning/ROADMAP.md, .planning/STATE.md, .planning/phases/17-visual-variants/17-SUMMARY.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (full file — section `### Visual Variants (CUST-03)` + Traceability table)
    - .planning/ROADMAP.md (full file — Phase 17 entry block + progress table + v1.1 customization summary)
    - .planning/STATE.md (full file — frontmatter + Current Position + Performance Metrics + Pending Todos + Roadmap Evolution)
    - .planning/phases/17-visual-variants/17-01-SUMMARY.md through 17-05-SUMMARY.md (the per-plan summaries — synthesize into the phase summary)
    - $HOME/.claude-personal-personal/get-shit-done/templates/summary.md (the GSD phase-summary template — use as the structural baseline for 17-SUMMARY.md)
  </read_first>
  <action>
    1. **Edit `.planning/REQUIREMENTS.md`:**
       - In section `### Visual Variants (CUST-03)` (lines ~46-53), flip every `- [ ] **VARIANT-XX**:` → `- [x] **VARIANT-XX**:` for VARIANT-01..07.
       - In the Traceability table at the bottom, change each row `| VARIANT-XX | Phase 17 | Pending |` → `| VARIANT-XX | Phase 17 | Done |` for VARIANT-01..07.
       - Update the footer line `*Last updated: ...*` to today's date with a note like "Phase 17 (Visual Variants) closed — VARIANT-01..07 flipped to Done".

    2. **Edit `.planning/ROADMAP.md`:**
       - In the `📋 v1.1 Customization (planned)` summary at line ~54, change `- [ ] **Phase 17: Visual Variants**` → `- [x] **Phase 17: Visual Variants**`.
       - In section `### Phase 17: Visual Variants` (lines ~181-192), update `**Plans**:` from `TBD` to a 6-line list:
         ```
         **Plans**: 6 plans
           - [x] 17-01-PLAN.md — CSS + TSX + test-selector rename `.orb-ring--*` → `.shape-marker--*` (D-15 atlas)
           - [x] 17-02-PLAN.md — OrbShape extraction from BreathingShape (verbatim Body + LeadIn move + data-variant='orb' addition + shapeConstants module)
           - [x] 17-03-PLAN.md — SquareShape + RingShape components + 6 [data-variant] CSS overrides (token reuse via D-13)
           - [x] 17-04-PLAN.md — useVisualVariant + useVariantChoice hooks (cross-tab storage + same-tab CustomEvent via D-22)
           - [x] 17-05-PLAN.md — BreathingShape full 3-way dispatcher (optional variant? prop, default 'orb') + VariantPicker radiogroup body with inline shape swatches
           - [x] 17-06-PLAN.md — App.tsx integration (sessionVariantRef snapshot at Start + clear on session end per D-10) + App.session.test.tsx VARIANT-03 coverage + phase close
         ```
       - Update the Progress table at the bottom: change the Phase 17 row from `| 17. Visual Variants | v1.1 | 0/? | Not started | - |` → `| 17. Visual Variants | v1.1 | 6/6 | Complete | YYYY-MM-DD |` (with today's date).

    3. **Edit `.planning/STATE.md`:**
       - Frontmatter:
         - `last_updated`: today (ISO format).
         - `last_activity`: e.g. `2026-05-XX — Phase 17 (Visual Variants) closed; VARIANT-01..07 shipped; sessionVariantRef capture-at-Start integration green; per-palette + reduced-motion + cross-tab UAT approved`.
         - `progress.completed_phases`: 7 (was 6).
         - `progress.completed_plans`: 28 (was 22 — added 6 from Phase 17).
         - `progress.percent`: ~90 (was 85; recompute as 28 / total_plans ≈ 32-ish).
         - `stopped_at`: e.g. `Phase 17 closed`.
       - `## Current Position` block: Phase: 18 (next planned — Audio Timbres); Plan: not started; Status: Phase 17 complete; awaiting Phase 18 planning.
       - `## Performance Metrics → By Phase (v1.1)` table: change Phase 17 row from `| 17. Visual Variants | TBD | Not started |` → `| 17. Visual Variants | 6 | Complete |`.
       - `### Roadmap Evolution`: add a bullet:
         `- Phase 17 closed YYYY-MM-DD — Visual Variants: 3 render variants (Orb default + Square 18% + Ring annulus 35% hollow center) via dispatcher + sibling-shape pattern (D-01); .shape-marker--* CSS rename (D-15); render-local data-variant attribute (D-16); zero new color tokens (D-13 token reuse across variants); sessionVariantRef snapshot at Start (D-09/D-10); 6 plans across 5 waves; per-palette + reduced-motion + cross-tab UAT approved`.
       - `### Pending Todos`: remove the existing `Phase 17 planning (after 16.3): \`/gsd-plan-phase 17\`` bullet (it's now done). Add: `Phase 18 planning: \`/gsd-plan-phase 18\` (Audio Timbres)` and `Phase 19 planning: \`/gsd-plan-phase 19\` (Language Switching)`.
       - `## Operator Next Steps`: replace the Phase 17 next-step pointer with a Phase 18 next-step pointer.
       - `## Session Continuity`: bump `Last session` + `Stopped at` + `Resume file` + `Next command` to reflect Phase 17 close + Phase 18 readiness.

    4. **Create `.planning/phases/17-visual-variants/17-SUMMARY.md`:**
       - Use the GSD summary template at `$HOME/.claude-personal-personal/get-shit-done/templates/summary.md` as the structural baseline.
       - Sections to include:
         - Title + metadata (Phase 17, closed YYYY-MM-DD, milestone v1.1).
         - Goal restatement: 3 visual variants (Orb default + Square + Ring), capture-at-Start, picker disabled in-session, reduced-motion + lead-in + a11y honored, persistence via Envelope.prefs.variant.
         - Plans completed (link to each 17-XX-SUMMARY.md):
           - Plan 01: CSS + TSX + test-selector rename
           - Plan 02: OrbShape extraction
           - Plan 03: SquareShape + RingShape + per-variant CSS overrides
           - Plan 04: useVisualVariant + useVariantChoice hooks
           - Plan 05: BreathingShape dispatcher + VariantPicker radiogroup
           - Plan 06: App.tsx integration + phase close
         - Key decisions implemented (cite D-01 through D-24 from 17-CONTEXT.md).
         - Architectural patterns: sibling-shape + thin dispatcher (D-01); render-local data-variant (D-16); token reuse — no new color tokens (D-13); reduced-motion class reuse (D-14); CustomEvent reuse (D-22); sessionVariantRef snapshot (D-09/D-10/D-11).
         - Carry-forward invariants honored: D-17 per-commit green-gate; D-18 zero new deps; D-19 Phase 14 file-split (settings.ts + prefs.ts unchanged); D-20 Phase 15 picker invariant (SettingsDialog.tsx unchanged); D-23 THEME-UI-01 + THEME-05 guards green.
         - UAT result: per-palette (5/5) + reduced-motion (3 variants) + cross-tab capture-at-Start + a11y (44×44 + focus-visible) approved.
         - Deferred items: per-variant token sets (v1.2); .orb-layer--* class rename (v1.2 ergonomics pass); live idle preview (deferred); additional variants beyond 3 (v1.2+); variant-specific aria-labels (v1.x).
         - Files added: 5 (OrbShape.tsx + SquareShape.tsx + RingShape.tsx + shapeConstants.ts + 2 hook files + 5 test files).
         - Files modified: theme.css, BreathingShape.tsx, VariantPicker.tsx, App.tsx, + related test files.
         - Files NOT touched (per D-19/D-20): settings.ts, prefs.ts, SettingsDialog.tsx.
         - Carry-forwards for v1.2 (the deferred items list).

    5. **Single commit at phase close** (per D-13 close cadence pattern from prior phases): stage all the above edits + create 17-SUMMARY.md in one commit with message like `docs(17): close Phase 17 — Visual Variants shipped (VARIANT-01..07; 3 variants; sessionVariantRef capture-at-Start; per-palette + reduced-motion + cross-tab UAT approved)`.
  </action>
  <verify>
    <automated>grep -cE "VARIANT-(0[1-7]) \\| Phase 17 \\| Done" .planning/REQUIREMENTS.md ; grep -c "^- \\[x\\] \\*\\*Phase 17:" .planning/ROADMAP.md ; ls -la .planning/phases/17-visual-variants/17-SUMMARY.md &amp;&amp; npx tsc --noEmit &amp;&amp; npm run lint &amp;&amp; npm run build &amp;&amp; npm test -- --run</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE "VARIANT-(0[1-7]) \\| Phase 17 \\| Done" .planning/REQUIREMENTS.md` returns exactly 7 (VARIANT-01 through VARIANT-07 all flipped to Done in the traceability table).
    - `grep -cE "^- \\[x\\] \\*\\*VARIANT-(0[1-7])" .planning/REQUIREMENTS.md` returns at least 7 (the per-requirement checkboxes flipped).
    - `grep -c "^- \\[x\\] \\*\\*Phase 17:" .planning/ROADMAP.md` returns exactly 1 (the v1.1 summary checkbox flipped).
    - `grep -cE "17. Visual Variants \\| v1.1 \\| 6/6 \\| Complete" .planning/ROADMAP.md` returns exactly 1 (the Progress table row updated).
    - `grep -cE "17-01-PLAN.md.*Complete|17-02-PLAN.md|17-03-PLAN.md|17-04-PLAN.md|17-05-PLAN.md|17-06-PLAN.md" .planning/ROADMAP.md` returns at least 6 (all 6 plan links present in the Phase 17 detail section).
    - `grep -cE "completed_phases: 7" .planning/STATE.md` returns exactly 1.
    - `grep -cE "completed_plans: 28" .planning/STATE.md` returns at least 1 (or whatever the correct post-Phase-17 count is — 22 + 6 = 28).
    - File `.planning/phases/17-visual-variants/17-SUMMARY.md` exists and is non-empty.
    - `npx tsc --noEmit && npm run lint && npm run build && npm test -- --run` exits 0 at the phase-close commit boundary.
    - Single git commit at phase close — the operator confirms the commit message captures the phase's deliverables succinctly.
  </acceptance_criteria>
  <done>
    Phase 17 is closed: REQUIREMENTS.md VARIANT-01..07 flipped to Done; ROADMAP.md Phase 17 entry updated with 6 plans + Complete status + date stamp; STATE.md progress counters incremented + Pending Todos updated for Phase 18/19 next steps; 17-SUMMARY.md created synthesizing the 6 per-plan summaries + UAT result + deferred-items list. Per-commit green-gate green. Operator-ready for `/gsd-plan-phase 18` (Audio Timbres).
  </done>
</task>

</tasks>

<verification>
**Overall green-gate at phase close:** `npx tsc --noEmit && npm run lint && npm run build && npm test -- --run` exits 0.

**Phase 17 success criteria (per ROADMAP Phase 17 Success Criteria block):**
1. Opening SettingsDialog and selecting a variant (Square or Ring) immediately updates the visual guide to the selected style when not in session; the Orb default is unchanged for users who never open the picker (VARIANT-02) — **VERIFIED** via Plan 05 default-to-orb dispatcher branch + Plan 06 Task 2 UAT.
2. The variant picker is disabled while `inSessionView` is true; the active variant is captured at session start and does not change mid-session (VARIANT-03) — **VERIFIED** via SettingsDialog disabled-gating (Phase 15 carry-forward) + sessionVariantRef snapshot (Plan 06 Task 1).
3. Every variant renders the 3-2-1 lead-in countdown digit correctly through the existing `leadInDigit` prop path (VARIANT-05) — **VERIFIED** via Plan 03 SquareLeadIn + RingLeadIn tests + Plan 06 Task 1 Test 5 lead-in capture test.
4. Every variant applies the `prefers-reduced-motion: reduce` fixed-mid-scale + crossfade fallback equivalent to the existing orb contract (VARIANT-04) — **VERIFIED** via D-14 class reuse + Plan 03 reduced-motion tests + Plan 06 Task 2 UAT.
5. Selected variant persists across reloads via `Envelope.prefs.variant`; `tsc && lint && build && test` exit 0 (VARIANT-07) — **VERIFIED** via Plan 04 useVariantChoice round-trip tests + Plan 06 Task 2 UAT step 11.

**Carry-forward guard checks:**
- THEME-05 ≥ 1.5 contrast guard across all 5 palettes — green (token reuse, no edits).
- THEME-UI-01 hardcoded-class guard — green (all new .tsx files token-bound).

**Phase 17 → Phase 18 readiness:** STATE.md updated. Operator can run `/gsd-discuss-phase 18` (Audio Timbres) next; Phase 18 will follow the same Phase 15 / Phase 16 picker patterns established in Phases 16-17.
</verification>

<success_criteria>
- `src/app/App.tsx` invokes useVisualVariant, declares sessionVariantRef, snapshots at Start, clears on session end, and threads `variant={sessionVariantRef.current ?? liveVariant}` to BreathingShape.
- `src/app/App.session.test.tsx` covers VARIANT-03 with 5 new integration tests (capture-at-Start, no-mid-session-swap, next-session-picks-new, VARIANT-02 default-orb, lead-in-uses-captured-variant).
- Operator UAT approval — all 5 palettes + 3 variants + reduced-motion + cross-tab + a11y items pass.
- REQUIREMENTS.md: VARIANT-01..07 flipped Pending → Done.
- ROADMAP.md: Phase 17 entry marked Complete with 6 plans + date.
- STATE.md: progress counters + position + pending-todos + roadmap-evolution updated.
- 17-SUMMARY.md created synthesizing all 6 plan summaries + UAT result + deferred items.
- `tsc && lint && build && test` exits 0 at phase-close commit boundary.
</success_criteria>

<output>
After completion, the phase is closed:
- `.planning/phases/17-visual-variants/17-SUMMARY.md` is the phase summary.
- The 6 per-plan SUMMARY files (17-01-SUMMARY.md through 17-06-SUMMARY.md) are in place.
- REQUIREMENTS.md + ROADMAP.md + STATE.md reflect Phase 17 complete.
- Operator next steps: `/gsd-discuss-phase 18` (Audio Timbres) — same picker pattern as Phase 16 themes + Phase 17 variants; TIMBRE-01..05 requirements pending.
</output>
