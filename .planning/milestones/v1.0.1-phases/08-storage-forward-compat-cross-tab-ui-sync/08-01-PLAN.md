---
phase: 08-storage-forward-compat-cross-tab-ui-sync
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/storage/storage.ts
  - src/storage/storage.test.ts
  - src/storage/stats.ts
autonomous: true
requirements:
  - STORAGE-01
  - STORAGE-02
user_setup: []

must_haves:
  truths:
    - "readEnvelope returns the on-disk numeric version field; STATE_VERSION is only the fallback when disk lacks a finite numeric version."
    - "readEnvelope preserves unknown top-level fields from the parsed envelope (D-01) — a future v2 envelope with a new top-level subtree survives a v1 read."
    - "writeEnvelope refuses (no-op) to overwrite a future-version on-disk envelope when on-disk version > STATE_VERSION. Refusal is silent — no console.warn, no log, no DEV-mode branch (D-03)."
    - "writeEnvelope stamps STATE_VERSION on every successful write (D-04, ignores caller-passed version)."
    - "A throwing getItem during the D-04a inner re-read does NOT skip the write — the inner try/catch is nested separately from the outer D-16 catch."
    - "Subtree coercers (coerceSettings / coerceMute / coerceStats) keep stripping unknown sub-keys; forward-compat is top-level only (D-02)."
    - "Vitest suite reports 10 tests passing in src/storage/storage.test.ts; overall phase-wide suite >= 364 after Plan 01 (was 363 baseline). STORAGE-01 preserve case REPLACES the old re-stamp case at lines 77-83; STORAGE-02 no-downgrade case is APPENDED inside the same describe('writeEnvelope', ...) block; per D-07 the storage adapter contracts live in src/storage/storage.test.ts (cross-tab UI sync STORAGE-03 lives in src/app/App.persistence.test.tsx, owned by Plan 02)."
  artifacts:
    - path: "src/storage/storage.ts"
      provides: "Widened Envelope.version: number; readEnvelope D-01 spread-and-override; writeEnvelope D-04a nested-try-catch downgrade refusal."
      contains: "version: number"
    - path: "src/storage/storage.test.ts"
      provides: "Replaced re-stamp test with preserve-on-disk-version case; new STORAGE-02 no-downgrade case inside describe('writeEnvelope', ...)."
      contains: "preserves on-disk version when reading"
    - path: "src/storage/stats.ts"
      provides: "WR-07 comment updated to reflect STORAGE-03 carry-forward (UI consistency restored; increment race remains v1.x debt)."
      contains: "UI consistency restored via the STORAGE-03"
  key_links:
    - from: "src/storage/storage.ts readEnvelope"
      to: "object literal return"
      via: "spread of `parsed` Record<string, unknown> before key overrides"
      pattern: "\\.\\.\\.p,\\s*version: onDiskVersion"
    - from: "src/storage/storage.ts writeEnvelope"
      to: "early return"
      via: "inline disk re-read followed by `if (currentVersion > STATE_VERSION) return`"
      pattern: "if \\(currentVersion > STATE_VERSION\\) return"
    - from: "src/storage/storage.test.ts describe('writeEnvelope', ...)"
      to: "STORAGE-02 disk-unchanged assertion"
      via: "seed { version: 2 } -> writeEnvelope({ version: 1, ... }) -> getItem matches seed"
      pattern: "refuses to overwrite a future-version on-disk envelope"
---

<objective>
Land STORAGE-01 (`readEnvelope` preserves on-disk numeric `version`; top-level unknown fields survive) and STORAGE-02 (`writeEnvelope` refuses to overwrite a future-version on-disk envelope, with D-04a's inline re-read wrapped in a nested try/catch so a throwing `getItem` falls through to attempt the write). All changes are surgical edits to the existing storage adapter + tests; no new files, no new dependencies, no new public exports.

Purpose: Make the localStorage envelope forward-compatible with future schema bumps (read side) and refuse silent schema downgrades from older builds running concurrently in another tab (write side). Locks the contracts with two Vitest cases inside `src/storage/storage.test.ts`. Comment-only update in `src/storage/stats.ts` keeps the WR-07 audit trail coherent now that STORAGE-03 (Plan 02) closes the UI-consistency half of the cross-tab story.

Output:
- `src/storage/storage.ts` — `Envelope.version` widened to `number`; `readEnvelope` adopts D-01 spread-then-override; `writeEnvelope` adopts D-04a nested-try-catch + STORAGE-02 downgrade refusal; `D-04` stamping behavior unchanged.
- `src/storage/storage.test.ts` — line 77-83 case REPLACED with STORAGE-01 preserve-version case; new STORAGE-02 case APPENDED inside the same `describe('writeEnvelope', ...)` block.
- `src/storage/stats.ts` — WR-07 comment block at lines 76-81 updated (comment-only; no logic change).
</objective>

<execution_context>
@$HOME/.claude-personal-personal/get-shit-done/workflows/execute-plan.md
@$HOME/.claude-personal-personal/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-storage-forward-compat-cross-tab-ui-sync/08-CONTEXT.md
@.planning/phases/08-storage-forward-compat-cross-tab-ui-sync/08-RESEARCH.md
@.planning/phases/08-storage-forward-compat-cross-tab-ui-sync/08-PATTERNS.md
@.planning/phases/08-storage-forward-compat-cross-tab-ui-sync/08-UI-SPEC.md
@.planning/phases/08-storage-forward-compat-cross-tab-ui-sync/08-VALIDATION.md
@src/storage/storage.ts
@src/storage/storage.test.ts
@src/storage/stats.ts

<interfaces>
<!-- Identifiers and exact surface the executor needs from src/storage/storage.ts -->
<!-- Source: src/storage/storage.ts (verified by direct read) -->

From src/storage/storage.ts (current state):
- export const STATE_KEY = 'hrv:state:v1'                  // line 35
- export const STATE_VERSION = 1 as const                  // line 36
- export interface StorageDeps { now?: () => number; storage?: Storage }  // lines 38-41
- export interface Envelope {                              // lines 43-48
    version: typeof STATE_VERSION
    settings?: unknown
    mute?: unknown
    stats?: unknown
  }
- const EMPTY_ENVELOPE: Envelope = { version: STATE_VERSION }   // line 50
- export function readEnvelope(deps: StorageDeps = {}): Envelope    // lines 52-79
- export function writeEnvelope(env: Envelope, deps: StorageDeps = {}): void   // lines 81-89

REQUIRED post-change identifier shapes (no renames, no API surface changes):
- Envelope.version: number    // widened from `typeof STATE_VERSION`
- readEnvelope return type: Envelope (unchanged); D-01 spread of `p` propagates unknown top-level fields at runtime even though they aren't surfaced in the static type (per RESEARCH.md RQ-4 Option b).
- writeEnvelope signature: unchanged.
- EMPTY_ENVELOPE: unchanged — `STATE_VERSION` (1 as const) satisfies `number`.

From src/storage/storage.test.ts (current state, line numbers verified):
- Lines 1-12: imports + beforeEach/afterEach.
- describe('readEnvelope', ...) at line 14 — 5 tests inside (lines 15-48). UNCHANGED by this plan.
- describe('writeEnvelope', ...) at line 51 — 4 tests inside (lines 52-83). The 4th (lines 77-83) is the one to REPLACE.
- Lines 77-83: 'always re-stamps version: 1 even if a caller passes a wrong version' — REPLACE body and rename `it()` description.

From src/storage/stats.ts (current state):
- Lines 76-81: WR-07 comment block inside `recordSession`. Comment-only update target.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: STORAGE-01 — widen Envelope.version + read-preserve + write-refuse-downgrade in src/storage/storage.ts</name>
  <files>src/storage/storage.ts</files>
  <read_first>
    - src/storage/storage.ts (full file — 90 lines; lines 43-48 `Envelope`, lines 52-79 `readEnvelope`, lines 81-89 `writeEnvelope`)
    - .planning/phases/08-storage-forward-compat-cross-tab-ui-sync/08-CONTEXT.md §decisions (D-01, D-02, D-03, D-04, D-04a) and §specifics (D-04a code sketch)
    - .planning/phases/08-storage-forward-compat-cross-tab-ui-sync/08-RESEARCH.md §"Pattern 1: readEnvelope — Spread + Override", §"Pattern 2: writeEnvelope — Inline Re-Read Guard", §"Anti-Patterns to Avoid", §"Pitfall 1: D-04a try/catch Nesting Order", §"Code Examples" (Full readEnvelope and Full writeEnvelope)
    - .planning/phases/08-storage-forward-compat-cross-tab-ui-sync/08-PATTERNS.md §"src/storage/storage.ts — STORAGE-01 (`readEnvelope`) + STORAGE-02 (`writeEnvelope`) + `Envelope` interface" (verbatim replacement bodies for both functions)
  </read_first>
  <behavior>
    - Envelope.version is typed `number` (not `typeof STATE_VERSION` literal `1`). EMPTY_ENVELOPE compiles unchanged.
    - readEnvelope({ getItem returns `'{"version":2,"settings":{"bpm":4},"prefs":{"theme":"dark"}}'` }) returns `{ version: 2, settings: { bpm: 4 }, mute: undefined, stats: undefined, prefs: { theme: 'dark' } }` at runtime (D-01: unknown top-level field `prefs` survives via the `...p` spread). The static return type still says `Envelope` — that is intentional per RESEARCH RQ-4 Option b.
    - readEnvelope({ getItem returns `'{"version":"bogus","settings":{}}'` }) returns `{ version: STATE_VERSION, settings: {}, mute: undefined, stats: undefined }` (fallback because the disk version is non-numeric per the `typeof p.version === 'number' && Number.isFinite(p.version)` check).
    - readEnvelope existing tests at lines 15-48 (5 cases) continue to pass UNCHANGED — they seed `version: 1` or empty/corrupt storage; `1 === STATE_VERSION` so `env.version` still equals `STATE_VERSION` in those tests.
    - writeEnvelope({ version: 1, stats: ... }) called against a disk envelope with `{ version: 2 }` MUST return without calling setItem (D-03 silent refusal; no console.warn; D-04a guard via inline `getItem` + JSON.parse + Number.isFinite check).
    - writeEnvelope's inner re-read MUST be in its own try/catch. A `vi.spyOn(Storage.prototype, 'getItem').mockImplementation(() => { throw ... })` should NOT prevent the subsequent setItem call. (No new test for this scenario in this plan, but the nested-try shape is required.)
    - writeEnvelope on every successful path stamps `version: STATE_VERSION` (D-04). Existing storage.test.ts line 52-58 case ("persists the envelope with version stamped") continues to pass UNCHANGED.
    - writeEnvelope D-16 outer catch still swallows quota/ITP errors — existing lines 61-75 tests pass UNCHANGED.
    - tsc --noEmit exits 0 against the Phase 7 strict baseline (`strict: true`, `noUncheckedIndexedAccess: true`, `noImplicitReturns: true`).
  </behavior>
  <action>
    Apply three surgical edits to src/storage/storage.ts:

    1) Widen the `Envelope` interface (lines 43-48). Change `version: typeof STATE_VERSION` to `version: number`. Leave the optional `settings`, `mute`, `stats` fields alone. Leave EMPTY_ENVELOPE (line 50) unchanged — `STATE_VERSION` (declared `1 as const`) satisfies `number`. Do NOT add an `[k: string]: unknown` index signature (RESEARCH RQ-4 Option b explicitly rejects it; value-level spread carries forward-compat at runtime).

    2) Rewrite the object-check branch of `readEnvelope` (lines 58-72) to implement D-01. Keep the outer `try`, the `raw === null` early return, the `JSON.parse` call, the `typeof parsed === 'object' && !Array.isArray(parsed)` guard, the `parsed as Record<string, unknown>` cast (existing pattern at line 66, do not change the type-narrowing approach), and the outer `catch` D-17 fallback — all unchanged. INSIDE the object-check branch, replace the existing CR-01 pick-only-known return object (lines 66-72) with the D-01 spread-then-override pattern:
       - Compute `const onDiskVersion = typeof p.version === 'number' && Number.isFinite(p.version) ? p.version : STATE_VERSION` (use `Number.isFinite` per RESEARCH §"Recommended Implementation Approach" — looser shape matches REVIEW.md sketch and the existing `stats.ts` validation style; do NOT add `Number.isInteger(p.version) && p.version >= 1`).
       - Return `{ ...p, version: onDiskVersion, settings: p.settings, mute: p.mute, stats: p.stats }`. The `...p` spread MUST come first so the explicit overrides win. The explicit overrides keep the four known fields surfacing on the static Envelope type even when `p` has them via the spread.
       - Replace the existing CR-01 docstring block above the return so it explains D-01: the spread preserves unknown top-level fields (forward-compat for a future v2 envelope with a new top-level subtree); the explicit overrides keep `version` pinned to the on-disk numeric value (or STATE_VERSION fallback); subtree coercers (D-02) still strip unknown sub-keys inside `settings`/`mute`/`stats`. Reference D-01 and D-02 by name in the comment so a future PR reverting back to "pick only known keys" gets caught (Pitfall 3).
       - The `{ ...EMPTY_ENVELOPE }` fallback returns at lines 56 and 74-77 stay UNCHANGED (Pitfall 4 — keep the spreads to return fresh objects).

    3) Rewrite `writeEnvelope` (lines 81-89) to implement STORAGE-02 + D-04a + D-04. Keep the outer `try` and outer `catch` (D-16) — DO NOT collapse the new inner re-read into the outer try (Pitfall 1: a throwing getItem would short-circuit the entire write via the outer catch, which is wrong — the guard must be fail-open). Inside the outer `try`, BEFORE the JSON.stringify+setItem sequence:
       - Declare `let currentVersion: number = STATE_VERSION`.
       - Open a NEW nested `try` block whose body does: `const raw = storage.getItem(STATE_KEY); if (raw !== null) { const parsed: unknown = JSON.parse(raw); if (parsed !== null && typeof parsed === 'object' && !Array.isArray(parsed)) { const v = (parsed as Record<string, unknown>).version; if (typeof v === 'number' && Number.isFinite(v)) currentVersion = v } }`. Close with `catch { /* D-17: treat as no version info; proceed with write */ }`.
       - After the nested try/catch, add `if (currentVersion > STATE_VERSION) return` (STORAGE-02 / D-03 silent refusal — no console.warn, no log, no DEV branch).
       - Keep the existing `const payload = JSON.stringify({ ...env, version: STATE_VERSION }); storage.setItem(STATE_KEY, payload)` as the final lines of the outer try (D-04 stamp behavior unchanged).
       - Above the guard, add an explanatory comment block citing D-04a (inline re-read), STORAGE-02 (downgrade refusal), D-03 (silent), and the WR-08 posture (caller's RAM state stays authoritative). Reference the cross-tab race scope: the guard catches a CROSS-tab newer-version landing between caller-read and caller-write; the WR-07 in-tab race is unchanged (documented v1.x debt; STORAGE-03 handles only UI consistency).

    Do NOT touch: STATE_KEY, STATE_VERSION, StorageDeps, the `deps.storage ?? window.localStorage` resolution, the `{ ...EMPTY_ENVELOPE }` fallback returns inside readEnvelope, or the file's header comment block (lines 1-34). Do NOT add new exports. Do NOT add `import.meta.env.DEV` checks. Do NOT add `Number.isInteger` checks (per RESEARCH recommendation — use `Number.isFinite` only).
  </action>
  <acceptance_criteria>
    - `grep -n "version: number" src/storage/storage.ts` returns exactly one match inside the `Envelope` interface body.
    - `grep -nE "version: typeof STATE_VERSION" src/storage/storage.ts` returns zero matches (the literal-typed declaration is gone).
    - `grep -nE "\\.\\.\\.p," src/storage/storage.ts` returns at least one match inside readEnvelope (D-01 spread present).
    - `grep -nE "onDiskVersion" src/storage/storage.ts` returns at least one match.
    - `grep -nE "Number\\.isFinite" src/storage/storage.ts` returns at least 2 matches (one in readEnvelope D-01 branch, one in writeEnvelope D-04a inner re-read).
    - `grep -nE "if \\(currentVersion > STATE_VERSION\\) return" src/storage/storage.ts` returns exactly one match.
    - `grep -cE "^\\s*try \\{" src/storage/storage.ts` is at least 3 (readEnvelope outer try, writeEnvelope outer try, writeEnvelope inner re-read try).
    - `grep -cE "^\\s*\\} catch" src/storage/storage.ts` is at least 3 (matching the 3 try blocks).
    - `grep -nE "console\\." src/storage/storage.ts` returns zero matches (D-03 silent refusal — no warn/log even in DEV).
    - `npx tsc --noEmit` exits 0.
    - `npx vitest run src/storage/storage.test.ts` continues to pass all 9 existing tests (the file's existing tests survive unmodified per RESEARCH RQ-7; the case at lines 77-83 is REPLACED by Task 2 — that task changes the test, not this one).
    - `npx eslint src/storage/storage.ts` exits 0 (no new lint errors under the Phase 7 strictTypeChecked baseline).
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit &amp;&amp; npx vitest run src/storage/storage.test.ts &amp;&amp; npx eslint src/storage/storage.ts</automated>
  </verify>
  <done>Envelope.version is `number`; readEnvelope spreads then overrides (D-01); writeEnvelope has nested-try-catch inline re-read followed by `if (currentVersion > STATE_VERSION) return` (STORAGE-02 + D-04a); existing 9 storage.test.ts tests still pass; tsc and eslint clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Replace storage.test.ts line 77-83 with STORAGE-01 preserve-version case; append STORAGE-02 no-downgrade case</name>
  <files>src/storage/storage.test.ts</files>
  <read_first>
    - src/storage/storage.test.ts (full file — 85 lines; existing test at lines 77-83 is the slot to REPLACE)
    - src/storage/storage.ts (post-Task-1 state — for Envelope.version: number, readEnvelope D-01 spread, writeEnvelope D-04a guard)
    - .planning/phases/08-storage-forward-compat-cross-tab-ui-sync/08-CONTEXT.md §decisions (D-07 test layout), §specifics ("Existing test case to REPLACE, not delete" and "New STORAGE-02 test case")
    - .planning/phases/08-storage-forward-compat-cross-tab-ui-sync/08-RESEARCH.md §"Replaced Test (storage.test.ts lines 77-83)" and §"RQ-7: Test Count Invariant"
    - .planning/phases/08-storage-forward-compat-cross-tab-ui-sync/08-PATTERNS.md §"src/storage/storage.test.ts — Replace lines 77–83; add STORAGE-02 case" (verbatim test bodies including the non-null assertion eslint-disable pattern)
  </read_first>
  <behavior>
    - After this task, `describe('writeEnvelope', ...)` contains 5 tests instead of 4: the existing 3 (lines 52-75 unchanged) + the REPLACED slot (was "always re-stamps version: 1...", now "preserves on-disk version when reading; stamps STATE_VERSION on write") + the NEW APPENDED case ("writeEnvelope refuses to overwrite a future-version on-disk envelope (STORAGE-02)").
    - `describe('readEnvelope', ...)` is UNCHANGED (5 tests; lines 14-48 untouched).
    - Total storage.test.ts case count: was 9, now 10 (5 readEnvelope + 5 writeEnvelope). Final phase-level test count contribution: +1 here; Plan 02 adds +1 (cross-tab refresh) and optionally +1 (key filter) → 365 or 366 phase-final per VALIDATION.md.
    - STORAGE-01 case asserts: seed `{ version: 2, settings: { bpm: 4 }, prefs: { theme: 'dark' } }` → `readEnvelope().version === 2` and `readEnvelope().settings` equals `{ bpm: 4 }`. After calling `writeEnvelope({ version: 1, settings: { bpm: 5 } })`, `JSON.parse(getItem(STATE_KEY))` STILL matches `{ version: 2, settings: { bpm: 4 } }` — the write was refused (D-04a guard fired because disk version 2 > STATE_VERSION 1).
    - STORAGE-02 case (separate `it()` for clarity): seed `{ version: 2 }`, call `writeEnvelope({ version: 1, stats: { totalSessions: 99, ... } })`, assert `JSON.parse(getItem(STATE_KEY))` matches `{ version: 2 }` AND does NOT match `{ stats: { totalSessions: 99 } }`.
    - Casts: `writeEnvelope({ version: 1, ... })` does NOT need `as any` anywhere — `1` satisfies `number` after the Task 1 Envelope widening. RESEARCH RQ-3 confirmed this.
    - All Vitest commands continue to use the project's existing test scripts (`npx vitest run` / `npm run test`).
  </behavior>
  <action>
    Apply two surgical edits to src/storage/storage.test.ts. Do NOT touch lines 1-76 (the imports, beforeEach/afterEach, the entire `describe('readEnvelope', ...)` block at lines 14-49, the writeEnvelope describe header at line 51, or the existing 3 writeEnvelope tests at lines 52-75).

    1) REPLACE the `it()` slot currently at lines 77-83 (`'always re-stamps version: 1 even if a caller passes a wrong version'`). Keep the slot INSIDE `describe('writeEnvelope', ...)`. The new `it()` is named `'preserves on-disk version when reading; stamps STATE_VERSION on write'`. The body:
       - Seed: `window.localStorage.setItem(STATE_KEY, JSON.stringify({ version: 2, settings: { bpm: 4 }, prefs: { theme: 'dark' } }))`. The `prefs` field is the future-v2 forward-compat probe.
       - Read assertion: `const env = readEnvelope(); expect(env.version).toBe(2); expect(env.settings).toEqual({ bpm: 4 })`. (STORAGE-01: on-disk version preserved.)
       - Write attempt: `writeEnvelope({ version: 1, settings: { bpm: 5 } })`. No cast needed.
       - Post-write disk-unchanged assertion: read raw via `const rawAfter = window.localStorage.getItem(STATE_KEY)`, then `expect(JSON.parse(rawAfter!) as unknown).toMatchObject({ version: 2, settings: { bpm: 4 } })`. Use the existing non-null assertion + eslint-disable pattern from line 80-81 of the current file: `// eslint-disable-next-line @typescript-eslint/no-non-null-assertion` on the line above the `!` use. (STORAGE-02: write refused by D-04a guard; disk unchanged.)

    2) APPEND a new `it()` AFTER the replaced case, still inside the same `describe('writeEnvelope', ...)` block (before its closing `})`). Name: `'writeEnvelope refuses to overwrite a future-version on-disk envelope (STORAGE-02)'`. Body:
       - Seed: `window.localStorage.setItem(STATE_KEY, JSON.stringify({ version: 2 }))`.
       - Write attempt: `writeEnvelope({ version: 1, stats: { totalSessions: 99, totalElapsedSeconds: 0, lastSessionAtMs: null, lastSessionDurationSeconds: null } })`. The 99-sessions stats payload is the probe — if the guard fails, we'd see it on disk.
       - Disk-match assertion: `const rawAfter = window.localStorage.getItem(STATE_KEY)` (with the same eslint-disable non-null assertion comment), `expect(JSON.parse(rawAfter!) as unknown).toMatchObject({ version: 2 })`.
       - Negative assertion: `expect(JSON.parse(rawAfter!) as unknown).not.toMatchObject({ stats: { totalSessions: 99 } })`. This confirms the 99-sessions write was rejected, not just stamped on top of version 2.

    Do NOT introduce: new imports (the existing `readEnvelope, writeEnvelope, STATE_KEY, STATE_VERSION` import covers everything), `as any` casts, console.warn spies (D-03 silent — there is nothing to assert), or any spy on `Storage.prototype.getItem`/`setItem` for these two cases (the D-04a guard relies on the real localStorage flow; do not mock).
  </action>
  <acceptance_criteria>
    - `grep -nE "preserves on-disk version when reading" src/storage/storage.test.ts` returns exactly one match.
    - `grep -nE "writeEnvelope refuses to overwrite a future-version on-disk envelope \\(STORAGE-02\\)" src/storage/storage.test.ts` returns exactly one match.
    - `grep -nE "always re-stamps version: 1" src/storage/storage.test.ts` returns zero matches (the old case is gone, not merely renamed).
    - `grep -c "^  it(" src/storage/storage.test.ts` returns 10 (5 readEnvelope tests + 5 writeEnvelope tests). Use `grep -c "^  it("` with the two-space indent prefix to count only top-level `it(` declarations (filters out commented lines).
    - `grep -nE "prefs: \\{ theme: 'dark' \\}" src/storage/storage.test.ts` returns at least one match (forward-compat probe present).
    - `grep -nE "totalSessions: 99" src/storage/storage.test.ts` returns at least one match (STORAGE-02 negative-assertion probe present).
    - `npx vitest run src/storage/storage.test.ts` exits 0 with `10 passed` reported in the summary (was 9).
    - `npx tsc --noEmit` exits 0.
    - `npx eslint src/storage/storage.test.ts` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/storage/storage.test.ts &amp;&amp; npx tsc --noEmit &amp;&amp; npx eslint src/storage/storage.test.ts</automated>
  </verify>
  <done>storage.test.ts contains 10 tests; the old re-stamp case is replaced by the STORAGE-01 preserve-version case; a new STORAGE-02 standalone case follows; full suite for this file passes; tsc and eslint clean.</done>
</task>

<task type="auto">
  <name>Task 3: Update WR-07 comment block in src/storage/stats.ts:76-81 (comment-only)</name>
  <files>src/storage/stats.ts</files>
  <read_first>
    - src/storage/stats.ts lines 70-98 (the `recordSession` function body and the WR-07 comment block at lines 76-81)
    - .planning/phases/08-storage-forward-compat-cross-tab-ui-sync/08-CONTEXT.md §deferred ("WR-07 increment race fix")
    - .planning/phases/08-storage-forward-compat-cross-tab-ui-sync/08-RESEARCH.md §"RQ-8: WR-07 Comment Update — Bundle or Defer?" (research recommends bundling)
    - .planning/phases/08-storage-forward-compat-cross-tab-ui-sync/08-PATTERNS.md §"src/storage/stats.ts — Optional comment update at lines 76–81" (verbatim updated comment text)
  </read_first>
  <behavior>
    - The comment at src/storage/stats.ts:76-81 no longer says "(cross-tab sync is still a v2 concern)". The updated wording escalates the parenthetical from "v2 concern" to "documented v1.x work; UI consistency restored via the STORAGE-03 storage-event listener in App.tsx."
    - Zero logic change — `recordSession` function body (lines 82-97) is byte-for-byte identical before and after.
    - Existing tests in `src/storage/stats.test.ts` (if any) and `src/app/App.persistence.test.tsx` continue to pass unmodified.
  </behavior>
  <action>
    In src/storage/stats.ts, replace the comment block at lines 76-81 with the updated WR-07 wording from PATTERNS.md §"src/storage/stats.ts — Optional comment update at lines 76–81". Specifically, change the final parenthetical sentence "Collapsing to one read closes that window for in-tab correctness (cross-tab sync is still a v2 concern)." to a two-sentence form: "Collapsing to one read closes that window for in-tab correctness. Cross-tab concurrent ends lose one increment — documented v1.x work; UI consistency restored via the STORAGE-03 storage-event listener in App.tsx." Preserve the leading `  // ` comment prefix on every wrapped line. Keep the comment indentation aligned with the surrounding code (two-space indent inside the function body). Do NOT modify any code on lines 71-75 or 82-97.
  </action>
  <acceptance_criteria>
    - `grep -nE "cross-tab sync is still a v2 concern" src/storage/stats.ts` returns zero matches (old wording removed).
    - `grep -nE "UI consistency restored via the STORAGE-03" src/storage/stats.ts` returns exactly one match.
    - `grep -nE "Cross-tab concurrent ends lose one increment" src/storage/stats.ts` returns exactly one match.
    - `git diff src/storage/stats.ts` (in the executor's environment) shows comment-only changes inside the WR-07 block at lines 76-81; no lines outside that block are touched.
    - `npx tsc --noEmit` exits 0.
    - `npx vitest run src/storage` exits 0 (full storage-folder suite unaffected — no logic change).
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit &amp;&amp; npx vitest run src/storage</automated>
  </verify>
  <done>Comment block at stats.ts:76-81 updated to escalate "v2 concern" to "documented v1.x work; UI consistency restored via STORAGE-03"; recordSession logic untouched; storage-folder tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| localStorage <-> readEnvelope | Untrusted JSON payload from disk crosses into the running process. `JSON.parse` may throw on malformed input; `typeof p.version` may be any unknown shape. |
| Cross-tab writer <-> writeEnvelope | A concurrently running build in another tab may have written a future-version envelope between this tab's `readEnvelope` and `writeEnvelope`. The D-04a inline re-read is the only race-safe primitive available against localStorage (no atomic test-and-set). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 | Tampering | `readEnvelope` JSON.parse path | mitigate | `JSON.parse` is wrapped in the existing outer try/catch (D-17). Falls back to `EMPTY_ENVELOPE` on throw. Pattern unchanged from Phase 4; STORAGE-01 only extends the success branch. |
| T-08-02 | Tampering | `readEnvelope` D-01 version-field type confusion | mitigate | `typeof p.version === 'number' && Number.isFinite(p.version)` rejects non-number values (strings, booleans, null, undefined, NaN, Infinity) before they reach the returned envelope. Fallback to `STATE_VERSION`. ASVS L1: input validation at the trust boundary. |
| T-08-03 | Denial of Service | `writeEnvelope` inline re-read throws | mitigate | D-04a's nested inner try/catch ensures a throwing `getItem` (Safari ITP private mode) does NOT prevent the subsequent write attempt. Fail-open for the guard; D-16 outer catch still silences the actual write if quota fires. Pitfall 1 explicitly addresses this. |
| T-08-04 | Tampering | `writeEnvelope` caller passes future `version` field | mitigate | D-04 stamps `STATE_VERSION` on every successful write via the existing `JSON.stringify({ ...env, version: STATE_VERSION })` line. Caller-passed `env.version` is structurally ignored. |
| T-08-05 | Repudiation / data corruption | Cross-tab v2 writer vs. v1 build write | mitigate | STORAGE-02 / D-03 — disk version > STATE_VERSION → silent no-op return. Protects against a v1 build overwriting a v2 envelope with v1 stats and triggering data loss on the v2 build's next read. Disposition: mitigate (this is the core threat the phase exists to address). |
| T-08-06 | Information disclosure | Refused-downgrade silent | accept | D-03 locks silent refusal — no console.warn, no toast, no DEV-mode branch. Trade-off: a debugging developer cannot distinguish a downgrade-refusal from a quota failure. Accepted because both yield identical "RAM state authoritative; disk may not have synced" semantics for the running app, and surfacing the refusal would invite UI noise on the v1.0.1 fix-only milestone. |
| T-08-07 | Denial of Service | Forward-compat `...p` spread of attacker-controlled `parsed` object | accept | An attacker with DevTools access can inject arbitrary top-level fields (e.g., `{__hack: 'x'}`) that survive the round-trip via the D-01 spread. Accepted per CONTEXT.md D-01: "garbage at the top level is judged less harmful than dropping forward-compatible v2 fields." Attacker model: DevTools access = full local compromise; the surviving drift is not amplified into a security boundary. ASVS L1: not a remote attacker scenario. |

No `high` severity threats remain — all severe threats (T-08-03 throwing-getItem skipping write; T-08-05 v1 clobbering v2) are mitigated. T-08-06 and T-08-07 are explicit accept-with-rationale per locked CONTEXT.md decisions.
</threat_model>

<verification>
After all three tasks land:

- `npx tsc --noEmit` exits 0 (Phase 7 strict baseline holds — `Envelope.version: number` widening has no caller fallout per RESEARCH RQ-3).
- `npx eslint src/storage/storage.ts src/storage/storage.test.ts src/storage/stats.ts` exits 0 (no new lint violations under `strictTypeChecked`).
- `npx vitest run src/storage` exits 0 with 10 `storage.test.ts` cases passing (was 9; +1 from the STORAGE-02 append; the REPLACE nets 0). Phase-wide test contribution from Plan 01: +1.
- Phase invariant: `npx vitest run` overall test count is at least 364 before Plan 02 (was 363 baseline). Final phase-end target is 365 or 366 (depends on Plan 02 optional key-filter test).
- Subtree integrity: spot-check by reading `src/storage/settings.ts` and `src/storage/stats.ts` — neither file's coercer (`coerceSettings`, `coerceStats`) is modified, so D-02 (subtree coercers stay strict) holds without further action.
- WR-07 comment audit: `grep -rn "cross-tab sync is still a v2 concern" src/` returns zero matches (old wording purged from the entire src tree, not just stats.ts — verifying no copy-paste echoes elsewhere).
</verification>

<success_criteria>
- Envelope.version is `number`; `EMPTY_ENVELOPE` and all readEnvelope/writeEnvelope call sites compile cleanly under Phase 7 strict.
- readEnvelope preserves on-disk numeric `version` and spreads unknown top-level fields (D-01); existing 5 readEnvelope tests still pass; the new replaced case verifies preservation with a `prefs: { theme: 'dark' }` forward-compat probe.
- writeEnvelope refuses to write when on-disk `version > STATE_VERSION` (STORAGE-02), implemented via D-04a's nested-try-catch inline re-read; silent (D-03); D-04 still stamps STATE_VERSION on every successful write; existing 3 writeEnvelope tests still pass; the new STORAGE-02 case verifies refusal with a 99-session stats probe.
- WR-07 comment at stats.ts:76-81 escalated to reference STORAGE-03's UI-consistency restoration; no logic change.
- `npx vitest run src/storage/storage.test.ts` = 10 passed, 0 failed.
- Plan 02 (depends_on: [01]) can proceed against this contract.
</success_criteria>

<output>
After completion, create `.planning/phases/08-storage-forward-compat-cross-tab-ui-sync/08-01-SUMMARY.md` recording: (1) the three files modified with line-range diffs, (2) the new test count (10 in storage.test.ts), (3) confirmation that tsc/eslint/vitest are all green, (4) any deviations from the plan (expected: none — this is a tightly-scoped surgical change).
</output>
