---
phase: 11-domain-ui-contracts-accessibility
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/domain/sessionController.ts
  - src/domain/sessionController.test.ts
  - src/components/SessionReadout.tsx
  - src/components/SessionReadout.test.tsx
  - src/components/MuteToggle.tsx
  - src/components/MuteToggle.test.tsx
  - src/components/SessionControls.tsx
  - src/app/App.tsx
  - src/app/App.dialog.test.tsx
autonomous: true
requirements:
  - DOMAIN-01
  - UI-01
  - UI-02
  - A11Y-01
tags:
  - domain-validation
  - ui-contract
  - dialog-lifecycle
  - accessibility
  - react

must_haves:
  truths:
    - "D-01, D-02: extendTimedSession throws RangeError at the boundary for a finite numeric durationMinutes that is not in DURATION_OPTIONS, AFTER the open-ended throw and BEFORE the existing Number.isFinite/<= comparison check fires. Parameter type stays `durationMinutes: number` — no narrowing."
    - "D-04, D-05: SessionReadout accepts isLeadInPlaceholder?: boolean; when true with a non-null frame the component branches FIRST (before the existing idle/null/undefined early-return) and renders the timer chip (label + formatted duration), ignoring status/message."
    - "D-05: App passes status={state.status} verbatim to SessionReadout (no 'idle' override) and passes isLeadInPlaceholder={appPhase === 'lead-in'} — the existing status hack is dropped."
    - "D-07, D-08: An App-level useEffect on [inSessionView] force-closes BOTH learnDialogOpen AND resetDialogOpen when inSessionView becomes true. The existing onLearnClick open-guard at App.tsx:396-399 is PRESERVED as defense in depth; onResetClick gains no symmetric guard."
    - "D-10: MuteToggle has a required resumeHintId: string prop and renders aria-describedby={resumeHintId} ONLY when needsResume is true; aria-pressed behavior is unchanged."
    - "D-10, D-11: App's sr-only role=status aria-live=polite resume-hint div has id='mute-toggle-resume-hint' and App forwards resumeHintId='mute-toggle-resume-hint' through SessionControls (added to SessionControlsProps) to MuteToggle."
    - "D-13: Single plan, single wave, four task groups — DOMAIN-01, UI-01, UI-02, A11Y-01 ship together. App.tsx is touched by three of the four tasks at non-overlapping line ranges, so tasks serialize cleanly inside this one plan."
    - "D-14: Task ordering inside the plan is fixed — (1) DOMAIN-01 sessionController allowlist throw + test, (2) UI-01 SessionReadout prop + NEW test file + App wiring, (3) UI-02 App useEffect + dialog test, (4) A11Y-01 MuteToggle/SessionControls plumbing + App id + MuteToggle test."
    - "D-15: The new UI-02 useEffect's `// eslint-disable-next-line react-hooks/set-state-in-effect` MUST carry a `// Reason:` annotation describing the subscribe-and-reflect pattern (mirrors EndSessionDialog auto-close at App.tsx:247-253)."
    - "D-16: New contract tests co-locate in existing *.test.{ts,tsx} neighbors (sessionController.test.ts, App.dialog.test.tsx, MuteToggle.test.tsx); src/components/SessionReadout.test.tsx is the structural gap-fill exception (NEW file)."
    - "D-17: Vitest, tsc --noEmit, npm run lint, and npm run build all exit 0 at every commit boundary inside this phase; test count rises from 391 to ~397-400."
    - "D-18: No user-facing behavior change in normal flows (milestone invariant)."
  artifacts:
    - path: "src/domain/sessionController.ts"
      provides: "extendTimedSession allowlist throw between the open-ended throw and the Number.isFinite comparison"
      contains: "DURATION_OPTIONS as readonly DurationOption[]"
    - path: "src/domain/sessionController.test.ts"
      provides: "Regression test for the new allowlist throw (D-03)"
      contains: "extendTimedSession(running, 7)"
    - path: "src/components/SessionReadout.tsx"
      provides: "SessionReadoutProps with isLeadInPlaceholder?: boolean and an early-branch render for the placeholder case"
      contains: "isLeadInPlaceholder"
    - path: "src/components/SessionReadout.test.tsx"
      provides: "NEW file — 3-4 cases locking the lead-in placeholder contract (D-06)"
      contains: "SessionReadout"
    - path: "src/components/MuteToggle.tsx"
      provides: "MuteToggleProps with required resumeHintId: string; aria-describedby conditional on needsResume"
      contains: "aria-describedby"
    - path: "src/components/MuteToggle.test.tsx"
      provides: "Updated renderToggle helper + 2 new describedby cases (D-12)"
      contains: "aria-describedby"
    - path: "src/components/SessionControls.tsx"
      provides: "SessionControlsProps with resumeHintId?: string; forwarded to MuteToggle"
      contains: "resumeHintId"
    - path: "src/app/App.tsx"
      provides: "Subscribe-and-reflect effect on [inSessionView] + SessionReadout wiring + sr-only id + SessionControls resumeHintId"
      contains: "id=\"mute-toggle-resume-hint\""
    - path: "src/app/App.dialog.test.tsx"
      provides: "2 new WR-09 auto-close cases mirroring the WR-01 template (D-09)"
      contains: "WR-09"
  key_links:
    - from: "src/domain/sessionController.ts"
      to: "src/domain/settings.ts"
      via: "import of DURATION_OPTIONS + DurationOption"
      pattern: "from './settings'"
    - from: "src/app/App.tsx"
      to: "src/components/SessionReadout.tsx"
      via: "isLeadInPlaceholder JSX prop at the <SessionReadout> callsite"
      pattern: "isLeadInPlaceholder"
    - from: "src/app/App.tsx"
      to: "src/components/SessionControls.tsx"
      via: "resumeHintId='mute-toggle-resume-hint' JSX prop"
      pattern: "resumeHintId"
    - from: "src/components/SessionControls.tsx"
      to: "src/components/MuteToggle.tsx"
      via: "resumeHintId forward at the <MuteToggle> callsite inside SessionControls"
      pattern: "resumeHintId"
    - from: "src/app/App.tsx"
      to: "(self) sr-only resume-hint div"
      via: "id='mute-toggle-resume-hint' attribute — aria-describedby target"
      pattern: "id=\"mute-toggle-resume-hint\""
---

<objective>
Tighten four boundary contracts so invalid inputs throw at the domain boundary, dialog state cannot drift between session views, and `MuteToggle` carries semantically correct attributes when the audio engine needs resume. Fix-only patch — no user-facing behavior changes (milestone invariant per D-18).

Purpose: Close WR-01 (DOMAIN-01), WR-08 (UI-01), WR-09 (UI-02), and IN-06 (A11Y-01) from REVIEW.md. Per D-13 these ship in a single plan, single wave, four ordered task groups. App.tsx is touched by three of the four tasks at non-overlapping line ranges, so the tasks serialize cleanly inside this one plan.

Output: Four atomic commits, each green on `tsc --noEmit`, `npm run lint`, `npm run build`, and the full Vitest suite. Test count rises from 391 baseline to ~397-400.
</objective>

<execution_context>
@$HOME/.claude-personal/get-shit-done/workflows/execute-plan.md
@$HOME/.claude-personal/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/11-domain-ui-contracts-accessibility/11-CONTEXT.md
@.planning/phases/11-domain-ui-contracts-accessibility/11-PATTERNS.md

<interfaces>
<!-- Existing contracts the executor will consume or extend. Extracted from codebase. -->

From src/domain/settings.ts:
- export type DurationOption — discriminated union; literal-number members are 5 | 10 | 15 | 20 | 25 | 30 | 35 | 40 | 45 | 50 | 55 | 60 plus the literal 'open-ended'.
- export const DURATION_OPTIONS — readonly tuple of DurationOption members, declared with `as const satisfies readonly DurationOption[]`.
- Existing allowlist throw pattern inside validateSettings: `if (!(DURATION_OPTIONS as readonly DurationOption[]).includes(settings.durationMinutes)) { throw new RangeError(...) }`.

From src/domain/sessionController.ts (lines 59-69, current):
- export function extendTimedSession(state: RunningSessionState, durationMinutes: number): RunningSessionState
- Current body order: (1) open-ended throw, (2) `Number.isFinite(durationMinutes) || durationMinutes <= state.lockedSettings.durationMinutes` throw.
- Existing import at line 5: `import type { SessionSettings } from './settings'` — must be widened to also import `DurationOption` (type) and `DURATION_OPTIONS` (value).

From src/components/SessionReadout.tsx (lines 5-9, current):
```
export interface SessionReadoutProps {
  frame: SessionFrame | null
  status: SessionStatus
  message?: 'Session complete'
}
```
- Component head destructures `{ frame, status, message }`.
- Existing early-return: `if (status === 'idle' && frame === null && message === undefined) return null`.
- Existing chip gate: `const showTimeChip = status !== 'complete' && frame !== null`.

From src/components/MuteToggle.tsx (lines 10-37, current):
```
export interface MuteToggleProps {
  muted: boolean
  audioAvailable: boolean
  needsResume?: boolean
  onToggle(this: void): void
}
```
- Existing conditional-attribute template at line 37: `aria-pressed={needsResume ? undefined : muted}`.
- Destructuring at line 20: `{ muted, audioAvailable, needsResume, onToggle }`.

From src/components/SessionControls.tsx (lines 4-21 + 68-73, current):
```
export interface SessionControlsProps {
  status: SessionStatus
  onStart(this: void): void
  onEnd(this: void): void
  muted?: boolean
  audioAvailable?: boolean
  needsResume?: boolean
  onMuteToggle?(this: void): void
}
```
- Existing MuteToggle render forwards `muted`, `audioAvailable`, `needsResume`, `onMuteToggle`. resumeHintId must be piggybacked on the same forward.

From src/app/App.tsx:
- Line 142: `const inSessionView = appPhase !== 'idle'` — the dep for the UI-02 effect.
- Lines 247-253: existing EndSessionDialog subscribe-and-reflect useEffect — verbatim template for the UI-02 new effect.
- Lines 396-399: existing `onLearnClick` open-guard — PRESERVED, not edited.
- Lines 576-585: existing `<SessionReadout>` callsite with `status={appPhase === 'lead-in' ? 'idle' : state.status}` hack — replaced.
- Lines 592-600: existing `<SessionControls>` callsite — `resumeHintId` prop is appended.
- Lines 605-611: existing sr-only `role="status" aria-live="polite"` div — `id` is appended.
- Existing state setters used: `setLearnDialogOpen`, `setResetDialogOpen` (both declared in the App body, near the dialog open/close handlers).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: DOMAIN-01 — Allowlist throw in extendTimedSession + regression test</name>
  <files>src/domain/sessionController.ts, src/domain/sessionController.test.ts</files>
  <read_first>
    - src/domain/sessionController.ts (full file; the only edit lands inside `extendTimedSession`)
    - src/domain/settings.ts (lines 1-65) — confirm `DurationOption` and `DURATION_OPTIONS` exports and re-read the analog allowlist throw inside `validateSettings`
    - src/domain/sessionController.test.ts (full file; describe block for `extendTimedSession` is around lines 79-94)
    - .planning/phases/11-domain-ui-contracts-accessibility/11-PATTERNS.md (the `src/domain/sessionController.ts — DOMAIN-01 allowlist throw` section and the `src/domain/sessionController.test.ts — DOMAIN-01 new test case` section)
  </read_first>
  <action>
    Implement DOMAIN-01 per D-01, D-02, D-03 in `src/domain/sessionController.ts`. Widen the existing `import type { SessionSettings } from './settings'` to also bring in the `DurationOption` type AND the `DURATION_OPTIONS` value (the value import must NOT be `import type`). Inside `extendTimedSession(state, durationMinutes)` insert a NEW guard AFTER the existing `state.lockedSettings.durationMinutes === 'open-ended'` throw and BEFORE the existing `Number.isFinite(...) || ... <= ...` throw. The new guard uses the verbatim allowlist-cast pattern from `validateSettings` in `settings.ts`: `if (!(DURATION_OPTIONS as readonly DurationOption[]).includes(durationMinutes)) throw new RangeError('durationMinutes must be one of DURATION_OPTIONS')`. Preserve the same `RangeError` class — do NOT introduce a subclass (deferred item per CONTEXT). Do NOT narrow the parameter type from `number` to `DurationOption` (per D-01 rationale — keeps caller boundary diff minimal). Preserve the parameter name `durationMinutes` and the function signature exactly.

    In `src/domain/sessionController.test.ts`, EXTEND the existing `extendTimedSession` describe block (around lines 79-94). Preserve every existing case verbatim. Add exactly ONE new `it(...)` case asserting `expect(() => extendTimedSession(running, 7)).toThrow(RangeError)` where `running = startSession({ ...baseSettings, durationMinutes: 10 }, 0)` (7 is finite but not in DURATION_OPTIONS). Mirror the existing case's setup style. Co-locate per D-03 / D-16 — do NOT create a new test file.

    Commit this task independently with a descriptive message such as `fix(domain): allowlist-throw extendTimedSession (DOMAIN-01)` before moving to Task 2. Verify the per-commit green-gate (D-17) BEFORE committing.
  </action>
  <verify>
    <automated>npx vitest run src/domain/sessionController.test.ts --reporter=basic</automated>
    Also run, in order, BEFORE the commit: `npx tsc --noEmit`, `npm run lint`, `npm run build`, and the full `npm test` — each must exit 0 per D-17.
  </verify>
  <acceptance_criteria>
    - `grep -n "DURATION_OPTIONS as readonly DurationOption\[\]" src/domain/sessionController.ts` returns exactly one new match inside `extendTimedSession`.
    - `grep -n "extendTimedSession" src/domain/sessionController.ts` shows the allowlist guard sits between the open-ended throw and the `Number.isFinite` check (visual inspection of line ordering).
    - `extendTimedSession(running, 7)` throws `RangeError` (asserted by the new test case).
    - All four pre-existing cases inside the `extendTimedSession` describe block (`15→15`, `15→10`, `Infinity`, `'open-ended'`) still pass without modification.
    - The new `import` line in sessionController.ts brings in both `DurationOption` (as a type) and `DURATION_OPTIONS` (as a value) from `./settings`.
    - `npx tsc --noEmit` exits 0.
    - `npm run lint` exits 0.
    - `npm run build` exits 0.
    - `npm test` exits 0 with test count >= 392 (baseline 391 + 1 new case).
    - No user-facing behavior change: the only delta is which `RangeError` message a previously-failing call surfaces (per D-18).
  </acceptance_criteria>
  <done>
    DOMAIN-01 committed: `extendTimedSession` rejects non-allowlist finite numerics at the boundary; new regression test locks the throw; all four green-gate commands exit 0.
  </done>
</task>

<task type="auto">
  <name>Task 2: UI-01 — SessionReadout isLeadInPlaceholder prop + new test file + App wiring</name>
  <files>src/components/SessionReadout.tsx, src/components/SessionReadout.test.tsx, src/app/App.tsx</files>
  <read_first>
    - src/components/SessionReadout.tsx (full file — props interface at lines 5-9 + component head at lines 11-50)
    - src/components/MuteToggle.test.tsx (full file — used as the renderHelper analog for the NEW test file)
    - src/app/App.tsx (lines 576-585 — the `<SessionReadout>` callsite; lines 1-40 for the imports block sanity check)
    - .planning/phases/11-domain-ui-contracts-accessibility/11-PATTERNS.md (sections `SessionReadout.tsx — UI-01 isLeadInPlaceholder prop`, `SessionReadout.test.tsx (NEW) — UI-01 tests`, `App.tsx — UI-01 SessionReadout wiring`)
    - .planning/phases/11-domain-ui-contracts-accessibility/11-CONTEXT.md decisions D-04, D-05, D-06
  </read_first>
  <action>
    In `src/components/SessionReadout.tsx` add a NEW optional prop `isLeadInPlaceholder?: boolean` to `SessionReadoutProps`. Include the JSDoc block specified in PATTERNS.md (UI-01 / WR-08 — caller commits to non-null `frame`; component renders the timer chip and ignores `status` / `message`). Update the destructuring at the component head to extract `isLeadInPlaceholder`. Insert an EARLY-branch render BEFORE the existing `status === 'idle' && frame === null && message === undefined` early-return: when `isLeadInPlaceholder` is truthy, render the same `<section aria-label="Session readout">` shell that the non-placeholder branch already uses for the chip-only case, with the inner `<div role="status" aria-label="Session announcement" aria-live="polite" aria-atomic="true" />` headline placeholder followed by the timer chip (label `'Remaining'` when `frame?.remainingMs !== null`, otherwise `'Elapsed'`; value formatted via the existing `formatDuration` helper from `frame.remainingMs ?? frame.elapsedMs`; fallback `'0:00'` when `frame` is null). Use the existing classNames verbatim from the current chip rendering path. Do NOT alter the existing post-branch code path (it still owns the non-placeholder render including the `showTimeChip` gate).

    Create NEW file `src/components/SessionReadout.test.tsx`. Mirror `MuteToggle.test.tsx` for the import block (`'@testing-library/jest-dom/vitest'`, `{ render, screen }` from `@testing-library/react`, `{ describe, expect, it }` from `vitest`, and the typed component+props import). Define a `renderReadout(props: Partial<SessionReadoutProps> = {})` helper with sane defaults (`frame: null`, `status: 'idle'`, `message: undefined`, `isLeadInPlaceholder: undefined`). Define a `sampleFrame` constant matching the `SessionFrame` shape used by `SessionReadout` (non-null `remainingMs: 600_000` → renders as `10:00`, `elapsedMs: 0`, `phaseLabel: 'In'`, `phaseProgress: 0`, `cycleIndex: 0`, `isComplete: false`). Add the FOUR cases in D-06 verbatim in intent: (1) `isLeadInPlaceholder=true` + non-null frame → assert both `'Remaining'` and `'10:00'` are in the document; (2) `isLeadInPlaceholder=true` + status `'complete'` + non-null frame + message `'Session complete'` → assert `'Remaining'` and `'10:00'` are still in the document AND `'Session complete'` is NOT in the document (placeholder beats the headline); (3) `isLeadInPlaceholder=false` + status `'complete'` + non-null frame + message `'Session complete'` → assert `'Session complete'` is in the document AND `'Remaining'` is NOT (the existing `showTimeChip` regression lock); (4) `isLeadInPlaceholder` absent + status `'idle'` + null frame + no message → assert `container.firstChild` is `null` (existing early-return preserved).

    In `src/app/App.tsx` at the `<SessionReadout>` callsite (currently lines 576-585), DELETE the comment block at lines 578-582 that explains the `status` override hack AND DELETE the `status={appPhase === 'lead-in' ? 'idle' : state.status}` line. REPLACE with `status={state.status}` and ADD `isLeadInPlaceholder={appPhase === 'lead-in'}` as a sibling prop. The `frame={leadInPlaceholderFrame ?? session.liveFrame}` and `message={...}` lines are PRESERVED verbatim.

    Commit this task independently with a message such as `fix(ui): explicit lead-in placeholder contract on SessionReadout (UI-01)` after verifying the per-commit green-gate.
  </action>
  <verify>
    <automated>npx vitest run src/components/SessionReadout.test.tsx src/app/App.dialog.test.tsx --reporter=basic</automated>
    Run BEFORE commit: `npx tsc --noEmit`, `npm run lint`, `npm run build`, full `npm test`. Each must exit 0.
  </verify>
  <acceptance_criteria>
    - `grep -n "isLeadInPlaceholder" src/components/SessionReadout.tsx` shows the prop in `SessionReadoutProps`, in the function destructuring, and in the early-branch conditional.
    - `src/components/SessionReadout.test.tsx` exists with four `it(...)` cases matching the D-06 intents.
    - `grep -n "isLeadInPlaceholder={appPhase === 'lead-in'}" src/app/App.tsx` returns exactly one match inside the `<SessionReadout>` callsite.
    - `grep -n "appPhase === 'lead-in' ? 'idle' : state.status" src/app/App.tsx` returns zero matches (the override hack is removed).
    - `grep -n "status={state.status}" src/app/App.tsx` shows the simplified line replacing the hack.
    - All four new SessionReadout test cases pass.
    - Visual sanity: a manual mental walk-through confirms that in the normal happy-path render (lead-in → running → complete), the rendered output is IDENTICAL to the pre-fix output (per D-18 — placeholder branch produces same chip as the prior override).
    - `npx tsc --noEmit`, `npm run lint`, `npm run build`, `npm test` each exit 0.
    - Test count >= 395 (Task 1's 392 + 3-4 new cases from the new file).
  </acceptance_criteria>
  <done>
    UI-01 committed: SessionReadout owns an explicit `isLeadInPlaceholder` contract; new test file locks the four-case behavior matrix; App passes `state.status` verbatim and reflects lead-in via the dedicated prop; all four green-gate commands exit 0.
  </done>
</task>

<task type="auto">
  <name>Task 3: UI-02 — App-level subscribe-and-reflect effect closing Learn/Reset dialogs on inSessionView transition</name>
  <files>src/app/App.tsx, src/app/App.dialog.test.tsx</files>
  <read_first>
    - src/app/App.tsx lines 240-253 (the verbatim EndSessionDialog subscribe-and-reflect template); lines 130-150 (confirm the `inSessionView` declaration at line 142); the surrounding context where `setLearnDialogOpen` and `setResetDialogOpen` are declared (search for their `useState` call sites)
    - src/app/App.tsx lines 396-399 (the existing `onLearnClick` open-guard — PRESERVED, not edited)
    - src/app/App.dialog.test.tsx (full file — the WR-01 auto-close template lives at the `auto-closes the modal when the session completes underneath it` case near line 259; helper `startAndAdvancePastLeadIn` is defined earlier in the same file)
    - .planning/phases/11-domain-ui-contracts-accessibility/11-PATTERNS.md (sections `App.tsx — UI-02 new useEffect` and `App.dialog.test.tsx — UI-02 WR-09 auto-close cases`)
    - .planning/phases/11-domain-ui-contracts-accessibility/11-CONTEXT.md decisions D-07, D-08, D-09, D-15
  </read_first>
  <action>
    In `src/app/App.tsx`, insert a NEW `useEffect` block IMMEDIATELY AFTER the existing EndSessionDialog subscribe-and-reflect effect (i.e. after the closing `}, [state.status, endDialogOpen])` line — currently line 253). The new effect:
    - Header comment block explains WR-09 + the rationale: when `inSessionView` becomes true, force-close both `learnDialogOpen` and `resetDialogOpen` to satisfy WR-09's "cannot remain open" requirement under any race; the `onLearnClick` open-guard at lines 396-399 is the first line of defense, this effect is the second.
    - Effect body: `if (inSessionView) { setLearnDialogOpen(false); setResetDialogOpen(false); }`.
    - Each setState call MUST be preceded by its own `// Reason: subscribe-and-reflect — dialog visibility mirrors external inSessionView; setting local state from this trigger effect is the documented React pattern, identical posture to the EndSessionDialog auto-close at App.tsx:247-253 (WR-01).` line AND `// eslint-disable-next-line react-hooks/set-state-in-effect` per D-15.
    - Deps: `[inSessionView]` — only the external trigger signal.
    - Do NOT add an `inSessionView` early-return to `onResetClick` (per D-08 — Reset button is unreachable mid-session via the hidden StatsFooter; the reactive close is the second-line defense). Do NOT touch the existing `onLearnClick` open-guard at lines 396-399.

    In `src/app/App.dialog.test.tsx`, append a NEW top-level `describe('WR-09 in-session dialog auto-close', ...)` block AFTER the existing `'end-session confirmation modal (App integration)'` describe ends. Use the same `vi.useFakeTimers()` / `vi.setSystemTime(...)` / `vi.useRealTimers()` setup/teardown as the existing WR-01 block. Add exactly TWO `it(...)` cases:
    1. `auto-closes LearnDialog when the session starts underneath it (WR-09)` — `render(<App />)`, click the Learn-anchor button (planner: verify the accessible name by inspecting App.tsx JSX — current expectation is the corner Learn anchor labeled `Learn`; if the actual name differs, use the actual accessible name), assert `screen.getByRole('dialog')` is visible, call `await startAndAdvancePastLeadIn()`, then assert `screen.queryByRole('dialog')` is NOT in the document.
    2. `auto-closes ResetStatsDialog when the session starts underneath it (WR-09)` — same pattern but click the Reset-stats trigger button first; same final assertion.

    Mirror the WR-01 case's setup, timer setup/teardown, and assertion shape verbatim. Co-locate per D-09 / D-16. Adjust the accessible-name selectors to match the actual JSX (PATTERNS.md flagged `{ name: /learn/i }` and `{ name: /reset/i }` as indicative — verify against the real labels and use the canonical match).

    Commit this task independently with a message such as `fix(ui): auto-close Learn/Reset dialogs on in-session transition (UI-02)` after verifying the per-commit green-gate.
  </action>
  <verify>
    <automated>npx vitest run src/app/App.dialog.test.tsx --reporter=basic</automated>
    Run BEFORE commit: `npx tsc --noEmit`, `npm run lint` (must exit 0 with the new `// Reason:` annotation and disable directive intact), `npm run build`, full `npm test`. Each must exit 0.
  </verify>
  <acceptance_criteria>
    - `grep -c "useEffect" src/app/App.tsx` increases by exactly 1 vs the pre-Task-3 state.
    - `grep -n "setLearnDialogOpen(false)" src/app/App.tsx` shows at least one match inside the new effect body.
    - `grep -n "setResetDialogOpen(false)" src/app/App.tsx` shows at least one match inside the new effect body.
    - `grep -n "react-hooks/set-state-in-effect" src/app/App.tsx` shows two new annotated-disable lines inside the new effect (one per setState), each preceded by a `// Reason: subscribe-and-reflect — ...` comment per D-15.
    - `grep -n "if (inSessionView)" src/app/App.tsx` shows the new effect's body condition is `if (inSessionView)` (matching D-07 verbatim).
    - The new effect's deps array is `[inSessionView]` (visual inspection — single dep, matches the trigger signal).
    - `onLearnClick` open-guard at lines 396-399 is byte-identical to the pre-task state (no edit).
    - The two new WR-09 test cases pass.
    - Existing WR-01 auto-close test at the prior line 259 still passes (no regression).
    - `npx tsc --noEmit`, `npm run lint`, `npm run build`, `npm test` each exit 0.
    - Test count >= 397 (Task 2's 395 + 2 new cases).
    - Manual visual UAT: open Learn modal, click Start — the modal should disappear when the session view appears (lead-in starts). Same for Reset (though Reset trigger is normally unreachable mid-session).
  </acceptance_criteria>
  <done>
    UI-02 committed: new App-level effect on `[inSessionView]` force-closes both dialogs; two regression tests lock the WR-09 contract; `// Reason:` annotation and eslint disable directive follow the D-15 policy; all four green-gate commands exit 0.
  </done>
</task>

<task type="auto">
  <name>Task 4: A11Y-01 — MuteToggle resumeHintId prop + SessionControls plumbing + App id wiring + tests</name>
  <files>src/components/MuteToggle.tsx, src/components/MuteToggle.test.tsx, src/components/SessionControls.tsx, src/app/App.tsx</files>
  <read_first>
    - src/components/MuteToggle.tsx (full file — props interface at lines 10-18; conditional-attribute template at line 37 `aria-pressed={needsResume ? undefined : muted}`)
    - src/components/MuteToggle.test.tsx (full file — `renderToggle` helper at lines 8-19 and the `needsResume` describe block around lines 111-141)
    - src/components/SessionControls.tsx (full file — props interface at lines 4-21 and the `<MuteToggle>` render at lines 68-73)
    - src/app/App.tsx lines 592-600 (the `<SessionControls>` callsite) and lines 605-611 (the sr-only `role="status" aria-live="polite"` resume-hint div)
    - .planning/phases/11-domain-ui-contracts-accessibility/11-PATTERNS.md (sections `MuteToggle.tsx — A11Y-01 resumeHintId prop`, `MuteToggle.test.tsx — A11Y-01 conditional describedby cases`, `SessionControls.tsx — A11Y-01 resumeHintId plumbing`, `App.tsx — A11Y-01 sr-only region id`, `App.tsx — A11Y-01 SessionControls resumeHintId prop`)
    - .planning/phases/11-domain-ui-contracts-accessibility/11-CONTEXT.md decisions D-10, D-11, D-12
  </read_first>
  <action>
    In `src/components/MuteToggle.tsx`:
    - Add a NEW REQUIRED prop `resumeHintId: string` to `MuteToggleProps` with the JSDoc block specified in PATTERNS.md (A11Y-01 — id of the App-level aria-live resume-hint region; aria-describedby is set only when `needsResume` is true).
    - Update the destructuring at line 20 to extract `resumeHintId` alongside the existing props.
    - Adjacent to the existing `aria-pressed={needsResume ? undefined : muted}` line (line 37), add `aria-describedby={needsResume ? resumeHintId : undefined}`. Use the verbatim conditional-attribute ternary pattern.
    - PRESERVE the existing `aria-pressed={needsResume ? undefined : muted}` line exactly as-is — Plan 06 D-32 already locked it correctly, NOT a Phase 11 delta (D-11).

    In `src/components/SessionControls.tsx`:
    - Add a NEW OPTIONAL prop `resumeHintId?: string` to `SessionControlsProps` with a JSDoc comment noting it's forwarded verbatim to MuteToggle.resumeHintId (A11Y-01; App owns the id string).
    - Add `resumeHintId` to the destructuring.
    - In the existing `<MuteToggle>` JSX (around lines 68-73), forward the prop as `resumeHintId={resumeHintId ?? ''}` (the empty-string fallback is safe because MuteToggle only consumes the value when `needsResume` is true, and App always passes both alongside).

    In `src/app/App.tsx`:
    - At the `<SessionControls>` callsite (currently lines 592-600), ADD a new prop `resumeHintId="mute-toggle-resume-hint"` as a sibling to the existing props. Do NOT change any other prop.
    - At the sr-only resume-hint `<div>` (currently lines 605-611), ADD an `id="mute-toggle-resume-hint"` attribute on the same element as the existing `role="status"`, `aria-live="polite"`, and `className="sr-only"`. Do NOT change the children text expression `{audio.audioStatus === 'needs-resume' ? 'Audio paused, tap to resume' : ''}` or any other attribute.

    In `src/components/MuteToggle.test.tsx`:
    - Update the existing `renderToggle` helper to pass `resumeHintId={props.resumeHintId ?? 'mute-toggle-resume-hint'}` (the prop is now required, so the test helper must provide a default to keep existing cases green without modification).
    - Append exactly TWO new `it(...)` cases per D-12:
      1. `needsResume=true → button has aria-describedby set to resumeHintId` — `renderToggle({ needsResume: true, resumeHintId: 'x', audioAvailable: true })`; query `getByRole('button', { name: 'Resume audio' })`; assert `toHaveAttribute('aria-describedby', 'x')`.
      2. `needsResume=false → button has NO aria-describedby attribute` — `renderToggle({ needsResume: false, resumeHintId: 'x', muted: false, audioAvailable: true })`; query `getByRole('button', { name: 'Mute audio cues' })`; assert `not.toHaveAttribute('aria-describedby')`.
    - PRESERVE every existing `MuteToggle.test.tsx` case verbatim; the renderToggle helper update is the only existing-case change.

    Commit this task independently with a message such as `fix(a11y): conditional aria-describedby on MuteToggle resume mode (A11Y-01)` after verifying the per-commit green-gate.
  </action>
  <verify>
    <automated>npx vitest run src/components/MuteToggle.test.tsx src/components/SessionControls.test.tsx --reporter=basic</automated>
    Run BEFORE commit: `npx tsc --noEmit`, `npm run lint`, `npm run build`, full `npm test`. Each must exit 0. The full Vitest suite must include the new SessionReadout test file AND the MuteToggle additions; final count target ~398-400.
  </verify>
  <acceptance_criteria>
    - `grep -n "resumeHintId" src/components/MuteToggle.tsx` shows the prop in the interface, in the destructuring, and in the new `aria-describedby` ternary.
    - `grep -n "aria-describedby={needsResume ? resumeHintId : undefined}" src/components/MuteToggle.tsx` returns exactly one match adjacent to the existing `aria-pressed` line.
    - `grep -n "aria-pressed={needsResume ? undefined : muted}" src/components/MuteToggle.tsx` still returns exactly one match (unchanged from pre-task).
    - `grep -n "resumeHintId" src/components/SessionControls.tsx` shows the prop in the interface, destructuring, and `<MuteToggle>` forward with `?? ''` fallback.
    - `grep -n 'resumeHintId="mute-toggle-resume-hint"' src/app/App.tsx` returns exactly one match inside the `<SessionControls>` callsite.
    - `grep -n 'id="mute-toggle-resume-hint"' src/app/App.tsx` returns exactly one match on the sr-only resume-hint `<div>`.
    - The two new MuteToggle test cases pass; all pre-existing MuteToggle test cases also pass (renderToggle helper update preserved them).
    - The two new App.dialog tests from Task 3 still pass (no cross-task regression).
    - SessionControls test file (if one exists) still passes — verify by `npx vitest run src/components/SessionControls.test.tsx` if the file exists; otherwise the SessionControls plumbing is exercised by App-level tests.
    - `npx tsc --noEmit`, `npm run lint`, `npm run build`, `npm test` each exit 0.
    - Final test count: 398-400 (Task 3's 397 + 2 new MuteToggle cases).
    - Manual visual UAT: no sighted-user visible change in the normal flow (per D-18 — `aria-describedby` is screen-reader-only). Screen-reader probe (optional): focus the MuteToggle button in needs-resume mode and confirm the announcement includes the hint text from the sr-only region.
  </acceptance_criteria>
  <done>
    A11Y-01 committed: MuteToggle has a required `resumeHintId` prop and conditional `aria-describedby`; SessionControls plumbs the prop through; App owns the id string and forwards it; MuteToggle test extensions lock the two-state behavior; all four green-gate commands exit 0; full Phase 11 lands at ~398-400 Vitest cases with all four REQ-IDs (DOMAIN-01, UI-01, UI-02, A11Y-01) complete.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| In-app caller → `extendTimedSession` | Component-layer code passes a `number` across the domain boundary into `extendTimedSession`; today an invalid value discovers failure deep inside `createBreathingPlan`. Post-fix the throw lands at the boundary itself (DOMAIN-01). |
| App state → React prop (UI-01) | `appPhase === 'lead-in'` is a typed boolean prop passed to a child component; no untrusted data crosses. |
| App state → React effect (UI-02) | `inSessionView` is an internal React-derived boolean; local-only state mutation reacts to it. |
| App DOM id → child component aria-describedby (A11Y-01) | A static string id is plumbed through React props. No user input, no DOM injection. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-01 | Tampering | `extendTimedSession` accepting an out-of-allowlist finite `durationMinutes` (DOMAIN-01) | none / informational | Local-only fix-only patch with no untrusted input source. The new throw at the boundary is itself a defensive narrowing that REDUCES (not introduces) attack surface. ASVS L1 has no requirement implicated; logged for completeness. |
| T-11-02 | Information Disclosure | DOM render of `SessionReadout` props (UI-01) | none | Component renders typed React props (string label, number-formatted duration). No user-controlled text reaches the DOM. No XSS vector introduced. |
| T-11-03 | Denial of Service | Effect-driven state mutation on `[inSessionView]` (UI-02) | low | Effect fires only on a single boolean transition; React reconciler suppresses no-op setStates. No unbounded loop possible (deps array is a single primitive boolean; no derived object identity churn). |
| T-11-04 | Spoofing | `aria-describedby` referencing a static id (A11Y-01) | none | Static string `"mute-toggle-resume-hint"` plumbed through typed React props. Not user-controllable. ARIA references are read by assistive tech, not parsed as URLs or code. |
| T-11-05 | Repudiation / Auditing | Per-commit green-gate (tsc/lint/build/vitest) per D-17 | mitigate | Acceptance criteria of every task require the four-gate to exit 0 BEFORE the commit lands; broken state cannot be committed (rolled back, not patched-forward). |

Overall: ASVS L1 threat surface for this fix-only patch is minimal. No `high` severity threats. Block-on-high gate is satisfied.
</threat_model>

<verification>
## Phase-level verification (all four tasks complete)

1. **Per-commit green-gate (D-17, applied at every task commit boundary):**
   - `npx tsc --noEmit` exits 0
   - `npm run lint` exits 0
   - `npm run build` exits 0
   - `npm test` exits 0; test count rises monotonically (~391 → ~392 → ~395 → ~397 → ~398-400)

2. **Roadmap Phase 11 Success Criteria 1..5:**
   1. `grep -n "DURATION_OPTIONS as readonly DurationOption\[\]" src/domain/sessionController.ts` matches inside `extendTimedSession`. ✓ DOMAIN-01
   2. `grep -n "isLeadInPlaceholder" src/components/SessionReadout.tsx` matches the prop, destructure, and conditional render. ✓ UI-01
   3. `grep -nA 6 "if (inSessionView)" src/app/App.tsx` shows a new effect with `setLearnDialogOpen(false)` and `setResetDialogOpen(false)` and dep `[inSessionView]`. ✓ UI-02
   4. `grep -n "aria-describedby={needsResume ? resumeHintId : undefined}" src/components/MuteToggle.tsx` matches; `grep -n 'id="mute-toggle-resume-hint"' src/app/App.tsx` matches; `grep -n "aria-pressed={needsResume ? undefined : muted}" src/components/MuteToggle.tsx` still matches (preserved). ✓ A11Y-01
   5. Full Vitest suite passes with new cases for DOMAIN-01 (1), UI-01 (3-4), UI-02 (2), A11Y-01 (2). ✓

3. **Milestone invariant (D-18):** Manual UAT walk-through confirms no user-facing behavior change in the normal happy-path flow. The four touched user-facing surfaces (extend-duration error message, lead-in placeholder rendering, dialog visibility during normal session start, MuteToggle visual appearance) render identically before and after the patch.
</verification>

<success_criteria>
- All four ROADMAP Phase 11 Success Criteria 1..5 are TRUE (see `<verification>` above).
- All four requirement IDs (DOMAIN-01, UI-01, UI-02, A11Y-01) have at least one new locking Vitest case.
- Vitest baseline 391 climbs to 397-400; `tsc --noEmit`, `npm run lint`, `npm run build`, `npm test` exit 0 at every task commit boundary.
- No new files outside the four task `files` declarations; only `src/components/SessionReadout.test.tsx` is created NEW (structural gap-fill per D-16 exception).
- Every new `// eslint-disable-next-line react-hooks/*` carries a `// Reason:` annotation per D-15.
- No user-facing behavior change in normal flows (D-18).
</success_criteria>

<output>
After completion, create `.planning/phases/11-domain-ui-contracts-accessibility/11-01-SUMMARY.md` summarizing: requirement IDs closed (DOMAIN-01, UI-01, UI-02, A11Y-01), commit shas, final test count, any deviations from the plan, and a note that the phase is ready for `/gsd-verify-phase 11`.
</output>
