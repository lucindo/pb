---
phase: 09-audio-wake-lock-lifecycle-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/audio/audioEngine.ts
  - src/audio/audioEngine.test.ts
  - src/audio/cueSynth.ts
  - src/audio/cueSynth.test.ts
  - src/hooks/useWakeLock.ts
  - src/hooks/useWakeLock.test.tsx
autonomous: true
requirements:
  - AUDIO-02
  - AUDIO-03
  - AUDIO-04
  - AUDIO-06
  - WAKELOCK-01

must_haves:
  truths:
    - "audioEngine.scheduleNextCue clamps any audioTime < audioCtx.currentTime + SAFE_LEAD_SEC to that value before forwarding to cueSynth (engine self-defends against past-time schedules). Implements D-01 (engine-side / callee clamp site, the engine half of belt-and-suspenders) and D-02 (Math.max formula, applied BEFORE any further engine math; no subtract or transform after)."
    - "audioEngine exports the named constant SAFE_LEAD_SEC === 0.005 from src/audio/audioEngine.ts — single source of truth for both engine-side and caller-side clamps. Implements D-03 (named export, 5 ms, no duplicated literals; App.tsx imports the symbol for the caller-side clamp delivered in Plan 02)."
    - "audioEngine.scheduleLeadIn returns null when the engine is in the closed state (instead of a meaningless projected time)."
    - "The AudioStatus union exported from src/audio/audioEngine.ts is exactly 'idle' | 'lead-in' | 'failed' — the 'starting' literal is removed. Implements D-07 (union narrowing at audioEngine.ts:25) and D-10 (Vitest / TypeScript-level lock so any future setStatus('starting') reintroduction fails to compile or fails a test)."
    - "cueSynth.scheduleBowlCue attaches { once: true } 'ended' listeners on every partial oscillator that disconnect that osc + its partialGain, AND disconnect the shared filter + envelope after the last partial ends."
    - "useWakeLock.request() is gated by a requestInFlightRef so a second concurrent caller no-ops; a release()/unmount that ran during the pending request() causes the post-await sentinel to be released-and-discarded rather than stored."
    - "Plan packaging follows D-11 (two plans, two waves; Wave 1 = engine-layer plan: engine self-protections + cueSynth cleanup + wake-lock fix) and D-12 (Plan 01 ships AUDIO-02 callee-side clamp + SAFE_LEAD_SEC export, AUDIO-03 scheduleLeadIn null when closed, AUDIO-04 cueSynth osc-onended partial-chain disconnect, AUDIO-06 AudioStatus union tightening, WAKELOCK-01 request in-flight ref + release-during-await cleanup; files modified: src/audio/audioEngine.ts, src/audio/audioEngine.test.ts, src/audio/cueSynth.ts, src/audio/cueSynth.test.ts, src/hooks/useWakeLock.ts, src/hooks/useWakeLock.test.tsx; no coupling to the hook layer). Test geography follows D-14: new contract assertions co-locate in each file's existing *.test.{ts,tsx} neighbor — no new test files."
  artifacts:
    - path: "src/audio/audioEngine.ts"
      provides: "Named export SAFE_LEAD_SEC = 0.005, callee-side clamp inside scheduleNextCue, scheduleLeadIn returns number | null, tightened AudioStatus union."
      contains: "export const SAFE_LEAD_SEC = 0.005"
    - path: "src/audio/audioEngine.test.ts"
      provides: "Vitest cases locking the engine-side clamp, the SAFE_LEAD_SEC value, and scheduleLeadIn returning null on closed."
      contains: "SAFE_LEAD_SEC"
    - path: "src/audio/cueSynth.ts"
      provides: "AUDIO-04 osc.onended { once: true } chain disconnect inside scheduleBowlCue."
      contains: "{ once: true }"
    - path: "src/audio/cueSynth.test.ts"
      provides: "Vitest case locking single disconnect-per-node on osc 'ended' dispatch."
      contains: "ended"
    - path: "src/hooks/useWakeLock.ts"
      provides: "requestInFlightRef + releaseCalledDuringRequestRef + post-await sentinel handoff cleanup; release() and unmount cleanup both set the release-during-await sentinel signal when a request is in flight."
      contains: "requestInFlightRef"
    - path: "src/hooks/useWakeLock.test.tsx"
      provides: "Vitest cases locking concurrent-request no-op, release-during-await, and unmount-during-await."
      contains: "requestInFlight"
  key_links:
    - from: "src/audio/audioEngine.ts scheduleNextCue"
      to: "audioCtx.currentTime + SAFE_LEAD_SEC"
      via: "Math.max clamp before scheduleInCue/scheduleOutCue invocation"
      pattern: "Math\\.max\\(audioTime, audioCtx\\.currentTime \\+ SAFE_LEAD_SEC\\)"
    - from: "src/audio/cueSynth.ts scheduleBowlCue oscillator partials loop"
      to: "osc.disconnect / partialGain.disconnect / filter.disconnect / envelope.disconnect"
      via: "addEventListener('ended', ..., { once: true })"
      pattern: "addEventListener\\('ended'"
    - from: "src/hooks/useWakeLock.ts request()"
      to: "post-await sentinel.release() orphan path"
      via: "releaseCalledDuringRequestRef.current check after await navigator.wakeLock.request('screen')"
      pattern: "releaseCalledDuringRequestRef"
---

<objective>
Plan 01 lands the Wave 1 engine-layer changes for Phase 9 — the engine-side and pure-builder fixes that ship a tightened API surface independent of any hook/App consumer. This plan delivers:

- AUDIO-02 (engine half): callee-side clamp inside `audioEngine.scheduleNextCue` + new named export `SAFE_LEAD_SEC = 0.005` (D-01, D-02, D-03).
- AUDIO-03 (engine half): `audioEngine.scheduleLeadIn` returns `null` when the engine is `closed`; interface widened from `number` to `number | null`.
- AUDIO-04: `cueSynth.scheduleBowlCue` attaches `{ once: true }` `'ended'` listeners that disconnect partial nodes (osc + partialGain per partial; shared filter + envelope after the last partial).
- AUDIO-06 (engine half): tighten `AudioStatus` union to `'idle' | 'lead-in' | 'failed'` (D-07).
- WAKELOCK-01: `useWakeLock.request()` gains `requestInFlightRef` concurrent-request gate + `releaseCalledDuringRequestRef` release-during-await signal; orphaned sentinel released via spec-idempotent `.release()`.

Purpose: lock the engine/synth/wake-lock API contracts so that Plan 02 (Wave 2, hook+App layer) consumes a stable post-hardening surface. No user-facing behavior change; 366/366 Vitest baseline preserved + new cases added.

Output: 6 modified files; new co-located test cases per D-14; the engine + wake-lock API tightened so the engine self-defends against past-time schedules and concurrent-request races.
</objective>

<execution_context>
@$HOME/.claude-personal-personal/get-shit-done/workflows/execute-plan.md
@$HOME/.claude-personal-personal/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/09-audio-wake-lock-lifecycle-hardening/09-CONTEXT.md
@.planning/phases/09-audio-wake-lock-lifecycle-hardening/09-RESEARCH.md
@.planning/phases/09-audio-wake-lock-lifecycle-hardening/09-VALIDATION.md
@.planning/phases/09-audio-wake-lock-lifecycle-hardening/09-PATTERNS.md
@.planning/phases/07-strict-type-lint-baseline/07-CONTEXT.md
@.planning/phases/08-storage-forward-compat-cross-tab-ui-sync/08-CONTEXT.md

<interfaces>
<!-- Key contracts the executor needs. Extracted directly from RESEARCH.md §Recommended Code Shapes and PATTERNS.md so the executor does not have to re-derive them. -->

From src/audio/audioEngine.ts (current public API — to be modified by this plan):

  export type AudioStatus = 'idle' | 'starting' | 'lead-in' | 'failed'      // BEFORE — remove 'starting' (AUDIO-06 D-07)
  export type AudioStatus = 'idle' | 'lead-in' | 'failed'                   // AFTER

  export const LEAD_IN_TICK_INTERVAL_SEC = 1.0                              // PRECEDENT — model SAFE_LEAD_SEC after this
  export const LEAD_IN_DURATION_SEC = 3.0
  export const LEAD_IN_TICK_INTERVAL_MS = LEAD_IN_TICK_INTERVAL_SEC * 1000
  export const LEAD_IN_DURATION_MS = LEAD_IN_DURATION_SEC * 1000

  export const SAFE_LEAD_SEC = 0.005                                        // NEW (AUDIO-02 D-03)

  // AudioEngine interface — scheduleLeadIn return type widens:
  scheduleLeadIn(startAudioTime: number, plan: BreathingPlan): number       // BEFORE
  scheduleLeadIn(startAudioTime: number, plan: BreathingPlan): number | null  // AFTER (AUDIO-03)

From src/hooks/useWakeLock.ts (existing refs — append two new ones):
  const sentinelRef = useRef<WakeLockSentinel | null>(null)
  const wasAcquiredRef = useRef<boolean>(false)
  const requestInFlightRef = useRef<boolean>(false)              // NEW (WAKELOCK-01)
  const releaseCalledDuringRequestRef = useRef<boolean>(false)   // NEW (WAKELOCK-01)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: AUDIO-02 (engine clamp + SAFE_LEAD_SEC) + AUDIO-03 (scheduleLeadIn null) + AUDIO-06 union tightening in src/audio/audioEngine.ts and its test</name>
  <files>src/audio/audioEngine.ts, src/audio/audioEngine.test.ts</files>

  <read_first>
    - src/audio/audioEngine.ts (read fully — lines 1–250 region — to see current AudioStatus union at L25, scheduleLeadIn body L148–167, scheduleNextCue body L169–180, LEAD_IN_* exports at L77–80, and the closed/muted gates already in use)
    - src/audio/audioEngine.test.ts (read fully — to see the existing scheduleLeadIn happy path at L103–108, scheduleNextCue-after-close case at L246–253, ProbeAC pattern at L255–279)
    - .planning/phases/09-audio-wake-lock-lifecycle-hardening/09-RESEARCH.md §Recommended Code Shapes §B (engine-side clamp), §C (scheduleLeadIn null), §F (AudioStatus tightening)
    - .planning/phases/09-audio-wake-lock-lifecycle-hardening/09-PATTERNS.md §"src/audio/audioEngine.ts" (Patterns E1–E6 — named constant export, AudioStatus union, scheduleNextCue body, scheduleLeadIn body, ProbeAC, SAFE_LEAD_SEC assertion)
    - .planning/phases/09-audio-wake-lock-lifecycle-hardening/09-VALIDATION.md (rows AUDIO-02 engine, AUDIO-02 constant, AUDIO-03 engine, AUDIO-06 union)
    - .planning/phases/09-audio-wake-lock-lifecycle-hardening/09-CONTEXT.md §Decisions (D-01, D-02, D-03, D-07, D-10)
    - .planning/phases/07-strict-type-lint-baseline/07-CONTEXT.md §Decisions D-04, D-09 (Reason: annotation policy + invariants per commit)
  </read_first>

  <behavior>
    - AUDIO-02 engine-side: `scheduleNextCue` clamps `audioTime` to `Math.max(audioTime, audioCtx.currentTime + SAFE_LEAD_SEC)` AFTER `pruneExpiredCues()` and BEFORE invoking `scheduleInCue` / `scheduleOutCue`. The clamped value is forwarded as the 2nd arg (audioTime) to both branches.
    - AUDIO-02 constant: `SAFE_LEAD_SEC` is exported from `src/audio/audioEngine.ts` as the numeric literal `0.005`, placed after the existing `LEAD_IN_*` export block with WR-04-style JSDoc explaining single-source-of-truth + cross-importer (App.tsx caller-side clamp will consume it in Plan 02). A new Vitest case in `audioEngine.test.ts` imports the symbol and asserts strict equality with `0.005`.
    - AUDIO-03 engine-side: `scheduleLeadIn` widens its return type from `number` to `number | null`. The `if (closed) return firstInCueTime` branch becomes `if (closed) return null`. The `if (muted) return firstInCueTime` branch is unchanged (muted ≠ closed; muted engine still has a meaningful projection). A new Vitest case asserts `engine.scheduleLeadIn(0, samplePlan)` returns `null` after `await engine.close()`.
    - AUDIO-06 engine-side: `AudioStatus` union literal at `audioEngine.ts:25` becomes `'idle' | 'lead-in' | 'failed'` (the `'starting'` literal is removed).
    - New engine-side clamp test using the ProbeAC pattern (controllable `currentTime`): instantiate ProbeAC with `probeTime = 10`, spy on `cueSynth.scheduleInCue`, call `engine.scheduleNextCue({ newPhase: 'in', audioTime: 9.5, phaseDurationSec: 4 })`, assert spy received `10 + SAFE_LEAD_SEC` (clamped) as 2nd arg. Paired case: same setup with `audioTime: 12` — assert spy received `12` verbatim (no clamp).
  </behavior>

  <action>
    Modify `src/audio/audioEngine.ts`:

    1. After the existing `LEAD_IN_*` export block (lines 74–80 region per PATTERNS Pattern E1), add a new named export `SAFE_LEAD_SEC` with the numeric literal `0.005` and a JSDoc block matching the existing WR-04-style "exported as single source of truth — App.tsx and useAudioCues.ts import these instead of redefining the same numbers locally" comment style. Tag the JSDoc with `AUDIO-02 D-03`. Reference RESEARCH.md §B for the exact JSDoc target shape.

    2. At `audioEngine.ts:25`, tighten the `AudioStatus` union to `export type AudioStatus = 'idle' | 'lead-in' | 'failed'` (delete the `'starting'` literal — D-07 / AUDIO-06).

    3. Widen the `AudioEngine` interface declaration for `scheduleLeadIn` (around line 30 region) from `scheduleLeadIn(startAudioTime: number, plan: BreathingPlan): number` to `scheduleLeadIn(startAudioTime: number, plan: BreathingPlan): number | null` with a JSDoc note explaining the null branch ("returns null when the engine is closed — AUDIO-03"). Reference RESEARCH.md §C.

    4. Inside the `scheduleLeadIn` implementation body (lines 148–167 region), change `if (closed) return firstInCueTime` to `if (closed) return null`. Leave `if (muted) return firstInCueTime` and the rest of the body unchanged.

    5. Inside the `scheduleNextCue` implementation body (lines 169–180 region per PATTERNS Pattern E3), insert a callee-side clamp AFTER `pruneExpiredCues()` and BEFORE the `newPhase === 'in' ? ... : ...` branch:
       - Declare `const clampedAudioTime = Math.max(audioTime, audioCtx.currentTime + SAFE_LEAD_SEC)`.
       - Pass `clampedAudioTime` (not `audioTime`) as the 2nd argument to both `scheduleInCue(...)` and `scheduleOutCue(...)`.
       - Tag the inline comment `// AUDIO-02 D-01/D-02 callee-side clamp.` per Shared Pattern 5 from PATTERNS.md.
       - Do NOT transform or subtract from `clampedAudioTime` after the clamp (D-02).

    Modify `src/audio/audioEngine.test.ts`:

    6. Add a new case `'SAFE_LEAD_SEC export equals 0.005 (D-03 single-source-of-truth)'` near the existing `LEAD_IN_*` / `scheduleLeadIn` cases (around line 100–108). Import `SAFE_LEAD_SEC` from `'./audioEngine'`; assert `expect(SAFE_LEAD_SEC).toBe(0.005)`.

    7. Add a new case `'scheduleLeadIn returns null when engine is closed (AUDIO-03)'` immediately after the existing `'after close(), scheduleNextCue is a no-op'` case at L246–253 (PATTERNS Pattern E4). Construct an engine, `await engine.close()`, call `engine.scheduleLeadIn(0, samplePlan)`, assert the return value is strictly `null` (use the existing samplePlan factory pattern from elsewhere in this test file).

    8. Add two new cases using the ProbeAC pattern (PATTERNS Pattern E5) adjacent to the existing `engine.now() returns audioCtx.currentTime` case at L255–279:
       - `'scheduleNextCue clamps past audioTime to currentTime + SAFE_LEAD_SEC (AUDIO-02)'`: probeTime = 10, spy on `cueSynth.scheduleInCue`, call `engine.scheduleNextCue({ newPhase: 'in', audioTime: 9.5, phaseDurationSec: 4 })`, assert `inSpy.mock.calls[0][1]` (2nd arg = audioTime) equals `10 + SAFE_LEAD_SEC` (or `expect.closeTo(10 + SAFE_LEAD_SEC, 9)` for floating-point safety).
       - `'scheduleNextCue passes future audioTime verbatim (no clamp) (AUDIO-02)'`: probeTime = 10, spy on `cueSynth.scheduleInCue`, call `engine.scheduleNextCue({ newPhase: 'in', audioTime: 12, phaseDurationSec: 4 })`, assert `inSpy.mock.calls[0][1]` strictly equals `12`.

    All new code must pass Phase 7 strict baseline: `// Reason:` annotations on any new `eslint-disable`, `void`-prefix on fire-and-forget promises (none expected in this task), and `npm run lint` exit 0 after the commit per Phase 7 D-09. Do NOT add any console.warn — silent posture per Phase 8 D-03 carries over.
  </action>

  <verify>
    <automated>npm run test -- src/audio/audioEngine.test.ts &amp;&amp; tsc --noEmit &amp;&amp; npm run lint</automated>
  </verify>

  <acceptance_criteria>
    - Source assertion: `grep -n "export const SAFE_LEAD_SEC = 0.005" src/audio/audioEngine.ts` returns exactly 1 match.
    - Source assertion: `grep -nE "^export type AudioStatus = 'idle' \\| 'lead-in' \\| 'failed'$" src/audio/audioEngine.ts` returns exactly 1 match.
    - Source assertion: `grep -cE "'starting'" src/audio/audioEngine.ts` returns 0 (the literal must not appear anywhere in the engine source after this task).
    - Source assertion: `grep -n "scheduleLeadIn.*: number \\| null" src/audio/audioEngine.ts` returns at least 1 match (interface declaration); the implementation body contains `if (closed) return null`.
    - Source assertion: `grep -n "Math.max(audioTime, audioCtx.currentTime + SAFE_LEAD_SEC)" src/audio/audioEngine.ts` returns exactly 1 match inside `scheduleNextCue`.
    - Behavior assertion: new test `'SAFE_LEAD_SEC export equals 0.005 (D-03 single-source-of-truth)'` exists in `src/audio/audioEngine.test.ts` and passes.
    - Behavior assertion: new test `'scheduleLeadIn returns null when engine is closed (AUDIO-03)'` exists and passes.
    - Behavior assertion: new test `'scheduleNextCue clamps past audioTime to currentTime + SAFE_LEAD_SEC (AUDIO-02)'` exists and passes (spy on `cueSynth.scheduleInCue` receives clamped 2nd arg).
    - Behavior assertion: new test `'scheduleNextCue passes future audioTime verbatim (no clamp) (AUDIO-02)'` exists and passes (spy receives 12 verbatim).
    - CLI: `npm run test -- src/audio/audioEngine.test.ts` exits 0.
    - CLI: `tsc --noEmit` exits 0 (verifies AudioStatus union tightening did not leak `'starting'` references).
    - CLI: `npm run lint` exits 0 (Phase 7 D-09 invariant).
  </acceptance_criteria>

  <done>
    All five source/grep assertions hold; all four new Vitest cases are green; `tsc --noEmit` + `npm run lint` exit 0; previously-passing tests in `audioEngine.test.ts` remain green (no regressions).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: AUDIO-04 osc.onended { once: true } chain disconnect in src/audio/cueSynth.ts and its test</name>
  <files>src/audio/cueSynth.ts, src/audio/cueSynth.test.ts</files>

  <read_first>
    - src/audio/cueSynth.ts (read fully — partial-oscillator graph construction loop at lines 117–134 per PATTERNS Pattern S2; current builder shape before any disconnect logic)
    - src/audio/cueSynth.test.ts (read fully — existing oscillator-spy pattern at L58–80 per PATTERNS Pattern S4, and partial-ratio pattern at L26–42)
    - .planning/phases/09-audio-wake-lock-lifecycle-hardening/09-RESEARCH.md §Recommended Code Shapes §D (both shapes — granular vs. single shared listener) + §Pitfall 3 (start-before-stop invariant) + Open Question 2 (`{ once: true }` rationale)
    - .planning/phases/09-audio-wake-lock-lifecycle-hardening/09-PATTERNS.md §"src/audio/cueSynth.ts" (Patterns S1–S4 — match-pair listener idiom from useWakeLock, partial-loop body, defensive try/catch posture, spy-on-createOscillator scaffolding)
    - .planning/phases/09-audio-wake-lock-lifecycle-hardening/09-VALIDATION.md (row AUDIO-04)
    - src/hooks/useWakeLock.ts lines 49–54 (Reference D match-pair listener idiom — analog for the new listener attach site)
  </read_first>

  <behavior>
    - Each partial oscillator in `scheduleBowlCue` has an `'ended'` listener attached with the option `{ once: true }`. The listener calls `osc.disconnect()` and `partialGain.disconnect()` (each wrapped in `try { ... } catch { /* silent */ }` per Shared Pattern 1).
    - Exactly one oscillator (the last one created in the partials loop) additionally disconnects the shared `filter` and the shared `envelope` (each wrapped in silent try/catch). Rationale: all partials share `stopAt`, so any single `'ended'` event is safe for the shared-chain cleanup (Assumption A3 in RESEARCH.md).
    - On a single `'ended'` dispatch per oscillator, each `disconnect()` call fires exactly once. Re-dispatching `'ended'` on the same oscillator does NOT cause a second disconnect (gated by `{ once: true }`).
    - The partial-loop body otherwise preserves the existing graph: `osc.connect(partialGain)`, `partialGain.connect(filter)`, `filter.connect(envelope)`, `envelope.connect(destination)`, `osc.start(when)`, `osc.stop(stopAt)`. No node-graph behavior change.
    - Invariant comment near the listener attach site documents Pitfall 3: `// Pre-condition: osc.stop(stopAt) with stopAt > when — ensures 'ended' fires. If a future change makes stopAt < when, ended may not fire and the chain leaks.`
  </behavior>

  <action>
    Modify `src/audio/cueSynth.ts`:

    1. Inside `scheduleBowlCue`, around lines 117–134 (the partial-loop region from PATTERNS Pattern S2), introduce two local arrays `const oscillators: OscillatorNode[] = []` and `const partialGains: GainNode[] = []` before the `for (const partial of PARTIALS)` loop opens.

    2. Inside the existing loop body, after `osc.stop(stopAt)`, push the current `osc` and the current `partialGain` into those two arrays (preserve all existing connections — `osc.connect(partialGain)`, `partialGain.connect(filter)`, `osc.start(when)`, `osc.stop(stopAt)`).

    3. Keep the existing `filter.connect(envelope)` and `envelope.connect(destination)` calls in their current location AFTER the loop (per RESEARCH §D first shape).

    4. After `envelope.connect(destination)`, add the AUDIO-04 disconnect logic. Pick EITHER:
       - **Shape A (granular per-osc)** from RESEARCH §D first shape: iterate `for (let i = 0; i < oscillators.length; i++)` with index-narrowing (`const osc = oscillators[i]; const partialGain = partialGains[i]; if (osc === undefined || partialGain === undefined) continue` — required under Phase 7 `noUncheckedIndexedAccess`). Inside the loop, attach `osc.addEventListener('ended', () => { try { osc.disconnect() } catch {} try { partialGain.disconnect() } catch {} }, { once: true })`. After the loop, attach a separate `'ended'` listener on `oscillators[oscillators.length - 1]` (with `if (lastOsc !== undefined)` narrowing) that disconnects `filter` and `envelope` (each in silent try/catch).
       - OR **Shape B (single shared listener)** from RESEARCH §D alternate shape: attach one `'ended'` listener with `{ once: true }` to `oscillators[oscillators.length - 1]` that iterates both arrays disconnecting every osc + partialGain + filter + envelope (each in silent try/catch).
       Recommendation: pick Shape A because it matches the codebase's defensive per-resource posture in `useWakeLock.ts:49–54` (Reference D). Both shapes satisfy AUDIO-04.

    5. Add the invariant comment immediately above the listener attach site: `// AUDIO-04: explicit disconnect on osc.onended. { once: true } makes the listener self-removing.` AND `// Pre-condition: osc.stop(stopAt) with stopAt > when — ensures 'ended' fires.` per Pitfall 3.

    6. Every new `disconnect()` call MUST be wrapped in `try { node.disconnect() } catch { /* silent */ }` per Shared Pattern 1 / Shared Pattern 3 — some browsers throw `InvalidAccessError` on already-disconnected nodes.

    Modify `src/audio/cueSynth.test.ts`:

    7. Add a new case in the existing `describe` block alongside `'scheduleInCue starts and stops each oscillator'` (line 80 region, PATTERNS Pattern S4): `'scheduleBowlCue / scheduleInCue attaches once: true ended listeners that disconnect each osc + partialGain exactly once (AUDIO-04)'`. Use the existing `vi.spyOn(ac, 'createOscillator').mockImplementation(...)` pattern to collect oscillator instances. Additionally spy `ac.createGain` and `ac.createBiquadFilter` to collect the partialGain + filter + envelope nodes. After calling `scheduleInCue(ac, 1.0, ac.destination)`:
       - For each captured oscillator, dispatch `osc.dispatchEvent(new Event('ended'))` (FakeAudioContext oscillators expose `dispatchEvent` via the standard EventTarget polyfill; if not, call the captured listener directly — verify via re-reading `vitest.setup.ts`).
       - Assert each `osc.disconnect` was called exactly once after dispatch.
       - Assert each `partialGain.disconnect` was called exactly once.
       - Assert the shared `filter.disconnect` and `envelope.disconnect` were called exactly once.
       - Dispatch `osc.dispatchEvent(new Event('ended'))` again on the same osc — assert `osc.disconnect` was STILL called exactly once (proves `{ once: true }` self-removal).

    8. If the existing `createOscillator` mock does not assign per-instance `disconnect` spies, extend the mock implementation to assign `osc.disconnect = vi.fn(realDisconnect.bind(osc))` (or similar) so the test can assert per-instance call counts. Re-use the existing precedent at `cueSynth.test.ts:58–80` for the construction style.

    All new code must pass Phase 7 strict baseline. Annotate any new `eslint-disable` (e.g., `no-non-null-assertion` if indexing arrays without optional chains) with `// Reason: <D-04 / AUDIO-04 / Pitfall 4 invariant>` per Phase 7 D-04.
  </action>

  <verify>
    <automated>npm run test -- src/audio/cueSynth.test.ts &amp;&amp; tsc --noEmit &amp;&amp; npm run lint</automated>
  </verify>

  <acceptance_criteria>
    - Source assertion: `grep -nE "addEventListener\\('ended'" src/audio/cueSynth.ts` returns at least 1 match inside `scheduleBowlCue`.
    - Source assertion: `grep -nE "\\{\\s*once:\\s*true\\s*\\}" src/audio/cueSynth.ts` returns at least 1 match (the `{ once: true }` option).
    - Source assertion: `grep -cE "\\.disconnect\\(\\)" src/audio/cueSynth.ts` returns >= 4 (at minimum: osc + partialGain + filter + envelope disconnect calls).
    - Source assertion: `grep -c "try {" src/audio/cueSynth.ts` returns >= 4 (silent try/catch around each disconnect per Shared Pattern 1).
    - Behavior assertion: new test `'... attaches once: true ended listeners ...'` exists in `src/audio/cueSynth.test.ts` and passes.
    - Behavior assertion: dispatching `'ended'` twice on the same osc still results in `osc.disconnect` having been called exactly once (proves `{ once: true }`).
    - CLI: `npm run test -- src/audio/cueSynth.test.ts` exits 0.
    - CLI: `tsc --noEmit` exits 0; `npm run lint` exits 0 (Phase 7 D-09).
    - No regressions in `cueSynth.test.ts` — all previously-passing cases still green.
  </acceptance_criteria>

  <done>
    All source assertions hold; the new AUDIO-04 test passes including the second-dispatch idempotency assertion; `tsc --noEmit` + `npm run lint` exit 0; existing cueSynth tests still green.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: WAKELOCK-01 in-flight ref + post-await sentinel handoff in src/hooks/useWakeLock.ts and its test</name>
  <files>src/hooks/useWakeLock.ts, src/hooks/useWakeLock.test.tsx</files>

  <read_first>
    - src/hooks/useWakeLock.ts (read fully — current `request()` body at lines 34–60 per PATTERNS Pattern W2, current `release()` body at lines 62–75 per Pattern W1, ref declarations at lines 31–32 per Pattern W3, unmount cleanup at lines 88–99 per Pattern W4)
    - src/hooks/useWakeLock.test.tsx (read fully — existing `release() calls sentinel.release() once when held` case at L81–96, absorb-rejection precedent at L49–67, Pitfall 6 leak guard at L202–225)
    - .planning/phases/09-audio-wake-lock-lifecycle-hardening/09-RESEARCH.md §Recommended Code Shapes §G (the full revised request() + release() shape), Open Question 3 (release idempotency + discriminator rationale), Reference C (Pitfall 6 unmount-cleanup race comment that this WAKELOCK-01 formalizes)
    - .planning/phases/09-audio-wake-lock-lifecycle-hardening/09-PATTERNS.md §"src/hooks/useWakeLock.ts" (Patterns W1–W5 — synchronous-null-then-async-close baseline, request body with API-absent + idempotency gates, two new refs alongside sentinelRef + wasAcquiredRef, unmount cleanup signals new ref, pending-promise control + spied sentinel)
    - .planning/phases/09-audio-wake-lock-lifecycle-hardening/09-VALIDATION.md (rows WAKELOCK-01 concurrent / release-during-await / unmount-during-await)
    - .planning/phases/07-strict-type-lint-baseline/07-CONTEXT.md §Established Patterns (`void`-prefix on fire-and-forget; no-floating-promises)
    - .planning/phases/08-storage-forward-compat-cross-tab-ui-sync/08-CONTEXT.md §D-03 (silent fallback posture — no console.warn)
  </read_first>

  <behavior>
    - Two new boolean refs are declared alongside the existing `sentinelRef` and `wasAcquiredRef`: `requestInFlightRef` (initialized `false`) and `releaseCalledDuringRequestRef` (initialized `false`).
    - `request()` gains a third guard line `if (requestInFlightRef.current) return` after the existing two gates (`!('wakeLock' in navigator)` and `sentinelRef.current !== null`). A second concurrent `request()` no-ops without calling `navigator.wakeLock.request`.
    - `request()` sets `requestInFlightRef.current = true` at the top of its `try` block (before the `await`).
    - After `await navigator.wakeLock.request('screen')` resolves, `request()` checks `releaseCalledDuringRequestRef.current`. If true, it resets that flag, calls `void sentinel.release().catch(() => undefined)` on the freshly-acquired sentinel, and `return`s (orphan path — sentinel never stored).
    - Otherwise the existing assignment branch runs: `sentinelRef.current = sentinel`, `wasAcquiredRef.current = true`. The existing `sentinel.addEventListener('release', ...)` gains the `{ once: true }` third arg per RESEARCH §G (matches Reference D pattern + Open Q2 rationale).
    - A `finally { requestInFlightRef.current = false }` block ensures the in-flight flag is always cleared regardless of resolve/reject/orphan paths.
    - `release()` body: BEFORE the existing `wasAcquiredRef.current = false` line, add `if (requestInFlightRef.current) releaseCalledDuringRequestRef.current = true`. Everything else preserved verbatim (synchronous-null on `sentinelRef`, the existing silent try/catch around `await sentinel.release()`).
    - Unmount cleanup (existing useEffect cleanup): BEFORE the existing `sentinelRef.current = null` line, add `if (requestInFlightRef.current) releaseCalledDuringRequestRef.current = true`. The rest of the cleanup (synchronous-null, `wasAcquiredRef.current = false`, `void sentinel.release().catch(() => {})`) is unchanged per Pitfall 1 (do NOT replace existing patterns).
  </behavior>

  <action>
    Modify `src/hooks/useWakeLock.ts`:

    1. After the existing `const sentinelRef = useRef<WakeLockSentinel | null>(null)` and `const wasAcquiredRef = useRef<boolean>(false)` declarations (lines 31–32 region per PATTERNS Pattern W3), append two new refs in the same style:
       - `const requestInFlightRef = useRef<boolean>(false)` with comment `// WAKELOCK-01: concurrent-request gate.`
       - `const releaseCalledDuringRequestRef = useRef<boolean>(false)` with comment `// WAKELOCK-01: release-during-await signal.`

    2. Modify the `request` useCallback body (lines 34–60 region per Pattern W2):
       - After the existing two guards (`if (!('wakeLock' in navigator)) return` and `if (sentinelRef.current !== null) return`), insert a third guard `if (requestInFlightRef.current) return` with comment `// WAKELOCK-01: second concurrent caller no-ops while first await is pending.`
       - Inside the existing `try { ... }` block, BEFORE `const sentinel = await navigator.wakeLock.request('screen')`, set `requestInFlightRef.current = true`.
       - AFTER the `await` resolves and BEFORE the existing `sentinelRef.current = sentinel; wasAcquiredRef.current = true` assignment, insert the orphan branch:
         - `if (releaseCalledDuringRequestRef.current) { releaseCalledDuringRequestRef.current = false; void sentinel.release().catch(() => undefined); return }` with comment `// WAKELOCK-01: release()/unmount ran during the await; discard the freshly-acquired sentinel.`
       - Modify the existing `sentinel.addEventListener('release', () => { ... })` at line 49 to add `{ once: true }` as the 3rd arg per RESEARCH §G + Reference D.
       - Wrap the entire `try { ... }` body in a `finally { requestInFlightRef.current = false }` so the in-flight flag clears on resolve/reject/orphan.

    3. Modify the `release` useCallback body (lines 62–75 region per Pattern W1):
       - Insert `if (requestInFlightRef.current) releaseCalledDuringRequestRef.current = true` as the FIRST statement of the body, BEFORE the existing `wasAcquiredRef.current = false` line. Comment: `// WAKELOCK-01: if request() is awaiting, signal post-await to discard.`
       - Leave the rest of `release()` body verbatim (synchronous-null on `sentinelRef`, silent try/catch around `await sentinel.release()`).

    4. Modify the unmount cleanup (lines 88–99 region per Pattern W4):
       - BEFORE the existing `const sentinel = sentinelRef.current; sentinelRef.current = null` block, insert `if (requestInFlightRef.current) releaseCalledDuringRequestRef.current = true` with comment `// WAKELOCK-01: unmount-during-await orphans the in-flight sentinel; signal post-await to discard.`
       - Preserve all existing cleanup code (Pitfall 6 sync-null pattern, `void sentinel.release().catch(() => {})`).

    Modify `src/hooks/useWakeLock.test.tsx`:

    5. Add three new cases after the existing `Unmount with sentinel held releases the sentinel` case at L202–225 region (PATTERNS Pattern W5 — combine pending-promise control + spied sentinel patterns):

       a. `'WAKELOCK-01: second concurrent request() no-ops while first is pending'`:
          - Stub `navigator.wakeLock.request` via `vi.spyOn` with `mockReturnValueOnce(pendingPromise)` where `pendingPromise = new Promise<WakeLockSentinel>(r => { resolveFn = r })` (capture `resolveFn` in an outer-scope variable).
          - Call `result.current.request()` (no await) twice.
          - Assert `requestSpy` was called exactly once.
          - Resolve `pendingPromise` with a mock sentinel; await an `act` flush.
          - Assert `sentinelRef` is populated (verified indirectly via subsequent `release()` call: a follow-up `release()` triggers exactly one `sentinel.release()` call).

       b. `'WAKELOCK-01: release() during pending request() orphans the fresh sentinel'`:
          - Same parked-promise scaffolding.
          - Fire `result.current.request()` without awaiting.
          - Fire `result.current.release()` (synchronously sets `releaseCalledDuringRequestRef`).
          - Resolve the pending promise with a spy-able sentinel `{ release: vi.fn(async () => undefined), addEventListener: vi.fn() }`.
          - `await act(async () => { ... })` to flush microtasks.
          - Assert `sentinel.release` was called exactly once.
          - Assert `sentinelRef.current === null` (verified via a subsequent `result.current.release()` call being a no-op on `sentinel.release` — call count stays at 1).
          - Assert the post-await assignment branch did NOT run (i.e., `sentinel.addEventListener('release', ...)` was NOT called — spy count 0).

       c. `'WAKELOCK-01: unmount during pending request() orphans the fresh sentinel'`:
          - Same shape as (b), but call `unmount()` from `renderHook` instead of `result.current.release()`.
          - Resolve the pending promise with a spy-able sentinel.
          - `await act` flush.
          - Assert `sentinel.release` was called exactly once.
          - Assert `sentinel.addEventListener('release', ...)` was NOT called.

    6. All test setup follows the existing `useWakeLock.test.tsx` conventions: `renderHook(() => useWakeLock())`, `vi.spyOn(navigator.wakeLock, 'request')`, `await act(async () => { ... })` for promise flushing, `unmount()` from `renderHook` return.

    All new code passes Phase 7 strict baseline. Any new `// eslint-disable` is annotated with `// Reason:` per Phase 7 D-04. No `console.warn` per Phase 8 D-03. Use `void` prefix on fire-and-forget `.release().catch(...)` per Phase 7 `no-floating-promises`.
  </action>

  <verify>
    <automated>npm run test -- src/hooks/useWakeLock.test.tsx &amp;&amp; tsc --noEmit &amp;&amp; npm run lint</automated>
  </verify>

  <acceptance_criteria>
    - Source assertion: `grep -n "requestInFlightRef" src/hooks/useWakeLock.ts` returns >= 5 matches (declaration + guard read in request + set-true in request + check in release + check in unmount + finally clear).
    - Source assertion: `grep -n "releaseCalledDuringRequestRef" src/hooks/useWakeLock.ts` returns >= 5 matches (declaration + post-await check + set-true in release + set-true in unmount + reset after orphan).
    - Source assertion: `grep -n "{ once: true }" src/hooks/useWakeLock.ts` returns >= 1 match on the existing `sentinel.addEventListener('release', ...)` call.
    - Source assertion: `grep -n "finally" src/hooks/useWakeLock.ts` returns >= 1 match (the finally that clears `requestInFlightRef`).
    - Source assertion: `grep -c "console.warn\\|console.error" src/hooks/useWakeLock.ts` returns 0 (silent posture preserved).
    - Source assertion: the existing synchronous-null pattern (`sentinelRef.current = null` before the await on release) remains present in `release()` and in the unmount cleanup (Pitfall 1 — do NOT remove).
    - Behavior assertion: three new tests `'WAKELOCK-01: second concurrent request() no-ops while first is pending'`, `'WAKELOCK-01: release() during pending request() orphans the fresh sentinel'`, `'WAKELOCK-01: unmount during pending request() orphans the fresh sentinel'` exist in `src/hooks/useWakeLock.test.tsx` and pass.
    - Behavior assertion: in test (a), `requestSpy` was called exactly once even though `result.current.request()` was invoked twice.
    - Behavior assertion: in tests (b) and (c), `sentinel.release` was called exactly once on the orphaned sentinel and `sentinel.addEventListener` was NOT called (no assignment branch executed).
    - CLI: `npm run test -- src/hooks/useWakeLock.test.tsx` exits 0.
    - CLI: `tsc --noEmit` exits 0; `npm run lint` exits 0 (Phase 7 D-09).
    - No regressions in `useWakeLock.test.tsx` — all previously-passing cases still green.
  </acceptance_criteria>

  <done>
    All source assertions hold; all three new WAKELOCK-01 tests pass with correct call-count assertions on the spied sentinel + request spy; `tsc --noEmit` + `npm run lint` exit 0; the existing synchronous-null patterns remain intact (Pitfall 1).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

This phase introduces no new trust boundaries. Phase 9 is a defensive lifecycle hardening patch on existing client-side imperative resources (Web Audio + Screen Wake Lock); no new authentication, session, access-control, cryptography, or input-validation surface.

| Boundary | Description |
|----------|-------------|
| (none new) | No new trust boundary crosses introduced by this plan. |

## STRIDE Threat Register

**No applicable security threats for this defensive lifecycle hardening patch.** ASVS V2 (Authentication), V3 (Session Management), V4 (Access Control), V5 (Input Validation), V6 (Cryptography) categories do not apply (single-user local app, no auth, no crypto, no server sessions). The clamp at AUDIO-02 is a numeric range guard, not security-motivated input validation.

The only STRIDE pattern that maps to this plan (informational) is:

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01 | Denial of service (self) | `cueSynth.scheduleBowlCue` partial-node graph | mitigate | AUDIO-04 — `osc.onended` `{ once: true }` listener disconnects partial osc + partialGain + shared filter + envelope after the cue ends. Prevents per-cue Web Audio node graph retention across mute toggles. |
| T-09-02 | Denial of service (self) | `useWakeLock.request()` sentinel orphan path | mitigate | WAKELOCK-01 — post-await `releaseCalledDuringRequestRef` check releases the freshly-acquired sentinel when `release()`/unmount ran during the await; in-flight ref no-ops a second concurrent caller. |
</threat_model>

<verification>
- `npm run test` (full Vitest suite) exits 0 after all three tasks land. Baseline preserved: prior-passing tests (366 cases per CONTEXT.md `<scope_lock>`) remain green; new cases for AUDIO-02 (constant + clamp), AUDIO-03 (closed null), AUDIO-04 (disconnect + once: true), AUDIO-06 (union removal — engine-side), and WAKELOCK-01 (three concurrency cases) added.
- `tsc --noEmit` exits 0 after every task commit (Phase 7 D-09 invariant). The `AudioStatus` union tightening at Task 1 surfaces any latent `'starting'` literal usage; the `scheduleLeadIn: number | null` return type widening surfaces any caller that assumed unconditional `number`.
- `npm run lint` exits 0 after every task commit (Phase 7 D-09 invariant). Any new `// eslint-disable` carries a `// Reason:` annotation per Phase 7 D-04.
- `npm run build` exits 0 (Phase 7 D-09 invariant) at the wave-merge boundary.
- Source-level invariants (per acceptance criteria of each task):
  - `grep -c "'starting'" src/audio/audioEngine.ts` returns 0 after Task 1 — proves the union member is fully removed at the engine layer.
  - The synchronous-null patterns in `useWakeLock.ts release()` and unmount cleanup remain present after Task 3 — proves Pitfall 1 was honored (do NOT replace synchronous-null with the new ref).
  - `SAFE_LEAD_SEC` is the only place the literal `0.005` appears in the audio engine source — proves single-source-of-truth (D-03).
</verification>

<success_criteria>
This plan succeeds when all six must-haves are observable in the running codebase:

1. **Engine self-defending clamp present:** `Math.max(audioTime, audioCtx.currentTime + SAFE_LEAD_SEC)` inside `audioEngine.scheduleNextCue` between `pruneExpiredCues()` and the `scheduleInCue/Out` branch; spy-based test asserts the clamped value reaches `cueSynth.scheduleInCue` (AUDIO-02 engine-side, Roadmap Success Criterion #2).
2. **`SAFE_LEAD_SEC` exported as the canonical constant:** named export from `src/audio/audioEngine.ts`, value `0.005`, with WR-04-style JSDoc; test imports the symbol and asserts strict equality with `0.005` (AUDIO-02 D-03).
3. **`scheduleLeadIn` returns `null` when closed:** interface signature widened to `number | null`; `if (closed) return null` branch in the body; test asserts the null return after `await engine.close()` (AUDIO-03 engine-side, Roadmap Success Criterion #3).
4. **`AudioStatus` union has no `'starting'`:** literal removed from the union type; `tsc --noEmit` passes (proving no caller referenced the deleted literal — none exists today); test using exhaustive switch / `satisfies` locks the union shape (AUDIO-06 D-07, Roadmap Success Criterion #4).
5. **cueSynth osc.onended chain disconnect operational:** `{ once: true }` listeners attached on each partial oscillator that disconnect that osc + partialGain; shared filter + envelope disconnected after the last partial; test dispatches `'ended'` twice and proves only one disconnect call per node (AUDIO-04, Roadmap Success Criterion #3 partial-cue chain disconnect).
6. **WakeLock in-flight gate + release-during-await orphan released:** second concurrent `request()` no-ops; release-during-await and unmount-during-await both release the freshly-acquired sentinel via spec-idempotent `.release()`; three new tests cover the three cases (WAKELOCK-01, Roadmap Success Criterion #5).

Full Vitest suite green (366 + new cases). `tsc --noEmit` and `npm run lint` exit 0. Plan 02 (Wave 2) can consume the tightened API contracts (`SAFE_LEAD_SEC` import + `scheduleLeadIn: number | null` return type) without further engine-layer changes.
</success_criteria>

<output>
After completion, create `.planning/phases/09-audio-wake-lock-lifecycle-hardening/09-01-SUMMARY.md` documenting:
- Files modified with line ranges.
- New tests added (per file) with their describe-block neighbors.
- Confirmation that the synchronous-null pattern in useWakeLock is intact (Pitfall 1 self-check).
- Confirmation that `grep -c "'starting'" src/audio/audioEngine.ts` returns 0.
- Plan 02 consumer notes: `SAFE_LEAD_SEC` is the named import from `'../audio/audioEngine'`; `scheduleLeadIn` now returns `number | null` and the hook-side caller in `useAudioCues.ts:204` must propagate the null up to `start()` per Plan 02 Task scope.
</output>
