---
phase: 07-strict-type-lint-baseline
plan: 03
type: execute
wave: 3
depends_on:
  - 07-01
  - 07-02
files_modified:
  # Production sources — remaining isolated rule fires (16 errors across 7 files)
  - src/hooks/usePrefersReducedMotion.ts
  - src/audio/audioEngine.ts
  - src/audio/cueSynth.ts
  - src/components/SettingsStepper.tsx
  - src/hooks/useAudioCues.ts
  - src/main.tsx
  - src/app/App.tsx
  # Test/setup files (162 ESLint errors across ~9 files; representative — executor may edit additional test files surfaced by lint)
  - vitest.setup.ts
  - src/app/App.audio.test.tsx
  - src/app/App.wakeLock.test.tsx
  - src/app/App.persistence.test.tsx
  - src/audio/cueSynth.test.ts
  - src/audio/audioEngine.test.ts
  - src/hooks/useAudioCues.test.tsx
  - src/storage/settings.test.ts
  - src/storage/storage.test.ts
autonomous: true
requirements:
  - BUILD-02

must_haves:
  truths:
    - "All 16 remaining production-side ESLint errors are eliminated by inline fixes per PATTERNS.md §Fix Category 6 + §Fix Category 9 (no-unnecessary-condition × 6, no-unnecessary-type-assertion × 5 incl. audioEngine WebKit casts kept-and-annotated, no-non-null-assertion × 1 main.tsx, no-useless-assignment × 1 useAudioCues, exhaustive-deps × 1 App.tsx audio dep, plus the 2 usePrefersReducedMotion typeof-window dead-checks removed per RESEARCH.md Open Q2 Option A)"
    - "All ~162 test-file ESLint errors are eliminated by inline fixes (JSON.parse typing in persistence/settings tests, non-null assertion annotation in test files, no-extraneous-class on FakeAudioContext variants, require-await on FakeAudioContext suspend/close in vitest.setup.ts and test helpers)"
    - "App.tsx:411 stale react-hooks/set-state-in-effect disable is REMOVED in this plan (no annotation needed; the rule does not fire at that site under react-hooks 7.1.1). The two surviving disables at App.tsx:205 and App.tsx:390 are LEFT UNTOUCHED (Plan 04 annotates them per D-04)."
    - "npm run lint exits 0 against the eslint.config.js with strictTypeChecked + projectService — this is the FIRST plan boundary at which the full D-09 lint-exit-0 invariant is satisfied for ALL files in src/"
    - "npx tsc --noEmit -p tsconfig.app.json and tsconfig.node.json continue to exit 0"
    - "npm run build exits 0"
    - "npm run test -- --run reports 363/363 passing — D-09 behavior preservation"
    - "Every surviving // eslint-disable-next-line in test files added by this plan is preceded by a // Reason: line per D-04. Production-side disables added (audioEngine WebKit casts, useAudioCues line 124, cueSynth.ts line 104 non-null) are also each preceded by a // Reason: line per D-04. The two App.tsx surviving react-hooks disables are STILL UNANNOTATED at end-of-plan (Plan 04 annotates them as part of BUILD-03)."
  artifacts:
    - path: "src/hooks/usePrefersReducedMotion.ts"
      provides: "Dead SSR guards (typeof window === 'undefined') REMOVED at lines 7,14 per RESEARCH.md Open Q2 Option A; setReduced(mql.matches) call UNTOUCHED (Plan 04 adds the set-state-in-effect annotation there)"
    - path: "src/audio/audioEngine.ts"
      provides: "Three WebKit AudioContextState 'interrupted' casts at lines 125, 233, 241 are KEPT (D-37 documentation intent) and each preceded by // Reason: + // eslint-disable-next-line @typescript-eslint/no-unnecessary-type-assertion; line 232 onStateChange optional chain similarly KEPT and annotated"
    - path: "src/audio/cueSynth.ts"
      provides: "Line 104 redundant phaseDurationSec !== undefined check restructured to single needsSustain branch with // Reason: documenting the outer-condition invariant + non-null assertion on phaseDurationSec"
    - path: "src/hooks/useAudioCues.ts"
      provides: "Line ~124 explicit state === 'interrupted' check KEPT and annotated (D-37 WebKit branch documentation); line ~261 let newEngine = null restructured to remove the useless assignment"
    - path: "src/components/SettingsStepper.tsx"
      provides: "Line 28 nextValue narrowing switched from bracket access to .at(selectedIndex + offset) for unambiguous T | undefined return type"
    - path: "src/main.tsx"
      provides: "Line 6 createRoot(document.getElementById('root')!) replaced with inline narrowing (rootEl null-check throwing descriptive Error) per D-07"
    - path: "src/app/App.tsx"
      provides: "Single exhaustive-deps fix (audio added to the affected useCallback deps, no disable); App.tsx:411 stale react-hooks/set-state-in-effect disable REMOVED; App.tsx:205 and App.tsx:390 disables UNTOUCHED for Plan 04"
    - path: "vitest.setup.ts"
      provides: "FakeAudioContext suspend (~line 199) and close (~line 203) methods each preceded by // Reason: + // eslint-disable-next-line @typescript-eslint/require-await; resume (~line 187) UNTOUCHED (it has conditional throw and is meaningfully async)"
    - path: "src/app/App.persistence.test.tsx"
      provides: "readEnvelope() typed return Record<string, unknown> | null with downstream assertion sites converted to expect(env).toMatchObject({...}) per PATTERNS.md §Fix Category 7 (matches existing storage.test.ts:56 analog)"
  key_links:
    - from: "Each production-side surviving // eslint-disable-next-line added by this plan (audioEngine WebKit casts × 3, audioEngine onStateChange × 1, useAudioCues state-equality × 1, cueSynth.ts non-null × 1)"
      to: "// Reason: annotation on the immediately preceding line"
      via: "D-04 annotation policy"
      pattern: "Reason:"
    - from: "Each test-file // eslint-disable-next-line added by this plan (no-non-null-assertion × ~42, no-extraneous-class × 4, require-await × ~16 in tests + ~2 in vitest.setup.ts, plus per-site no-unsafe-* annotations as needed)"
      to: "// Reason: annotation on the immediately preceding line"
      via: "D-04 annotation policy"
      pattern: "Reason:"
    - from: "App.tsx:411 stale disable removal"
      to: "lint-exit-0 invariant"
      via: "Removing the unused-disable warning so npm run lint reports zero problems at this plan's commit boundary"
      pattern: '(?!.*set-state-in-effect)'
---

<objective>
Close the remainder of BUILD-02 to satisfy the full D-09 lint-exit-0 invariant for the FIRST time in Phase 7. This plan contains all the work that did not fit in Plan 02's production-rule-cluster batches:

- 16 isolated production-side ESLint errors (no-unnecessary-condition × 6, no-unnecessary-type-assertion × 5, no-non-null-assertion × 1, no-useless-assignment × 1, exhaustive-deps × 1, plus 2 dead-SSR-guard removals in usePrefersReducedMotion.ts)
- ~162 test-file ESLint errors dominated by no-non-null-assertion (~42), no-unsafe-member-access (~37), no-unsafe-assignment (~16), require-await (~14), no-unnecessary-type-assertion (~13), no-extraneous-class (4)
- Removal of the 1 stale App.tsx:411 react-hooks/set-state-in-effect disable that fires as a warning under react-hooks 7.1.1

Purpose: Drive `npm run lint` to exit 0 across ALL src/ + vitest.setup.ts files for the first time in Phase 7 — completing BUILD-02's lint-clean invariant. After this plan ships, every file in the project lints clean under strictTypeChecked + projectService, and only Plan 04's BUILD-03 hooks-audit work (react-hooks/exhaustive-deps explicit error override + // Reason: annotations on the 3 surviving react-hooks disables in App.tsx and usePrefersReducedMotion.ts) remains for Phase 7.

Output: Seven production source files with isolated rule fires fixed inline (no helpers, D-07); ~9 test/setup files with bulk-pattern fixes (typed JSON.parse helpers, annotated non-null assertions, annotated FakeAudioContext require-await); App.tsx with the line 411 stale disable removed and the audio-dependency added to the affected useCallback. Lines 205/390 of App.tsx are LEFT UNTOUCHED (Plan 04 annotates them). The setReduced(mql.matches) call in usePrefersReducedMotion.ts is LEFT UNTOUCHED for set-state-in-effect (Plan 04 adds that annotation as part of BUILD-03); only the dead typeof-window guards are removed in this plan.

**Scope split rationale (D-11 / checker-blocker resolution):** This plan is the second half of the original 5-task Plan 02. Plan 02 (post-revision) handles the 3 large production-rule clusters (eslint.config upgrade + unbound-method + string/JSX hygiene = ~48 production errors). This Plan 03 handles the 16 isolated production rule fires + the 162 test-file errors + the App.tsx:411 stale disable removal. The split keeps each plan ≤ 30% context per task and ensures the high-volume test-file fix pass is not co-located with delicate production-side judgment work (WebKit cast preservation, useAudioCues definite-assignment refactor, main.tsx null-check shape).

**Important scope boundary:** This plan does NOT change `react-hooks/exhaustive-deps` to error level (Plan 04 BUILD-03 scope), does NOT add `// Reason:` annotations to the EXISTING App.tsx:205 and App.tsx:390 react-hooks/set-state-in-effect disables (Plan 04 scope), and does NOT add the new set-state-in-effect annotated disable to usePrefersReducedMotion.ts:setReduced (Plan 04 scope). The dead `typeof window === 'undefined'` guards at usePrefersReducedMotion.ts:7,14 ARE removed here per RESEARCH.md Open Question 2 Option A, because they are no-unnecessary-condition production fires that this plan owns.
</objective>

<execution_context>
@$HOME/.claude-personal-personal/get-shit-done/workflows/execute-plan.md
@$HOME/.claude-personal-personal/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-strict-type-lint-baseline/07-CONTEXT.md
@.planning/phases/07-strict-type-lint-baseline/07-RESEARCH.md
@.planning/phases/07-strict-type-lint-baseline/07-PATTERNS.md
@.planning/phases/07-strict-type-lint-baseline/07-VALIDATION.md
@.planning/phases/07-strict-type-lint-baseline/07-01-PLAN.md
@.planning/phases/07-strict-type-lint-baseline/07-02-PLAN.md

<interfaces>
<!-- Verified pre-edit shapes for the production-side isolated fires (PATTERNS.md §Fix Category 6 + §Fix Category 9). All fixes preserve runtime behavior per D-08; surviving disables carry // Reason: annotations per D-04. -->

usePrefersReducedMotion.ts no-unnecessary-condition fix (PATTERNS.md §"Fix Category 6 / Option A"):
- Remove the `typeof window === 'undefined'` checks at usePrefersReducedMotion.ts:7,14 (dead code in this Vite SPA — recommended approach in RESEARCH.md Open Question 2). Keep the `!window.matchMedia` guard. Do NOT add the `set-state-in-effect` disable here — that is Plan 04 scope.

audioEngine.ts no-unnecessary-type-assertion (PATTERNS.md §"Fix Category 9"):
- Three sites at audioEngine.ts:125,233,241 use `audioCtx.state as AudioContextState | 'interrupted'`. D-08 fix: KEEP the cast (it documents the WebKit widening per Phase 5.1 D-37) and prefix each with `// Reason: AudioContextState widened to include WebKit 'interrupted' extension (D-37); cast documents intent.` then `// eslint-disable-next-line @typescript-eslint/no-unnecessary-type-assertion`.

audioEngine.ts:232 no-unnecessary-condition: confirm at lint time whether `opts.onStateChange?.()` fires; if so, add `// Reason: …` + `// eslint-disable-next-line @typescript-eslint/no-unnecessary-condition`. Do NOT remove the optional chain (defensive intent).

cueSynth.ts:104 no-unnecessary-condition (PATTERNS.md §"Fix Category 9"):
- Restructure `if (needsSustain && phaseDurationSec !== undefined)` → `if (needsSustain) { … const dur = phaseDurationSec! /* Reason: phaseDurationSec is always defined when needsSustain is true per outer condition. */ }`

useAudioCues.ts:124 no-unnecessary-condition (PATTERNS.md §"Fix Category 9"):
- D-08 fix: keep the explicit `state === 'interrupted'` check, add `// Reason: explicit state check documents the WebKit-specific state-machine branch (D-37).` then `// eslint-disable-next-line @typescript-eslint/no-unnecessary-condition` directly above.

useAudioCues.ts:261 no-useless-assignment (PATTERNS.md §"Fix Category 9"):
- Restructure: `let newEngine: AudioEngine` (no initial null) and rely on the catch's `return` for definite assignment. OR change type to `let newEngine: AudioEngine | undefined` and remove the `= null` initializer. Either is fine; choose the option that does not trigger any other rule.

SettingsStepper.tsx:28 no-unnecessary-condition (PATTERNS.md §"Fix Category 9"):
- Replace `options[selectedIndex + offset]` with `options.at(selectedIndex + offset)` to make the `T | undefined` return type unambiguous to the linter.

main.tsx no-non-null-assertion (PATTERNS.md §"Fix Category 9 / main.tsx:6"):
- Inline narrowing per D-07: `const rootEl = document.getElementById('root'); if (rootEl === null) throw new Error('Root element #root not found in index.html'); createRoot(rootEl).render(...)`. Do NOT use the annotated-assertion fallback here — the inline narrowing is preferred per D-07.

App.tsx exhaustive-deps fix (RESEARCH.md §"Production Errors by Rule" / 1 occurrence):
- The single `react-hooks/exhaustive-deps` warning in App.tsx (audio missing from useCallback deps). Fix BY ADDING `audio` to the deps list — do NOT add a disable. Behavior is preserved because audio is the actual dependency the callback closes over (this is a real but harmless missing dep; adding it stabilizes the rule).

App.tsx:411 stale disable removal (PATTERNS.md §"App.tsx:411 — REMOVE (stale disable)"):
- The line 411 `// eslint-disable-next-line react-hooks/set-state-in-effect` is unused (the rule does not fire on the suppressed `setStats(updated)` call under react-hooks 7.1.1). REMOVE the entire eslint-disable comment line. Do NOT add an annotation. Do NOT touch the surrounding code.

JSON.parse in tests fix pattern (PATTERNS.md §"Fix Category 7"):
- Type readEnvelope return: `function readEnvelope(): Record<string, unknown> | null { … return raw ? (JSON.parse(raw) as Record<string, unknown>) : null }`
- Replace nested member-access assertions with `expect(env).toMatchObject({ … })` where semantically equivalent (existing analog: storage.test.ts:56)

require-await on FakeAudioContext (PATTERNS.md §"Fix Category 8"):
- Add `// Reason: async signature matches AudioContext.<method>() API contract; no real async work in the fake.` then `// eslint-disable-next-line @typescript-eslint/require-await` directly above EACH `vi.fn(async () => {...})` that has no await. Apply to `suspend` and `close` (NOT `resume` — it has conditional throw and is meaningfully async).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Fix the remaining production single/few-occurrence rules — no-unnecessary-condition, no-unnecessary-type-assertion, no-non-null-assertion, no-useless-assignment, exhaustive-deps, App.tsx:411 stale disable removal</name>
  <files>src/hooks/usePrefersReducedMotion.ts, src/audio/audioEngine.ts, src/audio/cueSynth.ts, src/components/SettingsStepper.tsx, src/hooks/useAudioCues.ts, src/main.tsx, src/app/App.tsx</files>
  <read_first>
    - src/hooks/usePrefersReducedMotion.ts (full file — lines 7 and 14 contain `typeof window === 'undefined'` checks; line ~22 contains `setReduced(mql.matches)` which fires set-state-in-effect — Plan 04 scope, do NOT touch line 22 here)
    - src/audio/audioEngine.ts (lines 120-134 and 228-242 — three `audioCtx.state as AudioContextState | 'interrupted'` casts at lines 125, 233, 241; also line 232 `opts.onStateChange?.()` no-unnecessary-condition site)
    - src/audio/cueSynth.ts (lines 98-112 — line 104 `if (needsSustain && phaseDurationSec !== undefined)` post-narrowing redundant check)
    - src/hooks/useAudioCues.ts (lines 44-58 = interface — already touched by Plan 02 Task 2 with `this: void`; lines 120-130 = the `state === 'interrupted'` check at ~line 124; lines 258-278 = the `let newEngine: AudioEngine | null = null` site at ~line 261)
    - src/components/SettingsStepper.tsx (lines 25-32 — `const nextValue = options[selectedIndex + offset]; if (nextValue !== undefined) { … }`)
    - src/main.tsx (full file — line 6 `createRoot(document.getElementById('root')!)`)
    - src/app/App.tsx (locate via lint output: (a) the single `react-hooks/exhaustive-deps` site where `audio` is missing from a useCallback deps list — `npm run lint 2>&1 | grep -A2 exhaustive-deps`; (b) the line ~411 stale `// eslint-disable-next-line react-hooks/set-state-in-effect` comment that fires as a warning. Confirm line 205 and line 390 disables are PRESENT and UNTOUCHED — Plan 04 annotates them.)
    - .planning/phases/07-strict-type-lint-baseline/07-PATTERNS.md (§"Fix Category 6: // Reason: Annotations" for usePrefersReducedMotion Option A and the App.tsx:411 REMOVE entry; §"Fix Category 9: Remaining Isolated Fixes" for audioEngine, cueSynth, useAudioCues, SettingsStepper, main.tsx)
    - .planning/phases/07-strict-type-lint-baseline/07-RESEARCH.md (§"no-unnecessary-condition in Production (D-08 flag)", §Open Question 2 — recommends Option A for usePrefersReducedMotion: remove typeof window dead checks; §Open Question 3 — recommends restructuring useAudioCues let newEngine; §Production Errors by Rule — confirms App.tsx:411 is stale)
    - .planning/phases/07-strict-type-lint-baseline/07-CONTEXT.md (D-04: every surviving // eslint-disable preceded by // Reason:; D-07: prefer inline narrowing over helpers, but local non-null assertions WITH // Reason: are allowed; D-08: smallest behavior-preserving fix)
  </read_first>
  <action>
    Eight rule-batched fix passes — apply in order:

    Pass A — usePrefersReducedMotion.ts no-unnecessary-condition (2 errors at lines 7,14):
    - Apply RESEARCH.md Open Question 2 Option A (recommended): remove the `typeof window === 'undefined'` checks entirely. Both are dead code in this Vite SPA. Keep the `!window.matchMedia` guard on its own (it returns falsy if matchMedia is absent, sufficient defensive check).
    - Specific edits: at line 7 (the useState initializer guard) change `if (typeof window === 'undefined' || !window.matchMedia) return false` to `if (!window.matchMedia) return false`. At line 14 (the useEffect early-return) change `if (typeof window === 'undefined' || !window.matchMedia) return` to `if (!window.matchMedia) return`.
    - Do NOT touch line 22 `setReduced(mql.matches)` here — that is the new `set-state-in-effect` error, Plan 04 scope.

    Pass B — audioEngine.ts no-unnecessary-type-assertion (3 errors at lines 125, 233, 241):
    - Apply D-08 KEEP-AND-ANNOTATE pattern (the casts document the WebKit widening per Phase 5.1 D-37; removing them would lose the documentation). For each of the three sites add the two-line preface:
      `// Reason: AudioContextState widened to include WebKit 'interrupted' extension (D-37 / Phase 5.1); cast documents intent even if TS DOM lib does not require it.`
      `// eslint-disable-next-line @typescript-eslint/no-unnecessary-type-assertion`
    - The disable line MUST be on the line IMMEDIATELY above the cast (D-04). Do NOT use a single block disable for all three sites — annotate each individually.

    Pass C — audioEngine.ts no-unnecessary-condition (1 error, line 232):
    - Confirm at lint time which precise expression fires (`opts.onStateChange?.()` per RESEARCH.md). Apply D-08 keep-and-annotate (defensive optional chain has documentation value): add `// Reason: defensive optional chain — onStateChange is typed optional in CreateAudioEngineOptions; the chain documents that intent even where the call site happens to always supply it.` then `// eslint-disable-next-line @typescript-eslint/no-unnecessary-condition`. Do NOT remove the `?.`.

    Pass D — cueSynth.ts no-unnecessary-condition (1 error, line 104):
    - Restructure per PATTERNS.md §"Fix Category 9 / cueSynth.ts:104": change `if (needsSustain && phaseDurationSec !== undefined) { … }` to `if (needsSustain) { /* Reason: phaseDurationSec is always defined when needsSustain is true per outer condition at line 88. */ const dur = phaseDurationSec!; … }` and use `dur` (or `phaseDurationSec!` directly with the // Reason: annotation above the use site) for the body. Add `// Reason:` line above the non-null assertion. Do NOT remove the `needsSustain` check — only the redundant `phaseDurationSec !== undefined` part.

    Pass E — useAudioCues.ts (2 fixes):
    - useAudioCues.ts no-unnecessary-condition at line ~124 (`state === 'interrupted'`): apply D-08 keep-and-annotate. Add `// Reason: explicit state check documents the WebKit-specific 'interrupted' state-machine branch (D-37); TS narrowing here is incidental to the documentation purpose.` then `// eslint-disable-next-line @typescript-eslint/no-unnecessary-condition`.
    - useAudioCues.ts no-useless-assignment at line ~261 (`let newEngine: AudioEngine | null = null`): restructure per PATTERNS.md §"Fix Category 9 / useAudioCues.ts:261". Recommended approach: change to `let newEngine: AudioEngine` (no initializer) and rely on the catch's `return` for definite assignment. Verify TypeScript narrows correctly. If TypeScript complains about definite assignment, use the alternative `let newEngine: AudioEngine | undefined`. Do NOT keep the `= null` initializer.

    Pass F — main.tsx no-non-null-assertion (1 error, line 6):
    - Apply PATTERNS.md §"Fix Category 9 / main.tsx:6" inline-narrowing pattern (NOT the annotated-assertion fallback — D-07 prefers inline narrowing for boilerplate sites). Replace `createRoot(document.getElementById('root')!).render(...)` with:
      `const rootEl = document.getElementById('root')`
      `if (rootEl === null) throw new Error('Root element #root not found in index.html')`
      `createRoot(rootEl).render(...)`
    - This adds a runtime check that throws with a descriptive message if `#root` is missing. Behavior is preserved for the happy path (tag exists in index.html); the error message is more informative than a silent null-deref.

    Pass G — SettingsStepper.tsx no-unnecessary-condition (1 error, line 28):
    - Replace `const nextValue = options[selectedIndex + offset]` with `const nextValue = options.at(selectedIndex + offset)`. This makes the `T | undefined` return type unambiguous to the linter. The downstream `if (nextValue !== undefined)` check then correctly narrows.

    Pass H — App.tsx fixes (2 sites):
    - exhaustive-deps in App.tsx (1 error): locate via `npm run lint 2>&1 | grep -B1 -A3 exhaustive-deps`. Add the missing `audio` identifier to the affected useCallback's dependency array. Do NOT add a disable. The dep is a real dependency; adding it is behavior-preserving (the closure already references `audio`).
    - App.tsx:411 stale disable removal: locate the `// eslint-disable-next-line react-hooks/set-state-in-effect` comment on line ~411 (sits ABOVE the `setStats(updated)` call — RESEARCH.md confirms this comment is unused / stale because the rule does not fire on the suppressed call under react-hooks 7.1.1). REMOVE the entire eslint-disable comment line. Do NOT add a // Reason: annotation (the disable is being removed, not preserved). The surrounding code (the `setStats(updated)` call and the `recordedSessionKeyRef.current = snap.key` assignment that follows) MUST be UNCHANGED. After this edit, `grep -c "set-state-in-effect" src/app/App.tsx` returns exactly 2 (the surviving disables at lines 205 and 390 that Plan 04 will annotate).

    After all eight passes: `npm run lint 2>&1 | grep -E "(no-unnecessary-condition|no-unnecessary-type-assertion|no-non-null-assertion|no-useless-assignment|exhaustive-deps)" | grep -v "\.test\." | grep -v "vitest.setup"` MUST return zero production matches.

    DO NOT touch any test file in this task — Task 2 owns those.
    DO NOT touch the App.tsx:205 or App.tsx:390 react-hooks/set-state-in-effect disables (Plan 04 annotates them).
    DO NOT touch the usePrefersReducedMotion.ts setReduced(mql.matches) call site (Plan 04 adds the set-state-in-effect annotation there as part of BUILD-03).
  </action>
  <verify>
    <automated>npm run lint 2>&amp;1 | grep -E "(no-unnecessary-condition|no-unnecessary-type-assertion|no-non-null-assertion|no-useless-assignment|exhaustive-deps)" | grep -v "\.test\." | grep -v "vitest.setup" | wc -l | tr -d ' ' | grep -qx 0 &amp;&amp; npx tsc --noEmit -p tsconfig.app.json &amp;&amp; npm run build &amp;&amp; npm run test -- --run 2>&amp;1 | tail -5 | grep -E "363 passed" &amp;&amp; node -e "const fs=require('fs');const c=fs.readFileSync('src/app/App.tsx','utf8');const n=(c.match(/set-state-in-effect/g)||[]).length;if(n!==2){console.error('FAIL: expected exactly 2 set-state-in-effect occurrences in App.tsx after stale-disable removal, got '+n);process.exit(1)}console.log('OK: App.tsx has exactly 2 set-state-in-effect (205 + 390 surviving for Plan 04)')"</automated>
  </verify>
  <acceptance_criteria>
    - `npm run lint 2>&1 | grep -E "(no-unnecessary-condition|no-unnecessary-type-assertion|no-non-null-assertion|no-useless-assignment|exhaustive-deps)" | grep -v "\.test\." | grep -v "vitest.setup"` returns ZERO lines (all production-file errors for these 5 rules eliminated)
    - `npx tsc --noEmit -p tsconfig.app.json` and `tsconfig.node.json` exit 0
    - `npm run build` exits 0
    - `npm run test -- --run` reports `363 passed` — D-09 behavior preservation
    - usePrefersReducedMotion.ts: line 22 `setReduced(mql.matches)` is UNCHANGED by this task (Plan 04 scope; verified by `git diff src/hooks/usePrefersReducedMotion.ts | grep -c "set-state-in-effect"` returning 0)
    - Every `// eslint-disable-next-line` ADDED by this task is preceded by a `// Reason:` line on the immediately previous line (verified by `git diff` review; or by `awk` script that scans for any added eslint-disable line and confirms the prior line starts with `// Reason:`)
    - Every non-null assertion (`!`) added by this task is preceded by a `// Reason:` annotation
    - main.tsx: `createRoot(document.getElementById('root')!)` is REPLACED with the inline-narrowing pattern (`grep -c "rootEl === null" src/main.tsx` >= 1 AND `grep -c "getElementById('root')!" src/main.tsx` returns 0)
    - useAudioCues.ts:261 no longer initializes `newEngine = null` (verified by `grep -c "let newEngine.*=.*null" src/hooks/useAudioCues.ts` returning 0)
    - SettingsStepper.tsx:28 uses `.at(` not bracket access for `nextValue` (`grep -c "options.at(selectedIndex" src/components/SettingsStepper.tsx` >= 1)
    - App.tsx: the single exhaustive-deps fix added `audio` to a useCallback deps array (do NOT add a disable; verified by `git diff src/app/App.tsx | grep -c "exhaustive-deps"` returning 0 NEW disable additions)
    - App.tsx:411 stale disable REMOVED — `grep -c "set-state-in-effect" src/app/App.tsx` returns exactly 2 (down from 3 pre-task). The two surviving entries at lines 205 and 390 are UNCHANGED in this task (Plan 04 annotates them).
    - D-09 commit-boundary trio fully green at task end (lint count strictly decreases vs Plan 02 end; production-side rule fires now zero)
  </acceptance_criteria>
  <done>All 16 production-side single/few-occurrence rule errors eliminated by the eight inline-fix passes per PATTERNS.md. SPA SSR dead-code in usePrefersReducedMotion removed. WebKit widening casts in audioEngine preserved with // Reason: documentation. main.tsx now has informative null-check error. useAudioCues let-newEngine restructured. App.tsx exhaustive-deps fix landed (audio added to deps). App.tsx:411 stale disable removed (set-state-in-effect occurrences in App.tsx now exactly 2 — lines 205 and 390 untouched for Plan 04). 363/363 tests; build green.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Fix the 162 test-file ESLint errors — JSON.parse typing, mock non-null annotation, no-extraneous-class on test classes, FakeAudioContext require-await</name>
  <files>src/app/App.persistence.test.tsx, src/storage/settings.test.ts, src/storage/storage.test.ts, src/app/App.audio.test.tsx, src/app/App.wakeLock.test.tsx, src/audio/cueSynth.test.ts, src/audio/audioEngine.test.ts, src/hooks/useAudioCues.test.tsx, vitest.setup.ts</files>
  <read_first>
    - src/app/App.persistence.test.tsx (lines 30-50 — readEnvelope helper using JSON.parse; downstream env?.settings?.bpm member-access sites — this is the bulk of the unsafe-* errors)
    - src/storage/settings.test.ts (full file — JSON.parse + property-access sites; ~remainder of unsafe-* errors)
    - src/storage/storage.test.ts (lines 50-60 — has the existing `expect(JSON.parse(raw!)).toMatchObject({...})` analog at line 56; 2 errors)
    - src/app/App.audio.test.tsx (4 no-extraneous-class errors on FakeAudioContext variant classes; ~13 no-unnecessary-type-assertion sites)
    - src/app/App.wakeLock.test.tsx (require-await sites in async test helpers; non-null assertions added by Plan 01 may need annotation review)
    - src/audio/cueSynth.test.ts (require-await + non-null annotation review)
    - src/audio/audioEngine.test.ts (require-await on async test helpers)
    - src/hooks/useAudioCues.test.tsx (require-await sites)
    - vitest.setup.ts (lines 185-213 — FakeAudioContext class with `suspend = vi.fn(async () => { … })` at line 199 and `close = vi.fn(async () => { … })` at line 203; resume at line 187 has conditional throw and is NOT a require-await target)
    - .planning/phases/07-strict-type-lint-baseline/07-PATTERNS.md (§"Fix Category 7: no-unsafe-member-access on JSON.parse Results in Tests" — toMatchObject preferred; §"Fix Category 8: require-await on FakeAudioContext Methods")
    - .planning/phases/07-strict-type-lint-baseline/07-RESEARCH.md (§"Pitfall 4: Test Files Generating Bulk Unsafe-any Errors from Parsed JSON", §"Pitfall 5: require-await on FakeAudioContext Methods in Tests", §"Test Errors by Rule" — 162 errors with rule breakdown)
    - .planning/phases/07-strict-type-lint-baseline/07-02-PLAN.md (frontmatter — confirm test files were NOT in 07-02 files_modified, so no overlap risk)
  </read_first>
  <action>
    Five test-file fix passes — apply in order:

    Pass A — readEnvelope JSON.parse typing in App.persistence.test.tsx (~37 unsafe-* errors clustered here):
    - Find the `readEnvelope()` helper (PATTERNS.md §"Fix Category 7" shows lines 30-50). Add explicit return type annotation: `function readEnvelope(): Record<string, unknown> | null { … return raw ? (JSON.parse(raw) as Record<string, unknown>) : null }`. Add `// Reason: test helper reads raw localStorage; structural shape validated by downstream test assertions.` directly above the `JSON.parse(...) as Record<string, unknown>` cast (the `as` cast may itself fire `no-unsafe-type-assertion`-class rules — annotate if so).
    - For downstream usage sites (`env?.settings?.bpm`, etc.) — REPLACE with `expect(env).toMatchObject({ settings: { bpm: <expected> } })` wherever the test is asserting on an expected value (this matches the existing analog at storage.test.ts:56 and eliminates the chained unsafe member access entirely). For sites that need the value for further computation (not just assertion), narrow incrementally: `const settings = env?.settings as Record<string, unknown> | undefined; expect(settings?.bpm).toBe(<value>)`.
    - Apply the same readEnvelope-typing pattern to settings.test.ts and storage.test.ts wherever they have a similar JSON.parse helper. The 2 errors in storage.test.ts may require minor adjustment around the existing `JSON.parse(raw!)` site.

    Pass B — non-null assertion annotations in test files (Plan 01 added `!` for tsc fixes; with strictTypeChecked the no-non-null-assertion rule fires on every test `!`):
    - For every `!` non-null assertion in test files (App.audio.test.tsx, App.wakeLock.test.tsx, cueSynth.test.ts, useAudioCues.test.tsx) — including the ones added in Plan 01 — add `// Reason:` line above the assertion AND add `// eslint-disable-next-line @typescript-eslint/no-non-null-assertion` directly above. Reason text examples: "Reason: length asserted by toHaveBeenCalledTimes(N) immediately above.", "Reason: array populated by FakeAudioContext constructor; length is test-invariant.", "Reason: mock.results[0] is the requested promise resolution; presence asserted by spy invocation count above."
    - Approximate count: 42 in tests per RESEARCH.md.
    - Alternative: where Option B narrowing (`.at(0)` + `expect(item).toBeDefined()`) was used in Plan 01, no `!` annotation is needed for the original site — but the post-narrowing usage `item!` still needs the annotation. Apply per actual lint output, not pre-emptive guessing.

    Pass C — no-unsafe-member-access / no-unsafe-assignment remainder (~16+37 spread across test files):
    - Wherever `JSON.parse(...)` returns are not yet covered by Pass A's readEnvelope helper, apply the same `as Record<string, unknown>` cast with `// Reason:` annotation, and prefer `toMatchObject` for assertions over chained property access.
    - Sites that are not JSON.parse but are accessing `any`-typed values from third-party APIs may need `as unknown` + narrowing — handle case by case per lint output. Do NOT use `as unknown as T` double-casts (RESEARCH.md anti-pattern).

    Pass D — no-extraneous-class on FakeAudioContext variants in App.audio.test.tsx (4 errors):
    - Per PATTERNS.md §"No Analog Found" guidance: classes are required for `new FakeAudioContext()` invocation in `vi.spyOn` / window.AudioContext substitution. Fix by adding `// Reason: test double for AudioContext API; class syntax required for new invocation in vi.spyOn / global polyfill context.` then `// eslint-disable-next-line @typescript-eslint/no-extraneous-class` directly above each affected class declaration.

    Pass E — require-await on FakeAudioContext methods in vitest.setup.ts (and any test helper async-without-await):
    - In vitest.setup.ts, the `suspend` (line 199) and `close` (line 203) methods declared as `vi.fn(async () => { … })` have no `await` inside. Add a two-line preface DIRECTLY ABOVE EACH of those two `vi.fn(async …)` lines:
      `// Reason: async signature matches AudioContext.<methodName>() API contract; no real async work in the fake.`
      `// eslint-disable-next-line @typescript-eslint/require-await`
    - Do NOT touch the `resume` method at line 187 — RESEARCH.md confirms it has conditional throw logic that is meaningfully async and does NOT fire require-await.
    - For other test files with `require-await` fires (App.audio.test.tsx, audioEngine.test.ts, useAudioCues.test.tsx — RESEARCH.md table shows ~14 occurrences total across these): if the helper has no real await and the async signature is required for caller `await`-style usage, apply the same disable+Reason pattern. If the `async` is unnecessary (no caller depends on the Promise return type), remove the `async` keyword instead.

    After all five passes, `npm run lint` MUST exit 0 (zero errors, zero warnings). This is the FIRST commit boundary in Phase 7 at which the full D-09 lint-exit-0 invariant is satisfied for all files in src/ + vitest.setup.ts.

    DO NOT touch any production source file in this task — Task 1 owns the production work. The only File overlap risk is App.tsx (Task 1 already removed the line 411 stale disable and added the audio dep — Task 2 must NOT touch App.tsx at all).
    DO NOT touch the App.tsx:205 or App.tsx:390 react-hooks/set-state-in-effect disables (Plan 04 annotates them as part of BUILD-03).
  </action>
  <verify>
    <automated>npm run lint 2>&amp;1 | tail -3 | grep -E "(0 problems|^$)" || (npm run lint; false) &amp;&amp; npx tsc --noEmit -p tsconfig.app.json &amp;&amp; npx tsc --noEmit -p tsconfig.node.json &amp;&amp; npm run build &amp;&amp; npm run test -- --run 2>&amp;1 | tail -5 | grep -E "363 passed"</automated>
  </verify>
  <acceptance_criteria>
    - `npm run lint` exits 0 (zero problems — this is the FULL BUILD-02 lint-exit-0 gate, satisfied for the first time in Phase 7)
    - `npx tsc --noEmit -p tsconfig.app.json` and `tsconfig.node.json` exit 0
    - `npm run build` exits 0
    - `npm run test -- --run` reports `363 passed` — D-09 (test edits must not break test semantics; toMatchObject substitutions preserve assertion intent)
    - vitest.setup.ts: `suspend` (line ~199) and `close` (line ~203) `vi.fn(async () => …)` declarations are EACH preceded by a `// Reason:` line and a `// eslint-disable-next-line @typescript-eslint/require-await` line; verified by `grep -B2 "vi.fn(async" vitest.setup.ts | grep -c "Reason:"` returning >= 2
    - vitest.setup.ts: the `resume` method at line ~187 is UNCHANGED (verified by `git diff vitest.setup.ts` not touching the resume implementation)
    - App.persistence.test.tsx: `readEnvelope` has explicit return type `Record<string, unknown> | null` (verified by `grep -c "function readEnvelope.*Record<string, unknown>" src/app/App.persistence.test.tsx` >= 1)
    - App.tsx: this task did NOT modify App.tsx — verified by `git diff src/app/App.tsx` being empty for this task's commit (Task 1 already handled the App.tsx changes; Plan 04 will handle the remaining App.tsx hook-disable annotations)
    - The 2 react-hooks/set-state-in-effect disables at App.tsx:205 and App.tsx:390 remain UNCHANGED (verified by `grep -c "set-state-in-effect" src/app/App.tsx` returning 2 — same count as end of Task 1)
    - Every `!` non-null assertion in the modified test files is preceded by `// Reason:` and `// eslint-disable-next-line @typescript-eslint/no-non-null-assertion` (verified by an awk/grep script that scans test files for `!` and checks the preceding two lines)
    - Every `// eslint-disable-next-line` added by this task is preceded by `// Reason:` (D-04 enforcement)
    - No new helper file, mock factory module, or shared test utility is added (verified by `git status` showing only existing files modified, no new files in this plan)
    - The total ESLint problem count goes from ~178 (post-Task-1 baseline = post-Plan-02 ~178 minus 16 production isolated fires fixed in Task 1 = ~162 test errors + 1 stale warning, of which Task 1 also removed the warning, leaving ~162 test errors at Task 2 entry) to 0
  </acceptance_criteria>
  <done>npm run lint exits 0 — the full BUILD-02 lint-exit-0 invariant is satisfied for the first time in Phase 7. All 162 test-file ESLint errors eliminated by Pass A-E inline fixes per PATTERNS.md. Behavior preserved (363/363 tests). Build green. App.tsx untouched in this task (Task 1 handled the production-side App.tsx work). The two surviving react-hooks/set-state-in-effect disables at lines 205 and 390 remain untouched for Plan 04 to annotate as part of BUILD-03.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none introduced) | This plan modifies production source (isolated rule fires: dead-SSR-guard removal in a SPA, WebKit-cast preservation with documentation, defensive-optional-chain preservation with documentation, definite-assignment refactor, inline rootEl null-check, deps-array fix, stale-disable removal) and test-only narrowing/typing (typed JSON.parse helpers, annotated non-null assertions, annotated FakeAudioContext require-await disables). No new endpoints, no new auth/authz logic, no new data flows, no new runtime dependencies. Every fix is behavior-preserving inline per D-08. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-03-01 | (n/a) | 7 production source files (isolated rule fires) + ~9 test/setup files (bulk pattern fixes) + App.tsx (stale-disable removal + 1-line deps fix) | accept | No runtime threat surface — all production fixes preserve runtime semantics (WebKit casts kept-and-annotated, defensive optional chains kept-and-annotated, dead SSR guards removed in a verified SPA context with no SSR build target, definite-assignment refactor preserves catch-return contract, main.tsx null-check throws a descriptive error instead of silent null-deref). Test-only fixes do not affect production code. Indirect security tightening: no-non-null-assertion annotations now make EVERY surviving `!` traceable to a documented invariant; no-unsafe-member-access enforcement on JSON.parse results in tests prevents type confusion in test fixtures. Behavior-preservation invariant D-08/D-09 enforced by full Vitest suite (363/363) at every commit boundary. |
</threat_model>

<verification>
## Plan-Level Verification

Run at the end of this plan (before Plan 04 begins):

1. `npm run lint` — exits 0 with zero problems (BUILD-02 lint-exit-0 gate FULLY satisfied for the first time in Phase 7)
2. `npx tsc --noEmit -p tsconfig.app.json && npx tsc --noEmit -p tsconfig.node.json` — both exit 0
3. `npm run build` — exits 0
4. `npm run test -- --run` — reports `363 passed` (D-09 invariant)
5. `grep -c "set-state-in-effect" src/app/App.tsx` returns exactly 2 (the line 205 and line 390 disables; the line 411 stale disable was REMOVED by this plan's Task 1 Pass H)
6. `grep -c "set-state-in-effect" src/hooks/usePrefersReducedMotion.ts` returns 0 (the new annotated disable on the setReduced re-seed call is Plan 04 scope and has NOT yet been added)
7. Total file-modified count by this plan matches the `files_modified` frontmatter (no scope creep into Plan 04 territory: App.tsx:205 and App.tsx:390 disables UNTOUCHED, usePrefersReducedMotion.ts setReduced site UNTOUCHED)
8. Every `// eslint-disable-next-line` added by this plan in production files is preceded by a `// Reason:` line (D-04 enforcement, verified by grep over Task 1 diff)
9. Every `// eslint-disable-next-line` added by this plan in test files is preceded by a `// Reason:` line (D-04 enforcement, verified by grep over Task 2 diff)
</verification>

<success_criteria>
- BUILD-02 fully satisfied: `npm run lint` exits 0; ALL 226 ESLint errors from the original strictTypeChecked baseline (48 cleared in Plan 02 + 16 production isolated cleared in this plan's Task 1 + 162 test-file cleared in this plan's Task 2 + 1 stale disable removed in this plan's Task 1 = 227 fixes including the warning) are eliminated by inline fixes per PATTERNS.md
- D-09 quad green at task and plan boundaries: tsc, lint, build, test (363/363) all green at every commit — and `npm run lint` exits 0 at this plan's end (the FIRST plan boundary in Phase 7 at which the full lint-exit-0 invariant binds)
- App.tsx:411 stale disable removed; lines 205 and 390 preserved untouched for Plan 04 to annotate
- usePrefersReducedMotion.ts setReduced site preserved untouched for Plan 04 to add the set-state-in-effect annotation as part of BUILD-03
- Plan 04 unblocked: only the BUILD-03 hooks-audit work (react-hooks/exhaustive-deps explicit error override + 3 // Reason: annotations on the surviving react-hooks disables) remains for Phase 7 completion
</success_criteria>

<output>
After completion, create `.planning/phases/07-strict-type-lint-baseline/07-03-SUMMARY.md` per the standard summary template, including:
- Pre/post ESLint problem counts (~178 at plan entry → 0 at plan exit)
- Per-rule eliminated counts: production singletons (no-unnecessary-condition × 6, no-unnecessary-type-assertion × 5, no-non-null-assertion × 1, no-useless-assignment × 1, exhaustive-deps × 1, plus 2 dead-SSR-guard removals = 16); test-file bulk fixes (no-non-null-assertion × ~42, no-unsafe-member-access × ~37, no-unsafe-assignment × ~16, require-await × ~14+2, no-unnecessary-type-assertion × ~13, no-extraneous-class × 4 ≈ ~128 fixes); 1 stale disable removal at App.tsx:411
- Test pass count (363/363)
- List of every file with the count of fixes applied
- List of every `// eslint-disable-next-line` added in this plan with its `// Reason:` text and rule name
- Confirmation App.tsx:411 stale disable removed and 205/390 disables UNTOUCHED for Plan 04
- Confirmation usePrefersReducedMotion.ts setReduced site UNTOUCHED for Plan 04 (only the typeof-window dead checks were removed in this plan)
- Note any tasks where the actual lint fire-pattern differed from RESEARCH.md (executor judgment log)
</output>
</content>
</invoke>