---
phase: 10-hooks-identity-effect-hygiene
plan: "02"
type: execute
wave: 2
depends_on: ["01"]
files_modified:
  - src/app/App.tsx
  - src/app/App.audio.test.tsx
  - src/hooks/useSessionEngine.test.tsx
autonomous: true
gap_closure: true
requirements:
  - HOOKS-01
  - HOOKS-02
  - HOOKS-03
  - HOOKS-04
  - HOOKS-05

must_haves:
  truths:
    - "src/app/App.tsx:81 declares `const sessionFrameRef = useRef(session.liveFrame)` (NOT `session.currentFrame`); the ref-updater effect at :82-84 mirrors `session.liveFrame` with deps `[session.liveFrame]` and passes react-hooks/exhaustive-deps (HOOKS-05 surface contract preserved)."
    - "After CR-01 fix, the iOS reconstruction path's audioAnchor math uses LIVE session-elapsed (per-rAF `liveFrame.elapsedMs`) instead of phase-start-frozen `currentFrame.elapsedMs`; the dual-anchor invariant from Plan 06 Task 8 kitchen-sink (visual <-> audio sync) is restored."
    - "src/app/App.audio.test.tsx contains a NEW regression test (in the existing `describe` block surrounding the D-42 (4) test at :579-612) that drives engine reconstruction at a known mid-phase elapsed time (~2000ms into inhale) and asserts the next-boundary `audioTime` passed to `audio.notifyPhaseBoundary` lands within +/- 0.05s of `newAC.currentTime + (expectedRemainingPhaseMs / 1000)`. The test FAILS if sessionFrameRef regresses back to `session.currentFrame`."
    - "src/hooks/useSessionEngine.test.tsx:253-279 cancel-guard test (HOOKS-04 regression lock) is HARDENED with `vi.spyOn(console, 'error')` setup and a positive assertion that React's `setState on unmounted component` warning was NOT emitted (negative-assertion-by-absence eliminated; WR-01 closed). The hardened test FAILS if the top-of-tick `if (cancelled) return` guard at useSessionEngine.ts:102 is removed."
    - "src/app/App.tsx:442-447 leave-running cleanup comment block is updated to match the actual hook lifecycle (engine PERSISTS the snapshot across transition-out; cleanup reads the most-recent running snapshot) — the stale `the engine owns null-out` wording is replaced with prose that aligns with useSessionEngine.ts:79-91 (WR-02 closed)."
    - "Full Vitest suite (existing 390 tests + 1 new regression test = 391 tests) passes; `npm run lint` exits 0; `npx tsc --noEmit -p tsconfig.app.json` exits 0; `npm run build` succeeds."
  artifacts:
    - path: "src/app/App.tsx"
      provides: "CR-01 fix — sessionFrameRef sourced from session.liveFrame (per-rAF live); WR-02 fix — stale lifecycle comment at :442-447 corrected to match engine-owned persistence semantics."
      contains: "sessionFrameRef = useRef(session.liveFrame)"
    - path: "src/app/App.audio.test.tsx"
      provides: "CR-01 regression lock — new test asserts reconstruct-path audioAnchor math uses live session-elapsed, locking the audio-anchor offset against future regressions of the same shape."
      contains: "CR-01"
    - path: "src/hooks/useSessionEngine.test.tsx"
      provides: "WR-01 fix — cancel-guard test hardened with positive console.error absence assertion (negative-assertion-by-absence eliminated)."
      contains: "vi.spyOn(console, 'error')"
  key_links:
    - from: "src/app/App.tsx:81-84 sessionFrameRef"
      to: "session.liveFrame (per-rAF fresh elapsedMs)"
      via: "useRef + useEffect mirror with deps [session.liveFrame]"
      pattern: "sessionFrameRef = useRef\\(session\\.liveFrame\\)"
    - from: "src/app/App.tsx:125-132 onAudioReanchorRequired"
      to: "sessionFrameRef.current.elapsedMs (now LIVE post-fix)"
      via: "synchronous read inside useCallback body"
      pattern: "sessionFrameRef\\.current\\?\\.elapsedMs"
    - from: "src/app/App.audio.test.tsx CR-01 regression test"
      to: "audio.notifyPhaseBoundary({ audioTime })"
      via: "spy on cueSynth boundary-cue scheduling or mock notifyPhaseBoundary"
      pattern: "notifyPhaseBoundary"
---

<objective>
Close the two verifier-surfaced gaps from `10-VERIFICATION.md` (one BLOCKER, one WARNING) plus one stale-comment WARNING, without re-doing any of `10-01-PLAN.md`'s work. The five HOOKS-* requirements remain SATISFIED at the surface level; this plan repairs the semantic regression that the lint contract masked.

Purpose:
- **CR-01 (BLOCKER):** `session.currentFrame` is now per-phase-stable (D-03 memo on `[state.status, cycleKey, phaseKey]`), but `sessionFrameRef` still mirrors it at `src/app/App.tsx:81-84`. The downstream consumer `onAudioReanchorRequired` at `App.tsx:125-132` reads `sessionFrameRef.current?.elapsedMs` as a LIVE value and subtracts it from the new AC's currentTime. With phase-start-frozen elapsedMs, the audio-anchor offset is wrong by up to one phase duration (~4.4s inhale or ~6.5s exhale at default BPM 5.5), causing audio cues to fire seconds late on the iOS reconstruction path (Plan 06 D-31/D-33/D-35). Fix: switch `sessionFrameRef` to mirror `session.liveFrame` — the boundary effect at App.tsx:494 already reads `session.currentFrame` directly, so removing sessionFrameRef from the currentFrame chain is safe.
- **WR-01 (WARNING):** The HOOKS-04 cancel-guard regression-lock test at `useSessionEngine.test.tsx:253-279` exercises the post-unmount rAF path but lacks a positive assertion that React's `setState on unmounted component` console.error was not emitted. Vitest does NOT fail on `console.error` by default — a regression that removes `if (cancelled) return` would silently leave this test green. Fix: add a `vi.spyOn(console, 'error')` setup + an assertion that no `unmounted` warning was logged.
- **WR-02 (WARNING):** Stale comment at `App.tsx:442-447` says `the engine owns null-out` — but the engine deliberately does NOT null on transition out (`useSessionEngine.ts:79-91` commentary). Fix: update the comment to match the actual persistence behavior.
- **Regression test (mandatory pair with CR-01 fix):** Extend the existing D-42 (4) reconstruction test block at `App.audio.test.tsx:579-612` with a new test that drives reconstruction at a known mid-phase elapsed time and asserts the next-boundary `audioTime` matches `newAC.currentTime + remainingPhaseMs/1000` within +/- 0.05s. This locks the audio-anchor math against the same regression shape recurring.

Output:
- `src/app/App.tsx` with the 2-character source change at :81-84 (`currentFrame` -> `liveFrame`, both occurrences) and a corrected comment block at :442-447.
- `src/app/App.audio.test.tsx` with one new regression test asserting the reconstruct-path audioAnchor math.
- `src/hooks/useSessionEngine.test.tsx` with the cancel-guard test hardened by a positive console.error absence assertion.
- All four quality gates green: `npm run lint`, `npx tsc --noEmit -p tsconfig.app.json`, `npm run build`, `npm run test -- --run`.
</objective>

<execution_context>
@$HOME/.claude-personal-personal/get-shit-done/workflows/execute-plan.md
@$HOME/.claude-personal-personal/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/10-hooks-identity-effect-hygiene/10-CONTEXT.md
@.planning/phases/10-hooks-identity-effect-hygiene/10-RESEARCH.md
@.planning/phases/10-hooks-identity-effect-hygiene/10-PATTERNS.md
@.planning/phases/10-hooks-identity-effect-hygiene/10-VERIFICATION.md
@.planning/phases/10-hooks-identity-effect-hygiene/10-REVIEW.md
@.planning/phases/10-hooks-identity-effect-hygiene/10-01-SUMMARY.md
@.planning/phases/10-hooks-identity-effect-hygiene/10-VALIDATION.md

<interfaces>
<!-- Contracts the executor needs. Extracted from src/app/App.tsx and src/hooks/useSessionEngine.ts. -->
<!-- The executor should use these directly — no codebase exploration needed. -->

From src/hooks/useSessionEngine.ts (current shape post-10-01):
```typescript
// :31-35
export interface RunningSnapshot {
  key: string
  startedAtMs: number
  lastElapsedMs: number
}

// SessionEngine surface (excerpt — current vs. live frame split):
// :50      liveFrame: SessionFrame | null         // per-rAF — what visual consumers + LIVE elapsedMs readers want
// :60      runningSnapshotRef: RefObject<RunningSnapshot | null>
// :165-170 currentFrame: useMemo, deps [state.status, cycleKey, phaseKey]   // per-PHASE-STABLE — boundary effect consumer
// :175     liveFrame: state.status === 'running' ? state.lastFrame : null   // direct read, no memo
```

From src/app/App.tsx (current state — CR-01 site):
```typescript
// :81-84  THE OFFENDING LINES (must be changed):
const sessionFrameRef = useRef(session.currentFrame)
useEffect(() => {
  sessionFrameRef.current = session.currentFrame
}, [session.currentFrame])

// :125-132  THE CONSUMER (must NOT be modified — it stays the same, the source flips):
const onAudioReanchorRequired = useCallback((newAudioAnchor: number) => {
  const elapsedMs = sessionFrameRef.current?.elapsedMs ?? 0
  audioAnchorRef.current = newAudioAnchor - elapsedMs / 1000
}, [])

// :489-536  THE OTHER CONSUMER (must NOT be affected — reads session.currentFrame DIRECTLY, not via sessionFrameRef):
//   line 494: const frame = session.currentFrame
//   line 518: const audioTime = audioAnchor + boundaryStartMs / 1000
//   line 535: audioNotifyPhaseBoundary({ newPhase, audioTime: clampedAudioTime, phaseDurationSec })
```

From src/hooks/useSessionEngine.test.tsx (current shape — WR-01 site):
```typescript
// :253-279  rAF cancel-guard test (HOOKS-04 regression lock):
it('rAF cancel-guard: tick after teardown is a no-op (HOOKS-04 D-10)', () => {
  const { result, unmount } = renderHook(() => useSessionEngine(defaultSettings))
  act(() => { result.current.start() })
  act(() => { vi.advanceTimersByTime(1_000) })
  const frameBeforeUnmount = result.current.currentFrame
  expect(frameBeforeUnmount).not.toBeNull()
  unmount()
  // After unmount: regression that removed the guard would surface as a console.error from React.
  // CURRENT GAP (WR-01): assertion is implicit — no spy, no positive check.
  act(() => { vi.advanceTimersByTime(1_000) })
})
```

From src/app/App.audio.test.tsx (D-42 (4) test — the block to extend at :579-612):
```typescript
// The existing reconstruction test installs a tracked AC, drives a resume rejection,
// fires visibilitychange + click(muteButton) to escalate to reconstruction, and asserts
// only that a new AC was constructed. The NEW regression test must extend this pattern
// to also capture and assert the audioTime argument passed to the next notifyPhaseBoundary
// call after reconstruction.
//
// Available helpers in scope:
//   - installTrackedAC()           -> tracker with .instances[], .constructed(), .restore()
//   - startAndAdvancePastLeadIn()  -> fires Start click + advances 3000ms lead-in
//   - muteButton()                 -> resolves the MuteToggle button by accessible name
//   - vi.advanceTimersByTime(ms)   -> moves fake timers forward
//   - cueSynth is imported as `* as cueSynth` at the top — already spy-able via vi.spyOn(cueSynth, ...)
//
// SessionSettings default at default BPM 5.5 + ratio 40:60:
//   - inhaleMs ~ 4363ms; exhaleMs ~ 6545ms; cycleMs ~ 10909ms.
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: CR-01 fix — switch sessionFrameRef to mirror session.liveFrame</name>
  <files>src/app/App.tsx</files>
  <read_first>
    - src/app/App.tsx lines 60-150 (the verbose comment block at :70-80, the offending sessionFrameRef at :81-84, AND the onAudioReanchorRequired consumer at :125-132 — must see all three together to understand the data flow being repaired).
    - src/hooks/useSessionEngine.ts lines 160-180 (the `currentFrame` useMemo at :165-170 and the `liveFrame` direct read at :175 — to confirm `liveFrame` is the per-rAF surface the fix points to).
    - .planning/phases/10-hooks-identity-effect-hygiene/10-VERIFICATION.md (CR-01 evidence trace at "Code-Review BLOCKER Inspection: CR-01" section — particularly steps 1-7 and the "Fix per review (App.tsx:81-84)" code block).
  </read_first>
  <action>
    Edit `src/app/App.tsx` to switch the `sessionFrameRef` source from `session.currentFrame` (per-phase-stable, frozen elapsedMs) to `session.liveFrame` (per-rAF, fresh elapsedMs). Three line edits across a contiguous 4-line block (lines 81-84):

    BEFORE (lines 81-84):
    - line 81: `useRef(session.currentFrame)` -> `useRef(session.liveFrame)`
    - line 83: `sessionFrameRef.current = session.currentFrame` -> `sessionFrameRef.current = session.liveFrame`
    - line 84: `}, [session.currentFrame])` -> `}, [session.liveFrame])`

    Do NOT modify:
    - The verbose comment block at :70-80 (it correctly describes the SEMANTIC intent — subtracting the live session-elapsed visual offset — which is precisely what the fix restores).
    - The `onAudioReanchorRequired` callback at :125-132 (the consumer is correct; only the source of `sessionFrameRef` is wrong).
    - The boundary effect at :489-536 (already reads `session.currentFrame` directly at :494, not via sessionFrameRef — verified-safe per VERIFICATION.md Key Link table). Specifically, the boundary effect's body at :494 (`const frame = session.currentFrame`) AND the same effect's dep-array entry at :536 (`session.currentFrame` alongside `appPhase`, `audioNotifyPhaseBoundary`, `audioAudioNow`) MUST remain intact — these are the 2 surviving occurrences of `session.currentFrame` after this task completes (down from 5 pre-fix). HOOKS-05 lint contract REQUIRES the dep-array entry to remain — do not "clean it up" alongside the sessionFrameRef switch.

    No other edits in this task. WR-02 comment update is Task 4 to keep the CR-01 fix surgically isolated and bisectable.

    Why the fix is mechanically safe (per VERIFICATION.md CR-01 evidence steps 3-4):
    - `sessionFrameRef` has exactly ONE consumer in App.tsx: `onAudioReanchorRequired` at :125-132, which needs LIVE `elapsedMs`.
    - The boundary effect at :494 reads `session.currentFrame` DIRECTLY (not via sessionFrameRef). It benefits from per-phase memoization and is unaffected by this switch.
    - HOOKS-05 surface contract (explicit deps, lint passes) is preserved — `[session.liveFrame]` is just as explicit as `[session.currentFrame]` from `react-hooks/exhaustive-deps`'s perspective.

    Per requirement IDs (frontmatter): this is the literal-vs-semantic repair of HOOKS-05. The surface contract (explicit deps + lint passes) remains satisfied; the data-flow contract (the elapsedMs feeding `onAudioReanchorRequired` is live) is restored.
  </action>
  <verify>
    <automated>npm run lint 2>&amp;1 | tail -5; echo "---"; npx tsc --noEmit -p tsconfig.app.json 2>&amp;1 | tail -5; echo "---"; grep -n 'sessionFrameRef = useRef\|sessionFrameRef\.current = session\|}, \[session\.\(live\|current\)Frame\])' src/app/App.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'sessionFrameRef = useRef(session.liveFrame)' src/app/App.tsx` returns exactly 1 match (at the post-edit line containing the useRef declaration).
    - `grep -n 'sessionFrameRef.current = session.liveFrame' src/app/App.tsx` returns exactly 1 match.
    - `grep -c 'session.currentFrame' src/app/App.tsx` returns exactly 2 (down from 5 pre-fix). The 2 surviving occurrences are: (a) the boundary effect's direct read at the line containing `const frame = session.currentFrame`, and (b) that same effect's dep array containing `session.currentFrame` alongside `appPhase`, `audioNotifyPhaseBoundary`, `audioAudioNow`. NEITHER survivor is within the `sessionFrameRef = useRef(...)` block or its updater `useEffect(...)`. Verify by running `grep -n 'session.currentFrame' src/app/App.tsx` and confirming the two reported line numbers both fall inside the boundary-effect span (around lines 489-536), NOT inside the sessionFrameRef block (lines 81-84).
    - `grep -c 'session.liveFrame' src/app/App.tsx` increases by 3 relative to pre-fix (the three new sessionFrameRef references at :81/:83/:84), AND `grep -n 'sessionFrameRef' src/app/App.tsx | head -3` shows all three references on contiguous lines (the `useRef` call on line 81, the updater effect body assignment, and the updater effect's dep array — all within the 4-line block at :81-84).
    - `npm run lint` exits 0 (no `react-hooks/exhaustive-deps` warning on the new `[session.liveFrame]` deps).
    - `npx tsc --noEmit -p tsconfig.app.json` exits 0 (no type errors; `session.liveFrame` and `session.currentFrame` share type `SessionFrame | null`).
    - `npx vitest run src/app/App.audio.test.tsx` exits 0 (the existing 21-test audio suite, including D-42 (4) at :579-612, still passes — the fix does not regress reconstruction behavior).
  </acceptance_criteria>
  <done>App.tsx:81-84 mirrors `session.liveFrame` instead of `session.currentFrame`. Lint, TypeScript, and the existing audio test suite remain green. The CR-01 semantic regression is closed at the source level. The next task adds the regression test that locks the fix.</done>
</task>

<task type="auto">
  <name>Task 2: CR-01 regression test — assert reconstruct-path audioAnchor math uses live elapsed</name>
  <files>src/app/App.audio.test.tsx</files>
  <read_first>
    - src/app/App.audio.test.tsx lines 1-60 (imports — note `import * as cueSynth from '../audio/cueSynth'` at :7; helper `startAndAdvancePastLeadIn` at :29-36; default `vi.useFakeTimers()` setup).
    - src/app/App.audio.test.tsx lines 575-615 (the existing D-42 (4) test at :579-612 — the new test should be inserted INSIDE the same `describe` block, immediately after the existing D-42 (4) test, and before the D-42 (5) test that begins at :614).
    - src/app/App.tsx lines 60-150 (the sessionFrameRef -> onAudioReanchorRequired chain under test — post-Task-1 the ref mirrors `session.liveFrame`).
    - src/app/App.tsx lines 485-536 (the boundary effect that calls `audioNotifyPhaseBoundary({ ..., audioTime: clampedAudioTime, ... })` at :535 — this is the call site the new test must capture).
    - .planning/phases/10-hooks-identity-effect-hygiene/10-VERIFICATION.md (CR-01 missing item 2: "Add a regression test in App.audio.test.tsx that drives engine reconstruction at a known mid-phase elapsed time, then asserts the next-boundary `audioTime` argument passed to `audio.notifyPhaseBoundary` matches the new AC's currentTime within an epsilon").
  </read_first>
  <action>
    Add ONE new test inside the existing `describe` block in `src/app/App.audio.test.tsx`, immediately after the D-42 (4) test (ends at :612) and before the D-42 (5) test (begins at :614). Test name: `"CR-01 (Phase 10 gap closure): reconstruction at mid-phase re-anchors using LIVE session-elapsed, not phase-start-frozen elapsed"`.

    Test structure (the executor MUST adapt the harness to the existing patterns in this file — do NOT introduce new helpers if existing ones suffice):

    1. Install a tracked AudioContext via `installTrackedAC()` and `render(<App />)`.
    2. Spy on `audio.notifyPhaseBoundary` calls. Two viable approaches — pick whichever fits the existing test harness:
       a. Spy on the underlying `cueSynth` module function that `notifyPhaseBoundary` ultimately calls (the file already does `import * as cueSynth from '../audio/cueSynth'` at :7 — pattern is in use elsewhere in this file).
       b. If `notifyPhaseBoundary` resolves to a property assignment that's not directly spyable on `cueSynth`, follow the pattern used by adjacent tests in the same `describe` block — the file already contains spies on cueSynth methods; the executor MUST mirror that pattern.
    3. `await startAndAdvancePastLeadIn()` to begin the session at t=0 of the In phase.
    4. Advance fake timers to land mid-inhale (see step 7 for the precise mid-phase point derived from `createBreathingPlan(defaultSettings).inhaleMs`).
    5. Trigger reconstruction using the SAME path as the existing D-42 (4) test (lines 590-608): `_simulateInterrupted()`, `_simulateResumeReject('InvalidStateError')`, dispatch `visibilitychange`, click `muteButton()` — adapt timing so the reconstruction fires AFTER the timer advance, not before.
    6. **Capture `currentTime` synchronously at the reconstruction instant.** Install `vi.spyOn` on the `onAudioReanchorRequired` callback (or the underlying `audio.notifyPhaseBoundary` / new-AC-construction path) BEFORE step 5 fires reconstruction. In the spy's implementation, read `tracker.instances[tracker.instances.length - 1].currentTime` synchronously and stash it into a test-scoped variable (e.g., `let capturedAcNow: number | null = null; spy.mockImplementation((...args) => { capturedAcNow = tracker.instances[tracker.instances.length - 1].currentTime; return originalImpl(...args) })`). This avoids any drift between the reconstruction event-loop turn and the subsequent boundary tick. If a synchronous spy is impractical for the existing test harness (e.g., the reanchor callback is not directly spyable because it's captured by useCallback closure and not exported), document why in an inline test comment and use the post-reconstruction read with an enlarged epsilon (~0.1s) AND assert that the FakeAudioContext's `currentTime` did not advance by more than 1 fake tick between reconstruction and read (positive guard: `expect(tracker.instances[last].currentTime - capturedAcNow).toBeLessThan(0.05)` or equivalent — depends on the FakeAudioContext tick granularity in the test harness).
    7. **Derive expected values from `createBreathingPlan(defaultSettings)` (primary path).** Import `createBreathingPlan` and the same `defaultSettings` used by the test harness (typically from `src/audio/breathingPlan.ts` or wherever the production code derives them). Compute `plan = createBreathingPlan(defaultSettings)` once at test setup. Use `plan.inhaleMs` to compute `elapsedAtReconstructionMs = plan.inhaleMs * 0.45` (a stable mid-phase point that survives ratio tweaks) and `expectedRemainingMs = plan.inhaleMs - elapsedAtReconstructionMs`. The epsilon stays at 0.05s. In step 4, advance fake timers by `elapsedAtReconstructionMs` (rounded to nearest integer ms) so the reconstruction lands at the derived mid-phase point. Only if importing `createBreathingPlan` is structurally infeasible (e.g., it pulls in side effects, has a circular import that breaks the test harness, or the import resolution fails in the test environment), fall back to hardcoded literals (`inhaleMs ≈ 4363ms`, `elapsedAtReconstructionMs ≈ 2000ms`, `expectedRemainingMs ≈ 2363ms`) AND add an inline comment in the test explaining the derivation source AND why the import was infeasible.
    8. Advance fake timers past the inhale boundary so the boundary effect fires and calls `audioNotifyPhaseBoundary` with `audioTime = audioAnchor + boundaryStartMs/1000` (App.tsx:518) where `boundaryStartMs = cycleIndex * cycleMs + inhaleMs` for the Out boundary.
    9. Capture the `audioTime` argument from the latest `notifyPhaseBoundary` call recorded by the spy.
    10. Assert: `Math.abs(audioTime - (capturedAcNow + expectedRemainingMs / 1000)) < 0.05` (50ms epsilon).

    Per requirement IDs (frontmatter): this test pairs with Task 1's source fix to lock the literal-vs-semantic repair of HOOKS-05. With CR-01 fixed (`liveFrame.elapsedMs` is live), the assertion passes. With CR-01 reverted (`currentFrame.elapsedMs` frozen at phase start = 0), `audioAnchorRef.current` would be set to `newAudioAnchor - 0/1000 = capturedAcNow`, making the eventual `audioTime ~ capturedAcNow + inhaleMs/1000` — off by `expectedRemainingMs/1000 - inhaleMs/1000 = -elapsedAtReconstructionMs/1000 ~ -1.96s` (with `elapsedAtReconstructionMs ~ 1963ms` at the 45%-of-inhale derivation point). The 0.05s epsilon comfortably rejects the regression.

    Reason: the assertion shape `audioTime ~ capturedAcNow + remaining/1000` is the SAME math the verbose App.tsx:70-80 comment describes ("yields audioTime ~ newAC.currentTime at the upcoming boundary"). The test encodes the comment's contract into an executable expectation.

    DO NOT introduce a second test for the D-42 (5) plain-resume path — that test exists at App.audio.test.tsx:614+ and is unaffected by CR-01.
  </action>
  <verify>
    <automated>npx vitest run src/app/App.audio.test.tsx -t "CR-01" 2>&amp;1 | tail -15; echo "---"; npx vitest run src/app/App.audio.test.tsx 2>&amp;1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'CR-01' src/app/App.audio.test.tsx` returns at least 1 (the new test name contains `CR-01`).
    - `npx vitest run src/app/App.audio.test.tsx -t "CR-01"` exits 0 with exactly 1 test passing (the new regression test).
    - `npx vitest run src/app/App.audio.test.tsx` reports 22 tests passing (existing 21 + 1 new).
    - The test contains a numeric epsilon assertion of the form `Math.abs(audioTime - (capturedAcNow + expectedRemaining / 1000)) < 0.05` (or equivalent — the literal `< 0.05` or `0.05` must appear in the assertion).
    - The test contains an `_simulateInterrupted` or equivalent reconstruction-trigger call mirroring the D-42 (4) test pattern.
    - Expected `audioTime` value is derived from `createBreathingPlan(defaultSettings)` at test setup (the test file imports `createBreathingPlan` AND references `plan.inhaleMs` to compute `elapsedAtReconstructionMs` and `expectedRemainingMs`), OR the test contains an explicit inline comment justifying a literal fallback (with the comment naming both the derivation source and the specific infeasibility reason). The literal-fallback escape hatch is acceptable ONLY when accompanied by the justifying comment.
    - The `currentTime` capture is performed synchronously at reconstruction (via a `vi.spyOn` installed BEFORE the reconstruction trigger), OR the test contains an inline comment justifying a post-reconstruction read AND asserts the FakeAudioContext's `currentTime` did not advance by more than one fake tick between reconstruction and read.
    - Reverting Task 1's source change (sessionFrameRef -> session.currentFrame) MUST cause this test to fail. Executor MUST verify this once in a scratch run by temporarily reverting App.tsx:81-84, running `npx vitest run src/app/App.audio.test.tsx -t "CR-01"` to confirm a FAIL, then restoring the Task 1 fix. The scratch revert MUST NOT be committed.
  </acceptance_criteria>
  <done>App.audio.test.tsx contains a new regression test that asserts the reconstruct-path audioAnchor math uses live session-elapsed. The test passes with Task 1's fix in place and fails when sessionFrameRef regresses to mirror session.currentFrame. The CR-01 BLOCKER is closed and locked.</done>
</task>

<task type="auto">
  <name>Task 3: WR-01 fix — harden cancel-guard test with positive console.error absence assertion</name>
  <files>src/hooks/useSessionEngine.test.tsx</files>
  <read_first>
    - src/hooks/useSessionEngine.test.tsx lines 1-30 (imports — `vi` is already imported from vitest at :2; `beforeEach`/`afterEach` setup at :13-21 uses `vi.useFakeTimers()` and `vi.useRealTimers()`).
    - src/hooks/useSessionEngine.test.tsx lines 245-285 (the existing cancel-guard test at :253-279 — the target of this hardening edit).
    - src/hooks/useSessionEngine.ts lines 95-150 (the top-of-tick cancel-guard at :102 and the defensive recheck at :137 — context for what the test is locking).
    - .planning/phases/10-hooks-identity-effect-hygiene/10-VERIFICATION.md (WR-01 row in the Anti-Patterns table at lines 144-145, AND the missing-item description in the gaps frontmatter).
  </read_first>
  <action>
    Modify the existing `'rAF cancel-guard: tick after teardown is a no-op (HOOKS-04 D-10)'` test at `src/hooks/useSessionEngine.test.tsx:253-279` to add a positive assertion that React's `setState on unmounted component` warning was NOT emitted.

    Specific edits:

    1. At the top of the test body (immediately after the line `const { result, unmount } = renderHook(...)`, before `act(() => { result.current.start() })`), insert:
       `const consoleErrorSpy = vi.spyOn(console, 'error').mockImplementation(() => {})`
       (The `mockImplementation(() => {})` swallows the output so a real regression's React warning does not pollute test output; the spy still records the call so the assertion can read it.)

    2. After the post-unmount `act(() => { vi.advanceTimersByTime(1_000) })` call (currently the last statement of the test at :276-278), add a positive assertion:
       ```
       expect(consoleErrorSpy).not.toHaveBeenCalledWith(
         expect.stringContaining('unmounted'),
       )
       ```
       Match `'unmounted'` (lowercase substring) — React 19's warning string for `setState`-on-unmounted scenarios contains the word `unmounted` regardless of the exact phrasing variant. The substring matcher is robust to React minor-version wording changes.

    3. End the test body with `consoleErrorSpy.mockRestore()` to release the spy. (The outer `afterEach(() => { vi.useRealTimers() })` at :19-21 does NOT restore spies — `vi.restoreAllMocks()` is not in this file's afterEach, so explicit restore is required to avoid cross-test pollution.)

    Reason: Vitest does NOT fail tests on uncaught console.error by default. The existing test exercises the post-unmount rAF code path but has no positive assertion that the guard prevented the React warning. A regression that removes `if (cancelled) return` at useSessionEngine.ts:102 would surface as a console.error call containing `unmounted`. With the spy + assertion, that regression becomes a test failure.

    Verify-the-test-has-teeth step (mandatory): after committing the hardened test, in a SCRATCH run (do NOT commit this break):
    1. Comment out `if (cancelled) return` at useSessionEngine.ts:102.
    2. Run `npx vitest run src/hooks/useSessionEngine.test.tsx -t "rAF cancel-guard"`.
    3. Confirm the test now FAILS with the new positive assertion (the spy captures React's warning).
    4. Restore `if (cancelled) return`.
    5. Confirm the test now PASSES again.

    DO NOT touch the existing 5 tests at lines 22-137 or the other 5 identity tests in the new describe block (lines 156-251 and 281-end). The edit is confined to the body of ONE test at :253-279.

    Per requirement IDs (frontmatter): this is the WR-01 closure for HOOKS-04 — the cancel-guard structural contract was satisfied by 10-01, but the regression-lock test is now hardened against silent regression.
  </action>
  <verify>
    <automated>npx vitest run src/hooks/useSessionEngine.test.tsx -t "rAF cancel-guard" 2>&amp;1 | tail -10; echo "---"; grep -n "consoleErrorSpy\|spyOn(console" src/hooks/useSessionEngine.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "vi.spyOn(console, 'error')" src/hooks/useSessionEngine.test.tsx` returns at least 1.
    - `grep -c "expect.stringContaining('unmounted')" src/hooks/useSessionEngine.test.tsx` returns at least 1 (the positive assertion).
    - `grep -c "consoleErrorSpy.mockRestore" src/hooks/useSessionEngine.test.tsx` returns at least 1 (explicit cleanup).
    - `npx vitest run src/hooks/useSessionEngine.test.tsx -t "rAF cancel-guard"` exits 0 with the test passing.
    - `npx vitest run src/hooks/useSessionEngine.test.tsx` reports the same total test count as before this task (no tests added or removed — the existing test is hardened in place).
    - Scratch verification (executor logs evidence, does NOT commit): temporarily commenting out `if (cancelled) return` at useSessionEngine.ts:102 causes this specific test to FAIL with the new spy assertion; restoring the guard returns the test to PASS.
  </acceptance_criteria>
  <done>The HOOKS-04 cancel-guard test now has a positive console.error absence assertion. A regression that removes the top-of-tick cancel-guard surfaces as a test failure (verified via scratch revert). WR-01 closed.</done>
</task>

<task type="auto">
  <name>Task 4: WR-02 fix — correct stale leave-running cleanup comment</name>
  <files>src/app/App.tsx</files>
  <read_first>
    - src/app/App.tsx lines 425-475 (the leave-running cleanup effect — the comment block at :442-447 is the target).
    - src/hooks/useSessionEngine.ts lines 75-95 (the engine's transition-out behavior — the `useEffect` at :79-91 explicitly RETURNS without nulling `runningSnapshotRef.current` on transition out; the inline comment at :81-91 explains why hook-effect ordering vs. consumer-effect ordering forced the persistence choice).
    - .planning/phases/10-hooks-identity-effect-hygiene/10-VERIFICATION.md (WR-02 row in the Anti-Patterns table at line 146: stale comment says "the engine owns null-out" but the engine deliberately does NOT null on transition out).
  </read_first>
  <action>
    Replace the stale lifecycle commentary in the leave-running cleanup block at `src/app/App.tsx:442-447`. The current comment reads (approximate — executor MUST read the actual file content):

    > // ... App-side no-longer null the ref here; the engine owns null-out.

    The claim "the engine owns null-out" is FALSE — the engine's transition-out branch at useSessionEngine.ts:79-91 explicitly returns WITHOUT nulling the snapshot ref, with an inline comment at :81-91 explaining why (hook-effect ordering: hook useEffects fire BEFORE consumer-component useEffects, so nulling on transition out would clobber `runningSnapshotRef.current` BEFORE App's cleanup effect reads it).

    Rewrite the comment to match the actual lifecycle. The executor MUST adapt the wording to fit the surrounding sentence flow; the substantive claims that MUST be present are listed below as content requirements, not as a verbatim string.

    Required claims (substance, not exact wording — executor MUST preserve the surrounding comment context that explains LOCL-02 + HOOKS-02 stats writing):
    1. The snapshot ref PERSISTS across the transition out of running (engine does NOT null on transition out).
    2. The persistence is intentional — hook useEffects fire BEFORE consumer useEffects, so nulling on transition-out would clobber the snapshot before App's cleanup effect reads it. (Cross-reference: useSessionEngine.ts:79-91 commentary.)
    3. The snapshot is overwritten on the next session's first rAF tick (the in-updater write inside `tick`), so reading a stale snapshot on a subsequent idle render is safe; `recordedSessionKeyRef.current` dedupes idempotently via the snapshot's `key`.
    4. Preserve the existing surrounding context about the 'complete' vs. 'idle' (manual End) elapsedMs branches at App.tsx:455-460 and the cancel-during-lead-in guard at :450 — those statements are correct and must NOT be lost in the rewrite.

    The rewrite is COMMENT-ONLY — no executable code in this block changes. The behavior at :454-464 (read snap, branch on completedAtMs, recordSession, setStats, set recordedSessionKeyRef) is correct as-is.

    DO NOT modify:
    - Code at :454-464 (the stats-recording branch — correct as shipped).
    - The other comments in this effect (e.g., the `// Phase 4 LOCL-02 + Phase 10 HOOKS-02 (D-06/D-09)` header at :442 — keep the requirement-ID cross-references; only the specific claim about "engine owns null-out" is stale and must be replaced).
    - Any other file (this task is App.tsx comment-only).

    Reason: stale comments mislead future maintainers. The implementation chose snapshot persistence for the hook-vs-consumer effect-ordering reason described in useSessionEngine.ts:79-91; the App-side comment was not updated when that decision landed. WR-02 is cheap and reduces future maintainer confusion when bisecting subtle effect-ordering bugs.

    Per requirement IDs (frontmatter): this is a documentation-only correction adjacent to HOOKS-02; the requirement remains SATISFIED (the behavior was correct, only the comment was wrong).
  </action>
  <verify>
    <automated>grep -n "engine owns null-out" src/app/App.tsx; echo "---should-be-empty---"; grep -n "persists across the transition\|persists across transition\|snapshot persists\|engine does NOT null\|does not null on transition" src/app/App.tsx; echo "---"; npx tsc --noEmit -p tsconfig.app.json 2>&amp;1 | tail -3; echo "---"; npm run lint 2>&amp;1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "engine owns null-out" src/app/App.tsx` returns 0 (the stale claim is gone).
    - `grep -c "persists across\|does NOT null\|does not null" src/app/App.tsx` returns at least 1 (the corrected claim is present in some form).
    - `grep -c "useSessionEngine.ts" src/app/App.tsx` returns at least 1 within the cleanup-effect comment block (cross-reference to the engine's persistence rationale).
    - `npx tsc --noEmit -p tsconfig.app.json` exits 0 (comment-only edit cannot break types, but verify regardless).
    - `npm run lint` exits 0.
    - `npm run test -- --run` exits 0 (full suite green; the comment edit does not affect runtime behavior).
  </acceptance_criteria>
  <done>The stale "engine owns null-out" claim is removed from App.tsx:442-447; the replacement prose matches the actual engine-persists-on-transition-out lifecycle and cross-references useSessionEngine.ts:79-91. WR-02 closed. All four quality gates remain green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| iOS Safari AudioContext suspend/resume/reconstruct | The reconstruct path is the regression vector — a hostile or unexpected sequence of `_simulateInterrupted` -> `_simulateResumeReject` -> visibilitychange -> reconstruct must produce correct audio-anchor math. CR-01 is precisely a failure at this boundary. |
| Test harness console.error stream | Vitest does not fail on console.error by default; the WR-01 gap exploited this gap-in-the-gates. Task 3 turns the boundary into an explicit positive assertion. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-02-01 | Tampering (data-flow regression) | src/app/App.tsx sessionFrameRef chain | mitigate | Task 1 switches the source to session.liveFrame; Task 2 locks the contract with an executable regression test asserting the audio-anchor math (epsilon < 0.05s). |
| T-10-02-02 | Information disclosure (silent regression) | src/hooks/useSessionEngine.test.tsx cancel-guard test | mitigate | Task 3 adds vi.spyOn(console, 'error') + positive `not.toHaveBeenCalledWith(stringContaining('unmounted'))` assertion. Verified-with-teeth via scratch revert of useSessionEngine.ts:102. |
| T-10-02-03 | Repudiation (misleading docs) | src/app/App.tsx:442-447 stale comment | mitigate | Task 4 rewrites the comment to match the actual engine-persists semantics and cross-references useSessionEngine.ts:79-91. |
| T-10-02-04 | Denial of service (test pollution from unrestored spies) | src/hooks/useSessionEngine.test.tsx | mitigate | Task 3's third acceptance criterion requires `consoleErrorSpy.mockRestore()` because the file's afterEach does not call `vi.restoreAllMocks()`. Without this, the spy persists into subsequent tests. |
| T-10-02-05 | Elevation of privilege (out-of-scope expansion) | Plan boundary | accept | This plan explicitly EXCLUDES WR-03 (useAudioCues.ts:208-212 pre-existing recovery gap), WR-04 (useSessionEngine.ts:124-139 defensive recheck), and IN-02 (SpyableAC duplication). They are documented as out-of-scope in 10-VERIFICATION.md and tracked for a future cleanup phase. |
</threat_model>

<verification>
After all four tasks complete, run the full phase-10 verification suite to confirm all gates close cleanly:

```
npm run lint               # exit 0
npx tsc --noEmit -p tsconfig.app.json   # exit 0
npm run build              # successful
npm run test -- --run      # 391 tests pass (390 existing + 1 new CR-01 regression)
```

Spot-check each closed gap:

```
# CR-01 source repair landed
grep -n 'sessionFrameRef = useRef(session.liveFrame)' src/app/App.tsx

# CR-01 regression test landed and passes
npx vitest run src/app/App.audio.test.tsx -t "CR-01"

# WR-01 cancel-guard hardening landed
grep -n "expect.stringContaining('unmounted')" src/hooks/useSessionEngine.test.tsx
npx vitest run src/hooks/useSessionEngine.test.tsx -t "rAF cancel-guard"

# WR-02 stale comment removed
grep -c "engine owns null-out" src/app/App.tsx   # must return 0
```

Re-running `gsd-verifier` against `10-VERIFICATION.md` after this plan ships must produce a status of `verified` (no gaps), with Truth #4 (HOOKS-05) flipped from FAILED to VERIFIED.
</verification>

<success_criteria>
- All four tasks complete with their `<acceptance_criteria>` satisfied.
- `npm run lint`, `npx tsc --noEmit -p tsconfig.app.json`, `npm run build`, and `npm run test -- --run` all exit 0.
- Vitest reports 391 passing tests (390 existing + 1 new CR-01 regression).
- `grep -c "engine owns null-out" src/app/App.tsx` returns 0.
- `grep -c "sessionFrameRef = useRef(session.liveFrame)" src/app/App.tsx` returns 1.
- The scratch-revert teeth checks for Task 2 (revert App.tsx:81-84) and Task 3 (comment out useSessionEngine.ts:102) each cause their corresponding new/hardened test to FAIL, confirming the regression locks have teeth. Both reverts MUST be restored before commit.
- 10-VERIFICATION.md, when re-run by the verifier after this plan ships, returns `status: verified` with Truth #4 flipped to VERIFIED and no remaining BLOCKER or WARNING gaps for the in-scope items (CR-01, WR-01, WR-02). Out-of-scope items (WR-03, WR-04, IN-02) remain as INFO entries for follow-up.
</success_criteria>

<output>
After completion, create `.planning/phases/10-hooks-identity-effect-hygiene/10-02-SUMMARY.md` documenting:
- The three gaps closed (CR-01 BLOCKER, WR-01 WARNING, WR-02 WARNING) and the regression test added.
- The scratch-revert evidence for the two teeth-checks (Task 2 + Task 3).
- Final test count (391) and gate exit codes.
- Cross-reference to 10-VERIFICATION.md for the original gap descriptions.
- Note that out-of-scope items (WR-03, WR-04, IN-02) are explicitly NOT addressed and remain tracked for a follow-up cleanup phase.
</output>
</content>
</invoke>