---
phase: 10-hooks-identity-effect-hygiene
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/hooks/useSessionEngine.ts
  - src/hooks/useSessionEngine.test.tsx
  - src/hooks/useAudioCues.ts
  - src/hooks/useAudioCues.test.tsx
  - src/app/App.tsx
autonomous: true
requirements:
  - HOOKS-01
  - HOOKS-02
  - HOOKS-03
  - HOOKS-04
  - HOOKS-05
threat_model_ref: "see <threat_model> block below"

must_haves:
  truths:
    - "useAudioCues.start() and reconstructEngine read mute via mutedRef; start callback identity is `===` stable across setMuted(true)/setMuted(false) toggles in renderHook"
    - "App leave-running cleanup effect at App.tsx:464 depends on [state.status, audioStop, wakeLockRelease, clearLeadInTimeouts] (NOT [state, ...]); running-snapshot writer no longer lives in a React effect at App.tsx:412-420"
    - "useSessionEngine.currentFrame returns the same `===` reference across renders within the same cycleIndex:phase; useSessionEngine.liveFrame exposes per-rAF SessionFrame | null for visual consumers"
    - "rAF tick() short-circuits via `if (cancelled) return` at its first statement; an extra rAF firing after teardown produces no setState; App.tsx:81-84 sessionFrameRef updater retains explicit [session.currentFrame] deps and passes react-hooks/exhaustive-deps"
    - "No regressions in the existing 381-test Vitest baseline; boundary cues still fire exactly once per phase transition; new identity-stability tests added (~+8 tests, target ~389-390)"
  artifacts:
    - path: "src/hooks/useSessionEngine.ts"
      provides: "SessionEngine with new liveFrame field, runningSnapshotRef field, RunningSnapshot exported type, primitives-only currentFrame deps, top-of-tick cancel-guard, snapshot ref-write inside setState updater"
      contains: "liveFrame"
    - path: "src/hooks/useSessionEngine.ts"
      provides: "exported RunningSnapshot type for App-side consumption"
      contains: "export interface RunningSnapshot"
    - path: "src/hooks/useAudioCues.ts"
      provides: "mutedRef mirroring muted state; start and reconstructEngine read mutedRef.current and drop `muted` from useCallback deps"
      contains: "mutedRef"
    - path: "src/hooks/useSessionEngine.test.tsx"
      provides: "EXTENDED with 5-6 identity-stability tests in a new describe block (HOOKS-02/03/04). The 5 existing tests are preserved verbatim."
      contains: "identity contracts (Phase 10 HOOKS-03/04)"
    - path: "src/hooks/useAudioCues.test.tsx"
      provides: "EXTENDED with 2-3 callback-identity tests reusing the existing SpyableAC harness (HOOKS-01). Existing tests preserved verbatim."
      contains: "callback identity (Phase 10 HOOKS-01)"
    - path: "src/app/App.tsx"
      provides: "Local runningSnapshotRef declaration + running-snapshot effect (lines 179-183 and 412-420) DELETED; cleanup effect at :464 reads session.runningSnapshotRef.current and depends on [state.status, audioStop, wakeLockRelease, clearLeadInTimeouts]; BreathingShape + SessionReadout consume session.liveFrame; sessionFrameRef updater unchanged"
      contains: "session.liveFrame"
  key_links:
    - from: "src/hooks/useSessionEngine.ts (rAF tick)"
      to: "runningSnapshotRef.current"
      via: "ref-write inside setState((currentState) => { ... }) updater BEFORE the completeIfNeeded return"
      pattern: "runningSnapshotRef\\.current\\s*=\\s*\\{[^}]*currentState\\.startedAtMs"
    - from: "src/app/App.tsx (leave-running cleanup)"
      to: "session.runningSnapshotRef.current"
      via: "synchronous read inside effect body (no dep on the ref — refs are stable)"
      pattern: "session\\.runningSnapshotRef\\.current"
    - from: "src/hooks/useAudioCues.ts (start)"
      to: "mutedRef.current"
      via: "engine.setMuted(mutedRef.current); useCallback deps = [handleStateChange]"
      pattern: "engine\\.setMuted\\(mutedRef\\.current\\)"
    - from: "src/hooks/useAudioCues.ts (reconstructEngine)"
      to: "mutedRef.current"
      via: "const currentMuted = mutedRef.current; useCallback deps = [handleStateChange]"
      pattern: "const\\s+currentMuted\\s*=\\s*mutedRef\\.current"
    - from: "src/app/App.tsx:556 BreathingShape"
      to: "session.liveFrame"
      via: "frame={appPhase === 'running' ? session.liveFrame : null}"
      pattern: "frame=\\{appPhase === 'running' \\? session\\.liveFrame"
    - from: "src/app/App.tsx:560 SessionReadout"
      to: "session.liveFrame"
      via: "frame={leadInPlaceholderFrame ?? session.liveFrame}"
      pattern: "leadInPlaceholderFrame \\?\\? session\\.liveFrame"
---

<objective>
Land all five HOOKS-* requirements in a single plan, single wave, engine-first task ordering (per CONTEXT D-16/D-17). Stabilize callback identity in useAudioCues (mutedRef), split useSessionEngine frame identity into per-phase-stable `currentFrame` and per-rAF `liveFrame`, move running-snapshot ownership from an App-level per-render effect into the engine's rAF tick (with the ref-write INSIDE the setState updater to avoid closure staleness), add a top-of-tick cancel-guard, and tighten the App leave-running cleanup effect deps from `[state, ...]` to `[state.status, ...]`. Verification-only confirm HOOKS-05 (App.tsx:81-84 sessionFrameRef updater already has explicit `[session.currentFrame]` deps; `npm run lint` must still exit 0 after the App.tsx edits).

Purpose: Stop App-level rAF effects from re-running every animation frame. Reduces wasted React work, eliminates a closure-staleness DoS surface (HOOKS-04 cancel-guard mitigates uncancelled rAF after teardown), and locks identity contracts with renderHook tests.

Output:
- `src/hooks/useSessionEngine.ts` (modified — new liveFrame + runningSnapshotRef + RunningSnapshot export + primitives-only memo deps + cancel-guard at top of tick + snapshot ref-write inside updater)
- `src/hooks/useSessionEngine.test.tsx` (EXTENDED — +5-6 tests in a new `describe('useSessionEngine — identity contracts (Phase 10 HOOKS-03/04)', ...)` block; the existing 5 tests stay intact)
- `src/hooks/useAudioCues.ts` (modified — mutedRef + start/reconstructEngine read mutedRef.current and drop `muted` from useCallback deps)
- `src/hooks/useAudioCues.test.tsx` (EXTENDED — +2-3 tests in a new describe block, reusing SpyableAC)
- `src/app/App.tsx` (modified — DELETE lines 179-183 local runningSnapshotRef + DELETE lines 412-420 running-snapshot effect; tighten line 464 cleanup deps; migrate lines 556 & 560 to session.liveFrame; consume session.runningSnapshotRef at line 452)
</objective>

<execution_context>
@$HOME/.claude-personal-personal/get-shit-done/workflows/execute-plan.md
@$HOME/.claude-personal-personal/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/10-hooks-identity-effect-hygiene/10-CONTEXT.md
@.planning/phases/10-hooks-identity-effect-hygiene/10-RESEARCH.md
@.planning/phases/10-hooks-identity-effect-hygiene/10-PATTERNS.md
@.planning/phases/10-hooks-identity-effect-hygiene/10-VALIDATION.md
@.planning/phases/07-strict-type-lint-baseline/07-CONTEXT.md
@.planning/phases/09-audio-wake-lock-lifecycle-hardening/09-CONTEXT.md
@REVIEW.md

<interfaces>
<!-- Key contracts extracted from HEAD. Executor should use these directly — no codebase exploration needed. -->

From `src/hooks/useSessionEngine.ts` (HEAD lines 14-21) — SessionEngine interface that Task 1 EXTENDS:
```typescript
export interface SessionEngine {
  state: SessionState
  currentFrame: SessionFrame | null
  setSelectedSettings(this: void, settings: SessionSettings): void
  start(this: void): void
  end(this: void): void
  extendDuration(this: void, durationMinutes: number): void
}
```
Task 1 adds two fields BEFORE setSelectedSettings: `liveFrame: SessionFrame | null` and `runningSnapshotRef: React.RefObject<RunningSnapshot | null>`. Task 1 also adds an exported type:
```typescript
export interface RunningSnapshot {
  key: string
  startedAtMs: number
  lastElapsedMs: number
}
```

From `src/hooks/useAudioCues.ts` (HEAD line 91) — existing muted state that STAYS (D-11, ref ON TOP of state):
```typescript
const [muted, setMutedState] = useState<boolean>(initialMuted ?? false)
```

From `src/hooks/useAudioCues.ts` (HEAD lines 106-109) — DIRECT TEMPLATE for mutedRef (Pattern A):
```typescript
const onReanchorRequiredRef = useRef<typeof onReanchorRequired>(onReanchorRequired)
useEffect(() => {
  onReanchorRequiredRef.current = onReanchorRequired
}, [onReanchorRequired])
```

From `src/hooks/useAudioCues.ts` (HEAD line 86) — Pattern B exemplar (ref ON TOP of state — DO NOT replace state):
```typescript
const reconstructGenerationRef = useRef<number>(0)
```

From `src/hooks/useAudioCues.ts` (HEAD line 36-50) — UseAudioCues return interface that exposes `start`, `setMuted`, `resume` consumed by D-12 tests (no edit to the interface; identity is captured via `result.current.start`).

From `src/app/App.tsx` (HEAD lines 179-183) — LOCAL runningSnapshotRef declaration that Task 3 DELETES:
```typescript
const runningSnapshotRef = useRef<{
  key: string
  startedAtMs: number
  lastElapsedMs: number
} | null>(null)
```

From `src/app/App.tsx` (HEAD line 81-84) — HOOKS-05 verification target (NO EDIT; deps already explicit and correct):
```typescript
const sessionFrameRef = useRef(session.currentFrame)
useEffect(() => {
  sessionFrameRef.current = session.currentFrame
}, [session.currentFrame])
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: useSessionEngine — HOOKS-03 (currentFrame memo + liveFrame split) + HOOKS-04 (top-of-tick cancel-guard) + HOOKS-02 (runningSnapshotRef ownership inside rAF updater) + EXTEND useSessionEngine.test.tsx</name>

  <read_first>
    - `.planning/phases/10-hooks-identity-effect-hygiene/10-CONTEXT.md` (locked decisions D-01..D-21, especially D-03/D-04/D-06/D-07/D-08/D-10/D-13/D-20)
    - `.planning/phases/10-hooks-identity-effect-hygiene/10-RESEARCH.md` §"Common Pitfalls — Pitfall 1" (closure-staleness; ref-write MUST be inside the setState updater, NOT in the outer closure), §"Code Examples — Example 1" (full target shape with JSDoc), §"Code Examples — Example 4" (test shapes), §"Architecture Patterns — Pattern 4" (ref-owner-writes-from-tick)
    - `.planning/phases/10-hooks-identity-effect-hygiene/10-PATTERNS.md` §"`src/hooks/useSessionEngine.ts` (hook, event-driven rAF + ref-owned snapshot)" (Excerpts 1-5), §"Shared Patterns — Pattern C / D / F", §"Drift call-out" (D-13 file is `.tsx` not `.ts` — EXTEND, do NOT create)
    - `.planning/phases/10-hooks-identity-effect-hygiene/10-VALIDATION.md` (per-task verify commands for 10-01-01..10-01-05)
    - `.planning/phases/07-strict-type-lint-baseline/07-CONTEXT.md` D-04 (`// Reason:` annotation policy) + D-09 (per-commit tsc/lint/build/Vitest gate)
    - `.planning/phases/09-audio-wake-lock-lifecycle-hardening/09-CONTEXT.md` D-14 (test colocation in existing `*.test.{ts,tsx}` neighbors) + AUDIO-01 "ref ON TOP of synchronous-null" posture
    - `src/hooks/useSessionEngine.ts` (full file — 123 lines; current state of interface, rAF effect, useMemo)
    - `src/hooks/useSessionEngine.test.tsx` (full file — 138 lines; existing 5 tests preserved verbatim, new describe block appended)
    - `src/domain/sessionMath.ts` (SessionFrame type — for JSDoc accuracy)
    - `src/hooks/useAudioCues.ts` lines 83-86 (Pattern B exemplar: `reconstructGenerationRef` "Layered ON TOP of ..." comment style)
  </read_first>

  <files>
    src/hooks/useSessionEngine.ts,
    src/hooks/useSessionEngine.test.tsx
  </files>

  <behavior>
    - `currentFrame` returns the SAME `===` reference across renders within the same `cycleIndex:phase`; identity changes only at a phase boundary (cycleIndex or phase change) or on transition out of `running`.
    - `liveFrame` returns a NEW `SessionFrame | null` per render-cycle while running (per-rAF identity churn); when status is not `running`, `liveFrame` is `null`.
    - `liveFrame.phaseProgress` advances within a phase as `vi.advanceTimersByTime(1000)` ticks the rAF.
    - `runningSnapshotRef.current` is `null` at idle baseline; populated `{ key: string(startedAtMs), startedAtMs, lastElapsedMs }` after at least one rAF tick while running; nulled when status transitions out of running (the hook handles null-out on the early `if (state.status !== 'running') { runningSnapshotRef.current = null; return undefined }` branch of the rAF effect).
    - After `unmount()`, a subsequent `vi.advanceTimersByTime(...)` produces no `act()` warning or `setState`-after-unmount console.error (HOOKS-04 cancel-guard at top of tick is what prevents the setState).
    - All 5 existing tests in `useSessionEngine.test.tsx` continue to pass verbatim.
  </behavior>

  <action>
Modify `src/hooks/useSessionEngine.ts` per locked decisions D-01..D-10:

(a) Add `import type` for `React` ref typing (or use `RefObject` directly from React) — required for `runningSnapshotRef: React.RefObject<RunningSnapshot | null>` field in the SessionEngine interface. Match the existing import style in the file (currently `import { useCallback, useEffect, useMemo, useState } from 'react'`); add `useRef` to that import and add `import type { RefObject } from 'react'` if cleaner. Executor's discretion — pick the form that matches strict-mode tsconfig (Phase 7 baseline).

(b) Add exported type `RunningSnapshot` matching App.tsx:179-183 shape VERBATIM — three fields `key: string`, `startedAtMs: number`, `lastElapsedMs: number` (D-07). Export it as a named export so App.tsx can import the type.

(c) Extend the `SessionEngine` interface to add two new fields adjacent to `currentFrame` (placement is Claude's discretion per RESEARCH §"Claude's Discretion"; choose adjacent for readability):
   - `liveFrame: SessionFrame | null` with JSDoc: "Phase 10 HOOKS-03: per-rAF frame value. Identity changes every animation frame; phaseProgress and elapsedMs are fresh. Use for visual rendering (BreathingShape, SessionReadout) — NOT for phase-boundary effects."
   - `runningSnapshotRef: React.RefObject<RunningSnapshot | null>` with JSDoc: "Phase 10 HOOKS-02 (D-06/D-07/D-08): running-session snapshot owned by the engine, written from inside the rAF tick setState updater. Consumed by App's leave-running cleanup. Read .current synchronously; refs are stable — do NOT put in effect dep arrays."
   - Also expand `currentFrame`'s JSDoc to: "Phase 10 HOOKS-03: per-phase-stable frame identity. Same `===` reference across renders within the same cycleIndex:phase. Use for effects that should fire once per phase boundary — NOT for per-frame visual rendering."

(d) Inside the hook body, after the `useState<SessionState>` declaration at line 24, add the ref declaration with a comment mirroring the Pattern B exemplar style at `useAudioCues.ts:83-86`: `// HOOKS-02 / D-06 / D-07: hook owns the running-snapshot ref. Written from inside the rAF tick setState updater (D-08). App.tsx's running-snapshot effect at lines 412-420 is DELETED in Task 3.` then `const runningSnapshotRef = useRef<RunningSnapshot | null>(null)`.

(e) Modify the rAF effect (current lines 29-57) per D-06/D-08/D-10:
   - On the early-return branch (`if (state.status !== 'running') return undefined`), null the ref BEFORE returning: `runningSnapshotRef.current = null` then `return undefined`. This handles transition out of running (option (a) from RESEARCH A2; chosen per Pitfall 1's "owner of state owns the writer" rule).
   - Inside `const tick = () => { ... }`, the FIRST statement is `if (cancelled) return` (HOOKS-04 / D-10). The setState call moves below this guard.
   - Inside the `setState((currentState) => { ... })` updater, BEFORE the `return completeIfNeeded(currentState, performance.now())` line, AND inside the `if (currentState.status !== 'running') return currentState` branch's else path (i.e., once we know currentState is running), write the snapshot from `currentState` (NOT from the outer-closure `state` — Pitfall 1):
     ```
     runningSnapshotRef.current = {
       key: String(currentState.startedAtMs),
       startedAtMs: currentState.startedAtMs,
       lastElapsedMs: currentState.lastFrame.elapsedMs,
     }
     ```
     Place this AFTER the `if (currentState.status !== 'running') return currentState` early-return, BEFORE the `return completeIfNeeded(...)`. This guarantees the snapshot is only written when `currentState` is narrowed to `RunningSessionState` (where `startedAtMs` and `lastFrame` exist on the type).
   - The dep array `[state.status]` stays unchanged.

(f) Modify the `currentFrame` useMemo (current lines 59-62) per D-03 — tighten the dep array. PREFER Variant A (inline access to optional-chain, per RESEARCH A4): `useMemo<SessionFrame | null>(() => (state.status === 'running' ? state.lastFrame : null), [state.status, state.lastFrame?.cycleIndex, state.lastFrame?.phase])`. If `npm run lint` trips with `react-hooks/exhaustive-deps` on this dep form, fall back to Variant B (local-narrow): extract `const lastFrame = state.status === 'running' ? state.lastFrame : null; const cycleKey = lastFrame?.cycleIndex; const phaseKey = lastFrame?.phase;` BEFORE the useMemo, then deps `[state.status, cycleKey, phaseKey]`. Do NOT add an `// eslint-disable-next-line react-hooks/exhaustive-deps` — Variant B is the safe-harbor escape (D-19 / Phase 7 D-04).

(g) Add the `liveFrame` direct read AFTER the `currentFrame` useMemo, BEFORE the existing callbacks (per D-04 — NO memoization):
```
const liveFrame: SessionFrame | null = state.status === 'running' ? state.lastFrame : null
```

(h) Extend the return object to include the two new fields: `liveFrame` and `runningSnapshotRef`. Existing fields stay in order; append the new fields where it reads cleanest (executor's discretion — recommend adjacent to `currentFrame` for symmetry with the interface).

Test extension — `src/hooks/useSessionEngine.test.tsx` (EXTEND, not create — drift correction from PATTERNS.md §"Drift call-out"):
   - Append a new `describe('useSessionEngine — identity contracts (Phase 10 HOOKS-03/04)', () => { ... })` block AFTER the closing `})` of the existing `describe('useSessionEngine', ...)` block (i.e., at the file's top level, sibling to the existing describe).
   - Inside the new describe, reuse the existing `beforeEach(() => { vi.useFakeTimers(); vi.setSystemTime(new Date('2026-05-09T00:00:00.000Z')) })` and `afterEach(() => { vi.useRealTimers() })` pattern (copy verbatim from lines 14-21).
   - Add 5-6 tests matching the shapes in RESEARCH §"Code Examples — Example 4" (lines 760-857). Required tests (test names should match `-t` patterns in 10-VALIDATION.md):
     1. `it('currentFrame identity is stable across renders within the same phase', ...)` — capture `firstFrame = result.current.currentFrame`, advance 1000ms (well under inhaleMs≈4363ms at default bpm 5.5 ratio 40:60), assert `result.current.currentFrame).toBe(firstFrame)`.
     2. `it('currentFrame identity changes at a phase boundary', ...)` — capture inFrame at phase 'in', advance 5000ms to cross into 'out', assert `outFrame).not.toBe(inFrame)` AND `outFrame?.phase === 'out'`.
     3. `it('liveFrame identity changes per rAF (per advance) while currentFrame stays stable', ...)` — capture live1 + stable1, advance 100ms, assert `live2).not.toBe(live1)` AND `stable2).toBe(stable1)`.
     4. `it('liveFrame.phaseProgress advances within a phase', ...)` — capture p1, advance 1000ms, assert `p2).toBeGreaterThan(p1)`.
     5. `it('rAF cancel-guard: tick after teardown is a no-op (HOOKS-04)', ...)` — start, advance 1000ms, unmount, then `act(() => vi.advanceTimersByTime(1000))` and assert no act-warning/setState-after-unmount (negative assertion is implicit via clean suite output; add an explicit sanity guard `expect(frameBeforeUnmount).not.toBeNull()`).
     6. `it('runningSnapshotRef.current is populated while running and nulled on transition out', ...)` — assert null at idle, start + advance 1000ms, assert `snap?.key === String(snap.startedAtMs)` AND `snap?.lastElapsedMs >= 900` (allow rAF jitter), call `result.current.end()`, assert `runningSnapshotRef.current).toBeNull()`.

NEVER introduce a new `// eslint-disable-next-line react-hooks/*` without a `// Reason:` line above it (Phase 7 D-04 / D-19). Phase 10's changes are not expected to require new disables.
  </action>

  <verify>
    <automated>cd /Users/lucindo/Code/hrv && npx vitest run src/hooks/useSessionEngine.test.tsx --reporter=default && npx tsc --noEmit -p tsconfig.app.json && npm run lint && npm run build 2>&1 | tail -50</automated>
  </verify>

  <acceptance_criteria>
    Source assertions:
    - `grep -n "export interface RunningSnapshot" src/hooks/useSessionEngine.ts` returns ≥ 1 match.
    - `grep -n "liveFrame: SessionFrame" src/hooks/useSessionEngine.ts` returns ≥ 1 match (interface declaration).
    - `grep -n "runningSnapshotRef: React.RefObject" src/hooks/useSessionEngine.ts` returns ≥ 1 match (interface declaration).
    - `grep -nE "useRef<RunningSnapshot \\| null>\\(null\\)" src/hooks/useSessionEngine.ts` returns ≥ 1 match.
    - `grep -nE "^\\s*if \\(cancelled\\) return" src/hooks/useSessionEngine.ts` returns ≥ 1 match inside the rAF effect (HOOKS-04 D-10).
    - `grep -nE "runningSnapshotRef\\.current\\s*=\\s*\\{" src/hooks/useSessionEngine.ts` returns ≥ 1 match, AND the match is positioned INSIDE the `setState((currentState) => { ... })` updater (verify by reading the surrounding 10 lines; the surrounding context MUST reference `currentState.startedAtMs`, NOT outer-closure `state.startedAtMs` — Pitfall 1).
    - `grep -nE "\\[state\\.status, state\\.lastFrame\\?\\.cycleIndex" src/hooks/useSessionEngine.ts` returns ≥ 1 match (Variant A) OR a local-narrow form with `[state.status, cycleKey, phaseKey]` pattern (Variant B).
    - `grep -v '^[[:space:]]*//' src/hooks/useSessionEngine.ts | grep -cE "useMemo\\(.*\\[state\\]\\s*,?\\s*\\)" | grep -q "^0$"` (no remaining `[state]` deps in useMemo — comments excluded).
    - The 5 existing tests at `src/hooks/useSessionEngine.test.tsx:23-136` are preserved verbatim (verify by `git diff --stat` showing additions only inside the new describe block, no edits to the original describe).
    - New describe block `useSessionEngine — identity contracts (Phase 10 HOOKS-03/04)` exists: `grep -c "identity contracts (Phase 10 HOOKS-03/04)" src/hooks/useSessionEngine.test.tsx` returns ≥ 1.

    Test commands (from 10-VALIDATION.md):
    - `npx vitest run src/hooks/useSessionEngine.test.tsx -t "currentFrame identity is stable"` — exits 0.
    - `npx vitest run src/hooks/useSessionEngine.test.tsx -t "currentFrame identity changes"` — exits 0.
    - `npx vitest run src/hooks/useSessionEngine.test.tsx -t "liveFrame"` — exits 0 (matches both liveFrame tests).
    - `npx vitest run src/hooks/useSessionEngine.test.tsx -t "rAF cancel-guard"` — exits 0.
    - `npx vitest run src/hooks/useSessionEngine.test.tsx -t "runningSnapshotRef"` — exits 0.

    Behavior:
    - `currentFrame` identity stable within a phase (`===`); changes only at phase boundary (cycleIndex or phase change).
    - `liveFrame` identity new per render-cycle while running; `null` outside running.
    - rAF tick after teardown is a no-op — no setState observed (test 5 passes cleanly with no act-warning).
    - `runningSnapshotRef.current` populated mid-running with values derived from `currentState` (not the stale outer-closure `state`); nulled on transition out.

    Build/lint gates (Phase 7 D-09 / Phase 10 D-21 per-commit gate):
    - `tsc --noEmit -p tsconfig.app.json` exits 0.
    - `npm run lint` exits 0 with NO new `// eslint-disable-next-line react-hooks/*` introduced (if one IS needed for Variant A's optional-chain, fall back to Variant B per D-19 — do NOT disable).
    - `npm run build` exits 0.
    - `npm run test -- --run` exits 0; full Vitest suite passes; baseline 381 → ~386-387 (Task 1 adds 5-6 tests; Task 2 will add 2-3 more reaching ~389-390).

    Commit boundary (D-21): All four gates green BEFORE committing this task. Failure of ANY gate ROLLS BACK this task; do NOT patch-forward.
  </acceptance_criteria>

  <done>
    useSessionEngine.ts exports RunningSnapshot, exposes liveFrame + runningSnapshotRef on SessionEngine, snapshot ref-write lives INSIDE the rAF setState updater (Pitfall 1 resolved), `if (cancelled) return` is the first statement of tick (HOOKS-04), currentFrame useMemo deps are primitives-only (HOOKS-03), 5 existing tests preserved + 5-6 new identity tests appended in a new describe block, all four per-commit gates exit 0.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: useAudioCues — HOOKS-01 mutedRef (ref ON TOP of muted state) + drop `muted` from `start` and `reconstructEngine` useCallback deps + EXTEND useAudioCues.test.tsx</name>

  <read_first>
    - `.planning/phases/10-hooks-identity-effect-hygiene/10-CONTEXT.md` D-11/D-12/D-14 (mutedRef pattern; identity-stability tests via renderHook + SpyableAC)
    - `.planning/phases/10-hooks-identity-effect-hygiene/10-RESEARCH.md` §"Architecture Patterns — Pattern 2" (Ref-on-top-of-state), §"Code Examples — Example 2" (mutedRef target shape), §"Code Examples — Example 5" (D-14 test shapes), §"Code-State Verification" (line numbers verified at HEAD)
    - `.planning/phases/10-hooks-identity-effect-hygiene/10-PATTERNS.md` §"`src/hooks/useAudioCues.ts`" (Excerpts 1-4), §"Shared Patterns — Pattern A / B / F"
    - `.planning/phases/10-hooks-identity-effect-hygiene/10-VALIDATION.md` (10-02-01, 10-02-02 verify commands)
    - `src/hooks/useAudioCues.ts` lines 71-160 (hook intro + existing refs + onReanchorRequiredRef analog at 106-109)
    - `src/hooks/useAudioCues.ts` lines 192-232 (`start` useCallback — edit target)
    - `src/hooks/useAudioCues.ts` lines 269-325 (`reconstructEngine` useCallback — edit target)
    - `src/hooks/useAudioCues.ts` lines 346-360 (`setMuted` useCallback — REFERENCE only; do NOT modify; the React state setter stays as the UI binding source per D-11)
    - `src/hooks/useAudioCues.test.tsx` lines 337-369 (SpyableAC class — reuse for D-14 tests) AND the file's existing `describe`/`afterEach` shape
  </read_first>

  <files>
    src/hooks/useAudioCues.ts,
    src/hooks/useAudioCues.test.tsx
  </files>

  <behavior>
    - `mutedRef.current` is initialized to `initialMuted ?? false` and kept in sync with the `muted` state via a `useEffect(() => { mutedRef.current = muted }, [muted])`.
    - `start` reads `mutedRef.current` (not the `muted` state) when calling `engine.setMuted(...)`; `start`'s useCallback dep array drops `muted` and lists `[handleStateChange]` only.
    - `reconstructEngine` reads `const currentMuted = mutedRef.current` (replacing the current `const currentMuted = muted`); its useCallback dep array drops `muted` and lists `[handleStateChange]` only.
    - The React `muted` state and `setMutedState` setter (line 91) are NOT removed — `setMuted` (line 346) still calls `setMutedState(next)` (UI binding for MuteToggle + LOCL-01 persistence). The ref is ADDITIVE.
    - In renderHook: `result.current.start` reference is `===` stable across `setMuted(true)` AND `setMuted(false)` toggles. Same for `result.current.resume` (proxy for `reconstructEngine` identity since `resume` calls `reconstructEngine` internally).
    - All existing `useAudioCues.test.tsx` tests pass verbatim.
  </behavior>

  <action>
Modify `src/hooks/useAudioCues.ts` per locked decisions D-11 / D-12:

(a) Add `mutedRef` declaration after the existing `[muted, setMutedState]` useState at line 91 (or grouped near the other refs at lines 77-106 — executor's discretion; recommend AFTER `[muted, setMutedState]` line 91 for readability). Use the Pattern B comment template (mirrors the `// AUDIO-01: ... Layered ON TOP of ...` style at lines 83-86):
```
// HOOKS-01 / D-11: mute state mirrored into a ref so callbacks that read mute
// (start, reconstructEngine) drop `muted` from their useCallback dep arrays
// without losing access to the current value. Layered ON TOP of the existing
// useState [muted, setMutedState] above — does NOT replace it (the React
// state is still the UI-binding source for MuteToggle and the LOCL-01
// persistence path). Mirrors the onReanchorRequiredRef pattern at lines
// 106-109 and the AUDIO-01 reconstructGenerationRef posture at line 86.
const mutedRef = useRef<boolean>(initialMuted ?? false)
useEffect(() => {
  mutedRef.current = muted
}, [muted])
```

(b) Modify the `start` useCallback (HEAD lines 192-232):
   - Line 210: `engine.setMuted(muted)` → `engine.setMuted(mutedRef.current)`.
   - Line 231: dep array `[muted, handleStateChange]` → `[handleStateChange]`.
   - No other body changes; the gesture-preserving synchronous-AC-construct path stays intact.

(c) Modify the `reconstructEngine` useCallback (HEAD lines 269-325):
   - Line 273: `const currentMuted = muted` → `const currentMuted = mutedRef.current`.
   - Line 325: dep array `[muted, handleStateChange]` → `[handleStateChange]`.
   - Do NOT touch the synchronous-null pattern at lines 274-280 (`engineRef.current = null` BEFORE await), the AUDIO-01 generation check at line 295, or the `newEngine.setMuted(currentMuted)` call at line 310 (which now reads the ref-captured value — same semantic since `currentMuted` is captured synchronously before any await).
   - The `// Pitfall 1: DO NOT remove — the synchronous-null is preserved ON TOP of the generation counter` comment at lines 277-278 stays — Phase 10's mutedRef is the SAME posture (Pattern B); add a brief note in your commit message that this mirrors the same layering.

(d) Do NOT modify `setMuted` (line 346) — `setMutedState(next)` remains the source of truth that fires the `useEffect([muted])` sync, which then writes `mutedRef.current`. The chain is: user toggles → `setMuted(next)` → `setMutedState(next)` → React re-render → effect fires → `mutedRef.current = muted`. Same render cycle, before any subsequent callback observation.

Test extension — `src/hooks/useAudioCues.test.tsx`:
   - Append a new `describe('useAudioCues — callback identity (Phase 10 HOOKS-01)', () => { ... })` block at the END of the file (sibling to any existing top-level describe).
   - Inside, add an `afterEach(() => { vi.unstubAllGlobals(); vi.restoreAllMocks() })` matching the existing file's cleanup pattern.
   - Add 2-3 tests matching RESEARCH §"Code Examples — Example 5" (lines 866-911). Required tests (names match -t patterns in 10-VALIDATION.md):
     1. `it('start callback identity is stable across setMuted toggle (HOOKS-01)', async () => { ... })` — `vi.stubGlobal('AudioContext', SpyableAC)` (reusing the existing SpyableAC class at lines 337-369), `renderHook(() => useAudioCues())`, capture `startBefore = result.current.start`, `act(() => result.current.setMuted(true))`, capture `startAfterMute`, `act(() => result.current.setMuted(false))`, capture `startAfterUnmute`, assert `startAfterMute).toBe(startBefore)` AND `startAfterUnmute).toBe(startBefore)`. Match test name to `-t "start callback identity is stable"`.
     2. `it('resume callback (proxy for reconstructEngine identity) is stable across setMuted', () => { ... })` — same pattern but capture `result.current.resume` before/after `setMuted(true)`; assert `===`. Match test name to `-t "resume callback.+stable"` (regex pattern — keep "resume callback" and "stable" both in the name).
     3. (Optional 3rd test) `it('baseline: handleStateChange identity unchanged across setMuted (regression guard)', ...)` — same shape; asserts `result.current.start === startBefore` after `setMuted(true)` (proves `handleStateChange` identity stable since it's `start`'s remaining dep). Stop after 2 tests if 3rd is redundant per executor's reading.
   - Each test must `unmount()` at the end (matches existing file pattern).

NEVER introduce a new `// eslint-disable-next-line react-hooks/*` without a `// Reason:` line above it (Phase 7 D-04 / D-19).
  </action>

  <verify>
    <automated>cd /Users/lucindo/Code/hrv && npx vitest run src/hooks/useAudioCues.test.tsx --reporter=default && npx tsc --noEmit -p tsconfig.app.json && npm run lint && npm run build 2>&1 | tail -50</automated>
  </verify>

  <acceptance_criteria>
    Source assertions:
    - `grep -nE "const mutedRef = useRef<boolean>" src/hooks/useAudioCues.ts` returns ≥ 1 match.
    - `grep -nE "mutedRef\\.current = muted" src/hooks/useAudioCues.ts` returns ≥ 1 match inside a `useEffect(() => { ... }, [muted])` (verify surrounding 5 lines).
    - `grep -nE "engine\\.setMuted\\(mutedRef\\.current\\)" src/hooks/useAudioCues.ts` returns ≥ 1 match (in `start`).
    - `grep -nE "const currentMuted = mutedRef\\.current" src/hooks/useAudioCues.ts` returns ≥ 1 match (in `reconstructEngine`).
    - `grep -v '^[[:space:]]*//' src/hooks/useAudioCues.ts | grep -cE "engine\\.setMuted\\(muted\\)"` returns `0` (the old form is gone — comments excluded).
    - `grep -v '^[[:space:]]*//' src/hooks/useAudioCues.ts | grep -cE "const currentMuted = muted$"` returns `0` (no remaining old form — comments excluded).
    - The `[muted, setMutedState] = useState<boolean>` declaration at HEAD line 91 is PRESERVED (state stays per D-11): `grep -nE "setMutedState\\]\\s*=\\s*useState<boolean>" src/hooks/useAudioCues.ts` returns ≥ 1 match.
    - The `setMuted` useCallback at HEAD line 346 is UNCHANGED in body (still calls `setMutedState(next)`): `grep -nE "setMutedState\\(next\\)" src/hooks/useAudioCues.ts` returns ≥ 1 match.
    - New describe block exists: `grep -c "callback identity (Phase 10 HOOKS-01)" src/hooks/useAudioCues.test.tsx` returns ≥ 1.

    Test commands (from 10-VALIDATION.md):
    - `npx vitest run src/hooks/useAudioCues.test.tsx -t "start callback identity is stable"` — exits 0.
    - `npx vitest run src/hooks/useAudioCues.test.tsx -t "resume callback.+stable"` — exits 0.

    Behavior:
    - In renderHook, `result.current.start` reference is `===` stable across `setMuted(true)` AND `setMuted(false)`.
    - `result.current.resume` reference is `===` stable across `setMuted(true)`.
    - Existing `useAudioCues.test.tsx` tests all pass.

    Build/lint gates (D-21):
    - `tsc --noEmit -p tsconfig.app.json` exits 0.
    - `npm run lint` exits 0 with NO new `// eslint-disable-next-line react-hooks/*` introduced.
    - `npm run build` exits 0.
    - `npm run test -- --run` exits 0; baseline 381 + 5-6 (Task 1) + 2-3 (Task 2) ≈ 388-390 tests passing.

    Commit boundary (D-21): All four gates green before committing.
  </acceptance_criteria>

  <done>
    useAudioCues.ts has mutedRef + sync effect mirroring `onReanchorRequiredRef` pattern; `start` and `reconstructEngine` read mutedRef.current and drop `muted` from their useCallback deps; React `muted` state preserved; 2-3 new identity tests added in a new describe block reusing SpyableAC; all four per-commit gates exit 0.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: App.tsx — DELETE local runningSnapshotRef + DELETE running-snapshot effect (lines 179-183 + 412-420) + tighten leave-running cleanup deps at line 464 (HOOKS-02) + migrate BreathingShape (:556) + SessionReadout (:560) to session.liveFrame (D-05) + HOOKS-05 verify sessionFrameRef updater (:81-84) untouched and lint passes</name>

  <read_first>
    - `.planning/phases/10-hooks-identity-effect-hygiene/10-CONTEXT.md` D-05/D-06/D-09/D-15/D-18/D-19 (consumer migration; running-snapshot delete; cleanup dep tightening; HOOKS-05 passive verify; eslint-disable Reason annotation policy)
    - `.planning/phases/10-hooks-identity-effect-hygiene/10-RESEARCH.md` §"Common Pitfalls — Pitfall 3" (HOOKS-05 lint-regression risk; three mitigation options: const-extract, snapshot-shape extension, annotated disable as last resort), §"Code Examples — Example 3" (App.tsx target edits), §"Architecture Patterns — Pattern 5 (anti-pattern — per-rAF object deps)"
    - `.planning/phases/10-hooks-identity-effect-hygiene/10-PATTERNS.md` §"`src/app/App.tsx`" (Excerpts 1-4), §"Shared Patterns — Pattern E (discriminated-union narrowing) / Pattern G (Reason annotation)"
    - `.planning/phases/10-hooks-identity-effect-hygiene/10-VALIDATION.md` (10-03-01, 10-03-02 verify commands)
    - `.planning/phases/07-strict-type-lint-baseline/07-CONTEXT.md` D-04 (`// Reason:` annotation policy — REQUIRED if Pitfall 3 Option C is exercised)
    - `src/app/App.tsx` lines 75-135 (session destructure + sessionFrameRef effect at 81-84 — HOOKS-05 verification target, NO EDIT)
    - `src/app/App.tsx` lines 175-195 (local runningSnapshotRef declaration at 179-183 — DELETE; surrounding refs `recordedSessionKeyRef` and `planRef` stay)
    - `src/app/App.tsx` lines 405-465 (running-snapshot effect at 412-420 — DELETE; leave-running cleanup at 428-464 — TIGHTEN deps + read `session.runningSnapshotRef.current`)
    - `src/app/App.tsx` lines 545-575 (BreathingShape + SessionReadout JSX call sites — D-05 migration)
    - `src/hooks/useSessionEngine.ts` (POST-Task-1 — has `liveFrame` + `runningSnapshotRef` + `RunningSnapshot` exported)
  </read_first>

  <files>
    src/app/App.tsx
  </files>

  <behavior>
    - The session destructure consumes `liveFrame` and `runningSnapshotRef` from the hook (executor's discretion on form: full destructure `const { state, currentFrame, liveFrame, runningSnapshotRef } = session` OR keep current style and read `session.liveFrame` / `session.runningSnapshotRef` inline).
    - Local `runningSnapshotRef = useRef<...>(null)` at HEAD lines 179-183 is DELETED (the hook now owns it).
    - The running-snapshot effect at HEAD lines 412-420 (`useEffect(() => { if (state.status === 'running') runningSnapshotRef.current = {...} }, [state])`) is DELETED entirely (including the comment above it).
    - The leave-running cleanup effect at HEAD lines 428-464:
      - dep array changes from `[state, audioStop, wakeLockRelease, clearLeadInTimeouts]` to `[state.status, audioStop, wakeLockRelease, clearLeadInTimeouts]` (HOOKS-02 / D-09).
      - line 452 `const snap = runningSnapshotRef.current` → `const snap = session.runningSnapshotRef.current`.
      - line 462 `runningSnapshotRef.current = null` is REMOVED (the hook owns the null-out on transition out of running per RESEARCH A2 Option (a)).
      - The existing `// Reason:` annotation + `// eslint-disable-next-line react-hooks/set-state-in-effect` at lines 437-438 STAYS unchanged (Phase 7 D-04 / D-19 carry-forward).
    - BreathingShape at line 556: `frame={appPhase === 'running' ? session.currentFrame : null}` → `frame={appPhase === 'running' ? session.liveFrame : null}` (D-05).
    - SessionReadout at line 560: `frame={leadInPlaceholderFrame ?? session.currentFrame}` → `frame={leadInPlaceholderFrame ?? session.liveFrame}` (D-05).
    - `sessionFrameRef` updater at lines 81-84 KEEPS reading `session.currentFrame` (per D-05 — boundary effect needs per-phase identity). NO EDIT here.
    - The boundary effect at App.tsx lines 480-527 (audio cue scheduling, dep array includes `session.currentFrame`) NO EDIT — its dep array already references `session.currentFrame` which is now per-phase-stable, so the effect fires once per phase boundary instead of every rAF. The existing `lastBoundaryKeyRef` gate is defense-in-depth and stays.
    - `npm run lint` exits 0 with NO new `// eslint-disable-next-line react-hooks/exhaustive-deps` introduced (HOOKS-05 D-18 verification).
  </behavior>

  <action>
Modify `src/app/App.tsx` per locked decisions D-05 / D-06 / D-09 / D-18 / D-19:

(a) Update the existing `useSessionEngine` destructure / consumption. HEAD has `const session = useSessionEngine(...)` (or similar — verify by reading surrounding code; the variable name is `session` per HEAD lines 81+ and 556). Executor's discretion: either:
   - Option I (preferred — minimal diff): keep `const session = useSessionEngine(...)` and read `session.liveFrame` / `session.runningSnapshotRef.current` at the call sites.
   - Option II (cleaner reads): destructure `liveFrame` and `runningSnapshotRef` at the top.
   Pick Option I for the smallest diff against HEAD.

(b) DELETE the local runningSnapshotRef declaration at HEAD lines 179-183 (the full multi-line `const runningSnapshotRef = useRef<{ key: string; startedAtMs: number; lastElapsedMs: number } | null>(null)` plus the Phase 4 LOCL-02 comment block at lines 174-178 that documents it). Replace with NOTHING — surrounding refs (`recordedSessionKeyRef` at HEAD line 188, `planRef`, etc.) stay.

(c) DELETE the entire running-snapshot effect at HEAD lines 407-420 (the `// Phase 4 LOCL-02: keep runningSnapshotRef fresh on every render while running.` comment block AND the `useEffect(() => { if (state.status === 'running') runningSnapshotRef.current = {...} }, [state])` block). Replace with NOTHING — the hook's rAF tick now writes the snapshot (Task 1's D-08 ref-write inside updater).

(d) Modify the leave-running cleanup effect at HEAD lines 428-464:
   - Inside the body, change line 452 `const snap = runningSnapshotRef.current` → `const snap = session.runningSnapshotRef.current`.
   - DELETE line 462 `runningSnapshotRef.current = null` entirely (the hook owns the null-out per RESEARCH A2 Option (a)).
   - Change the dep array at line 464 from `[state, audioStop, wakeLockRelease, clearLeadInTimeouts]` to `[state.status, audioStop, wakeLockRelease, clearLeadInTimeouts]` (HOOKS-02 D-09).
   - Keep the existing `// Reason: subscribe-and-reflect — appPhase resets to 'idle' ...` annotation + `// eslint-disable-next-line react-hooks/set-state-in-effect` at lines 437-438 EXACTLY as-is.

(e) HOOKS-05 lint-regression handling (Pitfall 3 / RESEARCH A3): Inside the leave-running cleanup body, line 455-456 reads `state.completedAtMs`. With deps narrowed to `[state.status]`, react-hooks/exhaustive-deps MAY flag `state.completedAtMs` as a missing dep. Run `npm run lint` IMMEDIATELY after edits (d) — DO NOT commit until lint passes. Mitigation order (apply only if lint trips):
   - **First (preferred):** Extract `const completedAtMs = state.status === 'complete' ? state.completedAtMs : null` as the FIRST statement inside the effect body (BEFORE the early-return guard); reference `completedAtMs` instead of `state.completedAtMs` inside the `if (isComplete)` branch; add `completedAtMs` to the dep array → `[state.status, completedAtMs, audioStop, wakeLockRelease, clearLeadInTimeouts]`.
   - **Second (fallback):** If extraction is awkward, annotated disable per Phase 7 D-04:
     ```
     // Reason: state.completedAtMs is narrowed via state.status === 'complete' discriminant; safe to read inside isComplete branch. State.status drives the effect; completedAtMs is set atomically with the status transition.
     // eslint-disable-next-line react-hooks/exhaustive-deps
     ```
     placed directly above the `}, [state.status, ...])` dep array line.
   - The annotated disable is LAST RESORT. Try const-extract first.

(f) Migrate visual consumers per D-05:
   - Line 556: `frame={appPhase === 'running' ? session.currentFrame : null}` → `frame={appPhase === 'running' ? session.liveFrame : null}`.
   - Line 560: `frame={leadInPlaceholderFrame ?? session.currentFrame}` → `frame={leadInPlaceholderFrame ?? session.liveFrame}`.

(g) HOOKS-05 passive verification (D-18): Confirm the `sessionFrameRef` updater at lines 81-84 (HEAD verbatim: `const sessionFrameRef = useRef(session.currentFrame); useEffect(() => { sessionFrameRef.current = session.currentFrame }, [session.currentFrame])`) is UNCHANGED. The deps already explicit and correct; HOOKS-05's contribution is verifying lint still passes after the engine changes shift the trigger cadence from per-rAF to per-phase. NO CODE EDIT here.

(h) The boundary effect at App.tsx lines 480-527 (audio cue scheduling) is UNCHANGED. Its dep array continues to reference `session.currentFrame`; under HOOKS-03's new per-phase identity, this effect fires once per phase boundary instead of per-rAF — exactly the desired cadence change.

After all edits, run all four per-commit gates (D-21):
   - `npx tsc --noEmit -p tsconfig.app.json` (must exit 0)
   - `npm run lint` (must exit 0; if it trips on `state.completedAtMs`, apply Pitfall 3 mitigation)
   - `npm run build` (must exit 0)
   - `npm run test -- --run` (must exit 0 — full Vitest suite passes, boundary-cue exactly-once tests in `App.audio.test.tsx` and persistence/cleanup tests in `App.persistence.test.tsx` indirectly lock the cadence change per D-15)

If any gate fails, ROLL BACK this task; do NOT patch-forward.
  </action>

  <verify>
    <automated>cd /Users/lucindo/Code/hrv && npm run lint && npx tsc --noEmit -p tsconfig.app.json && npm run build && npm run test -- --run 2>&1 | tail -60</automated>
  </verify>

  <acceptance_criteria>
    Source assertions (after edits land):
    - `grep -nE "^\\s*const runningSnapshotRef = useRef<\\{" src/app/App.tsx` returns `0` matches (local declaration DELETED).
    - `grep -nE "if \\(state\\.status === 'running'\\) \\{[^}]*runningSnapshotRef\\.current" src/app/App.tsx` returns `0` matches (running-snapshot effect DELETED).
    - `grep -cE "session\\.runningSnapshotRef\\.current" src/app/App.tsx` returns ≥ 1 (cleanup effect now reads from the hook).
    - `grep -nE "session\\.liveFrame" src/app/App.tsx` returns exactly 2 matches (BreathingShape line 556 + SessionReadout line 560).
    - `grep -v '^[[:space:]]*//' src/app/App.tsx | grep -cE "frame=\\{appPhase === 'running' \\? session\\.currentFrame"` returns `0` (BreathingShape migrated — comments excluded).
    - `grep -v '^[[:space:]]*//' src/app/App.tsx | grep -cE "frame=\\{leadInPlaceholderFrame \\?\\? session\\.currentFrame\\}"` returns `0` (SessionReadout migrated — comments excluded).
    - The leave-running cleanup dep array is tightened: `grep -nE "\\}, \\[state\\.status, audioStop, wakeLockRelease, clearLeadInTimeouts" src/app/App.tsx` returns ≥ 1 match OR the form with `completedAtMs` extracted: `\\}, \\[state\\.status, completedAtMs, audioStop, wakeLockRelease, clearLeadInTimeouts`.
    - The OLD dep form is gone: `grep -v '^[[:space:]]*//' src/app/App.tsx | grep -cE "\\}, \\[state, audioStop, wakeLockRelease, clearLeadInTimeouts\\]"` returns `0`.
    - The sessionFrameRef updater at lines 81-84 is unchanged: `grep -nE "sessionFrameRef\\.current = session\\.currentFrame" src/app/App.tsx` returns ≥ 1 (HEAD line 83 preserved); `grep -nE "\\}, \\[session\\.currentFrame\\]\\)" src/app/App.tsx` returns ≥ 1 (HEAD line 84 dep array preserved).
    - If Pitfall 3 fallback (annotated disable) was needed, it carries a `// Reason:` line directly above the `// eslint-disable-next-line react-hooks/exhaustive-deps` (Phase 7 D-04 / D-19); `grep -B 1 "eslint-disable-next-line react-hooks/exhaustive-deps" src/app/App.tsx | grep -c "// Reason:"` equals the count of disables.

    Test commands:
    - `npm run test -- --run` exits 0; full Vitest suite passes (target ~389-390 tests).
    - Specifically: `npx vitest run src/app/App.audio.test.tsx` exits 0 (boundary-cue exactly-once locked per D-15).
    - Specifically: `npx vitest run src/app/App.persistence.test.tsx` exits 0 (stats-write idempotency locked).

    Behavior:
    - The leave-running cleanup fires once per status-transition-out-of-running (NOT per rAF) — verified indirectly by the existing `App.audio.test.tsx` boundary-cue-exactly-once tests staying green.
    - BreathingShape + SessionReadout receive per-rAF `liveFrame` values; visual animation cadence unchanged from user perspective.
    - `sessionFrameRef.current` now updates per-phase (cadence change inherited from HOOKS-03 `currentFrame` memo); behavior unchanged because the boundary effect's `lastBoundaryKeyRef` already gates on cycleIndex:phase.

    Build/lint gates (D-21):
    - `tsc --noEmit -p tsconfig.app.json` exits 0.
    - `npm run lint` exits 0 (HOOKS-05 verification). NO new `// eslint-disable-next-line react-hooks/*` introduced UNLESS Pitfall 3 Option C was the only viable mitigation — in which case verify the `// Reason:` annotation is present.
    - `npm run build` exits 0.
    - `npm run test -- --run` exits 0; target test count ~389-390 (baseline 381 + Task 1's 5-6 + Task 2's 2-3).

    Commit boundary (D-21): All four gates green BEFORE committing.
  </acceptance_criteria>

  <done>
    Local runningSnapshotRef and the App.tsx:412-420 running-snapshot effect are DELETED; the leave-running cleanup at :428-464 reads `session.runningSnapshotRef.current` and depends on `[state.status, ...]` (with `completedAtMs` either narrowed-and-added to deps OR Reason-annotated per Pitfall 3); BreathingShape + SessionReadout consume `session.liveFrame`; sessionFrameRef updater at :81-84 unchanged; lint passes with no new un-annotated disables; full Vitest suite passes at ~389-390 tests; all four per-commit gates exit 0.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| rAF tick callback ↔ React render lifecycle | An rAF callback queued before `cancelAnimationFrame` may still fire AFTER the effect cleanup ran (jsdom's fake-timer rAF backing may not synchronously drain). Without a top-of-tick guard, the callback would call `setState` on an unmounted/torn-down state owner. Mitigated by HOOKS-04 D-10 cancel-guard. |
| Outer-closure `state` ↔ rAF tick `currentState` | The rAF effect closure captures `state` at install time; subsequent ticks see stale `state` because the dep array is `[state.status]` — only re-installs on status change, not per-frame. Writing the snapshot from outer-closure `state.lastFrame.elapsedMs` produces stale snapshots; the setState updater's `currentState` is the live value. Mitigated by D-08 inside-updater ref-write (RESEARCH Pitfall 1). |
| `useAudioCues` callback identity ↔ React useMemo/useCallback dep chains | When `start`'s useCallback deps include `[muted, handleStateChange]`, every `setMuted` toggle creates a new `start` identity. Downstream `useCallback`s in App.tsx that include `audio.start` in deps cascade — every mute toggle forces re-creates across the App. Identity churn = render explosion = DoS-like wasted work. Mitigated by HOOKS-01 mutedRef. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01 | Denial of Service (resource exhaustion) | `useSessionEngine.ts` rAF tick callback | mitigate | HOOKS-04 D-10: `if (cancelled) return` as the FIRST statement of `tick()`. Locked by Task 1 test 5 ("rAF cancel-guard: tick after teardown is a no-op"). After `cancelAnimationFrame(animationFrameId)` runs in the effect cleanup, `cancelled = true` is the closure flag; the guard guarantees any already-queued rAF callback that survives the cancel becomes a no-op. Specific implementation: insert `if (cancelled) return` as the literal first statement of the `const tick = () => { ... }` body in `src/hooks/useSessionEngine.ts`. |
| T-10-02 | Tampering (data integrity, self-inflicted) | `useSessionEngine.ts` runningSnapshotRef write site | mitigate | D-08 inside-updater ref-write resolves RESEARCH Pitfall 1 (closure staleness). The snapshot is written from `currentState` inside `setState((currentState) => { ... })`, NOT from the outer closure's stale `state`. Specific implementation: place `runningSnapshotRef.current = { key: String(currentState.startedAtMs), startedAtMs: currentState.startedAtMs, lastElapsedMs: currentState.lastFrame.elapsedMs }` AFTER `if (currentState.status !== 'running') return currentState` and BEFORE `return completeIfNeeded(...)`. Locked by Task 1 test 6 ("runningSnapshotRef.current is populated while running and nulled on transition out") — the test reads back the snapshot after `advanceTimersByTime(1000)` and asserts `snap.lastElapsedMs` advanced, which is only true under the inside-updater implementation. |
| T-10-03 | Denial of Service (identity churn render explosion) | `useAudioCues.ts` `start` + `reconstructEngine` callback identity AND `useSessionEngine.ts` `currentFrame` useMemo | mitigate | HOOKS-01 mutedRef removes `muted` from `start` / `reconstructEngine` useCallback deps → stable identity across `setMuted` toggles (locked by Task 2 tests 1-2). HOOKS-03 primitives-only useMemo deps for `currentFrame` (`[state.status, state.lastFrame?.cycleIndex, state.lastFrame?.phase]`) eliminate the per-rAF identity churn that was forcing the App.tsx:464 leave-running cleanup effect to re-run every frame (Task 1's test 1 locks `currentFrame === currentFrame` within a phase). Combined with App.tsx:464 dep tightening from `[state, ...]` to `[state.status, ...]`, the App-level effect re-runs collapse from ~60/s to ≤1/phase-transition. |
| T-10-04 | (Implicit — no new security surface) | All Phase 10 edits | accept | RESEARCH §"Security Domain" confirms no auth, no sessions in the security sense, no access control, no user input changes, no crypto. Phase 10 net posture IMPROVES (reduces wasted work + closes one DoS surface via T-10-01). No new threat IDs needed beyond the three resource/integrity threats Phase 10 directly mitigates. |
</threat_model>

<verification>
## Phase-Level Verification

After all three tasks land and each task's per-commit gates passed, run the phase-level checks:

1. **Full Vitest suite green:**
   - `npm run test -- --run` exits 0.
   - Test count ≈ 389-390 (baseline 381 + Task 1 ~5-6 + Task 2 ~2-3). Within RESEARCH A6 tolerance (±2 of D-15's stated 389 target).
   - Specifically: `App.audio.test.tsx` (boundary-cue exactly-once) green; `App.persistence.test.tsx` (stats-write idempotency) green; `useSessionEngine.test.tsx` 5 original + new identity tests green; `useAudioCues.test.tsx` original + new identity tests green.

2. **Strict TS + lint + build all exit 0 (D-21 / Phase 7 D-09 gate):**
   - `npx tsc --noEmit -p tsconfig.app.json` exits 0.
   - `npm run lint` exits 0. CRITICAL: verify with `grep -E "// eslint-disable-next-line react-hooks/(exhaustive-deps|set-state-in-effect)" src/hooks/useSessionEngine.ts src/hooks/useAudioCues.ts src/app/App.tsx` — count NEW disables; for any NEW disable introduced by Phase 10, `grep -B 1` must show a `// Reason:` line directly above (Phase 7 D-04 / D-19). The two preserved disables at `App.tsx:251-253` and `:437-439` (set-state-in-effect for appPhase reset) are pre-existing and already annotated.
   - `npm run build` exits 0.

3. **Source assertions across all three tasks:**
   - `grep -c "session\\.liveFrame" src/app/App.tsx` returns exactly 2 (BreathingShape + SessionReadout).
   - `grep -c "session\\.runningSnapshotRef\\.current" src/app/App.tsx` returns ≥ 1 (cleanup effect).
   - `grep -c "mutedRef\\.current" src/hooks/useAudioCues.ts` returns ≥ 2 (`start` + `reconstructEngine` read sites).
   - `grep -c "if (cancelled) return" src/hooks/useSessionEngine.ts` returns ≥ 1 (HOOKS-04 cancel-guard).
   - `grep -c "export interface RunningSnapshot" src/hooks/useSessionEngine.ts` returns ≥ 1 (D-07 named export).

4. **No new un-annotated react-hooks disables:**
   - Run `grep -nE "// eslint-disable-next-line react-hooks/" src/hooks/useSessionEngine.ts src/hooks/useAudioCues.ts src/app/App.tsx > /tmp/phase10_disables.txt`.
   - For each line in that file, verify the line ABOVE contains `// Reason:` — `awk` script: `awk '/eslint-disable-next-line react-hooks/ { if (prev !~ /\/\/ Reason:/) print FILENAME":"NR":MISSING_REASON:"$0; } { prev=$0 }' src/hooks/useSessionEngine.ts src/hooks/useAudioCues.ts src/app/App.tsx` must produce NO output.

5. **Boundary-cue exactly-once invariant preserved (HOOKS-02 cadence change):**
   - The cadence shift (per-rAF → per-phase) is verified INDIRECTLY by `App.audio.test.tsx` tests covering `lastBoundaryKeyRef` gate (Phase 3 + Phase 9 cumulative). If any of those tests fail, the cadence change broke a contract — ROLL BACK to investigate.

6. **HOOKS-05 verification (D-18 passive check):**
   - The sessionFrameRef updater at App.tsx:81-84 is unchanged: `git diff src/app/App.tsx --unified=0 | grep -E "^[\\+\\-].*sessionFrameRef\\.current = session\\.currentFrame"` should produce NO output (i.e., the line is not modified).
   - `npm run lint` continues to exit 0 (the effect's existing `[session.currentFrame]` deps still satisfy react-hooks/exhaustive-deps after the engine changes).

</verification>

<success_criteria>
Phase 10 succeeds when:

1. **HOOKS-01:** `useAudioCues.start()` and `reconstructEngine` read mute via `mutedRef` (kept in sync by a `useEffect(() => { mutedRef.current = muted }, [muted])`); `start` callback identity is `===` stable across `setMuted(true)`/`setMuted(false)` toggles in renderHook (verified by Task 2 tests 1-2).

2. **HOOKS-02:** App leave-running cleanup effect at `App.tsx:464` depends on `[state.status, audioStop, wakeLockRelease, clearLeadInTimeouts]` (and, if Pitfall 3 mitigation Option A was applied, additionally `completedAtMs`); the local `runningSnapshotRef` declaration at `App.tsx:179-183` and the running-snapshot effect at `App.tsx:412-420` are both DELETED; the snapshot writer is owned by `useSessionEngine` and writes from inside the rAF tick `setState((currentState) => ...)` updater (Pitfall 1 / closure-staleness resolved).

3. **HOOKS-03:** `useSessionEngine.currentFrame` returns the same `===` reference across renders within the same `cycleIndex:phase` (Task 1 test 1); identity changes at phase boundary (Task 1 test 2). A new `liveFrame` field exposes per-rAF `SessionFrame | null` for visual consumers (Task 1 tests 3-4); BreathingShape at App.tsx:556 and SessionReadout at App.tsx:560 consume `session.liveFrame`.

4. **HOOKS-04:** `useSessionEngine.ts`'s rAF `tick()` short-circuits via `if (cancelled) return` as its FIRST statement; a synthetic post-unmount `vi.advanceTimersByTime(...)` produces no `setState`-after-unmount warning (Task 1 test 5).

5. **HOOKS-05:** `App.tsx:81-84` sessionFrameRef updater is UNCHANGED, retains explicit `[session.currentFrame]` deps, and `npm run lint` continues to exit 0 after the engine + App.tsx edits. NO new `// eslint-disable-next-line react-hooks/*` introduced (or, if Pitfall 3 Option C forced one, it carries a Phase 7 D-04 `// Reason:` annotation).

6. **No regressions:** Full Vitest suite passes; test count baseline 381 → final ~389-390. `tsc --noEmit`, `npm run lint`, `npm run build`, `npm run test -- --run` all exit 0 at each task commit boundary (per D-21).

7. **Identity contract drift surfaced if any future consumer reaches for `session.currentFrame` for per-frame visual rendering:** the contract is documented in JSDoc on the SessionEngine interface (Task 1 action item c) — runtime drift is not lint-enforced (deferred to v1.x per CONTEXT §"Deferred Ideas"), but the JSDoc differentiates the two `SessionFrame | null` fields explicitly.
</success_criteria>

<output>
After completion, create `.planning/phases/10-hooks-identity-effect-hygiene/10-01-SUMMARY.md` documenting:
- What changed in each of the three source files + two test files (file-by-file diff summary)
- The Pitfall 1 resolution (ref-write inside the setState updater — record the line range in `useSessionEngine.ts` post-edit)
- Whether Pitfall 3 was exercised (i.e., did `npm run lint` trip on `state.completedAtMs` after the App.tsx dep tightening), and if so which mitigation option (A const-extract / B snapshot-shape / C annotated disable) was applied
- Final test count (verify against RESEARCH A6 target of ~390)
- Confirmation that HOOKS-05 verification was passive (no edit to App.tsx:81-84)
- Any new `// Reason:` annotations introduced and their justifications
</output>

## Truths — Decision Traceability

Every locked decision from `10-CONTEXT.md` `<decisions>` is implemented by this plan. Mapping (decision → task / plan section):

- **D-01:** `currentFrame` name kept; `liveFrame` added as sibling. → Task 1 action (a) + (c); `must_haves.truths[2]`.
- **D-02:** `currentFrame: SessionFrame | null` + `liveFrame: SessionFrame | null` type contract unchanged. → Task 1 action (c) SessionEngine interface extension.
- **D-03:** Primitives-only useMemo deps: `[state.status, state.lastFrame?.cycleIndex, state.lastFrame?.phase]`. → Task 1 action (d); `key_links` regex for the dep array.
- **D-04:** `liveFrame` is a direct read, no useMemo wrapper. → Task 1 action (d) — `liveFrame = state.status === 'running' ? state.lastFrame : null`.
- **D-05:** Consumer migration at App.tsx call sites (`BreathingShape` + `SessionReadout` → `session.liveFrame`). → Task 3 action (c) + (d); `must_haves.artifacts[4-5]` `contains:` markers.
- **D-06:** `useSessionEngine` owns `runningSnapshotRef` writes inside rAF tick; `App.tsx:412-420` effect DELETED. → Task 1 action (e) + (f); Task 3 action (b).
- **D-07:** `RunningSnapshot = { key: string; startedAtMs: number; lastElapsedMs: number }` exported from `useSessionEngine`. → Task 1 action (c) — verbatim type-export contract.
- **D-08:** Ref-write timing — INSIDE `setState((currentState) => ...)` updater (Pitfall 1 / closure-staleness resolved). → Task 1 action (e); `success_criteria[2]`.
- **D-09:** Leave-running cleanup deps `[state.status, audioStop, wakeLockRelease, clearLeadInTimeouts]`; reads `session.runningSnapshotRef.current`. → Task 3 action (e); `must_haves.truths[1]`.
- **D-10:** rAF `tick()` `if (cancelled) return` as FIRST statement. → Task 1 action (g); `must_haves.truths[3]`; test 5.
- **D-11:** `mutedRef` inside `useAudioCues`, layered ON TOP of `muted` state. → Task 2 action (a) + (b) + (c).
- **D-12:** renderHook + ref-equality assertions for `start` / reconstruction in existing `useAudioCues.test.tsx`. → Task 2 verify; tests 1-2.
- **D-13:** Test geography for engine. **DRIFT-CORRECTED via PATTERNS.md / RESEARCH §Code-State Verification:** `src/hooks/useSessionEngine.test.tsx` already exists; plan EXTENDS in a new `describe` block, not creates `.ts`. → Task 1 verify; tests 1-5; `files_modified` lists `.tsx`.
- **D-14:** Extend `useAudioCues.test.tsx` with 2-3 identity tests. → Task 2 verify.
- **D-15:** No new App-level test file; existing boundary-cue exactly-once tests carry the regression lock. → No App test in `files_modified`; `must_haves.truths[4]` cites "boundary cues still fire exactly once per phase transition".
- **D-16:** Single plan, single wave. → This plan = `10-01`; `wave: 1`; `depends_on: []`.
- **D-17:** Engine-first task ordering (useSessionEngine → useAudioCues → App.tsx). → Tasks 1-2-3 in this exact order.
- **D-18:** HOOKS-05 passive verification (no edit unless lint trips). → Task 3 action (f); `success_criteria[5]`.
- **D-19:** `// Reason:` annotation policy for any new `react-hooks/*` disable (Phase 7 D-04). → Task 3 action (e) Pitfall 3 fallback C; `success_criteria[5]`.
- **D-20:** Co-locate new contract tests in existing `*.test.{ts,tsx}` neighbors (D-13 drift exception noted above). → Both test files extended in-place.
- **D-21:** Per-commit four-gate (tsc/lint/build/Vitest) green or rollback. → Every task `<acceptance_criteria>` lists all four gates; `success_criteria[6]`.

No decision is silently dropped. No decision is reduced to a stub or "future enhancement".
</content>
</invoke>