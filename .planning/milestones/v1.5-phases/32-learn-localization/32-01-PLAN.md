---
phase: 32-learn-localization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/content/learnContent.ts
  - src/content/learnContent.test.ts
  - src/content/strings.ts
  - src/content/strings.test.ts
autonomous: false
requirements: [LEARN-02, LEARN-03, I18N-08]

must_haves:
  truths:
    - "learnContent.ts has a practices map with resonant and naviKriya partitions"
    - "Resonant Learn data (hrv, timing, heroVideo, keyVideos) is preserved under practices.resonant — nothing lost"
    - "D-03: naviKriya partition mirrors the resonant two-section explainer shape — holds two operator-locked description sections and exactly two video links"
    - "D-05: Claude drafts the EN Navi Kriya description copy; the operator reviews and locks it (Task 3) before Plan 02 renders it"
    - "D-06: the naviKriya video block holds exactly two links in fixed order — Guardian In Meditation (...yak8) then Navi Kriya Walkthrough (...9fI)"
    - "D-11: the Phase 26 drift-guard workflow is extended to the new NK Learn copy — pt-BR NK strings carry // TODO: native-speaker review markers here"
    - "D-12: NK video titles stay English in the pt-BR catalog"
    - "strings.ts UiStrings.learn has 3 new heading keys, both locales"
    - "learnContent.test.ts and strings.test.ts pass against the new shape"
  artifacts:
    - path: "src/content/learnContent.ts"
      provides: "PracticeLearnContent interface + practices map (resonant + naviKriya) over the shared base"
      contains: "practices"
    - path: "src/content/strings.ts"
      provides: "3 new UiStrings.learn heading keys in both locales"
      contains: "naviKriyaVideosHeading"
    - path: "src/content/learnContent.test.ts"
      provides: "Updated structural tests + NK clinical-verbs guard + practices video URL identity tests"
      contains: "practices.naviKriya"
    - path: "src/content/strings.test.ts"
      provides: "Exhaustiveness checks for new learn.* keys + explicit nkReadout/nkControls non-empty checks"
      contains: "naviKriyaVideosHeading"
  key_links:
    - from: "src/content/learnContent.ts"
      to: "Readonly<Record<LocaleId, LearnContent>>"
      via: "as const satisfies"
      pattern: "as const satisfies Readonly<Record<LocaleId, LearnContent>>"
---

<objective>
Restructure `src/content/learnContent.ts` from a flat resonant-only shape into a per-practice partition: a `practices` map keyed by `PracticeId` (`'resonant' | 'naviKriya'`) over a shared base. Add the three new `UiStrings.learn` heading keys to `strings.ts` in both locales. Update the two affected test files so the suite stays green against the new shape.

This is the data foundation for the whole phase — the component refactor (Plan 02) and the PT-BR finalization (Plan 03) both depend on this shape existing.

Purpose: LEARN-02 requires practice-scoped Learn content; LEARN-03 requires the shared sections to stay shared. This plan creates the data structure that makes both possible.
Output: Restructured `learnContent.ts` (EN content final, PT-BR NK content drafted with review markers), new EN string keys, updated tests. Full suite green, `npm run build` succeeds.
</objective>

<execution_context>
@$HOME/.claude-personal-personal/get-shit-done/workflows/execute-plan.md
@$HOME/.claude-personal-personal/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32-learn-localization/32-CONTEXT.md
@.planning/phases/32-learn-localization/32-RESEARCH.md
@.planning/phases/32-learn-localization/32-PATTERNS.md
@.planning/phases/32-learn-localization/32-UI-SPEC.md

<interfaces>
<!-- Current learnContent.ts type shape — extracted from codebase. -->

From src/content/learnContent.ts (current — being restructured):
- `interface ExplainerSection { readonly title: string; readonly body: string }`
- `interface LearnLink { readonly label: string; readonly url: string }`
- `interface LearnContent { explainer: { hrv, timing, forrest: ExplainerSection }; links: { youtubeChannel, website, book, patreon, heroVideo, keyVideos[], appStoreIos, googlePlayAndroid } }`
- `export const LEARN_CONTENT: Readonly<Record<LocaleId, LearnContent>> = { en: {...}, 'pt-BR': {...} } as const satisfies Readonly<Record<LocaleId, LearnContent>>`

From src/storage/practices.ts:
- `export type PracticeId = 'resonant' | 'naviKriya'` (line 23)

From src/content/strings.ts (current UiStrings.learn — line 133):
- `learn: { title, close, resourcesHeading, videosHeading, nativeAppsHeading: string }`
- export ends `} as const satisfies Readonly<Record<LocaleId, UiStrings>>`

From src/content/content.no-review-markers.test.ts:
- `REVIEW_MARKER = 'TODO: native-speaker review'` (exact string, case-sensitive)
- Scans every non-`.test.ts` file under `src/content/`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Restructure learnContent.ts into a per-practice partition</name>
  <files>src/content/learnContent.ts</files>
  <read_first>
    - src/content/learnContent.ts (full current state — both EN and PT-BR entries, all sections)
    - src/storage/practices.ts (PracticeId type definition, line 23)
    - .planning/phases/32-learn-localization/32-PATTERNS.md (section "src/content/learnContent.ts" — exact target shape, lines 28-150)
    - .planning/phases/32-learn-localization/32-RESEARCH.md (Pattern 1, Pitfall 5, Open Question 1)
  </read_first>
  <behavior>
    - The exported `LEARN_CONTENT` still compiles against `as const satisfies Readonly<Record<LocaleId, LearnContent>>` with the new `practices` field.
    - `LEARN_CONTENT.en.practices.resonant.description.section1` equals the old `explainer.hrv`; `section2` equals the old `explainer.timing` (same title and body text, byte-identical).
    - `LEARN_CONTENT.en.practices.resonant.videos` equals `[old heroVideo, ...old keyVideos]` in that order (same labels, same URLs).
    - `LEARN_CONTENT[locale].explainer` has exactly one key: `forrest`.
    - `LEARN_CONTENT[locale].links` no longer has `heroVideo` or `keyVideos`; still has `youtubeChannel`, `website`, `book`, `patreon`, `appStoreIos`, `googlePlayAndroid`.
    - `LEARN_CONTENT.en.practices.naviKriya.videos` has exactly 2 entries with the exact labels and URLs from D-06.
  </behavior>
  <action>
    Add a new exported interface `PracticeLearnContent` before `LearnContent`: `{ readonly description: { readonly section1: ExplainerSection; readonly section2: ExplainerSection }; readonly videos: readonly LearnLink[] }`.

    Update the `LearnContent` interface: remove `hrv` and `timing` from `explainer` (leaving only `forrest`); remove `heroVideo` and `keyVideos` from `links` (keep `youtubeChannel`, `website`, `book`, `patreon`, `appStoreIos`, `googlePlayAndroid`); add a new field `readonly practices: { readonly resonant: PracticeLearnContent; readonly naviKriya: PracticeLearnContent }`.

    Restructure the EN entry: move `explainer.hrv` into `practices.resonant.description.section1` (byte-identical title and body); move `explainer.timing` into `practices.resonant.description.section2`; build `practices.resonant.videos` as a flat array `[heroVideo, ...keyVideos]` preserving the existing 4 entries in order. Do the same restructure for the `pt-BR` entry (the existing pt-BR `hrv`/`timing`/`heroVideo`/`keyVideos` values move verbatim — no translation work in this plan).

    Add the `practices.naviKriya` partition to the EN entry. D-05: Claude drafts the two description sections — claim-safe, calm, non-medical, mirroring the resonant explainer voice (the operator reviews and locks this copy in Task 3 before Plan 02 renders it): `section1` heading "What is Navi Kriya" with a body explaining Navi Kriya is an OM-counting meditation practice taught by Forrest Knutson; `section2` heading "How this app paces it" with a body explaining the app counts each OM at a chosen pace and auto-advances front phase to back phase at a fixed 4:1 ratio across the configured rounds. D-03: these are exactly two parallel sections, symmetric with resonant's hrv + timing explainers. Avoid all clinical verbs (improves, treats, cures, heals, diagnoses). D-06: add `practices.naviKriya.videos` as exactly two entries in this exact order — `{ label: 'The Guardian In Meditation', url: 'https://www.youtube.com/watch?v=M3t7gY_yak8' }`, then `{ label: 'Navi Kriya Walkthrough', url: 'https://www.youtube.com/watch?v=A4BGQCIp9fI' }`.

    For the `pt-BR` entry's `practices.naviKriya`, add the description section1/section2 title and body fields with Claude-drafted Brazilian Portuguese. D-11: extend the Phase 26 drift-guard workflow to this new NK Learn copy — append the exact comment `// TODO: native-speaker review` on the same line as each of the four pt-BR NK string values (section1 title, section1 body, section2 title, section2 body) so the markers stay until the operator's review in Plan 03. D-12: the pt-BR `practices.naviKriya.videos` entries carry the SAME English labels as EN (video titles stay English) and the SAME URLs; copy the existing "Video title kept in English" comment verbatim above each pt-BR NK video entry. Marker string must be exactly `TODO: native-speaker review` (the drift-guard test in `content.no-review-markers.test.ts` matches this exact string).

    Update both `en` and `pt-BR` in the same edit — the `as const satisfies` constraint fails compilation if either locale is missing the new `practices` field. Keep the closing `} as const satisfies Readonly<Record<LocaleId, LearnContent>>`.
  </action>
  <verify>
    <automated>npx tsc --noEmit && grep -c "TODO: native-speaker review" src/content/learnContent.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` exits 0 (the `as const satisfies` constraint passes for both locales).
    - `grep -c "TODO: native-speaker review" src/content/learnContent.ts` returns exactly 4 (one per pt-BR NK description string: 2 titles + 2 bodies).
    - `grep -c "M3t7gY_yak8" src/content/learnContent.ts` returns 2 (one per locale, EN + pt-BR NK video 1).
    - `grep -c "A4BGQCIp9fI" src/content/learnContent.ts` returns 2 (EN + pt-BR NK video 2).
    - `src/content/learnContent.ts` contains the string `practices.naviKriya` is renderable: `grep -c "naviKriya:" src/content/learnContent.ts` returns at least 2 (EN + pt-BR partition keys).
    - The EN `practices.naviKriya` description bodies contain no clinical verb: a manual grep for `improves|treats|cures|heals|diagnoses` over the EN naviKriya body lines returns nothing.
  </acceptance_criteria>
  <done>learnContent.ts has the practices partition, resonant data preserved verbatim, naviKriya EN content final and pt-BR drafted with 4 review markers, tsc passes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add new learn heading keys to strings.ts and update content tests</name>
  <files>src/content/strings.ts, src/content/learnContent.test.ts, src/content/strings.test.ts</files>
  <read_first>
    - src/content/strings.ts (UiStrings.learn interface at line 133; EN learn block at line 309; pt-BR learn block at line 486)
    - src/content/learnContent.test.ts (full — existing explainer-keys test, clinical-verbs guard, URL identity tests)
    - src/content/strings.test.ts (full — existing exhaustiveness pattern)
    - .planning/phases/32-learn-localization/32-PATTERNS.md (sections "src/content/strings.ts", "src/content/learnContent.test.ts", "src/content/strings.test.ts")
    - .planning/phases/32-learn-localization/32-RESEARCH.md (Pitfall 1, Pitfall 4, Wave 0 Gaps)
  </read_first>
  <behavior>
    - `UI_STRINGS.en.learn` and `UI_STRINGS['pt-BR'].learn` both carry the 3 new keys: `naviKriyaVideosHeading`, `naviKriyaDescriptionSection1Title`, `naviKriyaDescriptionSection2Title`.
    - `learnContent.test.ts` explainer-keys test asserts `Object.keys(...explainer)` equals `['forrest']`.
    - `learnContent.test.ts` clinical-verbs guard covers `practices.resonant.description.section1.body`, `practices.resonant.description.section2.body`, `practices.naviKriya.description.section1.body`, `practices.naviKriya.description.section2.body`, and `explainer.forrest.body`.
    - `learnContent.test.ts` video URL identity covers `practices.resonant.videos` and `practices.naviKriya.videos` across locales.
    - `strings.test.ts` asserts the 3 new `learn.*` keys are non-empty in both locales.
    - `npx vitest run src/content/learnContent.test.ts src/content/strings.test.ts` passes.
  </behavior>
  <action>
    In `strings.ts`, add 3 keys to the `UiStrings.learn` interface (after `nativeAppsHeading`): `readonly naviKriyaVideosHeading: string`, `readonly naviKriyaDescriptionSection1Title: string`, `readonly naviKriyaDescriptionSection2Title: string`. Note: only the heading strings live in `strings.ts`; the description BODY strings live in `learnContent.ts` (Task 1) — do not add body keys here.

    In the EN `learn` block, add the 3 values: `naviKriyaVideosHeading: 'Selected Navi Kriya Videos'`, `naviKriyaDescriptionSection1Title: 'What is Navi Kriya'`, `naviKriyaDescriptionSection2Title: 'How this app paces it'`.

    In the `pt-BR` `learn` block, add the 3 keys with Claude-drafted Brazilian Portuguese values. D-11: each value is followed on the same line by the exact comment `// TODO: native-speaker review` (drift-guard marker, removed only after the operator review in Plan 03). "Navi Kriya" stays untranslated as a Sanskrit proper noun (Phase 30 D-05).

    Keep the closing `} as const satisfies Readonly<Record<LocaleId, UiStrings>>`.

    In `learnContent.test.ts`: update the explainer-keys structural test — change the expected array from `['hrv', 'timing', 'forrest']` to `['forrest']`. Update the existing clinical-verbs guards that reference `explainer.hrv.body` / `explainer.timing.body` to the new paths `practices.resonant.description.section1.body` / `practices.resonant.description.section2.body`; keep the `explainer.forrest.body` guard. Add two new clinical-verbs guard tests inside the same `for (const locale of LOCALE_OPTIONS)` loop covering `practices.naviKriya.description.section1.body` and `section2.body` (regex `enClinicalVerbs` / `ptBrClinicalVerbs` as in the existing file). Update the URL-identity tests: replace `links.heroVideo` / `links.keyVideos` references with parallel tests over `practices.resonant.videos` and `practices.naviKriya.videos` — assert URL identity across locales AND label identity across locales (D-12: labels stay English). Add a structural test asserting `practices.naviKriya.videos` has exactly 2 entries.

    In `strings.test.ts`: add exhaustiveness assertions that the 3 new `learn.*` keys are non-empty strings in both locales. Add explicit non-empty assertions for every `nkReadout.*` and `nkControls.*` key in `pt-BR` (currently only implicitly covered by the `satisfies` constraint — make them explicit per the research Wave 0 Gaps).
  </action>
  <verify>
    <automated>npx vitest run src/content/learnContent.test.ts src/content/strings.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/content/learnContent.test.ts src/content/strings.test.ts` exits 0, all tests pass.
    - `grep -c "naviKriyaVideosHeading" src/content/strings.ts` returns 3 (interface decl + EN value + pt-BR value).
    - `grep -c "TODO: native-speaker review" src/content/strings.ts` returns 3 (one per pt-BR new learn key).
    - `learnContent.test.ts` contains an assertion referencing `practices.naviKriya.description.section1.body` and `section2.body` for clinical-verbs.
    - `learnContent.test.ts` no longer references `explainer.hrv` or `explainer.timing` or `links.heroVideo` or `links.keyVideos` — `grep -c "explainer.hrv\|explainer.timing\|links.heroVideo\|links.keyVideos" src/content/learnContent.test.ts` returns 0.
    - `strings.test.ts` references `nkReadout` and `nkControls` in explicit non-empty assertions.
  </acceptance_criteria>
  <done>strings.ts has the 3 new learn keys in both locales, both content test files updated and passing against the new shape.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Operator review and lock the EN Navi Kriya description copy</name>
  <what-built>
    Claude drafted the English Navi Kriya Learn description copy in `learnContent.ts`:
    - Section 1 heading "What is Navi Kriya" + body (what the practice is — claim-safe, calm, non-medical)
    - Section 2 heading "How this app paces it" + body (app-paced OM counting, fixed 4:1 front:back ratio, auto-advance across rounds)
    D-05: the EN copy must be operator-reviewed and locked before the component refactor (Plan 02) renders it — same flow as the original HRV/timing copy.
  </what-built>
  <how-to-verify>
    1. Open `src/content/learnContent.ts` and read the EN `practices.naviKriya.description.section1` and `section2` title and body strings.
    2. Confirm the tone matches the existing resonant explainer voice — calm, non-medical, "independent web app" framing, no clinical claims.
    3. Confirm section2 accurately describes the app behavior: it counts each OM, auto-advances front to back at a fixed 4:1 ratio, repeats for the configured rounds.
    4. If any wording should change, tell Claude the exact replacement text. Claude applies it, re-runs `npx vitest run src/content/learnContent.test.ts`, and re-presents.
  </how-to-verify>
  <resume-signal>Type "approved" to lock the EN Navi Kriya copy, or provide the exact wording changes.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| App → YouTube (external links) | The two Navi Kriya video URLs point to youtube.com; the user's click crosses to a third-party origin |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-32-01 | Tampering | `learnContent.ts` external video URLs | mitigate | URLs are static authored constants — no user input, no interpolation. Both URLs verified by the operator (D-06). The rendering of these links (target/rel attributes) is handled in Plan 02. |
| T-32-02 | Information Disclosure | `<a>` link rendering (deferred to Plan 02) | accept | This plan adds no rendering code — only static data. No PII, no secrets in `learnContent.ts`. Link-attribute hardening lives in Plan 02's threat model. |

No high-severity threats. This plan is pure static-content data restructuring with no network calls, no user input, no auth, no persistence.
</threat_model>

<verification>
- `npx tsc --noEmit` exits 0 (full project typechecks against the new `LearnContent` shape).
- `npx vitest run src/content/learnContent.test.ts src/content/strings.test.ts` passes.
- `npx vitest run` — full suite (78 files) green; restructuring `learnContent.ts` must not break any consumer's tests except the two updated files.
- `grep -c "TODO: native-speaker review" src/content/learnContent.ts` returns 4; in `strings.ts` returns 3 (markers intentionally remain until Plan 03).
- Operator has approved the EN Navi Kriya copy (Task 3 checkpoint).
</verification>

<success_criteria>
- `learnContent.ts` exposes a `practices` map with `resonant` and `naviKriya` partitions over the shared `explainer.forrest` + `links.*` base.
- Existing resonant Learn data is preserved byte-identically inside `practices.resonant`.
- `practices.naviKriya` carries operator-locked EN description copy and exactly two video links in D-06 order.
- `strings.ts` has 3 new `learn.*` heading keys in both locales.
- `learnContent.test.ts` and `strings.test.ts` pass against the new shape; full suite green.
- pt-BR NK content carries `// TODO: native-speaker review` markers (4 in `learnContent.ts`, 3 in `strings.ts`) — finalized in Plan 03.
</success_criteria>

<output>
After completion, create `.planning/phases/32-learn-localization/32-01-SUMMARY.md`
</output>
</output>
</content>
</invoke>
