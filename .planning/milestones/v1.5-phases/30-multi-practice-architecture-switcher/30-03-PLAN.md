---
phase: 30-multi-practice-architecture-switcher
plan: 03
type: execute
wave: 2
depends_on: [30-01]
files_modified:
  - src/storage/storage.ts
  - src/storage/storage.test.ts
  - src/storage/practices.ts
  - src/storage/practices.test.ts
  - src/storage/index.ts
autonomous: true
requirements: [PRACTICE-02, PRACTICE-04]
must_haves:
  truths:
    - "A returning user's flat v1 settings + stats migrate losslessly into practices.resonant"
    - "Each practice's settings and stats persist and reload across page reloads"
    - "The migration is idempotent on already-migrated v2 data"
    - "D-02: the NaviKriyaSettings non-throwing coercer is built here and wired into the per-practice persistence map, completing PRACTICE-02 for both practices"
  artifacts:
    - path: "src/storage/storage.ts"
      provides: "STATE_VERSION=2, Envelope.practices/activePractice fields, migrateEnvelope v1→v2 ladder"
      contains: "fromVersion < 2"
    - path: "src/storage/practices.ts"
      provides: "PracticeId, PracticeMap, coercers, load/save/record/reset per-practice API"
      contains: "export function coercePractices"
    - path: "src/storage/practices.test.ts"
      provides: "Unit coverage for PRACTICE-02"
      contains: "describe('coercePractices"
  key_links:
    - from: "src/storage/storage.ts migrateEnvelope"
      to: "Envelope.practices.resonant"
      via: "v1→v2 ladder coerces flat settings/stats into practices.resonant"
      pattern: "practices.*resonant"
    - from: "src/storage/practices.ts"
      to: "src/domain/naviKriyaSettings.ts"
      via: "imports validators to build coerceNaviKriyaSettings"
      pattern: "from '../domain/naviKriyaSettings'"
---

<objective>
Build the per-practice persistence layer: bump `STATE_VERSION` to 2, extend the `Envelope`
type, implement the `migrateEnvelope` v1→v2 ladder that coerces a returning user's flat
`settings`/`stats` into `practices.resonant` (PRACTICE-04), and create the new
`src/storage/practices.ts` module with coercers and load/save/record/reset functions
(PRACTICE-02).

Purpose: This is the architectural core of the phase. PRACTICE-04 (returning user loses
nothing) hinges entirely on the migration ladder being lossless and idempotent.
PRACTICE-02 (per-practice settings + stats persist) is satisfied by the `practices.ts`
load/save API for BOTH practices once this plan completes — including the Navi Kriya
settings, whose data model was defined in plan 30-01 (D-02).

Output: modifications to `src/storage/storage.ts` + its test, the new
`src/storage/practices.ts` + test, and an `src/storage/index.ts` re-export.
</objective>

<execution_context>
@$HOME/.claude-personal-personal/get-shit-done/workflows/execute-plan.md
@$HOME/.claude-personal-personal/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-multi-practice-architecture-switcher/30-RESEARCH.md
@.planning/phases/30-multi-practice-architecture-switcher/30-PATTERNS.md
@.planning/phases/30-multi-practice-architecture-switcher/30-01-SUMMARY.md

<interfaces>
<!-- Built on plan 30-01's domain module: -->
From src/domain/naviKriyaSettings.ts (plan 30-01):
- interface NaviKriyaSettings { frontCount; omLength; rounds; perOmCue }
- const DEFAULT_NK_SETTINGS: NaviKriyaSettings
- isValidFrontCount, isValidOmLength, isValidRounds predicates

<!-- Existing storage primitives (from src/storage/storage.ts) — DO NOT change STATE_KEY: -->
- export const STATE_KEY = 'hrv:state:v1'   — MUST stay 'hrv:state:v1' (the :v1 suffix is
  NOT a version number; index.html FOUC script hardcodes it; changing it orphans all data)
- export const STATE_VERSION = 1 as const   — bump to `2 as const`
- export interface Envelope { version: number; settings?; mute?; stats?; prefs? }
- export function migrateEnvelope(env, fromVersion)  — currently a `void fromVersion` no-op
- readEnvelope(deps) / writeEnvelope(env, deps) / type StorageDeps

<!-- Existing coercers to reuse (from src/storage/settings.ts and stats.ts): -->
- coerceSettings(raw: unknown): SessionSettings        — from './settings'
- coerceStats(raw: unknown): PersistedStats            — from './stats'
- ZERO_STATS: PersistedStats                            — from './stats'
- recordSession / resetStats — the analogs for recordResonantSession / resetPracticeStats

<!-- The full target practices.ts module body is given in RESEARCH.md "practices.ts -->
<!-- storage module (new file)" Code Example — that example is authoritative. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Bump STATE_VERSION, extend Envelope, implement the v1→v2 migration ladder</name>
  <files>src/storage/storage.ts, src/storage/storage.test.ts</files>
  <read_first>
    - src/storage/storage.ts (the full file — Envelope interface ~lines 45-61, STATE_VERSION line 38, STATE_KEY line 37 with the dual-versioning comment lines 13-32, the migrateEnvelope no-op ~lines 80-85, readEnvelope's forward-compat `...p` spread)
    - src/storage/storage.test.ts (the existing test structure — vitest header, beforeEach/afterEach localStorage.clear, readEnvelope/writeEnvelope round-trip tests)
    - .planning/phases/30-multi-practice-architecture-switcher/30-RESEARCH.md (Pattern 1 + Pitfalls 1-2-7 + the "migrateEnvelope v1→v2 ladder" and "Envelope type extension" Code Examples)
    - index.html (line ~18 — the FOUC script that hardcodes the `'hrv:state:v1'` key; confirms STATE_KEY must not change)
  </read_first>
  <behavior>
    - migrateEnvelope({version:1, settings:{...}, stats:{...}}, 1) returns an envelope with `practices.resonant.settings` === the original flat settings and `practices.resonant.stats` === the original flat stats
    - The returned envelope has `activePractice` === 'resonant'
    - migrateEnvelope is idempotent: calling it with fromVersion 2 returns the envelope with its `practices`/`activePractice` unchanged (the `fromVersion < 2` guard is false)
    - The original `settings`/`stats` top-level fields are NOT deleted by the migration (forward-compat spread preserves them as orphaned fields)
    - After seeding localStorage with a v1-shaped payload and calling readEnvelope(), the result has `practices.resonant` populated with the migrated data
    - STATE_KEY remains exactly 'hrv:state:v1' after a writeEnvelope call
  </behavior>
  <action>
    In src/storage/storage.ts: change `STATE_VERSION` from `1 as const` to `2 as const`.
    Leave `STATE_KEY = 'hrv:state:v1'` EXACTLY as is — the `:v1` suffix is the key name,
    not the version field (Pitfall 1); changing it breaks the index.html FOUC theme script
    and orphans all existing user data. Extend the `Envelope` interface with two new
    optional fields `practices?: unknown` and `activePractice?: unknown`, following the
    same `unknown` pattern as the existing `settings?`/`stats?`/`prefs?` fields (Pitfall 7
    — coercers narrow at the boundary). Replace the `migrateEnvelope` `void fromVersion`
    no-op with the v1→v2 ladder: when `fromVersion < 2`, return a spread of the envelope
    with `practices: { resonant: { settings: out.settings, stats: out.stats } }` (naviKriya
    deliberately absent so the coercer in Task 2 supplies defaults) and `activePractice:
    'resonant'`. Do NOT delete `out.settings` / `out.stats` — leave them as orphaned
    top-level fields (the forward-compat `...p` spread in readEnvelope preserves unknown
    fields; orphans are harmless). Keep the `let out = { ...env }` / `return out` shape so
    future migration steps can chain. Do NOT touch writeEnvelope — it already stamps
    `STATE_VERSION` on every write (Pitfall 2), so the bumped constant is correct by
    construction.
    In src/storage/storage.test.ts: add a `describe('migrateEnvelope v1→v2 (PRACTICE-04)')`
    block. Seed `STATE_KEY` with a v1-shaped envelope (flat `settings`/`stats`,
    `version: 1`), call `readEnvelope()`, and assert `practices.resonant.settings` /
    `practices.resonant.stats` carry the migrated data and `activePractice === 'resonant'`.
    Add an idempotency case: `migrateEnvelope(v2Envelope, 2)` returns `practices`/
    `activePractice` unchanged. Add a guard case asserting `STATE_KEY === 'hrv:state:v1'`
    after a write (catches accidental key bumps).
  </action>
  <verify>
    <automated>npm run test:run -- src/storage/storage.test.ts && npx tsc --noEmit && grep -v '^//' src/storage/storage.ts | grep -c "'hrv:state:v1'"</automated>
  </verify>
  <acceptance_criteria>
    - `STATE_VERSION` is `2 as const` in src/storage/storage.ts
    - `STATE_KEY` is still exactly `'hrv:state:v1'` (the grep in verify finds it; no `:v2` key)
    - `Envelope` interface has `practices?: unknown` and `activePractice?: unknown`
    - `migrateEnvelope` body contains the `fromVersion < 2` guard and assigns `practices.resonant` + `activePractice`
    - `npm run test:run -- src/storage/storage.test.ts` passes including the new migration block
    - Test output shows a passing assertion that a seeded v1 envelope reloads with `practices.resonant` populated
    - Test output shows a passing idempotency assertion for `fromVersion === 2`
  </acceptance_criteria>
  <done>STATE_VERSION is 2, Envelope carries practices/activePractice, the v1→v2 ladder migrates flat data losslessly and is idempotent, and STATE_KEY is untouched; the storage suite passes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create the practices.ts per-practice storage module</name>
  <files>src/storage/practices.ts, src/storage/practices.test.ts, src/storage/index.ts</files>
  <read_first>
    - src/storage/prefs.ts (the exact role-match analog — the prototype-pollution-safe coercer guard lines 63-76, the load/save pair pattern lines 78-85)
    - src/storage/settings.ts (coerceSettings — reused as the resonant settings coercer)
    - src/storage/stats.ts (coerceStats, ZERO_STATS, recordSession lines 80-117, resetStats lines 119-123 — the analogs for recordResonantSession and resetPracticeStats)
    - src/domain/naviKriyaSettings.ts (plan 30-01 — DEFAULT_NK_SETTINGS, isValid* predicates needed for coerceNaviKriyaSettings)
    - src/storage/prefs.test.ts (the test-file analog — beforeEach/afterEach localStorage.clear, per-field-fallback test cases)
    - src/storage/index.ts (the barrel file — append the practices re-export)
    - .planning/phases/30-multi-practice-architecture-switcher/30-RESEARCH.md (the authoritative "practices.ts storage module" Code Example + Pitfalls 3-4-5-6)
  </read_first>
  <behavior>
    - coercePractices(null | undefined | non-object | array) returns { resonant: {settings: coerced-defaults, stats: ZERO_STATS}, naviKriya: {settings: DEFAULT_NK_SETTINGS, stats: ZERO_STATS} }
    - coercePractices preserves valid resonant + naviKriya slices and falls back per-field for drifted ones
    - coerceActivePractice('resonant') === 'resonant'; coerceActivePractice('naviKriya') === 'naviKriya'; coerceActivePractice('garbage' | null | 5) === 'resonant'
    - coerceNaviKriyaSettings({frontCount: 102, ...}) returns frontCount 100 (rounds 102 DOWN to nearest lower multiple of 4 — NOT a fallback to the 100 default by coincidence; verify with an input like 88→88 and 90→88)
    - coerceNaviKriyaSettings(unknown) returns DEFAULT_NK_SETTINGS field-by-field
    - saveActivePractice(id) then loadActivePractice() round-trips the id
    - saveNaviKriyaSettings(s) then loadPractices().naviKriya.settings round-trips s
    - recordResonantSession(elapsedMs, isComplete) increments only practices.resonant.stats and leaves practices.naviKriya.stats untouched
    - resetPracticeStats('resonant') writes ZERO_STATS to practices.resonant.stats and leaves practices.naviKriya.stats untouched (and vice versa)
  </behavior>
  <action>
    Create src/storage/practices.ts following the RESEARCH.md "practices.ts storage module"
    Code Example, which is authoritative. Export: `type PracticeId = 'resonant' |
    'naviKriya'`; `interface PracticeSlice<S> { settings: S; stats: PersistedStats }`;
    `interface PracticeMap { resonant: PracticeSlice<SessionSettings>; naviKriya:
    PracticeSlice<NaviKriyaSettings> }`. Implement `coerceActivePractice` (returns the raw
    only if it equals 'resonant' or 'naviKriya', else 'resonant'). Implement
    `coerceNaviKriyaSettings(raw)` using the prefs.ts prototype-pollution-safe guard
    (`raw !== null && typeof raw === 'object' && !Array.isArray(raw)` → `r as
    Record<string, unknown>`, else `{}`); for `frontCount`, when the stored value is a
    finite positive number, round DOWN to the nearest multiple of 4 (`Math.floor(v/4)*4`)
    and keep it if > 0, else fall back to `DEFAULT_NK_SETTINGS.frontCount` (Pitfall 5);
    `omLength`/`rounds` use the `isValid*` predicates from plan 30-01 with per-field
    fallback to `DEFAULT_NK_SETTINGS`; `perOmCue` falls back when not a boolean. This
    non-throwing `coerceNaviKriyaSettings` coercer wired into `coercePractices` is what
    completes D-02 (the NK settings data model from plan 30-01 is now persisted in the
    per-practice map). Implement `coercePractices` using the same guard, delegating each
    slice to a `coercePracticeSlice` helper that runs `coerceSettings`/`coerceNaviKriyaSettings`
    for settings and `coerceStats` for stats. Implement the load/save API: `loadPractices`,
    `loadActivePractice`, `saveActivePractice`, `saveResonantSettings`,
    `saveNaviKriyaSettings` — each following the prefs.ts read-modify-write pattern.
    Implement `recordResonantSession(elapsedMs, isComplete, deps)` mirroring
    `recordSession` in stats.ts exactly (same COUNT_THRESHOLD_MS / Number.isFinite guards)
    but reading/writing `env.practices.resonant.stats` instead of the flat `env.stats`
    (Pitfall 3) — return the new PersistedStats. Implement
    `resetPracticeStats(practice: PracticeId, deps)` mirroring `resetStats` but writing
    `{ ...ZERO_STATS }` into `env.practices[practice].stats` only, leaving the other
    practice's slice untouched (Pitfall 4). Never spread `raw` into a prototype-accessible
    object — only read named keys (ASVS V5).
    Append `export * from './practices'` to src/storage/index.ts following the existing
    barrel pattern.
    Create src/storage/practices.test.ts using the prefs.test.ts structure
    (beforeEach/afterEach localStorage.clear). Cover every behavior listed above. Include
    one test that seeds a v1-shaped localStorage envelope (flat settings/stats) and asserts
    `loadPractices().resonant` is populated — exercising the full
    readEnvelope→migrateEnvelope→coercePractices path for PRACTICE-04. Include the explicit
    Pitfall-5 case: `coerceNaviKriyaSettings({ frontCount: 90 })` → frontCount 88.
  </action>
  <verify>
    <automated>npm run test:run -- src/storage/practices.test.ts src/storage/storage.test.ts && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `src/storage/practices.ts` exists and exports `PracticeId`, `PracticeMap`, `PracticeSlice`, `coercePractices`, `coerceActivePractice`, `coerceNaviKriyaSettings`, `loadPractices`, `loadActivePractice`, `saveActivePractice`, `saveResonantSettings`, `saveNaviKriyaSettings`, `recordResonantSession`, `resetPracticeStats`
    - `npx tsc --noEmit` passes
    - `src/storage/index.ts` contains `export * from './practices'`
    - `npm run test:run -- src/storage/practices.test.ts` passes
    - Test output shows a passing assertion that `coerceNaviKriyaSettings({ frontCount: 90 })` yields `frontCount: 88`
    - Test output shows a passing assertion that `resetPracticeStats('resonant')` leaves `practices.naviKriya.stats` unchanged
    - Test output shows a passing assertion that a seeded v1 envelope yields a populated `loadPractices().resonant`
    - `coercePractices` reads named keys from a guarded `Record<string, unknown>` and never spreads `raw`
  </acceptance_criteria>
  <done>practices.ts provides the full per-practice persistence API with prototype-safe non-throwing coercers; resetPracticeStats and recordResonantSession are practice-scoped; the barrel re-exports it; both storage suites pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| localStorage → app | The persisted envelope is attacker-controllable (any script with same-origin access, or a user editing devtools). All fields are untrusted until coerced. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-05 | Tampering | `coercePractices` / `coerceNaviKriyaSettings` / `coerceActivePractice` | mitigate | Prototype-pollution-safe guard (`typeof === 'object' && !Array.isArray`, never spread `raw`; only read named keys), reusing the verified prefs.ts pattern (T-14-01/T-25-01). Non-throwing per-field fallback so one drifted field never discards the envelope. ASVS V5. Unit-tested with null/array/non-object input. |
| T-30-06 | Tampering | `coerceNaviKriyaSettings` frontCount | mitigate | A tampered non-multiple-of-4 `frontCount` is rounded down to a safe multiple of 4, never producing a fractional `backCount` in Phase 31 arithmetic (Pitfall 5). Unit-tested (90→88). |
| T-30-07 | Tampering / data integrity | `migrateEnvelope` v1→v2 | mitigate | Migration is lossless (orphaned `settings`/`stats` preserved) and idempotent (`fromVersion < 2` guard), so a returning user's data survives the upgrade exactly (PRACTICE-04). Unit-tested. |
| T-30-08 | Repudiation | `recordResonantSession` writing to wrong subtree | mitigate | recordResonantSession writes `practices.resonant.stats` (Pitfall 3), so completed sessions are correctly attributed and counted; verified by test that naviKriya.stats stays untouched. |
</threat_model>

<verification>
- `npm run test:run -- src/storage/practices.test.ts src/storage/storage.test.ts` passes.
- `npx tsc --noEmit` passes.
- `STATE_KEY` is unchanged (`'hrv:state:v1'`); `STATE_VERSION` is `2`.
- Migration is lossless and idempotent; coercers are prototype-pollution-safe.
</verification>

<success_criteria>
- PRACTICE-04: a v1-shaped envelope migrates losslessly into `practices.resonant`;
  re-reading is idempotent.
- PRACTICE-02: `practices.ts` persists and reloads BOTH practices' settings and stats,
  including Navi Kriya settings from the plan 30-01 data model.
- All new coercers follow the non-throwing, prototype-pollution-safe project pattern.
</success_criteria>

<output>
After completion, create `.planning/phases/30-multi-practice-architecture-switcher/30-03-SUMMARY.md`
</output>
</content>
</invoke>
