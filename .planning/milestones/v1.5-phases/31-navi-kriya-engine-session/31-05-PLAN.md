---
phase: 31-navi-kriya-engine-session
plan: 05
type: execute
wave: 2
depends_on: [04]
files_modified:
  - src/components/SettingsForm.tsx
  - src/components/SettingsForm.nk.test.tsx
autonomous: true
requirements: [NK-02, NK-03, NK-04, NK-06]
must_haves:
  truths:
    - "User can set rounds, front OM count, and OM length from the NK controls"
    - "The front-count stepper offers only multiples of 4"
    - "User can turn the per-OM tick on or off"
    - "D-14: the estimated session duration is shown next to the controls and updates live"
    - "The disabled Phase 30 stub is replaced with a live Start session button"
  artifacts:
    - path: "src/components/SettingsForm.tsx"
      provides: "Navi Kriya controls UI filling the Phase 30 stub slot"
      contains: "nkControls"
  key_links:
    - from: "src/components/SettingsForm.tsx NK branch"
      to: "onNKSettingsChange"
      via: "each stepper/toggle calls back with the next NaviKriyaSettings"
      pattern: "onNKSettingsChange"
    - from: "src/components/SettingsForm.tsx NK branch"
      to: "onNKStartClick"
      via: "the live Start session button"
      pattern: "onNKStartClick"
---

<objective>
Fill the Phase 30 stub slot in `SettingsForm`'s `naviKriya` branch with the real
Navi Kriya controls (D-14): four `SettingsStepper`-style controls — rounds,
front OM count (multiples of 4 only), OM length (fast/medium/slow), per-OM tick
toggle — plus a live estimated-duration line and a live Start session button.

Purpose: NK-02/03/04/06 are the configurable session parameters; the user needs
the editing UI for the `NaviKriyaSettings` model that already shipped in Phase
30. This plan builds that UI; App.tsx (Wave 3) wires the callbacks to real state.

Output: edit `src/components/SettingsForm.tsx` (NK branch) + new
`SettingsForm.nk.test.tsx`.
</objective>

<execution_context>
@$HOME/.claude-personal-personal/get-shit-done/workflows/execute-plan.md
@$HOME/.claude-personal-personal/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31-navi-kriya-engine-session/31-CONTEXT.md
@.planning/phases/31-navi-kriya-engine-session/31-RESEARCH.md
@.planning/phases/31-navi-kriya-engine-session/31-PATTERNS.md
@.planning/phases/31-navi-kriya-engine-session/31-UI-SPEC.md
@.planning/phases/31-navi-kriya-engine-session/31-04-SUMMARY.md

<interfaces>
<!-- Contracts the executor needs. Plan 04 added the nkControls strings. -->

From src/domain/naviKriyaSettings.ts:
```typescript
export type OmLength = 'fast' | 'medium' | 'slow'
export const OM_LENGTH_OPTIONS = ['fast', 'medium', 'slow'] as const
export interface NaviKriyaSettings {
  frontCount: number; omLength: OmLength; rounds: number; perOmCue: boolean
}
export const DEFAULT_NK_SETTINGS: NaviKriyaSettings  // frontCount 100, omLength 'medium', rounds 3, perOmCue true
// isValidFrontCount / isValidOmLength / isValidRounds guards
```

From src/content/strings.ts (added by plan 04):
```typescript
UiStrings['nkControls']  // roundsLabel, frontCountLabel, omLengthLabel,
                         // omLengthFast/Medium/Slow, perOmCueLabel/On/Off,
                         // estimatedDuration(minutes)
```

From src/hooks/useNKEngine.ts (plan 01 — for the duration estimate math):
```typescript
export const NK_OM_SECONDS: Record<OmLength, number>  // fast 1.75, medium 2.16, slow 3.0
export const NK_SETTLE_MS: number  // settle overhead to add to the estimate
```

SettingsForm NK-branch props to add (NEW — PATTERNS "src/components/SettingsForm.tsx"):
```typescript
nkSettings: NaviKriyaSettings
onNKSettingsChange(this: void, settings: NaviKriyaSettings): void
onNKStartClick(this: void): void
isNKSessionRunning: boolean
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Replace the Phase 30 NK stub with the real controls and Start button</name>
  <files>src/components/SettingsForm.tsx, src/components/SettingsForm.nk.test.tsx</files>
  <behavior>
    - Test: rendering the `naviKriya` branch shows four controls — rounds, front OMs, OM pace, OM tick — with labels from `strings.nkControls`.
    - Test: changing the rounds stepper calls `onNKSettingsChange` with the updated `rounds`.
    - Test: the front-count stepper only offers multiples of 4 (assert no offered option fails `% 4 === 0`).
    - Test: changing the front-count stepper calls `onNKSettingsChange` with a multiple-of-4 `frontCount`.
    - Test: the OM-length control offers fast/medium/slow and calls back with the chosen `omLength`.
    - Test: toggling the per-OM cue calls `onNKSettingsChange` with the flipped `perOmCue`.
    - D-14: the estimated-duration line is present, has `aria-live="polite"`, and changing rounds updates the displayed minutes live.
    - Test: with `isNKSessionRunning: true`, the four steppers are disabled EXCEPT the per-OM tick toggle, which stays enabled.
    - Test: the Start session button is rendered enabled (not the disabled stub) and clicking it calls `onNKStartClick`.
  </behavior>
  <read_first>
    - src/components/SettingsForm.tsx (the FULL file — the `activePractice === 'resonant' ? (...) : (...)` dispatch at line ~96, the Phase 30 NK stub at lines ~201-221 being replaced, the `SettingsStepper` usage pattern in the resonant branch at lines ~110-198, the existing `SettingsFormProps` interface, the `disabled={isRunning}` convention)
    - src/components/SettingsStepper.tsx (the FULL file — props: label, value, options, formatValue, onChange, disabled, strings; the card markup, the fieldset/legend pattern, `size-12` buttons)
    - src/components/ModeToggle.tsx (the segmented two-option toggle pattern — recommended for the per-OM cue toggle per UI-SPEC)
    - src/components/SettingsForm.stretch.test.tsx (the existing SettingsForm test structure to mirror)
    - src/domain/naviKriyaSettings.ts (NaviKriyaSettings, OmLength, OM_LENGTH_OPTIONS, DEFAULT_NK_SETTINGS, the validators)
    - src/content/strings.ts (the `nkControls` sub-object added by plan 04)
    - src/hooks/useNKEngine.ts (NK_OM_SECONDS and NK_SETTLE_MS for the duration estimate)
    - .planning/phases/31-navi-kriya-engine-session/31-UI-SPEC.md ("NK Controls in SettingsForm" — the controls layout top-to-bottom, the front-count option list guidance, the duration-estimate position/style/aria, the Start button class byte-for-byte, the per-OM-toggle-stays-enabled-while-running rule)
    - .planning/phases/31-navi-kriya-engine-session/31-PATTERNS.md ("src/components/SettingsForm.tsx" section — the props additions, the SettingsStepper usage, the Start button class, the duration estimate formula, the disabled-while-running exception for perOmCue)
  </read_first>
  <action>
    Edit `src/components/SettingsForm.tsx`. Extend `SettingsFormProps` with the four NK props from the interfaces block: `nkSettings`, `onNKSettingsChange`, `onNKStartClick`, `isNKSessionRunning`.
    Replace the Phase 30 NK stub (the `<div>` placeholder and the disabled `<button>` at lines ~201-221) in the `naviKriya` branch with the real controls, top to bottom:
    1. Rounds — a `SettingsStepper` with `label={strings.nkControls.roundsLabel}`, `value={nkSettings.rounds}`, `options={[1,2,3,4,5]}`, plain-integer format, `disabled={isNKSessionRunning}`, `onChange` -> `onNKSettingsChange({ ...nkSettings, rounds })`.
    2. Front OMs — a `SettingsStepper` with `label={strings.nkControls.frontCountLabel}`, `value={nkSettings.frontCount}`, `options` = a pre-filtered array of multiples of 4 (e.g. `[4, 8, 12, 20, 40, 60, 80, 100, 120, 140, 160]` — every entry MUST satisfy `% 4 === 0`; the domain `coerceNaviKriyaSettings` enforces multiple-of-4), plain-integer format, `disabled={isNKSessionRunning}`, `onChange` -> `onNKSettingsChange({ ...nkSettings, frontCount })`.
    3. OM pace — a `SettingsStepper` with `label={strings.nkControls.omLengthLabel}`, `value={nkSettings.omLength}`, `options={OM_LENGTH_OPTIONS}` (`['fast','medium','slow']`), `formatValue` mapping each to `strings.nkControls.omLengthFast`/`Medium`/`Slow`, `disabled={isNKSessionRunning}`, `onChange` -> `onNKSettingsChange({ ...nkSettings, omLength })`.
    4. OM tick — a two-option toggle (use the `ModeToggle` segmented pattern, or a `SettingsStepper` with `[false, true]` options) with `label={strings.nkControls.perOmCueLabel}`, formatted `strings.nkControls.perOmCueOff` / `perOmCueOn`, `value={nkSettings.perOmCue}`, `onChange` -> `onNKSettingsChange({ ...nkSettings, perOmCue })`. This control is NOT disabled while a session runs (the engine reads `cueOn` from its mutable ref — stale-closure-safe). Add a brief code comment noting this deliberate exception.
    5. D-14 duration estimate line — a `<p aria-live="polite" className="text-sm text-center text-[var(--color-breathing-muted)]">` showing `strings.nkControls.estimatedDuration(estimatedMinutes)`, positioned next to the NK controls. Compute `estimatedMinutes = Math.round((nkSettings.rounds * (nkSettings.frontCount + nkSettings.frontCount / 4) * NK_OM_SECONDS[nkSettings.omLength] * 1000 + NK_SETTLE_MS) / 60000)`. It must recompute on every settings change (derive it in render — not memoized stale) so the user sees the live time impact while configuring (D-14).
    6. Start session button — replace the disabled stub `<button>` with a live one using the byte-identical class from UI-SPEC §3.6 (`mt-6 min-h-11 w-full rounded-full bg-[var(--color-breathing-accent-strong)] ...`), label `strings.controls.startSession`, `onClick={onNKStartClick}`, enabled when `!isNKSessionRunning`. Drop the `opacity-50 cursor-not-allowed` classes the stub had.
    Create `src/components/SettingsForm.nk.test.tsx` with the cases in the `<behavior>` block.
  </action>
  <verify>
    <automated>npx vitest run src/components/SettingsForm.nk.test.tsx src/components/SettingsForm.stretch.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/components/SettingsForm.nk.test.tsx` passes (>= 9 cases).
    - `npx vitest run src/components/SettingsForm.stretch.test.tsx` still passes (no resonant regression).
    - `grep -n "naviKriyaControlsPlaceholder" src/components/SettingsForm.tsx` returns nothing (stub removed).
    - `grep -nE "disabled\b" src/components/SettingsForm.tsx` shows the front-count/rounds/OM-length steppers gated on `isNKSessionRunning` and the per-OM toggle NOT so gated.
    - `SettingsForm.nk.test.tsx` has a test asserting every front-count option satisfies `% 4 === 0`.
    - `grep -n "estimatedDuration" src/components/SettingsForm.tsx` matches and the line carries `aria-live="polite"` (D-14).
    - `npx tsc --noEmit` reports no new errors.
  </acceptance_criteria>
  <done>The NK controls (rounds, front OMs, OM pace, OM tick), the live duration estimate, and the live Start button replace the Phase 30 stub; tests green; resonant branch unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| stepper input -> NaviKriyaSettings | stepper options are a fixed in-code array; the user picks from a typed set, not free text |
| onNKSettingsChange -> caller | the next settings object is passed up; persistence/coercion happens in App.tsx (Wave 3) downstream |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-11 | Tampering | The front-count stepper offering a non-multiple-of-4 value (yields a fractional back count) | mitigate | The stepper `options` array is pre-filtered to multiples of 4 in code; `SettingsForm.nk.test.tsx` asserts every option satisfies `% 4 === 0`; `coerceNaviKriyaSettings` is the storage-boundary backstop. |
| T-31-12 | Tampering | A control producing a `NaviKriyaSettings` outside the valid range | accept | Stepper `options` arrays are fixed valid sets; `OM_LENGTH_OPTIONS` is the typed union; `isValidRounds`/`isValidFrontCount` at the storage boundary are the backstop. No free-text input. |
</threat_model>

<verification>
- `npx vitest run src/components/SettingsForm.nk.test.tsx src/components/SettingsForm.stretch.test.tsx` — all green.
- `npx vitest run` — full suite green (no regressions).
- `npx tsc --noEmit` — no new type errors.
</verification>

<success_criteria>
- User can set rounds (NK-02), front OM count in multiples of 4 (NK-04), and OM length (NK-03).
- User can toggle the per-OM tick (NK-06).
- D-14: the estimated duration shows next to the controls and updates live.
- The disabled Phase 30 stub is replaced with a live Start session button.
</success_criteria>

<output>
After completion, create `.planning/phases/31-navi-kriya-engine-session/31-05-SUMMARY.md`
</output>
</output>
