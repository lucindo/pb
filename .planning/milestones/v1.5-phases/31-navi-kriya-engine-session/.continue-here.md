---
context: phase
phase: 31-navi-kriya-engine-session
task: 6
total_tasks: 6
status: in_progress
last_updated: 2026-05-17T19:52:51.056Z
---

# Continue Here — Phase 31 Navi Kriya, UAT refinement

<current_state>
Phase 31 (Navi Kriya engine + session) was implemented, verified, and code-reviewed
in earlier sessions. This session was a **UAT-driven consistency pass** making the
Navi practice behave like the HRV (resonant) practice. All 5 changes are committed
to `main`; working tree is clean; **1124 tests pass**; `tsc` + `eslint` clean (one
pre-existing `react-refresh` warning in App.tsx).

**Next up:** rework the **Navi RUNNING session** — the window between the 3-2-1
countdown ending and end-session. The operator will give specific feedback
(bug fixes + behavior changes) conversationally. Scope is strictly that window.
</current_state>

<completed_work>

This session — 5 commits on `main` (c19c0e1 → d2bdcaf):

- `c19c0e1` — Align Navi flow with HRV: switcher labels shortened (HRV / Navi),
  3-2-1 countdown with a Cancel button, pause button removed, completion popup
  replaced by an inline "Session complete" headline, mute toggle visible on the
  config screen, shared countdown beep + shared end chord, orphan strings removed.
- `9a26bf0` — Temporary 1-minute HRV duration option (testing aid, marked TEMPORARY).
- `b09dec2` — Clear the resonant completion headline on practice switch.
- `e22d40f` — "Status" legend on the session status panel (new `StatusPanel`).
- `d2bdcaf` — Show the stretch readout during the HRV stretch lead-in.
</completed_work>

<remaining_work>

- Task 6: Rework the Navi running session (countdown-end → end-session). Bug fixes
  and behavior changes. **Operator provides the specifics after /clear.** Do not
  guess the scope — wait for the feedback.
</remaining_work>

<decisions_made>

- HRV (resonant) is the source of truth; Navi mirrors it.
- Countdown beep (`scheduleCountdownTick`) and Navi per-OM tick (`scheduleNKTick`)
  are SEPARATE functions/constants — change one without affecting the other.
- End chord (`scheduleEndChord`) is a SINGLE shared function — HRV + Navi completion
  both use it; a change applies to both.
- Countdown duration stays 3s — operator said "do not change".
- Navi natural completion: inline headline, no popup; cleared on practice switch.
</decisions_made>

<blockers>
- None. Waiting on the operator's Navi-session feedback.
</blockers>

## Required Reading (in order)

1. `.planning/HANDOFF.json` — structured state mirror of this file.
2. `src/hooks/useNKEngine.ts` — the Navi OM-counting engine: `start` / `end` /
   `pause` / `resume` / `toggleCue`, the `stepOm` self-rescheduling loop, the
   `front` / `back` / `done` phases. **This is the running-session core — the
   likely focus of the next work.**
3. `src/app/App.tsx` — the Navi handlers and render. Key symbols:
   `onNKStartClick` (builds the AudioContext + cue callbacks, runs the 3-2-1
   countdown, then `nkStart`), `onNKCancelClick`, `onNKEndClick` / `confirmNKEnd`,
   `onNKComplete` (natural completion → inline headline, deferred AC close).
   Render: countdown shows `BreathingShape` with `leadInDigit`; running shows
   `NKShape` + `NKSessionReadout`; the NK CTA row (Start / Cancel / End + mute).
4. `src/components/NKShape.tsx` — the OM-counting shape (per-OM pulse, live count).
5. `src/components/NKSessionReadout.tsx` — Phase / Round / Count strip.
6. `src/audio/nkCueSynth.ts` — Navi cues: `scheduleNKFrontMarker`,
   `scheduleNKBackMarker`, `scheduleNKTick` (per-OM tick), `scheduleEndChord`
   (shared), `scheduleCountdownTick` (shared).
7. The `spike-findings-hrv` skill auto-loads — implementation patterns/gotchas
   for the Navi Kriya practice.

## Infrastructure State

- Temporary 1-minute HRV duration option is live (testing aid — commit `9a26bf0`,
  marked TEMPORARY in `src/domain/settings.ts`). Remove before release.
- `31-HUMAN-UAT.md` is `status: partial` — 4 human-judgment tests still pending
  (audio cue quality, per-OM shape pulse, completion UX, reduced-motion). Deferred
  while the Navi session is reworked.

## Verification (run after every change)

- `npx tsc -b --force` — must be clean.
- `npx eslint <changed files>` — clean (one pre-existing `react-refresh`
  warning in App.tsx is expected).
- `npx vitest run` — must stay at **1124 passing**.
- Commits go directly to `main`, conventional-commit style, scope `(31)`.

<context>
The Navi running session is engine-driven: after the countdown, `nkStart` boots
`useNKEngine`, which fires the front marker and self-reschedules OM steps
(front phase → back phase, per round) until the final back phase, where it sets
phase `done`, fires the end chord, and calls `onNKComplete`. The operator wants
to change how this running window behaves — treat `useNKEngine.ts` as the prime
suspect, and the running-state render in App.tsx (NKShape / NKSessionReadout /
CTA row) as the secondary surface.
</context>

<next_action>
Start with: after /clear, run `/gsd-resume-work` to reload this context, then ask
the operator for their Navi running-session feedback before changing anything.
</next_action>
