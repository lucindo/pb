---
phase: 34-stretch-as-a-distinct-practice
plan: 06
type: execute
wave: 1
depends_on: []
gap_closure: true
files_modified:
  - src/domain/stretchRamp.ts
  - src/domain/stretchRamp.test.ts
  - src/components/LearnDialog.tsx
  - src/components/LearnDialog.test.tsx
  - src/domain/sessionController.ts
  - src/domain/sessionController.test.ts
  - src/hooks/useSessionEngine.ts
  - src/hooks/useSessionEngine.test.tsx
autonomous: true
requirements: [STRETCH-03, STRETCH-04]
must_haves:
  truths:
    - "computeStretchTotalMs returns the snapped segment table's final endMs — the same source of truth that governs getStretchFrame's isComplete check (CR-01)"
    - "The displayed stretch Duration agrees with the real session completion time for non-cycle-aligned BPM ramps"
    - "LearnDialog's videos heading tracks the resolved practiceContentKey, not the raw activePractice — Stretch shows resonant content under the resonant heading (WR-01)"
    - "A resonant selectedSettings value survives a full stretch session start→end round-trip — the engine's idle selectedSettings is not clobbered by stretch-derived synthetic settings (WR-03)"
  artifacts:
    - path: "src/domain/stretchRamp.ts"
      provides: "computeStretchTotalMs derived from buildStretchSegments' snapped segment table"
      contains: "buildStretchSegments(settings)"
    - path: "src/components/LearnDialog.tsx"
      provides: "videosHeading resolved from practiceContentKey"
      contains: "practiceContentKey === 'resonant'"
    - path: "src/domain/sessionController.ts"
      provides: "startStretchSession that preserves the caller's resonant selectedSettings"
      contains: "startStretchSession"
  key_links:
    - from: "src/domain/stretchRamp.ts"
      to: "buildStretchSegments"
      via: "computeStretchTotalMs reads the last segment's endMs"
      pattern: "segments\\[segments.length - 1\\]"
    - from: "src/hooks/useSessionEngine.ts"
      to: "startStretchSession"
      via: "start() passes currentState.selectedSettings through to be preserved"
      pattern: "startStretchSession\\("
---

<objective>
Close the three gaps Phase 34 verification and code review found: CR-01 (the read-only
stretch Duration display disagrees with the real session end), WR-01 (LearnDialog shows
the Navi Kriya videos heading for the Stretch practice), and WR-03 (a stretch session's
synthetic lead-in settings clobber the engine's resonant `selectedSettings`).

Purpose: STRETCH-03 / STRETCH-04 — Stretch is a distinct practice with its own settings
and stats; these three defects undermine that. CR-01 is a verification BLOCKER; the user
explicitly chose to fix it together with both warnings.
Output: corrected `computeStretchTotalMs`, `LearnDialog` heading, and `startStretchSession`
contract, each with a regression test, plus a green verification run.
</objective>

<execution_context>
@$HOME/.claude-personal-personal/get-shit-done/workflows/execute-plan.md
@$HOME/.claude-personal-personal/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/34-stretch-as-a-distinct-practice/34-CONTEXT.md
@.planning/phases/34-stretch-as-a-distinct-practice/34-VERIFICATION.md
@.planning/phases/34-stretch-as-a-distinct-practice/34-REVIEW.md

<interfaces>
<!-- The three gaps touch already-shipped Phase 34 code. Exact shapes the executor changes. -->

src/domain/stretchRamp.ts (current — CR-01):
  buildStretchSegments(settings: StretchSettings): StretchSegment[]   # snaps every segment to whole cycles — correct
  computeStretchTotalMs(settings: StretchSettings): number | null    # lines 228-232 — returns raw minute sum — WRONG
  // getStretchFrame already derives sessionEndMs from `segments[segments.length - 1].endMs` (line 198) — the source of truth.

src/components/LearnDialog.tsx (current — WR-01, lines 90-93):
  const practiceContentKey = activePractice === 'stretch' ? 'resonant' : activePractice   # stretch falls back to resonant content
  const videosHeading = activePractice === 'resonant' ? strings.videosHeading : strings.naviKriyaVideosHeading   # WRONG — uses raw activePractice

src/domain/sessionController.ts (current — WR-03, lines 62-94):
  startStretchSession(stretchSettings: StretchSettings, nowMs: number): RunningSessionState
    // builds synthetic leadInSettings and assigns it to BOTH selectedSettings and lockedSettings
  endSession(state): IdleSessionState   # returns selectedSettings: cloneSettings(state.selectedSettings)
  // RunningSessionState.selectedSettings: SessionSettings — App.tsx:1182 passes state.selectedSettings to SettingsForm.

src/hooks/useSessionEngine.ts (current — WR-03, lines 212-227):
  start() — when stretchSettingsRef.current !== null calls startStretchSession(sSettings, performance.now())
            else calls startSession(currentState.selectedSettings, performance.now())
  // currentState.selectedSettings inside the setState updater holds the resonant config to preserve.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: CR-01 — derive computeStretchTotalMs from the snapped segment table</name>
  <read_first>
    - src/domain/stretchRamp.ts (computeStretchTotalMs lines 220-232 — the function being fixed; buildStretchSegments + getStretchFrame's sessionEndMs derivation at line 198 — the source of truth to match)
    - src/domain/stretchRamp.test.ts (baseSettings fixture lines 12-25, the buildStretchSegments + computeStretchTotalMs suites — where the regression test is added)
    - .planning/phases/34-stretch-as-a-distinct-practice/34-VERIFICATION.md (CR-01 gap entry + the Gaps Summary fix snippet)
    - .planning/phases/34-stretch-as-a-distinct-practice/34-REVIEW.md (CR-01 section — the exact two-line fix and why fixtures hid the drift)
  </read_first>
  <behavior>
    - Test: for the existing cycle-aligned `baseSettings` fixture, `computeStretchTotalMs(baseSettings)` still equals `buildStretchSegments(baseSettings).at(-1)!.endMs` (no regression).
    - Test: for a non-cycle-aligned config (`initialBpm: 5.5, targetBpm: 4`, integer warm/ramp/cool minutes), `computeStretchTotalMs(settings)` exactly equals `buildStretchSegments(settings).at(-1)!.endMs` — and is NOT equal to the raw `(warmUpMinutes + rampDurationMinutes + coolDownMinutes) * 60_000` sum (proving the drift is now captured).
    - Test: `computeStretchTotalMs` still returns `null` when `coolDownMinutes === 'open-ended'`.
  </behavior>
  <action>
    Replace the body of `computeStretchTotalMs` in `src/domain/stretchRamp.ts` (lines 228-232).
    Preserve the existing `coolDownMinutes === 'open-ended'` → `return null` early return verbatim.
    For the non-open-ended path, instead of returning the raw minute sum, call
    `buildStretchSegments(settings)` and return `segments[segments.length - 1]!.endMs` — the same
    snapped, cycle-aligned end the `getStretchFrame` `sessionEndMs` derivation uses (stretchRamp.ts
    line 198), so the displayed Duration agrees with the elapsed time at which the session reports
    `isComplete`. Update the function's doc comment to state the total is derived from the snapped
    segment table (not a raw minute sum). In `src/domain/stretchRamp.test.ts`, add the regression
    test described in the behavior block to the `computeStretchTotalMs` suite, using a non-cycle-aligned
    BPM fixture (e.g. `{ ...baseSettings, initialBpm: 5.5, targetBpm: 4 }`); assert equality against
    `buildStretchSegments(...).at(-1)!.endMs` and inequality against the raw minute-sum value.
  </action>
  <verify>
    <automated>npx vitest run src/domain/stretchRamp.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/domain/stretchRamp.test.ts` passes.
    - `grep -n "warmUpMinutes + rampDurationMinutes + coolDownMinutes" src/domain/stretchRamp.ts` returns no matches (the raw sum is gone).
    - `grep -c "buildStretchSegments(settings)" src/domain/stretchRamp.ts` returns at least 1 inside `computeStretchTotalMs`.
    - The new test asserts `computeStretchTotalMs(settings) === buildStretchSegments(settings).at(-1)!.endMs` for a non-cycle-aligned BPM config and `!==` the raw minute sum.
  </acceptance_criteria>
  <done>computeStretchTotalMs returns the snapped segment table's final endMs; the displayed Duration matches the real session end for drift-prone BPM ramps; the open-ended early return is preserved.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: WR-01 — LearnDialog videos heading tracks practiceContentKey</name>
  <read_first>
    - src/components/LearnDialog.tsx (lines 86-93 — practiceContentKey resolution and the videosHeading line being fixed)
    - src/components/LearnDialog.test.tsx (existing suite — where the heading-tracking test is added/extended)
    - src/content/strings.ts (videosHeading and naviKriyaVideosHeading string keys)
    - .planning/phases/34-stretch-as-a-distinct-practice/34-REVIEW.md (WR-01 section — the exact one-line fix)
  </read_first>
  <behavior>
    - Test: with `activePractice='stretch'`, LearnDialog renders the resonant `videosHeading` string (NOT `naviKriyaVideosHeading`) — the heading matches the resonant content fallback.
    - Test: with `activePractice='resonant'`, the heading is still `videosHeading` (no regression).
    - Test: with `activePractice='naviKriya'`, the heading is still `naviKriyaVideosHeading` (no regression).
  </behavior>
  <action>
    In `src/components/LearnDialog.tsx` line 93, change `videosHeading` from
    `activePractice === 'resonant' ? strings.videosHeading : strings.naviKriyaVideosHeading`
    to `practiceContentKey === 'resonant' ? strings.videosHeading : strings.naviKriyaVideosHeading`
    — using the already-resolved `practiceContentKey` (line 90) so the heading matches the content
    actually rendered. Update the `D-01 (SECOND)` comment on line 92 to note the heading follows the
    resolved content key (stretch falls back to resonant content AND the resonant heading). In
    `src/components/LearnDialog.test.tsx`, add or extend a test covering all three `activePractice`
    values per the behavior block — the stretch case is the regression that proves the fix.
  </action>
  <verify>
    <automated>npx vitest run src/components/LearnDialog.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/components/LearnDialog.test.tsx` passes.
    - `grep -n "videosHeading = " src/components/LearnDialog.tsx` shows the expression keyed on `practiceContentKey`, not `activePractice`.
    - `grep -c "activePractice === 'resonant' ? strings.videosHeading" src/components/LearnDialog.tsx` returns 0.
    - LearnDialog.test.tsx asserts the resonant `videosHeading` is shown when `activePractice='stretch'`.
  </acceptance_criteria>
  <done>LearnDialog's videos heading is derived from practiceContentKey; the Stretch practice shows resonant video links under the resonant heading, not the Navi Kriya heading.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: WR-03 — preserve resonant selectedSettings across a stretch session round-trip</name>
  <read_first>
    - src/domain/sessionController.ts (startStretchSession lines 60-87, endSession lines 89-94, RunningSessionState / IdleSessionState type declarations lines 12-37)
    - src/domain/sessionController.test.ts (startStretchSession suite lines 122-185 — signature changes ripple here)
    - src/hooks/useSessionEngine.ts (start() callback lines 212-227 — the only production caller of startStretchSession)
    - src/hooks/useSessionEngine.test.tsx (engine tests — where the round-trip regression test is added)
    - .planning/phases/34-stretch-as-a-distinct-practice/34-REVIEW.md (WR-03 section — the recommended fix: keep the engine's selectedSettings untouched, store the lead-in only in lockedSettings)
  </read_first>
  <behavior>
    - Test: `startStretchSession(stretchSettings, resonantSelectedSettings, now)` returns `status: 'running'` with `selectedSettings` deep-equal to the passed-in resonant settings (NOT the synthetic lead-in), and `lockedSettings` equal to the synthetic lead-in (`bpm: initialBpm`, `ratio`, `durationMinutes: 'open-ended'`), with a populated `stretchSegments` table.
    - Test: `endSession` on that running state returns idle `selectedSettings` deep-equal to the original resonant settings.
    - Test (engine-level): a full stretch session round-trip — `start()` then `end()` while a non-null stretch settings object is wired — leaves the engine's idle `state.selectedSettings` equal to the resonant settings the engine was initialized with (not stretch-derived synthetic values).
  </behavior>
  <action>
    Apply the review's WR-03 recommendation: keep the engine's `selectedSettings` untouched for
    stretch starts; the synthetic lead-in lives only in `lockedSettings`. Change
    `startStretchSession` in `src/domain/sessionController.ts` to accept the caller's current
    resonant `selectedSettings` as a parameter:
    `startStretchSession(stretchSettings: StretchSettings, selectedSettings: SessionSettings, nowMs: number): RunningSessionState`.
    Build the synthetic `leadInSettings` exactly as today and assign it ONLY to `lockedSettings`
    (it still drives `createBreathingPlan` for the lead-in plan). Set the returned state's
    `selectedSettings` to `cloneSettings(selectedSettings)` — the untouched resonant config — so
    `endSession`'s `cloneSettings(state.selectedSettings)` preserves it through to idle. Update the
    doc comment to record that `lockedSettings` carries the synthetic lead-in while `selectedSettings`
    is the caller's resonant config, passed through unchanged. In `src/hooks/useSessionEngine.ts`
    `start()` callback (lines 220-223), pass `currentState.selectedSettings` as the new middle
    argument: `startStretchSession(sSettings, currentState.selectedSettings, performance.now())`.
    Update `src/domain/sessionController.test.ts`'s `startStretchSession` suite to the new 3-arg
    signature and add the `selectedSettings`/`lockedSettings` assertions from the behavior block.
    Add the engine-level round-trip regression test to `src/hooks/useSessionEngine.test.tsx`.
  </action>
  <verify>
    <automated>npx vitest run src/domain/sessionController.test.ts src/hooks/useSessionEngine.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/domain/sessionController.test.ts src/hooks/useSessionEngine.test.tsx` passes.
    - `grep -n "selectedSettings: leadInSettings" src/domain/sessionController.ts` returns no matches (the synthetic value is no longer assigned to selectedSettings).
    - `startStretchSession`'s signature takes three arguments (`stretchSettings`, `selectedSettings`, `nowMs`).
    - `grep -n "startStretchSession(sSettings" src/hooks/useSessionEngine.ts` shows the call passing `currentState.selectedSettings`.
    - The engine test asserts idle `state.selectedSettings` equals the resonant init settings after a stretch session start→end cycle.
  </acceptance_criteria>
  <done>startStretchSession stores the synthetic lead-in only in lockedSettings and passes the caller's resonant selectedSettings through unchanged; a stretch session round-trip no longer clobbers the engine's resonant config.</done>
</task>

<task type="auto">
  <name>Task 4: Verification — run the affected vitest suites green</name>
  <read_first>
    - src/domain/stretchRamp.test.ts, src/components/LearnDialog.test.tsx, src/domain/sessionController.test.ts, src/hooks/useSessionEngine.test.tsx (the four suites carrying the regression tests from Tasks 1-3)
    - .planning/phases/34-stretch-as-a-distinct-practice/34-VERIFICATION.md (Behavioral Spot-Checks — the full-suite + tsc gate the phase used)
  </read_first>
  <action>
    Run the affected vitest suites and the typecheck to confirm Tasks 1-3 land cleanly with no
    regressions. Run `npx vitest run src/domain/stretchRamp.test.ts src/components/LearnDialog.test.tsx
    src/domain/sessionController.test.ts src/hooks/useSessionEngine.test.tsx` — all four must be green.
    Run `npx tsc -b` (the project typecheck gate) — it must pass with no errors, since
    `startStretchSession`'s signature changed and all call sites must agree. If any failure surfaces,
    fix the offending source/test in this task. Do NOT broaden scope beyond the three gap fixes.
  </action>
  <verify>
    <automated>npx vitest run src/domain/stretchRamp.test.ts src/components/LearnDialog.test.tsx src/domain/sessionController.test.ts src/hooks/useSessionEngine.test.tsx && npx tsc -b</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/domain/stretchRamp.test.ts src/components/LearnDialog.test.tsx src/domain/sessionController.test.ts src/hooks/useSessionEngine.test.tsx` reports all suites passing.
    - `npx tsc -b` exits 0 with no type errors.
  </acceptance_criteria>
  <done>All four affected suites are green and the project typechecks — the three gaps are closed without regression.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new in this plan) | All three fixes are corrections to already-reviewed pure-domain and presentational code. No new untrusted input crosses any boundary; the storage-layer coercers (Plan 02) remain the input gate. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34-11 | Tampering | `computeStretchTotalMs` reading a corrupt `StretchSettings` | accept | The function now calls `buildStretchSegments`, which already operates on a coerced `StretchSettings` (per-field coercion at the storage boundary, Plan 02); the segment table is always finite — no NaN/Infinity total introduced by this change. |
| T-34-12 | Information Disclosure | `startStretchSession` carrying `selectedSettings` through | accept | `selectedSettings` is the user's own resonant config already held in engine state; passing it through unchanged exposes nothing new — it strictly preserves data that was previously being silently overwritten. |
</threat_model>

<verification>
- `npx vitest run src/domain/stretchRamp.test.ts src/components/LearnDialog.test.tsx src/domain/sessionController.test.ts src/hooks/useSessionEngine.test.tsx` — all four suites green (Task 4 gate).
- `npx tsc -b` passes — `startStretchSession`'s changed signature agrees at every call site.
- Recommended before `/gsd-verify-work`: `npx vitest run` full suite green, confirming no cross-suite regression from the signature change.
</verification>

<success_criteria>
- CR-01 closed: `computeStretchTotalMs` returns the snapped segment table's final `endMs`; the displayed stretch Duration agrees with the real session completion time for non-cycle-aligned BPM ramps.
- WR-01 closed: LearnDialog's videos heading tracks `practiceContentKey`; the Stretch practice shows resonant content under the resonant heading.
- WR-03 closed: a resonant `selectedSettings` value survives a full stretch session start→end round-trip in the engine.
- Each fix has a dedicated regression test; the four affected suites and `npx tsc -b` are green.
</success_criteria>

<output>
After completion, create `.planning/phases/34-stretch-as-a-distinct-practice/34-06-SUMMARY.md`
</output>
