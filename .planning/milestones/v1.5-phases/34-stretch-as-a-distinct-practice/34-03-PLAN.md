---
phase: 34-stretch-as-a-distinct-practice
plan: 03
type: execute
wave: 3
depends_on: [01, 02]
files_modified:
  - vite.config.ts
  - .env.example
  - src/components/PracticeToggle.tsx
  - src/components/PracticeToggle.test.tsx
autonomous: true
requirements: [STRETCH-01, STRETCH-02]
must_haves:
  truths:
    - "Implements decisions: D-06, D-07, D-08, D-11"
    - "The practice switcher renders three pills in the order HRV · Stretch · Navi"
    - "When VITE_SWITCHER_TREATMENT is unset or invalid the switcher renders treatment A (text-only pills)"
    - "When VITE_SWITCHER_TREATMENT=B the switcher renders an inline glyph beside each label"
    - "VITE_SWITCHER_TREATMENT is build-time only — it is not read at runtime and not a Settings control"
  artifacts:
    - path: "src/components/PracticeToggle.tsx"
      provides: "3-pill switcher with A/B treatment branch and PracticeGlyph"
      contains: "PRACTICE_IDS"
    - path: "vite.config.ts"
      provides: "define block injecting __SWITCHER_TREATMENT__"
      contains: "__SWITCHER_TREATMENT__"
    - path: "src/components/PracticeToggle.test.tsx"
      provides: "3-pill order + treatment A default coverage"
      contains: "stretch"
  key_links:
    - from: "vite.config.ts define"
      to: "PracticeToggle.tsx"
      via: "__SWITCHER_TREATMENT__ compile-time constant"
      pattern: "__SWITCHER_TREATMENT__"
---

<objective>
Extend the practice switcher to three practices (HRV · Stretch · Navi) and ship both label
treatments behind the build-time `VITE_SWITCHER_TREATMENT` env var. Treatment A is text-only
equal pills (today's component, extended to 3); treatment B adds an inline SVG glyph per
practice. Missing/invalid env var falls back to A.

Purpose: D-06, D-07, D-08, D-11 — STRETCH-01, STRETCH-02. The 3-practice switcher validated
in spike 007; both treatments selectable by a developer-only knob, never a user setting.
Output: 3-pill `PracticeToggle` with A/B branch + glyphs, `vite.config.ts` `define` block,
`.env.example` entry.
</objective>

<execution_context>
@$HOME/.claude-personal-personal/get-shit-done/workflows/execute-plan.md
@$HOME/.claude-personal-personal/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/34-stretch-as-a-distinct-practice/34-CONTEXT.md
@.planning/phases/34-stretch-as-a-distinct-practice/34-RESEARCH.md
@.planning/phases/34-stretch-as-a-distinct-practice/34-PATTERNS.md
@.planning/spikes/007-three-practice-switcher/README.md

<interfaces>
<!-- Depends on Plan 02: PracticeId is now 'resonant' | 'stretch' | 'naviKriya'. -->

src/components/PracticeToggle.tsx (current — 2-pill):
  export type PracticeId = 'resonant' | 'naviKriya'         # REMOVE local alias — import from storage/practices
  const PRACTICE_IDS: PracticeId[] = ['resonant', 'naviKriya']   # extend to 3 in D-11 order
  PracticeToggleProps.strings.practiceNames: { resonant, naviKriya }  # Record<PracticeId, string>
  pill: <button className="flex-1 rounded-full min-h-[44px] ...">{label}</button>

src/components/CueGlyph.tsx — the inline-SVG-with-theme-tokens pattern PracticeGlyph mirrors.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the build-time VITE_SWITCHER_TREATMENT define</name>
  <read_first>
    - vite.config.ts (full file — where to add the define block, relative to plugins/test)
    - .planning/phases/34-stretch-as-a-distinct-practice/34-PATTERNS.md (the vite.config.ts section — exact define block)
    - .planning/phases/34-stretch-as-a-distinct-practice/34-RESEARCH.md (Pattern 4 — Vite define + declare const)
    - .env.example (if it exists — current env var documentation)
  </read_first>
  <action>
    Add a `define` block inside `defineConfig({})` (after `plugins`, before `test`):
    `__SWITCHER_TREATMENT__: JSON.stringify(process.env.VITE_SWITCHER_TREATMENT === 'B' ? 'B' : 'A')`.
    This is the D-06/D-07 contract — only the literal `'B'` selects treatment B; anything else
    (including unset) compiles to `'A'`. Add a documented `VITE_SWITCHER_TREATMENT` entry to
    `.env.example` (create the file if absent) noting: build-time only, values `A` (text pills,
    default) or `B` (icon + label), requires rebuild + redeploy, NOT a user setting.
  </action>
  <verify>
    <automated>node -e "process.env.VITE_SWITCHER_TREATMENT='B'; require('child_process').execSync('npx vite build --mode production', {stdio:'inherit'})" 2>&1 | tail -5 || npx tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "__SWITCHER_TREATMENT__" vite.config.ts` returns 1.
    - `grep -c "VITE_SWITCHER_TREATMENT" .env.example` returns at least 1.
    - The `define` value uses `process.env.VITE_SWITCHER_TREATMENT === 'B' ? 'B' : 'A'` — only `'B'` selects B.
  </acceptance_criteria>
  <done>The compile-time constant __SWITCHER_TREATMENT__ is injected by Vite; the env var is documented as build-time-only.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 3-pill PracticeToggle with A/B treatment branch and PracticeGlyph</name>
  <read_first>
    - src/components/PracticeToggle.tsx (full file — local PracticeId alias, PRACTICE_IDS, pill render, disabled posture)
    - src/components/PracticeToggle.test.tsx (existing suite — extend for 3 pills + treatments)
    - src/components/CueGlyph.tsx (the inline-SVG theme-token pattern PracticeGlyph mirrors)
    - src/storage/practices.ts (the canonical PracticeId to import)
    - .planning/phases/34-stretch-as-a-distinct-practice/34-PATTERNS.md (the PracticeToggle.tsx section — exact PracticeGlyph SVGs, treatment branch, import reconciliation)
    - .planning/spikes/007-three-practice-switcher/README.md (treatment A/B decision, glyph motifs, 320px fit)
  </read_first>
  <behavior>
    - Test: the switcher renders exactly three pills with accessible names in the order resonant, stretch, naviKriya (HRV · Stretch · Navi) — assert via `getAllByRole('button')` order.
    - Test: with the default build (`__SWITCHER_TREATMENT__` = 'A'), no glyph SVG is rendered inside the pills — text labels only.
    - Test (treatment B): when the component is rendered under treatment B, each pill contains an inline SVG (`aria-hidden`) alongside its label. Drive this by stubbing/overriding the `TREATMENT` derivation so the B path is exercised without a separate build — e.g. export the glyph render as a testable unit, or `vi.stubGlobal('__SWITCHER_TREATMENT__', 'B')` before import.
    - Test: every pill is `disabled` when the `disabled`/session-running prop is set; the active pill has `aria-pressed="true"`.
  </behavior>
  <action>
    Remove the local `export type PracticeId` alias and import `PracticeId` from `../storage/practices`
    (resolves the Phase 30 reconciliation note). Set `PRACTICE_IDS: PracticeId[] = ['resonant', 'stretch', 'naviKriya']`
    (D-11). The `strings.practiceNames` prop type is `Record<PracticeId, string>` — it gains the
    `stretch` key automatically. Add `declare const __SWITCHER_TREATMENT__: string` and
    `const TREATMENT: 'A' | 'B' = __SWITCHER_TREATMENT__ === 'B' ? 'B' : 'A'` at module scope (D-07
    fallback). Add a `PracticeGlyph({ id }: { id: PracticeId })` component rendering small inline SVGs
    with `aria-hidden="true"` and `stroke="currentColor"` / `fill="currentColor"` so they follow theme
    tokens (D-08): `resonant` = a stroked circle (orb), `stretch` = a descending diagonal polyline
    (BPM walk-down), `naviKriya` = three filled dots (OM counting). In the pill `.map()`, render
    `{TREATMENT === 'B' && <PracticeGlyph id={id} />}` before the label. Keep the existing container/pill
    classes (`flex rounded-full border`, `flex-1 rounded-full min-h-[44px]`) unchanged — they carry to 3
    pills. Extend `PracticeToggle.test.tsx` for the behaviors above. To exercise the treatment-B branch
    deterministically in tests, make the glyph render path unit-testable (export `PracticeGlyph`, or
    use `vi.stubGlobal('__SWITCHER_TREATMENT__', 'B')` with a dynamic re-import) — pick one and document
    it in the test file.
  </action>
  <verify>
    <automated>npx vitest run src/components/PracticeToggle.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/components/PracticeToggle.test.tsx` passes.
    - `grep -c "'resonant', 'stretch', 'naviKriya'" src/components/PracticeToggle.tsx` returns 1.
    - `grep -n "export type PracticeId" src/components/PracticeToggle.tsx` returns no matches (local alias removed).
    - `grep -c "PracticeGlyph" src/components/PracticeToggle.tsx` returns at least 2 (definition + use).
    - `grep -n "currentColor" src/components/PracticeToggle.tsx` shows the glyphs use theme-following color (no hardcoded hex).
  </acceptance_criteria>
  <done>The switcher renders 3 pills in HRV·Stretch·Navi order, branches A/B on the compile-time flag, and treatment B shows theme-token glyphs.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI/build env → bundle | `VITE_SWITCHER_TREATMENT` is read from `process.env` at build time. The build environment is trusted (operator/CI controlled); there is no runtime user-controlled path into the switcher treatment. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34-05 | Tampering | `VITE_SWITCHER_TREATMENT` build-time define | accept | Build-time only; no user-controlled input path (RESEARCH Security Domain). The `=== 'B'` strict equality means any unexpected value compiles to the safe default `'A'` — no injection surface, no runtime evaluation. Low-value target. |
| T-34-06 | Information Disclosure | switcher treatment exposed as a dev knob | accept | The treatment is a cosmetic label-rendering choice; it carries no secret and no privileged capability. D-06 keeps it out of the Settings dialog so users cannot toggle it, but its presence in `.env.example` is intentional developer documentation. |
</threat_model>

<verification>
- `npx vitest run src/components/PracticeToggle.test.tsx` is green.
- `npx tsc --noEmit` passes — `PracticeToggle` imports the canonical `PracticeId`, no duplicate type.
</verification>

<success_criteria>
- The switcher renders 3 pills HRV·Stretch·Navi; treatment defaults to A, B selectable at build time.
- Treatment B glyphs are theme-token inline SVGs; no hardcoded colors.
- `PracticeToggle.test.tsx` green.
</success_criteria>

<output>
After completion, create `.planning/phases/34-stretch-as-a-distinct-practice/34-03-SUMMARY.md`
</output>
</output>
