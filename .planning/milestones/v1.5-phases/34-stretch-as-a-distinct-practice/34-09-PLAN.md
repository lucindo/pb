---
phase: 34-stretch-as-a-distinct-practice
plan: 09
type: execute
wave: 1
depends_on: []
gap_closure: true
files_modified:
  - src/domain/stretchRamp.ts
  - src/domain/stretchRamp.test.ts
autonomous: true
requirements: [STRETCH-02]
must_haves:
  truths:
    - "When a stretch session's countdown reaches zero the breathing orb completes the in-flight in/out cycle before stopping — it never freezes mid-cycle"
    - "The stretch frame's phaseProgress keeps advancing through the entire final half-cycle until isComplete fires"
    - "An elapsed value landing exactly on the final bounded segment's endMs still produces a frame on the last real cycle — no phantom extra in-phase cycle"
  artifacts:
    - path: "src/domain/stretchRamp.ts"
      provides: "getStretchFrame DS-WR-03 clamp narrowed to guard only the exact endMs landing, not the whole final half-cycle"
      contains: "elapsedInSegment"
  key_links:
    - from: "src/domain/stretchRamp.ts getStretchFrame"
      to: "elapsedInSegment clamp"
      via: "clamp ceiling narrowed from segmentSpan - cycleMs/2 to just inside the segment span"
      pattern: "activeSeg.endMs - activeSeg.startMs"
---

<objective>
Close UAT GAP 3 (major) from Phase 34 testing: at the end of a stretch session the
breathing orb freezes on the last exhale before the countdown reaches zero — the session
stops abruptly mid-cycle instead of finishing the in-flight in/out cycle the way HRV does.

Purpose: STRETCH-02 — Stretch is a distinct practice that must behave like the resonant
practice during a session. HRV's getSessionFrame lets the rendered frame advance freely
until completion; the stretch path must match so the orb completes its last breath cycle
gracefully. This is the sole major UAT gap blocking Phase 34 sign-off.
Output: a narrowed DS-WR-03 completion clamp in getStretchFrame so the rendered frame
advances through the entire final half-cycle, a regression test proving phaseProgress is
not frozen, and a green verification run — while preserving the original DS-WR-03
protection against a phantom extra cycle at the exact endMs landing.
</objective>

<execution_context>
@$HOME/.claude-personal-personal/get-shit-done/workflows/execute-plan.md
@$HOME/.claude-personal-personal/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/34-stretch-as-a-distinct-practice/34-UAT.md
@.planning/debug/stretch-orb-stops-mid-cycle.md

<interfaces>
<!-- The GAP 3 fix touches one function in already-shipped Phase 34 code. -->

src/domain/stretchRamp.ts getStretchFrame (current — DS-WR-03 clamp, lines 180-194):
  // For a BOUNDED final segment the clamp pulls elapsedInSegment back to
  //   segmentSpan - cycleMs/2
  // which freezes phase / phaseProgress / cycleElapsedMs for the entire final
  // HALF-cycle (the last exhale) while remainingMs and isComplete keep running
  // off the UNCLAMPED safeElapsedMs (lines 208-210). That is the freeze.
  const rawElapsedInSegment = safeElapsedMs - activeSeg.startMs
  const elapsedInSegment =
    activeSeg.endMs === Infinity
      ? rawElapsedInSegment
      : Math.min(rawElapsedInSegment, activeSeg.endMs - activeSeg.startMs - activeSeg.cycleMs / 2)
  // Downstream of the clamp (UNCHANGED by this plan):
  //   cycleInSegment   = Math.floor(elapsedInSegment / activeSeg.cycleMs)
  //   cycleElapsedMs   = elapsedInSegment - cycleInSegment * activeSeg.cycleMs
  //   phaseProgress    derives from cycleElapsedMs
  // sessionEndMs / remainingMs / isComplete (lines 207-210) read the UNCLAMPED
  // safeElapsedMs — leave them exactly as-is.

  // ORIGINAL DS-WR-03 INTENT (must be preserved): when safeElapsedMs lands
  // EXACTLY on a bounded segment's endMs, no segment satisfies
  // `safeElapsedMs < seg.endMs`, activeSeg falls through to the final segment,
  // and an unclamped rawElapsedInSegment === segmentSpan makes
  // `Math.floor(segmentSpan / cycleMs)` compute exactly `cycleCount` — one past
  // the last valid cycle index — a phantom extra in-phase cycle. The clamp must
  // still pull that exact-endMs landing back onto the last real cycle.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: GAP 3 — narrow the DS-WR-03 clamp so the final cycle animates instead of freezing</name>
  <read_first>
    - src/domain/stretchRamp.ts (lines 165-228 — the whole getStretchFrame function; lines 180-194 — the DS-WR-03 clamp being narrowed; lines 196-210 — cycleInSegment / cycleElapsedMs / phaseProgress derivation and the unclamped sessionEndMs / remainingMs / isComplete)
    - src/domain/stretchRamp.test.ts (lines 12-25 — baseSettings / openEndedSettings fixtures; lines 178-260 — the getStretchFrame suite, where boundary tests live and where the regression test is added)
    - src/domain/sessionMath.ts (lines 32-47 — getSessionFrame, the HRV reference: completion uses Math.ceil(totalMs/cycleMs)*cycleMs decoupled from the countdown, and the frame is NEVER clamped — confirm the target behavior)
    - .planning/debug/stretch-orb-stops-mid-cycle.md (Evidence + Resolution — the exact -cycleMs/2 over-correction and the 5/5/5 cool-down simulation)
    - .planning/phases/34-stretch-as-a-distinct-practice/34-UAT.md (Gaps section — GAP 3 root_cause and the `missing` items)
  </read_first>
  <behavior>
    - Test (freeze fixed): for a bounded stretch session built from baseSettings, sample getStretchFrame across the FINAL cycle of the last segment — from `sessionEndMs - cycleMs` up to `sessionEndMs - 1` in small steps — and assert phaseProgress is NOT frozen: the rendered frame keeps advancing (the last exhale's phaseProgress strictly increases across the final half-cycle, and the phase transitions in→out within that final cycle) right up until isComplete fires.
    - Test (countdown still agrees): across that same final-cycle sampling, remainingMs decreases monotonically toward 0 and isComplete is false until safeElapsedMs >= sessionEndMs — the countdown and the animation now finish together.
    - Test (phantom-cycle protection preserved — the original DS-WR-03 case): getStretchFrame at exactly `sessionEndMs` produces a frame whose absolute cycleIndex equals the last real cycle index (the index at `sessionEndMs - 1`), NOT one past it — no phantom extra in-phase cycle at the exact endMs landing. isComplete is true at exactly sessionEndMs.
    - Test (open-ended unaffected): for openEndedSettings the final segment endMs is Infinity, elapsedInSegment stays unclamped (rawElapsedInSegment), and getStretchFrame at a large elapsed still advances normally — no regression.
  </behavior>
  <action>
    One change inside `getStretchFrame` in `src/domain/stretchRamp.ts` — narrow the
    `elapsedInSegment` clamp ceiling (the `Math.min(...)` branch, currently line 193).

    The current ceiling `activeSeg.endMs - activeSeg.startMs - activeSeg.cycleMs / 2`
    pulls the rendered frame back by a full HALF cycle, so for the entire final exhale the
    frame is pinned. Replace the `- activeSeg.cycleMs / 2` term so the clamp only guards
    the exact-endMs landing rather than the whole final half-cycle. Clamp the ceiling to
    just *inside* the segment span — `activeSeg.endMs - activeSeg.startMs - EPSILON`, with
    EPSILON a tiny constant (1 ms is sufficient and matches the `segmentSpan - 1ms`
    direction the UAT `missing` item suggests). With a 1 ms pull-back, any elapsed value
    strictly below endMs flows through unclamped (the orb animates the full final cycle),
    and only an elapsed landing exactly on endMs (rawElapsedInSegment === segmentSpan) is
    nudged 1 ms below the boundary — which keeps `Math.floor(elapsedInSegment / cycleMs)`
    on the last real cycle index instead of computing one past it. That preserves the
    original DS-WR-03 phantom-cycle protection while removing the freeze.

    Define EPSILON as a small named module-level const (e.g. `CLAMP_EPSILON_MS = 1`) or an
    inline literal with an explanatory comment — the executor's choice, but it MUST be
    documented. Leave the `activeSeg.endMs === Infinity` branch (open-ended cool-down)
    exactly as-is — open-ended segments stay unclamped. Leave `cycleInSegment`,
    `cycleElapsedMs`, `phaseProgress`, `sessionEndMs`, `remainingMs`, and `isComplete`
    untouched — the only change is the clamp ceiling.

    Rewrite the DS-WR-03 comment block (lines 180-188) to describe the narrowed behavior:
    the clamp now guards ONLY the exact endMs landing (the phantom-extra-cycle case), the
    frame advances freely through the entire final cycle so the orb completes its last
    in/out cycle like HRV, and the countdown (remainingMs, computed from the unclamped
    safeElapsedMs) and the animation now finish together.

    In `src/domain/stretchRamp.test.ts`, add the regression tests from the behavior block
    to the `getStretchFrame` suite: (a) sample the final cycle and assert phaseProgress
    advances / is not frozen; (b) assert remainingMs decreases monotonically and isComplete
    stays false until sessionEndMs; (c) assert the frame at exactly sessionEndMs carries
    the last-real-cycle index (phantom-cycle protection preserved) and isComplete is true;
    (d) assert the open-ended path still advances at large elapsed.
  </action>
  <verify>
    <automated>npx vitest run src/domain/stretchRamp.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/domain/stretchRamp.test.ts` passes.
    - `grep -vn '^\s*//' src/domain/stretchRamp.ts | grep -c "activeSeg.cycleMs / 2"` returns 0 (the half-cycle pull-back is gone from executable code).
    - A test asserts the stretch frame's phaseProgress keeps advancing through the final half-cycle (it is NOT frozen) until isComplete fires.
    - A test asserts getStretchFrame at exactly sessionEndMs carries the last real cycle index — no phantom extra cycle (the original DS-WR-03 protection is preserved).
    - A test asserts the open-ended (endMs === Infinity) path is unaffected.
  </acceptance_criteria>
  <done>The DS-WR-03 clamp guards only the exact endMs landing, not the whole final half-cycle; the stretch orb completes its last in/out cycle like HRV instead of freezing, while the phantom-extra-cycle protection at the exact endMs boundary is preserved.</done>
</task>

<task type="auto">
  <name>Task 2: Verification — run the affected suite, the full suite, and typecheck green</name>
  <read_first>
    - src/domain/stretchRamp.test.ts (the suite carrying the Task 1 regression tests)
    - .planning/phases/34-stretch-as-a-distinct-practice/34-VERIFICATION.md (Behavioral Spot-Checks — the full-suite + tsc gate the phase uses)
  </read_first>
  <action>
    Confirm Task 1 lands cleanly with no regressions. Run
    `npx vitest run src/domain/stretchRamp.test.ts` — the suite must be green. Run the full
    `npx vitest run` once to confirm no cross-suite regression — `getStretchFrame` is
    consumed by the useSessionEngine, sessionController, and App session suites, which
    assert stretch frame and completion behavior. Run `npx tsc -b` — it must exit 0 with no
    errors. If any failure surfaces, fix the offending source or test within the scope of
    GAP 3 only. Do NOT broaden scope beyond this gap (the session-overrun gap is plan
    34-10, the glyph gap is plan 34-11 — leave both untouched).
  </action>
  <verify>
    <automated>npx vitest run src/domain/stretchRamp.test.ts && npx vitest run && npx tsc -b</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/domain/stretchRamp.test.ts` reports the suite passing.
    - `npx vitest run` (full suite) reports all tests passing.
    - `npx tsc -b` exits 0 with no type errors.
  </acceptance_criteria>
  <done>The stretchRamp suite and the full test suite are green and the project typechecks — UAT GAP 3 is closed without regression.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new in this plan) | The fix narrows an internal clamp in a pure-domain function. No new untrusted input crosses any boundary; `getStretchFrame` consumes an engine-built segment table and an elapsed-ms number, both engine-owned. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34-17 | Tampering | `getStretchFrame` elapsedInSegment clamp | accept | The clamp operates on `safeElapsedMs` (already floored at 0) and an engine-built segment table. Narrowing the ceiling changes only the rendered frame for the final cycle; it adds no input path and the phantom-extra-cycle protection at the exact endMs boundary is explicitly preserved. |
</threat_model>

<verification>
- `npx vitest run src/domain/stretchRamp.test.ts` — suite green (Task 2 gate).
- `npx vitest run` full suite green — confirms the clamp change causes no cross-suite regression in the engine / controller / App session suites.
- `npx tsc -b` passes — no type drift from the single edit.
</verification>

<success_criteria>
- UAT GAP 3 closed: when a stretch session's countdown reaches zero the breathing orb completes the in-flight in/out cycle before stopping — it no longer freezes on the last exhale.
- The DS-WR-03 clamp is narrowed to guard only the exact endMs landing; the original phantom-extra-cycle protection at that boundary is preserved.
- A dedicated regression test asserts phaseProgress keeps advancing through the final half-cycle; the affected suite, the full suite, and `npx tsc -b` are green.
</success_criteria>

<output>
After completion, create `.planning/phases/34-stretch-as-a-distinct-practice/34-09-SUMMARY.md`
</output>
