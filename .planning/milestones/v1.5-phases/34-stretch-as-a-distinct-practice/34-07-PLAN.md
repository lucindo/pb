---
phase: 34-stretch-as-a-distinct-practice
plan: 07
type: execute
wave: 1
depends_on: []
gap_closure: true
files_modified:
  - src/app/App.tsx
  - src/app/App.session.test.tsx
  - src/components/SettingsForm.tsx
  - src/components/SettingsForm.stretch.test.tsx
autonomous: true
requirements: [STRETCH-01, STRETCH-02, STRETCH-04]
must_haves:
  truths:
    - "The stretch Duration readout shows a rounded whole-minute value, never an unrounded float"
    - "During an active stretch session the stretch settings steppers are hidden, not rendered as editable controls"
    - "Ending a running stretch session opens the end-confirmation dialog before the session ends"
    - "The app layout is top-anchored — content does not float mid-viewport or shift vertically when switching practices"
  artifacts:
    - path: "src/components/SettingsForm.tsx"
      provides: "Rounded stretch Duration text; stretch steppers gated on !isRunning"
      contains: "Math.round(stretchTotalMs"
    - path: "src/app/App.tsx"
      provides: "requestEnd routes a running stretch session through the end dialog; root section top-anchored"
      contains: "state.stretchSegments !== null"
  key_links:
    - from: "src/app/App.tsx requestEnd"
      to: "setEndDialogOpen(true)"
      via: "condition extended with state.stretchSegments !== null"
      pattern: "stretchSegments !== null"
    - from: "src/components/SettingsForm.tsx stretch branch"
      to: "stretch steppers"
      via: "isRunning gate mirroring the resonant branch"
      pattern: "!isRunning"
---

<objective>
Close the four UAT gaps that paused Phase 34 testing (UAT tests 1, 2, 4): the stretch
Duration shows an unrounded float, the stretch settings steppers stay visible during a
running stretch session, ending a stretch session skips the confirmation dialog, and the
app layout is vertically centered instead of top-anchored.

Purpose: STRETCH-01/02/04 — the stretch practice must behave like resonant and Navi Kriya
during a session, present a clean computed Duration, and the three-practice switcher must
not jump content vertically when the user switches practices. The user explicitly cannot
continue UAT until these four are fixed.
Output: rounded stretch Duration text, stretch steppers hidden during a session, the
end-confirmation dialog reached for stretch sessions, a top-anchored layout — each with a
regression test, plus a green verification run.
</objective>

<execution_context>
@$HOME/.claude-personal-personal/get-shit-done/workflows/execute-plan.md
@$HOME/.claude-personal-personal/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/34-stretch-as-a-distinct-practice/34-UAT.md

<interfaces>
<!-- Exact shapes the executor changes. All four fixes touch already-shipped Phase 34 code. -->

src/components/SettingsForm.tsx (current):
  // line 84-87 — UAT GAP 1: stretchDurationText uses an unrounded ms→minutes quotient
  const stretchTotalMs = computeStretchTotalMs(stretchSettings)         // null when open-ended
  const stretchDurationText = stretchTotalMs === null
    ? strings.openEndedLabel
    : `${String(stretchTotalMs / 60_000)} ${strings.minutesUnit}`       // WRONG — no rounding
  // The stretch branch is `activePractice === 'stretch' ? (...) : ...` (line 186).
  // `isRunning` prop (line 38) is already passed `inSessionView` by App.tsx (line 1183).
  // The resonant branch hides bpm/ratio steppers via `{!isRunning && (...)}` (line 156);
  // the stretch branch (lines 191-248) has NO `isRunning` gate — UAT GAP 2.

src/app/App.tsx (current):
  // line 297 — inSessionView already flips true for a stretch session (appPhase !== 'idle')
  const inSessionView = appPhase !== 'idle'
  // line 643-650 — UAT GAP 3: requestEnd skips the dialog for open-ended sessions, and a
  // stretch session's lockedSettings is the synthetic lead-in with durationMinutes 'open-ended'.
  const requestEnd = useCallback(() => {
    if (state.status === 'running' && state.lockedSettings.durationMinutes !== 'open-ended') {
      setEndDialogOpen(true)
      return
    }
    session.end()
    void audioStop()
  }, [state, session, audioStop])
  // `state` is the SessionState union; `state.stretchSegments` is `StretchSegment[] | null`
  // on RunningSessionState (sessionController.ts:22) — non-null exactly for a stretch session.
  // line 1059 — UAT GAP 4: the root <section> is vertically centered
  <section className="mx-auto flex min-h-[calc(100vh-3rem)] max-w-3xl flex-col items-center justify-center text-center sm:min-h-[calc(100vh-4rem)]">
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: UAT GAP 1 + GAP 2 — round the stretch Duration and hide stretch steppers during a session</name>
  <read_first>
    - src/components/SettingsForm.tsx (lines 81-118 — stretchDurationText derivation and the updateStretchSettings/updateInitialBpm helpers; lines 151-185 — the resonant branch, the `{!isRunning && (...)}` pattern to mirror; lines 186-248 — the stretch branch being gated)
    - src/components/SettingsForm.stretch.test.tsx (the existing stretch suite — fixture setup, how steppers are queried via role="group", where the regression tests are added)
    - .planning/phases/34-stretch-as-a-distinct-practice/34-UAT.md (Gaps section — GAP 1 and GAP 2 root causes and `missing` items)
  </read_first>
  <behavior>
    - Test (GAP 1): for a `stretchSettings` config whose `computeStretchTotalMs` returns a non-cycle-aligned float (e.g. `{ ...DEFAULT_STRETCH_SETTINGS }` with the default 5/5/5 minutes — the verification report shows this produces ~15.05 min), the rendered Duration readout shows a rounded whole-minute string (`Math.round(totalMs / 60_000)` followed by the minutes unit) — it does NOT contain a fractional digit.
    - Test (GAP 1): when `coolDownMinutes === 'open-ended'`, the Duration readout still shows `strings.openEndedLabel` (the open-ended branch is untouched).
    - Test (GAP 2): with `activePractice='stretch'` and `isRunning={true}`, none of the stretch steppers (initial BPM, target BPM, ratio, warm-up, ramp duration, cool-down, Duration) render — the stretch branch produces no editable controls.
    - Test (GAP 2): with `activePractice='stretch'` and `isRunning={false}`, all stretch steppers render as before (no regression to the pre-session view).
  </behavior>
  <action>
    Two changes inside `src/components/SettingsForm.tsx`, both confined to the stretch branch.

    GAP 1 — round the Duration text. In the `stretchDurationText` derivation (currently line
    85-87), change the non-open-ended branch from `${String(stretchTotalMs / 60_000)} ...`
    to round the minute value first: compute `Math.round(stretchTotalMs / 60_000)` and render
    `${String(...)} ${strings.minutesUnit}`. Leave the `stretchTotalMs === null` branch
    (`strings.openEndedLabel`) exactly as-is — open-ended sessions never carry a numeric total.
    Add a brief comment noting that `computeStretchTotalMs` returns the snapped segment table's
    final `endMs` (since plan 34-06), which is not whole-minute-aligned, so the display rounds.

    GAP 2 — hide the stretch steppers during a session. The resonant branch already hides its
    bpm/ratio steppers behind `{!isRunning && (...)}` (line 156). Mirror that pattern for the
    stretch branch: wrap the entire stretch steppers fragment (the 6 `SettingsStepper`s plus
    the read-only Duration `SettingsStepper`, lines ~192-247) so it only renders when
    `!isRunning`. The stretch practice has no in-session "extend duration" affordance (unlike
    resonant's Duration stepper, which stays visible to allow extending a timed session), so
    ALL stretch controls are hidden during a running stretch session — the cleanest match to
    how Navi Kriya behaves (the whole NK form is unmounted while a session runs). `isRunning`
    is already passed `inSessionView` by App.tsx, which flips true for a stretch session — no
    new App.tsx flag is needed. Add a comment explaining the in-session gate.

    In `src/components/SettingsForm.stretch.test.tsx`, add the regression tests from the
    behavior block: a rounded-Duration assertion (GAP 1, render with `isRunning={false}` and
    assert the Duration readout text), an open-ended-Duration assertion (GAP 1), an
    in-session hidden-steppers assertion (GAP 2, render with `isRunning={true}` and assert the
    stretch steppers are absent — query by the same `role="group"` mechanism the existing
    suite uses), and a not-running steppers-present assertion (GAP 2, no regression).
  </action>
  <verify>
    <automated>npx vitest run src/components/SettingsForm.stretch.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/components/SettingsForm.stretch.test.tsx` passes.
    - `grep -c "Math.round(stretchTotalMs" src/components/SettingsForm.tsx` returns at least 1.
    - `grep -vn '^\s*//' src/components/SettingsForm.tsx | grep -c "String(stretchTotalMs / 60_000)"` returns 0 (the unrounded quotient is gone from executable code).
    - The stretch branch's steppers fragment is wrapped in a `!isRunning` gate (the stretch steppers do not render when `isRunning` is true).
    - The new tests assert: a rounded whole-minute Duration string; the open-ended label is preserved; the stretch steppers are absent when `isRunning={true}` and present when `isRunning={false}`.
  </acceptance_criteria>
  <done>The stretch Duration readout shows a rounded whole-minute value; the stretch settings steppers are hidden during a running stretch session, mirroring the resonant and Navi Kriya in-session behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: UAT GAP 3 + GAP 4 — route stretch session-end through the confirmation dialog and top-anchor the layout</name>
  <read_first>
    - src/app/App.tsx (lines 637-667 — requestEnd / confirmEnd / cancelEnd; line 1057-1075 — the root <main>/<section> layout; line 147-148 — activeStretchSettings + the useSessionEngine wiring; line 297 — inSessionView)
    - src/app/App.session.test.tsx (lines 270-340 — existing resonant end-dialog tests showing how the dialog is asserted; lines 739-810 — the Phase 34 stretch-stats test that currently ends a stretch session via the 'End session' button and expects an immediate end — this test MUST be updated to the new dialog-confirm flow)
    - src/domain/sessionController.ts (lines 17-25, 67-93 — RunningSessionState.stretchSegments and startStretchSession, confirming stretchSegments is non-null exactly for a stretch session)
    - .planning/phases/34-stretch-as-a-distinct-practice/34-UAT.md (Gaps section — GAP 3 and GAP 4 root causes and `missing` items)
  </read_first>
  <behavior>
    - Test (GAP 3): starting a stretch session (`activePractice='stretch'`) and clicking 'End session' opens the end-confirmation dialog — the session does NOT end immediately; confirming in the dialog then ends the session and records stretch stats.
    - Test (GAP 3): the resonant end-dialog behavior is unchanged — a running timed resonant session still raises the dialog; an open-ended resonant session (durationMinutes 'open-ended', stretchSegments null) still ends directly with no dialog.
    - Test (GAP 4): the root layout `<section>` is top-anchored (`justify-start`, not `justify-center`).
  </behavior>
  <action>
    Two changes in `src/app/App.tsx`.

    GAP 3 — extend `requestEnd` (lines 643-650). The current guard raises the dialog only for
    `state.status === 'running' && state.lockedSettings.durationMinutes !== 'open-ended'`. A
    stretch session's `lockedSettings` is the synthetic lead-in with `durationMinutes:
    'open-ended'`, so stretch sessions bypass the dialog. Extend the condition so a running
    stretch session also raises the dialog: change the guard to raise the dialog when
    `state.status === 'running'` AND (`state.lockedSettings.durationMinutes !== 'open-ended'`
    OR `state.stretchSegments !== null`). `state.stretchSegments` is non-null exactly for a
    stretch session (sessionController.ts:81-89). Keep `confirmEnd` / `cancelEnd` untouched —
    they already drive the dialog. Update the `requestEnd` doc comment (lines 637-642) to note
    that running stretch sessions also raise the modal even though their lockedSettings is
    open-ended.

    GAP 4 — top-anchor the layout. In the root `<section>` className (line 1059), replace
    `justify-center` with `justify-start` so content is anchored to the top of the viewport
    across all three practices. Leave every other class (`flex`, `min-h-[...]`,
    `items-center`, `text-center`, the `sm:` min-height) unchanged — only the vertical-axis
    justification changes. This stops a shorter practice (HRV, fewer elements) from floating
    mid-viewport and stops content jumping when switching practices.

    Update the tests. In `src/app/App.session.test.tsx`: (a) update the existing Phase 34
    stretch-stats test (~lines 782-810) — clicking 'End session' for a stretch session now
    opens the dialog, so the test must click the dialog's confirm/'End session' control to
    actually end the session before asserting stretch stats; adjust the comment that says the
    stretch session ends directly. (b) Add a regression test asserting that clicking
    'End session' on a running stretch session opens the end-confirmation dialog (the dialog
    is visible, the session is still running) and that confirming ends it. (c) Add a layout
    assertion that the root section carries `justify-start` (query the section element and
    assert its className, or assert `justify-center` is absent). If no resonant open-ended
    end-dialog-skip test already exists, add one to lock that the GAP 3 change does not make
    open-ended resonant sessions raise the dialog.
  </action>
  <verify>
    <automated>npx vitest run src/app/App.session.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/app/App.session.test.tsx` passes.
    - `grep -c "state.stretchSegments !== null" src/app/App.tsx` returns at least 1 inside `requestEnd`.
    - `grep -c "justify-start" src/app/App.tsx` returns at least 1; `grep -c "justify-center" src/app/App.tsx` returns 0.
    - A test asserts that ending a running stretch session opens the end-confirmation dialog (session not ended until confirm) and that confirming records stretch stats.
    - A test asserts the resonant open-ended end path still ends directly with no dialog (GAP 3 did not over-trigger).
  </acceptance_criteria>
  <done>Ending a running stretch session opens the end-confirmation dialog before the session ends; the resonant end behavior is unchanged; the app layout is top-anchored so content does not float or jump vertically between practices.</done>
</task>

<task type="auto">
  <name>Task 3: Verification — run the affected vitest suites and typecheck green</name>
  <read_first>
    - src/components/SettingsForm.stretch.test.tsx, src/app/App.session.test.tsx (the two suites carrying the regression tests from Tasks 1-2)
    - .planning/phases/34-stretch-as-a-distinct-practice/34-VERIFICATION.md (Behavioral Spot-Checks — the full-suite + tsc gate the phase uses)
  </read_first>
  <action>
    Confirm Tasks 1-2 land cleanly with no regressions. Run
    `npx vitest run src/components/SettingsForm.stretch.test.tsx src/app/App.session.test.tsx`
    — both suites must be green. Run `npx tsc -b` — it must exit 0 with no errors. Because the
    `requestEnd` change and the layout className change are localized, also run the full
    `npx vitest run` once to confirm no cross-suite regression (the existing App settings /
    persistence suites render SettingsForm and the layout). If any failure surfaces, fix the
    offending source or test within the scope of these four UAT gaps. Do NOT broaden scope
    beyond GAP 1-4.
  </action>
  <verify>
    <automated>npx vitest run src/components/SettingsForm.stretch.test.tsx src/app/App.session.test.tsx && npx tsc -b</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/components/SettingsForm.stretch.test.tsx src/app/App.session.test.tsx` reports all suites passing.
    - `npx vitest run` (full suite) reports all tests passing.
    - `npx tsc -b` exits 0 with no type errors.
  </acceptance_criteria>
  <done>The affected suites and the full test suite are green and the project typechecks — the four UAT gaps are closed without regression.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new in this plan) | All four fixes are corrections to already-reviewed presentational and control-flow code. No new untrusted input crosses any boundary; the storage-layer coercers remain the input gate (hardened by plan 34-08). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34-13 | Tampering | `requestEnd` reading `state.stretchSegments` | accept | `stretchSegments` is engine-owned RunningSessionState produced by `startStretchSession`; it is never user-supplied. Reading it to route the end dialog introduces no new input path. |
| T-34-14 | Denial of Service | stretch steppers hidden during a session | accept | Hiding controls is purely a render gate on `isRunning` (already-trusted `inSessionView`); it removes editable surface during a session rather than adding any. |
</threat_model>

<verification>
- `npx vitest run src/components/SettingsForm.stretch.test.tsx src/app/App.session.test.tsx` — both suites green (Task 3 gate).
- `npx vitest run` full suite green — confirms the `requestEnd` and layout changes cause no cross-suite regression.
- `npx tsc -b` passes — no type drift from the four edits.
</verification>

<success_criteria>
- UAT GAP 1 closed: the stretch Duration readout shows a rounded whole-minute value, never an unrounded float; the open-ended branch is preserved.
- UAT GAP 2 closed: the stretch settings steppers are hidden during a running stretch session, matching how resonant and Navi Kriya behave.
- UAT GAP 3 closed: ending a running stretch session opens the end-confirmation dialog before the session ends; resonant end behavior is unchanged.
- UAT GAP 4 closed: the app layout is top-anchored — content does not float mid-viewport or shift vertically when switching practices.
- Each fix has a dedicated regression test; the two affected suites, the full suite, and `npx tsc -b` are green.
</success_criteria>

<output>
After completion, create `.planning/phases/34-stretch-as-a-distinct-practice/34-07-SUMMARY.md`
</output>
